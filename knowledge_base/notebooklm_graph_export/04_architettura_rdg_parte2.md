# Architettura e codice del framework RDG — parte 2

## Metadati

- Dominio: Architettura e codice del framework RDG.
- Data di generazione: 2026-07-31.
- Versione dei grafi sorgente: KB documentale `5f9434fc0f01de5d74dd3c6d72fffc217b7f8629`; KB codice RDG `06d5dd91cef2e12574b63a36d73b049a3588cdf8`; KB unificata `null`.
- Nodi coperti: 60 nodi canonici e 120 occorrenze fisiche; tipi canonici: code: 44, rationale: 16.
- Archi coperti: 420 occorrenze fisiche incidenti ai nodi del documento; tipi: calls: 196, contains: 64, imports: 48, imports_from: 32, indirect_call: 16, method: 12, rationale_for: 32, references: 16, uses: 4.
- Iperarchi coperti: 0 occorrenze fisiche, replicate anche negli altri file coinvolti quando attraversano la segmentazione editoriale.
- Nota sulla direzione: tutti e tre i grafi dichiarano `directed=false`; la prosa conserva comunque l'orientamento dei campi JSON `source` e `target` senza trasformarlo in causalità.

## Glossario

- EXTRACTED — Valore della proprietà `confidence` che il grafo assegna a una relazione estratta.
- INFERRED — Valore della proprietà `confidence` che il grafo assegna a una relazione inferita; la prosa non promuove una relazione INFERRED a fatto estratto.
- RDG — Nome editoriale canonico del repository Request-Dataset-Generator.
- AST — Valore di provenienza `_origin=ast` registrato dal grafo.
- FHIR — Nome canonico dell'acronimo usato negli identificatori RDG.
- RVE — Nome canonico del prefisso usato per i flussi RDG.
- UI — Nome canonico dell'acronimo usato nei moduli di interfaccia.

### Mappa dei nomi canonici e delle varianti

La mappa seguente non fonde nodi solo perché semanticamente simili. Le occorrenze del grafo sorgente e del grafo unificato sono ricondotte allo stesso nome soltanto tramite `repo` e `local_id`; le etichette duplicate di nodi distinti sono disambiguate con l'identificatore locale.

- Il nome canonico `.test_dataset_to_engine_mock_immediate()` identifica il nodo `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate`; le etichette originali conservate sono `.test_dataset_to_engine_mock_immediate()`.
- Il nome canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` identifica il nodo `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps`; le etichette originali conservate sono `.test_error_injection_produces_measurable_failures_and_skipped_steps()`.
- Il nome canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` identifica il nodo `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset`; le etichette originali conservate sono `.test_framework_log_is_sufficient_for_normalized_dataset()`.
- Il nome canonico `.test_metric_engine_writes_csv_tables()` identifica il nodo `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables`; le etichette originali conservate sono `.test_metric_engine_writes_csv_tables()`.
- Il nome canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` identifica il nodo `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids`; le etichette originali conservate sono `.test_timeline_assigns_unique_flow_occurrence_ids()`.
- Il nome canonico `.test_timeline_tags_burst_events()` identifica il nodo `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_tags_burst_events`; le etichette originali conservate sono `.test_timeline_tags_burst_events()`.
- Il nome canonico `_annotate_dataset_flows()` identifica il nodo `Request-Dataset-Generator::main_annotate_dataset_flows`; le etichette originali conservate sono `_annotate_dataset_flows()`.
- Il nome canonico `_append_json()` identifica il nodo `Request-Dataset-Generator::main_append_json`; le etichette originali conservate sono `_append_json()`.
- Il nome canonico `_build_namespace()` identifica il nodo `Request-Dataset-Generator::main_build_namespace`; le etichette originali conservate sono `_build_namespace()`.
- Il nome canonico `_cfg_section()` identifica il nodo `Request-Dataset-Generator::main_cfg_section`; le etichette originali conservate sono `_cfg_section()`.
- Il nome canonico `_configure_run_outputs()` identifica il nodo `Request-Dataset-Generator::main_configure_run_outputs`; le etichette originali conservate sono `_configure_run_outputs()`.
- Il nome canonico `_ensure_experiment_metadata()` identifica il nodo `Request-Dataset-Generator::main_ensure_experiment_metadata`; le etichette originali conservate sono `_ensure_experiment_metadata()`.
- Il nome canonico `_frange()` identifica il nodo `Request-Dataset-Generator::timeline_generator_frange`; le etichette originali conservate sono `_frange()`.
- Il nome canonico `_load_config() [main_load_config]` identifica il nodo `Request-Dataset-Generator::main_load_config`; le etichette originali conservate sono `_load_config()`.
- Il nome canonico `_resolve_output_path() [main_resolve_output_path]` identifica il nodo `Request-Dataset-Generator::main_resolve_output_path`; le etichette originali conservate sono `_resolve_output_path()`.
- Il nome canonico `_slug()` identifica il nodo `Request-Dataset-Generator::main_slug`; le etichette originali conservate sono `_slug()`.
- Il nome canonico `_write_output()` identifica il nodo `Request-Dataset-Generator::main_write_output`; le etichette originali conservate sono `_write_output()`.
- Il nome canonico `_write_resolved_config()` identifica il nodo `Request-Dataset-Generator::main_write_resolved_config`; le etichette originali conservate sono `_write_resolved_config()`.
- Il nome canonico `build_timeline()` identifica il nodo `Request-Dataset-Generator::timeline_generator_build_timeline`; le etichette originali conservate sono `build_timeline()`.
- Il nome canonico `build_workload()` identifica il nodo `Request-Dataset-Generator::request_dataset_generator_build_workload`; le etichette originali conservate sono `build_workload()`.
- Il nome canonico `Calcola metriche e genera report/grafici da config o da main.py.` identifica il nodo `Request-Dataset-Generator::metric_engine_rationale_1432`; le etichette originali conservate sono `Calcola metriche e genera report/grafici da config o da main.py.`.
- Il nome canonico `Calcola statistiche descrittive della timeline generata.` identifica il nodo `Request-Dataset-Generator::timeline_generator_rationale_294`; le etichette originali conservate sono `Calcola statistiche descrittive della timeline generata.`.
- Il nome canonico `cmd_collect()` identifica il nodo `Request-Dataset-Generator::main_cmd_collect`; le etichette originali conservate sono `cmd_collect()`.
- Il nome canonico `cmd_execute()` identifica il nodo `Request-Dataset-Generator::main_cmd_execute`; le etichette originali conservate sono `cmd_execute()`.
- Il nome canonico `cmd_metrics()` identifica il nodo `Request-Dataset-Generator::main_cmd_metrics`; le etichette originali conservate sono `cmd_metrics()`.
- Il nome canonico `cmd_patients()` identifica il nodo `Request-Dataset-Generator::main_cmd_patients`; le etichette originali conservate sono `cmd_patients()`.
- Il nome canonico `cmd_pipeline()` identifica il nodo `Request-Dataset-Generator::main_cmd_pipeline`; le etichette originali conservate sono `cmd_pipeline()`.
- Il nome canonico `cmd_requests()` identifica il nodo `Request-Dataset-Generator::main_cmd_requests`; le etichette originali conservate sono `cmd_requests()`.
- Il nome canonico `cmd_timeline()` identifica il nodo `Request-Dataset-Generator::main_cmd_timeline`; le etichette originali conservate sono `cmd_timeline()`.
- Il nome canonico `constant_rate()` identifica il nodo `Request-Dataset-Generator::timeline_generator_constant_rate`; le etichette originali conservate sono `constant_rate()`.
- Il nome canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` identifica il nodo `Request-Dataset-Generator::main_rationale_116`; le etichette originali conservate sono `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf`.
- Il nome canonico `daily_clinical_profile()` identifica il nodo `Request-Dataset-Generator::timeline_generator_daily_clinical_profile`; le etichette originali conservate sono `daily_clinical_profile()`.
- Il nome canonico `EngineSmokeTest` identifica il nodo `Request-Dataset-Generator::tests_test_core_enginesmoketest`; le etichette originali conservate sono `EngineSmokeTest`.
- Il nome canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_rationale_976`; le etichette originali conservate sono `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re`.
- Il nome canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` identifica il nodo `Request-Dataset-Generator::timeline_generator_rationale_88`; le etichette originali conservate sono `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor`.
- Il nome canonico `Genera un dataset di flussi RVE pre-composti.` identifica il nodo `Request-Dataset-Generator::main_rationale_278`; le etichette originali conservate sono `Genera un dataset di flussi RVE pre-composti.`.
- Il nome canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` identifica il nodo `Request-Dataset-Generator::request_dataset_generator_rationale_75`; le etichette originali conservate sono `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer`.
- Il nome canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` identifica il nodo `Request-Dataset-Generator::timeline_generator_rationale_191`; le etichette originali conservate sono `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim`.
- Il nome canonico `Genera una timeline temporizzata di flussi RVE.` identifica il nodo `Request-Dataset-Generator::main_rationale_409`; le etichette originali conservate sono `Genera una timeline temporizzata di flussi RVE.`.
- Il nome canonico `generate_arrivals()` identifica il nodo `Request-Dataset-Generator::timeline_generator_generate_arrivals`; le etichette originali conservate sono `generate_arrivals()`.
- Il nome canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` identifica il nodo `Request-Dataset-Generator::timeline_generator_rationale_145`; le etichette originali conservate sono `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro`.
- Il nome canonico `inject_bursts()` identifica il nodo `Request-Dataset-Generator::timeline_generator_inject_bursts`; le etichette originali conservate sono `inject_bursts()`.
- Il nome canonico `main() [main_main]` identifica il nodo `Request-Dataset-Generator::main_main`; le etichette originali conservate sono `main()`.
- Il nome canonico `main.py` identifica il nodo `Request-Dataset-Generator::main`; le etichette originali conservate sono `main.py`.
- Il nome canonico `Namespace` identifica il nodo `Request-Dataset-Generator::namespace`; le etichette originali conservate sono `Namespace`.
- Il nome canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` identifica il nodo `Request-Dataset-Generator::main_rationale_296`; le etichette originali conservate sono `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.`.
- Il nome canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` identifica il nodo `Request-Dataset-Generator::timeline_generator_rationale_60`; le etichette originali conservate sono `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r`.
- Il nome canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` identifica il nodo `Request-Dataset-Generator::timeline_generator_rationale_34`; le etichette originali conservate sono `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas`.
- Il nome canonico `Rate costante: processo di Poisson omogeneo.` identifica il nodo `Request-Dataset-Generator::timeline_generator_rationale_23`; le etichette originali conservate sono `Rate costante: processo di Poisson omogeneo.`.
- Il nome canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` identifica il nodo `Request-Dataset-Generator::main_rationale_106`; le etichette originali conservate sono `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica`.
- Il nome canonico `run()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_run`; le etichette originali conservate sono `run()`.
- Il nome canonico `run_metric_engine()` identifica il nodo `Request-Dataset-Generator::metric_engine_run_metric_engine`; le etichette originali conservate sono `run_metric_engine()`.
- Il nome canonico `step_profile()` identifica il nodo `Request-Dataset-Generator::timeline_generator_step_profile`; le etichette originali conservate sono `step_profile()`.
- Il nome canonico `test_core.py` identifica il nodo `Request-Dataset-Generator::tests_test_core`; le etichette originali conservate sono `test_core.py`.
- Il nome canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` identifica il nodo `Request-Dataset-Generator::timeline_generator_rationale_1`; le etichette originali conservate sono `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste`.
- Il nome canonico `timeline_generator.py` identifica il nodo `Request-Dataset-Generator::timeline_generator`; le etichette originali conservate sono `timeline_generator.py`.
- Il nome canonico `timeline_stats()` identifica il nodo `Request-Dataset-Generator::timeline_generator_timeline_stats`; le etichette originali conservate sono `timeline_stats()`.
- Il nome canonico `TimelineIdentityTest` identifica il nodo `Request-Dataset-Generator::tests_test_core_timelineidentitytest`; le etichette originali conservate sono `TimelineIdentityTest`.
- Il nome canonico `to_json()` identifica il nodo `Request-Dataset-Generator::fhir_patient_generator_to_json`; le etichette originali conservate sono `to_json()`.
- Il nome canonico `to_xml()` identifica il nodo `Request-Dataset-Generator::fhir_patient_generator_to_xml`; le etichette originali conservate sono `to_xml()`.

## Cluster Graphify 1 — CLI Command Pipeline

Il cluster editoriale Graphify 1 del dominio Architettura e codice del framework RDG contiene 60 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### .test_dataset_to_engine_mock_immediate()

<!-- GRAPH_NODE graph=codice index=310 id=tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_dataset_to_engine_mock_immediate()` è `.test_dataset_to_engine_mock_immediate()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_dataset_to_engine_mock_immediate()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `L549`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_dataset_to_engine_mock_immediate()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_dataset_to_engine_mock_immediate()` possiede l'identificatore esatto `tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_dataset_to_engine_mock_immediate()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_dataset_to_engine_mock_immediate()` è `.test_dataset_to_engine_mock_immediate()`.
<!-- GRAPH_EDGE graph=codice index=325 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_dataset_to_engine_mock_immediate()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `.test_dataset_to_engine_mock_immediate()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 325 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 325 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 325 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 325 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 325 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 325 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `L555`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 325 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 325 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `ast`. Nel grafo del codice RDG, l'arco 325 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate` per la relazione che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()`. Nel grafo del codice RDG, l'arco 325 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()`.
<!-- GRAPH_EDGE graph=codice index=624 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_dataset_to_engine_mock_immediate()` è la sorgente `source` della relazione `calls` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate` all'ID `traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.test_dataset_to_engine_mock_immediate()` chiama `run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 624 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 624 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 624 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 624 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 624 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 624 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `L583`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 624 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 624 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `ast`. Nel grafo del codice RDG, l'arco 624 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate` per la relazione che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()`. Nel grafo del codice RDG, l'arco 624 registra come `target` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()`.
<!-- GRAPH_EDGE graph=codice index=620 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_dataset_to_engine_mock_immediate()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `EngineSmokeTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest` all'ID `tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate`. La relazione tecnica esplicita è la seguente: `EngineSmokeTest` registra come proprio metodo `.test_dataset_to_engine_mock_immediate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 620 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 620 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 620 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 620 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `L549`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 620 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 620 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `ast`. Nel grafo del codice RDG, l'arco 620 registra come `source` l'identificatore `tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()`. Nel grafo del codice RDG, l'arco 620 registra come `target` l'identificatore `tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate` per la relazione che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 620 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=310 id=Request-Dataset-Generator::tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_dataset_to_engine_mock_immediate()` è `.test_dataset_to_engine_mock_immediate()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_dataset_to_engine_mock_immediate()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `L549`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_dataset_to_engine_mock_immediate()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_dataset_to_engine_mock_immediate()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_dataset_to_engine_mock_immediate()` è `.test_dataset_to_engine_mock_immediate()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_dataset_to_engine_mock_immediate()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate`. Nel grafo unificato, il nodo canonico `.test_dataset_to_engine_mock_immediate()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate`.
<!-- GRAPH_EDGE graph=unificata index=624 role=outgoing -->
Nel grafo unificato, il nodo canonico `.test_dataset_to_engine_mock_immediate()` è la sorgente `source` della relazione `calls` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.test_dataset_to_engine_mock_immediate()` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 624 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 624 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 624 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 624 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 624 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 624 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `L583`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 624 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 624 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `ast`. Nel grafo unificato, l'arco 624 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate` per la relazione che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()`. Nel grafo unificato, l'arco 624 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()`.
<!-- GRAPH_EDGE graph=unificata index=325 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_dataset_to_engine_mock_immediate()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `.test_dataset_to_engine_mock_immediate()`. Nel grafo unificato, il tipo dichiarato dell'arco 325 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 325 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 325 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 325 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 325 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 325 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `L555`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 325 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 325 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `ast`. Nel grafo unificato, l'arco 325 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()`. Nel grafo unificato, l'arco 325 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate` per la relazione che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()`.
<!-- GRAPH_EDGE graph=unificata index=620 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_dataset_to_engine_mock_immediate()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `EngineSmokeTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate`. La relazione tecnica esplicita è la seguente: `EngineSmokeTest` registra come proprio metodo `.test_dataset_to_engine_mock_immediate()`. Nel grafo unificato, il tipo dichiarato dell'arco 620 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 620 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 620 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 620 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `L549`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 620 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 620 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 620 che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()` è `1.0`. Nel grafo unificato, l'arco 620 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()`. Nel grafo unificato, l'arco 620 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate` per la relazione che coinvolge il nodo canonico `.test_dataset_to_engine_mock_immediate()`.

### .test_error_injection_produces_measurable_failures_and_skipped_steps()

<!-- GRAPH_NODE graph=codice index=312 id=tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `L650`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` possiede l'identificatore esatto `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `.test_error_injection_produces_measurable_failures_and_skipped_steps()`.
<!-- GRAPH_EDGE graph=codice index=113 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è la sorgente `source` della relazione `calls` verso il nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` all'ID `log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `.test_error_injection_produces_measurable_failures_and_skipped_steps()` chiama `collect_logs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 113 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 113 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 113 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 113 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 113 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 113 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `L695`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 113 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 113 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `ast`. Nel grafo del codice RDG, l'arco 113 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo del codice RDG, l'arco 113 registra come `target` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`.
<!-- GRAPH_EDGE graph=codice index=326 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `.test_error_injection_produces_measurable_failures_and_skipped_steps()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 326 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 326 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 326 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 326 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 326 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 326 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `L663`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 326 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 326 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `ast`. Nel grafo del codice RDG, l'arco 326 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo del codice RDG, l'arco 326 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`.
<!-- GRAPH_EDGE graph=codice index=628 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `.test_error_injection_produces_measurable_failures_and_skipped_steps()` chiama `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 628 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 628 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 628 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 628 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 628 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 628 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `L670`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 628 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 628 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `ast`. Nel grafo del codice RDG, l'arco 628 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo del codice RDG, l'arco 628 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`.
<!-- GRAPH_EDGE graph=codice index=629 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è la sorgente `source` della relazione `calls` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `.test_error_injection_produces_measurable_failures_and_skipped_steps()` chiama `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 629 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 629 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 629 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 629 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 629 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 629 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `L673`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 629 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 629 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `ast`. Nel grafo del codice RDG, l'arco 629 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo del codice RDG, l'arco 629 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`.
<!-- GRAPH_EDGE graph=codice index=630 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è la sorgente `source` della relazione `calls` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` all'ID `traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.test_error_injection_produces_measurable_failures_and_skipped_steps()` chiama `run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 630 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 630 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 630 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 630 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 630 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 630 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `L685`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 630 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 630 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `ast`. Nel grafo del codice RDG, l'arco 630 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo del codice RDG, l'arco 630 registra come `target` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`.
<!-- GRAPH_EDGE graph=codice index=621 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `EngineSmokeTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest` all'ID `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps`. La relazione tecnica esplicita è la seguente: `EngineSmokeTest` registra come proprio metodo `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 621 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 621 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 621 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 621 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `L650`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 621 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 621 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `ast`. Nel grafo del codice RDG, l'arco 621 registra come `source` l'identificatore `tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo del codice RDG, l'arco 621 registra come `target` l'identificatore `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 621 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=312 id=Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `L650`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps`. Nel grafo unificato, il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps`.
<!-- GRAPH_EDGE graph=unificata index=628 role=outgoing -->
Nel grafo unificato, il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `.test_error_injection_produces_measurable_failures_and_skipped_steps()` chiama `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 628 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 628 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 628 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 628 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 628 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 628 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `L670`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 628 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 628 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `ast`. Nel grafo unificato, l'arco 628 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo unificato, l'arco 628 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`.
<!-- GRAPH_EDGE graph=unificata index=629 role=outgoing -->
Nel grafo unificato, il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è la sorgente `source` della relazione `calls` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` all'ID `Request-Dataset-Generator::timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `.test_error_injection_produces_measurable_failures_and_skipped_steps()` chiama `constant_rate()`. Nel grafo unificato, il tipo dichiarato dell'arco 629 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 629 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 629 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 629 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 629 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 629 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `L673`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 629 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 629 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `ast`. Nel grafo unificato, l'arco 629 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo unificato, l'arco 629 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`.
<!-- GRAPH_EDGE graph=unificata index=630 role=outgoing -->
Nel grafo unificato, il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è la sorgente `source` della relazione `calls` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.test_error_injection_produces_measurable_failures_and_skipped_steps()` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 630 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 630 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 630 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 630 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 630 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 630 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `L685`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 630 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 630 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `ast`. Nel grafo unificato, l'arco 630 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo unificato, l'arco 630 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`.
<!-- GRAPH_EDGE graph=unificata index=113 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_collect_logs` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo unificato, il tipo dichiarato dell'arco 113 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 113 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 113 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 113 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 113 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 113 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `L695`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 113 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 113 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `ast`. Nel grafo unificato, l'arco 113 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo unificato, l'arco 113 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`.
<!-- GRAPH_EDGE graph=unificata index=326 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo unificato, il tipo dichiarato dell'arco 326 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 326 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 326 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 326 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 326 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 326 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `L663`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 326 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 326 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `ast`. Nel grafo unificato, l'arco 326 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo unificato, l'arco 326 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`.
<!-- GRAPH_EDGE graph=unificata index=621 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `EngineSmokeTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps`. La relazione tecnica esplicita è la seguente: `EngineSmokeTest` registra come proprio metodo `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo unificato, il tipo dichiarato dell'arco 621 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 621 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 621 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 621 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `L650`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 621 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 621 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 621 che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()` è `1.0`. Nel grafo unificato, l'arco 621 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo unificato, l'arco 621 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`.

### .test_framework_log_is_sufficient_for_normalized_dataset()

<!-- GRAPH_NODE graph=codice index=311 id=tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `L599`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` possiede l'identificatore esatto `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `.test_framework_log_is_sufficient_for_normalized_dataset()`.
<!-- GRAPH_EDGE graph=codice index=114 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è la sorgente `source` della relazione `calls` verso il nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` all'ID `log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `.test_framework_log_is_sufficient_for_normalized_dataset()` chiama `collect_logs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 114 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 114 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 114 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 114 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 114 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 114 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `L637`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 114 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 114 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `ast`. Nel grafo del codice RDG, l'arco 114 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo del codice RDG, l'arco 114 registra come `target` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`.
<!-- GRAPH_EDGE graph=codice index=327 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `.test_framework_log_is_sufficient_for_normalized_dataset()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 327 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 327 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 327 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 327 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 327 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 327 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `L605`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 327 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 327 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `ast`. Nel grafo del codice RDG, l'arco 327 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo del codice RDG, l'arco 327 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`.
<!-- GRAPH_EDGE graph=codice index=625 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `.test_framework_log_is_sufficient_for_normalized_dataset()` chiama `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 625 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 625 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 625 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 625 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 625 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 625 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `L612`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 625 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 625 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `ast`. Nel grafo del codice RDG, l'arco 625 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo del codice RDG, l'arco 625 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`.
<!-- GRAPH_EDGE graph=codice index=626 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è la sorgente `source` della relazione `calls` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `.test_framework_log_is_sufficient_for_normalized_dataset()` chiama `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 626 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 626 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 626 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 626 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 626 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 626 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `L615`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 626 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 626 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `ast`. Nel grafo del codice RDG, l'arco 626 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo del codice RDG, l'arco 626 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`.
<!-- GRAPH_EDGE graph=codice index=627 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è la sorgente `source` della relazione `calls` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` all'ID `traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.test_framework_log_is_sufficient_for_normalized_dataset()` chiama `run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 627 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 627 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 627 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 627 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 627 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 627 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `L627`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 627 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 627 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `ast`. Nel grafo del codice RDG, l'arco 627 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo del codice RDG, l'arco 627 registra come `target` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`.
<!-- GRAPH_EDGE graph=codice index=622 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `EngineSmokeTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest` all'ID `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset`. La relazione tecnica esplicita è la seguente: `EngineSmokeTest` registra come proprio metodo `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 622 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 622 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 622 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 622 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `L599`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 622 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 622 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `ast`. Nel grafo del codice RDG, l'arco 622 registra come `source` l'identificatore `tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo del codice RDG, l'arco 622 registra come `target` l'identificatore `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 622 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=311 id=Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `L599`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset`. Nel grafo unificato, il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset`.
<!-- GRAPH_EDGE graph=unificata index=625 role=outgoing -->
Nel grafo unificato, il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `.test_framework_log_is_sufficient_for_normalized_dataset()` chiama `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 625 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 625 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 625 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 625 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 625 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 625 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `L612`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 625 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 625 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `ast`. Nel grafo unificato, l'arco 625 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo unificato, l'arco 625 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`.
<!-- GRAPH_EDGE graph=unificata index=626 role=outgoing -->
Nel grafo unificato, il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è la sorgente `source` della relazione `calls` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` all'ID `Request-Dataset-Generator::timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `.test_framework_log_is_sufficient_for_normalized_dataset()` chiama `constant_rate()`. Nel grafo unificato, il tipo dichiarato dell'arco 626 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 626 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 626 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 626 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 626 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 626 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `L615`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 626 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 626 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `ast`. Nel grafo unificato, l'arco 626 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo unificato, l'arco 626 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`.
<!-- GRAPH_EDGE graph=unificata index=627 role=outgoing -->
Nel grafo unificato, il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è la sorgente `source` della relazione `calls` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.test_framework_log_is_sufficient_for_normalized_dataset()` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 627 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 627 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 627 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 627 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 627 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 627 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `L627`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 627 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 627 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `ast`. Nel grafo unificato, l'arco 627 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo unificato, l'arco 627 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`.
<!-- GRAPH_EDGE graph=unificata index=114 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_collect_logs` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo unificato, il tipo dichiarato dell'arco 114 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 114 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 114 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 114 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 114 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 114 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `L637`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 114 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 114 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `ast`. Nel grafo unificato, l'arco 114 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo unificato, l'arco 114 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`.
<!-- GRAPH_EDGE graph=unificata index=327 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo unificato, il tipo dichiarato dell'arco 327 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 327 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 327 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 327 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 327 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 327 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `L605`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 327 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 327 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `ast`. Nel grafo unificato, l'arco 327 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo unificato, l'arco 327 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`.
<!-- GRAPH_EDGE graph=unificata index=622 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `EngineSmokeTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset`. La relazione tecnica esplicita è la seguente: `EngineSmokeTest` registra come proprio metodo `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo unificato, il tipo dichiarato dell'arco 622 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 622 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 622 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 622 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `L599`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 622 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 622 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 622 che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()` è `1.0`. Nel grafo unificato, l'arco 622 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo unificato, l'arco 622 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`.

### .test_metric_engine_writes_csv_tables()

<!-- GRAPH_NODE graph=codice index=313 id=tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_metric_engine_writes_csv_tables()` è `.test_metric_engine_writes_csv_tables()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_metric_engine_writes_csv_tables()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_metric_engine_writes_csv_tables()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_metric_engine_writes_csv_tables()` è `L711`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_metric_engine_writes_csv_tables()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_metric_engine_writes_csv_tables()` possiede l'identificatore esatto `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_metric_engine_writes_csv_tables()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_metric_engine_writes_csv_tables()` è `.test_metric_engine_writes_csv_tables()`.
<!-- GRAPH_EDGE graph=codice index=115 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_metric_engine_writes_csv_tables()` è la sorgente `source` della relazione `calls` verso il nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` all'ID `log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `.test_metric_engine_writes_csv_tables()` chiama `collect_logs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 115 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 115 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 115 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 115 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 115 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 115 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `L749`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 115 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 115 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `ast`. Nel grafo del codice RDG, l'arco 115 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`. Nel grafo del codice RDG, l'arco 115 registra come `target` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`.
<!-- GRAPH_EDGE graph=codice index=279 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_metric_engine_writes_csv_tables()` è la sorgente `source` della relazione `calls` verso il nodo canonico `run_metric_engine()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` all'ID `metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `.test_metric_engine_writes_csv_tables()` chiama `run_metric_engine()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 279 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 279 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 279 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 279 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 279 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 279 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `L756`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 279 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 279 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `ast`. Nel grafo del codice RDG, l'arco 279 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`. Nel grafo del codice RDG, l'arco 279 registra come `target` l'identificatore `metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`.
<!-- GRAPH_EDGE graph=codice index=328 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_metric_engine_writes_csv_tables()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `.test_metric_engine_writes_csv_tables()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 328 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 328 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 328 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 328 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 328 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 328 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `L717`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 328 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 328 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `ast`. Nel grafo del codice RDG, l'arco 328 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`. Nel grafo del codice RDG, l'arco 328 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`.
<!-- GRAPH_EDGE graph=codice index=631 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_metric_engine_writes_csv_tables()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `.test_metric_engine_writes_csv_tables()` chiama `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 631 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 631 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 631 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 631 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 631 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 631 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `L724`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 631 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 631 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `ast`. Nel grafo del codice RDG, l'arco 631 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`. Nel grafo del codice RDG, l'arco 631 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`.
<!-- GRAPH_EDGE graph=codice index=632 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_metric_engine_writes_csv_tables()` è la sorgente `source` della relazione `calls` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `.test_metric_engine_writes_csv_tables()` chiama `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 632 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 632 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 632 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 632 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 632 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 632 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `L727`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 632 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 632 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `ast`. Nel grafo del codice RDG, l'arco 632 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`. Nel grafo del codice RDG, l'arco 632 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`.
<!-- GRAPH_EDGE graph=codice index=633 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_metric_engine_writes_csv_tables()` è la sorgente `source` della relazione `calls` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` all'ID `traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.test_metric_engine_writes_csv_tables()` chiama `run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 633 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 633 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 633 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 633 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 633 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 633 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `L739`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 633 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 633 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `ast`. Nel grafo del codice RDG, l'arco 633 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`. Nel grafo del codice RDG, l'arco 633 registra come `target` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`.
<!-- GRAPH_EDGE graph=codice index=623 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_metric_engine_writes_csv_tables()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `EngineSmokeTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest` all'ID `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables`. La relazione tecnica esplicita è la seguente: `EngineSmokeTest` registra come proprio metodo `.test_metric_engine_writes_csv_tables()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 623 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 623 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 623 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 623 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `L711`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 623 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 623 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `ast`. Nel grafo del codice RDG, l'arco 623 registra come `source` l'identificatore `tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`. Nel grafo del codice RDG, l'arco 623 registra come `target` l'identificatore `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 623 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=313 id=Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_metric_engine_writes_csv_tables()` è `.test_metric_engine_writes_csv_tables()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_metric_engine_writes_csv_tables()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_metric_engine_writes_csv_tables()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_metric_engine_writes_csv_tables()` è `L711`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_metric_engine_writes_csv_tables()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_metric_engine_writes_csv_tables()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_metric_engine_writes_csv_tables()` è `.test_metric_engine_writes_csv_tables()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_metric_engine_writes_csv_tables()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_metric_engine_writes_csv_tables()` è `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables`. Nel grafo unificato, il nodo canonico `.test_metric_engine_writes_csv_tables()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables`.
<!-- GRAPH_EDGE graph=unificata index=631 role=outgoing -->
Nel grafo unificato, il nodo canonico `.test_metric_engine_writes_csv_tables()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `.test_metric_engine_writes_csv_tables()` chiama `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 631 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 631 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 631 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 631 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 631 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 631 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `L724`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 631 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 631 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `ast`. Nel grafo unificato, l'arco 631 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`. Nel grafo unificato, l'arco 631 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`.
<!-- GRAPH_EDGE graph=unificata index=632 role=outgoing -->
Nel grafo unificato, il nodo canonico `.test_metric_engine_writes_csv_tables()` è la sorgente `source` della relazione `calls` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` all'ID `Request-Dataset-Generator::timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `.test_metric_engine_writes_csv_tables()` chiama `constant_rate()`. Nel grafo unificato, il tipo dichiarato dell'arco 632 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 632 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 632 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 632 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 632 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 632 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `L727`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 632 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 632 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `ast`. Nel grafo unificato, l'arco 632 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`. Nel grafo unificato, l'arco 632 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`.
<!-- GRAPH_EDGE graph=unificata index=633 role=outgoing -->
Nel grafo unificato, il nodo canonico `.test_metric_engine_writes_csv_tables()` è la sorgente `source` della relazione `calls` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.test_metric_engine_writes_csv_tables()` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 633 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 633 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 633 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 633 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 633 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 633 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `L739`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 633 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 633 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `ast`. Nel grafo unificato, l'arco 633 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`. Nel grafo unificato, l'arco 633 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`.
<!-- GRAPH_EDGE graph=unificata index=115 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_metric_engine_writes_csv_tables()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_collect_logs` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `.test_metric_engine_writes_csv_tables()`. Nel grafo unificato, il tipo dichiarato dell'arco 115 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 115 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 115 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 115 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 115 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 115 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `L749`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 115 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 115 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `ast`. Nel grafo unificato, l'arco 115 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`. Nel grafo unificato, l'arco 115 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`.
<!-- GRAPH_EDGE graph=unificata index=279 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_metric_engine_writes_csv_tables()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `run_metric_engine()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::metric_engine_run_metric_engine` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables`. La relazione tecnica esplicita è la seguente: `run_metric_engine()` chiama `.test_metric_engine_writes_csv_tables()`. Nel grafo unificato, il tipo dichiarato dell'arco 279 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 279 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 279 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 279 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 279 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 279 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `L756`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 279 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 279 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `ast`. Nel grafo unificato, l'arco 279 registra come `source` l'identificatore `Request-Dataset-Generator::metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`. Nel grafo unificato, l'arco 279 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`.
<!-- GRAPH_EDGE graph=unificata index=328 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_metric_engine_writes_csv_tables()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `.test_metric_engine_writes_csv_tables()`. Nel grafo unificato, il tipo dichiarato dell'arco 328 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 328 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 328 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 328 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 328 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 328 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `L717`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 328 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 328 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `ast`. Nel grafo unificato, l'arco 328 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`. Nel grafo unificato, l'arco 328 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`.
<!-- GRAPH_EDGE graph=unificata index=623 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_metric_engine_writes_csv_tables()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `EngineSmokeTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables`. La relazione tecnica esplicita è la seguente: `EngineSmokeTest` registra come proprio metodo `.test_metric_engine_writes_csv_tables()`. Nel grafo unificato, il tipo dichiarato dell'arco 623 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 623 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 623 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 623 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `L711`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 623 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 623 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 623 che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()` è `1.0`. Nel grafo unificato, l'arco 623 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`. Nel grafo unificato, l'arco 623 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `.test_metric_engine_writes_csv_tables()`.

### .test_timeline_assigns_unique_flow_occurrence_ids()

<!-- GRAPH_NODE graph=codice index=315 id=tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `.test_timeline_assigns_unique_flow_occurrence_ids()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `L772`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` possiede l'identificatore esatto `tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `.test_timeline_assigns_unique_flow_occurrence_ids()`.
<!-- GRAPH_EDGE graph=codice index=333 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `.test_timeline_assigns_unique_flow_occurrence_ids()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 333 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 333 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 333 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 333 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 333 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 333 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `L773`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 333 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 333 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `ast`. Nel grafo del codice RDG, l'arco 333 registra come `source` l'identificatore `tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` per la relazione che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`. Nel grafo del codice RDG, l'arco 333 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`.
<!-- GRAPH_EDGE graph=codice index=636 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `.test_timeline_assigns_unique_flow_occurrence_ids()` chiama `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 636 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 636 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 636 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 636 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 636 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 636 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `L780`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 636 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 636 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `ast`. Nel grafo del codice RDG, l'arco 636 registra come `source` l'identificatore `tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` per la relazione che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`. Nel grafo del codice RDG, l'arco 636 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`.
<!-- GRAPH_EDGE graph=codice index=637 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è la sorgente `source` della relazione `calls` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `.test_timeline_assigns_unique_flow_occurrence_ids()` chiama `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 637 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 637 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 637 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 637 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 637 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 637 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `L783`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 637 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 637 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `ast`. Nel grafo del codice RDG, l'arco 637 registra come `source` l'identificatore `tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` per la relazione che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`. Nel grafo del codice RDG, l'arco 637 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`.
<!-- GRAPH_EDGE graph=codice index=634 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `TimelineIdentityTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_timelineidentitytest` all'ID `tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids`. La relazione tecnica esplicita è la seguente: `TimelineIdentityTest` registra come proprio metodo `.test_timeline_assigns_unique_flow_occurrence_ids()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 634 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 634 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 634 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 634 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `L772`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 634 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 634 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `ast`. Nel grafo del codice RDG, l'arco 634 registra come `source` l'identificatore `tests_test_core_timelineidentitytest` per la relazione che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`. Nel grafo del codice RDG, l'arco 634 registra come `target` l'identificatore `tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` per la relazione che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 634 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=315 id=Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `.test_timeline_assigns_unique_flow_occurrence_ids()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `L772`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `.test_timeline_assigns_unique_flow_occurrence_ids()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids`. Nel grafo unificato, il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids`.
<!-- GRAPH_EDGE graph=unificata index=636 role=outgoing -->
Nel grafo unificato, il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `.test_timeline_assigns_unique_flow_occurrence_ids()` chiama `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 636 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 636 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 636 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 636 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 636 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 636 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `L780`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 636 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 636 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `ast`. Nel grafo unificato, l'arco 636 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` per la relazione che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`. Nel grafo unificato, l'arco 636 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`.
<!-- GRAPH_EDGE graph=unificata index=637 role=outgoing -->
Nel grafo unificato, il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è la sorgente `source` della relazione `calls` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` all'ID `Request-Dataset-Generator::timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `.test_timeline_assigns_unique_flow_occurrence_ids()` chiama `constant_rate()`. Nel grafo unificato, il tipo dichiarato dell'arco 637 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 637 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 637 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 637 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 637 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 637 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `L783`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 637 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 637 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `ast`. Nel grafo unificato, l'arco 637 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` per la relazione che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`. Nel grafo unificato, l'arco 637 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`.
<!-- GRAPH_EDGE graph=unificata index=333 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `.test_timeline_assigns_unique_flow_occurrence_ids()`. Nel grafo unificato, il tipo dichiarato dell'arco 333 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 333 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 333 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 333 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 333 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 333 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `L773`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 333 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 333 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `ast`. Nel grafo unificato, l'arco 333 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`. Nel grafo unificato, l'arco 333 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` per la relazione che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`.
<!-- GRAPH_EDGE graph=unificata index=634 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `TimelineIdentityTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest` all'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids`. La relazione tecnica esplicita è la seguente: `TimelineIdentityTest` registra come proprio metodo `.test_timeline_assigns_unique_flow_occurrence_ids()`. Nel grafo unificato, il tipo dichiarato dell'arco 634 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 634 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 634 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 634 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `L772`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 634 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 634 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 634 che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()` è `1.0`. Nel grafo unificato, l'arco 634 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest` per la relazione che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`. Nel grafo unificato, l'arco 634 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` per la relazione che coinvolge il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`.

### .test_timeline_tags_burst_events()

<!-- GRAPH_NODE graph=codice index=316 id=tests_test_core_timelineidentitytest_test_timeline_tags_burst_events -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_timeline_tags_burst_events()` è `.test_timeline_tags_burst_events()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_timeline_tags_burst_events()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_timeline_tags_burst_events()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_timeline_tags_burst_events()` è `L795`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_timeline_tags_burst_events()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_timeline_tags_burst_events()` possiede l'identificatore esatto `tests_test_core_timelineidentitytest_test_timeline_tags_burst_events`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_timeline_tags_burst_events()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_timeline_tags_burst_events()` è `.test_timeline_tags_burst_events()`.
<!-- GRAPH_EDGE graph=codice index=334 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_timeline_tags_burst_events()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `.test_timeline_tags_burst_events()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 334 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 334 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 334 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 334 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 334 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 334 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `L796`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 334 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 334 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `ast`. Nel grafo del codice RDG, l'arco 334 registra come `source` l'identificatore `tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` per la relazione che coinvolge il nodo canonico `.test_timeline_tags_burst_events()`. Nel grafo del codice RDG, l'arco 334 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `.test_timeline_tags_burst_events()`.
<!-- GRAPH_EDGE graph=codice index=638 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_timeline_tags_burst_events()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `.test_timeline_tags_burst_events()` chiama `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 638 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 638 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 638 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 638 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 638 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 638 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `L803`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 638 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 638 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `ast`. Nel grafo del codice RDG, l'arco 638 registra come `source` l'identificatore `tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` per la relazione che coinvolge il nodo canonico `.test_timeline_tags_burst_events()`. Nel grafo del codice RDG, l'arco 638 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `.test_timeline_tags_burst_events()`.
<!-- GRAPH_EDGE graph=codice index=639 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_timeline_tags_burst_events()` è la sorgente `source` della relazione `calls` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `.test_timeline_tags_burst_events()` chiama `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 639 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 639 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 639 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 639 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 639 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 639 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `L806`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 639 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 639 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `ast`. Nel grafo del codice RDG, l'arco 639 registra come `source` l'identificatore `tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` per la relazione che coinvolge il nodo canonico `.test_timeline_tags_burst_events()`. Nel grafo del codice RDG, l'arco 639 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `.test_timeline_tags_burst_events()`.
<!-- GRAPH_EDGE graph=codice index=635 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_timeline_tags_burst_events()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `TimelineIdentityTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_timelineidentitytest` all'ID `tests_test_core_timelineidentitytest_test_timeline_tags_burst_events`. La relazione tecnica esplicita è la seguente: `TimelineIdentityTest` registra come proprio metodo `.test_timeline_tags_burst_events()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 635 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 635 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 635 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 635 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `L795`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 635 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 635 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `ast`. Nel grafo del codice RDG, l'arco 635 registra come `source` l'identificatore `tests_test_core_timelineidentitytest` per la relazione che coinvolge il nodo canonico `.test_timeline_tags_burst_events()`. Nel grafo del codice RDG, l'arco 635 registra come `target` l'identificatore `tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` per la relazione che coinvolge il nodo canonico `.test_timeline_tags_burst_events()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 635 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=316 id=Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_tags_burst_events -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_timeline_tags_burst_events()` è `.test_timeline_tags_burst_events()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_timeline_tags_burst_events()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_timeline_tags_burst_events()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_timeline_tags_burst_events()` è `L795`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_timeline_tags_burst_events()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_timeline_tags_burst_events()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_timeline_tags_burst_events()` è `.test_timeline_tags_burst_events()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_timeline_tags_burst_events()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_timeline_tags_burst_events()` è `tests_test_core_timelineidentitytest_test_timeline_tags_burst_events`. Nel grafo unificato, il nodo canonico `.test_timeline_tags_burst_events()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_tags_burst_events`.
<!-- GRAPH_EDGE graph=unificata index=638 role=outgoing -->
Nel grafo unificato, il nodo canonico `.test_timeline_tags_burst_events()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `.test_timeline_tags_burst_events()` chiama `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 638 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 638 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 638 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 638 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 638 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 638 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `L803`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 638 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 638 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `ast`. Nel grafo unificato, l'arco 638 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` per la relazione che coinvolge il nodo canonico `.test_timeline_tags_burst_events()`. Nel grafo unificato, l'arco 638 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `.test_timeline_tags_burst_events()`.
<!-- GRAPH_EDGE graph=unificata index=639 role=outgoing -->
Nel grafo unificato, il nodo canonico `.test_timeline_tags_burst_events()` è la sorgente `source` della relazione `calls` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` all'ID `Request-Dataset-Generator::timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `.test_timeline_tags_burst_events()` chiama `constant_rate()`. Nel grafo unificato, il tipo dichiarato dell'arco 639 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 639 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 639 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 639 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 639 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 639 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `L806`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 639 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 639 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `ast`. Nel grafo unificato, l'arco 639 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` per la relazione che coinvolge il nodo canonico `.test_timeline_tags_burst_events()`. Nel grafo unificato, l'arco 639 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `.test_timeline_tags_burst_events()`.
<!-- GRAPH_EDGE graph=unificata index=334 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_timeline_tags_burst_events()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_tags_burst_events`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `.test_timeline_tags_burst_events()`. Nel grafo unificato, il tipo dichiarato dell'arco 334 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 334 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 334 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 334 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 334 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 334 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `L796`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 334 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 334 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `ast`. Nel grafo unificato, l'arco 334 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `.test_timeline_tags_burst_events()`. Nel grafo unificato, l'arco 334 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` per la relazione che coinvolge il nodo canonico `.test_timeline_tags_burst_events()`.
<!-- GRAPH_EDGE graph=unificata index=635 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_timeline_tags_burst_events()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `TimelineIdentityTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest` all'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_tags_burst_events`. La relazione tecnica esplicita è la seguente: `TimelineIdentityTest` registra come proprio metodo `.test_timeline_tags_burst_events()`. Nel grafo unificato, il tipo dichiarato dell'arco 635 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 635 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 635 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 635 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `L795`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 635 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 635 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 635 che coinvolge il nodo canonico `.test_timeline_tags_burst_events()` è `1.0`. Nel grafo unificato, l'arco 635 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest` per la relazione che coinvolge il nodo canonico `.test_timeline_tags_burst_events()`. Nel grafo unificato, l'arco 635 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` per la relazione che coinvolge il nodo canonico `.test_timeline_tags_burst_events()`.

### _annotate_dataset_flows()

<!-- GRAPH_NODE graph=codice index=68 id=main_annotate_dataset_flows -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_annotate_dataset_flows()` è `_annotate_dataset_flows()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_annotate_dataset_flows()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_annotate_dataset_flows()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_annotate_dataset_flows()` è `L198`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_annotate_dataset_flows()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_annotate_dataset_flows()` possiede l'identificatore esatto `main_annotate_dataset_flows`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_annotate_dataset_flows()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_annotate_dataset_flows()` è `_annotate_dataset_flows()`.
Nel grafo del codice RDG, il nodo canonico `_annotate_dataset_flows()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=119 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_annotate_dataset_flows()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_annotate_dataset_flows`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_annotate_dataset_flows()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 119 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 119 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 119 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 119 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `L198`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 119 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 119 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `ast`. Nel grafo del codice RDG, l'arco 119 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `_annotate_dataset_flows()`. Nel grafo del codice RDG, l'arco 119 registra come `target` l'identificatore `main_annotate_dataset_flows` per la relazione che coinvolge il nodo canonico `_annotate_dataset_flows()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 119 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=175 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_annotate_dataset_flows()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_execute` all'ID `main_annotate_dataset_flows`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `_annotate_dataset_flows()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 175 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 175 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 175 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 175 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 175 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `L326`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 175 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 175 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `ast`. Nel grafo del codice RDG, l'arco 175 registra come `source` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `_annotate_dataset_flows()`. Nel grafo del codice RDG, l'arco 175 registra come `target` l'identificatore `main_annotate_dataset_flows` per la relazione che coinvolge il nodo canonico `_annotate_dataset_flows()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 175 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=176 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_annotate_dataset_flows()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_requests()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_requests` all'ID `main_annotate_dataset_flows`. La relazione tecnica esplicita è la seguente: `cmd_requests()` chiama `_annotate_dataset_flows()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 176 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 176 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 176 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 176 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 176 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `L290`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 176 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 176 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `ast`. Nel grafo del codice RDG, l'arco 176 registra come `source` l'identificatore `main_cmd_requests` per la relazione che coinvolge il nodo canonico `_annotate_dataset_flows()`. Nel grafo del codice RDG, l'arco 176 registra come `target` l'identificatore `main_annotate_dataset_flows` per la relazione che coinvolge il nodo canonico `_annotate_dataset_flows()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 176 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=177 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_annotate_dataset_flows()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_timeline` all'ID `main_annotate_dataset_flows`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `_annotate_dataset_flows()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 177 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 177 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 177 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 177 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 177 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `L426`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 177 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 177 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `ast`. Nel grafo del codice RDG, l'arco 177 registra come `source` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `_annotate_dataset_flows()`. Nel grafo del codice RDG, l'arco 177 registra come `target` l'identificatore `main_annotate_dataset_flows` per la relazione che coinvolge il nodo canonico `_annotate_dataset_flows()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 177 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=68 id=Request-Dataset-Generator::main_annotate_dataset_flows -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_annotate_dataset_flows()` è `_annotate_dataset_flows()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_annotate_dataset_flows()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_annotate_dataset_flows()` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_annotate_dataset_flows()` è `L198`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_annotate_dataset_flows()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_annotate_dataset_flows()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_annotate_dataset_flows()` è `_annotate_dataset_flows()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_annotate_dataset_flows()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_annotate_dataset_flows()` è `main_annotate_dataset_flows`. Nel grafo unificato, il nodo canonico `_annotate_dataset_flows()` possiede l'identificatore esatto `Request-Dataset-Generator::main_annotate_dataset_flows`.
<!-- GRAPH_EDGE graph=unificata index=175 role=outgoing -->
Nel grafo unificato, il nodo canonico `_annotate_dataset_flows()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_annotate_dataset_flows` all'ID `Request-Dataset-Generator::main_cmd_execute`. La relazione tecnica esplicita è la seguente: `_annotate_dataset_flows()` chiama `cmd_execute()`. Nel grafo unificato, il tipo dichiarato dell'arco 175 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 175 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 175 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 175 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 175 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `L326`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 175 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 175 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 175 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `1.0`. Nel grafo unificato, l'arco 175 registra come `source` l'identificatore `Request-Dataset-Generator::main_annotate_dataset_flows` per la relazione che coinvolge il nodo canonico `_annotate_dataset_flows()`. Nel grafo unificato, l'arco 175 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `_annotate_dataset_flows()`.
<!-- GRAPH_EDGE graph=unificata index=176 role=outgoing -->
Nel grafo unificato, il nodo canonico `_annotate_dataset_flows()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_requests()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_annotate_dataset_flows` all'ID `Request-Dataset-Generator::main_cmd_requests`. La relazione tecnica esplicita è la seguente: `_annotate_dataset_flows()` chiama `cmd_requests()`. Nel grafo unificato, il tipo dichiarato dell'arco 176 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 176 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 176 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 176 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 176 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `L290`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 176 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 176 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 176 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `1.0`. Nel grafo unificato, l'arco 176 registra come `source` l'identificatore `Request-Dataset-Generator::main_annotate_dataset_flows` per la relazione che coinvolge il nodo canonico `_annotate_dataset_flows()`. Nel grafo unificato, l'arco 176 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_requests` per la relazione che coinvolge il nodo canonico `_annotate_dataset_flows()`.
<!-- GRAPH_EDGE graph=unificata index=177 role=outgoing -->
Nel grafo unificato, il nodo canonico `_annotate_dataset_flows()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_annotate_dataset_flows` all'ID `Request-Dataset-Generator::main_cmd_timeline`. La relazione tecnica esplicita è la seguente: `_annotate_dataset_flows()` chiama `cmd_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 177 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 177 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 177 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 177 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 177 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `L426`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 177 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 177 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 177 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `1.0`. Nel grafo unificato, l'arco 177 registra come `source` l'identificatore `Request-Dataset-Generator::main_annotate_dataset_flows` per la relazione che coinvolge il nodo canonico `_annotate_dataset_flows()`. Nel grafo unificato, l'arco 177 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `_annotate_dataset_flows()`.
<!-- GRAPH_EDGE graph=unificata index=119 role=incoming -->
Nel grafo unificato, il nodo canonico `_annotate_dataset_flows()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_annotate_dataset_flows`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_annotate_dataset_flows()`. Nel grafo unificato, il tipo dichiarato dell'arco 119 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 119 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 119 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 119 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `L198`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 119 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 119 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 119 che coinvolge il nodo canonico `_annotate_dataset_flows()` è `1.0`. Nel grafo unificato, l'arco 119 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `_annotate_dataset_flows()`. Nel grafo unificato, l'arco 119 registra come `target` l'identificatore `Request-Dataset-Generator::main_annotate_dataset_flows` per la relazione che coinvolge il nodo canonico `_annotate_dataset_flows()`.

### _append_json()

<!-- GRAPH_NODE graph=codice index=71 id=main_append_json -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_append_json()` è `_append_json()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_append_json()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_append_json()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_append_json()` è `L235`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_append_json()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_append_json()` possiede l'identificatore esatto `main_append_json`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_append_json()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_append_json()` è `_append_json()`.
Nel grafo del codice RDG, il nodo canonico `_append_json()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=120 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_append_json()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_append_json`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_append_json()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 120 che coinvolge il nodo canonico `_append_json()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 120 che coinvolge il nodo canonico `_append_json()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 120 che coinvolge il nodo canonico `_append_json()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 120 che coinvolge il nodo canonico `_append_json()` è `L235`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 120 che coinvolge il nodo canonico `_append_json()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 120 che coinvolge il nodo canonico `_append_json()` è `ast`. Nel grafo del codice RDG, l'arco 120 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `_append_json()`. Nel grafo del codice RDG, l'arco 120 registra come `target` l'identificatore `main_append_json` per la relazione che coinvolge il nodo canonico `_append_json()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 120 che coinvolge il nodo canonico `_append_json()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=186 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_append_json()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_write_output()`; l'orientamento registrato nel JSON va dall'ID `main_write_output` all'ID `main_append_json`. La relazione tecnica esplicita è la seguente: `_write_output()` chiama `_append_json()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 186 che coinvolge il nodo canonico `_append_json()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 186 che coinvolge il nodo canonico `_append_json()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 186 che coinvolge il nodo canonico `_append_json()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 186 che coinvolge il nodo canonico `_append_json()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 186 che coinvolge il nodo canonico `_append_json()` è `L226`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 186 che coinvolge il nodo canonico `_append_json()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 186 che coinvolge il nodo canonico `_append_json()` è `ast`. Nel grafo del codice RDG, l'arco 186 registra come `source` l'identificatore `main_write_output` per la relazione che coinvolge il nodo canonico `_append_json()`. Nel grafo del codice RDG, l'arco 186 registra come `target` l'identificatore `main_append_json` per la relazione che coinvolge il nodo canonico `_append_json()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 186 che coinvolge il nodo canonico `_append_json()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=71 id=Request-Dataset-Generator::main_append_json -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_append_json()` è `_append_json()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_append_json()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_append_json()` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_append_json()` è `L235`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_append_json()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_append_json()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_append_json()` è `_append_json()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_append_json()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_append_json()` è `main_append_json`. Nel grafo unificato, il nodo canonico `_append_json()` possiede l'identificatore esatto `Request-Dataset-Generator::main_append_json`.
Nel grafo unificato, il nodo canonico `_append_json()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=120 role=incoming -->
Nel grafo unificato, il nodo canonico `_append_json()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_append_json`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_append_json()`. Nel grafo unificato, il tipo dichiarato dell'arco 120 che coinvolge il nodo canonico `_append_json()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 120 che coinvolge il nodo canonico `_append_json()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 120 che coinvolge il nodo canonico `_append_json()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 120 che coinvolge il nodo canonico `_append_json()` è `L235`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 120 che coinvolge il nodo canonico `_append_json()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 120 che coinvolge il nodo canonico `_append_json()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 120 che coinvolge il nodo canonico `_append_json()` è `1.0`. Nel grafo unificato, l'arco 120 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `_append_json()`. Nel grafo unificato, l'arco 120 registra come `target` l'identificatore `Request-Dataset-Generator::main_append_json` per la relazione che coinvolge il nodo canonico `_append_json()`.
<!-- GRAPH_EDGE graph=unificata index=186 role=incoming -->
Nel grafo unificato, il nodo canonico `_append_json()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_write_output()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_write_output` all'ID `Request-Dataset-Generator::main_append_json`. La relazione tecnica esplicita è la seguente: `_write_output()` chiama `_append_json()`. Nel grafo unificato, il tipo dichiarato dell'arco 186 che coinvolge il nodo canonico `_append_json()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 186 che coinvolge il nodo canonico `_append_json()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 186 che coinvolge il nodo canonico `_append_json()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 186 che coinvolge il nodo canonico `_append_json()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 186 che coinvolge il nodo canonico `_append_json()` è `L226`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 186 che coinvolge il nodo canonico `_append_json()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 186 che coinvolge il nodo canonico `_append_json()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 186 che coinvolge il nodo canonico `_append_json()` è `1.0`. Nel grafo unificato, l'arco 186 registra come `source` l'identificatore `Request-Dataset-Generator::main_write_output` per la relazione che coinvolge il nodo canonico `_append_json()`. Nel grafo unificato, l'arco 186 registra come `target` l'identificatore `Request-Dataset-Generator::main_append_json` per la relazione che coinvolge il nodo canonico `_append_json()`.

### _build_namespace()

<!-- GRAPH_NODE graph=codice index=62 id=main_build_namespace -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_build_namespace()` è `_build_namespace()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_build_namespace()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_build_namespace()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_build_namespace()` è `L115`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_build_namespace()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_build_namespace()` possiede l'identificatore esatto `main_build_namespace`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_build_namespace()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_build_namespace()` è `_build_namespace()`.
<!-- GRAPH_EDGE graph=codice index=159 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_build_namespace()` è la sorgente `source` della relazione `references` verso il nodo canonico `Namespace`; l'orientamento registrato nel JSON va dall'ID `main_build_namespace` all'ID `namespace`. La relazione tecnica esplicita è la seguente: `_build_namespace()` fa riferimento a `Namespace`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 159 che coinvolge il nodo canonico `_build_namespace()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 159 che coinvolge il nodo canonico `_build_namespace()` è `return_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 159 che coinvolge il nodo canonico `_build_namespace()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 159 che coinvolge il nodo canonico `_build_namespace()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 159 che coinvolge il nodo canonico `_build_namespace()` è `L115`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 159 che coinvolge il nodo canonico `_build_namespace()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 159 che coinvolge il nodo canonico `_build_namespace()` è `ast`. Nel grafo del codice RDG, l'arco 159 registra come `source` l'identificatore `main_build_namespace` per la relazione che coinvolge il nodo canonico `_build_namespace()`. Nel grafo del codice RDG, l'arco 159 registra come `target` l'identificatore `namespace` per la relazione che coinvolge il nodo canonico `_build_namespace()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 159 che coinvolge il nodo canonico `_build_namespace()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=121 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_namespace()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_build_namespace`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_build_namespace()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 121 che coinvolge il nodo canonico `_build_namespace()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 121 che coinvolge il nodo canonico `_build_namespace()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 121 che coinvolge il nodo canonico `_build_namespace()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 121 che coinvolge il nodo canonico `_build_namespace()` è `L115`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 121 che coinvolge il nodo canonico `_build_namespace()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 121 che coinvolge il nodo canonico `_build_namespace()` è `ast`. Nel grafo del codice RDG, l'arco 121 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `_build_namespace()`. Nel grafo del codice RDG, l'arco 121 registra come `target` l'identificatore `main_build_namespace` per la relazione che coinvolge il nodo canonico `_build_namespace()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 121 che coinvolge il nodo canonico `_build_namespace()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=160 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_namespace()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_pipeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_pipeline` all'ID `main_build_namespace`. La relazione tecnica esplicita è la seguente: `cmd_pipeline()` chiama `_build_namespace()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 160 che coinvolge il nodo canonico `_build_namespace()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 160 che coinvolge il nodo canonico `_build_namespace()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 160 che coinvolge il nodo canonico `_build_namespace()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 160 che coinvolge il nodo canonico `_build_namespace()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 160 che coinvolge il nodo canonico `_build_namespace()` è `L521`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 160 che coinvolge il nodo canonico `_build_namespace()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 160 che coinvolge il nodo canonico `_build_namespace()` è `ast`. Nel grafo del codice RDG, l'arco 160 registra come `source` l'identificatore `main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `_build_namespace()`. Nel grafo del codice RDG, l'arco 160 registra come `target` l'identificatore `main_build_namespace` per la relazione che coinvolge il nodo canonico `_build_namespace()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 160 che coinvolge il nodo canonico `_build_namespace()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=161 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_namespace()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `main() [main_main]`; l'orientamento registrato nel JSON va dall'ID `main_main` all'ID `main_build_namespace`. La relazione tecnica esplicita è la seguente: `main() [main_main]` chiama `_build_namespace()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 161 che coinvolge il nodo canonico `_build_namespace()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 161 che coinvolge il nodo canonico `_build_namespace()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 161 che coinvolge il nodo canonico `_build_namespace()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 161 che coinvolge il nodo canonico `_build_namespace()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 161 che coinvolge il nodo canonico `_build_namespace()` è `L620`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 161 che coinvolge il nodo canonico `_build_namespace()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 161 che coinvolge il nodo canonico `_build_namespace()` è `ast`. Nel grafo del codice RDG, l'arco 161 registra come `source` l'identificatore `main_main` per la relazione che coinvolge il nodo canonico `_build_namespace()`. Nel grafo del codice RDG, l'arco 161 registra come `target` l'identificatore `main_build_namespace` per la relazione che coinvolge il nodo canonico `_build_namespace()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 161 che coinvolge il nodo canonico `_build_namespace()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=162 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_namespace()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf`; l'orientamento registrato nel JSON va dall'ID `main_rationale_116` all'ID `main_build_namespace`. La relazione tecnica esplicita è la seguente: `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` costituisce una motivazione per `_build_namespace()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 162 che coinvolge il nodo canonico `_build_namespace()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 162 che coinvolge il nodo canonico `_build_namespace()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 162 che coinvolge il nodo canonico `_build_namespace()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 162 che coinvolge il nodo canonico `_build_namespace()` è `L116`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 162 che coinvolge il nodo canonico `_build_namespace()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 162 che coinvolge il nodo canonico `_build_namespace()` è `ast`. Nel grafo del codice RDG, l'arco 162 registra come `source` l'identificatore `main_rationale_116` per la relazione che coinvolge il nodo canonico `_build_namespace()`. Nel grafo del codice RDG, l'arco 162 registra come `target` l'identificatore `main_build_namespace` per la relazione che coinvolge il nodo canonico `_build_namespace()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 162 che coinvolge il nodo canonico `_build_namespace()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=62 id=Request-Dataset-Generator::main_build_namespace -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_build_namespace()` è `_build_namespace()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_build_namespace()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_build_namespace()` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_build_namespace()` è `L115`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_build_namespace()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_build_namespace()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_build_namespace()` è `_build_namespace()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_build_namespace()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_build_namespace()` è `main_build_namespace`. Nel grafo unificato, il nodo canonico `_build_namespace()` possiede l'identificatore esatto `Request-Dataset-Generator::main_build_namespace`.
<!-- GRAPH_EDGE graph=unificata index=159 role=outgoing -->
Nel grafo unificato, il nodo canonico `_build_namespace()` è la sorgente `source` della relazione `references` verso il nodo canonico `Namespace`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_build_namespace` all'ID `Request-Dataset-Generator::namespace`. La relazione tecnica esplicita è la seguente: `_build_namespace()` fa riferimento a `Namespace`. Nel grafo unificato, il tipo dichiarato dell'arco 159 che coinvolge il nodo canonico `_build_namespace()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 159 che coinvolge il nodo canonico `_build_namespace()` è `return_type`. Nel grafo unificato, la confidenza categoriale dell'arco 159 che coinvolge il nodo canonico `_build_namespace()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 159 che coinvolge il nodo canonico `_build_namespace()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 159 che coinvolge il nodo canonico `_build_namespace()` è `L115`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 159 che coinvolge il nodo canonico `_build_namespace()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 159 che coinvolge il nodo canonico `_build_namespace()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 159 che coinvolge il nodo canonico `_build_namespace()` è `1.0`. Nel grafo unificato, l'arco 159 registra come `source` l'identificatore `Request-Dataset-Generator::main_build_namespace` per la relazione che coinvolge il nodo canonico `_build_namespace()`. Nel grafo unificato, l'arco 159 registra come `target` l'identificatore `Request-Dataset-Generator::namespace` per la relazione che coinvolge il nodo canonico `_build_namespace()`.
<!-- GRAPH_EDGE graph=unificata index=160 role=outgoing -->
Nel grafo unificato, il nodo canonico `_build_namespace()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_pipeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_build_namespace` all'ID `Request-Dataset-Generator::main_cmd_pipeline`. La relazione tecnica esplicita è la seguente: `_build_namespace()` chiama `cmd_pipeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 160 che coinvolge il nodo canonico `_build_namespace()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 160 che coinvolge il nodo canonico `_build_namespace()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 160 che coinvolge il nodo canonico `_build_namespace()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 160 che coinvolge il nodo canonico `_build_namespace()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 160 che coinvolge il nodo canonico `_build_namespace()` è `L521`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 160 che coinvolge il nodo canonico `_build_namespace()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 160 che coinvolge il nodo canonico `_build_namespace()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 160 che coinvolge il nodo canonico `_build_namespace()` è `1.0`. Nel grafo unificato, l'arco 160 registra come `source` l'identificatore `Request-Dataset-Generator::main_build_namespace` per la relazione che coinvolge il nodo canonico `_build_namespace()`. Nel grafo unificato, l'arco 160 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `_build_namespace()`.
<!-- GRAPH_EDGE graph=unificata index=161 role=outgoing -->
Nel grafo unificato, il nodo canonico `_build_namespace()` è la sorgente `source` della relazione `calls` verso il nodo canonico `main() [main_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_build_namespace` all'ID `Request-Dataset-Generator::main_main`. La relazione tecnica esplicita è la seguente: `_build_namespace()` chiama `main() [main_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 161 che coinvolge il nodo canonico `_build_namespace()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 161 che coinvolge il nodo canonico `_build_namespace()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 161 che coinvolge il nodo canonico `_build_namespace()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 161 che coinvolge il nodo canonico `_build_namespace()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 161 che coinvolge il nodo canonico `_build_namespace()` è `L620`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 161 che coinvolge il nodo canonico `_build_namespace()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 161 che coinvolge il nodo canonico `_build_namespace()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 161 che coinvolge il nodo canonico `_build_namespace()` è `1.0`. Nel grafo unificato, l'arco 161 registra come `source` l'identificatore `Request-Dataset-Generator::main_build_namespace` per la relazione che coinvolge il nodo canonico `_build_namespace()`. Nel grafo unificato, l'arco 161 registra come `target` l'identificatore `Request-Dataset-Generator::main_main` per la relazione che coinvolge il nodo canonico `_build_namespace()`.
<!-- GRAPH_EDGE graph=unificata index=162 role=outgoing -->
Nel grafo unificato, il nodo canonico `_build_namespace()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_build_namespace` all'ID `Request-Dataset-Generator::main_rationale_116`. La relazione tecnica esplicita è la seguente: `_build_namespace()` costituisce una motivazione per `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf`. Nel grafo unificato, il tipo dichiarato dell'arco 162 che coinvolge il nodo canonico `_build_namespace()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 162 che coinvolge il nodo canonico `_build_namespace()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 162 che coinvolge il nodo canonico `_build_namespace()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 162 che coinvolge il nodo canonico `_build_namespace()` è `L116`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 162 che coinvolge il nodo canonico `_build_namespace()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 162 che coinvolge il nodo canonico `_build_namespace()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 162 che coinvolge il nodo canonico `_build_namespace()` è `1.0`. Nel grafo unificato, l'arco 162 registra come `source` l'identificatore `Request-Dataset-Generator::main_build_namespace` per la relazione che coinvolge il nodo canonico `_build_namespace()`. Nel grafo unificato, l'arco 162 registra come `target` l'identificatore `Request-Dataset-Generator::main_rationale_116` per la relazione che coinvolge il nodo canonico `_build_namespace()`.
<!-- GRAPH_EDGE graph=unificata index=121 role=incoming -->
Nel grafo unificato, il nodo canonico `_build_namespace()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_build_namespace`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_build_namespace()`. Nel grafo unificato, il tipo dichiarato dell'arco 121 che coinvolge il nodo canonico `_build_namespace()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 121 che coinvolge il nodo canonico `_build_namespace()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 121 che coinvolge il nodo canonico `_build_namespace()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 121 che coinvolge il nodo canonico `_build_namespace()` è `L115`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 121 che coinvolge il nodo canonico `_build_namespace()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 121 che coinvolge il nodo canonico `_build_namespace()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 121 che coinvolge il nodo canonico `_build_namespace()` è `1.0`. Nel grafo unificato, l'arco 121 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `_build_namespace()`. Nel grafo unificato, l'arco 121 registra come `target` l'identificatore `Request-Dataset-Generator::main_build_namespace` per la relazione che coinvolge il nodo canonico `_build_namespace()`.

### _cfg_section()

<!-- GRAPH_NODE graph=codice index=61 id=main_cfg_section -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_cfg_section()` è `_cfg_section()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_cfg_section()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_cfg_section()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_cfg_section()` è `L105`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_cfg_section()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_cfg_section()` possiede l'identificatore esatto `main_cfg_section`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_cfg_section()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_cfg_section()` è `_cfg_section()`.
Nel grafo del codice RDG, il nodo canonico `_cfg_section()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=122 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_cfg_section()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_cfg_section`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_cfg_section()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 122 che coinvolge il nodo canonico `_cfg_section()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 122 che coinvolge il nodo canonico `_cfg_section()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 122 che coinvolge il nodo canonico `_cfg_section()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 122 che coinvolge il nodo canonico `_cfg_section()` è `L105`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 122 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 122 che coinvolge il nodo canonico `_cfg_section()` è `ast`. Nel grafo del codice RDG, l'arco 122 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo del codice RDG, l'arco 122 registra come `target` l'identificatore `main_cfg_section` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 122 che coinvolge il nodo canonico `_cfg_section()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=152 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_cfg_section()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_collect()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_collect` all'ID `main_cfg_section`. La relazione tecnica esplicita è la seguente: `cmd_collect()` chiama `_cfg_section()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 152 che coinvolge il nodo canonico `_cfg_section()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 152 che coinvolge il nodo canonico `_cfg_section()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 152 che coinvolge il nodo canonico `_cfg_section()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 152 che coinvolge il nodo canonico `_cfg_section()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 152 che coinvolge il nodo canonico `_cfg_section()` è `L475`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 152 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 152 che coinvolge il nodo canonico `_cfg_section()` è `ast`. Nel grafo del codice RDG, l'arco 152 registra come `source` l'identificatore `main_cmd_collect` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo del codice RDG, l'arco 152 registra come `target` l'identificatore `main_cfg_section` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 152 che coinvolge il nodo canonico `_cfg_section()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=153 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_cfg_section()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_execute` all'ID `main_cfg_section`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `_cfg_section()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 153 che coinvolge il nodo canonico `_cfg_section()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 153 che coinvolge il nodo canonico `_cfg_section()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 153 che coinvolge il nodo canonico `_cfg_section()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 153 che coinvolge il nodo canonico `_cfg_section()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 153 che coinvolge il nodo canonico `_cfg_section()` è `L309`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 153 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 153 che coinvolge il nodo canonico `_cfg_section()` è `ast`. Nel grafo del codice RDG, l'arco 153 registra come `source` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo del codice RDG, l'arco 153 registra come `target` l'identificatore `main_cfg_section` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 153 che coinvolge il nodo canonico `_cfg_section()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=154 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_cfg_section()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_metrics()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_metrics` all'ID `main_cfg_section`. La relazione tecnica esplicita è la seguente: `cmd_metrics()` chiama `_cfg_section()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 154 che coinvolge il nodo canonico `_cfg_section()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 154 che coinvolge il nodo canonico `_cfg_section()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 154 che coinvolge il nodo canonico `_cfg_section()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 154 che coinvolge il nodo canonico `_cfg_section()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 154 che coinvolge il nodo canonico `_cfg_section()` è `L497`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 154 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 154 che coinvolge il nodo canonico `_cfg_section()` è `ast`. Nel grafo del codice RDG, l'arco 154 registra come `source` l'identificatore `main_cmd_metrics` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo del codice RDG, l'arco 154 registra come `target` l'identificatore `main_cfg_section` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 154 che coinvolge il nodo canonico `_cfg_section()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=155 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_cfg_section()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_pipeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_pipeline` all'ID `main_cfg_section`. La relazione tecnica esplicita è la seguente: `cmd_pipeline()` chiama `_cfg_section()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 155 che coinvolge il nodo canonico `_cfg_section()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 155 che coinvolge il nodo canonico `_cfg_section()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 155 che coinvolge il nodo canonico `_cfg_section()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 155 che coinvolge il nodo canonico `_cfg_section()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 155 che coinvolge il nodo canonico `_cfg_section()` è `L529`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 155 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 155 che coinvolge il nodo canonico `_cfg_section()` è `ast`. Nel grafo del codice RDG, l'arco 155 registra come `source` l'identificatore `main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo del codice RDG, l'arco 155 registra come `target` l'identificatore `main_cfg_section` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 155 che coinvolge il nodo canonico `_cfg_section()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=156 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_cfg_section()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_requests()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_requests` all'ID `main_cfg_section`. La relazione tecnica esplicita è la seguente: `cmd_requests()` chiama `_cfg_section()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 156 che coinvolge il nodo canonico `_cfg_section()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 156 che coinvolge il nodo canonico `_cfg_section()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 156 che coinvolge il nodo canonico `_cfg_section()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 156 che coinvolge il nodo canonico `_cfg_section()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 156 che coinvolge il nodo canonico `_cfg_section()` è `L280`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 156 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 156 che coinvolge il nodo canonico `_cfg_section()` è `ast`. Nel grafo del codice RDG, l'arco 156 registra come `source` l'identificatore `main_cmd_requests` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo del codice RDG, l'arco 156 registra come `target` l'identificatore `main_cfg_section` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 156 che coinvolge il nodo canonico `_cfg_section()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=157 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_cfg_section()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_timeline` all'ID `main_cfg_section`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `_cfg_section()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 157 che coinvolge il nodo canonico `_cfg_section()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 157 che coinvolge il nodo canonico `_cfg_section()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 157 che coinvolge il nodo canonico `_cfg_section()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 157 che coinvolge il nodo canonico `_cfg_section()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 157 che coinvolge il nodo canonico `_cfg_section()` è `L411`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 157 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 157 che coinvolge il nodo canonico `_cfg_section()` è `ast`. Nel grafo del codice RDG, l'arco 157 registra come `source` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo del codice RDG, l'arco 157 registra come `target` l'identificatore `main_cfg_section` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 157 che coinvolge il nodo canonico `_cfg_section()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=158 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_cfg_section()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica`; l'orientamento registrato nel JSON va dall'ID `main_rationale_106` all'ID `main_cfg_section`. La relazione tecnica esplicita è la seguente: `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` costituisce una motivazione per `_cfg_section()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 158 che coinvolge il nodo canonico `_cfg_section()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 158 che coinvolge il nodo canonico `_cfg_section()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 158 che coinvolge il nodo canonico `_cfg_section()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 158 che coinvolge il nodo canonico `_cfg_section()` è `L106`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 158 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 158 che coinvolge il nodo canonico `_cfg_section()` è `ast`. Nel grafo del codice RDG, l'arco 158 registra come `source` l'identificatore `main_rationale_106` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo del codice RDG, l'arco 158 registra come `target` l'identificatore `main_cfg_section` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 158 che coinvolge il nodo canonico `_cfg_section()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=61 id=Request-Dataset-Generator::main_cfg_section -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_cfg_section()` è `_cfg_section()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_cfg_section()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_cfg_section()` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_cfg_section()` è `L105`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_cfg_section()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_cfg_section()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_cfg_section()` è `_cfg_section()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_cfg_section()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_cfg_section()` è `main_cfg_section`. Nel grafo unificato, il nodo canonico `_cfg_section()` possiede l'identificatore esatto `Request-Dataset-Generator::main_cfg_section`.
<!-- GRAPH_EDGE graph=unificata index=152 role=outgoing -->
Nel grafo unificato, il nodo canonico `_cfg_section()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_collect()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cfg_section` all'ID `Request-Dataset-Generator::main_cmd_collect`. La relazione tecnica esplicita è la seguente: `_cfg_section()` chiama `cmd_collect()`. Nel grafo unificato, il tipo dichiarato dell'arco 152 che coinvolge il nodo canonico `_cfg_section()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 152 che coinvolge il nodo canonico `_cfg_section()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 152 che coinvolge il nodo canonico `_cfg_section()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 152 che coinvolge il nodo canonico `_cfg_section()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 152 che coinvolge il nodo canonico `_cfg_section()` è `L475`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 152 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 152 che coinvolge il nodo canonico `_cfg_section()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 152 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo unificato, l'arco 152 registra come `source` l'identificatore `Request-Dataset-Generator::main_cfg_section` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo unificato, l'arco 152 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_collect` per la relazione che coinvolge il nodo canonico `_cfg_section()`.
<!-- GRAPH_EDGE graph=unificata index=153 role=outgoing -->
Nel grafo unificato, il nodo canonico `_cfg_section()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cfg_section` all'ID `Request-Dataset-Generator::main_cmd_execute`. La relazione tecnica esplicita è la seguente: `_cfg_section()` chiama `cmd_execute()`. Nel grafo unificato, il tipo dichiarato dell'arco 153 che coinvolge il nodo canonico `_cfg_section()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 153 che coinvolge il nodo canonico `_cfg_section()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 153 che coinvolge il nodo canonico `_cfg_section()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 153 che coinvolge il nodo canonico `_cfg_section()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 153 che coinvolge il nodo canonico `_cfg_section()` è `L309`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 153 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 153 che coinvolge il nodo canonico `_cfg_section()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 153 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo unificato, l'arco 153 registra come `source` l'identificatore `Request-Dataset-Generator::main_cfg_section` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo unificato, l'arco 153 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `_cfg_section()`.
<!-- GRAPH_EDGE graph=unificata index=154 role=outgoing -->
Nel grafo unificato, il nodo canonico `_cfg_section()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_metrics()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cfg_section` all'ID `Request-Dataset-Generator::main_cmd_metrics`. La relazione tecnica esplicita è la seguente: `_cfg_section()` chiama `cmd_metrics()`. Nel grafo unificato, il tipo dichiarato dell'arco 154 che coinvolge il nodo canonico `_cfg_section()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 154 che coinvolge il nodo canonico `_cfg_section()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 154 che coinvolge il nodo canonico `_cfg_section()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 154 che coinvolge il nodo canonico `_cfg_section()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 154 che coinvolge il nodo canonico `_cfg_section()` è `L497`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 154 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 154 che coinvolge il nodo canonico `_cfg_section()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 154 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo unificato, l'arco 154 registra come `source` l'identificatore `Request-Dataset-Generator::main_cfg_section` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo unificato, l'arco 154 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_metrics` per la relazione che coinvolge il nodo canonico `_cfg_section()`.
<!-- GRAPH_EDGE graph=unificata index=155 role=outgoing -->
Nel grafo unificato, il nodo canonico `_cfg_section()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_pipeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cfg_section` all'ID `Request-Dataset-Generator::main_cmd_pipeline`. La relazione tecnica esplicita è la seguente: `_cfg_section()` chiama `cmd_pipeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 155 che coinvolge il nodo canonico `_cfg_section()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 155 che coinvolge il nodo canonico `_cfg_section()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 155 che coinvolge il nodo canonico `_cfg_section()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 155 che coinvolge il nodo canonico `_cfg_section()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 155 che coinvolge il nodo canonico `_cfg_section()` è `L529`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 155 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 155 che coinvolge il nodo canonico `_cfg_section()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 155 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo unificato, l'arco 155 registra come `source` l'identificatore `Request-Dataset-Generator::main_cfg_section` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo unificato, l'arco 155 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `_cfg_section()`.
<!-- GRAPH_EDGE graph=unificata index=156 role=outgoing -->
Nel grafo unificato, il nodo canonico `_cfg_section()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_requests()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cfg_section` all'ID `Request-Dataset-Generator::main_cmd_requests`. La relazione tecnica esplicita è la seguente: `_cfg_section()` chiama `cmd_requests()`. Nel grafo unificato, il tipo dichiarato dell'arco 156 che coinvolge il nodo canonico `_cfg_section()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 156 che coinvolge il nodo canonico `_cfg_section()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 156 che coinvolge il nodo canonico `_cfg_section()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 156 che coinvolge il nodo canonico `_cfg_section()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 156 che coinvolge il nodo canonico `_cfg_section()` è `L280`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 156 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 156 che coinvolge il nodo canonico `_cfg_section()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 156 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo unificato, l'arco 156 registra come `source` l'identificatore `Request-Dataset-Generator::main_cfg_section` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo unificato, l'arco 156 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_requests` per la relazione che coinvolge il nodo canonico `_cfg_section()`.
<!-- GRAPH_EDGE graph=unificata index=157 role=outgoing -->
Nel grafo unificato, il nodo canonico `_cfg_section()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cfg_section` all'ID `Request-Dataset-Generator::main_cmd_timeline`. La relazione tecnica esplicita è la seguente: `_cfg_section()` chiama `cmd_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 157 che coinvolge il nodo canonico `_cfg_section()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 157 che coinvolge il nodo canonico `_cfg_section()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 157 che coinvolge il nodo canonico `_cfg_section()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 157 che coinvolge il nodo canonico `_cfg_section()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 157 che coinvolge il nodo canonico `_cfg_section()` è `L411`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 157 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 157 che coinvolge il nodo canonico `_cfg_section()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 157 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo unificato, l'arco 157 registra come `source` l'identificatore `Request-Dataset-Generator::main_cfg_section` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo unificato, l'arco 157 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `_cfg_section()`.
<!-- GRAPH_EDGE graph=unificata index=158 role=outgoing -->
Nel grafo unificato, il nodo canonico `_cfg_section()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cfg_section` all'ID `Request-Dataset-Generator::main_rationale_106`. La relazione tecnica esplicita è la seguente: `_cfg_section()` costituisce una motivazione per `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica`. Nel grafo unificato, il tipo dichiarato dell'arco 158 che coinvolge il nodo canonico `_cfg_section()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 158 che coinvolge il nodo canonico `_cfg_section()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 158 che coinvolge il nodo canonico `_cfg_section()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 158 che coinvolge il nodo canonico `_cfg_section()` è `L106`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 158 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 158 che coinvolge il nodo canonico `_cfg_section()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 158 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo unificato, l'arco 158 registra come `source` l'identificatore `Request-Dataset-Generator::main_cfg_section` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo unificato, l'arco 158 registra come `target` l'identificatore `Request-Dataset-Generator::main_rationale_106` per la relazione che coinvolge il nodo canonico `_cfg_section()`.
<!-- GRAPH_EDGE graph=unificata index=122 role=incoming -->
Nel grafo unificato, il nodo canonico `_cfg_section()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_cfg_section`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_cfg_section()`. Nel grafo unificato, il tipo dichiarato dell'arco 122 che coinvolge il nodo canonico `_cfg_section()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 122 che coinvolge il nodo canonico `_cfg_section()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 122 che coinvolge il nodo canonico `_cfg_section()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 122 che coinvolge il nodo canonico `_cfg_section()` è `L105`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 122 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 122 che coinvolge il nodo canonico `_cfg_section()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 122 che coinvolge il nodo canonico `_cfg_section()` è `1.0`. Nel grafo unificato, l'arco 122 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `_cfg_section()`. Nel grafo unificato, l'arco 122 registra come `target` l'identificatore `Request-Dataset-Generator::main_cfg_section` per la relazione che coinvolge il nodo canonico `_cfg_section()`.

### _configure_run_outputs()

<!-- GRAPH_NODE graph=codice index=66 id=main_configure_run_outputs -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_configure_run_outputs()` è `_configure_run_outputs()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_configure_run_outputs()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_configure_run_outputs()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_configure_run_outputs()` è `L162`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_configure_run_outputs()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_configure_run_outputs()` possiede l'identificatore esatto `main_configure_run_outputs`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_configure_run_outputs()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_configure_run_outputs()` è `_configure_run_outputs()`.
<!-- GRAPH_EDGE graph=codice index=171 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_configure_run_outputs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_ensure_experiment_metadata()`; l'orientamento registrato nel JSON va dall'ID `main_configure_run_outputs` all'ID `main_ensure_experiment_metadata`. La relazione tecnica esplicita è la seguente: `_configure_run_outputs()` chiama `_ensure_experiment_metadata()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 171 che coinvolge il nodo canonico `_configure_run_outputs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 171 che coinvolge il nodo canonico `_configure_run_outputs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 171 che coinvolge il nodo canonico `_configure_run_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 171 che coinvolge il nodo canonico `_configure_run_outputs()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 171 che coinvolge il nodo canonico `_configure_run_outputs()` è `L163`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 171 che coinvolge il nodo canonico `_configure_run_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 171 che coinvolge il nodo canonico `_configure_run_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 171 registra come `source` l'identificatore `main_configure_run_outputs` per la relazione che coinvolge il nodo canonico `_configure_run_outputs()`. Nel grafo del codice RDG, l'arco 171 registra come `target` l'identificatore `main_ensure_experiment_metadata` per la relazione che coinvolge il nodo canonico `_configure_run_outputs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 171 che coinvolge il nodo canonico `_configure_run_outputs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=130 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_configure_run_outputs()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_configure_run_outputs`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_configure_run_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 130 che coinvolge il nodo canonico `_configure_run_outputs()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 130 che coinvolge il nodo canonico `_configure_run_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 130 che coinvolge il nodo canonico `_configure_run_outputs()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 130 che coinvolge il nodo canonico `_configure_run_outputs()` è `L162`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 130 che coinvolge il nodo canonico `_configure_run_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 130 che coinvolge il nodo canonico `_configure_run_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 130 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `_configure_run_outputs()`. Nel grafo del codice RDG, l'arco 130 registra come `target` l'identificatore `main_configure_run_outputs` per la relazione che coinvolge il nodo canonico `_configure_run_outputs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 130 che coinvolge il nodo canonico `_configure_run_outputs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=173 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_configure_run_outputs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_pipeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_pipeline` all'ID `main_configure_run_outputs`. La relazione tecnica esplicita è la seguente: `cmd_pipeline()` chiama `_configure_run_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 173 che coinvolge il nodo canonico `_configure_run_outputs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 173 che coinvolge il nodo canonico `_configure_run_outputs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 173 che coinvolge il nodo canonico `_configure_run_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 173 che coinvolge il nodo canonico `_configure_run_outputs()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 173 che coinvolge il nodo canonico `_configure_run_outputs()` è `L516`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 173 che coinvolge il nodo canonico `_configure_run_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 173 che coinvolge il nodo canonico `_configure_run_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 173 registra come `source` l'identificatore `main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `_configure_run_outputs()`. Nel grafo del codice RDG, l'arco 173 registra come `target` l'identificatore `main_configure_run_outputs` per la relazione che coinvolge il nodo canonico `_configure_run_outputs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 173 che coinvolge il nodo canonico `_configure_run_outputs()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=66 id=Request-Dataset-Generator::main_configure_run_outputs -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_configure_run_outputs()` è `_configure_run_outputs()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_configure_run_outputs()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_configure_run_outputs()` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_configure_run_outputs()` è `L162`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_configure_run_outputs()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_configure_run_outputs()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_configure_run_outputs()` è `_configure_run_outputs()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_configure_run_outputs()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_configure_run_outputs()` è `main_configure_run_outputs`. Nel grafo unificato, il nodo canonico `_configure_run_outputs()` possiede l'identificatore esatto `Request-Dataset-Generator::main_configure_run_outputs`.
<!-- GRAPH_EDGE graph=unificata index=173 role=outgoing -->
Nel grafo unificato, il nodo canonico `_configure_run_outputs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_pipeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_configure_run_outputs` all'ID `Request-Dataset-Generator::main_cmd_pipeline`. La relazione tecnica esplicita è la seguente: `_configure_run_outputs()` chiama `cmd_pipeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 173 che coinvolge il nodo canonico `_configure_run_outputs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 173 che coinvolge il nodo canonico `_configure_run_outputs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 173 che coinvolge il nodo canonico `_configure_run_outputs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 173 che coinvolge il nodo canonico `_configure_run_outputs()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 173 che coinvolge il nodo canonico `_configure_run_outputs()` è `L516`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 173 che coinvolge il nodo canonico `_configure_run_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 173 che coinvolge il nodo canonico `_configure_run_outputs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 173 che coinvolge il nodo canonico `_configure_run_outputs()` è `1.0`. Nel grafo unificato, l'arco 173 registra come `source` l'identificatore `Request-Dataset-Generator::main_configure_run_outputs` per la relazione che coinvolge il nodo canonico `_configure_run_outputs()`. Nel grafo unificato, l'arco 173 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `_configure_run_outputs()`.
<!-- GRAPH_EDGE graph=unificata index=130 role=incoming -->
Nel grafo unificato, il nodo canonico `_configure_run_outputs()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_configure_run_outputs`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_configure_run_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 130 che coinvolge il nodo canonico `_configure_run_outputs()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 130 che coinvolge il nodo canonico `_configure_run_outputs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 130 che coinvolge il nodo canonico `_configure_run_outputs()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 130 che coinvolge il nodo canonico `_configure_run_outputs()` è `L162`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 130 che coinvolge il nodo canonico `_configure_run_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 130 che coinvolge il nodo canonico `_configure_run_outputs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 130 che coinvolge il nodo canonico `_configure_run_outputs()` è `1.0`. Nel grafo unificato, l'arco 130 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `_configure_run_outputs()`. Nel grafo unificato, l'arco 130 registra come `target` l'identificatore `Request-Dataset-Generator::main_configure_run_outputs` per la relazione che coinvolge il nodo canonico `_configure_run_outputs()`.
<!-- GRAPH_EDGE graph=unificata index=171 role=incoming -->
Nel grafo unificato, il nodo canonico `_configure_run_outputs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_ensure_experiment_metadata()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_ensure_experiment_metadata` all'ID `Request-Dataset-Generator::main_configure_run_outputs`. La relazione tecnica esplicita è la seguente: `_ensure_experiment_metadata()` chiama `_configure_run_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 171 che coinvolge il nodo canonico `_configure_run_outputs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 171 che coinvolge il nodo canonico `_configure_run_outputs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 171 che coinvolge il nodo canonico `_configure_run_outputs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 171 che coinvolge il nodo canonico `_configure_run_outputs()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 171 che coinvolge il nodo canonico `_configure_run_outputs()` è `L163`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 171 che coinvolge il nodo canonico `_configure_run_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 171 che coinvolge il nodo canonico `_configure_run_outputs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 171 che coinvolge il nodo canonico `_configure_run_outputs()` è `1.0`. Nel grafo unificato, l'arco 171 registra come `source` l'identificatore `Request-Dataset-Generator::main_ensure_experiment_metadata` per la relazione che coinvolge il nodo canonico `_configure_run_outputs()`. Nel grafo unificato, l'arco 171 registra come `target` l'identificatore `Request-Dataset-Generator::main_configure_run_outputs` per la relazione che coinvolge il nodo canonico `_configure_run_outputs()`.

### _ensure_experiment_metadata()

<!-- GRAPH_NODE graph=codice index=65 id=main_ensure_experiment_metadata -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_ensure_experiment_metadata()` è `_ensure_experiment_metadata()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_ensure_experiment_metadata()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_ensure_experiment_metadata()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_ensure_experiment_metadata()` è `L137`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_ensure_experiment_metadata()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_ensure_experiment_metadata()` possiede l'identificatore esatto `main_ensure_experiment_metadata`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_ensure_experiment_metadata()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_ensure_experiment_metadata()` è `_ensure_experiment_metadata()`.
<!-- GRAPH_EDGE graph=codice index=170 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_ensure_experiment_metadata()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_slug()`; l'orientamento registrato nel JSON va dall'ID `main_ensure_experiment_metadata` all'ID `main_slug`. La relazione tecnica esplicita è la seguente: `_ensure_experiment_metadata()` chiama `_slug()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 170 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 170 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 170 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 170 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 170 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `L155`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 170 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 170 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `ast`. Nel grafo del codice RDG, l'arco 170 registra come `source` l'identificatore `main_ensure_experiment_metadata` per la relazione che coinvolge il nodo canonico `_ensure_experiment_metadata()`. Nel grafo del codice RDG, l'arco 170 registra come `target` l'identificatore `main_slug` per la relazione che coinvolge il nodo canonico `_ensure_experiment_metadata()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 170 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=131 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_ensure_experiment_metadata()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_ensure_experiment_metadata`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_ensure_experiment_metadata()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 131 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 131 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 131 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 131 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `L137`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 131 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 131 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `ast`. Nel grafo del codice RDG, l'arco 131 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `_ensure_experiment_metadata()`. Nel grafo del codice RDG, l'arco 131 registra come `target` l'identificatore `main_ensure_experiment_metadata` per la relazione che coinvolge il nodo canonico `_ensure_experiment_metadata()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 131 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=171 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_ensure_experiment_metadata()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_configure_run_outputs()`; l'orientamento registrato nel JSON va dall'ID `main_configure_run_outputs` all'ID `main_ensure_experiment_metadata`. La relazione tecnica esplicita è la seguente: `_configure_run_outputs()` chiama `_ensure_experiment_metadata()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 171 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 171 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 171 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 171 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 171 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `L163`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 171 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 171 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `ast`. Nel grafo del codice RDG, l'arco 171 registra come `source` l'identificatore `main_configure_run_outputs` per la relazione che coinvolge il nodo canonico `_ensure_experiment_metadata()`. Nel grafo del codice RDG, l'arco 171 registra come `target` l'identificatore `main_ensure_experiment_metadata` per la relazione che coinvolge il nodo canonico `_ensure_experiment_metadata()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 171 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=172 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_ensure_experiment_metadata()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `main() [main_main]`; l'orientamento registrato nel JSON va dall'ID `main_main` all'ID `main_ensure_experiment_metadata`. La relazione tecnica esplicita è la seguente: `main() [main_main]` chiama `_ensure_experiment_metadata()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 172 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 172 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 172 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 172 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 172 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `L619`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 172 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 172 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `ast`. Nel grafo del codice RDG, l'arco 172 registra come `source` l'identificatore `main_main` per la relazione che coinvolge il nodo canonico `_ensure_experiment_metadata()`. Nel grafo del codice RDG, l'arco 172 registra come `target` l'identificatore `main_ensure_experiment_metadata` per la relazione che coinvolge il nodo canonico `_ensure_experiment_metadata()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 172 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=65 id=Request-Dataset-Generator::main_ensure_experiment_metadata -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_ensure_experiment_metadata()` è `_ensure_experiment_metadata()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_ensure_experiment_metadata()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_ensure_experiment_metadata()` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_ensure_experiment_metadata()` è `L137`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_ensure_experiment_metadata()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_ensure_experiment_metadata()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_ensure_experiment_metadata()` è `_ensure_experiment_metadata()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_ensure_experiment_metadata()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_ensure_experiment_metadata()` è `main_ensure_experiment_metadata`. Nel grafo unificato, il nodo canonico `_ensure_experiment_metadata()` possiede l'identificatore esatto `Request-Dataset-Generator::main_ensure_experiment_metadata`.
<!-- GRAPH_EDGE graph=unificata index=171 role=outgoing -->
Nel grafo unificato, il nodo canonico `_ensure_experiment_metadata()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_configure_run_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_ensure_experiment_metadata` all'ID `Request-Dataset-Generator::main_configure_run_outputs`. La relazione tecnica esplicita è la seguente: `_ensure_experiment_metadata()` chiama `_configure_run_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 171 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 171 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 171 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 171 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 171 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `L163`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 171 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 171 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 171 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `1.0`. Nel grafo unificato, l'arco 171 registra come `source` l'identificatore `Request-Dataset-Generator::main_ensure_experiment_metadata` per la relazione che coinvolge il nodo canonico `_ensure_experiment_metadata()`. Nel grafo unificato, l'arco 171 registra come `target` l'identificatore `Request-Dataset-Generator::main_configure_run_outputs` per la relazione che coinvolge il nodo canonico `_ensure_experiment_metadata()`.
<!-- GRAPH_EDGE graph=unificata index=172 role=outgoing -->
Nel grafo unificato, il nodo canonico `_ensure_experiment_metadata()` è la sorgente `source` della relazione `calls` verso il nodo canonico `main() [main_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_ensure_experiment_metadata` all'ID `Request-Dataset-Generator::main_main`. La relazione tecnica esplicita è la seguente: `_ensure_experiment_metadata()` chiama `main() [main_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 172 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 172 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 172 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 172 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 172 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `L619`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 172 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 172 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 172 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `1.0`. Nel grafo unificato, l'arco 172 registra come `source` l'identificatore `Request-Dataset-Generator::main_ensure_experiment_metadata` per la relazione che coinvolge il nodo canonico `_ensure_experiment_metadata()`. Nel grafo unificato, l'arco 172 registra come `target` l'identificatore `Request-Dataset-Generator::main_main` per la relazione che coinvolge il nodo canonico `_ensure_experiment_metadata()`.
<!-- GRAPH_EDGE graph=unificata index=131 role=incoming -->
Nel grafo unificato, il nodo canonico `_ensure_experiment_metadata()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_ensure_experiment_metadata`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_ensure_experiment_metadata()`. Nel grafo unificato, il tipo dichiarato dell'arco 131 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 131 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 131 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 131 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `L137`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 131 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 131 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 131 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `1.0`. Nel grafo unificato, l'arco 131 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `_ensure_experiment_metadata()`. Nel grafo unificato, l'arco 131 registra come `target` l'identificatore `Request-Dataset-Generator::main_ensure_experiment_metadata` per la relazione che coinvolge il nodo canonico `_ensure_experiment_metadata()`.
<!-- GRAPH_EDGE graph=unificata index=170 role=incoming -->
Nel grafo unificato, il nodo canonico `_ensure_experiment_metadata()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_slug()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_slug` all'ID `Request-Dataset-Generator::main_ensure_experiment_metadata`. La relazione tecnica esplicita è la seguente: `_slug()` chiama `_ensure_experiment_metadata()`. Nel grafo unificato, il tipo dichiarato dell'arco 170 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 170 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 170 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 170 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 170 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `L155`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 170 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 170 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 170 che coinvolge il nodo canonico `_ensure_experiment_metadata()` è `1.0`. Nel grafo unificato, l'arco 170 registra come `source` l'identificatore `Request-Dataset-Generator::main_slug` per la relazione che coinvolge il nodo canonico `_ensure_experiment_metadata()`. Nel grafo unificato, l'arco 170 registra come `target` l'identificatore `Request-Dataset-Generator::main_ensure_experiment_metadata` per la relazione che coinvolge il nodo canonico `_ensure_experiment_metadata()`.

### _frange()

<!-- GRAPH_NODE graph=codice index=326 id=timeline_generator_frange -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_frange()` è `_frange()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_frange()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_frange()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_frange()` è `L126`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_frange()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_frange()` possiede l'identificatore esatto `timeline_generator_frange`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_frange()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_frange()` è `_frange()`.
Nel grafo del codice RDG, il nodo canonico `_frange()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=645 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_frange()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `timeline_generator` all'ID `timeline_generator_frange`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `_frange()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 645 che coinvolge il nodo canonico `_frange()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 645 che coinvolge il nodo canonico `_frange()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 645 che coinvolge il nodo canonico `_frange()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 645 che coinvolge il nodo canonico `_frange()` è `L126`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 645 che coinvolge il nodo canonico `_frange()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 645 che coinvolge il nodo canonico `_frange()` è `ast`. Nel grafo del codice RDG, l'arco 645 registra come `source` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `_frange()`. Nel grafo del codice RDG, l'arco 645 registra come `target` l'identificatore `timeline_generator_frange` per la relazione che coinvolge il nodo canonico `_frange()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 645 che coinvolge il nodo canonico `_frange()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=656 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_frange()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `generate_arrivals()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_generate_arrivals` all'ID `timeline_generator_frange`. La relazione tecnica esplicita è la seguente: `generate_arrivals()` chiama `_frange()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 656 che coinvolge il nodo canonico `_frange()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 656 che coinvolge il nodo canonico `_frange()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 656 che coinvolge il nodo canonico `_frange()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 656 che coinvolge il nodo canonico `_frange()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 656 che coinvolge il nodo canonico `_frange()` è `L104`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 656 che coinvolge il nodo canonico `_frange()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 656 che coinvolge il nodo canonico `_frange()` è `ast`. Nel grafo del codice RDG, l'arco 656 registra come `source` l'identificatore `timeline_generator_generate_arrivals` per la relazione che coinvolge il nodo canonico `_frange()`. Nel grafo del codice RDG, l'arco 656 registra come `target` l'identificatore `timeline_generator_frange` per la relazione che coinvolge il nodo canonico `_frange()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 656 che coinvolge il nodo canonico `_frange()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=326 id=Request-Dataset-Generator::timeline_generator_frange -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_frange()` è `_frange()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_frange()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_frange()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_frange()` è `L126`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_frange()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_frange()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_frange()` è `_frange()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_frange()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_frange()` è `timeline_generator_frange`. Nel grafo unificato, il nodo canonico `_frange()` possiede l'identificatore esatto `Request-Dataset-Generator::timeline_generator_frange`.
Nel grafo unificato, il nodo canonico `_frange()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=645 role=incoming -->
Nel grafo unificato, il nodo canonico `_frange()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator` all'ID `Request-Dataset-Generator::timeline_generator_frange`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `_frange()`. Nel grafo unificato, il tipo dichiarato dell'arco 645 che coinvolge il nodo canonico `_frange()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 645 che coinvolge il nodo canonico `_frange()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 645 che coinvolge il nodo canonico `_frange()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 645 che coinvolge il nodo canonico `_frange()` è `L126`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 645 che coinvolge il nodo canonico `_frange()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 645 che coinvolge il nodo canonico `_frange()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 645 che coinvolge il nodo canonico `_frange()` è `1.0`. Nel grafo unificato, l'arco 645 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `_frange()`. Nel grafo unificato, l'arco 645 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_frange` per la relazione che coinvolge il nodo canonico `_frange()`.
<!-- GRAPH_EDGE graph=unificata index=656 role=incoming -->
Nel grafo unificato, il nodo canonico `_frange()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `generate_arrivals()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator_generate_arrivals` all'ID `Request-Dataset-Generator::timeline_generator_frange`. La relazione tecnica esplicita è la seguente: `generate_arrivals()` chiama `_frange()`. Nel grafo unificato, il tipo dichiarato dell'arco 656 che coinvolge il nodo canonico `_frange()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 656 che coinvolge il nodo canonico `_frange()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 656 che coinvolge il nodo canonico `_frange()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 656 che coinvolge il nodo canonico `_frange()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 656 che coinvolge il nodo canonico `_frange()` è `L104`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 656 che coinvolge il nodo canonico `_frange()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 656 che coinvolge il nodo canonico `_frange()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 656 che coinvolge il nodo canonico `_frange()` è `1.0`. Nel grafo unificato, l'arco 656 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator_generate_arrivals` per la relazione che coinvolge il nodo canonico `_frange()`. Nel grafo unificato, l'arco 656 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_frange` per la relazione che coinvolge il nodo canonico `_frange()`.

### _load_config() [main_load_config]

<!-- GRAPH_NODE graph=codice index=60 id=main_load_config -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_load_config() [main_load_config]` è `_load_config()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_load_config() [main_load_config]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_load_config() [main_load_config]` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_load_config() [main_load_config]` è `L100`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_load_config() [main_load_config]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_load_config() [main_load_config]` possiede l'identificatore esatto `main_load_config`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_load_config() [main_load_config]` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_load_config() [main_load_config]` è `_load_config()`.
Nel grafo del codice RDG, il nodo canonico `_load_config() [main_load_config]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=132 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_load_config() [main_load_config]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_load_config`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_load_config() [main_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 132 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 132 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 132 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 132 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `L100`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 132 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 132 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `ast`. Nel grafo del codice RDG, l'arco 132 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `_load_config() [main_load_config]`. Nel grafo del codice RDG, l'arco 132 registra come `target` l'identificatore `main_load_config` per la relazione che coinvolge il nodo canonico `_load_config() [main_load_config]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 132 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=151 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_load_config() [main_load_config]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `main() [main_main]`; l'orientamento registrato nel JSON va dall'ID `main_main` all'ID `main_load_config`. La relazione tecnica esplicita è la seguente: `main() [main_main]` chiama `_load_config() [main_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 151 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 151 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 151 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 151 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 151 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `L611`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 151 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 151 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `ast`. Nel grafo del codice RDG, l'arco 151 registra come `source` l'identificatore `main_main` per la relazione che coinvolge il nodo canonico `_load_config() [main_load_config]`. Nel grafo del codice RDG, l'arco 151 registra come `target` l'identificatore `main_load_config` per la relazione che coinvolge il nodo canonico `_load_config() [main_load_config]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 151 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=60 id=Request-Dataset-Generator::main_load_config -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_load_config() [main_load_config]` è `_load_config()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_load_config() [main_load_config]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_load_config() [main_load_config]` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_load_config() [main_load_config]` è `L100`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_load_config() [main_load_config]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_load_config() [main_load_config]` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_load_config() [main_load_config]` è `_load_config()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_load_config() [main_load_config]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_load_config() [main_load_config]` è `main_load_config`. Nel grafo unificato, il nodo canonico `_load_config() [main_load_config]` possiede l'identificatore esatto `Request-Dataset-Generator::main_load_config`.
<!-- GRAPH_EDGE graph=unificata index=151 role=outgoing -->
Nel grafo unificato, il nodo canonico `_load_config() [main_load_config]` è la sorgente `source` della relazione `calls` verso il nodo canonico `main() [main_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_load_config` all'ID `Request-Dataset-Generator::main_main`. La relazione tecnica esplicita è la seguente: `_load_config() [main_load_config]` chiama `main() [main_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 151 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 151 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 151 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 151 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 151 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `L611`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 151 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 151 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 151 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `1.0`. Nel grafo unificato, l'arco 151 registra come `source` l'identificatore `Request-Dataset-Generator::main_load_config` per la relazione che coinvolge il nodo canonico `_load_config() [main_load_config]`. Nel grafo unificato, l'arco 151 registra come `target` l'identificatore `Request-Dataset-Generator::main_main` per la relazione che coinvolge il nodo canonico `_load_config() [main_load_config]`.
<!-- GRAPH_EDGE graph=unificata index=132 role=incoming -->
Nel grafo unificato, il nodo canonico `_load_config() [main_load_config]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_load_config`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_load_config() [main_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 132 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 132 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 132 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 132 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `L100`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 132 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 132 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 132 che coinvolge il nodo canonico `_load_config() [main_load_config]` è `1.0`. Nel grafo unificato, l'arco 132 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `_load_config() [main_load_config]`. Nel grafo unificato, l'arco 132 registra come `target` l'identificatore `Request-Dataset-Generator::main_load_config` per la relazione che coinvolge il nodo canonico `_load_config() [main_load_config]`.

### _resolve_output_path() [main_resolve_output_path]

<!-- GRAPH_NODE graph=codice index=69 id=main_resolve_output_path -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `_resolve_output_path()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_resolve_output_path() [main_resolve_output_path]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `L211`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_resolve_output_path() [main_resolve_output_path]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_resolve_output_path() [main_resolve_output_path]` possiede l'identificatore esatto `main_resolve_output_path`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_resolve_output_path() [main_resolve_output_path]` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `_resolve_output_path()`.
Nel grafo del codice RDG, il nodo canonico `_resolve_output_path() [main_resolve_output_path]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=134 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_resolve_output_path`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 134 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 134 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 134 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 134 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `L211`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 134 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 134 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `ast`. Nel grafo del codice RDG, l'arco 134 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, l'arco 134 registra come `target` l'identificatore `main_resolve_output_path` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 134 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=178 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_collect()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_collect` all'ID `main_resolve_output_path`. La relazione tecnica esplicita è la seguente: `cmd_collect()` chiama `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 178 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 178 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 178 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 178 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 178 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `L486`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 178 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 178 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `ast`. Nel grafo del codice RDG, l'arco 178 registra come `source` l'identificatore `main_cmd_collect` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, l'arco 178 registra come `target` l'identificatore `main_resolve_output_path` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 178 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=179 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_execute` all'ID `main_resolve_output_path`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 179 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 179 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 179 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 179 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 179 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `L330`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 179 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 179 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `ast`. Nel grafo del codice RDG, l'arco 179 registra come `source` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, l'arco 179 registra come `target` l'identificatore `main_resolve_output_path` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 179 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=180 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_metrics()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_metrics` all'ID `main_resolve_output_path`. La relazione tecnica esplicita è la seguente: `cmd_metrics()` chiama `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 180 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 180 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 180 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 180 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 180 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `L505`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 180 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 180 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `ast`. Nel grafo del codice RDG, l'arco 180 registra come `source` l'identificatore `main_cmd_metrics` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, l'arco 180 registra come `target` l'identificatore `main_resolve_output_path` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 180 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=181 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_pipeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_pipeline` all'ID `main_resolve_output_path`. La relazione tecnica esplicita è la seguente: `cmd_pipeline()` chiama `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 181 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 181 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 181 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 181 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 181 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `L530`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 181 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 181 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `ast`. Nel grafo del codice RDG, l'arco 181 registra come `source` l'identificatore `main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, l'arco 181 registra come `target` l'identificatore `main_resolve_output_path` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 181 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=182 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_write_output()`; l'orientamento registrato nel JSON va dall'ID `main_write_output` all'ID `main_resolve_output_path`. La relazione tecnica esplicita è la seguente: `_write_output()` chiama `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 182 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 182 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 182 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 182 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 182 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `L223`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 182 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 182 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `ast`. Nel grafo del codice RDG, l'arco 182 registra come `source` l'identificatore `main_write_output` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, l'arco 182 registra come `target` l'identificatore `main_resolve_output_path` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 182 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=69 id=Request-Dataset-Generator::main_resolve_output_path -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `_resolve_output_path()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_resolve_output_path() [main_resolve_output_path]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `L211`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_resolve_output_path() [main_resolve_output_path]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_resolve_output_path() [main_resolve_output_path]` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `_resolve_output_path()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_resolve_output_path() [main_resolve_output_path]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `main_resolve_output_path`. Nel grafo unificato, il nodo canonico `_resolve_output_path() [main_resolve_output_path]` possiede l'identificatore esatto `Request-Dataset-Generator::main_resolve_output_path`.
<!-- GRAPH_EDGE graph=unificata index=178 role=outgoing -->
Nel grafo unificato, il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_collect()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_resolve_output_path` all'ID `Request-Dataset-Generator::main_cmd_collect`. La relazione tecnica esplicita è la seguente: `_resolve_output_path() [main_resolve_output_path]` chiama `cmd_collect()`. Nel grafo unificato, il tipo dichiarato dell'arco 178 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 178 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 178 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 178 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 178 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `L486`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 178 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 178 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 178 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`. Nel grafo unificato, l'arco 178 registra come `source` l'identificatore `Request-Dataset-Generator::main_resolve_output_path` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`. Nel grafo unificato, l'arco 178 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_collect` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`.
<!-- GRAPH_EDGE graph=unificata index=179 role=outgoing -->
Nel grafo unificato, il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_resolve_output_path` all'ID `Request-Dataset-Generator::main_cmd_execute`. La relazione tecnica esplicita è la seguente: `_resolve_output_path() [main_resolve_output_path]` chiama `cmd_execute()`. Nel grafo unificato, il tipo dichiarato dell'arco 179 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 179 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 179 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 179 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 179 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `L330`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 179 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 179 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 179 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`. Nel grafo unificato, l'arco 179 registra come `source` l'identificatore `Request-Dataset-Generator::main_resolve_output_path` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`. Nel grafo unificato, l'arco 179 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`.
<!-- GRAPH_EDGE graph=unificata index=180 role=outgoing -->
Nel grafo unificato, il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_metrics()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_resolve_output_path` all'ID `Request-Dataset-Generator::main_cmd_metrics`. La relazione tecnica esplicita è la seguente: `_resolve_output_path() [main_resolve_output_path]` chiama `cmd_metrics()`. Nel grafo unificato, il tipo dichiarato dell'arco 180 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 180 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 180 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 180 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 180 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `L505`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 180 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 180 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 180 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`. Nel grafo unificato, l'arco 180 registra come `source` l'identificatore `Request-Dataset-Generator::main_resolve_output_path` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`. Nel grafo unificato, l'arco 180 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_metrics` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`.
<!-- GRAPH_EDGE graph=unificata index=181 role=outgoing -->
Nel grafo unificato, il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_pipeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_resolve_output_path` all'ID `Request-Dataset-Generator::main_cmd_pipeline`. La relazione tecnica esplicita è la seguente: `_resolve_output_path() [main_resolve_output_path]` chiama `cmd_pipeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 181 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 181 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 181 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 181 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 181 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `L530`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 181 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 181 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 181 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`. Nel grafo unificato, l'arco 181 registra come `source` l'identificatore `Request-Dataset-Generator::main_resolve_output_path` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`. Nel grafo unificato, l'arco 181 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`.
<!-- GRAPH_EDGE graph=unificata index=182 role=outgoing -->
Nel grafo unificato, il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_write_output()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_resolve_output_path` all'ID `Request-Dataset-Generator::main_write_output`. La relazione tecnica esplicita è la seguente: `_resolve_output_path() [main_resolve_output_path]` chiama `_write_output()`. Nel grafo unificato, il tipo dichiarato dell'arco 182 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 182 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 182 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 182 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 182 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `L223`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 182 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 182 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 182 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`. Nel grafo unificato, l'arco 182 registra come `source` l'identificatore `Request-Dataset-Generator::main_resolve_output_path` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`. Nel grafo unificato, l'arco 182 registra come `target` l'identificatore `Request-Dataset-Generator::main_write_output` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`.
<!-- GRAPH_EDGE graph=unificata index=134 role=incoming -->
Nel grafo unificato, il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_resolve_output_path`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_resolve_output_path() [main_resolve_output_path]`. Nel grafo unificato, il tipo dichiarato dell'arco 134 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 134 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 134 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 134 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `L211`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 134 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 134 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 134 che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]` è `1.0`. Nel grafo unificato, l'arco 134 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`. Nel grafo unificato, l'arco 134 registra come `target` l'identificatore `Request-Dataset-Generator::main_resolve_output_path` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [main_resolve_output_path]`.

### _slug()

<!-- GRAPH_NODE graph=codice index=64 id=main_slug -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_slug()` è `_slug()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_slug()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_slug()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_slug()` è `L132`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_slug()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_slug()` possiede l'identificatore esatto `main_slug`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_slug()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_slug()` è `_slug()`.
Nel grafo del codice RDG, il nodo canonico `_slug()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=135 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_slug()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_slug`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_slug()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 135 che coinvolge il nodo canonico `_slug()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 135 che coinvolge il nodo canonico `_slug()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 135 che coinvolge il nodo canonico `_slug()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 135 che coinvolge il nodo canonico `_slug()` è `L132`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 135 che coinvolge il nodo canonico `_slug()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 135 che coinvolge il nodo canonico `_slug()` è `ast`. Nel grafo del codice RDG, l'arco 135 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `_slug()`. Nel grafo del codice RDG, l'arco 135 registra come `target` l'identificatore `main_slug` per la relazione che coinvolge il nodo canonico `_slug()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 135 che coinvolge il nodo canonico `_slug()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=170 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_slug()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_ensure_experiment_metadata()`; l'orientamento registrato nel JSON va dall'ID `main_ensure_experiment_metadata` all'ID `main_slug`. La relazione tecnica esplicita è la seguente: `_ensure_experiment_metadata()` chiama `_slug()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 170 che coinvolge il nodo canonico `_slug()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 170 che coinvolge il nodo canonico `_slug()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 170 che coinvolge il nodo canonico `_slug()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 170 che coinvolge il nodo canonico `_slug()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 170 che coinvolge il nodo canonico `_slug()` è `L155`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 170 che coinvolge il nodo canonico `_slug()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 170 che coinvolge il nodo canonico `_slug()` è `ast`. Nel grafo del codice RDG, l'arco 170 registra come `source` l'identificatore `main_ensure_experiment_metadata` per la relazione che coinvolge il nodo canonico `_slug()`. Nel grafo del codice RDG, l'arco 170 registra come `target` l'identificatore `main_slug` per la relazione che coinvolge il nodo canonico `_slug()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 170 che coinvolge il nodo canonico `_slug()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=64 id=Request-Dataset-Generator::main_slug -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_slug()` è `_slug()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_slug()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_slug()` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_slug()` è `L132`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_slug()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_slug()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_slug()` è `_slug()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_slug()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_slug()` è `main_slug`. Nel grafo unificato, il nodo canonico `_slug()` possiede l'identificatore esatto `Request-Dataset-Generator::main_slug`.
<!-- GRAPH_EDGE graph=unificata index=170 role=outgoing -->
Nel grafo unificato, il nodo canonico `_slug()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_ensure_experiment_metadata()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_slug` all'ID `Request-Dataset-Generator::main_ensure_experiment_metadata`. La relazione tecnica esplicita è la seguente: `_slug()` chiama `_ensure_experiment_metadata()`. Nel grafo unificato, il tipo dichiarato dell'arco 170 che coinvolge il nodo canonico `_slug()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 170 che coinvolge il nodo canonico `_slug()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 170 che coinvolge il nodo canonico `_slug()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 170 che coinvolge il nodo canonico `_slug()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 170 che coinvolge il nodo canonico `_slug()` è `L155`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 170 che coinvolge il nodo canonico `_slug()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 170 che coinvolge il nodo canonico `_slug()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 170 che coinvolge il nodo canonico `_slug()` è `1.0`. Nel grafo unificato, l'arco 170 registra come `source` l'identificatore `Request-Dataset-Generator::main_slug` per la relazione che coinvolge il nodo canonico `_slug()`. Nel grafo unificato, l'arco 170 registra come `target` l'identificatore `Request-Dataset-Generator::main_ensure_experiment_metadata` per la relazione che coinvolge il nodo canonico `_slug()`.
<!-- GRAPH_EDGE graph=unificata index=135 role=incoming -->
Nel grafo unificato, il nodo canonico `_slug()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_slug`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_slug()`. Nel grafo unificato, il tipo dichiarato dell'arco 135 che coinvolge il nodo canonico `_slug()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 135 che coinvolge il nodo canonico `_slug()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 135 che coinvolge il nodo canonico `_slug()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 135 che coinvolge il nodo canonico `_slug()` è `L132`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 135 che coinvolge il nodo canonico `_slug()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 135 che coinvolge il nodo canonico `_slug()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 135 che coinvolge il nodo canonico `_slug()` è `1.0`. Nel grafo unificato, l'arco 135 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `_slug()`. Nel grafo unificato, l'arco 135 registra come `target` l'identificatore `Request-Dataset-Generator::main_slug` per la relazione che coinvolge il nodo canonico `_slug()`.

### _write_output()

<!-- GRAPH_NODE graph=codice index=70 id=main_write_output -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_write_output()` è `_write_output()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_write_output()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_write_output()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_write_output()` è `L221`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_write_output()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_write_output()` possiede l'identificatore esatto `main_write_output`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_write_output()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_write_output()` è `_write_output()`.
<!-- GRAPH_EDGE graph=codice index=182 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_write_output()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_resolve_output_path() [main_resolve_output_path]`; l'orientamento registrato nel JSON va dall'ID `main_write_output` all'ID `main_resolve_output_path`. La relazione tecnica esplicita è la seguente: `_write_output()` chiama `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 182 che coinvolge il nodo canonico `_write_output()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 182 che coinvolge il nodo canonico `_write_output()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 182 che coinvolge il nodo canonico `_write_output()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 182 che coinvolge il nodo canonico `_write_output()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 182 che coinvolge il nodo canonico `_write_output()` è `L223`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 182 che coinvolge il nodo canonico `_write_output()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 182 che coinvolge il nodo canonico `_write_output()` è `ast`. Nel grafo del codice RDG, l'arco 182 registra come `source` l'identificatore `main_write_output` per la relazione che coinvolge il nodo canonico `_write_output()`. Nel grafo del codice RDG, l'arco 182 registra come `target` l'identificatore `main_resolve_output_path` per la relazione che coinvolge il nodo canonico `_write_output()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 182 che coinvolge il nodo canonico `_write_output()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=186 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_write_output()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_append_json()`; l'orientamento registrato nel JSON va dall'ID `main_write_output` all'ID `main_append_json`. La relazione tecnica esplicita è la seguente: `_write_output()` chiama `_append_json()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 186 che coinvolge il nodo canonico `_write_output()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 186 che coinvolge il nodo canonico `_write_output()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 186 che coinvolge il nodo canonico `_write_output()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 186 che coinvolge il nodo canonico `_write_output()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 186 che coinvolge il nodo canonico `_write_output()` è `L226`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 186 che coinvolge il nodo canonico `_write_output()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 186 che coinvolge il nodo canonico `_write_output()` è `ast`. Nel grafo del codice RDG, l'arco 186 registra come `source` l'identificatore `main_write_output` per la relazione che coinvolge il nodo canonico `_write_output()`. Nel grafo del codice RDG, l'arco 186 registra come `target` l'identificatore `main_append_json` per la relazione che coinvolge il nodo canonico `_write_output()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 186 che coinvolge il nodo canonico `_write_output()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=136 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_write_output()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_write_output`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_write_output()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 136 che coinvolge il nodo canonico `_write_output()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 136 che coinvolge il nodo canonico `_write_output()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 136 che coinvolge il nodo canonico `_write_output()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 136 che coinvolge il nodo canonico `_write_output()` è `L221`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 136 che coinvolge il nodo canonico `_write_output()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 136 che coinvolge il nodo canonico `_write_output()` è `ast`. Nel grafo del codice RDG, l'arco 136 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `_write_output()`. Nel grafo del codice RDG, l'arco 136 registra come `target` l'identificatore `main_write_output` per la relazione che coinvolge il nodo canonico `_write_output()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 136 che coinvolge il nodo canonico `_write_output()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=183 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_write_output()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_patients()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_patients` all'ID `main_write_output`. La relazione tecnica esplicita è la seguente: `cmd_patients()` chiama `_write_output()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 183 che coinvolge il nodo canonico `_write_output()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 183 che coinvolge il nodo canonico `_write_output()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 183 che coinvolge il nodo canonico `_write_output()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 183 che coinvolge il nodo canonico `_write_output()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 183 che coinvolge il nodo canonico `_write_output()` è `L274`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 183 che coinvolge il nodo canonico `_write_output()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 183 che coinvolge il nodo canonico `_write_output()` è `ast`. Nel grafo del codice RDG, l'arco 183 registra come `source` l'identificatore `main_cmd_patients` per la relazione che coinvolge il nodo canonico `_write_output()`. Nel grafo del codice RDG, l'arco 183 registra come `target` l'identificatore `main_write_output` per la relazione che coinvolge il nodo canonico `_write_output()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 183 che coinvolge il nodo canonico `_write_output()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=184 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_write_output()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_requests()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_requests` all'ID `main_write_output`. La relazione tecnica esplicita è la seguente: `cmd_requests()` chiama `_write_output()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 184 che coinvolge il nodo canonico `_write_output()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 184 che coinvolge il nodo canonico `_write_output()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 184 che coinvolge il nodo canonico `_write_output()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 184 che coinvolge il nodo canonico `_write_output()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 184 che coinvolge il nodo canonico `_write_output()` è `L292`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 184 che coinvolge il nodo canonico `_write_output()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 184 che coinvolge il nodo canonico `_write_output()` è `ast`. Nel grafo del codice RDG, l'arco 184 registra come `source` l'identificatore `main_cmd_requests` per la relazione che coinvolge il nodo canonico `_write_output()`. Nel grafo del codice RDG, l'arco 184 registra come `target` l'identificatore `main_write_output` per la relazione che coinvolge il nodo canonico `_write_output()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 184 che coinvolge il nodo canonico `_write_output()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=185 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_write_output()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_timeline` all'ID `main_write_output`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `_write_output()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 185 che coinvolge il nodo canonico `_write_output()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 185 che coinvolge il nodo canonico `_write_output()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 185 che coinvolge il nodo canonico `_write_output()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 185 che coinvolge il nodo canonico `_write_output()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 185 che coinvolge il nodo canonico `_write_output()` è `L470`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 185 che coinvolge il nodo canonico `_write_output()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 185 che coinvolge il nodo canonico `_write_output()` è `ast`. Nel grafo del codice RDG, l'arco 185 registra come `source` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `_write_output()`. Nel grafo del codice RDG, l'arco 185 registra come `target` l'identificatore `main_write_output` per la relazione che coinvolge il nodo canonico `_write_output()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 185 che coinvolge il nodo canonico `_write_output()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=70 id=Request-Dataset-Generator::main_write_output -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_write_output()` è `_write_output()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_write_output()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_write_output()` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_write_output()` è `L221`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_write_output()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_write_output()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_write_output()` è `_write_output()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_write_output()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_write_output()` è `main_write_output`. Nel grafo unificato, il nodo canonico `_write_output()` possiede l'identificatore esatto `Request-Dataset-Generator::main_write_output`.
<!-- GRAPH_EDGE graph=unificata index=183 role=outgoing -->
Nel grafo unificato, il nodo canonico `_write_output()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_patients()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_write_output` all'ID `Request-Dataset-Generator::main_cmd_patients`. La relazione tecnica esplicita è la seguente: `_write_output()` chiama `cmd_patients()`. Nel grafo unificato, il tipo dichiarato dell'arco 183 che coinvolge il nodo canonico `_write_output()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 183 che coinvolge il nodo canonico `_write_output()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 183 che coinvolge il nodo canonico `_write_output()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 183 che coinvolge il nodo canonico `_write_output()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 183 che coinvolge il nodo canonico `_write_output()` è `L274`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 183 che coinvolge il nodo canonico `_write_output()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 183 che coinvolge il nodo canonico `_write_output()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 183 che coinvolge il nodo canonico `_write_output()` è `1.0`. Nel grafo unificato, l'arco 183 registra come `source` l'identificatore `Request-Dataset-Generator::main_write_output` per la relazione che coinvolge il nodo canonico `_write_output()`. Nel grafo unificato, l'arco 183 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_patients` per la relazione che coinvolge il nodo canonico `_write_output()`.
<!-- GRAPH_EDGE graph=unificata index=184 role=outgoing -->
Nel grafo unificato, il nodo canonico `_write_output()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_requests()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_write_output` all'ID `Request-Dataset-Generator::main_cmd_requests`. La relazione tecnica esplicita è la seguente: `_write_output()` chiama `cmd_requests()`. Nel grafo unificato, il tipo dichiarato dell'arco 184 che coinvolge il nodo canonico `_write_output()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 184 che coinvolge il nodo canonico `_write_output()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 184 che coinvolge il nodo canonico `_write_output()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 184 che coinvolge il nodo canonico `_write_output()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 184 che coinvolge il nodo canonico `_write_output()` è `L292`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 184 che coinvolge il nodo canonico `_write_output()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 184 che coinvolge il nodo canonico `_write_output()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 184 che coinvolge il nodo canonico `_write_output()` è `1.0`. Nel grafo unificato, l'arco 184 registra come `source` l'identificatore `Request-Dataset-Generator::main_write_output` per la relazione che coinvolge il nodo canonico `_write_output()`. Nel grafo unificato, l'arco 184 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_requests` per la relazione che coinvolge il nodo canonico `_write_output()`.
<!-- GRAPH_EDGE graph=unificata index=185 role=outgoing -->
Nel grafo unificato, il nodo canonico `_write_output()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_write_output` all'ID `Request-Dataset-Generator::main_cmd_timeline`. La relazione tecnica esplicita è la seguente: `_write_output()` chiama `cmd_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 185 che coinvolge il nodo canonico `_write_output()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 185 che coinvolge il nodo canonico `_write_output()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 185 che coinvolge il nodo canonico `_write_output()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 185 che coinvolge il nodo canonico `_write_output()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 185 che coinvolge il nodo canonico `_write_output()` è `L470`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 185 che coinvolge il nodo canonico `_write_output()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 185 che coinvolge il nodo canonico `_write_output()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 185 che coinvolge il nodo canonico `_write_output()` è `1.0`. Nel grafo unificato, l'arco 185 registra come `source` l'identificatore `Request-Dataset-Generator::main_write_output` per la relazione che coinvolge il nodo canonico `_write_output()`. Nel grafo unificato, l'arco 185 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `_write_output()`.
<!-- GRAPH_EDGE graph=unificata index=186 role=outgoing -->
Nel grafo unificato, il nodo canonico `_write_output()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_append_json()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_write_output` all'ID `Request-Dataset-Generator::main_append_json`. La relazione tecnica esplicita è la seguente: `_write_output()` chiama `_append_json()`. Nel grafo unificato, il tipo dichiarato dell'arco 186 che coinvolge il nodo canonico `_write_output()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 186 che coinvolge il nodo canonico `_write_output()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 186 che coinvolge il nodo canonico `_write_output()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 186 che coinvolge il nodo canonico `_write_output()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 186 che coinvolge il nodo canonico `_write_output()` è `L226`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 186 che coinvolge il nodo canonico `_write_output()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 186 che coinvolge il nodo canonico `_write_output()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 186 che coinvolge il nodo canonico `_write_output()` è `1.0`. Nel grafo unificato, l'arco 186 registra come `source` l'identificatore `Request-Dataset-Generator::main_write_output` per la relazione che coinvolge il nodo canonico `_write_output()`. Nel grafo unificato, l'arco 186 registra come `target` l'identificatore `Request-Dataset-Generator::main_append_json` per la relazione che coinvolge il nodo canonico `_write_output()`.
<!-- GRAPH_EDGE graph=unificata index=136 role=incoming -->
Nel grafo unificato, il nodo canonico `_write_output()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_write_output`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_write_output()`. Nel grafo unificato, il tipo dichiarato dell'arco 136 che coinvolge il nodo canonico `_write_output()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 136 che coinvolge il nodo canonico `_write_output()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 136 che coinvolge il nodo canonico `_write_output()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 136 che coinvolge il nodo canonico `_write_output()` è `L221`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 136 che coinvolge il nodo canonico `_write_output()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 136 che coinvolge il nodo canonico `_write_output()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 136 che coinvolge il nodo canonico `_write_output()` è `1.0`. Nel grafo unificato, l'arco 136 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `_write_output()`. Nel grafo unificato, l'arco 136 registra come `target` l'identificatore `Request-Dataset-Generator::main_write_output` per la relazione che coinvolge il nodo canonico `_write_output()`.
<!-- GRAPH_EDGE graph=unificata index=182 role=incoming -->
Nel grafo unificato, il nodo canonico `_write_output()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_resolve_output_path() [main_resolve_output_path]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_resolve_output_path` all'ID `Request-Dataset-Generator::main_write_output`. La relazione tecnica esplicita è la seguente: `_resolve_output_path() [main_resolve_output_path]` chiama `_write_output()`. Nel grafo unificato, il tipo dichiarato dell'arco 182 che coinvolge il nodo canonico `_write_output()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 182 che coinvolge il nodo canonico `_write_output()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 182 che coinvolge il nodo canonico `_write_output()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 182 che coinvolge il nodo canonico `_write_output()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 182 che coinvolge il nodo canonico `_write_output()` è `L223`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 182 che coinvolge il nodo canonico `_write_output()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 182 che coinvolge il nodo canonico `_write_output()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 182 che coinvolge il nodo canonico `_write_output()` è `1.0`. Nel grafo unificato, l'arco 182 registra come `source` l'identificatore `Request-Dataset-Generator::main_resolve_output_path` per la relazione che coinvolge il nodo canonico `_write_output()`. Nel grafo unificato, l'arco 182 registra come `target` l'identificatore `Request-Dataset-Generator::main_write_output` per la relazione che coinvolge il nodo canonico `_write_output()`.

### _write_resolved_config()

<!-- GRAPH_NODE graph=codice index=67 id=main_write_resolved_config -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_write_resolved_config()` è `_write_resolved_config()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_write_resolved_config()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_write_resolved_config()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_write_resolved_config()` è `L189`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_write_resolved_config()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_write_resolved_config()` possiede l'identificatore esatto `main_write_resolved_config`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_write_resolved_config()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_write_resolved_config()` è `_write_resolved_config()`.
Nel grafo del codice RDG, il nodo canonico `_write_resolved_config()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=137 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_write_resolved_config()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_write_resolved_config`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_write_resolved_config()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 137 che coinvolge il nodo canonico `_write_resolved_config()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 137 che coinvolge il nodo canonico `_write_resolved_config()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 137 che coinvolge il nodo canonico `_write_resolved_config()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 137 che coinvolge il nodo canonico `_write_resolved_config()` è `L189`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 137 che coinvolge il nodo canonico `_write_resolved_config()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 137 che coinvolge il nodo canonico `_write_resolved_config()` è `ast`. Nel grafo del codice RDG, l'arco 137 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `_write_resolved_config()`. Nel grafo del codice RDG, l'arco 137 registra come `target` l'identificatore `main_write_resolved_config` per la relazione che coinvolge il nodo canonico `_write_resolved_config()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 137 che coinvolge il nodo canonico `_write_resolved_config()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=174 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_write_resolved_config()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_pipeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_pipeline` all'ID `main_write_resolved_config`. La relazione tecnica esplicita è la seguente: `cmd_pipeline()` chiama `_write_resolved_config()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 174 che coinvolge il nodo canonico `_write_resolved_config()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 174 che coinvolge il nodo canonico `_write_resolved_config()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 174 che coinvolge il nodo canonico `_write_resolved_config()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 174 che coinvolge il nodo canonico `_write_resolved_config()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 174 che coinvolge il nodo canonico `_write_resolved_config()` è `L517`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 174 che coinvolge il nodo canonico `_write_resolved_config()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 174 che coinvolge il nodo canonico `_write_resolved_config()` è `ast`. Nel grafo del codice RDG, l'arco 174 registra come `source` l'identificatore `main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `_write_resolved_config()`. Nel grafo del codice RDG, l'arco 174 registra come `target` l'identificatore `main_write_resolved_config` per la relazione che coinvolge il nodo canonico `_write_resolved_config()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 174 che coinvolge il nodo canonico `_write_resolved_config()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=67 id=Request-Dataset-Generator::main_write_resolved_config -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_write_resolved_config()` è `_write_resolved_config()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_write_resolved_config()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_write_resolved_config()` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_write_resolved_config()` è `L189`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_write_resolved_config()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_write_resolved_config()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_write_resolved_config()` è `_write_resolved_config()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_write_resolved_config()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_write_resolved_config()` è `main_write_resolved_config`. Nel grafo unificato, il nodo canonico `_write_resolved_config()` possiede l'identificatore esatto `Request-Dataset-Generator::main_write_resolved_config`.
<!-- GRAPH_EDGE graph=unificata index=174 role=outgoing -->
Nel grafo unificato, il nodo canonico `_write_resolved_config()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_pipeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_write_resolved_config` all'ID `Request-Dataset-Generator::main_cmd_pipeline`. La relazione tecnica esplicita è la seguente: `_write_resolved_config()` chiama `cmd_pipeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 174 che coinvolge il nodo canonico `_write_resolved_config()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 174 che coinvolge il nodo canonico `_write_resolved_config()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 174 che coinvolge il nodo canonico `_write_resolved_config()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 174 che coinvolge il nodo canonico `_write_resolved_config()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 174 che coinvolge il nodo canonico `_write_resolved_config()` è `L517`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 174 che coinvolge il nodo canonico `_write_resolved_config()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 174 che coinvolge il nodo canonico `_write_resolved_config()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 174 che coinvolge il nodo canonico `_write_resolved_config()` è `1.0`. Nel grafo unificato, l'arco 174 registra come `source` l'identificatore `Request-Dataset-Generator::main_write_resolved_config` per la relazione che coinvolge il nodo canonico `_write_resolved_config()`. Nel grafo unificato, l'arco 174 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `_write_resolved_config()`.
<!-- GRAPH_EDGE graph=unificata index=137 role=incoming -->
Nel grafo unificato, il nodo canonico `_write_resolved_config()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_write_resolved_config`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_write_resolved_config()`. Nel grafo unificato, il tipo dichiarato dell'arco 137 che coinvolge il nodo canonico `_write_resolved_config()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 137 che coinvolge il nodo canonico `_write_resolved_config()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 137 che coinvolge il nodo canonico `_write_resolved_config()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 137 che coinvolge il nodo canonico `_write_resolved_config()` è `L189`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 137 che coinvolge il nodo canonico `_write_resolved_config()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 137 che coinvolge il nodo canonico `_write_resolved_config()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 137 che coinvolge il nodo canonico `_write_resolved_config()` è `1.0`. Nel grafo unificato, l'arco 137 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `_write_resolved_config()`. Nel grafo unificato, l'arco 137 registra come `target` l'identificatore `Request-Dataset-Generator::main_write_resolved_config` per la relazione che coinvolge il nodo canonico `_write_resolved_config()`.

### build_timeline()

<!-- GRAPH_NODE graph=codice index=328 id=timeline_generator_build_timeline -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `build_timeline()` è `build_timeline()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `build_timeline()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `build_timeline()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `build_timeline()` è `L177`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `build_timeline()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `build_timeline()` possiede l'identificatore esatto `timeline_generator_build_timeline`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `build_timeline()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `build_timeline()` è `build_timeline()`.
<!-- GRAPH_EDGE graph=codice index=651 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_timeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_build_timeline` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `build_timeline()` chiama `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 651 che coinvolge il nodo canonico `build_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 651 che coinvolge il nodo canonico `build_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 651 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 651 che coinvolge il nodo canonico `build_timeline()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 651 che coinvolge il nodo canonico `build_timeline()` è `L219`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 651 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 651 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 651 registra come `source` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, l'arco 651 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 651 che coinvolge il nodo canonico `build_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=655 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_timeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `generate_arrivals()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_build_timeline` all'ID `timeline_generator_generate_arrivals`. La relazione tecnica esplicita è la seguente: `build_timeline()` chiama `generate_arrivals()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 655 che coinvolge il nodo canonico `build_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 655 che coinvolge il nodo canonico `build_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 655 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 655 che coinvolge il nodo canonico `build_timeline()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 655 che coinvolge il nodo canonico `build_timeline()` è `L223`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 655 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 655 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 655 registra come `source` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, l'arco 655 registra come `target` l'identificatore `timeline_generator_generate_arrivals` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 655 che coinvolge il nodo canonico `build_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=143 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `main.py` importa `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 143 che coinvolge il nodo canonico `build_timeline()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 143 che coinvolge il nodo canonico `build_timeline()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 143 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 143 che coinvolge il nodo canonico `build_timeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 143 che coinvolge il nodo canonico `build_timeline()` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 143 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 143 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 143 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, l'arco 143 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 143 che coinvolge il nodo canonico `build_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=190 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_execute` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 190 che coinvolge il nodo canonico `build_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 190 che coinvolge il nodo canonico `build_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 190 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 190 che coinvolge il nodo canonico `build_timeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 190 che coinvolge il nodo canonico `build_timeline()` è `L355`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 190 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 190 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 190 registra come `source` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, l'arco 190 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 190 che coinvolge il nodo canonico `build_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=198 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_timeline` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 198 che coinvolge il nodo canonico `build_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 198 che coinvolge il nodo canonico `build_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 198 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 198 che coinvolge il nodo canonico `build_timeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 198 che coinvolge il nodo canonico `build_timeline()` è `L444`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 198 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 198 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 198 registra come `source` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, l'arco 198 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 198 che coinvolge il nodo canonico `build_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=586 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 586 che coinvolge il nodo canonico `build_timeline()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 586 che coinvolge il nodo canonico `build_timeline()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 586 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 586 che coinvolge il nodo canonico `build_timeline()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 586 che coinvolge il nodo canonico `build_timeline()` è `L13`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 586 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 586 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 586 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, l'arco 586 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 586 che coinvolge il nodo canonico `build_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=625 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `.test_framework_log_is_sufficient_for_normalized_dataset()` chiama `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 625 che coinvolge il nodo canonico `build_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 625 che coinvolge il nodo canonico `build_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 625 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 625 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 625 che coinvolge il nodo canonico `build_timeline()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 625 che coinvolge il nodo canonico `build_timeline()` è `L612`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 625 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 625 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 625 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, l'arco 625 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`.
<!-- GRAPH_EDGE graph=codice index=628 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `.test_error_injection_produces_measurable_failures_and_skipped_steps()` chiama `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 628 che coinvolge il nodo canonico `build_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 628 che coinvolge il nodo canonico `build_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 628 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 628 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 628 che coinvolge il nodo canonico `build_timeline()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 628 che coinvolge il nodo canonico `build_timeline()` è `L670`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 628 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 628 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 628 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, l'arco 628 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`.
<!-- GRAPH_EDGE graph=codice index=631 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_metric_engine_writes_csv_tables()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `.test_metric_engine_writes_csv_tables()` chiama `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 631 che coinvolge il nodo canonico `build_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 631 che coinvolge il nodo canonico `build_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 631 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 631 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 631 che coinvolge il nodo canonico `build_timeline()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 631 che coinvolge il nodo canonico `build_timeline()` è `L724`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 631 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 631 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 631 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, l'arco 631 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`.
<!-- GRAPH_EDGE graph=codice index=636 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `.test_timeline_assigns_unique_flow_occurrence_ids()` chiama `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 636 che coinvolge il nodo canonico `build_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 636 che coinvolge il nodo canonico `build_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 636 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 636 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 636 che coinvolge il nodo canonico `build_timeline()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 636 che coinvolge il nodo canonico `build_timeline()` è `L780`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 636 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 636 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 636 registra come `source` l'identificatore `tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, l'arco 636 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`.
<!-- GRAPH_EDGE graph=codice index=638 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_timeline_tags_burst_events()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `.test_timeline_tags_burst_events()` chiama `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 638 che coinvolge il nodo canonico `build_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 638 che coinvolge il nodo canonico `build_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 638 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 638 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 638 che coinvolge il nodo canonico `build_timeline()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 638 che coinvolge il nodo canonico `build_timeline()` è `L803`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 638 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 638 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 638 registra come `source` l'identificatore `tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, l'arco 638 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`.
<!-- GRAPH_EDGE graph=codice index=642 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `timeline_generator` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 642 che coinvolge il nodo canonico `build_timeline()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 642 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 642 che coinvolge il nodo canonico `build_timeline()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 642 che coinvolge il nodo canonico `build_timeline()` è `L177`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 642 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 642 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 642 registra come `source` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, l'arco 642 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 642 che coinvolge il nodo canonico `build_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=659 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_rationale_191` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` costituisce una motivazione per `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 659 che coinvolge il nodo canonico `build_timeline()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 659 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 659 che coinvolge il nodo canonico `build_timeline()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 659 che coinvolge il nodo canonico `build_timeline()` è `L191`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 659 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 659 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 659 registra come `source` l'identificatore `timeline_generator_rationale_191` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, l'arco 659 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 659 che coinvolge il nodo canonico `build_timeline()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=328 id=Request-Dataset-Generator::timeline_generator_build_timeline -->
Nel grafo unificato, l'etichetta originale del nodo canonico `build_timeline()` è `build_timeline()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `build_timeline()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `build_timeline()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `build_timeline()` è `L177`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `build_timeline()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `build_timeline()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `build_timeline()` è `build_timeline()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `build_timeline()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `build_timeline()` è `timeline_generator_build_timeline`. Nel grafo unificato, il nodo canonico `build_timeline()` possiede l'identificatore esatto `Request-Dataset-Generator::timeline_generator_build_timeline`.
<!-- GRAPH_EDGE graph=unificata index=659 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_timeline()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator_build_timeline` all'ID `Request-Dataset-Generator::timeline_generator_rationale_191`. La relazione tecnica esplicita è la seguente: `build_timeline()` costituisce una motivazione per `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim`. Nel grafo unificato, il tipo dichiarato dell'arco 659 che coinvolge il nodo canonico `build_timeline()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 659 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 659 che coinvolge il nodo canonico `build_timeline()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 659 che coinvolge il nodo canonico `build_timeline()` è `L191`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 659 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 659 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 659 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, l'arco 659 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo unificato, l'arco 659 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_rationale_191` per la relazione che coinvolge il nodo canonico `build_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=143 role=incoming -->
Nel grafo unificato, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `main.py` importa `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 143 che coinvolge il nodo canonico `build_timeline()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 143 che coinvolge il nodo canonico `build_timeline()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 143 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 143 che coinvolge il nodo canonico `build_timeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 143 che coinvolge il nodo canonico `build_timeline()` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 143 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 143 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 143 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, l'arco 143 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo unificato, l'arco 143 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=190 role=incoming -->
Nel grafo unificato, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_execute` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 190 che coinvolge il nodo canonico `build_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 190 che coinvolge il nodo canonico `build_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 190 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 190 che coinvolge il nodo canonico `build_timeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 190 che coinvolge il nodo canonico `build_timeline()` è `L355`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 190 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 190 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 190 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, l'arco 190 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo unificato, l'arco 190 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=198 role=incoming -->
Nel grafo unificato, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_timeline` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 198 che coinvolge il nodo canonico `build_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 198 che coinvolge il nodo canonico `build_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 198 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 198 che coinvolge il nodo canonico `build_timeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 198 che coinvolge il nodo canonico `build_timeline()` è `L444`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 198 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 198 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 198 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, l'arco 198 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo unificato, l'arco 198 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=586 role=incoming -->
Nel grafo unificato, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 586 che coinvolge il nodo canonico `build_timeline()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 586 che coinvolge il nodo canonico `build_timeline()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 586 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 586 che coinvolge il nodo canonico `build_timeline()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 586 che coinvolge il nodo canonico `build_timeline()` è `L13`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 586 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 586 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 586 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, l'arco 586 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo unificato, l'arco 586 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=625 role=incoming -->
Nel grafo unificato, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `.test_framework_log_is_sufficient_for_normalized_dataset()` chiama `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 625 che coinvolge il nodo canonico `build_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 625 che coinvolge il nodo canonico `build_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 625 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 625 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 625 che coinvolge il nodo canonico `build_timeline()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 625 che coinvolge il nodo canonico `build_timeline()` è `L612`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 625 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 625 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo unificato, l'arco 625 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo unificato, l'arco 625 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=628 role=incoming -->
Nel grafo unificato, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `.test_error_injection_produces_measurable_failures_and_skipped_steps()` chiama `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 628 che coinvolge il nodo canonico `build_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 628 che coinvolge il nodo canonico `build_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 628 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 628 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 628 che coinvolge il nodo canonico `build_timeline()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 628 che coinvolge il nodo canonico `build_timeline()` è `L670`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 628 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 628 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo unificato, l'arco 628 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo unificato, l'arco 628 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=631 role=incoming -->
Nel grafo unificato, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_metric_engine_writes_csv_tables()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `.test_metric_engine_writes_csv_tables()` chiama `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 631 che coinvolge il nodo canonico `build_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 631 che coinvolge il nodo canonico `build_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 631 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 631 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 631 che coinvolge il nodo canonico `build_timeline()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 631 che coinvolge il nodo canonico `build_timeline()` è `L724`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 631 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 631 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo unificato, l'arco 631 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo unificato, l'arco 631 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=636 role=incoming -->
Nel grafo unificato, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `.test_timeline_assigns_unique_flow_occurrence_ids()` chiama `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 636 che coinvolge il nodo canonico `build_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 636 che coinvolge il nodo canonico `build_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 636 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 636 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 636 che coinvolge il nodo canonico `build_timeline()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 636 che coinvolge il nodo canonico `build_timeline()` è `L780`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 636 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 636 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo unificato, l'arco 636 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo unificato, l'arco 636 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=638 role=incoming -->
Nel grafo unificato, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_timeline_tags_burst_events()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `.test_timeline_tags_burst_events()` chiama `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 638 che coinvolge il nodo canonico `build_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 638 che coinvolge il nodo canonico `build_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 638 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 638 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 638 che coinvolge il nodo canonico `build_timeline()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 638 che coinvolge il nodo canonico `build_timeline()` è `L803`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 638 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 638 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo unificato, l'arco 638 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo unificato, l'arco 638 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=642 role=incoming -->
Nel grafo unificato, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 642 che coinvolge il nodo canonico `build_timeline()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 642 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 642 che coinvolge il nodo canonico `build_timeline()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 642 che coinvolge il nodo canonico `build_timeline()` è `L177`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 642 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 642 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 642 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, l'arco 642 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo unificato, l'arco 642 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=651 role=incoming -->
Nel grafo unificato, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator_constant_rate` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `constant_rate()` chiama `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 651 che coinvolge il nodo canonico `build_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 651 che coinvolge il nodo canonico `build_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 651 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 651 che coinvolge il nodo canonico `build_timeline()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 651 che coinvolge il nodo canonico `build_timeline()` è `L219`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 651 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 651 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 651 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, l'arco 651 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo unificato, l'arco 651 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=655 role=incoming -->
Nel grafo unificato, il nodo canonico `build_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `generate_arrivals()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator_generate_arrivals` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `generate_arrivals()` chiama `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 655 che coinvolge il nodo canonico `build_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 655 che coinvolge il nodo canonico `build_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 655 che coinvolge il nodo canonico `build_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 655 che coinvolge il nodo canonico `build_timeline()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 655 che coinvolge il nodo canonico `build_timeline()` è `L223`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 655 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 655 che coinvolge il nodo canonico `build_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 655 che coinvolge il nodo canonico `build_timeline()` è `1.0`. Nel grafo unificato, l'arco 655 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator_generate_arrivals` per la relazione che coinvolge il nodo canonico `build_timeline()`. Nel grafo unificato, l'arco 655 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `build_timeline()`.

### build_workload()

<!-- GRAPH_NODE graph=codice index=154 id=request_dataset_generator_build_workload -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `build_workload()` è `build_workload()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `build_workload()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `build_workload()` è `request_dataset_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `build_workload()` è `L68`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `build_workload()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `build_workload()` possiede l'identificatore esatto `request_dataset_generator_build_workload`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `build_workload()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `build_workload()` è `build_workload()`.
<!-- GRAPH_EDGE graph=codice index=43 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_workload()` è la sorgente `source` della relazione `calls` verso il nodo canonico `genera_paziente()`; l'orientamento registrato nel JSON va dall'ID `request_dataset_generator_build_workload` all'ID `fhir_patient_generator_genera_paziente`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `genera_paziente()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 43 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 43 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 43 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 43 che coinvolge il nodo canonico `build_workload()` è `request_dataset_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 43 che coinvolge il nodo canonico `build_workload()` è `L158`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 43 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 43 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo del codice RDG, l'arco 43 registra come `source` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, l'arco 43 registra come `target` l'identificatore `fhir_patient_generator_genera_paziente` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 43 che coinvolge il nodo canonico `build_workload()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=321 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_workload()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_ensure_middleware_patient_fields()`; l'orientamento registrato nel JSON va dall'ID `request_dataset_generator_build_workload` all'ID `request_dataset_generator_ensure_middleware_patient_fields`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `_ensure_middleware_patient_fields()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 321 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 321 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 321 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 321 che coinvolge il nodo canonico `build_workload()` è `request_dataset_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 321 che coinvolge il nodo canonico `build_workload()` è `L160`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 321 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 321 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo del codice RDG, l'arco 321 registra come `source` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, l'arco 321 registra come `target` l'identificatore `request_dataset_generator_ensure_middleware_patient_fields` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 321 che coinvolge il nodo canonico `build_workload()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=322 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_workload()` è la sorgente `source` della relazione `calls` verso il nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `request_dataset_generator_build_workload` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 322 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 322 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 322 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 322 che coinvolge il nodo canonico `build_workload()` è `request_dataset_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 322 che coinvolge il nodo canonico `build_workload()` è `L153`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 322 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 322 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo del codice RDG, l'arco 322 registra come `source` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, l'arco 322 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 322 che coinvolge il nodo canonico `build_workload()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=141 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_workload()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `main.py` importa `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 141 che coinvolge il nodo canonico `build_workload()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 141 che coinvolge il nodo canonico `build_workload()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 141 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 141 che coinvolge il nodo canonico `build_workload()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 141 che coinvolge il nodo canonico `build_workload()` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 141 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 141 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo del codice RDG, l'arco 141 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, l'arco 141 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 141 che coinvolge il nodo canonico `build_workload()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=187 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_workload()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_requests()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_requests` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `cmd_requests()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 187 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 187 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 187 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 187 che coinvolge il nodo canonico `build_workload()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 187 che coinvolge il nodo canonico `build_workload()` è `L283`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 187 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 187 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo del codice RDG, l'arco 187 registra come `source` l'identificatore `main_cmd_requests` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, l'arco 187 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 187 che coinvolge il nodo canonico `build_workload()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=189 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_workload()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_execute` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 189 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 189 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 189 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 189 che coinvolge il nodo canonico `build_workload()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 189 che coinvolge il nodo canonico `build_workload()` è `L319`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 189 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 189 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo del codice RDG, l'arco 189 registra come `source` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, l'arco 189 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 189 che coinvolge il nodo canonico `build_workload()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=197 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_workload()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_timeline` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 197 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 197 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 197 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 197 che coinvolge il nodo canonico `build_workload()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 197 che coinvolge il nodo canonico `build_workload()` è `L419`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 197 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 197 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo del codice RDG, l'arco 197 registra come `source` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, l'arco 197 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 197 che coinvolge il nodo canonico `build_workload()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=315 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_workload()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `request_dataset_generator.py`; l'orientamento registrato nel JSON va dall'ID `request_dataset_generator` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `request_dataset_generator.py` contiene `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 315 che coinvolge il nodo canonico `build_workload()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 315 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 315 che coinvolge il nodo canonico `build_workload()` è `request_dataset_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 315 che coinvolge il nodo canonico `build_workload()` è `L68`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 315 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 315 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo del codice RDG, l'arco 315 registra come `source` l'identificatore `request_dataset_generator` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, l'arco 315 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 315 che coinvolge il nodo canonico `build_workload()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=323 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_workload()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer`; l'orientamento registrato nel JSON va dall'ID `request_dataset_generator_rationale_75` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` costituisce una motivazione per `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 323 che coinvolge il nodo canonico `build_workload()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 323 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 323 che coinvolge il nodo canonico `build_workload()` è `request_dataset_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 323 che coinvolge il nodo canonico `build_workload()` è `L75`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 323 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 323 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo del codice RDG, l'arco 323 registra come `source` l'identificatore `request_dataset_generator_rationale_75` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, l'arco 323 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 323 che coinvolge il nodo canonico `build_workload()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=324 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_workload()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 324 che coinvolge il nodo canonico `build_workload()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 324 che coinvolge il nodo canonico `build_workload()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 324 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 324 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 324 che coinvolge il nodo canonico `build_workload()` è `L12`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 324 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 324 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo del codice RDG, l'arco 324 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, l'arco 324 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 324 che coinvolge il nodo canonico `build_workload()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=325 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_workload()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_dataset_to_engine_mock_immediate()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `.test_dataset_to_engine_mock_immediate()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 325 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 325 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 325 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 325 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 325 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 325 che coinvolge il nodo canonico `build_workload()` è `L555`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 325 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 325 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo del codice RDG, l'arco 325 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, l'arco 325 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=codice index=326 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_workload()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `.test_error_injection_produces_measurable_failures_and_skipped_steps()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 326 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 326 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 326 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 326 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 326 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 326 che coinvolge il nodo canonico `build_workload()` è `L663`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 326 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 326 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo del codice RDG, l'arco 326 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, l'arco 326 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=codice index=327 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_workload()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `.test_framework_log_is_sufficient_for_normalized_dataset()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 327 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 327 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 327 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 327 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 327 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 327 che coinvolge il nodo canonico `build_workload()` è `L605`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 327 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 327 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo del codice RDG, l'arco 327 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, l'arco 327 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=codice index=328 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_workload()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_metric_engine_writes_csv_tables()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `.test_metric_engine_writes_csv_tables()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 328 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 328 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 328 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 328 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 328 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 328 che coinvolge il nodo canonico `build_workload()` è `L717`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 328 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 328 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo del codice RDG, l'arco 328 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, l'arco 328 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=codice index=329 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_workload()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_live_rve121_matches_soapui_body_shape()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `.test_live_rve121_matches_soapui_body_shape()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 329 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 329 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 329 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 329 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 329 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 329 che coinvolge il nodo canonico `build_workload()` è `L510`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 329 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 329 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo del codice RDG, l'arco 329 registra come `source` l'identificatore `tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, l'arco 329 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=codice index=330 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_workload()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `.test_live_rve54_uses_saml_header_and_patient_query_url()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 330 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 330 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 330 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 330 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 330 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 330 che coinvolge il nodo canonico `build_workload()` è `L459`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 330 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 330 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo del codice RDG, l'arco 330 registra come `source` l'identificatore `tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, l'arco 330 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=codice index=331 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_workload()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 331 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 331 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 331 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 331 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 331 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 331 che coinvolge il nodo canonico `build_workload()` è `L482`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 331 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 331 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo del codice RDG, l'arco 331 registra come `source` l'identificatore `tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, l'arco 331 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=codice index=332 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_workload()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `.test_mock_middleware_flow_builds_gateway_steps()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 332 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 332 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 332 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 332 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 332 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 332 che coinvolge il nodo canonico `build_workload()` è `L535`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 332 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 332 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo del codice RDG, l'arco 332 registra come `source` l'identificatore `tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, l'arco 332 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=codice index=333 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_workload()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `.test_timeline_assigns_unique_flow_occurrence_ids()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 333 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 333 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 333 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 333 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 333 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 333 che coinvolge il nodo canonico `build_workload()` è `L773`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 333 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 333 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo del codice RDG, l'arco 333 registra come `source` l'identificatore `tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, l'arco 333 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=codice index=334 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_workload()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_timeline_tags_burst_events()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `.test_timeline_tags_burst_events()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 334 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 334 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 334 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 334 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 334 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 334 che coinvolge il nodo canonico `build_workload()` è `L796`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 334 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 334 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo del codice RDG, l'arco 334 registra come `source` l'identificatore `tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo del codice RDG, l'arco 334 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`.

<!-- GRAPH_NODE graph=unificata index=154 id=Request-Dataset-Generator::request_dataset_generator_build_workload -->
Nel grafo unificato, l'etichetta originale del nodo canonico `build_workload()` è `build_workload()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `build_workload()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `build_workload()` è `request_dataset_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `build_workload()` è `L68`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `build_workload()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `build_workload()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `build_workload()` è `build_workload()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `build_workload()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `build_workload()` è `request_dataset_generator_build_workload`. Nel grafo unificato, il nodo canonico `build_workload()` possiede l'identificatore esatto `Request-Dataset-Generator::request_dataset_generator_build_workload`.
<!-- GRAPH_EDGE graph=unificata index=321 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_workload()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_ensure_middleware_patient_fields()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::request_dataset_generator_ensure_middleware_patient_fields`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `_ensure_middleware_patient_fields()`. Nel grafo unificato, il tipo dichiarato dell'arco 321 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 321 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 321 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 321 che coinvolge il nodo canonico `build_workload()` è `request_dataset_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 321 che coinvolge il nodo canonico `build_workload()` è `L160`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 321 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 321 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 321 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, l'arco 321 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo unificato, l'arco 321 registra come `target` l'identificatore `Request-Dataset-Generator::request_dataset_generator_ensure_middleware_patient_fields` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=unificata index=322 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_workload()` è la sorgente `source` della relazione `calls` verso il nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `FlowOrchestrator`. Nel grafo unificato, il tipo dichiarato dell'arco 322 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 322 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 322 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 322 che coinvolge il nodo canonico `build_workload()` è `request_dataset_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 322 che coinvolge il nodo canonico `build_workload()` è `L153`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 322 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 322 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 322 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, l'arco 322 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo unificato, l'arco 322 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=unificata index=323 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_workload()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::request_dataset_generator_rationale_75`. La relazione tecnica esplicita è la seguente: `build_workload()` costituisce una motivazione per `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer`. Nel grafo unificato, il tipo dichiarato dell'arco 323 che coinvolge il nodo canonico `build_workload()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 323 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 323 che coinvolge il nodo canonico `build_workload()` è `request_dataset_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 323 che coinvolge il nodo canonico `build_workload()` è `L75`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 323 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 323 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 323 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, l'arco 323 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo unificato, l'arco 323 registra come `target` l'identificatore `Request-Dataset-Generator::request_dataset_generator_rationale_75` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=unificata index=324 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_workload()` è la sorgente `source` della relazione `imports` verso il nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core`. La relazione tecnica esplicita è la seguente: `build_workload()` importa `test_core.py`. Nel grafo unificato, il tipo dichiarato dell'arco 324 che coinvolge il nodo canonico `build_workload()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 324 che coinvolge il nodo canonico `build_workload()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 324 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 324 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 324 che coinvolge il nodo canonico `build_workload()` è `L12`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 324 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 324 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 324 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, l'arco 324 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo unificato, l'arco 324 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=unificata index=325 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_workload()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.test_dataset_to_engine_mock_immediate()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `.test_dataset_to_engine_mock_immediate()`. Nel grafo unificato, il tipo dichiarato dell'arco 325 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 325 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 325 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 325 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 325 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 325 che coinvolge il nodo canonico `build_workload()` è `L555`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 325 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 325 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo unificato, l'arco 325 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo unificato, l'arco 325 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=unificata index=326 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_workload()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo unificato, il tipo dichiarato dell'arco 326 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 326 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 326 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 326 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 326 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 326 che coinvolge il nodo canonico `build_workload()` è `L663`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 326 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 326 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo unificato, l'arco 326 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo unificato, l'arco 326 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=unificata index=327 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_workload()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo unificato, il tipo dichiarato dell'arco 327 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 327 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 327 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 327 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 327 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 327 che coinvolge il nodo canonico `build_workload()` è `L605`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 327 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 327 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo unificato, l'arco 327 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo unificato, l'arco 327 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=unificata index=328 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_workload()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.test_metric_engine_writes_csv_tables()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `.test_metric_engine_writes_csv_tables()`. Nel grafo unificato, il tipo dichiarato dell'arco 328 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 328 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 328 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 328 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 328 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 328 che coinvolge il nodo canonico `build_workload()` è `L717`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 328 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 328 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo unificato, l'arco 328 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo unificato, l'arco 328 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=unificata index=329 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_workload()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.test_live_rve121_matches_soapui_body_shape()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `.test_live_rve121_matches_soapui_body_shape()`. Nel grafo unificato, il tipo dichiarato dell'arco 329 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 329 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 329 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 329 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 329 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 329 che coinvolge il nodo canonico `build_workload()` è `L510`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 329 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 329 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo unificato, l'arco 329 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo unificato, l'arco 329 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=unificata index=330 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_workload()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `.test_live_rve54_uses_saml_header_and_patient_query_url()`. Nel grafo unificato, il tipo dichiarato dell'arco 330 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 330 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 330 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 330 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 330 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 330 che coinvolge il nodo canonico `build_workload()` è `L459`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 330 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 330 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo unificato, l'arco 330 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo unificato, l'arco 330 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=unificata index=331 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_workload()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`. Nel grafo unificato, il tipo dichiarato dell'arco 331 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 331 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 331 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 331 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 331 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 331 che coinvolge il nodo canonico `build_workload()` è `L482`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 331 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 331 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo unificato, l'arco 331 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo unificato, l'arco 331 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=unificata index=332 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_workload()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `.test_mock_middleware_flow_builds_gateway_steps()`. Nel grafo unificato, il tipo dichiarato dell'arco 332 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 332 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 332 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 332 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 332 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 332 che coinvolge il nodo canonico `build_workload()` è `L535`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 332 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 332 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo unificato, l'arco 332 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo unificato, l'arco 332 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=unificata index=333 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_workload()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `.test_timeline_assigns_unique_flow_occurrence_ids()`. Nel grafo unificato, il tipo dichiarato dell'arco 333 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 333 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 333 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 333 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 333 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 333 che coinvolge il nodo canonico `build_workload()` è `L773`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 333 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 333 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo unificato, l'arco 333 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo unificato, l'arco 333 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=unificata index=334 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_workload()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.test_timeline_tags_burst_events()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_tags_burst_events`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `.test_timeline_tags_burst_events()`. Nel grafo unificato, il tipo dichiarato dell'arco 334 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 334 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 334 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 334 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 334 che coinvolge il nodo canonico `build_workload()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 334 che coinvolge il nodo canonico `build_workload()` è `L796`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 334 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 334 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo unificato, l'arco 334 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo unificato, l'arco 334 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=unificata index=43 role=incoming -->
Nel grafo unificato, il nodo canonico `build_workload()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `genera_paziente()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::fhir_patient_generator_genera_paziente` all'ID `Request-Dataset-Generator::request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `genera_paziente()` chiama `build_workload()`. Nel grafo unificato, il tipo dichiarato dell'arco 43 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 43 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 43 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 43 che coinvolge il nodo canonico `build_workload()` è `request_dataset_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 43 che coinvolge il nodo canonico `build_workload()` è `L158`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 43 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 43 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 43 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, l'arco 43 registra come `source` l'identificatore `Request-Dataset-Generator::fhir_patient_generator_genera_paziente` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo unificato, l'arco 43 registra come `target` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=unificata index=141 role=incoming -->
Nel grafo unificato, il nodo canonico `build_workload()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `main.py` importa `build_workload()`. Nel grafo unificato, il tipo dichiarato dell'arco 141 che coinvolge il nodo canonico `build_workload()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 141 che coinvolge il nodo canonico `build_workload()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 141 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 141 che coinvolge il nodo canonico `build_workload()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 141 che coinvolge il nodo canonico `build_workload()` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 141 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 141 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 141 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, l'arco 141 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo unificato, l'arco 141 registra come `target` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=unificata index=187 role=incoming -->
Nel grafo unificato, il nodo canonico `build_workload()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_requests()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_requests` all'ID `Request-Dataset-Generator::request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `cmd_requests()` chiama `build_workload()`. Nel grafo unificato, il tipo dichiarato dell'arco 187 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 187 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 187 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 187 che coinvolge il nodo canonico `build_workload()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 187 che coinvolge il nodo canonico `build_workload()` è `L283`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 187 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 187 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 187 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, l'arco 187 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_requests` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo unificato, l'arco 187 registra come `target` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=unificata index=189 role=incoming -->
Nel grafo unificato, il nodo canonico `build_workload()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_execute` all'ID `Request-Dataset-Generator::request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `build_workload()`. Nel grafo unificato, il tipo dichiarato dell'arco 189 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 189 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 189 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 189 che coinvolge il nodo canonico `build_workload()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 189 che coinvolge il nodo canonico `build_workload()` è `L319`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 189 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 189 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 189 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, l'arco 189 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo unificato, l'arco 189 registra come `target` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=unificata index=197 role=incoming -->
Nel grafo unificato, il nodo canonico `build_workload()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_timeline` all'ID `Request-Dataset-Generator::request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `build_workload()`. Nel grafo unificato, il tipo dichiarato dell'arco 197 che coinvolge il nodo canonico `build_workload()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 197 che coinvolge il nodo canonico `build_workload()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 197 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 197 che coinvolge il nodo canonico `build_workload()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 197 che coinvolge il nodo canonico `build_workload()` è `L419`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 197 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 197 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 197 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, l'arco 197 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo unificato, l'arco 197 registra come `target` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`.
<!-- GRAPH_EDGE graph=unificata index=315 role=incoming -->
Nel grafo unificato, il nodo canonico `build_workload()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `request_dataset_generator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator` all'ID `Request-Dataset-Generator::request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `request_dataset_generator.py` contiene `build_workload()`. Nel grafo unificato, il tipo dichiarato dell'arco 315 che coinvolge il nodo canonico `build_workload()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 315 che coinvolge il nodo canonico `build_workload()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 315 che coinvolge il nodo canonico `build_workload()` è `request_dataset_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 315 che coinvolge il nodo canonico `build_workload()` è `L68`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 315 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 315 che coinvolge il nodo canonico `build_workload()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 315 che coinvolge il nodo canonico `build_workload()` è `1.0`. Nel grafo unificato, l'arco 315 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator` per la relazione che coinvolge il nodo canonico `build_workload()`. Nel grafo unificato, l'arco 315 registra come `target` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `build_workload()`.

### Calcola metriche e genera report/grafici da config o da main.py.

<!-- GRAPH_NODE graph=codice index=129 id=metric_engine_rationale_1432 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `Calcola metriche e genera report/grafici da config o da main.py.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `metric_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `L1432`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` possiede l'identificatore esatto `metric_engine_rationale_1432`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `calcola metriche e genera report/grafici da config o da main.py.`.
<!-- GRAPH_EDGE graph=codice index=277 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `run_metric_engine()`; l'orientamento registrato nel JSON va dall'ID `metric_engine_rationale_1432` all'ID `metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `Calcola metriche e genera report/grafici da config o da main.py.` costituisce una motivazione per `run_metric_engine()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 277 che coinvolge il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 277 che coinvolge il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 277 che coinvolge il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `metric_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 277 che coinvolge il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `L1432`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 277 che coinvolge il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 277 che coinvolge il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `ast`. Nel grafo del codice RDG, l'arco 277 registra come `source` l'identificatore `metric_engine_rationale_1432` per la relazione che coinvolge il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.`. Nel grafo del codice RDG, l'arco 277 registra come `target` l'identificatore `metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 277 che coinvolge il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=129 id=Request-Dataset-Generator::metric_engine_rationale_1432 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `Calcola metriche e genera report/grafici da config o da main.py.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `metric_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `L1432`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `calcola metriche e genera report/grafici da config o da main.py.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `metric_engine_rationale_1432`. Nel grafo unificato, il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` possiede l'identificatore esatto `Request-Dataset-Generator::metric_engine_rationale_1432`.
Nel grafo unificato, il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=277 role=incoming -->
Nel grafo unificato, il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `run_metric_engine()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::metric_engine_run_metric_engine` all'ID `Request-Dataset-Generator::metric_engine_rationale_1432`. La relazione tecnica esplicita è la seguente: `run_metric_engine()` costituisce una motivazione per `Calcola metriche e genera report/grafici da config o da main.py.`. Nel grafo unificato, il tipo dichiarato dell'arco 277 che coinvolge il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 277 che coinvolge il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 277 che coinvolge il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `metric_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 277 che coinvolge il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `L1432`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 277 che coinvolge il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 277 che coinvolge il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 277 che coinvolge il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.` è `1.0`. Nel grafo unificato, l'arco 277 registra come `source` l'identificatore `Request-Dataset-Generator::metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.`. Nel grafo unificato, l'arco 277 registra come `target` l'identificatore `Request-Dataset-Generator::metric_engine_rationale_1432` per la relazione che coinvolge il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.`.

### Calcola statistiche descrittive della timeline generata.

<!-- GRAPH_NODE graph=codice index=337 id=timeline_generator_rationale_294 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Calcola statistiche descrittive della timeline generata.` è `Calcola statistiche descrittive della timeline generata.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Calcola statistiche descrittive della timeline generata.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Calcola statistiche descrittive della timeline generata.` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Calcola statistiche descrittive della timeline generata.` è `L294`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Calcola statistiche descrittive della timeline generata.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Calcola statistiche descrittive della timeline generata.` possiede l'identificatore esatto `timeline_generator_rationale_294`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Calcola statistiche descrittive della timeline generata.` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Calcola statistiche descrittive della timeline generata.` è `calcola statistiche descrittive della timeline generata.`.
<!-- GRAPH_EDGE graph=codice index=660 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Calcola statistiche descrittive della timeline generata.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `timeline_stats()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_rationale_294` all'ID `timeline_generator_timeline_stats`. La relazione tecnica esplicita è la seguente: `Calcola statistiche descrittive della timeline generata.` costituisce una motivazione per `timeline_stats()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 660 che coinvolge il nodo canonico `Calcola statistiche descrittive della timeline generata.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 660 che coinvolge il nodo canonico `Calcola statistiche descrittive della timeline generata.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 660 che coinvolge il nodo canonico `Calcola statistiche descrittive della timeline generata.` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 660 che coinvolge il nodo canonico `Calcola statistiche descrittive della timeline generata.` è `L294`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 660 che coinvolge il nodo canonico `Calcola statistiche descrittive della timeline generata.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 660 che coinvolge il nodo canonico `Calcola statistiche descrittive della timeline generata.` è `ast`. Nel grafo del codice RDG, l'arco 660 registra come `source` l'identificatore `timeline_generator_rationale_294` per la relazione che coinvolge il nodo canonico `Calcola statistiche descrittive della timeline generata.`. Nel grafo del codice RDG, l'arco 660 registra come `target` l'identificatore `timeline_generator_timeline_stats` per la relazione che coinvolge il nodo canonico `Calcola statistiche descrittive della timeline generata.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 660 che coinvolge il nodo canonico `Calcola statistiche descrittive della timeline generata.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Calcola statistiche descrittive della timeline generata.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=337 id=Request-Dataset-Generator::timeline_generator_rationale_294 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Calcola statistiche descrittive della timeline generata.` è `Calcola statistiche descrittive della timeline generata.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Calcola statistiche descrittive della timeline generata.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Calcola statistiche descrittive della timeline generata.` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Calcola statistiche descrittive della timeline generata.` è `L294`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Calcola statistiche descrittive della timeline generata.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Calcola statistiche descrittive della timeline generata.` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Calcola statistiche descrittive della timeline generata.` è `calcola statistiche descrittive della timeline generata.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Calcola statistiche descrittive della timeline generata.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Calcola statistiche descrittive della timeline generata.` è `timeline_generator_rationale_294`. Nel grafo unificato, il nodo canonico `Calcola statistiche descrittive della timeline generata.` possiede l'identificatore esatto `Request-Dataset-Generator::timeline_generator_rationale_294`.
Nel grafo unificato, il nodo canonico `Calcola statistiche descrittive della timeline generata.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=660 role=incoming -->
Nel grafo unificato, il nodo canonico `Calcola statistiche descrittive della timeline generata.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `timeline_stats()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator_timeline_stats` all'ID `Request-Dataset-Generator::timeline_generator_rationale_294`. La relazione tecnica esplicita è la seguente: `timeline_stats()` costituisce una motivazione per `Calcola statistiche descrittive della timeline generata.`. Nel grafo unificato, il tipo dichiarato dell'arco 660 che coinvolge il nodo canonico `Calcola statistiche descrittive della timeline generata.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 660 che coinvolge il nodo canonico `Calcola statistiche descrittive della timeline generata.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 660 che coinvolge il nodo canonico `Calcola statistiche descrittive della timeline generata.` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 660 che coinvolge il nodo canonico `Calcola statistiche descrittive della timeline generata.` è `L294`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 660 che coinvolge il nodo canonico `Calcola statistiche descrittive della timeline generata.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 660 che coinvolge il nodo canonico `Calcola statistiche descrittive della timeline generata.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 660 che coinvolge il nodo canonico `Calcola statistiche descrittive della timeline generata.` è `1.0`. Nel grafo unificato, l'arco 660 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator_timeline_stats` per la relazione che coinvolge il nodo canonico `Calcola statistiche descrittive della timeline generata.`. Nel grafo unificato, l'arco 660 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_rationale_294` per la relazione che coinvolge il nodo canonico `Calcola statistiche descrittive della timeline generata.`.

### cmd_collect()

<!-- GRAPH_NODE graph=codice index=76 id=main_cmd_collect -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `cmd_collect()` è `cmd_collect()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `cmd_collect()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `cmd_collect()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `cmd_collect()` è `L473`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `cmd_collect()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `cmd_collect()` possiede l'identificatore esatto `main_cmd_collect`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `cmd_collect()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `cmd_collect()` è `cmd_collect()`.
<!-- GRAPH_EDGE graph=codice index=110 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_collect()` è la sorgente `source` della relazione `calls` verso il nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_collect` all'ID `log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `cmd_collect()` chiama `collect_logs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 110 che coinvolge il nodo canonico `cmd_collect()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 110 che coinvolge il nodo canonico `cmd_collect()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 110 che coinvolge il nodo canonico `cmd_collect()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 110 che coinvolge il nodo canonico `cmd_collect()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 110 che coinvolge il nodo canonico `cmd_collect()` è `L485`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 110 che coinvolge il nodo canonico `cmd_collect()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 110 che coinvolge il nodo canonico `cmd_collect()` è `ast`. Nel grafo del codice RDG, l'arco 110 registra come `source` l'identificatore `main_cmd_collect` per la relazione che coinvolge il nodo canonico `cmd_collect()`. Nel grafo del codice RDG, l'arco 110 registra come `target` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `cmd_collect()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 110 che coinvolge il nodo canonico `cmd_collect()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=152 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_collect()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_cfg_section()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_collect` all'ID `main_cfg_section`. La relazione tecnica esplicita è la seguente: `cmd_collect()` chiama `_cfg_section()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 152 che coinvolge il nodo canonico `cmd_collect()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 152 che coinvolge il nodo canonico `cmd_collect()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 152 che coinvolge il nodo canonico `cmd_collect()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 152 che coinvolge il nodo canonico `cmd_collect()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 152 che coinvolge il nodo canonico `cmd_collect()` è `L475`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 152 che coinvolge il nodo canonico `cmd_collect()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 152 che coinvolge il nodo canonico `cmd_collect()` è `ast`. Nel grafo del codice RDG, l'arco 152 registra come `source` l'identificatore `main_cmd_collect` per la relazione che coinvolge il nodo canonico `cmd_collect()`. Nel grafo del codice RDG, l'arco 152 registra come `target` l'identificatore `main_cfg_section` per la relazione che coinvolge il nodo canonico `cmd_collect()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 152 che coinvolge il nodo canonico `cmd_collect()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=163 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_collect()` è la sorgente `source` della relazione `references` verso il nodo canonico `Namespace`; l'orientamento registrato nel JSON va dall'ID `main_cmd_collect` all'ID `namespace`. La relazione tecnica esplicita è la seguente: `cmd_collect()` fa riferimento a `Namespace`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 163 che coinvolge il nodo canonico `cmd_collect()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 163 che coinvolge il nodo canonico `cmd_collect()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 163 che coinvolge il nodo canonico `cmd_collect()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 163 che coinvolge il nodo canonico `cmd_collect()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 163 che coinvolge il nodo canonico `cmd_collect()` è `L473`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 163 che coinvolge il nodo canonico `cmd_collect()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 163 che coinvolge il nodo canonico `cmd_collect()` è `ast`. Nel grafo del codice RDG, l'arco 163 registra come `source` l'identificatore `main_cmd_collect` per la relazione che coinvolge il nodo canonico `cmd_collect()`. Nel grafo del codice RDG, l'arco 163 registra come `target` l'identificatore `namespace` per la relazione che coinvolge il nodo canonico `cmd_collect()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 163 che coinvolge il nodo canonico `cmd_collect()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=178 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_collect()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_resolve_output_path() [main_resolve_output_path]`; l'orientamento registrato nel JSON va dall'ID `main_cmd_collect` all'ID `main_resolve_output_path`. La relazione tecnica esplicita è la seguente: `cmd_collect()` chiama `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 178 che coinvolge il nodo canonico `cmd_collect()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 178 che coinvolge il nodo canonico `cmd_collect()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 178 che coinvolge il nodo canonico `cmd_collect()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 178 che coinvolge il nodo canonico `cmd_collect()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 178 che coinvolge il nodo canonico `cmd_collect()` è `L486`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 178 che coinvolge il nodo canonico `cmd_collect()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 178 che coinvolge il nodo canonico `cmd_collect()` è `ast`. Nel grafo del codice RDG, l'arco 178 registra come `source` l'identificatore `main_cmd_collect` per la relazione che coinvolge il nodo canonico `cmd_collect()`. Nel grafo del codice RDG, l'arco 178 registra come `target` l'identificatore `main_resolve_output_path` per la relazione che coinvolge il nodo canonico `cmd_collect()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 178 che coinvolge il nodo canonico `cmd_collect()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=123 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `cmd_collect()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_cmd_collect`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_collect()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 123 che coinvolge il nodo canonico `cmd_collect()` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 123 che coinvolge il nodo canonico `cmd_collect()` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 123 che coinvolge il nodo canonico `cmd_collect()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 123 che coinvolge il nodo canonico `cmd_collect()` è `L585`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 123 che coinvolge il nodo canonico `cmd_collect()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 123 che coinvolge il nodo canonico `cmd_collect()` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 123 che coinvolge il nodo canonico `cmd_collect()` è `collection`. Nel grafo del codice RDG, l'arco 123 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `cmd_collect()`. Nel grafo del codice RDG, l'arco 123 registra come `target` l'identificatore `main_cmd_collect` per la relazione che coinvolge il nodo canonico `cmd_collect()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 123 che coinvolge il nodo canonico `cmd_collect()` è `0.5`.

<!-- GRAPH_NODE graph=unificata index=76 id=Request-Dataset-Generator::main_cmd_collect -->
Nel grafo unificato, l'etichetta originale del nodo canonico `cmd_collect()` è `cmd_collect()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `cmd_collect()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `cmd_collect()` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `cmd_collect()` è `L473`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `cmd_collect()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `cmd_collect()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `cmd_collect()` è `cmd_collect()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `cmd_collect()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `cmd_collect()` è `main_cmd_collect`. Nel grafo unificato, il nodo canonico `cmd_collect()` possiede l'identificatore esatto `Request-Dataset-Generator::main_cmd_collect`.
Nel grafo unificato, il nodo canonico `cmd_collect()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=110 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_collect()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_collect_logs` all'ID `Request-Dataset-Generator::main_cmd_collect`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `cmd_collect()`. Nel grafo unificato, il tipo dichiarato dell'arco 110 che coinvolge il nodo canonico `cmd_collect()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 110 che coinvolge il nodo canonico `cmd_collect()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 110 che coinvolge il nodo canonico `cmd_collect()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 110 che coinvolge il nodo canonico `cmd_collect()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 110 che coinvolge il nodo canonico `cmd_collect()` è `L485`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 110 che coinvolge il nodo canonico `cmd_collect()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 110 che coinvolge il nodo canonico `cmd_collect()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 110 che coinvolge il nodo canonico `cmd_collect()` è `1.0`. Nel grafo unificato, l'arco 110 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `cmd_collect()`. Nel grafo unificato, l'arco 110 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_collect` per la relazione che coinvolge il nodo canonico `cmd_collect()`.
<!-- GRAPH_EDGE graph=unificata index=123 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_collect()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_cmd_collect`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_collect()`. Nel grafo unificato, il tipo dichiarato dell'arco 123 che coinvolge il nodo canonico `cmd_collect()` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 123 che coinvolge il nodo canonico `cmd_collect()` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 123 che coinvolge il nodo canonico `cmd_collect()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 123 che coinvolge il nodo canonico `cmd_collect()` è `L585`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 123 che coinvolge il nodo canonico `cmd_collect()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 123 che coinvolge il nodo canonico `cmd_collect()` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 123 che coinvolge il nodo canonico `cmd_collect()` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 123 che coinvolge il nodo canonico `cmd_collect()` è `0.5`. Nel grafo unificato, l'arco 123 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `cmd_collect()`. Nel grafo unificato, l'arco 123 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_collect` per la relazione che coinvolge il nodo canonico `cmd_collect()`.
<!-- GRAPH_EDGE graph=unificata index=152 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_collect()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_cfg_section()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cfg_section` all'ID `Request-Dataset-Generator::main_cmd_collect`. La relazione tecnica esplicita è la seguente: `_cfg_section()` chiama `cmd_collect()`. Nel grafo unificato, il tipo dichiarato dell'arco 152 che coinvolge il nodo canonico `cmd_collect()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 152 che coinvolge il nodo canonico `cmd_collect()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 152 che coinvolge il nodo canonico `cmd_collect()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 152 che coinvolge il nodo canonico `cmd_collect()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 152 che coinvolge il nodo canonico `cmd_collect()` è `L475`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 152 che coinvolge il nodo canonico `cmd_collect()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 152 che coinvolge il nodo canonico `cmd_collect()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 152 che coinvolge il nodo canonico `cmd_collect()` è `1.0`. Nel grafo unificato, l'arco 152 registra come `source` l'identificatore `Request-Dataset-Generator::main_cfg_section` per la relazione che coinvolge il nodo canonico `cmd_collect()`. Nel grafo unificato, l'arco 152 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_collect` per la relazione che coinvolge il nodo canonico `cmd_collect()`.
<!-- GRAPH_EDGE graph=unificata index=163 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_collect()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Namespace`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::namespace` all'ID `Request-Dataset-Generator::main_cmd_collect`. La relazione tecnica esplicita è la seguente: `Namespace` fa riferimento a `cmd_collect()`. Nel grafo unificato, il tipo dichiarato dell'arco 163 che coinvolge il nodo canonico `cmd_collect()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 163 che coinvolge il nodo canonico `cmd_collect()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 163 che coinvolge il nodo canonico `cmd_collect()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 163 che coinvolge il nodo canonico `cmd_collect()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 163 che coinvolge il nodo canonico `cmd_collect()` è `L473`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 163 che coinvolge il nodo canonico `cmd_collect()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 163 che coinvolge il nodo canonico `cmd_collect()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 163 che coinvolge il nodo canonico `cmd_collect()` è `1.0`. Nel grafo unificato, l'arco 163 registra come `source` l'identificatore `Request-Dataset-Generator::namespace` per la relazione che coinvolge il nodo canonico `cmd_collect()`. Nel grafo unificato, l'arco 163 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_collect` per la relazione che coinvolge il nodo canonico `cmd_collect()`.
<!-- GRAPH_EDGE graph=unificata index=178 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_collect()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_resolve_output_path() [main_resolve_output_path]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_resolve_output_path` all'ID `Request-Dataset-Generator::main_cmd_collect`. La relazione tecnica esplicita è la seguente: `_resolve_output_path() [main_resolve_output_path]` chiama `cmd_collect()`. Nel grafo unificato, il tipo dichiarato dell'arco 178 che coinvolge il nodo canonico `cmd_collect()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 178 che coinvolge il nodo canonico `cmd_collect()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 178 che coinvolge il nodo canonico `cmd_collect()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 178 che coinvolge il nodo canonico `cmd_collect()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 178 che coinvolge il nodo canonico `cmd_collect()` è `L486`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 178 che coinvolge il nodo canonico `cmd_collect()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 178 che coinvolge il nodo canonico `cmd_collect()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 178 che coinvolge il nodo canonico `cmd_collect()` è `1.0`. Nel grafo unificato, l'arco 178 registra come `source` l'identificatore `Request-Dataset-Generator::main_resolve_output_path` per la relazione che coinvolge il nodo canonico `cmd_collect()`. Nel grafo unificato, l'arco 178 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_collect` per la relazione che coinvolge il nodo canonico `cmd_collect()`.

### cmd_execute()

<!-- GRAPH_NODE graph=codice index=74 id=main_cmd_execute -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `cmd_execute()` è `cmd_execute()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `cmd_execute()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `cmd_execute()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `cmd_execute()` è `L295`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `cmd_execute()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `cmd_execute()` possiede l'identificatore esatto `main_cmd_execute`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `cmd_execute()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `cmd_execute()` è `cmd_execute()`.
<!-- GRAPH_EDGE graph=codice index=153 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_execute()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_cfg_section()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_execute` all'ID `main_cfg_section`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `_cfg_section()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 153 che coinvolge il nodo canonico `cmd_execute()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 153 che coinvolge il nodo canonico `cmd_execute()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 153 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 153 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 153 che coinvolge il nodo canonico `cmd_execute()` è `L309`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 153 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 153 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo del codice RDG, l'arco 153 registra come `source` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, l'arco 153 registra come `target` l'identificatore `main_cfg_section` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 153 che coinvolge il nodo canonico `cmd_execute()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=164 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_execute()` è la sorgente `source` della relazione `references` verso il nodo canonico `Namespace`; l'orientamento registrato nel JSON va dall'ID `main_cmd_execute` all'ID `namespace`. La relazione tecnica esplicita è la seguente: `cmd_execute()` fa riferimento a `Namespace`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 164 che coinvolge il nodo canonico `cmd_execute()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 164 che coinvolge il nodo canonico `cmd_execute()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 164 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 164 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 164 che coinvolge il nodo canonico `cmd_execute()` è `L295`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 164 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 164 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo del codice RDG, l'arco 164 registra come `source` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, l'arco 164 registra come `target` l'identificatore `namespace` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 164 che coinvolge il nodo canonico `cmd_execute()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=175 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_execute()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_annotate_dataset_flows()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_execute` all'ID `main_annotate_dataset_flows`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `_annotate_dataset_flows()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 175 che coinvolge il nodo canonico `cmd_execute()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 175 che coinvolge il nodo canonico `cmd_execute()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 175 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 175 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 175 che coinvolge il nodo canonico `cmd_execute()` è `L326`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 175 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 175 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo del codice RDG, l'arco 175 registra come `source` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, l'arco 175 registra come `target` l'identificatore `main_annotate_dataset_flows` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 175 che coinvolge il nodo canonico `cmd_execute()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=179 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_execute()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_resolve_output_path() [main_resolve_output_path]`; l'orientamento registrato nel JSON va dall'ID `main_cmd_execute` all'ID `main_resolve_output_path`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 179 che coinvolge il nodo canonico `cmd_execute()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 179 che coinvolge il nodo canonico `cmd_execute()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 179 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 179 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 179 che coinvolge il nodo canonico `cmd_execute()` è `L330`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 179 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 179 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo del codice RDG, l'arco 179 registra come `source` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, l'arco 179 registra come `target` l'identificatore `main_resolve_output_path` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 179 che coinvolge il nodo canonico `cmd_execute()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=189 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_execute()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_execute` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 189 che coinvolge il nodo canonico `cmd_execute()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 189 che coinvolge il nodo canonico `cmd_execute()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 189 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 189 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 189 che coinvolge il nodo canonico `cmd_execute()` è `L319`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 189 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 189 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo del codice RDG, l'arco 189 registra come `source` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, l'arco 189 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 189 che coinvolge il nodo canonico `cmd_execute()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=190 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_execute()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_execute` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 190 che coinvolge il nodo canonico `cmd_execute()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 190 che coinvolge il nodo canonico `cmd_execute()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 190 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 190 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 190 che coinvolge il nodo canonico `cmd_execute()` è `L355`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 190 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 190 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo del codice RDG, l'arco 190 registra come `source` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, l'arco 190 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 190 che coinvolge il nodo canonico `cmd_execute()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=191 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_execute()` è la sorgente `source` della relazione `calls` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_execute` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 191 che coinvolge il nodo canonico `cmd_execute()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 191 che coinvolge il nodo canonico `cmd_execute()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 191 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 191 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 191 che coinvolge il nodo canonico `cmd_execute()` è `L353`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 191 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 191 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo del codice RDG, l'arco 191 registra come `source` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, l'arco 191 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 191 che coinvolge il nodo canonico `cmd_execute()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=192 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_execute()` è la sorgente `source` della relazione `calls` verso il nodo canonico `daily_clinical_profile()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_execute` all'ID `timeline_generator_daily_clinical_profile`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `daily_clinical_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 192 che coinvolge il nodo canonico `cmd_execute()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 192 che coinvolge il nodo canonico `cmd_execute()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 192 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 192 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 192 che coinvolge il nodo canonico `cmd_execute()` è `L347`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 192 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 192 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo del codice RDG, l'arco 192 registra come `source` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, l'arco 192 registra come `target` l'identificatore `timeline_generator_daily_clinical_profile` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 192 che coinvolge il nodo canonico `cmd_execute()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=193 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_execute()` è la sorgente `source` della relazione `calls` verso il nodo canonico `step_profile()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_execute` all'ID `timeline_generator_step_profile`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `step_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 193 che coinvolge il nodo canonico `cmd_execute()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 193 che coinvolge il nodo canonico `cmd_execute()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 193 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 193 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 193 che coinvolge il nodo canonico `cmd_execute()` è `L345`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 193 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 193 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo del codice RDG, l'arco 193 registra come `source` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, l'arco 193 registra come `target` l'identificatore `timeline_generator_step_profile` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 193 che coinvolge il nodo canonico `cmd_execute()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=194 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_execute()` è la sorgente `source` della relazione `calls` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_execute` all'ID `traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 194 che coinvolge il nodo canonico `cmd_execute()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 194 che coinvolge il nodo canonico `cmd_execute()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 194 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 194 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 194 che coinvolge il nodo canonico `cmd_execute()` è `L385`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 194 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 194 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo del codice RDG, l'arco 194 registra come `source` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, l'arco 194 registra come `target` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 194 che coinvolge il nodo canonico `cmd_execute()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=124 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `cmd_execute()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_cmd_execute`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_execute()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 124 che coinvolge il nodo canonico `cmd_execute()` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 124 che coinvolge il nodo canonico `cmd_execute()` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 124 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 124 che coinvolge il nodo canonico `cmd_execute()` è `L584`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 124 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 124 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 124 che coinvolge il nodo canonico `cmd_execute()` è `collection`. Nel grafo del codice RDG, l'arco 124 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, l'arco 124 registra come `target` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 124 che coinvolge il nodo canonico `cmd_execute()` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=195 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `cmd_execute()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_pipeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_pipeline` all'ID `main_cmd_execute`. La relazione tecnica esplicita è la seguente: `cmd_pipeline()` chiama `cmd_execute()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 195 che coinvolge il nodo canonico `cmd_execute()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 195 che coinvolge il nodo canonico `cmd_execute()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 195 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 195 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 195 che coinvolge il nodo canonico `cmd_execute()` è `L522`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 195 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 195 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo del codice RDG, l'arco 195 registra come `source` l'identificatore `main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, l'arco 195 registra come `target` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 195 che coinvolge il nodo canonico `cmd_execute()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=196 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `cmd_execute()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.`; l'orientamento registrato nel JSON va dall'ID `main_rationale_296` all'ID `main_cmd_execute`. La relazione tecnica esplicita è la seguente: `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` costituisce una motivazione per `cmd_execute()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 196 che coinvolge il nodo canonico `cmd_execute()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 196 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 196 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 196 che coinvolge il nodo canonico `cmd_execute()` è `L296`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 196 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 196 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo del codice RDG, l'arco 196 registra come `source` l'identificatore `main_rationale_296` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, l'arco 196 registra come `target` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 196 che coinvolge il nodo canonico `cmd_execute()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=74 id=Request-Dataset-Generator::main_cmd_execute -->
Nel grafo unificato, l'etichetta originale del nodo canonico `cmd_execute()` è `cmd_execute()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `cmd_execute()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `cmd_execute()` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `cmd_execute()` è `L295`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `cmd_execute()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `cmd_execute()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `cmd_execute()` è `cmd_execute()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `cmd_execute()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `cmd_execute()` è `main_cmd_execute`. Nel grafo unificato, il nodo canonico `cmd_execute()` possiede l'identificatore esatto `Request-Dataset-Generator::main_cmd_execute`.
<!-- GRAPH_EDGE graph=unificata index=189 role=outgoing -->
Nel grafo unificato, il nodo canonico `cmd_execute()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_execute` all'ID `Request-Dataset-Generator::request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `build_workload()`. Nel grafo unificato, il tipo dichiarato dell'arco 189 che coinvolge il nodo canonico `cmd_execute()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 189 che coinvolge il nodo canonico `cmd_execute()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 189 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 189 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 189 che coinvolge il nodo canonico `cmd_execute()` è `L319`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 189 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 189 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 189 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, l'arco 189 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo unificato, l'arco 189 registra come `target` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `cmd_execute()`.
<!-- GRAPH_EDGE graph=unificata index=190 role=outgoing -->
Nel grafo unificato, il nodo canonico `cmd_execute()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_execute` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 190 che coinvolge il nodo canonico `cmd_execute()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 190 che coinvolge il nodo canonico `cmd_execute()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 190 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 190 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 190 che coinvolge il nodo canonico `cmd_execute()` è `L355`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 190 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 190 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 190 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, l'arco 190 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo unificato, l'arco 190 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `cmd_execute()`.
<!-- GRAPH_EDGE graph=unificata index=191 role=outgoing -->
Nel grafo unificato, il nodo canonico `cmd_execute()` è la sorgente `source` della relazione `calls` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_execute` all'ID `Request-Dataset-Generator::timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `constant_rate()`. Nel grafo unificato, il tipo dichiarato dell'arco 191 che coinvolge il nodo canonico `cmd_execute()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 191 che coinvolge il nodo canonico `cmd_execute()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 191 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 191 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 191 che coinvolge il nodo canonico `cmd_execute()` è `L353`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 191 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 191 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 191 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, l'arco 191 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo unificato, l'arco 191 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `cmd_execute()`.
<!-- GRAPH_EDGE graph=unificata index=192 role=outgoing -->
Nel grafo unificato, il nodo canonico `cmd_execute()` è la sorgente `source` della relazione `calls` verso il nodo canonico `daily_clinical_profile()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_execute` all'ID `Request-Dataset-Generator::timeline_generator_daily_clinical_profile`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `daily_clinical_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 192 che coinvolge il nodo canonico `cmd_execute()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 192 che coinvolge il nodo canonico `cmd_execute()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 192 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 192 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 192 che coinvolge il nodo canonico `cmd_execute()` è `L347`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 192 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 192 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 192 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, l'arco 192 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo unificato, l'arco 192 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_daily_clinical_profile` per la relazione che coinvolge il nodo canonico `cmd_execute()`.
<!-- GRAPH_EDGE graph=unificata index=193 role=outgoing -->
Nel grafo unificato, il nodo canonico `cmd_execute()` è la sorgente `source` della relazione `calls` verso il nodo canonico `step_profile()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_execute` all'ID `Request-Dataset-Generator::timeline_generator_step_profile`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `step_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 193 che coinvolge il nodo canonico `cmd_execute()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 193 che coinvolge il nodo canonico `cmd_execute()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 193 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 193 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 193 che coinvolge il nodo canonico `cmd_execute()` è `L345`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 193 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 193 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 193 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, l'arco 193 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo unificato, l'arco 193 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_step_profile` per la relazione che coinvolge il nodo canonico `cmd_execute()`.
<!-- GRAPH_EDGE graph=unificata index=194 role=outgoing -->
Nel grafo unificato, il nodo canonico `cmd_execute()` è la sorgente `source` della relazione `calls` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_execute` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 194 che coinvolge il nodo canonico `cmd_execute()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 194 che coinvolge il nodo canonico `cmd_execute()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 194 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 194 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 194 che coinvolge il nodo canonico `cmd_execute()` è `L385`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 194 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 194 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 194 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, l'arco 194 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo unificato, l'arco 194 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `cmd_execute()`.
<!-- GRAPH_EDGE graph=unificata index=195 role=outgoing -->
Nel grafo unificato, il nodo canonico `cmd_execute()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_pipeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_execute` all'ID `Request-Dataset-Generator::main_cmd_pipeline`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `cmd_pipeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 195 che coinvolge il nodo canonico `cmd_execute()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 195 che coinvolge il nodo canonico `cmd_execute()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 195 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 195 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 195 che coinvolge il nodo canonico `cmd_execute()` è `L522`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 195 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 195 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 195 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, l'arco 195 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo unificato, l'arco 195 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_execute()`.
<!-- GRAPH_EDGE graph=unificata index=196 role=outgoing -->
Nel grafo unificato, il nodo canonico `cmd_execute()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_execute` all'ID `Request-Dataset-Generator::main_rationale_296`. La relazione tecnica esplicita è la seguente: `cmd_execute()` costituisce una motivazione per `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.`. Nel grafo unificato, il tipo dichiarato dell'arco 196 che coinvolge il nodo canonico `cmd_execute()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 196 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 196 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 196 che coinvolge il nodo canonico `cmd_execute()` è `L296`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 196 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 196 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 196 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, l'arco 196 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo unificato, l'arco 196 registra come `target` l'identificatore `Request-Dataset-Generator::main_rationale_296` per la relazione che coinvolge il nodo canonico `cmd_execute()`.
<!-- GRAPH_EDGE graph=unificata index=124 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_execute()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_cmd_execute`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_execute()`. Nel grafo unificato, il tipo dichiarato dell'arco 124 che coinvolge il nodo canonico `cmd_execute()` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 124 che coinvolge il nodo canonico `cmd_execute()` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 124 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 124 che coinvolge il nodo canonico `cmd_execute()` è `L584`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 124 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 124 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 124 che coinvolge il nodo canonico `cmd_execute()` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 124 che coinvolge il nodo canonico `cmd_execute()` è `0.5`. Nel grafo unificato, l'arco 124 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo unificato, l'arco 124 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`.
<!-- GRAPH_EDGE graph=unificata index=153 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_execute()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_cfg_section()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cfg_section` all'ID `Request-Dataset-Generator::main_cmd_execute`. La relazione tecnica esplicita è la seguente: `_cfg_section()` chiama `cmd_execute()`. Nel grafo unificato, il tipo dichiarato dell'arco 153 che coinvolge il nodo canonico `cmd_execute()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 153 che coinvolge il nodo canonico `cmd_execute()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 153 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 153 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 153 che coinvolge il nodo canonico `cmd_execute()` è `L309`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 153 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 153 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 153 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, l'arco 153 registra come `source` l'identificatore `Request-Dataset-Generator::main_cfg_section` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo unificato, l'arco 153 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`.
<!-- GRAPH_EDGE graph=unificata index=164 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_execute()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Namespace`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::namespace` all'ID `Request-Dataset-Generator::main_cmd_execute`. La relazione tecnica esplicita è la seguente: `Namespace` fa riferimento a `cmd_execute()`. Nel grafo unificato, il tipo dichiarato dell'arco 164 che coinvolge il nodo canonico `cmd_execute()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 164 che coinvolge il nodo canonico `cmd_execute()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 164 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 164 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 164 che coinvolge il nodo canonico `cmd_execute()` è `L295`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 164 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 164 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 164 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, l'arco 164 registra come `source` l'identificatore `Request-Dataset-Generator::namespace` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo unificato, l'arco 164 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`.
<!-- GRAPH_EDGE graph=unificata index=175 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_execute()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_annotate_dataset_flows()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_annotate_dataset_flows` all'ID `Request-Dataset-Generator::main_cmd_execute`. La relazione tecnica esplicita è la seguente: `_annotate_dataset_flows()` chiama `cmd_execute()`. Nel grafo unificato, il tipo dichiarato dell'arco 175 che coinvolge il nodo canonico `cmd_execute()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 175 che coinvolge il nodo canonico `cmd_execute()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 175 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 175 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 175 che coinvolge il nodo canonico `cmd_execute()` è `L326`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 175 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 175 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 175 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, l'arco 175 registra come `source` l'identificatore `Request-Dataset-Generator::main_annotate_dataset_flows` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo unificato, l'arco 175 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`.
<!-- GRAPH_EDGE graph=unificata index=179 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_execute()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_resolve_output_path() [main_resolve_output_path]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_resolve_output_path` all'ID `Request-Dataset-Generator::main_cmd_execute`. La relazione tecnica esplicita è la seguente: `_resolve_output_path() [main_resolve_output_path]` chiama `cmd_execute()`. Nel grafo unificato, il tipo dichiarato dell'arco 179 che coinvolge il nodo canonico `cmd_execute()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 179 che coinvolge il nodo canonico `cmd_execute()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 179 che coinvolge il nodo canonico `cmd_execute()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 179 che coinvolge il nodo canonico `cmd_execute()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 179 che coinvolge il nodo canonico `cmd_execute()` è `L330`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 179 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 179 che coinvolge il nodo canonico `cmd_execute()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 179 che coinvolge il nodo canonico `cmd_execute()` è `1.0`. Nel grafo unificato, l'arco 179 registra come `source` l'identificatore `Request-Dataset-Generator::main_resolve_output_path` per la relazione che coinvolge il nodo canonico `cmd_execute()`. Nel grafo unificato, l'arco 179 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_execute()`.

### cmd_metrics()

<!-- GRAPH_NODE graph=codice index=77 id=main_cmd_metrics -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `cmd_metrics()` è `cmd_metrics()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `cmd_metrics()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `cmd_metrics()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `cmd_metrics()` è `L495`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `cmd_metrics()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `cmd_metrics()` possiede l'identificatore esatto `main_cmd_metrics`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `cmd_metrics()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `cmd_metrics()` è `cmd_metrics()`.
<!-- GRAPH_EDGE graph=codice index=154 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_metrics()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_cfg_section()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_metrics` all'ID `main_cfg_section`. La relazione tecnica esplicita è la seguente: `cmd_metrics()` chiama `_cfg_section()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 154 che coinvolge il nodo canonico `cmd_metrics()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 154 che coinvolge il nodo canonico `cmd_metrics()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 154 che coinvolge il nodo canonico `cmd_metrics()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 154 che coinvolge il nodo canonico `cmd_metrics()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 154 che coinvolge il nodo canonico `cmd_metrics()` è `L497`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 154 che coinvolge il nodo canonico `cmd_metrics()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 154 che coinvolge il nodo canonico `cmd_metrics()` è `ast`. Nel grafo del codice RDG, l'arco 154 registra come `source` l'identificatore `main_cmd_metrics` per la relazione che coinvolge il nodo canonico `cmd_metrics()`. Nel grafo del codice RDG, l'arco 154 registra come `target` l'identificatore `main_cfg_section` per la relazione che coinvolge il nodo canonico `cmd_metrics()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 154 che coinvolge il nodo canonico `cmd_metrics()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=165 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_metrics()` è la sorgente `source` della relazione `references` verso il nodo canonico `Namespace`; l'orientamento registrato nel JSON va dall'ID `main_cmd_metrics` all'ID `namespace`. La relazione tecnica esplicita è la seguente: `cmd_metrics()` fa riferimento a `Namespace`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 165 che coinvolge il nodo canonico `cmd_metrics()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 165 che coinvolge il nodo canonico `cmd_metrics()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 165 che coinvolge il nodo canonico `cmd_metrics()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 165 che coinvolge il nodo canonico `cmd_metrics()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 165 che coinvolge il nodo canonico `cmd_metrics()` è `L495`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 165 che coinvolge il nodo canonico `cmd_metrics()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 165 che coinvolge il nodo canonico `cmd_metrics()` è `ast`. Nel grafo del codice RDG, l'arco 165 registra come `source` l'identificatore `main_cmd_metrics` per la relazione che coinvolge il nodo canonico `cmd_metrics()`. Nel grafo del codice RDG, l'arco 165 registra come `target` l'identificatore `namespace` per la relazione che coinvolge il nodo canonico `cmd_metrics()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 165 che coinvolge il nodo canonico `cmd_metrics()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=180 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_metrics()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_resolve_output_path() [main_resolve_output_path]`; l'orientamento registrato nel JSON va dall'ID `main_cmd_metrics` all'ID `main_resolve_output_path`. La relazione tecnica esplicita è la seguente: `cmd_metrics()` chiama `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 180 che coinvolge il nodo canonico `cmd_metrics()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 180 che coinvolge il nodo canonico `cmd_metrics()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 180 che coinvolge il nodo canonico `cmd_metrics()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 180 che coinvolge il nodo canonico `cmd_metrics()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 180 che coinvolge il nodo canonico `cmd_metrics()` è `L505`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 180 che coinvolge il nodo canonico `cmd_metrics()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 180 che coinvolge il nodo canonico `cmd_metrics()` è `ast`. Nel grafo del codice RDG, l'arco 180 registra come `source` l'identificatore `main_cmd_metrics` per la relazione che coinvolge il nodo canonico `cmd_metrics()`. Nel grafo del codice RDG, l'arco 180 registra come `target` l'identificatore `main_resolve_output_path` per la relazione che coinvolge il nodo canonico `cmd_metrics()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 180 che coinvolge il nodo canonico `cmd_metrics()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=204 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_metrics()` è la sorgente `source` della relazione `calls` verso il nodo canonico `run_metric_engine()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_metrics` all'ID `metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `cmd_metrics()` chiama `run_metric_engine()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 204 che coinvolge il nodo canonico `cmd_metrics()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 204 che coinvolge il nodo canonico `cmd_metrics()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 204 che coinvolge il nodo canonico `cmd_metrics()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 204 che coinvolge il nodo canonico `cmd_metrics()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 204 che coinvolge il nodo canonico `cmd_metrics()` è `L504`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 204 che coinvolge il nodo canonico `cmd_metrics()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 204 che coinvolge il nodo canonico `cmd_metrics()` è `ast`. Nel grafo del codice RDG, l'arco 204 registra come `source` l'identificatore `main_cmd_metrics` per la relazione che coinvolge il nodo canonico `cmd_metrics()`. Nel grafo del codice RDG, l'arco 204 registra come `target` l'identificatore `metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `cmd_metrics()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 204 che coinvolge il nodo canonico `cmd_metrics()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=125 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `cmd_metrics()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_cmd_metrics`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_metrics()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 125 che coinvolge il nodo canonico `cmd_metrics()` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 125 che coinvolge il nodo canonico `cmd_metrics()` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 125 che coinvolge il nodo canonico `cmd_metrics()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 125 che coinvolge il nodo canonico `cmd_metrics()` è `L586`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 125 che coinvolge il nodo canonico `cmd_metrics()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 125 che coinvolge il nodo canonico `cmd_metrics()` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 125 che coinvolge il nodo canonico `cmd_metrics()` è `collection`. Nel grafo del codice RDG, l'arco 125 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `cmd_metrics()`. Nel grafo del codice RDG, l'arco 125 registra come `target` l'identificatore `main_cmd_metrics` per la relazione che coinvolge il nodo canonico `cmd_metrics()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 125 che coinvolge il nodo canonico `cmd_metrics()` è `0.5`.

<!-- GRAPH_NODE graph=unificata index=77 id=Request-Dataset-Generator::main_cmd_metrics -->
Nel grafo unificato, l'etichetta originale del nodo canonico `cmd_metrics()` è `cmd_metrics()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `cmd_metrics()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `cmd_metrics()` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `cmd_metrics()` è `L495`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `cmd_metrics()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `cmd_metrics()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `cmd_metrics()` è `cmd_metrics()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `cmd_metrics()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `cmd_metrics()` è `main_cmd_metrics`. Nel grafo unificato, il nodo canonico `cmd_metrics()` possiede l'identificatore esatto `Request-Dataset-Generator::main_cmd_metrics`.
<!-- GRAPH_EDGE graph=unificata index=204 role=outgoing -->
Nel grafo unificato, il nodo canonico `cmd_metrics()` è la sorgente `source` della relazione `calls` verso il nodo canonico `run_metric_engine()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_metrics` all'ID `Request-Dataset-Generator::metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `cmd_metrics()` chiama `run_metric_engine()`. Nel grafo unificato, il tipo dichiarato dell'arco 204 che coinvolge il nodo canonico `cmd_metrics()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 204 che coinvolge il nodo canonico `cmd_metrics()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 204 che coinvolge il nodo canonico `cmd_metrics()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 204 che coinvolge il nodo canonico `cmd_metrics()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 204 che coinvolge il nodo canonico `cmd_metrics()` è `L504`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 204 che coinvolge il nodo canonico `cmd_metrics()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 204 che coinvolge il nodo canonico `cmd_metrics()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 204 che coinvolge il nodo canonico `cmd_metrics()` è `1.0`. Nel grafo unificato, l'arco 204 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_metrics` per la relazione che coinvolge il nodo canonico `cmd_metrics()`. Nel grafo unificato, l'arco 204 registra come `target` l'identificatore `Request-Dataset-Generator::metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `cmd_metrics()`.
<!-- GRAPH_EDGE graph=unificata index=125 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_metrics()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_cmd_metrics`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_metrics()`. Nel grafo unificato, il tipo dichiarato dell'arco 125 che coinvolge il nodo canonico `cmd_metrics()` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 125 che coinvolge il nodo canonico `cmd_metrics()` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 125 che coinvolge il nodo canonico `cmd_metrics()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 125 che coinvolge il nodo canonico `cmd_metrics()` è `L586`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 125 che coinvolge il nodo canonico `cmd_metrics()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 125 che coinvolge il nodo canonico `cmd_metrics()` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 125 che coinvolge il nodo canonico `cmd_metrics()` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 125 che coinvolge il nodo canonico `cmd_metrics()` è `0.5`. Nel grafo unificato, l'arco 125 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `cmd_metrics()`. Nel grafo unificato, l'arco 125 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_metrics` per la relazione che coinvolge il nodo canonico `cmd_metrics()`.
<!-- GRAPH_EDGE graph=unificata index=154 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_metrics()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_cfg_section()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cfg_section` all'ID `Request-Dataset-Generator::main_cmd_metrics`. La relazione tecnica esplicita è la seguente: `_cfg_section()` chiama `cmd_metrics()`. Nel grafo unificato, il tipo dichiarato dell'arco 154 che coinvolge il nodo canonico `cmd_metrics()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 154 che coinvolge il nodo canonico `cmd_metrics()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 154 che coinvolge il nodo canonico `cmd_metrics()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 154 che coinvolge il nodo canonico `cmd_metrics()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 154 che coinvolge il nodo canonico `cmd_metrics()` è `L497`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 154 che coinvolge il nodo canonico `cmd_metrics()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 154 che coinvolge il nodo canonico `cmd_metrics()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 154 che coinvolge il nodo canonico `cmd_metrics()` è `1.0`. Nel grafo unificato, l'arco 154 registra come `source` l'identificatore `Request-Dataset-Generator::main_cfg_section` per la relazione che coinvolge il nodo canonico `cmd_metrics()`. Nel grafo unificato, l'arco 154 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_metrics` per la relazione che coinvolge il nodo canonico `cmd_metrics()`.
<!-- GRAPH_EDGE graph=unificata index=165 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_metrics()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Namespace`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::namespace` all'ID `Request-Dataset-Generator::main_cmd_metrics`. La relazione tecnica esplicita è la seguente: `Namespace` fa riferimento a `cmd_metrics()`. Nel grafo unificato, il tipo dichiarato dell'arco 165 che coinvolge il nodo canonico `cmd_metrics()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 165 che coinvolge il nodo canonico `cmd_metrics()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 165 che coinvolge il nodo canonico `cmd_metrics()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 165 che coinvolge il nodo canonico `cmd_metrics()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 165 che coinvolge il nodo canonico `cmd_metrics()` è `L495`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 165 che coinvolge il nodo canonico `cmd_metrics()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 165 che coinvolge il nodo canonico `cmd_metrics()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 165 che coinvolge il nodo canonico `cmd_metrics()` è `1.0`. Nel grafo unificato, l'arco 165 registra come `source` l'identificatore `Request-Dataset-Generator::namespace` per la relazione che coinvolge il nodo canonico `cmd_metrics()`. Nel grafo unificato, l'arco 165 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_metrics` per la relazione che coinvolge il nodo canonico `cmd_metrics()`.
<!-- GRAPH_EDGE graph=unificata index=180 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_metrics()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_resolve_output_path() [main_resolve_output_path]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_resolve_output_path` all'ID `Request-Dataset-Generator::main_cmd_metrics`. La relazione tecnica esplicita è la seguente: `_resolve_output_path() [main_resolve_output_path]` chiama `cmd_metrics()`. Nel grafo unificato, il tipo dichiarato dell'arco 180 che coinvolge il nodo canonico `cmd_metrics()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 180 che coinvolge il nodo canonico `cmd_metrics()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 180 che coinvolge il nodo canonico `cmd_metrics()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 180 che coinvolge il nodo canonico `cmd_metrics()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 180 che coinvolge il nodo canonico `cmd_metrics()` è `L505`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 180 che coinvolge il nodo canonico `cmd_metrics()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 180 che coinvolge il nodo canonico `cmd_metrics()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 180 che coinvolge il nodo canonico `cmd_metrics()` è `1.0`. Nel grafo unificato, l'arco 180 registra come `source` l'identificatore `Request-Dataset-Generator::main_resolve_output_path` per la relazione che coinvolge il nodo canonico `cmd_metrics()`. Nel grafo unificato, l'arco 180 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_metrics` per la relazione che coinvolge il nodo canonico `cmd_metrics()`.

### cmd_patients()

<!-- GRAPH_NODE graph=codice index=72 id=main_cmd_patients -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `cmd_patients()` è `cmd_patients()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `cmd_patients()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `cmd_patients()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `cmd_patients()` è `L258`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `cmd_patients()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `cmd_patients()` possiede l'identificatore esatto `main_cmd_patients`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `cmd_patients()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `cmd_patients()` è `cmd_patients()`.
<!-- GRAPH_EDGE graph=codice index=41 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_patients()` è la sorgente `source` della relazione `calls` verso il nodo canonico `genera_paziente()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_patients` all'ID `fhir_patient_generator_genera_paziente`. La relazione tecnica esplicita è la seguente: `cmd_patients()` chiama `genera_paziente()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 41 che coinvolge il nodo canonico `cmd_patients()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 41 che coinvolge il nodo canonico `cmd_patients()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 41 che coinvolge il nodo canonico `cmd_patients()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 41 che coinvolge il nodo canonico `cmd_patients()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 41 che coinvolge il nodo canonico `cmd_patients()` è `L262`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 41 che coinvolge il nodo canonico `cmd_patients()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 41 che coinvolge il nodo canonico `cmd_patients()` è `ast`. Nel grafo del codice RDG, l'arco 41 registra come `source` l'identificatore `main_cmd_patients` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo del codice RDG, l'arco 41 registra come `target` l'identificatore `fhir_patient_generator_genera_paziente` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 41 che coinvolge il nodo canonico `cmd_patients()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=46 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_patients()` è la sorgente `source` della relazione `calls` verso il nodo canonico `genera_bundle_rve55()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_patients` all'ID `fhir_patient_generator_genera_bundle_rve55`. La relazione tecnica esplicita è la seguente: `cmd_patients()` chiama `genera_bundle_rve55()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 46 che coinvolge il nodo canonico `cmd_patients()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 46 che coinvolge il nodo canonico `cmd_patients()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 46 che coinvolge il nodo canonico `cmd_patients()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 46 che coinvolge il nodo canonico `cmd_patients()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 46 che coinvolge il nodo canonico `cmd_patients()` è `L269`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 46 che coinvolge il nodo canonico `cmd_patients()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 46 che coinvolge il nodo canonico `cmd_patients()` è `ast`. Nel grafo del codice RDG, l'arco 46 registra come `source` l'identificatore `main_cmd_patients` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo del codice RDG, l'arco 46 registra come `target` l'identificatore `fhir_patient_generator_genera_bundle_rve55` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 46 che coinvolge il nodo canonico `cmd_patients()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=50 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_patients()` è la sorgente `source` della relazione `calls` verso il nodo canonico `to_json()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_patients` all'ID `fhir_patient_generator_to_json`. La relazione tecnica esplicita è la seguente: `cmd_patients()` chiama `to_json()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 50 che coinvolge il nodo canonico `cmd_patients()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 50 che coinvolge il nodo canonico `cmd_patients()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 50 che coinvolge il nodo canonico `cmd_patients()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 50 che coinvolge il nodo canonico `cmd_patients()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 50 che coinvolge il nodo canonico `cmd_patients()` è `L265`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 50 che coinvolge il nodo canonico `cmd_patients()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 50 che coinvolge il nodo canonico `cmd_patients()` è `ast`. Nel grafo del codice RDG, l'arco 50 registra come `source` l'identificatore `main_cmd_patients` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo del codice RDG, l'arco 50 registra come `target` l'identificatore `fhir_patient_generator_to_json` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 50 che coinvolge il nodo canonico `cmd_patients()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=55 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_patients()` è la sorgente `source` della relazione `calls` verso il nodo canonico `to_xml()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_patients` all'ID `fhir_patient_generator_to_xml`. La relazione tecnica esplicita è la seguente: `cmd_patients()` chiama `to_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 55 che coinvolge il nodo canonico `cmd_patients()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 55 che coinvolge il nodo canonico `cmd_patients()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 55 che coinvolge il nodo canonico `cmd_patients()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 55 che coinvolge il nodo canonico `cmd_patients()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 55 che coinvolge il nodo canonico `cmd_patients()` è `L267`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 55 che coinvolge il nodo canonico `cmd_patients()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 55 che coinvolge il nodo canonico `cmd_patients()` è `ast`. Nel grafo del codice RDG, l'arco 55 registra come `source` l'identificatore `main_cmd_patients` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo del codice RDG, l'arco 55 registra come `target` l'identificatore `fhir_patient_generator_to_xml` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 55 che coinvolge il nodo canonico `cmd_patients()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=166 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_patients()` è la sorgente `source` della relazione `references` verso il nodo canonico `Namespace`; l'orientamento registrato nel JSON va dall'ID `main_cmd_patients` all'ID `namespace`. La relazione tecnica esplicita è la seguente: `cmd_patients()` fa riferimento a `Namespace`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 166 che coinvolge il nodo canonico `cmd_patients()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 166 che coinvolge il nodo canonico `cmd_patients()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 166 che coinvolge il nodo canonico `cmd_patients()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 166 che coinvolge il nodo canonico `cmd_patients()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 166 che coinvolge il nodo canonico `cmd_patients()` è `L258`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 166 che coinvolge il nodo canonico `cmd_patients()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 166 che coinvolge il nodo canonico `cmd_patients()` è `ast`. Nel grafo del codice RDG, l'arco 166 registra come `source` l'identificatore `main_cmd_patients` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo del codice RDG, l'arco 166 registra come `target` l'identificatore `namespace` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 166 che coinvolge il nodo canonico `cmd_patients()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=183 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_patients()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_write_output()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_patients` all'ID `main_write_output`. La relazione tecnica esplicita è la seguente: `cmd_patients()` chiama `_write_output()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 183 che coinvolge il nodo canonico `cmd_patients()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 183 che coinvolge il nodo canonico `cmd_patients()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 183 che coinvolge il nodo canonico `cmd_patients()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 183 che coinvolge il nodo canonico `cmd_patients()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 183 che coinvolge il nodo canonico `cmd_patients()` è `L274`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 183 che coinvolge il nodo canonico `cmd_patients()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 183 che coinvolge il nodo canonico `cmd_patients()` è `ast`. Nel grafo del codice RDG, l'arco 183 registra come `source` l'identificatore `main_cmd_patients` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo del codice RDG, l'arco 183 registra come `target` l'identificatore `main_write_output` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 183 che coinvolge il nodo canonico `cmd_patients()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=126 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `cmd_patients()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_cmd_patients`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_patients()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 126 che coinvolge il nodo canonico `cmd_patients()` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 126 che coinvolge il nodo canonico `cmd_patients()` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 126 che coinvolge il nodo canonico `cmd_patients()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 126 che coinvolge il nodo canonico `cmd_patients()` è `L581`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 126 che coinvolge il nodo canonico `cmd_patients()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 126 che coinvolge il nodo canonico `cmd_patients()` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 126 che coinvolge il nodo canonico `cmd_patients()` è `collection`. Nel grafo del codice RDG, l'arco 126 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo del codice RDG, l'arco 126 registra come `target` l'identificatore `main_cmd_patients` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 126 che coinvolge il nodo canonico `cmd_patients()` è `0.5`.

<!-- GRAPH_NODE graph=unificata index=72 id=Request-Dataset-Generator::main_cmd_patients -->
Nel grafo unificato, l'etichetta originale del nodo canonico `cmd_patients()` è `cmd_patients()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `cmd_patients()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `cmd_patients()` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `cmd_patients()` è `L258`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `cmd_patients()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `cmd_patients()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `cmd_patients()` è `cmd_patients()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `cmd_patients()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `cmd_patients()` è `main_cmd_patients`. Nel grafo unificato, il nodo canonico `cmd_patients()` possiede l'identificatore esatto `Request-Dataset-Generator::main_cmd_patients`.
Nel grafo unificato, il nodo canonico `cmd_patients()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=41 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_patients()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `genera_paziente()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::fhir_patient_generator_genera_paziente` all'ID `Request-Dataset-Generator::main_cmd_patients`. La relazione tecnica esplicita è la seguente: `genera_paziente()` chiama `cmd_patients()`. Nel grafo unificato, il tipo dichiarato dell'arco 41 che coinvolge il nodo canonico `cmd_patients()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 41 che coinvolge il nodo canonico `cmd_patients()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 41 che coinvolge il nodo canonico `cmd_patients()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 41 che coinvolge il nodo canonico `cmd_patients()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 41 che coinvolge il nodo canonico `cmd_patients()` è `L262`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 41 che coinvolge il nodo canonico `cmd_patients()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 41 che coinvolge il nodo canonico `cmd_patients()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 41 che coinvolge il nodo canonico `cmd_patients()` è `1.0`. Nel grafo unificato, l'arco 41 registra come `source` l'identificatore `Request-Dataset-Generator::fhir_patient_generator_genera_paziente` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo unificato, l'arco 41 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_patients` per la relazione che coinvolge il nodo canonico `cmd_patients()`.
<!-- GRAPH_EDGE graph=unificata index=46 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_patients()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `genera_bundle_rve55()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::fhir_patient_generator_genera_bundle_rve55` all'ID `Request-Dataset-Generator::main_cmd_patients`. La relazione tecnica esplicita è la seguente: `genera_bundle_rve55()` chiama `cmd_patients()`. Nel grafo unificato, il tipo dichiarato dell'arco 46 che coinvolge il nodo canonico `cmd_patients()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 46 che coinvolge il nodo canonico `cmd_patients()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 46 che coinvolge il nodo canonico `cmd_patients()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 46 che coinvolge il nodo canonico `cmd_patients()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 46 che coinvolge il nodo canonico `cmd_patients()` è `L269`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 46 che coinvolge il nodo canonico `cmd_patients()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 46 che coinvolge il nodo canonico `cmd_patients()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 46 che coinvolge il nodo canonico `cmd_patients()` è `1.0`. Nel grafo unificato, l'arco 46 registra come `source` l'identificatore `Request-Dataset-Generator::fhir_patient_generator_genera_bundle_rve55` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo unificato, l'arco 46 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_patients` per la relazione che coinvolge il nodo canonico `cmd_patients()`.
<!-- GRAPH_EDGE graph=unificata index=50 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_patients()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `to_json()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::fhir_patient_generator_to_json` all'ID `Request-Dataset-Generator::main_cmd_patients`. La relazione tecnica esplicita è la seguente: `to_json()` chiama `cmd_patients()`. Nel grafo unificato, il tipo dichiarato dell'arco 50 che coinvolge il nodo canonico `cmd_patients()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 50 che coinvolge il nodo canonico `cmd_patients()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 50 che coinvolge il nodo canonico `cmd_patients()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 50 che coinvolge il nodo canonico `cmd_patients()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 50 che coinvolge il nodo canonico `cmd_patients()` è `L265`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 50 che coinvolge il nodo canonico `cmd_patients()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 50 che coinvolge il nodo canonico `cmd_patients()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 50 che coinvolge il nodo canonico `cmd_patients()` è `1.0`. Nel grafo unificato, l'arco 50 registra come `source` l'identificatore `Request-Dataset-Generator::fhir_patient_generator_to_json` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo unificato, l'arco 50 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_patients` per la relazione che coinvolge il nodo canonico `cmd_patients()`.
<!-- GRAPH_EDGE graph=unificata index=55 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_patients()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `to_xml()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::fhir_patient_generator_to_xml` all'ID `Request-Dataset-Generator::main_cmd_patients`. La relazione tecnica esplicita è la seguente: `to_xml()` chiama `cmd_patients()`. Nel grafo unificato, il tipo dichiarato dell'arco 55 che coinvolge il nodo canonico `cmd_patients()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 55 che coinvolge il nodo canonico `cmd_patients()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 55 che coinvolge il nodo canonico `cmd_patients()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 55 che coinvolge il nodo canonico `cmd_patients()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 55 che coinvolge il nodo canonico `cmd_patients()` è `L267`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 55 che coinvolge il nodo canonico `cmd_patients()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 55 che coinvolge il nodo canonico `cmd_patients()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 55 che coinvolge il nodo canonico `cmd_patients()` è `1.0`. Nel grafo unificato, l'arco 55 registra come `source` l'identificatore `Request-Dataset-Generator::fhir_patient_generator_to_xml` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo unificato, l'arco 55 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_patients` per la relazione che coinvolge il nodo canonico `cmd_patients()`.
<!-- GRAPH_EDGE graph=unificata index=126 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_patients()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_cmd_patients`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_patients()`. Nel grafo unificato, il tipo dichiarato dell'arco 126 che coinvolge il nodo canonico `cmd_patients()` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 126 che coinvolge il nodo canonico `cmd_patients()` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 126 che coinvolge il nodo canonico `cmd_patients()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 126 che coinvolge il nodo canonico `cmd_patients()` è `L581`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 126 che coinvolge il nodo canonico `cmd_patients()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 126 che coinvolge il nodo canonico `cmd_patients()` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 126 che coinvolge il nodo canonico `cmd_patients()` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 126 che coinvolge il nodo canonico `cmd_patients()` è `0.5`. Nel grafo unificato, l'arco 126 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo unificato, l'arco 126 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_patients` per la relazione che coinvolge il nodo canonico `cmd_patients()`.
<!-- GRAPH_EDGE graph=unificata index=166 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_patients()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Namespace`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::namespace` all'ID `Request-Dataset-Generator::main_cmd_patients`. La relazione tecnica esplicita è la seguente: `Namespace` fa riferimento a `cmd_patients()`. Nel grafo unificato, il tipo dichiarato dell'arco 166 che coinvolge il nodo canonico `cmd_patients()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 166 che coinvolge il nodo canonico `cmd_patients()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 166 che coinvolge il nodo canonico `cmd_patients()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 166 che coinvolge il nodo canonico `cmd_patients()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 166 che coinvolge il nodo canonico `cmd_patients()` è `L258`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 166 che coinvolge il nodo canonico `cmd_patients()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 166 che coinvolge il nodo canonico `cmd_patients()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 166 che coinvolge il nodo canonico `cmd_patients()` è `1.0`. Nel grafo unificato, l'arco 166 registra come `source` l'identificatore `Request-Dataset-Generator::namespace` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo unificato, l'arco 166 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_patients` per la relazione che coinvolge il nodo canonico `cmd_patients()`.
<!-- GRAPH_EDGE graph=unificata index=183 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_patients()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_write_output()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_write_output` all'ID `Request-Dataset-Generator::main_cmd_patients`. La relazione tecnica esplicita è la seguente: `_write_output()` chiama `cmd_patients()`. Nel grafo unificato, il tipo dichiarato dell'arco 183 che coinvolge il nodo canonico `cmd_patients()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 183 che coinvolge il nodo canonico `cmd_patients()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 183 che coinvolge il nodo canonico `cmd_patients()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 183 che coinvolge il nodo canonico `cmd_patients()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 183 che coinvolge il nodo canonico `cmd_patients()` è `L274`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 183 che coinvolge il nodo canonico `cmd_patients()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 183 che coinvolge il nodo canonico `cmd_patients()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 183 che coinvolge il nodo canonico `cmd_patients()` è `1.0`. Nel grafo unificato, l'arco 183 registra come `source` l'identificatore `Request-Dataset-Generator::main_write_output` per la relazione che coinvolge il nodo canonico `cmd_patients()`. Nel grafo unificato, l'arco 183 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_patients` per la relazione che coinvolge il nodo canonico `cmd_patients()`.

### cmd_pipeline()

<!-- GRAPH_NODE graph=codice index=78 id=main_cmd_pipeline -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `cmd_pipeline()` è `cmd_pipeline()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `cmd_pipeline()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `cmd_pipeline()` è `L515`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `cmd_pipeline()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `cmd_pipeline()` possiede l'identificatore esatto `main_cmd_pipeline`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `cmd_pipeline()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `cmd_pipeline()` è `cmd_pipeline()`.
<!-- GRAPH_EDGE graph=codice index=111 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_pipeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_pipeline` all'ID `log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `cmd_pipeline()` chiama `collect_logs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 111 che coinvolge il nodo canonico `cmd_pipeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 111 che coinvolge il nodo canonico `cmd_pipeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 111 che coinvolge il nodo canonico `cmd_pipeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 111 che coinvolge il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 111 che coinvolge il nodo canonico `cmd_pipeline()` è `L531`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 111 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 111 che coinvolge il nodo canonico `cmd_pipeline()` è `ast`. Nel grafo del codice RDG, l'arco 111 registra come `source` l'identificatore `main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo del codice RDG, l'arco 111 registra come `target` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 111 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=155 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_pipeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_cfg_section()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_pipeline` all'ID `main_cfg_section`. La relazione tecnica esplicita è la seguente: `cmd_pipeline()` chiama `_cfg_section()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 155 che coinvolge il nodo canonico `cmd_pipeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 155 che coinvolge il nodo canonico `cmd_pipeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 155 che coinvolge il nodo canonico `cmd_pipeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 155 che coinvolge il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 155 che coinvolge il nodo canonico `cmd_pipeline()` è `L529`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 155 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 155 che coinvolge il nodo canonico `cmd_pipeline()` è `ast`. Nel grafo del codice RDG, l'arco 155 registra come `source` l'identificatore `main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo del codice RDG, l'arco 155 registra come `target` l'identificatore `main_cfg_section` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 155 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=160 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_pipeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_build_namespace()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_pipeline` all'ID `main_build_namespace`. La relazione tecnica esplicita è la seguente: `cmd_pipeline()` chiama `_build_namespace()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 160 che coinvolge il nodo canonico `cmd_pipeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 160 che coinvolge il nodo canonico `cmd_pipeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 160 che coinvolge il nodo canonico `cmd_pipeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 160 che coinvolge il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 160 che coinvolge il nodo canonico `cmd_pipeline()` è `L521`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 160 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 160 che coinvolge il nodo canonico `cmd_pipeline()` è `ast`. Nel grafo del codice RDG, l'arco 160 registra come `source` l'identificatore `main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo del codice RDG, l'arco 160 registra come `target` l'identificatore `main_build_namespace` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 160 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=167 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_pipeline()` è la sorgente `source` della relazione `references` verso il nodo canonico `Namespace`; l'orientamento registrato nel JSON va dall'ID `main_cmd_pipeline` all'ID `namespace`. La relazione tecnica esplicita è la seguente: `cmd_pipeline()` fa riferimento a `Namespace`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 167 che coinvolge il nodo canonico `cmd_pipeline()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 167 che coinvolge il nodo canonico `cmd_pipeline()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 167 che coinvolge il nodo canonico `cmd_pipeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 167 che coinvolge il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 167 che coinvolge il nodo canonico `cmd_pipeline()` è `L515`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 167 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 167 che coinvolge il nodo canonico `cmd_pipeline()` è `ast`. Nel grafo del codice RDG, l'arco 167 registra come `source` l'identificatore `main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo del codice RDG, l'arco 167 registra come `target` l'identificatore `namespace` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 167 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=173 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_pipeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_configure_run_outputs()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_pipeline` all'ID `main_configure_run_outputs`. La relazione tecnica esplicita è la seguente: `cmd_pipeline()` chiama `_configure_run_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 173 che coinvolge il nodo canonico `cmd_pipeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 173 che coinvolge il nodo canonico `cmd_pipeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 173 che coinvolge il nodo canonico `cmd_pipeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 173 che coinvolge il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 173 che coinvolge il nodo canonico `cmd_pipeline()` è `L516`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 173 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 173 che coinvolge il nodo canonico `cmd_pipeline()` è `ast`. Nel grafo del codice RDG, l'arco 173 registra come `source` l'identificatore `main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo del codice RDG, l'arco 173 registra come `target` l'identificatore `main_configure_run_outputs` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 173 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=174 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_pipeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_write_resolved_config()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_pipeline` all'ID `main_write_resolved_config`. La relazione tecnica esplicita è la seguente: `cmd_pipeline()` chiama `_write_resolved_config()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 174 che coinvolge il nodo canonico `cmd_pipeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 174 che coinvolge il nodo canonico `cmd_pipeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 174 che coinvolge il nodo canonico `cmd_pipeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 174 che coinvolge il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 174 che coinvolge il nodo canonico `cmd_pipeline()` è `L517`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 174 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 174 che coinvolge il nodo canonico `cmd_pipeline()` è `ast`. Nel grafo del codice RDG, l'arco 174 registra come `source` l'identificatore `main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo del codice RDG, l'arco 174 registra come `target` l'identificatore `main_write_resolved_config` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 174 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=181 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_pipeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_resolve_output_path() [main_resolve_output_path]`; l'orientamento registrato nel JSON va dall'ID `main_cmd_pipeline` all'ID `main_resolve_output_path`. La relazione tecnica esplicita è la seguente: `cmd_pipeline()` chiama `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 181 che coinvolge il nodo canonico `cmd_pipeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 181 che coinvolge il nodo canonico `cmd_pipeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 181 che coinvolge il nodo canonico `cmd_pipeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 181 che coinvolge il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 181 che coinvolge il nodo canonico `cmd_pipeline()` è `L530`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 181 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 181 che coinvolge il nodo canonico `cmd_pipeline()` è `ast`. Nel grafo del codice RDG, l'arco 181 registra come `source` l'identificatore `main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo del codice RDG, l'arco 181 registra come `target` l'identificatore `main_resolve_output_path` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 181 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=195 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_pipeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_pipeline` all'ID `main_cmd_execute`. La relazione tecnica esplicita è la seguente: `cmd_pipeline()` chiama `cmd_execute()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 195 che coinvolge il nodo canonico `cmd_pipeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 195 che coinvolge il nodo canonico `cmd_pipeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 195 che coinvolge il nodo canonico `cmd_pipeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 195 che coinvolge il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 195 che coinvolge il nodo canonico `cmd_pipeline()` è `L522`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 195 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 195 che coinvolge il nodo canonico `cmd_pipeline()` è `ast`. Nel grafo del codice RDG, l'arco 195 registra come `source` l'identificatore `main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo del codice RDG, l'arco 195 registra come `target` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 195 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=205 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_pipeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `run_metric_engine()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_pipeline` all'ID `metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `cmd_pipeline()` chiama `run_metric_engine()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 205 che coinvolge il nodo canonico `cmd_pipeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 205 che coinvolge il nodo canonico `cmd_pipeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 205 che coinvolge il nodo canonico `cmd_pipeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 205 che coinvolge il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 205 che coinvolge il nodo canonico `cmd_pipeline()` è `L545`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 205 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 205 che coinvolge il nodo canonico `cmd_pipeline()` è `ast`. Nel grafo del codice RDG, l'arco 205 registra come `source` l'identificatore `main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo del codice RDG, l'arco 205 registra come `target` l'identificatore `metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 205 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=127 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `cmd_pipeline()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_cmd_pipeline`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_pipeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 127 che coinvolge il nodo canonico `cmd_pipeline()` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 127 che coinvolge il nodo canonico `cmd_pipeline()` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 127 che coinvolge il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 127 che coinvolge il nodo canonico `cmd_pipeline()` è `L587`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 127 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 127 che coinvolge il nodo canonico `cmd_pipeline()` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 127 che coinvolge il nodo canonico `cmd_pipeline()` è `collection`. Nel grafo del codice RDG, l'arco 127 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo del codice RDG, l'arco 127 registra come `target` l'identificatore `main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 127 che coinvolge il nodo canonico `cmd_pipeline()` è `0.5`.

<!-- GRAPH_NODE graph=unificata index=78 id=Request-Dataset-Generator::main_cmd_pipeline -->
Nel grafo unificato, l'etichetta originale del nodo canonico `cmd_pipeline()` è `cmd_pipeline()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `cmd_pipeline()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `cmd_pipeline()` è `L515`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `cmd_pipeline()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `cmd_pipeline()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `cmd_pipeline()` è `cmd_pipeline()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `cmd_pipeline()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `cmd_pipeline()` è `main_cmd_pipeline`. Nel grafo unificato, il nodo canonico `cmd_pipeline()` possiede l'identificatore esatto `Request-Dataset-Generator::main_cmd_pipeline`.
<!-- GRAPH_EDGE graph=unificata index=205 role=outgoing -->
Nel grafo unificato, il nodo canonico `cmd_pipeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `run_metric_engine()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_pipeline` all'ID `Request-Dataset-Generator::metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `cmd_pipeline()` chiama `run_metric_engine()`. Nel grafo unificato, il tipo dichiarato dell'arco 205 che coinvolge il nodo canonico `cmd_pipeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 205 che coinvolge il nodo canonico `cmd_pipeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 205 che coinvolge il nodo canonico `cmd_pipeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 205 che coinvolge il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 205 che coinvolge il nodo canonico `cmd_pipeline()` è `L545`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 205 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 205 che coinvolge il nodo canonico `cmd_pipeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 205 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo unificato, l'arco 205 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo unificato, l'arco 205 registra come `target` l'identificatore `Request-Dataset-Generator::metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`.
<!-- GRAPH_EDGE graph=unificata index=111 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_pipeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_collect_logs` all'ID `Request-Dataset-Generator::main_cmd_pipeline`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `cmd_pipeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 111 che coinvolge il nodo canonico `cmd_pipeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 111 che coinvolge il nodo canonico `cmd_pipeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 111 che coinvolge il nodo canonico `cmd_pipeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 111 che coinvolge il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 111 che coinvolge il nodo canonico `cmd_pipeline()` è `L531`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 111 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 111 che coinvolge il nodo canonico `cmd_pipeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 111 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo unificato, l'arco 111 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo unificato, l'arco 111 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`.
<!-- GRAPH_EDGE graph=unificata index=127 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_pipeline()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_cmd_pipeline`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_pipeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 127 che coinvolge il nodo canonico `cmd_pipeline()` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 127 che coinvolge il nodo canonico `cmd_pipeline()` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 127 che coinvolge il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 127 che coinvolge il nodo canonico `cmd_pipeline()` è `L587`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 127 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 127 che coinvolge il nodo canonico `cmd_pipeline()` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 127 che coinvolge il nodo canonico `cmd_pipeline()` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 127 che coinvolge il nodo canonico `cmd_pipeline()` è `0.5`. Nel grafo unificato, l'arco 127 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo unificato, l'arco 127 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`.
<!-- GRAPH_EDGE graph=unificata index=155 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_pipeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_cfg_section()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cfg_section` all'ID `Request-Dataset-Generator::main_cmd_pipeline`. La relazione tecnica esplicita è la seguente: `_cfg_section()` chiama `cmd_pipeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 155 che coinvolge il nodo canonico `cmd_pipeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 155 che coinvolge il nodo canonico `cmd_pipeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 155 che coinvolge il nodo canonico `cmd_pipeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 155 che coinvolge il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 155 che coinvolge il nodo canonico `cmd_pipeline()` è `L529`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 155 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 155 che coinvolge il nodo canonico `cmd_pipeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 155 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo unificato, l'arco 155 registra come `source` l'identificatore `Request-Dataset-Generator::main_cfg_section` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo unificato, l'arco 155 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`.
<!-- GRAPH_EDGE graph=unificata index=160 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_pipeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_build_namespace()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_build_namespace` all'ID `Request-Dataset-Generator::main_cmd_pipeline`. La relazione tecnica esplicita è la seguente: `_build_namespace()` chiama `cmd_pipeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 160 che coinvolge il nodo canonico `cmd_pipeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 160 che coinvolge il nodo canonico `cmd_pipeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 160 che coinvolge il nodo canonico `cmd_pipeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 160 che coinvolge il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 160 che coinvolge il nodo canonico `cmd_pipeline()` è `L521`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 160 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 160 che coinvolge il nodo canonico `cmd_pipeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 160 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo unificato, l'arco 160 registra come `source` l'identificatore `Request-Dataset-Generator::main_build_namespace` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo unificato, l'arco 160 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`.
<!-- GRAPH_EDGE graph=unificata index=167 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_pipeline()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Namespace`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::namespace` all'ID `Request-Dataset-Generator::main_cmd_pipeline`. La relazione tecnica esplicita è la seguente: `Namespace` fa riferimento a `cmd_pipeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 167 che coinvolge il nodo canonico `cmd_pipeline()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 167 che coinvolge il nodo canonico `cmd_pipeline()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 167 che coinvolge il nodo canonico `cmd_pipeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 167 che coinvolge il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 167 che coinvolge il nodo canonico `cmd_pipeline()` è `L515`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 167 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 167 che coinvolge il nodo canonico `cmd_pipeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 167 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo unificato, l'arco 167 registra come `source` l'identificatore `Request-Dataset-Generator::namespace` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo unificato, l'arco 167 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`.
<!-- GRAPH_EDGE graph=unificata index=173 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_pipeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_configure_run_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_configure_run_outputs` all'ID `Request-Dataset-Generator::main_cmd_pipeline`. La relazione tecnica esplicita è la seguente: `_configure_run_outputs()` chiama `cmd_pipeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 173 che coinvolge il nodo canonico `cmd_pipeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 173 che coinvolge il nodo canonico `cmd_pipeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 173 che coinvolge il nodo canonico `cmd_pipeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 173 che coinvolge il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 173 che coinvolge il nodo canonico `cmd_pipeline()` è `L516`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 173 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 173 che coinvolge il nodo canonico `cmd_pipeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 173 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo unificato, l'arco 173 registra come `source` l'identificatore `Request-Dataset-Generator::main_configure_run_outputs` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo unificato, l'arco 173 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`.
<!-- GRAPH_EDGE graph=unificata index=174 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_pipeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_write_resolved_config()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_write_resolved_config` all'ID `Request-Dataset-Generator::main_cmd_pipeline`. La relazione tecnica esplicita è la seguente: `_write_resolved_config()` chiama `cmd_pipeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 174 che coinvolge il nodo canonico `cmd_pipeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 174 che coinvolge il nodo canonico `cmd_pipeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 174 che coinvolge il nodo canonico `cmd_pipeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 174 che coinvolge il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 174 che coinvolge il nodo canonico `cmd_pipeline()` è `L517`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 174 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 174 che coinvolge il nodo canonico `cmd_pipeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 174 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo unificato, l'arco 174 registra come `source` l'identificatore `Request-Dataset-Generator::main_write_resolved_config` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo unificato, l'arco 174 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`.
<!-- GRAPH_EDGE graph=unificata index=181 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_pipeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_resolve_output_path() [main_resolve_output_path]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_resolve_output_path` all'ID `Request-Dataset-Generator::main_cmd_pipeline`. La relazione tecnica esplicita è la seguente: `_resolve_output_path() [main_resolve_output_path]` chiama `cmd_pipeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 181 che coinvolge il nodo canonico `cmd_pipeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 181 che coinvolge il nodo canonico `cmd_pipeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 181 che coinvolge il nodo canonico `cmd_pipeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 181 che coinvolge il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 181 che coinvolge il nodo canonico `cmd_pipeline()` è `L530`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 181 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 181 che coinvolge il nodo canonico `cmd_pipeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 181 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo unificato, l'arco 181 registra come `source` l'identificatore `Request-Dataset-Generator::main_resolve_output_path` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo unificato, l'arco 181 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`.
<!-- GRAPH_EDGE graph=unificata index=195 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_pipeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_execute` all'ID `Request-Dataset-Generator::main_cmd_pipeline`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `cmd_pipeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 195 che coinvolge il nodo canonico `cmd_pipeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 195 che coinvolge il nodo canonico `cmd_pipeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 195 che coinvolge il nodo canonico `cmd_pipeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 195 che coinvolge il nodo canonico `cmd_pipeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 195 che coinvolge il nodo canonico `cmd_pipeline()` è `L522`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 195 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 195 che coinvolge il nodo canonico `cmd_pipeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 195 che coinvolge il nodo canonico `cmd_pipeline()` è `1.0`. Nel grafo unificato, l'arco 195 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`. Nel grafo unificato, l'arco 195 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `cmd_pipeline()`.

### cmd_requests()

<!-- GRAPH_NODE graph=codice index=73 id=main_cmd_requests -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `cmd_requests()` è `cmd_requests()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `cmd_requests()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `cmd_requests()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `cmd_requests()` è `L277`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `cmd_requests()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `cmd_requests()` possiede l'identificatore esatto `main_cmd_requests`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `cmd_requests()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `cmd_requests()` è `cmd_requests()`.
<!-- GRAPH_EDGE graph=codice index=156 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_requests()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_cfg_section()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_requests` all'ID `main_cfg_section`. La relazione tecnica esplicita è la seguente: `cmd_requests()` chiama `_cfg_section()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 156 che coinvolge il nodo canonico `cmd_requests()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 156 che coinvolge il nodo canonico `cmd_requests()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 156 che coinvolge il nodo canonico `cmd_requests()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 156 che coinvolge il nodo canonico `cmd_requests()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 156 che coinvolge il nodo canonico `cmd_requests()` è `L280`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 156 che coinvolge il nodo canonico `cmd_requests()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 156 che coinvolge il nodo canonico `cmd_requests()` è `ast`. Nel grafo del codice RDG, l'arco 156 registra come `source` l'identificatore `main_cmd_requests` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo del codice RDG, l'arco 156 registra come `target` l'identificatore `main_cfg_section` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 156 che coinvolge il nodo canonico `cmd_requests()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=168 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_requests()` è la sorgente `source` della relazione `references` verso il nodo canonico `Namespace`; l'orientamento registrato nel JSON va dall'ID `main_cmd_requests` all'ID `namespace`. La relazione tecnica esplicita è la seguente: `cmd_requests()` fa riferimento a `Namespace`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 168 che coinvolge il nodo canonico `cmd_requests()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 168 che coinvolge il nodo canonico `cmd_requests()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 168 che coinvolge il nodo canonico `cmd_requests()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 168 che coinvolge il nodo canonico `cmd_requests()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 168 che coinvolge il nodo canonico `cmd_requests()` è `L277`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 168 che coinvolge il nodo canonico `cmd_requests()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 168 che coinvolge il nodo canonico `cmd_requests()` è `ast`. Nel grafo del codice RDG, l'arco 168 registra come `source` l'identificatore `main_cmd_requests` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo del codice RDG, l'arco 168 registra come `target` l'identificatore `namespace` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 168 che coinvolge il nodo canonico `cmd_requests()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=176 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_requests()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_annotate_dataset_flows()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_requests` all'ID `main_annotate_dataset_flows`. La relazione tecnica esplicita è la seguente: `cmd_requests()` chiama `_annotate_dataset_flows()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 176 che coinvolge il nodo canonico `cmd_requests()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 176 che coinvolge il nodo canonico `cmd_requests()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 176 che coinvolge il nodo canonico `cmd_requests()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 176 che coinvolge il nodo canonico `cmd_requests()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 176 che coinvolge il nodo canonico `cmd_requests()` è `L290`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 176 che coinvolge il nodo canonico `cmd_requests()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 176 che coinvolge il nodo canonico `cmd_requests()` è `ast`. Nel grafo del codice RDG, l'arco 176 registra come `source` l'identificatore `main_cmd_requests` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo del codice RDG, l'arco 176 registra come `target` l'identificatore `main_annotate_dataset_flows` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 176 che coinvolge il nodo canonico `cmd_requests()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=184 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_requests()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_write_output()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_requests` all'ID `main_write_output`. La relazione tecnica esplicita è la seguente: `cmd_requests()` chiama `_write_output()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 184 che coinvolge il nodo canonico `cmd_requests()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 184 che coinvolge il nodo canonico `cmd_requests()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 184 che coinvolge il nodo canonico `cmd_requests()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 184 che coinvolge il nodo canonico `cmd_requests()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 184 che coinvolge il nodo canonico `cmd_requests()` è `L292`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 184 che coinvolge il nodo canonico `cmd_requests()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 184 che coinvolge il nodo canonico `cmd_requests()` è `ast`. Nel grafo del codice RDG, l'arco 184 registra come `source` l'identificatore `main_cmd_requests` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo del codice RDG, l'arco 184 registra come `target` l'identificatore `main_write_output` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 184 che coinvolge il nodo canonico `cmd_requests()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=187 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_requests()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_requests` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `cmd_requests()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 187 che coinvolge il nodo canonico `cmd_requests()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 187 che coinvolge il nodo canonico `cmd_requests()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 187 che coinvolge il nodo canonico `cmd_requests()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 187 che coinvolge il nodo canonico `cmd_requests()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 187 che coinvolge il nodo canonico `cmd_requests()` è `L283`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 187 che coinvolge il nodo canonico `cmd_requests()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 187 che coinvolge il nodo canonico `cmd_requests()` è `ast`. Nel grafo del codice RDG, l'arco 187 registra come `source` l'identificatore `main_cmd_requests` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo del codice RDG, l'arco 187 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 187 che coinvolge il nodo canonico `cmd_requests()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=128 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `cmd_requests()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_cmd_requests`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_requests()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 128 che coinvolge il nodo canonico `cmd_requests()` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 128 che coinvolge il nodo canonico `cmd_requests()` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 128 che coinvolge il nodo canonico `cmd_requests()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 128 che coinvolge il nodo canonico `cmd_requests()` è `L582`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 128 che coinvolge il nodo canonico `cmd_requests()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 128 che coinvolge il nodo canonico `cmd_requests()` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 128 che coinvolge il nodo canonico `cmd_requests()` è `collection`. Nel grafo del codice RDG, l'arco 128 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo del codice RDG, l'arco 128 registra come `target` l'identificatore `main_cmd_requests` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 128 che coinvolge il nodo canonico `cmd_requests()` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=188 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `cmd_requests()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Genera un dataset di flussi RVE pre-composti.`; l'orientamento registrato nel JSON va dall'ID `main_rationale_278` all'ID `main_cmd_requests`. La relazione tecnica esplicita è la seguente: `Genera un dataset di flussi RVE pre-composti.` costituisce una motivazione per `cmd_requests()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 188 che coinvolge il nodo canonico `cmd_requests()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 188 che coinvolge il nodo canonico `cmd_requests()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 188 che coinvolge il nodo canonico `cmd_requests()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 188 che coinvolge il nodo canonico `cmd_requests()` è `L278`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 188 che coinvolge il nodo canonico `cmd_requests()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 188 che coinvolge il nodo canonico `cmd_requests()` è `ast`. Nel grafo del codice RDG, l'arco 188 registra come `source` l'identificatore `main_rationale_278` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo del codice RDG, l'arco 188 registra come `target` l'identificatore `main_cmd_requests` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 188 che coinvolge il nodo canonico `cmd_requests()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=73 id=Request-Dataset-Generator::main_cmd_requests -->
Nel grafo unificato, l'etichetta originale del nodo canonico `cmd_requests()` è `cmd_requests()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `cmd_requests()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `cmd_requests()` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `cmd_requests()` è `L277`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `cmd_requests()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `cmd_requests()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `cmd_requests()` è `cmd_requests()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `cmd_requests()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `cmd_requests()` è `main_cmd_requests`. Nel grafo unificato, il nodo canonico `cmd_requests()` possiede l'identificatore esatto `Request-Dataset-Generator::main_cmd_requests`.
<!-- GRAPH_EDGE graph=unificata index=187 role=outgoing -->
Nel grafo unificato, il nodo canonico `cmd_requests()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_requests` all'ID `Request-Dataset-Generator::request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `cmd_requests()` chiama `build_workload()`. Nel grafo unificato, il tipo dichiarato dell'arco 187 che coinvolge il nodo canonico `cmd_requests()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 187 che coinvolge il nodo canonico `cmd_requests()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 187 che coinvolge il nodo canonico `cmd_requests()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 187 che coinvolge il nodo canonico `cmd_requests()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 187 che coinvolge il nodo canonico `cmd_requests()` è `L283`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 187 che coinvolge il nodo canonico `cmd_requests()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 187 che coinvolge il nodo canonico `cmd_requests()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 187 che coinvolge il nodo canonico `cmd_requests()` è `1.0`. Nel grafo unificato, l'arco 187 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_requests` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo unificato, l'arco 187 registra come `target` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `cmd_requests()`.
<!-- GRAPH_EDGE graph=unificata index=188 role=outgoing -->
Nel grafo unificato, il nodo canonico `cmd_requests()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Genera un dataset di flussi RVE pre-composti.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_requests` all'ID `Request-Dataset-Generator::main_rationale_278`. La relazione tecnica esplicita è la seguente: `cmd_requests()` costituisce una motivazione per `Genera un dataset di flussi RVE pre-composti.`. Nel grafo unificato, il tipo dichiarato dell'arco 188 che coinvolge il nodo canonico `cmd_requests()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 188 che coinvolge il nodo canonico `cmd_requests()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 188 che coinvolge il nodo canonico `cmd_requests()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 188 che coinvolge il nodo canonico `cmd_requests()` è `L278`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 188 che coinvolge il nodo canonico `cmd_requests()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 188 che coinvolge il nodo canonico `cmd_requests()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 188 che coinvolge il nodo canonico `cmd_requests()` è `1.0`. Nel grafo unificato, l'arco 188 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_requests` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo unificato, l'arco 188 registra come `target` l'identificatore `Request-Dataset-Generator::main_rationale_278` per la relazione che coinvolge il nodo canonico `cmd_requests()`.
<!-- GRAPH_EDGE graph=unificata index=128 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_requests()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_cmd_requests`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_requests()`. Nel grafo unificato, il tipo dichiarato dell'arco 128 che coinvolge il nodo canonico `cmd_requests()` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 128 che coinvolge il nodo canonico `cmd_requests()` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 128 che coinvolge il nodo canonico `cmd_requests()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 128 che coinvolge il nodo canonico `cmd_requests()` è `L582`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 128 che coinvolge il nodo canonico `cmd_requests()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 128 che coinvolge il nodo canonico `cmd_requests()` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 128 che coinvolge il nodo canonico `cmd_requests()` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 128 che coinvolge il nodo canonico `cmd_requests()` è `0.5`. Nel grafo unificato, l'arco 128 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo unificato, l'arco 128 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_requests` per la relazione che coinvolge il nodo canonico `cmd_requests()`.
<!-- GRAPH_EDGE graph=unificata index=156 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_requests()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_cfg_section()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cfg_section` all'ID `Request-Dataset-Generator::main_cmd_requests`. La relazione tecnica esplicita è la seguente: `_cfg_section()` chiama `cmd_requests()`. Nel grafo unificato, il tipo dichiarato dell'arco 156 che coinvolge il nodo canonico `cmd_requests()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 156 che coinvolge il nodo canonico `cmd_requests()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 156 che coinvolge il nodo canonico `cmd_requests()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 156 che coinvolge il nodo canonico `cmd_requests()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 156 che coinvolge il nodo canonico `cmd_requests()` è `L280`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 156 che coinvolge il nodo canonico `cmd_requests()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 156 che coinvolge il nodo canonico `cmd_requests()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 156 che coinvolge il nodo canonico `cmd_requests()` è `1.0`. Nel grafo unificato, l'arco 156 registra come `source` l'identificatore `Request-Dataset-Generator::main_cfg_section` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo unificato, l'arco 156 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_requests` per la relazione che coinvolge il nodo canonico `cmd_requests()`.
<!-- GRAPH_EDGE graph=unificata index=168 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_requests()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Namespace`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::namespace` all'ID `Request-Dataset-Generator::main_cmd_requests`. La relazione tecnica esplicita è la seguente: `Namespace` fa riferimento a `cmd_requests()`. Nel grafo unificato, il tipo dichiarato dell'arco 168 che coinvolge il nodo canonico `cmd_requests()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 168 che coinvolge il nodo canonico `cmd_requests()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 168 che coinvolge il nodo canonico `cmd_requests()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 168 che coinvolge il nodo canonico `cmd_requests()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 168 che coinvolge il nodo canonico `cmd_requests()` è `L277`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 168 che coinvolge il nodo canonico `cmd_requests()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 168 che coinvolge il nodo canonico `cmd_requests()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 168 che coinvolge il nodo canonico `cmd_requests()` è `1.0`. Nel grafo unificato, l'arco 168 registra come `source` l'identificatore `Request-Dataset-Generator::namespace` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo unificato, l'arco 168 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_requests` per la relazione che coinvolge il nodo canonico `cmd_requests()`.
<!-- GRAPH_EDGE graph=unificata index=176 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_requests()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_annotate_dataset_flows()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_annotate_dataset_flows` all'ID `Request-Dataset-Generator::main_cmd_requests`. La relazione tecnica esplicita è la seguente: `_annotate_dataset_flows()` chiama `cmd_requests()`. Nel grafo unificato, il tipo dichiarato dell'arco 176 che coinvolge il nodo canonico `cmd_requests()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 176 che coinvolge il nodo canonico `cmd_requests()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 176 che coinvolge il nodo canonico `cmd_requests()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 176 che coinvolge il nodo canonico `cmd_requests()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 176 che coinvolge il nodo canonico `cmd_requests()` è `L290`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 176 che coinvolge il nodo canonico `cmd_requests()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 176 che coinvolge il nodo canonico `cmd_requests()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 176 che coinvolge il nodo canonico `cmd_requests()` è `1.0`. Nel grafo unificato, l'arco 176 registra come `source` l'identificatore `Request-Dataset-Generator::main_annotate_dataset_flows` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo unificato, l'arco 176 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_requests` per la relazione che coinvolge il nodo canonico `cmd_requests()`.
<!-- GRAPH_EDGE graph=unificata index=184 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_requests()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_write_output()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_write_output` all'ID `Request-Dataset-Generator::main_cmd_requests`. La relazione tecnica esplicita è la seguente: `_write_output()` chiama `cmd_requests()`. Nel grafo unificato, il tipo dichiarato dell'arco 184 che coinvolge il nodo canonico `cmd_requests()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 184 che coinvolge il nodo canonico `cmd_requests()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 184 che coinvolge il nodo canonico `cmd_requests()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 184 che coinvolge il nodo canonico `cmd_requests()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 184 che coinvolge il nodo canonico `cmd_requests()` è `L292`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 184 che coinvolge il nodo canonico `cmd_requests()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 184 che coinvolge il nodo canonico `cmd_requests()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 184 che coinvolge il nodo canonico `cmd_requests()` è `1.0`. Nel grafo unificato, l'arco 184 registra come `source` l'identificatore `Request-Dataset-Generator::main_write_output` per la relazione che coinvolge il nodo canonico `cmd_requests()`. Nel grafo unificato, l'arco 184 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_requests` per la relazione che coinvolge il nodo canonico `cmd_requests()`.

### cmd_timeline()

<!-- GRAPH_NODE graph=codice index=75 id=main_cmd_timeline -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `cmd_timeline()` è `cmd_timeline()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `cmd_timeline()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `cmd_timeline()` è `L408`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `cmd_timeline()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `cmd_timeline()` possiede l'identificatore esatto `main_cmd_timeline`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `cmd_timeline()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `cmd_timeline()` è `cmd_timeline()`.
<!-- GRAPH_EDGE graph=codice index=157 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_timeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_cfg_section()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_timeline` all'ID `main_cfg_section`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `_cfg_section()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 157 che coinvolge il nodo canonico `cmd_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 157 che coinvolge il nodo canonico `cmd_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 157 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 157 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 157 che coinvolge il nodo canonico `cmd_timeline()` è `L411`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 157 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 157 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 157 registra come `source` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, l'arco 157 registra come `target` l'identificatore `main_cfg_section` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 157 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=169 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_timeline()` è la sorgente `source` della relazione `references` verso il nodo canonico `Namespace`; l'orientamento registrato nel JSON va dall'ID `main_cmd_timeline` all'ID `namespace`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` fa riferimento a `Namespace`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 169 che coinvolge il nodo canonico `cmd_timeline()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 169 che coinvolge il nodo canonico `cmd_timeline()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 169 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 169 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 169 che coinvolge il nodo canonico `cmd_timeline()` è `L408`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 169 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 169 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 169 registra come `source` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, l'arco 169 registra come `target` l'identificatore `namespace` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 169 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=177 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_timeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_annotate_dataset_flows()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_timeline` all'ID `main_annotate_dataset_flows`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `_annotate_dataset_flows()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 177 che coinvolge il nodo canonico `cmd_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 177 che coinvolge il nodo canonico `cmd_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 177 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 177 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 177 che coinvolge il nodo canonico `cmd_timeline()` è `L426`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 177 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 177 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 177 registra come `source` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, l'arco 177 registra come `target` l'identificatore `main_annotate_dataset_flows` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 177 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=185 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_timeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_write_output()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_timeline` all'ID `main_write_output`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `_write_output()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 185 che coinvolge il nodo canonico `cmd_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 185 che coinvolge il nodo canonico `cmd_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 185 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 185 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 185 che coinvolge il nodo canonico `cmd_timeline()` è `L470`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 185 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 185 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 185 registra come `source` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, l'arco 185 registra come `target` l'identificatore `main_write_output` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 185 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=197 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_timeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_timeline` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 197 che coinvolge il nodo canonico `cmd_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 197 che coinvolge il nodo canonico `cmd_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 197 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 197 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 197 che coinvolge il nodo canonico `cmd_timeline()` è `L419`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 197 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 197 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 197 registra come `source` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, l'arco 197 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 197 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=198 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_timeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_timeline` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 198 che coinvolge il nodo canonico `cmd_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 198 che coinvolge il nodo canonico `cmd_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 198 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 198 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 198 che coinvolge il nodo canonico `cmd_timeline()` è `L444`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 198 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 198 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 198 registra come `source` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, l'arco 198 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 198 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=199 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_timeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_timeline` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 199 che coinvolge il nodo canonico `cmd_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 199 che coinvolge il nodo canonico `cmd_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 199 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 199 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 199 che coinvolge il nodo canonico `cmd_timeline()` è `L442`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 199 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 199 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 199 registra come `source` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, l'arco 199 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 199 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=200 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_timeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `daily_clinical_profile()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_timeline` all'ID `timeline_generator_daily_clinical_profile`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `daily_clinical_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 200 che coinvolge il nodo canonico `cmd_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 200 che coinvolge il nodo canonico `cmd_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 200 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 200 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 200 che coinvolge il nodo canonico `cmd_timeline()` è `L436`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 200 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 200 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 200 registra come `source` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, l'arco 200 registra come `target` l'identificatore `timeline_generator_daily_clinical_profile` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 200 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=201 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_timeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `step_profile()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_timeline` all'ID `timeline_generator_step_profile`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `step_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 201 che coinvolge il nodo canonico `cmd_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 201 che coinvolge il nodo canonico `cmd_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 201 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 201 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 201 che coinvolge il nodo canonico `cmd_timeline()` è `L434`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 201 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 201 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 201 registra come `source` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, l'arco 201 registra come `target` l'identificatore `timeline_generator_step_profile` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 201 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=202 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cmd_timeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `timeline_stats()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_timeline` all'ID `timeline_generator_timeline_stats`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `timeline_stats()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 202 che coinvolge il nodo canonico `cmd_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 202 che coinvolge il nodo canonico `cmd_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 202 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 202 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 202 che coinvolge il nodo canonico `cmd_timeline()` è `L459`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 202 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 202 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 202 registra come `source` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, l'arco 202 registra come `target` l'identificatore `timeline_generator_timeline_stats` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 202 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=129 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `cmd_timeline()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_cmd_timeline`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 129 che coinvolge il nodo canonico `cmd_timeline()` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 129 che coinvolge il nodo canonico `cmd_timeline()` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 129 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 129 che coinvolge il nodo canonico `cmd_timeline()` è `L583`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 129 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 129 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 129 che coinvolge il nodo canonico `cmd_timeline()` è `collection`. Nel grafo del codice RDG, l'arco 129 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, l'arco 129 registra come `target` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 129 che coinvolge il nodo canonico `cmd_timeline()` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=203 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `cmd_timeline()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Genera una timeline temporizzata di flussi RVE.`; l'orientamento registrato nel JSON va dall'ID `main_rationale_409` all'ID `main_cmd_timeline`. La relazione tecnica esplicita è la seguente: `Genera una timeline temporizzata di flussi RVE.` costituisce una motivazione per `cmd_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 203 che coinvolge il nodo canonico `cmd_timeline()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 203 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 203 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 203 che coinvolge il nodo canonico `cmd_timeline()` è `L409`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 203 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 203 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 203 registra come `source` l'identificatore `main_rationale_409` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, l'arco 203 registra come `target` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 203 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=75 id=Request-Dataset-Generator::main_cmd_timeline -->
Nel grafo unificato, l'etichetta originale del nodo canonico `cmd_timeline()` è `cmd_timeline()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `cmd_timeline()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `cmd_timeline()` è `L408`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `cmd_timeline()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `cmd_timeline()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `cmd_timeline()` è `cmd_timeline()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `cmd_timeline()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `cmd_timeline()` è `main_cmd_timeline`. Nel grafo unificato, il nodo canonico `cmd_timeline()` possiede l'identificatore esatto `Request-Dataset-Generator::main_cmd_timeline`.
<!-- GRAPH_EDGE graph=unificata index=197 role=outgoing -->
Nel grafo unificato, il nodo canonico `cmd_timeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_timeline` all'ID `Request-Dataset-Generator::request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `build_workload()`. Nel grafo unificato, il tipo dichiarato dell'arco 197 che coinvolge il nodo canonico `cmd_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 197 che coinvolge il nodo canonico `cmd_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 197 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 197 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 197 che coinvolge il nodo canonico `cmd_timeline()` è `L419`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 197 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 197 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 197 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, l'arco 197 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo unificato, l'arco 197 registra come `target` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `cmd_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=198 role=outgoing -->
Nel grafo unificato, il nodo canonico `cmd_timeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_timeline` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 198 che coinvolge il nodo canonico `cmd_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 198 che coinvolge il nodo canonico `cmd_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 198 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 198 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 198 che coinvolge il nodo canonico `cmd_timeline()` è `L444`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 198 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 198 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 198 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, l'arco 198 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo unificato, l'arco 198 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=199 role=outgoing -->
Nel grafo unificato, il nodo canonico `cmd_timeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_timeline` all'ID `Request-Dataset-Generator::timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `constant_rate()`. Nel grafo unificato, il tipo dichiarato dell'arco 199 che coinvolge il nodo canonico `cmd_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 199 che coinvolge il nodo canonico `cmd_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 199 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 199 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 199 che coinvolge il nodo canonico `cmd_timeline()` è `L442`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 199 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 199 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 199 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, l'arco 199 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo unificato, l'arco 199 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `cmd_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=200 role=outgoing -->
Nel grafo unificato, il nodo canonico `cmd_timeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `daily_clinical_profile()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_timeline` all'ID `Request-Dataset-Generator::timeline_generator_daily_clinical_profile`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `daily_clinical_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 200 che coinvolge il nodo canonico `cmd_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 200 che coinvolge il nodo canonico `cmd_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 200 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 200 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 200 che coinvolge il nodo canonico `cmd_timeline()` è `L436`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 200 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 200 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 200 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, l'arco 200 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo unificato, l'arco 200 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_daily_clinical_profile` per la relazione che coinvolge il nodo canonico `cmd_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=201 role=outgoing -->
Nel grafo unificato, il nodo canonico `cmd_timeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `step_profile()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_timeline` all'ID `Request-Dataset-Generator::timeline_generator_step_profile`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `step_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 201 che coinvolge il nodo canonico `cmd_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 201 che coinvolge il nodo canonico `cmd_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 201 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 201 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 201 che coinvolge il nodo canonico `cmd_timeline()` è `L434`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 201 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 201 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 201 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, l'arco 201 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo unificato, l'arco 201 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_step_profile` per la relazione che coinvolge il nodo canonico `cmd_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=202 role=outgoing -->
Nel grafo unificato, il nodo canonico `cmd_timeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `timeline_stats()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_timeline` all'ID `Request-Dataset-Generator::timeline_generator_timeline_stats`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `timeline_stats()`. Nel grafo unificato, il tipo dichiarato dell'arco 202 che coinvolge il nodo canonico `cmd_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 202 che coinvolge il nodo canonico `cmd_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 202 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 202 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 202 che coinvolge il nodo canonico `cmd_timeline()` è `L459`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 202 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 202 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 202 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, l'arco 202 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo unificato, l'arco 202 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_timeline_stats` per la relazione che coinvolge il nodo canonico `cmd_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=203 role=outgoing -->
Nel grafo unificato, il nodo canonico `cmd_timeline()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Genera una timeline temporizzata di flussi RVE.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_timeline` all'ID `Request-Dataset-Generator::main_rationale_409`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` costituisce una motivazione per `Genera una timeline temporizzata di flussi RVE.`. Nel grafo unificato, il tipo dichiarato dell'arco 203 che coinvolge il nodo canonico `cmd_timeline()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 203 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 203 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 203 che coinvolge il nodo canonico `cmd_timeline()` è `L409`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 203 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 203 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 203 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, l'arco 203 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo unificato, l'arco 203 registra come `target` l'identificatore `Request-Dataset-Generator::main_rationale_409` per la relazione che coinvolge il nodo canonico `cmd_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=129 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_timeline()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_cmd_timeline`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 129 che coinvolge il nodo canonico `cmd_timeline()` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 129 che coinvolge il nodo canonico `cmd_timeline()` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 129 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 129 che coinvolge il nodo canonico `cmd_timeline()` è `L583`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 129 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 129 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 129 che coinvolge il nodo canonico `cmd_timeline()` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 129 che coinvolge il nodo canonico `cmd_timeline()` è `0.5`. Nel grafo unificato, l'arco 129 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo unificato, l'arco 129 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=157 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_cfg_section()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cfg_section` all'ID `Request-Dataset-Generator::main_cmd_timeline`. La relazione tecnica esplicita è la seguente: `_cfg_section()` chiama `cmd_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 157 che coinvolge il nodo canonico `cmd_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 157 che coinvolge il nodo canonico `cmd_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 157 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 157 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 157 che coinvolge il nodo canonico `cmd_timeline()` è `L411`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 157 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 157 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 157 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, l'arco 157 registra come `source` l'identificatore `Request-Dataset-Generator::main_cfg_section` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo unificato, l'arco 157 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=169 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_timeline()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Namespace`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::namespace` all'ID `Request-Dataset-Generator::main_cmd_timeline`. La relazione tecnica esplicita è la seguente: `Namespace` fa riferimento a `cmd_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 169 che coinvolge il nodo canonico `cmd_timeline()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 169 che coinvolge il nodo canonico `cmd_timeline()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 169 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 169 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 169 che coinvolge il nodo canonico `cmd_timeline()` è `L408`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 169 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 169 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 169 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, l'arco 169 registra come `source` l'identificatore `Request-Dataset-Generator::namespace` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo unificato, l'arco 169 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=177 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_annotate_dataset_flows()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_annotate_dataset_flows` all'ID `Request-Dataset-Generator::main_cmd_timeline`. La relazione tecnica esplicita è la seguente: `_annotate_dataset_flows()` chiama `cmd_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 177 che coinvolge il nodo canonico `cmd_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 177 che coinvolge il nodo canonico `cmd_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 177 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 177 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 177 che coinvolge il nodo canonico `cmd_timeline()` è `L426`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 177 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 177 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 177 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, l'arco 177 registra come `source` l'identificatore `Request-Dataset-Generator::main_annotate_dataset_flows` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo unificato, l'arco 177 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=185 role=incoming -->
Nel grafo unificato, il nodo canonico `cmd_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_write_output()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_write_output` all'ID `Request-Dataset-Generator::main_cmd_timeline`. La relazione tecnica esplicita è la seguente: `_write_output()` chiama `cmd_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 185 che coinvolge il nodo canonico `cmd_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 185 che coinvolge il nodo canonico `cmd_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 185 che coinvolge il nodo canonico `cmd_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 185 che coinvolge il nodo canonico `cmd_timeline()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 185 che coinvolge il nodo canonico `cmd_timeline()` è `L470`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 185 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 185 che coinvolge il nodo canonico `cmd_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 185 che coinvolge il nodo canonico `cmd_timeline()` è `1.0`. Nel grafo unificato, l'arco 185 registra come `source` l'identificatore `Request-Dataset-Generator::main_write_output` per la relazione che coinvolge il nodo canonico `cmd_timeline()`. Nel grafo unificato, l'arco 185 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `cmd_timeline()`.

### constant_rate()

<!-- GRAPH_NODE graph=codice index=322 id=timeline_generator_constant_rate -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `constant_rate()` è `constant_rate()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `constant_rate()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `constant_rate()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `constant_rate()` è `L22`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `constant_rate()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `constant_rate()` possiede l'identificatore esatto `timeline_generator_constant_rate`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `constant_rate()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `constant_rate()` è `constant_rate()`.
Nel grafo del codice RDG, il nodo canonico `constant_rate()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=144 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `main.py` importa `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 144 che coinvolge il nodo canonico `constant_rate()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 144 che coinvolge il nodo canonico `constant_rate()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 144 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 144 che coinvolge il nodo canonico `constant_rate()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 144 che coinvolge il nodo canonico `constant_rate()` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 144 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 144 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo del codice RDG, l'arco 144 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo del codice RDG, l'arco 144 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 144 che coinvolge il nodo canonico `constant_rate()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=191 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_execute` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 191 che coinvolge il nodo canonico `constant_rate()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 191 che coinvolge il nodo canonico `constant_rate()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 191 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 191 che coinvolge il nodo canonico `constant_rate()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 191 che coinvolge il nodo canonico `constant_rate()` è `L353`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 191 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 191 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo del codice RDG, l'arco 191 registra come `source` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo del codice RDG, l'arco 191 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 191 che coinvolge il nodo canonico `constant_rate()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=199 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_timeline` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 199 che coinvolge il nodo canonico `constant_rate()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 199 che coinvolge il nodo canonico `constant_rate()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 199 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 199 che coinvolge il nodo canonico `constant_rate()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 199 che coinvolge il nodo canonico `constant_rate()` è `L442`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 199 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 199 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo del codice RDG, l'arco 199 registra come `source` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo del codice RDG, l'arco 199 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 199 che coinvolge il nodo canonico `constant_rate()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=587 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 587 che coinvolge il nodo canonico `constant_rate()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 587 che coinvolge il nodo canonico `constant_rate()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 587 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 587 che coinvolge il nodo canonico `constant_rate()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 587 che coinvolge il nodo canonico `constant_rate()` è `L13`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 587 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 587 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo del codice RDG, l'arco 587 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo del codice RDG, l'arco 587 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 587 che coinvolge il nodo canonico `constant_rate()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=626 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `.test_framework_log_is_sufficient_for_normalized_dataset()` chiama `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 626 che coinvolge il nodo canonico `constant_rate()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 626 che coinvolge il nodo canonico `constant_rate()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 626 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 626 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 626 che coinvolge il nodo canonico `constant_rate()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 626 che coinvolge il nodo canonico `constant_rate()` è `L615`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 626 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 626 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo del codice RDG, l'arco 626 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo del codice RDG, l'arco 626 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`.
<!-- GRAPH_EDGE graph=codice index=629 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `.test_error_injection_produces_measurable_failures_and_skipped_steps()` chiama `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 629 che coinvolge il nodo canonico `constant_rate()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 629 che coinvolge il nodo canonico `constant_rate()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 629 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 629 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 629 che coinvolge il nodo canonico `constant_rate()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 629 che coinvolge il nodo canonico `constant_rate()` è `L673`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 629 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 629 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo del codice RDG, l'arco 629 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo del codice RDG, l'arco 629 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`.
<!-- GRAPH_EDGE graph=codice index=632 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_metric_engine_writes_csv_tables()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `.test_metric_engine_writes_csv_tables()` chiama `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 632 che coinvolge il nodo canonico `constant_rate()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 632 che coinvolge il nodo canonico `constant_rate()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 632 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 632 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 632 che coinvolge il nodo canonico `constant_rate()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 632 che coinvolge il nodo canonico `constant_rate()` è `L727`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 632 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 632 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo del codice RDG, l'arco 632 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo del codice RDG, l'arco 632 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`.
<!-- GRAPH_EDGE graph=codice index=637 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `.test_timeline_assigns_unique_flow_occurrence_ids()` chiama `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 637 che coinvolge il nodo canonico `constant_rate()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 637 che coinvolge il nodo canonico `constant_rate()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 637 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 637 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 637 che coinvolge il nodo canonico `constant_rate()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 637 che coinvolge il nodo canonico `constant_rate()` è `L783`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 637 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 637 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo del codice RDG, l'arco 637 registra come `source` l'identificatore `tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo del codice RDG, l'arco 637 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`.
<!-- GRAPH_EDGE graph=codice index=639 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_timeline_tags_burst_events()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `.test_timeline_tags_burst_events()` chiama `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 639 che coinvolge il nodo canonico `constant_rate()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 639 che coinvolge il nodo canonico `constant_rate()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 639 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 639 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 639 che coinvolge il nodo canonico `constant_rate()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 639 che coinvolge il nodo canonico `constant_rate()` è `L806`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 639 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 639 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo del codice RDG, l'arco 639 registra come `source` l'identificatore `tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo del codice RDG, l'arco 639 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`.
<!-- GRAPH_EDGE graph=codice index=643 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `timeline_generator` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 643 che coinvolge il nodo canonico `constant_rate()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 643 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 643 che coinvolge il nodo canonico `constant_rate()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 643 che coinvolge il nodo canonico `constant_rate()` è `L22`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 643 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 643 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo del codice RDG, l'arco 643 registra come `source` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo del codice RDG, l'arco 643 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 643 che coinvolge il nodo canonico `constant_rate()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=651 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_build_timeline` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `build_timeline()` chiama `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 651 che coinvolge il nodo canonico `constant_rate()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 651 che coinvolge il nodo canonico `constant_rate()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 651 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 651 che coinvolge il nodo canonico `constant_rate()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 651 che coinvolge il nodo canonico `constant_rate()` è `L219`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 651 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 651 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo del codice RDG, l'arco 651 registra come `source` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo del codice RDG, l'arco 651 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 651 che coinvolge il nodo canonico `constant_rate()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=652 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Rate costante: processo di Poisson omogeneo.`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_rationale_23` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `Rate costante: processo di Poisson omogeneo.` costituisce una motivazione per `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 652 che coinvolge il nodo canonico `constant_rate()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 652 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 652 che coinvolge il nodo canonico `constant_rate()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 652 che coinvolge il nodo canonico `constant_rate()` è `L23`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 652 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 652 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo del codice RDG, l'arco 652 registra come `source` l'identificatore `timeline_generator_rationale_23` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo del codice RDG, l'arco 652 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 652 che coinvolge il nodo canonico `constant_rate()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=322 id=Request-Dataset-Generator::timeline_generator_constant_rate -->
Nel grafo unificato, l'etichetta originale del nodo canonico `constant_rate()` è `constant_rate()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `constant_rate()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `constant_rate()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `constant_rate()` è `L22`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `constant_rate()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `constant_rate()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `constant_rate()` è `constant_rate()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `constant_rate()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `constant_rate()` è `timeline_generator_constant_rate`. Nel grafo unificato, il nodo canonico `constant_rate()` possiede l'identificatore esatto `Request-Dataset-Generator::timeline_generator_constant_rate`.
<!-- GRAPH_EDGE graph=unificata index=651 role=outgoing -->
Nel grafo unificato, il nodo canonico `constant_rate()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator_constant_rate` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `constant_rate()` chiama `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 651 che coinvolge il nodo canonico `constant_rate()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 651 che coinvolge il nodo canonico `constant_rate()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 651 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 651 che coinvolge il nodo canonico `constant_rate()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 651 che coinvolge il nodo canonico `constant_rate()` è `L219`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 651 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 651 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 651 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, l'arco 651 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo unificato, l'arco 651 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `constant_rate()`.
<!-- GRAPH_EDGE graph=unificata index=652 role=outgoing -->
Nel grafo unificato, il nodo canonico `constant_rate()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Rate costante: processo di Poisson omogeneo.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator_constant_rate` all'ID `Request-Dataset-Generator::timeline_generator_rationale_23`. La relazione tecnica esplicita è la seguente: `constant_rate()` costituisce una motivazione per `Rate costante: processo di Poisson omogeneo.`. Nel grafo unificato, il tipo dichiarato dell'arco 652 che coinvolge il nodo canonico `constant_rate()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 652 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 652 che coinvolge il nodo canonico `constant_rate()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 652 che coinvolge il nodo canonico `constant_rate()` è `L23`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 652 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 652 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 652 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, l'arco 652 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo unificato, l'arco 652 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_rationale_23` per la relazione che coinvolge il nodo canonico `constant_rate()`.
<!-- GRAPH_EDGE graph=unificata index=144 role=incoming -->
Nel grafo unificato, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `main.py` importa `constant_rate()`. Nel grafo unificato, il tipo dichiarato dell'arco 144 che coinvolge il nodo canonico `constant_rate()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 144 che coinvolge il nodo canonico `constant_rate()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 144 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 144 che coinvolge il nodo canonico `constant_rate()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 144 che coinvolge il nodo canonico `constant_rate()` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 144 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 144 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 144 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, l'arco 144 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo unificato, l'arco 144 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`.
<!-- GRAPH_EDGE graph=unificata index=191 role=incoming -->
Nel grafo unificato, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_execute` all'ID `Request-Dataset-Generator::timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `constant_rate()`. Nel grafo unificato, il tipo dichiarato dell'arco 191 che coinvolge il nodo canonico `constant_rate()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 191 che coinvolge il nodo canonico `constant_rate()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 191 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 191 che coinvolge il nodo canonico `constant_rate()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 191 che coinvolge il nodo canonico `constant_rate()` è `L353`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 191 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 191 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 191 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, l'arco 191 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo unificato, l'arco 191 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`.
<!-- GRAPH_EDGE graph=unificata index=199 role=incoming -->
Nel grafo unificato, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_timeline` all'ID `Request-Dataset-Generator::timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `constant_rate()`. Nel grafo unificato, il tipo dichiarato dell'arco 199 che coinvolge il nodo canonico `constant_rate()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 199 che coinvolge il nodo canonico `constant_rate()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 199 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 199 che coinvolge il nodo canonico `constant_rate()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 199 che coinvolge il nodo canonico `constant_rate()` è `L442`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 199 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 199 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 199 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, l'arco 199 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo unificato, l'arco 199 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`.
<!-- GRAPH_EDGE graph=unificata index=587 role=incoming -->
Nel grafo unificato, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `constant_rate()`. Nel grafo unificato, il tipo dichiarato dell'arco 587 che coinvolge il nodo canonico `constant_rate()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 587 che coinvolge il nodo canonico `constant_rate()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 587 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 587 che coinvolge il nodo canonico `constant_rate()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 587 che coinvolge il nodo canonico `constant_rate()` è `L13`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 587 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 587 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 587 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, l'arco 587 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo unificato, l'arco 587 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`.
<!-- GRAPH_EDGE graph=unificata index=626 role=incoming -->
Nel grafo unificato, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` all'ID `Request-Dataset-Generator::timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `.test_framework_log_is_sufficient_for_normalized_dataset()` chiama `constant_rate()`. Nel grafo unificato, il tipo dichiarato dell'arco 626 che coinvolge il nodo canonico `constant_rate()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 626 che coinvolge il nodo canonico `constant_rate()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 626 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 626 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 626 che coinvolge il nodo canonico `constant_rate()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 626 che coinvolge il nodo canonico `constant_rate()` è `L615`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 626 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 626 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo unificato, l'arco 626 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo unificato, l'arco 626 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`.
<!-- GRAPH_EDGE graph=unificata index=629 role=incoming -->
Nel grafo unificato, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` all'ID `Request-Dataset-Generator::timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `.test_error_injection_produces_measurable_failures_and_skipped_steps()` chiama `constant_rate()`. Nel grafo unificato, il tipo dichiarato dell'arco 629 che coinvolge il nodo canonico `constant_rate()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 629 che coinvolge il nodo canonico `constant_rate()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 629 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 629 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 629 che coinvolge il nodo canonico `constant_rate()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 629 che coinvolge il nodo canonico `constant_rate()` è `L673`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 629 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 629 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo unificato, l'arco 629 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo unificato, l'arco 629 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`.
<!-- GRAPH_EDGE graph=unificata index=632 role=incoming -->
Nel grafo unificato, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_metric_engine_writes_csv_tables()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` all'ID `Request-Dataset-Generator::timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `.test_metric_engine_writes_csv_tables()` chiama `constant_rate()`. Nel grafo unificato, il tipo dichiarato dell'arco 632 che coinvolge il nodo canonico `constant_rate()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 632 che coinvolge il nodo canonico `constant_rate()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 632 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 632 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 632 che coinvolge il nodo canonico `constant_rate()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 632 che coinvolge il nodo canonico `constant_rate()` è `L727`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 632 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 632 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo unificato, l'arco 632 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo unificato, l'arco 632 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`.
<!-- GRAPH_EDGE graph=unificata index=637 role=incoming -->
Nel grafo unificato, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` all'ID `Request-Dataset-Generator::timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `.test_timeline_assigns_unique_flow_occurrence_ids()` chiama `constant_rate()`. Nel grafo unificato, il tipo dichiarato dell'arco 637 che coinvolge il nodo canonico `constant_rate()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 637 che coinvolge il nodo canonico `constant_rate()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 637 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 637 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 637 che coinvolge il nodo canonico `constant_rate()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 637 che coinvolge il nodo canonico `constant_rate()` è `L783`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 637 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 637 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo unificato, l'arco 637 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo unificato, l'arco 637 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`.
<!-- GRAPH_EDGE graph=unificata index=639 role=incoming -->
Nel grafo unificato, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_timeline_tags_burst_events()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` all'ID `Request-Dataset-Generator::timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `.test_timeline_tags_burst_events()` chiama `constant_rate()`. Nel grafo unificato, il tipo dichiarato dell'arco 639 che coinvolge il nodo canonico `constant_rate()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 639 che coinvolge il nodo canonico `constant_rate()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 639 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 639 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 639 che coinvolge il nodo canonico `constant_rate()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 639 che coinvolge il nodo canonico `constant_rate()` è `L806`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 639 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 639 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo unificato, l'arco 639 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo unificato, l'arco 639 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`.
<!-- GRAPH_EDGE graph=unificata index=643 role=incoming -->
Nel grafo unificato, il nodo canonico `constant_rate()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator` all'ID `Request-Dataset-Generator::timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `constant_rate()`. Nel grafo unificato, il tipo dichiarato dell'arco 643 che coinvolge il nodo canonico `constant_rate()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 643 che coinvolge il nodo canonico `constant_rate()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 643 che coinvolge il nodo canonico `constant_rate()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 643 che coinvolge il nodo canonico `constant_rate()` è `L22`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 643 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 643 che coinvolge il nodo canonico `constant_rate()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 643 che coinvolge il nodo canonico `constant_rate()` è `1.0`. Nel grafo unificato, l'arco 643 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `constant_rate()`. Nel grafo unificato, l'arco 643 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `constant_rate()`.

### Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf

<!-- GRAPH_NODE graph=codice index=81 id=main_rationale_116 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `L116`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` possiede l'identificatore esatto `main_rationale_116`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `costruisce un argparse.namespace dai valori nel config json.      priorita: conf`.
<!-- GRAPH_EDGE graph=codice index=162 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `_build_namespace()`; l'orientamento registrato nel JSON va dall'ID `main_rationale_116` all'ID `main_build_namespace`. La relazione tecnica esplicita è la seguente: `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` costituisce una motivazione per `_build_namespace()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 162 che coinvolge il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 162 che coinvolge il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 162 che coinvolge il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 162 che coinvolge il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `L116`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 162 che coinvolge il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 162 che coinvolge il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `ast`. Nel grafo del codice RDG, l'arco 162 registra come `source` l'identificatore `main_rationale_116` per la relazione che coinvolge il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf`. Nel grafo del codice RDG, l'arco 162 registra come `target` l'identificatore `main_build_namespace` per la relazione che coinvolge il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 162 che coinvolge il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=81 id=Request-Dataset-Generator::main_rationale_116 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `L116`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `costruisce un argparse.namespace dai valori nel config json.      priorita: conf`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `main_rationale_116`. Nel grafo unificato, il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` possiede l'identificatore esatto `Request-Dataset-Generator::main_rationale_116`.
Nel grafo unificato, il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=162 role=incoming -->
Nel grafo unificato, il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `_build_namespace()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_build_namespace` all'ID `Request-Dataset-Generator::main_rationale_116`. La relazione tecnica esplicita è la seguente: `_build_namespace()` costituisce una motivazione per `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf`. Nel grafo unificato, il tipo dichiarato dell'arco 162 che coinvolge il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 162 che coinvolge il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 162 che coinvolge il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 162 che coinvolge il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `L116`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 162 che coinvolge il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 162 che coinvolge il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 162 che coinvolge il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf` è `1.0`. Nel grafo unificato, l'arco 162 registra come `source` l'identificatore `Request-Dataset-Generator::main_build_namespace` per la relazione che coinvolge il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf`. Nel grafo unificato, l'arco 162 registra come `target` l'identificatore `Request-Dataset-Generator::main_rationale_116` per la relazione che coinvolge il nodo canonico `Costruisce un argparse.Namespace dai valori nel config JSON.      Priorità: conf`.

### daily_clinical_profile()

<!-- GRAPH_NODE graph=codice index=323 id=timeline_generator_daily_clinical_profile -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `daily_clinical_profile()` è `daily_clinical_profile()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `daily_clinical_profile()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `daily_clinical_profile()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `daily_clinical_profile()` è `L29`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `daily_clinical_profile()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `daily_clinical_profile()` possiede l'identificatore esatto `timeline_generator_daily_clinical_profile`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `daily_clinical_profile()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `daily_clinical_profile()` è `daily_clinical_profile()`.
Nel grafo del codice RDG, il nodo canonico `daily_clinical_profile()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=145 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `daily_clinical_profile()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `timeline_generator_daily_clinical_profile`. La relazione tecnica esplicita è la seguente: `main.py` importa `daily_clinical_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 145 che coinvolge il nodo canonico `daily_clinical_profile()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 145 che coinvolge il nodo canonico `daily_clinical_profile()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 145 che coinvolge il nodo canonico `daily_clinical_profile()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 145 che coinvolge il nodo canonico `daily_clinical_profile()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 145 che coinvolge il nodo canonico `daily_clinical_profile()` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 145 che coinvolge il nodo canonico `daily_clinical_profile()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 145 che coinvolge il nodo canonico `daily_clinical_profile()` è `ast`. Nel grafo del codice RDG, l'arco 145 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `daily_clinical_profile()`. Nel grafo del codice RDG, l'arco 145 registra come `target` l'identificatore `timeline_generator_daily_clinical_profile` per la relazione che coinvolge il nodo canonico `daily_clinical_profile()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 145 che coinvolge il nodo canonico `daily_clinical_profile()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=192 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `daily_clinical_profile()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_execute` all'ID `timeline_generator_daily_clinical_profile`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `daily_clinical_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 192 che coinvolge il nodo canonico `daily_clinical_profile()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 192 che coinvolge il nodo canonico `daily_clinical_profile()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 192 che coinvolge il nodo canonico `daily_clinical_profile()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 192 che coinvolge il nodo canonico `daily_clinical_profile()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 192 che coinvolge il nodo canonico `daily_clinical_profile()` è `L347`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 192 che coinvolge il nodo canonico `daily_clinical_profile()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 192 che coinvolge il nodo canonico `daily_clinical_profile()` è `ast`. Nel grafo del codice RDG, l'arco 192 registra come `source` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `daily_clinical_profile()`. Nel grafo del codice RDG, l'arco 192 registra come `target` l'identificatore `timeline_generator_daily_clinical_profile` per la relazione che coinvolge il nodo canonico `daily_clinical_profile()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 192 che coinvolge il nodo canonico `daily_clinical_profile()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=200 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `daily_clinical_profile()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_timeline` all'ID `timeline_generator_daily_clinical_profile`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `daily_clinical_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 200 che coinvolge il nodo canonico `daily_clinical_profile()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 200 che coinvolge il nodo canonico `daily_clinical_profile()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 200 che coinvolge il nodo canonico `daily_clinical_profile()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 200 che coinvolge il nodo canonico `daily_clinical_profile()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 200 che coinvolge il nodo canonico `daily_clinical_profile()` è `L436`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 200 che coinvolge il nodo canonico `daily_clinical_profile()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 200 che coinvolge il nodo canonico `daily_clinical_profile()` è `ast`. Nel grafo del codice RDG, l'arco 200 registra come `source` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `daily_clinical_profile()`. Nel grafo del codice RDG, l'arco 200 registra come `target` l'identificatore `timeline_generator_daily_clinical_profile` per la relazione che coinvolge il nodo canonico `daily_clinical_profile()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 200 che coinvolge il nodo canonico `daily_clinical_profile()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=644 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `daily_clinical_profile()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `timeline_generator` all'ID `timeline_generator_daily_clinical_profile`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `daily_clinical_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 644 che coinvolge il nodo canonico `daily_clinical_profile()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 644 che coinvolge il nodo canonico `daily_clinical_profile()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 644 che coinvolge il nodo canonico `daily_clinical_profile()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 644 che coinvolge il nodo canonico `daily_clinical_profile()` è `L29`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 644 che coinvolge il nodo canonico `daily_clinical_profile()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 644 che coinvolge il nodo canonico `daily_clinical_profile()` è `ast`. Nel grafo del codice RDG, l'arco 644 registra come `source` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `daily_clinical_profile()`. Nel grafo del codice RDG, l'arco 644 registra come `target` l'identificatore `timeline_generator_daily_clinical_profile` per la relazione che coinvolge il nodo canonico `daily_clinical_profile()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 644 che coinvolge il nodo canonico `daily_clinical_profile()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=653 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `daily_clinical_profile()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_rationale_34` all'ID `timeline_generator_daily_clinical_profile`. La relazione tecnica esplicita è la seguente: `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` costituisce una motivazione per `daily_clinical_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 653 che coinvolge il nodo canonico `daily_clinical_profile()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 653 che coinvolge il nodo canonico `daily_clinical_profile()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 653 che coinvolge il nodo canonico `daily_clinical_profile()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 653 che coinvolge il nodo canonico `daily_clinical_profile()` è `L34`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 653 che coinvolge il nodo canonico `daily_clinical_profile()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 653 che coinvolge il nodo canonico `daily_clinical_profile()` è `ast`. Nel grafo del codice RDG, l'arco 653 registra come `source` l'identificatore `timeline_generator_rationale_34` per la relazione che coinvolge il nodo canonico `daily_clinical_profile()`. Nel grafo del codice RDG, l'arco 653 registra come `target` l'identificatore `timeline_generator_daily_clinical_profile` per la relazione che coinvolge il nodo canonico `daily_clinical_profile()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 653 che coinvolge il nodo canonico `daily_clinical_profile()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=323 id=Request-Dataset-Generator::timeline_generator_daily_clinical_profile -->
Nel grafo unificato, l'etichetta originale del nodo canonico `daily_clinical_profile()` è `daily_clinical_profile()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `daily_clinical_profile()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `daily_clinical_profile()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `daily_clinical_profile()` è `L29`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `daily_clinical_profile()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `daily_clinical_profile()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `daily_clinical_profile()` è `daily_clinical_profile()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `daily_clinical_profile()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `daily_clinical_profile()` è `timeline_generator_daily_clinical_profile`. Nel grafo unificato, il nodo canonico `daily_clinical_profile()` possiede l'identificatore esatto `Request-Dataset-Generator::timeline_generator_daily_clinical_profile`.
<!-- GRAPH_EDGE graph=unificata index=653 role=outgoing -->
Nel grafo unificato, il nodo canonico `daily_clinical_profile()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator_daily_clinical_profile` all'ID `Request-Dataset-Generator::timeline_generator_rationale_34`. La relazione tecnica esplicita è la seguente: `daily_clinical_profile()` costituisce una motivazione per `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas`. Nel grafo unificato, il tipo dichiarato dell'arco 653 che coinvolge il nodo canonico `daily_clinical_profile()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 653 che coinvolge il nodo canonico `daily_clinical_profile()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 653 che coinvolge il nodo canonico `daily_clinical_profile()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 653 che coinvolge il nodo canonico `daily_clinical_profile()` è `L34`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 653 che coinvolge il nodo canonico `daily_clinical_profile()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 653 che coinvolge il nodo canonico `daily_clinical_profile()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 653 che coinvolge il nodo canonico `daily_clinical_profile()` è `1.0`. Nel grafo unificato, l'arco 653 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator_daily_clinical_profile` per la relazione che coinvolge il nodo canonico `daily_clinical_profile()`. Nel grafo unificato, l'arco 653 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_rationale_34` per la relazione che coinvolge il nodo canonico `daily_clinical_profile()`.
<!-- GRAPH_EDGE graph=unificata index=145 role=incoming -->
Nel grafo unificato, il nodo canonico `daily_clinical_profile()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::timeline_generator_daily_clinical_profile`. La relazione tecnica esplicita è la seguente: `main.py` importa `daily_clinical_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 145 che coinvolge il nodo canonico `daily_clinical_profile()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 145 che coinvolge il nodo canonico `daily_clinical_profile()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 145 che coinvolge il nodo canonico `daily_clinical_profile()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 145 che coinvolge il nodo canonico `daily_clinical_profile()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 145 che coinvolge il nodo canonico `daily_clinical_profile()` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 145 che coinvolge il nodo canonico `daily_clinical_profile()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 145 che coinvolge il nodo canonico `daily_clinical_profile()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 145 che coinvolge il nodo canonico `daily_clinical_profile()` è `1.0`. Nel grafo unificato, l'arco 145 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `daily_clinical_profile()`. Nel grafo unificato, l'arco 145 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_daily_clinical_profile` per la relazione che coinvolge il nodo canonico `daily_clinical_profile()`.
<!-- GRAPH_EDGE graph=unificata index=192 role=incoming -->
Nel grafo unificato, il nodo canonico `daily_clinical_profile()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_execute` all'ID `Request-Dataset-Generator::timeline_generator_daily_clinical_profile`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `daily_clinical_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 192 che coinvolge il nodo canonico `daily_clinical_profile()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 192 che coinvolge il nodo canonico `daily_clinical_profile()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 192 che coinvolge il nodo canonico `daily_clinical_profile()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 192 che coinvolge il nodo canonico `daily_clinical_profile()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 192 che coinvolge il nodo canonico `daily_clinical_profile()` è `L347`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 192 che coinvolge il nodo canonico `daily_clinical_profile()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 192 che coinvolge il nodo canonico `daily_clinical_profile()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 192 che coinvolge il nodo canonico `daily_clinical_profile()` è `1.0`. Nel grafo unificato, l'arco 192 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `daily_clinical_profile()`. Nel grafo unificato, l'arco 192 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_daily_clinical_profile` per la relazione che coinvolge il nodo canonico `daily_clinical_profile()`.
<!-- GRAPH_EDGE graph=unificata index=200 role=incoming -->
Nel grafo unificato, il nodo canonico `daily_clinical_profile()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_timeline` all'ID `Request-Dataset-Generator::timeline_generator_daily_clinical_profile`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `daily_clinical_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 200 che coinvolge il nodo canonico `daily_clinical_profile()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 200 che coinvolge il nodo canonico `daily_clinical_profile()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 200 che coinvolge il nodo canonico `daily_clinical_profile()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 200 che coinvolge il nodo canonico `daily_clinical_profile()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 200 che coinvolge il nodo canonico `daily_clinical_profile()` è `L436`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 200 che coinvolge il nodo canonico `daily_clinical_profile()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 200 che coinvolge il nodo canonico `daily_clinical_profile()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 200 che coinvolge il nodo canonico `daily_clinical_profile()` è `1.0`. Nel grafo unificato, l'arco 200 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `daily_clinical_profile()`. Nel grafo unificato, l'arco 200 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_daily_clinical_profile` per la relazione che coinvolge il nodo canonico `daily_clinical_profile()`.
<!-- GRAPH_EDGE graph=unificata index=644 role=incoming -->
Nel grafo unificato, il nodo canonico `daily_clinical_profile()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator` all'ID `Request-Dataset-Generator::timeline_generator_daily_clinical_profile`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `daily_clinical_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 644 che coinvolge il nodo canonico `daily_clinical_profile()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 644 che coinvolge il nodo canonico `daily_clinical_profile()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 644 che coinvolge il nodo canonico `daily_clinical_profile()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 644 che coinvolge il nodo canonico `daily_clinical_profile()` è `L29`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 644 che coinvolge il nodo canonico `daily_clinical_profile()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 644 che coinvolge il nodo canonico `daily_clinical_profile()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 644 che coinvolge il nodo canonico `daily_clinical_profile()` è `1.0`. Nel grafo unificato, l'arco 644 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `daily_clinical_profile()`. Nel grafo unificato, l'arco 644 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_daily_clinical_profile` per la relazione che coinvolge il nodo canonico `daily_clinical_profile()`.

### EngineSmokeTest

<!-- GRAPH_NODE graph=codice index=309 id=tests_test_core_enginesmoketest -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `EngineSmokeTest` è `EngineSmokeTest`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `EngineSmokeTest` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `EngineSmokeTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `EngineSmokeTest` è `L548`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `EngineSmokeTest` vale `ast`. Nel grafo del codice RDG, il nodo canonico `EngineSmokeTest` possiede l'identificatore esatto `tests_test_core_enginesmoketest`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `EngineSmokeTest` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `EngineSmokeTest` è `enginesmoketest`.
<!-- GRAPH_EDGE graph=codice index=434 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `EngineSmokeTest` è la sorgente `source` della relazione `uses` verso il nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `EngineSmokeTest` usa `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 434 che coinvolge il nodo canonico `EngineSmokeTest` è `uses`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 434 che coinvolge il nodo canonico `EngineSmokeTest` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 434 che coinvolge il nodo canonico `EngineSmokeTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 434 che coinvolge il nodo canonico `EngineSmokeTest` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 434 che coinvolge il nodo canonico `EngineSmokeTest` è `0.8`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 434 che coinvolge il nodo canonico `EngineSmokeTest` è `ast`. Nel grafo del codice RDG, l'arco 434 registra come `source` l'identificatore `tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`. Nel grafo del codice RDG, l'arco 434 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 434 che coinvolge il nodo canonico `EngineSmokeTest` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=620 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `EngineSmokeTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_dataset_to_engine_mock_immediate()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest` all'ID `tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate`. La relazione tecnica esplicita è la seguente: `EngineSmokeTest` registra come proprio metodo `.test_dataset_to_engine_mock_immediate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 620 che coinvolge il nodo canonico `EngineSmokeTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 620 che coinvolge il nodo canonico `EngineSmokeTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 620 che coinvolge il nodo canonico `EngineSmokeTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 620 che coinvolge il nodo canonico `EngineSmokeTest` è `L549`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 620 che coinvolge il nodo canonico `EngineSmokeTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 620 che coinvolge il nodo canonico `EngineSmokeTest` è `ast`. Nel grafo del codice RDG, l'arco 620 registra come `source` l'identificatore `tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`. Nel grafo del codice RDG, l'arco 620 registra come `target` l'identificatore `tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 620 che coinvolge il nodo canonico `EngineSmokeTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=621 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `EngineSmokeTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest` all'ID `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps`. La relazione tecnica esplicita è la seguente: `EngineSmokeTest` registra come proprio metodo `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 621 che coinvolge il nodo canonico `EngineSmokeTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 621 che coinvolge il nodo canonico `EngineSmokeTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 621 che coinvolge il nodo canonico `EngineSmokeTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 621 che coinvolge il nodo canonico `EngineSmokeTest` è `L650`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 621 che coinvolge il nodo canonico `EngineSmokeTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 621 che coinvolge il nodo canonico `EngineSmokeTest` è `ast`. Nel grafo del codice RDG, l'arco 621 registra come `source` l'identificatore `tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`. Nel grafo del codice RDG, l'arco 621 registra come `target` l'identificatore `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 621 che coinvolge il nodo canonico `EngineSmokeTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=622 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `EngineSmokeTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest` all'ID `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset`. La relazione tecnica esplicita è la seguente: `EngineSmokeTest` registra come proprio metodo `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 622 che coinvolge il nodo canonico `EngineSmokeTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 622 che coinvolge il nodo canonico `EngineSmokeTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 622 che coinvolge il nodo canonico `EngineSmokeTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 622 che coinvolge il nodo canonico `EngineSmokeTest` è `L599`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 622 che coinvolge il nodo canonico `EngineSmokeTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 622 che coinvolge il nodo canonico `EngineSmokeTest` è `ast`. Nel grafo del codice RDG, l'arco 622 registra come `source` l'identificatore `tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`. Nel grafo del codice RDG, l'arco 622 registra come `target` l'identificatore `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 622 che coinvolge il nodo canonico `EngineSmokeTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=623 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `EngineSmokeTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_metric_engine_writes_csv_tables()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest` all'ID `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables`. La relazione tecnica esplicita è la seguente: `EngineSmokeTest` registra come proprio metodo `.test_metric_engine_writes_csv_tables()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 623 che coinvolge il nodo canonico `EngineSmokeTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 623 che coinvolge il nodo canonico `EngineSmokeTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 623 che coinvolge il nodo canonico `EngineSmokeTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 623 che coinvolge il nodo canonico `EngineSmokeTest` è `L711`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 623 che coinvolge il nodo canonico `EngineSmokeTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 623 che coinvolge il nodo canonico `EngineSmokeTest` è `ast`. Nel grafo del codice RDG, l'arco 623 registra come `source` l'identificatore `tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`. Nel grafo del codice RDG, l'arco 623 registra come `target` l'identificatore `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 623 che coinvolge il nodo canonico `EngineSmokeTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=579 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `EngineSmokeTest` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `tests_test_core_enginesmoketest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `EngineSmokeTest`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 579 che coinvolge il nodo canonico `EngineSmokeTest` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 579 che coinvolge il nodo canonico `EngineSmokeTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 579 che coinvolge il nodo canonico `EngineSmokeTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 579 che coinvolge il nodo canonico `EngineSmokeTest` è `L548`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 579 che coinvolge il nodo canonico `EngineSmokeTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 579 che coinvolge il nodo canonico `EngineSmokeTest` è `ast`. Nel grafo del codice RDG, l'arco 579 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`. Nel grafo del codice RDG, l'arco 579 registra come `target` l'identificatore `tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 579 che coinvolge il nodo canonico `EngineSmokeTest` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=309 id=Request-Dataset-Generator::tests_test_core_enginesmoketest -->
Nel grafo unificato, l'etichetta originale del nodo canonico `EngineSmokeTest` è `EngineSmokeTest`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `EngineSmokeTest` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `EngineSmokeTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `EngineSmokeTest` è `L548`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `EngineSmokeTest` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `EngineSmokeTest` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `EngineSmokeTest` è `enginesmoketest`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `EngineSmokeTest` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `EngineSmokeTest` è `tests_test_core_enginesmoketest`. Nel grafo unificato, il nodo canonico `EngineSmokeTest` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_enginesmoketest`.
<!-- GRAPH_EDGE graph=unificata index=620 role=outgoing -->
Nel grafo unificato, il nodo canonico `EngineSmokeTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_dataset_to_engine_mock_immediate()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate`. La relazione tecnica esplicita è la seguente: `EngineSmokeTest` registra come proprio metodo `.test_dataset_to_engine_mock_immediate()`. Nel grafo unificato, il tipo dichiarato dell'arco 620 che coinvolge il nodo canonico `EngineSmokeTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 620 che coinvolge il nodo canonico `EngineSmokeTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 620 che coinvolge il nodo canonico `EngineSmokeTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 620 che coinvolge il nodo canonico `EngineSmokeTest` è `L549`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 620 che coinvolge il nodo canonico `EngineSmokeTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 620 che coinvolge il nodo canonico `EngineSmokeTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 620 che coinvolge il nodo canonico `EngineSmokeTest` è `1.0`. Nel grafo unificato, l'arco 620 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`. Nel grafo unificato, l'arco 620 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`.
<!-- GRAPH_EDGE graph=unificata index=621 role=outgoing -->
Nel grafo unificato, il nodo canonico `EngineSmokeTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps`. La relazione tecnica esplicita è la seguente: `EngineSmokeTest` registra come proprio metodo `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo unificato, il tipo dichiarato dell'arco 621 che coinvolge il nodo canonico `EngineSmokeTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 621 che coinvolge il nodo canonico `EngineSmokeTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 621 che coinvolge il nodo canonico `EngineSmokeTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 621 che coinvolge il nodo canonico `EngineSmokeTest` è `L650`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 621 che coinvolge il nodo canonico `EngineSmokeTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 621 che coinvolge il nodo canonico `EngineSmokeTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 621 che coinvolge il nodo canonico `EngineSmokeTest` è `1.0`. Nel grafo unificato, l'arco 621 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`. Nel grafo unificato, l'arco 621 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`.
<!-- GRAPH_EDGE graph=unificata index=622 role=outgoing -->
Nel grafo unificato, il nodo canonico `EngineSmokeTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset`. La relazione tecnica esplicita è la seguente: `EngineSmokeTest` registra come proprio metodo `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo unificato, il tipo dichiarato dell'arco 622 che coinvolge il nodo canonico `EngineSmokeTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 622 che coinvolge il nodo canonico `EngineSmokeTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 622 che coinvolge il nodo canonico `EngineSmokeTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 622 che coinvolge il nodo canonico `EngineSmokeTest` è `L599`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 622 che coinvolge il nodo canonico `EngineSmokeTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 622 che coinvolge il nodo canonico `EngineSmokeTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 622 che coinvolge il nodo canonico `EngineSmokeTest` è `1.0`. Nel grafo unificato, l'arco 622 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`. Nel grafo unificato, l'arco 622 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`.
<!-- GRAPH_EDGE graph=unificata index=623 role=outgoing -->
Nel grafo unificato, il nodo canonico `EngineSmokeTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_metric_engine_writes_csv_tables()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables`. La relazione tecnica esplicita è la seguente: `EngineSmokeTest` registra come proprio metodo `.test_metric_engine_writes_csv_tables()`. Nel grafo unificato, il tipo dichiarato dell'arco 623 che coinvolge il nodo canonico `EngineSmokeTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 623 che coinvolge il nodo canonico `EngineSmokeTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 623 che coinvolge il nodo canonico `EngineSmokeTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 623 che coinvolge il nodo canonico `EngineSmokeTest` è `L711`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 623 che coinvolge il nodo canonico `EngineSmokeTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 623 che coinvolge il nodo canonico `EngineSmokeTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 623 che coinvolge il nodo canonico `EngineSmokeTest` è `1.0`. Nel grafo unificato, l'arco 623 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`. Nel grafo unificato, l'arco 623 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`.
<!-- GRAPH_EDGE graph=unificata index=434 role=incoming -->
Nel grafo unificato, il nodo canonico `EngineSmokeTest` è il destinatario `target` della relazione `uses` proveniente dal nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` usa `EngineSmokeTest`. Nel grafo unificato, il tipo dichiarato dell'arco 434 che coinvolge il nodo canonico `EngineSmokeTest` è `uses`. Nel grafo unificato, la confidenza categoriale dell'arco 434 che coinvolge il nodo canonico `EngineSmokeTest` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 434 che coinvolge il nodo canonico `EngineSmokeTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 434 che coinvolge il nodo canonico `EngineSmokeTest` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 434 che coinvolge il nodo canonico `EngineSmokeTest` è `0.8`. Nel grafo unificato, la provenienza `_origin` dell'arco 434 che coinvolge il nodo canonico `EngineSmokeTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 434 che coinvolge il nodo canonico `EngineSmokeTest` è `0.5`. Nel grafo unificato, l'arco 434 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`. Nel grafo unificato, l'arco 434 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`.
<!-- GRAPH_EDGE graph=unificata index=579 role=incoming -->
Nel grafo unificato, il nodo canonico `EngineSmokeTest` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `EngineSmokeTest`. Nel grafo unificato, il tipo dichiarato dell'arco 579 che coinvolge il nodo canonico `EngineSmokeTest` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 579 che coinvolge il nodo canonico `EngineSmokeTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 579 che coinvolge il nodo canonico `EngineSmokeTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 579 che coinvolge il nodo canonico `EngineSmokeTest` è `L548`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 579 che coinvolge il nodo canonico `EngineSmokeTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 579 che coinvolge il nodo canonico `EngineSmokeTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 579 che coinvolge il nodo canonico `EngineSmokeTest` è `1.0`. Nel grafo unificato, l'arco 579 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`. Nel grafo unificato, l'arco 579 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `EngineSmokeTest`.

### Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re

<!-- GRAPH_NODE graph=codice index=409 id=traffic_exec_engine_rationale_976 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `L976`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` possiede l'identificatore esatto `traffic_exec_engine_rationale_976`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `esegue tutti i flussi della timeline rispettando i timestamp schedulati.      re`.
<!-- GRAPH_EDGE graph=codice index=812 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_976` all'ID `traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` costituisce una motivazione per `run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 812 che coinvolge il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 812 che coinvolge il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 812 che coinvolge il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 812 che coinvolge il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `L976`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 812 che coinvolge il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 812 che coinvolge il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `ast`. Nel grafo del codice RDG, l'arco 812 registra come `source` l'identificatore `traffic_exec_engine_rationale_976` per la relazione che coinvolge il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re`. Nel grafo del codice RDG, l'arco 812 registra come `target` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 812 che coinvolge il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=409 id=Request-Dataset-Generator::traffic_exec_engine_rationale_976 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `L976`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `esegue tutti i flussi della timeline rispettando i timestamp schedulati.      re`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `traffic_exec_engine_rationale_976`. Nel grafo unificato, il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_rationale_976`.
Nel grafo unificato, il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=812 role=incoming -->
Nel grafo unificato, il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_run` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_976`. La relazione tecnica esplicita è la seguente: `run()` costituisce una motivazione per `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re`. Nel grafo unificato, il tipo dichiarato dell'arco 812 che coinvolge il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 812 che coinvolge il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 812 che coinvolge il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 812 che coinvolge il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `L976`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 812 che coinvolge il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 812 che coinvolge il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 812 che coinvolge il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` è `1.0`. Nel grafo unificato, l'arco 812 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re`. Nel grafo unificato, l'arco 812 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_976` per la relazione che coinvolge il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re`.

### Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l&#x27;algor

<!-- GRAPH_NODE graph=codice index=334 id=timeline_generator_rationale_88 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `L88`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` possiede l'identificatore esatto `timeline_generator_rationale_88`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `genera tempi di arrivo con processo di poisson non stazionario.      usa l'algor`.
<!-- GRAPH_EDGE graph=codice index=657 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `generate_arrivals()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_rationale_88` all'ID `timeline_generator_generate_arrivals`. La relazione tecnica esplicita è la seguente: `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` costituisce una motivazione per `generate_arrivals()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 657 che coinvolge il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 657 che coinvolge il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 657 che coinvolge il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 657 che coinvolge il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `L88`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 657 che coinvolge il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 657 che coinvolge il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `ast`. Nel grafo del codice RDG, l'arco 657 registra come `source` l'identificatore `timeline_generator_rationale_88` per la relazione che coinvolge il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor`. Nel grafo del codice RDG, l'arco 657 registra come `target` l'identificatore `timeline_generator_generate_arrivals` per la relazione che coinvolge il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 657 che coinvolge il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=334 id=Request-Dataset-Generator::timeline_generator_rationale_88 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `L88`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `genera tempi di arrivo con processo di poisson non stazionario.      usa l'algor`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `timeline_generator_rationale_88`. Nel grafo unificato, il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` possiede l'identificatore esatto `Request-Dataset-Generator::timeline_generator_rationale_88`.
Nel grafo unificato, il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=657 role=incoming -->
Nel grafo unificato, il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `generate_arrivals()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator_generate_arrivals` all'ID `Request-Dataset-Generator::timeline_generator_rationale_88`. La relazione tecnica esplicita è la seguente: `generate_arrivals()` costituisce una motivazione per `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor`. Nel grafo unificato, il tipo dichiarato dell'arco 657 che coinvolge il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 657 che coinvolge il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 657 che coinvolge il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 657 che coinvolge il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `L88`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 657 che coinvolge il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 657 che coinvolge il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 657 che coinvolge il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` è `1.0`. Nel grafo unificato, l'arco 657 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator_generate_arrivals` per la relazione che coinvolge il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor`. Nel grafo unificato, l'arco 657 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_rationale_88` per la relazione che coinvolge il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor`.

### Genera un dataset di flussi RVE pre-composti.

<!-- GRAPH_NODE graph=codice index=82 id=main_rationale_278 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `Genera un dataset di flussi RVE pre-composti.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Genera un dataset di flussi RVE pre-composti.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `L278`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Genera un dataset di flussi RVE pre-composti.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Genera un dataset di flussi RVE pre-composti.` possiede l'identificatore esatto `main_rationale_278`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Genera un dataset di flussi RVE pre-composti.` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `genera un dataset di flussi rve pre-composti.`.
<!-- GRAPH_EDGE graph=codice index=188 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `cmd_requests()`; l'orientamento registrato nel JSON va dall'ID `main_rationale_278` all'ID `main_cmd_requests`. La relazione tecnica esplicita è la seguente: `Genera un dataset di flussi RVE pre-composti.` costituisce una motivazione per `cmd_requests()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 188 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 188 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 188 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 188 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `L278`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 188 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 188 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `ast`. Nel grafo del codice RDG, l'arco 188 registra come `source` l'identificatore `main_rationale_278` per la relazione che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.`. Nel grafo del codice RDG, l'arco 188 registra come `target` l'identificatore `main_cmd_requests` per la relazione che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 188 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Genera un dataset di flussi RVE pre-composti.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=82 id=Request-Dataset-Generator::main_rationale_278 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `Genera un dataset di flussi RVE pre-composti.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Genera un dataset di flussi RVE pre-composti.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `L278`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Genera un dataset di flussi RVE pre-composti.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Genera un dataset di flussi RVE pre-composti.` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `genera un dataset di flussi rve pre-composti.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Genera un dataset di flussi RVE pre-composti.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `main_rationale_278`. Nel grafo unificato, il nodo canonico `Genera un dataset di flussi RVE pre-composti.` possiede l'identificatore esatto `Request-Dataset-Generator::main_rationale_278`.
Nel grafo unificato, il nodo canonico `Genera un dataset di flussi RVE pre-composti.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=188 role=incoming -->
Nel grafo unificato, il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `cmd_requests()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_requests` all'ID `Request-Dataset-Generator::main_rationale_278`. La relazione tecnica esplicita è la seguente: `cmd_requests()` costituisce una motivazione per `Genera un dataset di flussi RVE pre-composti.`. Nel grafo unificato, il tipo dichiarato dell'arco 188 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 188 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 188 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 188 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `L278`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 188 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 188 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 188 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.` è `1.0`. Nel grafo unificato, l'arco 188 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_requests` per la relazione che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.`. Nel grafo unificato, l'arco 188 registra come `target` l'identificatore `Request-Dataset-Generator::main_rationale_278` per la relazione che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.`.

### Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer

<!-- GRAPH_NODE graph=codice index=157 id=request_dataset_generator_rationale_75 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `request_dataset_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `L75`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` possiede l'identificatore esatto `request_dataset_generator_rationale_75`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `genera un dataset di flussi rve pre-composti.      args:         count:    numer`.
<!-- GRAPH_EDGE graph=codice index=323 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `request_dataset_generator_rationale_75` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` costituisce una motivazione per `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 323 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 323 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 323 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `request_dataset_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 323 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `L75`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 323 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 323 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `ast`. Nel grafo del codice RDG, l'arco 323 registra come `source` l'identificatore `request_dataset_generator_rationale_75` per la relazione che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer`. Nel grafo del codice RDG, l'arco 323 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 323 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=157 id=Request-Dataset-Generator::request_dataset_generator_rationale_75 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `request_dataset_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `L75`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `genera un dataset di flussi rve pre-composti.      args:         count:    numer`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `request_dataset_generator_rationale_75`. Nel grafo unificato, il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` possiede l'identificatore esatto `Request-Dataset-Generator::request_dataset_generator_rationale_75`.
Nel grafo unificato, il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=323 role=incoming -->
Nel grafo unificato, il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::request_dataset_generator_rationale_75`. La relazione tecnica esplicita è la seguente: `build_workload()` costituisce una motivazione per `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer`. Nel grafo unificato, il tipo dichiarato dell'arco 323 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 323 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 323 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `request_dataset_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 323 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `L75`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 323 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 323 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 323 che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer` è `1.0`. Nel grafo unificato, l'arco 323 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer`. Nel grafo unificato, l'arco 323 registra come `target` l'identificatore `Request-Dataset-Generator::request_dataset_generator_rationale_75` per la relazione che coinvolge il nodo canonico `Genera un dataset di flussi RVE pre-composti.      Args:         count:    numer`.

### Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim

<!-- GRAPH_NODE graph=codice index=336 id=timeline_generator_rationale_191 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `L191`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` possiede l'identificatore esatto `timeline_generator_rationale_191`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `genera una timeline temporizzata a partire dal dataset di richieste.      il tim`.
<!-- GRAPH_EDGE graph=codice index=659 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_rationale_191` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` costituisce una motivazione per `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 659 che coinvolge il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 659 che coinvolge il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 659 che coinvolge il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 659 che coinvolge il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `L191`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 659 che coinvolge il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 659 che coinvolge il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `ast`. Nel grafo del codice RDG, l'arco 659 registra come `source` l'identificatore `timeline_generator_rationale_191` per la relazione che coinvolge il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim`. Nel grafo del codice RDG, l'arco 659 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 659 che coinvolge il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=336 id=Request-Dataset-Generator::timeline_generator_rationale_191 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `L191`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `genera una timeline temporizzata a partire dal dataset di richieste.      il tim`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `timeline_generator_rationale_191`. Nel grafo unificato, il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` possiede l'identificatore esatto `Request-Dataset-Generator::timeline_generator_rationale_191`.
Nel grafo unificato, il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=659 role=incoming -->
Nel grafo unificato, il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator_build_timeline` all'ID `Request-Dataset-Generator::timeline_generator_rationale_191`. La relazione tecnica esplicita è la seguente: `build_timeline()` costituisce una motivazione per `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim`. Nel grafo unificato, il tipo dichiarato dell'arco 659 che coinvolge il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 659 che coinvolge il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 659 che coinvolge il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 659 che coinvolge il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `L191`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 659 che coinvolge il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 659 che coinvolge il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 659 che coinvolge il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim` è `1.0`. Nel grafo unificato, l'arco 659 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim`. Nel grafo unificato, l'arco 659 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_rationale_191` per la relazione che coinvolge il nodo canonico `Genera una timeline temporizzata a partire dal dataset di richieste.      Il tim`.

### Genera una timeline temporizzata di flussi RVE.

<!-- GRAPH_NODE graph=codice index=84 id=main_rationale_409 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `Genera una timeline temporizzata di flussi RVE.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Genera una timeline temporizzata di flussi RVE.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `L409`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Genera una timeline temporizzata di flussi RVE.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Genera una timeline temporizzata di flussi RVE.` possiede l'identificatore esatto `main_rationale_409`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Genera una timeline temporizzata di flussi RVE.` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `genera una timeline temporizzata di flussi rve.`.
<!-- GRAPH_EDGE graph=codice index=203 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `main_rationale_409` all'ID `main_cmd_timeline`. La relazione tecnica esplicita è la seguente: `Genera una timeline temporizzata di flussi RVE.` costituisce una motivazione per `cmd_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 203 che coinvolge il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 203 che coinvolge il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 203 che coinvolge il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 203 che coinvolge il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `L409`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 203 che coinvolge il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 203 che coinvolge il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `ast`. Nel grafo del codice RDG, l'arco 203 registra come `source` l'identificatore `main_rationale_409` per la relazione che coinvolge il nodo canonico `Genera una timeline temporizzata di flussi RVE.`. Nel grafo del codice RDG, l'arco 203 registra come `target` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `Genera una timeline temporizzata di flussi RVE.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 203 che coinvolge il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Genera una timeline temporizzata di flussi RVE.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=84 id=Request-Dataset-Generator::main_rationale_409 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `Genera una timeline temporizzata di flussi RVE.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Genera una timeline temporizzata di flussi RVE.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `L409`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Genera una timeline temporizzata di flussi RVE.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Genera una timeline temporizzata di flussi RVE.` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `genera una timeline temporizzata di flussi rve.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Genera una timeline temporizzata di flussi RVE.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `main_rationale_409`. Nel grafo unificato, il nodo canonico `Genera una timeline temporizzata di flussi RVE.` possiede l'identificatore esatto `Request-Dataset-Generator::main_rationale_409`.
Nel grafo unificato, il nodo canonico `Genera una timeline temporizzata di flussi RVE.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=203 role=incoming -->
Nel grafo unificato, il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_timeline` all'ID `Request-Dataset-Generator::main_rationale_409`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` costituisce una motivazione per `Genera una timeline temporizzata di flussi RVE.`. Nel grafo unificato, il tipo dichiarato dell'arco 203 che coinvolge il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 203 che coinvolge il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 203 che coinvolge il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 203 che coinvolge il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `L409`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 203 che coinvolge il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 203 che coinvolge il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 203 che coinvolge il nodo canonico `Genera una timeline temporizzata di flussi RVE.` è `1.0`. Nel grafo unificato, l'arco 203 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `Genera una timeline temporizzata di flussi RVE.`. Nel grafo unificato, l'arco 203 registra come `target` l'identificatore `Request-Dataset-Generator::main_rationale_409` per la relazione che coinvolge il nodo canonico `Genera una timeline temporizzata di flussi RVE.`.

### generate_arrivals()

<!-- GRAPH_NODE graph=codice index=325 id=timeline_generator_generate_arrivals -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `generate_arrivals()` è `generate_arrivals()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `generate_arrivals()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `generate_arrivals()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `generate_arrivals()` è `L83`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `generate_arrivals()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `generate_arrivals()` possiede l'identificatore esatto `timeline_generator_generate_arrivals`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `generate_arrivals()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `generate_arrivals()` è `generate_arrivals()`.
<!-- GRAPH_EDGE graph=codice index=656 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `generate_arrivals()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_frange()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_generate_arrivals` all'ID `timeline_generator_frange`. La relazione tecnica esplicita è la seguente: `generate_arrivals()` chiama `_frange()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 656 che coinvolge il nodo canonico `generate_arrivals()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 656 che coinvolge il nodo canonico `generate_arrivals()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 656 che coinvolge il nodo canonico `generate_arrivals()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 656 che coinvolge il nodo canonico `generate_arrivals()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 656 che coinvolge il nodo canonico `generate_arrivals()` è `L104`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 656 che coinvolge il nodo canonico `generate_arrivals()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 656 che coinvolge il nodo canonico `generate_arrivals()` è `ast`. Nel grafo del codice RDG, l'arco 656 registra come `source` l'identificatore `timeline_generator_generate_arrivals` per la relazione che coinvolge il nodo canonico `generate_arrivals()`. Nel grafo del codice RDG, l'arco 656 registra come `target` l'identificatore `timeline_generator_frange` per la relazione che coinvolge il nodo canonico `generate_arrivals()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 656 che coinvolge il nodo canonico `generate_arrivals()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=646 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `generate_arrivals()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `timeline_generator` all'ID `timeline_generator_generate_arrivals`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `generate_arrivals()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 646 che coinvolge il nodo canonico `generate_arrivals()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 646 che coinvolge il nodo canonico `generate_arrivals()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 646 che coinvolge il nodo canonico `generate_arrivals()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 646 che coinvolge il nodo canonico `generate_arrivals()` è `L83`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 646 che coinvolge il nodo canonico `generate_arrivals()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 646 che coinvolge il nodo canonico `generate_arrivals()` è `ast`. Nel grafo del codice RDG, l'arco 646 registra come `source` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `generate_arrivals()`. Nel grafo del codice RDG, l'arco 646 registra come `target` l'identificatore `timeline_generator_generate_arrivals` per la relazione che coinvolge il nodo canonico `generate_arrivals()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 646 che coinvolge il nodo canonico `generate_arrivals()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=655 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `generate_arrivals()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_build_timeline` all'ID `timeline_generator_generate_arrivals`. La relazione tecnica esplicita è la seguente: `build_timeline()` chiama `generate_arrivals()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 655 che coinvolge il nodo canonico `generate_arrivals()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 655 che coinvolge il nodo canonico `generate_arrivals()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 655 che coinvolge il nodo canonico `generate_arrivals()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 655 che coinvolge il nodo canonico `generate_arrivals()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 655 che coinvolge il nodo canonico `generate_arrivals()` è `L223`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 655 che coinvolge il nodo canonico `generate_arrivals()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 655 che coinvolge il nodo canonico `generate_arrivals()` è `ast`. Nel grafo del codice RDG, l'arco 655 registra come `source` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `generate_arrivals()`. Nel grafo del codice RDG, l'arco 655 registra come `target` l'identificatore `timeline_generator_generate_arrivals` per la relazione che coinvolge il nodo canonico `generate_arrivals()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 655 che coinvolge il nodo canonico `generate_arrivals()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=657 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `generate_arrivals()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_rationale_88` all'ID `timeline_generator_generate_arrivals`. La relazione tecnica esplicita è la seguente: `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor` costituisce una motivazione per `generate_arrivals()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 657 che coinvolge il nodo canonico `generate_arrivals()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 657 che coinvolge il nodo canonico `generate_arrivals()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 657 che coinvolge il nodo canonico `generate_arrivals()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 657 che coinvolge il nodo canonico `generate_arrivals()` è `L88`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 657 che coinvolge il nodo canonico `generate_arrivals()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 657 che coinvolge il nodo canonico `generate_arrivals()` è `ast`. Nel grafo del codice RDG, l'arco 657 registra come `source` l'identificatore `timeline_generator_rationale_88` per la relazione che coinvolge il nodo canonico `generate_arrivals()`. Nel grafo del codice RDG, l'arco 657 registra come `target` l'identificatore `timeline_generator_generate_arrivals` per la relazione che coinvolge il nodo canonico `generate_arrivals()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 657 che coinvolge il nodo canonico `generate_arrivals()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=325 id=Request-Dataset-Generator::timeline_generator_generate_arrivals -->
Nel grafo unificato, l'etichetta originale del nodo canonico `generate_arrivals()` è `generate_arrivals()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `generate_arrivals()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `generate_arrivals()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `generate_arrivals()` è `L83`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `generate_arrivals()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `generate_arrivals()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `generate_arrivals()` è `generate_arrivals()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `generate_arrivals()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `generate_arrivals()` è `timeline_generator_generate_arrivals`. Nel grafo unificato, il nodo canonico `generate_arrivals()` possiede l'identificatore esatto `Request-Dataset-Generator::timeline_generator_generate_arrivals`.
<!-- GRAPH_EDGE graph=unificata index=655 role=outgoing -->
Nel grafo unificato, il nodo canonico `generate_arrivals()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator_generate_arrivals` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `generate_arrivals()` chiama `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 655 che coinvolge il nodo canonico `generate_arrivals()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 655 che coinvolge il nodo canonico `generate_arrivals()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 655 che coinvolge il nodo canonico `generate_arrivals()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 655 che coinvolge il nodo canonico `generate_arrivals()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 655 che coinvolge il nodo canonico `generate_arrivals()` è `L223`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 655 che coinvolge il nodo canonico `generate_arrivals()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 655 che coinvolge il nodo canonico `generate_arrivals()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 655 che coinvolge il nodo canonico `generate_arrivals()` è `1.0`. Nel grafo unificato, l'arco 655 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator_generate_arrivals` per la relazione che coinvolge il nodo canonico `generate_arrivals()`. Nel grafo unificato, l'arco 655 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `generate_arrivals()`.
<!-- GRAPH_EDGE graph=unificata index=656 role=outgoing -->
Nel grafo unificato, il nodo canonico `generate_arrivals()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_frange()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator_generate_arrivals` all'ID `Request-Dataset-Generator::timeline_generator_frange`. La relazione tecnica esplicita è la seguente: `generate_arrivals()` chiama `_frange()`. Nel grafo unificato, il tipo dichiarato dell'arco 656 che coinvolge il nodo canonico `generate_arrivals()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 656 che coinvolge il nodo canonico `generate_arrivals()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 656 che coinvolge il nodo canonico `generate_arrivals()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 656 che coinvolge il nodo canonico `generate_arrivals()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 656 che coinvolge il nodo canonico `generate_arrivals()` è `L104`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 656 che coinvolge il nodo canonico `generate_arrivals()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 656 che coinvolge il nodo canonico `generate_arrivals()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 656 che coinvolge il nodo canonico `generate_arrivals()` è `1.0`. Nel grafo unificato, l'arco 656 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator_generate_arrivals` per la relazione che coinvolge il nodo canonico `generate_arrivals()`. Nel grafo unificato, l'arco 656 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_frange` per la relazione che coinvolge il nodo canonico `generate_arrivals()`.
<!-- GRAPH_EDGE graph=unificata index=657 role=outgoing -->
Nel grafo unificato, il nodo canonico `generate_arrivals()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator_generate_arrivals` all'ID `Request-Dataset-Generator::timeline_generator_rationale_88`. La relazione tecnica esplicita è la seguente: `generate_arrivals()` costituisce una motivazione per `Genera tempi di arrivo con processo di Poisson non stazionario.      Usa l'algor`. Nel grafo unificato, il tipo dichiarato dell'arco 657 che coinvolge il nodo canonico `generate_arrivals()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 657 che coinvolge il nodo canonico `generate_arrivals()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 657 che coinvolge il nodo canonico `generate_arrivals()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 657 che coinvolge il nodo canonico `generate_arrivals()` è `L88`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 657 che coinvolge il nodo canonico `generate_arrivals()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 657 che coinvolge il nodo canonico `generate_arrivals()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 657 che coinvolge il nodo canonico `generate_arrivals()` è `1.0`. Nel grafo unificato, l'arco 657 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator_generate_arrivals` per la relazione che coinvolge il nodo canonico `generate_arrivals()`. Nel grafo unificato, l'arco 657 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_rationale_88` per la relazione che coinvolge il nodo canonico `generate_arrivals()`.
<!-- GRAPH_EDGE graph=unificata index=646 role=incoming -->
Nel grafo unificato, il nodo canonico `generate_arrivals()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator` all'ID `Request-Dataset-Generator::timeline_generator_generate_arrivals`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `generate_arrivals()`. Nel grafo unificato, il tipo dichiarato dell'arco 646 che coinvolge il nodo canonico `generate_arrivals()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 646 che coinvolge il nodo canonico `generate_arrivals()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 646 che coinvolge il nodo canonico `generate_arrivals()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 646 che coinvolge il nodo canonico `generate_arrivals()` è `L83`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 646 che coinvolge il nodo canonico `generate_arrivals()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 646 che coinvolge il nodo canonico `generate_arrivals()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 646 che coinvolge il nodo canonico `generate_arrivals()` è `1.0`. Nel grafo unificato, l'arco 646 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `generate_arrivals()`. Nel grafo unificato, l'arco 646 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_generate_arrivals` per la relazione che coinvolge il nodo canonico `generate_arrivals()`.

### Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro

<!-- GRAPH_NODE graph=codice index=335 id=timeline_generator_rationale_145 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `L145`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` possiede l'identificatore esatto `timeline_generator_rationale_145`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `inietta burst stocastici nella timeline.      per ogni arrivo esistente, con pro`.
<!-- GRAPH_EDGE graph=codice index=658 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `inject_bursts()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_rationale_145` all'ID `timeline_generator_inject_bursts`. La relazione tecnica esplicita è la seguente: `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` costituisce una motivazione per `inject_bursts()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 658 che coinvolge il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 658 che coinvolge il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 658 che coinvolge il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 658 che coinvolge il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `L145`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 658 che coinvolge il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 658 che coinvolge il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `ast`. Nel grafo del codice RDG, l'arco 658 registra come `source` l'identificatore `timeline_generator_rationale_145` per la relazione che coinvolge il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro`. Nel grafo del codice RDG, l'arco 658 registra come `target` l'identificatore `timeline_generator_inject_bursts` per la relazione che coinvolge il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 658 che coinvolge il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=335 id=Request-Dataset-Generator::timeline_generator_rationale_145 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `L145`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `inietta burst stocastici nella timeline.      per ogni arrivo esistente, con pro`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `timeline_generator_rationale_145`. Nel grafo unificato, il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` possiede l'identificatore esatto `Request-Dataset-Generator::timeline_generator_rationale_145`.
Nel grafo unificato, il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=658 role=incoming -->
Nel grafo unificato, il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `inject_bursts()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator_inject_bursts` all'ID `Request-Dataset-Generator::timeline_generator_rationale_145`. La relazione tecnica esplicita è la seguente: `inject_bursts()` costituisce una motivazione per `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro`. Nel grafo unificato, il tipo dichiarato dell'arco 658 che coinvolge il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 658 che coinvolge il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 658 che coinvolge il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 658 che coinvolge il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `L145`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 658 che coinvolge il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 658 che coinvolge il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 658 che coinvolge il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` è `1.0`. Nel grafo unificato, l'arco 658 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator_inject_bursts` per la relazione che coinvolge il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro`. Nel grafo unificato, l'arco 658 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_rationale_145` per la relazione che coinvolge il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro`.

### inject_bursts()

<!-- GRAPH_NODE graph=codice index=327 id=timeline_generator_inject_bursts -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `inject_bursts()` è `inject_bursts()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `inject_bursts()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `inject_bursts()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `inject_bursts()` è `L138`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `inject_bursts()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `inject_bursts()` possiede l'identificatore esatto `timeline_generator_inject_bursts`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `inject_bursts()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `inject_bursts()` è `inject_bursts()`.
Nel grafo del codice RDG, il nodo canonico `inject_bursts()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=647 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `inject_bursts()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `timeline_generator` all'ID `timeline_generator_inject_bursts`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `inject_bursts()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 647 che coinvolge il nodo canonico `inject_bursts()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 647 che coinvolge il nodo canonico `inject_bursts()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 647 che coinvolge il nodo canonico `inject_bursts()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 647 che coinvolge il nodo canonico `inject_bursts()` è `L138`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 647 che coinvolge il nodo canonico `inject_bursts()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 647 che coinvolge il nodo canonico `inject_bursts()` è `ast`. Nel grafo del codice RDG, l'arco 647 registra come `source` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `inject_bursts()`. Nel grafo del codice RDG, l'arco 647 registra come `target` l'identificatore `timeline_generator_inject_bursts` per la relazione che coinvolge il nodo canonico `inject_bursts()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 647 che coinvolge il nodo canonico `inject_bursts()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=658 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `inject_bursts()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_rationale_145` all'ID `timeline_generator_inject_bursts`. La relazione tecnica esplicita è la seguente: `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro` costituisce una motivazione per `inject_bursts()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 658 che coinvolge il nodo canonico `inject_bursts()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 658 che coinvolge il nodo canonico `inject_bursts()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 658 che coinvolge il nodo canonico `inject_bursts()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 658 che coinvolge il nodo canonico `inject_bursts()` è `L145`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 658 che coinvolge il nodo canonico `inject_bursts()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 658 che coinvolge il nodo canonico `inject_bursts()` è `ast`. Nel grafo del codice RDG, l'arco 658 registra come `source` l'identificatore `timeline_generator_rationale_145` per la relazione che coinvolge il nodo canonico `inject_bursts()`. Nel grafo del codice RDG, l'arco 658 registra come `target` l'identificatore `timeline_generator_inject_bursts` per la relazione che coinvolge il nodo canonico `inject_bursts()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 658 che coinvolge il nodo canonico `inject_bursts()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=327 id=Request-Dataset-Generator::timeline_generator_inject_bursts -->
Nel grafo unificato, l'etichetta originale del nodo canonico `inject_bursts()` è `inject_bursts()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `inject_bursts()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `inject_bursts()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `inject_bursts()` è `L138`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `inject_bursts()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `inject_bursts()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `inject_bursts()` è `inject_bursts()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `inject_bursts()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `inject_bursts()` è `timeline_generator_inject_bursts`. Nel grafo unificato, il nodo canonico `inject_bursts()` possiede l'identificatore esatto `Request-Dataset-Generator::timeline_generator_inject_bursts`.
<!-- GRAPH_EDGE graph=unificata index=658 role=outgoing -->
Nel grafo unificato, il nodo canonico `inject_bursts()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator_inject_bursts` all'ID `Request-Dataset-Generator::timeline_generator_rationale_145`. La relazione tecnica esplicita è la seguente: `inject_bursts()` costituisce una motivazione per `Inietta burst stocastici nella timeline.      Per ogni arrivo esistente, con pro`. Nel grafo unificato, il tipo dichiarato dell'arco 658 che coinvolge il nodo canonico `inject_bursts()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 658 che coinvolge il nodo canonico `inject_bursts()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 658 che coinvolge il nodo canonico `inject_bursts()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 658 che coinvolge il nodo canonico `inject_bursts()` è `L145`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 658 che coinvolge il nodo canonico `inject_bursts()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 658 che coinvolge il nodo canonico `inject_bursts()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 658 che coinvolge il nodo canonico `inject_bursts()` è `1.0`. Nel grafo unificato, l'arco 658 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator_inject_bursts` per la relazione che coinvolge il nodo canonico `inject_bursts()`. Nel grafo unificato, l'arco 658 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_rationale_145` per la relazione che coinvolge il nodo canonico `inject_bursts()`.
<!-- GRAPH_EDGE graph=unificata index=647 role=incoming -->
Nel grafo unificato, il nodo canonico `inject_bursts()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator` all'ID `Request-Dataset-Generator::timeline_generator_inject_bursts`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `inject_bursts()`. Nel grafo unificato, il tipo dichiarato dell'arco 647 che coinvolge il nodo canonico `inject_bursts()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 647 che coinvolge il nodo canonico `inject_bursts()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 647 che coinvolge il nodo canonico `inject_bursts()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 647 che coinvolge il nodo canonico `inject_bursts()` è `L138`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 647 che coinvolge il nodo canonico `inject_bursts()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 647 che coinvolge il nodo canonico `inject_bursts()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 647 che coinvolge il nodo canonico `inject_bursts()` è `1.0`. Nel grafo unificato, l'arco 647 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `inject_bursts()`. Nel grafo unificato, l'arco 647 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_inject_bursts` per la relazione che coinvolge il nodo canonico `inject_bursts()`.

### main() [main_main]

<!-- GRAPH_NODE graph=codice index=79 id=main_main -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `main() [main_main]` è `main()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `main() [main_main]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `main() [main_main]` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `main() [main_main]` è `L591`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `main() [main_main]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `main() [main_main]` possiede l'identificatore esatto `main_main`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `main() [main_main]` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `main() [main_main]` è `main()`.
<!-- GRAPH_EDGE graph=codice index=151 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main() [main_main]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_load_config() [main_load_config]`; l'orientamento registrato nel JSON va dall'ID `main_main` all'ID `main_load_config`. La relazione tecnica esplicita è la seguente: `main() [main_main]` chiama `_load_config() [main_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 151 che coinvolge il nodo canonico `main() [main_main]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 151 che coinvolge il nodo canonico `main() [main_main]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 151 che coinvolge il nodo canonico `main() [main_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 151 che coinvolge il nodo canonico `main() [main_main]` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 151 che coinvolge il nodo canonico `main() [main_main]` è `L611`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 151 che coinvolge il nodo canonico `main() [main_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 151 che coinvolge il nodo canonico `main() [main_main]` è `ast`. Nel grafo del codice RDG, l'arco 151 registra come `source` l'identificatore `main_main` per la relazione che coinvolge il nodo canonico `main() [main_main]`. Nel grafo del codice RDG, l'arco 151 registra come `target` l'identificatore `main_load_config` per la relazione che coinvolge il nodo canonico `main() [main_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 151 che coinvolge il nodo canonico `main() [main_main]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=161 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main() [main_main]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_build_namespace()`; l'orientamento registrato nel JSON va dall'ID `main_main` all'ID `main_build_namespace`. La relazione tecnica esplicita è la seguente: `main() [main_main]` chiama `_build_namespace()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 161 che coinvolge il nodo canonico `main() [main_main]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 161 che coinvolge il nodo canonico `main() [main_main]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 161 che coinvolge il nodo canonico `main() [main_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 161 che coinvolge il nodo canonico `main() [main_main]` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 161 che coinvolge il nodo canonico `main() [main_main]` è `L620`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 161 che coinvolge il nodo canonico `main() [main_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 161 che coinvolge il nodo canonico `main() [main_main]` è `ast`. Nel grafo del codice RDG, l'arco 161 registra come `source` l'identificatore `main_main` per la relazione che coinvolge il nodo canonico `main() [main_main]`. Nel grafo del codice RDG, l'arco 161 registra come `target` l'identificatore `main_build_namespace` per la relazione che coinvolge il nodo canonico `main() [main_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 161 che coinvolge il nodo canonico `main() [main_main]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=172 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main() [main_main]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_ensure_experiment_metadata()`; l'orientamento registrato nel JSON va dall'ID `main_main` all'ID `main_ensure_experiment_metadata`. La relazione tecnica esplicita è la seguente: `main() [main_main]` chiama `_ensure_experiment_metadata()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 172 che coinvolge il nodo canonico `main() [main_main]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 172 che coinvolge il nodo canonico `main() [main_main]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 172 che coinvolge il nodo canonico `main() [main_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 172 che coinvolge il nodo canonico `main() [main_main]` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 172 che coinvolge il nodo canonico `main() [main_main]` è `L619`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 172 che coinvolge il nodo canonico `main() [main_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 172 che coinvolge il nodo canonico `main() [main_main]` è `ast`. Nel grafo del codice RDG, l'arco 172 registra come `source` l'identificatore `main_main` per la relazione che coinvolge il nodo canonico `main() [main_main]`. Nel grafo del codice RDG, l'arco 172 registra come `target` l'identificatore `main_ensure_experiment_metadata` per la relazione che coinvolge il nodo canonico `main() [main_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 172 che coinvolge il nodo canonico `main() [main_main]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=133 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `main() [main_main]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_main`. La relazione tecnica esplicita è la seguente: `main.py` contiene `main() [main_main]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 133 che coinvolge il nodo canonico `main() [main_main]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 133 che coinvolge il nodo canonico `main() [main_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 133 che coinvolge il nodo canonico `main() [main_main]` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 133 che coinvolge il nodo canonico `main() [main_main]` è `L591`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 133 che coinvolge il nodo canonico `main() [main_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 133 che coinvolge il nodo canonico `main() [main_main]` è `ast`. Nel grafo del codice RDG, l'arco 133 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main() [main_main]`. Nel grafo del codice RDG, l'arco 133 registra come `target` l'identificatore `main_main` per la relazione che coinvolge il nodo canonico `main() [main_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 133 che coinvolge il nodo canonico `main() [main_main]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=79 id=Request-Dataset-Generator::main_main -->
Nel grafo unificato, l'etichetta originale del nodo canonico `main() [main_main]` è `main()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `main() [main_main]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `main() [main_main]` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `main() [main_main]` è `L591`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `main() [main_main]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `main() [main_main]` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `main() [main_main]` è `main()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `main() [main_main]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `main() [main_main]` è `main_main`. Nel grafo unificato, il nodo canonico `main() [main_main]` possiede l'identificatore esatto `Request-Dataset-Generator::main_main`.
Nel grafo unificato, il nodo canonico `main() [main_main]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=133 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [main_main]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_main`. La relazione tecnica esplicita è la seguente: `main.py` contiene `main() [main_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 133 che coinvolge il nodo canonico `main() [main_main]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 133 che coinvolge il nodo canonico `main() [main_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 133 che coinvolge il nodo canonico `main() [main_main]` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 133 che coinvolge il nodo canonico `main() [main_main]` è `L591`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 133 che coinvolge il nodo canonico `main() [main_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 133 che coinvolge il nodo canonico `main() [main_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 133 che coinvolge il nodo canonico `main() [main_main]` è `1.0`. Nel grafo unificato, l'arco 133 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main() [main_main]`. Nel grafo unificato, l'arco 133 registra come `target` l'identificatore `Request-Dataset-Generator::main_main` per la relazione che coinvolge il nodo canonico `main() [main_main]`.
<!-- GRAPH_EDGE graph=unificata index=151 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [main_main]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_load_config() [main_load_config]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_load_config` all'ID `Request-Dataset-Generator::main_main`. La relazione tecnica esplicita è la seguente: `_load_config() [main_load_config]` chiama `main() [main_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 151 che coinvolge il nodo canonico `main() [main_main]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 151 che coinvolge il nodo canonico `main() [main_main]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 151 che coinvolge il nodo canonico `main() [main_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 151 che coinvolge il nodo canonico `main() [main_main]` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 151 che coinvolge il nodo canonico `main() [main_main]` è `L611`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 151 che coinvolge il nodo canonico `main() [main_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 151 che coinvolge il nodo canonico `main() [main_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 151 che coinvolge il nodo canonico `main() [main_main]` è `1.0`. Nel grafo unificato, l'arco 151 registra come `source` l'identificatore `Request-Dataset-Generator::main_load_config` per la relazione che coinvolge il nodo canonico `main() [main_main]`. Nel grafo unificato, l'arco 151 registra come `target` l'identificatore `Request-Dataset-Generator::main_main` per la relazione che coinvolge il nodo canonico `main() [main_main]`.
<!-- GRAPH_EDGE graph=unificata index=161 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [main_main]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_build_namespace()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_build_namespace` all'ID `Request-Dataset-Generator::main_main`. La relazione tecnica esplicita è la seguente: `_build_namespace()` chiama `main() [main_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 161 che coinvolge il nodo canonico `main() [main_main]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 161 che coinvolge il nodo canonico `main() [main_main]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 161 che coinvolge il nodo canonico `main() [main_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 161 che coinvolge il nodo canonico `main() [main_main]` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 161 che coinvolge il nodo canonico `main() [main_main]` è `L620`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 161 che coinvolge il nodo canonico `main() [main_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 161 che coinvolge il nodo canonico `main() [main_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 161 che coinvolge il nodo canonico `main() [main_main]` è `1.0`. Nel grafo unificato, l'arco 161 registra come `source` l'identificatore `Request-Dataset-Generator::main_build_namespace` per la relazione che coinvolge il nodo canonico `main() [main_main]`. Nel grafo unificato, l'arco 161 registra come `target` l'identificatore `Request-Dataset-Generator::main_main` per la relazione che coinvolge il nodo canonico `main() [main_main]`.
<!-- GRAPH_EDGE graph=unificata index=172 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [main_main]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_ensure_experiment_metadata()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_ensure_experiment_metadata` all'ID `Request-Dataset-Generator::main_main`. La relazione tecnica esplicita è la seguente: `_ensure_experiment_metadata()` chiama `main() [main_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 172 che coinvolge il nodo canonico `main() [main_main]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 172 che coinvolge il nodo canonico `main() [main_main]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 172 che coinvolge il nodo canonico `main() [main_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 172 che coinvolge il nodo canonico `main() [main_main]` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 172 che coinvolge il nodo canonico `main() [main_main]` è `L619`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 172 che coinvolge il nodo canonico `main() [main_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 172 che coinvolge il nodo canonico `main() [main_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 172 che coinvolge il nodo canonico `main() [main_main]` è `1.0`. Nel grafo unificato, l'arco 172 registra come `source` l'identificatore `Request-Dataset-Generator::main_ensure_experiment_metadata` per la relazione che coinvolge il nodo canonico `main() [main_main]`. Nel grafo unificato, l'arco 172 registra come `target` l'identificatore `Request-Dataset-Generator::main_main` per la relazione che coinvolge il nodo canonico `main() [main_main]`.

### main.py

<!-- GRAPH_NODE graph=codice index=59 id=main -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `main.py` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `main.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `main.py` vale `ast`. Nel grafo del codice RDG, il nodo canonico `main.py` possiede l'identificatore esatto `main`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `main.py` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `main.py` è `main.py`.
<!-- GRAPH_EDGE graph=codice index=21 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `fhir_patient_generator.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `fhir_patient_generator`. La relazione tecnica esplicita è la seguente: `main.py` importa da `fhir_patient_generator.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 21 che coinvolge il nodo canonico `main.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 21 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 21 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 21 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 21 che coinvolge il nodo canonico `main.py` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 21 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 21 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 21 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 21 registra come `target` l'identificatore `fhir_patient_generator` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 21 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=40 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `genera_paziente()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `fhir_patient_generator_genera_paziente`. La relazione tecnica esplicita è la seguente: `main.py` importa `genera_paziente()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 40 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 40 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 40 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 40 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 40 che coinvolge il nodo canonico `main.py` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 40 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 40 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 40 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 40 registra come `target` l'identificatore `fhir_patient_generator_genera_paziente` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 40 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=45 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `genera_bundle_rve55()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `fhir_patient_generator_genera_bundle_rve55`. La relazione tecnica esplicita è la seguente: `main.py` importa `genera_bundle_rve55()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 45 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 45 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 45 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 45 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 45 che coinvolge il nodo canonico `main.py` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 45 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 45 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 45 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 45 registra come `target` l'identificatore `fhir_patient_generator_genera_bundle_rve55` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 45 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=49 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `to_json()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `fhir_patient_generator_to_json`. La relazione tecnica esplicita è la seguente: `main.py` importa `to_json()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 49 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 49 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 49 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 49 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 49 che coinvolge il nodo canonico `main.py` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 49 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 49 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 49 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 49 registra come `target` l'identificatore `fhir_patient_generator_to_json` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 49 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=54 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `to_xml()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `fhir_patient_generator_to_xml`. La relazione tecnica esplicita è la seguente: `main.py` importa `to_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 54 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 54 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 54 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 54 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 54 che coinvolge il nodo canonico `main.py` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 54 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 54 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 54 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 54 registra come `target` l'identificatore `fhir_patient_generator_to_xml` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 54 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=71 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `log_collector`. La relazione tecnica esplicita è la seguente: `main.py` importa da `log_collector.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 71 che coinvolge il nodo canonico `main.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 71 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 71 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 71 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 71 che coinvolge il nodo canonico `main.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 71 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 71 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 71 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 71 registra come `target` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 71 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=109 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `main.py` importa `collect_logs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 109 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 109 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 109 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 109 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 109 che coinvolge il nodo canonico `main.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 109 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 109 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 109 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 109 registra come `target` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 109 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=119 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_annotate_dataset_flows()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_annotate_dataset_flows`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_annotate_dataset_flows()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 119 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 119 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 119 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 119 che coinvolge il nodo canonico `main.py` è `L198`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 119 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 119 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 119 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 119 registra come `target` l'identificatore `main_annotate_dataset_flows` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 119 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=120 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_append_json()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_append_json`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_append_json()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 120 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 120 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 120 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 120 che coinvolge il nodo canonico `main.py` è `L235`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 120 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 120 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 120 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 120 registra come `target` l'identificatore `main_append_json` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 120 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=121 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_build_namespace()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_build_namespace`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_build_namespace()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 121 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 121 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 121 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 121 che coinvolge il nodo canonico `main.py` è `L115`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 121 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 121 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 121 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 121 registra come `target` l'identificatore `main_build_namespace` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 121 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=122 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_cfg_section()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_cfg_section`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_cfg_section()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 122 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 122 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 122 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 122 che coinvolge il nodo canonico `main.py` è `L105`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 122 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 122 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 122 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 122 registra come `target` l'identificatore `main_cfg_section` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 122 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=123 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `cmd_collect()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_cmd_collect`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_collect()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 123 che coinvolge il nodo canonico `main.py` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 123 che coinvolge il nodo canonico `main.py` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 123 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 123 che coinvolge il nodo canonico `main.py` è `L585`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 123 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 123 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 123 che coinvolge il nodo canonico `main.py` è `collection`. Nel grafo del codice RDG, l'arco 123 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 123 registra come `target` l'identificatore `main_cmd_collect` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 123 che coinvolge il nodo canonico `main.py` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=124 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_cmd_execute`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_execute()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 124 che coinvolge il nodo canonico `main.py` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 124 che coinvolge il nodo canonico `main.py` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 124 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 124 che coinvolge il nodo canonico `main.py` è `L584`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 124 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 124 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 124 che coinvolge il nodo canonico `main.py` è `collection`. Nel grafo del codice RDG, l'arco 124 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 124 registra come `target` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 124 che coinvolge il nodo canonico `main.py` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=125 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `cmd_metrics()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_cmd_metrics`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_metrics()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 125 che coinvolge il nodo canonico `main.py` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 125 che coinvolge il nodo canonico `main.py` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 125 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 125 che coinvolge il nodo canonico `main.py` è `L586`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 125 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 125 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 125 che coinvolge il nodo canonico `main.py` è `collection`. Nel grafo del codice RDG, l'arco 125 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 125 registra come `target` l'identificatore `main_cmd_metrics` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 125 che coinvolge il nodo canonico `main.py` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=126 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `cmd_patients()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_cmd_patients`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_patients()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 126 che coinvolge il nodo canonico `main.py` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 126 che coinvolge il nodo canonico `main.py` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 126 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 126 che coinvolge il nodo canonico `main.py` è `L581`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 126 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 126 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 126 che coinvolge il nodo canonico `main.py` è `collection`. Nel grafo del codice RDG, l'arco 126 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 126 registra come `target` l'identificatore `main_cmd_patients` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 126 che coinvolge il nodo canonico `main.py` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=127 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `cmd_pipeline()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_cmd_pipeline`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_pipeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 127 che coinvolge il nodo canonico `main.py` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 127 che coinvolge il nodo canonico `main.py` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 127 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 127 che coinvolge il nodo canonico `main.py` è `L587`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 127 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 127 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 127 che coinvolge il nodo canonico `main.py` è `collection`. Nel grafo del codice RDG, l'arco 127 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 127 registra come `target` l'identificatore `main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 127 che coinvolge il nodo canonico `main.py` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=128 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `cmd_requests()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_cmd_requests`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_requests()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 128 che coinvolge il nodo canonico `main.py` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 128 che coinvolge il nodo canonico `main.py` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 128 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 128 che coinvolge il nodo canonico `main.py` è `L582`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 128 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 128 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 128 che coinvolge il nodo canonico `main.py` è `collection`. Nel grafo del codice RDG, l'arco 128 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 128 registra come `target` l'identificatore `main_cmd_requests` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 128 che coinvolge il nodo canonico `main.py` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=129 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_cmd_timeline`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 129 che coinvolge il nodo canonico `main.py` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 129 che coinvolge il nodo canonico `main.py` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 129 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 129 che coinvolge il nodo canonico `main.py` è `L583`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 129 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 129 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 129 che coinvolge il nodo canonico `main.py` è `collection`. Nel grafo del codice RDG, l'arco 129 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 129 registra come `target` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 129 che coinvolge il nodo canonico `main.py` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=130 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_configure_run_outputs()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_configure_run_outputs`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_configure_run_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 130 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 130 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 130 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 130 che coinvolge il nodo canonico `main.py` è `L162`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 130 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 130 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 130 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 130 registra come `target` l'identificatore `main_configure_run_outputs` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 130 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=131 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_ensure_experiment_metadata()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_ensure_experiment_metadata`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_ensure_experiment_metadata()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 131 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 131 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 131 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 131 che coinvolge il nodo canonico `main.py` è `L137`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 131 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 131 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 131 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 131 registra come `target` l'identificatore `main_ensure_experiment_metadata` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 131 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=132 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_load_config() [main_load_config]`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_load_config`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_load_config() [main_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 132 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 132 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 132 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 132 che coinvolge il nodo canonico `main.py` è `L100`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 132 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 132 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 132 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 132 registra come `target` l'identificatore `main_load_config` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 132 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=133 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `main() [main_main]`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_main`. La relazione tecnica esplicita è la seguente: `main.py` contiene `main() [main_main]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 133 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 133 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 133 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 133 che coinvolge il nodo canonico `main.py` è `L591`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 133 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 133 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 133 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 133 registra come `target` l'identificatore `main_main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 133 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=134 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_resolve_output_path() [main_resolve_output_path]`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_resolve_output_path`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_resolve_output_path() [main_resolve_output_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 134 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 134 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 134 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 134 che coinvolge il nodo canonico `main.py` è `L211`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 134 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 134 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 134 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 134 registra come `target` l'identificatore `main_resolve_output_path` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 134 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=135 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_slug()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_slug`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_slug()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 135 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 135 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 135 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 135 che coinvolge il nodo canonico `main.py` è `L132`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 135 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 135 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 135 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 135 registra come `target` l'identificatore `main_slug` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 135 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=136 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_write_output()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_write_output`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_write_output()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 136 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 136 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 136 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 136 che coinvolge il nodo canonico `main.py` è `L221`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 136 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 136 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 136 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 136 registra come `target` l'identificatore `main_write_output` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 136 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=137 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_write_resolved_config()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `main_write_resolved_config`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_write_resolved_config()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 137 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 137 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 137 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 137 che coinvolge il nodo canonico `main.py` è `L189`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 137 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 137 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 137 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 137 registra come `target` l'identificatore `main_write_resolved_config` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 137 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=138 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `metric_engine.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `metric_engine`. La relazione tecnica esplicita è la seguente: `main.py` importa da `metric_engine.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 138 che coinvolge il nodo canonico `main.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 138 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 138 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 138 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 138 che coinvolge il nodo canonico `main.py` è `L22`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 138 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 138 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 138 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 138 registra come `target` l'identificatore `metric_engine` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 138 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=139 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `run_metric_engine()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `main.py` importa `run_metric_engine()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 139 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 139 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 139 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 139 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 139 che coinvolge il nodo canonico `main.py` è `L22`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 139 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 139 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 139 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 139 registra come `target` l'identificatore `metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 139 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=140 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `request_dataset_generator.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `request_dataset_generator`. La relazione tecnica esplicita è la seguente: `main.py` importa da `request_dataset_generator.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 140 che coinvolge il nodo canonico `main.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 140 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 140 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 140 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 140 che coinvolge il nodo canonico `main.py` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 140 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 140 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 140 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 140 registra come `target` l'identificatore `request_dataset_generator` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 140 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=141 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `main.py` importa `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 141 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 141 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 141 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 141 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 141 che coinvolge il nodo canonico `main.py` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 141 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 141 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 141 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 141 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 141 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=142 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `timeline_generator`. La relazione tecnica esplicita è la seguente: `main.py` importa da `timeline_generator.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 142 che coinvolge il nodo canonico `main.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 142 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 142 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 142 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 142 che coinvolge il nodo canonico `main.py` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 142 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 142 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 142 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 142 registra come `target` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 142 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=143 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `main.py` importa `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 143 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 143 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 143 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 143 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 143 che coinvolge il nodo canonico `main.py` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 143 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 143 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 143 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 143 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 143 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=144 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `main.py` importa `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 144 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 144 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 144 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 144 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 144 che coinvolge il nodo canonico `main.py` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 144 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 144 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 144 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 144 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 144 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=145 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `daily_clinical_profile()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `timeline_generator_daily_clinical_profile`. La relazione tecnica esplicita è la seguente: `main.py` importa `daily_clinical_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 145 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 145 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 145 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 145 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 145 che coinvolge il nodo canonico `main.py` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 145 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 145 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 145 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 145 registra come `target` l'identificatore `timeline_generator_daily_clinical_profile` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 145 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=146 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `step_profile()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `timeline_generator_step_profile`. La relazione tecnica esplicita è la seguente: `main.py` importa `step_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 146 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 146 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 146 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 146 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 146 che coinvolge il nodo canonico `main.py` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 146 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 146 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 146 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 146 registra come `target` l'identificatore `timeline_generator_step_profile` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 146 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=147 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `timeline_stats()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `timeline_generator_timeline_stats`. La relazione tecnica esplicita è la seguente: `main.py` importa `timeline_stats()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 147 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 147 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 147 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 147 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 147 che coinvolge il nodo canonico `main.py` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 147 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 147 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 147 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 147 registra come `target` l'identificatore `timeline_generator_timeline_stats` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 147 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=148 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `traffic_exec_engine`. La relazione tecnica esplicita è la seguente: `main.py` importa da `traffic_exec_engine.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 148 che coinvolge il nodo canonico `main.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 148 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 148 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 148 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 148 che coinvolge il nodo canonico `main.py` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 148 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 148 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 148 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 148 registra come `target` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 148 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=149 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `main.py` importa `run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 149 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 149 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 149 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 149 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 149 che coinvolge il nodo canonico `main.py` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 149 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 149 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 149 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 149 registra come `target` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 149 che coinvolge il nodo canonico `main.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=150 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `main.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `main`. La relazione tecnica esplicita è la seguente: `test_core.py` importa da `main.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 150 che coinvolge il nodo canonico `main.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 150 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 150 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 150 che coinvolge il nodo canonico `main.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 150 che coinvolge il nodo canonico `main.py` è `L31`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 150 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 150 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo del codice RDG, l'arco 150 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, l'arco 150 registra come `target` l'identificatore `main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 150 che coinvolge il nodo canonico `main.py` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=59 id=Request-Dataset-Generator::main -->
Nel grafo unificato, l'etichetta originale del nodo canonico `main.py` è `main.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `main.py` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `main.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `main.py` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `main.py` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `main.py` è `main.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `main.py` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `main.py` è `main`. Nel grafo unificato, il nodo canonico `main.py` possiede l'identificatore esatto `Request-Dataset-Generator::main`.
<!-- GRAPH_EDGE graph=unificata index=119 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_annotate_dataset_flows()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_annotate_dataset_flows`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_annotate_dataset_flows()`. Nel grafo unificato, il tipo dichiarato dell'arco 119 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 119 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 119 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 119 che coinvolge il nodo canonico `main.py` è `L198`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 119 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 119 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 119 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 119 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 119 registra come `target` l'identificatore `Request-Dataset-Generator::main_annotate_dataset_flows` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=120 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_append_json()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_append_json`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_append_json()`. Nel grafo unificato, il tipo dichiarato dell'arco 120 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 120 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 120 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 120 che coinvolge il nodo canonico `main.py` è `L235`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 120 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 120 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 120 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 120 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 120 registra come `target` l'identificatore `Request-Dataset-Generator::main_append_json` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=121 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_build_namespace()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_build_namespace`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_build_namespace()`. Nel grafo unificato, il tipo dichiarato dell'arco 121 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 121 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 121 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 121 che coinvolge il nodo canonico `main.py` è `L115`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 121 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 121 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 121 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 121 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 121 registra come `target` l'identificatore `Request-Dataset-Generator::main_build_namespace` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=122 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_cfg_section()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_cfg_section`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_cfg_section()`. Nel grafo unificato, il tipo dichiarato dell'arco 122 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 122 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 122 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 122 che coinvolge il nodo canonico `main.py` è `L105`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 122 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 122 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 122 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 122 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 122 registra come `target` l'identificatore `Request-Dataset-Generator::main_cfg_section` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=123 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `cmd_collect()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_cmd_collect`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_collect()`. Nel grafo unificato, il tipo dichiarato dell'arco 123 che coinvolge il nodo canonico `main.py` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 123 che coinvolge il nodo canonico `main.py` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 123 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 123 che coinvolge il nodo canonico `main.py` è `L585`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 123 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 123 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 123 che coinvolge il nodo canonico `main.py` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 123 che coinvolge il nodo canonico `main.py` è `0.5`. Nel grafo unificato, l'arco 123 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 123 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_collect` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=124 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_cmd_execute`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_execute()`. Nel grafo unificato, il tipo dichiarato dell'arco 124 che coinvolge il nodo canonico `main.py` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 124 che coinvolge il nodo canonico `main.py` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 124 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 124 che coinvolge il nodo canonico `main.py` è `L584`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 124 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 124 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 124 che coinvolge il nodo canonico `main.py` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 124 che coinvolge il nodo canonico `main.py` è `0.5`. Nel grafo unificato, l'arco 124 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 124 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=125 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `cmd_metrics()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_cmd_metrics`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_metrics()`. Nel grafo unificato, il tipo dichiarato dell'arco 125 che coinvolge il nodo canonico `main.py` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 125 che coinvolge il nodo canonico `main.py` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 125 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 125 che coinvolge il nodo canonico `main.py` è `L586`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 125 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 125 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 125 che coinvolge il nodo canonico `main.py` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 125 che coinvolge il nodo canonico `main.py` è `0.5`. Nel grafo unificato, l'arco 125 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 125 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_metrics` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=126 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `cmd_patients()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_cmd_patients`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_patients()`. Nel grafo unificato, il tipo dichiarato dell'arco 126 che coinvolge il nodo canonico `main.py` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 126 che coinvolge il nodo canonico `main.py` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 126 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 126 che coinvolge il nodo canonico `main.py` è `L581`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 126 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 126 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 126 che coinvolge il nodo canonico `main.py` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 126 che coinvolge il nodo canonico `main.py` è `0.5`. Nel grafo unificato, l'arco 126 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 126 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_patients` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=127 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `cmd_pipeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_cmd_pipeline`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_pipeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 127 che coinvolge il nodo canonico `main.py` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 127 che coinvolge il nodo canonico `main.py` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 127 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 127 che coinvolge il nodo canonico `main.py` è `L587`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 127 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 127 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 127 che coinvolge il nodo canonico `main.py` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 127 che coinvolge il nodo canonico `main.py` è `0.5`. Nel grafo unificato, l'arco 127 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 127 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=128 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `cmd_requests()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_cmd_requests`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_requests()`. Nel grafo unificato, il tipo dichiarato dell'arco 128 che coinvolge il nodo canonico `main.py` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 128 che coinvolge il nodo canonico `main.py` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 128 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 128 che coinvolge il nodo canonico `main.py` è `L582`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 128 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 128 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 128 che coinvolge il nodo canonico `main.py` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 128 che coinvolge il nodo canonico `main.py` è `0.5`. Nel grafo unificato, l'arco 128 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 128 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_requests` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=129 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_cmd_timeline`. La relazione tecnica esplicita è la seguente: `main.py` effettua una chiamata indiretta verso `cmd_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 129 che coinvolge il nodo canonico `main.py` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 129 che coinvolge il nodo canonico `main.py` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 129 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 129 che coinvolge il nodo canonico `main.py` è `L583`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 129 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 129 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 129 che coinvolge il nodo canonico `main.py` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 129 che coinvolge il nodo canonico `main.py` è `0.5`. Nel grafo unificato, l'arco 129 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 129 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=130 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_configure_run_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_configure_run_outputs`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_configure_run_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 130 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 130 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 130 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 130 che coinvolge il nodo canonico `main.py` è `L162`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 130 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 130 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 130 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 130 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 130 registra come `target` l'identificatore `Request-Dataset-Generator::main_configure_run_outputs` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=131 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_ensure_experiment_metadata()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_ensure_experiment_metadata`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_ensure_experiment_metadata()`. Nel grafo unificato, il tipo dichiarato dell'arco 131 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 131 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 131 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 131 che coinvolge il nodo canonico `main.py` è `L137`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 131 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 131 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 131 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 131 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 131 registra come `target` l'identificatore `Request-Dataset-Generator::main_ensure_experiment_metadata` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=132 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_load_config() [main_load_config]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_load_config`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_load_config() [main_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 132 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 132 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 132 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 132 che coinvolge il nodo canonico `main.py` è `L100`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 132 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 132 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 132 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 132 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 132 registra come `target` l'identificatore `Request-Dataset-Generator::main_load_config` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=133 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `main() [main_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_main`. La relazione tecnica esplicita è la seguente: `main.py` contiene `main() [main_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 133 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 133 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 133 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 133 che coinvolge il nodo canonico `main.py` è `L591`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 133 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 133 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 133 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 133 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 133 registra come `target` l'identificatore `Request-Dataset-Generator::main_main` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=134 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_resolve_output_path() [main_resolve_output_path]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_resolve_output_path`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_resolve_output_path() [main_resolve_output_path]`. Nel grafo unificato, il tipo dichiarato dell'arco 134 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 134 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 134 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 134 che coinvolge il nodo canonico `main.py` è `L211`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 134 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 134 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 134 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 134 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 134 registra come `target` l'identificatore `Request-Dataset-Generator::main_resolve_output_path` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=135 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_slug()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_slug`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_slug()`. Nel grafo unificato, il tipo dichiarato dell'arco 135 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 135 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 135 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 135 che coinvolge il nodo canonico `main.py` è `L132`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 135 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 135 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 135 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 135 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 135 registra come `target` l'identificatore `Request-Dataset-Generator::main_slug` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=136 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_write_output()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_write_output`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_write_output()`. Nel grafo unificato, il tipo dichiarato dell'arco 136 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 136 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 136 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 136 che coinvolge il nodo canonico `main.py` è `L221`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 136 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 136 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 136 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 136 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 136 registra come `target` l'identificatore `Request-Dataset-Generator::main_write_output` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=137 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_write_resolved_config()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::main_write_resolved_config`. La relazione tecnica esplicita è la seguente: `main.py` contiene `_write_resolved_config()`. Nel grafo unificato, il tipo dichiarato dell'arco 137 che coinvolge il nodo canonico `main.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 137 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 137 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 137 che coinvolge il nodo canonico `main.py` è `L189`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 137 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 137 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 137 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 137 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 137 registra come `target` l'identificatore `Request-Dataset-Generator::main_write_resolved_config` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=138 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `metric_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::metric_engine`. La relazione tecnica esplicita è la seguente: `main.py` importa da `metric_engine.py`. Nel grafo unificato, il tipo dichiarato dell'arco 138 che coinvolge il nodo canonico `main.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 138 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 138 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 138 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 138 che coinvolge il nodo canonico `main.py` è `L22`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 138 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 138 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 138 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 138 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 138 registra come `target` l'identificatore `Request-Dataset-Generator::metric_engine` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=139 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `run_metric_engine()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `main.py` importa `run_metric_engine()`. Nel grafo unificato, il tipo dichiarato dell'arco 139 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 139 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 139 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 139 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 139 che coinvolge il nodo canonico `main.py` è `L22`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 139 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 139 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 139 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 139 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 139 registra come `target` l'identificatore `Request-Dataset-Generator::metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=140 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `request_dataset_generator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::request_dataset_generator`. La relazione tecnica esplicita è la seguente: `main.py` importa da `request_dataset_generator.py`. Nel grafo unificato, il tipo dichiarato dell'arco 140 che coinvolge il nodo canonico `main.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 140 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 140 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 140 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 140 che coinvolge il nodo canonico `main.py` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 140 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 140 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 140 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 140 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 140 registra come `target` l'identificatore `Request-Dataset-Generator::request_dataset_generator` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=141 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `main.py` importa `build_workload()`. Nel grafo unificato, il tipo dichiarato dell'arco 141 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 141 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 141 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 141 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 141 che coinvolge il nodo canonico `main.py` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 141 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 141 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 141 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 141 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 141 registra come `target` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=142 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::timeline_generator`. La relazione tecnica esplicita è la seguente: `main.py` importa da `timeline_generator.py`. Nel grafo unificato, il tipo dichiarato dell'arco 142 che coinvolge il nodo canonico `main.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 142 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 142 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 142 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 142 che coinvolge il nodo canonico `main.py` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 142 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 142 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 142 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 142 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 142 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=143 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `main.py` importa `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 143 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 143 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 143 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 143 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 143 che coinvolge il nodo canonico `main.py` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 143 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 143 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 143 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 143 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 143 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=144 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `main.py` importa `constant_rate()`. Nel grafo unificato, il tipo dichiarato dell'arco 144 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 144 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 144 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 144 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 144 che coinvolge il nodo canonico `main.py` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 144 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 144 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 144 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 144 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 144 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=145 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `daily_clinical_profile()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::timeline_generator_daily_clinical_profile`. La relazione tecnica esplicita è la seguente: `main.py` importa `daily_clinical_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 145 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 145 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 145 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 145 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 145 che coinvolge il nodo canonico `main.py` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 145 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 145 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 145 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 145 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 145 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_daily_clinical_profile` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=146 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `step_profile()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::timeline_generator_step_profile`. La relazione tecnica esplicita è la seguente: `main.py` importa `step_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 146 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 146 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 146 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 146 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 146 che coinvolge il nodo canonico `main.py` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 146 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 146 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 146 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 146 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 146 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_step_profile` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=147 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `timeline_stats()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::timeline_generator_timeline_stats`. La relazione tecnica esplicita è la seguente: `main.py` importa `timeline_stats()`. Nel grafo unificato, il tipo dichiarato dell'arco 147 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 147 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 147 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 147 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 147 che coinvolge il nodo canonico `main.py` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 147 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 147 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 147 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 147 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 147 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_timeline_stats` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=148 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::traffic_exec_engine`. La relazione tecnica esplicita è la seguente: `main.py` importa da `traffic_exec_engine.py`. Nel grafo unificato, il tipo dichiarato dell'arco 148 che coinvolge il nodo canonico `main.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 148 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 148 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 148 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 148 che coinvolge il nodo canonico `main.py` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 148 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 148 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 148 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 148 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 148 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=149 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `main.py` importa `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 149 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 149 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 149 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 149 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 149 che coinvolge il nodo canonico `main.py` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 149 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 149 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 149 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 149 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 149 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=150 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::tests_test_core`. La relazione tecnica esplicita è la seguente: `main.py` importa da `test_core.py`. Nel grafo unificato, il tipo dichiarato dell'arco 150 che coinvolge il nodo canonico `main.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 150 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 150 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 150 che coinvolge il nodo canonico `main.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 150 che coinvolge il nodo canonico `main.py` è `L31`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 150 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 150 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 150 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 150 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 150 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=21 role=incoming -->
Nel grafo unificato, il nodo canonico `main.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `fhir_patient_generator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::fhir_patient_generator` all'ID `Request-Dataset-Generator::main`. La relazione tecnica esplicita è la seguente: `fhir_patient_generator.py` importa da `main.py`. Nel grafo unificato, il tipo dichiarato dell'arco 21 che coinvolge il nodo canonico `main.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 21 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 21 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 21 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 21 che coinvolge il nodo canonico `main.py` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 21 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 21 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 21 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 21 registra come `source` l'identificatore `Request-Dataset-Generator::fhir_patient_generator` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 21 registra come `target` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=40 role=incoming -->
Nel grafo unificato, il nodo canonico `main.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `genera_paziente()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::fhir_patient_generator_genera_paziente` all'ID `Request-Dataset-Generator::main`. La relazione tecnica esplicita è la seguente: `genera_paziente()` importa `main.py`. Nel grafo unificato, il tipo dichiarato dell'arco 40 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 40 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 40 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 40 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 40 che coinvolge il nodo canonico `main.py` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 40 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 40 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 40 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 40 registra come `source` l'identificatore `Request-Dataset-Generator::fhir_patient_generator_genera_paziente` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 40 registra come `target` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=45 role=incoming -->
Nel grafo unificato, il nodo canonico `main.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `genera_bundle_rve55()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::fhir_patient_generator_genera_bundle_rve55` all'ID `Request-Dataset-Generator::main`. La relazione tecnica esplicita è la seguente: `genera_bundle_rve55()` importa `main.py`. Nel grafo unificato, il tipo dichiarato dell'arco 45 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 45 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 45 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 45 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 45 che coinvolge il nodo canonico `main.py` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 45 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 45 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 45 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 45 registra come `source` l'identificatore `Request-Dataset-Generator::fhir_patient_generator_genera_bundle_rve55` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 45 registra come `target` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=49 role=incoming -->
Nel grafo unificato, il nodo canonico `main.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `to_json()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::fhir_patient_generator_to_json` all'ID `Request-Dataset-Generator::main`. La relazione tecnica esplicita è la seguente: `to_json()` importa `main.py`. Nel grafo unificato, il tipo dichiarato dell'arco 49 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 49 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 49 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 49 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 49 che coinvolge il nodo canonico `main.py` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 49 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 49 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 49 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 49 registra come `source` l'identificatore `Request-Dataset-Generator::fhir_patient_generator_to_json` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 49 registra come `target` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=54 role=incoming -->
Nel grafo unificato, il nodo canonico `main.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `to_xml()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::fhir_patient_generator_to_xml` all'ID `Request-Dataset-Generator::main`. La relazione tecnica esplicita è la seguente: `to_xml()` importa `main.py`. Nel grafo unificato, il tipo dichiarato dell'arco 54 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 54 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 54 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 54 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 54 che coinvolge il nodo canonico `main.py` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 54 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 54 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 54 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 54 registra come `source` l'identificatore `Request-Dataset-Generator::fhir_patient_generator_to_xml` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 54 registra come `target` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=71 role=incoming -->
Nel grafo unificato, il nodo canonico `main.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::main`. La relazione tecnica esplicita è la seguente: `log_collector.py` importa da `main.py`. Nel grafo unificato, il tipo dichiarato dell'arco 71 che coinvolge il nodo canonico `main.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 71 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 71 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 71 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 71 che coinvolge il nodo canonico `main.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 71 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 71 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 71 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 71 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 71 registra come `target` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`.
<!-- GRAPH_EDGE graph=unificata index=109 role=incoming -->
Nel grafo unificato, il nodo canonico `main.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_collect_logs` all'ID `Request-Dataset-Generator::main`. La relazione tecnica esplicita è la seguente: `collect_logs()` importa `main.py`. Nel grafo unificato, il tipo dichiarato dell'arco 109 che coinvolge il nodo canonico `main.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 109 che coinvolge il nodo canonico `main.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 109 che coinvolge il nodo canonico `main.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 109 che coinvolge il nodo canonico `main.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 109 che coinvolge il nodo canonico `main.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 109 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 109 che coinvolge il nodo canonico `main.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 109 che coinvolge il nodo canonico `main.py` è `1.0`. Nel grafo unificato, l'arco 109 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `main.py`. Nel grafo unificato, l'arco 109 registra come `target` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `main.py`.

### Namespace

<!-- GRAPH_NODE graph=codice index=63 id=namespace -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Namespace` è `Namespace`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Namespace` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Namespace` è ``. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Namespace` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Namespace` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Namespace` possiede l'identificatore esatto `namespace`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Namespace` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Namespace` è `namespace`.
Nel grafo del codice RDG, il nodo canonico `Namespace` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=159 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Namespace` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_build_namespace()`; l'orientamento registrato nel JSON va dall'ID `main_build_namespace` all'ID `namespace`. La relazione tecnica esplicita è la seguente: `_build_namespace()` fa riferimento a `Namespace`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 159 che coinvolge il nodo canonico `Namespace` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 159 che coinvolge il nodo canonico `Namespace` è `return_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 159 che coinvolge il nodo canonico `Namespace` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 159 che coinvolge il nodo canonico `Namespace` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 159 che coinvolge il nodo canonico `Namespace` è `L115`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 159 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 159 che coinvolge il nodo canonico `Namespace` è `ast`. Nel grafo del codice RDG, l'arco 159 registra come `source` l'identificatore `main_build_namespace` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo del codice RDG, l'arco 159 registra come `target` l'identificatore `namespace` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 159 che coinvolge il nodo canonico `Namespace` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=163 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Namespace` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `cmd_collect()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_collect` all'ID `namespace`. La relazione tecnica esplicita è la seguente: `cmd_collect()` fa riferimento a `Namespace`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 163 che coinvolge il nodo canonico `Namespace` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 163 che coinvolge il nodo canonico `Namespace` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 163 che coinvolge il nodo canonico `Namespace` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 163 che coinvolge il nodo canonico `Namespace` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 163 che coinvolge il nodo canonico `Namespace` è `L473`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 163 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 163 che coinvolge il nodo canonico `Namespace` è `ast`. Nel grafo del codice RDG, l'arco 163 registra come `source` l'identificatore `main_cmd_collect` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo del codice RDG, l'arco 163 registra come `target` l'identificatore `namespace` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 163 che coinvolge il nodo canonico `Namespace` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=164 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Namespace` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_execute` all'ID `namespace`. La relazione tecnica esplicita è la seguente: `cmd_execute()` fa riferimento a `Namespace`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 164 che coinvolge il nodo canonico `Namespace` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 164 che coinvolge il nodo canonico `Namespace` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 164 che coinvolge il nodo canonico `Namespace` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 164 che coinvolge il nodo canonico `Namespace` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 164 che coinvolge il nodo canonico `Namespace` è `L295`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 164 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 164 che coinvolge il nodo canonico `Namespace` è `ast`. Nel grafo del codice RDG, l'arco 164 registra come `source` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo del codice RDG, l'arco 164 registra come `target` l'identificatore `namespace` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 164 che coinvolge il nodo canonico `Namespace` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=165 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Namespace` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `cmd_metrics()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_metrics` all'ID `namespace`. La relazione tecnica esplicita è la seguente: `cmd_metrics()` fa riferimento a `Namespace`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 165 che coinvolge il nodo canonico `Namespace` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 165 che coinvolge il nodo canonico `Namespace` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 165 che coinvolge il nodo canonico `Namespace` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 165 che coinvolge il nodo canonico `Namespace` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 165 che coinvolge il nodo canonico `Namespace` è `L495`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 165 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 165 che coinvolge il nodo canonico `Namespace` è `ast`. Nel grafo del codice RDG, l'arco 165 registra come `source` l'identificatore `main_cmd_metrics` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo del codice RDG, l'arco 165 registra come `target` l'identificatore `namespace` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 165 che coinvolge il nodo canonico `Namespace` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=166 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Namespace` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `cmd_patients()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_patients` all'ID `namespace`. La relazione tecnica esplicita è la seguente: `cmd_patients()` fa riferimento a `Namespace`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 166 che coinvolge il nodo canonico `Namespace` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 166 che coinvolge il nodo canonico `Namespace` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 166 che coinvolge il nodo canonico `Namespace` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 166 che coinvolge il nodo canonico `Namespace` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 166 che coinvolge il nodo canonico `Namespace` è `L258`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 166 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 166 che coinvolge il nodo canonico `Namespace` è `ast`. Nel grafo del codice RDG, l'arco 166 registra come `source` l'identificatore `main_cmd_patients` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo del codice RDG, l'arco 166 registra come `target` l'identificatore `namespace` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 166 che coinvolge il nodo canonico `Namespace` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=167 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Namespace` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `cmd_pipeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_pipeline` all'ID `namespace`. La relazione tecnica esplicita è la seguente: `cmd_pipeline()` fa riferimento a `Namespace`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 167 che coinvolge il nodo canonico `Namespace` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 167 che coinvolge il nodo canonico `Namespace` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 167 che coinvolge il nodo canonico `Namespace` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 167 che coinvolge il nodo canonico `Namespace` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 167 che coinvolge il nodo canonico `Namespace` è `L515`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 167 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 167 che coinvolge il nodo canonico `Namespace` è `ast`. Nel grafo del codice RDG, l'arco 167 registra come `source` l'identificatore `main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo del codice RDG, l'arco 167 registra come `target` l'identificatore `namespace` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 167 che coinvolge il nodo canonico `Namespace` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=168 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Namespace` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `cmd_requests()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_requests` all'ID `namespace`. La relazione tecnica esplicita è la seguente: `cmd_requests()` fa riferimento a `Namespace`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 168 che coinvolge il nodo canonico `Namespace` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 168 che coinvolge il nodo canonico `Namespace` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 168 che coinvolge il nodo canonico `Namespace` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 168 che coinvolge il nodo canonico `Namespace` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 168 che coinvolge il nodo canonico `Namespace` è `L277`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 168 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 168 che coinvolge il nodo canonico `Namespace` è `ast`. Nel grafo del codice RDG, l'arco 168 registra come `source` l'identificatore `main_cmd_requests` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo del codice RDG, l'arco 168 registra come `target` l'identificatore `namespace` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 168 che coinvolge il nodo canonico `Namespace` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=169 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Namespace` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_timeline` all'ID `namespace`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` fa riferimento a `Namespace`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 169 che coinvolge il nodo canonico `Namespace` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 169 che coinvolge il nodo canonico `Namespace` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 169 che coinvolge il nodo canonico `Namespace` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 169 che coinvolge il nodo canonico `Namespace` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 169 che coinvolge il nodo canonico `Namespace` è `L408`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 169 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 169 che coinvolge il nodo canonico `Namespace` è `ast`. Nel grafo del codice RDG, l'arco 169 registra come `source` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo del codice RDG, l'arco 169 registra come `target` l'identificatore `namespace` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 169 che coinvolge il nodo canonico `Namespace` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=63 id=Request-Dataset-Generator::namespace -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Namespace` è `Namespace`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Namespace` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Namespace` è ``. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Namespace` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Namespace` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Namespace` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Namespace` è `namespace`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Namespace` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Namespace` è `namespace`. Nel grafo unificato, il nodo canonico `Namespace` possiede l'identificatore esatto `Request-Dataset-Generator::namespace`.
<!-- GRAPH_EDGE graph=unificata index=163 role=outgoing -->
Nel grafo unificato, il nodo canonico `Namespace` è la sorgente `source` della relazione `references` verso il nodo canonico `cmd_collect()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::namespace` all'ID `Request-Dataset-Generator::main_cmd_collect`. La relazione tecnica esplicita è la seguente: `Namespace` fa riferimento a `cmd_collect()`. Nel grafo unificato, il tipo dichiarato dell'arco 163 che coinvolge il nodo canonico `Namespace` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 163 che coinvolge il nodo canonico `Namespace` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 163 che coinvolge il nodo canonico `Namespace` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 163 che coinvolge il nodo canonico `Namespace` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 163 che coinvolge il nodo canonico `Namespace` è `L473`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 163 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 163 che coinvolge il nodo canonico `Namespace` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 163 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo unificato, l'arco 163 registra come `source` l'identificatore `Request-Dataset-Generator::namespace` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo unificato, l'arco 163 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_collect` per la relazione che coinvolge il nodo canonico `Namespace`.
<!-- GRAPH_EDGE graph=unificata index=164 role=outgoing -->
Nel grafo unificato, il nodo canonico `Namespace` è la sorgente `source` della relazione `references` verso il nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::namespace` all'ID `Request-Dataset-Generator::main_cmd_execute`. La relazione tecnica esplicita è la seguente: `Namespace` fa riferimento a `cmd_execute()`. Nel grafo unificato, il tipo dichiarato dell'arco 164 che coinvolge il nodo canonico `Namespace` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 164 che coinvolge il nodo canonico `Namespace` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 164 che coinvolge il nodo canonico `Namespace` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 164 che coinvolge il nodo canonico `Namespace` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 164 che coinvolge il nodo canonico `Namespace` è `L295`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 164 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 164 che coinvolge il nodo canonico `Namespace` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 164 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo unificato, l'arco 164 registra come `source` l'identificatore `Request-Dataset-Generator::namespace` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo unificato, l'arco 164 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `Namespace`.
<!-- GRAPH_EDGE graph=unificata index=165 role=outgoing -->
Nel grafo unificato, il nodo canonico `Namespace` è la sorgente `source` della relazione `references` verso il nodo canonico `cmd_metrics()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::namespace` all'ID `Request-Dataset-Generator::main_cmd_metrics`. La relazione tecnica esplicita è la seguente: `Namespace` fa riferimento a `cmd_metrics()`. Nel grafo unificato, il tipo dichiarato dell'arco 165 che coinvolge il nodo canonico `Namespace` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 165 che coinvolge il nodo canonico `Namespace` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 165 che coinvolge il nodo canonico `Namespace` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 165 che coinvolge il nodo canonico `Namespace` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 165 che coinvolge il nodo canonico `Namespace` è `L495`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 165 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 165 che coinvolge il nodo canonico `Namespace` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 165 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo unificato, l'arco 165 registra come `source` l'identificatore `Request-Dataset-Generator::namespace` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo unificato, l'arco 165 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_metrics` per la relazione che coinvolge il nodo canonico `Namespace`.
<!-- GRAPH_EDGE graph=unificata index=166 role=outgoing -->
Nel grafo unificato, il nodo canonico `Namespace` è la sorgente `source` della relazione `references` verso il nodo canonico `cmd_patients()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::namespace` all'ID `Request-Dataset-Generator::main_cmd_patients`. La relazione tecnica esplicita è la seguente: `Namespace` fa riferimento a `cmd_patients()`. Nel grafo unificato, il tipo dichiarato dell'arco 166 che coinvolge il nodo canonico `Namespace` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 166 che coinvolge il nodo canonico `Namespace` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 166 che coinvolge il nodo canonico `Namespace` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 166 che coinvolge il nodo canonico `Namespace` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 166 che coinvolge il nodo canonico `Namespace` è `L258`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 166 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 166 che coinvolge il nodo canonico `Namespace` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 166 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo unificato, l'arco 166 registra come `source` l'identificatore `Request-Dataset-Generator::namespace` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo unificato, l'arco 166 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_patients` per la relazione che coinvolge il nodo canonico `Namespace`.
<!-- GRAPH_EDGE graph=unificata index=167 role=outgoing -->
Nel grafo unificato, il nodo canonico `Namespace` è la sorgente `source` della relazione `references` verso il nodo canonico `cmd_pipeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::namespace` all'ID `Request-Dataset-Generator::main_cmd_pipeline`. La relazione tecnica esplicita è la seguente: `Namespace` fa riferimento a `cmd_pipeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 167 che coinvolge il nodo canonico `Namespace` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 167 che coinvolge il nodo canonico `Namespace` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 167 che coinvolge il nodo canonico `Namespace` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 167 che coinvolge il nodo canonico `Namespace` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 167 che coinvolge il nodo canonico `Namespace` è `L515`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 167 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 167 che coinvolge il nodo canonico `Namespace` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 167 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo unificato, l'arco 167 registra come `source` l'identificatore `Request-Dataset-Generator::namespace` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo unificato, l'arco 167 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `Namespace`.
<!-- GRAPH_EDGE graph=unificata index=168 role=outgoing -->
Nel grafo unificato, il nodo canonico `Namespace` è la sorgente `source` della relazione `references` verso il nodo canonico `cmd_requests()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::namespace` all'ID `Request-Dataset-Generator::main_cmd_requests`. La relazione tecnica esplicita è la seguente: `Namespace` fa riferimento a `cmd_requests()`. Nel grafo unificato, il tipo dichiarato dell'arco 168 che coinvolge il nodo canonico `Namespace` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 168 che coinvolge il nodo canonico `Namespace` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 168 che coinvolge il nodo canonico `Namespace` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 168 che coinvolge il nodo canonico `Namespace` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 168 che coinvolge il nodo canonico `Namespace` è `L277`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 168 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 168 che coinvolge il nodo canonico `Namespace` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 168 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo unificato, l'arco 168 registra come `source` l'identificatore `Request-Dataset-Generator::namespace` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo unificato, l'arco 168 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_requests` per la relazione che coinvolge il nodo canonico `Namespace`.
<!-- GRAPH_EDGE graph=unificata index=169 role=outgoing -->
Nel grafo unificato, il nodo canonico `Namespace` è la sorgente `source` della relazione `references` verso il nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::namespace` all'ID `Request-Dataset-Generator::main_cmd_timeline`. La relazione tecnica esplicita è la seguente: `Namespace` fa riferimento a `cmd_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 169 che coinvolge il nodo canonico `Namespace` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 169 che coinvolge il nodo canonico `Namespace` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 169 che coinvolge il nodo canonico `Namespace` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 169 che coinvolge il nodo canonico `Namespace` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 169 che coinvolge il nodo canonico `Namespace` è `L408`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 169 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 169 che coinvolge il nodo canonico `Namespace` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 169 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo unificato, l'arco 169 registra come `source` l'identificatore `Request-Dataset-Generator::namespace` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo unificato, l'arco 169 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `Namespace`.
<!-- GRAPH_EDGE graph=unificata index=159 role=incoming -->
Nel grafo unificato, il nodo canonico `Namespace` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_build_namespace()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_build_namespace` all'ID `Request-Dataset-Generator::namespace`. La relazione tecnica esplicita è la seguente: `_build_namespace()` fa riferimento a `Namespace`. Nel grafo unificato, il tipo dichiarato dell'arco 159 che coinvolge il nodo canonico `Namespace` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 159 che coinvolge il nodo canonico `Namespace` è `return_type`. Nel grafo unificato, la confidenza categoriale dell'arco 159 che coinvolge il nodo canonico `Namespace` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 159 che coinvolge il nodo canonico `Namespace` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 159 che coinvolge il nodo canonico `Namespace` è `L115`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 159 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 159 che coinvolge il nodo canonico `Namespace` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 159 che coinvolge il nodo canonico `Namespace` è `1.0`. Nel grafo unificato, l'arco 159 registra come `source` l'identificatore `Request-Dataset-Generator::main_build_namespace` per la relazione che coinvolge il nodo canonico `Namespace`. Nel grafo unificato, l'arco 159 registra come `target` l'identificatore `Request-Dataset-Generator::namespace` per la relazione che coinvolge il nodo canonico `Namespace`.

### Pipeline completa: genera dataset → genera timeline → esegue traffic engine.

<!-- GRAPH_NODE graph=codice index=83 id=main_rationale_296 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `L296`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` possiede l'identificatore esatto `main_rationale_296`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `pipeline completa: genera dataset → genera timeline → esegue traffic engine.`.
<!-- GRAPH_EDGE graph=codice index=196 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `main_rationale_296` all'ID `main_cmd_execute`. La relazione tecnica esplicita è la seguente: `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` costituisce una motivazione per `cmd_execute()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 196 che coinvolge il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 196 che coinvolge il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 196 che coinvolge il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 196 che coinvolge il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `L296`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 196 che coinvolge il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 196 che coinvolge il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `ast`. Nel grafo del codice RDG, l'arco 196 registra come `source` l'identificatore `main_rationale_296` per la relazione che coinvolge il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.`. Nel grafo del codice RDG, l'arco 196 registra come `target` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 196 che coinvolge il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=83 id=Request-Dataset-Generator::main_rationale_296 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `L296`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `pipeline completa: genera dataset → genera timeline → esegue traffic engine.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `main_rationale_296`. Nel grafo unificato, il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` possiede l'identificatore esatto `Request-Dataset-Generator::main_rationale_296`.
Nel grafo unificato, il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=196 role=incoming -->
Nel grafo unificato, il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_execute` all'ID `Request-Dataset-Generator::main_rationale_296`. La relazione tecnica esplicita è la seguente: `cmd_execute()` costituisce una motivazione per `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.`. Nel grafo unificato, il tipo dichiarato dell'arco 196 che coinvolge il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 196 che coinvolge il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 196 che coinvolge il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 196 che coinvolge il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `L296`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 196 che coinvolge il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 196 che coinvolge il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 196 che coinvolge il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.` è `1.0`. Nel grafo unificato, l'arco 196 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.`. Nel grafo unificato, l'arco 196 registra come `target` l'identificatore `Request-Dataset-Generator::main_rationale_296` per la relazione che coinvolge il nodo canonico `Pipeline completa: genera dataset → genera timeline → esegue traffic engine.`.

### Profilo a gradini definito dall&#x27;utente.      Ogni step è {&quot;until&quot;: &lt;secondi&gt;, &quot;r

<!-- GRAPH_NODE graph=codice index=333 id=timeline_generator_rationale_60 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `L60`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` possiede l'identificatore esatto `timeline_generator_rationale_60`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `profilo a gradini definito dall'utente.      ogni step e {"until": <secondi>, "r`.
<!-- GRAPH_EDGE graph=codice index=654 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `step_profile()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_rationale_60` all'ID `timeline_generator_step_profile`. La relazione tecnica esplicita è la seguente: `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` costituisce una motivazione per `step_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 654 che coinvolge il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 654 che coinvolge il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 654 che coinvolge il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 654 che coinvolge il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `L60`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 654 che coinvolge il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 654 che coinvolge il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `ast`. Nel grafo del codice RDG, l'arco 654 registra come `source` l'identificatore `timeline_generator_rationale_60` per la relazione che coinvolge il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r`. Nel grafo del codice RDG, l'arco 654 registra come `target` l'identificatore `timeline_generator_step_profile` per la relazione che coinvolge il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 654 che coinvolge il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=333 id=Request-Dataset-Generator::timeline_generator_rationale_60 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `L60`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `profilo a gradini definito dall'utente.      ogni step e {"until": <secondi>, "r`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `timeline_generator_rationale_60`. Nel grafo unificato, il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` possiede l'identificatore esatto `Request-Dataset-Generator::timeline_generator_rationale_60`.
Nel grafo unificato, il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=654 role=incoming -->
Nel grafo unificato, il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `step_profile()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator_step_profile` all'ID `Request-Dataset-Generator::timeline_generator_rationale_60`. La relazione tecnica esplicita è la seguente: `step_profile()` costituisce una motivazione per `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r`. Nel grafo unificato, il tipo dichiarato dell'arco 654 che coinvolge il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 654 che coinvolge il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 654 che coinvolge il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 654 che coinvolge il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `L60`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 654 che coinvolge il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 654 che coinvolge il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 654 che coinvolge il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` è `1.0`. Nel grafo unificato, l'arco 654 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator_step_profile` per la relazione che coinvolge il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r`. Nel grafo unificato, l'arco 654 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_rationale_60` per la relazione che coinvolge il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r`.

### Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas

<!-- GRAPH_NODE graph=codice index=332 id=timeline_generator_rationale_34 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `L34`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` possiede l'identificatore esatto `timeline_generator_rationale_34`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      fas`.
<!-- GRAPH_EDGE graph=codice index=653 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `daily_clinical_profile()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_rationale_34` all'ID `timeline_generator_daily_clinical_profile`. La relazione tecnica esplicita è la seguente: `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` costituisce una motivazione per `daily_clinical_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 653 che coinvolge il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 653 che coinvolge il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 653 che coinvolge il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 653 che coinvolge il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `L34`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 653 che coinvolge il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 653 che coinvolge il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `ast`. Nel grafo del codice RDG, l'arco 653 registra come `source` l'identificatore `timeline_generator_rationale_34` per la relazione che coinvolge il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas`. Nel grafo del codice RDG, l'arco 653 registra come `target` l'identificatore `timeline_generator_daily_clinical_profile` per la relazione che coinvolge il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 653 che coinvolge il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=332 id=Request-Dataset-Generator::timeline_generator_rationale_34 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `L34`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      fas`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `timeline_generator_rationale_34`. Nel grafo unificato, il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` possiede l'identificatore esatto `Request-Dataset-Generator::timeline_generator_rationale_34`.
Nel grafo unificato, il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=653 role=incoming -->
Nel grafo unificato, il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `daily_clinical_profile()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator_daily_clinical_profile` all'ID `Request-Dataset-Generator::timeline_generator_rationale_34`. La relazione tecnica esplicita è la seguente: `daily_clinical_profile()` costituisce una motivazione per `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas`. Nel grafo unificato, il tipo dichiarato dell'arco 653 che coinvolge il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 653 che coinvolge il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 653 che coinvolge il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 653 che coinvolge il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `L34`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 653 che coinvolge il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 653 che coinvolge il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 653 che coinvolge il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas` è `1.0`. Nel grafo unificato, l'arco 653 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator_daily_clinical_profile` per la relazione che coinvolge il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas`. Nel grafo unificato, l'arco 653 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_rationale_34` per la relazione che coinvolge il nodo canonico `Profilo giornaliero ospedaliero (durata simulata: 1 giornata = 86400s).      Fas`.

### Rate costante: processo di Poisson omogeneo.

<!-- GRAPH_NODE graph=codice index=331 id=timeline_generator_rationale_23 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Rate costante: processo di Poisson omogeneo.` è `Rate costante: processo di Poisson omogeneo.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Rate costante: processo di Poisson omogeneo.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Rate costante: processo di Poisson omogeneo.` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Rate costante: processo di Poisson omogeneo.` è `L23`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Rate costante: processo di Poisson omogeneo.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Rate costante: processo di Poisson omogeneo.` possiede l'identificatore esatto `timeline_generator_rationale_23`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Rate costante: processo di Poisson omogeneo.` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Rate costante: processo di Poisson omogeneo.` è `rate costante: processo di poisson omogeneo.`.
<!-- GRAPH_EDGE graph=codice index=652 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Rate costante: processo di Poisson omogeneo.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_rationale_23` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `Rate costante: processo di Poisson omogeneo.` costituisce una motivazione per `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 652 che coinvolge il nodo canonico `Rate costante: processo di Poisson omogeneo.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 652 che coinvolge il nodo canonico `Rate costante: processo di Poisson omogeneo.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 652 che coinvolge il nodo canonico `Rate costante: processo di Poisson omogeneo.` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 652 che coinvolge il nodo canonico `Rate costante: processo di Poisson omogeneo.` è `L23`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 652 che coinvolge il nodo canonico `Rate costante: processo di Poisson omogeneo.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 652 che coinvolge il nodo canonico `Rate costante: processo di Poisson omogeneo.` è `ast`. Nel grafo del codice RDG, l'arco 652 registra come `source` l'identificatore `timeline_generator_rationale_23` per la relazione che coinvolge il nodo canonico `Rate costante: processo di Poisson omogeneo.`. Nel grafo del codice RDG, l'arco 652 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `Rate costante: processo di Poisson omogeneo.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 652 che coinvolge il nodo canonico `Rate costante: processo di Poisson omogeneo.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Rate costante: processo di Poisson omogeneo.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=331 id=Request-Dataset-Generator::timeline_generator_rationale_23 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Rate costante: processo di Poisson omogeneo.` è `Rate costante: processo di Poisson omogeneo.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Rate costante: processo di Poisson omogeneo.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Rate costante: processo di Poisson omogeneo.` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Rate costante: processo di Poisson omogeneo.` è `L23`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Rate costante: processo di Poisson omogeneo.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Rate costante: processo di Poisson omogeneo.` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Rate costante: processo di Poisson omogeneo.` è `rate costante: processo di poisson omogeneo.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Rate costante: processo di Poisson omogeneo.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Rate costante: processo di Poisson omogeneo.` è `timeline_generator_rationale_23`. Nel grafo unificato, il nodo canonico `Rate costante: processo di Poisson omogeneo.` possiede l'identificatore esatto `Request-Dataset-Generator::timeline_generator_rationale_23`.
Nel grafo unificato, il nodo canonico `Rate costante: processo di Poisson omogeneo.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=652 role=incoming -->
Nel grafo unificato, il nodo canonico `Rate costante: processo di Poisson omogeneo.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator_constant_rate` all'ID `Request-Dataset-Generator::timeline_generator_rationale_23`. La relazione tecnica esplicita è la seguente: `constant_rate()` costituisce una motivazione per `Rate costante: processo di Poisson omogeneo.`. Nel grafo unificato, il tipo dichiarato dell'arco 652 che coinvolge il nodo canonico `Rate costante: processo di Poisson omogeneo.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 652 che coinvolge il nodo canonico `Rate costante: processo di Poisson omogeneo.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 652 che coinvolge il nodo canonico `Rate costante: processo di Poisson omogeneo.` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 652 che coinvolge il nodo canonico `Rate costante: processo di Poisson omogeneo.` è `L23`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 652 che coinvolge il nodo canonico `Rate costante: processo di Poisson omogeneo.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 652 che coinvolge il nodo canonico `Rate costante: processo di Poisson omogeneo.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 652 che coinvolge il nodo canonico `Rate costante: processo di Poisson omogeneo.` è `1.0`. Nel grafo unificato, l'arco 652 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `Rate costante: processo di Poisson omogeneo.`. Nel grafo unificato, l'arco 652 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_rationale_23` per la relazione che coinvolge il nodo canonico `Rate costante: processo di Poisson omogeneo.`.

### Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica

<!-- GRAPH_NODE graph=codice index=80 id=main_rationale_106 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `main.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `L106`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` possiede l'identificatore esatto `main_rationale_106`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `ritorna la sezione del config mergiata con i defaults corrispondenti.     unica`.
<!-- GRAPH_EDGE graph=codice index=158 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `_cfg_section()`; l'orientamento registrato nel JSON va dall'ID `main_rationale_106` all'ID `main_cfg_section`. La relazione tecnica esplicita è la seguente: `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` costituisce una motivazione per `_cfg_section()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 158 che coinvolge il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 158 che coinvolge il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 158 che coinvolge il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 158 che coinvolge il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `L106`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 158 che coinvolge il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 158 che coinvolge il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `ast`. Nel grafo del codice RDG, l'arco 158 registra come `source` l'identificatore `main_rationale_106` per la relazione che coinvolge il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica`. Nel grafo del codice RDG, l'arco 158 registra come `target` l'identificatore `main_cfg_section` per la relazione che coinvolge il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 158 che coinvolge il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=80 id=Request-Dataset-Generator::main_rationale_106 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `main.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `L106`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `ritorna la sezione del config mergiata con i defaults corrispondenti.     unica`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `main_rationale_106`. Nel grafo unificato, il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` possiede l'identificatore esatto `Request-Dataset-Generator::main_rationale_106`.
Nel grafo unificato, il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=158 role=incoming -->
Nel grafo unificato, il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `_cfg_section()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cfg_section` all'ID `Request-Dataset-Generator::main_rationale_106`. La relazione tecnica esplicita è la seguente: `_cfg_section()` costituisce una motivazione per `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica`. Nel grafo unificato, il tipo dichiarato dell'arco 158 che coinvolge il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 158 che coinvolge il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 158 che coinvolge il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 158 che coinvolge il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `L106`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 158 che coinvolge il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 158 che coinvolge il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 158 che coinvolge il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica` è `1.0`. Nel grafo unificato, l'arco 158 registra come `source` l'identificatore `Request-Dataset-Generator::main_cfg_section` per la relazione che coinvolge il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica`. Nel grafo unificato, l'arco 158 registra come `target` l'identificatore `Request-Dataset-Generator::main_rationale_106` per la relazione che coinvolge il nodo canonico `Ritorna la sezione del config mergiata con i DEFAULTS corrispondenti.     Unica`.

### run()

<!-- GRAPH_NODE graph=codice index=388 id=traffic_exec_engine_run -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `run()` è `run()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `run()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `run()` è `L967`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `run()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `run()` possiede l'identificatore esatto `traffic_exec_engine_run`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `run()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `run()` è `run()`.
<!-- GRAPH_EDGE graph=codice index=750 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `run()` è la sorgente `source` della relazione `calls` verso il nodo canonico `MtlsContextFactory`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_run` all'ID `traffic_exec_engine_mtlscontextfactory`. La relazione tecnica esplicita è la seguente: `run()` chiama `MtlsContextFactory`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 750 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 750 che coinvolge il nodo canonico `run()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 750 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 750 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 750 che coinvolge il nodo canonico `run()` è `L997`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 750 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 750 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo del codice RDG, l'arco 750 registra come `source` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, l'arco 750 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 750 che coinvolge il nodo canonico `run()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=764 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `run()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_run` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `run()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 764 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 764 che coinvolge il nodo canonico `run()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 764 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 764 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 764 che coinvolge il nodo canonico `run()` è `L982`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 764 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 764 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo del codice RDG, l'arco 764 registra come `source` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, l'arco 764 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 764 che coinvolge il nodo canonico `run()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=773 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `run()` è la sorgente `source` della relazione `calls` verso il nodo canonico `FaultInjector`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_run` all'ID `traffic_exec_engine_faultinjector`. La relazione tecnica esplicita è la seguente: `run()` chiama `FaultInjector`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 773 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 773 che coinvolge il nodo canonico `run()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 773 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 773 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 773 che coinvolge il nodo canonico `run()` è `L996`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 773 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 773 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo del codice RDG, l'arco 773 registra come `source` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, l'arco 773 registra come `target` l'identificatore `traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 773 che coinvolge il nodo canonico `run()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=787 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `run()` è la sorgente `source` della relazione `calls` verso il nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_run` all'ID `traffic_exec_engine_executionlogger`. La relazione tecnica esplicita è la seguente: `run()` chiama `ExecutionLogger`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 787 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 787 che coinvolge il nodo canonico `run()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 787 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 787 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 787 che coinvolge il nodo canonico `run()` è `L993`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 787 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 787 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo del codice RDG, l'arco 787 registra come `source` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, l'arco 787 registra come `target` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 787 che coinvolge il nodo canonico `run()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=794 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `run()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.flush()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_run` all'ID `traffic_exec_engine_executionlogger_flush`. La relazione tecnica esplicita è la seguente: `run()` chiama `.flush()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 794 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 794 che coinvolge il nodo canonico `run()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 794 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 794 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 794 che coinvolge il nodo canonico `run()` è `L1075`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 794 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 794 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo del codice RDG, l'arco 794 registra come `source` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, l'arco 794 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_flush` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 794 che coinvolge il nodo canonico `run()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=802 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `run()` è la sorgente `source` della relazione `calls` verso il nodo canonico `ConcurrencyTracker`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_run` all'ID `traffic_exec_engine_concurrencytracker`. La relazione tecnica esplicita è la seguente: `run()` chiama `ConcurrencyTracker`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 802 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 802 che coinvolge il nodo canonico `run()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 802 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 802 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 802 che coinvolge il nodo canonico `run()` è `L995`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 802 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 802 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo del codice RDG, l'arco 802 registra come `source` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, l'arco 802 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 802 che coinvolge il nodo canonico `run()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=807 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `run()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_run` all'ID `traffic_exec_engine_concurrencytracker_summary`. La relazione tecnica esplicita è la seguente: `run()` chiama `.summary() [traffic_exec_engine_concurrencytracker_summary]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 807 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 807 che coinvolge il nodo canonico `run()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 807 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 807 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 807 che coinvolge il nodo canonico `run()` è `L1077`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 807 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 807 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo del codice RDG, l'arco 807 registra come `source` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, l'arco 807 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_summary` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 807 che coinvolge il nodo canonico `run()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=810 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `run()` è la sorgente `source` della relazione `calls` verso il nodo canonico `Semaphore`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_run` all'ID `semaphore`. La relazione tecnica esplicita è la seguente: `run()` chiama `Semaphore`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 810 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 810 che coinvolge il nodo canonico `run()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 810 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 810 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 810 che coinvolge il nodo canonico `run()` è `L994`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 810 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 810 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo del codice RDG, l'arco 810 registra come `source` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, l'arco 810 registra come `target` l'identificatore `semaphore` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 810 che coinvolge il nodo canonico `run()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=149 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `run()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `main.py` importa `run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 149 che coinvolge il nodo canonico `run()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 149 che coinvolge il nodo canonico `run()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 149 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 149 che coinvolge il nodo canonico `run()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 149 che coinvolge il nodo canonico `run()` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 149 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 149 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo del codice RDG, l'arco 149 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, l'arco 149 registra come `target` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 149 che coinvolge il nodo canonico `run()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=194 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_execute` all'ID `traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 194 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 194 che coinvolge il nodo canonico `run()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 194 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 194 che coinvolge il nodo canonico `run()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 194 che coinvolge il nodo canonico `run()` è `L385`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 194 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 194 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo del codice RDG, l'arco 194 registra come `source` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, l'arco 194 registra come `target` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 194 che coinvolge il nodo canonico `run()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=591 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `run()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 591 che coinvolge il nodo canonico `run()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 591 che coinvolge il nodo canonico `run()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 591 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 591 che coinvolge il nodo canonico `run()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 591 che coinvolge il nodo canonico `run()` è `L25`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 591 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 591 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo del codice RDG, l'arco 591 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, l'arco 591 registra come `target` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 591 che coinvolge il nodo canonico `run()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=624 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_dataset_to_engine_mock_immediate()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate` all'ID `traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.test_dataset_to_engine_mock_immediate()` chiama `run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 624 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 624 che coinvolge il nodo canonico `run()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 624 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 624 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 624 che coinvolge il nodo canonico `run()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 624 che coinvolge il nodo canonico `run()` è `L583`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 624 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 624 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo del codice RDG, l'arco 624 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, l'arco 624 registra come `target` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=codice index=627 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` all'ID `traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.test_framework_log_is_sufficient_for_normalized_dataset()` chiama `run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 627 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 627 che coinvolge il nodo canonico `run()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 627 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 627 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 627 che coinvolge il nodo canonico `run()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 627 che coinvolge il nodo canonico `run()` è `L627`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 627 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 627 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo del codice RDG, l'arco 627 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, l'arco 627 registra come `target` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=codice index=630 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` all'ID `traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.test_error_injection_produces_measurable_failures_and_skipped_steps()` chiama `run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 630 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 630 che coinvolge il nodo canonico `run()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 630 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 630 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 630 che coinvolge il nodo canonico `run()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 630 che coinvolge il nodo canonico `run()` è `L685`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 630 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 630 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo del codice RDG, l'arco 630 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, l'arco 630 registra come `target` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=codice index=633 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_metric_engine_writes_csv_tables()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` all'ID `traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.test_metric_engine_writes_csv_tables()` chiama `run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 633 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 633 che coinvolge il nodo canonico `run()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 633 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 633 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 633 che coinvolge il nodo canonico `run()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 633 che coinvolge il nodo canonico `run()` è `L739`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 633 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 633 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo del codice RDG, l'arco 633 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, l'arco 633 registra come `target` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=codice index=685 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `run()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 685 che coinvolge il nodo canonico `run()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 685 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 685 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 685 che coinvolge il nodo canonico `run()` è `L967`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 685 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 685 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo del codice RDG, l'arco 685 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, l'arco 685 registra come `target` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 685 che coinvolge il nodo canonico `run()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=811 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `main() [traffic_exec_engine_main]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_main` all'ID `traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `main() [traffic_exec_engine_main]` chiama `run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 811 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 811 che coinvolge il nodo canonico `run()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 811 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 811 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 811 che coinvolge il nodo canonico `run()` è `L1213`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 811 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 811 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo del codice RDG, l'arco 811 registra come `source` l'identificatore `traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, l'arco 811 registra come `target` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 811 che coinvolge il nodo canonico `run()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=812 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `run()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_976` all'ID `traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re` costituisce una motivazione per `run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 812 che coinvolge il nodo canonico `run()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 812 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 812 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 812 che coinvolge il nodo canonico `run()` è `L976`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 812 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 812 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo del codice RDG, l'arco 812 registra come `source` l'identificatore `traffic_exec_engine_rationale_976` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, l'arco 812 registra come `target` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 812 che coinvolge il nodo canonico `run()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=388 id=Request-Dataset-Generator::traffic_exec_engine_run -->
Nel grafo unificato, l'etichetta originale del nodo canonico `run()` è `run()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `run()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `run()` è `L967`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `run()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `run()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `run()` è `run()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `run()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `run()` è `traffic_exec_engine_run`. Nel grafo unificato, il nodo canonico `run()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_run`.
<!-- GRAPH_EDGE graph=unificata index=811 role=outgoing -->
Nel grafo unificato, il nodo canonico `run()` è la sorgente `source` della relazione `calls` verso il nodo canonico `main() [traffic_exec_engine_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_run` all'ID `Request-Dataset-Generator::traffic_exec_engine_main`. La relazione tecnica esplicita è la seguente: `run()` chiama `main() [traffic_exec_engine_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 811 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 811 che coinvolge il nodo canonico `run()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 811 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 811 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 811 che coinvolge il nodo canonico `run()` è `L1213`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 811 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 811 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 811 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, l'arco 811 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo unificato, l'arco 811 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=unificata index=812 role=outgoing -->
Nel grafo unificato, il nodo canonico `run()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_run` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_976`. La relazione tecnica esplicita è la seguente: `run()` costituisce una motivazione per `Esegue tutti i flussi della timeline rispettando i timestamp schedulati.      Re`. Nel grafo unificato, il tipo dichiarato dell'arco 812 che coinvolge il nodo canonico `run()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 812 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 812 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 812 che coinvolge il nodo canonico `run()` è `L976`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 812 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 812 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 812 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, l'arco 812 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo unificato, l'arco 812 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_976` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=unificata index=149 role=incoming -->
Nel grafo unificato, il nodo canonico `run()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `main.py` importa `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 149 che coinvolge il nodo canonico `run()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 149 che coinvolge il nodo canonico `run()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 149 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 149 che coinvolge il nodo canonico `run()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 149 che coinvolge il nodo canonico `run()` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 149 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 149 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 149 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, l'arco 149 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo unificato, l'arco 149 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=unificata index=194 role=incoming -->
Nel grafo unificato, il nodo canonico `run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_execute` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 194 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 194 che coinvolge il nodo canonico `run()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 194 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 194 che coinvolge il nodo canonico `run()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 194 che coinvolge il nodo canonico `run()` è `L385`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 194 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 194 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 194 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, l'arco 194 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo unificato, l'arco 194 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=unificata index=591 role=incoming -->
Nel grafo unificato, il nodo canonico `run()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 591 che coinvolge il nodo canonico `run()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 591 che coinvolge il nodo canonico `run()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 591 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 591 che coinvolge il nodo canonico `run()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 591 che coinvolge il nodo canonico `run()` è `L25`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 591 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 591 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 591 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, l'arco 591 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo unificato, l'arco 591 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=unificata index=624 role=incoming -->
Nel grafo unificato, il nodo canonico `run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_dataset_to_engine_mock_immediate()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.test_dataset_to_engine_mock_immediate()` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 624 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 624 che coinvolge il nodo canonico `run()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 624 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 624 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 624 che coinvolge il nodo canonico `run()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 624 che coinvolge il nodo canonico `run()` è `L583`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 624 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 624 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo unificato, l'arco 624 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_dataset_to_engine_mock_immediate` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo unificato, l'arco 624 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=unificata index=627 role=incoming -->
Nel grafo unificato, il nodo canonico `run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.test_framework_log_is_sufficient_for_normalized_dataset()` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 627 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 627 che coinvolge il nodo canonico `run()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 627 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 627 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 627 che coinvolge il nodo canonico `run()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 627 che coinvolge il nodo canonico `run()` è `L627`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 627 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 627 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo unificato, l'arco 627 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo unificato, l'arco 627 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=unificata index=630 role=incoming -->
Nel grafo unificato, il nodo canonico `run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.test_error_injection_produces_measurable_failures_and_skipped_steps()` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 630 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 630 che coinvolge il nodo canonico `run()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 630 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 630 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 630 che coinvolge il nodo canonico `run()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 630 che coinvolge il nodo canonico `run()` è `L685`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 630 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 630 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo unificato, l'arco 630 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo unificato, l'arco 630 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=unificata index=633 role=incoming -->
Nel grafo unificato, il nodo canonico `run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_metric_engine_writes_csv_tables()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.test_metric_engine_writes_csv_tables()` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 633 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 633 che coinvolge il nodo canonico `run()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 633 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 633 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 633 che coinvolge il nodo canonico `run()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 633 che coinvolge il nodo canonico `run()` è `L739`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 633 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 633 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo unificato, l'arco 633 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo unificato, l'arco 633 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=unificata index=685 role=incoming -->
Nel grafo unificato, il nodo canonico `run()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 685 che coinvolge il nodo canonico `run()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 685 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 685 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 685 che coinvolge il nodo canonico `run()` è `L967`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 685 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 685 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 685 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, l'arco 685 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo unificato, l'arco 685 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=unificata index=750 role=incoming -->
Nel grafo unificato, il nodo canonico `run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `MtlsContextFactory`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `MtlsContextFactory` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 750 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 750 che coinvolge il nodo canonico `run()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 750 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 750 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 750 che coinvolge il nodo canonico `run()` è `L997`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 750 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 750 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 750 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, l'arco 750 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo unificato, l'arco 750 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=unificata index=764 role=incoming -->
Nel grafo unificato, il nodo canonico `run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.get()` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 764 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 764 che coinvolge il nodo canonico `run()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 764 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 764 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 764 che coinvolge il nodo canonico `run()` è `L982`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 764 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 764 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 764 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, l'arco 764 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo unificato, l'arco 764 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=unificata index=773 role=incoming -->
Nel grafo unificato, il nodo canonico `run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `FaultInjector`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `FaultInjector` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 773 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 773 che coinvolge il nodo canonico `run()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 773 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 773 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 773 che coinvolge il nodo canonico `run()` è `L996`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 773 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 773 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 773 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, l'arco 773 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo unificato, l'arco 773 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=unificata index=787 role=incoming -->
Nel grafo unificato, il nodo canonico `run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 787 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 787 che coinvolge il nodo canonico `run()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 787 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 787 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 787 che coinvolge il nodo canonico `run()` è `L993`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 787 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 787 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 787 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, l'arco 787 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo unificato, l'arco 787 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=unificata index=794 role=incoming -->
Nel grafo unificato, il nodo canonico `run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.flush()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_flush` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.flush()` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 794 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 794 che coinvolge il nodo canonico `run()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 794 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 794 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 794 che coinvolge il nodo canonico `run()` è `L1075`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 794 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 794 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 794 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, l'arco 794 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_flush` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo unificato, l'arco 794 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=unificata index=802 role=incoming -->
Nel grafo unificato, il nodo canonico `run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `ConcurrencyTracker`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 802 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 802 che coinvolge il nodo canonico `run()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 802 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 802 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 802 che coinvolge il nodo canonico `run()` è `L995`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 802 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 802 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 802 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, l'arco 802 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo unificato, l'arco 802 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=unificata index=807 role=incoming -->
Nel grafo unificato, il nodo canonico `run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_summary` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.summary() [traffic_exec_engine_concurrencytracker_summary]` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 807 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 807 che coinvolge il nodo canonico `run()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 807 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 807 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 807 che coinvolge il nodo canonico `run()` è `L1077`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 807 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 807 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 807 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, l'arco 807 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_summary` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo unificato, l'arco 807 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`.
<!-- GRAPH_EDGE graph=unificata index=810 role=incoming -->
Nel grafo unificato, il nodo canonico `run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `Semaphore`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::semaphore` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `Semaphore` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 810 che coinvolge il nodo canonico `run()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 810 che coinvolge il nodo canonico `run()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 810 che coinvolge il nodo canonico `run()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 810 che coinvolge il nodo canonico `run()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 810 che coinvolge il nodo canonico `run()` è `L994`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 810 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 810 che coinvolge il nodo canonico `run()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 810 che coinvolge il nodo canonico `run()` è `1.0`. Nel grafo unificato, l'arco 810 registra come `source` l'identificatore `Request-Dataset-Generator::semaphore` per la relazione che coinvolge il nodo canonico `run()`. Nel grafo unificato, l'arco 810 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `run()`.

### run_metric_engine()

<!-- GRAPH_NODE graph=codice index=113 id=metric_engine_run_metric_engine -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `run_metric_engine()` è `run_metric_engine()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `run_metric_engine()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `run_metric_engine()` è `metric_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `run_metric_engine()` è `L1423`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `run_metric_engine()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `run_metric_engine()` possiede l'identificatore esatto `metric_engine_run_metric_engine`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `run_metric_engine()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `run_metric_engine()` è `run_metric_engine()`.
<!-- GRAPH_EDGE graph=codice index=252 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `run_metric_engine()` è la sorgente `source` della relazione `calls` verso il nodo canonico `MetricsCalculator`; l'orientamento registrato nel JSON va dall'ID `metric_engine_run_metric_engine` all'ID `metric_engine_metricscalculator`. La relazione tecnica esplicita è la seguente: `run_metric_engine()` chiama `MetricsCalculator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 252 che coinvolge il nodo canonico `run_metric_engine()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 252 che coinvolge il nodo canonico `run_metric_engine()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 252 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 252 che coinvolge il nodo canonico `run_metric_engine()` è `metric_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 252 che coinvolge il nodo canonico `run_metric_engine()` è `L1450`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 252 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 252 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo del codice RDG, l'arco 252 registra come `source` l'identificatore `metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, l'arco 252 registra come `target` l'identificatore `metric_engine_metricscalculator` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 252 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=258 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `run_metric_engine()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.calculate_all()`; l'orientamento registrato nel JSON va dall'ID `metric_engine_run_metric_engine` all'ID `metric_engine_metricscalculator_calculate_all`. La relazione tecnica esplicita è la seguente: `run_metric_engine()` chiama `.calculate_all()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 258 che coinvolge il nodo canonico `run_metric_engine()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 258 che coinvolge il nodo canonico `run_metric_engine()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 258 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 258 che coinvolge il nodo canonico `run_metric_engine()` è `metric_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 258 che coinvolge il nodo canonico `run_metric_engine()` è `L1451`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 258 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 258 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo del codice RDG, l'arco 258 registra come `source` l'identificatore `metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, l'arco 258 registra come `target` l'identificatore `metric_engine_metricscalculator_calculate_all` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 258 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=260 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `run_metric_engine()` è la sorgente `source` della relazione `calls` verso il nodo canonico `WarningDetector`; l'orientamento registrato nel JSON va dall'ID `metric_engine_run_metric_engine` all'ID `metric_engine_warningdetector`. La relazione tecnica esplicita è la seguente: `run_metric_engine()` chiama `WarningDetector`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 260 che coinvolge il nodo canonico `run_metric_engine()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 260 che coinvolge il nodo canonico `run_metric_engine()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 260 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 260 che coinvolge il nodo canonico `run_metric_engine()` è `metric_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 260 che coinvolge il nodo canonico `run_metric_engine()` è `L1453`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 260 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 260 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo del codice RDG, l'arco 260 registra come `source` l'identificatore `metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, l'arco 260 registra come `target` l'identificatore `metric_engine_warningdetector` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 260 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=263 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `run_metric_engine()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.detect()`; l'orientamento registrato nel JSON va dall'ID `metric_engine_run_metric_engine` all'ID `metric_engine_warningdetector_detect`. La relazione tecnica esplicita è la seguente: `run_metric_engine()` chiama `.detect()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 263 che coinvolge il nodo canonico `run_metric_engine()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 263 che coinvolge il nodo canonico `run_metric_engine()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 263 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 263 che coinvolge il nodo canonico `run_metric_engine()` è `metric_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 263 che coinvolge il nodo canonico `run_metric_engine()` è `L1454`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 263 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 263 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo del codice RDG, l'arco 263 registra come `source` l'identificatore `metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, l'arco 263 registra come `target` l'identificatore `metric_engine_warningdetector_detect` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 263 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=275 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `run_metric_engine()` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `_write_reports()`; l'orientamento registrato nel JSON va dall'ID `metric_engine_run_metric_engine` all'ID `metric_engine_write_reports`. La relazione tecnica esplicita è la seguente: `run_metric_engine()` effettua una chiamata indiretta verso `_write_reports()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 275 che coinvolge il nodo canonico `run_metric_engine()` è `indirect_call`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 275 che coinvolge il nodo canonico `run_metric_engine()` è `argument`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 275 che coinvolge il nodo canonico `run_metric_engine()` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 275 che coinvolge il nodo canonico `run_metric_engine()` è `metric_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 275 che coinvolge il nodo canonico `run_metric_engine()` è `L1474`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 275 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 275 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo del codice RDG, l'arco 275 registra come `source` l'identificatore `metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, l'arco 275 registra come `target` l'identificatore `metric_engine_write_reports` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 275 che coinvolge il nodo canonico `run_metric_engine()` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=139 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `run_metric_engine()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `main.py` importa `run_metric_engine()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 139 che coinvolge il nodo canonico `run_metric_engine()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 139 che coinvolge il nodo canonico `run_metric_engine()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 139 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 139 che coinvolge il nodo canonico `run_metric_engine()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 139 che coinvolge il nodo canonico `run_metric_engine()` è `L22`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 139 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 139 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo del codice RDG, l'arco 139 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, l'arco 139 registra come `target` l'identificatore `metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 139 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=204 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `run_metric_engine()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_metrics()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_metrics` all'ID `metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `cmd_metrics()` chiama `run_metric_engine()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 204 che coinvolge il nodo canonico `run_metric_engine()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 204 che coinvolge il nodo canonico `run_metric_engine()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 204 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 204 che coinvolge il nodo canonico `run_metric_engine()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 204 che coinvolge il nodo canonico `run_metric_engine()` è `L504`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 204 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 204 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo del codice RDG, l'arco 204 registra come `source` l'identificatore `main_cmd_metrics` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, l'arco 204 registra come `target` l'identificatore `metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 204 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=205 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `run_metric_engine()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_pipeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_pipeline` all'ID `metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `cmd_pipeline()` chiama `run_metric_engine()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 205 che coinvolge il nodo canonico `run_metric_engine()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 205 che coinvolge il nodo canonico `run_metric_engine()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 205 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 205 che coinvolge il nodo canonico `run_metric_engine()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 205 che coinvolge il nodo canonico `run_metric_engine()` è `L545`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 205 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 205 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo del codice RDG, l'arco 205 registra come `source` l'identificatore `main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, l'arco 205 registra come `target` l'identificatore `metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 205 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=214 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `run_metric_engine()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `metric_engine.py`; l'orientamento registrato nel JSON va dall'ID `metric_engine` all'ID `metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `metric_engine.py` contiene `run_metric_engine()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 214 che coinvolge il nodo canonico `run_metric_engine()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 214 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 214 che coinvolge il nodo canonico `run_metric_engine()` è `metric_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 214 che coinvolge il nodo canonico `run_metric_engine()` è `L1423`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 214 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 214 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo del codice RDG, l'arco 214 registra come `source` l'identificatore `metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, l'arco 214 registra come `target` l'identificatore `metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 214 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=276 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `run_metric_engine()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `main() [metric_engine_main]`; l'orientamento registrato nel JSON va dall'ID `metric_engine_main` all'ID `metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `main() [metric_engine_main]` chiama `run_metric_engine()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 276 che coinvolge il nodo canonico `run_metric_engine()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 276 che coinvolge il nodo canonico `run_metric_engine()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 276 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 276 che coinvolge il nodo canonico `run_metric_engine()` è `metric_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 276 che coinvolge il nodo canonico `run_metric_engine()` è `L1534`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 276 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 276 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo del codice RDG, l'arco 276 registra come `source` l'identificatore `metric_engine_main` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, l'arco 276 registra come `target` l'identificatore `metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 276 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=277 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `run_metric_engine()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.`; l'orientamento registrato nel JSON va dall'ID `metric_engine_rationale_1432` all'ID `metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `Calcola metriche e genera report/grafici da config o da main.py.` costituisce una motivazione per `run_metric_engine()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 277 che coinvolge il nodo canonico `run_metric_engine()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 277 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 277 che coinvolge il nodo canonico `run_metric_engine()` è `metric_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 277 che coinvolge il nodo canonico `run_metric_engine()` è `L1432`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 277 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 277 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo del codice RDG, l'arco 277 registra come `source` l'identificatore `metric_engine_rationale_1432` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, l'arco 277 registra come `target` l'identificatore `metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 277 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=278 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `run_metric_engine()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `run_metric_engine()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 278 che coinvolge il nodo canonico `run_metric_engine()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 278 che coinvolge il nodo canonico `run_metric_engine()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 278 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 278 che coinvolge il nodo canonico `run_metric_engine()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 278 che coinvolge il nodo canonico `run_metric_engine()` è `L11`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 278 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 278 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo del codice RDG, l'arco 278 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, l'arco 278 registra come `target` l'identificatore `metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 278 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=279 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `run_metric_engine()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_metric_engine_writes_csv_tables()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` all'ID `metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `.test_metric_engine_writes_csv_tables()` chiama `run_metric_engine()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 279 che coinvolge il nodo canonico `run_metric_engine()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 279 che coinvolge il nodo canonico `run_metric_engine()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 279 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 279 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 279 che coinvolge il nodo canonico `run_metric_engine()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 279 che coinvolge il nodo canonico `run_metric_engine()` è `L756`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 279 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 279 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo del codice RDG, l'arco 279 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo del codice RDG, l'arco 279 registra come `target` l'identificatore `metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`.

<!-- GRAPH_NODE graph=unificata index=113 id=Request-Dataset-Generator::metric_engine_run_metric_engine -->
Nel grafo unificato, l'etichetta originale del nodo canonico `run_metric_engine()` è `run_metric_engine()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `run_metric_engine()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `run_metric_engine()` è `metric_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `run_metric_engine()` è `L1423`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `run_metric_engine()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `run_metric_engine()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `run_metric_engine()` è `run_metric_engine()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `run_metric_engine()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `run_metric_engine()` è `metric_engine_run_metric_engine`. Nel grafo unificato, il nodo canonico `run_metric_engine()` possiede l'identificatore esatto `Request-Dataset-Generator::metric_engine_run_metric_engine`.
<!-- GRAPH_EDGE graph=unificata index=276 role=outgoing -->
Nel grafo unificato, il nodo canonico `run_metric_engine()` è la sorgente `source` della relazione `calls` verso il nodo canonico `main() [metric_engine_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::metric_engine_run_metric_engine` all'ID `Request-Dataset-Generator::metric_engine_main`. La relazione tecnica esplicita è la seguente: `run_metric_engine()` chiama `main() [metric_engine_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 276 che coinvolge il nodo canonico `run_metric_engine()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 276 che coinvolge il nodo canonico `run_metric_engine()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 276 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 276 che coinvolge il nodo canonico `run_metric_engine()` è `metric_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 276 che coinvolge il nodo canonico `run_metric_engine()` è `L1534`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 276 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 276 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 276 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, l'arco 276 registra come `source` l'identificatore `Request-Dataset-Generator::metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo unificato, l'arco 276 registra come `target` l'identificatore `Request-Dataset-Generator::metric_engine_main` per la relazione che coinvolge il nodo canonico `run_metric_engine()`.
<!-- GRAPH_EDGE graph=unificata index=277 role=outgoing -->
Nel grafo unificato, il nodo canonico `run_metric_engine()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Calcola metriche e genera report/grafici da config o da main.py.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::metric_engine_run_metric_engine` all'ID `Request-Dataset-Generator::metric_engine_rationale_1432`. La relazione tecnica esplicita è la seguente: `run_metric_engine()` costituisce una motivazione per `Calcola metriche e genera report/grafici da config o da main.py.`. Nel grafo unificato, il tipo dichiarato dell'arco 277 che coinvolge il nodo canonico `run_metric_engine()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 277 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 277 che coinvolge il nodo canonico `run_metric_engine()` è `metric_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 277 che coinvolge il nodo canonico `run_metric_engine()` è `L1432`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 277 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 277 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 277 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, l'arco 277 registra come `source` l'identificatore `Request-Dataset-Generator::metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo unificato, l'arco 277 registra come `target` l'identificatore `Request-Dataset-Generator::metric_engine_rationale_1432` per la relazione che coinvolge il nodo canonico `run_metric_engine()`.
<!-- GRAPH_EDGE graph=unificata index=278 role=outgoing -->
Nel grafo unificato, il nodo canonico `run_metric_engine()` è la sorgente `source` della relazione `imports` verso il nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::metric_engine_run_metric_engine` all'ID `Request-Dataset-Generator::tests_test_core`. La relazione tecnica esplicita è la seguente: `run_metric_engine()` importa `test_core.py`. Nel grafo unificato, il tipo dichiarato dell'arco 278 che coinvolge il nodo canonico `run_metric_engine()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 278 che coinvolge il nodo canonico `run_metric_engine()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 278 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 278 che coinvolge il nodo canonico `run_metric_engine()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 278 che coinvolge il nodo canonico `run_metric_engine()` è `L11`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 278 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 278 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 278 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, l'arco 278 registra come `source` l'identificatore `Request-Dataset-Generator::metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo unificato, l'arco 278 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `run_metric_engine()`.
<!-- GRAPH_EDGE graph=unificata index=279 role=outgoing -->
Nel grafo unificato, il nodo canonico `run_metric_engine()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.test_metric_engine_writes_csv_tables()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::metric_engine_run_metric_engine` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables`. La relazione tecnica esplicita è la seguente: `run_metric_engine()` chiama `.test_metric_engine_writes_csv_tables()`. Nel grafo unificato, il tipo dichiarato dell'arco 279 che coinvolge il nodo canonico `run_metric_engine()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 279 che coinvolge il nodo canonico `run_metric_engine()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 279 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 279 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 279 che coinvolge il nodo canonico `run_metric_engine()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 279 che coinvolge il nodo canonico `run_metric_engine()` è `L756`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 279 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 279 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo unificato, l'arco 279 registra come `source` l'identificatore `Request-Dataset-Generator::metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo unificato, l'arco 279 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `run_metric_engine()`.
<!-- GRAPH_EDGE graph=unificata index=139 role=incoming -->
Nel grafo unificato, il nodo canonico `run_metric_engine()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `main.py` importa `run_metric_engine()`. Nel grafo unificato, il tipo dichiarato dell'arco 139 che coinvolge il nodo canonico `run_metric_engine()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 139 che coinvolge il nodo canonico `run_metric_engine()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 139 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 139 che coinvolge il nodo canonico `run_metric_engine()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 139 che coinvolge il nodo canonico `run_metric_engine()` è `L22`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 139 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 139 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 139 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, l'arco 139 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo unificato, l'arco 139 registra come `target` l'identificatore `Request-Dataset-Generator::metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`.
<!-- GRAPH_EDGE graph=unificata index=204 role=incoming -->
Nel grafo unificato, il nodo canonico `run_metric_engine()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_metrics()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_metrics` all'ID `Request-Dataset-Generator::metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `cmd_metrics()` chiama `run_metric_engine()`. Nel grafo unificato, il tipo dichiarato dell'arco 204 che coinvolge il nodo canonico `run_metric_engine()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 204 che coinvolge il nodo canonico `run_metric_engine()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 204 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 204 che coinvolge il nodo canonico `run_metric_engine()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 204 che coinvolge il nodo canonico `run_metric_engine()` è `L504`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 204 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 204 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 204 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, l'arco 204 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_metrics` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo unificato, l'arco 204 registra come `target` l'identificatore `Request-Dataset-Generator::metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`.
<!-- GRAPH_EDGE graph=unificata index=205 role=incoming -->
Nel grafo unificato, il nodo canonico `run_metric_engine()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_pipeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_pipeline` all'ID `Request-Dataset-Generator::metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `cmd_pipeline()` chiama `run_metric_engine()`. Nel grafo unificato, il tipo dichiarato dell'arco 205 che coinvolge il nodo canonico `run_metric_engine()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 205 che coinvolge il nodo canonico `run_metric_engine()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 205 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 205 che coinvolge il nodo canonico `run_metric_engine()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 205 che coinvolge il nodo canonico `run_metric_engine()` è `L545`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 205 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 205 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 205 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, l'arco 205 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo unificato, l'arco 205 registra come `target` l'identificatore `Request-Dataset-Generator::metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`.
<!-- GRAPH_EDGE graph=unificata index=214 role=incoming -->
Nel grafo unificato, il nodo canonico `run_metric_engine()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `metric_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::metric_engine` all'ID `Request-Dataset-Generator::metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `metric_engine.py` contiene `run_metric_engine()`. Nel grafo unificato, il tipo dichiarato dell'arco 214 che coinvolge il nodo canonico `run_metric_engine()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 214 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 214 che coinvolge il nodo canonico `run_metric_engine()` è `metric_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 214 che coinvolge il nodo canonico `run_metric_engine()` è `L1423`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 214 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 214 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 214 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, l'arco 214 registra come `source` l'identificatore `Request-Dataset-Generator::metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo unificato, l'arco 214 registra come `target` l'identificatore `Request-Dataset-Generator::metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`.
<!-- GRAPH_EDGE graph=unificata index=252 role=incoming -->
Nel grafo unificato, il nodo canonico `run_metric_engine()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `MetricsCalculator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::metric_engine_metricscalculator` all'ID `Request-Dataset-Generator::metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `MetricsCalculator` chiama `run_metric_engine()`. Nel grafo unificato, il tipo dichiarato dell'arco 252 che coinvolge il nodo canonico `run_metric_engine()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 252 che coinvolge il nodo canonico `run_metric_engine()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 252 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 252 che coinvolge il nodo canonico `run_metric_engine()` è `metric_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 252 che coinvolge il nodo canonico `run_metric_engine()` è `L1450`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 252 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 252 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 252 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, l'arco 252 registra come `source` l'identificatore `Request-Dataset-Generator::metric_engine_metricscalculator` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo unificato, l'arco 252 registra come `target` l'identificatore `Request-Dataset-Generator::metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`.
<!-- GRAPH_EDGE graph=unificata index=258 role=incoming -->
Nel grafo unificato, il nodo canonico `run_metric_engine()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.calculate_all()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::metric_engine_metricscalculator_calculate_all` all'ID `Request-Dataset-Generator::metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `.calculate_all()` chiama `run_metric_engine()`. Nel grafo unificato, il tipo dichiarato dell'arco 258 che coinvolge il nodo canonico `run_metric_engine()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 258 che coinvolge il nodo canonico `run_metric_engine()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 258 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 258 che coinvolge il nodo canonico `run_metric_engine()` è `metric_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 258 che coinvolge il nodo canonico `run_metric_engine()` è `L1451`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 258 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 258 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 258 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, l'arco 258 registra come `source` l'identificatore `Request-Dataset-Generator::metric_engine_metricscalculator_calculate_all` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo unificato, l'arco 258 registra come `target` l'identificatore `Request-Dataset-Generator::metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`.
<!-- GRAPH_EDGE graph=unificata index=260 role=incoming -->
Nel grafo unificato, il nodo canonico `run_metric_engine()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `WarningDetector`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::metric_engine_warningdetector` all'ID `Request-Dataset-Generator::metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `WarningDetector` chiama `run_metric_engine()`. Nel grafo unificato, il tipo dichiarato dell'arco 260 che coinvolge il nodo canonico `run_metric_engine()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 260 che coinvolge il nodo canonico `run_metric_engine()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 260 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 260 che coinvolge il nodo canonico `run_metric_engine()` è `metric_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 260 che coinvolge il nodo canonico `run_metric_engine()` è `L1453`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 260 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 260 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 260 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, l'arco 260 registra come `source` l'identificatore `Request-Dataset-Generator::metric_engine_warningdetector` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo unificato, l'arco 260 registra come `target` l'identificatore `Request-Dataset-Generator::metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`.
<!-- GRAPH_EDGE graph=unificata index=263 role=incoming -->
Nel grafo unificato, il nodo canonico `run_metric_engine()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.detect()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::metric_engine_warningdetector_detect` all'ID `Request-Dataset-Generator::metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `.detect()` chiama `run_metric_engine()`. Nel grafo unificato, il tipo dichiarato dell'arco 263 che coinvolge il nodo canonico `run_metric_engine()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 263 che coinvolge il nodo canonico `run_metric_engine()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 263 che coinvolge il nodo canonico `run_metric_engine()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 263 che coinvolge il nodo canonico `run_metric_engine()` è `metric_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 263 che coinvolge il nodo canonico `run_metric_engine()` è `L1454`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 263 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 263 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 263 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, l'arco 263 registra come `source` l'identificatore `Request-Dataset-Generator::metric_engine_warningdetector_detect` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo unificato, l'arco 263 registra come `target` l'identificatore `Request-Dataset-Generator::metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`.
<!-- GRAPH_EDGE graph=unificata index=275 role=incoming -->
Nel grafo unificato, il nodo canonico `run_metric_engine()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `_write_reports()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::metric_engine_write_reports` all'ID `Request-Dataset-Generator::metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `_write_reports()` effettua una chiamata indiretta verso `run_metric_engine()`. Nel grafo unificato, il tipo dichiarato dell'arco 275 che coinvolge il nodo canonico `run_metric_engine()` è `indirect_call`. Nel grafo unificato, il contesto dichiarato per l'arco 275 che coinvolge il nodo canonico `run_metric_engine()` è `argument`. Nel grafo unificato, la confidenza categoriale dell'arco 275 che coinvolge il nodo canonico `run_metric_engine()` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 275 che coinvolge il nodo canonico `run_metric_engine()` è `metric_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 275 che coinvolge il nodo canonico `run_metric_engine()` è `L1474`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 275 che coinvolge il nodo canonico `run_metric_engine()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 275 che coinvolge il nodo canonico `run_metric_engine()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 275 che coinvolge il nodo canonico `run_metric_engine()` è `0.5`. Nel grafo unificato, l'arco 275 registra come `source` l'identificatore `Request-Dataset-Generator::metric_engine_write_reports` per la relazione che coinvolge il nodo canonico `run_metric_engine()`. Nel grafo unificato, l'arco 275 registra come `target` l'identificatore `Request-Dataset-Generator::metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `run_metric_engine()`.

### step_profile()

<!-- GRAPH_NODE graph=codice index=324 id=timeline_generator_step_profile -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `step_profile()` è `step_profile()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `step_profile()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `step_profile()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `step_profile()` è `L59`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `step_profile()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `step_profile()` possiede l'identificatore esatto `timeline_generator_step_profile`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `step_profile()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `step_profile()` è `step_profile()`.
Nel grafo del codice RDG, il nodo canonico `step_profile()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=146 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `step_profile()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `timeline_generator_step_profile`. La relazione tecnica esplicita è la seguente: `main.py` importa `step_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 146 che coinvolge il nodo canonico `step_profile()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 146 che coinvolge il nodo canonico `step_profile()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 146 che coinvolge il nodo canonico `step_profile()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 146 che coinvolge il nodo canonico `step_profile()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 146 che coinvolge il nodo canonico `step_profile()` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 146 che coinvolge il nodo canonico `step_profile()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 146 che coinvolge il nodo canonico `step_profile()` è `ast`. Nel grafo del codice RDG, l'arco 146 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `step_profile()`. Nel grafo del codice RDG, l'arco 146 registra come `target` l'identificatore `timeline_generator_step_profile` per la relazione che coinvolge il nodo canonico `step_profile()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 146 che coinvolge il nodo canonico `step_profile()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=193 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `step_profile()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_execute` all'ID `timeline_generator_step_profile`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `step_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 193 che coinvolge il nodo canonico `step_profile()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 193 che coinvolge il nodo canonico `step_profile()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 193 che coinvolge il nodo canonico `step_profile()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 193 che coinvolge il nodo canonico `step_profile()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 193 che coinvolge il nodo canonico `step_profile()` è `L345`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 193 che coinvolge il nodo canonico `step_profile()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 193 che coinvolge il nodo canonico `step_profile()` è `ast`. Nel grafo del codice RDG, l'arco 193 registra come `source` l'identificatore `main_cmd_execute` per la relazione che coinvolge il nodo canonico `step_profile()`. Nel grafo del codice RDG, l'arco 193 registra come `target` l'identificatore `timeline_generator_step_profile` per la relazione che coinvolge il nodo canonico `step_profile()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 193 che coinvolge il nodo canonico `step_profile()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=201 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `step_profile()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_timeline` all'ID `timeline_generator_step_profile`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `step_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 201 che coinvolge il nodo canonico `step_profile()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 201 che coinvolge il nodo canonico `step_profile()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 201 che coinvolge il nodo canonico `step_profile()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 201 che coinvolge il nodo canonico `step_profile()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 201 che coinvolge il nodo canonico `step_profile()` è `L434`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 201 che coinvolge il nodo canonico `step_profile()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 201 che coinvolge il nodo canonico `step_profile()` è `ast`. Nel grafo del codice RDG, l'arco 201 registra come `source` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `step_profile()`. Nel grafo del codice RDG, l'arco 201 registra come `target` l'identificatore `timeline_generator_step_profile` per la relazione che coinvolge il nodo canonico `step_profile()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 201 che coinvolge il nodo canonico `step_profile()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=648 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `step_profile()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `timeline_generator` all'ID `timeline_generator_step_profile`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `step_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 648 che coinvolge il nodo canonico `step_profile()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 648 che coinvolge il nodo canonico `step_profile()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 648 che coinvolge il nodo canonico `step_profile()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 648 che coinvolge il nodo canonico `step_profile()` è `L59`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 648 che coinvolge il nodo canonico `step_profile()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 648 che coinvolge il nodo canonico `step_profile()` è `ast`. Nel grafo del codice RDG, l'arco 648 registra come `source` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `step_profile()`. Nel grafo del codice RDG, l'arco 648 registra come `target` l'identificatore `timeline_generator_step_profile` per la relazione che coinvolge il nodo canonico `step_profile()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 648 che coinvolge il nodo canonico `step_profile()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=654 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `step_profile()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_rationale_60` all'ID `timeline_generator_step_profile`. La relazione tecnica esplicita è la seguente: `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r` costituisce una motivazione per `step_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 654 che coinvolge il nodo canonico `step_profile()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 654 che coinvolge il nodo canonico `step_profile()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 654 che coinvolge il nodo canonico `step_profile()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 654 che coinvolge il nodo canonico `step_profile()` è `L60`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 654 che coinvolge il nodo canonico `step_profile()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 654 che coinvolge il nodo canonico `step_profile()` è `ast`. Nel grafo del codice RDG, l'arco 654 registra come `source` l'identificatore `timeline_generator_rationale_60` per la relazione che coinvolge il nodo canonico `step_profile()`. Nel grafo del codice RDG, l'arco 654 registra come `target` l'identificatore `timeline_generator_step_profile` per la relazione che coinvolge il nodo canonico `step_profile()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 654 che coinvolge il nodo canonico `step_profile()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=324 id=Request-Dataset-Generator::timeline_generator_step_profile -->
Nel grafo unificato, l'etichetta originale del nodo canonico `step_profile()` è `step_profile()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `step_profile()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `step_profile()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `step_profile()` è `L59`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `step_profile()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `step_profile()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `step_profile()` è `step_profile()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `step_profile()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `step_profile()` è `timeline_generator_step_profile`. Nel grafo unificato, il nodo canonico `step_profile()` possiede l'identificatore esatto `Request-Dataset-Generator::timeline_generator_step_profile`.
<!-- GRAPH_EDGE graph=unificata index=654 role=outgoing -->
Nel grafo unificato, il nodo canonico `step_profile()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator_step_profile` all'ID `Request-Dataset-Generator::timeline_generator_rationale_60`. La relazione tecnica esplicita è la seguente: `step_profile()` costituisce una motivazione per `Profilo a gradini definito dall'utente.      Ogni step è {"until": <secondi>, "r`. Nel grafo unificato, il tipo dichiarato dell'arco 654 che coinvolge il nodo canonico `step_profile()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 654 che coinvolge il nodo canonico `step_profile()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 654 che coinvolge il nodo canonico `step_profile()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 654 che coinvolge il nodo canonico `step_profile()` è `L60`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 654 che coinvolge il nodo canonico `step_profile()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 654 che coinvolge il nodo canonico `step_profile()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 654 che coinvolge il nodo canonico `step_profile()` è `1.0`. Nel grafo unificato, l'arco 654 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator_step_profile` per la relazione che coinvolge il nodo canonico `step_profile()`. Nel grafo unificato, l'arco 654 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_rationale_60` per la relazione che coinvolge il nodo canonico `step_profile()`.
<!-- GRAPH_EDGE graph=unificata index=146 role=incoming -->
Nel grafo unificato, il nodo canonico `step_profile()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::timeline_generator_step_profile`. La relazione tecnica esplicita è la seguente: `main.py` importa `step_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 146 che coinvolge il nodo canonico `step_profile()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 146 che coinvolge il nodo canonico `step_profile()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 146 che coinvolge il nodo canonico `step_profile()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 146 che coinvolge il nodo canonico `step_profile()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 146 che coinvolge il nodo canonico `step_profile()` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 146 che coinvolge il nodo canonico `step_profile()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 146 che coinvolge il nodo canonico `step_profile()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 146 che coinvolge il nodo canonico `step_profile()` è `1.0`. Nel grafo unificato, l'arco 146 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `step_profile()`. Nel grafo unificato, l'arco 146 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_step_profile` per la relazione che coinvolge il nodo canonico `step_profile()`.
<!-- GRAPH_EDGE graph=unificata index=193 role=incoming -->
Nel grafo unificato, il nodo canonico `step_profile()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_execute()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_execute` all'ID `Request-Dataset-Generator::timeline_generator_step_profile`. La relazione tecnica esplicita è la seguente: `cmd_execute()` chiama `step_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 193 che coinvolge il nodo canonico `step_profile()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 193 che coinvolge il nodo canonico `step_profile()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 193 che coinvolge il nodo canonico `step_profile()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 193 che coinvolge il nodo canonico `step_profile()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 193 che coinvolge il nodo canonico `step_profile()` è `L345`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 193 che coinvolge il nodo canonico `step_profile()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 193 che coinvolge il nodo canonico `step_profile()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 193 che coinvolge il nodo canonico `step_profile()` è `1.0`. Nel grafo unificato, l'arco 193 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_execute` per la relazione che coinvolge il nodo canonico `step_profile()`. Nel grafo unificato, l'arco 193 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_step_profile` per la relazione che coinvolge il nodo canonico `step_profile()`.
<!-- GRAPH_EDGE graph=unificata index=201 role=incoming -->
Nel grafo unificato, il nodo canonico `step_profile()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_timeline` all'ID `Request-Dataset-Generator::timeline_generator_step_profile`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `step_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 201 che coinvolge il nodo canonico `step_profile()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 201 che coinvolge il nodo canonico `step_profile()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 201 che coinvolge il nodo canonico `step_profile()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 201 che coinvolge il nodo canonico `step_profile()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 201 che coinvolge il nodo canonico `step_profile()` è `L434`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 201 che coinvolge il nodo canonico `step_profile()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 201 che coinvolge il nodo canonico `step_profile()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 201 che coinvolge il nodo canonico `step_profile()` è `1.0`. Nel grafo unificato, l'arco 201 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `step_profile()`. Nel grafo unificato, l'arco 201 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_step_profile` per la relazione che coinvolge il nodo canonico `step_profile()`.
<!-- GRAPH_EDGE graph=unificata index=648 role=incoming -->
Nel grafo unificato, il nodo canonico `step_profile()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator` all'ID `Request-Dataset-Generator::timeline_generator_step_profile`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `step_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 648 che coinvolge il nodo canonico `step_profile()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 648 che coinvolge il nodo canonico `step_profile()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 648 che coinvolge il nodo canonico `step_profile()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 648 che coinvolge il nodo canonico `step_profile()` è `L59`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 648 che coinvolge il nodo canonico `step_profile()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 648 che coinvolge il nodo canonico `step_profile()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 648 che coinvolge il nodo canonico `step_profile()` è `1.0`. Nel grafo unificato, l'arco 648 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `step_profile()`. Nel grafo unificato, l'arco 648 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_step_profile` per la relazione che coinvolge il nodo canonico `step_profile()`.

### test_core.py

<!-- GRAPH_NODE graph=codice index=284 id=tests_test_core -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `test_core.py` è `test_core.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `test_core.py` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `test_core.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `test_core.py` vale `ast`. Nel grafo del codice RDG, il nodo canonico `test_core.py` possiede l'identificatore esatto `tests_test_core`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `test_core.py` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `test_core.py` è `test_core.py`.
<!-- GRAPH_EDGE graph=codice index=72 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `log_collector`. La relazione tecnica esplicita è la seguente: `test_core.py` importa da `log_collector.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 72 che coinvolge il nodo canonico `test_core.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 72 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 72 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 72 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 72 che coinvolge il nodo canonico `test_core.py` è `L10`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 72 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 72 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 72 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 72 registra come `target` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 72 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=112 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `collect_logs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 112 che coinvolge il nodo canonico `test_core.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 112 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 112 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 112 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 112 che coinvolge il nodo canonico `test_core.py` è `L10`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 112 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 112 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 112 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 112 registra come `target` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 112 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=150 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `main`. La relazione tecnica esplicita è la seguente: `test_core.py` importa da `main.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 150 che coinvolge il nodo canonico `test_core.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 150 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 150 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 150 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 150 che coinvolge il nodo canonico `test_core.py` è `L31`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 150 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 150 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 150 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 150 registra come `target` l'identificatore `main` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 150 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=220 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `metric_engine.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `metric_engine`. La relazione tecnica esplicita è la seguente: `test_core.py` importa da `metric_engine.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 220 che coinvolge il nodo canonico `test_core.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 220 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 220 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 220 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 220 che coinvolge il nodo canonico `test_core.py` è `L11`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 220 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 220 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 220 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 220 registra come `target` l'identificatore `metric_engine` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 220 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=278 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `run_metric_engine()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `metric_engine_run_metric_engine`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `run_metric_engine()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 278 che coinvolge il nodo canonico `test_core.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 278 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 278 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 278 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 278 che coinvolge il nodo canonico `test_core.py` è `L11`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 278 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 278 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 278 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 278 registra come `target` l'identificatore `metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 278 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=320 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `request_dataset_generator.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `request_dataset_generator`. La relazione tecnica esplicita è la seguente: `test_core.py` importa da `request_dataset_generator.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 320 che coinvolge il nodo canonico `test_core.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 320 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 320 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 320 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 320 che coinvolge il nodo canonico `test_core.py` è `L12`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 320 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 320 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 320 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 320 registra come `target` l'identificatore `request_dataset_generator` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 320 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=324 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 324 che coinvolge il nodo canonico `test_core.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 324 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 324 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 324 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 324 che coinvolge il nodo canonico `test_core.py` è `L12`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 324 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 324 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 324 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 324 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 324 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=421 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `flow_orchestrator.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `rve_transactions_flow_orchestrator`. La relazione tecnica esplicita è la seguente: `test_core.py` importa da `flow_orchestrator.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 421 che coinvolge il nodo canonico `test_core.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 421 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 421 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 421 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 421 che coinvolge il nodo canonico `test_core.py` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 421 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 421 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 421 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 421 registra come `target` l'identificatore `rve_transactions_flow_orchestrator` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 421 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=432 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 432 che coinvolge il nodo canonico `test_core.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 432 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 432 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 432 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 432 che coinvolge il nodo canonico `test_core.py` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 432 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 432 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 432 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 432 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 432 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=471 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `test_core.py` importa da `rve_1b.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 471 che coinvolge il nodo canonico `test_core.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 471 che coinvolge il nodo canonico `test_core.py` è `submodule_import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 471 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 471 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 471 che coinvolge il nodo canonico `test_core.py` è `L24`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 471 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 471 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 471 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 471 registra come `target` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 471 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=574 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `scrybasign_get_user_info.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `rve_transactions_scrybasign_get_user_info`. La relazione tecnica esplicita è la seguente: `test_core.py` importa da `scrybasign_get_user_info.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 574 che coinvolge il nodo canonico `test_core.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 574 che coinvolge il nodo canonico `test_core.py` è `submodule_import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 574 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 574 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 574 che coinvolge il nodo canonico `test_core.py` è `L24`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 574 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 574 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 574 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 574 registra come `target` l'identificatore `rve_transactions_scrybasign_get_user_info` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 574 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=577 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `scrybasign_sign_one_doc.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `rve_transactions_scrybasign_sign_one_doc`. La relazione tecnica esplicita è la seguente: `test_core.py` importa da `scrybasign_sign_one_doc.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 577 che coinvolge il nodo canonico `test_core.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 577 che coinvolge il nodo canonico `test_core.py` è `submodule_import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 577 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 577 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 577 che coinvolge il nodo canonico `test_core.py` è `L24`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 577 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 577 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 577 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 577 registra come `target` l'identificatore `rve_transactions_scrybasign_sign_one_doc` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 577 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=578 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `ConfigJsonTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `tests_test_core_configjsontest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `ConfigJsonTest`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 578 che coinvolge il nodo canonico `test_core.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 578 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 578 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 578 che coinvolge il nodo canonico `test_core.py` è `L821`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 578 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 578 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 578 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 578 registra come `target` l'identificatore `tests_test_core_configjsontest` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 578 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=579 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `EngineSmokeTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `tests_test_core_enginesmoketest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `EngineSmokeTest`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 579 che coinvolge il nodo canonico `test_core.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 579 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 579 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 579 che coinvolge il nodo canonico `test_core.py` è `L548`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 579 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 579 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 579 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 579 registra come `target` l'identificatore `tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 579 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=580 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `ExtractionTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `tests_test_core_extractiontest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `ExtractionTest`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 580 che coinvolge il nodo canonico `test_core.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 580 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 580 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 580 che coinvolge il nodo canonico `test_core.py` è `L152`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 580 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 580 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 580 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 580 registra come `target` l'identificatore `tests_test_core_extractiontest` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 580 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=581 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `tests_test_core_flowbuildtest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `FlowBuildTest`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 581 che coinvolge il nodo canonico `test_core.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 581 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 581 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 581 che coinvolge il nodo canonico `test_core.py` è `L278`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 581 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 581 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 581 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 581 registra come `target` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 581 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=582 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `MainDispatchTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `tests_test_core_maindispatchtest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `MainDispatchTest`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 582 che coinvolge il nodo canonico `test_core.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 582 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 582 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 582 che coinvolge il nodo canonico `test_core.py` è `L852`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 582 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 582 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 582 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 582 registra come `target` l'identificatore `tests_test_core_maindispatchtest` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 582 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=583 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `PlaceholderTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `tests_test_core_placeholdertest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `PlaceholderTest`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 583 che coinvolge il nodo canonico `test_core.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 583 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 583 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 583 che coinvolge il nodo canonico `test_core.py` è `L239`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 583 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 583 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 583 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 583 registra come `target` l'identificatore `tests_test_core_placeholdertest` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 583 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=584 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `TimelineIdentityTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `tests_test_core_timelineidentitytest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `TimelineIdentityTest`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 584 che coinvolge il nodo canonico `test_core.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 584 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 584 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 584 che coinvolge il nodo canonico `test_core.py` è `L771`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 584 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 584 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 584 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 584 registra come `target` l'identificatore `tests_test_core_timelineidentitytest` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 584 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=585 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `timeline_generator`. La relazione tecnica esplicita è la seguente: `test_core.py` importa da `timeline_generator.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 585 che coinvolge il nodo canonico `test_core.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 585 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 585 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 585 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 585 che coinvolge il nodo canonico `test_core.py` è `L13`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 585 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 585 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 585 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 585 registra come `target` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 585 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=586 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 586 che coinvolge il nodo canonico `test_core.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 586 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 586 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 586 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 586 che coinvolge il nodo canonico `test_core.py` è `L13`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 586 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 586 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 586 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 586 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 586 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=587 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 587 che coinvolge il nodo canonico `test_core.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 587 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 587 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 587 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 587 che coinvolge il nodo canonico `test_core.py` è `L13`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 587 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 587 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 587 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 587 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 587 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=588 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `traffic_exec_engine`. La relazione tecnica esplicita è la seguente: `test_core.py` importa da `traffic_exec_engine.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 588 che coinvolge il nodo canonico `test_core.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 588 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 588 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 588 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 588 che coinvolge il nodo canonico `test_core.py` è `L25`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 588 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 588 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 588 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 588 registra come `target` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 588 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=589 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `extract_outputs()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `extract_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 589 che coinvolge il nodo canonico `test_core.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 589 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 589 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 589 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 589 che coinvolge il nodo canonico `test_core.py` è `L25`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 589 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 589 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 589 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 589 registra come `target` l'identificatore `traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 589 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=590 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `resolve_placeholders()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `traffic_exec_engine_resolve_placeholders`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `resolve_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 590 che coinvolge il nodo canonico `test_core.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 590 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 590 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 590 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 590 che coinvolge il nodo canonico `test_core.py` è `L25`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 590 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 590 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 590 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 590 registra come `target` l'identificatore `traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 590 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=591 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 591 che coinvolge il nodo canonico `test_core.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 591 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 591 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 591 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 591 che coinvolge il nodo canonico `test_core.py` è `L25`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 591 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 591 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 591 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 591 registra come `target` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 591 che coinvolge il nodo canonico `test_core.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=592 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `unresolved_placeholders()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `traffic_exec_engine_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `unresolved_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 592 che coinvolge il nodo canonico `test_core.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 592 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 592 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 592 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 592 che coinvolge il nodo canonico `test_core.py` è `L25`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 592 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 592 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo del codice RDG, l'arco 592 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, l'arco 592 registra come `target` l'identificatore `traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 592 che coinvolge il nodo canonico `test_core.py` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `test_core.py` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=284 id=Request-Dataset-Generator::tests_test_core -->
Nel grafo unificato, l'etichetta originale del nodo canonico `test_core.py` è `test_core.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `test_core.py` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `test_core.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `test_core.py` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `test_core.py` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `test_core.py` è `test_core.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `test_core.py` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `test_core.py` è `tests_test_core`. Nel grafo unificato, il nodo canonico `test_core.py` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core`.
<!-- GRAPH_EDGE graph=unificata index=578 role=outgoing -->
Nel grafo unificato, il nodo canonico `test_core.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `ConfigJsonTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::tests_test_core_configjsontest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `ConfigJsonTest`. Nel grafo unificato, il tipo dichiarato dell'arco 578 che coinvolge il nodo canonico `test_core.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 578 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 578 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 578 che coinvolge il nodo canonico `test_core.py` è `L821`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 578 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 578 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 578 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 578 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 578 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_configjsontest` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=579 role=outgoing -->
Nel grafo unificato, il nodo canonico `test_core.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `EngineSmokeTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `EngineSmokeTest`. Nel grafo unificato, il tipo dichiarato dell'arco 579 che coinvolge il nodo canonico `test_core.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 579 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 579 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 579 che coinvolge il nodo canonico `test_core.py` è `L548`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 579 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 579 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 579 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 579 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 579 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=580 role=outgoing -->
Nel grafo unificato, il nodo canonico `test_core.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `ExtractionTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::tests_test_core_extractiontest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `ExtractionTest`. Nel grafo unificato, il tipo dichiarato dell'arco 580 che coinvolge il nodo canonico `test_core.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 580 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 580 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 580 che coinvolge il nodo canonico `test_core.py` è `L152`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 580 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 580 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 580 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 580 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 580 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_extractiontest` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=581 role=outgoing -->
Nel grafo unificato, il nodo canonico `test_core.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `FlowBuildTest`. Nel grafo unificato, il tipo dichiarato dell'arco 581 che coinvolge il nodo canonico `test_core.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 581 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 581 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 581 che coinvolge il nodo canonico `test_core.py` è `L278`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 581 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 581 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 581 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 581 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 581 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=582 role=outgoing -->
Nel grafo unificato, il nodo canonico `test_core.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `MainDispatchTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::tests_test_core_maindispatchtest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `MainDispatchTest`. Nel grafo unificato, il tipo dichiarato dell'arco 582 che coinvolge il nodo canonico `test_core.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 582 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 582 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 582 che coinvolge il nodo canonico `test_core.py` è `L852`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 582 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 582 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 582 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 582 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 582 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_maindispatchtest` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=583 role=outgoing -->
Nel grafo unificato, il nodo canonico `test_core.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `PlaceholderTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::tests_test_core_placeholdertest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `PlaceholderTest`. Nel grafo unificato, il tipo dichiarato dell'arco 583 che coinvolge il nodo canonico `test_core.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 583 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 583 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 583 che coinvolge il nodo canonico `test_core.py` è `L239`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 583 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 583 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 583 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 583 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 583 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=584 role=outgoing -->
Nel grafo unificato, il nodo canonico `test_core.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `TimelineIdentityTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `TimelineIdentityTest`. Nel grafo unificato, il tipo dichiarato dell'arco 584 che coinvolge il nodo canonico `test_core.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 584 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 584 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 584 che coinvolge il nodo canonico `test_core.py` è `L771`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 584 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 584 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 584 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 584 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 584 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=585 role=outgoing -->
Nel grafo unificato, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::timeline_generator`. La relazione tecnica esplicita è la seguente: `test_core.py` importa da `timeline_generator.py`. Nel grafo unificato, il tipo dichiarato dell'arco 585 che coinvolge il nodo canonico `test_core.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 585 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 585 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 585 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 585 che coinvolge il nodo canonico `test_core.py` è `L13`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 585 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 585 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 585 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 585 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 585 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=586 role=outgoing -->
Nel grafo unificato, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 586 che coinvolge il nodo canonico `test_core.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 586 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 586 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 586 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 586 che coinvolge il nodo canonico `test_core.py` è `L13`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 586 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 586 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 586 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 586 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 586 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=587 role=outgoing -->
Nel grafo unificato, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `constant_rate()`. Nel grafo unificato, il tipo dichiarato dell'arco 587 che coinvolge il nodo canonico `test_core.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 587 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 587 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 587 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 587 che coinvolge il nodo canonico `test_core.py` è `L13`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 587 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 587 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 587 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 587 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 587 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=588 role=outgoing -->
Nel grafo unificato, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::traffic_exec_engine`. La relazione tecnica esplicita è la seguente: `test_core.py` importa da `traffic_exec_engine.py`. Nel grafo unificato, il tipo dichiarato dell'arco 588 che coinvolge il nodo canonico `test_core.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 588 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 588 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 588 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 588 che coinvolge il nodo canonico `test_core.py` è `L25`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 588 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 588 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 588 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 588 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 588 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=589 role=outgoing -->
Nel grafo unificato, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `extract_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `extract_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 589 che coinvolge il nodo canonico `test_core.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 589 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 589 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 589 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 589 che coinvolge il nodo canonico `test_core.py` è `L25`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 589 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 589 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 589 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 589 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 589 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=590 role=outgoing -->
Nel grafo unificato, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `resolve_placeholders()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `resolve_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 590 che coinvolge il nodo canonico `test_core.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 590 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 590 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 590 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 590 che coinvolge il nodo canonico `test_core.py` è `L25`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 590 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 590 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 590 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 590 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 590 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=591 role=outgoing -->
Nel grafo unificato, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 591 che coinvolge il nodo canonico `test_core.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 591 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 591 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 591 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 591 che coinvolge il nodo canonico `test_core.py` è `L25`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 591 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 591 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 591 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 591 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 591 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=592 role=outgoing -->
Nel grafo unificato, il nodo canonico `test_core.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `unresolved_placeholders()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `unresolved_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 592 che coinvolge il nodo canonico `test_core.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 592 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 592 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 592 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 592 che coinvolge il nodo canonico `test_core.py` è `L25`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 592 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 592 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 592 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 592 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 592 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=72 role=incoming -->
Nel grafo unificato, il nodo canonico `test_core.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::tests_test_core`. La relazione tecnica esplicita è la seguente: `log_collector.py` importa da `test_core.py`. Nel grafo unificato, il tipo dichiarato dell'arco 72 che coinvolge il nodo canonico `test_core.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 72 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 72 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 72 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 72 che coinvolge il nodo canonico `test_core.py` è `L10`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 72 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 72 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 72 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 72 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 72 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=112 role=incoming -->
Nel grafo unificato, il nodo canonico `test_core.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_collect_logs` all'ID `Request-Dataset-Generator::tests_test_core`. La relazione tecnica esplicita è la seguente: `collect_logs()` importa `test_core.py`. Nel grafo unificato, il tipo dichiarato dell'arco 112 che coinvolge il nodo canonico `test_core.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 112 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 112 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 112 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 112 che coinvolge il nodo canonico `test_core.py` è `L10`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 112 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 112 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 112 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 112 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 112 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=150 role=incoming -->
Nel grafo unificato, il nodo canonico `test_core.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::tests_test_core`. La relazione tecnica esplicita è la seguente: `main.py` importa da `test_core.py`. Nel grafo unificato, il tipo dichiarato dell'arco 150 che coinvolge il nodo canonico `test_core.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 150 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 150 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 150 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 150 che coinvolge il nodo canonico `test_core.py` è `L31`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 150 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 150 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 150 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 150 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 150 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=220 role=incoming -->
Nel grafo unificato, il nodo canonico `test_core.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `metric_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::metric_engine` all'ID `Request-Dataset-Generator::tests_test_core`. La relazione tecnica esplicita è la seguente: `metric_engine.py` importa da `test_core.py`. Nel grafo unificato, il tipo dichiarato dell'arco 220 che coinvolge il nodo canonico `test_core.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 220 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 220 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 220 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 220 che coinvolge il nodo canonico `test_core.py` è `L11`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 220 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 220 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 220 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 220 registra come `source` l'identificatore `Request-Dataset-Generator::metric_engine` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 220 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=278 role=incoming -->
Nel grafo unificato, il nodo canonico `test_core.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `run_metric_engine()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::metric_engine_run_metric_engine` all'ID `Request-Dataset-Generator::tests_test_core`. La relazione tecnica esplicita è la seguente: `run_metric_engine()` importa `test_core.py`. Nel grafo unificato, il tipo dichiarato dell'arco 278 che coinvolge il nodo canonico `test_core.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 278 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 278 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 278 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 278 che coinvolge il nodo canonico `test_core.py` è `L11`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 278 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 278 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 278 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 278 registra come `source` l'identificatore `Request-Dataset-Generator::metric_engine_run_metric_engine` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 278 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=320 role=incoming -->
Nel grafo unificato, il nodo canonico `test_core.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `request_dataset_generator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator` all'ID `Request-Dataset-Generator::tests_test_core`. La relazione tecnica esplicita è la seguente: `request_dataset_generator.py` importa da `test_core.py`. Nel grafo unificato, il tipo dichiarato dell'arco 320 che coinvolge il nodo canonico `test_core.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 320 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 320 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 320 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 320 che coinvolge il nodo canonico `test_core.py` è `L12`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 320 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 320 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 320 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 320 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 320 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=324 role=incoming -->
Nel grafo unificato, il nodo canonico `test_core.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core`. La relazione tecnica esplicita è la seguente: `build_workload()` importa `test_core.py`. Nel grafo unificato, il tipo dichiarato dell'arco 324 che coinvolge il nodo canonico `test_core.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 324 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 324 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 324 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 324 che coinvolge il nodo canonico `test_core.py` è `L12`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 324 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 324 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 324 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 324 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 324 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=421 role=incoming -->
Nel grafo unificato, il nodo canonico `test_core.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `flow_orchestrator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator` all'ID `Request-Dataset-Generator::tests_test_core`. La relazione tecnica esplicita è la seguente: `flow_orchestrator.py` importa da `test_core.py`. Nel grafo unificato, il tipo dichiarato dell'arco 421 che coinvolge il nodo canonico `test_core.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 421 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 421 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 421 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 421 che coinvolge il nodo canonico `test_core.py` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 421 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 421 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 421 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 421 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 421 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=432 role=incoming -->
Nel grafo unificato, il nodo canonico `test_core.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` importa `test_core.py`. Nel grafo unificato, il tipo dichiarato dell'arco 432 che coinvolge il nodo canonico `test_core.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 432 che coinvolge il nodo canonico `test_core.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 432 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 432 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 432 che coinvolge il nodo canonico `test_core.py` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 432 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 432 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 432 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 432 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 432 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=471 role=incoming -->
Nel grafo unificato, il nodo canonico `test_core.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b` all'ID `Request-Dataset-Generator::tests_test_core`. La relazione tecnica esplicita è la seguente: `rve_1b.py` importa da `test_core.py`. Nel grafo unificato, il tipo dichiarato dell'arco 471 che coinvolge il nodo canonico `test_core.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 471 che coinvolge il nodo canonico `test_core.py` è `submodule_import`. Nel grafo unificato, la confidenza categoriale dell'arco 471 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 471 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 471 che coinvolge il nodo canonico `test_core.py` è `L24`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 471 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 471 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 471 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 471 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 471 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=574 role=incoming -->
Nel grafo unificato, il nodo canonico `test_core.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `scrybasign_get_user_info.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_scrybasign_get_user_info` all'ID `Request-Dataset-Generator::tests_test_core`. La relazione tecnica esplicita è la seguente: `scrybasign_get_user_info.py` importa da `test_core.py`. Nel grafo unificato, il tipo dichiarato dell'arco 574 che coinvolge il nodo canonico `test_core.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 574 che coinvolge il nodo canonico `test_core.py` è `submodule_import`. Nel grafo unificato, la confidenza categoriale dell'arco 574 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 574 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 574 che coinvolge il nodo canonico `test_core.py` è `L24`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 574 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 574 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 574 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 574 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_scrybasign_get_user_info` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 574 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`.
<!-- GRAPH_EDGE graph=unificata index=577 role=incoming -->
Nel grafo unificato, il nodo canonico `test_core.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `scrybasign_sign_one_doc.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_scrybasign_sign_one_doc` all'ID `Request-Dataset-Generator::tests_test_core`. La relazione tecnica esplicita è la seguente: `scrybasign_sign_one_doc.py` importa da `test_core.py`. Nel grafo unificato, il tipo dichiarato dell'arco 577 che coinvolge il nodo canonico `test_core.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 577 che coinvolge il nodo canonico `test_core.py` è `submodule_import`. Nel grafo unificato, la confidenza categoriale dell'arco 577 che coinvolge il nodo canonico `test_core.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 577 che coinvolge il nodo canonico `test_core.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 577 che coinvolge il nodo canonico `test_core.py` è `L24`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 577 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 577 che coinvolge il nodo canonico `test_core.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 577 che coinvolge il nodo canonico `test_core.py` è `1.0`. Nel grafo unificato, l'arco 577 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_scrybasign_sign_one_doc` per la relazione che coinvolge il nodo canonico `test_core.py`. Nel grafo unificato, l'arco 577 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `test_core.py`.

### Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste

<!-- GRAPH_NODE graph=codice index=330 id=timeline_generator_rationale_1 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` possiede l'identificatore esatto `timeline_generator_rationale_1`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `timeline generator  terzo livello della pipeline: prende il dataset di richieste`.
<!-- GRAPH_EDGE graph=codice index=650 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_rationale_1` all'ID `timeline_generator`. La relazione tecnica esplicita è la seguente: `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` costituisce una motivazione per `timeline_generator.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 650 che coinvolge il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 650 che coinvolge il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 650 che coinvolge il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 650 che coinvolge il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 650 che coinvolge il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 650 che coinvolge il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `ast`. Nel grafo del codice RDG, l'arco 650 registra come `source` l'identificatore `timeline_generator_rationale_1` per la relazione che coinvolge il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste`. Nel grafo del codice RDG, l'arco 650 registra come `target` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 650 che coinvolge il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=330 id=Request-Dataset-Generator::timeline_generator_rationale_1 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `timeline generator  terzo livello della pipeline: prende il dataset di richieste`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `timeline_generator_rationale_1`. Nel grafo unificato, il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` possiede l'identificatore esatto `Request-Dataset-Generator::timeline_generator_rationale_1`.
Nel grafo unificato, il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=650 role=incoming -->
Nel grafo unificato, il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator` all'ID `Request-Dataset-Generator::timeline_generator_rationale_1`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` costituisce una motivazione per `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste`. Nel grafo unificato, il tipo dichiarato dell'arco 650 che coinvolge il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 650 che coinvolge il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 650 che coinvolge il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 650 che coinvolge il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 650 che coinvolge il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 650 che coinvolge il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 650 che coinvolge il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` è `1.0`. Nel grafo unificato, l'arco 650 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste`. Nel grafo unificato, l'arco 650 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_rationale_1` per la relazione che coinvolge il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste`.

### timeline_generator.py

<!-- GRAPH_NODE graph=codice index=321 id=timeline_generator -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `timeline_generator.py` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `timeline_generator.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `timeline_generator.py` vale `ast`. Nel grafo del codice RDG, il nodo canonico `timeline_generator.py` possiede l'identificatore esatto `timeline_generator`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `timeline_generator.py` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `timeline_generator.py` è `timeline_generator.py`.
<!-- GRAPH_EDGE graph=codice index=642 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `timeline_generator.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator` all'ID `timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `build_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 642 che coinvolge il nodo canonico `timeline_generator.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 642 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 642 che coinvolge il nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 642 che coinvolge il nodo canonico `timeline_generator.py` è `L177`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 642 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 642 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo del codice RDG, l'arco 642 registra come `source` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, l'arco 642 registra come `target` l'identificatore `timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 642 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=643 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `timeline_generator.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator` all'ID `timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `constant_rate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 643 che coinvolge il nodo canonico `timeline_generator.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 643 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 643 che coinvolge il nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 643 che coinvolge il nodo canonico `timeline_generator.py` è `L22`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 643 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 643 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo del codice RDG, l'arco 643 registra come `source` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, l'arco 643 registra come `target` l'identificatore `timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 643 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=644 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `timeline_generator.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `daily_clinical_profile()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator` all'ID `timeline_generator_daily_clinical_profile`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `daily_clinical_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 644 che coinvolge il nodo canonico `timeline_generator.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 644 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 644 che coinvolge il nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 644 che coinvolge il nodo canonico `timeline_generator.py` è `L29`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 644 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 644 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo del codice RDG, l'arco 644 registra come `source` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, l'arco 644 registra come `target` l'identificatore `timeline_generator_daily_clinical_profile` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 644 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=645 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `timeline_generator.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_frange()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator` all'ID `timeline_generator_frange`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `_frange()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 645 che coinvolge il nodo canonico `timeline_generator.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 645 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 645 che coinvolge il nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 645 che coinvolge il nodo canonico `timeline_generator.py` è `L126`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 645 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 645 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo del codice RDG, l'arco 645 registra come `source` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, l'arco 645 registra come `target` l'identificatore `timeline_generator_frange` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 645 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=646 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `timeline_generator.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `generate_arrivals()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator` all'ID `timeline_generator_generate_arrivals`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `generate_arrivals()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 646 che coinvolge il nodo canonico `timeline_generator.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 646 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 646 che coinvolge il nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 646 che coinvolge il nodo canonico `timeline_generator.py` è `L83`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 646 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 646 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo del codice RDG, l'arco 646 registra come `source` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, l'arco 646 registra come `target` l'identificatore `timeline_generator_generate_arrivals` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 646 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=647 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `timeline_generator.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `inject_bursts()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator` all'ID `timeline_generator_inject_bursts`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `inject_bursts()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 647 che coinvolge il nodo canonico `timeline_generator.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 647 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 647 che coinvolge il nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 647 che coinvolge il nodo canonico `timeline_generator.py` è `L138`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 647 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 647 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo del codice RDG, l'arco 647 registra come `source` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, l'arco 647 registra come `target` l'identificatore `timeline_generator_inject_bursts` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 647 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=648 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `timeline_generator.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `step_profile()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator` all'ID `timeline_generator_step_profile`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `step_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 648 che coinvolge il nodo canonico `timeline_generator.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 648 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 648 che coinvolge il nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 648 che coinvolge il nodo canonico `timeline_generator.py` è `L59`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 648 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 648 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo del codice RDG, l'arco 648 registra come `source` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, l'arco 648 registra come `target` l'identificatore `timeline_generator_step_profile` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 648 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=649 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `timeline_generator.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `timeline_stats()`; l'orientamento registrato nel JSON va dall'ID `timeline_generator` all'ID `timeline_generator_timeline_stats`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `timeline_stats()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 649 che coinvolge il nodo canonico `timeline_generator.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 649 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 649 che coinvolge il nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 649 che coinvolge il nodo canonico `timeline_generator.py` è `L293`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 649 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 649 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo del codice RDG, l'arco 649 registra come `source` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, l'arco 649 registra come `target` l'identificatore `timeline_generator_timeline_stats` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 649 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=142 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `timeline_generator.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `timeline_generator`. La relazione tecnica esplicita è la seguente: `main.py` importa da `timeline_generator.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 142 che coinvolge il nodo canonico `timeline_generator.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 142 che coinvolge il nodo canonico `timeline_generator.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 142 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 142 che coinvolge il nodo canonico `timeline_generator.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 142 che coinvolge il nodo canonico `timeline_generator.py` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 142 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 142 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo del codice RDG, l'arco 142 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, l'arco 142 registra come `target` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 142 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=585 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `timeline_generator.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `timeline_generator`. La relazione tecnica esplicita è la seguente: `test_core.py` importa da `timeline_generator.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 585 che coinvolge il nodo canonico `timeline_generator.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 585 che coinvolge il nodo canonico `timeline_generator.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 585 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 585 che coinvolge il nodo canonico `timeline_generator.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 585 che coinvolge il nodo canonico `timeline_generator.py` è `L13`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 585 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 585 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo del codice RDG, l'arco 585 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, l'arco 585 registra come `target` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 585 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=650 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `timeline_generator.py` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_rationale_1` all'ID `timeline_generator`. La relazione tecnica esplicita è la seguente: `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste` costituisce una motivazione per `timeline_generator.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 650 che coinvolge il nodo canonico `timeline_generator.py` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 650 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 650 che coinvolge il nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 650 che coinvolge il nodo canonico `timeline_generator.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 650 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 650 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo del codice RDG, l'arco 650 registra come `source` l'identificatore `timeline_generator_rationale_1` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, l'arco 650 registra come `target` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 650 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=321 id=Request-Dataset-Generator::timeline_generator -->
Nel grafo unificato, l'etichetta originale del nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `timeline_generator.py` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `timeline_generator.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `timeline_generator.py` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `timeline_generator.py` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `timeline_generator.py` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `timeline_generator.py` è `timeline_generator`. Nel grafo unificato, il nodo canonico `timeline_generator.py` possiede l'identificatore esatto `Request-Dataset-Generator::timeline_generator`.
<!-- GRAPH_EDGE graph=unificata index=642 role=outgoing -->
Nel grafo unificato, il nodo canonico `timeline_generator.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `build_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator` all'ID `Request-Dataset-Generator::timeline_generator_build_timeline`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `build_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 642 che coinvolge il nodo canonico `timeline_generator.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 642 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 642 che coinvolge il nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 642 che coinvolge il nodo canonico `timeline_generator.py` è `L177`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 642 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 642 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 642 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, l'arco 642 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo unificato, l'arco 642 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_build_timeline` per la relazione che coinvolge il nodo canonico `timeline_generator.py`.
<!-- GRAPH_EDGE graph=unificata index=643 role=outgoing -->
Nel grafo unificato, il nodo canonico `timeline_generator.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `constant_rate()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator` all'ID `Request-Dataset-Generator::timeline_generator_constant_rate`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `constant_rate()`. Nel grafo unificato, il tipo dichiarato dell'arco 643 che coinvolge il nodo canonico `timeline_generator.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 643 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 643 che coinvolge il nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 643 che coinvolge il nodo canonico `timeline_generator.py` è `L22`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 643 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 643 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 643 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, l'arco 643 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo unificato, l'arco 643 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_constant_rate` per la relazione che coinvolge il nodo canonico `timeline_generator.py`.
<!-- GRAPH_EDGE graph=unificata index=644 role=outgoing -->
Nel grafo unificato, il nodo canonico `timeline_generator.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `daily_clinical_profile()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator` all'ID `Request-Dataset-Generator::timeline_generator_daily_clinical_profile`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `daily_clinical_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 644 che coinvolge il nodo canonico `timeline_generator.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 644 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 644 che coinvolge il nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 644 che coinvolge il nodo canonico `timeline_generator.py` è `L29`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 644 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 644 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 644 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, l'arco 644 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo unificato, l'arco 644 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_daily_clinical_profile` per la relazione che coinvolge il nodo canonico `timeline_generator.py`.
<!-- GRAPH_EDGE graph=unificata index=645 role=outgoing -->
Nel grafo unificato, il nodo canonico `timeline_generator.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_frange()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator` all'ID `Request-Dataset-Generator::timeline_generator_frange`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `_frange()`. Nel grafo unificato, il tipo dichiarato dell'arco 645 che coinvolge il nodo canonico `timeline_generator.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 645 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 645 che coinvolge il nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 645 che coinvolge il nodo canonico `timeline_generator.py` è `L126`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 645 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 645 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 645 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, l'arco 645 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo unificato, l'arco 645 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_frange` per la relazione che coinvolge il nodo canonico `timeline_generator.py`.
<!-- GRAPH_EDGE graph=unificata index=646 role=outgoing -->
Nel grafo unificato, il nodo canonico `timeline_generator.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `generate_arrivals()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator` all'ID `Request-Dataset-Generator::timeline_generator_generate_arrivals`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `generate_arrivals()`. Nel grafo unificato, il tipo dichiarato dell'arco 646 che coinvolge il nodo canonico `timeline_generator.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 646 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 646 che coinvolge il nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 646 che coinvolge il nodo canonico `timeline_generator.py` è `L83`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 646 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 646 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 646 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, l'arco 646 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo unificato, l'arco 646 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_generate_arrivals` per la relazione che coinvolge il nodo canonico `timeline_generator.py`.
<!-- GRAPH_EDGE graph=unificata index=647 role=outgoing -->
Nel grafo unificato, il nodo canonico `timeline_generator.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `inject_bursts()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator` all'ID `Request-Dataset-Generator::timeline_generator_inject_bursts`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `inject_bursts()`. Nel grafo unificato, il tipo dichiarato dell'arco 647 che coinvolge il nodo canonico `timeline_generator.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 647 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 647 che coinvolge il nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 647 che coinvolge il nodo canonico `timeline_generator.py` è `L138`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 647 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 647 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 647 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, l'arco 647 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo unificato, l'arco 647 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_inject_bursts` per la relazione che coinvolge il nodo canonico `timeline_generator.py`.
<!-- GRAPH_EDGE graph=unificata index=648 role=outgoing -->
Nel grafo unificato, il nodo canonico `timeline_generator.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `step_profile()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator` all'ID `Request-Dataset-Generator::timeline_generator_step_profile`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `step_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 648 che coinvolge il nodo canonico `timeline_generator.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 648 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 648 che coinvolge il nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 648 che coinvolge il nodo canonico `timeline_generator.py` è `L59`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 648 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 648 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 648 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, l'arco 648 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo unificato, l'arco 648 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_step_profile` per la relazione che coinvolge il nodo canonico `timeline_generator.py`.
<!-- GRAPH_EDGE graph=unificata index=649 role=outgoing -->
Nel grafo unificato, il nodo canonico `timeline_generator.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `timeline_stats()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator` all'ID `Request-Dataset-Generator::timeline_generator_timeline_stats`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `timeline_stats()`. Nel grafo unificato, il tipo dichiarato dell'arco 649 che coinvolge il nodo canonico `timeline_generator.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 649 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 649 che coinvolge il nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 649 che coinvolge il nodo canonico `timeline_generator.py` è `L293`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 649 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 649 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 649 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, l'arco 649 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo unificato, l'arco 649 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_timeline_stats` per la relazione che coinvolge il nodo canonico `timeline_generator.py`.
<!-- GRAPH_EDGE graph=unificata index=650 role=outgoing -->
Nel grafo unificato, il nodo canonico `timeline_generator.py` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator` all'ID `Request-Dataset-Generator::timeline_generator_rationale_1`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` costituisce una motivazione per `Timeline Generator  Terzo livello della pipeline: prende il dataset di richieste`. Nel grafo unificato, il tipo dichiarato dell'arco 650 che coinvolge il nodo canonico `timeline_generator.py` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 650 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 650 che coinvolge il nodo canonico `timeline_generator.py` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 650 che coinvolge il nodo canonico `timeline_generator.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 650 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 650 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 650 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, l'arco 650 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo unificato, l'arco 650 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_rationale_1` per la relazione che coinvolge il nodo canonico `timeline_generator.py`.
<!-- GRAPH_EDGE graph=unificata index=142 role=incoming -->
Nel grafo unificato, il nodo canonico `timeline_generator.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::timeline_generator`. La relazione tecnica esplicita è la seguente: `main.py` importa da `timeline_generator.py`. Nel grafo unificato, il tipo dichiarato dell'arco 142 che coinvolge il nodo canonico `timeline_generator.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 142 che coinvolge il nodo canonico `timeline_generator.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 142 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 142 che coinvolge il nodo canonico `timeline_generator.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 142 che coinvolge il nodo canonico `timeline_generator.py` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 142 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 142 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 142 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, l'arco 142 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo unificato, l'arco 142 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`.
<!-- GRAPH_EDGE graph=unificata index=585 role=incoming -->
Nel grafo unificato, il nodo canonico `timeline_generator.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::timeline_generator`. La relazione tecnica esplicita è la seguente: `test_core.py` importa da `timeline_generator.py`. Nel grafo unificato, il tipo dichiarato dell'arco 585 che coinvolge il nodo canonico `timeline_generator.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 585 che coinvolge il nodo canonico `timeline_generator.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 585 che coinvolge il nodo canonico `timeline_generator.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 585 che coinvolge il nodo canonico `timeline_generator.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 585 che coinvolge il nodo canonico `timeline_generator.py` è `L13`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 585 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 585 che coinvolge il nodo canonico `timeline_generator.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 585 che coinvolge il nodo canonico `timeline_generator.py` è `1.0`. Nel grafo unificato, l'arco 585 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `timeline_generator.py`. Nel grafo unificato, l'arco 585 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_generator.py`.

### timeline_stats()

<!-- GRAPH_NODE graph=codice index=329 id=timeline_generator_timeline_stats -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `timeline_stats()` è `timeline_stats()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `timeline_stats()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `timeline_stats()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `timeline_stats()` è `L293`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `timeline_stats()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `timeline_stats()` possiede l'identificatore esatto `timeline_generator_timeline_stats`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `timeline_stats()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `timeline_stats()` è `timeline_stats()`.
Nel grafo del codice RDG, il nodo canonico `timeline_stats()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=147 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `timeline_stats()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `timeline_generator_timeline_stats`. La relazione tecnica esplicita è la seguente: `main.py` importa `timeline_stats()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 147 che coinvolge il nodo canonico `timeline_stats()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 147 che coinvolge il nodo canonico `timeline_stats()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 147 che coinvolge il nodo canonico `timeline_stats()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 147 che coinvolge il nodo canonico `timeline_stats()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 147 che coinvolge il nodo canonico `timeline_stats()` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 147 che coinvolge il nodo canonico `timeline_stats()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 147 che coinvolge il nodo canonico `timeline_stats()` è `ast`. Nel grafo del codice RDG, l'arco 147 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `timeline_stats()`. Nel grafo del codice RDG, l'arco 147 registra come `target` l'identificatore `timeline_generator_timeline_stats` per la relazione che coinvolge il nodo canonico `timeline_stats()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 147 che coinvolge il nodo canonico `timeline_stats()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=202 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `timeline_stats()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_timeline` all'ID `timeline_generator_timeline_stats`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `timeline_stats()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 202 che coinvolge il nodo canonico `timeline_stats()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 202 che coinvolge il nodo canonico `timeline_stats()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 202 che coinvolge il nodo canonico `timeline_stats()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 202 che coinvolge il nodo canonico `timeline_stats()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 202 che coinvolge il nodo canonico `timeline_stats()` è `L459`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 202 che coinvolge il nodo canonico `timeline_stats()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 202 che coinvolge il nodo canonico `timeline_stats()` è `ast`. Nel grafo del codice RDG, l'arco 202 registra come `source` l'identificatore `main_cmd_timeline` per la relazione che coinvolge il nodo canonico `timeline_stats()`. Nel grafo del codice RDG, l'arco 202 registra come `target` l'identificatore `timeline_generator_timeline_stats` per la relazione che coinvolge il nodo canonico `timeline_stats()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 202 che coinvolge il nodo canonico `timeline_stats()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=649 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `timeline_stats()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `timeline_generator` all'ID `timeline_generator_timeline_stats`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `timeline_stats()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 649 che coinvolge il nodo canonico `timeline_stats()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 649 che coinvolge il nodo canonico `timeline_stats()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 649 che coinvolge il nodo canonico `timeline_stats()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 649 che coinvolge il nodo canonico `timeline_stats()` è `L293`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 649 che coinvolge il nodo canonico `timeline_stats()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 649 che coinvolge il nodo canonico `timeline_stats()` è `ast`. Nel grafo del codice RDG, l'arco 649 registra come `source` l'identificatore `timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_stats()`. Nel grafo del codice RDG, l'arco 649 registra come `target` l'identificatore `timeline_generator_timeline_stats` per la relazione che coinvolge il nodo canonico `timeline_stats()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 649 che coinvolge il nodo canonico `timeline_stats()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=660 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `timeline_stats()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Calcola statistiche descrittive della timeline generata.`; l'orientamento registrato nel JSON va dall'ID `timeline_generator_rationale_294` all'ID `timeline_generator_timeline_stats`. La relazione tecnica esplicita è la seguente: `Calcola statistiche descrittive della timeline generata.` costituisce una motivazione per `timeline_stats()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 660 che coinvolge il nodo canonico `timeline_stats()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 660 che coinvolge il nodo canonico `timeline_stats()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 660 che coinvolge il nodo canonico `timeline_stats()` è `timeline_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 660 che coinvolge il nodo canonico `timeline_stats()` è `L294`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 660 che coinvolge il nodo canonico `timeline_stats()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 660 che coinvolge il nodo canonico `timeline_stats()` è `ast`. Nel grafo del codice RDG, l'arco 660 registra come `source` l'identificatore `timeline_generator_rationale_294` per la relazione che coinvolge il nodo canonico `timeline_stats()`. Nel grafo del codice RDG, l'arco 660 registra come `target` l'identificatore `timeline_generator_timeline_stats` per la relazione che coinvolge il nodo canonico `timeline_stats()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 660 che coinvolge il nodo canonico `timeline_stats()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=329 id=Request-Dataset-Generator::timeline_generator_timeline_stats -->
Nel grafo unificato, l'etichetta originale del nodo canonico `timeline_stats()` è `timeline_stats()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `timeline_stats()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `timeline_stats()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `timeline_stats()` è `L293`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `timeline_stats()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `timeline_stats()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `timeline_stats()` è `timeline_stats()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `timeline_stats()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `timeline_stats()` è `timeline_generator_timeline_stats`. Nel grafo unificato, il nodo canonico `timeline_stats()` possiede l'identificatore esatto `Request-Dataset-Generator::timeline_generator_timeline_stats`.
<!-- GRAPH_EDGE graph=unificata index=660 role=outgoing -->
Nel grafo unificato, il nodo canonico `timeline_stats()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Calcola statistiche descrittive della timeline generata.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator_timeline_stats` all'ID `Request-Dataset-Generator::timeline_generator_rationale_294`. La relazione tecnica esplicita è la seguente: `timeline_stats()` costituisce una motivazione per `Calcola statistiche descrittive della timeline generata.`. Nel grafo unificato, il tipo dichiarato dell'arco 660 che coinvolge il nodo canonico `timeline_stats()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 660 che coinvolge il nodo canonico `timeline_stats()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 660 che coinvolge il nodo canonico `timeline_stats()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 660 che coinvolge il nodo canonico `timeline_stats()` è `L294`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 660 che coinvolge il nodo canonico `timeline_stats()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 660 che coinvolge il nodo canonico `timeline_stats()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 660 che coinvolge il nodo canonico `timeline_stats()` è `1.0`. Nel grafo unificato, l'arco 660 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator_timeline_stats` per la relazione che coinvolge il nodo canonico `timeline_stats()`. Nel grafo unificato, l'arco 660 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_rationale_294` per la relazione che coinvolge il nodo canonico `timeline_stats()`.
<!-- GRAPH_EDGE graph=unificata index=147 role=incoming -->
Nel grafo unificato, il nodo canonico `timeline_stats()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::timeline_generator_timeline_stats`. La relazione tecnica esplicita è la seguente: `main.py` importa `timeline_stats()`. Nel grafo unificato, il tipo dichiarato dell'arco 147 che coinvolge il nodo canonico `timeline_stats()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 147 che coinvolge il nodo canonico `timeline_stats()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 147 che coinvolge il nodo canonico `timeline_stats()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 147 che coinvolge il nodo canonico `timeline_stats()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 147 che coinvolge il nodo canonico `timeline_stats()` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 147 che coinvolge il nodo canonico `timeline_stats()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 147 che coinvolge il nodo canonico `timeline_stats()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 147 che coinvolge il nodo canonico `timeline_stats()` è `1.0`. Nel grafo unificato, l'arco 147 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `timeline_stats()`. Nel grafo unificato, l'arco 147 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_timeline_stats` per la relazione che coinvolge il nodo canonico `timeline_stats()`.
<!-- GRAPH_EDGE graph=unificata index=202 role=incoming -->
Nel grafo unificato, il nodo canonico `timeline_stats()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main_cmd_timeline` all'ID `Request-Dataset-Generator::timeline_generator_timeline_stats`. La relazione tecnica esplicita è la seguente: `cmd_timeline()` chiama `timeline_stats()`. Nel grafo unificato, il tipo dichiarato dell'arco 202 che coinvolge il nodo canonico `timeline_stats()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 202 che coinvolge il nodo canonico `timeline_stats()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 202 che coinvolge il nodo canonico `timeline_stats()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 202 che coinvolge il nodo canonico `timeline_stats()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 202 che coinvolge il nodo canonico `timeline_stats()` è `L459`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 202 che coinvolge il nodo canonico `timeline_stats()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 202 che coinvolge il nodo canonico `timeline_stats()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 202 che coinvolge il nodo canonico `timeline_stats()` è `1.0`. Nel grafo unificato, l'arco 202 registra come `source` l'identificatore `Request-Dataset-Generator::main_cmd_timeline` per la relazione che coinvolge il nodo canonico `timeline_stats()`. Nel grafo unificato, l'arco 202 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_timeline_stats` per la relazione che coinvolge il nodo canonico `timeline_stats()`.
<!-- GRAPH_EDGE graph=unificata index=649 role=incoming -->
Nel grafo unificato, il nodo canonico `timeline_stats()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `timeline_generator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::timeline_generator` all'ID `Request-Dataset-Generator::timeline_generator_timeline_stats`. La relazione tecnica esplicita è la seguente: `timeline_generator.py` contiene `timeline_stats()`. Nel grafo unificato, il tipo dichiarato dell'arco 649 che coinvolge il nodo canonico `timeline_stats()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 649 che coinvolge il nodo canonico `timeline_stats()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 649 che coinvolge il nodo canonico `timeline_stats()` è `timeline_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 649 che coinvolge il nodo canonico `timeline_stats()` è `L293`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 649 che coinvolge il nodo canonico `timeline_stats()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 649 che coinvolge il nodo canonico `timeline_stats()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 649 che coinvolge il nodo canonico `timeline_stats()` è `1.0`. Nel grafo unificato, l'arco 649 registra come `source` l'identificatore `Request-Dataset-Generator::timeline_generator` per la relazione che coinvolge il nodo canonico `timeline_stats()`. Nel grafo unificato, l'arco 649 registra come `target` l'identificatore `Request-Dataset-Generator::timeline_generator_timeline_stats` per la relazione che coinvolge il nodo canonico `timeline_stats()`.

### TimelineIdentityTest

<!-- GRAPH_NODE graph=codice index=314 id=tests_test_core_timelineidentitytest -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `TimelineIdentityTest` è `TimelineIdentityTest`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `TimelineIdentityTest` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `TimelineIdentityTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `TimelineIdentityTest` è `L771`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `TimelineIdentityTest` vale `ast`. Nel grafo del codice RDG, il nodo canonico `TimelineIdentityTest` possiede l'identificatore esatto `tests_test_core_timelineidentitytest`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `TimelineIdentityTest` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `TimelineIdentityTest` è `timelineidentitytest`.
<!-- GRAPH_EDGE graph=codice index=445 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `TimelineIdentityTest` è la sorgente `source` della relazione `uses` verso il nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_timelineidentitytest` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `TimelineIdentityTest` usa `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 445 che coinvolge il nodo canonico `TimelineIdentityTest` è `uses`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 445 che coinvolge il nodo canonico `TimelineIdentityTest` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 445 che coinvolge il nodo canonico `TimelineIdentityTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 445 che coinvolge il nodo canonico `TimelineIdentityTest` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 445 che coinvolge il nodo canonico `TimelineIdentityTest` è `0.8`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 445 che coinvolge il nodo canonico `TimelineIdentityTest` è `ast`. Nel grafo del codice RDG, l'arco 445 registra come `source` l'identificatore `tests_test_core_timelineidentitytest` per la relazione che coinvolge il nodo canonico `TimelineIdentityTest`. Nel grafo del codice RDG, l'arco 445 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `TimelineIdentityTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 445 che coinvolge il nodo canonico `TimelineIdentityTest` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=634 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `TimelineIdentityTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_timelineidentitytest` all'ID `tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids`. La relazione tecnica esplicita è la seguente: `TimelineIdentityTest` registra come proprio metodo `.test_timeline_assigns_unique_flow_occurrence_ids()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 634 che coinvolge il nodo canonico `TimelineIdentityTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 634 che coinvolge il nodo canonico `TimelineIdentityTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 634 che coinvolge il nodo canonico `TimelineIdentityTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 634 che coinvolge il nodo canonico `TimelineIdentityTest` è `L772`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 634 che coinvolge il nodo canonico `TimelineIdentityTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 634 che coinvolge il nodo canonico `TimelineIdentityTest` è `ast`. Nel grafo del codice RDG, l'arco 634 registra come `source` l'identificatore `tests_test_core_timelineidentitytest` per la relazione che coinvolge il nodo canonico `TimelineIdentityTest`. Nel grafo del codice RDG, l'arco 634 registra come `target` l'identificatore `tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` per la relazione che coinvolge il nodo canonico `TimelineIdentityTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 634 che coinvolge il nodo canonico `TimelineIdentityTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=635 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `TimelineIdentityTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_timeline_tags_burst_events()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_timelineidentitytest` all'ID `tests_test_core_timelineidentitytest_test_timeline_tags_burst_events`. La relazione tecnica esplicita è la seguente: `TimelineIdentityTest` registra come proprio metodo `.test_timeline_tags_burst_events()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 635 che coinvolge il nodo canonico `TimelineIdentityTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 635 che coinvolge il nodo canonico `TimelineIdentityTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 635 che coinvolge il nodo canonico `TimelineIdentityTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 635 che coinvolge il nodo canonico `TimelineIdentityTest` è `L795`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 635 che coinvolge il nodo canonico `TimelineIdentityTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 635 che coinvolge il nodo canonico `TimelineIdentityTest` è `ast`. Nel grafo del codice RDG, l'arco 635 registra come `source` l'identificatore `tests_test_core_timelineidentitytest` per la relazione che coinvolge il nodo canonico `TimelineIdentityTest`. Nel grafo del codice RDG, l'arco 635 registra come `target` l'identificatore `tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` per la relazione che coinvolge il nodo canonico `TimelineIdentityTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 635 che coinvolge il nodo canonico `TimelineIdentityTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=584 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `TimelineIdentityTest` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `tests_test_core_timelineidentitytest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `TimelineIdentityTest`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 584 che coinvolge il nodo canonico `TimelineIdentityTest` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 584 che coinvolge il nodo canonico `TimelineIdentityTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 584 che coinvolge il nodo canonico `TimelineIdentityTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 584 che coinvolge il nodo canonico `TimelineIdentityTest` è `L771`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 584 che coinvolge il nodo canonico `TimelineIdentityTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 584 che coinvolge il nodo canonico `TimelineIdentityTest` è `ast`. Nel grafo del codice RDG, l'arco 584 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `TimelineIdentityTest`. Nel grafo del codice RDG, l'arco 584 registra come `target` l'identificatore `tests_test_core_timelineidentitytest` per la relazione che coinvolge il nodo canonico `TimelineIdentityTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 584 che coinvolge il nodo canonico `TimelineIdentityTest` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=314 id=Request-Dataset-Generator::tests_test_core_timelineidentitytest -->
Nel grafo unificato, l'etichetta originale del nodo canonico `TimelineIdentityTest` è `TimelineIdentityTest`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `TimelineIdentityTest` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `TimelineIdentityTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `TimelineIdentityTest` è `L771`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `TimelineIdentityTest` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `TimelineIdentityTest` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `TimelineIdentityTest` è `timelineidentitytest`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `TimelineIdentityTest` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `TimelineIdentityTest` è `tests_test_core_timelineidentitytest`. Nel grafo unificato, il nodo canonico `TimelineIdentityTest` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_timelineidentitytest`.
<!-- GRAPH_EDGE graph=unificata index=634 role=outgoing -->
Nel grafo unificato, il nodo canonico `TimelineIdentityTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_timeline_assigns_unique_flow_occurrence_ids()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest` all'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids`. La relazione tecnica esplicita è la seguente: `TimelineIdentityTest` registra come proprio metodo `.test_timeline_assigns_unique_flow_occurrence_ids()`. Nel grafo unificato, il tipo dichiarato dell'arco 634 che coinvolge il nodo canonico `TimelineIdentityTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 634 che coinvolge il nodo canonico `TimelineIdentityTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 634 che coinvolge il nodo canonico `TimelineIdentityTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 634 che coinvolge il nodo canonico `TimelineIdentityTest` è `L772`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 634 che coinvolge il nodo canonico `TimelineIdentityTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 634 che coinvolge il nodo canonico `TimelineIdentityTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 634 che coinvolge il nodo canonico `TimelineIdentityTest` è `1.0`. Nel grafo unificato, l'arco 634 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest` per la relazione che coinvolge il nodo canonico `TimelineIdentityTest`. Nel grafo unificato, l'arco 634 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_assigns_unique_flow_occurrence_ids` per la relazione che coinvolge il nodo canonico `TimelineIdentityTest`.
<!-- GRAPH_EDGE graph=unificata index=635 role=outgoing -->
Nel grafo unificato, il nodo canonico `TimelineIdentityTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_timeline_tags_burst_events()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest` all'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_tags_burst_events`. La relazione tecnica esplicita è la seguente: `TimelineIdentityTest` registra come proprio metodo `.test_timeline_tags_burst_events()`. Nel grafo unificato, il tipo dichiarato dell'arco 635 che coinvolge il nodo canonico `TimelineIdentityTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 635 che coinvolge il nodo canonico `TimelineIdentityTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 635 che coinvolge il nodo canonico `TimelineIdentityTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 635 che coinvolge il nodo canonico `TimelineIdentityTest` è `L795`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 635 che coinvolge il nodo canonico `TimelineIdentityTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 635 che coinvolge il nodo canonico `TimelineIdentityTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 635 che coinvolge il nodo canonico `TimelineIdentityTest` è `1.0`. Nel grafo unificato, l'arco 635 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest` per la relazione che coinvolge il nodo canonico `TimelineIdentityTest`. Nel grafo unificato, l'arco 635 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest_test_timeline_tags_burst_events` per la relazione che coinvolge il nodo canonico `TimelineIdentityTest`.
<!-- GRAPH_EDGE graph=unificata index=445 role=incoming -->
Nel grafo unificato, il nodo canonico `TimelineIdentityTest` è il destinatario `target` della relazione `uses` proveniente dal nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` usa `TimelineIdentityTest`. Nel grafo unificato, il tipo dichiarato dell'arco 445 che coinvolge il nodo canonico `TimelineIdentityTest` è `uses`. Nel grafo unificato, la confidenza categoriale dell'arco 445 che coinvolge il nodo canonico `TimelineIdentityTest` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 445 che coinvolge il nodo canonico `TimelineIdentityTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 445 che coinvolge il nodo canonico `TimelineIdentityTest` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 445 che coinvolge il nodo canonico `TimelineIdentityTest` è `0.8`. Nel grafo unificato, la provenienza `_origin` dell'arco 445 che coinvolge il nodo canonico `TimelineIdentityTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 445 che coinvolge il nodo canonico `TimelineIdentityTest` è `0.5`. Nel grafo unificato, l'arco 445 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `TimelineIdentityTest`. Nel grafo unificato, l'arco 445 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest` per la relazione che coinvolge il nodo canonico `TimelineIdentityTest`.
<!-- GRAPH_EDGE graph=unificata index=584 role=incoming -->
Nel grafo unificato, il nodo canonico `TimelineIdentityTest` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `TimelineIdentityTest`. Nel grafo unificato, il tipo dichiarato dell'arco 584 che coinvolge il nodo canonico `TimelineIdentityTest` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 584 che coinvolge il nodo canonico `TimelineIdentityTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 584 che coinvolge il nodo canonico `TimelineIdentityTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 584 che coinvolge il nodo canonico `TimelineIdentityTest` è `L771`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 584 che coinvolge il nodo canonico `TimelineIdentityTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 584 che coinvolge il nodo canonico `TimelineIdentityTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 584 che coinvolge il nodo canonico `TimelineIdentityTest` è `1.0`. Nel grafo unificato, l'arco 584 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `TimelineIdentityTest`. Nel grafo unificato, l'arco 584 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest` per la relazione che coinvolge il nodo canonico `TimelineIdentityTest`.

### to_json()

<!-- GRAPH_NODE graph=codice index=18 id=fhir_patient_generator_to_json -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `to_json()` è `to_json()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `to_json()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `to_json()` è `fhir_patient_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `to_json()` è `L603`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `to_json()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `to_json()` possiede l'identificatore esatto `fhir_patient_generator_to_json`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `to_json()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `to_json()` è `to_json()`.
Nel grafo del codice RDG, il nodo canonico `to_json()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=18 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `to_json()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `fhir_patient_generator.py`; l'orientamento registrato nel JSON va dall'ID `fhir_patient_generator` all'ID `fhir_patient_generator_to_json`. La relazione tecnica esplicita è la seguente: `fhir_patient_generator.py` contiene `to_json()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 18 che coinvolge il nodo canonico `to_json()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 18 che coinvolge il nodo canonico `to_json()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 18 che coinvolge il nodo canonico `to_json()` è `fhir_patient_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 18 che coinvolge il nodo canonico `to_json()` è `L603`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 18 che coinvolge il nodo canonico `to_json()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 18 che coinvolge il nodo canonico `to_json()` è `ast`. Nel grafo del codice RDG, l'arco 18 registra come `source` l'identificatore `fhir_patient_generator` per la relazione che coinvolge il nodo canonico `to_json()`. Nel grafo del codice RDG, l'arco 18 registra come `target` l'identificatore `fhir_patient_generator_to_json` per la relazione che coinvolge il nodo canonico `to_json()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 18 che coinvolge il nodo canonico `to_json()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=49 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `to_json()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `fhir_patient_generator_to_json`. La relazione tecnica esplicita è la seguente: `main.py` importa `to_json()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 49 che coinvolge il nodo canonico `to_json()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 49 che coinvolge il nodo canonico `to_json()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 49 che coinvolge il nodo canonico `to_json()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 49 che coinvolge il nodo canonico `to_json()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 49 che coinvolge il nodo canonico `to_json()` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 49 che coinvolge il nodo canonico `to_json()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 49 che coinvolge il nodo canonico `to_json()` è `ast`. Nel grafo del codice RDG, l'arco 49 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `to_json()`. Nel grafo del codice RDG, l'arco 49 registra come `target` l'identificatore `fhir_patient_generator_to_json` per la relazione che coinvolge il nodo canonico `to_json()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 49 che coinvolge il nodo canonico `to_json()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=50 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `to_json()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_patients()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_patients` all'ID `fhir_patient_generator_to_json`. La relazione tecnica esplicita è la seguente: `cmd_patients()` chiama `to_json()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 50 che coinvolge il nodo canonico `to_json()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 50 che coinvolge il nodo canonico `to_json()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 50 che coinvolge il nodo canonico `to_json()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 50 che coinvolge il nodo canonico `to_json()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 50 che coinvolge il nodo canonico `to_json()` è `L265`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 50 che coinvolge il nodo canonico `to_json()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 50 che coinvolge il nodo canonico `to_json()` è `ast`. Nel grafo del codice RDG, l'arco 50 registra come `source` l'identificatore `main_cmd_patients` per la relazione che coinvolge il nodo canonico `to_json()`. Nel grafo del codice RDG, l'arco 50 registra come `target` l'identificatore `fhir_patient_generator_to_json` per la relazione che coinvolge il nodo canonico `to_json()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 50 che coinvolge il nodo canonico `to_json()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=18 id=Request-Dataset-Generator::fhir_patient_generator_to_json -->
Nel grafo unificato, l'etichetta originale del nodo canonico `to_json()` è `to_json()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `to_json()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `to_json()` è `fhir_patient_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `to_json()` è `L603`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `to_json()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `to_json()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `to_json()` è `to_json()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `to_json()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `to_json()` è `fhir_patient_generator_to_json`. Nel grafo unificato, il nodo canonico `to_json()` possiede l'identificatore esatto `Request-Dataset-Generator::fhir_patient_generator_to_json`.
<!-- GRAPH_EDGE graph=unificata index=49 role=outgoing -->
Nel grafo unificato, il nodo canonico `to_json()` è la sorgente `source` della relazione `imports` verso il nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::fhir_patient_generator_to_json` all'ID `Request-Dataset-Generator::main`. La relazione tecnica esplicita è la seguente: `to_json()` importa `main.py`. Nel grafo unificato, il tipo dichiarato dell'arco 49 che coinvolge il nodo canonico `to_json()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 49 che coinvolge il nodo canonico `to_json()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 49 che coinvolge il nodo canonico `to_json()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 49 che coinvolge il nodo canonico `to_json()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 49 che coinvolge il nodo canonico `to_json()` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 49 che coinvolge il nodo canonico `to_json()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 49 che coinvolge il nodo canonico `to_json()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 49 che coinvolge il nodo canonico `to_json()` è `1.0`. Nel grafo unificato, l'arco 49 registra come `source` l'identificatore `Request-Dataset-Generator::fhir_patient_generator_to_json` per la relazione che coinvolge il nodo canonico `to_json()`. Nel grafo unificato, l'arco 49 registra come `target` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `to_json()`.
<!-- GRAPH_EDGE graph=unificata index=50 role=outgoing -->
Nel grafo unificato, il nodo canonico `to_json()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_patients()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::fhir_patient_generator_to_json` all'ID `Request-Dataset-Generator::main_cmd_patients`. La relazione tecnica esplicita è la seguente: `to_json()` chiama `cmd_patients()`. Nel grafo unificato, il tipo dichiarato dell'arco 50 che coinvolge il nodo canonico `to_json()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 50 che coinvolge il nodo canonico `to_json()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 50 che coinvolge il nodo canonico `to_json()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 50 che coinvolge il nodo canonico `to_json()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 50 che coinvolge il nodo canonico `to_json()` è `L265`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 50 che coinvolge il nodo canonico `to_json()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 50 che coinvolge il nodo canonico `to_json()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 50 che coinvolge il nodo canonico `to_json()` è `1.0`. Nel grafo unificato, l'arco 50 registra come `source` l'identificatore `Request-Dataset-Generator::fhir_patient_generator_to_json` per la relazione che coinvolge il nodo canonico `to_json()`. Nel grafo unificato, l'arco 50 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_patients` per la relazione che coinvolge il nodo canonico `to_json()`.
<!-- GRAPH_EDGE graph=unificata index=18 role=incoming -->
Nel grafo unificato, il nodo canonico `to_json()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `fhir_patient_generator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::fhir_patient_generator` all'ID `Request-Dataset-Generator::fhir_patient_generator_to_json`. La relazione tecnica esplicita è la seguente: `fhir_patient_generator.py` contiene `to_json()`. Nel grafo unificato, il tipo dichiarato dell'arco 18 che coinvolge il nodo canonico `to_json()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 18 che coinvolge il nodo canonico `to_json()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 18 che coinvolge il nodo canonico `to_json()` è `fhir_patient_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 18 che coinvolge il nodo canonico `to_json()` è `L603`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 18 che coinvolge il nodo canonico `to_json()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 18 che coinvolge il nodo canonico `to_json()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 18 che coinvolge il nodo canonico `to_json()` è `1.0`. Nel grafo unificato, l'arco 18 registra come `source` l'identificatore `Request-Dataset-Generator::fhir_patient_generator` per la relazione che coinvolge il nodo canonico `to_json()`. Nel grafo unificato, l'arco 18 registra come `target` l'identificatore `Request-Dataset-Generator::fhir_patient_generator_to_json` per la relazione che coinvolge il nodo canonico `to_json()`.

### to_xml()

<!-- GRAPH_NODE graph=codice index=21 id=fhir_patient_generator_to_xml -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `to_xml()` è `to_xml()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `to_xml()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `to_xml()` è `fhir_patient_generator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `to_xml()` è `L648`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `to_xml()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `to_xml()` possiede l'identificatore esatto `fhir_patient_generator_to_xml`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `to_xml()` alla comunità numerica `1`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `to_xml()` è `to_xml()`.
<!-- GRAPH_EDGE graph=codice index=53 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `to_xml()` è la sorgente `source` della relazione `calls` verso il nodo canonico `dict_to_xml_element()`; l'orientamento registrato nel JSON va dall'ID `fhir_patient_generator_to_xml` all'ID `fhir_patient_generator_dict_to_xml_element`. La relazione tecnica esplicita è la seguente: `to_xml()` chiama `dict_to_xml_element()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 53 che coinvolge il nodo canonico `to_xml()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 53 che coinvolge il nodo canonico `to_xml()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 53 che coinvolge il nodo canonico `to_xml()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 53 che coinvolge il nodo canonico `to_xml()` è `fhir_patient_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 53 che coinvolge il nodo canonico `to_xml()` è `L650`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 53 che coinvolge il nodo canonico `to_xml()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 53 che coinvolge il nodo canonico `to_xml()` è `ast`. Nel grafo del codice RDG, l'arco 53 registra come `source` l'identificatore `fhir_patient_generator_to_xml` per la relazione che coinvolge il nodo canonico `to_xml()`. Nel grafo del codice RDG, l'arco 53 registra come `target` l'identificatore `fhir_patient_generator_dict_to_xml_element` per la relazione che coinvolge il nodo canonico `to_xml()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 53 che coinvolge il nodo canonico `to_xml()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=19 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `to_xml()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `fhir_patient_generator.py`; l'orientamento registrato nel JSON va dall'ID `fhir_patient_generator` all'ID `fhir_patient_generator_to_xml`. La relazione tecnica esplicita è la seguente: `fhir_patient_generator.py` contiene `to_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 19 che coinvolge il nodo canonico `to_xml()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 19 che coinvolge il nodo canonico `to_xml()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 19 che coinvolge il nodo canonico `to_xml()` è `fhir_patient_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 19 che coinvolge il nodo canonico `to_xml()` è `L648`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 19 che coinvolge il nodo canonico `to_xml()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 19 che coinvolge il nodo canonico `to_xml()` è `ast`. Nel grafo del codice RDG, l'arco 19 registra come `source` l'identificatore `fhir_patient_generator` per la relazione che coinvolge il nodo canonico `to_xml()`. Nel grafo del codice RDG, l'arco 19 registra come `target` l'identificatore `fhir_patient_generator_to_xml` per la relazione che coinvolge il nodo canonico `to_xml()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 19 che coinvolge il nodo canonico `to_xml()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=54 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `to_xml()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `fhir_patient_generator_to_xml`. La relazione tecnica esplicita è la seguente: `main.py` importa `to_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 54 che coinvolge il nodo canonico `to_xml()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 54 che coinvolge il nodo canonico `to_xml()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 54 che coinvolge il nodo canonico `to_xml()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 54 che coinvolge il nodo canonico `to_xml()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 54 che coinvolge il nodo canonico `to_xml()` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 54 che coinvolge il nodo canonico `to_xml()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 54 che coinvolge il nodo canonico `to_xml()` è `ast`. Nel grafo del codice RDG, l'arco 54 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `to_xml()`. Nel grafo del codice RDG, l'arco 54 registra come `target` l'identificatore `fhir_patient_generator_to_xml` per la relazione che coinvolge il nodo canonico `to_xml()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 54 che coinvolge il nodo canonico `to_xml()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=55 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `to_xml()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_patients()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_patients` all'ID `fhir_patient_generator_to_xml`. La relazione tecnica esplicita è la seguente: `cmd_patients()` chiama `to_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 55 che coinvolge il nodo canonico `to_xml()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 55 che coinvolge il nodo canonico `to_xml()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 55 che coinvolge il nodo canonico `to_xml()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 55 che coinvolge il nodo canonico `to_xml()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 55 che coinvolge il nodo canonico `to_xml()` è `L267`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 55 che coinvolge il nodo canonico `to_xml()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 55 che coinvolge il nodo canonico `to_xml()` è `ast`. Nel grafo del codice RDG, l'arco 55 registra come `source` l'identificatore `main_cmd_patients` per la relazione che coinvolge il nodo canonico `to_xml()`. Nel grafo del codice RDG, l'arco 55 registra come `target` l'identificatore `fhir_patient_generator_to_xml` per la relazione che coinvolge il nodo canonico `to_xml()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 55 che coinvolge il nodo canonico `to_xml()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=56 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `to_xml()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `rve_55.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_55` all'ID `fhir_patient_generator_to_xml`. La relazione tecnica esplicita è la seguente: `rve_55.py` importa `to_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 56 che coinvolge il nodo canonico `to_xml()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 56 che coinvolge il nodo canonico `to_xml()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 56 che coinvolge il nodo canonico `to_xml()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 56 che coinvolge il nodo canonico `to_xml()` è `rve_transactions/rve_55.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 56 che coinvolge il nodo canonico `to_xml()` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 56 che coinvolge il nodo canonico `to_xml()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 56 che coinvolge il nodo canonico `to_xml()` è `ast`. Nel grafo del codice RDG, l'arco 56 registra come `source` l'identificatore `rve_transactions_rve_55` per la relazione che coinvolge il nodo canonico `to_xml()`. Nel grafo del codice RDG, l'arco 56 registra come `target` l'identificatore `fhir_patient_generator_to_xml` per la relazione che coinvolge il nodo canonico `to_xml()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 56 che coinvolge il nodo canonico `to_xml()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=57 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `to_xml()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_request() [rve_transactions_rve_55_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_55_build_request` all'ID `fhir_patient_generator_to_xml`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_55_build_request]` chiama `to_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 57 che coinvolge il nodo canonico `to_xml()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 57 che coinvolge il nodo canonico `to_xml()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 57 che coinvolge il nodo canonico `to_xml()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 57 che coinvolge il nodo canonico `to_xml()` è `rve_transactions/rve_55.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 57 che coinvolge il nodo canonico `to_xml()` è `L71`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 57 che coinvolge il nodo canonico `to_xml()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 57 che coinvolge il nodo canonico `to_xml()` è `ast`. Nel grafo del codice RDG, l'arco 57 registra come `source` l'identificatore `rve_transactions_rve_55_build_request` per la relazione che coinvolge il nodo canonico `to_xml()`. Nel grafo del codice RDG, l'arco 57 registra come `target` l'identificatore `fhir_patient_generator_to_xml` per la relazione che coinvolge il nodo canonico `to_xml()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 57 che coinvolge il nodo canonico `to_xml()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=21 id=Request-Dataset-Generator::fhir_patient_generator_to_xml -->
Nel grafo unificato, l'etichetta originale del nodo canonico `to_xml()` è `to_xml()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `to_xml()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `to_xml()` è `fhir_patient_generator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `to_xml()` è `L648`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `to_xml()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `to_xml()` alla comunità numerica `1`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `to_xml()` è `to_xml()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `to_xml()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `to_xml()` è `fhir_patient_generator_to_xml`. Nel grafo unificato, il nodo canonico `to_xml()` possiede l'identificatore esatto `Request-Dataset-Generator::fhir_patient_generator_to_xml`.
<!-- GRAPH_EDGE graph=unificata index=54 role=outgoing -->
Nel grafo unificato, il nodo canonico `to_xml()` è la sorgente `source` della relazione `imports` verso il nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::fhir_patient_generator_to_xml` all'ID `Request-Dataset-Generator::main`. La relazione tecnica esplicita è la seguente: `to_xml()` importa `main.py`. Nel grafo unificato, il tipo dichiarato dell'arco 54 che coinvolge il nodo canonico `to_xml()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 54 che coinvolge il nodo canonico `to_xml()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 54 che coinvolge il nodo canonico `to_xml()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 54 che coinvolge il nodo canonico `to_xml()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 54 che coinvolge il nodo canonico `to_xml()` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 54 che coinvolge il nodo canonico `to_xml()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 54 che coinvolge il nodo canonico `to_xml()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 54 che coinvolge il nodo canonico `to_xml()` è `1.0`. Nel grafo unificato, l'arco 54 registra come `source` l'identificatore `Request-Dataset-Generator::fhir_patient_generator_to_xml` per la relazione che coinvolge il nodo canonico `to_xml()`. Nel grafo unificato, l'arco 54 registra come `target` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `to_xml()`.
<!-- GRAPH_EDGE graph=unificata index=55 role=outgoing -->
Nel grafo unificato, il nodo canonico `to_xml()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_patients()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::fhir_patient_generator_to_xml` all'ID `Request-Dataset-Generator::main_cmd_patients`. La relazione tecnica esplicita è la seguente: `to_xml()` chiama `cmd_patients()`. Nel grafo unificato, il tipo dichiarato dell'arco 55 che coinvolge il nodo canonico `to_xml()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 55 che coinvolge il nodo canonico `to_xml()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 55 che coinvolge il nodo canonico `to_xml()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 55 che coinvolge il nodo canonico `to_xml()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 55 che coinvolge il nodo canonico `to_xml()` è `L267`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 55 che coinvolge il nodo canonico `to_xml()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 55 che coinvolge il nodo canonico `to_xml()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 55 che coinvolge il nodo canonico `to_xml()` è `1.0`. Nel grafo unificato, l'arco 55 registra come `source` l'identificatore `Request-Dataset-Generator::fhir_patient_generator_to_xml` per la relazione che coinvolge il nodo canonico `to_xml()`. Nel grafo unificato, l'arco 55 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_patients` per la relazione che coinvolge il nodo canonico `to_xml()`.
<!-- GRAPH_EDGE graph=unificata index=56 role=outgoing -->
Nel grafo unificato, il nodo canonico `to_xml()` è la sorgente `source` della relazione `imports` verso il nodo canonico `rve_55.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::fhir_patient_generator_to_xml` all'ID `Request-Dataset-Generator::rve_transactions_rve_55`. La relazione tecnica esplicita è la seguente: `to_xml()` importa `rve_55.py`. Nel grafo unificato, il tipo dichiarato dell'arco 56 che coinvolge il nodo canonico `to_xml()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 56 che coinvolge il nodo canonico `to_xml()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 56 che coinvolge il nodo canonico `to_xml()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 56 che coinvolge il nodo canonico `to_xml()` è `rve_transactions/rve_55.py`. Nel grafo unificato, la posizione sorgente dell'arco 56 che coinvolge il nodo canonico `to_xml()` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 56 che coinvolge il nodo canonico `to_xml()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 56 che coinvolge il nodo canonico `to_xml()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 56 che coinvolge il nodo canonico `to_xml()` è `1.0`. Nel grafo unificato, l'arco 56 registra come `source` l'identificatore `Request-Dataset-Generator::fhir_patient_generator_to_xml` per la relazione che coinvolge il nodo canonico `to_xml()`. Nel grafo unificato, l'arco 56 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_55` per la relazione che coinvolge il nodo canonico `to_xml()`.
<!-- GRAPH_EDGE graph=unificata index=57 role=outgoing -->
Nel grafo unificato, il nodo canonico `to_xml()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_request() [rve_transactions_rve_55_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::fhir_patient_generator_to_xml` all'ID `Request-Dataset-Generator::rve_transactions_rve_55_build_request`. La relazione tecnica esplicita è la seguente: `to_xml()` chiama `build_request() [rve_transactions_rve_55_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 57 che coinvolge il nodo canonico `to_xml()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 57 che coinvolge il nodo canonico `to_xml()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 57 che coinvolge il nodo canonico `to_xml()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 57 che coinvolge il nodo canonico `to_xml()` è `rve_transactions/rve_55.py`. Nel grafo unificato, la posizione sorgente dell'arco 57 che coinvolge il nodo canonico `to_xml()` è `L71`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 57 che coinvolge il nodo canonico `to_xml()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 57 che coinvolge il nodo canonico `to_xml()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 57 che coinvolge il nodo canonico `to_xml()` è `1.0`. Nel grafo unificato, l'arco 57 registra come `source` l'identificatore `Request-Dataset-Generator::fhir_patient_generator_to_xml` per la relazione che coinvolge il nodo canonico `to_xml()`. Nel grafo unificato, l'arco 57 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_55_build_request` per la relazione che coinvolge il nodo canonico `to_xml()`.
<!-- GRAPH_EDGE graph=unificata index=19 role=incoming -->
Nel grafo unificato, il nodo canonico `to_xml()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `fhir_patient_generator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::fhir_patient_generator` all'ID `Request-Dataset-Generator::fhir_patient_generator_to_xml`. La relazione tecnica esplicita è la seguente: `fhir_patient_generator.py` contiene `to_xml()`. Nel grafo unificato, il tipo dichiarato dell'arco 19 che coinvolge il nodo canonico `to_xml()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 19 che coinvolge il nodo canonico `to_xml()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 19 che coinvolge il nodo canonico `to_xml()` è `fhir_patient_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 19 che coinvolge il nodo canonico `to_xml()` è `L648`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 19 che coinvolge il nodo canonico `to_xml()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 19 che coinvolge il nodo canonico `to_xml()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 19 che coinvolge il nodo canonico `to_xml()` è `1.0`. Nel grafo unificato, l'arco 19 registra come `source` l'identificatore `Request-Dataset-Generator::fhir_patient_generator` per la relazione che coinvolge il nodo canonico `to_xml()`. Nel grafo unificato, l'arco 19 registra come `target` l'identificatore `Request-Dataset-Generator::fhir_patient_generator_to_xml` per la relazione che coinvolge il nodo canonico `to_xml()`.
<!-- GRAPH_EDGE graph=unificata index=53 role=incoming -->
Nel grafo unificato, il nodo canonico `to_xml()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `dict_to_xml_element()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::fhir_patient_generator_dict_to_xml_element` all'ID `Request-Dataset-Generator::fhir_patient_generator_to_xml`. La relazione tecnica esplicita è la seguente: `dict_to_xml_element()` chiama `to_xml()`. Nel grafo unificato, il tipo dichiarato dell'arco 53 che coinvolge il nodo canonico `to_xml()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 53 che coinvolge il nodo canonico `to_xml()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 53 che coinvolge il nodo canonico `to_xml()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 53 che coinvolge il nodo canonico `to_xml()` è `fhir_patient_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 53 che coinvolge il nodo canonico `to_xml()` è `L650`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 53 che coinvolge il nodo canonico `to_xml()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 53 che coinvolge il nodo canonico `to_xml()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 53 che coinvolge il nodo canonico `to_xml()` è `1.0`. Nel grafo unificato, l'arco 53 registra come `source` l'identificatore `Request-Dataset-Generator::fhir_patient_generator_dict_to_xml_element` per la relazione che coinvolge il nodo canonico `to_xml()`. Nel grafo unificato, l'arco 53 registra come `target` l'identificatore `Request-Dataset-Generator::fhir_patient_generator_to_xml` per la relazione che coinvolge il nodo canonico `to_xml()`.
