# Architettura e codice del framework RDG — parte 3

## Metadati

- Dominio: Architettura e codice del framework RDG.
- Data di generazione: 2026-07-31.
- Versione dei grafi sorgente: KB documentale `5f9434fc0f01de5d74dd3c6d72fffc217b7f8629`; KB codice RDG `06d5dd91cef2e12574b63a36d73b049a3588cdf8`; KB unificata `null`.
- Nodi coperti: 89 nodi canonici e 178 occorrenze fisiche; tipi canonici: code: 62, rationale: 27.
- Archi coperti: 376 occorrenze fisiche incidenti ai nodi del documento; tipi: calls: 108, conceptually_related_to: 1, contains: 64, implements: 1, imports: 38, imports_from: 40, method: 36, rationale_for: 54, references: 28, uses: 6.
- Iperarchi coperti: 0 occorrenze fisiche, replicate anche negli altri file coinvolti quando attraversano la segmentazione editoriale.
- Nota sulla direzione: tutti e tre i grafi dichiarano `directed=false`; la prosa conserva comunque l'orientamento dei campi JSON `source` e `target` senza trasformarlo in causalità.

## Glossario

- EXTRACTED — Valore della proprietà `confidence` che il grafo assegna a una relazione estratta.
- INFERRED — Valore della proprietà `confidence` che il grafo assegna a una relazione inferita; la prosa non promuove una relazione INFERRED a fatto estratto.
- RDG — Nome editoriale canonico del repository Request-Dataset-Generator.
- AST — Valore di provenienza `_origin=ast` registrato dal grafo.
- FHIR — Nome canonico dell'acronimo usato negli identificatori RDG.
- RVE — Nome canonico del prefisso usato per i flussi RDG.
- UI — Nome canonico dell'acronimo usato nei moduli di interfaccia.

### Mappa dei nomi canonici e delle varianti

La mappa seguente non fonde nodi solo perché semanticamente simili. Le occorrenze del grafo sorgente e del grafo unificato sono ricondotte allo stesso nome soltanto tramite `repo` e `local_id`; le etichette duplicate di nodi distinti sono disambiguate con l'identificatore locale.

- Il nome canonico `_build_mock_assertion()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_assertion`; le etichette originali conservate sono `_build_mock_assertion()`.
- Il nome canonico `_build_mock_searchset()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_54_build_mock_searchset`; le etichette originali conservate sono `_build_mock_searchset()`.
- Il nome canonico `_build_mock_soap_response()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_soap_response`; le etichette originali conservate sono `_build_mock_soap_response()`.
- Il nome canonico `_build_soap_request()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_1b_build_soap_request`; le etichette originali conservate sono `_build_soap_request()`.
- Il nome canonico `_common.py` identifica il nodo `Request-Dataset-Generator::rve_transactions_common`; le etichette originali conservate sono `_common.py`.
- Il nome canonico `_conditions_xml()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_1b_conditions_xml`; le etichette originali conservate sono `_conditions_xml()`.
- Il nome canonico `_format_timestamp()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_1b_format_timestamp`; le etichette originali conservate sono `_format_timestamp()`.
- Il nome canonico `_profile_name()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_1b_profile_name`; le etichette originali conservate sono `_profile_name()`.
- Il nome canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_replace_newlines_inside_tags`; le etichette originali conservate sono `_replace_newlines_inside_tags()`.
- Il nome canonico `_resolve_profile()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_1b_resolve_profile`; le etichette originali conservate sono `_resolve_profile()`.
- Il nome canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_276`; le etichette originali conservate sono `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.`.
- Il nome canonico `b64_decode()` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_b64_decode`; le etichette originali conservate sono `b64_decode()`.
- Il nome canonico `b64url_encode()` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_b64url_encode`; le etichette originali conservate sono `b64url_encode()`.
- Il nome canonico `Base64 decode → stringa UTF-8.` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_rationale_73`; le etichette originali conservate sono `Base64 decode → stringa UTF-8.`.
- Il nome canonico `Base64url senza padding — usato nei JWT.` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_rationale_119`; le etichette originali conservate sono `Base64url senza padding — usato nei JWT.`.
- Il nome canonico `build_mock_jwt()` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt`; le etichette originali conservate sono `build_mock_jwt()`.
- Il nome canonico `build_request() [rve_transactions_rve_121_build_request]` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_121_build_request`; le etichette originali conservate sono `build_request()`.
- Il nome canonico `build_request() [rve_transactions_rve_1b_build_request]` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`; le etichette originali conservate sono `build_request()`.
- Il nome canonico `build_request() [rve_transactions_rve_54_build_request]` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_54_build_request`; le etichette originali conservate sono `build_request()`.
- Il nome canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_rationale_52`; le etichette originali conservate sono `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.`.
- Il nome canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_310`; le etichette originali conservate sono `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob`.
- Il nome canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_121_rationale_39`; le etichette originali conservate sono `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:`.
- Il nome canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_54_rationale_35`; le etichette originali conservate sono `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:`.
- Il nome canonico `Costruisce un Bundle FHIR searchset di risposta mock.` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_54_rationale_124`; le etichette originali conservate sono `Costruisce un Bundle FHIR searchset di risposta mock.`.
- Il nome canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_rationale_139`; le etichette originali conservate sono `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).`.
- Il nome canonico `datetime [rve_transactions_common_py_datetime]` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_py_datetime`; le etichette originali conservate sono `datetime`.
- Il nome canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_rationale_174`; le etichette originali conservate sono `Estrae il Codice Fiscale dalla risorsa Patient FHIR.`.
- Il nome canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_rationale_182`; le etichette originali conservate sono `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.`.
- Il nome canonico `extract_patient_cf()` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf`; le etichette originali conservate sono `extract_patient_cf()`.
- Il nome canonico `extract_patient_mpi()` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi`; le etichette originali conservate sono `extract_patient_mpi()`.
- Il nome canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_237`; le etichette originali conservate sono `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).`.
- Il nome canonico `linearize_xml()` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_linearize_xml`; le etichette originali conservate sono `linearize_xml()`.
- Il nome canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_rationale_78`; le etichette originali conservate sono `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s`.
- Il nome canonico `msg_id_to_saml_id()` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_msg_id_to_saml_id`; le etichette originali conservate sono `msg_id_to_saml_id()`.
- Il nome canonico `new_uuid()` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_new_uuid`; le etichette originali conservate sono `new_uuid()`.
- Il nome canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_1`; le etichette originali conservate sono `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe`.
- Il nome canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_121_rationale_1`; le etichette originali conservate sono `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT`.
- Il nome canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_54_rationale_1`; le etichette originali conservate sono `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass`.
- Il nome canonico `rve_121.py` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_121`; le etichette originali conservate sono `rve_121.py`.
- Il nome canonico `rve_1b.py` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_1b`; le etichette originali conservate sono `rve_1b.py`.
- Il nome canonico `rve_54.py` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_54`; le etichette originali conservate sono `rve_54.py`.
- Il nome canonico `utc_iso()` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_utc_iso`; le etichette originali conservate sono `utc_iso()`.
- Il nome canonico `utc_iso_ms()` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms`; le etichette originali conservate sono `utc_iso_ms()`.
- Il nome canonico `utc_now()` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_utc_now`; le etichette originali conservate sono `utc_now()`.
- Il nome canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` identifica il nodo `Request-Dataset-Generator::rve_transactions_common_rationale_1`; le etichette originali conservate sono `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe`.
- Il nome canonico `.__init__() [ui_app_rdgapp_init]` identifica il nodo `Request-Dataset-Generator::ui_app_rdgapp_init`; le etichette originali conservate sono `.__init__()`.
- Il nome canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` identifica il nodo `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_init`; le etichette originali conservate sono `.__init__()`.
- Il nome canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` identifica il nodo `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build`; le etichette originali conservate sono `._build()`.
- Il nome canonico `._build_chart()` identifica il nodo `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_chart`; le etichette originali conservate sono `._build_chart()`.
- Il nome canonico `._build_metric_card()` identifica il nodo `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_metric_card`; le etichette originali conservate sono `._build_metric_card()`.
- Il nome canonico `._build_report()` identifica il nodo `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_report`; le etichette originali conservate sono `._build_report()`.
- Il nome canonico `._build_tabs()` identifica il nodo `Request-Dataset-Generator::ui_app_rdgapp_build_tabs`; le etichette originali conservate sono `._build_tabs()`.
- Il nome canonico `._build_titlebar()` identifica il nodo `Request-Dataset-Generator::ui_app_rdgapp_build_titlebar`; le etichette originali conservate sono `._build_titlebar()`.
- Il nome canonico `._chart_values()` identifica il nodo `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_chart_values`; le etichette originali conservate sono `._chart_values()`.
- Il nome canonico `._configure_report_tags()` identifica il nodo `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_configure_report_tags`; le etichette originali conservate sono `._configure_report_tags()`.
- Il nome canonico `._draw_chart()` identifica il nodo `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_draw_chart`; le etichette originali conservate sono `._draw_chart()`.
- Il nome canonico `._populate_metrics()` identifica il nodo `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_populate_metrics`; le etichette originali conservate sono `._populate_metrics()`.
- Il nome canonico `._read_metrics()` identifica il nodo `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics`; le etichette originali conservate sono `._read_metrics()`.
- Il nome canonico `._render_report()` identifica il nodo `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_render_report`; le etichette originali conservate sono `._render_report()`.
- Il nome canonico `._report_path()` identifica il nodo `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_report_path`; le etichette originali conservate sono `._report_path()`.
- Il nome canonico `._set_metric()` identifica il nodo `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_set_metric`; le etichette originali conservate sono `._set_metric()`.
- Il nome canonico `._style_segmented_tabs()` identifica il nodo `Request-Dataset-Generator::ui_app_rdgapp_style_segmented_tabs`; le etichette originali conservate sono `._style_segmented_tabs()`.
- Il nome canonico `.refresh_results()` identifica il nodo `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results`; le etichette originali conservate sono `.refresh_results()`.
- Il nome canonico `_defaults()` identifica il nodo `Request-Dataset-Generator::ui_ui_config_bridge_defaults`; le etichette originali conservate sono `_defaults()`.
- Il nome canonico `Any [ui_tabs_dashboard_tab_py_any]` identifica il nodo `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any`; le etichette originali conservate sono `Any`.
- Il nome canonico `Any [ui_ui_config_bridge_py_any]` identifica il nodo `Request-Dataset-Generator::ui_ui_config_bridge_py_any`; le etichette originali conservate sono `Any`.
- Il nome canonico `app.py` identifica il nodo `Request-Dataset-Generator::ui_app`; le etichette originali conservate sono `app.py`.
- Il nome canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` identifica il nodo `Request-Dataset-Generator::ui_ui_config_bridge_rationale_1`; le etichette originali conservate sono `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla`.
- Il nome canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` identifica il nodo `Request-Dataset-Generator::ui_ui_config_bridge_rationale_16`; le etichette originali conservate sono `Carica il file json. Se non esiste, o non e' valido, restituisce i default.`.
- Il nome canonico `config_tab.py` identifica il nodo `Request-Dataset-Generator::ui_tabs_config_tab`; le etichette originali conservate sono `config_tab.py`.
- Il nome canonico `Costanti visuali per la UI CustomTkinter di RDG.` identifica il nodo `Request-Dataset-Generator::ui_style_rationale_1`; le etichette originali conservate sono `Costanti visuali per la UI CustomTkinter di RDG.`.
- Il nome canonico `Dashboard risultati RDG.` identifica il nodo `Request-Dataset-Generator::ui_tabs_dashboard_tab_rationale_1`; le etichette originali conservate sono `Dashboard risultati RDG.`.
- Il nome canonico `dashboard_tab.py` identifica il nodo `Request-Dataset-Generator::ui_tabs_dashboard_tab`; le etichette originali conservate sono `dashboard_tab.py`.
- Il nome canonico `DashboardTab` identifica il nodo `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab`; le etichette originali conservate sono `DashboardTab`.
- Il nome canonico `Entry point CustomTkinter per Request-Dataset-Generator.` identifica il nodo `Request-Dataset-Generator::ui_app_rationale_1`; le etichette originali conservate sono `Entry point CustomTkinter per Request-Dataset-Generator.`.
- Il nome canonico `load_config() [ui_ui_config_bridge_load_config]` identifica il nodo `Request-Dataset-Generator::ui_ui_config_bridge_load_config`; le etichette originali conservate sono `load_config()`.
- Il nome canonico `main() [ui_app_main]` identifica il nodo `Request-Dataset-Generator::ui_app_main`; le etichette originali conservate sono `main()`.
- Il nome canonico `Metriche, grafico latenza e report finale.` identifica il nodo `Request-Dataset-Generator::ui_tabs_dashboard_tab_rationale_42`; le etichette originali conservate sono `Metriche, grafico latenza e report finale.`.
- Il nome canonico `Path [ui_tabs_dashboard_tab_py_path]` identifica il nodo `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_path`; le etichette originali conservate sono `Path`.
- Il nome canonico `Path [ui_ui_config_bridge_py_path]` identifica il nodo `Request-Dataset-Generator::ui_ui_config_bridge_py_path`; le etichette originali conservate sono `Path`.
- Il nome canonico `RDGApp` identifica il nodo `Request-Dataset-Generator::ui_app_rdgapp`; le etichette originali conservate sono `RDGApp`.
- Il nome canonico `run_tab.py` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab`; le etichette originali conservate sono `run_tab.py`.
- Il nome canonico `save_config()` identifica il nodo `Request-Dataset-Generator::ui_ui_config_bridge_save_config`; le etichette originali conservate sono `save_config()`.
- Il nome canonico `Shell principale con titlebar simulata e tabview.` identifica il nodo `Request-Dataset-Generator::ui_app_rationale_32`; le etichette originali conservate sono `Shell principale con titlebar simulata e tabview.`.
- Il nome canonico `Sovrascrive il file json con i valori dal form UI.` identifica il nodo `Request-Dataset-Generator::ui_ui_config_bridge_rationale_35`; le etichette originali conservate sono `Sovrascrive il file json con i valori dal form UI.`.
- Il nome canonico `style.py` identifica il nodo `Request-Dataset-Generator::ui_style`; le etichette originali conservate sono `style.py`.
- Il nome canonico `Tab di configurazione esperimento RDG.` identifica il nodo `Request-Dataset-Generator::ui_tabs_config_tab_rationale_1`; le etichette originali conservate sono `Tab di configurazione esperimento RDG.`.
- Il nome canonico `Tab di esecuzione pipeline RDG.` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_rationale_1`; le etichette originali conservate sono `Tab di esecuzione pipeline RDG.`.
- Il nome canonico `ui_config_bridge.py` identifica il nodo `Request-Dataset-Generator::ui_ui_config_bridge`; le etichette originali conservate sono `ui_config_bridge.py`.

## Cluster Graphify 2 — Shared Encoding Utilities

Il cluster editoriale Graphify 2 del dominio Architettura e codice del framework RDG contiene 45 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### _build_mock_assertion()

<!-- GRAPH_NODE graph=codice index=222 id=rve_transactions_rve_1b_build_mock_assertion -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_build_mock_assertion()` è `_build_mock_assertion()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_build_mock_assertion()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_build_mock_assertion()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_build_mock_assertion()` è `L226`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_build_mock_assertion()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_build_mock_assertion()` possiede l'identificatore esatto `rve_transactions_rve_1b_build_mock_assertion`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_build_mock_assertion()` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_build_mock_assertion()` è `_build_mock_assertion()`.
Nel grafo del codice RDG, il nodo canonico `_build_mock_assertion()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=462 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_mock_assertion()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_rve_1b_build_mock_assertion`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_build_mock_assertion()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 462 che coinvolge il nodo canonico `_build_mock_assertion()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 462 che coinvolge il nodo canonico `_build_mock_assertion()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 462 che coinvolge il nodo canonico `_build_mock_assertion()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 462 che coinvolge il nodo canonico `_build_mock_assertion()` è `L226`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 462 che coinvolge il nodo canonico `_build_mock_assertion()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 462 che coinvolge il nodo canonico `_build_mock_assertion()` è `ast`. Nel grafo del codice RDG, l'arco 462 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `_build_mock_assertion()`. Nel grafo del codice RDG, l'arco 462 registra come `target` l'identificatore `rve_transactions_rve_1b_build_mock_assertion` per la relazione che coinvolge il nodo canonico `_build_mock_assertion()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 462 che coinvolge il nodo canonico `_build_mock_assertion()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=477 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_mock_assertion()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_rve_1b_build_mock_assertion`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `_build_mock_assertion()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 477 che coinvolge il nodo canonico `_build_mock_assertion()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 477 che coinvolge il nodo canonico `_build_mock_assertion()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 477 che coinvolge il nodo canonico `_build_mock_assertion()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 477 che coinvolge il nodo canonico `_build_mock_assertion()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 477 che coinvolge il nodo canonico `_build_mock_assertion()` è `L410`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 477 che coinvolge il nodo canonico `_build_mock_assertion()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 477 che coinvolge il nodo canonico `_build_mock_assertion()` è `ast`. Nel grafo del codice RDG, l'arco 477 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `_build_mock_assertion()`. Nel grafo del codice RDG, l'arco 477 registra come `target` l'identificatore `rve_transactions_rve_1b_build_mock_assertion` per la relazione che coinvolge il nodo canonico `_build_mock_assertion()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 477 che coinvolge il nodo canonico `_build_mock_assertion()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=478 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_mock_assertion()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_rationale_237` all'ID `rve_transactions_rve_1b_build_mock_assertion`. La relazione tecnica esplicita è la seguente: `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` costituisce una motivazione per `_build_mock_assertion()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 478 che coinvolge il nodo canonico `_build_mock_assertion()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 478 che coinvolge il nodo canonico `_build_mock_assertion()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 478 che coinvolge il nodo canonico `_build_mock_assertion()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 478 che coinvolge il nodo canonico `_build_mock_assertion()` è `L237`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 478 che coinvolge il nodo canonico `_build_mock_assertion()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 478 che coinvolge il nodo canonico `_build_mock_assertion()` è `ast`. Nel grafo del codice RDG, l'arco 478 registra come `source` l'identificatore `rve_transactions_rve_1b_rationale_237` per la relazione che coinvolge il nodo canonico `_build_mock_assertion()`. Nel grafo del codice RDG, l'arco 478 registra come `target` l'identificatore `rve_transactions_rve_1b_build_mock_assertion` per la relazione che coinvolge il nodo canonico `_build_mock_assertion()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 478 che coinvolge il nodo canonico `_build_mock_assertion()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=222 id=Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_assertion -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_build_mock_assertion()` è `_build_mock_assertion()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_build_mock_assertion()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_build_mock_assertion()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_build_mock_assertion()` è `L226`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_build_mock_assertion()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_build_mock_assertion()` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_build_mock_assertion()` è `_build_mock_assertion()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_build_mock_assertion()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_build_mock_assertion()` è `rve_transactions_rve_1b_build_mock_assertion`. Nel grafo unificato, il nodo canonico `_build_mock_assertion()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_assertion`.
<!-- GRAPH_EDGE graph=unificata index=477 role=outgoing -->
Nel grafo unificato, il nodo canonico `_build_mock_assertion()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_assertion` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `_build_mock_assertion()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 477 che coinvolge il nodo canonico `_build_mock_assertion()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 477 che coinvolge il nodo canonico `_build_mock_assertion()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 477 che coinvolge il nodo canonico `_build_mock_assertion()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 477 che coinvolge il nodo canonico `_build_mock_assertion()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 477 che coinvolge il nodo canonico `_build_mock_assertion()` è `L410`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 477 che coinvolge il nodo canonico `_build_mock_assertion()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 477 che coinvolge il nodo canonico `_build_mock_assertion()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 477 che coinvolge il nodo canonico `_build_mock_assertion()` è `1.0`. Nel grafo unificato, l'arco 477 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_assertion` per la relazione che coinvolge il nodo canonico `_build_mock_assertion()`. Nel grafo unificato, l'arco 477 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `_build_mock_assertion()`.
<!-- GRAPH_EDGE graph=unificata index=478 role=outgoing -->
Nel grafo unificato, il nodo canonico `_build_mock_assertion()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_assertion` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_237`. La relazione tecnica esplicita è la seguente: `_build_mock_assertion()` costituisce una motivazione per `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).`. Nel grafo unificato, il tipo dichiarato dell'arco 478 che coinvolge il nodo canonico `_build_mock_assertion()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 478 che coinvolge il nodo canonico `_build_mock_assertion()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 478 che coinvolge il nodo canonico `_build_mock_assertion()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 478 che coinvolge il nodo canonico `_build_mock_assertion()` è `L237`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 478 che coinvolge il nodo canonico `_build_mock_assertion()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 478 che coinvolge il nodo canonico `_build_mock_assertion()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 478 che coinvolge il nodo canonico `_build_mock_assertion()` è `1.0`. Nel grafo unificato, l'arco 478 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_assertion` per la relazione che coinvolge il nodo canonico `_build_mock_assertion()`. Nel grafo unificato, l'arco 478 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_237` per la relazione che coinvolge il nodo canonico `_build_mock_assertion()`.
<!-- GRAPH_EDGE graph=unificata index=462 role=incoming -->
Nel grafo unificato, il nodo canonico `_build_mock_assertion()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_assertion`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_build_mock_assertion()`. Nel grafo unificato, il tipo dichiarato dell'arco 462 che coinvolge il nodo canonico `_build_mock_assertion()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 462 che coinvolge il nodo canonico `_build_mock_assertion()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 462 che coinvolge il nodo canonico `_build_mock_assertion()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 462 che coinvolge il nodo canonico `_build_mock_assertion()` è `L226`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 462 che coinvolge il nodo canonico `_build_mock_assertion()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 462 che coinvolge il nodo canonico `_build_mock_assertion()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 462 che coinvolge il nodo canonico `_build_mock_assertion()` è `1.0`. Nel grafo unificato, l'arco 462 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `_build_mock_assertion()`. Nel grafo unificato, l'arco 462 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_assertion` per la relazione che coinvolge il nodo canonico `_build_mock_assertion()`.

### _build_mock_searchset()

<!-- GRAPH_NODE graph=codice index=231 id=rve_transactions_rve_54_build_mock_searchset -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_build_mock_searchset()` è `_build_mock_searchset()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_build_mock_searchset()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_build_mock_searchset()` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_build_mock_searchset()` è `L123`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_build_mock_searchset()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_build_mock_searchset()` possiede l'identificatore esatto `rve_transactions_rve_54_build_mock_searchset`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_build_mock_searchset()` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_build_mock_searchset()` è `_build_mock_searchset()`.
Nel grafo del codice RDG, il nodo canonico `_build_mock_searchset()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=482 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_mock_searchset()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_54.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54` all'ID `rve_transactions_rve_54_build_mock_searchset`. La relazione tecnica esplicita è la seguente: `rve_54.py` contiene `_build_mock_searchset()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 482 che coinvolge il nodo canonico `_build_mock_searchset()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 482 che coinvolge il nodo canonico `_build_mock_searchset()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 482 che coinvolge il nodo canonico `_build_mock_searchset()` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 482 che coinvolge il nodo canonico `_build_mock_searchset()` è `L123`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 482 che coinvolge il nodo canonico `_build_mock_searchset()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 482 che coinvolge il nodo canonico `_build_mock_searchset()` è `ast`. Nel grafo del codice RDG, l'arco 482 registra come `source` l'identificatore `rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `_build_mock_searchset()`. Nel grafo del codice RDG, l'arco 482 registra come `target` l'identificatore `rve_transactions_rve_54_build_mock_searchset` per la relazione che coinvolge il nodo canonico `_build_mock_searchset()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 482 che coinvolge il nodo canonico `_build_mock_searchset()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=485 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_mock_searchset()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_request() [rve_transactions_rve_54_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54_build_request` all'ID `rve_transactions_rve_54_build_mock_searchset`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_54_build_request]` chiama `_build_mock_searchset()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 485 che coinvolge il nodo canonico `_build_mock_searchset()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 485 che coinvolge il nodo canonico `_build_mock_searchset()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 485 che coinvolge il nodo canonico `_build_mock_searchset()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 485 che coinvolge il nodo canonico `_build_mock_searchset()` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 485 che coinvolge il nodo canonico `_build_mock_searchset()` è `L112`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 485 che coinvolge il nodo canonico `_build_mock_searchset()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 485 che coinvolge il nodo canonico `_build_mock_searchset()` è `ast`. Nel grafo del codice RDG, l'arco 485 registra come `source` l'identificatore `rve_transactions_rve_54_build_request` per la relazione che coinvolge il nodo canonico `_build_mock_searchset()`. Nel grafo del codice RDG, l'arco 485 registra come `target` l'identificatore `rve_transactions_rve_54_build_mock_searchset` per la relazione che coinvolge il nodo canonico `_build_mock_searchset()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 485 che coinvolge il nodo canonico `_build_mock_searchset()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=487 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_mock_searchset()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54_rationale_124` all'ID `rve_transactions_rve_54_build_mock_searchset`. La relazione tecnica esplicita è la seguente: `Costruisce un Bundle FHIR searchset di risposta mock.` costituisce una motivazione per `_build_mock_searchset()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 487 che coinvolge il nodo canonico `_build_mock_searchset()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 487 che coinvolge il nodo canonico `_build_mock_searchset()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 487 che coinvolge il nodo canonico `_build_mock_searchset()` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 487 che coinvolge il nodo canonico `_build_mock_searchset()` è `L124`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 487 che coinvolge il nodo canonico `_build_mock_searchset()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 487 che coinvolge il nodo canonico `_build_mock_searchset()` è `ast`. Nel grafo del codice RDG, l'arco 487 registra come `source` l'identificatore `rve_transactions_rve_54_rationale_124` per la relazione che coinvolge il nodo canonico `_build_mock_searchset()`. Nel grafo del codice RDG, l'arco 487 registra come `target` l'identificatore `rve_transactions_rve_54_build_mock_searchset` per la relazione che coinvolge il nodo canonico `_build_mock_searchset()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 487 che coinvolge il nodo canonico `_build_mock_searchset()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=231 id=Request-Dataset-Generator::rve_transactions_rve_54_build_mock_searchset -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_build_mock_searchset()` è `_build_mock_searchset()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_build_mock_searchset()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_build_mock_searchset()` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_build_mock_searchset()` è `L123`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_build_mock_searchset()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_build_mock_searchset()` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_build_mock_searchset()` è `_build_mock_searchset()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_build_mock_searchset()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_build_mock_searchset()` è `rve_transactions_rve_54_build_mock_searchset`. Nel grafo unificato, il nodo canonico `_build_mock_searchset()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_54_build_mock_searchset`.
<!-- GRAPH_EDGE graph=unificata index=487 role=outgoing -->
Nel grafo unificato, il nodo canonico `_build_mock_searchset()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_54_build_mock_searchset` all'ID `Request-Dataset-Generator::rve_transactions_rve_54_rationale_124`. La relazione tecnica esplicita è la seguente: `_build_mock_searchset()` costituisce una motivazione per `Costruisce un Bundle FHIR searchset di risposta mock.`. Nel grafo unificato, il tipo dichiarato dell'arco 487 che coinvolge il nodo canonico `_build_mock_searchset()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 487 che coinvolge il nodo canonico `_build_mock_searchset()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 487 che coinvolge il nodo canonico `_build_mock_searchset()` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 487 che coinvolge il nodo canonico `_build_mock_searchset()` è `L124`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 487 che coinvolge il nodo canonico `_build_mock_searchset()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 487 che coinvolge il nodo canonico `_build_mock_searchset()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 487 che coinvolge il nodo canonico `_build_mock_searchset()` è `1.0`. Nel grafo unificato, l'arco 487 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_build_mock_searchset` per la relazione che coinvolge il nodo canonico `_build_mock_searchset()`. Nel grafo unificato, l'arco 487 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_rationale_124` per la relazione che coinvolge il nodo canonico `_build_mock_searchset()`.
<!-- GRAPH_EDGE graph=unificata index=482 role=incoming -->
Nel grafo unificato, il nodo canonico `_build_mock_searchset()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_54.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_54` all'ID `Request-Dataset-Generator::rve_transactions_rve_54_build_mock_searchset`. La relazione tecnica esplicita è la seguente: `rve_54.py` contiene `_build_mock_searchset()`. Nel grafo unificato, il tipo dichiarato dell'arco 482 che coinvolge il nodo canonico `_build_mock_searchset()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 482 che coinvolge il nodo canonico `_build_mock_searchset()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 482 che coinvolge il nodo canonico `_build_mock_searchset()` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 482 che coinvolge il nodo canonico `_build_mock_searchset()` è `L123`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 482 che coinvolge il nodo canonico `_build_mock_searchset()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 482 che coinvolge il nodo canonico `_build_mock_searchset()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 482 che coinvolge il nodo canonico `_build_mock_searchset()` è `1.0`. Nel grafo unificato, l'arco 482 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `_build_mock_searchset()`. Nel grafo unificato, l'arco 482 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_build_mock_searchset` per la relazione che coinvolge il nodo canonico `_build_mock_searchset()`.
<!-- GRAPH_EDGE graph=unificata index=485 role=incoming -->
Nel grafo unificato, il nodo canonico `_build_mock_searchset()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_request() [rve_transactions_rve_54_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_54_build_request` all'ID `Request-Dataset-Generator::rve_transactions_rve_54_build_mock_searchset`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_54_build_request]` chiama `_build_mock_searchset()`. Nel grafo unificato, il tipo dichiarato dell'arco 485 che coinvolge il nodo canonico `_build_mock_searchset()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 485 che coinvolge il nodo canonico `_build_mock_searchset()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 485 che coinvolge il nodo canonico `_build_mock_searchset()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 485 che coinvolge il nodo canonico `_build_mock_searchset()` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 485 che coinvolge il nodo canonico `_build_mock_searchset()` è `L112`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 485 che coinvolge il nodo canonico `_build_mock_searchset()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 485 che coinvolge il nodo canonico `_build_mock_searchset()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 485 che coinvolge il nodo canonico `_build_mock_searchset()` è `1.0`. Nel grafo unificato, l'arco 485 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_build_request` per la relazione che coinvolge il nodo canonico `_build_mock_searchset()`. Nel grafo unificato, l'arco 485 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_build_mock_searchset` per la relazione che coinvolge il nodo canonico `_build_mock_searchset()`.

### _build_mock_soap_response()

<!-- GRAPH_NODE graph=codice index=223 id=rve_transactions_rve_1b_build_mock_soap_response -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_build_mock_soap_response()` è `_build_mock_soap_response()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_build_mock_soap_response()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_build_mock_soap_response()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_build_mock_soap_response()` è `L275`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_build_mock_soap_response()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_build_mock_soap_response()` possiede l'identificatore esatto `rve_transactions_rve_1b_build_mock_soap_response`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_build_mock_soap_response()` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_build_mock_soap_response()` è `_build_mock_soap_response()`.
Nel grafo del codice RDG, il nodo canonico `_build_mock_soap_response()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=463 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_mock_soap_response()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_rve_1b_build_mock_soap_response`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_build_mock_soap_response()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 463 che coinvolge il nodo canonico `_build_mock_soap_response()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 463 che coinvolge il nodo canonico `_build_mock_soap_response()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 463 che coinvolge il nodo canonico `_build_mock_soap_response()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 463 che coinvolge il nodo canonico `_build_mock_soap_response()` è `L275`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 463 che coinvolge il nodo canonico `_build_mock_soap_response()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 463 che coinvolge il nodo canonico `_build_mock_soap_response()` è `ast`. Nel grafo del codice RDG, l'arco 463 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `_build_mock_soap_response()`. Nel grafo del codice RDG, l'arco 463 registra come `target` l'identificatore `rve_transactions_rve_1b_build_mock_soap_response` per la relazione che coinvolge il nodo canonico `_build_mock_soap_response()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 463 che coinvolge il nodo canonico `_build_mock_soap_response()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=479 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_mock_soap_response()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_rve_1b_build_mock_soap_response`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `_build_mock_soap_response()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 479 che coinvolge il nodo canonico `_build_mock_soap_response()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 479 che coinvolge il nodo canonico `_build_mock_soap_response()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 479 che coinvolge il nodo canonico `_build_mock_soap_response()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 479 che coinvolge il nodo canonico `_build_mock_soap_response()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 479 che coinvolge il nodo canonico `_build_mock_soap_response()` è `L422`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 479 che coinvolge il nodo canonico `_build_mock_soap_response()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 479 che coinvolge il nodo canonico `_build_mock_soap_response()` è `ast`. Nel grafo del codice RDG, l'arco 479 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `_build_mock_soap_response()`. Nel grafo del codice RDG, l'arco 479 registra come `target` l'identificatore `rve_transactions_rve_1b_build_mock_soap_response` per la relazione che coinvolge il nodo canonico `_build_mock_soap_response()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 479 che coinvolge il nodo canonico `_build_mock_soap_response()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=480 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_mock_soap_response()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_rationale_276` all'ID `rve_transactions_rve_1b_build_mock_soap_response`. La relazione tecnica esplicita è la seguente: `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` costituisce una motivazione per `_build_mock_soap_response()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 480 che coinvolge il nodo canonico `_build_mock_soap_response()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 480 che coinvolge il nodo canonico `_build_mock_soap_response()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 480 che coinvolge il nodo canonico `_build_mock_soap_response()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 480 che coinvolge il nodo canonico `_build_mock_soap_response()` è `L276`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 480 che coinvolge il nodo canonico `_build_mock_soap_response()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 480 che coinvolge il nodo canonico `_build_mock_soap_response()` è `ast`. Nel grafo del codice RDG, l'arco 480 registra come `source` l'identificatore `rve_transactions_rve_1b_rationale_276` per la relazione che coinvolge il nodo canonico `_build_mock_soap_response()`. Nel grafo del codice RDG, l'arco 480 registra come `target` l'identificatore `rve_transactions_rve_1b_build_mock_soap_response` per la relazione che coinvolge il nodo canonico `_build_mock_soap_response()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 480 che coinvolge il nodo canonico `_build_mock_soap_response()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=223 id=Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_soap_response -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_build_mock_soap_response()` è `_build_mock_soap_response()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_build_mock_soap_response()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_build_mock_soap_response()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_build_mock_soap_response()` è `L275`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_build_mock_soap_response()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_build_mock_soap_response()` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_build_mock_soap_response()` è `_build_mock_soap_response()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_build_mock_soap_response()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_build_mock_soap_response()` è `rve_transactions_rve_1b_build_mock_soap_response`. Nel grafo unificato, il nodo canonico `_build_mock_soap_response()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_soap_response`.
<!-- GRAPH_EDGE graph=unificata index=479 role=outgoing -->
Nel grafo unificato, il nodo canonico `_build_mock_soap_response()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_soap_response` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `_build_mock_soap_response()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 479 che coinvolge il nodo canonico `_build_mock_soap_response()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 479 che coinvolge il nodo canonico `_build_mock_soap_response()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 479 che coinvolge il nodo canonico `_build_mock_soap_response()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 479 che coinvolge il nodo canonico `_build_mock_soap_response()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 479 che coinvolge il nodo canonico `_build_mock_soap_response()` è `L422`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 479 che coinvolge il nodo canonico `_build_mock_soap_response()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 479 che coinvolge il nodo canonico `_build_mock_soap_response()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 479 che coinvolge il nodo canonico `_build_mock_soap_response()` è `1.0`. Nel grafo unificato, l'arco 479 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_soap_response` per la relazione che coinvolge il nodo canonico `_build_mock_soap_response()`. Nel grafo unificato, l'arco 479 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `_build_mock_soap_response()`.
<!-- GRAPH_EDGE graph=unificata index=480 role=outgoing -->
Nel grafo unificato, il nodo canonico `_build_mock_soap_response()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_soap_response` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_276`. La relazione tecnica esplicita è la seguente: `_build_mock_soap_response()` costituisce una motivazione per `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.`. Nel grafo unificato, il tipo dichiarato dell'arco 480 che coinvolge il nodo canonico `_build_mock_soap_response()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 480 che coinvolge il nodo canonico `_build_mock_soap_response()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 480 che coinvolge il nodo canonico `_build_mock_soap_response()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 480 che coinvolge il nodo canonico `_build_mock_soap_response()` è `L276`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 480 che coinvolge il nodo canonico `_build_mock_soap_response()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 480 che coinvolge il nodo canonico `_build_mock_soap_response()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 480 che coinvolge il nodo canonico `_build_mock_soap_response()` è `1.0`. Nel grafo unificato, l'arco 480 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_soap_response` per la relazione che coinvolge il nodo canonico `_build_mock_soap_response()`. Nel grafo unificato, l'arco 480 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_276` per la relazione che coinvolge il nodo canonico `_build_mock_soap_response()`.
<!-- GRAPH_EDGE graph=unificata index=463 role=incoming -->
Nel grafo unificato, il nodo canonico `_build_mock_soap_response()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_soap_response`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_build_mock_soap_response()`. Nel grafo unificato, il tipo dichiarato dell'arco 463 che coinvolge il nodo canonico `_build_mock_soap_response()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 463 che coinvolge il nodo canonico `_build_mock_soap_response()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 463 che coinvolge il nodo canonico `_build_mock_soap_response()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 463 che coinvolge il nodo canonico `_build_mock_soap_response()` è `L275`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 463 che coinvolge il nodo canonico `_build_mock_soap_response()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 463 che coinvolge il nodo canonico `_build_mock_soap_response()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 463 che coinvolge il nodo canonico `_build_mock_soap_response()` è `1.0`. Nel grafo unificato, l'arco 463 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `_build_mock_soap_response()`. Nel grafo unificato, l'arco 463 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_soap_response` per la relazione che coinvolge il nodo canonico `_build_mock_soap_response()`.

### _build_soap_request()

<!-- GRAPH_NODE graph=codice index=221 id=rve_transactions_rve_1b_build_soap_request -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_build_soap_request()` è `_build_soap_request()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_build_soap_request()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_build_soap_request()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_build_soap_request()` è `L142`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_build_soap_request()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_build_soap_request()` possiede l'identificatore esatto `rve_transactions_rve_1b_build_soap_request`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_build_soap_request()` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_build_soap_request()` è `_build_soap_request()`.
<!-- GRAPH_EDGE graph=codice index=475 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_build_soap_request()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_conditions_xml()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_soap_request` all'ID `rve_transactions_rve_1b_conditions_xml`. La relazione tecnica esplicita è la seguente: `_build_soap_request()` chiama `_conditions_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 475 che coinvolge il nodo canonico `_build_soap_request()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 475 che coinvolge il nodo canonico `_build_soap_request()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 475 che coinvolge il nodo canonico `_build_soap_request()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 475 che coinvolge il nodo canonico `_build_soap_request()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 475 che coinvolge il nodo canonico `_build_soap_request()` è `L157`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 475 che coinvolge il nodo canonico `_build_soap_request()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 475 che coinvolge il nodo canonico `_build_soap_request()` è `ast`. Nel grafo del codice RDG, l'arco 475 registra come `source` l'identificatore `rve_transactions_rve_1b_build_soap_request` per la relazione che coinvolge il nodo canonico `_build_soap_request()`. Nel grafo del codice RDG, l'arco 475 registra come `target` l'identificatore `rve_transactions_rve_1b_conditions_xml` per la relazione che coinvolge il nodo canonico `_build_soap_request()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 475 che coinvolge il nodo canonico `_build_soap_request()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=465 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_soap_request()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_rve_1b_build_soap_request`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_build_soap_request()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 465 che coinvolge il nodo canonico `_build_soap_request()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 465 che coinvolge il nodo canonico `_build_soap_request()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 465 che coinvolge il nodo canonico `_build_soap_request()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 465 che coinvolge il nodo canonico `_build_soap_request()` è `L142`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 465 che coinvolge il nodo canonico `_build_soap_request()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 465 che coinvolge il nodo canonico `_build_soap_request()` è `ast`. Nel grafo del codice RDG, l'arco 465 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `_build_soap_request()`. Nel grafo del codice RDG, l'arco 465 registra come `target` l'identificatore `rve_transactions_rve_1b_build_soap_request` per la relazione che coinvolge il nodo canonico `_build_soap_request()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 465 che coinvolge il nodo canonico `_build_soap_request()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=476 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_soap_request()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_rve_1b_build_soap_request`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `_build_soap_request()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 476 che coinvolge il nodo canonico `_build_soap_request()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 476 che coinvolge il nodo canonico `_build_soap_request()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 476 che coinvolge il nodo canonico `_build_soap_request()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 476 che coinvolge il nodo canonico `_build_soap_request()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 476 che coinvolge il nodo canonico `_build_soap_request()` è `L345`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 476 che coinvolge il nodo canonico `_build_soap_request()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 476 che coinvolge il nodo canonico `_build_soap_request()` è `ast`. Nel grafo del codice RDG, l'arco 476 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `_build_soap_request()`. Nel grafo del codice RDG, l'arco 476 registra come `target` l'identificatore `rve_transactions_rve_1b_build_soap_request` per la relazione che coinvolge il nodo canonico `_build_soap_request()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 476 che coinvolge il nodo canonico `_build_soap_request()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=221 id=Request-Dataset-Generator::rve_transactions_rve_1b_build_soap_request -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_build_soap_request()` è `_build_soap_request()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_build_soap_request()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_build_soap_request()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_build_soap_request()` è `L142`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_build_soap_request()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_build_soap_request()` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_build_soap_request()` è `_build_soap_request()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_build_soap_request()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_build_soap_request()` è `rve_transactions_rve_1b_build_soap_request`. Nel grafo unificato, il nodo canonico `_build_soap_request()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_1b_build_soap_request`.
<!-- GRAPH_EDGE graph=unificata index=476 role=outgoing -->
Nel grafo unificato, il nodo canonico `_build_soap_request()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_soap_request` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `_build_soap_request()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 476 che coinvolge il nodo canonico `_build_soap_request()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 476 che coinvolge il nodo canonico `_build_soap_request()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 476 che coinvolge il nodo canonico `_build_soap_request()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 476 che coinvolge il nodo canonico `_build_soap_request()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 476 che coinvolge il nodo canonico `_build_soap_request()` è `L345`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 476 che coinvolge il nodo canonico `_build_soap_request()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 476 che coinvolge il nodo canonico `_build_soap_request()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 476 che coinvolge il nodo canonico `_build_soap_request()` è `1.0`. Nel grafo unificato, l'arco 476 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_soap_request` per la relazione che coinvolge il nodo canonico `_build_soap_request()`. Nel grafo unificato, l'arco 476 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `_build_soap_request()`.
<!-- GRAPH_EDGE graph=unificata index=465 role=incoming -->
Nel grafo unificato, il nodo canonico `_build_soap_request()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_soap_request`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_build_soap_request()`. Nel grafo unificato, il tipo dichiarato dell'arco 465 che coinvolge il nodo canonico `_build_soap_request()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 465 che coinvolge il nodo canonico `_build_soap_request()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 465 che coinvolge il nodo canonico `_build_soap_request()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 465 che coinvolge il nodo canonico `_build_soap_request()` è `L142`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 465 che coinvolge il nodo canonico `_build_soap_request()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 465 che coinvolge il nodo canonico `_build_soap_request()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 465 che coinvolge il nodo canonico `_build_soap_request()` è `1.0`. Nel grafo unificato, l'arco 465 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `_build_soap_request()`. Nel grafo unificato, l'arco 465 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_soap_request` per la relazione che coinvolge il nodo canonico `_build_soap_request()`.
<!-- GRAPH_EDGE graph=unificata index=475 role=incoming -->
Nel grafo unificato, il nodo canonico `_build_soap_request()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_conditions_xml()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b_conditions_xml` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_soap_request`. La relazione tecnica esplicita è la seguente: `_conditions_xml()` chiama `_build_soap_request()`. Nel grafo unificato, il tipo dichiarato dell'arco 475 che coinvolge il nodo canonico `_build_soap_request()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 475 che coinvolge il nodo canonico `_build_soap_request()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 475 che coinvolge il nodo canonico `_build_soap_request()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 475 che coinvolge il nodo canonico `_build_soap_request()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 475 che coinvolge il nodo canonico `_build_soap_request()` è `L157`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 475 che coinvolge il nodo canonico `_build_soap_request()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 475 che coinvolge il nodo canonico `_build_soap_request()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 475 che coinvolge il nodo canonico `_build_soap_request()` è `1.0`. Nel grafo unificato, l'arco 475 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_conditions_xml` per la relazione che coinvolge il nodo canonico `_build_soap_request()`. Nel grafo unificato, l'arco 475 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_soap_request` per la relazione che coinvolge il nodo canonico `_build_soap_request()`.

### _common.py

<!-- GRAPH_NODE graph=codice index=161 id=rve_transactions_common -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_common.py` è `_common.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_common.py` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_common.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_common.py` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_common.py` possiede l'identificatore esatto `rve_transactions_common`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_common.py` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_common.py` è `_common.py`.
<!-- GRAPH_EDGE graph=codice index=339 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `b64_decode()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_b64_decode`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `b64_decode()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 339 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 339 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 339 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 339 che coinvolge il nodo canonico `_common.py` è `L72`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 339 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 339 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo del codice RDG, l'arco 339 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, l'arco 339 registra come `target` l'identificatore `rve_transactions_common_b64_decode` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 339 che coinvolge il nodo canonico `_common.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=340 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `b64_encode()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_b64_encode`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `b64_encode()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 340 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 340 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 340 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 340 che coinvolge il nodo canonico `_common.py` è `L65`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 340 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 340 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo del codice RDG, l'arco 340 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, l'arco 340 registra come `target` l'identificatore `rve_transactions_common_b64_encode` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 340 che coinvolge il nodo canonico `_common.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=341 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `b64url_encode()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_b64url_encode`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `b64url_encode()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 341 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 341 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 341 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 341 che coinvolge il nodo canonico `_common.py` è `L118`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 341 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 341 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo del codice RDG, l'arco 341 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, l'arco 341 registra come `target` l'identificatore `rve_transactions_common_b64url_encode` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 341 che coinvolge il nodo canonico `_common.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=342 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `build_mock_jwt()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_build_mock_jwt`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `build_mock_jwt()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 342 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 342 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 342 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 342 che coinvolge il nodo canonico `_common.py` è `L131`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 342 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 342 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo del codice RDG, l'arco 342 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, l'arco 342 registra come `target` l'identificatore `rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 342 che coinvolge il nodo canonico `_common.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=343 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `extract_patient_cf()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_extract_patient_cf`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `extract_patient_cf()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 343 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 343 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 343 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 343 che coinvolge il nodo canonico `_common.py` è `L173`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 343 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 343 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo del codice RDG, l'arco 343 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, l'arco 343 registra come `target` l'identificatore `rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 343 che coinvolge il nodo canonico `_common.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=344 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `extract_patient_mpi()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_extract_patient_mpi`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `extract_patient_mpi()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 344 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 344 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 344 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 344 che coinvolge il nodo canonico `_common.py` è `L181`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 344 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 344 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo del codice RDG, l'arco 344 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, l'arco 344 registra come `target` l'identificatore `rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 344 che coinvolge il nodo canonico `_common.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=345 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `linearize_xml()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_linearize_xml`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `linearize_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 345 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 345 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 345 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 345 che coinvolge il nodo canonico `_common.py` è `L77`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 345 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 345 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo del codice RDG, l'arco 345 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, l'arco 345 registra come `target` l'identificatore `rve_transactions_common_linearize_xml` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 345 che coinvolge il nodo canonico `_common.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=346 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `msg_id_to_saml_id()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_msg_id_to_saml_id`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `msg_id_to_saml_id()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 346 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 346 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 346 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 346 che coinvolge il nodo canonico `_common.py` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 346 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 346 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo del codice RDG, l'arco 346 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, l'arco 346 registra come `target` l'identificatore `rve_transactions_common_msg_id_to_saml_id` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 346 che coinvolge il nodo canonico `_common.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=347 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `new_uuid()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_new_uuid`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `new_uuid()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 347 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 347 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 347 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 347 che coinvolge il nodo canonico `_common.py` è `L47`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 347 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 347 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo del codice RDG, l'arco 347 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, l'arco 347 registra come `target` l'identificatore `rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 347 che coinvolge il nodo canonico `_common.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=348 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_common.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `datetime [rve_transactions_common_py_datetime]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_py_datetime`. La relazione tecnica esplicita è la seguente: `_common.py` importa da `datetime [rve_transactions_common_py_datetime]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 348 che coinvolge il nodo canonico `_common.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 348 che coinvolge il nodo canonico `_common.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 348 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 348 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 348 che coinvolge il nodo canonico `_common.py` è `L13`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 348 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 348 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo del codice RDG, l'arco 348 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, l'arco 348 registra come `target` l'identificatore `rve_transactions_common_py_datetime` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 348 che coinvolge il nodo canonico `_common.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=349 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_replace_newlines_inside_tags`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 349 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 349 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 349 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 349 che coinvolge il nodo canonico `_common.py` è `L95`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 349 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 349 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo del codice RDG, l'arco 349 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, l'arco 349 registra come `target` l'identificatore `rve_transactions_common_replace_newlines_inside_tags` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 349 che coinvolge il nodo canonico `_common.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=350 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `utc_iso()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_utc_iso`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `utc_iso()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 350 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 350 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 350 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 350 che coinvolge il nodo canonico `_common.py` è `L33`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 350 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 350 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo del codice RDG, l'arco 350 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, l'arco 350 registra come `target` l'identificatore `rve_transactions_common_utc_iso` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 350 che coinvolge il nodo canonico `_common.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=351 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `utc_iso_ms()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_utc_iso_ms`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `utc_iso_ms()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 351 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 351 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 351 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 351 che coinvolge il nodo canonico `_common.py` è `L38`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 351 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 351 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo del codice RDG, l'arco 351 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, l'arco 351 registra come `target` l'identificatore `rve_transactions_common_utc_iso_ms` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 351 che coinvolge il nodo canonico `_common.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=352 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `utc_now()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_utc_now`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `utc_now()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 352 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 352 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 352 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 352 che coinvolge il nodo canonico `_common.py` è `L29`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 352 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 352 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo del codice RDG, l'arco 352 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, l'arco 352 registra come `target` l'identificatore `rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 352 che coinvolge il nodo canonico `_common.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=353 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_common.py` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_rationale_1` all'ID `rve_transactions_common`. La relazione tecnica esplicita è la seguente: `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` costituisce una motivazione per `_common.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 353 che coinvolge il nodo canonico `_common.py` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 353 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 353 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 353 che coinvolge il nodo canonico `_common.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 353 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 353 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo del codice RDG, l'arco 353 registra come `source` l'identificatore `rve_transactions_common_rationale_1` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, l'arco 353 registra come `target` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 353 che coinvolge il nodo canonico `_common.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=354 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_common.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `rve_121.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121` all'ID `rve_transactions_common`. La relazione tecnica esplicita è la seguente: `rve_121.py` importa da `_common.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 354 che coinvolge il nodo canonico `_common.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 354 che coinvolge il nodo canonico `_common.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 354 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 354 che coinvolge il nodo canonico `_common.py` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 354 che coinvolge il nodo canonico `_common.py` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 354 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 354 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo del codice RDG, l'arco 354 registra come `source` l'identificatore `rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, l'arco 354 registra come `target` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 354 che coinvolge il nodo canonico `_common.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=355 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_common.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_common`. La relazione tecnica esplicita è la seguente: `rve_1b.py` importa da `_common.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 355 che coinvolge il nodo canonico `_common.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 355 che coinvolge il nodo canonico `_common.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 355 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 355 che coinvolge il nodo canonico `_common.py` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 355 che coinvolge il nodo canonico `_common.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 355 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 355 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo del codice RDG, l'arco 355 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, l'arco 355 registra come `target` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 355 che coinvolge il nodo canonico `_common.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=356 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_common.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `rve_54.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54` all'ID `rve_transactions_common`. La relazione tecnica esplicita è la seguente: `rve_54.py` importa da `_common.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 356 che coinvolge il nodo canonico `_common.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 356 che coinvolge il nodo canonico `_common.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 356 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 356 che coinvolge il nodo canonico `_common.py` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 356 che coinvolge il nodo canonico `_common.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 356 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 356 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo del codice RDG, l'arco 356 registra come `source` l'identificatore `rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, l'arco 356 registra come `target` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 356 che coinvolge il nodo canonico `_common.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=357 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_common.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `rve_55.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_55` all'ID `rve_transactions_common`. La relazione tecnica esplicita è la seguente: `rve_55.py` importa da `_common.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 357 che coinvolge il nodo canonico `_common.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 357 che coinvolge il nodo canonico `_common.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 357 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 357 che coinvolge il nodo canonico `_common.py` è `rve_transactions/rve_55.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 357 che coinvolge il nodo canonico `_common.py` è `L22`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 357 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 357 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo del codice RDG, l'arco 357 registra come `source` l'identificatore `rve_transactions_rve_55` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, l'arco 357 registra come `target` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 357 che coinvolge il nodo canonico `_common.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=358 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_common.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `scrybasign_common.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_scrybasign_common` all'ID `rve_transactions_common`. La relazione tecnica esplicita è la seguente: `scrybasign_common.py` importa da `_common.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 358 che coinvolge il nodo canonico `_common.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 358 che coinvolge il nodo canonico `_common.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 358 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 358 che coinvolge il nodo canonico `_common.py` è `rve_transactions/scrybasign_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 358 che coinvolge il nodo canonico `_common.py` è `L8`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 358 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 358 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo del codice RDG, l'arco 358 registra come `source` l'identificatore `rve_transactions_scrybasign_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, l'arco 358 registra come `target` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 358 che coinvolge il nodo canonico `_common.py` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=161 id=Request-Dataset-Generator::rve_transactions_common -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_common.py` è `_common.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_common.py` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_common.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_common.py` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_common.py` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_common.py` è `_common.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_common.py` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_common.py` è `rve_transactions_common`. Nel grafo unificato, il nodo canonico `_common.py` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common`.
<!-- GRAPH_EDGE graph=unificata index=339 role=outgoing -->
Nel grafo unificato, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `b64_decode()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_b64_decode`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `b64_decode()`. Nel grafo unificato, il tipo dichiarato dell'arco 339 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 339 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 339 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 339 che coinvolge il nodo canonico `_common.py` è `L72`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 339 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 339 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 339 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, l'arco 339 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo unificato, l'arco 339 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_b64_decode` per la relazione che coinvolge il nodo canonico `_common.py`.
<!-- GRAPH_EDGE graph=unificata index=340 role=outgoing -->
Nel grafo unificato, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `b64_encode()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_b64_encode`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `b64_encode()`. Nel grafo unificato, il tipo dichiarato dell'arco 340 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 340 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 340 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 340 che coinvolge il nodo canonico `_common.py` è `L65`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 340 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 340 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 340 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, l'arco 340 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo unificato, l'arco 340 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_b64_encode` per la relazione che coinvolge il nodo canonico `_common.py`.
<!-- GRAPH_EDGE graph=unificata index=341 role=outgoing -->
Nel grafo unificato, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `b64url_encode()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_b64url_encode`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `b64url_encode()`. Nel grafo unificato, il tipo dichiarato dell'arco 341 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 341 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 341 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 341 che coinvolge il nodo canonico `_common.py` è `L118`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 341 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 341 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 341 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, l'arco 341 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo unificato, l'arco 341 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_b64url_encode` per la relazione che coinvolge il nodo canonico `_common.py`.
<!-- GRAPH_EDGE graph=unificata index=342 role=outgoing -->
Nel grafo unificato, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `build_mock_jwt()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `build_mock_jwt()`. Nel grafo unificato, il tipo dichiarato dell'arco 342 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 342 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 342 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 342 che coinvolge il nodo canonico `_common.py` è `L131`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 342 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 342 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 342 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, l'arco 342 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo unificato, l'arco 342 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `_common.py`.
<!-- GRAPH_EDGE graph=unificata index=343 role=outgoing -->
Nel grafo unificato, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `extract_patient_cf()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `extract_patient_cf()`. Nel grafo unificato, il tipo dichiarato dell'arco 343 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 343 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 343 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 343 che coinvolge il nodo canonico `_common.py` è `L173`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 343 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 343 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 343 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, l'arco 343 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo unificato, l'arco 343 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `_common.py`.
<!-- GRAPH_EDGE graph=unificata index=344 role=outgoing -->
Nel grafo unificato, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `extract_patient_mpi()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `extract_patient_mpi()`. Nel grafo unificato, il tipo dichiarato dell'arco 344 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 344 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 344 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 344 che coinvolge il nodo canonico `_common.py` è `L181`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 344 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 344 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 344 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, l'arco 344 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo unificato, l'arco 344 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `_common.py`.
<!-- GRAPH_EDGE graph=unificata index=345 role=outgoing -->
Nel grafo unificato, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `linearize_xml()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_linearize_xml`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `linearize_xml()`. Nel grafo unificato, il tipo dichiarato dell'arco 345 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 345 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 345 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 345 che coinvolge il nodo canonico `_common.py` è `L77`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 345 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 345 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 345 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, l'arco 345 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo unificato, l'arco 345 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_linearize_xml` per la relazione che coinvolge il nodo canonico `_common.py`.
<!-- GRAPH_EDGE graph=unificata index=346 role=outgoing -->
Nel grafo unificato, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `msg_id_to_saml_id()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_msg_id_to_saml_id`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `msg_id_to_saml_id()`. Nel grafo unificato, il tipo dichiarato dell'arco 346 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 346 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 346 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 346 che coinvolge il nodo canonico `_common.py` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 346 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 346 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 346 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, l'arco 346 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo unificato, l'arco 346 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_msg_id_to_saml_id` per la relazione che coinvolge il nodo canonico `_common.py`.
<!-- GRAPH_EDGE graph=unificata index=347 role=outgoing -->
Nel grafo unificato, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `new_uuid()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_new_uuid`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `new_uuid()`. Nel grafo unificato, il tipo dichiarato dell'arco 347 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 347 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 347 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 347 che coinvolge il nodo canonico `_common.py` è `L47`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 347 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 347 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 347 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, l'arco 347 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo unificato, l'arco 347 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `_common.py`.
<!-- GRAPH_EDGE graph=unificata index=348 role=outgoing -->
Nel grafo unificato, il nodo canonico `_common.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `datetime [rve_transactions_common_py_datetime]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_py_datetime`. La relazione tecnica esplicita è la seguente: `_common.py` importa da `datetime [rve_transactions_common_py_datetime]`. Nel grafo unificato, il tipo dichiarato dell'arco 348 che coinvolge il nodo canonico `_common.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 348 che coinvolge il nodo canonico `_common.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 348 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 348 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 348 che coinvolge il nodo canonico `_common.py` è `L13`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 348 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 348 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 348 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, l'arco 348 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo unificato, l'arco 348 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_py_datetime` per la relazione che coinvolge il nodo canonico `_common.py`.
<!-- GRAPH_EDGE graph=unificata index=349 role=outgoing -->
Nel grafo unificato, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_replace_newlines_inside_tags`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]`. Nel grafo unificato, il tipo dichiarato dell'arco 349 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 349 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 349 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 349 che coinvolge il nodo canonico `_common.py` è `L95`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 349 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 349 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 349 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, l'arco 349 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo unificato, l'arco 349 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_replace_newlines_inside_tags` per la relazione che coinvolge il nodo canonico `_common.py`.
<!-- GRAPH_EDGE graph=unificata index=350 role=outgoing -->
Nel grafo unificato, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `utc_iso()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_utc_iso`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `utc_iso()`. Nel grafo unificato, il tipo dichiarato dell'arco 350 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 350 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 350 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 350 che coinvolge il nodo canonico `_common.py` è `L33`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 350 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 350 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 350 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, l'arco 350 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo unificato, l'arco 350 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_iso` per la relazione che coinvolge il nodo canonico `_common.py`.
<!-- GRAPH_EDGE graph=unificata index=351 role=outgoing -->
Nel grafo unificato, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `utc_iso_ms()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `utc_iso_ms()`. Nel grafo unificato, il tipo dichiarato dell'arco 351 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 351 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 351 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 351 che coinvolge il nodo canonico `_common.py` è `L38`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 351 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 351 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 351 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, l'arco 351 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo unificato, l'arco 351 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms` per la relazione che coinvolge il nodo canonico `_common.py`.
<!-- GRAPH_EDGE graph=unificata index=352 role=outgoing -->
Nel grafo unificato, il nodo canonico `_common.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `utc_now()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_utc_now`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `utc_now()`. Nel grafo unificato, il tipo dichiarato dell'arco 352 che coinvolge il nodo canonico `_common.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 352 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 352 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 352 che coinvolge il nodo canonico `_common.py` è `L29`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 352 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 352 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 352 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, l'arco 352 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo unificato, l'arco 352 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `_common.py`.
<!-- GRAPH_EDGE graph=unificata index=353 role=outgoing -->
Nel grafo unificato, il nodo canonico `_common.py` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_rationale_1`. La relazione tecnica esplicita è la seguente: `_common.py` costituisce una motivazione per `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe`. Nel grafo unificato, il tipo dichiarato dell'arco 353 che coinvolge il nodo canonico `_common.py` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 353 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 353 che coinvolge il nodo canonico `_common.py` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 353 che coinvolge il nodo canonico `_common.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 353 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 353 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 353 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, l'arco 353 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo unificato, l'arco 353 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_rationale_1` per la relazione che coinvolge il nodo canonico `_common.py`.
<!-- GRAPH_EDGE graph=unificata index=354 role=outgoing -->
Nel grafo unificato, il nodo canonico `_common.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `rve_121.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_rve_121`. La relazione tecnica esplicita è la seguente: `_common.py` importa da `rve_121.py`. Nel grafo unificato, il tipo dichiarato dell'arco 354 che coinvolge il nodo canonico `_common.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 354 che coinvolge il nodo canonico `_common.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 354 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 354 che coinvolge il nodo canonico `_common.py` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 354 che coinvolge il nodo canonico `_common.py` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 354 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 354 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 354 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, l'arco 354 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo unificato, l'arco 354 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `_common.py`.
<!-- GRAPH_EDGE graph=unificata index=355 role=outgoing -->
Nel grafo unificato, il nodo canonico `_common.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `_common.py` importa da `rve_1b.py`. Nel grafo unificato, il tipo dichiarato dell'arco 355 che coinvolge il nodo canonico `_common.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 355 che coinvolge il nodo canonico `_common.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 355 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 355 che coinvolge il nodo canonico `_common.py` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 355 che coinvolge il nodo canonico `_common.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 355 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 355 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 355 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, l'arco 355 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo unificato, l'arco 355 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `_common.py`.
<!-- GRAPH_EDGE graph=unificata index=356 role=outgoing -->
Nel grafo unificato, il nodo canonico `_common.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `rve_54.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_rve_54`. La relazione tecnica esplicita è la seguente: `_common.py` importa da `rve_54.py`. Nel grafo unificato, il tipo dichiarato dell'arco 356 che coinvolge il nodo canonico `_common.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 356 che coinvolge il nodo canonico `_common.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 356 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 356 che coinvolge il nodo canonico `_common.py` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 356 che coinvolge il nodo canonico `_common.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 356 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 356 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 356 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, l'arco 356 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo unificato, l'arco 356 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `_common.py`.
<!-- GRAPH_EDGE graph=unificata index=357 role=outgoing -->
Nel grafo unificato, il nodo canonico `_common.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `rve_55.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_rve_55`. La relazione tecnica esplicita è la seguente: `_common.py` importa da `rve_55.py`. Nel grafo unificato, il tipo dichiarato dell'arco 357 che coinvolge il nodo canonico `_common.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 357 che coinvolge il nodo canonico `_common.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 357 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 357 che coinvolge il nodo canonico `_common.py` è `rve_transactions/rve_55.py`. Nel grafo unificato, la posizione sorgente dell'arco 357 che coinvolge il nodo canonico `_common.py` è `L22`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 357 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 357 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 357 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, l'arco 357 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo unificato, l'arco 357 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_55` per la relazione che coinvolge il nodo canonico `_common.py`.
<!-- GRAPH_EDGE graph=unificata index=358 role=outgoing -->
Nel grafo unificato, il nodo canonico `_common.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `scrybasign_common.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_scrybasign_common`. La relazione tecnica esplicita è la seguente: `_common.py` importa da `scrybasign_common.py`. Nel grafo unificato, il tipo dichiarato dell'arco 358 che coinvolge il nodo canonico `_common.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 358 che coinvolge il nodo canonico `_common.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 358 che coinvolge il nodo canonico `_common.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 358 che coinvolge il nodo canonico `_common.py` è `rve_transactions/scrybasign_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 358 che coinvolge il nodo canonico `_common.py` è `L8`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 358 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 358 che coinvolge il nodo canonico `_common.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 358 che coinvolge il nodo canonico `_common.py` è `1.0`. Nel grafo unificato, l'arco 358 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_common.py`. Nel grafo unificato, l'arco 358 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_scrybasign_common` per la relazione che coinvolge il nodo canonico `_common.py`.
Nel grafo unificato, il nodo canonico `_common.py` non possiede archi entranti registrati nel campo `links`.

### _conditions_xml()

<!-- GRAPH_NODE graph=codice index=220 id=rve_transactions_rve_1b_conditions_xml -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_conditions_xml()` è `_conditions_xml()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_conditions_xml()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_conditions_xml()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_conditions_xml()` è `L127`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_conditions_xml()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_conditions_xml()` possiede l'identificatore esatto `rve_transactions_rve_1b_conditions_xml`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_conditions_xml()` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_conditions_xml()` è `_conditions_xml()`.
Nel grafo del codice RDG, il nodo canonico `_conditions_xml()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=466 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_conditions_xml()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_rve_1b_conditions_xml`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_conditions_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 466 che coinvolge il nodo canonico `_conditions_xml()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 466 che coinvolge il nodo canonico `_conditions_xml()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 466 che coinvolge il nodo canonico `_conditions_xml()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 466 che coinvolge il nodo canonico `_conditions_xml()` è `L127`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 466 che coinvolge il nodo canonico `_conditions_xml()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 466 che coinvolge il nodo canonico `_conditions_xml()` è `ast`. Nel grafo del codice RDG, l'arco 466 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `_conditions_xml()`. Nel grafo del codice RDG, l'arco 466 registra come `target` l'identificatore `rve_transactions_rve_1b_conditions_xml` per la relazione che coinvolge il nodo canonico `_conditions_xml()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 466 che coinvolge il nodo canonico `_conditions_xml()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=475 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_conditions_xml()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_build_soap_request()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_soap_request` all'ID `rve_transactions_rve_1b_conditions_xml`. La relazione tecnica esplicita è la seguente: `_build_soap_request()` chiama `_conditions_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 475 che coinvolge il nodo canonico `_conditions_xml()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 475 che coinvolge il nodo canonico `_conditions_xml()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 475 che coinvolge il nodo canonico `_conditions_xml()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 475 che coinvolge il nodo canonico `_conditions_xml()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 475 che coinvolge il nodo canonico `_conditions_xml()` è `L157`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 475 che coinvolge il nodo canonico `_conditions_xml()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 475 che coinvolge il nodo canonico `_conditions_xml()` è `ast`. Nel grafo del codice RDG, l'arco 475 registra come `source` l'identificatore `rve_transactions_rve_1b_build_soap_request` per la relazione che coinvolge il nodo canonico `_conditions_xml()`. Nel grafo del codice RDG, l'arco 475 registra come `target` l'identificatore `rve_transactions_rve_1b_conditions_xml` per la relazione che coinvolge il nodo canonico `_conditions_xml()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 475 che coinvolge il nodo canonico `_conditions_xml()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=220 id=Request-Dataset-Generator::rve_transactions_rve_1b_conditions_xml -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_conditions_xml()` è `_conditions_xml()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_conditions_xml()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_conditions_xml()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_conditions_xml()` è `L127`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_conditions_xml()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_conditions_xml()` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_conditions_xml()` è `_conditions_xml()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_conditions_xml()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_conditions_xml()` è `rve_transactions_rve_1b_conditions_xml`. Nel grafo unificato, il nodo canonico `_conditions_xml()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_1b_conditions_xml`.
<!-- GRAPH_EDGE graph=unificata index=475 role=outgoing -->
Nel grafo unificato, il nodo canonico `_conditions_xml()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_build_soap_request()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b_conditions_xml` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_soap_request`. La relazione tecnica esplicita è la seguente: `_conditions_xml()` chiama `_build_soap_request()`. Nel grafo unificato, il tipo dichiarato dell'arco 475 che coinvolge il nodo canonico `_conditions_xml()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 475 che coinvolge il nodo canonico `_conditions_xml()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 475 che coinvolge il nodo canonico `_conditions_xml()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 475 che coinvolge il nodo canonico `_conditions_xml()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 475 che coinvolge il nodo canonico `_conditions_xml()` è `L157`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 475 che coinvolge il nodo canonico `_conditions_xml()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 475 che coinvolge il nodo canonico `_conditions_xml()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 475 che coinvolge il nodo canonico `_conditions_xml()` è `1.0`. Nel grafo unificato, l'arco 475 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_conditions_xml` per la relazione che coinvolge il nodo canonico `_conditions_xml()`. Nel grafo unificato, l'arco 475 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_soap_request` per la relazione che coinvolge il nodo canonico `_conditions_xml()`.
<!-- GRAPH_EDGE graph=unificata index=466 role=incoming -->
Nel grafo unificato, il nodo canonico `_conditions_xml()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_conditions_xml`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_conditions_xml()`. Nel grafo unificato, il tipo dichiarato dell'arco 466 che coinvolge il nodo canonico `_conditions_xml()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 466 che coinvolge il nodo canonico `_conditions_xml()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 466 che coinvolge il nodo canonico `_conditions_xml()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 466 che coinvolge il nodo canonico `_conditions_xml()` è `L127`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 466 che coinvolge il nodo canonico `_conditions_xml()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 466 che coinvolge il nodo canonico `_conditions_xml()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 466 che coinvolge il nodo canonico `_conditions_xml()` è `1.0`. Nel grafo unificato, l'arco 466 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `_conditions_xml()`. Nel grafo unificato, l'arco 466 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_conditions_xml` per la relazione che coinvolge il nodo canonico `_conditions_xml()`.

### _format_timestamp()

<!-- GRAPH_NODE graph=codice index=219 id=rve_transactions_rve_1b_format_timestamp -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_format_timestamp()` è `_format_timestamp()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_format_timestamp()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_format_timestamp()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_format_timestamp()` è `L117`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_format_timestamp()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_format_timestamp()` possiede l'identificatore esatto `rve_transactions_rve_1b_format_timestamp`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_format_timestamp()` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_format_timestamp()` è `_format_timestamp()`.
<!-- GRAPH_EDGE graph=codice index=368 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_format_timestamp()` è la sorgente `source` della relazione `calls` verso il nodo canonico `utc_iso()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_format_timestamp` all'ID `rve_transactions_common_utc_iso`. La relazione tecnica esplicita è la seguente: `_format_timestamp()` chiama `utc_iso()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 368 che coinvolge il nodo canonico `_format_timestamp()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 368 che coinvolge il nodo canonico `_format_timestamp()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 368 che coinvolge il nodo canonico `_format_timestamp()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 368 che coinvolge il nodo canonico `_format_timestamp()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 368 che coinvolge il nodo canonico `_format_timestamp()` è `L119`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 368 che coinvolge il nodo canonico `_format_timestamp()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 368 che coinvolge il nodo canonico `_format_timestamp()` è `ast`. Nel grafo del codice RDG, l'arco 368 registra come `source` l'identificatore `rve_transactions_rve_1b_format_timestamp` per la relazione che coinvolge il nodo canonico `_format_timestamp()`. Nel grafo del codice RDG, l'arco 368 registra come `target` l'identificatore `rve_transactions_common_utc_iso` per la relazione che coinvolge il nodo canonico `_format_timestamp()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 368 che coinvolge il nodo canonico `_format_timestamp()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=370 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_format_timestamp()` è la sorgente `source` della relazione `calls` verso il nodo canonico `utc_iso_ms()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_format_timestamp` all'ID `rve_transactions_common_utc_iso_ms`. La relazione tecnica esplicita è la seguente: `_format_timestamp()` chiama `utc_iso_ms()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 370 che coinvolge il nodo canonico `_format_timestamp()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 370 che coinvolge il nodo canonico `_format_timestamp()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 370 che coinvolge il nodo canonico `_format_timestamp()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 370 che coinvolge il nodo canonico `_format_timestamp()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 370 che coinvolge il nodo canonico `_format_timestamp()` è `L120`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 370 che coinvolge il nodo canonico `_format_timestamp()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 370 che coinvolge il nodo canonico `_format_timestamp()` è `ast`. Nel grafo del codice RDG, l'arco 370 registra come `source` l'identificatore `rve_transactions_rve_1b_format_timestamp` per la relazione che coinvolge il nodo canonico `_format_timestamp()`. Nel grafo del codice RDG, l'arco 370 registra come `target` l'identificatore `rve_transactions_common_utc_iso_ms` per la relazione che coinvolge il nodo canonico `_format_timestamp()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 370 che coinvolge il nodo canonico `_format_timestamp()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=467 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_format_timestamp()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_rve_1b_format_timestamp`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_format_timestamp()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 467 che coinvolge il nodo canonico `_format_timestamp()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 467 che coinvolge il nodo canonico `_format_timestamp()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 467 che coinvolge il nodo canonico `_format_timestamp()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 467 che coinvolge il nodo canonico `_format_timestamp()` è `L117`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 467 che coinvolge il nodo canonico `_format_timestamp()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 467 che coinvolge il nodo canonico `_format_timestamp()` è `ast`. Nel grafo del codice RDG, l'arco 467 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `_format_timestamp()`. Nel grafo del codice RDG, l'arco 467 registra come `target` l'identificatore `rve_transactions_rve_1b_format_timestamp` per la relazione che coinvolge il nodo canonico `_format_timestamp()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 467 che coinvolge il nodo canonico `_format_timestamp()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=474 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_format_timestamp()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_rve_1b_format_timestamp`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `_format_timestamp()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 474 che coinvolge il nodo canonico `_format_timestamp()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 474 che coinvolge il nodo canonico `_format_timestamp()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 474 che coinvolge il nodo canonico `_format_timestamp()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 474 che coinvolge il nodo canonico `_format_timestamp()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 474 che coinvolge il nodo canonico `_format_timestamp()` è `L334`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 474 che coinvolge il nodo canonico `_format_timestamp()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 474 che coinvolge il nodo canonico `_format_timestamp()` è `ast`. Nel grafo del codice RDG, l'arco 474 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `_format_timestamp()`. Nel grafo del codice RDG, l'arco 474 registra come `target` l'identificatore `rve_transactions_rve_1b_format_timestamp` per la relazione che coinvolge il nodo canonico `_format_timestamp()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 474 che coinvolge il nodo canonico `_format_timestamp()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=219 id=Request-Dataset-Generator::rve_transactions_rve_1b_format_timestamp -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_format_timestamp()` è `_format_timestamp()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_format_timestamp()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_format_timestamp()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_format_timestamp()` è `L117`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_format_timestamp()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_format_timestamp()` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_format_timestamp()` è `_format_timestamp()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_format_timestamp()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_format_timestamp()` è `rve_transactions_rve_1b_format_timestamp`. Nel grafo unificato, il nodo canonico `_format_timestamp()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_1b_format_timestamp`.
<!-- GRAPH_EDGE graph=unificata index=474 role=outgoing -->
Nel grafo unificato, il nodo canonico `_format_timestamp()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b_format_timestamp` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `_format_timestamp()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 474 che coinvolge il nodo canonico `_format_timestamp()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 474 che coinvolge il nodo canonico `_format_timestamp()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 474 che coinvolge il nodo canonico `_format_timestamp()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 474 che coinvolge il nodo canonico `_format_timestamp()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 474 che coinvolge il nodo canonico `_format_timestamp()` è `L334`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 474 che coinvolge il nodo canonico `_format_timestamp()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 474 che coinvolge il nodo canonico `_format_timestamp()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 474 che coinvolge il nodo canonico `_format_timestamp()` è `1.0`. Nel grafo unificato, l'arco 474 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_format_timestamp` per la relazione che coinvolge il nodo canonico `_format_timestamp()`. Nel grafo unificato, l'arco 474 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `_format_timestamp()`.
<!-- GRAPH_EDGE graph=unificata index=368 role=incoming -->
Nel grafo unificato, il nodo canonico `_format_timestamp()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `utc_iso()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_utc_iso` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_format_timestamp`. La relazione tecnica esplicita è la seguente: `utc_iso()` chiama `_format_timestamp()`. Nel grafo unificato, il tipo dichiarato dell'arco 368 che coinvolge il nodo canonico `_format_timestamp()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 368 che coinvolge il nodo canonico `_format_timestamp()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 368 che coinvolge il nodo canonico `_format_timestamp()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 368 che coinvolge il nodo canonico `_format_timestamp()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 368 che coinvolge il nodo canonico `_format_timestamp()` è `L119`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 368 che coinvolge il nodo canonico `_format_timestamp()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 368 che coinvolge il nodo canonico `_format_timestamp()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 368 che coinvolge il nodo canonico `_format_timestamp()` è `1.0`. Nel grafo unificato, l'arco 368 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_iso` per la relazione che coinvolge il nodo canonico `_format_timestamp()`. Nel grafo unificato, l'arco 368 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_format_timestamp` per la relazione che coinvolge il nodo canonico `_format_timestamp()`.
<!-- GRAPH_EDGE graph=unificata index=370 role=incoming -->
Nel grafo unificato, il nodo canonico `_format_timestamp()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `utc_iso_ms()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_format_timestamp`. La relazione tecnica esplicita è la seguente: `utc_iso_ms()` chiama `_format_timestamp()`. Nel grafo unificato, il tipo dichiarato dell'arco 370 che coinvolge il nodo canonico `_format_timestamp()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 370 che coinvolge il nodo canonico `_format_timestamp()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 370 che coinvolge il nodo canonico `_format_timestamp()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 370 che coinvolge il nodo canonico `_format_timestamp()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 370 che coinvolge il nodo canonico `_format_timestamp()` è `L120`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 370 che coinvolge il nodo canonico `_format_timestamp()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 370 che coinvolge il nodo canonico `_format_timestamp()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 370 che coinvolge il nodo canonico `_format_timestamp()` è `1.0`. Nel grafo unificato, l'arco 370 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms` per la relazione che coinvolge il nodo canonico `_format_timestamp()`. Nel grafo unificato, l'arco 370 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_format_timestamp` per la relazione che coinvolge il nodo canonico `_format_timestamp()`.
<!-- GRAPH_EDGE graph=unificata index=467 role=incoming -->
Nel grafo unificato, il nodo canonico `_format_timestamp()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_format_timestamp`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_format_timestamp()`. Nel grafo unificato, il tipo dichiarato dell'arco 467 che coinvolge il nodo canonico `_format_timestamp()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 467 che coinvolge il nodo canonico `_format_timestamp()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 467 che coinvolge il nodo canonico `_format_timestamp()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 467 che coinvolge il nodo canonico `_format_timestamp()` è `L117`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 467 che coinvolge il nodo canonico `_format_timestamp()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 467 che coinvolge il nodo canonico `_format_timestamp()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 467 che coinvolge il nodo canonico `_format_timestamp()` è `1.0`. Nel grafo unificato, l'arco 467 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `_format_timestamp()`. Nel grafo unificato, l'arco 467 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_format_timestamp` per la relazione che coinvolge il nodo canonico `_format_timestamp()`.

### _profile_name()

<!-- GRAPH_NODE graph=codice index=217 id=rve_transactions_rve_1b_profile_name -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_profile_name()` è `_profile_name()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_profile_name()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_profile_name()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_profile_name()` è `L71`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_profile_name()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_profile_name()` possiede l'identificatore esatto `rve_transactions_rve_1b_profile_name`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_profile_name()` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_profile_name()` è `_profile_name()`.
Nel grafo del codice RDG, il nodo canonico `_profile_name()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=468 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_profile_name()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_rve_1b_profile_name`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_profile_name()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 468 che coinvolge il nodo canonico `_profile_name()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 468 che coinvolge il nodo canonico `_profile_name()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 468 che coinvolge il nodo canonico `_profile_name()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 468 che coinvolge il nodo canonico `_profile_name()` è `L71`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 468 che coinvolge il nodo canonico `_profile_name()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 468 che coinvolge il nodo canonico `_profile_name()` è `ast`. Nel grafo del codice RDG, l'arco 468 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `_profile_name()`. Nel grafo del codice RDG, l'arco 468 registra come `target` l'identificatore `rve_transactions_rve_1b_profile_name` per la relazione che coinvolge il nodo canonico `_profile_name()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 468 che coinvolge il nodo canonico `_profile_name()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=472 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_profile_name()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_rve_1b_profile_name`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `_profile_name()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 472 che coinvolge il nodo canonico `_profile_name()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 472 che coinvolge il nodo canonico `_profile_name()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 472 che coinvolge il nodo canonico `_profile_name()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 472 che coinvolge il nodo canonico `_profile_name()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 472 che coinvolge il nodo canonico `_profile_name()` è `L322`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 472 che coinvolge il nodo canonico `_profile_name()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 472 che coinvolge il nodo canonico `_profile_name()` è `ast`. Nel grafo del codice RDG, l'arco 472 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `_profile_name()`. Nel grafo del codice RDG, l'arco 472 registra come `target` l'identificatore `rve_transactions_rve_1b_profile_name` per la relazione che coinvolge il nodo canonico `_profile_name()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 472 che coinvolge il nodo canonico `_profile_name()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=217 id=Request-Dataset-Generator::rve_transactions_rve_1b_profile_name -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_profile_name()` è `_profile_name()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_profile_name()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_profile_name()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_profile_name()` è `L71`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_profile_name()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_profile_name()` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_profile_name()` è `_profile_name()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_profile_name()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_profile_name()` è `rve_transactions_rve_1b_profile_name`. Nel grafo unificato, il nodo canonico `_profile_name()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_1b_profile_name`.
<!-- GRAPH_EDGE graph=unificata index=472 role=outgoing -->
Nel grafo unificato, il nodo canonico `_profile_name()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b_profile_name` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `_profile_name()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 472 che coinvolge il nodo canonico `_profile_name()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 472 che coinvolge il nodo canonico `_profile_name()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 472 che coinvolge il nodo canonico `_profile_name()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 472 che coinvolge il nodo canonico `_profile_name()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 472 che coinvolge il nodo canonico `_profile_name()` è `L322`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 472 che coinvolge il nodo canonico `_profile_name()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 472 che coinvolge il nodo canonico `_profile_name()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 472 che coinvolge il nodo canonico `_profile_name()` è `1.0`. Nel grafo unificato, l'arco 472 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_profile_name` per la relazione che coinvolge il nodo canonico `_profile_name()`. Nel grafo unificato, l'arco 472 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `_profile_name()`.
<!-- GRAPH_EDGE graph=unificata index=468 role=incoming -->
Nel grafo unificato, il nodo canonico `_profile_name()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_profile_name`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_profile_name()`. Nel grafo unificato, il tipo dichiarato dell'arco 468 che coinvolge il nodo canonico `_profile_name()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 468 che coinvolge il nodo canonico `_profile_name()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 468 che coinvolge il nodo canonico `_profile_name()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 468 che coinvolge il nodo canonico `_profile_name()` è `L71`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 468 che coinvolge il nodo canonico `_profile_name()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 468 che coinvolge il nodo canonico `_profile_name()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 468 che coinvolge il nodo canonico `_profile_name()` è `1.0`. Nel grafo unificato, l'arco 468 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `_profile_name()`. Nel grafo unificato, l'arco 468 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_profile_name` per la relazione che coinvolge il nodo canonico `_profile_name()`.

### _replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]

<!-- GRAPH_NODE graph=codice index=171 id=rve_transactions_common_replace_newlines_inside_tags -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `_replace_newlines_inside_tags()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `L95`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` possiede l'identificatore esatto `rve_transactions_common_replace_newlines_inside_tags`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `_replace_newlines_inside_tags()`.
Nel grafo del codice RDG, il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=349 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_replace_newlines_inside_tags`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 349 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 349 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 349 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 349 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `L95`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 349 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 349 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `ast`. Nel grafo del codice RDG, l'arco 349 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]`. Nel grafo del codice RDG, l'arco 349 registra come `target` l'identificatore `rve_transactions_common_replace_newlines_inside_tags` per la relazione che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 349 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=385 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `linearize_xml()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_linearize_xml` all'ID `rve_transactions_common_replace_newlines_inside_tags`. La relazione tecnica esplicita è la seguente: `linearize_xml()` chiama `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 385 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 385 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 385 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 385 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 385 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `L91`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 385 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 385 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `ast`. Nel grafo del codice RDG, l'arco 385 registra come `source` l'identificatore `rve_transactions_common_linearize_xml` per la relazione che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]`. Nel grafo del codice RDG, l'arco 385 registra come `target` l'identificatore `rve_transactions_common_replace_newlines_inside_tags` per la relazione che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 385 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=171 id=Request-Dataset-Generator::rve_transactions_common_replace_newlines_inside_tags -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `_replace_newlines_inside_tags()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `L95`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `_replace_newlines_inside_tags()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `rve_transactions_common_replace_newlines_inside_tags`. Nel grafo unificato, il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_replace_newlines_inside_tags`.
Nel grafo unificato, il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=349 role=incoming -->
Nel grafo unificato, il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_replace_newlines_inside_tags`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]`. Nel grafo unificato, il tipo dichiarato dell'arco 349 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 349 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 349 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 349 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `L95`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 349 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 349 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 349 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `1.0`. Nel grafo unificato, l'arco 349 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]`. Nel grafo unificato, l'arco 349 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_replace_newlines_inside_tags` per la relazione che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]`.
<!-- GRAPH_EDGE graph=unificata index=385 role=incoming -->
Nel grafo unificato, il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `linearize_xml()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_linearize_xml` all'ID `Request-Dataset-Generator::rve_transactions_common_replace_newlines_inside_tags`. La relazione tecnica esplicita è la seguente: `linearize_xml()` chiama `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]`. Nel grafo unificato, il tipo dichiarato dell'arco 385 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 385 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 385 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 385 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 385 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `L91`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 385 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 385 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 385 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]` è `1.0`. Nel grafo unificato, l'arco 385 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_linearize_xml` per la relazione che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]`. Nel grafo unificato, l'arco 385 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_replace_newlines_inside_tags` per la relazione che coinvolge il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]`.

### _resolve_profile()

<!-- GRAPH_NODE graph=codice index=218 id=rve_transactions_rve_1b_resolve_profile -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_resolve_profile()` è `_resolve_profile()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_resolve_profile()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_resolve_profile()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_resolve_profile()` è `L79`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_resolve_profile()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_resolve_profile()` possiede l'identificatore esatto `rve_transactions_rve_1b_resolve_profile`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_resolve_profile()` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_resolve_profile()` è `_resolve_profile()`.
Nel grafo del codice RDG, il nodo canonico `_resolve_profile()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=469 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_resolve_profile()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_rve_1b_resolve_profile`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_resolve_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 469 che coinvolge il nodo canonico `_resolve_profile()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 469 che coinvolge il nodo canonico `_resolve_profile()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 469 che coinvolge il nodo canonico `_resolve_profile()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 469 che coinvolge il nodo canonico `_resolve_profile()` è `L79`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 469 che coinvolge il nodo canonico `_resolve_profile()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 469 che coinvolge il nodo canonico `_resolve_profile()` è `ast`. Nel grafo del codice RDG, l'arco 469 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `_resolve_profile()`. Nel grafo del codice RDG, l'arco 469 registra come `target` l'identificatore `rve_transactions_rve_1b_resolve_profile` per la relazione che coinvolge il nodo canonico `_resolve_profile()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 469 che coinvolge il nodo canonico `_resolve_profile()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=473 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_resolve_profile()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_rve_1b_resolve_profile`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `_resolve_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 473 che coinvolge il nodo canonico `_resolve_profile()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 473 che coinvolge il nodo canonico `_resolve_profile()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 473 che coinvolge il nodo canonico `_resolve_profile()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 473 che coinvolge il nodo canonico `_resolve_profile()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 473 che coinvolge il nodo canonico `_resolve_profile()` è `L323`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 473 che coinvolge il nodo canonico `_resolve_profile()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 473 che coinvolge il nodo canonico `_resolve_profile()` è `ast`. Nel grafo del codice RDG, l'arco 473 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `_resolve_profile()`. Nel grafo del codice RDG, l'arco 473 registra come `target` l'identificatore `rve_transactions_rve_1b_resolve_profile` per la relazione che coinvolge il nodo canonico `_resolve_profile()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 473 che coinvolge il nodo canonico `_resolve_profile()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=218 id=Request-Dataset-Generator::rve_transactions_rve_1b_resolve_profile -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_resolve_profile()` è `_resolve_profile()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_resolve_profile()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_resolve_profile()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_resolve_profile()` è `L79`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_resolve_profile()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_resolve_profile()` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_resolve_profile()` è `_resolve_profile()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_resolve_profile()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_resolve_profile()` è `rve_transactions_rve_1b_resolve_profile`. Nel grafo unificato, il nodo canonico `_resolve_profile()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_1b_resolve_profile`.
<!-- GRAPH_EDGE graph=unificata index=473 role=outgoing -->
Nel grafo unificato, il nodo canonico `_resolve_profile()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b_resolve_profile` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `_resolve_profile()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 473 che coinvolge il nodo canonico `_resolve_profile()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 473 che coinvolge il nodo canonico `_resolve_profile()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 473 che coinvolge il nodo canonico `_resolve_profile()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 473 che coinvolge il nodo canonico `_resolve_profile()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 473 che coinvolge il nodo canonico `_resolve_profile()` è `L323`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 473 che coinvolge il nodo canonico `_resolve_profile()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 473 che coinvolge il nodo canonico `_resolve_profile()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 473 che coinvolge il nodo canonico `_resolve_profile()` è `1.0`. Nel grafo unificato, l'arco 473 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_resolve_profile` per la relazione che coinvolge il nodo canonico `_resolve_profile()`. Nel grafo unificato, l'arco 473 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `_resolve_profile()`.
<!-- GRAPH_EDGE graph=unificata index=469 role=incoming -->
Nel grafo unificato, il nodo canonico `_resolve_profile()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_resolve_profile`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_resolve_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 469 che coinvolge il nodo canonico `_resolve_profile()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 469 che coinvolge il nodo canonico `_resolve_profile()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 469 che coinvolge il nodo canonico `_resolve_profile()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 469 che coinvolge il nodo canonico `_resolve_profile()` è `L79`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 469 che coinvolge il nodo canonico `_resolve_profile()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 469 che coinvolge il nodo canonico `_resolve_profile()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 469 che coinvolge il nodo canonico `_resolve_profile()` è `1.0`. Nel grafo unificato, l'arco 469 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `_resolve_profile()`. Nel grafo unificato, l'arco 469 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_resolve_profile` per la relazione che coinvolge il nodo canonico `_resolve_profile()`.

### Avvolge l&#x27;Assertion in un SOAP Response / samlp:Response di successo.

<!-- GRAPH_NODE graph=codice index=227 id=rve_transactions_rve_1b_rationale_276 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `L276`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` possiede l'identificatore esatto `rve_transactions_rve_1b_rationale_276`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `avvolge l'assertion in un soap response / samlp:response di successo.`.
<!-- GRAPH_EDGE graph=codice index=480 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `_build_mock_soap_response()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_rationale_276` all'ID `rve_transactions_rve_1b_build_mock_soap_response`. La relazione tecnica esplicita è la seguente: `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` costituisce una motivazione per `_build_mock_soap_response()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 480 che coinvolge il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 480 che coinvolge il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 480 che coinvolge il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 480 che coinvolge il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `L276`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 480 che coinvolge il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 480 che coinvolge il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `ast`. Nel grafo del codice RDG, l'arco 480 registra come `source` l'identificatore `rve_transactions_rve_1b_rationale_276` per la relazione che coinvolge il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.`. Nel grafo del codice RDG, l'arco 480 registra come `target` l'identificatore `rve_transactions_rve_1b_build_mock_soap_response` per la relazione che coinvolge il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 480 che coinvolge il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=227 id=Request-Dataset-Generator::rve_transactions_rve_1b_rationale_276 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `L276`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `avvolge l'assertion in un soap response / samlp:response di successo.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `rve_transactions_rve_1b_rationale_276`. Nel grafo unificato, il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_276`.
Nel grafo unificato, il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=480 role=incoming -->
Nel grafo unificato, il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `_build_mock_soap_response()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_soap_response` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_276`. La relazione tecnica esplicita è la seguente: `_build_mock_soap_response()` costituisce una motivazione per `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.`. Nel grafo unificato, il tipo dichiarato dell'arco 480 che coinvolge il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 480 che coinvolge il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 480 che coinvolge il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 480 che coinvolge il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `L276`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 480 che coinvolge il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 480 che coinvolge il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 480 che coinvolge il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.` è `1.0`. Nel grafo unificato, l'arco 480 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_soap_response` per la relazione che coinvolge il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.`. Nel grafo unificato, l'arco 480 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_276` per la relazione che coinvolge il nodo canonico `Avvolge l'Assertion in un SOAP Response / samlp:Response di successo.`.

### b64_decode()

<!-- GRAPH_NODE graph=codice index=169 id=rve_transactions_common_b64_decode -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `b64_decode()` è `b64_decode()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `b64_decode()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `b64_decode()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `b64_decode()` è `L72`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `b64_decode()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `b64_decode()` possiede l'identificatore esatto `rve_transactions_common_b64_decode`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `b64_decode()` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `b64_decode()` è `b64_decode()`.
Nel grafo del codice RDG, il nodo canonico `b64_decode()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=339 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `b64_decode()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_b64_decode`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `b64_decode()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 339 che coinvolge il nodo canonico `b64_decode()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 339 che coinvolge il nodo canonico `b64_decode()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 339 che coinvolge il nodo canonico `b64_decode()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 339 che coinvolge il nodo canonico `b64_decode()` è `L72`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 339 che coinvolge il nodo canonico `b64_decode()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 339 che coinvolge il nodo canonico `b64_decode()` è `ast`. Nel grafo del codice RDG, l'arco 339 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `b64_decode()`. Nel grafo del codice RDG, l'arco 339 registra come `target` l'identificatore `rve_transactions_common_b64_decode` per la relazione che coinvolge il nodo canonico `b64_decode()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 339 che coinvolge il nodo canonico `b64_decode()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=384 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `b64_decode()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Base64 decode → stringa UTF-8.`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_rationale_73` all'ID `rve_transactions_common_b64_decode`. La relazione tecnica esplicita è la seguente: `Base64 decode → stringa UTF-8.` costituisce una motivazione per `b64_decode()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 384 che coinvolge il nodo canonico `b64_decode()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 384 che coinvolge il nodo canonico `b64_decode()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 384 che coinvolge il nodo canonico `b64_decode()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 384 che coinvolge il nodo canonico `b64_decode()` è `L73`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 384 che coinvolge il nodo canonico `b64_decode()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 384 che coinvolge il nodo canonico `b64_decode()` è `ast`. Nel grafo del codice RDG, l'arco 384 registra come `source` l'identificatore `rve_transactions_common_rationale_73` per la relazione che coinvolge il nodo canonico `b64_decode()`. Nel grafo del codice RDG, l'arco 384 registra come `target` l'identificatore `rve_transactions_common_b64_decode` per la relazione che coinvolge il nodo canonico `b64_decode()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 384 che coinvolge il nodo canonico `b64_decode()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=169 id=Request-Dataset-Generator::rve_transactions_common_b64_decode -->
Nel grafo unificato, l'etichetta originale del nodo canonico `b64_decode()` è `b64_decode()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `b64_decode()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `b64_decode()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `b64_decode()` è `L72`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `b64_decode()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `b64_decode()` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `b64_decode()` è `b64_decode()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `b64_decode()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `b64_decode()` è `rve_transactions_common_b64_decode`. Nel grafo unificato, il nodo canonico `b64_decode()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_b64_decode`.
<!-- GRAPH_EDGE graph=unificata index=384 role=outgoing -->
Nel grafo unificato, il nodo canonico `b64_decode()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Base64 decode → stringa UTF-8.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_b64_decode` all'ID `Request-Dataset-Generator::rve_transactions_common_rationale_73`. La relazione tecnica esplicita è la seguente: `b64_decode()` costituisce una motivazione per `Base64 decode → stringa UTF-8.`. Nel grafo unificato, il tipo dichiarato dell'arco 384 che coinvolge il nodo canonico `b64_decode()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 384 che coinvolge il nodo canonico `b64_decode()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 384 che coinvolge il nodo canonico `b64_decode()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 384 che coinvolge il nodo canonico `b64_decode()` è `L73`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 384 che coinvolge il nodo canonico `b64_decode()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 384 che coinvolge il nodo canonico `b64_decode()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 384 che coinvolge il nodo canonico `b64_decode()` è `1.0`. Nel grafo unificato, l'arco 384 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_b64_decode` per la relazione che coinvolge il nodo canonico `b64_decode()`. Nel grafo unificato, l'arco 384 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_rationale_73` per la relazione che coinvolge il nodo canonico `b64_decode()`.
<!-- GRAPH_EDGE graph=unificata index=339 role=incoming -->
Nel grafo unificato, il nodo canonico `b64_decode()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_b64_decode`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `b64_decode()`. Nel grafo unificato, il tipo dichiarato dell'arco 339 che coinvolge il nodo canonico `b64_decode()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 339 che coinvolge il nodo canonico `b64_decode()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 339 che coinvolge il nodo canonico `b64_decode()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 339 che coinvolge il nodo canonico `b64_decode()` è `L72`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 339 che coinvolge il nodo canonico `b64_decode()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 339 che coinvolge il nodo canonico `b64_decode()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 339 che coinvolge il nodo canonico `b64_decode()` è `1.0`. Nel grafo unificato, l'arco 339 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `b64_decode()`. Nel grafo unificato, l'arco 339 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_b64_decode` per la relazione che coinvolge il nodo canonico `b64_decode()`.

### b64url_encode()

<!-- GRAPH_NODE graph=codice index=172 id=rve_transactions_common_b64url_encode -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `b64url_encode()` è `b64url_encode()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `b64url_encode()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `b64url_encode()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `b64url_encode()` è `L118`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `b64url_encode()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `b64url_encode()` possiede l'identificatore esatto `rve_transactions_common_b64url_encode`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `b64url_encode()` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `b64url_encode()` è `b64url_encode()`.
Nel grafo del codice RDG, il nodo canonico `b64url_encode()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=341 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `b64url_encode()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_b64url_encode`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `b64url_encode()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 341 che coinvolge il nodo canonico `b64url_encode()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 341 che coinvolge il nodo canonico `b64url_encode()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 341 che coinvolge il nodo canonico `b64url_encode()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 341 che coinvolge il nodo canonico `b64url_encode()` è `L118`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 341 che coinvolge il nodo canonico `b64url_encode()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 341 che coinvolge il nodo canonico `b64url_encode()` è `ast`. Nel grafo del codice RDG, l'arco 341 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `b64url_encode()`. Nel grafo del codice RDG, l'arco 341 registra come `target` l'identificatore `rve_transactions_common_b64url_encode` per la relazione che coinvolge il nodo canonico `b64url_encode()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 341 che coinvolge il nodo canonico `b64url_encode()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=389 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `b64url_encode()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_mock_jwt()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_build_mock_jwt` all'ID `rve_transactions_common_b64url_encode`. La relazione tecnica esplicita è la seguente: `build_mock_jwt()` chiama `b64url_encode()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 389 che coinvolge il nodo canonico `b64url_encode()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 389 che coinvolge il nodo canonico `b64url_encode()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 389 che coinvolge il nodo canonico `b64url_encode()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 389 che coinvolge il nodo canonico `b64url_encode()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 389 che coinvolge il nodo canonico `b64url_encode()` è `L162`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 389 che coinvolge il nodo canonico `b64url_encode()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 389 che coinvolge il nodo canonico `b64url_encode()` è `ast`. Nel grafo del codice RDG, l'arco 389 registra come `source` l'identificatore `rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `b64url_encode()`. Nel grafo del codice RDG, l'arco 389 registra come `target` l'identificatore `rve_transactions_common_b64url_encode` per la relazione che coinvolge il nodo canonico `b64url_encode()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 389 che coinvolge il nodo canonico `b64url_encode()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=390 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `b64url_encode()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Base64url senza padding — usato nei JWT.`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_rationale_119` all'ID `rve_transactions_common_b64url_encode`. La relazione tecnica esplicita è la seguente: `Base64url senza padding — usato nei JWT.` costituisce una motivazione per `b64url_encode()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 390 che coinvolge il nodo canonico `b64url_encode()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 390 che coinvolge il nodo canonico `b64url_encode()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 390 che coinvolge il nodo canonico `b64url_encode()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 390 che coinvolge il nodo canonico `b64url_encode()` è `L119`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 390 che coinvolge il nodo canonico `b64url_encode()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 390 che coinvolge il nodo canonico `b64url_encode()` è `ast`. Nel grafo del codice RDG, l'arco 390 registra come `source` l'identificatore `rve_transactions_common_rationale_119` per la relazione che coinvolge il nodo canonico `b64url_encode()`. Nel grafo del codice RDG, l'arco 390 registra come `target` l'identificatore `rve_transactions_common_b64url_encode` per la relazione che coinvolge il nodo canonico `b64url_encode()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 390 che coinvolge il nodo canonico `b64url_encode()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=172 id=Request-Dataset-Generator::rve_transactions_common_b64url_encode -->
Nel grafo unificato, l'etichetta originale del nodo canonico `b64url_encode()` è `b64url_encode()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `b64url_encode()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `b64url_encode()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `b64url_encode()` è `L118`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `b64url_encode()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `b64url_encode()` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `b64url_encode()` è `b64url_encode()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `b64url_encode()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `b64url_encode()` è `rve_transactions_common_b64url_encode`. Nel grafo unificato, il nodo canonico `b64url_encode()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_b64url_encode`.
<!-- GRAPH_EDGE graph=unificata index=389 role=outgoing -->
Nel grafo unificato, il nodo canonico `b64url_encode()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_mock_jwt()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_b64url_encode` all'ID `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt`. La relazione tecnica esplicita è la seguente: `b64url_encode()` chiama `build_mock_jwt()`. Nel grafo unificato, il tipo dichiarato dell'arco 389 che coinvolge il nodo canonico `b64url_encode()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 389 che coinvolge il nodo canonico `b64url_encode()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 389 che coinvolge il nodo canonico `b64url_encode()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 389 che coinvolge il nodo canonico `b64url_encode()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 389 che coinvolge il nodo canonico `b64url_encode()` è `L162`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 389 che coinvolge il nodo canonico `b64url_encode()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 389 che coinvolge il nodo canonico `b64url_encode()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 389 che coinvolge il nodo canonico `b64url_encode()` è `1.0`. Nel grafo unificato, l'arco 389 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_b64url_encode` per la relazione che coinvolge il nodo canonico `b64url_encode()`. Nel grafo unificato, l'arco 389 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `b64url_encode()`.
<!-- GRAPH_EDGE graph=unificata index=390 role=outgoing -->
Nel grafo unificato, il nodo canonico `b64url_encode()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Base64url senza padding — usato nei JWT.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_b64url_encode` all'ID `Request-Dataset-Generator::rve_transactions_common_rationale_119`. La relazione tecnica esplicita è la seguente: `b64url_encode()` costituisce una motivazione per `Base64url senza padding — usato nei JWT.`. Nel grafo unificato, il tipo dichiarato dell'arco 390 che coinvolge il nodo canonico `b64url_encode()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 390 che coinvolge il nodo canonico `b64url_encode()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 390 che coinvolge il nodo canonico `b64url_encode()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 390 che coinvolge il nodo canonico `b64url_encode()` è `L119`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 390 che coinvolge il nodo canonico `b64url_encode()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 390 che coinvolge il nodo canonico `b64url_encode()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 390 che coinvolge il nodo canonico `b64url_encode()` è `1.0`. Nel grafo unificato, l'arco 390 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_b64url_encode` per la relazione che coinvolge il nodo canonico `b64url_encode()`. Nel grafo unificato, l'arco 390 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_rationale_119` per la relazione che coinvolge il nodo canonico `b64url_encode()`.
<!-- GRAPH_EDGE graph=unificata index=341 role=incoming -->
Nel grafo unificato, il nodo canonico `b64url_encode()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_b64url_encode`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `b64url_encode()`. Nel grafo unificato, il tipo dichiarato dell'arco 341 che coinvolge il nodo canonico `b64url_encode()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 341 che coinvolge il nodo canonico `b64url_encode()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 341 che coinvolge il nodo canonico `b64url_encode()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 341 che coinvolge il nodo canonico `b64url_encode()` è `L118`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 341 che coinvolge il nodo canonico `b64url_encode()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 341 che coinvolge il nodo canonico `b64url_encode()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 341 che coinvolge il nodo canonico `b64url_encode()` è `1.0`. Nel grafo unificato, l'arco 341 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `b64url_encode()`. Nel grafo unificato, l'arco 341 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_b64url_encode` per la relazione che coinvolge il nodo canonico `b64url_encode()`.

### Base64 decode → stringa UTF-8.

<!-- GRAPH_NODE graph=codice index=179 id=rve_transactions_common_rationale_73 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Base64 decode → stringa UTF-8.` è `Base64 decode → stringa UTF-8.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Base64 decode → stringa UTF-8.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Base64 decode → stringa UTF-8.` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Base64 decode → stringa UTF-8.` è `L73`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Base64 decode → stringa UTF-8.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Base64 decode → stringa UTF-8.` possiede l'identificatore esatto `rve_transactions_common_rationale_73`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Base64 decode → stringa UTF-8.` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Base64 decode → stringa UTF-8.` è `base64 decode → stringa utf-8.`.
<!-- GRAPH_EDGE graph=codice index=384 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Base64 decode → stringa UTF-8.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `b64_decode()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_rationale_73` all'ID `rve_transactions_common_b64_decode`. La relazione tecnica esplicita è la seguente: `Base64 decode → stringa UTF-8.` costituisce una motivazione per `b64_decode()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 384 che coinvolge il nodo canonico `Base64 decode → stringa UTF-8.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 384 che coinvolge il nodo canonico `Base64 decode → stringa UTF-8.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 384 che coinvolge il nodo canonico `Base64 decode → stringa UTF-8.` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 384 che coinvolge il nodo canonico `Base64 decode → stringa UTF-8.` è `L73`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 384 che coinvolge il nodo canonico `Base64 decode → stringa UTF-8.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 384 che coinvolge il nodo canonico `Base64 decode → stringa UTF-8.` è `ast`. Nel grafo del codice RDG, l'arco 384 registra come `source` l'identificatore `rve_transactions_common_rationale_73` per la relazione che coinvolge il nodo canonico `Base64 decode → stringa UTF-8.`. Nel grafo del codice RDG, l'arco 384 registra come `target` l'identificatore `rve_transactions_common_b64_decode` per la relazione che coinvolge il nodo canonico `Base64 decode → stringa UTF-8.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 384 che coinvolge il nodo canonico `Base64 decode → stringa UTF-8.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Base64 decode → stringa UTF-8.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=179 id=Request-Dataset-Generator::rve_transactions_common_rationale_73 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Base64 decode → stringa UTF-8.` è `Base64 decode → stringa UTF-8.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Base64 decode → stringa UTF-8.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Base64 decode → stringa UTF-8.` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Base64 decode → stringa UTF-8.` è `L73`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Base64 decode → stringa UTF-8.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Base64 decode → stringa UTF-8.` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Base64 decode → stringa UTF-8.` è `base64 decode → stringa utf-8.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Base64 decode → stringa UTF-8.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Base64 decode → stringa UTF-8.` è `rve_transactions_common_rationale_73`. Nel grafo unificato, il nodo canonico `Base64 decode → stringa UTF-8.` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_rationale_73`.
Nel grafo unificato, il nodo canonico `Base64 decode → stringa UTF-8.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=384 role=incoming -->
Nel grafo unificato, il nodo canonico `Base64 decode → stringa UTF-8.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `b64_decode()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_b64_decode` all'ID `Request-Dataset-Generator::rve_transactions_common_rationale_73`. La relazione tecnica esplicita è la seguente: `b64_decode()` costituisce una motivazione per `Base64 decode → stringa UTF-8.`. Nel grafo unificato, il tipo dichiarato dell'arco 384 che coinvolge il nodo canonico `Base64 decode → stringa UTF-8.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 384 che coinvolge il nodo canonico `Base64 decode → stringa UTF-8.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 384 che coinvolge il nodo canonico `Base64 decode → stringa UTF-8.` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 384 che coinvolge il nodo canonico `Base64 decode → stringa UTF-8.` è `L73`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 384 che coinvolge il nodo canonico `Base64 decode → stringa UTF-8.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 384 che coinvolge il nodo canonico `Base64 decode → stringa UTF-8.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 384 che coinvolge il nodo canonico `Base64 decode → stringa UTF-8.` è `1.0`. Nel grafo unificato, l'arco 384 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_b64_decode` per la relazione che coinvolge il nodo canonico `Base64 decode → stringa UTF-8.`. Nel grafo unificato, l'arco 384 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_rationale_73` per la relazione che coinvolge il nodo canonico `Base64 decode → stringa UTF-8.`.

### Base64url senza padding — usato nei JWT.

<!-- GRAPH_NODE graph=codice index=181 id=rve_transactions_common_rationale_119 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Base64url senza padding — usato nei JWT.` è `Base64url senza padding — usato nei JWT.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Base64url senza padding — usato nei JWT.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Base64url senza padding — usato nei JWT.` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Base64url senza padding — usato nei JWT.` è `L119`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Base64url senza padding — usato nei JWT.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Base64url senza padding — usato nei JWT.` possiede l'identificatore esatto `rve_transactions_common_rationale_119`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Base64url senza padding — usato nei JWT.` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Base64url senza padding — usato nei JWT.` è `base64url senza padding — usato nei jwt.`.
<!-- GRAPH_EDGE graph=codice index=390 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Base64url senza padding — usato nei JWT.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `b64url_encode()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_rationale_119` all'ID `rve_transactions_common_b64url_encode`. La relazione tecnica esplicita è la seguente: `Base64url senza padding — usato nei JWT.` costituisce una motivazione per `b64url_encode()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 390 che coinvolge il nodo canonico `Base64url senza padding — usato nei JWT.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 390 che coinvolge il nodo canonico `Base64url senza padding — usato nei JWT.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 390 che coinvolge il nodo canonico `Base64url senza padding — usato nei JWT.` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 390 che coinvolge il nodo canonico `Base64url senza padding — usato nei JWT.` è `L119`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 390 che coinvolge il nodo canonico `Base64url senza padding — usato nei JWT.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 390 che coinvolge il nodo canonico `Base64url senza padding — usato nei JWT.` è `ast`. Nel grafo del codice RDG, l'arco 390 registra come `source` l'identificatore `rve_transactions_common_rationale_119` per la relazione che coinvolge il nodo canonico `Base64url senza padding — usato nei JWT.`. Nel grafo del codice RDG, l'arco 390 registra come `target` l'identificatore `rve_transactions_common_b64url_encode` per la relazione che coinvolge il nodo canonico `Base64url senza padding — usato nei JWT.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 390 che coinvolge il nodo canonico `Base64url senza padding — usato nei JWT.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Base64url senza padding — usato nei JWT.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=181 id=Request-Dataset-Generator::rve_transactions_common_rationale_119 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Base64url senza padding — usato nei JWT.` è `Base64url senza padding — usato nei JWT.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Base64url senza padding — usato nei JWT.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Base64url senza padding — usato nei JWT.` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Base64url senza padding — usato nei JWT.` è `L119`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Base64url senza padding — usato nei JWT.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Base64url senza padding — usato nei JWT.` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Base64url senza padding — usato nei JWT.` è `base64url senza padding — usato nei jwt.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Base64url senza padding — usato nei JWT.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Base64url senza padding — usato nei JWT.` è `rve_transactions_common_rationale_119`. Nel grafo unificato, il nodo canonico `Base64url senza padding — usato nei JWT.` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_rationale_119`.
Nel grafo unificato, il nodo canonico `Base64url senza padding — usato nei JWT.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=390 role=incoming -->
Nel grafo unificato, il nodo canonico `Base64url senza padding — usato nei JWT.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `b64url_encode()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_b64url_encode` all'ID `Request-Dataset-Generator::rve_transactions_common_rationale_119`. La relazione tecnica esplicita è la seguente: `b64url_encode()` costituisce una motivazione per `Base64url senza padding — usato nei JWT.`. Nel grafo unificato, il tipo dichiarato dell'arco 390 che coinvolge il nodo canonico `Base64url senza padding — usato nei JWT.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 390 che coinvolge il nodo canonico `Base64url senza padding — usato nei JWT.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 390 che coinvolge il nodo canonico `Base64url senza padding — usato nei JWT.` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 390 che coinvolge il nodo canonico `Base64url senza padding — usato nei JWT.` è `L119`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 390 che coinvolge il nodo canonico `Base64url senza padding — usato nei JWT.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 390 che coinvolge il nodo canonico `Base64url senza padding — usato nei JWT.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 390 che coinvolge il nodo canonico `Base64url senza padding — usato nei JWT.` è `1.0`. Nel grafo unificato, l'arco 390 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_b64url_encode` per la relazione che coinvolge il nodo canonico `Base64url senza padding — usato nei JWT.`. Nel grafo unificato, l'arco 390 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_rationale_119` per la relazione che coinvolge il nodo canonico `Base64url senza padding — usato nei JWT.`.

### build_mock_jwt()

<!-- GRAPH_NODE graph=codice index=173 id=rve_transactions_common_build_mock_jwt -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `build_mock_jwt()` è `build_mock_jwt()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `build_mock_jwt()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `build_mock_jwt()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `build_mock_jwt()` è `L131`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `build_mock_jwt()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `build_mock_jwt()` possiede l'identificatore esatto `rve_transactions_common_build_mock_jwt`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `build_mock_jwt()` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `build_mock_jwt()` è `build_mock_jwt()`.
<!-- GRAPH_EDGE graph=codice index=359 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_mock_jwt()` è la sorgente `source` della relazione `calls` verso il nodo canonico `utc_now()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_build_mock_jwt` all'ID `rve_transactions_common_utc_now`. La relazione tecnica esplicita è la seguente: `build_mock_jwt()` chiama `utc_now()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 359 che coinvolge il nodo canonico `build_mock_jwt()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 359 che coinvolge il nodo canonico `build_mock_jwt()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 359 che coinvolge il nodo canonico `build_mock_jwt()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 359 che coinvolge il nodo canonico `build_mock_jwt()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 359 che coinvolge il nodo canonico `build_mock_jwt()` è `L146`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 359 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 359 che coinvolge il nodo canonico `build_mock_jwt()` è `ast`. Nel grafo del codice RDG, l'arco 359 registra come `source` l'identificatore `rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo del codice RDG, l'arco 359 registra come `target` l'identificatore `rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 359 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=371 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_mock_jwt()` è la sorgente `source` della relazione `calls` verso il nodo canonico `new_uuid()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_build_mock_jwt` all'ID `rve_transactions_common_new_uuid`. La relazione tecnica esplicita è la seguente: `build_mock_jwt()` chiama `new_uuid()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 371 che coinvolge il nodo canonico `build_mock_jwt()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 371 che coinvolge il nodo canonico `build_mock_jwt()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 371 che coinvolge il nodo canonico `build_mock_jwt()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 371 che coinvolge il nodo canonico `build_mock_jwt()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 371 che coinvolge il nodo canonico `build_mock_jwt()` è `L164`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 371 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 371 che coinvolge il nodo canonico `build_mock_jwt()` è `ast`. Nel grafo del codice RDG, l'arco 371 registra come `source` l'identificatore `rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo del codice RDG, l'arco 371 registra come `target` l'identificatore `rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 371 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=389 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_mock_jwt()` è la sorgente `source` della relazione `calls` verso il nodo canonico `b64url_encode()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_build_mock_jwt` all'ID `rve_transactions_common_b64url_encode`. La relazione tecnica esplicita è la seguente: `build_mock_jwt()` chiama `b64url_encode()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 389 che coinvolge il nodo canonico `build_mock_jwt()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 389 che coinvolge il nodo canonico `build_mock_jwt()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 389 che coinvolge il nodo canonico `build_mock_jwt()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 389 che coinvolge il nodo canonico `build_mock_jwt()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 389 che coinvolge il nodo canonico `build_mock_jwt()` è `L162`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 389 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 389 che coinvolge il nodo canonico `build_mock_jwt()` è `ast`. Nel grafo del codice RDG, l'arco 389 registra come `source` l'identificatore `rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo del codice RDG, l'arco 389 registra come `target` l'identificatore `rve_transactions_common_b64url_encode` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 389 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=342 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_mock_jwt()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_build_mock_jwt`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `build_mock_jwt()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 342 che coinvolge il nodo canonico `build_mock_jwt()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 342 che coinvolge il nodo canonico `build_mock_jwt()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 342 che coinvolge il nodo canonico `build_mock_jwt()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 342 che coinvolge il nodo canonico `build_mock_jwt()` è `L131`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 342 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 342 che coinvolge il nodo canonico `build_mock_jwt()` è `ast`. Nel grafo del codice RDG, l'arco 342 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo del codice RDG, l'arco 342 registra come `target` l'identificatore `rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 342 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=391 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_mock_jwt()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_rationale_139` all'ID `rve_transactions_common_build_mock_jwt`. La relazione tecnica esplicita è la seguente: `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` costituisce una motivazione per `build_mock_jwt()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 391 che coinvolge il nodo canonico `build_mock_jwt()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 391 che coinvolge il nodo canonico `build_mock_jwt()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 391 che coinvolge il nodo canonico `build_mock_jwt()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 391 che coinvolge il nodo canonico `build_mock_jwt()` è `L139`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 391 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 391 che coinvolge il nodo canonico `build_mock_jwt()` è `ast`. Nel grafo del codice RDG, l'arco 391 registra come `source` l'identificatore `rve_transactions_common_rationale_139` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo del codice RDG, l'arco 391 registra come `target` l'identificatore `rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 391 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=392 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_mock_jwt()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `rve_121.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121` all'ID `rve_transactions_common_build_mock_jwt`. La relazione tecnica esplicita è la seguente: `rve_121.py` importa `build_mock_jwt()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 392 che coinvolge il nodo canonico `build_mock_jwt()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 392 che coinvolge il nodo canonico `build_mock_jwt()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 392 che coinvolge il nodo canonico `build_mock_jwt()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 392 che coinvolge il nodo canonico `build_mock_jwt()` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 392 che coinvolge il nodo canonico `build_mock_jwt()` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 392 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 392 che coinvolge il nodo canonico `build_mock_jwt()` è `ast`. Nel grafo del codice RDG, l'arco 392 registra come `source` l'identificatore `rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo del codice RDG, l'arco 392 registra come `target` l'identificatore `rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 392 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=393 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_mock_jwt()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_request() [rve_transactions_rve_121_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121_build_request` all'ID `rve_transactions_common_build_mock_jwt`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_121_build_request]` chiama `build_mock_jwt()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 393 che coinvolge il nodo canonico `build_mock_jwt()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 393 che coinvolge il nodo canonico `build_mock_jwt()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 393 che coinvolge il nodo canonico `build_mock_jwt()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 393 che coinvolge il nodo canonico `build_mock_jwt()` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 393 che coinvolge il nodo canonico `build_mock_jwt()` è `L127`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 393 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 393 che coinvolge il nodo canonico `build_mock_jwt()` è `ast`. Nel grafo del codice RDG, l'arco 393 registra come `source` l'identificatore `rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo del codice RDG, l'arco 393 registra come `target` l'identificatore `rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 393 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=173 id=Request-Dataset-Generator::rve_transactions_common_build_mock_jwt -->
Nel grafo unificato, l'etichetta originale del nodo canonico `build_mock_jwt()` è `build_mock_jwt()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `build_mock_jwt()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `build_mock_jwt()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `build_mock_jwt()` è `L131`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `build_mock_jwt()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `build_mock_jwt()` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `build_mock_jwt()` è `build_mock_jwt()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `build_mock_jwt()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `build_mock_jwt()` è `rve_transactions_common_build_mock_jwt`. Nel grafo unificato, il nodo canonico `build_mock_jwt()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt`.
<!-- GRAPH_EDGE graph=unificata index=391 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_mock_jwt()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt` all'ID `Request-Dataset-Generator::rve_transactions_common_rationale_139`. La relazione tecnica esplicita è la seguente: `build_mock_jwt()` costituisce una motivazione per `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).`. Nel grafo unificato, il tipo dichiarato dell'arco 391 che coinvolge il nodo canonico `build_mock_jwt()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 391 che coinvolge il nodo canonico `build_mock_jwt()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 391 che coinvolge il nodo canonico `build_mock_jwt()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 391 che coinvolge il nodo canonico `build_mock_jwt()` è `L139`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 391 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 391 che coinvolge il nodo canonico `build_mock_jwt()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 391 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo unificato, l'arco 391 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo unificato, l'arco 391 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_rationale_139` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`.
<!-- GRAPH_EDGE graph=unificata index=392 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_mock_jwt()` è la sorgente `source` della relazione `imports` verso il nodo canonico `rve_121.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt` all'ID `Request-Dataset-Generator::rve_transactions_rve_121`. La relazione tecnica esplicita è la seguente: `build_mock_jwt()` importa `rve_121.py`. Nel grafo unificato, il tipo dichiarato dell'arco 392 che coinvolge il nodo canonico `build_mock_jwt()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 392 che coinvolge il nodo canonico `build_mock_jwt()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 392 che coinvolge il nodo canonico `build_mock_jwt()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 392 che coinvolge il nodo canonico `build_mock_jwt()` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 392 che coinvolge il nodo canonico `build_mock_jwt()` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 392 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 392 che coinvolge il nodo canonico `build_mock_jwt()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 392 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo unificato, l'arco 392 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo unificato, l'arco 392 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`.
<!-- GRAPH_EDGE graph=unificata index=393 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_mock_jwt()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt` all'ID `Request-Dataset-Generator::rve_transactions_rve_121_build_request`. La relazione tecnica esplicita è la seguente: `build_mock_jwt()` chiama `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 393 che coinvolge il nodo canonico `build_mock_jwt()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 393 che coinvolge il nodo canonico `build_mock_jwt()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 393 che coinvolge il nodo canonico `build_mock_jwt()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 393 che coinvolge il nodo canonico `build_mock_jwt()` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 393 che coinvolge il nodo canonico `build_mock_jwt()` è `L127`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 393 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 393 che coinvolge il nodo canonico `build_mock_jwt()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 393 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo unificato, l'arco 393 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo unificato, l'arco 393 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`.
<!-- GRAPH_EDGE graph=unificata index=342 role=incoming -->
Nel grafo unificato, il nodo canonico `build_mock_jwt()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `build_mock_jwt()`. Nel grafo unificato, il tipo dichiarato dell'arco 342 che coinvolge il nodo canonico `build_mock_jwt()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 342 che coinvolge il nodo canonico `build_mock_jwt()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 342 che coinvolge il nodo canonico `build_mock_jwt()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 342 che coinvolge il nodo canonico `build_mock_jwt()` è `L131`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 342 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 342 che coinvolge il nodo canonico `build_mock_jwt()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 342 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo unificato, l'arco 342 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo unificato, l'arco 342 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`.
<!-- GRAPH_EDGE graph=unificata index=359 role=incoming -->
Nel grafo unificato, il nodo canonico `build_mock_jwt()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `utc_now()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_utc_now` all'ID `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt`. La relazione tecnica esplicita è la seguente: `utc_now()` chiama `build_mock_jwt()`. Nel grafo unificato, il tipo dichiarato dell'arco 359 che coinvolge il nodo canonico `build_mock_jwt()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 359 che coinvolge il nodo canonico `build_mock_jwt()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 359 che coinvolge il nodo canonico `build_mock_jwt()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 359 che coinvolge il nodo canonico `build_mock_jwt()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 359 che coinvolge il nodo canonico `build_mock_jwt()` è `L146`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 359 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 359 che coinvolge il nodo canonico `build_mock_jwt()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 359 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo unificato, l'arco 359 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo unificato, l'arco 359 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`.
<!-- GRAPH_EDGE graph=unificata index=371 role=incoming -->
Nel grafo unificato, il nodo canonico `build_mock_jwt()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `new_uuid()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_new_uuid` all'ID `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt`. La relazione tecnica esplicita è la seguente: `new_uuid()` chiama `build_mock_jwt()`. Nel grafo unificato, il tipo dichiarato dell'arco 371 che coinvolge il nodo canonico `build_mock_jwt()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 371 che coinvolge il nodo canonico `build_mock_jwt()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 371 che coinvolge il nodo canonico `build_mock_jwt()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 371 che coinvolge il nodo canonico `build_mock_jwt()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 371 che coinvolge il nodo canonico `build_mock_jwt()` è `L164`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 371 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 371 che coinvolge il nodo canonico `build_mock_jwt()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 371 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo unificato, l'arco 371 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo unificato, l'arco 371 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`.
<!-- GRAPH_EDGE graph=unificata index=389 role=incoming -->
Nel grafo unificato, il nodo canonico `build_mock_jwt()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `b64url_encode()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_b64url_encode` all'ID `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt`. La relazione tecnica esplicita è la seguente: `b64url_encode()` chiama `build_mock_jwt()`. Nel grafo unificato, il tipo dichiarato dell'arco 389 che coinvolge il nodo canonico `build_mock_jwt()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 389 che coinvolge il nodo canonico `build_mock_jwt()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 389 che coinvolge il nodo canonico `build_mock_jwt()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 389 che coinvolge il nodo canonico `build_mock_jwt()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 389 che coinvolge il nodo canonico `build_mock_jwt()` è `L162`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 389 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 389 che coinvolge il nodo canonico `build_mock_jwt()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 389 che coinvolge il nodo canonico `build_mock_jwt()` è `1.0`. Nel grafo unificato, l'arco 389 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_b64url_encode` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`. Nel grafo unificato, l'arco 389 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `build_mock_jwt()`.

### build_request() [rve_transactions_rve_121_build_request]

<!-- GRAPH_NODE graph=codice index=209 id=rve_transactions_rve_121_build_request -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `build_request()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `L32`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` possiede l'identificatore esatto `rve_transactions_rve_121_build_request`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `build_request()`.
<!-- GRAPH_EDGE graph=codice index=373 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è la sorgente `source` della relazione `calls` verso il nodo canonico `new_uuid()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121_build_request` all'ID `rve_transactions_common_new_uuid`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_121_build_request]` chiama `new_uuid()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 373 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 373 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 373 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 373 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 373 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `L112`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 373 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 373 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 373 registra come `source` l'identificatore `rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo del codice RDG, l'arco 373 registra come `target` l'identificatore `rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 373 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=393 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_mock_jwt()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121_build_request` all'ID `rve_transactions_common_build_mock_jwt`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_121_build_request]` chiama `build_mock_jwt()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 393 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 393 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 393 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 393 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 393 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `L127`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 393 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 393 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 393 registra come `source` l'identificatore `rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo del codice RDG, l'arco 393 registra come `target` l'identificatore `rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 393 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=396 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è la sorgente `source` della relazione `calls` verso il nodo canonico `extract_patient_cf()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121_build_request` all'ID `rve_transactions_common_extract_patient_cf`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_121_build_request]` chiama `extract_patient_cf()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 396 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 396 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 396 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 396 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 396 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `L66`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 396 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 396 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 396 registra come `source` l'identificatore `rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo del codice RDG, l'arco 396 registra come `target` l'identificatore `rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 396 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=402 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è la sorgente `source` della relazione `calls` verso il nodo canonico `extract_patient_mpi()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121_build_request` all'ID `rve_transactions_common_extract_patient_mpi`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_121_build_request]` chiama `extract_patient_mpi()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 402 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 402 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 402 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 402 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 402 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `L67`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 402 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 402 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 402 registra come `source` l'identificatore `rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo del codice RDG, l'arco 402 registra come `target` l'identificatore `rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 402 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=456 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_121.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121` all'ID `rve_transactions_rve_121_build_request`. La relazione tecnica esplicita è la seguente: `rve_121.py` contiene `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 456 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 456 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 456 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 456 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `L32`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 456 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 456 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 456 registra come `source` l'identificatore `rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo del codice RDG, l'arco 456 registra come `target` l'identificatore `rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 456 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=458 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121_rationale_39` all'ID `rve_transactions_rve_121_build_request`. La relazione tecnica esplicita è la seguente: `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` costituisce una motivazione per `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 458 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 458 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 458 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 458 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `L39`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 458 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 458 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 458 registra come `source` l'identificatore `rve_transactions_rve_121_rationale_39` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo del codice RDG, l'arco 458 registra come `target` l'identificatore `rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 458 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=209 id=Request-Dataset-Generator::rve_transactions_rve_121_build_request -->
Nel grafo unificato, l'etichetta originale del nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `build_request()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `L32`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `build_request()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `rve_transactions_rve_121_build_request`. Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_121_build_request`.
<!-- GRAPH_EDGE graph=unificata index=458 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_121_build_request` all'ID `Request-Dataset-Generator::rve_transactions_rve_121_rationale_39`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_121_build_request]` costituisce una motivazione per `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:`. Nel grafo unificato, il tipo dichiarato dell'arco 458 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 458 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 458 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 458 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `L39`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 458 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 458 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 458 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`. Nel grafo unificato, l'arco 458 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo unificato, l'arco 458 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121_rationale_39` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=373 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `new_uuid()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_new_uuid` all'ID `Request-Dataset-Generator::rve_transactions_rve_121_build_request`. La relazione tecnica esplicita è la seguente: `new_uuid()` chiama `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 373 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 373 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 373 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 373 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 373 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `L112`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 373 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 373 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 373 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`. Nel grafo unificato, l'arco 373 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo unificato, l'arco 373 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=393 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_mock_jwt()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt` all'ID `Request-Dataset-Generator::rve_transactions_rve_121_build_request`. La relazione tecnica esplicita è la seguente: `build_mock_jwt()` chiama `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 393 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 393 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 393 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 393 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 393 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `L127`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 393 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 393 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 393 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`. Nel grafo unificato, l'arco 393 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo unificato, l'arco 393 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=396 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `extract_patient_cf()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` all'ID `Request-Dataset-Generator::rve_transactions_rve_121_build_request`. La relazione tecnica esplicita è la seguente: `extract_patient_cf()` chiama `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 396 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 396 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 396 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 396 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 396 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `L66`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 396 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 396 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 396 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`. Nel grafo unificato, l'arco 396 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo unificato, l'arco 396 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=402 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `extract_patient_mpi()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` all'ID `Request-Dataset-Generator::rve_transactions_rve_121_build_request`. La relazione tecnica esplicita è la seguente: `extract_patient_mpi()` chiama `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 402 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 402 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 402 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 402 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 402 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `L67`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 402 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 402 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 402 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`. Nel grafo unificato, l'arco 402 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo unificato, l'arco 402 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=456 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_121.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_121` all'ID `Request-Dataset-Generator::rve_transactions_rve_121_build_request`. La relazione tecnica esplicita è la seguente: `rve_121.py` contiene `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 456 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 456 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 456 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 456 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `L32`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 456 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 456 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 456 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]` è `1.0`. Nel grafo unificato, l'arco 456 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo unificato, l'arco 456 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`.

### build_request() [rve_transactions_rve_1b_build_request]

<!-- GRAPH_NODE graph=codice index=224 id=rve_transactions_rve_1b_build_request -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `build_request()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L301`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` possiede l'identificatore esatto `rve_transactions_rve_1b_build_request`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `build_request()`.
<!-- GRAPH_EDGE graph=codice index=364 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è la sorgente `source` della relazione `calls` verso il nodo canonico `utc_now()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_common_utc_now`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `utc_now()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 364 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 364 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 364 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 364 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 364 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L333`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 364 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 364 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 364 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, l'arco 364 registra come `target` l'identificatore `rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 364 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=375 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è la sorgente `source` della relazione `calls` verso il nodo canonico `new_uuid()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_common_new_uuid`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `new_uuid()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 375 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 375 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 375 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 375 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 375 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L342`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 375 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 375 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 375 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, l'arco 375 registra come `target` l'identificatore `rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 375 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=378 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è la sorgente `source` della relazione `calls` verso il nodo canonico `msg_id_to_saml_id()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_common_msg_id_to_saml_id`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `msg_id_to_saml_id()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 378 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 378 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 378 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 378 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 378 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L343`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 378 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 378 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 378 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, l'arco 378 registra come `target` l'identificatore `rve_transactions_common_msg_id_to_saml_id` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 378 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=381 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è la sorgente `source` della relazione `calls` verso il nodo canonico `b64_encode()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_common_b64_encode`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `b64_encode()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 381 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 381 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 381 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 381 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 381 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L435`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 381 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 381 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 381 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, l'arco 381 registra come `target` l'identificatore `rve_transactions_common_b64_encode` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 381 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=388 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è la sorgente `source` della relazione `calls` verso il nodo canonico `linearize_xml()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_common_linearize_xml`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `linearize_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 388 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 388 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 388 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 388 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 388 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L421`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 388 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 388 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 388 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, l'arco 388 registra come `target` l'identificatore `rve_transactions_common_linearize_xml` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 388 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=472 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_profile_name()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_rve_1b_profile_name`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `_profile_name()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 472 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 472 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 472 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 472 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 472 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L322`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 472 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 472 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 472 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, l'arco 472 registra come `target` l'identificatore `rve_transactions_rve_1b_profile_name` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 472 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=473 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_resolve_profile()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_rve_1b_resolve_profile`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `_resolve_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 473 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 473 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 473 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 473 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 473 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L323`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 473 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 473 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 473 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, l'arco 473 registra come `target` l'identificatore `rve_transactions_rve_1b_resolve_profile` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 473 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=474 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_format_timestamp()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_rve_1b_format_timestamp`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `_format_timestamp()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 474 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 474 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 474 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 474 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 474 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L334`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 474 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 474 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 474 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, l'arco 474 registra come `target` l'identificatore `rve_transactions_rve_1b_format_timestamp` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 474 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=476 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_build_soap_request()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_rve_1b_build_soap_request`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `_build_soap_request()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 476 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 476 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 476 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 476 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 476 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L345`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 476 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 476 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 476 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, l'arco 476 registra come `target` l'identificatore `rve_transactions_rve_1b_build_soap_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 476 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=477 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_build_mock_assertion()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_rve_1b_build_mock_assertion`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `_build_mock_assertion()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 477 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 477 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 477 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 477 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 477 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L410`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 477 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 477 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 477 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, l'arco 477 registra come `target` l'identificatore `rve_transactions_rve_1b_build_mock_assertion` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 477 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=479 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_build_mock_soap_response()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_rve_1b_build_mock_soap_response`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `_build_mock_soap_response()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 479 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 479 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 479 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 479 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 479 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L422`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 479 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 479 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 479 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, l'arco 479 registra come `target` l'identificatore `rve_transactions_rve_1b_build_mock_soap_response` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 479 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=464 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 464 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 464 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 464 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 464 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L301`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 464 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 464 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 464 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, l'arco 464 registra come `target` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 464 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=481 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_rationale_310` all'ID `rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` costituisce una motivazione per `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 481 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 481 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 481 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 481 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L310`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 481 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 481 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 481 registra come `source` l'identificatore `rve_transactions_rve_1b_rationale_310` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, l'arco 481 registra come `target` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 481 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=224 id=Request-Dataset-Generator::rve_transactions_rve_1b_build_request -->
Nel grafo unificato, l'etichetta originale del nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `build_request()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L301`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `build_request()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions_rve_1b_build_request`. Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`.
<!-- GRAPH_EDGE graph=unificata index=481 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_310`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` costituisce una motivazione per `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob`. Nel grafo unificato, il tipo dichiarato dell'arco 481 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 481 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 481 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 481 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L310`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 481 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 481 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 481 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, l'arco 481 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, l'arco 481 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_310` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=364 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `utc_now()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_utc_now` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `utc_now()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 364 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 364 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 364 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 364 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 364 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L333`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 364 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 364 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 364 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, l'arco 364 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, l'arco 364 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=375 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `new_uuid()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_new_uuid` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `new_uuid()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 375 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 375 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 375 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 375 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 375 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L342`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 375 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 375 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 375 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, l'arco 375 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, l'arco 375 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=378 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `msg_id_to_saml_id()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_msg_id_to_saml_id` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `msg_id_to_saml_id()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 378 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 378 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 378 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 378 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 378 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L343`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 378 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 378 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 378 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, l'arco 378 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_msg_id_to_saml_id` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, l'arco 378 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=381 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `b64_encode()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_b64_encode` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `b64_encode()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 381 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 381 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 381 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 381 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 381 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L435`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 381 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 381 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 381 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, l'arco 381 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_b64_encode` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, l'arco 381 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=388 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `linearize_xml()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_linearize_xml` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `linearize_xml()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 388 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 388 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 388 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 388 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 388 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L421`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 388 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 388 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 388 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, l'arco 388 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_linearize_xml` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, l'arco 388 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=464 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 464 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 464 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 464 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 464 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L301`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 464 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 464 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 464 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, l'arco 464 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, l'arco 464 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=472 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_profile_name()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b_profile_name` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `_profile_name()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 472 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 472 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 472 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 472 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 472 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L322`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 472 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 472 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 472 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, l'arco 472 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_profile_name` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, l'arco 472 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=473 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_resolve_profile()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b_resolve_profile` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `_resolve_profile()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 473 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 473 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 473 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 473 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 473 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L323`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 473 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 473 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 473 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, l'arco 473 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_resolve_profile` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, l'arco 473 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=474 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_format_timestamp()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b_format_timestamp` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `_format_timestamp()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 474 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 474 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 474 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 474 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 474 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L334`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 474 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 474 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 474 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, l'arco 474 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_format_timestamp` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, l'arco 474 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=476 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_build_soap_request()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_soap_request` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `_build_soap_request()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 476 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 476 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 476 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 476 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 476 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L345`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 476 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 476 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 476 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, l'arco 476 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_soap_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, l'arco 476 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=477 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_build_mock_assertion()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_assertion` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `_build_mock_assertion()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 477 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 477 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 477 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 477 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 477 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L410`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 477 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 477 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 477 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, l'arco 477 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_assertion` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, l'arco 477 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=479 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_build_mock_soap_response()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_soap_response` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `_build_mock_soap_response()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 479 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 479 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 479 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 479 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 479 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `L422`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 479 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 479 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 479 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]` è `1.0`. Nel grafo unificato, l'arco 479 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_soap_response` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, l'arco 479 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`.

### build_request() [rve_transactions_rve_54_build_request]

<!-- GRAPH_NODE graph=codice index=230 id=rve_transactions_rve_54_build_request -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `build_request()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `L28`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` possiede l'identificatore esatto `rve_transactions_rve_54_build_request`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `build_request()`.
<!-- GRAPH_EDGE graph=codice index=398 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è la sorgente `source` della relazione `calls` verso il nodo canonico `extract_patient_cf()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54_build_request` all'ID `rve_transactions_common_extract_patient_cf`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_54_build_request]` chiama `extract_patient_cf()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 398 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 398 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 398 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 398 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 398 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `L69`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 398 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 398 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 398 registra come `source` l'identificatore `rve_transactions_rve_54_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo del codice RDG, l'arco 398 registra come `target` l'identificatore `rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 398 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=404 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è la sorgente `source` della relazione `calls` verso il nodo canonico `extract_patient_mpi()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54_build_request` all'ID `rve_transactions_common_extract_patient_mpi`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_54_build_request]` chiama `extract_patient_mpi()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 404 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 404 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 404 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 404 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 404 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `L70`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 404 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 404 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 404 registra come `source` l'identificatore `rve_transactions_rve_54_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo del codice RDG, l'arco 404 registra come `target` l'identificatore `rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 404 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=485 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_build_mock_searchset()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54_build_request` all'ID `rve_transactions_rve_54_build_mock_searchset`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_54_build_request]` chiama `_build_mock_searchset()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 485 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 485 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 485 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 485 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 485 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `L112`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 485 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 485 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 485 registra come `source` l'identificatore `rve_transactions_rve_54_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo del codice RDG, l'arco 485 registra come `target` l'identificatore `rve_transactions_rve_54_build_mock_searchset` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 485 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=483 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_54.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54` all'ID `rve_transactions_rve_54_build_request`. La relazione tecnica esplicita è la seguente: `rve_54.py` contiene `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 483 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 483 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 483 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 483 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 483 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 483 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 483 registra come `source` l'identificatore `rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo del codice RDG, l'arco 483 registra come `target` l'identificatore `rve_transactions_rve_54_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 483 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=486 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54_rationale_35` all'ID `rve_transactions_rve_54_build_request`. La relazione tecnica esplicita è la seguente: `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` costituisce una motivazione per `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 486 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 486 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 486 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 486 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `L35`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 486 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 486 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 486 registra come `source` l'identificatore `rve_transactions_rve_54_rationale_35` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo del codice RDG, l'arco 486 registra come `target` l'identificatore `rve_transactions_rve_54_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 486 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=230 id=Request-Dataset-Generator::rve_transactions_rve_54_build_request -->
Nel grafo unificato, l'etichetta originale del nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `build_request()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `L28`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `build_request()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `rve_transactions_rve_54_build_request`. Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_54_build_request`.
<!-- GRAPH_EDGE graph=unificata index=485 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_build_mock_searchset()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_54_build_request` all'ID `Request-Dataset-Generator::rve_transactions_rve_54_build_mock_searchset`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_54_build_request]` chiama `_build_mock_searchset()`. Nel grafo unificato, il tipo dichiarato dell'arco 485 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 485 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 485 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 485 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 485 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `L112`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 485 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 485 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 485 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `1.0`. Nel grafo unificato, l'arco 485 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo unificato, l'arco 485 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_build_mock_searchset` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=486 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_54_build_request` all'ID `Request-Dataset-Generator::rve_transactions_rve_54_rationale_35`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_54_build_request]` costituisce una motivazione per `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:`. Nel grafo unificato, il tipo dichiarato dell'arco 486 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 486 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 486 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 486 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `L35`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 486 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 486 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 486 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `1.0`. Nel grafo unificato, l'arco 486 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo unificato, l'arco 486 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_rationale_35` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=398 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `extract_patient_cf()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` all'ID `Request-Dataset-Generator::rve_transactions_rve_54_build_request`. La relazione tecnica esplicita è la seguente: `extract_patient_cf()` chiama `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 398 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 398 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 398 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 398 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 398 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `L69`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 398 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 398 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 398 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `1.0`. Nel grafo unificato, l'arco 398 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo unificato, l'arco 398 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=404 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `extract_patient_mpi()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` all'ID `Request-Dataset-Generator::rve_transactions_rve_54_build_request`. La relazione tecnica esplicita è la seguente: `extract_patient_mpi()` chiama `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 404 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 404 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 404 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 404 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 404 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `L70`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 404 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 404 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 404 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `1.0`. Nel grafo unificato, l'arco 404 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo unificato, l'arco 404 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=483 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_54.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_54` all'ID `Request-Dataset-Generator::rve_transactions_rve_54_build_request`. La relazione tecnica esplicita è la seguente: `rve_54.py` contiene `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 483 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 483 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 483 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 483 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 483 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 483 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 483 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]` è `1.0`. Nel grafo unificato, l'arco 483 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo unificato, l'arco 483 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`.

### Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.

<!-- GRAPH_NODE graph=codice index=177 id=rve_transactions_common_rationale_52 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `L52`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` possiede l'identificatore esatto `rve_transactions_common_rationale_52`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `converte un uuid in un saml id valido preservando il formato usato da soapui.`.
<!-- GRAPH_EDGE graph=codice index=376 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `msg_id_to_saml_id()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_rationale_52` all'ID `rve_transactions_common_msg_id_to_saml_id`. La relazione tecnica esplicita è la seguente: `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` costituisce una motivazione per `msg_id_to_saml_id()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 376 che coinvolge il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 376 che coinvolge il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 376 che coinvolge il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 376 che coinvolge il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `L52`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 376 che coinvolge il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 376 che coinvolge il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `ast`. Nel grafo del codice RDG, l'arco 376 registra come `source` l'identificatore `rve_transactions_common_rationale_52` per la relazione che coinvolge il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.`. Nel grafo del codice RDG, l'arco 376 registra come `target` l'identificatore `rve_transactions_common_msg_id_to_saml_id` per la relazione che coinvolge il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 376 che coinvolge il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=177 id=Request-Dataset-Generator::rve_transactions_common_rationale_52 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `L52`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `converte un uuid in un saml id valido preservando il formato usato da soapui.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `rve_transactions_common_rationale_52`. Nel grafo unificato, il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_rationale_52`.
Nel grafo unificato, il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=376 role=incoming -->
Nel grafo unificato, il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `msg_id_to_saml_id()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_msg_id_to_saml_id` all'ID `Request-Dataset-Generator::rve_transactions_common_rationale_52`. La relazione tecnica esplicita è la seguente: `msg_id_to_saml_id()` costituisce una motivazione per `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.`. Nel grafo unificato, il tipo dichiarato dell'arco 376 che coinvolge il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 376 che coinvolge il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 376 che coinvolge il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 376 che coinvolge il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `L52`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 376 che coinvolge il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 376 che coinvolge il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 376 che coinvolge il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` è `1.0`. Nel grafo unificato, l'arco 376 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_msg_id_to_saml_id` per la relazione che coinvolge il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.`. Nel grafo unificato, l'arco 376 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_rationale_52` per la relazione che coinvolge il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.`.

### Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob

<!-- GRAPH_NODE graph=codice index=228 id=rve_transactions_rve_1b_rationale_310 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `L310`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` possiede l'identificatore esatto `rve_transactions_rve_1b_rationale_310`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `costruisce lo step rve-1.b.      args:         config:       configurazione glob`.
<!-- GRAPH_EDGE graph=codice index=481 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_rationale_310` all'ID `rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` costituisce una motivazione per `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 481 che coinvolge il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 481 che coinvolge il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 481 che coinvolge il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 481 che coinvolge il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `L310`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 481 che coinvolge il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 481 che coinvolge il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `ast`. Nel grafo del codice RDG, l'arco 481 registra come `source` l'identificatore `rve_transactions_rve_1b_rationale_310` per la relazione che coinvolge il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob`. Nel grafo del codice RDG, l'arco 481 registra come `target` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 481 che coinvolge il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=228 id=Request-Dataset-Generator::rve_transactions_rve_1b_rationale_310 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `L310`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `costruisce lo step rve-1.b.      args:         config:       configurazione glob`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `rve_transactions_rve_1b_rationale_310`. Nel grafo unificato, il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_310`.
Nel grafo unificato, il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=481 role=incoming -->
Nel grafo unificato, il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_310`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` costituisce una motivazione per `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob`. Nel grafo unificato, il tipo dichiarato dell'arco 481 che coinvolge il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 481 che coinvolge il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 481 che coinvolge il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 481 che coinvolge il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `L310`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 481 che coinvolge il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 481 che coinvolge il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 481 che coinvolge il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob` è `1.0`. Nel grafo unificato, l'arco 481 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob`. Nel grafo unificato, l'arco 481 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_310` per la relazione che coinvolge il nodo canonico `Costruisce lo step RVE-1.b.      Args:         config:       configurazione glob`.

### Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:

<!-- GRAPH_NODE graph=codice index=211 id=rve_transactions_rve_121_rationale_39 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `L39`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` possiede l'identificatore esatto `rve_transactions_rve_121_rationale_39`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `costruisce lo step rve-121 rest getaccesstoken.      args:         config:`.
<!-- GRAPH_EDGE graph=codice index=458 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121_rationale_39` all'ID `rve_transactions_rve_121_build_request`. La relazione tecnica esplicita è la seguente: `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` costituisce una motivazione per `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 458 che coinvolge il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 458 che coinvolge il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 458 che coinvolge il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 458 che coinvolge il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `L39`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 458 che coinvolge il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 458 che coinvolge il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `ast`. Nel grafo del codice RDG, l'arco 458 registra come `source` l'identificatore `rve_transactions_rve_121_rationale_39` per la relazione che coinvolge il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:`. Nel grafo del codice RDG, l'arco 458 registra come `target` l'identificatore `rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 458 che coinvolge il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=211 id=Request-Dataset-Generator::rve_transactions_rve_121_rationale_39 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `L39`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `costruisce lo step rve-121 rest getaccesstoken.      args:         config:`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `rve_transactions_rve_121_rationale_39`. Nel grafo unificato, il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_121_rationale_39`.
Nel grafo unificato, il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=458 role=incoming -->
Nel grafo unificato, il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `build_request() [rve_transactions_rve_121_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_121_build_request` all'ID `Request-Dataset-Generator::rve_transactions_rve_121_rationale_39`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_121_build_request]` costituisce una motivazione per `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:`. Nel grafo unificato, il tipo dichiarato dell'arco 458 che coinvolge il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 458 che coinvolge il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 458 che coinvolge il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 458 che coinvolge il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `L39`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 458 che coinvolge il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 458 che coinvolge il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 458 che coinvolge il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:` è `1.0`. Nel grafo unificato, l'arco 458 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:`. Nel grafo unificato, l'arco 458 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121_rationale_39` per la relazione che coinvolge il nodo canonico `Costruisce lo step RVE-121 REST GetAccessToken.      Args:         config:`.

### Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:

<!-- GRAPH_NODE graph=codice index=233 id=rve_transactions_rve_54_rationale_35 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `L35`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` possiede l'identificatore esatto `rve_transactions_rve_54_rationale_35`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `costruisce lo step rve-54 rest patient query.      la strategia di ricerca e:`.
<!-- GRAPH_EDGE graph=codice index=486 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54_rationale_35` all'ID `rve_transactions_rve_54_build_request`. La relazione tecnica esplicita è la seguente: `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` costituisce una motivazione per `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 486 che coinvolge il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 486 che coinvolge il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 486 che coinvolge il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 486 che coinvolge il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `L35`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 486 che coinvolge il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 486 che coinvolge il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `ast`. Nel grafo del codice RDG, l'arco 486 registra come `source` l'identificatore `rve_transactions_rve_54_rationale_35` per la relazione che coinvolge il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:`. Nel grafo del codice RDG, l'arco 486 registra come `target` l'identificatore `rve_transactions_rve_54_build_request` per la relazione che coinvolge il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 486 che coinvolge il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=233 id=Request-Dataset-Generator::rve_transactions_rve_54_rationale_35 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `L35`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `costruisce lo step rve-54 rest patient query.      la strategia di ricerca e:`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `rve_transactions_rve_54_rationale_35`. Nel grafo unificato, il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_54_rationale_35`.
Nel grafo unificato, il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=486 role=incoming -->
Nel grafo unificato, il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `build_request() [rve_transactions_rve_54_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_54_build_request` all'ID `Request-Dataset-Generator::rve_transactions_rve_54_rationale_35`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_54_build_request]` costituisce una motivazione per `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:`. Nel grafo unificato, il tipo dichiarato dell'arco 486 che coinvolge il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 486 che coinvolge il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 486 che coinvolge il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 486 che coinvolge il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `L35`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 486 che coinvolge il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 486 che coinvolge il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 486 che coinvolge il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:` è `1.0`. Nel grafo unificato, l'arco 486 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_build_request` per la relazione che coinvolge il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:`. Nel grafo unificato, l'arco 486 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_rationale_35` per la relazione che coinvolge il nodo canonico `Costruisce lo step RVE-54 REST Patient Query.      La strategia di ricerca è:`.

### Costruisce un Bundle FHIR searchset di risposta mock.

<!-- GRAPH_NODE graph=codice index=234 id=rve_transactions_rve_54_rationale_124 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `Costruisce un Bundle FHIR searchset di risposta mock.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `L124`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` possiede l'identificatore esatto `rve_transactions_rve_54_rationale_124`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `costruisce un bundle fhir searchset di risposta mock.`.
<!-- GRAPH_EDGE graph=codice index=487 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `_build_mock_searchset()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54_rationale_124` all'ID `rve_transactions_rve_54_build_mock_searchset`. La relazione tecnica esplicita è la seguente: `Costruisce un Bundle FHIR searchset di risposta mock.` costituisce una motivazione per `_build_mock_searchset()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 487 che coinvolge il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 487 che coinvolge il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 487 che coinvolge il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 487 che coinvolge il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `L124`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 487 che coinvolge il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 487 che coinvolge il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `ast`. Nel grafo del codice RDG, l'arco 487 registra come `source` l'identificatore `rve_transactions_rve_54_rationale_124` per la relazione che coinvolge il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.`. Nel grafo del codice RDG, l'arco 487 registra come `target` l'identificatore `rve_transactions_rve_54_build_mock_searchset` per la relazione che coinvolge il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 487 che coinvolge il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=234 id=Request-Dataset-Generator::rve_transactions_rve_54_rationale_124 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `Costruisce un Bundle FHIR searchset di risposta mock.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `L124`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `costruisce un bundle fhir searchset di risposta mock.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `rve_transactions_rve_54_rationale_124`. Nel grafo unificato, il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_54_rationale_124`.
Nel grafo unificato, il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=487 role=incoming -->
Nel grafo unificato, il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `_build_mock_searchset()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_54_build_mock_searchset` all'ID `Request-Dataset-Generator::rve_transactions_rve_54_rationale_124`. La relazione tecnica esplicita è la seguente: `_build_mock_searchset()` costituisce una motivazione per `Costruisce un Bundle FHIR searchset di risposta mock.`. Nel grafo unificato, il tipo dichiarato dell'arco 487 che coinvolge il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 487 che coinvolge il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 487 che coinvolge il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 487 che coinvolge il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `L124`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 487 che coinvolge il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 487 che coinvolge il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 487 che coinvolge il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.` è `1.0`. Nel grafo unificato, l'arco 487 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_build_mock_searchset` per la relazione che coinvolge il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.`. Nel grafo unificato, l'arco 487 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_rationale_124` per la relazione che coinvolge il nodo canonico `Costruisce un Bundle FHIR searchset di risposta mock.`.

### Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).

<!-- GRAPH_NODE graph=codice index=182 id=rve_transactions_common_rationale_139 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `L139`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` possiede l'identificatore esatto `rve_transactions_common_rationale_139`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `costruisce un jwt mock strutturalmente corretto (non firmato con chiave reale).`.
<!-- GRAPH_EDGE graph=codice index=391 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `build_mock_jwt()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_rationale_139` all'ID `rve_transactions_common_build_mock_jwt`. La relazione tecnica esplicita è la seguente: `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` costituisce una motivazione per `build_mock_jwt()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 391 che coinvolge il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 391 che coinvolge il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 391 che coinvolge il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 391 che coinvolge il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `L139`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 391 che coinvolge il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 391 che coinvolge il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `ast`. Nel grafo del codice RDG, l'arco 391 registra come `source` l'identificatore `rve_transactions_common_rationale_139` per la relazione che coinvolge il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).`. Nel grafo del codice RDG, l'arco 391 registra come `target` l'identificatore `rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 391 che coinvolge il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=182 id=Request-Dataset-Generator::rve_transactions_common_rationale_139 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `L139`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `costruisce un jwt mock strutturalmente corretto (non firmato con chiave reale).`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `rve_transactions_common_rationale_139`. Nel grafo unificato, il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_rationale_139`.
Nel grafo unificato, il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=391 role=incoming -->
Nel grafo unificato, il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `build_mock_jwt()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt` all'ID `Request-Dataset-Generator::rve_transactions_common_rationale_139`. La relazione tecnica esplicita è la seguente: `build_mock_jwt()` costituisce una motivazione per `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).`. Nel grafo unificato, il tipo dichiarato dell'arco 391 che coinvolge il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 391 che coinvolge il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 391 che coinvolge il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 391 che coinvolge il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `L139`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 391 che coinvolge il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 391 che coinvolge il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 391 che coinvolge il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).` è `1.0`. Nel grafo unificato, l'arco 391 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).`. Nel grafo unificato, l'arco 391 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_rationale_139` per la relazione che coinvolge il nodo canonico `Costruisce un JWT mock strutturalmente corretto (non firmato con chiave reale).`.

### datetime [rve_transactions_common_py_datetime]

<!-- GRAPH_NODE graph=codice index=163 id=rve_transactions_common_py_datetime -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `datetime [rve_transactions_common_py_datetime]` è `datetime`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `datetime [rve_transactions_common_py_datetime]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `datetime [rve_transactions_common_py_datetime]` è ``. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `datetime [rve_transactions_common_py_datetime]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `datetime [rve_transactions_common_py_datetime]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `datetime [rve_transactions_common_py_datetime]` possiede l'identificatore esatto `rve_transactions_common_py_datetime`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `datetime [rve_transactions_common_py_datetime]` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `datetime [rve_transactions_common_py_datetime]` è `datetime`.
Nel grafo del codice RDG, il nodo canonico `datetime [rve_transactions_common_py_datetime]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=348 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `datetime [rve_transactions_common_py_datetime]` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_py_datetime`. La relazione tecnica esplicita è la seguente: `_common.py` importa da `datetime [rve_transactions_common_py_datetime]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 348 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 348 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 348 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 348 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 348 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `L13`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 348 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 348 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `ast`. Nel grafo del codice RDG, l'arco 348 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]`. Nel grafo del codice RDG, l'arco 348 registra come `target` l'identificatore `rve_transactions_common_py_datetime` per la relazione che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 348 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=362 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `datetime [rve_transactions_common_py_datetime]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `utc_now()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_utc_now` all'ID `rve_transactions_common_py_datetime`. La relazione tecnica esplicita è la seguente: `utc_now()` fa riferimento a `datetime [rve_transactions_common_py_datetime]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 362 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 362 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `return_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 362 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 362 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 362 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `L29`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 362 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 362 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `ast`. Nel grafo del codice RDG, l'arco 362 registra come `source` l'identificatore `rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]`. Nel grafo del codice RDG, l'arco 362 registra come `target` l'identificatore `rve_transactions_common_py_datetime` per la relazione che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 362 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=365 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `datetime [rve_transactions_common_py_datetime]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `utc_iso()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_utc_iso` all'ID `rve_transactions_common_py_datetime`. La relazione tecnica esplicita è la seguente: `utc_iso()` fa riferimento a `datetime [rve_transactions_common_py_datetime]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 365 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 365 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 365 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 365 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 365 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `L33`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 365 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 365 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `ast`. Nel grafo del codice RDG, l'arco 365 registra come `source` l'identificatore `rve_transactions_common_utc_iso` per la relazione che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]`. Nel grafo del codice RDG, l'arco 365 registra come `target` l'identificatore `rve_transactions_common_py_datetime` per la relazione che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 365 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=366 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `datetime [rve_transactions_common_py_datetime]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `utc_iso_ms()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_utc_iso_ms` all'ID `rve_transactions_common_py_datetime`. La relazione tecnica esplicita è la seguente: `utc_iso_ms()` fa riferimento a `datetime [rve_transactions_common_py_datetime]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 366 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 366 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 366 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 366 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 366 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `L38`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 366 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 366 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `ast`. Nel grafo del codice RDG, l'arco 366 registra come `source` l'identificatore `rve_transactions_common_utc_iso_ms` per la relazione che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]`. Nel grafo del codice RDG, l'arco 366 registra come `target` l'identificatore `rve_transactions_common_py_datetime` per la relazione che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 366 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=163 id=Request-Dataset-Generator::rve_transactions_common_py_datetime -->
Nel grafo unificato, l'etichetta originale del nodo canonico `datetime [rve_transactions_common_py_datetime]` è `datetime`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `datetime [rve_transactions_common_py_datetime]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `datetime [rve_transactions_common_py_datetime]` è ``. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `datetime [rve_transactions_common_py_datetime]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `datetime [rve_transactions_common_py_datetime]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `datetime [rve_transactions_common_py_datetime]` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `datetime [rve_transactions_common_py_datetime]` è `datetime`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `datetime [rve_transactions_common_py_datetime]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `rve_transactions_common_py_datetime`. Nel grafo unificato, il nodo canonico `datetime [rve_transactions_common_py_datetime]` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_py_datetime`.
<!-- GRAPH_EDGE graph=unificata index=365 role=outgoing -->
Nel grafo unificato, il nodo canonico `datetime [rve_transactions_common_py_datetime]` è la sorgente `source` della relazione `references` verso il nodo canonico `utc_iso()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_py_datetime` all'ID `Request-Dataset-Generator::rve_transactions_common_utc_iso`. La relazione tecnica esplicita è la seguente: `datetime [rve_transactions_common_py_datetime]` fa riferimento a `utc_iso()`. Nel grafo unificato, il tipo dichiarato dell'arco 365 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 365 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 365 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 365 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 365 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `L33`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 365 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 365 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 365 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `1.0`. Nel grafo unificato, l'arco 365 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_py_datetime` per la relazione che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]`. Nel grafo unificato, l'arco 365 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_iso` per la relazione che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]`.
<!-- GRAPH_EDGE graph=unificata index=366 role=outgoing -->
Nel grafo unificato, il nodo canonico `datetime [rve_transactions_common_py_datetime]` è la sorgente `source` della relazione `references` verso il nodo canonico `utc_iso_ms()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_py_datetime` all'ID `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms`. La relazione tecnica esplicita è la seguente: `datetime [rve_transactions_common_py_datetime]` fa riferimento a `utc_iso_ms()`. Nel grafo unificato, il tipo dichiarato dell'arco 366 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 366 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 366 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 366 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 366 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `L38`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 366 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 366 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 366 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `1.0`. Nel grafo unificato, l'arco 366 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_py_datetime` per la relazione che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]`. Nel grafo unificato, l'arco 366 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms` per la relazione che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]`.
<!-- GRAPH_EDGE graph=unificata index=348 role=incoming -->
Nel grafo unificato, il nodo canonico `datetime [rve_transactions_common_py_datetime]` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_py_datetime`. La relazione tecnica esplicita è la seguente: `_common.py` importa da `datetime [rve_transactions_common_py_datetime]`. Nel grafo unificato, il tipo dichiarato dell'arco 348 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 348 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 348 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 348 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 348 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `L13`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 348 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 348 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 348 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `1.0`. Nel grafo unificato, l'arco 348 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]`. Nel grafo unificato, l'arco 348 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_py_datetime` per la relazione che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]`.
<!-- GRAPH_EDGE graph=unificata index=362 role=incoming -->
Nel grafo unificato, il nodo canonico `datetime [rve_transactions_common_py_datetime]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `utc_now()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_utc_now` all'ID `Request-Dataset-Generator::rve_transactions_common_py_datetime`. La relazione tecnica esplicita è la seguente: `utc_now()` fa riferimento a `datetime [rve_transactions_common_py_datetime]`. Nel grafo unificato, il tipo dichiarato dell'arco 362 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 362 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `return_type`. Nel grafo unificato, la confidenza categoriale dell'arco 362 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 362 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 362 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `L29`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 362 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 362 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 362 che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]` è `1.0`. Nel grafo unificato, l'arco 362 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]`. Nel grafo unificato, l'arco 362 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_py_datetime` per la relazione che coinvolge il nodo canonico `datetime [rve_transactions_common_py_datetime]`.

### Estrae il Codice Fiscale dalla risorsa Patient FHIR.

<!-- GRAPH_NODE graph=codice index=183 id=rve_transactions_common_rationale_174 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `Estrae il Codice Fiscale dalla risorsa Patient FHIR.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `L174`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` possiede l'identificatore esatto `rve_transactions_common_rationale_174`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `estrae il codice fiscale dalla risorsa patient fhir.`.
<!-- GRAPH_EDGE graph=codice index=394 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `extract_patient_cf()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_rationale_174` all'ID `rve_transactions_common_extract_patient_cf`. La relazione tecnica esplicita è la seguente: `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` costituisce una motivazione per `extract_patient_cf()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 394 che coinvolge il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 394 che coinvolge il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 394 che coinvolge il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 394 che coinvolge il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `L174`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 394 che coinvolge il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 394 che coinvolge il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `ast`. Nel grafo del codice RDG, l'arco 394 registra come `source` l'identificatore `rve_transactions_common_rationale_174` per la relazione che coinvolge il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.`. Nel grafo del codice RDG, l'arco 394 registra come `target` l'identificatore `rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 394 che coinvolge il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=183 id=Request-Dataset-Generator::rve_transactions_common_rationale_174 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `Estrae il Codice Fiscale dalla risorsa Patient FHIR.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `L174`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `estrae il codice fiscale dalla risorsa patient fhir.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `rve_transactions_common_rationale_174`. Nel grafo unificato, il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_rationale_174`.
Nel grafo unificato, il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=394 role=incoming -->
Nel grafo unificato, il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `extract_patient_cf()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` all'ID `Request-Dataset-Generator::rve_transactions_common_rationale_174`. La relazione tecnica esplicita è la seguente: `extract_patient_cf()` costituisce una motivazione per `Estrae il Codice Fiscale dalla risorsa Patient FHIR.`. Nel grafo unificato, il tipo dichiarato dell'arco 394 che coinvolge il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 394 che coinvolge il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 394 che coinvolge il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 394 che coinvolge il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `L174`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 394 che coinvolge il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 394 che coinvolge il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 394 che coinvolge il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` è `1.0`. Nel grafo unificato, l'arco 394 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.`. Nel grafo unificato, l'arco 394 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_rationale_174` per la relazione che coinvolge il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.`.

### Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.

<!-- GRAPH_NODE graph=codice index=184 id=rve_transactions_common_rationale_182 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `L182`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` possiede l'identificatore esatto `rve_transactions_common_rationale_182`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `estrae il codice mpi (master patient index) dalla risorsa patient fhir.`.
<!-- GRAPH_EDGE graph=codice index=400 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `extract_patient_mpi()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_rationale_182` all'ID `rve_transactions_common_extract_patient_mpi`. La relazione tecnica esplicita è la seguente: `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` costituisce una motivazione per `extract_patient_mpi()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 400 che coinvolge il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 400 che coinvolge il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 400 che coinvolge il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 400 che coinvolge il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `L182`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 400 che coinvolge il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 400 che coinvolge il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `ast`. Nel grafo del codice RDG, l'arco 400 registra come `source` l'identificatore `rve_transactions_common_rationale_182` per la relazione che coinvolge il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.`. Nel grafo del codice RDG, l'arco 400 registra come `target` l'identificatore `rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 400 che coinvolge il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=184 id=Request-Dataset-Generator::rve_transactions_common_rationale_182 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `L182`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `estrae il codice mpi (master patient index) dalla risorsa patient fhir.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `rve_transactions_common_rationale_182`. Nel grafo unificato, il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_rationale_182`.
Nel grafo unificato, il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=400 role=incoming -->
Nel grafo unificato, il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `extract_patient_mpi()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` all'ID `Request-Dataset-Generator::rve_transactions_common_rationale_182`. La relazione tecnica esplicita è la seguente: `extract_patient_mpi()` costituisce una motivazione per `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.`. Nel grafo unificato, il tipo dichiarato dell'arco 400 che coinvolge il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 400 che coinvolge il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 400 che coinvolge il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 400 che coinvolge il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `L182`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 400 che coinvolge il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 400 che coinvolge il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 400 che coinvolge il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` è `1.0`. Nel grafo unificato, l'arco 400 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.`. Nel grafo unificato, l'arco 400 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_rationale_182` per la relazione che coinvolge il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.`.

### extract_patient_cf()

<!-- GRAPH_NODE graph=codice index=174 id=rve_transactions_common_extract_patient_cf -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `extract_patient_cf()` è `extract_patient_cf()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `extract_patient_cf()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `extract_patient_cf()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `extract_patient_cf()` è `L173`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `extract_patient_cf()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `extract_patient_cf()` possiede l'identificatore esatto `rve_transactions_common_extract_patient_cf`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `extract_patient_cf()` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `extract_patient_cf()` è `extract_patient_cf()`.
Nel grafo del codice RDG, il nodo canonico `extract_patient_cf()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=343 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `extract_patient_cf()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_extract_patient_cf`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `extract_patient_cf()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 343 che coinvolge il nodo canonico `extract_patient_cf()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 343 che coinvolge il nodo canonico `extract_patient_cf()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 343 che coinvolge il nodo canonico `extract_patient_cf()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 343 che coinvolge il nodo canonico `extract_patient_cf()` è `L173`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 343 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 343 che coinvolge il nodo canonico `extract_patient_cf()` è `ast`. Nel grafo del codice RDG, l'arco 343 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo del codice RDG, l'arco 343 registra come `target` l'identificatore `rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 343 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=394 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `extract_patient_cf()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_rationale_174` all'ID `rve_transactions_common_extract_patient_cf`. La relazione tecnica esplicita è la seguente: `Estrae il Codice Fiscale dalla risorsa Patient FHIR.` costituisce una motivazione per `extract_patient_cf()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 394 che coinvolge il nodo canonico `extract_patient_cf()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 394 che coinvolge il nodo canonico `extract_patient_cf()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 394 che coinvolge il nodo canonico `extract_patient_cf()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 394 che coinvolge il nodo canonico `extract_patient_cf()` è `L174`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 394 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 394 che coinvolge il nodo canonico `extract_patient_cf()` è `ast`. Nel grafo del codice RDG, l'arco 394 registra come `source` l'identificatore `rve_transactions_common_rationale_174` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo del codice RDG, l'arco 394 registra come `target` l'identificatore `rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 394 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=395 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `extract_patient_cf()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `rve_121.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121` all'ID `rve_transactions_common_extract_patient_cf`. La relazione tecnica esplicita è la seguente: `rve_121.py` importa `extract_patient_cf()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 395 che coinvolge il nodo canonico `extract_patient_cf()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 395 che coinvolge il nodo canonico `extract_patient_cf()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 395 che coinvolge il nodo canonico `extract_patient_cf()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 395 che coinvolge il nodo canonico `extract_patient_cf()` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 395 che coinvolge il nodo canonico `extract_patient_cf()` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 395 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 395 che coinvolge il nodo canonico `extract_patient_cf()` è `ast`. Nel grafo del codice RDG, l'arco 395 registra come `source` l'identificatore `rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo del codice RDG, l'arco 395 registra come `target` l'identificatore `rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 395 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=396 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `extract_patient_cf()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_request() [rve_transactions_rve_121_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121_build_request` all'ID `rve_transactions_common_extract_patient_cf`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_121_build_request]` chiama `extract_patient_cf()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 396 che coinvolge il nodo canonico `extract_patient_cf()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 396 che coinvolge il nodo canonico `extract_patient_cf()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 396 che coinvolge il nodo canonico `extract_patient_cf()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 396 che coinvolge il nodo canonico `extract_patient_cf()` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 396 che coinvolge il nodo canonico `extract_patient_cf()` è `L66`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 396 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 396 che coinvolge il nodo canonico `extract_patient_cf()` è `ast`. Nel grafo del codice RDG, l'arco 396 registra come `source` l'identificatore `rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo del codice RDG, l'arco 396 registra come `target` l'identificatore `rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 396 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=397 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `extract_patient_cf()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `rve_54.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54` all'ID `rve_transactions_common_extract_patient_cf`. La relazione tecnica esplicita è la seguente: `rve_54.py` importa `extract_patient_cf()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 397 che coinvolge il nodo canonico `extract_patient_cf()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 397 che coinvolge il nodo canonico `extract_patient_cf()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 397 che coinvolge il nodo canonico `extract_patient_cf()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 397 che coinvolge il nodo canonico `extract_patient_cf()` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 397 che coinvolge il nodo canonico `extract_patient_cf()` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 397 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 397 che coinvolge il nodo canonico `extract_patient_cf()` è `ast`. Nel grafo del codice RDG, l'arco 397 registra come `source` l'identificatore `rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo del codice RDG, l'arco 397 registra come `target` l'identificatore `rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 397 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=398 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `extract_patient_cf()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_request() [rve_transactions_rve_54_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54_build_request` all'ID `rve_transactions_common_extract_patient_cf`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_54_build_request]` chiama `extract_patient_cf()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 398 che coinvolge il nodo canonico `extract_patient_cf()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 398 che coinvolge il nodo canonico `extract_patient_cf()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 398 che coinvolge il nodo canonico `extract_patient_cf()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 398 che coinvolge il nodo canonico `extract_patient_cf()` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 398 che coinvolge il nodo canonico `extract_patient_cf()` è `L69`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 398 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 398 che coinvolge il nodo canonico `extract_patient_cf()` è `ast`. Nel grafo del codice RDG, l'arco 398 registra come `source` l'identificatore `rve_transactions_rve_54_build_request` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo del codice RDG, l'arco 398 registra come `target` l'identificatore `rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 398 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=399 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `extract_patient_cf()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `rve_55.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_55` all'ID `rve_transactions_common_extract_patient_cf`. La relazione tecnica esplicita è la seguente: `rve_55.py` importa `extract_patient_cf()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 399 che coinvolge il nodo canonico `extract_patient_cf()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 399 che coinvolge il nodo canonico `extract_patient_cf()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 399 che coinvolge il nodo canonico `extract_patient_cf()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 399 che coinvolge il nodo canonico `extract_patient_cf()` è `rve_transactions/rve_55.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 399 che coinvolge il nodo canonico `extract_patient_cf()` è `L22`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 399 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 399 che coinvolge il nodo canonico `extract_patient_cf()` è `ast`. Nel grafo del codice RDG, l'arco 399 registra come `source` l'identificatore `rve_transactions_rve_55` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo del codice RDG, l'arco 399 registra come `target` l'identificatore `rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 399 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=174 id=Request-Dataset-Generator::rve_transactions_common_extract_patient_cf -->
Nel grafo unificato, l'etichetta originale del nodo canonico `extract_patient_cf()` è `extract_patient_cf()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `extract_patient_cf()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `extract_patient_cf()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `extract_patient_cf()` è `L173`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `extract_patient_cf()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `extract_patient_cf()` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `extract_patient_cf()` è `extract_patient_cf()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `extract_patient_cf()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `extract_patient_cf()` è `rve_transactions_common_extract_patient_cf`. Nel grafo unificato, il nodo canonico `extract_patient_cf()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf`.
<!-- GRAPH_EDGE graph=unificata index=394 role=outgoing -->
Nel grafo unificato, il nodo canonico `extract_patient_cf()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Estrae il Codice Fiscale dalla risorsa Patient FHIR.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` all'ID `Request-Dataset-Generator::rve_transactions_common_rationale_174`. La relazione tecnica esplicita è la seguente: `extract_patient_cf()` costituisce una motivazione per `Estrae il Codice Fiscale dalla risorsa Patient FHIR.`. Nel grafo unificato, il tipo dichiarato dell'arco 394 che coinvolge il nodo canonico `extract_patient_cf()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 394 che coinvolge il nodo canonico `extract_patient_cf()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 394 che coinvolge il nodo canonico `extract_patient_cf()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 394 che coinvolge il nodo canonico `extract_patient_cf()` è `L174`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 394 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 394 che coinvolge il nodo canonico `extract_patient_cf()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 394 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo unificato, l'arco 394 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo unificato, l'arco 394 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_rationale_174` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`.
<!-- GRAPH_EDGE graph=unificata index=395 role=outgoing -->
Nel grafo unificato, il nodo canonico `extract_patient_cf()` è la sorgente `source` della relazione `imports` verso il nodo canonico `rve_121.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` all'ID `Request-Dataset-Generator::rve_transactions_rve_121`. La relazione tecnica esplicita è la seguente: `extract_patient_cf()` importa `rve_121.py`. Nel grafo unificato, il tipo dichiarato dell'arco 395 che coinvolge il nodo canonico `extract_patient_cf()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 395 che coinvolge il nodo canonico `extract_patient_cf()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 395 che coinvolge il nodo canonico `extract_patient_cf()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 395 che coinvolge il nodo canonico `extract_patient_cf()` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 395 che coinvolge il nodo canonico `extract_patient_cf()` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 395 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 395 che coinvolge il nodo canonico `extract_patient_cf()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 395 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo unificato, l'arco 395 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo unificato, l'arco 395 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`.
<!-- GRAPH_EDGE graph=unificata index=396 role=outgoing -->
Nel grafo unificato, il nodo canonico `extract_patient_cf()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` all'ID `Request-Dataset-Generator::rve_transactions_rve_121_build_request`. La relazione tecnica esplicita è la seguente: `extract_patient_cf()` chiama `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 396 che coinvolge il nodo canonico `extract_patient_cf()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 396 che coinvolge il nodo canonico `extract_patient_cf()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 396 che coinvolge il nodo canonico `extract_patient_cf()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 396 che coinvolge il nodo canonico `extract_patient_cf()` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 396 che coinvolge il nodo canonico `extract_patient_cf()` è `L66`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 396 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 396 che coinvolge il nodo canonico `extract_patient_cf()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 396 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo unificato, l'arco 396 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo unificato, l'arco 396 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`.
<!-- GRAPH_EDGE graph=unificata index=397 role=outgoing -->
Nel grafo unificato, il nodo canonico `extract_patient_cf()` è la sorgente `source` della relazione `imports` verso il nodo canonico `rve_54.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` all'ID `Request-Dataset-Generator::rve_transactions_rve_54`. La relazione tecnica esplicita è la seguente: `extract_patient_cf()` importa `rve_54.py`. Nel grafo unificato, il tipo dichiarato dell'arco 397 che coinvolge il nodo canonico `extract_patient_cf()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 397 che coinvolge il nodo canonico `extract_patient_cf()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 397 che coinvolge il nodo canonico `extract_patient_cf()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 397 che coinvolge il nodo canonico `extract_patient_cf()` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 397 che coinvolge il nodo canonico `extract_patient_cf()` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 397 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 397 che coinvolge il nodo canonico `extract_patient_cf()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 397 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo unificato, l'arco 397 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo unificato, l'arco 397 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`.
<!-- GRAPH_EDGE graph=unificata index=398 role=outgoing -->
Nel grafo unificato, il nodo canonico `extract_patient_cf()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` all'ID `Request-Dataset-Generator::rve_transactions_rve_54_build_request`. La relazione tecnica esplicita è la seguente: `extract_patient_cf()` chiama `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 398 che coinvolge il nodo canonico `extract_patient_cf()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 398 che coinvolge il nodo canonico `extract_patient_cf()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 398 che coinvolge il nodo canonico `extract_patient_cf()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 398 che coinvolge il nodo canonico `extract_patient_cf()` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 398 che coinvolge il nodo canonico `extract_patient_cf()` è `L69`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 398 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 398 che coinvolge il nodo canonico `extract_patient_cf()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 398 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo unificato, l'arco 398 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo unificato, l'arco 398 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_build_request` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`.
<!-- GRAPH_EDGE graph=unificata index=399 role=outgoing -->
Nel grafo unificato, il nodo canonico `extract_patient_cf()` è la sorgente `source` della relazione `imports` verso il nodo canonico `rve_55.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` all'ID `Request-Dataset-Generator::rve_transactions_rve_55`. La relazione tecnica esplicita è la seguente: `extract_patient_cf()` importa `rve_55.py`. Nel grafo unificato, il tipo dichiarato dell'arco 399 che coinvolge il nodo canonico `extract_patient_cf()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 399 che coinvolge il nodo canonico `extract_patient_cf()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 399 che coinvolge il nodo canonico `extract_patient_cf()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 399 che coinvolge il nodo canonico `extract_patient_cf()` è `rve_transactions/rve_55.py`. Nel grafo unificato, la posizione sorgente dell'arco 399 che coinvolge il nodo canonico `extract_patient_cf()` è `L22`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 399 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 399 che coinvolge il nodo canonico `extract_patient_cf()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 399 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo unificato, l'arco 399 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo unificato, l'arco 399 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_55` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`.
<!-- GRAPH_EDGE graph=unificata index=343 role=incoming -->
Nel grafo unificato, il nodo canonico `extract_patient_cf()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `extract_patient_cf()`. Nel grafo unificato, il tipo dichiarato dell'arco 343 che coinvolge il nodo canonico `extract_patient_cf()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 343 che coinvolge il nodo canonico `extract_patient_cf()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 343 che coinvolge il nodo canonico `extract_patient_cf()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 343 che coinvolge il nodo canonico `extract_patient_cf()` è `L173`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 343 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 343 che coinvolge il nodo canonico `extract_patient_cf()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 343 che coinvolge il nodo canonico `extract_patient_cf()` è `1.0`. Nel grafo unificato, l'arco 343 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`. Nel grafo unificato, l'arco 343 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `extract_patient_cf()`.

### extract_patient_mpi()

<!-- GRAPH_NODE graph=codice index=175 id=rve_transactions_common_extract_patient_mpi -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `extract_patient_mpi()` è `extract_patient_mpi()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `extract_patient_mpi()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `extract_patient_mpi()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `extract_patient_mpi()` è `L181`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `extract_patient_mpi()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `extract_patient_mpi()` possiede l'identificatore esatto `rve_transactions_common_extract_patient_mpi`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `extract_patient_mpi()` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `extract_patient_mpi()` è `extract_patient_mpi()`.
Nel grafo del codice RDG, il nodo canonico `extract_patient_mpi()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=344 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `extract_patient_mpi()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_extract_patient_mpi`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `extract_patient_mpi()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 344 che coinvolge il nodo canonico `extract_patient_mpi()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 344 che coinvolge il nodo canonico `extract_patient_mpi()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 344 che coinvolge il nodo canonico `extract_patient_mpi()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 344 che coinvolge il nodo canonico `extract_patient_mpi()` è `L181`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 344 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 344 che coinvolge il nodo canonico `extract_patient_mpi()` è `ast`. Nel grafo del codice RDG, l'arco 344 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`. Nel grafo del codice RDG, l'arco 344 registra come `target` l'identificatore `rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 344 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=400 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `extract_patient_mpi()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_rationale_182` all'ID `rve_transactions_common_extract_patient_mpi`. La relazione tecnica esplicita è la seguente: `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.` costituisce una motivazione per `extract_patient_mpi()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 400 che coinvolge il nodo canonico `extract_patient_mpi()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 400 che coinvolge il nodo canonico `extract_patient_mpi()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 400 che coinvolge il nodo canonico `extract_patient_mpi()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 400 che coinvolge il nodo canonico `extract_patient_mpi()` è `L182`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 400 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 400 che coinvolge il nodo canonico `extract_patient_mpi()` è `ast`. Nel grafo del codice RDG, l'arco 400 registra come `source` l'identificatore `rve_transactions_common_rationale_182` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`. Nel grafo del codice RDG, l'arco 400 registra come `target` l'identificatore `rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 400 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=401 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `extract_patient_mpi()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `rve_121.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121` all'ID `rve_transactions_common_extract_patient_mpi`. La relazione tecnica esplicita è la seguente: `rve_121.py` importa `extract_patient_mpi()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 401 che coinvolge il nodo canonico `extract_patient_mpi()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 401 che coinvolge il nodo canonico `extract_patient_mpi()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 401 che coinvolge il nodo canonico `extract_patient_mpi()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 401 che coinvolge il nodo canonico `extract_patient_mpi()` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 401 che coinvolge il nodo canonico `extract_patient_mpi()` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 401 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 401 che coinvolge il nodo canonico `extract_patient_mpi()` è `ast`. Nel grafo del codice RDG, l'arco 401 registra come `source` l'identificatore `rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`. Nel grafo del codice RDG, l'arco 401 registra come `target` l'identificatore `rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 401 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=402 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `extract_patient_mpi()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_request() [rve_transactions_rve_121_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121_build_request` all'ID `rve_transactions_common_extract_patient_mpi`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_121_build_request]` chiama `extract_patient_mpi()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 402 che coinvolge il nodo canonico `extract_patient_mpi()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 402 che coinvolge il nodo canonico `extract_patient_mpi()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 402 che coinvolge il nodo canonico `extract_patient_mpi()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 402 che coinvolge il nodo canonico `extract_patient_mpi()` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 402 che coinvolge il nodo canonico `extract_patient_mpi()` è `L67`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 402 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 402 che coinvolge il nodo canonico `extract_patient_mpi()` è `ast`. Nel grafo del codice RDG, l'arco 402 registra come `source` l'identificatore `rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`. Nel grafo del codice RDG, l'arco 402 registra come `target` l'identificatore `rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 402 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=403 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `extract_patient_mpi()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `rve_54.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54` all'ID `rve_transactions_common_extract_patient_mpi`. La relazione tecnica esplicita è la seguente: `rve_54.py` importa `extract_patient_mpi()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 403 che coinvolge il nodo canonico `extract_patient_mpi()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 403 che coinvolge il nodo canonico `extract_patient_mpi()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 403 che coinvolge il nodo canonico `extract_patient_mpi()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 403 che coinvolge il nodo canonico `extract_patient_mpi()` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 403 che coinvolge il nodo canonico `extract_patient_mpi()` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 403 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 403 che coinvolge il nodo canonico `extract_patient_mpi()` è `ast`. Nel grafo del codice RDG, l'arco 403 registra come `source` l'identificatore `rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`. Nel grafo del codice RDG, l'arco 403 registra come `target` l'identificatore `rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 403 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=404 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `extract_patient_mpi()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_request() [rve_transactions_rve_54_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54_build_request` all'ID `rve_transactions_common_extract_patient_mpi`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_54_build_request]` chiama `extract_patient_mpi()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 404 che coinvolge il nodo canonico `extract_patient_mpi()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 404 che coinvolge il nodo canonico `extract_patient_mpi()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 404 che coinvolge il nodo canonico `extract_patient_mpi()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 404 che coinvolge il nodo canonico `extract_patient_mpi()` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 404 che coinvolge il nodo canonico `extract_patient_mpi()` è `L70`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 404 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 404 che coinvolge il nodo canonico `extract_patient_mpi()` è `ast`. Nel grafo del codice RDG, l'arco 404 registra come `source` l'identificatore `rve_transactions_rve_54_build_request` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`. Nel grafo del codice RDG, l'arco 404 registra come `target` l'identificatore `rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 404 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=175 id=Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi -->
Nel grafo unificato, l'etichetta originale del nodo canonico `extract_patient_mpi()` è `extract_patient_mpi()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `extract_patient_mpi()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `extract_patient_mpi()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `extract_patient_mpi()` è `L181`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `extract_patient_mpi()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `extract_patient_mpi()` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `extract_patient_mpi()` è `extract_patient_mpi()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `extract_patient_mpi()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `extract_patient_mpi()` è `rve_transactions_common_extract_patient_mpi`. Nel grafo unificato, il nodo canonico `extract_patient_mpi()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi`.
<!-- GRAPH_EDGE graph=unificata index=400 role=outgoing -->
Nel grafo unificato, il nodo canonico `extract_patient_mpi()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` all'ID `Request-Dataset-Generator::rve_transactions_common_rationale_182`. La relazione tecnica esplicita è la seguente: `extract_patient_mpi()` costituisce una motivazione per `Estrae il codice MPI (Master Patient Index) dalla risorsa Patient FHIR.`. Nel grafo unificato, il tipo dichiarato dell'arco 400 che coinvolge il nodo canonico `extract_patient_mpi()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 400 che coinvolge il nodo canonico `extract_patient_mpi()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 400 che coinvolge il nodo canonico `extract_patient_mpi()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 400 che coinvolge il nodo canonico `extract_patient_mpi()` è `L182`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 400 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 400 che coinvolge il nodo canonico `extract_patient_mpi()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 400 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`. Nel grafo unificato, l'arco 400 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`. Nel grafo unificato, l'arco 400 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_rationale_182` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`.
<!-- GRAPH_EDGE graph=unificata index=401 role=outgoing -->
Nel grafo unificato, il nodo canonico `extract_patient_mpi()` è la sorgente `source` della relazione `imports` verso il nodo canonico `rve_121.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` all'ID `Request-Dataset-Generator::rve_transactions_rve_121`. La relazione tecnica esplicita è la seguente: `extract_patient_mpi()` importa `rve_121.py`. Nel grafo unificato, il tipo dichiarato dell'arco 401 che coinvolge il nodo canonico `extract_patient_mpi()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 401 che coinvolge il nodo canonico `extract_patient_mpi()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 401 che coinvolge il nodo canonico `extract_patient_mpi()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 401 che coinvolge il nodo canonico `extract_patient_mpi()` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 401 che coinvolge il nodo canonico `extract_patient_mpi()` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 401 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 401 che coinvolge il nodo canonico `extract_patient_mpi()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 401 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`. Nel grafo unificato, l'arco 401 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`. Nel grafo unificato, l'arco 401 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`.
<!-- GRAPH_EDGE graph=unificata index=402 role=outgoing -->
Nel grafo unificato, il nodo canonico `extract_patient_mpi()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` all'ID `Request-Dataset-Generator::rve_transactions_rve_121_build_request`. La relazione tecnica esplicita è la seguente: `extract_patient_mpi()` chiama `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 402 che coinvolge il nodo canonico `extract_patient_mpi()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 402 che coinvolge il nodo canonico `extract_patient_mpi()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 402 che coinvolge il nodo canonico `extract_patient_mpi()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 402 che coinvolge il nodo canonico `extract_patient_mpi()` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 402 che coinvolge il nodo canonico `extract_patient_mpi()` è `L67`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 402 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 402 che coinvolge il nodo canonico `extract_patient_mpi()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 402 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`. Nel grafo unificato, l'arco 402 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`. Nel grafo unificato, l'arco 402 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`.
<!-- GRAPH_EDGE graph=unificata index=403 role=outgoing -->
Nel grafo unificato, il nodo canonico `extract_patient_mpi()` è la sorgente `source` della relazione `imports` verso il nodo canonico `rve_54.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` all'ID `Request-Dataset-Generator::rve_transactions_rve_54`. La relazione tecnica esplicita è la seguente: `extract_patient_mpi()` importa `rve_54.py`. Nel grafo unificato, il tipo dichiarato dell'arco 403 che coinvolge il nodo canonico `extract_patient_mpi()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 403 che coinvolge il nodo canonico `extract_patient_mpi()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 403 che coinvolge il nodo canonico `extract_patient_mpi()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 403 che coinvolge il nodo canonico `extract_patient_mpi()` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 403 che coinvolge il nodo canonico `extract_patient_mpi()` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 403 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 403 che coinvolge il nodo canonico `extract_patient_mpi()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 403 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`. Nel grafo unificato, l'arco 403 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`. Nel grafo unificato, l'arco 403 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`.
<!-- GRAPH_EDGE graph=unificata index=404 role=outgoing -->
Nel grafo unificato, il nodo canonico `extract_patient_mpi()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` all'ID `Request-Dataset-Generator::rve_transactions_rve_54_build_request`. La relazione tecnica esplicita è la seguente: `extract_patient_mpi()` chiama `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 404 che coinvolge il nodo canonico `extract_patient_mpi()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 404 che coinvolge il nodo canonico `extract_patient_mpi()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 404 che coinvolge il nodo canonico `extract_patient_mpi()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 404 che coinvolge il nodo canonico `extract_patient_mpi()` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 404 che coinvolge il nodo canonico `extract_patient_mpi()` è `L70`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 404 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 404 che coinvolge il nodo canonico `extract_patient_mpi()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 404 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`. Nel grafo unificato, l'arco 404 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`. Nel grafo unificato, l'arco 404 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_build_request` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`.
<!-- GRAPH_EDGE graph=unificata index=344 role=incoming -->
Nel grafo unificato, il nodo canonico `extract_patient_mpi()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `extract_patient_mpi()`. Nel grafo unificato, il tipo dichiarato dell'arco 344 che coinvolge il nodo canonico `extract_patient_mpi()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 344 che coinvolge il nodo canonico `extract_patient_mpi()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 344 che coinvolge il nodo canonico `extract_patient_mpi()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 344 che coinvolge il nodo canonico `extract_patient_mpi()` è `L181`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 344 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 344 che coinvolge il nodo canonico `extract_patient_mpi()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 344 che coinvolge il nodo canonico `extract_patient_mpi()` è `1.0`. Nel grafo unificato, l'arco 344 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`. Nel grafo unificato, l'arco 344 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `extract_patient_mpi()`.

### Genera un elemento &lt;saml:Assertion&gt; strutturalmente valido (non firmato).

<!-- GRAPH_NODE graph=codice index=226 id=rve_transactions_rve_1b_rationale_237 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `L237`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` possiede l'identificatore esatto `rve_transactions_rve_1b_rationale_237`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `genera un elemento <saml:assertion> strutturalmente valido (non firmato).`.
<!-- GRAPH_EDGE graph=codice index=478 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `_build_mock_assertion()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_rationale_237` all'ID `rve_transactions_rve_1b_build_mock_assertion`. La relazione tecnica esplicita è la seguente: `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` costituisce una motivazione per `_build_mock_assertion()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 478 che coinvolge il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 478 che coinvolge il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 478 che coinvolge il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 478 che coinvolge il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `L237`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 478 che coinvolge il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 478 che coinvolge il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `ast`. Nel grafo del codice RDG, l'arco 478 registra come `source` l'identificatore `rve_transactions_rve_1b_rationale_237` per la relazione che coinvolge il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).`. Nel grafo del codice RDG, l'arco 478 registra come `target` l'identificatore `rve_transactions_rve_1b_build_mock_assertion` per la relazione che coinvolge il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 478 che coinvolge il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=226 id=Request-Dataset-Generator::rve_transactions_rve_1b_rationale_237 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `L237`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `genera un elemento <saml:assertion> strutturalmente valido (non firmato).`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `rve_transactions_rve_1b_rationale_237`. Nel grafo unificato, il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_237`.
Nel grafo unificato, il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=478 role=incoming -->
Nel grafo unificato, il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `_build_mock_assertion()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_assertion` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_237`. La relazione tecnica esplicita è la seguente: `_build_mock_assertion()` costituisce una motivazione per `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).`. Nel grafo unificato, il tipo dichiarato dell'arco 478 che coinvolge il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 478 che coinvolge il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 478 che coinvolge il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 478 che coinvolge il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `L237`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 478 che coinvolge il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 478 che coinvolge il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 478 che coinvolge il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).` è `1.0`. Nel grafo unificato, l'arco 478 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_assertion` per la relazione che coinvolge il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).`. Nel grafo unificato, l'arco 478 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_237` per la relazione che coinvolge il nodo canonico `Genera un elemento <saml:Assertion> strutturalmente valido (non firmato).`.

### linearize_xml()

<!-- GRAPH_NODE graph=codice index=170 id=rve_transactions_common_linearize_xml -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `linearize_xml()` è `linearize_xml()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `linearize_xml()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `linearize_xml()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `linearize_xml()` è `L77`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `linearize_xml()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `linearize_xml()` possiede l'identificatore esatto `rve_transactions_common_linearize_xml`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `linearize_xml()` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `linearize_xml()` è `linearize_xml()`.
<!-- GRAPH_EDGE graph=codice index=385 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `linearize_xml()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_linearize_xml` all'ID `rve_transactions_common_replace_newlines_inside_tags`. La relazione tecnica esplicita è la seguente: `linearize_xml()` chiama `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 385 che coinvolge il nodo canonico `linearize_xml()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 385 che coinvolge il nodo canonico `linearize_xml()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 385 che coinvolge il nodo canonico `linearize_xml()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 385 che coinvolge il nodo canonico `linearize_xml()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 385 che coinvolge il nodo canonico `linearize_xml()` è `L91`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 385 che coinvolge il nodo canonico `linearize_xml()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 385 che coinvolge il nodo canonico `linearize_xml()` è `ast`. Nel grafo del codice RDG, l'arco 385 registra come `source` l'identificatore `rve_transactions_common_linearize_xml` per la relazione che coinvolge il nodo canonico `linearize_xml()`. Nel grafo del codice RDG, l'arco 385 registra come `target` l'identificatore `rve_transactions_common_replace_newlines_inside_tags` per la relazione che coinvolge il nodo canonico `linearize_xml()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 385 che coinvolge il nodo canonico `linearize_xml()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=345 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `linearize_xml()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_linearize_xml`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `linearize_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 345 che coinvolge il nodo canonico `linearize_xml()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 345 che coinvolge il nodo canonico `linearize_xml()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 345 che coinvolge il nodo canonico `linearize_xml()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 345 che coinvolge il nodo canonico `linearize_xml()` è `L77`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 345 che coinvolge il nodo canonico `linearize_xml()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 345 che coinvolge il nodo canonico `linearize_xml()` è `ast`. Nel grafo del codice RDG, l'arco 345 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `linearize_xml()`. Nel grafo del codice RDG, l'arco 345 registra come `target` l'identificatore `rve_transactions_common_linearize_xml` per la relazione che coinvolge il nodo canonico `linearize_xml()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 345 che coinvolge il nodo canonico `linearize_xml()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=386 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `linearize_xml()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_rationale_78` all'ID `rve_transactions_common_linearize_xml`. La relazione tecnica esplicita è la seguente: `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` costituisce una motivazione per `linearize_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 386 che coinvolge il nodo canonico `linearize_xml()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 386 che coinvolge il nodo canonico `linearize_xml()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 386 che coinvolge il nodo canonico `linearize_xml()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 386 che coinvolge il nodo canonico `linearize_xml()` è `L78`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 386 che coinvolge il nodo canonico `linearize_xml()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 386 che coinvolge il nodo canonico `linearize_xml()` è `ast`. Nel grafo del codice RDG, l'arco 386 registra come `source` l'identificatore `rve_transactions_common_rationale_78` per la relazione che coinvolge il nodo canonico `linearize_xml()`. Nel grafo del codice RDG, l'arco 386 registra come `target` l'identificatore `rve_transactions_common_linearize_xml` per la relazione che coinvolge il nodo canonico `linearize_xml()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 386 che coinvolge il nodo canonico `linearize_xml()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=387 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `linearize_xml()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_common_linearize_xml`. La relazione tecnica esplicita è la seguente: `rve_1b.py` importa `linearize_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 387 che coinvolge il nodo canonico `linearize_xml()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 387 che coinvolge il nodo canonico `linearize_xml()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 387 che coinvolge il nodo canonico `linearize_xml()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 387 che coinvolge il nodo canonico `linearize_xml()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 387 che coinvolge il nodo canonico `linearize_xml()` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 387 che coinvolge il nodo canonico `linearize_xml()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 387 che coinvolge il nodo canonico `linearize_xml()` è `ast`. Nel grafo del codice RDG, l'arco 387 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `linearize_xml()`. Nel grafo del codice RDG, l'arco 387 registra come `target` l'identificatore `rve_transactions_common_linearize_xml` per la relazione che coinvolge il nodo canonico `linearize_xml()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 387 che coinvolge il nodo canonico `linearize_xml()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=388 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `linearize_xml()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_common_linearize_xml`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `linearize_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 388 che coinvolge il nodo canonico `linearize_xml()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 388 che coinvolge il nodo canonico `linearize_xml()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 388 che coinvolge il nodo canonico `linearize_xml()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 388 che coinvolge il nodo canonico `linearize_xml()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 388 che coinvolge il nodo canonico `linearize_xml()` è `L421`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 388 che coinvolge il nodo canonico `linearize_xml()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 388 che coinvolge il nodo canonico `linearize_xml()` è `ast`. Nel grafo del codice RDG, l'arco 388 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `linearize_xml()`. Nel grafo del codice RDG, l'arco 388 registra come `target` l'identificatore `rve_transactions_common_linearize_xml` per la relazione che coinvolge il nodo canonico `linearize_xml()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 388 che coinvolge il nodo canonico `linearize_xml()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=170 id=Request-Dataset-Generator::rve_transactions_common_linearize_xml -->
Nel grafo unificato, l'etichetta originale del nodo canonico `linearize_xml()` è `linearize_xml()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `linearize_xml()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `linearize_xml()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `linearize_xml()` è `L77`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `linearize_xml()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `linearize_xml()` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `linearize_xml()` è `linearize_xml()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `linearize_xml()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `linearize_xml()` è `rve_transactions_common_linearize_xml`. Nel grafo unificato, il nodo canonico `linearize_xml()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_linearize_xml`.
<!-- GRAPH_EDGE graph=unificata index=385 role=outgoing -->
Nel grafo unificato, il nodo canonico `linearize_xml()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_linearize_xml` all'ID `Request-Dataset-Generator::rve_transactions_common_replace_newlines_inside_tags`. La relazione tecnica esplicita è la seguente: `linearize_xml()` chiama `_replace_newlines_inside_tags() [rve_transactions_common_replace_newlines_inside_tags]`. Nel grafo unificato, il tipo dichiarato dell'arco 385 che coinvolge il nodo canonico `linearize_xml()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 385 che coinvolge il nodo canonico `linearize_xml()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 385 che coinvolge il nodo canonico `linearize_xml()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 385 che coinvolge il nodo canonico `linearize_xml()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 385 che coinvolge il nodo canonico `linearize_xml()` è `L91`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 385 che coinvolge il nodo canonico `linearize_xml()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 385 che coinvolge il nodo canonico `linearize_xml()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 385 che coinvolge il nodo canonico `linearize_xml()` è `1.0`. Nel grafo unificato, l'arco 385 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_linearize_xml` per la relazione che coinvolge il nodo canonico `linearize_xml()`. Nel grafo unificato, l'arco 385 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_replace_newlines_inside_tags` per la relazione che coinvolge il nodo canonico `linearize_xml()`.
<!-- GRAPH_EDGE graph=unificata index=386 role=outgoing -->
Nel grafo unificato, il nodo canonico `linearize_xml()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_linearize_xml` all'ID `Request-Dataset-Generator::rve_transactions_common_rationale_78`. La relazione tecnica esplicita è la seguente: `linearize_xml()` costituisce una motivazione per `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s`. Nel grafo unificato, il tipo dichiarato dell'arco 386 che coinvolge il nodo canonico `linearize_xml()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 386 che coinvolge il nodo canonico `linearize_xml()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 386 che coinvolge il nodo canonico `linearize_xml()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 386 che coinvolge il nodo canonico `linearize_xml()` è `L78`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 386 che coinvolge il nodo canonico `linearize_xml()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 386 che coinvolge il nodo canonico `linearize_xml()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 386 che coinvolge il nodo canonico `linearize_xml()` è `1.0`. Nel grafo unificato, l'arco 386 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_linearize_xml` per la relazione che coinvolge il nodo canonico `linearize_xml()`. Nel grafo unificato, l'arco 386 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_rationale_78` per la relazione che coinvolge il nodo canonico `linearize_xml()`.
<!-- GRAPH_EDGE graph=unificata index=387 role=outgoing -->
Nel grafo unificato, il nodo canonico `linearize_xml()` è la sorgente `source` della relazione `imports` verso il nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_linearize_xml` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `linearize_xml()` importa `rve_1b.py`. Nel grafo unificato, il tipo dichiarato dell'arco 387 che coinvolge il nodo canonico `linearize_xml()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 387 che coinvolge il nodo canonico `linearize_xml()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 387 che coinvolge il nodo canonico `linearize_xml()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 387 che coinvolge il nodo canonico `linearize_xml()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 387 che coinvolge il nodo canonico `linearize_xml()` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 387 che coinvolge il nodo canonico `linearize_xml()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 387 che coinvolge il nodo canonico `linearize_xml()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 387 che coinvolge il nodo canonico `linearize_xml()` è `1.0`. Nel grafo unificato, l'arco 387 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_linearize_xml` per la relazione che coinvolge il nodo canonico `linearize_xml()`. Nel grafo unificato, l'arco 387 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `linearize_xml()`.
<!-- GRAPH_EDGE graph=unificata index=388 role=outgoing -->
Nel grafo unificato, il nodo canonico `linearize_xml()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_linearize_xml` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `linearize_xml()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 388 che coinvolge il nodo canonico `linearize_xml()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 388 che coinvolge il nodo canonico `linearize_xml()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 388 che coinvolge il nodo canonico `linearize_xml()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 388 che coinvolge il nodo canonico `linearize_xml()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 388 che coinvolge il nodo canonico `linearize_xml()` è `L421`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 388 che coinvolge il nodo canonico `linearize_xml()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 388 che coinvolge il nodo canonico `linearize_xml()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 388 che coinvolge il nodo canonico `linearize_xml()` è `1.0`. Nel grafo unificato, l'arco 388 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_linearize_xml` per la relazione che coinvolge il nodo canonico `linearize_xml()`. Nel grafo unificato, l'arco 388 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `linearize_xml()`.
<!-- GRAPH_EDGE graph=unificata index=345 role=incoming -->
Nel grafo unificato, il nodo canonico `linearize_xml()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_linearize_xml`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `linearize_xml()`. Nel grafo unificato, il tipo dichiarato dell'arco 345 che coinvolge il nodo canonico `linearize_xml()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 345 che coinvolge il nodo canonico `linearize_xml()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 345 che coinvolge il nodo canonico `linearize_xml()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 345 che coinvolge il nodo canonico `linearize_xml()` è `L77`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 345 che coinvolge il nodo canonico `linearize_xml()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 345 che coinvolge il nodo canonico `linearize_xml()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 345 che coinvolge il nodo canonico `linearize_xml()` è `1.0`. Nel grafo unificato, l'arco 345 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `linearize_xml()`. Nel grafo unificato, l'arco 345 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_linearize_xml` per la relazione che coinvolge il nodo canonico `linearize_xml()`.

### Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s

<!-- GRAPH_NODE graph=codice index=180 id=rve_transactions_common_rationale_78 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `L78`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` possiede l'identificatore esatto `rve_transactions_common_rationale_78`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `minificazione conservativa xml: rimuove solo whitespace tra tag.      preserva s`.
<!-- GRAPH_EDGE graph=codice index=386 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `linearize_xml()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_rationale_78` all'ID `rve_transactions_common_linearize_xml`. La relazione tecnica esplicita è la seguente: `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` costituisce una motivazione per `linearize_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 386 che coinvolge il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 386 che coinvolge il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 386 che coinvolge il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 386 che coinvolge il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `L78`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 386 che coinvolge il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 386 che coinvolge il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `ast`. Nel grafo del codice RDG, l'arco 386 registra come `source` l'identificatore `rve_transactions_common_rationale_78` per la relazione che coinvolge il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s`. Nel grafo del codice RDG, l'arco 386 registra come `target` l'identificatore `rve_transactions_common_linearize_xml` per la relazione che coinvolge il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 386 che coinvolge il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=180 id=Request-Dataset-Generator::rve_transactions_common_rationale_78 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `L78`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `minificazione conservativa xml: rimuove solo whitespace tra tag.      preserva s`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `rve_transactions_common_rationale_78`. Nel grafo unificato, il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_rationale_78`.
Nel grafo unificato, il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=386 role=incoming -->
Nel grafo unificato, il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `linearize_xml()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_linearize_xml` all'ID `Request-Dataset-Generator::rve_transactions_common_rationale_78`. La relazione tecnica esplicita è la seguente: `linearize_xml()` costituisce una motivazione per `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s`. Nel grafo unificato, il tipo dichiarato dell'arco 386 che coinvolge il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 386 che coinvolge il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 386 che coinvolge il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 386 che coinvolge il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `L78`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 386 che coinvolge il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 386 che coinvolge il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 386 che coinvolge il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s` è `1.0`. Nel grafo unificato, l'arco 386 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_linearize_xml` per la relazione che coinvolge il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s`. Nel grafo unificato, l'arco 386 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_rationale_78` per la relazione che coinvolge il nodo canonico `Minificazione conservativa XML: rimuove solo whitespace tra tag.      Preserva s`.

### msg_id_to_saml_id()

<!-- GRAPH_NODE graph=codice index=167 id=rve_transactions_common_msg_id_to_saml_id -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `msg_id_to_saml_id()` è `msg_id_to_saml_id()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `msg_id_to_saml_id()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `msg_id_to_saml_id()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `msg_id_to_saml_id()` è `L51`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `msg_id_to_saml_id()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `msg_id_to_saml_id()` possiede l'identificatore esatto `rve_transactions_common_msg_id_to_saml_id`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `msg_id_to_saml_id()` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `msg_id_to_saml_id()` è `msg_id_to_saml_id()`.
Nel grafo del codice RDG, il nodo canonico `msg_id_to_saml_id()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=346 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `msg_id_to_saml_id()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_msg_id_to_saml_id`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `msg_id_to_saml_id()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 346 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 346 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 346 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 346 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 346 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 346 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `ast`. Nel grafo del codice RDG, l'arco 346 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `msg_id_to_saml_id()`. Nel grafo del codice RDG, l'arco 346 registra come `target` l'identificatore `rve_transactions_common_msg_id_to_saml_id` per la relazione che coinvolge il nodo canonico `msg_id_to_saml_id()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 346 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=376 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `msg_id_to_saml_id()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_rationale_52` all'ID `rve_transactions_common_msg_id_to_saml_id`. La relazione tecnica esplicita è la seguente: `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.` costituisce una motivazione per `msg_id_to_saml_id()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 376 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 376 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 376 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 376 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `L52`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 376 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 376 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `ast`. Nel grafo del codice RDG, l'arco 376 registra come `source` l'identificatore `rve_transactions_common_rationale_52` per la relazione che coinvolge il nodo canonico `msg_id_to_saml_id()`. Nel grafo del codice RDG, l'arco 376 registra come `target` l'identificatore `rve_transactions_common_msg_id_to_saml_id` per la relazione che coinvolge il nodo canonico `msg_id_to_saml_id()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 376 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=377 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `msg_id_to_saml_id()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_common_msg_id_to_saml_id`. La relazione tecnica esplicita è la seguente: `rve_1b.py` importa `msg_id_to_saml_id()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 377 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 377 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 377 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 377 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 377 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 377 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 377 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `ast`. Nel grafo del codice RDG, l'arco 377 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `msg_id_to_saml_id()`. Nel grafo del codice RDG, l'arco 377 registra come `target` l'identificatore `rve_transactions_common_msg_id_to_saml_id` per la relazione che coinvolge il nodo canonico `msg_id_to_saml_id()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 377 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=378 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `msg_id_to_saml_id()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_common_msg_id_to_saml_id`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `msg_id_to_saml_id()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 378 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 378 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 378 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 378 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 378 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `L343`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 378 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 378 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `ast`. Nel grafo del codice RDG, l'arco 378 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `msg_id_to_saml_id()`. Nel grafo del codice RDG, l'arco 378 registra come `target` l'identificatore `rve_transactions_common_msg_id_to_saml_id` per la relazione che coinvolge il nodo canonico `msg_id_to_saml_id()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 378 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=167 id=Request-Dataset-Generator::rve_transactions_common_msg_id_to_saml_id -->
Nel grafo unificato, l'etichetta originale del nodo canonico `msg_id_to_saml_id()` è `msg_id_to_saml_id()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `msg_id_to_saml_id()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `msg_id_to_saml_id()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `msg_id_to_saml_id()` è `L51`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `msg_id_to_saml_id()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `msg_id_to_saml_id()` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `msg_id_to_saml_id()` è `msg_id_to_saml_id()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `msg_id_to_saml_id()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `msg_id_to_saml_id()` è `rve_transactions_common_msg_id_to_saml_id`. Nel grafo unificato, il nodo canonico `msg_id_to_saml_id()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_msg_id_to_saml_id`.
<!-- GRAPH_EDGE graph=unificata index=376 role=outgoing -->
Nel grafo unificato, il nodo canonico `msg_id_to_saml_id()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_msg_id_to_saml_id` all'ID `Request-Dataset-Generator::rve_transactions_common_rationale_52`. La relazione tecnica esplicita è la seguente: `msg_id_to_saml_id()` costituisce una motivazione per `Converte un UUID in un SAML ID valido preservando il formato usato da SoapUI.`. Nel grafo unificato, il tipo dichiarato dell'arco 376 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 376 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 376 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 376 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `L52`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 376 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 376 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 376 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `1.0`. Nel grafo unificato, l'arco 376 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_msg_id_to_saml_id` per la relazione che coinvolge il nodo canonico `msg_id_to_saml_id()`. Nel grafo unificato, l'arco 376 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_rationale_52` per la relazione che coinvolge il nodo canonico `msg_id_to_saml_id()`.
<!-- GRAPH_EDGE graph=unificata index=377 role=outgoing -->
Nel grafo unificato, il nodo canonico `msg_id_to_saml_id()` è la sorgente `source` della relazione `imports` verso il nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_msg_id_to_saml_id` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `msg_id_to_saml_id()` importa `rve_1b.py`. Nel grafo unificato, il tipo dichiarato dell'arco 377 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 377 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 377 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 377 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 377 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 377 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 377 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 377 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `1.0`. Nel grafo unificato, l'arco 377 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_msg_id_to_saml_id` per la relazione che coinvolge il nodo canonico `msg_id_to_saml_id()`. Nel grafo unificato, l'arco 377 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `msg_id_to_saml_id()`.
<!-- GRAPH_EDGE graph=unificata index=378 role=outgoing -->
Nel grafo unificato, il nodo canonico `msg_id_to_saml_id()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_msg_id_to_saml_id` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `msg_id_to_saml_id()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 378 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 378 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 378 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 378 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 378 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `L343`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 378 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 378 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 378 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `1.0`. Nel grafo unificato, l'arco 378 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_msg_id_to_saml_id` per la relazione che coinvolge il nodo canonico `msg_id_to_saml_id()`. Nel grafo unificato, l'arco 378 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `msg_id_to_saml_id()`.
<!-- GRAPH_EDGE graph=unificata index=346 role=incoming -->
Nel grafo unificato, il nodo canonico `msg_id_to_saml_id()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_msg_id_to_saml_id`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `msg_id_to_saml_id()`. Nel grafo unificato, il tipo dichiarato dell'arco 346 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 346 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 346 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 346 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 346 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 346 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 346 che coinvolge il nodo canonico `msg_id_to_saml_id()` è `1.0`. Nel grafo unificato, l'arco 346 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `msg_id_to_saml_id()`. Nel grafo unificato, l'arco 346 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_msg_id_to_saml_id` per la relazione che coinvolge il nodo canonico `msg_id_to_saml_id()`.

### new_uuid()

<!-- GRAPH_NODE graph=codice index=166 id=rve_transactions_common_new_uuid -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `new_uuid()` è `new_uuid()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `new_uuid()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `new_uuid()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `new_uuid()` è `L47`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `new_uuid()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `new_uuid()` possiede l'identificatore esatto `rve_transactions_common_new_uuid`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `new_uuid()` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `new_uuid()` è `new_uuid()`.
Nel grafo del codice RDG, il nodo canonico `new_uuid()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=347 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `new_uuid()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_new_uuid`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `new_uuid()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 347 che coinvolge il nodo canonico `new_uuid()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 347 che coinvolge il nodo canonico `new_uuid()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 347 che coinvolge il nodo canonico `new_uuid()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 347 che coinvolge il nodo canonico `new_uuid()` è `L47`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 347 che coinvolge il nodo canonico `new_uuid()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 347 che coinvolge il nodo canonico `new_uuid()` è `ast`. Nel grafo del codice RDG, l'arco 347 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `new_uuid()`. Nel grafo del codice RDG, l'arco 347 registra come `target` l'identificatore `rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `new_uuid()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 347 che coinvolge il nodo canonico `new_uuid()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=371 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `new_uuid()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_mock_jwt()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_build_mock_jwt` all'ID `rve_transactions_common_new_uuid`. La relazione tecnica esplicita è la seguente: `build_mock_jwt()` chiama `new_uuid()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 371 che coinvolge il nodo canonico `new_uuid()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 371 che coinvolge il nodo canonico `new_uuid()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 371 che coinvolge il nodo canonico `new_uuid()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 371 che coinvolge il nodo canonico `new_uuid()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 371 che coinvolge il nodo canonico `new_uuid()` è `L164`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 371 che coinvolge il nodo canonico `new_uuid()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 371 che coinvolge il nodo canonico `new_uuid()` è `ast`. Nel grafo del codice RDG, l'arco 371 registra come `source` l'identificatore `rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `new_uuid()`. Nel grafo del codice RDG, l'arco 371 registra come `target` l'identificatore `rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `new_uuid()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 371 che coinvolge il nodo canonico `new_uuid()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=372 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `new_uuid()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `rve_121.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121` all'ID `rve_transactions_common_new_uuid`. La relazione tecnica esplicita è la seguente: `rve_121.py` importa `new_uuid()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 372 che coinvolge il nodo canonico `new_uuid()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 372 che coinvolge il nodo canonico `new_uuid()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 372 che coinvolge il nodo canonico `new_uuid()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 372 che coinvolge il nodo canonico `new_uuid()` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 372 che coinvolge il nodo canonico `new_uuid()` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 372 che coinvolge il nodo canonico `new_uuid()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 372 che coinvolge il nodo canonico `new_uuid()` è `ast`. Nel grafo del codice RDG, l'arco 372 registra come `source` l'identificatore `rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `new_uuid()`. Nel grafo del codice RDG, l'arco 372 registra come `target` l'identificatore `rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `new_uuid()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 372 che coinvolge il nodo canonico `new_uuid()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=373 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `new_uuid()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_request() [rve_transactions_rve_121_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121_build_request` all'ID `rve_transactions_common_new_uuid`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_121_build_request]` chiama `new_uuid()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 373 che coinvolge il nodo canonico `new_uuid()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 373 che coinvolge il nodo canonico `new_uuid()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 373 che coinvolge il nodo canonico `new_uuid()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 373 che coinvolge il nodo canonico `new_uuid()` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 373 che coinvolge il nodo canonico `new_uuid()` è `L112`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 373 che coinvolge il nodo canonico `new_uuid()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 373 che coinvolge il nodo canonico `new_uuid()` è `ast`. Nel grafo del codice RDG, l'arco 373 registra come `source` l'identificatore `rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `new_uuid()`. Nel grafo del codice RDG, l'arco 373 registra come `target` l'identificatore `rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `new_uuid()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 373 che coinvolge il nodo canonico `new_uuid()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=374 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `new_uuid()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_common_new_uuid`. La relazione tecnica esplicita è la seguente: `rve_1b.py` importa `new_uuid()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 374 che coinvolge il nodo canonico `new_uuid()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 374 che coinvolge il nodo canonico `new_uuid()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 374 che coinvolge il nodo canonico `new_uuid()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 374 che coinvolge il nodo canonico `new_uuid()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 374 che coinvolge il nodo canonico `new_uuid()` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 374 che coinvolge il nodo canonico `new_uuid()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 374 che coinvolge il nodo canonico `new_uuid()` è `ast`. Nel grafo del codice RDG, l'arco 374 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `new_uuid()`. Nel grafo del codice RDG, l'arco 374 registra come `target` l'identificatore `rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `new_uuid()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 374 che coinvolge il nodo canonico `new_uuid()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=375 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `new_uuid()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_common_new_uuid`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `new_uuid()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 375 che coinvolge il nodo canonico `new_uuid()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 375 che coinvolge il nodo canonico `new_uuid()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 375 che coinvolge il nodo canonico `new_uuid()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 375 che coinvolge il nodo canonico `new_uuid()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 375 che coinvolge il nodo canonico `new_uuid()` è `L342`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 375 che coinvolge il nodo canonico `new_uuid()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 375 che coinvolge il nodo canonico `new_uuid()` è `ast`. Nel grafo del codice RDG, l'arco 375 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `new_uuid()`. Nel grafo del codice RDG, l'arco 375 registra come `target` l'identificatore `rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `new_uuid()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 375 che coinvolge il nodo canonico `new_uuid()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=166 id=Request-Dataset-Generator::rve_transactions_common_new_uuid -->
Nel grafo unificato, l'etichetta originale del nodo canonico `new_uuid()` è `new_uuid()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `new_uuid()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `new_uuid()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `new_uuid()` è `L47`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `new_uuid()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `new_uuid()` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `new_uuid()` è `new_uuid()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `new_uuid()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `new_uuid()` è `rve_transactions_common_new_uuid`. Nel grafo unificato, il nodo canonico `new_uuid()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_new_uuid`.
<!-- GRAPH_EDGE graph=unificata index=371 role=outgoing -->
Nel grafo unificato, il nodo canonico `new_uuid()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_mock_jwt()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_new_uuid` all'ID `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt`. La relazione tecnica esplicita è la seguente: `new_uuid()` chiama `build_mock_jwt()`. Nel grafo unificato, il tipo dichiarato dell'arco 371 che coinvolge il nodo canonico `new_uuid()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 371 che coinvolge il nodo canonico `new_uuid()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 371 che coinvolge il nodo canonico `new_uuid()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 371 che coinvolge il nodo canonico `new_uuid()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 371 che coinvolge il nodo canonico `new_uuid()` è `L164`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 371 che coinvolge il nodo canonico `new_uuid()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 371 che coinvolge il nodo canonico `new_uuid()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 371 che coinvolge il nodo canonico `new_uuid()` è `1.0`. Nel grafo unificato, l'arco 371 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `new_uuid()`. Nel grafo unificato, l'arco 371 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `new_uuid()`.
<!-- GRAPH_EDGE graph=unificata index=372 role=outgoing -->
Nel grafo unificato, il nodo canonico `new_uuid()` è la sorgente `source` della relazione `imports` verso il nodo canonico `rve_121.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_new_uuid` all'ID `Request-Dataset-Generator::rve_transactions_rve_121`. La relazione tecnica esplicita è la seguente: `new_uuid()` importa `rve_121.py`. Nel grafo unificato, il tipo dichiarato dell'arco 372 che coinvolge il nodo canonico `new_uuid()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 372 che coinvolge il nodo canonico `new_uuid()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 372 che coinvolge il nodo canonico `new_uuid()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 372 che coinvolge il nodo canonico `new_uuid()` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 372 che coinvolge il nodo canonico `new_uuid()` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 372 che coinvolge il nodo canonico `new_uuid()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 372 che coinvolge il nodo canonico `new_uuid()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 372 che coinvolge il nodo canonico `new_uuid()` è `1.0`. Nel grafo unificato, l'arco 372 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `new_uuid()`. Nel grafo unificato, l'arco 372 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `new_uuid()`.
<!-- GRAPH_EDGE graph=unificata index=373 role=outgoing -->
Nel grafo unificato, il nodo canonico `new_uuid()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_new_uuid` all'ID `Request-Dataset-Generator::rve_transactions_rve_121_build_request`. La relazione tecnica esplicita è la seguente: `new_uuid()` chiama `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 373 che coinvolge il nodo canonico `new_uuid()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 373 che coinvolge il nodo canonico `new_uuid()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 373 che coinvolge il nodo canonico `new_uuid()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 373 che coinvolge il nodo canonico `new_uuid()` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 373 che coinvolge il nodo canonico `new_uuid()` è `L112`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 373 che coinvolge il nodo canonico `new_uuid()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 373 che coinvolge il nodo canonico `new_uuid()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 373 che coinvolge il nodo canonico `new_uuid()` è `1.0`. Nel grafo unificato, l'arco 373 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `new_uuid()`. Nel grafo unificato, l'arco 373 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `new_uuid()`.
<!-- GRAPH_EDGE graph=unificata index=374 role=outgoing -->
Nel grafo unificato, il nodo canonico `new_uuid()` è la sorgente `source` della relazione `imports` verso il nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_new_uuid` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `new_uuid()` importa `rve_1b.py`. Nel grafo unificato, il tipo dichiarato dell'arco 374 che coinvolge il nodo canonico `new_uuid()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 374 che coinvolge il nodo canonico `new_uuid()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 374 che coinvolge il nodo canonico `new_uuid()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 374 che coinvolge il nodo canonico `new_uuid()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 374 che coinvolge il nodo canonico `new_uuid()` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 374 che coinvolge il nodo canonico `new_uuid()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 374 che coinvolge il nodo canonico `new_uuid()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 374 che coinvolge il nodo canonico `new_uuid()` è `1.0`. Nel grafo unificato, l'arco 374 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `new_uuid()`. Nel grafo unificato, l'arco 374 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `new_uuid()`.
<!-- GRAPH_EDGE graph=unificata index=375 role=outgoing -->
Nel grafo unificato, il nodo canonico `new_uuid()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_new_uuid` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `new_uuid()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 375 che coinvolge il nodo canonico `new_uuid()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 375 che coinvolge il nodo canonico `new_uuid()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 375 che coinvolge il nodo canonico `new_uuid()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 375 che coinvolge il nodo canonico `new_uuid()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 375 che coinvolge il nodo canonico `new_uuid()` è `L342`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 375 che coinvolge il nodo canonico `new_uuid()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 375 che coinvolge il nodo canonico `new_uuid()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 375 che coinvolge il nodo canonico `new_uuid()` è `1.0`. Nel grafo unificato, l'arco 375 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `new_uuid()`. Nel grafo unificato, l'arco 375 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `new_uuid()`.
<!-- GRAPH_EDGE graph=unificata index=347 role=incoming -->
Nel grafo unificato, il nodo canonico `new_uuid()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_new_uuid`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `new_uuid()`. Nel grafo unificato, il tipo dichiarato dell'arco 347 che coinvolge il nodo canonico `new_uuid()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 347 che coinvolge il nodo canonico `new_uuid()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 347 che coinvolge il nodo canonico `new_uuid()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 347 che coinvolge il nodo canonico `new_uuid()` è `L47`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 347 che coinvolge il nodo canonico `new_uuid()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 347 che coinvolge il nodo canonico `new_uuid()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 347 che coinvolge il nodo canonico `new_uuid()` è `1.0`. Nel grafo unificato, l'arco 347 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `new_uuid()`. Nel grafo unificato, l'arco 347 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `new_uuid()`.

### RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe

<!-- GRAPH_NODE graph=codice index=225 id=rve_transactions_rve_1b_rationale_1 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` vale `ast`. Nel grafo del codice RDG, il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` possiede l'identificatore esatto `rve_transactions_rve_1b_rationale_1`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `rve-1.b - soap authenticateandgetassertion-b  costruisce il soap envelope 1.2 pe`.
<!-- GRAPH_EDGE graph=codice index=470 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_rationale_1` all'ID `rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` costituisce una motivazione per `rve_1b.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 470 che coinvolge il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 470 che coinvolge il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 470 che coinvolge il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 470 che coinvolge il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 470 che coinvolge il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 470 che coinvolge il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `ast`. Nel grafo del codice RDG, l'arco 470 registra come `source` l'identificatore `rve_transactions_rve_1b_rationale_1` per la relazione che coinvolge il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe`. Nel grafo del codice RDG, l'arco 470 registra come `target` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 470 che coinvolge il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=225 id=Request-Dataset-Generator::rve_transactions_rve_1b_rationale_1 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `rve-1.b - soap authenticateandgetassertion-b  costruisce il soap envelope 1.2 pe`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `rve_transactions_rve_1b_rationale_1`. Nel grafo unificato, il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_1`.
Nel grafo unificato, il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=470 role=incoming -->
Nel grafo unificato, il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_1`. La relazione tecnica esplicita è la seguente: `rve_1b.py` costituisce una motivazione per `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe`. Nel grafo unificato, il tipo dichiarato dell'arco 470 che coinvolge il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 470 che coinvolge il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 470 che coinvolge il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 470 che coinvolge il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 470 che coinvolge il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 470 che coinvolge il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 470 che coinvolge il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` è `1.0`. Nel grafo unificato, l'arco 470 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe`. Nel grafo unificato, l'arco 470 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_1` per la relazione che coinvolge il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe`.

### RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT

<!-- GRAPH_NODE graph=codice index=210 id=rve_transactions_rve_121_rationale_1 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` vale `ast`. Nel grafo del codice RDG, il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` possiede l'identificatore esatto `rve_transactions_rve_121_rationale_1`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `rve-121 — rest getaccesstoken  scambia la saml assertion (da rve-1.b) con un jwt`.
<!-- GRAPH_EDGE graph=codice index=457 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `rve_121.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121_rationale_1` all'ID `rve_transactions_rve_121`. La relazione tecnica esplicita è la seguente: `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` costituisce una motivazione per `rve_121.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 457 che coinvolge il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 457 che coinvolge il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 457 che coinvolge il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 457 che coinvolge il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 457 che coinvolge il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 457 che coinvolge il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `ast`. Nel grafo del codice RDG, l'arco 457 registra come `source` l'identificatore `rve_transactions_rve_121_rationale_1` per la relazione che coinvolge il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT`. Nel grafo del codice RDG, l'arco 457 registra come `target` l'identificatore `rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 457 che coinvolge il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=210 id=Request-Dataset-Generator::rve_transactions_rve_121_rationale_1 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `rve-121 — rest getaccesstoken  scambia la saml assertion (da rve-1.b) con un jwt`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `rve_transactions_rve_121_rationale_1`. Nel grafo unificato, il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_121_rationale_1`.
Nel grafo unificato, il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=457 role=incoming -->
Nel grafo unificato, il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `rve_121.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_121` all'ID `Request-Dataset-Generator::rve_transactions_rve_121_rationale_1`. La relazione tecnica esplicita è la seguente: `rve_121.py` costituisce una motivazione per `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT`. Nel grafo unificato, il tipo dichiarato dell'arco 457 che coinvolge il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 457 che coinvolge il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 457 che coinvolge il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 457 che coinvolge il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 457 che coinvolge il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 457 che coinvolge il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 457 che coinvolge il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` è `1.0`. Nel grafo unificato, l'arco 457 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT`. Nel grafo unificato, l'arco 457 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121_rationale_1` per la relazione che coinvolge il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT`.

### RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass

<!-- GRAPH_NODE graph=codice index=232 id=rve_transactions_rve_54_rationale_1 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` vale `ast`. Nel grafo del codice RDG, il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` possiede l'identificatore esatto `rve_transactions_rve_54_rationale_1`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `rve-54 — rest patient query  ricerca anagrafica paziente presentando la saml ass`.
<!-- GRAPH_EDGE graph=codice index=484 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `rve_54.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54_rationale_1` all'ID `rve_transactions_rve_54`. La relazione tecnica esplicita è la seguente: `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` costituisce una motivazione per `rve_54.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 484 che coinvolge il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 484 che coinvolge il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 484 che coinvolge il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 484 che coinvolge il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 484 che coinvolge il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 484 che coinvolge il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `ast`. Nel grafo del codice RDG, l'arco 484 registra come `source` l'identificatore `rve_transactions_rve_54_rationale_1` per la relazione che coinvolge il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass`. Nel grafo del codice RDG, l'arco 484 registra come `target` l'identificatore `rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 484 che coinvolge il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=232 id=Request-Dataset-Generator::rve_transactions_rve_54_rationale_1 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `rve-54 — rest patient query  ricerca anagrafica paziente presentando la saml ass`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `rve_transactions_rve_54_rationale_1`. Nel grafo unificato, il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_54_rationale_1`.
Nel grafo unificato, il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=484 role=incoming -->
Nel grafo unificato, il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `rve_54.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_54` all'ID `Request-Dataset-Generator::rve_transactions_rve_54_rationale_1`. La relazione tecnica esplicita è la seguente: `rve_54.py` costituisce una motivazione per `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass`. Nel grafo unificato, il tipo dichiarato dell'arco 484 che coinvolge il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 484 che coinvolge il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 484 che coinvolge il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 484 che coinvolge il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 484 che coinvolge il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 484 che coinvolge il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 484 che coinvolge il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` è `1.0`. Nel grafo unificato, l'arco 484 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass`. Nel grafo unificato, l'arco 484 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_rationale_1` per la relazione che coinvolge il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass`.

### rve_121.py

<!-- GRAPH_NODE graph=codice index=208 id=rve_transactions_rve_121 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `rve_121.py` è `rve_121.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `rve_121.py` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `rve_121.py` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `rve_121.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `rve_121.py` vale `ast`. Nel grafo del codice RDG, il nodo canonico `rve_121.py` possiede l'identificatore esatto `rve_transactions_rve_121`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `rve_121.py` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `rve_121.py` è `rve_121.py`.
<!-- GRAPH_EDGE graph=codice index=354 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_121.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121` all'ID `rve_transactions_common`. La relazione tecnica esplicita è la seguente: `rve_121.py` importa da `_common.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 354 che coinvolge il nodo canonico `rve_121.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 354 che coinvolge il nodo canonico `rve_121.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 354 che coinvolge il nodo canonico `rve_121.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 354 che coinvolge il nodo canonico `rve_121.py` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 354 che coinvolge il nodo canonico `rve_121.py` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 354 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 354 che coinvolge il nodo canonico `rve_121.py` è `ast`. Nel grafo del codice RDG, l'arco 354 registra come `source` l'identificatore `rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo del codice RDG, l'arco 354 registra come `target` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 354 che coinvolge il nodo canonico `rve_121.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=372 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_121.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `new_uuid()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121` all'ID `rve_transactions_common_new_uuid`. La relazione tecnica esplicita è la seguente: `rve_121.py` importa `new_uuid()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 372 che coinvolge il nodo canonico `rve_121.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 372 che coinvolge il nodo canonico `rve_121.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 372 che coinvolge il nodo canonico `rve_121.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 372 che coinvolge il nodo canonico `rve_121.py` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 372 che coinvolge il nodo canonico `rve_121.py` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 372 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 372 che coinvolge il nodo canonico `rve_121.py` è `ast`. Nel grafo del codice RDG, l'arco 372 registra come `source` l'identificatore `rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo del codice RDG, l'arco 372 registra come `target` l'identificatore `rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 372 che coinvolge il nodo canonico `rve_121.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=392 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_121.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `build_mock_jwt()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121` all'ID `rve_transactions_common_build_mock_jwt`. La relazione tecnica esplicita è la seguente: `rve_121.py` importa `build_mock_jwt()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 392 che coinvolge il nodo canonico `rve_121.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 392 che coinvolge il nodo canonico `rve_121.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 392 che coinvolge il nodo canonico `rve_121.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 392 che coinvolge il nodo canonico `rve_121.py` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 392 che coinvolge il nodo canonico `rve_121.py` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 392 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 392 che coinvolge il nodo canonico `rve_121.py` è `ast`. Nel grafo del codice RDG, l'arco 392 registra come `source` l'identificatore `rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo del codice RDG, l'arco 392 registra come `target` l'identificatore `rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 392 che coinvolge il nodo canonico `rve_121.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=395 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_121.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `extract_patient_cf()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121` all'ID `rve_transactions_common_extract_patient_cf`. La relazione tecnica esplicita è la seguente: `rve_121.py` importa `extract_patient_cf()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 395 che coinvolge il nodo canonico `rve_121.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 395 che coinvolge il nodo canonico `rve_121.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 395 che coinvolge il nodo canonico `rve_121.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 395 che coinvolge il nodo canonico `rve_121.py` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 395 che coinvolge il nodo canonico `rve_121.py` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 395 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 395 che coinvolge il nodo canonico `rve_121.py` è `ast`. Nel grafo del codice RDG, l'arco 395 registra come `source` l'identificatore `rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo del codice RDG, l'arco 395 registra come `target` l'identificatore `rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 395 che coinvolge il nodo canonico `rve_121.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=401 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_121.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `extract_patient_mpi()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121` all'ID `rve_transactions_common_extract_patient_mpi`. La relazione tecnica esplicita è la seguente: `rve_121.py` importa `extract_patient_mpi()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 401 che coinvolge il nodo canonico `rve_121.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 401 che coinvolge il nodo canonico `rve_121.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 401 che coinvolge il nodo canonico `rve_121.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 401 che coinvolge il nodo canonico `rve_121.py` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 401 che coinvolge il nodo canonico `rve_121.py` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 401 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 401 che coinvolge il nodo canonico `rve_121.py` è `ast`. Nel grafo del codice RDG, l'arco 401 registra come `source` l'identificatore `rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo del codice RDG, l'arco 401 registra come `target` l'identificatore `rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 401 che coinvolge il nodo canonico `rve_121.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=456 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_121.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121` all'ID `rve_transactions_rve_121_build_request`. La relazione tecnica esplicita è la seguente: `rve_121.py` contiene `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 456 che coinvolge il nodo canonico `rve_121.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 456 che coinvolge il nodo canonico `rve_121.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 456 che coinvolge il nodo canonico `rve_121.py` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 456 che coinvolge il nodo canonico `rve_121.py` è `L32`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 456 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 456 che coinvolge il nodo canonico `rve_121.py` è `ast`. Nel grafo del codice RDG, l'arco 456 registra come `source` l'identificatore `rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo del codice RDG, l'arco 456 registra come `target` l'identificatore `rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 456 che coinvolge il nodo canonico `rve_121.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=411 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `rve_121.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `flow_orchestrator.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_flow_orchestrator` all'ID `rve_transactions_rve_121`. La relazione tecnica esplicita è la seguente: `flow_orchestrator.py` importa da `rve_121.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 411 che coinvolge il nodo canonico `rve_121.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 411 che coinvolge il nodo canonico `rve_121.py` è `submodule_import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 411 che coinvolge il nodo canonico `rve_121.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 411 che coinvolge il nodo canonico `rve_121.py` è `rve_transactions/flow_orchestrator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 411 che coinvolge il nodo canonico `rve_121.py` è `L26`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 411 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 411 che coinvolge il nodo canonico `rve_121.py` è `ast`. Nel grafo del codice RDG, l'arco 411 registra come `source` l'identificatore `rve_transactions_flow_orchestrator` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo del codice RDG, l'arco 411 registra come `target` l'identificatore `rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 411 che coinvolge il nodo canonico `rve_121.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=457 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `rve_121.py` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_121_rationale_1` all'ID `rve_transactions_rve_121`. La relazione tecnica esplicita è la seguente: `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT` costituisce una motivazione per `rve_121.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 457 che coinvolge il nodo canonico `rve_121.py` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 457 che coinvolge il nodo canonico `rve_121.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 457 che coinvolge il nodo canonico `rve_121.py` è `rve_transactions/rve_121.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 457 che coinvolge il nodo canonico `rve_121.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 457 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 457 che coinvolge il nodo canonico `rve_121.py` è `ast`. Nel grafo del codice RDG, l'arco 457 registra come `source` l'identificatore `rve_transactions_rve_121_rationale_1` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo del codice RDG, l'arco 457 registra come `target` l'identificatore `rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 457 che coinvolge il nodo canonico `rve_121.py` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=208 id=Request-Dataset-Generator::rve_transactions_rve_121 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `rve_121.py` è `rve_121.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `rve_121.py` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `rve_121.py` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `rve_121.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `rve_121.py` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `rve_121.py` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `rve_121.py` è `rve_121.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `rve_121.py` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `rve_121.py` è `rve_transactions_rve_121`. Nel grafo unificato, il nodo canonico `rve_121.py` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_121`.
<!-- GRAPH_EDGE graph=unificata index=456 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_121.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `build_request() [rve_transactions_rve_121_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_121` all'ID `Request-Dataset-Generator::rve_transactions_rve_121_build_request`. La relazione tecnica esplicita è la seguente: `rve_121.py` contiene `build_request() [rve_transactions_rve_121_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 456 che coinvolge il nodo canonico `rve_121.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 456 che coinvolge il nodo canonico `rve_121.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 456 che coinvolge il nodo canonico `rve_121.py` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 456 che coinvolge il nodo canonico `rve_121.py` è `L32`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 456 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 456 che coinvolge il nodo canonico `rve_121.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 456 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo unificato, l'arco 456 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo unificato, l'arco 456 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121_build_request` per la relazione che coinvolge il nodo canonico `rve_121.py`.
<!-- GRAPH_EDGE graph=unificata index=457 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_121.py` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_121` all'ID `Request-Dataset-Generator::rve_transactions_rve_121_rationale_1`. La relazione tecnica esplicita è la seguente: `rve_121.py` costituisce una motivazione per `RVE-121 — REST GetAccessToken  Scambia la SAML Assertion (da RVE-1.b) con un JWT`. Nel grafo unificato, il tipo dichiarato dell'arco 457 che coinvolge il nodo canonico `rve_121.py` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 457 che coinvolge il nodo canonico `rve_121.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 457 che coinvolge il nodo canonico `rve_121.py` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 457 che coinvolge il nodo canonico `rve_121.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 457 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 457 che coinvolge il nodo canonico `rve_121.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 457 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo unificato, l'arco 457 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo unificato, l'arco 457 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121_rationale_1` per la relazione che coinvolge il nodo canonico `rve_121.py`.
<!-- GRAPH_EDGE graph=unificata index=1149 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_121.py` è la sorgente `source` della relazione `implements` verso il nodo canonico `RVE-121 GetAccessToken`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_121` all'ID `graphify_kb::context_rve121_get_access_token`. La relazione tecnica esplicita è la seguente: `rve_121.py` implementa `RVE-121 GetAccessToken`. Nel grafo unificato, l'arco 1149 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo unificato, l'arco 1149 registra come `target` l'identificatore `graphify_kb::context_rve121_get_access_token` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo unificato, il tipo dichiarato dell'arco 1149 che coinvolge il nodo canonico `rve_121.py` è `implements`. Nel grafo unificato, la confidenza categoriale dell'arco 1149 che coinvolge il nodo canonico `rve_121.py` è `INFERRED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1149 che coinvolge il nodo canonico `rve_121.py` è `0.95`. Nel grafo unificato, il file sorgente dell'arco 1149 che coinvolge il nodo canonico `rve_121.py` è `knowledge_base/unified_graph/README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1149 che coinvolge il nodo canonico `rve_121.py` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1149 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 1149 che coinvolge il nodo canonico `rve_121.py` è `unified_bridge`.
<!-- GRAPH_EDGE graph=unificata index=354 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_121.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_rve_121`. La relazione tecnica esplicita è la seguente: `_common.py` importa da `rve_121.py`. Nel grafo unificato, il tipo dichiarato dell'arco 354 che coinvolge il nodo canonico `rve_121.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 354 che coinvolge il nodo canonico `rve_121.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 354 che coinvolge il nodo canonico `rve_121.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 354 che coinvolge il nodo canonico `rve_121.py` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 354 che coinvolge il nodo canonico `rve_121.py` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 354 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 354 che coinvolge il nodo canonico `rve_121.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 354 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo unificato, l'arco 354 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo unificato, l'arco 354 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `rve_121.py`.
<!-- GRAPH_EDGE graph=unificata index=372 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_121.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `new_uuid()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_new_uuid` all'ID `Request-Dataset-Generator::rve_transactions_rve_121`. La relazione tecnica esplicita è la seguente: `new_uuid()` importa `rve_121.py`. Nel grafo unificato, il tipo dichiarato dell'arco 372 che coinvolge il nodo canonico `rve_121.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 372 che coinvolge il nodo canonico `rve_121.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 372 che coinvolge il nodo canonico `rve_121.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 372 che coinvolge il nodo canonico `rve_121.py` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 372 che coinvolge il nodo canonico `rve_121.py` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 372 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 372 che coinvolge il nodo canonico `rve_121.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 372 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo unificato, l'arco 372 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo unificato, l'arco 372 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `rve_121.py`.
<!-- GRAPH_EDGE graph=unificata index=392 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_121.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `build_mock_jwt()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt` all'ID `Request-Dataset-Generator::rve_transactions_rve_121`. La relazione tecnica esplicita è la seguente: `build_mock_jwt()` importa `rve_121.py`. Nel grafo unificato, il tipo dichiarato dell'arco 392 che coinvolge il nodo canonico `rve_121.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 392 che coinvolge il nodo canonico `rve_121.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 392 che coinvolge il nodo canonico `rve_121.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 392 che coinvolge il nodo canonico `rve_121.py` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 392 che coinvolge il nodo canonico `rve_121.py` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 392 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 392 che coinvolge il nodo canonico `rve_121.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 392 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo unificato, l'arco 392 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo unificato, l'arco 392 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `rve_121.py`.
<!-- GRAPH_EDGE graph=unificata index=395 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_121.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `extract_patient_cf()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` all'ID `Request-Dataset-Generator::rve_transactions_rve_121`. La relazione tecnica esplicita è la seguente: `extract_patient_cf()` importa `rve_121.py`. Nel grafo unificato, il tipo dichiarato dell'arco 395 che coinvolge il nodo canonico `rve_121.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 395 che coinvolge il nodo canonico `rve_121.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 395 che coinvolge il nodo canonico `rve_121.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 395 che coinvolge il nodo canonico `rve_121.py` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 395 che coinvolge il nodo canonico `rve_121.py` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 395 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 395 che coinvolge il nodo canonico `rve_121.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 395 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo unificato, l'arco 395 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo unificato, l'arco 395 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `rve_121.py`.
<!-- GRAPH_EDGE graph=unificata index=401 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_121.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `extract_patient_mpi()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` all'ID `Request-Dataset-Generator::rve_transactions_rve_121`. La relazione tecnica esplicita è la seguente: `extract_patient_mpi()` importa `rve_121.py`. Nel grafo unificato, il tipo dichiarato dell'arco 401 che coinvolge il nodo canonico `rve_121.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 401 che coinvolge il nodo canonico `rve_121.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 401 che coinvolge il nodo canonico `rve_121.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 401 che coinvolge il nodo canonico `rve_121.py` è `rve_transactions/rve_121.py`. Nel grafo unificato, la posizione sorgente dell'arco 401 che coinvolge il nodo canonico `rve_121.py` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 401 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 401 che coinvolge il nodo canonico `rve_121.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 401 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo unificato, l'arco 401 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo unificato, l'arco 401 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `rve_121.py`.
<!-- GRAPH_EDGE graph=unificata index=411 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_121.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `flow_orchestrator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator` all'ID `Request-Dataset-Generator::rve_transactions_rve_121`. La relazione tecnica esplicita è la seguente: `flow_orchestrator.py` importa da `rve_121.py`. Nel grafo unificato, il tipo dichiarato dell'arco 411 che coinvolge il nodo canonico `rve_121.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 411 che coinvolge il nodo canonico `rve_121.py` è `submodule_import`. Nel grafo unificato, la confidenza categoriale dell'arco 411 che coinvolge il nodo canonico `rve_121.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 411 che coinvolge il nodo canonico `rve_121.py` è `rve_transactions/flow_orchestrator.py`. Nel grafo unificato, la posizione sorgente dell'arco 411 che coinvolge il nodo canonico `rve_121.py` è `L26`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 411 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 411 che coinvolge il nodo canonico `rve_121.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 411 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo unificato, l'arco 411 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo unificato, l'arco 411 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `rve_121.py`.
<!-- GRAPH_EDGE graph=unificata index=1151 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_121.py` è il destinatario `target` della relazione `conceptually_related_to` proveniente dal nodo canonico `E-RVE121-001`; l'orientamento registrato nel JSON va dall'ID `experiments::knowledge_base_experiments_20260727t185227_experiment_summary_e_rve121_001` all'ID `Request-Dataset-Generator::rve_transactions_rve_121`. La relazione tecnica esplicita è la seguente: `E-RVE121-001` è concettualmente correlato a `rve_121.py`. Nel grafo unificato, l'arco 1151 registra come `source` l'identificatore `experiments::knowledge_base_experiments_20260727t185227_experiment_summary_e_rve121_001` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo unificato, l'arco 1151 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_121` per la relazione che coinvolge il nodo canonico `rve_121.py`. Nel grafo unificato, il tipo dichiarato dell'arco 1151 che coinvolge il nodo canonico `rve_121.py` è `conceptually_related_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1151 che coinvolge il nodo canonico `rve_121.py` è `INFERRED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1151 che coinvolge il nodo canonico `rve_121.py` è `0.85`. Nel grafo unificato, il file sorgente dell'arco 1151 che coinvolge il nodo canonico `rve_121.py` è `knowledge_base/unified_graph/README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1151 che coinvolge il nodo canonico `rve_121.py` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1151 che coinvolge il nodo canonico `rve_121.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 1151 che coinvolge il nodo canonico `rve_121.py` è `unified_bridge`.

### rve_1b.py

<!-- GRAPH_NODE graph=codice index=216 id=rve_transactions_rve_1b -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `rve_1b.py` è `rve_1b.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `rve_1b.py` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `rve_1b.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `rve_1b.py` vale `ast`. Nel grafo del codice RDG, il nodo canonico `rve_1b.py` possiede l'identificatore esatto `rve_transactions_rve_1b`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `rve_1b.py` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `rve_1b.py` è `rve_1b.py`.
<!-- GRAPH_EDGE graph=codice index=355 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_common`. La relazione tecnica esplicita è la seguente: `rve_1b.py` importa da `_common.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 355 che coinvolge il nodo canonico `rve_1b.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 355 che coinvolge il nodo canonico `rve_1b.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 355 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 355 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 355 che coinvolge il nodo canonico `rve_1b.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 355 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 355 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo del codice RDG, l'arco 355 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, l'arco 355 registra come `target` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 355 che coinvolge il nodo canonico `rve_1b.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=363 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `utc_now()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_common_utc_now`. La relazione tecnica esplicita è la seguente: `rve_1b.py` importa `utc_now()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 363 che coinvolge il nodo canonico `rve_1b.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 363 che coinvolge il nodo canonico `rve_1b.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 363 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 363 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 363 che coinvolge il nodo canonico `rve_1b.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 363 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 363 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo del codice RDG, l'arco 363 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, l'arco 363 registra come `target` l'identificatore `rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 363 che coinvolge il nodo canonico `rve_1b.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=367 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `utc_iso()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_common_utc_iso`. La relazione tecnica esplicita è la seguente: `rve_1b.py` importa `utc_iso()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 367 che coinvolge il nodo canonico `rve_1b.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 367 che coinvolge il nodo canonico `rve_1b.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 367 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 367 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 367 che coinvolge il nodo canonico `rve_1b.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 367 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 367 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo del codice RDG, l'arco 367 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, l'arco 367 registra come `target` l'identificatore `rve_transactions_common_utc_iso` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 367 che coinvolge il nodo canonico `rve_1b.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=369 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `utc_iso_ms()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_common_utc_iso_ms`. La relazione tecnica esplicita è la seguente: `rve_1b.py` importa `utc_iso_ms()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 369 che coinvolge il nodo canonico `rve_1b.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 369 che coinvolge il nodo canonico `rve_1b.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 369 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 369 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 369 che coinvolge il nodo canonico `rve_1b.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 369 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 369 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo del codice RDG, l'arco 369 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, l'arco 369 registra come `target` l'identificatore `rve_transactions_common_utc_iso_ms` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 369 che coinvolge il nodo canonico `rve_1b.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=374 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `new_uuid()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_common_new_uuid`. La relazione tecnica esplicita è la seguente: `rve_1b.py` importa `new_uuid()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 374 che coinvolge il nodo canonico `rve_1b.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 374 che coinvolge il nodo canonico `rve_1b.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 374 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 374 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 374 che coinvolge il nodo canonico `rve_1b.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 374 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 374 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo del codice RDG, l'arco 374 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, l'arco 374 registra come `target` l'identificatore `rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 374 che coinvolge il nodo canonico `rve_1b.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=377 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `msg_id_to_saml_id()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_common_msg_id_to_saml_id`. La relazione tecnica esplicita è la seguente: `rve_1b.py` importa `msg_id_to_saml_id()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 377 che coinvolge il nodo canonico `rve_1b.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 377 che coinvolge il nodo canonico `rve_1b.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 377 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 377 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 377 che coinvolge il nodo canonico `rve_1b.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 377 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 377 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo del codice RDG, l'arco 377 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, l'arco 377 registra come `target` l'identificatore `rve_transactions_common_msg_id_to_saml_id` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 377 che coinvolge il nodo canonico `rve_1b.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=380 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `b64_encode()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_common_b64_encode`. La relazione tecnica esplicita è la seguente: `rve_1b.py` importa `b64_encode()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 380 che coinvolge il nodo canonico `rve_1b.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 380 che coinvolge il nodo canonico `rve_1b.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 380 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 380 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 380 che coinvolge il nodo canonico `rve_1b.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 380 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 380 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo del codice RDG, l'arco 380 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, l'arco 380 registra come `target` l'identificatore `rve_transactions_common_b64_encode` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 380 che coinvolge il nodo canonico `rve_1b.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=387 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `linearize_xml()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_common_linearize_xml`. La relazione tecnica esplicita è la seguente: `rve_1b.py` importa `linearize_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 387 che coinvolge il nodo canonico `rve_1b.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 387 che coinvolge il nodo canonico `rve_1b.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 387 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 387 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 387 che coinvolge il nodo canonico `rve_1b.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 387 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 387 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo del codice RDG, l'arco 387 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, l'arco 387 registra come `target` l'identificatore `rve_transactions_common_linearize_xml` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 387 che coinvolge il nodo canonico `rve_1b.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=462 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_build_mock_assertion()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_rve_1b_build_mock_assertion`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_build_mock_assertion()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 462 che coinvolge il nodo canonico `rve_1b.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 462 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 462 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 462 che coinvolge il nodo canonico `rve_1b.py` è `L226`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 462 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 462 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo del codice RDG, l'arco 462 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, l'arco 462 registra come `target` l'identificatore `rve_transactions_rve_1b_build_mock_assertion` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 462 che coinvolge il nodo canonico `rve_1b.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=463 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_build_mock_soap_response()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_rve_1b_build_mock_soap_response`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_build_mock_soap_response()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 463 che coinvolge il nodo canonico `rve_1b.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 463 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 463 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 463 che coinvolge il nodo canonico `rve_1b.py` è `L275`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 463 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 463 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo del codice RDG, l'arco 463 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, l'arco 463 registra come `target` l'identificatore `rve_transactions_rve_1b_build_mock_soap_response` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 463 che coinvolge il nodo canonico `rve_1b.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=464 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 464 che coinvolge il nodo canonico `rve_1b.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 464 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 464 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 464 che coinvolge il nodo canonico `rve_1b.py` è `L301`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 464 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 464 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo del codice RDG, l'arco 464 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, l'arco 464 registra come `target` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 464 che coinvolge il nodo canonico `rve_1b.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=465 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_build_soap_request()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_rve_1b_build_soap_request`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_build_soap_request()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 465 che coinvolge il nodo canonico `rve_1b.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 465 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 465 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 465 che coinvolge il nodo canonico `rve_1b.py` è `L142`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 465 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 465 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo del codice RDG, l'arco 465 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, l'arco 465 registra come `target` l'identificatore `rve_transactions_rve_1b_build_soap_request` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 465 che coinvolge il nodo canonico `rve_1b.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=466 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_conditions_xml()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_rve_1b_conditions_xml`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_conditions_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 466 che coinvolge il nodo canonico `rve_1b.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 466 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 466 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 466 che coinvolge il nodo canonico `rve_1b.py` è `L127`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 466 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 466 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo del codice RDG, l'arco 466 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, l'arco 466 registra come `target` l'identificatore `rve_transactions_rve_1b_conditions_xml` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 466 che coinvolge il nodo canonico `rve_1b.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=467 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_format_timestamp()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_rve_1b_format_timestamp`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_format_timestamp()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 467 che coinvolge il nodo canonico `rve_1b.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 467 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 467 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 467 che coinvolge il nodo canonico `rve_1b.py` è `L117`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 467 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 467 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo del codice RDG, l'arco 467 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, l'arco 467 registra come `target` l'identificatore `rve_transactions_rve_1b_format_timestamp` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 467 che coinvolge il nodo canonico `rve_1b.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=468 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_profile_name()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_rve_1b_profile_name`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_profile_name()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 468 che coinvolge il nodo canonico `rve_1b.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 468 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 468 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 468 che coinvolge il nodo canonico `rve_1b.py` è `L71`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 468 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 468 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo del codice RDG, l'arco 468 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, l'arco 468 registra come `target` l'identificatore `rve_transactions_rve_1b_profile_name` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 468 che coinvolge il nodo canonico `rve_1b.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=469 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_resolve_profile()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_rve_1b_resolve_profile`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_resolve_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 469 che coinvolge il nodo canonico `rve_1b.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 469 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 469 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 469 che coinvolge il nodo canonico `rve_1b.py` è `L79`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 469 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 469 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo del codice RDG, l'arco 469 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, l'arco 469 registra come `target` l'identificatore `rve_transactions_rve_1b_resolve_profile` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 469 che coinvolge il nodo canonico `rve_1b.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=413 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `rve_1b.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `flow_orchestrator.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_flow_orchestrator` all'ID `rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `flow_orchestrator.py` importa da `rve_1b.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 413 che coinvolge il nodo canonico `rve_1b.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 413 che coinvolge il nodo canonico `rve_1b.py` è `submodule_import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 413 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 413 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/flow_orchestrator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 413 che coinvolge il nodo canonico `rve_1b.py` è `L26`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 413 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 413 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo del codice RDG, l'arco 413 registra come `source` l'identificatore `rve_transactions_flow_orchestrator` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, l'arco 413 registra come `target` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 413 che coinvolge il nodo canonico `rve_1b.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=470 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `rve_1b.py` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_rationale_1` all'ID `rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe` costituisce una motivazione per `rve_1b.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 470 che coinvolge il nodo canonico `rve_1b.py` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 470 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 470 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 470 che coinvolge il nodo canonico `rve_1b.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 470 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 470 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo del codice RDG, l'arco 470 registra come `source` l'identificatore `rve_transactions_rve_1b_rationale_1` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, l'arco 470 registra come `target` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 470 che coinvolge il nodo canonico `rve_1b.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=471 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `rve_1b.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `test_core.py` importa da `rve_1b.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 471 che coinvolge il nodo canonico `rve_1b.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 471 che coinvolge il nodo canonico `rve_1b.py` è `submodule_import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 471 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 471 che coinvolge il nodo canonico `rve_1b.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 471 che coinvolge il nodo canonico `rve_1b.py` è `L24`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 471 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 471 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo del codice RDG, l'arco 471 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, l'arco 471 registra come `target` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 471 che coinvolge il nodo canonico `rve_1b.py` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=216 id=Request-Dataset-Generator::rve_transactions_rve_1b -->
Nel grafo unificato, l'etichetta originale del nodo canonico `rve_1b.py` è `rve_1b.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `rve_1b.py` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `rve_1b.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `rve_1b.py` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `rve_1b.py` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `rve_1b.py` è `rve_1b.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `rve_1b.py` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `rve_1b.py` è `rve_transactions_rve_1b`. Nel grafo unificato, il nodo canonico `rve_1b.py` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_1b`.
<!-- GRAPH_EDGE graph=unificata index=462 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_build_mock_assertion()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_assertion`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_build_mock_assertion()`. Nel grafo unificato, il tipo dichiarato dell'arco 462 che coinvolge il nodo canonico `rve_1b.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 462 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 462 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 462 che coinvolge il nodo canonico `rve_1b.py` è `L226`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 462 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 462 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 462 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, l'arco 462 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo unificato, l'arco 462 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_assertion` per la relazione che coinvolge il nodo canonico `rve_1b.py`.
<!-- GRAPH_EDGE graph=unificata index=463 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_build_mock_soap_response()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_soap_response`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_build_mock_soap_response()`. Nel grafo unificato, il tipo dichiarato dell'arco 463 che coinvolge il nodo canonico `rve_1b.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 463 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 463 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 463 che coinvolge il nodo canonico `rve_1b.py` è `L275`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 463 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 463 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 463 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, l'arco 463 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo unificato, l'arco 463 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_mock_soap_response` per la relazione che coinvolge il nodo canonico `rve_1b.py`.
<!-- GRAPH_EDGE graph=unificata index=464 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 464 che coinvolge il nodo canonico `rve_1b.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 464 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 464 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 464 che coinvolge il nodo canonico `rve_1b.py` è `L301`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 464 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 464 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 464 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, l'arco 464 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo unificato, l'arco 464 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `rve_1b.py`.
<!-- GRAPH_EDGE graph=unificata index=465 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_build_soap_request()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_soap_request`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_build_soap_request()`. Nel grafo unificato, il tipo dichiarato dell'arco 465 che coinvolge il nodo canonico `rve_1b.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 465 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 465 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 465 che coinvolge il nodo canonico `rve_1b.py` è `L142`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 465 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 465 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 465 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, l'arco 465 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo unificato, l'arco 465 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_soap_request` per la relazione che coinvolge il nodo canonico `rve_1b.py`.
<!-- GRAPH_EDGE graph=unificata index=466 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_conditions_xml()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_conditions_xml`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_conditions_xml()`. Nel grafo unificato, il tipo dichiarato dell'arco 466 che coinvolge il nodo canonico `rve_1b.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 466 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 466 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 466 che coinvolge il nodo canonico `rve_1b.py` è `L127`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 466 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 466 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 466 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, l'arco 466 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo unificato, l'arco 466 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_conditions_xml` per la relazione che coinvolge il nodo canonico `rve_1b.py`.
<!-- GRAPH_EDGE graph=unificata index=467 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_format_timestamp()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_format_timestamp`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_format_timestamp()`. Nel grafo unificato, il tipo dichiarato dell'arco 467 che coinvolge il nodo canonico `rve_1b.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 467 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 467 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 467 che coinvolge il nodo canonico `rve_1b.py` è `L117`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 467 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 467 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 467 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, l'arco 467 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo unificato, l'arco 467 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_format_timestamp` per la relazione che coinvolge il nodo canonico `rve_1b.py`.
<!-- GRAPH_EDGE graph=unificata index=468 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_profile_name()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_profile_name`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_profile_name()`. Nel grafo unificato, il tipo dichiarato dell'arco 468 che coinvolge il nodo canonico `rve_1b.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 468 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 468 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 468 che coinvolge il nodo canonico `rve_1b.py` è `L71`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 468 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 468 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 468 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, l'arco 468 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo unificato, l'arco 468 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_profile_name` per la relazione che coinvolge il nodo canonico `rve_1b.py`.
<!-- GRAPH_EDGE graph=unificata index=469 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_resolve_profile()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_resolve_profile`. La relazione tecnica esplicita è la seguente: `rve_1b.py` contiene `_resolve_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 469 che coinvolge il nodo canonico `rve_1b.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 469 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 469 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 469 che coinvolge il nodo canonico `rve_1b.py` è `L79`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 469 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 469 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 469 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, l'arco 469 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo unificato, l'arco 469 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_resolve_profile` per la relazione che coinvolge il nodo canonico `rve_1b.py`.
<!-- GRAPH_EDGE graph=unificata index=470 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_1`. La relazione tecnica esplicita è la seguente: `rve_1b.py` costituisce una motivazione per `RVE-1.b - SOAP AuthenticateAndGetAssertion-b  Costruisce il SOAP Envelope 1.2 pe`. Nel grafo unificato, il tipo dichiarato dell'arco 470 che coinvolge il nodo canonico `rve_1b.py` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 470 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 470 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 470 che coinvolge il nodo canonico `rve_1b.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 470 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 470 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 470 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, l'arco 470 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo unificato, l'arco 470 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_rationale_1` per la relazione che coinvolge il nodo canonico `rve_1b.py`.
<!-- GRAPH_EDGE graph=unificata index=471 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_1b.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_1b` all'ID `Request-Dataset-Generator::tests_test_core`. La relazione tecnica esplicita è la seguente: `rve_1b.py` importa da `test_core.py`. Nel grafo unificato, il tipo dichiarato dell'arco 471 che coinvolge il nodo canonico `rve_1b.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 471 che coinvolge il nodo canonico `rve_1b.py` è `submodule_import`. Nel grafo unificato, la confidenza categoriale dell'arco 471 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 471 che coinvolge il nodo canonico `rve_1b.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 471 che coinvolge il nodo canonico `rve_1b.py` è `L24`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 471 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 471 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 471 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, l'arco 471 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo unificato, l'arco 471 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `rve_1b.py`.
<!-- GRAPH_EDGE graph=unificata index=355 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_1b.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `_common.py` importa da `rve_1b.py`. Nel grafo unificato, il tipo dichiarato dell'arco 355 che coinvolge il nodo canonico `rve_1b.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 355 che coinvolge il nodo canonico `rve_1b.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 355 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 355 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 355 che coinvolge il nodo canonico `rve_1b.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 355 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 355 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 355 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, l'arco 355 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo unificato, l'arco 355 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`.
<!-- GRAPH_EDGE graph=unificata index=363 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_1b.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `utc_now()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_utc_now` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `utc_now()` importa `rve_1b.py`. Nel grafo unificato, il tipo dichiarato dell'arco 363 che coinvolge il nodo canonico `rve_1b.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 363 che coinvolge il nodo canonico `rve_1b.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 363 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 363 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 363 che coinvolge il nodo canonico `rve_1b.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 363 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 363 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 363 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, l'arco 363 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo unificato, l'arco 363 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`.
<!-- GRAPH_EDGE graph=unificata index=367 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_1b.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `utc_iso()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_utc_iso` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `utc_iso()` importa `rve_1b.py`. Nel grafo unificato, il tipo dichiarato dell'arco 367 che coinvolge il nodo canonico `rve_1b.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 367 che coinvolge il nodo canonico `rve_1b.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 367 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 367 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 367 che coinvolge il nodo canonico `rve_1b.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 367 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 367 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 367 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, l'arco 367 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_iso` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo unificato, l'arco 367 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`.
<!-- GRAPH_EDGE graph=unificata index=369 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_1b.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `utc_iso_ms()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `utc_iso_ms()` importa `rve_1b.py`. Nel grafo unificato, il tipo dichiarato dell'arco 369 che coinvolge il nodo canonico `rve_1b.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 369 che coinvolge il nodo canonico `rve_1b.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 369 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 369 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 369 che coinvolge il nodo canonico `rve_1b.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 369 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 369 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 369 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, l'arco 369 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo unificato, l'arco 369 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`.
<!-- GRAPH_EDGE graph=unificata index=374 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_1b.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `new_uuid()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_new_uuid` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `new_uuid()` importa `rve_1b.py`. Nel grafo unificato, il tipo dichiarato dell'arco 374 che coinvolge il nodo canonico `rve_1b.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 374 che coinvolge il nodo canonico `rve_1b.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 374 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 374 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 374 che coinvolge il nodo canonico `rve_1b.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 374 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 374 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 374 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, l'arco 374 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_new_uuid` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo unificato, l'arco 374 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`.
<!-- GRAPH_EDGE graph=unificata index=377 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_1b.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `msg_id_to_saml_id()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_msg_id_to_saml_id` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `msg_id_to_saml_id()` importa `rve_1b.py`. Nel grafo unificato, il tipo dichiarato dell'arco 377 che coinvolge il nodo canonico `rve_1b.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 377 che coinvolge il nodo canonico `rve_1b.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 377 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 377 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 377 che coinvolge il nodo canonico `rve_1b.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 377 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 377 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 377 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, l'arco 377 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_msg_id_to_saml_id` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo unificato, l'arco 377 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`.
<!-- GRAPH_EDGE graph=unificata index=380 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_1b.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `b64_encode()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_b64_encode` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `b64_encode()` importa `rve_1b.py`. Nel grafo unificato, il tipo dichiarato dell'arco 380 che coinvolge il nodo canonico `rve_1b.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 380 che coinvolge il nodo canonico `rve_1b.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 380 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 380 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 380 che coinvolge il nodo canonico `rve_1b.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 380 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 380 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 380 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, l'arco 380 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_b64_encode` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo unificato, l'arco 380 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`.
<!-- GRAPH_EDGE graph=unificata index=387 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_1b.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `linearize_xml()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_linearize_xml` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `linearize_xml()` importa `rve_1b.py`. Nel grafo unificato, il tipo dichiarato dell'arco 387 che coinvolge il nodo canonico `rve_1b.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 387 che coinvolge il nodo canonico `rve_1b.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 387 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 387 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 387 che coinvolge il nodo canonico `rve_1b.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 387 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 387 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 387 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, l'arco 387 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_linearize_xml` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo unificato, l'arco 387 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`.
<!-- GRAPH_EDGE graph=unificata index=413 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_1b.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `flow_orchestrator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `flow_orchestrator.py` importa da `rve_1b.py`. Nel grafo unificato, il tipo dichiarato dell'arco 413 che coinvolge il nodo canonico `rve_1b.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 413 che coinvolge il nodo canonico `rve_1b.py` è `submodule_import`. Nel grafo unificato, la confidenza categoriale dell'arco 413 che coinvolge il nodo canonico `rve_1b.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 413 che coinvolge il nodo canonico `rve_1b.py` è `rve_transactions/flow_orchestrator.py`. Nel grafo unificato, la posizione sorgente dell'arco 413 che coinvolge il nodo canonico `rve_1b.py` è `L26`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 413 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 413 che coinvolge il nodo canonico `rve_1b.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 413 che coinvolge il nodo canonico `rve_1b.py` è `1.0`. Nel grafo unificato, l'arco 413 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator` per la relazione che coinvolge il nodo canonico `rve_1b.py`. Nel grafo unificato, l'arco 413 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `rve_1b.py`.

### rve_54.py

<!-- GRAPH_NODE graph=codice index=229 id=rve_transactions_rve_54 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `rve_54.py` è `rve_54.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `rve_54.py` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `rve_54.py` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `rve_54.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `rve_54.py` vale `ast`. Nel grafo del codice RDG, il nodo canonico `rve_54.py` possiede l'identificatore esatto `rve_transactions_rve_54`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `rve_54.py` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `rve_54.py` è `rve_54.py`.
<!-- GRAPH_EDGE graph=codice index=356 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_54.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54` all'ID `rve_transactions_common`. La relazione tecnica esplicita è la seguente: `rve_54.py` importa da `_common.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 356 che coinvolge il nodo canonico `rve_54.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 356 che coinvolge il nodo canonico `rve_54.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 356 che coinvolge il nodo canonico `rve_54.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 356 che coinvolge il nodo canonico `rve_54.py` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 356 che coinvolge il nodo canonico `rve_54.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 356 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 356 che coinvolge il nodo canonico `rve_54.py` è `ast`. Nel grafo del codice RDG, l'arco 356 registra come `source` l'identificatore `rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo del codice RDG, l'arco 356 registra come `target` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 356 che coinvolge il nodo canonico `rve_54.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=397 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_54.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `extract_patient_cf()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54` all'ID `rve_transactions_common_extract_patient_cf`. La relazione tecnica esplicita è la seguente: `rve_54.py` importa `extract_patient_cf()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 397 che coinvolge il nodo canonico `rve_54.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 397 che coinvolge il nodo canonico `rve_54.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 397 che coinvolge il nodo canonico `rve_54.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 397 che coinvolge il nodo canonico `rve_54.py` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 397 che coinvolge il nodo canonico `rve_54.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 397 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 397 che coinvolge il nodo canonico `rve_54.py` è `ast`. Nel grafo del codice RDG, l'arco 397 registra come `source` l'identificatore `rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo del codice RDG, l'arco 397 registra come `target` l'identificatore `rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 397 che coinvolge il nodo canonico `rve_54.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=403 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_54.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `extract_patient_mpi()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54` all'ID `rve_transactions_common_extract_patient_mpi`. La relazione tecnica esplicita è la seguente: `rve_54.py` importa `extract_patient_mpi()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 403 che coinvolge il nodo canonico `rve_54.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 403 che coinvolge il nodo canonico `rve_54.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 403 che coinvolge il nodo canonico `rve_54.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 403 che coinvolge il nodo canonico `rve_54.py` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 403 che coinvolge il nodo canonico `rve_54.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 403 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 403 che coinvolge il nodo canonico `rve_54.py` è `ast`. Nel grafo del codice RDG, l'arco 403 registra come `source` l'identificatore `rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo del codice RDG, l'arco 403 registra come `target` l'identificatore `rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 403 che coinvolge il nodo canonico `rve_54.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=482 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_54.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_build_mock_searchset()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54` all'ID `rve_transactions_rve_54_build_mock_searchset`. La relazione tecnica esplicita è la seguente: `rve_54.py` contiene `_build_mock_searchset()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 482 che coinvolge il nodo canonico `rve_54.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 482 che coinvolge il nodo canonico `rve_54.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 482 che coinvolge il nodo canonico `rve_54.py` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 482 che coinvolge il nodo canonico `rve_54.py` è `L123`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 482 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 482 che coinvolge il nodo canonico `rve_54.py` è `ast`. Nel grafo del codice RDG, l'arco 482 registra come `source` l'identificatore `rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo del codice RDG, l'arco 482 registra come `target` l'identificatore `rve_transactions_rve_54_build_mock_searchset` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 482 che coinvolge il nodo canonico `rve_54.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=483 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_54.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54` all'ID `rve_transactions_rve_54_build_request`. La relazione tecnica esplicita è la seguente: `rve_54.py` contiene `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 483 che coinvolge il nodo canonico `rve_54.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 483 che coinvolge il nodo canonico `rve_54.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 483 che coinvolge il nodo canonico `rve_54.py` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 483 che coinvolge il nodo canonico `rve_54.py` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 483 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 483 che coinvolge il nodo canonico `rve_54.py` è `ast`. Nel grafo del codice RDG, l'arco 483 registra come `source` l'identificatore `rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo del codice RDG, l'arco 483 registra come `target` l'identificatore `rve_transactions_rve_54_build_request` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 483 che coinvolge il nodo canonico `rve_54.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=414 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `rve_54.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `flow_orchestrator.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_flow_orchestrator` all'ID `rve_transactions_rve_54`. La relazione tecnica esplicita è la seguente: `flow_orchestrator.py` importa da `rve_54.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 414 che coinvolge il nodo canonico `rve_54.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 414 che coinvolge il nodo canonico `rve_54.py` è `submodule_import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 414 che coinvolge il nodo canonico `rve_54.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 414 che coinvolge il nodo canonico `rve_54.py` è `rve_transactions/flow_orchestrator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 414 che coinvolge il nodo canonico `rve_54.py` è `L26`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 414 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 414 che coinvolge il nodo canonico `rve_54.py` è `ast`. Nel grafo del codice RDG, l'arco 414 registra come `source` l'identificatore `rve_transactions_flow_orchestrator` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo del codice RDG, l'arco 414 registra come `target` l'identificatore `rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 414 che coinvolge il nodo canonico `rve_54.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=484 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `rve_54.py` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_54_rationale_1` all'ID `rve_transactions_rve_54`. La relazione tecnica esplicita è la seguente: `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass` costituisce una motivazione per `rve_54.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 484 che coinvolge il nodo canonico `rve_54.py` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 484 che coinvolge il nodo canonico `rve_54.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 484 che coinvolge il nodo canonico `rve_54.py` è `rve_transactions/rve_54.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 484 che coinvolge il nodo canonico `rve_54.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 484 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 484 che coinvolge il nodo canonico `rve_54.py` è `ast`. Nel grafo del codice RDG, l'arco 484 registra come `source` l'identificatore `rve_transactions_rve_54_rationale_1` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo del codice RDG, l'arco 484 registra come `target` l'identificatore `rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 484 che coinvolge il nodo canonico `rve_54.py` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=229 id=Request-Dataset-Generator::rve_transactions_rve_54 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `rve_54.py` è `rve_54.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `rve_54.py` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `rve_54.py` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `rve_54.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `rve_54.py` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `rve_54.py` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `rve_54.py` è `rve_54.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `rve_54.py` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `rve_54.py` è `rve_transactions_rve_54`. Nel grafo unificato, il nodo canonico `rve_54.py` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_54`.
<!-- GRAPH_EDGE graph=unificata index=482 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_54.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_build_mock_searchset()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_54` all'ID `Request-Dataset-Generator::rve_transactions_rve_54_build_mock_searchset`. La relazione tecnica esplicita è la seguente: `rve_54.py` contiene `_build_mock_searchset()`. Nel grafo unificato, il tipo dichiarato dell'arco 482 che coinvolge il nodo canonico `rve_54.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 482 che coinvolge il nodo canonico `rve_54.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 482 che coinvolge il nodo canonico `rve_54.py` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 482 che coinvolge il nodo canonico `rve_54.py` è `L123`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 482 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 482 che coinvolge il nodo canonico `rve_54.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 482 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo unificato, l'arco 482 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo unificato, l'arco 482 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_build_mock_searchset` per la relazione che coinvolge il nodo canonico `rve_54.py`.
<!-- GRAPH_EDGE graph=unificata index=483 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_54.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `build_request() [rve_transactions_rve_54_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_54` all'ID `Request-Dataset-Generator::rve_transactions_rve_54_build_request`. La relazione tecnica esplicita è la seguente: `rve_54.py` contiene `build_request() [rve_transactions_rve_54_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 483 che coinvolge il nodo canonico `rve_54.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 483 che coinvolge il nodo canonico `rve_54.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 483 che coinvolge il nodo canonico `rve_54.py` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 483 che coinvolge il nodo canonico `rve_54.py` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 483 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 483 che coinvolge il nodo canonico `rve_54.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 483 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo unificato, l'arco 483 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo unificato, l'arco 483 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_build_request` per la relazione che coinvolge il nodo canonico `rve_54.py`.
<!-- GRAPH_EDGE graph=unificata index=484 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_54.py` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_54` all'ID `Request-Dataset-Generator::rve_transactions_rve_54_rationale_1`. La relazione tecnica esplicita è la seguente: `rve_54.py` costituisce una motivazione per `RVE-54 — REST Patient Query  Ricerca anagrafica paziente presentando la SAML Ass`. Nel grafo unificato, il tipo dichiarato dell'arco 484 che coinvolge il nodo canonico `rve_54.py` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 484 che coinvolge il nodo canonico `rve_54.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 484 che coinvolge il nodo canonico `rve_54.py` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 484 che coinvolge il nodo canonico `rve_54.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 484 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 484 che coinvolge il nodo canonico `rve_54.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 484 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo unificato, l'arco 484 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo unificato, l'arco 484 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54_rationale_1` per la relazione che coinvolge il nodo canonico `rve_54.py`.
<!-- GRAPH_EDGE graph=unificata index=356 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_54.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_rve_54`. La relazione tecnica esplicita è la seguente: `_common.py` importa da `rve_54.py`. Nel grafo unificato, il tipo dichiarato dell'arco 356 che coinvolge il nodo canonico `rve_54.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 356 che coinvolge il nodo canonico `rve_54.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 356 che coinvolge il nodo canonico `rve_54.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 356 che coinvolge il nodo canonico `rve_54.py` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 356 che coinvolge il nodo canonico `rve_54.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 356 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 356 che coinvolge il nodo canonico `rve_54.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 356 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo unificato, l'arco 356 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo unificato, l'arco 356 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `rve_54.py`.
<!-- GRAPH_EDGE graph=unificata index=397 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_54.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `extract_patient_cf()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` all'ID `Request-Dataset-Generator::rve_transactions_rve_54`. La relazione tecnica esplicita è la seguente: `extract_patient_cf()` importa `rve_54.py`. Nel grafo unificato, il tipo dichiarato dell'arco 397 che coinvolge il nodo canonico `rve_54.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 397 che coinvolge il nodo canonico `rve_54.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 397 che coinvolge il nodo canonico `rve_54.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 397 che coinvolge il nodo canonico `rve_54.py` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 397 che coinvolge il nodo canonico `rve_54.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 397 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 397 che coinvolge il nodo canonico `rve_54.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 397 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo unificato, l'arco 397 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_cf` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo unificato, l'arco 397 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `rve_54.py`.
<!-- GRAPH_EDGE graph=unificata index=403 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_54.py` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `extract_patient_mpi()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` all'ID `Request-Dataset-Generator::rve_transactions_rve_54`. La relazione tecnica esplicita è la seguente: `extract_patient_mpi()` importa `rve_54.py`. Nel grafo unificato, il tipo dichiarato dell'arco 403 che coinvolge il nodo canonico `rve_54.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 403 che coinvolge il nodo canonico `rve_54.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 403 che coinvolge il nodo canonico `rve_54.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 403 che coinvolge il nodo canonico `rve_54.py` è `rve_transactions/rve_54.py`. Nel grafo unificato, la posizione sorgente dell'arco 403 che coinvolge il nodo canonico `rve_54.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 403 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 403 che coinvolge il nodo canonico `rve_54.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 403 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo unificato, l'arco 403 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_extract_patient_mpi` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo unificato, l'arco 403 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `rve_54.py`.
<!-- GRAPH_EDGE graph=unificata index=414 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_54.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `flow_orchestrator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator` all'ID `Request-Dataset-Generator::rve_transactions_rve_54`. La relazione tecnica esplicita è la seguente: `flow_orchestrator.py` importa da `rve_54.py`. Nel grafo unificato, il tipo dichiarato dell'arco 414 che coinvolge il nodo canonico `rve_54.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 414 che coinvolge il nodo canonico `rve_54.py` è `submodule_import`. Nel grafo unificato, la confidenza categoriale dell'arco 414 che coinvolge il nodo canonico `rve_54.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 414 che coinvolge il nodo canonico `rve_54.py` è `rve_transactions/flow_orchestrator.py`. Nel grafo unificato, la posizione sorgente dell'arco 414 che coinvolge il nodo canonico `rve_54.py` è `L26`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 414 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 414 che coinvolge il nodo canonico `rve_54.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 414 che coinvolge il nodo canonico `rve_54.py` è `1.0`. Nel grafo unificato, l'arco 414 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator` per la relazione che coinvolge il nodo canonico `rve_54.py`. Nel grafo unificato, l'arco 414 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_54` per la relazione che coinvolge il nodo canonico `rve_54.py`.

### utc_iso()

<!-- GRAPH_NODE graph=codice index=164 id=rve_transactions_common_utc_iso -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `utc_iso()` è `utc_iso()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `utc_iso()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `utc_iso()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `utc_iso()` è `L33`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `utc_iso()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `utc_iso()` possiede l'identificatore esatto `rve_transactions_common_utc_iso`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `utc_iso()` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `utc_iso()` è `utc_iso()`.
<!-- GRAPH_EDGE graph=codice index=360 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `utc_iso()` è la sorgente `source` della relazione `calls` verso il nodo canonico `utc_now()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_utc_iso` all'ID `rve_transactions_common_utc_now`. La relazione tecnica esplicita è la seguente: `utc_iso()` chiama `utc_now()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 360 che coinvolge il nodo canonico `utc_iso()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 360 che coinvolge il nodo canonico `utc_iso()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 360 che coinvolge il nodo canonico `utc_iso()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 360 che coinvolge il nodo canonico `utc_iso()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 360 che coinvolge il nodo canonico `utc_iso()` è `L34`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 360 che coinvolge il nodo canonico `utc_iso()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 360 che coinvolge il nodo canonico `utc_iso()` è `ast`. Nel grafo del codice RDG, l'arco 360 registra come `source` l'identificatore `rve_transactions_common_utc_iso` per la relazione che coinvolge il nodo canonico `utc_iso()`. Nel grafo del codice RDG, l'arco 360 registra come `target` l'identificatore `rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `utc_iso()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 360 che coinvolge il nodo canonico `utc_iso()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=365 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `utc_iso()` è la sorgente `source` della relazione `references` verso il nodo canonico `datetime [rve_transactions_common_py_datetime]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_utc_iso` all'ID `rve_transactions_common_py_datetime`. La relazione tecnica esplicita è la seguente: `utc_iso()` fa riferimento a `datetime [rve_transactions_common_py_datetime]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 365 che coinvolge il nodo canonico `utc_iso()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 365 che coinvolge il nodo canonico `utc_iso()` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 365 che coinvolge il nodo canonico `utc_iso()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 365 che coinvolge il nodo canonico `utc_iso()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 365 che coinvolge il nodo canonico `utc_iso()` è `L33`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 365 che coinvolge il nodo canonico `utc_iso()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 365 che coinvolge il nodo canonico `utc_iso()` è `ast`. Nel grafo del codice RDG, l'arco 365 registra come `source` l'identificatore `rve_transactions_common_utc_iso` per la relazione che coinvolge il nodo canonico `utc_iso()`. Nel grafo del codice RDG, l'arco 365 registra come `target` l'identificatore `rve_transactions_common_py_datetime` per la relazione che coinvolge il nodo canonico `utc_iso()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 365 che coinvolge il nodo canonico `utc_iso()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=350 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `utc_iso()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_utc_iso`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `utc_iso()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 350 che coinvolge il nodo canonico `utc_iso()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 350 che coinvolge il nodo canonico `utc_iso()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 350 che coinvolge il nodo canonico `utc_iso()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 350 che coinvolge il nodo canonico `utc_iso()` è `L33`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 350 che coinvolge il nodo canonico `utc_iso()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 350 che coinvolge il nodo canonico `utc_iso()` è `ast`. Nel grafo del codice RDG, l'arco 350 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `utc_iso()`. Nel grafo del codice RDG, l'arco 350 registra come `target` l'identificatore `rve_transactions_common_utc_iso` per la relazione che coinvolge il nodo canonico `utc_iso()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 350 che coinvolge il nodo canonico `utc_iso()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=367 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `utc_iso()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_common_utc_iso`. La relazione tecnica esplicita è la seguente: `rve_1b.py` importa `utc_iso()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 367 che coinvolge il nodo canonico `utc_iso()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 367 che coinvolge il nodo canonico `utc_iso()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 367 che coinvolge il nodo canonico `utc_iso()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 367 che coinvolge il nodo canonico `utc_iso()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 367 che coinvolge il nodo canonico `utc_iso()` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 367 che coinvolge il nodo canonico `utc_iso()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 367 che coinvolge il nodo canonico `utc_iso()` è `ast`. Nel grafo del codice RDG, l'arco 367 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `utc_iso()`. Nel grafo del codice RDG, l'arco 367 registra come `target` l'identificatore `rve_transactions_common_utc_iso` per la relazione che coinvolge il nodo canonico `utc_iso()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 367 che coinvolge il nodo canonico `utc_iso()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=368 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `utc_iso()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_format_timestamp()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_format_timestamp` all'ID `rve_transactions_common_utc_iso`. La relazione tecnica esplicita è la seguente: `_format_timestamp()` chiama `utc_iso()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 368 che coinvolge il nodo canonico `utc_iso()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 368 che coinvolge il nodo canonico `utc_iso()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 368 che coinvolge il nodo canonico `utc_iso()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 368 che coinvolge il nodo canonico `utc_iso()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 368 che coinvolge il nodo canonico `utc_iso()` è `L119`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 368 che coinvolge il nodo canonico `utc_iso()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 368 che coinvolge il nodo canonico `utc_iso()` è `ast`. Nel grafo del codice RDG, l'arco 368 registra come `source` l'identificatore `rve_transactions_rve_1b_format_timestamp` per la relazione che coinvolge il nodo canonico `utc_iso()`. Nel grafo del codice RDG, l'arco 368 registra come `target` l'identificatore `rve_transactions_common_utc_iso` per la relazione che coinvolge il nodo canonico `utc_iso()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 368 che coinvolge il nodo canonico `utc_iso()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=164 id=Request-Dataset-Generator::rve_transactions_common_utc_iso -->
Nel grafo unificato, l'etichetta originale del nodo canonico `utc_iso()` è `utc_iso()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `utc_iso()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `utc_iso()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `utc_iso()` è `L33`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `utc_iso()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `utc_iso()` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `utc_iso()` è `utc_iso()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `utc_iso()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `utc_iso()` è `rve_transactions_common_utc_iso`. Nel grafo unificato, il nodo canonico `utc_iso()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_utc_iso`.
<!-- GRAPH_EDGE graph=unificata index=367 role=outgoing -->
Nel grafo unificato, il nodo canonico `utc_iso()` è la sorgente `source` della relazione `imports` verso il nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_utc_iso` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `utc_iso()` importa `rve_1b.py`. Nel grafo unificato, il tipo dichiarato dell'arco 367 che coinvolge il nodo canonico `utc_iso()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 367 che coinvolge il nodo canonico `utc_iso()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 367 che coinvolge il nodo canonico `utc_iso()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 367 che coinvolge il nodo canonico `utc_iso()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 367 che coinvolge il nodo canonico `utc_iso()` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 367 che coinvolge il nodo canonico `utc_iso()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 367 che coinvolge il nodo canonico `utc_iso()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 367 che coinvolge il nodo canonico `utc_iso()` è `1.0`. Nel grafo unificato, l'arco 367 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_iso` per la relazione che coinvolge il nodo canonico `utc_iso()`. Nel grafo unificato, l'arco 367 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `utc_iso()`.
<!-- GRAPH_EDGE graph=unificata index=368 role=outgoing -->
Nel grafo unificato, il nodo canonico `utc_iso()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_format_timestamp()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_utc_iso` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_format_timestamp`. La relazione tecnica esplicita è la seguente: `utc_iso()` chiama `_format_timestamp()`. Nel grafo unificato, il tipo dichiarato dell'arco 368 che coinvolge il nodo canonico `utc_iso()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 368 che coinvolge il nodo canonico `utc_iso()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 368 che coinvolge il nodo canonico `utc_iso()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 368 che coinvolge il nodo canonico `utc_iso()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 368 che coinvolge il nodo canonico `utc_iso()` è `L119`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 368 che coinvolge il nodo canonico `utc_iso()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 368 che coinvolge il nodo canonico `utc_iso()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 368 che coinvolge il nodo canonico `utc_iso()` è `1.0`. Nel grafo unificato, l'arco 368 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_iso` per la relazione che coinvolge il nodo canonico `utc_iso()`. Nel grafo unificato, l'arco 368 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_format_timestamp` per la relazione che coinvolge il nodo canonico `utc_iso()`.
<!-- GRAPH_EDGE graph=unificata index=350 role=incoming -->
Nel grafo unificato, il nodo canonico `utc_iso()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_utc_iso`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `utc_iso()`. Nel grafo unificato, il tipo dichiarato dell'arco 350 che coinvolge il nodo canonico `utc_iso()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 350 che coinvolge il nodo canonico `utc_iso()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 350 che coinvolge il nodo canonico `utc_iso()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 350 che coinvolge il nodo canonico `utc_iso()` è `L33`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 350 che coinvolge il nodo canonico `utc_iso()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 350 che coinvolge il nodo canonico `utc_iso()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 350 che coinvolge il nodo canonico `utc_iso()` è `1.0`. Nel grafo unificato, l'arco 350 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `utc_iso()`. Nel grafo unificato, l'arco 350 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_iso` per la relazione che coinvolge il nodo canonico `utc_iso()`.
<!-- GRAPH_EDGE graph=unificata index=360 role=incoming -->
Nel grafo unificato, il nodo canonico `utc_iso()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `utc_now()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_utc_now` all'ID `Request-Dataset-Generator::rve_transactions_common_utc_iso`. La relazione tecnica esplicita è la seguente: `utc_now()` chiama `utc_iso()`. Nel grafo unificato, il tipo dichiarato dell'arco 360 che coinvolge il nodo canonico `utc_iso()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 360 che coinvolge il nodo canonico `utc_iso()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 360 che coinvolge il nodo canonico `utc_iso()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 360 che coinvolge il nodo canonico `utc_iso()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 360 che coinvolge il nodo canonico `utc_iso()` è `L34`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 360 che coinvolge il nodo canonico `utc_iso()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 360 che coinvolge il nodo canonico `utc_iso()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 360 che coinvolge il nodo canonico `utc_iso()` è `1.0`. Nel grafo unificato, l'arco 360 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `utc_iso()`. Nel grafo unificato, l'arco 360 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_iso` per la relazione che coinvolge il nodo canonico `utc_iso()`.
<!-- GRAPH_EDGE graph=unificata index=365 role=incoming -->
Nel grafo unificato, il nodo canonico `utc_iso()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `datetime [rve_transactions_common_py_datetime]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_py_datetime` all'ID `Request-Dataset-Generator::rve_transactions_common_utc_iso`. La relazione tecnica esplicita è la seguente: `datetime [rve_transactions_common_py_datetime]` fa riferimento a `utc_iso()`. Nel grafo unificato, il tipo dichiarato dell'arco 365 che coinvolge il nodo canonico `utc_iso()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 365 che coinvolge il nodo canonico `utc_iso()` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 365 che coinvolge il nodo canonico `utc_iso()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 365 che coinvolge il nodo canonico `utc_iso()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 365 che coinvolge il nodo canonico `utc_iso()` è `L33`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 365 che coinvolge il nodo canonico `utc_iso()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 365 che coinvolge il nodo canonico `utc_iso()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 365 che coinvolge il nodo canonico `utc_iso()` è `1.0`. Nel grafo unificato, l'arco 365 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_py_datetime` per la relazione che coinvolge il nodo canonico `utc_iso()`. Nel grafo unificato, l'arco 365 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_iso` per la relazione che coinvolge il nodo canonico `utc_iso()`.

### utc_iso_ms()

<!-- GRAPH_NODE graph=codice index=165 id=rve_transactions_common_utc_iso_ms -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `utc_iso_ms()` è `utc_iso_ms()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `utc_iso_ms()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `utc_iso_ms()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `utc_iso_ms()` è `L38`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `utc_iso_ms()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `utc_iso_ms()` possiede l'identificatore esatto `rve_transactions_common_utc_iso_ms`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `utc_iso_ms()` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `utc_iso_ms()` è `utc_iso_ms()`.
<!-- GRAPH_EDGE graph=codice index=361 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `utc_iso_ms()` è la sorgente `source` della relazione `calls` verso il nodo canonico `utc_now()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_utc_iso_ms` all'ID `rve_transactions_common_utc_now`. La relazione tecnica esplicita è la seguente: `utc_iso_ms()` chiama `utc_now()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 361 che coinvolge il nodo canonico `utc_iso_ms()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 361 che coinvolge il nodo canonico `utc_iso_ms()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 361 che coinvolge il nodo canonico `utc_iso_ms()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 361 che coinvolge il nodo canonico `utc_iso_ms()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 361 che coinvolge il nodo canonico `utc_iso_ms()` è `L39`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 361 che coinvolge il nodo canonico `utc_iso_ms()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 361 che coinvolge il nodo canonico `utc_iso_ms()` è `ast`. Nel grafo del codice RDG, l'arco 361 registra come `source` l'identificatore `rve_transactions_common_utc_iso_ms` per la relazione che coinvolge il nodo canonico `utc_iso_ms()`. Nel grafo del codice RDG, l'arco 361 registra come `target` l'identificatore `rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `utc_iso_ms()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 361 che coinvolge il nodo canonico `utc_iso_ms()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=366 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `utc_iso_ms()` è la sorgente `source` della relazione `references` verso il nodo canonico `datetime [rve_transactions_common_py_datetime]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_utc_iso_ms` all'ID `rve_transactions_common_py_datetime`. La relazione tecnica esplicita è la seguente: `utc_iso_ms()` fa riferimento a `datetime [rve_transactions_common_py_datetime]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 366 che coinvolge il nodo canonico `utc_iso_ms()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 366 che coinvolge il nodo canonico `utc_iso_ms()` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 366 che coinvolge il nodo canonico `utc_iso_ms()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 366 che coinvolge il nodo canonico `utc_iso_ms()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 366 che coinvolge il nodo canonico `utc_iso_ms()` è `L38`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 366 che coinvolge il nodo canonico `utc_iso_ms()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 366 che coinvolge il nodo canonico `utc_iso_ms()` è `ast`. Nel grafo del codice RDG, l'arco 366 registra come `source` l'identificatore `rve_transactions_common_utc_iso_ms` per la relazione che coinvolge il nodo canonico `utc_iso_ms()`. Nel grafo del codice RDG, l'arco 366 registra come `target` l'identificatore `rve_transactions_common_py_datetime` per la relazione che coinvolge il nodo canonico `utc_iso_ms()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 366 che coinvolge il nodo canonico `utc_iso_ms()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=351 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `utc_iso_ms()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_utc_iso_ms`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `utc_iso_ms()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 351 che coinvolge il nodo canonico `utc_iso_ms()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 351 che coinvolge il nodo canonico `utc_iso_ms()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 351 che coinvolge il nodo canonico `utc_iso_ms()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 351 che coinvolge il nodo canonico `utc_iso_ms()` è `L38`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 351 che coinvolge il nodo canonico `utc_iso_ms()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 351 che coinvolge il nodo canonico `utc_iso_ms()` è `ast`. Nel grafo del codice RDG, l'arco 351 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `utc_iso_ms()`. Nel grafo del codice RDG, l'arco 351 registra come `target` l'identificatore `rve_transactions_common_utc_iso_ms` per la relazione che coinvolge il nodo canonico `utc_iso_ms()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 351 che coinvolge il nodo canonico `utc_iso_ms()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=369 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `utc_iso_ms()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_common_utc_iso_ms`. La relazione tecnica esplicita è la seguente: `rve_1b.py` importa `utc_iso_ms()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 369 che coinvolge il nodo canonico `utc_iso_ms()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 369 che coinvolge il nodo canonico `utc_iso_ms()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 369 che coinvolge il nodo canonico `utc_iso_ms()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 369 che coinvolge il nodo canonico `utc_iso_ms()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 369 che coinvolge il nodo canonico `utc_iso_ms()` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 369 che coinvolge il nodo canonico `utc_iso_ms()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 369 che coinvolge il nodo canonico `utc_iso_ms()` è `ast`. Nel grafo del codice RDG, l'arco 369 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `utc_iso_ms()`. Nel grafo del codice RDG, l'arco 369 registra come `target` l'identificatore `rve_transactions_common_utc_iso_ms` per la relazione che coinvolge il nodo canonico `utc_iso_ms()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 369 che coinvolge il nodo canonico `utc_iso_ms()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=370 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `utc_iso_ms()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_format_timestamp()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_format_timestamp` all'ID `rve_transactions_common_utc_iso_ms`. La relazione tecnica esplicita è la seguente: `_format_timestamp()` chiama `utc_iso_ms()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 370 che coinvolge il nodo canonico `utc_iso_ms()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 370 che coinvolge il nodo canonico `utc_iso_ms()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 370 che coinvolge il nodo canonico `utc_iso_ms()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 370 che coinvolge il nodo canonico `utc_iso_ms()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 370 che coinvolge il nodo canonico `utc_iso_ms()` è `L120`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 370 che coinvolge il nodo canonico `utc_iso_ms()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 370 che coinvolge il nodo canonico `utc_iso_ms()` è `ast`. Nel grafo del codice RDG, l'arco 370 registra come `source` l'identificatore `rve_transactions_rve_1b_format_timestamp` per la relazione che coinvolge il nodo canonico `utc_iso_ms()`. Nel grafo del codice RDG, l'arco 370 registra come `target` l'identificatore `rve_transactions_common_utc_iso_ms` per la relazione che coinvolge il nodo canonico `utc_iso_ms()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 370 che coinvolge il nodo canonico `utc_iso_ms()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=165 id=Request-Dataset-Generator::rve_transactions_common_utc_iso_ms -->
Nel grafo unificato, l'etichetta originale del nodo canonico `utc_iso_ms()` è `utc_iso_ms()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `utc_iso_ms()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `utc_iso_ms()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `utc_iso_ms()` è `L38`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `utc_iso_ms()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `utc_iso_ms()` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `utc_iso_ms()` è `utc_iso_ms()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `utc_iso_ms()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `utc_iso_ms()` è `rve_transactions_common_utc_iso_ms`. Nel grafo unificato, il nodo canonico `utc_iso_ms()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms`.
<!-- GRAPH_EDGE graph=unificata index=369 role=outgoing -->
Nel grafo unificato, il nodo canonico `utc_iso_ms()` è la sorgente `source` della relazione `imports` verso il nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `utc_iso_ms()` importa `rve_1b.py`. Nel grafo unificato, il tipo dichiarato dell'arco 369 che coinvolge il nodo canonico `utc_iso_ms()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 369 che coinvolge il nodo canonico `utc_iso_ms()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 369 che coinvolge il nodo canonico `utc_iso_ms()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 369 che coinvolge il nodo canonico `utc_iso_ms()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 369 che coinvolge il nodo canonico `utc_iso_ms()` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 369 che coinvolge il nodo canonico `utc_iso_ms()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 369 che coinvolge il nodo canonico `utc_iso_ms()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 369 che coinvolge il nodo canonico `utc_iso_ms()` è `1.0`. Nel grafo unificato, l'arco 369 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms` per la relazione che coinvolge il nodo canonico `utc_iso_ms()`. Nel grafo unificato, l'arco 369 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `utc_iso_ms()`.
<!-- GRAPH_EDGE graph=unificata index=370 role=outgoing -->
Nel grafo unificato, il nodo canonico `utc_iso_ms()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_format_timestamp()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_format_timestamp`. La relazione tecnica esplicita è la seguente: `utc_iso_ms()` chiama `_format_timestamp()`. Nel grafo unificato, il tipo dichiarato dell'arco 370 che coinvolge il nodo canonico `utc_iso_ms()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 370 che coinvolge il nodo canonico `utc_iso_ms()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 370 che coinvolge il nodo canonico `utc_iso_ms()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 370 che coinvolge il nodo canonico `utc_iso_ms()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 370 che coinvolge il nodo canonico `utc_iso_ms()` è `L120`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 370 che coinvolge il nodo canonico `utc_iso_ms()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 370 che coinvolge il nodo canonico `utc_iso_ms()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 370 che coinvolge il nodo canonico `utc_iso_ms()` è `1.0`. Nel grafo unificato, l'arco 370 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms` per la relazione che coinvolge il nodo canonico `utc_iso_ms()`. Nel grafo unificato, l'arco 370 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_format_timestamp` per la relazione che coinvolge il nodo canonico `utc_iso_ms()`.
<!-- GRAPH_EDGE graph=unificata index=351 role=incoming -->
Nel grafo unificato, il nodo canonico `utc_iso_ms()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `utc_iso_ms()`. Nel grafo unificato, il tipo dichiarato dell'arco 351 che coinvolge il nodo canonico `utc_iso_ms()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 351 che coinvolge il nodo canonico `utc_iso_ms()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 351 che coinvolge il nodo canonico `utc_iso_ms()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 351 che coinvolge il nodo canonico `utc_iso_ms()` è `L38`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 351 che coinvolge il nodo canonico `utc_iso_ms()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 351 che coinvolge il nodo canonico `utc_iso_ms()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 351 che coinvolge il nodo canonico `utc_iso_ms()` è `1.0`. Nel grafo unificato, l'arco 351 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `utc_iso_ms()`. Nel grafo unificato, l'arco 351 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms` per la relazione che coinvolge il nodo canonico `utc_iso_ms()`.
<!-- GRAPH_EDGE graph=unificata index=361 role=incoming -->
Nel grafo unificato, il nodo canonico `utc_iso_ms()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `utc_now()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_utc_now` all'ID `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms`. La relazione tecnica esplicita è la seguente: `utc_now()` chiama `utc_iso_ms()`. Nel grafo unificato, il tipo dichiarato dell'arco 361 che coinvolge il nodo canonico `utc_iso_ms()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 361 che coinvolge il nodo canonico `utc_iso_ms()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 361 che coinvolge il nodo canonico `utc_iso_ms()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 361 che coinvolge il nodo canonico `utc_iso_ms()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 361 che coinvolge il nodo canonico `utc_iso_ms()` è `L39`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 361 che coinvolge il nodo canonico `utc_iso_ms()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 361 che coinvolge il nodo canonico `utc_iso_ms()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 361 che coinvolge il nodo canonico `utc_iso_ms()` è `1.0`. Nel grafo unificato, l'arco 361 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `utc_iso_ms()`. Nel grafo unificato, l'arco 361 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms` per la relazione che coinvolge il nodo canonico `utc_iso_ms()`.
<!-- GRAPH_EDGE graph=unificata index=366 role=incoming -->
Nel grafo unificato, il nodo canonico `utc_iso_ms()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `datetime [rve_transactions_common_py_datetime]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_py_datetime` all'ID `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms`. La relazione tecnica esplicita è la seguente: `datetime [rve_transactions_common_py_datetime]` fa riferimento a `utc_iso_ms()`. Nel grafo unificato, il tipo dichiarato dell'arco 366 che coinvolge il nodo canonico `utc_iso_ms()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 366 che coinvolge il nodo canonico `utc_iso_ms()` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 366 che coinvolge il nodo canonico `utc_iso_ms()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 366 che coinvolge il nodo canonico `utc_iso_ms()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 366 che coinvolge il nodo canonico `utc_iso_ms()` è `L38`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 366 che coinvolge il nodo canonico `utc_iso_ms()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 366 che coinvolge il nodo canonico `utc_iso_ms()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 366 che coinvolge il nodo canonico `utc_iso_ms()` è `1.0`. Nel grafo unificato, l'arco 366 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_py_datetime` per la relazione che coinvolge il nodo canonico `utc_iso_ms()`. Nel grafo unificato, l'arco 366 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms` per la relazione che coinvolge il nodo canonico `utc_iso_ms()`.

### utc_now()

<!-- GRAPH_NODE graph=codice index=162 id=rve_transactions_common_utc_now -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `utc_now()` è `utc_now()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `utc_now()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `utc_now()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `utc_now()` è `L29`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `utc_now()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `utc_now()` possiede l'identificatore esatto `rve_transactions_common_utc_now`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `utc_now()` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `utc_now()` è `utc_now()`.
<!-- GRAPH_EDGE graph=codice index=362 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `utc_now()` è la sorgente `source` della relazione `references` verso il nodo canonico `datetime [rve_transactions_common_py_datetime]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_utc_now` all'ID `rve_transactions_common_py_datetime`. La relazione tecnica esplicita è la seguente: `utc_now()` fa riferimento a `datetime [rve_transactions_common_py_datetime]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 362 che coinvolge il nodo canonico `utc_now()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 362 che coinvolge il nodo canonico `utc_now()` è `return_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 362 che coinvolge il nodo canonico `utc_now()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 362 che coinvolge il nodo canonico `utc_now()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 362 che coinvolge il nodo canonico `utc_now()` è `L29`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 362 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 362 che coinvolge il nodo canonico `utc_now()` è `ast`. Nel grafo del codice RDG, l'arco 362 registra come `source` l'identificatore `rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo del codice RDG, l'arco 362 registra come `target` l'identificatore `rve_transactions_common_py_datetime` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 362 che coinvolge il nodo canonico `utc_now()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=352 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `utc_now()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common` all'ID `rve_transactions_common_utc_now`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `utc_now()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 352 che coinvolge il nodo canonico `utc_now()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 352 che coinvolge il nodo canonico `utc_now()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 352 che coinvolge il nodo canonico `utc_now()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 352 che coinvolge il nodo canonico `utc_now()` è `L29`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 352 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 352 che coinvolge il nodo canonico `utc_now()` è `ast`. Nel grafo del codice RDG, l'arco 352 registra come `source` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo del codice RDG, l'arco 352 registra come `target` l'identificatore `rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 352 che coinvolge il nodo canonico `utc_now()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=359 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `utc_now()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_mock_jwt()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_build_mock_jwt` all'ID `rve_transactions_common_utc_now`. La relazione tecnica esplicita è la seguente: `build_mock_jwt()` chiama `utc_now()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 359 che coinvolge il nodo canonico `utc_now()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 359 che coinvolge il nodo canonico `utc_now()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 359 che coinvolge il nodo canonico `utc_now()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 359 che coinvolge il nodo canonico `utc_now()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 359 che coinvolge il nodo canonico `utc_now()` è `L146`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 359 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 359 che coinvolge il nodo canonico `utc_now()` è `ast`. Nel grafo del codice RDG, l'arco 359 registra come `source` l'identificatore `rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo del codice RDG, l'arco 359 registra come `target` l'identificatore `rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 359 che coinvolge il nodo canonico `utc_now()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=360 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `utc_now()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `utc_iso()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_utc_iso` all'ID `rve_transactions_common_utc_now`. La relazione tecnica esplicita è la seguente: `utc_iso()` chiama `utc_now()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 360 che coinvolge il nodo canonico `utc_now()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 360 che coinvolge il nodo canonico `utc_now()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 360 che coinvolge il nodo canonico `utc_now()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 360 che coinvolge il nodo canonico `utc_now()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 360 che coinvolge il nodo canonico `utc_now()` è `L34`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 360 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 360 che coinvolge il nodo canonico `utc_now()` è `ast`. Nel grafo del codice RDG, l'arco 360 registra come `source` l'identificatore `rve_transactions_common_utc_iso` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo del codice RDG, l'arco 360 registra come `target` l'identificatore `rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 360 che coinvolge il nodo canonico `utc_now()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=361 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `utc_now()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `utc_iso_ms()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_utc_iso_ms` all'ID `rve_transactions_common_utc_now`. La relazione tecnica esplicita è la seguente: `utc_iso_ms()` chiama `utc_now()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 361 che coinvolge il nodo canonico `utc_now()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 361 che coinvolge il nodo canonico `utc_now()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 361 che coinvolge il nodo canonico `utc_now()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 361 che coinvolge il nodo canonico `utc_now()` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 361 che coinvolge il nodo canonico `utc_now()` è `L39`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 361 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 361 che coinvolge il nodo canonico `utc_now()` è `ast`. Nel grafo del codice RDG, l'arco 361 registra come `source` l'identificatore `rve_transactions_common_utc_iso_ms` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo del codice RDG, l'arco 361 registra come `target` l'identificatore `rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 361 che coinvolge il nodo canonico `utc_now()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=363 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `utc_now()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b` all'ID `rve_transactions_common_utc_now`. La relazione tecnica esplicita è la seguente: `rve_1b.py` importa `utc_now()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 363 che coinvolge il nodo canonico `utc_now()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 363 che coinvolge il nodo canonico `utc_now()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 363 che coinvolge il nodo canonico `utc_now()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 363 che coinvolge il nodo canonico `utc_now()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 363 che coinvolge il nodo canonico `utc_now()` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 363 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 363 che coinvolge il nodo canonico `utc_now()` è `ast`. Nel grafo del codice RDG, l'arco 363 registra come `source` l'identificatore `rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo del codice RDG, l'arco 363 registra come `target` l'identificatore `rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 363 che coinvolge il nodo canonico `utc_now()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=364 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `utc_now()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_1b_build_request` all'ID `rve_transactions_common_utc_now`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_1b_build_request]` chiama `utc_now()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 364 che coinvolge il nodo canonico `utc_now()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 364 che coinvolge il nodo canonico `utc_now()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 364 che coinvolge il nodo canonico `utc_now()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 364 che coinvolge il nodo canonico `utc_now()` è `rve_transactions/rve_1b.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 364 che coinvolge il nodo canonico `utc_now()` è `L333`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 364 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 364 che coinvolge il nodo canonico `utc_now()` è `ast`. Nel grafo del codice RDG, l'arco 364 registra come `source` l'identificatore `rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo del codice RDG, l'arco 364 registra come `target` l'identificatore `rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 364 che coinvolge il nodo canonico `utc_now()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=162 id=Request-Dataset-Generator::rve_transactions_common_utc_now -->
Nel grafo unificato, l'etichetta originale del nodo canonico `utc_now()` è `utc_now()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `utc_now()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `utc_now()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `utc_now()` è `L29`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `utc_now()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `utc_now()` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `utc_now()` è `utc_now()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `utc_now()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `utc_now()` è `rve_transactions_common_utc_now`. Nel grafo unificato, il nodo canonico `utc_now()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_utc_now`.
<!-- GRAPH_EDGE graph=unificata index=359 role=outgoing -->
Nel grafo unificato, il nodo canonico `utc_now()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_mock_jwt()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_utc_now` all'ID `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt`. La relazione tecnica esplicita è la seguente: `utc_now()` chiama `build_mock_jwt()`. Nel grafo unificato, il tipo dichiarato dell'arco 359 che coinvolge il nodo canonico `utc_now()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 359 che coinvolge il nodo canonico `utc_now()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 359 che coinvolge il nodo canonico `utc_now()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 359 che coinvolge il nodo canonico `utc_now()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 359 che coinvolge il nodo canonico `utc_now()` è `L146`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 359 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 359 che coinvolge il nodo canonico `utc_now()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 359 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo unificato, l'arco 359 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo unificato, l'arco 359 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_build_mock_jwt` per la relazione che coinvolge il nodo canonico `utc_now()`.
<!-- GRAPH_EDGE graph=unificata index=360 role=outgoing -->
Nel grafo unificato, il nodo canonico `utc_now()` è la sorgente `source` della relazione `calls` verso il nodo canonico `utc_iso()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_utc_now` all'ID `Request-Dataset-Generator::rve_transactions_common_utc_iso`. La relazione tecnica esplicita è la seguente: `utc_now()` chiama `utc_iso()`. Nel grafo unificato, il tipo dichiarato dell'arco 360 che coinvolge il nodo canonico `utc_now()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 360 che coinvolge il nodo canonico `utc_now()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 360 che coinvolge il nodo canonico `utc_now()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 360 che coinvolge il nodo canonico `utc_now()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 360 che coinvolge il nodo canonico `utc_now()` è `L34`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 360 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 360 che coinvolge il nodo canonico `utc_now()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 360 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo unificato, l'arco 360 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo unificato, l'arco 360 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_iso` per la relazione che coinvolge il nodo canonico `utc_now()`.
<!-- GRAPH_EDGE graph=unificata index=361 role=outgoing -->
Nel grafo unificato, il nodo canonico `utc_now()` è la sorgente `source` della relazione `calls` verso il nodo canonico `utc_iso_ms()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_utc_now` all'ID `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms`. La relazione tecnica esplicita è la seguente: `utc_now()` chiama `utc_iso_ms()`. Nel grafo unificato, il tipo dichiarato dell'arco 361 che coinvolge il nodo canonico `utc_now()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 361 che coinvolge il nodo canonico `utc_now()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 361 che coinvolge il nodo canonico `utc_now()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 361 che coinvolge il nodo canonico `utc_now()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 361 che coinvolge il nodo canonico `utc_now()` è `L39`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 361 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 361 che coinvolge il nodo canonico `utc_now()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 361 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo unificato, l'arco 361 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo unificato, l'arco 361 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_iso_ms` per la relazione che coinvolge il nodo canonico `utc_now()`.
<!-- GRAPH_EDGE graph=unificata index=362 role=outgoing -->
Nel grafo unificato, il nodo canonico `utc_now()` è la sorgente `source` della relazione `references` verso il nodo canonico `datetime [rve_transactions_common_py_datetime]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_utc_now` all'ID `Request-Dataset-Generator::rve_transactions_common_py_datetime`. La relazione tecnica esplicita è la seguente: `utc_now()` fa riferimento a `datetime [rve_transactions_common_py_datetime]`. Nel grafo unificato, il tipo dichiarato dell'arco 362 che coinvolge il nodo canonico `utc_now()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 362 che coinvolge il nodo canonico `utc_now()` è `return_type`. Nel grafo unificato, la confidenza categoriale dell'arco 362 che coinvolge il nodo canonico `utc_now()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 362 che coinvolge il nodo canonico `utc_now()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 362 che coinvolge il nodo canonico `utc_now()` è `L29`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 362 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 362 che coinvolge il nodo canonico `utc_now()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 362 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo unificato, l'arco 362 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo unificato, l'arco 362 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_py_datetime` per la relazione che coinvolge il nodo canonico `utc_now()`.
<!-- GRAPH_EDGE graph=unificata index=363 role=outgoing -->
Nel grafo unificato, il nodo canonico `utc_now()` è la sorgente `source` della relazione `imports` verso il nodo canonico `rve_1b.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_utc_now` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b`. La relazione tecnica esplicita è la seguente: `utc_now()` importa `rve_1b.py`. Nel grafo unificato, il tipo dichiarato dell'arco 363 che coinvolge il nodo canonico `utc_now()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 363 che coinvolge il nodo canonico `utc_now()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 363 che coinvolge il nodo canonico `utc_now()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 363 che coinvolge il nodo canonico `utc_now()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 363 che coinvolge il nodo canonico `utc_now()` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 363 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 363 che coinvolge il nodo canonico `utc_now()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 363 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo unificato, l'arco 363 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo unificato, l'arco 363 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b` per la relazione che coinvolge il nodo canonico `utc_now()`.
<!-- GRAPH_EDGE graph=unificata index=364 role=outgoing -->
Nel grafo unificato, il nodo canonico `utc_now()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_request() [rve_transactions_rve_1b_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common_utc_now` all'ID `Request-Dataset-Generator::rve_transactions_rve_1b_build_request`. La relazione tecnica esplicita è la seguente: `utc_now()` chiama `build_request() [rve_transactions_rve_1b_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 364 che coinvolge il nodo canonico `utc_now()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 364 che coinvolge il nodo canonico `utc_now()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 364 che coinvolge il nodo canonico `utc_now()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 364 che coinvolge il nodo canonico `utc_now()` è `rve_transactions/rve_1b.py`. Nel grafo unificato, la posizione sorgente dell'arco 364 che coinvolge il nodo canonico `utc_now()` è `L333`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 364 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 364 che coinvolge il nodo canonico `utc_now()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 364 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo unificato, l'arco 364 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo unificato, l'arco 364 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_1b_build_request` per la relazione che coinvolge il nodo canonico `utc_now()`.
<!-- GRAPH_EDGE graph=unificata index=352 role=incoming -->
Nel grafo unificato, il nodo canonico `utc_now()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_utc_now`. La relazione tecnica esplicita è la seguente: `_common.py` contiene `utc_now()`. Nel grafo unificato, il tipo dichiarato dell'arco 352 che coinvolge il nodo canonico `utc_now()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 352 che coinvolge il nodo canonico `utc_now()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 352 che coinvolge il nodo canonico `utc_now()` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 352 che coinvolge il nodo canonico `utc_now()` è `L29`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 352 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 352 che coinvolge il nodo canonico `utc_now()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 352 che coinvolge il nodo canonico `utc_now()` è `1.0`. Nel grafo unificato, l'arco 352 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `utc_now()`. Nel grafo unificato, l'arco 352 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_utc_now` per la relazione che coinvolge il nodo canonico `utc_now()`.

### Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe

<!-- GRAPH_NODE graph=codice index=176 id=rve_transactions_common_rationale_1 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` possiede l'identificatore esatto `rve_transactions_common_rationale_1`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` alla comunità numerica `2`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `utility condivise da tutti i moduli di transazione rve.  funzioni di supporto pe`.
<!-- GRAPH_EDGE graph=codice index=353 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_common_rationale_1` all'ID `rve_transactions_common`. La relazione tecnica esplicita è la seguente: `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` costituisce una motivazione per `_common.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 353 che coinvolge il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 353 che coinvolge il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 353 che coinvolge il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `rve_transactions/_common.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 353 che coinvolge il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 353 che coinvolge il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 353 che coinvolge il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `ast`. Nel grafo del codice RDG, l'arco 353 registra come `source` l'identificatore `rve_transactions_common_rationale_1` per la relazione che coinvolge il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe`. Nel grafo del codice RDG, l'arco 353 registra come `target` l'identificatore `rve_transactions_common` per la relazione che coinvolge il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 353 che coinvolge il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=176 id=Request-Dataset-Generator::rve_transactions_common_rationale_1 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` alla comunità numerica `2`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `utility condivise da tutti i moduli di transazione rve.  funzioni di supporto pe`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `rve_transactions_common_rationale_1`. Nel grafo unificato, il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_common_rationale_1`.
Nel grafo unificato, il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=353 role=incoming -->
Nel grafo unificato, il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `_common.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_common` all'ID `Request-Dataset-Generator::rve_transactions_common_rationale_1`. La relazione tecnica esplicita è la seguente: `_common.py` costituisce una motivazione per `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe`. Nel grafo unificato, il tipo dichiarato dell'arco 353 che coinvolge il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 353 che coinvolge il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 353 che coinvolge il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `rve_transactions/_common.py`. Nel grafo unificato, la posizione sorgente dell'arco 353 che coinvolge il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 353 che coinvolge il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 353 che coinvolge il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 353 che coinvolge il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe` è `1.0`. Nel grafo unificato, l'arco 353 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_common` per la relazione che coinvolge il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe`. Nel grafo unificato, l'arco 353 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_common_rationale_1` per la relazione che coinvolge il nodo canonico `Utility condivise da tutti i moduli di transazione RVE.  Funzioni di supporto pe`.

## Cluster Graphify 3 — CustomTkinter App Shell

Il cluster editoriale Graphify 3 del dominio Architettura e codice del framework RDG contiene 44 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### .__init__() [ui_app_rdgapp_init]

<!-- GRAPH_NODE graph=codice index=415 id=ui_app_rdgapp_init -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.__init__() [ui_app_rdgapp_init]` è `.__init__()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.__init__() [ui_app_rdgapp_init]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `L36`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [ui_app_rdgapp_init]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.__init__() [ui_app_rdgapp_init]` possiede l'identificatore esatto `ui_app_rdgapp_init`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.__init__() [ui_app_rdgapp_init]` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.__init__() [ui_app_rdgapp_init]` è `.__init__()`.
<!-- GRAPH_EDGE graph=codice index=839 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [ui_app_rdgapp_init]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_tabs()`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp_init` all'ID `ui_app_rdgapp_build_tabs`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_app_rdgapp_init]` chiama `._build_tabs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 839 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 839 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 839 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 839 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 839 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `L48`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 839 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 839 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `ast`. Nel grafo del codice RDG, l'arco 839 registra come `source` l'identificatore `ui_app_rdgapp_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]`. Nel grafo del codice RDG, l'arco 839 registra come `target` l'identificatore `ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 839 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=840 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [ui_app_rdgapp_init]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_titlebar()`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp_init` all'ID `ui_app_rdgapp_build_titlebar`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_app_rdgapp_init]` chiama `._build_titlebar()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 840 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 840 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 840 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 840 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 840 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `L47`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 840 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 840 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `ast`. Nel grafo del codice RDG, l'arco 840 registra come `source` l'identificatore `ui_app_rdgapp_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]`. Nel grafo del codice RDG, l'arco 840 registra come `target` l'identificatore `ui_app_rdgapp_build_titlebar` per la relazione che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 840 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=834 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [ui_app_rdgapp_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RDGApp`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp` all'ID `ui_app_rdgapp_init`. La relazione tecnica esplicita è la seguente: `RDGApp` registra come proprio metodo `.__init__() [ui_app_rdgapp_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 834 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 834 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 834 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 834 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `L36`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 834 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 834 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `ast`. Nel grafo del codice RDG, l'arco 834 registra come `source` l'identificatore `ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]`. Nel grafo del codice RDG, l'arco 834 registra come `target` l'identificatore `ui_app_rdgapp_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 834 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=415 id=Request-Dataset-Generator::ui_app_rdgapp_init -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.__init__() [ui_app_rdgapp_init]` è `.__init__()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.__init__() [ui_app_rdgapp_init]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `ui/app.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `L36`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [ui_app_rdgapp_init]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.__init__() [ui_app_rdgapp_init]` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.__init__() [ui_app_rdgapp_init]` è `.__init__()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.__init__() [ui_app_rdgapp_init]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `ui_app_rdgapp_init`. Nel grafo unificato, il nodo canonico `.__init__() [ui_app_rdgapp_init]` possiede l'identificatore esatto `Request-Dataset-Generator::ui_app_rdgapp_init`.
<!-- GRAPH_EDGE graph=unificata index=839 role=outgoing -->
Nel grafo unificato, il nodo canonico `.__init__() [ui_app_rdgapp_init]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_tabs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp_init` all'ID `Request-Dataset-Generator::ui_app_rdgapp_build_tabs`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_app_rdgapp_init]` chiama `._build_tabs()`. Nel grafo unificato, il tipo dichiarato dell'arco 839 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 839 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 839 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 839 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 839 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `L48`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 839 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 839 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 839 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `1.0`. Nel grafo unificato, l'arco 839 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]`. Nel grafo unificato, l'arco 839 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]`.
<!-- GRAPH_EDGE graph=unificata index=840 role=outgoing -->
Nel grafo unificato, il nodo canonico `.__init__() [ui_app_rdgapp_init]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_titlebar()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp_init` all'ID `Request-Dataset-Generator::ui_app_rdgapp_build_titlebar`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_app_rdgapp_init]` chiama `._build_titlebar()`. Nel grafo unificato, il tipo dichiarato dell'arco 840 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 840 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 840 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 840 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 840 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `L47`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 840 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 840 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 840 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `1.0`. Nel grafo unificato, l'arco 840 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]`. Nel grafo unificato, l'arco 840 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_build_titlebar` per la relazione che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]`.
<!-- GRAPH_EDGE graph=unificata index=834 role=incoming -->
Nel grafo unificato, il nodo canonico `.__init__() [ui_app_rdgapp_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RDGApp`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp` all'ID `Request-Dataset-Generator::ui_app_rdgapp_init`. La relazione tecnica esplicita è la seguente: `RDGApp` registra come proprio metodo `.__init__() [ui_app_rdgapp_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 834 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 834 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 834 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 834 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `L36`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 834 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 834 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 834 che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]` è `1.0`. Nel grafo unificato, l'arco 834 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]`. Nel grafo unificato, l'arco 834 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_app_rdgapp_init]`.

### .__init__() [ui_tabs_dashboard_tab_dashboardtab_init]

<!-- GRAPH_NODE graph=codice index=447 id=ui_tabs_dashboard_tab_dashboardtab_init -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `.__init__()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `L44`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` possiede l'identificatore esatto `ui_tabs_dashboard_tab_dashboardtab_init`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `.__init__()`.
<!-- GRAPH_EDGE graph=codice index=902 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_init` all'ID `ui_tabs_dashboard_tab_dashboardtab_build`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` chiama `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 902 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 902 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 902 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 902 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 902 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `L50`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 902 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 902 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `ast`. Nel grafo del codice RDG, l'arco 902 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`. Nel grafo del codice RDG, l'arco 902 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 902 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=903 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è la sorgente `source` della relazione `calls` verso il nodo canonico `.refresh_results()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_init` all'ID `ui_tabs_dashboard_tab_dashboardtab_refresh_results`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` chiama `.refresh_results()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 903 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 903 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 903 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 903 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 903 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 903 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 903 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `ast`. Nel grafo del codice RDG, l'arco 903 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`. Nel grafo del codice RDG, l'arco 903 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 903 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=894 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_init`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 894 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 894 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 894 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 894 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `L44`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 894 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 894 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `ast`. Nel grafo del codice RDG, l'arco 894 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`. Nel grafo del codice RDG, l'arco 894 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 894 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=447 id=Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_init -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `.__init__()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `L44`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `.__init__()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `ui_tabs_dashboard_tab_dashboardtab_init`. Nel grafo unificato, il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_init`.
<!-- GRAPH_EDGE graph=unificata index=902 role=outgoing -->
Nel grafo unificato, il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_init` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` chiama `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo unificato, il tipo dichiarato dell'arco 902 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 902 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 902 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 902 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 902 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `L50`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 902 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 902 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 902 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `1.0`. Nel grafo unificato, l'arco 902 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`. Nel grafo unificato, l'arco 902 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`.
<!-- GRAPH_EDGE graph=unificata index=903 role=outgoing -->
Nel grafo unificato, il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è la sorgente `source` della relazione `calls` verso il nodo canonico `.refresh_results()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_init` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` chiama `.refresh_results()`. Nel grafo unificato, il tipo dichiarato dell'arco 903 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 903 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 903 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 903 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 903 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 903 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 903 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 903 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `1.0`. Nel grafo unificato, l'arco 903 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`. Nel grafo unificato, l'arco 903 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`.
<!-- GRAPH_EDGE graph=unificata index=894 role=incoming -->
Nel grafo unificato, il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_init`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 894 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 894 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 894 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 894 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `L44`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 894 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 894 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 894 che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` è `1.0`. Nel grafo unificato, l'arco 894 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`. Nel grafo unificato, l'arco 894 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`.

### ._build() [ui_tabs_dashboard_tab_dashboardtab_build]

<!-- GRAPH_NODE graph=codice index=448 id=ui_tabs_dashboard_tab_dashboardtab_build -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `._build()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `L54`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` possiede l'identificatore esatto `ui_tabs_dashboard_tab_dashboardtab_build`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `._build()`.
<!-- GRAPH_EDGE graph=codice index=904 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_chart()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_build` all'ID `ui_tabs_dashboard_tab_dashboardtab_build_chart`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` chiama `._build_chart()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 904 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 904 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 904 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 904 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 904 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `L64`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 904 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 904 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ast`. Nel grafo del codice RDG, l'arco 904 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo del codice RDG, l'arco 904 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build_chart` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 904 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=905 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_metric_card()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_build` all'ID `ui_tabs_dashboard_tab_dashboardtab_build_metric_card`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` chiama `._build_metric_card()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 905 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 905 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 905 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 905 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 905 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `L62`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 905 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 905 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ast`. Nel grafo del codice RDG, l'arco 905 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo del codice RDG, l'arco 905 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build_metric_card` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 905 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=906 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_report()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_build` all'ID `ui_tabs_dashboard_tab_dashboardtab_build_report`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` chiama `._build_report()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 906 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 906 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 906 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 906 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 906 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `L65`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 906 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 906 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ast`. Nel grafo del codice RDG, l'arco 906 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo del codice RDG, l'arco 906 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build_report` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 906 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=887 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_build`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 887 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 887 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 887 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 887 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `L54`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 887 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 887 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ast`. Nel grafo del codice RDG, l'arco 887 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo del codice RDG, l'arco 887 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 887 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=902 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_init` all'ID `ui_tabs_dashboard_tab_dashboardtab_build`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` chiama `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 902 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 902 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 902 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 902 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 902 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `L50`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 902 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 902 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ast`. Nel grafo del codice RDG, l'arco 902 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_init` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo del codice RDG, l'arco 902 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 902 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=448 id=Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `._build()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `L54`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `._build()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ui_tabs_dashboard_tab_dashboardtab_build`. Nel grafo unificato, il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build`.
<!-- GRAPH_EDGE graph=unificata index=904 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_chart()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_chart`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` chiama `._build_chart()`. Nel grafo unificato, il tipo dichiarato dell'arco 904 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 904 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 904 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 904 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 904 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `L64`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 904 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 904 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 904 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `1.0`. Nel grafo unificato, l'arco 904 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo unificato, l'arco 904 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_chart` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`.
<!-- GRAPH_EDGE graph=unificata index=905 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_metric_card()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_metric_card`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` chiama `._build_metric_card()`. Nel grafo unificato, il tipo dichiarato dell'arco 905 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 905 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 905 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 905 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 905 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `L62`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 905 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 905 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 905 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `1.0`. Nel grafo unificato, l'arco 905 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo unificato, l'arco 905 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_metric_card` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`.
<!-- GRAPH_EDGE graph=unificata index=906 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_report()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_report`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` chiama `._build_report()`. Nel grafo unificato, il tipo dichiarato dell'arco 906 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 906 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 906 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 906 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 906 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `L65`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 906 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 906 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 906 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `1.0`. Nel grafo unificato, l'arco 906 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo unificato, l'arco 906 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_report` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`.
<!-- GRAPH_EDGE graph=unificata index=887 role=incoming -->
Nel grafo unificato, il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo unificato, il tipo dichiarato dell'arco 887 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 887 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 887 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 887 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `L54`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 887 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 887 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 887 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `1.0`. Nel grafo unificato, l'arco 887 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo unificato, l'arco 887 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`.
<!-- GRAPH_EDGE graph=unificata index=902 role=incoming -->
Nel grafo unificato, il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_init` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` chiama `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo unificato, il tipo dichiarato dell'arco 902 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 902 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 902 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 902 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 902 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `L50`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 902 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 902 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 902 che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` è `1.0`. Nel grafo unificato, l'arco 902 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_init` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo unificato, l'arco 902 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`.

### ._build_chart()

<!-- GRAPH_NODE graph=codice index=450 id=ui_tabs_dashboard_tab_dashboardtab_build_chart -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._build_chart()` è `._build_chart()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._build_chart()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._build_chart()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._build_chart()` è `L124`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._build_chart()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._build_chart()` possiede l'identificatore esatto `ui_tabs_dashboard_tab_dashboardtab_build_chart`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._build_chart()` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._build_chart()` è `._build_chart()`.
Nel grafo del codice RDG, il nodo canonico `._build_chart()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=888 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_chart()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_build_chart`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._build_chart()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 888 che coinvolge il nodo canonico `._build_chart()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 888 che coinvolge il nodo canonico `._build_chart()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 888 che coinvolge il nodo canonico `._build_chart()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 888 che coinvolge il nodo canonico `._build_chart()` è `L124`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 888 che coinvolge il nodo canonico `._build_chart()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 888 che coinvolge il nodo canonico `._build_chart()` è `ast`. Nel grafo del codice RDG, l'arco 888 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._build_chart()`. Nel grafo del codice RDG, l'arco 888 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build_chart` per la relazione che coinvolge il nodo canonico `._build_chart()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 888 che coinvolge il nodo canonico `._build_chart()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=904 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_chart()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_build` all'ID `ui_tabs_dashboard_tab_dashboardtab_build_chart`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` chiama `._build_chart()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 904 che coinvolge il nodo canonico `._build_chart()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 904 che coinvolge il nodo canonico `._build_chart()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 904 che coinvolge il nodo canonico `._build_chart()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 904 che coinvolge il nodo canonico `._build_chart()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 904 che coinvolge il nodo canonico `._build_chart()` è `L64`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 904 che coinvolge il nodo canonico `._build_chart()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 904 che coinvolge il nodo canonico `._build_chart()` è `ast`. Nel grafo del codice RDG, l'arco 904 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build` per la relazione che coinvolge il nodo canonico `._build_chart()`. Nel grafo del codice RDG, l'arco 904 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build_chart` per la relazione che coinvolge il nodo canonico `._build_chart()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 904 che coinvolge il nodo canonico `._build_chart()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=450 id=Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_chart -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._build_chart()` è `._build_chart()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._build_chart()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._build_chart()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._build_chart()` è `L124`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._build_chart()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._build_chart()` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._build_chart()` è `._build_chart()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._build_chart()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._build_chart()` è `ui_tabs_dashboard_tab_dashboardtab_build_chart`. Nel grafo unificato, il nodo canonico `._build_chart()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_chart`.
Nel grafo unificato, il nodo canonico `._build_chart()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=888 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_chart()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_chart`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._build_chart()`. Nel grafo unificato, il tipo dichiarato dell'arco 888 che coinvolge il nodo canonico `._build_chart()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 888 che coinvolge il nodo canonico `._build_chart()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 888 che coinvolge il nodo canonico `._build_chart()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 888 che coinvolge il nodo canonico `._build_chart()` è `L124`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 888 che coinvolge il nodo canonico `._build_chart()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 888 che coinvolge il nodo canonico `._build_chart()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 888 che coinvolge il nodo canonico `._build_chart()` è `1.0`. Nel grafo unificato, l'arco 888 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._build_chart()`. Nel grafo unificato, l'arco 888 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_chart` per la relazione che coinvolge il nodo canonico `._build_chart()`.
<!-- GRAPH_EDGE graph=unificata index=904 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_chart()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_chart`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` chiama `._build_chart()`. Nel grafo unificato, il tipo dichiarato dell'arco 904 che coinvolge il nodo canonico `._build_chart()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 904 che coinvolge il nodo canonico `._build_chart()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 904 che coinvolge il nodo canonico `._build_chart()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 904 che coinvolge il nodo canonico `._build_chart()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 904 che coinvolge il nodo canonico `._build_chart()` è `L64`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 904 che coinvolge il nodo canonico `._build_chart()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 904 che coinvolge il nodo canonico `._build_chart()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 904 che coinvolge il nodo canonico `._build_chart()` è `1.0`. Nel grafo unificato, l'arco 904 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build` per la relazione che coinvolge il nodo canonico `._build_chart()`. Nel grafo unificato, l'arco 904 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_chart` per la relazione che coinvolge il nodo canonico `._build_chart()`.

### ._build_metric_card()

<!-- GRAPH_NODE graph=codice index=449 id=ui_tabs_dashboard_tab_dashboardtab_build_metric_card -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._build_metric_card()` è `._build_metric_card()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._build_metric_card()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._build_metric_card()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._build_metric_card()` è `L67`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._build_metric_card()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._build_metric_card()` possiede l'identificatore esatto `ui_tabs_dashboard_tab_dashboardtab_build_metric_card`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._build_metric_card()` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._build_metric_card()` è `._build_metric_card()`.
Nel grafo del codice RDG, il nodo canonico `._build_metric_card()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=889 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_metric_card()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_build_metric_card`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._build_metric_card()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 889 che coinvolge il nodo canonico `._build_metric_card()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 889 che coinvolge il nodo canonico `._build_metric_card()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 889 che coinvolge il nodo canonico `._build_metric_card()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 889 che coinvolge il nodo canonico `._build_metric_card()` è `L67`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 889 che coinvolge il nodo canonico `._build_metric_card()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 889 che coinvolge il nodo canonico `._build_metric_card()` è `ast`. Nel grafo del codice RDG, l'arco 889 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._build_metric_card()`. Nel grafo del codice RDG, l'arco 889 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build_metric_card` per la relazione che coinvolge il nodo canonico `._build_metric_card()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 889 che coinvolge il nodo canonico `._build_metric_card()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=905 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_metric_card()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_build` all'ID `ui_tabs_dashboard_tab_dashboardtab_build_metric_card`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` chiama `._build_metric_card()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 905 che coinvolge il nodo canonico `._build_metric_card()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 905 che coinvolge il nodo canonico `._build_metric_card()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 905 che coinvolge il nodo canonico `._build_metric_card()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 905 che coinvolge il nodo canonico `._build_metric_card()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 905 che coinvolge il nodo canonico `._build_metric_card()` è `L62`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 905 che coinvolge il nodo canonico `._build_metric_card()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 905 che coinvolge il nodo canonico `._build_metric_card()` è `ast`. Nel grafo del codice RDG, l'arco 905 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build` per la relazione che coinvolge il nodo canonico `._build_metric_card()`. Nel grafo del codice RDG, l'arco 905 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build_metric_card` per la relazione che coinvolge il nodo canonico `._build_metric_card()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 905 che coinvolge il nodo canonico `._build_metric_card()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=449 id=Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_metric_card -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._build_metric_card()` è `._build_metric_card()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._build_metric_card()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._build_metric_card()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._build_metric_card()` è `L67`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._build_metric_card()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._build_metric_card()` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._build_metric_card()` è `._build_metric_card()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._build_metric_card()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._build_metric_card()` è `ui_tabs_dashboard_tab_dashboardtab_build_metric_card`. Nel grafo unificato, il nodo canonico `._build_metric_card()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_metric_card`.
Nel grafo unificato, il nodo canonico `._build_metric_card()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=889 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_metric_card()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_metric_card`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._build_metric_card()`. Nel grafo unificato, il tipo dichiarato dell'arco 889 che coinvolge il nodo canonico `._build_metric_card()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 889 che coinvolge il nodo canonico `._build_metric_card()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 889 che coinvolge il nodo canonico `._build_metric_card()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 889 che coinvolge il nodo canonico `._build_metric_card()` è `L67`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 889 che coinvolge il nodo canonico `._build_metric_card()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 889 che coinvolge il nodo canonico `._build_metric_card()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 889 che coinvolge il nodo canonico `._build_metric_card()` è `1.0`. Nel grafo unificato, l'arco 889 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._build_metric_card()`. Nel grafo unificato, l'arco 889 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_metric_card` per la relazione che coinvolge il nodo canonico `._build_metric_card()`.
<!-- GRAPH_EDGE graph=unificata index=905 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_metric_card()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_metric_card`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` chiama `._build_metric_card()`. Nel grafo unificato, il tipo dichiarato dell'arco 905 che coinvolge il nodo canonico `._build_metric_card()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 905 che coinvolge il nodo canonico `._build_metric_card()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 905 che coinvolge il nodo canonico `._build_metric_card()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 905 che coinvolge il nodo canonico `._build_metric_card()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 905 che coinvolge il nodo canonico `._build_metric_card()` è `L62`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 905 che coinvolge il nodo canonico `._build_metric_card()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 905 che coinvolge il nodo canonico `._build_metric_card()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 905 che coinvolge il nodo canonico `._build_metric_card()` è `1.0`. Nel grafo unificato, l'arco 905 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build` per la relazione che coinvolge il nodo canonico `._build_metric_card()`. Nel grafo unificato, l'arco 905 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_metric_card` per la relazione che coinvolge il nodo canonico `._build_metric_card()`.

### ._build_report()

<!-- GRAPH_NODE graph=codice index=451 id=ui_tabs_dashboard_tab_dashboardtab_build_report -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._build_report()` è `._build_report()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._build_report()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._build_report()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._build_report()` è `L148`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._build_report()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._build_report()` possiede l'identificatore esatto `ui_tabs_dashboard_tab_dashboardtab_build_report`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._build_report()` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._build_report()` è `._build_report()`.
<!-- GRAPH_EDGE graph=codice index=907 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build_report()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._configure_report_tags()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_build_report` all'ID `ui_tabs_dashboard_tab_dashboardtab_configure_report_tags`. La relazione tecnica esplicita è la seguente: `._build_report()` chiama `._configure_report_tags()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 907 che coinvolge il nodo canonico `._build_report()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 907 che coinvolge il nodo canonico `._build_report()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 907 che coinvolge il nodo canonico `._build_report()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 907 che coinvolge il nodo canonico `._build_report()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 907 che coinvolge il nodo canonico `._build_report()` è `L197`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 907 che coinvolge il nodo canonico `._build_report()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 907 che coinvolge il nodo canonico `._build_report()` è `ast`. Nel grafo del codice RDG, l'arco 907 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build_report` per la relazione che coinvolge il nodo canonico `._build_report()`. Nel grafo del codice RDG, l'arco 907 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_configure_report_tags` per la relazione che coinvolge il nodo canonico `._build_report()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 907 che coinvolge il nodo canonico `._build_report()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=890 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_report()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_build_report`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._build_report()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 890 che coinvolge il nodo canonico `._build_report()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 890 che coinvolge il nodo canonico `._build_report()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 890 che coinvolge il nodo canonico `._build_report()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 890 che coinvolge il nodo canonico `._build_report()` è `L148`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 890 che coinvolge il nodo canonico `._build_report()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 890 che coinvolge il nodo canonico `._build_report()` è `ast`. Nel grafo del codice RDG, l'arco 890 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._build_report()`. Nel grafo del codice RDG, l'arco 890 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build_report` per la relazione che coinvolge il nodo canonico `._build_report()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 890 che coinvolge il nodo canonico `._build_report()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=906 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_report()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_build` all'ID `ui_tabs_dashboard_tab_dashboardtab_build_report`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` chiama `._build_report()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 906 che coinvolge il nodo canonico `._build_report()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 906 che coinvolge il nodo canonico `._build_report()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 906 che coinvolge il nodo canonico `._build_report()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 906 che coinvolge il nodo canonico `._build_report()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 906 che coinvolge il nodo canonico `._build_report()` è `L65`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 906 che coinvolge il nodo canonico `._build_report()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 906 che coinvolge il nodo canonico `._build_report()` è `ast`. Nel grafo del codice RDG, l'arco 906 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build` per la relazione che coinvolge il nodo canonico `._build_report()`. Nel grafo del codice RDG, l'arco 906 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build_report` per la relazione che coinvolge il nodo canonico `._build_report()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 906 che coinvolge il nodo canonico `._build_report()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=451 id=Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_report -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._build_report()` è `._build_report()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._build_report()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._build_report()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._build_report()` è `L148`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._build_report()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._build_report()` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._build_report()` è `._build_report()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._build_report()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._build_report()` è `ui_tabs_dashboard_tab_dashboardtab_build_report`. Nel grafo unificato, il nodo canonico `._build_report()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_report`.
<!-- GRAPH_EDGE graph=unificata index=907 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build_report()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._configure_report_tags()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_report` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_configure_report_tags`. La relazione tecnica esplicita è la seguente: `._build_report()` chiama `._configure_report_tags()`. Nel grafo unificato, il tipo dichiarato dell'arco 907 che coinvolge il nodo canonico `._build_report()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 907 che coinvolge il nodo canonico `._build_report()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 907 che coinvolge il nodo canonico `._build_report()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 907 che coinvolge il nodo canonico `._build_report()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 907 che coinvolge il nodo canonico `._build_report()` è `L197`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 907 che coinvolge il nodo canonico `._build_report()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 907 che coinvolge il nodo canonico `._build_report()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 907 che coinvolge il nodo canonico `._build_report()` è `1.0`. Nel grafo unificato, l'arco 907 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_report` per la relazione che coinvolge il nodo canonico `._build_report()`. Nel grafo unificato, l'arco 907 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_configure_report_tags` per la relazione che coinvolge il nodo canonico `._build_report()`.
<!-- GRAPH_EDGE graph=unificata index=890 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_report()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_report`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._build_report()`. Nel grafo unificato, il tipo dichiarato dell'arco 890 che coinvolge il nodo canonico `._build_report()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 890 che coinvolge il nodo canonico `._build_report()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 890 che coinvolge il nodo canonico `._build_report()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 890 che coinvolge il nodo canonico `._build_report()` è `L148`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 890 che coinvolge il nodo canonico `._build_report()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 890 che coinvolge il nodo canonico `._build_report()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 890 che coinvolge il nodo canonico `._build_report()` è `1.0`. Nel grafo unificato, l'arco 890 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._build_report()`. Nel grafo unificato, l'arco 890 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_report` per la relazione che coinvolge il nodo canonico `._build_report()`.
<!-- GRAPH_EDGE graph=unificata index=906 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_report()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_report`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_dashboard_tab_dashboardtab_build]` chiama `._build_report()`. Nel grafo unificato, il tipo dichiarato dell'arco 906 che coinvolge il nodo canonico `._build_report()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 906 che coinvolge il nodo canonico `._build_report()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 906 che coinvolge il nodo canonico `._build_report()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 906 che coinvolge il nodo canonico `._build_report()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 906 che coinvolge il nodo canonico `._build_report()` è `L65`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 906 che coinvolge il nodo canonico `._build_report()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 906 che coinvolge il nodo canonico `._build_report()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 906 che coinvolge il nodo canonico `._build_report()` è `1.0`. Nel grafo unificato, l'arco 906 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build` per la relazione che coinvolge il nodo canonico `._build_report()`. Nel grafo unificato, l'arco 906 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_report` per la relazione che coinvolge il nodo canonico `._build_report()`.

### ._build_tabs()

<!-- GRAPH_NODE graph=codice index=417 id=ui_app_rdgapp_build_tabs -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._build_tabs()` è `._build_tabs()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._build_tabs()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._build_tabs()` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._build_tabs()` è `L85`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._build_tabs()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._build_tabs()` possiede l'identificatore esatto `ui_app_rdgapp_build_tabs`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._build_tabs()` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._build_tabs()` è `._build_tabs()`.
<!-- GRAPH_EDGE graph=codice index=841 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build_tabs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._style_segmented_tabs()`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp_build_tabs` all'ID `ui_app_rdgapp_style_segmented_tabs`. La relazione tecnica esplicita è la seguente: `._build_tabs()` chiama `._style_segmented_tabs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 841 che coinvolge il nodo canonico `._build_tabs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 841 che coinvolge il nodo canonico `._build_tabs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 841 che coinvolge il nodo canonico `._build_tabs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 841 che coinvolge il nodo canonico `._build_tabs()` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 841 che coinvolge il nodo canonico `._build_tabs()` è `L124`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 841 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 841 che coinvolge il nodo canonico `._build_tabs()` è `ast`. Nel grafo del codice RDG, l'arco 841 registra come `source` l'identificatore `ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `._build_tabs()`. Nel grafo del codice RDG, l'arco 841 registra come `target` l'identificatore `ui_app_rdgapp_style_segmented_tabs` per la relazione che coinvolge il nodo canonico `._build_tabs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 841 che coinvolge il nodo canonico `._build_tabs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=842 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build_tabs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp_build_tabs` all'ID `ui_tabs_config_tab_configtab`. La relazione tecnica esplicita è la seguente: `._build_tabs()` chiama `ConfigTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 842 che coinvolge il nodo canonico `._build_tabs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 842 che coinvolge il nodo canonico `._build_tabs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 842 che coinvolge il nodo canonico `._build_tabs()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 842 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 842 che coinvolge il nodo canonico `._build_tabs()` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 842 che coinvolge il nodo canonico `._build_tabs()` è `L114`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 842 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 842 che coinvolge il nodo canonico `._build_tabs()` è `ast`. Nel grafo del codice RDG, l'arco 842 registra come `source` l'identificatore `ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `._build_tabs()`. Nel grafo del codice RDG, l'arco 842 registra come `target` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._build_tabs()`.
<!-- GRAPH_EDGE graph=codice index=843 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build_tabs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp_build_tabs` all'ID `ui_tabs_dashboard_tab_dashboardtab`. La relazione tecnica esplicita è la seguente: `._build_tabs()` chiama `DashboardTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 843 che coinvolge il nodo canonico `._build_tabs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 843 che coinvolge il nodo canonico `._build_tabs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 843 che coinvolge il nodo canonico `._build_tabs()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 843 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 843 che coinvolge il nodo canonico `._build_tabs()` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 843 che coinvolge il nodo canonico `._build_tabs()` è `L111`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 843 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 843 che coinvolge il nodo canonico `._build_tabs()` è `ast`. Nel grafo del codice RDG, l'arco 843 registra come `source` l'identificatore `ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `._build_tabs()`. Nel grafo del codice RDG, l'arco 843 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._build_tabs()`.
<!-- GRAPH_EDGE graph=codice index=844 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build_tabs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp_build_tabs` all'ID `ui_tabs_run_tab_runtab`. La relazione tecnica esplicita è la seguente: `._build_tabs()` chiama `RunTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 844 che coinvolge il nodo canonico `._build_tabs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 844 che coinvolge il nodo canonico `._build_tabs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 844 che coinvolge il nodo canonico `._build_tabs()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 844 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 844 che coinvolge il nodo canonico `._build_tabs()` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 844 che coinvolge il nodo canonico `._build_tabs()` è `L117`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 844 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 844 che coinvolge il nodo canonico `._build_tabs()` è `ast`. Nel grafo del codice RDG, l'arco 844 registra come `source` l'identificatore `ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `._build_tabs()`. Nel grafo del codice RDG, l'arco 844 registra come `target` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._build_tabs()`.
<!-- GRAPH_EDGE graph=codice index=832 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_tabs()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RDGApp`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp` all'ID `ui_app_rdgapp_build_tabs`. La relazione tecnica esplicita è la seguente: `RDGApp` registra come proprio metodo `._build_tabs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 832 che coinvolge il nodo canonico `._build_tabs()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 832 che coinvolge il nodo canonico `._build_tabs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 832 che coinvolge il nodo canonico `._build_tabs()` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 832 che coinvolge il nodo canonico `._build_tabs()` è `L85`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 832 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 832 che coinvolge il nodo canonico `._build_tabs()` è `ast`. Nel grafo del codice RDG, l'arco 832 registra come `source` l'identificatore `ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `._build_tabs()`. Nel grafo del codice RDG, l'arco 832 registra come `target` l'identificatore `ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `._build_tabs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 832 che coinvolge il nodo canonico `._build_tabs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=839 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_tabs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.__init__() [ui_app_rdgapp_init]`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp_init` all'ID `ui_app_rdgapp_build_tabs`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_app_rdgapp_init]` chiama `._build_tabs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 839 che coinvolge il nodo canonico `._build_tabs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 839 che coinvolge il nodo canonico `._build_tabs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 839 che coinvolge il nodo canonico `._build_tabs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 839 che coinvolge il nodo canonico `._build_tabs()` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 839 che coinvolge il nodo canonico `._build_tabs()` è `L48`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 839 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 839 che coinvolge il nodo canonico `._build_tabs()` è `ast`. Nel grafo del codice RDG, l'arco 839 registra come `source` l'identificatore `ui_app_rdgapp_init` per la relazione che coinvolge il nodo canonico `._build_tabs()`. Nel grafo del codice RDG, l'arco 839 registra come `target` l'identificatore `ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `._build_tabs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 839 che coinvolge il nodo canonico `._build_tabs()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=417 id=Request-Dataset-Generator::ui_app_rdgapp_build_tabs -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._build_tabs()` è `._build_tabs()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._build_tabs()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._build_tabs()` è `ui/app.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._build_tabs()` è `L85`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._build_tabs()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._build_tabs()` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._build_tabs()` è `._build_tabs()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._build_tabs()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._build_tabs()` è `ui_app_rdgapp_build_tabs`. Nel grafo unificato, il nodo canonico `._build_tabs()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_app_rdgapp_build_tabs`.
<!-- GRAPH_EDGE graph=unificata index=841 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build_tabs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._style_segmented_tabs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp_build_tabs` all'ID `Request-Dataset-Generator::ui_app_rdgapp_style_segmented_tabs`. La relazione tecnica esplicita è la seguente: `._build_tabs()` chiama `._style_segmented_tabs()`. Nel grafo unificato, il tipo dichiarato dell'arco 841 che coinvolge il nodo canonico `._build_tabs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 841 che coinvolge il nodo canonico `._build_tabs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 841 che coinvolge il nodo canonico `._build_tabs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 841 che coinvolge il nodo canonico `._build_tabs()` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 841 che coinvolge il nodo canonico `._build_tabs()` è `L124`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 841 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 841 che coinvolge il nodo canonico `._build_tabs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 841 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo unificato, l'arco 841 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `._build_tabs()`. Nel grafo unificato, l'arco 841 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_style_segmented_tabs` per la relazione che coinvolge il nodo canonico `._build_tabs()`.
<!-- GRAPH_EDGE graph=unificata index=842 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build_tabs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp_build_tabs` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab`. La relazione tecnica esplicita è la seguente: `._build_tabs()` chiama `ConfigTab`. Nel grafo unificato, il tipo dichiarato dell'arco 842 che coinvolge il nodo canonico `._build_tabs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 842 che coinvolge il nodo canonico `._build_tabs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 842 che coinvolge il nodo canonico `._build_tabs()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 842 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 842 che coinvolge il nodo canonico `._build_tabs()` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 842 che coinvolge il nodo canonico `._build_tabs()` è `L114`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 842 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 842 che coinvolge il nodo canonico `._build_tabs()` è `ast`. Nel grafo unificato, l'arco 842 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `._build_tabs()`. Nel grafo unificato, l'arco 842 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._build_tabs()`.
<!-- GRAPH_EDGE graph=unificata index=843 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build_tabs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp_build_tabs` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab`. La relazione tecnica esplicita è la seguente: `._build_tabs()` chiama `DashboardTab`. Nel grafo unificato, il tipo dichiarato dell'arco 843 che coinvolge il nodo canonico `._build_tabs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 843 che coinvolge il nodo canonico `._build_tabs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 843 che coinvolge il nodo canonico `._build_tabs()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 843 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 843 che coinvolge il nodo canonico `._build_tabs()` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 843 che coinvolge il nodo canonico `._build_tabs()` è `L111`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 843 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 843 che coinvolge il nodo canonico `._build_tabs()` è `ast`. Nel grafo unificato, l'arco 843 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `._build_tabs()`. Nel grafo unificato, l'arco 843 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._build_tabs()`.
<!-- GRAPH_EDGE graph=unificata index=844 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build_tabs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp_build_tabs` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab`. La relazione tecnica esplicita è la seguente: `._build_tabs()` chiama `RunTab`. Nel grafo unificato, il tipo dichiarato dell'arco 844 che coinvolge il nodo canonico `._build_tabs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 844 che coinvolge il nodo canonico `._build_tabs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 844 che coinvolge il nodo canonico `._build_tabs()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 844 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 844 che coinvolge il nodo canonico `._build_tabs()` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 844 che coinvolge il nodo canonico `._build_tabs()` è `L117`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 844 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 844 che coinvolge il nodo canonico `._build_tabs()` è `ast`. Nel grafo unificato, l'arco 844 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `._build_tabs()`. Nel grafo unificato, l'arco 844 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._build_tabs()`.
<!-- GRAPH_EDGE graph=unificata index=832 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_tabs()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RDGApp`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp` all'ID `Request-Dataset-Generator::ui_app_rdgapp_build_tabs`. La relazione tecnica esplicita è la seguente: `RDGApp` registra come proprio metodo `._build_tabs()`. Nel grafo unificato, il tipo dichiarato dell'arco 832 che coinvolge il nodo canonico `._build_tabs()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 832 che coinvolge il nodo canonico `._build_tabs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 832 che coinvolge il nodo canonico `._build_tabs()` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 832 che coinvolge il nodo canonico `._build_tabs()` è `L85`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 832 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 832 che coinvolge il nodo canonico `._build_tabs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 832 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo unificato, l'arco 832 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `._build_tabs()`. Nel grafo unificato, l'arco 832 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `._build_tabs()`.
<!-- GRAPH_EDGE graph=unificata index=839 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_tabs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.__init__() [ui_app_rdgapp_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp_init` all'ID `Request-Dataset-Generator::ui_app_rdgapp_build_tabs`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_app_rdgapp_init]` chiama `._build_tabs()`. Nel grafo unificato, il tipo dichiarato dell'arco 839 che coinvolge il nodo canonico `._build_tabs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 839 che coinvolge il nodo canonico `._build_tabs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 839 che coinvolge il nodo canonico `._build_tabs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 839 che coinvolge il nodo canonico `._build_tabs()` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 839 che coinvolge il nodo canonico `._build_tabs()` è `L48`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 839 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 839 che coinvolge il nodo canonico `._build_tabs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 839 che coinvolge il nodo canonico `._build_tabs()` è `1.0`. Nel grafo unificato, l'arco 839 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_init` per la relazione che coinvolge il nodo canonico `._build_tabs()`. Nel grafo unificato, l'arco 839 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `._build_tabs()`.

### ._build_titlebar()

<!-- GRAPH_NODE graph=codice index=416 id=ui_app_rdgapp_build_titlebar -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._build_titlebar()` è `._build_titlebar()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._build_titlebar()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._build_titlebar()` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._build_titlebar()` è `L51`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._build_titlebar()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._build_titlebar()` possiede l'identificatore esatto `ui_app_rdgapp_build_titlebar`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._build_titlebar()` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._build_titlebar()` è `._build_titlebar()`.
Nel grafo del codice RDG, il nodo canonico `._build_titlebar()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=833 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_titlebar()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RDGApp`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp` all'ID `ui_app_rdgapp_build_titlebar`. La relazione tecnica esplicita è la seguente: `RDGApp` registra come proprio metodo `._build_titlebar()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 833 che coinvolge il nodo canonico `._build_titlebar()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 833 che coinvolge il nodo canonico `._build_titlebar()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 833 che coinvolge il nodo canonico `._build_titlebar()` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 833 che coinvolge il nodo canonico `._build_titlebar()` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 833 che coinvolge il nodo canonico `._build_titlebar()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 833 che coinvolge il nodo canonico `._build_titlebar()` è `ast`. Nel grafo del codice RDG, l'arco 833 registra come `source` l'identificatore `ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `._build_titlebar()`. Nel grafo del codice RDG, l'arco 833 registra come `target` l'identificatore `ui_app_rdgapp_build_titlebar` per la relazione che coinvolge il nodo canonico `._build_titlebar()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 833 che coinvolge il nodo canonico `._build_titlebar()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=840 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_titlebar()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.__init__() [ui_app_rdgapp_init]`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp_init` all'ID `ui_app_rdgapp_build_titlebar`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_app_rdgapp_init]` chiama `._build_titlebar()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 840 che coinvolge il nodo canonico `._build_titlebar()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 840 che coinvolge il nodo canonico `._build_titlebar()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 840 che coinvolge il nodo canonico `._build_titlebar()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 840 che coinvolge il nodo canonico `._build_titlebar()` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 840 che coinvolge il nodo canonico `._build_titlebar()` è `L47`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 840 che coinvolge il nodo canonico `._build_titlebar()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 840 che coinvolge il nodo canonico `._build_titlebar()` è `ast`. Nel grafo del codice RDG, l'arco 840 registra come `source` l'identificatore `ui_app_rdgapp_init` per la relazione che coinvolge il nodo canonico `._build_titlebar()`. Nel grafo del codice RDG, l'arco 840 registra come `target` l'identificatore `ui_app_rdgapp_build_titlebar` per la relazione che coinvolge il nodo canonico `._build_titlebar()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 840 che coinvolge il nodo canonico `._build_titlebar()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=416 id=Request-Dataset-Generator::ui_app_rdgapp_build_titlebar -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._build_titlebar()` è `._build_titlebar()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._build_titlebar()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._build_titlebar()` è `ui/app.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._build_titlebar()` è `L51`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._build_titlebar()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._build_titlebar()` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._build_titlebar()` è `._build_titlebar()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._build_titlebar()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._build_titlebar()` è `ui_app_rdgapp_build_titlebar`. Nel grafo unificato, il nodo canonico `._build_titlebar()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_app_rdgapp_build_titlebar`.
Nel grafo unificato, il nodo canonico `._build_titlebar()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=833 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_titlebar()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RDGApp`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp` all'ID `Request-Dataset-Generator::ui_app_rdgapp_build_titlebar`. La relazione tecnica esplicita è la seguente: `RDGApp` registra come proprio metodo `._build_titlebar()`. Nel grafo unificato, il tipo dichiarato dell'arco 833 che coinvolge il nodo canonico `._build_titlebar()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 833 che coinvolge il nodo canonico `._build_titlebar()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 833 che coinvolge il nodo canonico `._build_titlebar()` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 833 che coinvolge il nodo canonico `._build_titlebar()` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 833 che coinvolge il nodo canonico `._build_titlebar()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 833 che coinvolge il nodo canonico `._build_titlebar()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 833 che coinvolge il nodo canonico `._build_titlebar()` è `1.0`. Nel grafo unificato, l'arco 833 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `._build_titlebar()`. Nel grafo unificato, l'arco 833 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_build_titlebar` per la relazione che coinvolge il nodo canonico `._build_titlebar()`.
<!-- GRAPH_EDGE graph=unificata index=840 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_titlebar()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.__init__() [ui_app_rdgapp_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp_init` all'ID `Request-Dataset-Generator::ui_app_rdgapp_build_titlebar`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_app_rdgapp_init]` chiama `._build_titlebar()`. Nel grafo unificato, il tipo dichiarato dell'arco 840 che coinvolge il nodo canonico `._build_titlebar()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 840 che coinvolge il nodo canonico `._build_titlebar()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 840 che coinvolge il nodo canonico `._build_titlebar()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 840 che coinvolge il nodo canonico `._build_titlebar()` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 840 che coinvolge il nodo canonico `._build_titlebar()` è `L47`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 840 che coinvolge il nodo canonico `._build_titlebar()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 840 che coinvolge il nodo canonico `._build_titlebar()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 840 che coinvolge il nodo canonico `._build_titlebar()` è `1.0`. Nel grafo unificato, l'arco 840 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_init` per la relazione che coinvolge il nodo canonico `._build_titlebar()`. Nel grafo unificato, l'arco 840 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_build_titlebar` per la relazione che coinvolge il nodo canonico `._build_titlebar()`.

### ._chart_values()

<!-- GRAPH_NODE graph=codice index=461 id=ui_tabs_dashboard_tab_dashboardtab_chart_values -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._chart_values()` è `._chart_values()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._chart_values()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._chart_values()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._chart_values()` è `L336`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._chart_values()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._chart_values()` possiede l'identificatore esatto `ui_tabs_dashboard_tab_dashboardtab_chart_values`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._chart_values()` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._chart_values()` è `._chart_values()`.
<!-- GRAPH_EDGE graph=codice index=914 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._chart_values()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_chart_values` all'ID `ui_tabs_dashboard_tab_py_any`. La relazione tecnica esplicita è la seguente: `._chart_values()` fa riferimento a `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 914 che coinvolge il nodo canonico `._chart_values()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 914 che coinvolge il nodo canonico `._chart_values()` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 914 che coinvolge il nodo canonico `._chart_values()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 914 che coinvolge il nodo canonico `._chart_values()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 914 che coinvolge il nodo canonico `._chart_values()` è `L336`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 914 che coinvolge il nodo canonico `._chart_values()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 914 che coinvolge il nodo canonico `._chart_values()` è `ast`. Nel grafo del codice RDG, l'arco 914 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_chart_values` per la relazione che coinvolge il nodo canonico `._chart_values()`. Nel grafo del codice RDG, l'arco 914 registra come `target` l'identificatore `ui_tabs_dashboard_tab_py_any` per la relazione che coinvolge il nodo canonico `._chart_values()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 914 che coinvolge il nodo canonico `._chart_values()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=891 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._chart_values()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_chart_values`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._chart_values()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 891 che coinvolge il nodo canonico `._chart_values()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 891 che coinvolge il nodo canonico `._chart_values()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 891 che coinvolge il nodo canonico `._chart_values()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 891 che coinvolge il nodo canonico `._chart_values()` è `L336`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 891 che coinvolge il nodo canonico `._chart_values()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 891 che coinvolge il nodo canonico `._chart_values()` è `ast`. Nel grafo del codice RDG, l'arco 891 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._chart_values()`. Nel grafo del codice RDG, l'arco 891 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_chart_values` per la relazione che coinvolge il nodo canonico `._chart_values()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 891 che coinvolge il nodo canonico `._chart_values()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=918 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._chart_values()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._draw_chart()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_draw_chart` all'ID `ui_tabs_dashboard_tab_dashboardtab_chart_values`. La relazione tecnica esplicita è la seguente: `._draw_chart()` chiama `._chart_values()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 918 che coinvolge il nodo canonico `._chart_values()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 918 che coinvolge il nodo canonico `._chart_values()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 918 che coinvolge il nodo canonico `._chart_values()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 918 che coinvolge il nodo canonico `._chart_values()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 918 che coinvolge il nodo canonico `._chart_values()` è `L246`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 918 che coinvolge il nodo canonico `._chart_values()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 918 che coinvolge il nodo canonico `._chart_values()` è `ast`. Nel grafo del codice RDG, l'arco 918 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_draw_chart` per la relazione che coinvolge il nodo canonico `._chart_values()`. Nel grafo del codice RDG, l'arco 918 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_chart_values` per la relazione che coinvolge il nodo canonico `._chart_values()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 918 che coinvolge il nodo canonico `._chart_values()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=461 id=Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_chart_values -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._chart_values()` è `._chart_values()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._chart_values()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._chart_values()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._chart_values()` è `L336`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._chart_values()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._chart_values()` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._chart_values()` è `._chart_values()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._chart_values()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._chart_values()` è `ui_tabs_dashboard_tab_dashboardtab_chart_values`. Nel grafo unificato, il nodo canonico `._chart_values()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_chart_values`.
Nel grafo unificato, il nodo canonico `._chart_values()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=891 role=incoming -->
Nel grafo unificato, il nodo canonico `._chart_values()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_chart_values`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._chart_values()`. Nel grafo unificato, il tipo dichiarato dell'arco 891 che coinvolge il nodo canonico `._chart_values()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 891 che coinvolge il nodo canonico `._chart_values()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 891 che coinvolge il nodo canonico `._chart_values()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 891 che coinvolge il nodo canonico `._chart_values()` è `L336`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 891 che coinvolge il nodo canonico `._chart_values()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 891 che coinvolge il nodo canonico `._chart_values()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 891 che coinvolge il nodo canonico `._chart_values()` è `1.0`. Nel grafo unificato, l'arco 891 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._chart_values()`. Nel grafo unificato, l'arco 891 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_chart_values` per la relazione che coinvolge il nodo canonico `._chart_values()`.
<!-- GRAPH_EDGE graph=unificata index=914 role=incoming -->
Nel grafo unificato, il nodo canonico `._chart_values()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_chart_values`. La relazione tecnica esplicita è la seguente: `Any [ui_tabs_dashboard_tab_py_any]` fa riferimento a `._chart_values()`. Nel grafo unificato, il tipo dichiarato dell'arco 914 che coinvolge il nodo canonico `._chart_values()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 914 che coinvolge il nodo canonico `._chart_values()` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 914 che coinvolge il nodo canonico `._chart_values()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 914 che coinvolge il nodo canonico `._chart_values()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 914 che coinvolge il nodo canonico `._chart_values()` è `L336`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 914 che coinvolge il nodo canonico `._chart_values()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 914 che coinvolge il nodo canonico `._chart_values()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 914 che coinvolge il nodo canonico `._chart_values()` è `1.0`. Nel grafo unificato, l'arco 914 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any` per la relazione che coinvolge il nodo canonico `._chart_values()`. Nel grafo unificato, l'arco 914 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_chart_values` per la relazione che coinvolge il nodo canonico `._chart_values()`.
<!-- GRAPH_EDGE graph=unificata index=918 role=incoming -->
Nel grafo unificato, il nodo canonico `._chart_values()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._draw_chart()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_draw_chart` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_chart_values`. La relazione tecnica esplicita è la seguente: `._draw_chart()` chiama `._chart_values()`. Nel grafo unificato, il tipo dichiarato dell'arco 918 che coinvolge il nodo canonico `._chart_values()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 918 che coinvolge il nodo canonico `._chart_values()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 918 che coinvolge il nodo canonico `._chart_values()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 918 che coinvolge il nodo canonico `._chart_values()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 918 che coinvolge il nodo canonico `._chart_values()` è `L246`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 918 che coinvolge il nodo canonico `._chart_values()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 918 che coinvolge il nodo canonico `._chart_values()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 918 che coinvolge il nodo canonico `._chart_values()` è `1.0`. Nel grafo unificato, l'arco 918 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_draw_chart` per la relazione che coinvolge il nodo canonico `._chart_values()`. Nel grafo unificato, l'arco 918 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_chart_values` per la relazione che coinvolge il nodo canonico `._chart_values()`.

### ._configure_report_tags()

<!-- GRAPH_NODE graph=codice index=462 id=ui_tabs_dashboard_tab_dashboardtab_configure_report_tags -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._configure_report_tags()` è `._configure_report_tags()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._configure_report_tags()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._configure_report_tags()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._configure_report_tags()` è `L354`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._configure_report_tags()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._configure_report_tags()` possiede l'identificatore esatto `ui_tabs_dashboard_tab_dashboardtab_configure_report_tags`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._configure_report_tags()` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._configure_report_tags()` è `._configure_report_tags()`.
Nel grafo del codice RDG, il nodo canonico `._configure_report_tags()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=892 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._configure_report_tags()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_configure_report_tags`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._configure_report_tags()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 892 che coinvolge il nodo canonico `._configure_report_tags()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 892 che coinvolge il nodo canonico `._configure_report_tags()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 892 che coinvolge il nodo canonico `._configure_report_tags()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 892 che coinvolge il nodo canonico `._configure_report_tags()` è `L354`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 892 che coinvolge il nodo canonico `._configure_report_tags()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 892 che coinvolge il nodo canonico `._configure_report_tags()` è `ast`. Nel grafo del codice RDG, l'arco 892 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._configure_report_tags()`. Nel grafo del codice RDG, l'arco 892 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_configure_report_tags` per la relazione che coinvolge il nodo canonico `._configure_report_tags()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 892 che coinvolge il nodo canonico `._configure_report_tags()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=907 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._configure_report_tags()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build_report()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_build_report` all'ID `ui_tabs_dashboard_tab_dashboardtab_configure_report_tags`. La relazione tecnica esplicita è la seguente: `._build_report()` chiama `._configure_report_tags()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 907 che coinvolge il nodo canonico `._configure_report_tags()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 907 che coinvolge il nodo canonico `._configure_report_tags()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 907 che coinvolge il nodo canonico `._configure_report_tags()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 907 che coinvolge il nodo canonico `._configure_report_tags()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 907 che coinvolge il nodo canonico `._configure_report_tags()` è `L197`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 907 che coinvolge il nodo canonico `._configure_report_tags()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 907 che coinvolge il nodo canonico `._configure_report_tags()` è `ast`. Nel grafo del codice RDG, l'arco 907 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build_report` per la relazione che coinvolge il nodo canonico `._configure_report_tags()`. Nel grafo del codice RDG, l'arco 907 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_configure_report_tags` per la relazione che coinvolge il nodo canonico `._configure_report_tags()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 907 che coinvolge il nodo canonico `._configure_report_tags()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=462 id=Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_configure_report_tags -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._configure_report_tags()` è `._configure_report_tags()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._configure_report_tags()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._configure_report_tags()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._configure_report_tags()` è `L354`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._configure_report_tags()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._configure_report_tags()` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._configure_report_tags()` è `._configure_report_tags()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._configure_report_tags()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._configure_report_tags()` è `ui_tabs_dashboard_tab_dashboardtab_configure_report_tags`. Nel grafo unificato, il nodo canonico `._configure_report_tags()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_configure_report_tags`.
Nel grafo unificato, il nodo canonico `._configure_report_tags()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=892 role=incoming -->
Nel grafo unificato, il nodo canonico `._configure_report_tags()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_configure_report_tags`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._configure_report_tags()`. Nel grafo unificato, il tipo dichiarato dell'arco 892 che coinvolge il nodo canonico `._configure_report_tags()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 892 che coinvolge il nodo canonico `._configure_report_tags()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 892 che coinvolge il nodo canonico `._configure_report_tags()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 892 che coinvolge il nodo canonico `._configure_report_tags()` è `L354`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 892 che coinvolge il nodo canonico `._configure_report_tags()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 892 che coinvolge il nodo canonico `._configure_report_tags()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 892 che coinvolge il nodo canonico `._configure_report_tags()` è `1.0`. Nel grafo unificato, l'arco 892 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._configure_report_tags()`. Nel grafo unificato, l'arco 892 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_configure_report_tags` per la relazione che coinvolge il nodo canonico `._configure_report_tags()`.
<!-- GRAPH_EDGE graph=unificata index=907 role=incoming -->
Nel grafo unificato, il nodo canonico `._configure_report_tags()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build_report()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_report` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_configure_report_tags`. La relazione tecnica esplicita è la seguente: `._build_report()` chiama `._configure_report_tags()`. Nel grafo unificato, il tipo dichiarato dell'arco 907 che coinvolge il nodo canonico `._configure_report_tags()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 907 che coinvolge il nodo canonico `._configure_report_tags()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 907 che coinvolge il nodo canonico `._configure_report_tags()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 907 che coinvolge il nodo canonico `._configure_report_tags()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 907 che coinvolge il nodo canonico `._configure_report_tags()` è `L197`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 907 che coinvolge il nodo canonico `._configure_report_tags()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 907 che coinvolge il nodo canonico `._configure_report_tags()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 907 che coinvolge il nodo canonico `._configure_report_tags()` è `1.0`. Nel grafo unificato, l'arco 907 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_report` per la relazione che coinvolge il nodo canonico `._configure_report_tags()`. Nel grafo unificato, l'arco 907 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_configure_report_tags` per la relazione che coinvolge il nodo canonico `._configure_report_tags()`.

### ._draw_chart()

<!-- GRAPH_NODE graph=codice index=456 id=ui_tabs_dashboard_tab_dashboardtab_draw_chart -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._draw_chart()` è `._draw_chart()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._draw_chart()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._draw_chart()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._draw_chart()` è `L242`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._draw_chart()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._draw_chart()` possiede l'identificatore esatto `ui_tabs_dashboard_tab_dashboardtab_draw_chart`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._draw_chart()` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._draw_chart()` è `._draw_chart()`.
<!-- GRAPH_EDGE graph=codice index=915 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._draw_chart()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_draw_chart` all'ID `ui_tabs_dashboard_tab_py_any`. La relazione tecnica esplicita è la seguente: `._draw_chart()` fa riferimento a `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 915 che coinvolge il nodo canonico `._draw_chart()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 915 che coinvolge il nodo canonico `._draw_chart()` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 915 che coinvolge il nodo canonico `._draw_chart()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 915 che coinvolge il nodo canonico `._draw_chart()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 915 che coinvolge il nodo canonico `._draw_chart()` è `L242`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 915 che coinvolge il nodo canonico `._draw_chart()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 915 che coinvolge il nodo canonico `._draw_chart()` è `ast`. Nel grafo del codice RDG, l'arco 915 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_draw_chart` per la relazione che coinvolge il nodo canonico `._draw_chart()`. Nel grafo del codice RDG, l'arco 915 registra come `target` l'identificatore `ui_tabs_dashboard_tab_py_any` per la relazione che coinvolge il nodo canonico `._draw_chart()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 915 che coinvolge il nodo canonico `._draw_chart()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=918 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._draw_chart()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._chart_values()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_draw_chart` all'ID `ui_tabs_dashboard_tab_dashboardtab_chart_values`. La relazione tecnica esplicita è la seguente: `._draw_chart()` chiama `._chart_values()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 918 che coinvolge il nodo canonico `._draw_chart()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 918 che coinvolge il nodo canonico `._draw_chart()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 918 che coinvolge il nodo canonico `._draw_chart()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 918 che coinvolge il nodo canonico `._draw_chart()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 918 che coinvolge il nodo canonico `._draw_chart()` è `L246`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 918 che coinvolge il nodo canonico `._draw_chart()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 918 che coinvolge il nodo canonico `._draw_chart()` è `ast`. Nel grafo del codice RDG, l'arco 918 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_draw_chart` per la relazione che coinvolge il nodo canonico `._draw_chart()`. Nel grafo del codice RDG, l'arco 918 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_chart_values` per la relazione che coinvolge il nodo canonico `._draw_chart()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 918 che coinvolge il nodo canonico `._draw_chart()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=893 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._draw_chart()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_draw_chart`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._draw_chart()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 893 che coinvolge il nodo canonico `._draw_chart()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 893 che coinvolge il nodo canonico `._draw_chart()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 893 che coinvolge il nodo canonico `._draw_chart()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 893 che coinvolge il nodo canonico `._draw_chart()` è `L242`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 893 che coinvolge il nodo canonico `._draw_chart()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 893 che coinvolge il nodo canonico `._draw_chart()` è `ast`. Nel grafo del codice RDG, l'arco 893 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._draw_chart()`. Nel grafo del codice RDG, l'arco 893 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_draw_chart` per la relazione che coinvolge il nodo canonico `._draw_chart()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 893 che coinvolge il nodo canonico `._draw_chart()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=908 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._draw_chart()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.refresh_results()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_refresh_results` all'ID `ui_tabs_dashboard_tab_dashboardtab_draw_chart`. La relazione tecnica esplicita è la seguente: `.refresh_results()` chiama `._draw_chart()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 908 che coinvolge il nodo canonico `._draw_chart()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 908 che coinvolge il nodo canonico `._draw_chart()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 908 che coinvolge il nodo canonico `._draw_chart()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 908 che coinvolge il nodo canonico `._draw_chart()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 908 che coinvolge il nodo canonico `._draw_chart()` è `L203`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 908 che coinvolge il nodo canonico `._draw_chart()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 908 che coinvolge il nodo canonico `._draw_chart()` è `ast`. Nel grafo del codice RDG, l'arco 908 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `._draw_chart()`. Nel grafo del codice RDG, l'arco 908 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_draw_chart` per la relazione che coinvolge il nodo canonico `._draw_chart()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 908 che coinvolge il nodo canonico `._draw_chart()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=456 id=Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_draw_chart -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._draw_chart()` è `._draw_chart()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._draw_chart()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._draw_chart()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._draw_chart()` è `L242`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._draw_chart()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._draw_chart()` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._draw_chart()` è `._draw_chart()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._draw_chart()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._draw_chart()` è `ui_tabs_dashboard_tab_dashboardtab_draw_chart`. Nel grafo unificato, il nodo canonico `._draw_chart()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_draw_chart`.
<!-- GRAPH_EDGE graph=unificata index=918 role=outgoing -->
Nel grafo unificato, il nodo canonico `._draw_chart()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._chart_values()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_draw_chart` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_chart_values`. La relazione tecnica esplicita è la seguente: `._draw_chart()` chiama `._chart_values()`. Nel grafo unificato, il tipo dichiarato dell'arco 918 che coinvolge il nodo canonico `._draw_chart()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 918 che coinvolge il nodo canonico `._draw_chart()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 918 che coinvolge il nodo canonico `._draw_chart()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 918 che coinvolge il nodo canonico `._draw_chart()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 918 che coinvolge il nodo canonico `._draw_chart()` è `L246`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 918 che coinvolge il nodo canonico `._draw_chart()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 918 che coinvolge il nodo canonico `._draw_chart()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 918 che coinvolge il nodo canonico `._draw_chart()` è `1.0`. Nel grafo unificato, l'arco 918 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_draw_chart` per la relazione che coinvolge il nodo canonico `._draw_chart()`. Nel grafo unificato, l'arco 918 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_chart_values` per la relazione che coinvolge il nodo canonico `._draw_chart()`.
<!-- GRAPH_EDGE graph=unificata index=893 role=incoming -->
Nel grafo unificato, il nodo canonico `._draw_chart()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_draw_chart`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._draw_chart()`. Nel grafo unificato, il tipo dichiarato dell'arco 893 che coinvolge il nodo canonico `._draw_chart()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 893 che coinvolge il nodo canonico `._draw_chart()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 893 che coinvolge il nodo canonico `._draw_chart()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 893 che coinvolge il nodo canonico `._draw_chart()` è `L242`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 893 che coinvolge il nodo canonico `._draw_chart()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 893 che coinvolge il nodo canonico `._draw_chart()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 893 che coinvolge il nodo canonico `._draw_chart()` è `1.0`. Nel grafo unificato, l'arco 893 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._draw_chart()`. Nel grafo unificato, l'arco 893 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_draw_chart` per la relazione che coinvolge il nodo canonico `._draw_chart()`.
<!-- GRAPH_EDGE graph=unificata index=908 role=incoming -->
Nel grafo unificato, il nodo canonico `._draw_chart()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.refresh_results()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_draw_chart`. La relazione tecnica esplicita è la seguente: `.refresh_results()` chiama `._draw_chart()`. Nel grafo unificato, il tipo dichiarato dell'arco 908 che coinvolge il nodo canonico `._draw_chart()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 908 che coinvolge il nodo canonico `._draw_chart()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 908 che coinvolge il nodo canonico `._draw_chart()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 908 che coinvolge il nodo canonico `._draw_chart()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 908 che coinvolge il nodo canonico `._draw_chart()` è `L203`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 908 che coinvolge il nodo canonico `._draw_chart()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 908 che coinvolge il nodo canonico `._draw_chart()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 908 che coinvolge il nodo canonico `._draw_chart()` è `1.0`. Nel grafo unificato, l'arco 908 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `._draw_chart()`. Nel grafo unificato, l'arco 908 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_draw_chart` per la relazione che coinvolge il nodo canonico `._draw_chart()`.
<!-- GRAPH_EDGE graph=unificata index=915 role=incoming -->
Nel grafo unificato, il nodo canonico `._draw_chart()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_draw_chart`. La relazione tecnica esplicita è la seguente: `Any [ui_tabs_dashboard_tab_py_any]` fa riferimento a `._draw_chart()`. Nel grafo unificato, il tipo dichiarato dell'arco 915 che coinvolge il nodo canonico `._draw_chart()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 915 che coinvolge il nodo canonico `._draw_chart()` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 915 che coinvolge il nodo canonico `._draw_chart()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 915 che coinvolge il nodo canonico `._draw_chart()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 915 che coinvolge il nodo canonico `._draw_chart()` è `L242`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 915 che coinvolge il nodo canonico `._draw_chart()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 915 che coinvolge il nodo canonico `._draw_chart()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 915 che coinvolge il nodo canonico `._draw_chart()` è `1.0`. Nel grafo unificato, l'arco 915 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any` per la relazione che coinvolge il nodo canonico `._draw_chart()`. Nel grafo unificato, l'arco 915 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_draw_chart` per la relazione che coinvolge il nodo canonico `._draw_chart()`.

### ._populate_metrics()

<!-- GRAPH_NODE graph=codice index=453 id=ui_tabs_dashboard_tab_dashboardtab_populate_metrics -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._populate_metrics()` è `._populate_metrics()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._populate_metrics()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._populate_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._populate_metrics()` è `L206`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._populate_metrics()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._populate_metrics()` possiede l'identificatore esatto `ui_tabs_dashboard_tab_dashboardtab_populate_metrics`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._populate_metrics()` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._populate_metrics()` è `._populate_metrics()`.
<!-- GRAPH_EDGE graph=codice index=912 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._populate_metrics()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._set_metric()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_populate_metrics` all'ID `ui_tabs_dashboard_tab_dashboardtab_set_metric`. La relazione tecnica esplicita è la seguente: `._populate_metrics()` chiama `._set_metric()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 912 che coinvolge il nodo canonico `._populate_metrics()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 912 che coinvolge il nodo canonico `._populate_metrics()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 912 che coinvolge il nodo canonico `._populate_metrics()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 912 che coinvolge il nodo canonico `._populate_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 912 che coinvolge il nodo canonico `._populate_metrics()` è `L223`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 912 che coinvolge il nodo canonico `._populate_metrics()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 912 che coinvolge il nodo canonico `._populate_metrics()` è `ast`. Nel grafo del codice RDG, l'arco 912 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_populate_metrics` per la relazione che coinvolge il nodo canonico `._populate_metrics()`. Nel grafo del codice RDG, l'arco 912 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_set_metric` per la relazione che coinvolge il nodo canonico `._populate_metrics()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 912 che coinvolge il nodo canonico `._populate_metrics()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=913 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._populate_metrics()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_populate_metrics` all'ID `ui_tabs_dashboard_tab_py_any`. La relazione tecnica esplicita è la seguente: `._populate_metrics()` fa riferimento a `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 913 che coinvolge il nodo canonico `._populate_metrics()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 913 che coinvolge il nodo canonico `._populate_metrics()` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 913 che coinvolge il nodo canonico `._populate_metrics()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 913 che coinvolge il nodo canonico `._populate_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 913 che coinvolge il nodo canonico `._populate_metrics()` è `L206`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 913 che coinvolge il nodo canonico `._populate_metrics()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 913 che coinvolge il nodo canonico `._populate_metrics()` è `ast`. Nel grafo del codice RDG, l'arco 913 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_populate_metrics` per la relazione che coinvolge il nodo canonico `._populate_metrics()`. Nel grafo del codice RDG, l'arco 913 registra come `target` l'identificatore `ui_tabs_dashboard_tab_py_any` per la relazione che coinvolge il nodo canonico `._populate_metrics()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 913 che coinvolge il nodo canonico `._populate_metrics()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=895 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._populate_metrics()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_populate_metrics`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._populate_metrics()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 895 che coinvolge il nodo canonico `._populate_metrics()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 895 che coinvolge il nodo canonico `._populate_metrics()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 895 che coinvolge il nodo canonico `._populate_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 895 che coinvolge il nodo canonico `._populate_metrics()` è `L206`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 895 che coinvolge il nodo canonico `._populate_metrics()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 895 che coinvolge il nodo canonico `._populate_metrics()` è `ast`. Nel grafo del codice RDG, l'arco 895 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._populate_metrics()`. Nel grafo del codice RDG, l'arco 895 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_populate_metrics` per la relazione che coinvolge il nodo canonico `._populate_metrics()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 895 che coinvolge il nodo canonico `._populate_metrics()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=909 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._populate_metrics()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.refresh_results()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_refresh_results` all'ID `ui_tabs_dashboard_tab_dashboardtab_populate_metrics`. La relazione tecnica esplicita è la seguente: `.refresh_results()` chiama `._populate_metrics()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 909 che coinvolge il nodo canonico `._populate_metrics()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 909 che coinvolge il nodo canonico `._populate_metrics()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 909 che coinvolge il nodo canonico `._populate_metrics()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 909 che coinvolge il nodo canonico `._populate_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 909 che coinvolge il nodo canonico `._populate_metrics()` è `L202`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 909 che coinvolge il nodo canonico `._populate_metrics()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 909 che coinvolge il nodo canonico `._populate_metrics()` è `ast`. Nel grafo del codice RDG, l'arco 909 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `._populate_metrics()`. Nel grafo del codice RDG, l'arco 909 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_populate_metrics` per la relazione che coinvolge il nodo canonico `._populate_metrics()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 909 che coinvolge il nodo canonico `._populate_metrics()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=453 id=Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_populate_metrics -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._populate_metrics()` è `._populate_metrics()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._populate_metrics()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._populate_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._populate_metrics()` è `L206`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._populate_metrics()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._populate_metrics()` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._populate_metrics()` è `._populate_metrics()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._populate_metrics()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._populate_metrics()` è `ui_tabs_dashboard_tab_dashboardtab_populate_metrics`. Nel grafo unificato, il nodo canonico `._populate_metrics()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_populate_metrics`.
<!-- GRAPH_EDGE graph=unificata index=912 role=outgoing -->
Nel grafo unificato, il nodo canonico `._populate_metrics()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._set_metric()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_populate_metrics` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_set_metric`. La relazione tecnica esplicita è la seguente: `._populate_metrics()` chiama `._set_metric()`. Nel grafo unificato, il tipo dichiarato dell'arco 912 che coinvolge il nodo canonico `._populate_metrics()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 912 che coinvolge il nodo canonico `._populate_metrics()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 912 che coinvolge il nodo canonico `._populate_metrics()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 912 che coinvolge il nodo canonico `._populate_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 912 che coinvolge il nodo canonico `._populate_metrics()` è `L223`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 912 che coinvolge il nodo canonico `._populate_metrics()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 912 che coinvolge il nodo canonico `._populate_metrics()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 912 che coinvolge il nodo canonico `._populate_metrics()` è `1.0`. Nel grafo unificato, l'arco 912 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_populate_metrics` per la relazione che coinvolge il nodo canonico `._populate_metrics()`. Nel grafo unificato, l'arco 912 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_set_metric` per la relazione che coinvolge il nodo canonico `._populate_metrics()`.
<!-- GRAPH_EDGE graph=unificata index=913 role=outgoing -->
Nel grafo unificato, il nodo canonico `._populate_metrics()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_populate_metrics` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any`. La relazione tecnica esplicita è la seguente: `._populate_metrics()` fa riferimento a `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo unificato, il tipo dichiarato dell'arco 913 che coinvolge il nodo canonico `._populate_metrics()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 913 che coinvolge il nodo canonico `._populate_metrics()` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 913 che coinvolge il nodo canonico `._populate_metrics()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 913 che coinvolge il nodo canonico `._populate_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 913 che coinvolge il nodo canonico `._populate_metrics()` è `L206`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 913 che coinvolge il nodo canonico `._populate_metrics()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 913 che coinvolge il nodo canonico `._populate_metrics()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 913 che coinvolge il nodo canonico `._populate_metrics()` è `1.0`. Nel grafo unificato, l'arco 913 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_populate_metrics` per la relazione che coinvolge il nodo canonico `._populate_metrics()`. Nel grafo unificato, l'arco 913 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any` per la relazione che coinvolge il nodo canonico `._populate_metrics()`.
<!-- GRAPH_EDGE graph=unificata index=895 role=incoming -->
Nel grafo unificato, il nodo canonico `._populate_metrics()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_populate_metrics`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._populate_metrics()`. Nel grafo unificato, il tipo dichiarato dell'arco 895 che coinvolge il nodo canonico `._populate_metrics()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 895 che coinvolge il nodo canonico `._populate_metrics()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 895 che coinvolge il nodo canonico `._populate_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 895 che coinvolge il nodo canonico `._populate_metrics()` è `L206`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 895 che coinvolge il nodo canonico `._populate_metrics()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 895 che coinvolge il nodo canonico `._populate_metrics()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 895 che coinvolge il nodo canonico `._populate_metrics()` è `1.0`. Nel grafo unificato, l'arco 895 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._populate_metrics()`. Nel grafo unificato, l'arco 895 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_populate_metrics` per la relazione che coinvolge il nodo canonico `._populate_metrics()`.
<!-- GRAPH_EDGE graph=unificata index=909 role=incoming -->
Nel grafo unificato, il nodo canonico `._populate_metrics()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.refresh_results()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_populate_metrics`. La relazione tecnica esplicita è la seguente: `.refresh_results()` chiama `._populate_metrics()`. Nel grafo unificato, il tipo dichiarato dell'arco 909 che coinvolge il nodo canonico `._populate_metrics()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 909 che coinvolge il nodo canonico `._populate_metrics()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 909 che coinvolge il nodo canonico `._populate_metrics()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 909 che coinvolge il nodo canonico `._populate_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 909 che coinvolge il nodo canonico `._populate_metrics()` è `L202`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 909 che coinvolge il nodo canonico `._populate_metrics()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 909 che coinvolge il nodo canonico `._populate_metrics()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 909 che coinvolge il nodo canonico `._populate_metrics()` è `1.0`. Nel grafo unificato, l'arco 909 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `._populate_metrics()`. Nel grafo unificato, l'arco 909 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_populate_metrics` per la relazione che coinvolge il nodo canonico `._populate_metrics()`.

### ._read_metrics()

<!-- GRAPH_NODE graph=codice index=458 id=ui_tabs_dashboard_tab_dashboardtab_read_metrics -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._read_metrics()` è `._read_metrics()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._read_metrics()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._read_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._read_metrics()` è `L306`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._read_metrics()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._read_metrics()` possiede l'identificatore esatto `ui_tabs_dashboard_tab_dashboardtab_read_metrics`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._read_metrics()` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._read_metrics()` è `._read_metrics()`.
<!-- GRAPH_EDGE graph=codice index=916 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._read_metrics()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_read_metrics` all'ID `ui_tabs_dashboard_tab_py_any`. La relazione tecnica esplicita è la seguente: `._read_metrics()` fa riferimento a `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 916 che coinvolge il nodo canonico `._read_metrics()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 916 che coinvolge il nodo canonico `._read_metrics()` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 916 che coinvolge il nodo canonico `._read_metrics()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 916 che coinvolge il nodo canonico `._read_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 916 che coinvolge il nodo canonico `._read_metrics()` è `L306`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 916 che coinvolge il nodo canonico `._read_metrics()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 916 che coinvolge il nodo canonico `._read_metrics()` è `ast`. Nel grafo del codice RDG, l'arco 916 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_read_metrics` per la relazione che coinvolge il nodo canonico `._read_metrics()`. Nel grafo del codice RDG, l'arco 916 registra come `target` l'identificatore `ui_tabs_dashboard_tab_py_any` per la relazione che coinvolge il nodo canonico `._read_metrics()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 916 che coinvolge il nodo canonico `._read_metrics()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=920 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._read_metrics()` è la sorgente `source` della relazione `calls` verso il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_read_metrics` all'ID `ui_tabs_dashboard_tab_py_path`. La relazione tecnica esplicita è la seguente: `._read_metrics()` chiama `Path [ui_tabs_dashboard_tab_py_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 920 che coinvolge il nodo canonico `._read_metrics()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 920 che coinvolge il nodo canonico `._read_metrics()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 920 che coinvolge il nodo canonico `._read_metrics()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 920 che coinvolge il nodo canonico `._read_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 920 che coinvolge il nodo canonico `._read_metrics()` è `L310`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 920 che coinvolge il nodo canonico `._read_metrics()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 920 che coinvolge il nodo canonico `._read_metrics()` è `ast`. Nel grafo del codice RDG, l'arco 920 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_read_metrics` per la relazione che coinvolge il nodo canonico `._read_metrics()`. Nel grafo del codice RDG, l'arco 920 registra come `target` l'identificatore `ui_tabs_dashboard_tab_py_path` per la relazione che coinvolge il nodo canonico `._read_metrics()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 920 che coinvolge il nodo canonico `._read_metrics()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=921 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._read_metrics()` è la sorgente `source` della relazione `calls` verso il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_read_metrics` all'ID `ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `._read_metrics()` chiama `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 921 che coinvolge il nodo canonico `._read_metrics()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 921 che coinvolge il nodo canonico `._read_metrics()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 921 che coinvolge il nodo canonico `._read_metrics()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 921 che coinvolge il nodo canonico `._read_metrics()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 921 che coinvolge il nodo canonico `._read_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 921 che coinvolge il nodo canonico `._read_metrics()` è `L307`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 921 che coinvolge il nodo canonico `._read_metrics()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 921 che coinvolge il nodo canonico `._read_metrics()` è `ast`. Nel grafo del codice RDG, l'arco 921 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_read_metrics` per la relazione che coinvolge il nodo canonico `._read_metrics()`. Nel grafo del codice RDG, l'arco 921 registra come `target` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `._read_metrics()`.
<!-- GRAPH_EDGE graph=codice index=896 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._read_metrics()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_read_metrics`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._read_metrics()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 896 che coinvolge il nodo canonico `._read_metrics()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 896 che coinvolge il nodo canonico `._read_metrics()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 896 che coinvolge il nodo canonico `._read_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 896 che coinvolge il nodo canonico `._read_metrics()` è `L306`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 896 che coinvolge il nodo canonico `._read_metrics()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 896 che coinvolge il nodo canonico `._read_metrics()` è `ast`. Nel grafo del codice RDG, l'arco 896 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._read_metrics()`. Nel grafo del codice RDG, l'arco 896 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_read_metrics` per la relazione che coinvolge il nodo canonico `._read_metrics()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 896 che coinvolge il nodo canonico `._read_metrics()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=910 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._read_metrics()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.refresh_results()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_refresh_results` all'ID `ui_tabs_dashboard_tab_dashboardtab_read_metrics`. La relazione tecnica esplicita è la seguente: `.refresh_results()` chiama `._read_metrics()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 910 che coinvolge il nodo canonico `._read_metrics()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 910 che coinvolge il nodo canonico `._read_metrics()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 910 che coinvolge il nodo canonico `._read_metrics()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 910 che coinvolge il nodo canonico `._read_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 910 che coinvolge il nodo canonico `._read_metrics()` è `L201`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 910 che coinvolge il nodo canonico `._read_metrics()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 910 che coinvolge il nodo canonico `._read_metrics()` è `ast`. Nel grafo del codice RDG, l'arco 910 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `._read_metrics()`. Nel grafo del codice RDG, l'arco 910 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_read_metrics` per la relazione che coinvolge il nodo canonico `._read_metrics()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 910 che coinvolge il nodo canonico `._read_metrics()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=458 id=Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._read_metrics()` è `._read_metrics()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._read_metrics()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._read_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._read_metrics()` è `L306`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._read_metrics()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._read_metrics()` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._read_metrics()` è `._read_metrics()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._read_metrics()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._read_metrics()` è `ui_tabs_dashboard_tab_dashboardtab_read_metrics`. Nel grafo unificato, il nodo canonico `._read_metrics()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics`.
<!-- GRAPH_EDGE graph=unificata index=920 role=outgoing -->
Nel grafo unificato, il nodo canonico `._read_metrics()` è la sorgente `source` della relazione `calls` verso il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_path`. La relazione tecnica esplicita è la seguente: `._read_metrics()` chiama `Path [ui_tabs_dashboard_tab_py_path]`. Nel grafo unificato, il tipo dichiarato dell'arco 920 che coinvolge il nodo canonico `._read_metrics()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 920 che coinvolge il nodo canonico `._read_metrics()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 920 che coinvolge il nodo canonico `._read_metrics()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 920 che coinvolge il nodo canonico `._read_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 920 che coinvolge il nodo canonico `._read_metrics()` è `L310`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 920 che coinvolge il nodo canonico `._read_metrics()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 920 che coinvolge il nodo canonico `._read_metrics()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 920 che coinvolge il nodo canonico `._read_metrics()` è `1.0`. Nel grafo unificato, l'arco 920 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics` per la relazione che coinvolge il nodo canonico `._read_metrics()`. Nel grafo unificato, l'arco 920 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_path` per la relazione che coinvolge il nodo canonico `._read_metrics()`.
<!-- GRAPH_EDGE graph=unificata index=921 role=outgoing -->
Nel grafo unificato, il nodo canonico `._read_metrics()` è la sorgente `source` della relazione `calls` verso il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `._read_metrics()` chiama `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 921 che coinvolge il nodo canonico `._read_metrics()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 921 che coinvolge il nodo canonico `._read_metrics()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 921 che coinvolge il nodo canonico `._read_metrics()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 921 che coinvolge il nodo canonico `._read_metrics()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 921 che coinvolge il nodo canonico `._read_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 921 che coinvolge il nodo canonico `._read_metrics()` è `L307`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 921 che coinvolge il nodo canonico `._read_metrics()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 921 che coinvolge il nodo canonico `._read_metrics()` è `ast`. Nel grafo unificato, l'arco 921 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics` per la relazione che coinvolge il nodo canonico `._read_metrics()`. Nel grafo unificato, l'arco 921 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `._read_metrics()`.
<!-- GRAPH_EDGE graph=unificata index=896 role=incoming -->
Nel grafo unificato, il nodo canonico `._read_metrics()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._read_metrics()`. Nel grafo unificato, il tipo dichiarato dell'arco 896 che coinvolge il nodo canonico `._read_metrics()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 896 che coinvolge il nodo canonico `._read_metrics()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 896 che coinvolge il nodo canonico `._read_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 896 che coinvolge il nodo canonico `._read_metrics()` è `L306`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 896 che coinvolge il nodo canonico `._read_metrics()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 896 che coinvolge il nodo canonico `._read_metrics()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 896 che coinvolge il nodo canonico `._read_metrics()` è `1.0`. Nel grafo unificato, l'arco 896 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._read_metrics()`. Nel grafo unificato, l'arco 896 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics` per la relazione che coinvolge il nodo canonico `._read_metrics()`.
<!-- GRAPH_EDGE graph=unificata index=910 role=incoming -->
Nel grafo unificato, il nodo canonico `._read_metrics()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.refresh_results()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics`. La relazione tecnica esplicita è la seguente: `.refresh_results()` chiama `._read_metrics()`. Nel grafo unificato, il tipo dichiarato dell'arco 910 che coinvolge il nodo canonico `._read_metrics()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 910 che coinvolge il nodo canonico `._read_metrics()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 910 che coinvolge il nodo canonico `._read_metrics()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 910 che coinvolge il nodo canonico `._read_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 910 che coinvolge il nodo canonico `._read_metrics()` è `L201`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 910 che coinvolge il nodo canonico `._read_metrics()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 910 che coinvolge il nodo canonico `._read_metrics()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 910 che coinvolge il nodo canonico `._read_metrics()` è `1.0`. Nel grafo unificato, l'arco 910 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `._read_metrics()`. Nel grafo unificato, l'arco 910 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics` per la relazione che coinvolge il nodo canonico `._read_metrics()`.
<!-- GRAPH_EDGE graph=unificata index=916 role=incoming -->
Nel grafo unificato, il nodo canonico `._read_metrics()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics`. La relazione tecnica esplicita è la seguente: `Any [ui_tabs_dashboard_tab_py_any]` fa riferimento a `._read_metrics()`. Nel grafo unificato, il tipo dichiarato dell'arco 916 che coinvolge il nodo canonico `._read_metrics()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 916 che coinvolge il nodo canonico `._read_metrics()` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 916 che coinvolge il nodo canonico `._read_metrics()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 916 che coinvolge il nodo canonico `._read_metrics()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 916 che coinvolge il nodo canonico `._read_metrics()` è `L306`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 916 che coinvolge il nodo canonico `._read_metrics()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 916 che coinvolge il nodo canonico `._read_metrics()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 916 che coinvolge il nodo canonico `._read_metrics()` è `1.0`. Nel grafo unificato, l'arco 916 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any` per la relazione che coinvolge il nodo canonico `._read_metrics()`. Nel grafo unificato, l'arco 916 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics` per la relazione che coinvolge il nodo canonico `._read_metrics()`.

### ._render_report()

<!-- GRAPH_NODE graph=codice index=457 id=ui_tabs_dashboard_tab_dashboardtab_render_report -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._render_report()` è `._render_report()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._render_report()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._render_report()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._render_report()` è `L278`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._render_report()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._render_report()` possiede l'identificatore esatto `ui_tabs_dashboard_tab_dashboardtab_render_report`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._render_report()` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._render_report()` è `._render_report()`.
<!-- GRAPH_EDGE graph=codice index=919 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._render_report()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._report_path()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_render_report` all'ID `ui_tabs_dashboard_tab_dashboardtab_report_path`. La relazione tecnica esplicita è la seguente: `._render_report()` chiama `._report_path()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 919 che coinvolge il nodo canonico `._render_report()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 919 che coinvolge il nodo canonico `._render_report()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 919 che coinvolge il nodo canonico `._render_report()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 919 che coinvolge il nodo canonico `._render_report()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 919 che coinvolge il nodo canonico `._render_report()` è `L279`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 919 che coinvolge il nodo canonico `._render_report()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 919 che coinvolge il nodo canonico `._render_report()` è `ast`. Nel grafo del codice RDG, l'arco 919 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_render_report` per la relazione che coinvolge il nodo canonico `._render_report()`. Nel grafo del codice RDG, l'arco 919 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_report_path` per la relazione che coinvolge il nodo canonico `._render_report()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 919 che coinvolge il nodo canonico `._render_report()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=898 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._render_report()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_render_report`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._render_report()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 898 che coinvolge il nodo canonico `._render_report()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 898 che coinvolge il nodo canonico `._render_report()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 898 che coinvolge il nodo canonico `._render_report()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 898 che coinvolge il nodo canonico `._render_report()` è `L278`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 898 che coinvolge il nodo canonico `._render_report()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 898 che coinvolge il nodo canonico `._render_report()` è `ast`. Nel grafo del codice RDG, l'arco 898 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._render_report()`. Nel grafo del codice RDG, l'arco 898 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_render_report` per la relazione che coinvolge il nodo canonico `._render_report()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 898 che coinvolge il nodo canonico `._render_report()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=911 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._render_report()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.refresh_results()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_refresh_results` all'ID `ui_tabs_dashboard_tab_dashboardtab_render_report`. La relazione tecnica esplicita è la seguente: `.refresh_results()` chiama `._render_report()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 911 che coinvolge il nodo canonico `._render_report()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 911 che coinvolge il nodo canonico `._render_report()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 911 che coinvolge il nodo canonico `._render_report()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 911 che coinvolge il nodo canonico `._render_report()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 911 che coinvolge il nodo canonico `._render_report()` è `L204`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 911 che coinvolge il nodo canonico `._render_report()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 911 che coinvolge il nodo canonico `._render_report()` è `ast`. Nel grafo del codice RDG, l'arco 911 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `._render_report()`. Nel grafo del codice RDG, l'arco 911 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_render_report` per la relazione che coinvolge il nodo canonico `._render_report()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 911 che coinvolge il nodo canonico `._render_report()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=457 id=Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_render_report -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._render_report()` è `._render_report()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._render_report()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._render_report()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._render_report()` è `L278`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._render_report()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._render_report()` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._render_report()` è `._render_report()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._render_report()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._render_report()` è `ui_tabs_dashboard_tab_dashboardtab_render_report`. Nel grafo unificato, il nodo canonico `._render_report()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_render_report`.
<!-- GRAPH_EDGE graph=unificata index=919 role=outgoing -->
Nel grafo unificato, il nodo canonico `._render_report()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._report_path()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_render_report` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_report_path`. La relazione tecnica esplicita è la seguente: `._render_report()` chiama `._report_path()`. Nel grafo unificato, il tipo dichiarato dell'arco 919 che coinvolge il nodo canonico `._render_report()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 919 che coinvolge il nodo canonico `._render_report()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 919 che coinvolge il nodo canonico `._render_report()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 919 che coinvolge il nodo canonico `._render_report()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 919 che coinvolge il nodo canonico `._render_report()` è `L279`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 919 che coinvolge il nodo canonico `._render_report()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 919 che coinvolge il nodo canonico `._render_report()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 919 che coinvolge il nodo canonico `._render_report()` è `1.0`. Nel grafo unificato, l'arco 919 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_render_report` per la relazione che coinvolge il nodo canonico `._render_report()`. Nel grafo unificato, l'arco 919 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_report_path` per la relazione che coinvolge il nodo canonico `._render_report()`.
<!-- GRAPH_EDGE graph=unificata index=898 role=incoming -->
Nel grafo unificato, il nodo canonico `._render_report()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_render_report`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._render_report()`. Nel grafo unificato, il tipo dichiarato dell'arco 898 che coinvolge il nodo canonico `._render_report()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 898 che coinvolge il nodo canonico `._render_report()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 898 che coinvolge il nodo canonico `._render_report()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 898 che coinvolge il nodo canonico `._render_report()` è `L278`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 898 che coinvolge il nodo canonico `._render_report()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 898 che coinvolge il nodo canonico `._render_report()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 898 che coinvolge il nodo canonico `._render_report()` è `1.0`. Nel grafo unificato, l'arco 898 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._render_report()`. Nel grafo unificato, l'arco 898 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_render_report` per la relazione che coinvolge il nodo canonico `._render_report()`.
<!-- GRAPH_EDGE graph=unificata index=911 role=incoming -->
Nel grafo unificato, il nodo canonico `._render_report()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.refresh_results()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_render_report`. La relazione tecnica esplicita è la seguente: `.refresh_results()` chiama `._render_report()`. Nel grafo unificato, il tipo dichiarato dell'arco 911 che coinvolge il nodo canonico `._render_report()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 911 che coinvolge il nodo canonico `._render_report()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 911 che coinvolge il nodo canonico `._render_report()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 911 che coinvolge il nodo canonico `._render_report()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 911 che coinvolge il nodo canonico `._render_report()` è `L204`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 911 che coinvolge il nodo canonico `._render_report()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 911 che coinvolge il nodo canonico `._render_report()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 911 che coinvolge il nodo canonico `._render_report()` è `1.0`. Nel grafo unificato, l'arco 911 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `._render_report()`. Nel grafo unificato, l'arco 911 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_render_report` per la relazione che coinvolge il nodo canonico `._render_report()`.

### ._report_path()

<!-- GRAPH_NODE graph=codice index=459 id=ui_tabs_dashboard_tab_dashboardtab_report_path -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._report_path()` è `._report_path()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._report_path()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._report_path()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._report_path()` è `L323`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._report_path()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._report_path()` possiede l'identificatore esatto `ui_tabs_dashboard_tab_dashboardtab_report_path`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._report_path()` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._report_path()` è `._report_path()`.
<!-- GRAPH_EDGE graph=codice index=922 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._report_path()` è la sorgente `source` della relazione `references` verso il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_report_path` all'ID `ui_tabs_dashboard_tab_py_path`. La relazione tecnica esplicita è la seguente: `._report_path()` fa riferimento a `Path [ui_tabs_dashboard_tab_py_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 922 che coinvolge il nodo canonico `._report_path()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 922 che coinvolge il nodo canonico `._report_path()` è `return_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 922 che coinvolge il nodo canonico `._report_path()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 922 che coinvolge il nodo canonico `._report_path()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 922 che coinvolge il nodo canonico `._report_path()` è `L323`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 922 che coinvolge il nodo canonico `._report_path()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 922 che coinvolge il nodo canonico `._report_path()` è `ast`. Nel grafo del codice RDG, l'arco 922 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_report_path` per la relazione che coinvolge il nodo canonico `._report_path()`. Nel grafo del codice RDG, l'arco 922 registra come `target` l'identificatore `ui_tabs_dashboard_tab_py_path` per la relazione che coinvolge il nodo canonico `._report_path()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 922 che coinvolge il nodo canonico `._report_path()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=923 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._report_path()` è la sorgente `source` della relazione `calls` verso il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_report_path` all'ID `ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `._report_path()` chiama `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 923 che coinvolge il nodo canonico `._report_path()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 923 che coinvolge il nodo canonico `._report_path()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 923 che coinvolge il nodo canonico `._report_path()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 923 che coinvolge il nodo canonico `._report_path()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 923 che coinvolge il nodo canonico `._report_path()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 923 che coinvolge il nodo canonico `._report_path()` è `L324`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 923 che coinvolge il nodo canonico `._report_path()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 923 che coinvolge il nodo canonico `._report_path()` è `ast`. Nel grafo del codice RDG, l'arco 923 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_report_path` per la relazione che coinvolge il nodo canonico `._report_path()`. Nel grafo del codice RDG, l'arco 923 registra come `target` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `._report_path()`.
<!-- GRAPH_EDGE graph=codice index=899 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._report_path()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_report_path`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._report_path()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 899 che coinvolge il nodo canonico `._report_path()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 899 che coinvolge il nodo canonico `._report_path()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 899 che coinvolge il nodo canonico `._report_path()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 899 che coinvolge il nodo canonico `._report_path()` è `L323`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 899 che coinvolge il nodo canonico `._report_path()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 899 che coinvolge il nodo canonico `._report_path()` è `ast`. Nel grafo del codice RDG, l'arco 899 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._report_path()`. Nel grafo del codice RDG, l'arco 899 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_report_path` per la relazione che coinvolge il nodo canonico `._report_path()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 899 che coinvolge il nodo canonico `._report_path()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=919 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._report_path()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._render_report()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_render_report` all'ID `ui_tabs_dashboard_tab_dashboardtab_report_path`. La relazione tecnica esplicita è la seguente: `._render_report()` chiama `._report_path()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 919 che coinvolge il nodo canonico `._report_path()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 919 che coinvolge il nodo canonico `._report_path()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 919 che coinvolge il nodo canonico `._report_path()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 919 che coinvolge il nodo canonico `._report_path()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 919 che coinvolge il nodo canonico `._report_path()` è `L279`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 919 che coinvolge il nodo canonico `._report_path()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 919 che coinvolge il nodo canonico `._report_path()` è `ast`. Nel grafo del codice RDG, l'arco 919 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_render_report` per la relazione che coinvolge il nodo canonico `._report_path()`. Nel grafo del codice RDG, l'arco 919 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_report_path` per la relazione che coinvolge il nodo canonico `._report_path()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 919 che coinvolge il nodo canonico `._report_path()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=459 id=Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_report_path -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._report_path()` è `._report_path()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._report_path()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._report_path()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._report_path()` è `L323`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._report_path()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._report_path()` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._report_path()` è `._report_path()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._report_path()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._report_path()` è `ui_tabs_dashboard_tab_dashboardtab_report_path`. Nel grafo unificato, il nodo canonico `._report_path()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_report_path`.
<!-- GRAPH_EDGE graph=unificata index=922 role=outgoing -->
Nel grafo unificato, il nodo canonico `._report_path()` è la sorgente `source` della relazione `references` verso il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_report_path` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_path`. La relazione tecnica esplicita è la seguente: `._report_path()` fa riferimento a `Path [ui_tabs_dashboard_tab_py_path]`. Nel grafo unificato, il tipo dichiarato dell'arco 922 che coinvolge il nodo canonico `._report_path()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 922 che coinvolge il nodo canonico `._report_path()` è `return_type`. Nel grafo unificato, la confidenza categoriale dell'arco 922 che coinvolge il nodo canonico `._report_path()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 922 che coinvolge il nodo canonico `._report_path()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 922 che coinvolge il nodo canonico `._report_path()` è `L323`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 922 che coinvolge il nodo canonico `._report_path()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 922 che coinvolge il nodo canonico `._report_path()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 922 che coinvolge il nodo canonico `._report_path()` è `1.0`. Nel grafo unificato, l'arco 922 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_report_path` per la relazione che coinvolge il nodo canonico `._report_path()`. Nel grafo unificato, l'arco 922 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_path` per la relazione che coinvolge il nodo canonico `._report_path()`.
<!-- GRAPH_EDGE graph=unificata index=923 role=outgoing -->
Nel grafo unificato, il nodo canonico `._report_path()` è la sorgente `source` della relazione `calls` verso il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_report_path` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `._report_path()` chiama `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 923 che coinvolge il nodo canonico `._report_path()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 923 che coinvolge il nodo canonico `._report_path()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 923 che coinvolge il nodo canonico `._report_path()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 923 che coinvolge il nodo canonico `._report_path()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 923 che coinvolge il nodo canonico `._report_path()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 923 che coinvolge il nodo canonico `._report_path()` è `L324`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 923 che coinvolge il nodo canonico `._report_path()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 923 che coinvolge il nodo canonico `._report_path()` è `ast`. Nel grafo unificato, l'arco 923 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_report_path` per la relazione che coinvolge il nodo canonico `._report_path()`. Nel grafo unificato, l'arco 923 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `._report_path()`.
<!-- GRAPH_EDGE graph=unificata index=899 role=incoming -->
Nel grafo unificato, il nodo canonico `._report_path()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_report_path`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._report_path()`. Nel grafo unificato, il tipo dichiarato dell'arco 899 che coinvolge il nodo canonico `._report_path()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 899 che coinvolge il nodo canonico `._report_path()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 899 che coinvolge il nodo canonico `._report_path()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 899 che coinvolge il nodo canonico `._report_path()` è `L323`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 899 che coinvolge il nodo canonico `._report_path()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 899 che coinvolge il nodo canonico `._report_path()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 899 che coinvolge il nodo canonico `._report_path()` è `1.0`. Nel grafo unificato, l'arco 899 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._report_path()`. Nel grafo unificato, l'arco 899 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_report_path` per la relazione che coinvolge il nodo canonico `._report_path()`.
<!-- GRAPH_EDGE graph=unificata index=919 role=incoming -->
Nel grafo unificato, il nodo canonico `._report_path()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._render_report()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_render_report` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_report_path`. La relazione tecnica esplicita è la seguente: `._render_report()` chiama `._report_path()`. Nel grafo unificato, il tipo dichiarato dell'arco 919 che coinvolge il nodo canonico `._report_path()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 919 che coinvolge il nodo canonico `._report_path()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 919 che coinvolge il nodo canonico `._report_path()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 919 che coinvolge il nodo canonico `._report_path()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 919 che coinvolge il nodo canonico `._report_path()` è `L279`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 919 che coinvolge il nodo canonico `._report_path()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 919 che coinvolge il nodo canonico `._report_path()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 919 che coinvolge il nodo canonico `._report_path()` è `1.0`. Nel grafo unificato, l'arco 919 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_render_report` per la relazione che coinvolge il nodo canonico `._report_path()`. Nel grafo unificato, l'arco 919 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_report_path` per la relazione che coinvolge il nodo canonico `._report_path()`.

### ._set_metric()

<!-- GRAPH_NODE graph=codice index=455 id=ui_tabs_dashboard_tab_dashboardtab_set_metric -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._set_metric()` è `._set_metric()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._set_metric()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._set_metric()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._set_metric()` è `L229`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._set_metric()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._set_metric()` possiede l'identificatore esatto `ui_tabs_dashboard_tab_dashboardtab_set_metric`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._set_metric()` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._set_metric()` è `._set_metric()`.
<!-- GRAPH_EDGE graph=codice index=917 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._set_metric()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_set_metric` all'ID `ui_tabs_dashboard_tab_py_any`. La relazione tecnica esplicita è la seguente: `._set_metric()` fa riferimento a `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 917 che coinvolge il nodo canonico `._set_metric()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 917 che coinvolge il nodo canonico `._set_metric()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 917 che coinvolge il nodo canonico `._set_metric()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 917 che coinvolge il nodo canonico `._set_metric()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 917 che coinvolge il nodo canonico `._set_metric()` è `L229`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 917 che coinvolge il nodo canonico `._set_metric()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 917 che coinvolge il nodo canonico `._set_metric()` è `ast`. Nel grafo del codice RDG, l'arco 917 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_set_metric` per la relazione che coinvolge il nodo canonico `._set_metric()`. Nel grafo del codice RDG, l'arco 917 registra come `target` l'identificatore `ui_tabs_dashboard_tab_py_any` per la relazione che coinvolge il nodo canonico `._set_metric()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 917 che coinvolge il nodo canonico `._set_metric()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=900 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._set_metric()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_set_metric`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._set_metric()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 900 che coinvolge il nodo canonico `._set_metric()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 900 che coinvolge il nodo canonico `._set_metric()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 900 che coinvolge il nodo canonico `._set_metric()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 900 che coinvolge il nodo canonico `._set_metric()` è `L229`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 900 che coinvolge il nodo canonico `._set_metric()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 900 che coinvolge il nodo canonico `._set_metric()` è `ast`. Nel grafo del codice RDG, l'arco 900 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._set_metric()`. Nel grafo del codice RDG, l'arco 900 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_set_metric` per la relazione che coinvolge il nodo canonico `._set_metric()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 900 che coinvolge il nodo canonico `._set_metric()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=912 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._set_metric()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._populate_metrics()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_populate_metrics` all'ID `ui_tabs_dashboard_tab_dashboardtab_set_metric`. La relazione tecnica esplicita è la seguente: `._populate_metrics()` chiama `._set_metric()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 912 che coinvolge il nodo canonico `._set_metric()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 912 che coinvolge il nodo canonico `._set_metric()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 912 che coinvolge il nodo canonico `._set_metric()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 912 che coinvolge il nodo canonico `._set_metric()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 912 che coinvolge il nodo canonico `._set_metric()` è `L223`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 912 che coinvolge il nodo canonico `._set_metric()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 912 che coinvolge il nodo canonico `._set_metric()` è `ast`. Nel grafo del codice RDG, l'arco 912 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_populate_metrics` per la relazione che coinvolge il nodo canonico `._set_metric()`. Nel grafo del codice RDG, l'arco 912 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_set_metric` per la relazione che coinvolge il nodo canonico `._set_metric()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 912 che coinvolge il nodo canonico `._set_metric()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=455 id=Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_set_metric -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._set_metric()` è `._set_metric()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._set_metric()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._set_metric()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._set_metric()` è `L229`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._set_metric()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._set_metric()` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._set_metric()` è `._set_metric()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._set_metric()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._set_metric()` è `ui_tabs_dashboard_tab_dashboardtab_set_metric`. Nel grafo unificato, il nodo canonico `._set_metric()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_set_metric`.
Nel grafo unificato, il nodo canonico `._set_metric()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=900 role=incoming -->
Nel grafo unificato, il nodo canonico `._set_metric()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_set_metric`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._set_metric()`. Nel grafo unificato, il tipo dichiarato dell'arco 900 che coinvolge il nodo canonico `._set_metric()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 900 che coinvolge il nodo canonico `._set_metric()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 900 che coinvolge il nodo canonico `._set_metric()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 900 che coinvolge il nodo canonico `._set_metric()` è `L229`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 900 che coinvolge il nodo canonico `._set_metric()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 900 che coinvolge il nodo canonico `._set_metric()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 900 che coinvolge il nodo canonico `._set_metric()` è `1.0`. Nel grafo unificato, l'arco 900 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `._set_metric()`. Nel grafo unificato, l'arco 900 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_set_metric` per la relazione che coinvolge il nodo canonico `._set_metric()`.
<!-- GRAPH_EDGE graph=unificata index=912 role=incoming -->
Nel grafo unificato, il nodo canonico `._set_metric()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._populate_metrics()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_populate_metrics` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_set_metric`. La relazione tecnica esplicita è la seguente: `._populate_metrics()` chiama `._set_metric()`. Nel grafo unificato, il tipo dichiarato dell'arco 912 che coinvolge il nodo canonico `._set_metric()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 912 che coinvolge il nodo canonico `._set_metric()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 912 che coinvolge il nodo canonico `._set_metric()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 912 che coinvolge il nodo canonico `._set_metric()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 912 che coinvolge il nodo canonico `._set_metric()` è `L223`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 912 che coinvolge il nodo canonico `._set_metric()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 912 che coinvolge il nodo canonico `._set_metric()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 912 che coinvolge il nodo canonico `._set_metric()` è `1.0`. Nel grafo unificato, l'arco 912 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_populate_metrics` per la relazione che coinvolge il nodo canonico `._set_metric()`. Nel grafo unificato, l'arco 912 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_set_metric` per la relazione che coinvolge il nodo canonico `._set_metric()`.
<!-- GRAPH_EDGE graph=unificata index=917 role=incoming -->
Nel grafo unificato, il nodo canonico `._set_metric()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_set_metric`. La relazione tecnica esplicita è la seguente: `Any [ui_tabs_dashboard_tab_py_any]` fa riferimento a `._set_metric()`. Nel grafo unificato, il tipo dichiarato dell'arco 917 che coinvolge il nodo canonico `._set_metric()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 917 che coinvolge il nodo canonico `._set_metric()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 917 che coinvolge il nodo canonico `._set_metric()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 917 che coinvolge il nodo canonico `._set_metric()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 917 che coinvolge il nodo canonico `._set_metric()` è `L229`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 917 che coinvolge il nodo canonico `._set_metric()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 917 che coinvolge il nodo canonico `._set_metric()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 917 che coinvolge il nodo canonico `._set_metric()` è `1.0`. Nel grafo unificato, l'arco 917 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any` per la relazione che coinvolge il nodo canonico `._set_metric()`. Nel grafo unificato, l'arco 917 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_set_metric` per la relazione che coinvolge il nodo canonico `._set_metric()`.

### ._style_segmented_tabs()

<!-- GRAPH_NODE graph=codice index=418 id=ui_app_rdgapp_style_segmented_tabs -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._style_segmented_tabs()` è `._style_segmented_tabs()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._style_segmented_tabs()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._style_segmented_tabs()` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._style_segmented_tabs()` è `L126`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._style_segmented_tabs()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._style_segmented_tabs()` possiede l'identificatore esatto `ui_app_rdgapp_style_segmented_tabs`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._style_segmented_tabs()` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._style_segmented_tabs()` è `._style_segmented_tabs()`.
Nel grafo del codice RDG, il nodo canonico `._style_segmented_tabs()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=835 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._style_segmented_tabs()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RDGApp`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp` all'ID `ui_app_rdgapp_style_segmented_tabs`. La relazione tecnica esplicita è la seguente: `RDGApp` registra come proprio metodo `._style_segmented_tabs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 835 che coinvolge il nodo canonico `._style_segmented_tabs()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 835 che coinvolge il nodo canonico `._style_segmented_tabs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 835 che coinvolge il nodo canonico `._style_segmented_tabs()` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 835 che coinvolge il nodo canonico `._style_segmented_tabs()` è `L126`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 835 che coinvolge il nodo canonico `._style_segmented_tabs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 835 che coinvolge il nodo canonico `._style_segmented_tabs()` è `ast`. Nel grafo del codice RDG, l'arco 835 registra come `source` l'identificatore `ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `._style_segmented_tabs()`. Nel grafo del codice RDG, l'arco 835 registra come `target` l'identificatore `ui_app_rdgapp_style_segmented_tabs` per la relazione che coinvolge il nodo canonico `._style_segmented_tabs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 835 che coinvolge il nodo canonico `._style_segmented_tabs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=841 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._style_segmented_tabs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build_tabs()`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp_build_tabs` all'ID `ui_app_rdgapp_style_segmented_tabs`. La relazione tecnica esplicita è la seguente: `._build_tabs()` chiama `._style_segmented_tabs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 841 che coinvolge il nodo canonico `._style_segmented_tabs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 841 che coinvolge il nodo canonico `._style_segmented_tabs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 841 che coinvolge il nodo canonico `._style_segmented_tabs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 841 che coinvolge il nodo canonico `._style_segmented_tabs()` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 841 che coinvolge il nodo canonico `._style_segmented_tabs()` è `L124`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 841 che coinvolge il nodo canonico `._style_segmented_tabs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 841 che coinvolge il nodo canonico `._style_segmented_tabs()` è `ast`. Nel grafo del codice RDG, l'arco 841 registra come `source` l'identificatore `ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `._style_segmented_tabs()`. Nel grafo del codice RDG, l'arco 841 registra come `target` l'identificatore `ui_app_rdgapp_style_segmented_tabs` per la relazione che coinvolge il nodo canonico `._style_segmented_tabs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 841 che coinvolge il nodo canonico `._style_segmented_tabs()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=418 id=Request-Dataset-Generator::ui_app_rdgapp_style_segmented_tabs -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._style_segmented_tabs()` è `._style_segmented_tabs()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._style_segmented_tabs()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._style_segmented_tabs()` è `ui/app.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._style_segmented_tabs()` è `L126`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._style_segmented_tabs()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._style_segmented_tabs()` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._style_segmented_tabs()` è `._style_segmented_tabs()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._style_segmented_tabs()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._style_segmented_tabs()` è `ui_app_rdgapp_style_segmented_tabs`. Nel grafo unificato, il nodo canonico `._style_segmented_tabs()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_app_rdgapp_style_segmented_tabs`.
Nel grafo unificato, il nodo canonico `._style_segmented_tabs()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=835 role=incoming -->
Nel grafo unificato, il nodo canonico `._style_segmented_tabs()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RDGApp`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp` all'ID `Request-Dataset-Generator::ui_app_rdgapp_style_segmented_tabs`. La relazione tecnica esplicita è la seguente: `RDGApp` registra come proprio metodo `._style_segmented_tabs()`. Nel grafo unificato, il tipo dichiarato dell'arco 835 che coinvolge il nodo canonico `._style_segmented_tabs()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 835 che coinvolge il nodo canonico `._style_segmented_tabs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 835 che coinvolge il nodo canonico `._style_segmented_tabs()` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 835 che coinvolge il nodo canonico `._style_segmented_tabs()` è `L126`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 835 che coinvolge il nodo canonico `._style_segmented_tabs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 835 che coinvolge il nodo canonico `._style_segmented_tabs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 835 che coinvolge il nodo canonico `._style_segmented_tabs()` è `1.0`. Nel grafo unificato, l'arco 835 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `._style_segmented_tabs()`. Nel grafo unificato, l'arco 835 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_style_segmented_tabs` per la relazione che coinvolge il nodo canonico `._style_segmented_tabs()`.
<!-- GRAPH_EDGE graph=unificata index=841 role=incoming -->
Nel grafo unificato, il nodo canonico `._style_segmented_tabs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build_tabs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp_build_tabs` all'ID `Request-Dataset-Generator::ui_app_rdgapp_style_segmented_tabs`. La relazione tecnica esplicita è la seguente: `._build_tabs()` chiama `._style_segmented_tabs()`. Nel grafo unificato, il tipo dichiarato dell'arco 841 che coinvolge il nodo canonico `._style_segmented_tabs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 841 che coinvolge il nodo canonico `._style_segmented_tabs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 841 che coinvolge il nodo canonico `._style_segmented_tabs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 841 che coinvolge il nodo canonico `._style_segmented_tabs()` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 841 che coinvolge il nodo canonico `._style_segmented_tabs()` è `L124`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 841 che coinvolge il nodo canonico `._style_segmented_tabs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 841 che coinvolge il nodo canonico `._style_segmented_tabs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 841 che coinvolge il nodo canonico `._style_segmented_tabs()` è `1.0`. Nel grafo unificato, l'arco 841 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `._style_segmented_tabs()`. Nel grafo unificato, l'arco 841 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_style_segmented_tabs` per la relazione che coinvolge il nodo canonico `._style_segmented_tabs()`.

### .refresh_results()

<!-- GRAPH_NODE graph=codice index=452 id=ui_tabs_dashboard_tab_dashboardtab_refresh_results -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.refresh_results()` è `.refresh_results()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.refresh_results()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.refresh_results()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.refresh_results()` è `L200`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.refresh_results()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.refresh_results()` possiede l'identificatore esatto `ui_tabs_dashboard_tab_dashboardtab_refresh_results`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.refresh_results()` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.refresh_results()` è `.refresh_results()`.
<!-- GRAPH_EDGE graph=codice index=908 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.refresh_results()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._draw_chart()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_refresh_results` all'ID `ui_tabs_dashboard_tab_dashboardtab_draw_chart`. La relazione tecnica esplicita è la seguente: `.refresh_results()` chiama `._draw_chart()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 908 che coinvolge il nodo canonico `.refresh_results()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 908 che coinvolge il nodo canonico `.refresh_results()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 908 che coinvolge il nodo canonico `.refresh_results()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 908 che coinvolge il nodo canonico `.refresh_results()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 908 che coinvolge il nodo canonico `.refresh_results()` è `L203`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 908 che coinvolge il nodo canonico `.refresh_results()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 908 che coinvolge il nodo canonico `.refresh_results()` è `ast`. Nel grafo del codice RDG, l'arco 908 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `.refresh_results()`. Nel grafo del codice RDG, l'arco 908 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_draw_chart` per la relazione che coinvolge il nodo canonico `.refresh_results()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 908 che coinvolge il nodo canonico `.refresh_results()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=909 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.refresh_results()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._populate_metrics()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_refresh_results` all'ID `ui_tabs_dashboard_tab_dashboardtab_populate_metrics`. La relazione tecnica esplicita è la seguente: `.refresh_results()` chiama `._populate_metrics()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 909 che coinvolge il nodo canonico `.refresh_results()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 909 che coinvolge il nodo canonico `.refresh_results()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 909 che coinvolge il nodo canonico `.refresh_results()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 909 che coinvolge il nodo canonico `.refresh_results()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 909 che coinvolge il nodo canonico `.refresh_results()` è `L202`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 909 che coinvolge il nodo canonico `.refresh_results()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 909 che coinvolge il nodo canonico `.refresh_results()` è `ast`. Nel grafo del codice RDG, l'arco 909 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `.refresh_results()`. Nel grafo del codice RDG, l'arco 909 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_populate_metrics` per la relazione che coinvolge il nodo canonico `.refresh_results()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 909 che coinvolge il nodo canonico `.refresh_results()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=910 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.refresh_results()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._read_metrics()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_refresh_results` all'ID `ui_tabs_dashboard_tab_dashboardtab_read_metrics`. La relazione tecnica esplicita è la seguente: `.refresh_results()` chiama `._read_metrics()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 910 che coinvolge il nodo canonico `.refresh_results()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 910 che coinvolge il nodo canonico `.refresh_results()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 910 che coinvolge il nodo canonico `.refresh_results()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 910 che coinvolge il nodo canonico `.refresh_results()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 910 che coinvolge il nodo canonico `.refresh_results()` è `L201`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 910 che coinvolge il nodo canonico `.refresh_results()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 910 che coinvolge il nodo canonico `.refresh_results()` è `ast`. Nel grafo del codice RDG, l'arco 910 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `.refresh_results()`. Nel grafo del codice RDG, l'arco 910 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_read_metrics` per la relazione che coinvolge il nodo canonico `.refresh_results()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 910 che coinvolge il nodo canonico `.refresh_results()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=911 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.refresh_results()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._render_report()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_refresh_results` all'ID `ui_tabs_dashboard_tab_dashboardtab_render_report`. La relazione tecnica esplicita è la seguente: `.refresh_results()` chiama `._render_report()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 911 che coinvolge il nodo canonico `.refresh_results()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 911 che coinvolge il nodo canonico `.refresh_results()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 911 che coinvolge il nodo canonico `.refresh_results()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 911 che coinvolge il nodo canonico `.refresh_results()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 911 che coinvolge il nodo canonico `.refresh_results()` è `L204`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 911 che coinvolge il nodo canonico `.refresh_results()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 911 che coinvolge il nodo canonico `.refresh_results()` è `ast`. Nel grafo del codice RDG, l'arco 911 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `.refresh_results()`. Nel grafo del codice RDG, l'arco 911 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_render_report` per la relazione che coinvolge il nodo canonico `.refresh_results()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 911 che coinvolge il nodo canonico `.refresh_results()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=897 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.refresh_results()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_refresh_results`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `.refresh_results()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 897 che coinvolge il nodo canonico `.refresh_results()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 897 che coinvolge il nodo canonico `.refresh_results()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 897 che coinvolge il nodo canonico `.refresh_results()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 897 che coinvolge il nodo canonico `.refresh_results()` è `L200`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 897 che coinvolge il nodo canonico `.refresh_results()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 897 che coinvolge il nodo canonico `.refresh_results()` è `ast`. Nel grafo del codice RDG, l'arco 897 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `.refresh_results()`. Nel grafo del codice RDG, l'arco 897 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `.refresh_results()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 897 che coinvolge il nodo canonico `.refresh_results()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=903 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.refresh_results()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_init` all'ID `ui_tabs_dashboard_tab_dashboardtab_refresh_results`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` chiama `.refresh_results()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 903 che coinvolge il nodo canonico `.refresh_results()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 903 che coinvolge il nodo canonico `.refresh_results()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 903 che coinvolge il nodo canonico `.refresh_results()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 903 che coinvolge il nodo canonico `.refresh_results()` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 903 che coinvolge il nodo canonico `.refresh_results()` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 903 che coinvolge il nodo canonico `.refresh_results()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 903 che coinvolge il nodo canonico `.refresh_results()` è `ast`. Nel grafo del codice RDG, l'arco 903 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_init` per la relazione che coinvolge il nodo canonico `.refresh_results()`. Nel grafo del codice RDG, l'arco 903 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `.refresh_results()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 903 che coinvolge il nodo canonico `.refresh_results()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=452 id=Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.refresh_results()` è `.refresh_results()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.refresh_results()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.refresh_results()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.refresh_results()` è `L200`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.refresh_results()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.refresh_results()` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.refresh_results()` è `.refresh_results()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.refresh_results()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.refresh_results()` è `ui_tabs_dashboard_tab_dashboardtab_refresh_results`. Nel grafo unificato, il nodo canonico `.refresh_results()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results`.
<!-- GRAPH_EDGE graph=unificata index=908 role=outgoing -->
Nel grafo unificato, il nodo canonico `.refresh_results()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._draw_chart()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_draw_chart`. La relazione tecnica esplicita è la seguente: `.refresh_results()` chiama `._draw_chart()`. Nel grafo unificato, il tipo dichiarato dell'arco 908 che coinvolge il nodo canonico `.refresh_results()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 908 che coinvolge il nodo canonico `.refresh_results()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 908 che coinvolge il nodo canonico `.refresh_results()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 908 che coinvolge il nodo canonico `.refresh_results()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 908 che coinvolge il nodo canonico `.refresh_results()` è `L203`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 908 che coinvolge il nodo canonico `.refresh_results()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 908 che coinvolge il nodo canonico `.refresh_results()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 908 che coinvolge il nodo canonico `.refresh_results()` è `1.0`. Nel grafo unificato, l'arco 908 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `.refresh_results()`. Nel grafo unificato, l'arco 908 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_draw_chart` per la relazione che coinvolge il nodo canonico `.refresh_results()`.
<!-- GRAPH_EDGE graph=unificata index=909 role=outgoing -->
Nel grafo unificato, il nodo canonico `.refresh_results()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._populate_metrics()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_populate_metrics`. La relazione tecnica esplicita è la seguente: `.refresh_results()` chiama `._populate_metrics()`. Nel grafo unificato, il tipo dichiarato dell'arco 909 che coinvolge il nodo canonico `.refresh_results()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 909 che coinvolge il nodo canonico `.refresh_results()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 909 che coinvolge il nodo canonico `.refresh_results()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 909 che coinvolge il nodo canonico `.refresh_results()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 909 che coinvolge il nodo canonico `.refresh_results()` è `L202`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 909 che coinvolge il nodo canonico `.refresh_results()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 909 che coinvolge il nodo canonico `.refresh_results()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 909 che coinvolge il nodo canonico `.refresh_results()` è `1.0`. Nel grafo unificato, l'arco 909 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `.refresh_results()`. Nel grafo unificato, l'arco 909 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_populate_metrics` per la relazione che coinvolge il nodo canonico `.refresh_results()`.
<!-- GRAPH_EDGE graph=unificata index=910 role=outgoing -->
Nel grafo unificato, il nodo canonico `.refresh_results()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._read_metrics()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics`. La relazione tecnica esplicita è la seguente: `.refresh_results()` chiama `._read_metrics()`. Nel grafo unificato, il tipo dichiarato dell'arco 910 che coinvolge il nodo canonico `.refresh_results()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 910 che coinvolge il nodo canonico `.refresh_results()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 910 che coinvolge il nodo canonico `.refresh_results()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 910 che coinvolge il nodo canonico `.refresh_results()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 910 che coinvolge il nodo canonico `.refresh_results()` è `L201`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 910 che coinvolge il nodo canonico `.refresh_results()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 910 che coinvolge il nodo canonico `.refresh_results()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 910 che coinvolge il nodo canonico `.refresh_results()` è `1.0`. Nel grafo unificato, l'arco 910 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `.refresh_results()`. Nel grafo unificato, l'arco 910 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics` per la relazione che coinvolge il nodo canonico `.refresh_results()`.
<!-- GRAPH_EDGE graph=unificata index=911 role=outgoing -->
Nel grafo unificato, il nodo canonico `.refresh_results()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._render_report()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_render_report`. La relazione tecnica esplicita è la seguente: `.refresh_results()` chiama `._render_report()`. Nel grafo unificato, il tipo dichiarato dell'arco 911 che coinvolge il nodo canonico `.refresh_results()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 911 che coinvolge il nodo canonico `.refresh_results()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 911 che coinvolge il nodo canonico `.refresh_results()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 911 che coinvolge il nodo canonico `.refresh_results()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 911 che coinvolge il nodo canonico `.refresh_results()` è `L204`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 911 che coinvolge il nodo canonico `.refresh_results()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 911 che coinvolge il nodo canonico `.refresh_results()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 911 che coinvolge il nodo canonico `.refresh_results()` è `1.0`. Nel grafo unificato, l'arco 911 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `.refresh_results()`. Nel grafo unificato, l'arco 911 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_render_report` per la relazione che coinvolge il nodo canonico `.refresh_results()`.
<!-- GRAPH_EDGE graph=unificata index=897 role=incoming -->
Nel grafo unificato, il nodo canonico `.refresh_results()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `.refresh_results()`. Nel grafo unificato, il tipo dichiarato dell'arco 897 che coinvolge il nodo canonico `.refresh_results()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 897 che coinvolge il nodo canonico `.refresh_results()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 897 che coinvolge il nodo canonico `.refresh_results()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 897 che coinvolge il nodo canonico `.refresh_results()` è `L200`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 897 che coinvolge il nodo canonico `.refresh_results()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 897 che coinvolge il nodo canonico `.refresh_results()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 897 che coinvolge il nodo canonico `.refresh_results()` è `1.0`. Nel grafo unificato, l'arco 897 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `.refresh_results()`. Nel grafo unificato, l'arco 897 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `.refresh_results()`.
<!-- GRAPH_EDGE graph=unificata index=903 role=incoming -->
Nel grafo unificato, il nodo canonico `.refresh_results()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_init` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]` chiama `.refresh_results()`. Nel grafo unificato, il tipo dichiarato dell'arco 903 che coinvolge il nodo canonico `.refresh_results()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 903 che coinvolge il nodo canonico `.refresh_results()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 903 che coinvolge il nodo canonico `.refresh_results()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 903 che coinvolge il nodo canonico `.refresh_results()` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 903 che coinvolge il nodo canonico `.refresh_results()` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 903 che coinvolge il nodo canonico `.refresh_results()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 903 che coinvolge il nodo canonico `.refresh_results()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 903 che coinvolge il nodo canonico `.refresh_results()` è `1.0`. Nel grafo unificato, l'arco 903 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_init` per la relazione che coinvolge il nodo canonico `.refresh_results()`. Nel grafo unificato, l'arco 903 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `.refresh_results()`.

### _defaults()

<!-- GRAPH_NODE graph=codice index=500 id=ui_ui_config_bridge_defaults -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_defaults()` è `_defaults()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_defaults()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_defaults()` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_defaults()` è `L42`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_defaults()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_defaults()` possiede l'identificatore esatto `ui_ui_config_bridge_defaults`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_defaults()` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_defaults()` è `_defaults()`.
<!-- GRAPH_EDGE graph=codice index=995 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_defaults()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [ui_ui_config_bridge_py_any]`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge_defaults` all'ID `ui_ui_config_bridge_py_any`. La relazione tecnica esplicita è la seguente: `_defaults()` fa riferimento a `Any [ui_ui_config_bridge_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 995 che coinvolge il nodo canonico `_defaults()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 995 che coinvolge il nodo canonico `_defaults()` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 995 che coinvolge il nodo canonico `_defaults()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 995 che coinvolge il nodo canonico `_defaults()` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 995 che coinvolge il nodo canonico `_defaults()` è `L42`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 995 che coinvolge il nodo canonico `_defaults()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 995 che coinvolge il nodo canonico `_defaults()` è `ast`. Nel grafo del codice RDG, l'arco 995 registra come `source` l'identificatore `ui_ui_config_bridge_defaults` per la relazione che coinvolge il nodo canonico `_defaults()`. Nel grafo del codice RDG, l'arco 995 registra come `target` l'identificatore `ui_ui_config_bridge_py_any` per la relazione che coinvolge il nodo canonico `_defaults()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 995 che coinvolge il nodo canonico `_defaults()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=986 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_defaults()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `ui_config_bridge.py`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge` all'ID `ui_ui_config_bridge_defaults`. La relazione tecnica esplicita è la seguente: `ui_config_bridge.py` contiene `_defaults()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 986 che coinvolge il nodo canonico `_defaults()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 986 che coinvolge il nodo canonico `_defaults()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 986 che coinvolge il nodo canonico `_defaults()` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 986 che coinvolge il nodo canonico `_defaults()` è `L42`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 986 che coinvolge il nodo canonico `_defaults()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 986 che coinvolge il nodo canonico `_defaults()` è `ast`. Nel grafo del codice RDG, l'arco 986 registra come `source` l'identificatore `ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `_defaults()`. Nel grafo del codice RDG, l'arco 986 registra come `target` l'identificatore `ui_ui_config_bridge_defaults` per la relazione che coinvolge il nodo canonico `_defaults()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 986 che coinvolge il nodo canonico `_defaults()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=990 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_defaults()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge_load_config` all'ID `ui_ui_config_bridge_defaults`. La relazione tecnica esplicita è la seguente: `load_config() [ui_ui_config_bridge_load_config]` chiama `_defaults()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 990 che coinvolge il nodo canonico `_defaults()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 990 che coinvolge il nodo canonico `_defaults()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 990 che coinvolge il nodo canonico `_defaults()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 990 che coinvolge il nodo canonico `_defaults()` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 990 che coinvolge il nodo canonico `_defaults()` è `L18`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 990 che coinvolge il nodo canonico `_defaults()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 990 che coinvolge il nodo canonico `_defaults()` è `ast`. Nel grafo del codice RDG, l'arco 990 registra come `source` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `_defaults()`. Nel grafo del codice RDG, l'arco 990 registra come `target` l'identificatore `ui_ui_config_bridge_defaults` per la relazione che coinvolge il nodo canonico `_defaults()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 990 che coinvolge il nodo canonico `_defaults()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=500 id=Request-Dataset-Generator::ui_ui_config_bridge_defaults -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_defaults()` è `_defaults()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_defaults()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_defaults()` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_defaults()` è `L42`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_defaults()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_defaults()` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_defaults()` è `_defaults()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_defaults()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_defaults()` è `ui_ui_config_bridge_defaults`. Nel grafo unificato, il nodo canonico `_defaults()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_ui_config_bridge_defaults`.
Nel grafo unificato, il nodo canonico `_defaults()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=986 role=incoming -->
Nel grafo unificato, il nodo canonico `_defaults()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `ui_config_bridge.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_defaults`. La relazione tecnica esplicita è la seguente: `ui_config_bridge.py` contiene `_defaults()`. Nel grafo unificato, il tipo dichiarato dell'arco 986 che coinvolge il nodo canonico `_defaults()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 986 che coinvolge il nodo canonico `_defaults()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 986 che coinvolge il nodo canonico `_defaults()` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 986 che coinvolge il nodo canonico `_defaults()` è `L42`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 986 che coinvolge il nodo canonico `_defaults()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 986 che coinvolge il nodo canonico `_defaults()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 986 che coinvolge il nodo canonico `_defaults()` è `1.0`. Nel grafo unificato, l'arco 986 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `_defaults()`. Nel grafo unificato, l'arco 986 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_defaults` per la relazione che coinvolge il nodo canonico `_defaults()`.
<!-- GRAPH_EDGE graph=unificata index=990 role=incoming -->
Nel grafo unificato, il nodo canonico `_defaults()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_defaults`. La relazione tecnica esplicita è la seguente: `load_config() [ui_ui_config_bridge_load_config]` chiama `_defaults()`. Nel grafo unificato, il tipo dichiarato dell'arco 990 che coinvolge il nodo canonico `_defaults()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 990 che coinvolge il nodo canonico `_defaults()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 990 che coinvolge il nodo canonico `_defaults()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 990 che coinvolge il nodo canonico `_defaults()` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 990 che coinvolge il nodo canonico `_defaults()` è `L18`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 990 che coinvolge il nodo canonico `_defaults()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 990 che coinvolge il nodo canonico `_defaults()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 990 che coinvolge il nodo canonico `_defaults()` è `1.0`. Nel grafo unificato, l'arco 990 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `_defaults()`. Nel grafo unificato, l'arco 990 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_defaults` per la relazione che coinvolge il nodo canonico `_defaults()`.
<!-- GRAPH_EDGE graph=unificata index=995 role=incoming -->
Nel grafo unificato, il nodo canonico `_defaults()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Any [ui_ui_config_bridge_py_any]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge_py_any` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_defaults`. La relazione tecnica esplicita è la seguente: `Any [ui_ui_config_bridge_py_any]` fa riferimento a `_defaults()`. Nel grafo unificato, il tipo dichiarato dell'arco 995 che coinvolge il nodo canonico `_defaults()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 995 che coinvolge il nodo canonico `_defaults()` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 995 che coinvolge il nodo canonico `_defaults()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 995 che coinvolge il nodo canonico `_defaults()` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 995 che coinvolge il nodo canonico `_defaults()` è `L42`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 995 che coinvolge il nodo canonico `_defaults()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 995 che coinvolge il nodo canonico `_defaults()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 995 che coinvolge il nodo canonico `_defaults()` è `1.0`. Nel grafo unificato, l'arco 995 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_py_any` per la relazione che coinvolge il nodo canonico `_defaults()`. Nel grafo unificato, l'arco 995 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_defaults` per la relazione che coinvolge il nodo canonico `_defaults()`.

### Any [ui_tabs_dashboard_tab_py_any]

<!-- GRAPH_NODE graph=codice index=454 id=ui_tabs_dashboard_tab_py_any -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `Any`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è ``. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` possiede l'identificatore esatto `ui_tabs_dashboard_tab_py_any`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `any`.
Nel grafo del codice RDG, il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=913 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `._populate_metrics()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_populate_metrics` all'ID `ui_tabs_dashboard_tab_py_any`. La relazione tecnica esplicita è la seguente: `._populate_metrics()` fa riferimento a `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 913 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 913 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 913 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 913 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 913 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `L206`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 913 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 913 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ast`. Nel grafo del codice RDG, l'arco 913 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_populate_metrics` per la relazione che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo del codice RDG, l'arco 913 registra come `target` l'identificatore `ui_tabs_dashboard_tab_py_any` per la relazione che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 913 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=914 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `._chart_values()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_chart_values` all'ID `ui_tabs_dashboard_tab_py_any`. La relazione tecnica esplicita è la seguente: `._chart_values()` fa riferimento a `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 914 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 914 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 914 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 914 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 914 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `L336`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 914 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 914 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ast`. Nel grafo del codice RDG, l'arco 914 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_chart_values` per la relazione che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo del codice RDG, l'arco 914 registra come `target` l'identificatore `ui_tabs_dashboard_tab_py_any` per la relazione che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 914 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=915 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `._draw_chart()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_draw_chart` all'ID `ui_tabs_dashboard_tab_py_any`. La relazione tecnica esplicita è la seguente: `._draw_chart()` fa riferimento a `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 915 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 915 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 915 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 915 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 915 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `L242`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 915 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 915 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ast`. Nel grafo del codice RDG, l'arco 915 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_draw_chart` per la relazione che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo del codice RDG, l'arco 915 registra come `target` l'identificatore `ui_tabs_dashboard_tab_py_any` per la relazione che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 915 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=916 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `._read_metrics()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_read_metrics` all'ID `ui_tabs_dashboard_tab_py_any`. La relazione tecnica esplicita è la seguente: `._read_metrics()` fa riferimento a `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 916 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 916 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 916 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 916 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 916 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `L306`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 916 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 916 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ast`. Nel grafo del codice RDG, l'arco 916 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_read_metrics` per la relazione che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo del codice RDG, l'arco 916 registra come `target` l'identificatore `ui_tabs_dashboard_tab_py_any` per la relazione che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 916 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=917 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `._set_metric()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_set_metric` all'ID `ui_tabs_dashboard_tab_py_any`. La relazione tecnica esplicita è la seguente: `._set_metric()` fa riferimento a `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 917 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 917 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 917 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 917 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 917 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `L229`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 917 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 917 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ast`. Nel grafo del codice RDG, l'arco 917 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_set_metric` per la relazione che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo del codice RDG, l'arco 917 registra come `target` l'identificatore `ui_tabs_dashboard_tab_py_any` per la relazione che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 917 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=454 id=Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `Any`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è ``. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `any`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ui_tabs_dashboard_tab_py_any`. Nel grafo unificato, il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any`.
<!-- GRAPH_EDGE graph=unificata index=914 role=outgoing -->
Nel grafo unificato, il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è la sorgente `source` della relazione `references` verso il nodo canonico `._chart_values()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_chart_values`. La relazione tecnica esplicita è la seguente: `Any [ui_tabs_dashboard_tab_py_any]` fa riferimento a `._chart_values()`. Nel grafo unificato, il tipo dichiarato dell'arco 914 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 914 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 914 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 914 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 914 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `L336`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 914 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 914 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 914 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `1.0`. Nel grafo unificato, l'arco 914 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any` per la relazione che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo unificato, l'arco 914 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_chart_values` per la relazione che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`.
<!-- GRAPH_EDGE graph=unificata index=915 role=outgoing -->
Nel grafo unificato, il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è la sorgente `source` della relazione `references` verso il nodo canonico `._draw_chart()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_draw_chart`. La relazione tecnica esplicita è la seguente: `Any [ui_tabs_dashboard_tab_py_any]` fa riferimento a `._draw_chart()`. Nel grafo unificato, il tipo dichiarato dell'arco 915 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 915 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 915 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 915 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 915 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `L242`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 915 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 915 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 915 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `1.0`. Nel grafo unificato, l'arco 915 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any` per la relazione che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo unificato, l'arco 915 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_draw_chart` per la relazione che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`.
<!-- GRAPH_EDGE graph=unificata index=916 role=outgoing -->
Nel grafo unificato, il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è la sorgente `source` della relazione `references` verso il nodo canonico `._read_metrics()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics`. La relazione tecnica esplicita è la seguente: `Any [ui_tabs_dashboard_tab_py_any]` fa riferimento a `._read_metrics()`. Nel grafo unificato, il tipo dichiarato dell'arco 916 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 916 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 916 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 916 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 916 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `L306`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 916 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 916 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 916 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `1.0`. Nel grafo unificato, l'arco 916 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any` per la relazione che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo unificato, l'arco 916 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics` per la relazione che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`.
<!-- GRAPH_EDGE graph=unificata index=917 role=outgoing -->
Nel grafo unificato, il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è la sorgente `source` della relazione `references` verso il nodo canonico `._set_metric()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_set_metric`. La relazione tecnica esplicita è la seguente: `Any [ui_tabs_dashboard_tab_py_any]` fa riferimento a `._set_metric()`. Nel grafo unificato, il tipo dichiarato dell'arco 917 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 917 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 917 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 917 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 917 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `L229`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 917 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 917 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 917 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `1.0`. Nel grafo unificato, l'arco 917 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any` per la relazione che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo unificato, l'arco 917 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_set_metric` per la relazione che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`.
<!-- GRAPH_EDGE graph=unificata index=913 role=incoming -->
Nel grafo unificato, il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `._populate_metrics()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_populate_metrics` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any`. La relazione tecnica esplicita è la seguente: `._populate_metrics()` fa riferimento a `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo unificato, il tipo dichiarato dell'arco 913 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 913 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 913 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 913 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 913 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `L206`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 913 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 913 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 913 che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]` è `1.0`. Nel grafo unificato, l'arco 913 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_populate_metrics` per la relazione che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`. Nel grafo unificato, l'arco 913 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_any` per la relazione che coinvolge il nodo canonico `Any [ui_tabs_dashboard_tab_py_any]`.

### Any [ui_ui_config_bridge_py_any]

<!-- GRAPH_NODE graph=codice index=498 id=ui_ui_config_bridge_py_any -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Any [ui_ui_config_bridge_py_any]` è `Any`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Any [ui_ui_config_bridge_py_any]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Any [ui_ui_config_bridge_py_any]` è ``. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Any [ui_ui_config_bridge_py_any]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Any [ui_ui_config_bridge_py_any]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Any [ui_ui_config_bridge_py_any]` possiede l'identificatore esatto `ui_ui_config_bridge_py_any`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Any [ui_ui_config_bridge_py_any]` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Any [ui_ui_config_bridge_py_any]` è `any`.
Nel grafo del codice RDG, il nodo canonico `Any [ui_ui_config_bridge_py_any]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=991 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Any [ui_ui_config_bridge_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge_load_config` all'ID `ui_ui_config_bridge_py_any`. La relazione tecnica esplicita è la seguente: `load_config() [ui_ui_config_bridge_load_config]` fa riferimento a `Any [ui_ui_config_bridge_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 991 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 991 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 991 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 991 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 991 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 991 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 991 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `ast`. Nel grafo del codice RDG, l'arco 991 registra come `source` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]`. Nel grafo del codice RDG, l'arco 991 registra come `target` l'identificatore `ui_ui_config_bridge_py_any` per la relazione che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 991 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=995 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Any [ui_ui_config_bridge_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_defaults()`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge_defaults` all'ID `ui_ui_config_bridge_py_any`. La relazione tecnica esplicita è la seguente: `_defaults()` fa riferimento a `Any [ui_ui_config_bridge_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 995 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 995 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 995 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 995 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 995 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `L42`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 995 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 995 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `ast`. Nel grafo del codice RDG, l'arco 995 registra come `source` l'identificatore `ui_ui_config_bridge_defaults` per la relazione che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]`. Nel grafo del codice RDG, l'arco 995 registra come `target` l'identificatore `ui_ui_config_bridge_py_any` per la relazione che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 995 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=996 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Any [ui_ui_config_bridge_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `save_config()`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge_save_config` all'ID `ui_ui_config_bridge_py_any`. La relazione tecnica esplicita è la seguente: `save_config()` fa riferimento a `Any [ui_ui_config_bridge_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 996 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 996 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 996 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 996 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 996 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `L34`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 996 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 996 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `ast`. Nel grafo del codice RDG, l'arco 996 registra come `source` l'identificatore `ui_ui_config_bridge_save_config` per la relazione che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]`. Nel grafo del codice RDG, l'arco 996 registra come `target` l'identificatore `ui_ui_config_bridge_py_any` per la relazione che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 996 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=498 id=Request-Dataset-Generator::ui_ui_config_bridge_py_any -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Any [ui_ui_config_bridge_py_any]` è `Any`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Any [ui_ui_config_bridge_py_any]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Any [ui_ui_config_bridge_py_any]` è ``. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Any [ui_ui_config_bridge_py_any]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Any [ui_ui_config_bridge_py_any]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Any [ui_ui_config_bridge_py_any]` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Any [ui_ui_config_bridge_py_any]` è `any`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Any [ui_ui_config_bridge_py_any]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `ui_ui_config_bridge_py_any`. Nel grafo unificato, il nodo canonico `Any [ui_ui_config_bridge_py_any]` possiede l'identificatore esatto `Request-Dataset-Generator::ui_ui_config_bridge_py_any`.
<!-- GRAPH_EDGE graph=unificata index=995 role=outgoing -->
Nel grafo unificato, il nodo canonico `Any [ui_ui_config_bridge_py_any]` è la sorgente `source` della relazione `references` verso il nodo canonico `_defaults()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge_py_any` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_defaults`. La relazione tecnica esplicita è la seguente: `Any [ui_ui_config_bridge_py_any]` fa riferimento a `_defaults()`. Nel grafo unificato, il tipo dichiarato dell'arco 995 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 995 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 995 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 995 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 995 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `L42`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 995 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 995 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 995 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `1.0`. Nel grafo unificato, l'arco 995 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_py_any` per la relazione che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]`. Nel grafo unificato, l'arco 995 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_defaults` per la relazione che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]`.
<!-- GRAPH_EDGE graph=unificata index=996 role=outgoing -->
Nel grafo unificato, il nodo canonico `Any [ui_ui_config_bridge_py_any]` è la sorgente `source` della relazione `references` verso il nodo canonico `save_config()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge_py_any` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_save_config`. La relazione tecnica esplicita è la seguente: `Any [ui_ui_config_bridge_py_any]` fa riferimento a `save_config()`. Nel grafo unificato, il tipo dichiarato dell'arco 996 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 996 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 996 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 996 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 996 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `L34`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 996 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 996 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 996 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `1.0`. Nel grafo unificato, l'arco 996 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_py_any` per la relazione che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]`. Nel grafo unificato, l'arco 996 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_save_config` per la relazione che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]`.
<!-- GRAPH_EDGE graph=unificata index=991 role=incoming -->
Nel grafo unificato, il nodo canonico `Any [ui_ui_config_bridge_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_py_any`. La relazione tecnica esplicita è la seguente: `load_config() [ui_ui_config_bridge_load_config]` fa riferimento a `Any [ui_ui_config_bridge_py_any]`. Nel grafo unificato, il tipo dichiarato dell'arco 991 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 991 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 991 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 991 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 991 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 991 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 991 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 991 che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]` è `1.0`. Nel grafo unificato, l'arco 991 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]`. Nel grafo unificato, l'arco 991 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_py_any` per la relazione che coinvolge il nodo canonico `Any [ui_ui_config_bridge_py_any]`.

### app.py

<!-- GRAPH_NODE graph=codice index=413 id=ui_app -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `app.py` è `app.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `app.py` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `app.py` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `app.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `app.py` vale `ast`. Nel grafo del codice RDG, il nodo canonico `app.py` possiede l'identificatore esatto `ui_app`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `app.py` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `app.py` è `app.py`.
<!-- GRAPH_EDGE graph=codice index=820 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `app.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `main() [ui_app_main]`; l'orientamento registrato nel JSON va dall'ID `ui_app` all'ID `ui_app_main`. La relazione tecnica esplicita è la seguente: `app.py` contiene `main() [ui_app_main]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 820 che coinvolge il nodo canonico `app.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 820 che coinvolge il nodo canonico `app.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 820 che coinvolge il nodo canonico `app.py` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 820 che coinvolge il nodo canonico `app.py` è `L136`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 820 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 820 che coinvolge il nodo canonico `app.py` è `ast`. Nel grafo del codice RDG, l'arco 820 registra come `source` l'identificatore `ui_app` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo del codice RDG, l'arco 820 registra come `target` l'identificatore `ui_app_main` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 820 che coinvolge il nodo canonico `app.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=821 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `app.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `RDGApp`; l'orientamento registrato nel JSON va dall'ID `ui_app` all'ID `ui_app_rdgapp`. La relazione tecnica esplicita è la seguente: `app.py` contiene `RDGApp`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 821 che coinvolge il nodo canonico `app.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 821 che coinvolge il nodo canonico `app.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 821 che coinvolge il nodo canonico `app.py` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 821 che coinvolge il nodo canonico `app.py` è `L31`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 821 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 821 che coinvolge il nodo canonico `app.py` è `ast`. Nel grafo del codice RDG, l'arco 821 registra come `source` l'identificatore `ui_app` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo del codice RDG, l'arco 821 registra come `target` l'identificatore `ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 821 che coinvolge il nodo canonico `app.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=822 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `app.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `style.py`; l'orientamento registrato nel JSON va dall'ID `ui_app` all'ID `ui_style`. La relazione tecnica esplicita è la seguente: `app.py` importa da `style.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 822 che coinvolge il nodo canonico `app.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 822 che coinvolge il nodo canonico `app.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 822 che coinvolge il nodo canonico `app.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 822 che coinvolge il nodo canonico `app.py` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 822 che coinvolge il nodo canonico `app.py` è `L13`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 822 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 822 che coinvolge il nodo canonico `app.py` è `ast`. Nel grafo del codice RDG, l'arco 822 registra come `source` l'identificatore `ui_app` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo del codice RDG, l'arco 822 registra come `target` l'identificatore `ui_style` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 822 che coinvolge il nodo canonico `app.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=823 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `app.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `config_tab.py`; l'orientamento registrato nel JSON va dall'ID `ui_app` all'ID `ui_tabs_config_tab`. La relazione tecnica esplicita è la seguente: `app.py` importa da `config_tab.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 823 che coinvolge il nodo canonico `app.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 823 che coinvolge il nodo canonico `app.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 823 che coinvolge il nodo canonico `app.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 823 che coinvolge il nodo canonico `app.py` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 823 che coinvolge il nodo canonico `app.py` è `L26`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 823 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 823 che coinvolge il nodo canonico `app.py` è `ast`. Nel grafo del codice RDG, l'arco 823 registra come `source` l'identificatore `ui_app` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo del codice RDG, l'arco 823 registra come `target` l'identificatore `ui_tabs_config_tab` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 823 che coinvolge il nodo canonico `app.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=824 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `app.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `ui_app` all'ID `ui_tabs_config_tab_configtab`. La relazione tecnica esplicita è la seguente: `app.py` importa `ConfigTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 824 che coinvolge il nodo canonico `app.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 824 che coinvolge il nodo canonico `app.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 824 che coinvolge il nodo canonico `app.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 824 che coinvolge il nodo canonico `app.py` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 824 che coinvolge il nodo canonico `app.py` è `L26`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 824 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 824 che coinvolge il nodo canonico `app.py` è `ast`. Nel grafo del codice RDG, l'arco 824 registra come `source` l'identificatore `ui_app` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo del codice RDG, l'arco 824 registra come `target` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 824 che coinvolge il nodo canonico `app.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=825 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `app.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `dashboard_tab.py`; l'orientamento registrato nel JSON va dall'ID `ui_app` all'ID `ui_tabs_dashboard_tab`. La relazione tecnica esplicita è la seguente: `app.py` importa da `dashboard_tab.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 825 che coinvolge il nodo canonico `app.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 825 che coinvolge il nodo canonico `app.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 825 che coinvolge il nodo canonico `app.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 825 che coinvolge il nodo canonico `app.py` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 825 che coinvolge il nodo canonico `app.py` è `L27`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 825 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 825 che coinvolge il nodo canonico `app.py` è `ast`. Nel grafo del codice RDG, l'arco 825 registra come `source` l'identificatore `ui_app` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo del codice RDG, l'arco 825 registra come `target` l'identificatore `ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 825 che coinvolge il nodo canonico `app.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=826 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `app.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `ui_app` all'ID `ui_tabs_dashboard_tab_dashboardtab`. La relazione tecnica esplicita è la seguente: `app.py` importa `DashboardTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 826 che coinvolge il nodo canonico `app.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 826 che coinvolge il nodo canonico `app.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 826 che coinvolge il nodo canonico `app.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 826 che coinvolge il nodo canonico `app.py` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 826 che coinvolge il nodo canonico `app.py` è `L27`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 826 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 826 che coinvolge il nodo canonico `app.py` è `ast`. Nel grafo del codice RDG, l'arco 826 registra come `source` l'identificatore `ui_app` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo del codice RDG, l'arco 826 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 826 che coinvolge il nodo canonico `app.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=827 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `app.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `run_tab.py`; l'orientamento registrato nel JSON va dall'ID `ui_app` all'ID `ui_tabs_run_tab`. La relazione tecnica esplicita è la seguente: `app.py` importa da `run_tab.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 827 che coinvolge il nodo canonico `app.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 827 che coinvolge il nodo canonico `app.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 827 che coinvolge il nodo canonico `app.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 827 che coinvolge il nodo canonico `app.py` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 827 che coinvolge il nodo canonico `app.py` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 827 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 827 che coinvolge il nodo canonico `app.py` è `ast`. Nel grafo del codice RDG, l'arco 827 registra come `source` l'identificatore `ui_app` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo del codice RDG, l'arco 827 registra come `target` l'identificatore `ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 827 che coinvolge il nodo canonico `app.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=828 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `app.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_app` all'ID `ui_tabs_run_tab_runtab`. La relazione tecnica esplicita è la seguente: `app.py` importa `RunTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 828 che coinvolge il nodo canonico `app.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 828 che coinvolge il nodo canonico `app.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 828 che coinvolge il nodo canonico `app.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 828 che coinvolge il nodo canonico `app.py` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 828 che coinvolge il nodo canonico `app.py` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 828 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 828 che coinvolge il nodo canonico `app.py` è `ast`. Nel grafo del codice RDG, l'arco 828 registra come `source` l'identificatore `ui_app` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo del codice RDG, l'arco 828 registra come `target` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 828 che coinvolge il nodo canonico `app.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=829 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `app.py` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.`; l'orientamento registrato nel JSON va dall'ID `ui_app_rationale_1` all'ID `ui_app`. La relazione tecnica esplicita è la seguente: `Entry point CustomTkinter per Request-Dataset-Generator.` costituisce una motivazione per `app.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 829 che coinvolge il nodo canonico `app.py` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 829 che coinvolge il nodo canonico `app.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 829 che coinvolge il nodo canonico `app.py` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 829 che coinvolge il nodo canonico `app.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 829 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 829 che coinvolge il nodo canonico `app.py` è `ast`. Nel grafo del codice RDG, l'arco 829 registra come `source` l'identificatore `ui_app_rationale_1` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo del codice RDG, l'arco 829 registra come `target` l'identificatore `ui_app` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 829 che coinvolge il nodo canonico `app.py` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=413 id=Request-Dataset-Generator::ui_app -->
Nel grafo unificato, l'etichetta originale del nodo canonico `app.py` è `app.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `app.py` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `app.py` è `ui/app.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `app.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `app.py` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `app.py` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `app.py` è `app.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `app.py` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `app.py` è `ui_app`. Nel grafo unificato, il nodo canonico `app.py` possiede l'identificatore esatto `Request-Dataset-Generator::ui_app`.
<!-- GRAPH_EDGE graph=unificata index=820 role=outgoing -->
Nel grafo unificato, il nodo canonico `app.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `main() [ui_app_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app` all'ID `Request-Dataset-Generator::ui_app_main`. La relazione tecnica esplicita è la seguente: `app.py` contiene `main() [ui_app_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 820 che coinvolge il nodo canonico `app.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 820 che coinvolge il nodo canonico `app.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 820 che coinvolge il nodo canonico `app.py` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 820 che coinvolge il nodo canonico `app.py` è `L136`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 820 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 820 che coinvolge il nodo canonico `app.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 820 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo unificato, l'arco 820 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo unificato, l'arco 820 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_main` per la relazione che coinvolge il nodo canonico `app.py`.
<!-- GRAPH_EDGE graph=unificata index=821 role=outgoing -->
Nel grafo unificato, il nodo canonico `app.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `RDGApp`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app` all'ID `Request-Dataset-Generator::ui_app_rdgapp`. La relazione tecnica esplicita è la seguente: `app.py` contiene `RDGApp`. Nel grafo unificato, il tipo dichiarato dell'arco 821 che coinvolge il nodo canonico `app.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 821 che coinvolge il nodo canonico `app.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 821 che coinvolge il nodo canonico `app.py` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 821 che coinvolge il nodo canonico `app.py` è `L31`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 821 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 821 che coinvolge il nodo canonico `app.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 821 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo unificato, l'arco 821 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo unificato, l'arco 821 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `app.py`.
<!-- GRAPH_EDGE graph=unificata index=822 role=outgoing -->
Nel grafo unificato, il nodo canonico `app.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `style.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app` all'ID `Request-Dataset-Generator::ui_style`. La relazione tecnica esplicita è la seguente: `app.py` importa da `style.py`. Nel grafo unificato, il tipo dichiarato dell'arco 822 che coinvolge il nodo canonico `app.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 822 che coinvolge il nodo canonico `app.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 822 che coinvolge il nodo canonico `app.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 822 che coinvolge il nodo canonico `app.py` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 822 che coinvolge il nodo canonico `app.py` è `L13`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 822 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 822 che coinvolge il nodo canonico `app.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 822 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo unificato, l'arco 822 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo unificato, l'arco 822 registra come `target` l'identificatore `Request-Dataset-Generator::ui_style` per la relazione che coinvolge il nodo canonico `app.py`.
<!-- GRAPH_EDGE graph=unificata index=823 role=outgoing -->
Nel grafo unificato, il nodo canonico `app.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `config_tab.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app` all'ID `Request-Dataset-Generator::ui_tabs_config_tab`. La relazione tecnica esplicita è la seguente: `app.py` importa da `config_tab.py`. Nel grafo unificato, il tipo dichiarato dell'arco 823 che coinvolge il nodo canonico `app.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 823 che coinvolge il nodo canonico `app.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 823 che coinvolge il nodo canonico `app.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 823 che coinvolge il nodo canonico `app.py` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 823 che coinvolge il nodo canonico `app.py` è `L26`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 823 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 823 che coinvolge il nodo canonico `app.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 823 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo unificato, l'arco 823 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo unificato, l'arco 823 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab` per la relazione che coinvolge il nodo canonico `app.py`.
<!-- GRAPH_EDGE graph=unificata index=824 role=outgoing -->
Nel grafo unificato, il nodo canonico `app.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab`. La relazione tecnica esplicita è la seguente: `app.py` importa `ConfigTab`. Nel grafo unificato, il tipo dichiarato dell'arco 824 che coinvolge il nodo canonico `app.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 824 che coinvolge il nodo canonico `app.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 824 che coinvolge il nodo canonico `app.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 824 che coinvolge il nodo canonico `app.py` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 824 che coinvolge il nodo canonico `app.py` è `L26`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 824 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 824 che coinvolge il nodo canonico `app.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 824 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo unificato, l'arco 824 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo unificato, l'arco 824 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `app.py`.
<!-- GRAPH_EDGE graph=unificata index=825 role=outgoing -->
Nel grafo unificato, il nodo canonico `app.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `dashboard_tab.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab`. La relazione tecnica esplicita è la seguente: `app.py` importa da `dashboard_tab.py`. Nel grafo unificato, il tipo dichiarato dell'arco 825 che coinvolge il nodo canonico `app.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 825 che coinvolge il nodo canonico `app.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 825 che coinvolge il nodo canonico `app.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 825 che coinvolge il nodo canonico `app.py` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 825 che coinvolge il nodo canonico `app.py` è `L27`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 825 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 825 che coinvolge il nodo canonico `app.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 825 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo unificato, l'arco 825 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo unificato, l'arco 825 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `app.py`.
<!-- GRAPH_EDGE graph=unificata index=826 role=outgoing -->
Nel grafo unificato, il nodo canonico `app.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab`. La relazione tecnica esplicita è la seguente: `app.py` importa `DashboardTab`. Nel grafo unificato, il tipo dichiarato dell'arco 826 che coinvolge il nodo canonico `app.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 826 che coinvolge il nodo canonico `app.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 826 che coinvolge il nodo canonico `app.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 826 che coinvolge il nodo canonico `app.py` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 826 che coinvolge il nodo canonico `app.py` è `L27`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 826 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 826 che coinvolge il nodo canonico `app.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 826 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo unificato, l'arco 826 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo unificato, l'arco 826 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `app.py`.
<!-- GRAPH_EDGE graph=unificata index=827 role=outgoing -->
Nel grafo unificato, il nodo canonico `app.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `run_tab.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app` all'ID `Request-Dataset-Generator::ui_tabs_run_tab`. La relazione tecnica esplicita è la seguente: `app.py` importa da `run_tab.py`. Nel grafo unificato, il tipo dichiarato dell'arco 827 che coinvolge il nodo canonico `app.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 827 che coinvolge il nodo canonico `app.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 827 che coinvolge il nodo canonico `app.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 827 che coinvolge il nodo canonico `app.py` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 827 che coinvolge il nodo canonico `app.py` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 827 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 827 che coinvolge il nodo canonico `app.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 827 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo unificato, l'arco 827 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo unificato, l'arco 827 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `app.py`.
<!-- GRAPH_EDGE graph=unificata index=828 role=outgoing -->
Nel grafo unificato, il nodo canonico `app.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab`. La relazione tecnica esplicita è la seguente: `app.py` importa `RunTab`. Nel grafo unificato, il tipo dichiarato dell'arco 828 che coinvolge il nodo canonico `app.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 828 che coinvolge il nodo canonico `app.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 828 che coinvolge il nodo canonico `app.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 828 che coinvolge il nodo canonico `app.py` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 828 che coinvolge il nodo canonico `app.py` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 828 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 828 che coinvolge il nodo canonico `app.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 828 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo unificato, l'arco 828 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo unificato, l'arco 828 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `app.py`.
<!-- GRAPH_EDGE graph=unificata index=829 role=outgoing -->
Nel grafo unificato, il nodo canonico `app.py` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app` all'ID `Request-Dataset-Generator::ui_app_rationale_1`. La relazione tecnica esplicita è la seguente: `app.py` costituisce una motivazione per `Entry point CustomTkinter per Request-Dataset-Generator.`. Nel grafo unificato, il tipo dichiarato dell'arco 829 che coinvolge il nodo canonico `app.py` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 829 che coinvolge il nodo canonico `app.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 829 che coinvolge il nodo canonico `app.py` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 829 che coinvolge il nodo canonico `app.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 829 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 829 che coinvolge il nodo canonico `app.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 829 che coinvolge il nodo canonico `app.py` è `1.0`. Nel grafo unificato, l'arco 829 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app` per la relazione che coinvolge il nodo canonico `app.py`. Nel grafo unificato, l'arco 829 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_rationale_1` per la relazione che coinvolge il nodo canonico `app.py`.
Nel grafo unificato, il nodo canonico `app.py` non possiede archi entranti registrati nel campo `links`.

### Bridge tra UI e file di configurazione json dell&#x27;esperimento. Non importa nulla

<!-- GRAPH_NODE graph=codice index=501 id=ui_ui_config_bridge_rationale_1 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` possiede l'identificatore esatto `ui_ui_config_bridge_rationale_1`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `bridge tra ui e file di configurazione json dell'esperimento. non importa nulla`.
<!-- GRAPH_EDGE graph=codice index=989 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `ui_config_bridge.py`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge_rationale_1` all'ID `ui_ui_config_bridge`. La relazione tecnica esplicita è la seguente: `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` costituisce una motivazione per `ui_config_bridge.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 989 che coinvolge il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 989 che coinvolge il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 989 che coinvolge il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 989 che coinvolge il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 989 che coinvolge il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 989 che coinvolge il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `ast`. Nel grafo del codice RDG, l'arco 989 registra come `source` l'identificatore `ui_ui_config_bridge_rationale_1` per la relazione che coinvolge il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla`. Nel grafo del codice RDG, l'arco 989 registra come `target` l'identificatore `ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 989 che coinvolge il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=501 id=Request-Dataset-Generator::ui_ui_config_bridge_rationale_1 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `bridge tra ui e file di configurazione json dell'esperimento. non importa nulla`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `ui_ui_config_bridge_rationale_1`. Nel grafo unificato, il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` possiede l'identificatore esatto `Request-Dataset-Generator::ui_ui_config_bridge_rationale_1`.
Nel grafo unificato, il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=989 role=incoming -->
Nel grafo unificato, il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `ui_config_bridge.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_rationale_1`. La relazione tecnica esplicita è la seguente: `ui_config_bridge.py` costituisce una motivazione per `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla`. Nel grafo unificato, il tipo dichiarato dell'arco 989 che coinvolge il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 989 che coinvolge il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 989 che coinvolge il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 989 che coinvolge il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 989 che coinvolge il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 989 che coinvolge il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 989 che coinvolge il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` è `1.0`. Nel grafo unificato, l'arco 989 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla`. Nel grafo unificato, l'arco 989 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_rationale_1` per la relazione che coinvolge il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla`.

### Carica il file json. Se non esiste, o non e&#x27; valido, restituisce i default.

<!-- GRAPH_NODE graph=codice index=502 id=ui_ui_config_bridge_rationale_16 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `Carica il file json. Se non esiste, o non e' valido, restituisce i default.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `L16`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` possiede l'identificatore esatto `ui_ui_config_bridge_rationale_16`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `carica il file json. se non esiste, o non e' valido, restituisce i default.`.
<!-- GRAPH_EDGE graph=codice index=993 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge_rationale_16` all'ID `ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` costituisce una motivazione per `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 993 che coinvolge il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 993 che coinvolge il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 993 che coinvolge il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 993 che coinvolge il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 993 che coinvolge il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 993 che coinvolge il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `ast`. Nel grafo del codice RDG, l'arco 993 registra come `source` l'identificatore `ui_ui_config_bridge_rationale_16` per la relazione che coinvolge il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.`. Nel grafo del codice RDG, l'arco 993 registra come `target` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 993 che coinvolge il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=502 id=Request-Dataset-Generator::ui_ui_config_bridge_rationale_16 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `Carica il file json. Se non esiste, o non e' valido, restituisce i default.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `L16`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `carica il file json. se non esiste, o non e' valido, restituisce i default.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `ui_ui_config_bridge_rationale_16`. Nel grafo unificato, il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` possiede l'identificatore esatto `Request-Dataset-Generator::ui_ui_config_bridge_rationale_16`.
Nel grafo unificato, il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=993 role=incoming -->
Nel grafo unificato, il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_rationale_16`. La relazione tecnica esplicita è la seguente: `load_config() [ui_ui_config_bridge_load_config]` costituisce una motivazione per `Carica il file json. Se non esiste, o non e' valido, restituisce i default.`. Nel grafo unificato, il tipo dichiarato dell'arco 993 che coinvolge il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 993 che coinvolge il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 993 che coinvolge il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 993 che coinvolge il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 993 che coinvolge il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 993 che coinvolge il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 993 che coinvolge il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` è `1.0`. Nel grafo unificato, l'arco 993 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.`. Nel grafo unificato, l'arco 993 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_rationale_16` per la relazione che coinvolge il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.`.

### config_tab.py

<!-- GRAPH_NODE graph=codice index=426 id=ui_tabs_config_tab -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `config_tab.py` è `config_tab.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `config_tab.py` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `config_tab.py` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `config_tab.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `config_tab.py` vale `ast`. Nel grafo del codice RDG, il nodo canonico `config_tab.py` possiede l'identificatore esatto `ui_tabs_config_tab`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `config_tab.py` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `config_tab.py` è `config_tab.py`.
<!-- GRAPH_EDGE graph=codice index=846 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `config_tab.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `style.py`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab` all'ID `ui_style`. La relazione tecnica esplicita è la seguente: `config_tab.py` importa da `style.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 846 che coinvolge il nodo canonico `config_tab.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 846 che coinvolge il nodo canonico `config_tab.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 846 che coinvolge il nodo canonico `config_tab.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 846 che coinvolge il nodo canonico `config_tab.py` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 846 che coinvolge il nodo canonico `config_tab.py` è `L8`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 846 che coinvolge il nodo canonico `config_tab.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 846 che coinvolge il nodo canonico `config_tab.py` è `ast`. Nel grafo del codice RDG, l'arco 846 registra come `source` l'identificatore `ui_tabs_config_tab` per la relazione che coinvolge il nodo canonico `config_tab.py`. Nel grafo del codice RDG, l'arco 846 registra come `target` l'identificatore `ui_style` per la relazione che coinvolge il nodo canonico `config_tab.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 846 che coinvolge il nodo canonico `config_tab.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=850 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `config_tab.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab` all'ID `ui_tabs_config_tab_configtab`. La relazione tecnica esplicita è la seguente: `config_tab.py` contiene `ConfigTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 850 che coinvolge il nodo canonico `config_tab.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 850 che coinvolge il nodo canonico `config_tab.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 850 che coinvolge il nodo canonico `config_tab.py` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 850 che coinvolge il nodo canonico `config_tab.py` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 850 che coinvolge il nodo canonico `config_tab.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 850 che coinvolge il nodo canonico `config_tab.py` è `ast`. Nel grafo del codice RDG, l'arco 850 registra come `source` l'identificatore `ui_tabs_config_tab` per la relazione che coinvolge il nodo canonico `config_tab.py`. Nel grafo del codice RDG, l'arco 850 registra come `target` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `config_tab.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 850 che coinvolge il nodo canonico `config_tab.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=851 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `config_tab.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `ui_config_bridge.py`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab` all'ID `ui_ui_config_bridge`. La relazione tecnica esplicita è la seguente: `config_tab.py` importa da `ui_config_bridge.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 851 che coinvolge il nodo canonico `config_tab.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 851 che coinvolge il nodo canonico `config_tab.py` è `submodule_import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 851 che coinvolge il nodo canonico `config_tab.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 851 che coinvolge il nodo canonico `config_tab.py` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 851 che coinvolge il nodo canonico `config_tab.py` è `L7`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 851 che coinvolge il nodo canonico `config_tab.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 851 che coinvolge il nodo canonico `config_tab.py` è `ast`. Nel grafo del codice RDG, l'arco 851 registra come `source` l'identificatore `ui_tabs_config_tab` per la relazione che coinvolge il nodo canonico `config_tab.py`. Nel grafo del codice RDG, l'arco 851 registra come `target` l'identificatore `ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `config_tab.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 851 che coinvolge il nodo canonico `config_tab.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=823 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `config_tab.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `app.py`; l'orientamento registrato nel JSON va dall'ID `ui_app` all'ID `ui_tabs_config_tab`. La relazione tecnica esplicita è la seguente: `app.py` importa da `config_tab.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 823 che coinvolge il nodo canonico `config_tab.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 823 che coinvolge il nodo canonico `config_tab.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 823 che coinvolge il nodo canonico `config_tab.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 823 che coinvolge il nodo canonico `config_tab.py` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 823 che coinvolge il nodo canonico `config_tab.py` è `L26`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 823 che coinvolge il nodo canonico `config_tab.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 823 che coinvolge il nodo canonico `config_tab.py` è `ast`. Nel grafo del codice RDG, l'arco 823 registra come `source` l'identificatore `ui_app` per la relazione che coinvolge il nodo canonico `config_tab.py`. Nel grafo del codice RDG, l'arco 823 registra come `target` l'identificatore `ui_tabs_config_tab` per la relazione che coinvolge il nodo canonico `config_tab.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 823 che coinvolge il nodo canonico `config_tab.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=852 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `config_tab.py` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Tab di configurazione esperimento RDG.`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_rationale_1` all'ID `ui_tabs_config_tab`. La relazione tecnica esplicita è la seguente: `Tab di configurazione esperimento RDG.` costituisce una motivazione per `config_tab.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 852 che coinvolge il nodo canonico `config_tab.py` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 852 che coinvolge il nodo canonico `config_tab.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 852 che coinvolge il nodo canonico `config_tab.py` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 852 che coinvolge il nodo canonico `config_tab.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 852 che coinvolge il nodo canonico `config_tab.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 852 che coinvolge il nodo canonico `config_tab.py` è `ast`. Nel grafo del codice RDG, l'arco 852 registra come `source` l'identificatore `ui_tabs_config_tab_rationale_1` per la relazione che coinvolge il nodo canonico `config_tab.py`. Nel grafo del codice RDG, l'arco 852 registra come `target` l'identificatore `ui_tabs_config_tab` per la relazione che coinvolge il nodo canonico `config_tab.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 852 che coinvolge il nodo canonico `config_tab.py` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=426 id=Request-Dataset-Generator::ui_tabs_config_tab -->
Nel grafo unificato, l'etichetta originale del nodo canonico `config_tab.py` è `config_tab.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `config_tab.py` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `config_tab.py` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `config_tab.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `config_tab.py` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `config_tab.py` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `config_tab.py` è `config_tab.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `config_tab.py` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `config_tab.py` è `ui_tabs_config_tab`. Nel grafo unificato, il nodo canonico `config_tab.py` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_config_tab`.
<!-- GRAPH_EDGE graph=unificata index=850 role=outgoing -->
Nel grafo unificato, il nodo canonico `config_tab.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab`. La relazione tecnica esplicita è la seguente: `config_tab.py` contiene `ConfigTab`. Nel grafo unificato, il tipo dichiarato dell'arco 850 che coinvolge il nodo canonico `config_tab.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 850 che coinvolge il nodo canonico `config_tab.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 850 che coinvolge il nodo canonico `config_tab.py` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 850 che coinvolge il nodo canonico `config_tab.py` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 850 che coinvolge il nodo canonico `config_tab.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 850 che coinvolge il nodo canonico `config_tab.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 850 che coinvolge il nodo canonico `config_tab.py` è `1.0`. Nel grafo unificato, l'arco 850 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab` per la relazione che coinvolge il nodo canonico `config_tab.py`. Nel grafo unificato, l'arco 850 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `config_tab.py`.
<!-- GRAPH_EDGE graph=unificata index=851 role=outgoing -->
Nel grafo unificato, il nodo canonico `config_tab.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `ui_config_bridge.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab` all'ID `Request-Dataset-Generator::ui_ui_config_bridge`. La relazione tecnica esplicita è la seguente: `config_tab.py` importa da `ui_config_bridge.py`. Nel grafo unificato, il tipo dichiarato dell'arco 851 che coinvolge il nodo canonico `config_tab.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 851 che coinvolge il nodo canonico `config_tab.py` è `submodule_import`. Nel grafo unificato, la confidenza categoriale dell'arco 851 che coinvolge il nodo canonico `config_tab.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 851 che coinvolge il nodo canonico `config_tab.py` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 851 che coinvolge il nodo canonico `config_tab.py` è `L7`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 851 che coinvolge il nodo canonico `config_tab.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 851 che coinvolge il nodo canonico `config_tab.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 851 che coinvolge il nodo canonico `config_tab.py` è `1.0`. Nel grafo unificato, l'arco 851 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab` per la relazione che coinvolge il nodo canonico `config_tab.py`. Nel grafo unificato, l'arco 851 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `config_tab.py`.
<!-- GRAPH_EDGE graph=unificata index=852 role=outgoing -->
Nel grafo unificato, il nodo canonico `config_tab.py` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Tab di configurazione esperimento RDG.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_rationale_1`. La relazione tecnica esplicita è la seguente: `config_tab.py` costituisce una motivazione per `Tab di configurazione esperimento RDG.`. Nel grafo unificato, il tipo dichiarato dell'arco 852 che coinvolge il nodo canonico `config_tab.py` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 852 che coinvolge il nodo canonico `config_tab.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 852 che coinvolge il nodo canonico `config_tab.py` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 852 che coinvolge il nodo canonico `config_tab.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 852 che coinvolge il nodo canonico `config_tab.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 852 che coinvolge il nodo canonico `config_tab.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 852 che coinvolge il nodo canonico `config_tab.py` è `1.0`. Nel grafo unificato, l'arco 852 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab` per la relazione che coinvolge il nodo canonico `config_tab.py`. Nel grafo unificato, l'arco 852 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_rationale_1` per la relazione che coinvolge il nodo canonico `config_tab.py`.
<!-- GRAPH_EDGE graph=unificata index=823 role=incoming -->
Nel grafo unificato, il nodo canonico `config_tab.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `app.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app` all'ID `Request-Dataset-Generator::ui_tabs_config_tab`. La relazione tecnica esplicita è la seguente: `app.py` importa da `config_tab.py`. Nel grafo unificato, il tipo dichiarato dell'arco 823 che coinvolge il nodo canonico `config_tab.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 823 che coinvolge il nodo canonico `config_tab.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 823 che coinvolge il nodo canonico `config_tab.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 823 che coinvolge il nodo canonico `config_tab.py` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 823 che coinvolge il nodo canonico `config_tab.py` è `L26`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 823 che coinvolge il nodo canonico `config_tab.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 823 che coinvolge il nodo canonico `config_tab.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 823 che coinvolge il nodo canonico `config_tab.py` è `1.0`. Nel grafo unificato, l'arco 823 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app` per la relazione che coinvolge il nodo canonico `config_tab.py`. Nel grafo unificato, l'arco 823 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab` per la relazione che coinvolge il nodo canonico `config_tab.py`.
<!-- GRAPH_EDGE graph=unificata index=846 role=incoming -->
Nel grafo unificato, il nodo canonico `config_tab.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `style.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_style` all'ID `Request-Dataset-Generator::ui_tabs_config_tab`. La relazione tecnica esplicita è la seguente: `style.py` importa da `config_tab.py`. Nel grafo unificato, il tipo dichiarato dell'arco 846 che coinvolge il nodo canonico `config_tab.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 846 che coinvolge il nodo canonico `config_tab.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 846 che coinvolge il nodo canonico `config_tab.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 846 che coinvolge il nodo canonico `config_tab.py` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 846 che coinvolge il nodo canonico `config_tab.py` è `L8`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 846 che coinvolge il nodo canonico `config_tab.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 846 che coinvolge il nodo canonico `config_tab.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 846 che coinvolge il nodo canonico `config_tab.py` è `1.0`. Nel grafo unificato, l'arco 846 registra come `source` l'identificatore `Request-Dataset-Generator::ui_style` per la relazione che coinvolge il nodo canonico `config_tab.py`. Nel grafo unificato, l'arco 846 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab` per la relazione che coinvolge il nodo canonico `config_tab.py`.

### Costanti visuali per la UI CustomTkinter di RDG.

<!-- GRAPH_NODE graph=codice index=423 id=ui_style_rationale_1 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `Costanti visuali per la UI CustomTkinter di RDG.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `ui/style.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` possiede l'identificatore esatto `ui_style_rationale_1`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `costanti visuali per la ui customtkinter di rdg.`.
<!-- GRAPH_EDGE graph=codice index=845 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `style.py`; l'orientamento registrato nel JSON va dall'ID `ui_style_rationale_1` all'ID `ui_style`. La relazione tecnica esplicita è la seguente: `Costanti visuali per la UI CustomTkinter di RDG.` costituisce una motivazione per `style.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 845 che coinvolge il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 845 che coinvolge il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 845 che coinvolge il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `ui/style.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 845 che coinvolge il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 845 che coinvolge il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 845 che coinvolge il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `ast`. Nel grafo del codice RDG, l'arco 845 registra come `source` l'identificatore `ui_style_rationale_1` per la relazione che coinvolge il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.`. Nel grafo del codice RDG, l'arco 845 registra come `target` l'identificatore `ui_style` per la relazione che coinvolge il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 845 che coinvolge il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=423 id=Request-Dataset-Generator::ui_style_rationale_1 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `Costanti visuali per la UI CustomTkinter di RDG.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `ui/style.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `costanti visuali per la ui customtkinter di rdg.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `ui_style_rationale_1`. Nel grafo unificato, il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` possiede l'identificatore esatto `Request-Dataset-Generator::ui_style_rationale_1`.
Nel grafo unificato, il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=845 role=incoming -->
Nel grafo unificato, il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `style.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_style` all'ID `Request-Dataset-Generator::ui_style_rationale_1`. La relazione tecnica esplicita è la seguente: `style.py` costituisce una motivazione per `Costanti visuali per la UI CustomTkinter di RDG.`. Nel grafo unificato, il tipo dichiarato dell'arco 845 che coinvolge il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 845 che coinvolge il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 845 che coinvolge il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `ui/style.py`. Nel grafo unificato, la posizione sorgente dell'arco 845 che coinvolge il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 845 che coinvolge il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 845 che coinvolge il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 845 che coinvolge il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.` è `1.0`. Nel grafo unificato, l'arco 845 registra come `source` l'identificatore `Request-Dataset-Generator::ui_style` per la relazione che coinvolge il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.`. Nel grafo unificato, l'arco 845 registra come `target` l'identificatore `Request-Dataset-Generator::ui_style_rationale_1` per la relazione che coinvolge il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.`.

### Dashboard risultati RDG.

<!-- GRAPH_NODE graph=codice index=463 id=ui_tabs_dashboard_tab_rationale_1 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Dashboard risultati RDG.` è `Dashboard risultati RDG.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Dashboard risultati RDG.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Dashboard risultati RDG.` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Dashboard risultati RDG.` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Dashboard risultati RDG.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Dashboard risultati RDG.` possiede l'identificatore esatto `ui_tabs_dashboard_tab_rationale_1`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Dashboard risultati RDG.` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Dashboard risultati RDG.` è `dashboard risultati rdg.`.
<!-- GRAPH_EDGE graph=codice index=886 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Dashboard risultati RDG.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `dashboard_tab.py`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_rationale_1` all'ID `ui_tabs_dashboard_tab`. La relazione tecnica esplicita è la seguente: `Dashboard risultati RDG.` costituisce una motivazione per `dashboard_tab.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 886 che coinvolge il nodo canonico `Dashboard risultati RDG.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 886 che coinvolge il nodo canonico `Dashboard risultati RDG.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 886 che coinvolge il nodo canonico `Dashboard risultati RDG.` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 886 che coinvolge il nodo canonico `Dashboard risultati RDG.` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 886 che coinvolge il nodo canonico `Dashboard risultati RDG.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 886 che coinvolge il nodo canonico `Dashboard risultati RDG.` è `ast`. Nel grafo del codice RDG, l'arco 886 registra come `source` l'identificatore `ui_tabs_dashboard_tab_rationale_1` per la relazione che coinvolge il nodo canonico `Dashboard risultati RDG.`. Nel grafo del codice RDG, l'arco 886 registra come `target` l'identificatore `ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `Dashboard risultati RDG.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 886 che coinvolge il nodo canonico `Dashboard risultati RDG.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Dashboard risultati RDG.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=463 id=Request-Dataset-Generator::ui_tabs_dashboard_tab_rationale_1 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Dashboard risultati RDG.` è `Dashboard risultati RDG.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Dashboard risultati RDG.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Dashboard risultati RDG.` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Dashboard risultati RDG.` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Dashboard risultati RDG.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Dashboard risultati RDG.` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Dashboard risultati RDG.` è `dashboard risultati rdg.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Dashboard risultati RDG.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Dashboard risultati RDG.` è `ui_tabs_dashboard_tab_rationale_1`. Nel grafo unificato, il nodo canonico `Dashboard risultati RDG.` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_dashboard_tab_rationale_1`.
Nel grafo unificato, il nodo canonico `Dashboard risultati RDG.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=886 role=incoming -->
Nel grafo unificato, il nodo canonico `Dashboard risultati RDG.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `dashboard_tab.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_rationale_1`. La relazione tecnica esplicita è la seguente: `dashboard_tab.py` costituisce una motivazione per `Dashboard risultati RDG.`. Nel grafo unificato, il tipo dichiarato dell'arco 886 che coinvolge il nodo canonico `Dashboard risultati RDG.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 886 che coinvolge il nodo canonico `Dashboard risultati RDG.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 886 che coinvolge il nodo canonico `Dashboard risultati RDG.` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 886 che coinvolge il nodo canonico `Dashboard risultati RDG.` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 886 che coinvolge il nodo canonico `Dashboard risultati RDG.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 886 che coinvolge il nodo canonico `Dashboard risultati RDG.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 886 che coinvolge il nodo canonico `Dashboard risultati RDG.` è `1.0`. Nel grafo unificato, l'arco 886 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `Dashboard risultati RDG.`. Nel grafo unificato, l'arco 886 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_rationale_1` per la relazione che coinvolge il nodo canonico `Dashboard risultati RDG.`.

### dashboard_tab.py

<!-- GRAPH_NODE graph=codice index=445 id=ui_tabs_dashboard_tab -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `dashboard_tab.py` è `dashboard_tab.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `dashboard_tab.py` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `dashboard_tab.py` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `dashboard_tab.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `dashboard_tab.py` vale `ast`. Nel grafo del codice RDG, il nodo canonico `dashboard_tab.py` possiede l'identificatore esatto `ui_tabs_dashboard_tab`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `dashboard_tab.py` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `dashboard_tab.py` è `dashboard_tab.py`.
<!-- GRAPH_EDGE graph=codice index=847 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `dashboard_tab.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `style.py`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab` all'ID `ui_style`. La relazione tecnica esplicita è la seguente: `dashboard_tab.py` importa da `style.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 847 che coinvolge il nodo canonico `dashboard_tab.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 847 che coinvolge il nodo canonico `dashboard_tab.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 847 che coinvolge il nodo canonico `dashboard_tab.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 847 che coinvolge il nodo canonico `dashboard_tab.py` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 847 che coinvolge il nodo canonico `dashboard_tab.py` è `L12`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 847 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 847 che coinvolge il nodo canonico `dashboard_tab.py` è `ast`. Nel grafo del codice RDG, l'arco 847 registra come `source` l'identificatore `ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`. Nel grafo del codice RDG, l'arco 847 registra come `target` l'identificatore `ui_style` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 847 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=883 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `dashboard_tab.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab` all'ID `ui_tabs_dashboard_tab_dashboardtab`. La relazione tecnica esplicita è la seguente: `dashboard_tab.py` contiene `DashboardTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 883 che coinvolge il nodo canonico `dashboard_tab.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 883 che coinvolge il nodo canonico `dashboard_tab.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 883 che coinvolge il nodo canonico `dashboard_tab.py` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 883 che coinvolge il nodo canonico `dashboard_tab.py` è `L41`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 883 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 883 che coinvolge il nodo canonico `dashboard_tab.py` è `ast`. Nel grafo del codice RDG, l'arco 883 registra come `source` l'identificatore `ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`. Nel grafo del codice RDG, l'arco 883 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 883 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=884 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `dashboard_tab.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `ui_config_bridge.py`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab` all'ID `ui_ui_config_bridge`. La relazione tecnica esplicita è la seguente: `dashboard_tab.py` importa da `ui_config_bridge.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 884 che coinvolge il nodo canonico `dashboard_tab.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 884 che coinvolge il nodo canonico `dashboard_tab.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 884 che coinvolge il nodo canonico `dashboard_tab.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 884 che coinvolge il nodo canonico `dashboard_tab.py` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 884 che coinvolge il nodo canonico `dashboard_tab.py` è `L11`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 884 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 884 che coinvolge il nodo canonico `dashboard_tab.py` è `ast`. Nel grafo del codice RDG, l'arco 884 registra come `source` l'identificatore `ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`. Nel grafo del codice RDG, l'arco 884 registra come `target` l'identificatore `ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 884 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=885 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `dashboard_tab.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab` all'ID `ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `dashboard_tab.py` importa `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 885 che coinvolge il nodo canonico `dashboard_tab.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 885 che coinvolge il nodo canonico `dashboard_tab.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 885 che coinvolge il nodo canonico `dashboard_tab.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 885 che coinvolge il nodo canonico `dashboard_tab.py` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 885 che coinvolge il nodo canonico `dashboard_tab.py` è `L11`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 885 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 885 che coinvolge il nodo canonico `dashboard_tab.py` è `ast`. Nel grafo del codice RDG, l'arco 885 registra come `source` l'identificatore `ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`. Nel grafo del codice RDG, l'arco 885 registra come `target` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 885 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=825 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `dashboard_tab.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `app.py`; l'orientamento registrato nel JSON va dall'ID `ui_app` all'ID `ui_tabs_dashboard_tab`. La relazione tecnica esplicita è la seguente: `app.py` importa da `dashboard_tab.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 825 che coinvolge il nodo canonico `dashboard_tab.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 825 che coinvolge il nodo canonico `dashboard_tab.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 825 che coinvolge il nodo canonico `dashboard_tab.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 825 che coinvolge il nodo canonico `dashboard_tab.py` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 825 che coinvolge il nodo canonico `dashboard_tab.py` è `L27`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 825 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 825 che coinvolge il nodo canonico `dashboard_tab.py` è `ast`. Nel grafo del codice RDG, l'arco 825 registra come `source` l'identificatore `ui_app` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`. Nel grafo del codice RDG, l'arco 825 registra come `target` l'identificatore `ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 825 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=886 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `dashboard_tab.py` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Dashboard risultati RDG.`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_rationale_1` all'ID `ui_tabs_dashboard_tab`. La relazione tecnica esplicita è la seguente: `Dashboard risultati RDG.` costituisce una motivazione per `dashboard_tab.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 886 che coinvolge il nodo canonico `dashboard_tab.py` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 886 che coinvolge il nodo canonico `dashboard_tab.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 886 che coinvolge il nodo canonico `dashboard_tab.py` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 886 che coinvolge il nodo canonico `dashboard_tab.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 886 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 886 che coinvolge il nodo canonico `dashboard_tab.py` è `ast`. Nel grafo del codice RDG, l'arco 886 registra come `source` l'identificatore `ui_tabs_dashboard_tab_rationale_1` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`. Nel grafo del codice RDG, l'arco 886 registra come `target` l'identificatore `ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 886 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=445 id=Request-Dataset-Generator::ui_tabs_dashboard_tab -->
Nel grafo unificato, l'etichetta originale del nodo canonico `dashboard_tab.py` è `dashboard_tab.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `dashboard_tab.py` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `dashboard_tab.py` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `dashboard_tab.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `dashboard_tab.py` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `dashboard_tab.py` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `dashboard_tab.py` è `dashboard_tab.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `dashboard_tab.py` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `dashboard_tab.py` è `ui_tabs_dashboard_tab`. Nel grafo unificato, il nodo canonico `dashboard_tab.py` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_dashboard_tab`.
<!-- GRAPH_EDGE graph=unificata index=883 role=outgoing -->
Nel grafo unificato, il nodo canonico `dashboard_tab.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab`. La relazione tecnica esplicita è la seguente: `dashboard_tab.py` contiene `DashboardTab`. Nel grafo unificato, il tipo dichiarato dell'arco 883 che coinvolge il nodo canonico `dashboard_tab.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 883 che coinvolge il nodo canonico `dashboard_tab.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 883 che coinvolge il nodo canonico `dashboard_tab.py` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 883 che coinvolge il nodo canonico `dashboard_tab.py` è `L41`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 883 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 883 che coinvolge il nodo canonico `dashboard_tab.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 883 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`. Nel grafo unificato, l'arco 883 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`. Nel grafo unificato, l'arco 883 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`.
<!-- GRAPH_EDGE graph=unificata index=884 role=outgoing -->
Nel grafo unificato, il nodo canonico `dashboard_tab.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `ui_config_bridge.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab` all'ID `Request-Dataset-Generator::ui_ui_config_bridge`. La relazione tecnica esplicita è la seguente: `dashboard_tab.py` importa da `ui_config_bridge.py`. Nel grafo unificato, il tipo dichiarato dell'arco 884 che coinvolge il nodo canonico `dashboard_tab.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 884 che coinvolge il nodo canonico `dashboard_tab.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 884 che coinvolge il nodo canonico `dashboard_tab.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 884 che coinvolge il nodo canonico `dashboard_tab.py` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 884 che coinvolge il nodo canonico `dashboard_tab.py` è `L11`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 884 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 884 che coinvolge il nodo canonico `dashboard_tab.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 884 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`. Nel grafo unificato, l'arco 884 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`. Nel grafo unificato, l'arco 884 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`.
<!-- GRAPH_EDGE graph=unificata index=885 role=outgoing -->
Nel grafo unificato, il nodo canonico `dashboard_tab.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `dashboard_tab.py` importa `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 885 che coinvolge il nodo canonico `dashboard_tab.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 885 che coinvolge il nodo canonico `dashboard_tab.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 885 che coinvolge il nodo canonico `dashboard_tab.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 885 che coinvolge il nodo canonico `dashboard_tab.py` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 885 che coinvolge il nodo canonico `dashboard_tab.py` è `L11`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 885 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 885 che coinvolge il nodo canonico `dashboard_tab.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 885 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`. Nel grafo unificato, l'arco 885 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`. Nel grafo unificato, l'arco 885 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`.
<!-- GRAPH_EDGE graph=unificata index=886 role=outgoing -->
Nel grafo unificato, il nodo canonico `dashboard_tab.py` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Dashboard risultati RDG.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_rationale_1`. La relazione tecnica esplicita è la seguente: `dashboard_tab.py` costituisce una motivazione per `Dashboard risultati RDG.`. Nel grafo unificato, il tipo dichiarato dell'arco 886 che coinvolge il nodo canonico `dashboard_tab.py` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 886 che coinvolge il nodo canonico `dashboard_tab.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 886 che coinvolge il nodo canonico `dashboard_tab.py` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 886 che coinvolge il nodo canonico `dashboard_tab.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 886 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 886 che coinvolge il nodo canonico `dashboard_tab.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 886 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`. Nel grafo unificato, l'arco 886 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`. Nel grafo unificato, l'arco 886 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_rationale_1` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`.
<!-- GRAPH_EDGE graph=unificata index=825 role=incoming -->
Nel grafo unificato, il nodo canonico `dashboard_tab.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `app.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab`. La relazione tecnica esplicita è la seguente: `app.py` importa da `dashboard_tab.py`. Nel grafo unificato, il tipo dichiarato dell'arco 825 che coinvolge il nodo canonico `dashboard_tab.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 825 che coinvolge il nodo canonico `dashboard_tab.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 825 che coinvolge il nodo canonico `dashboard_tab.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 825 che coinvolge il nodo canonico `dashboard_tab.py` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 825 che coinvolge il nodo canonico `dashboard_tab.py` è `L27`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 825 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 825 che coinvolge il nodo canonico `dashboard_tab.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 825 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`. Nel grafo unificato, l'arco 825 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`. Nel grafo unificato, l'arco 825 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`.
<!-- GRAPH_EDGE graph=unificata index=847 role=incoming -->
Nel grafo unificato, il nodo canonico `dashboard_tab.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `style.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_style` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab`. La relazione tecnica esplicita è la seguente: `style.py` importa da `dashboard_tab.py`. Nel grafo unificato, il tipo dichiarato dell'arco 847 che coinvolge il nodo canonico `dashboard_tab.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 847 che coinvolge il nodo canonico `dashboard_tab.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 847 che coinvolge il nodo canonico `dashboard_tab.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 847 che coinvolge il nodo canonico `dashboard_tab.py` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 847 che coinvolge il nodo canonico `dashboard_tab.py` è `L12`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 847 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 847 che coinvolge il nodo canonico `dashboard_tab.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 847 che coinvolge il nodo canonico `dashboard_tab.py` è `1.0`. Nel grafo unificato, l'arco 847 registra come `source` l'identificatore `Request-Dataset-Generator::ui_style` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`. Nel grafo unificato, l'arco 847 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `dashboard_tab.py`.

### DashboardTab

<!-- GRAPH_NODE graph=codice index=446 id=ui_tabs_dashboard_tab_dashboardtab -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `DashboardTab` è `DashboardTab`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `DashboardTab` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `DashboardTab` è `L41`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `DashboardTab` vale `ast`. Nel grafo del codice RDG, il nodo canonico `DashboardTab` possiede l'identificatore esatto `ui_tabs_dashboard_tab_dashboardtab`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `DashboardTab` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `DashboardTab` è `dashboardtab`.
<!-- GRAPH_EDGE graph=codice index=887 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_build`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 887 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 887 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 887 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 887 che coinvolge il nodo canonico `DashboardTab` è `L54`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 887 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 887 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo del codice RDG, l'arco 887 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, l'arco 887 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 887 che coinvolge il nodo canonico `DashboardTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=888 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_chart()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_build_chart`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._build_chart()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 888 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 888 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 888 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 888 che coinvolge il nodo canonico `DashboardTab` è `L124`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 888 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 888 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo del codice RDG, l'arco 888 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, l'arco 888 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build_chart` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 888 che coinvolge il nodo canonico `DashboardTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=889 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_metric_card()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_build_metric_card`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._build_metric_card()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 889 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 889 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 889 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 889 che coinvolge il nodo canonico `DashboardTab` è `L67`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 889 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 889 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo del codice RDG, l'arco 889 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, l'arco 889 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build_metric_card` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 889 che coinvolge il nodo canonico `DashboardTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=890 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_report()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_build_report`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._build_report()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 890 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 890 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 890 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 890 che coinvolge il nodo canonico `DashboardTab` è `L148`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 890 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 890 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo del codice RDG, l'arco 890 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, l'arco 890 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_build_report` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 890 che coinvolge il nodo canonico `DashboardTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=891 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._chart_values()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_chart_values`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._chart_values()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 891 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 891 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 891 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 891 che coinvolge il nodo canonico `DashboardTab` è `L336`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 891 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 891 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo del codice RDG, l'arco 891 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, l'arco 891 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_chart_values` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 891 che coinvolge il nodo canonico `DashboardTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=892 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._configure_report_tags()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_configure_report_tags`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._configure_report_tags()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 892 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 892 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 892 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 892 che coinvolge il nodo canonico `DashboardTab` è `L354`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 892 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 892 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo del codice RDG, l'arco 892 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, l'arco 892 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_configure_report_tags` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 892 che coinvolge il nodo canonico `DashboardTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=893 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._draw_chart()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_draw_chart`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._draw_chart()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 893 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 893 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 893 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 893 che coinvolge il nodo canonico `DashboardTab` è `L242`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 893 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 893 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo del codice RDG, l'arco 893 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, l'arco 893 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_draw_chart` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 893 che coinvolge il nodo canonico `DashboardTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=894 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_init`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 894 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 894 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 894 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 894 che coinvolge il nodo canonico `DashboardTab` è `L44`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 894 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 894 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo del codice RDG, l'arco 894 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, l'arco 894 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_init` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 894 che coinvolge il nodo canonico `DashboardTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=895 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._populate_metrics()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_populate_metrics`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._populate_metrics()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 895 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 895 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 895 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 895 che coinvolge il nodo canonico `DashboardTab` è `L206`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 895 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 895 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo del codice RDG, l'arco 895 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, l'arco 895 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_populate_metrics` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 895 che coinvolge il nodo canonico `DashboardTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=896 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._read_metrics()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_read_metrics`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._read_metrics()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 896 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 896 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 896 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 896 che coinvolge il nodo canonico `DashboardTab` è `L306`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 896 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 896 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo del codice RDG, l'arco 896 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, l'arco 896 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_read_metrics` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 896 che coinvolge il nodo canonico `DashboardTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=897 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `.refresh_results()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_refresh_results`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `.refresh_results()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 897 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 897 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 897 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 897 che coinvolge il nodo canonico `DashboardTab` è `L200`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 897 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 897 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo del codice RDG, l'arco 897 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, l'arco 897 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 897 che coinvolge il nodo canonico `DashboardTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=898 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._render_report()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_render_report`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._render_report()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 898 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 898 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 898 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 898 che coinvolge il nodo canonico `DashboardTab` è `L278`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 898 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 898 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo del codice RDG, l'arco 898 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, l'arco 898 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_render_report` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 898 che coinvolge il nodo canonico `DashboardTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=899 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._report_path()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_report_path`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._report_path()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 899 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 899 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 899 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 899 che coinvolge il nodo canonico `DashboardTab` è `L323`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 899 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 899 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo del codice RDG, l'arco 899 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, l'arco 899 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_report_path` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 899 che coinvolge il nodo canonico `DashboardTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=900 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._set_metric()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab` all'ID `ui_tabs_dashboard_tab_dashboardtab_set_metric`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._set_metric()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 900 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 900 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 900 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 900 che coinvolge il nodo canonico `DashboardTab` è `L229`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 900 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 900 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo del codice RDG, l'arco 900 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, l'arco 900 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_set_metric` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 900 che coinvolge il nodo canonico `DashboardTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=826 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `DashboardTab` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `app.py`; l'orientamento registrato nel JSON va dall'ID `ui_app` all'ID `ui_tabs_dashboard_tab_dashboardtab`. La relazione tecnica esplicita è la seguente: `app.py` importa `DashboardTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 826 che coinvolge il nodo canonico `DashboardTab` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 826 che coinvolge il nodo canonico `DashboardTab` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 826 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 826 che coinvolge il nodo canonico `DashboardTab` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 826 che coinvolge il nodo canonico `DashboardTab` è `L27`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 826 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 826 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo del codice RDG, l'arco 826 registra come `source` l'identificatore `ui_app` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, l'arco 826 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 826 che coinvolge il nodo canonico `DashboardTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=837 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `DashboardTab` è il destinatario `target` della relazione `uses` proveniente dal nodo canonico `RDGApp`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp` all'ID `ui_tabs_dashboard_tab_dashboardtab`. La relazione tecnica esplicita è la seguente: `RDGApp` usa `DashboardTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 837 che coinvolge il nodo canonico `DashboardTab` è `uses`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 837 che coinvolge il nodo canonico `DashboardTab` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 837 che coinvolge il nodo canonico `DashboardTab` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 837 che coinvolge il nodo canonico `DashboardTab` è `L27`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 837 che coinvolge il nodo canonico `DashboardTab` è `0.8`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 837 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo del codice RDG, l'arco 837 registra come `source` l'identificatore `ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, l'arco 837 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 837 che coinvolge il nodo canonico `DashboardTab` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=843 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `DashboardTab` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build_tabs()`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp_build_tabs` all'ID `ui_tabs_dashboard_tab_dashboardtab`. La relazione tecnica esplicita è la seguente: `._build_tabs()` chiama `DashboardTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 843 che coinvolge il nodo canonico `DashboardTab` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 843 che coinvolge il nodo canonico `DashboardTab` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 843 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 843 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 843 che coinvolge il nodo canonico `DashboardTab` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 843 che coinvolge il nodo canonico `DashboardTab` è `L111`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 843 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 843 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo del codice RDG, l'arco 843 registra come `source` l'identificatore `ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, l'arco 843 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`.
<!-- GRAPH_EDGE graph=codice index=883 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `DashboardTab` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `dashboard_tab.py`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab` all'ID `ui_tabs_dashboard_tab_dashboardtab`. La relazione tecnica esplicita è la seguente: `dashboard_tab.py` contiene `DashboardTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 883 che coinvolge il nodo canonico `DashboardTab` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 883 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 883 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 883 che coinvolge il nodo canonico `DashboardTab` è `L41`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 883 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 883 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo del codice RDG, l'arco 883 registra come `source` l'identificatore `ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, l'arco 883 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 883 che coinvolge il nodo canonico `DashboardTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=901 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `DashboardTab` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Metriche, grafico latenza e report finale.`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_rationale_42` all'ID `ui_tabs_dashboard_tab_dashboardtab`. La relazione tecnica esplicita è la seguente: `Metriche, grafico latenza e report finale.` costituisce una motivazione per `DashboardTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 901 che coinvolge il nodo canonico `DashboardTab` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 901 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 901 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 901 che coinvolge il nodo canonico `DashboardTab` è `L42`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 901 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 901 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo del codice RDG, l'arco 901 registra come `source` l'identificatore `ui_tabs_dashboard_tab_rationale_42` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, l'arco 901 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 901 che coinvolge il nodo canonico `DashboardTab` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=446 id=Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab -->
Nel grafo unificato, l'etichetta originale del nodo canonico `DashboardTab` è `DashboardTab`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `DashboardTab` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `DashboardTab` è `L41`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `DashboardTab` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `DashboardTab` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `DashboardTab` è `dashboardtab`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `DashboardTab` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `DashboardTab` è `ui_tabs_dashboard_tab_dashboardtab`. Nel grafo unificato, il nodo canonico `DashboardTab` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab`.
<!-- GRAPH_EDGE graph=unificata index=887 role=outgoing -->
Nel grafo unificato, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._build() [ui_tabs_dashboard_tab_dashboardtab_build]`. Nel grafo unificato, il tipo dichiarato dell'arco 887 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 887 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 887 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 887 che coinvolge il nodo canonico `DashboardTab` è `L54`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 887 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 887 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 887 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, l'arco 887 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo unificato, l'arco 887 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build` per la relazione che coinvolge il nodo canonico `DashboardTab`.
<!-- GRAPH_EDGE graph=unificata index=888 role=outgoing -->
Nel grafo unificato, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_chart()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_chart`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._build_chart()`. Nel grafo unificato, il tipo dichiarato dell'arco 888 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 888 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 888 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 888 che coinvolge il nodo canonico `DashboardTab` è `L124`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 888 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 888 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 888 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, l'arco 888 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo unificato, l'arco 888 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_chart` per la relazione che coinvolge il nodo canonico `DashboardTab`.
<!-- GRAPH_EDGE graph=unificata index=889 role=outgoing -->
Nel grafo unificato, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_metric_card()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_metric_card`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._build_metric_card()`. Nel grafo unificato, il tipo dichiarato dell'arco 889 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 889 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 889 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 889 che coinvolge il nodo canonico `DashboardTab` è `L67`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 889 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 889 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 889 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, l'arco 889 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo unificato, l'arco 889 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_metric_card` per la relazione che coinvolge il nodo canonico `DashboardTab`.
<!-- GRAPH_EDGE graph=unificata index=890 role=outgoing -->
Nel grafo unificato, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_report()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_report`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._build_report()`. Nel grafo unificato, il tipo dichiarato dell'arco 890 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 890 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 890 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 890 che coinvolge il nodo canonico `DashboardTab` è `L148`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 890 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 890 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 890 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, l'arco 890 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo unificato, l'arco 890 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_build_report` per la relazione che coinvolge il nodo canonico `DashboardTab`.
<!-- GRAPH_EDGE graph=unificata index=891 role=outgoing -->
Nel grafo unificato, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._chart_values()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_chart_values`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._chart_values()`. Nel grafo unificato, il tipo dichiarato dell'arco 891 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 891 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 891 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 891 che coinvolge il nodo canonico `DashboardTab` è `L336`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 891 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 891 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 891 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, l'arco 891 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo unificato, l'arco 891 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_chart_values` per la relazione che coinvolge il nodo canonico `DashboardTab`.
<!-- GRAPH_EDGE graph=unificata index=892 role=outgoing -->
Nel grafo unificato, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._configure_report_tags()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_configure_report_tags`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._configure_report_tags()`. Nel grafo unificato, il tipo dichiarato dell'arco 892 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 892 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 892 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 892 che coinvolge il nodo canonico `DashboardTab` è `L354`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 892 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 892 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 892 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, l'arco 892 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo unificato, l'arco 892 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_configure_report_tags` per la relazione che coinvolge il nodo canonico `DashboardTab`.
<!-- GRAPH_EDGE graph=unificata index=893 role=outgoing -->
Nel grafo unificato, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._draw_chart()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_draw_chart`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._draw_chart()`. Nel grafo unificato, il tipo dichiarato dell'arco 893 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 893 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 893 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 893 che coinvolge il nodo canonico `DashboardTab` è `L242`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 893 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 893 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 893 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, l'arco 893 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo unificato, l'arco 893 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_draw_chart` per la relazione che coinvolge il nodo canonico `DashboardTab`.
<!-- GRAPH_EDGE graph=unificata index=894 role=outgoing -->
Nel grafo unificato, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_init`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `.__init__() [ui_tabs_dashboard_tab_dashboardtab_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 894 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 894 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 894 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 894 che coinvolge il nodo canonico `DashboardTab` è `L44`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 894 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 894 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 894 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, l'arco 894 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo unificato, l'arco 894 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_init` per la relazione che coinvolge il nodo canonico `DashboardTab`.
<!-- GRAPH_EDGE graph=unificata index=895 role=outgoing -->
Nel grafo unificato, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._populate_metrics()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_populate_metrics`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._populate_metrics()`. Nel grafo unificato, il tipo dichiarato dell'arco 895 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 895 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 895 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 895 che coinvolge il nodo canonico `DashboardTab` è `L206`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 895 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 895 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 895 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, l'arco 895 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo unificato, l'arco 895 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_populate_metrics` per la relazione che coinvolge il nodo canonico `DashboardTab`.
<!-- GRAPH_EDGE graph=unificata index=896 role=outgoing -->
Nel grafo unificato, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._read_metrics()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._read_metrics()`. Nel grafo unificato, il tipo dichiarato dell'arco 896 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 896 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 896 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 896 che coinvolge il nodo canonico `DashboardTab` è `L306`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 896 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 896 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 896 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, l'arco 896 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo unificato, l'arco 896 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics` per la relazione che coinvolge il nodo canonico `DashboardTab`.
<!-- GRAPH_EDGE graph=unificata index=897 role=outgoing -->
Nel grafo unificato, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `.refresh_results()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `.refresh_results()`. Nel grafo unificato, il tipo dichiarato dell'arco 897 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 897 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 897 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 897 che coinvolge il nodo canonico `DashboardTab` è `L200`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 897 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 897 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 897 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, l'arco 897 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo unificato, l'arco 897 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_refresh_results` per la relazione che coinvolge il nodo canonico `DashboardTab`.
<!-- GRAPH_EDGE graph=unificata index=898 role=outgoing -->
Nel grafo unificato, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._render_report()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_render_report`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._render_report()`. Nel grafo unificato, il tipo dichiarato dell'arco 898 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 898 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 898 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 898 che coinvolge il nodo canonico `DashboardTab` è `L278`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 898 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 898 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 898 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, l'arco 898 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo unificato, l'arco 898 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_render_report` per la relazione che coinvolge il nodo canonico `DashboardTab`.
<!-- GRAPH_EDGE graph=unificata index=899 role=outgoing -->
Nel grafo unificato, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._report_path()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_report_path`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._report_path()`. Nel grafo unificato, il tipo dichiarato dell'arco 899 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 899 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 899 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 899 che coinvolge il nodo canonico `DashboardTab` è `L323`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 899 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 899 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 899 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, l'arco 899 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo unificato, l'arco 899 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_report_path` per la relazione che coinvolge il nodo canonico `DashboardTab`.
<!-- GRAPH_EDGE graph=unificata index=900 role=outgoing -->
Nel grafo unificato, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._set_metric()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_set_metric`. La relazione tecnica esplicita è la seguente: `DashboardTab` registra come proprio metodo `._set_metric()`. Nel grafo unificato, il tipo dichiarato dell'arco 900 che coinvolge il nodo canonico `DashboardTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 900 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 900 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 900 che coinvolge il nodo canonico `DashboardTab` è `L229`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 900 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 900 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 900 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, l'arco 900 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo unificato, l'arco 900 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_set_metric` per la relazione che coinvolge il nodo canonico `DashboardTab`.
<!-- GRAPH_EDGE graph=unificata index=901 role=outgoing -->
Nel grafo unificato, il nodo canonico `DashboardTab` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Metriche, grafico latenza e report finale.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_rationale_42`. La relazione tecnica esplicita è la seguente: `DashboardTab` costituisce una motivazione per `Metriche, grafico latenza e report finale.`. Nel grafo unificato, il tipo dichiarato dell'arco 901 che coinvolge il nodo canonico `DashboardTab` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 901 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 901 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 901 che coinvolge il nodo canonico `DashboardTab` è `L42`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 901 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 901 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 901 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, l'arco 901 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo unificato, l'arco 901 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_rationale_42` per la relazione che coinvolge il nodo canonico `DashboardTab`.
<!-- GRAPH_EDGE graph=unificata index=826 role=incoming -->
Nel grafo unificato, il nodo canonico `DashboardTab` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `app.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab`. La relazione tecnica esplicita è la seguente: `app.py` importa `DashboardTab`. Nel grafo unificato, il tipo dichiarato dell'arco 826 che coinvolge il nodo canonico `DashboardTab` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 826 che coinvolge il nodo canonico `DashboardTab` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 826 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 826 che coinvolge il nodo canonico `DashboardTab` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 826 che coinvolge il nodo canonico `DashboardTab` è `L27`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 826 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 826 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 826 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, l'arco 826 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo unificato, l'arco 826 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`.
<!-- GRAPH_EDGE graph=unificata index=837 role=incoming -->
Nel grafo unificato, il nodo canonico `DashboardTab` è il destinatario `target` della relazione `uses` proveniente dal nodo canonico `RDGApp`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab`. La relazione tecnica esplicita è la seguente: `RDGApp` usa `DashboardTab`. Nel grafo unificato, il tipo dichiarato dell'arco 837 che coinvolge il nodo canonico `DashboardTab` è `uses`. Nel grafo unificato, la confidenza categoriale dell'arco 837 che coinvolge il nodo canonico `DashboardTab` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 837 che coinvolge il nodo canonico `DashboardTab` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 837 che coinvolge il nodo canonico `DashboardTab` è `L27`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 837 che coinvolge il nodo canonico `DashboardTab` è `0.8`. Nel grafo unificato, la provenienza `_origin` dell'arco 837 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 837 che coinvolge il nodo canonico `DashboardTab` è `0.5`. Nel grafo unificato, l'arco 837 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo unificato, l'arco 837 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`.
<!-- GRAPH_EDGE graph=unificata index=843 role=incoming -->
Nel grafo unificato, il nodo canonico `DashboardTab` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build_tabs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp_build_tabs` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab`. La relazione tecnica esplicita è la seguente: `._build_tabs()` chiama `DashboardTab`. Nel grafo unificato, il tipo dichiarato dell'arco 843 che coinvolge il nodo canonico `DashboardTab` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 843 che coinvolge il nodo canonico `DashboardTab` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 843 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 843 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 843 che coinvolge il nodo canonico `DashboardTab` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 843 che coinvolge il nodo canonico `DashboardTab` è `L111`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 843 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 843 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo unificato, l'arco 843 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo unificato, l'arco 843 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`.
<!-- GRAPH_EDGE graph=unificata index=883 role=incoming -->
Nel grafo unificato, il nodo canonico `DashboardTab` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `dashboard_tab.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab`. La relazione tecnica esplicita è la seguente: `dashboard_tab.py` contiene `DashboardTab`. Nel grafo unificato, il tipo dichiarato dell'arco 883 che coinvolge il nodo canonico `DashboardTab` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 883 che coinvolge il nodo canonico `DashboardTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 883 che coinvolge il nodo canonico `DashboardTab` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 883 che coinvolge il nodo canonico `DashboardTab` è `L41`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 883 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 883 che coinvolge il nodo canonico `DashboardTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 883 che coinvolge il nodo canonico `DashboardTab` è `1.0`. Nel grafo unificato, l'arco 883 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `DashboardTab`. Nel grafo unificato, l'arco 883 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `DashboardTab`.

### Entry point CustomTkinter per Request-Dataset-Generator.

<!-- GRAPH_NODE graph=codice index=420 id=ui_app_rationale_1 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `Entry point CustomTkinter per Request-Dataset-Generator.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` possiede l'identificatore esatto `ui_app_rationale_1`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `entry point customtkinter per request-dataset-generator.`.
<!-- GRAPH_EDGE graph=codice index=829 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `app.py`; l'orientamento registrato nel JSON va dall'ID `ui_app_rationale_1` all'ID `ui_app`. La relazione tecnica esplicita è la seguente: `Entry point CustomTkinter per Request-Dataset-Generator.` costituisce una motivazione per `app.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 829 che coinvolge il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 829 che coinvolge il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 829 che coinvolge il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 829 che coinvolge il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 829 che coinvolge il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 829 che coinvolge il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `ast`. Nel grafo del codice RDG, l'arco 829 registra come `source` l'identificatore `ui_app_rationale_1` per la relazione che coinvolge il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.`. Nel grafo del codice RDG, l'arco 829 registra come `target` l'identificatore `ui_app` per la relazione che coinvolge il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 829 che coinvolge il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=420 id=Request-Dataset-Generator::ui_app_rationale_1 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `Entry point CustomTkinter per Request-Dataset-Generator.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `ui/app.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `entry point customtkinter per request-dataset-generator.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `ui_app_rationale_1`. Nel grafo unificato, il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` possiede l'identificatore esatto `Request-Dataset-Generator::ui_app_rationale_1`.
Nel grafo unificato, il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=829 role=incoming -->
Nel grafo unificato, il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `app.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app` all'ID `Request-Dataset-Generator::ui_app_rationale_1`. La relazione tecnica esplicita è la seguente: `app.py` costituisce una motivazione per `Entry point CustomTkinter per Request-Dataset-Generator.`. Nel grafo unificato, il tipo dichiarato dell'arco 829 che coinvolge il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 829 che coinvolge il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 829 che coinvolge il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 829 che coinvolge il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 829 che coinvolge il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 829 che coinvolge il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 829 che coinvolge il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.` è `1.0`. Nel grafo unificato, l'arco 829 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app` per la relazione che coinvolge il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.`. Nel grafo unificato, l'arco 829 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_rationale_1` per la relazione che coinvolge il nodo canonico `Entry point CustomTkinter per Request-Dataset-Generator.`.

### load_config() [ui_ui_config_bridge_load_config]

<!-- GRAPH_NODE graph=codice index=496 id=ui_ui_config_bridge_load_config -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `load_config()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L15`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` possiede l'identificatore esatto `ui_ui_config_bridge_load_config`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `load_config()`.
<!-- GRAPH_EDGE graph=codice index=990 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_defaults()`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge_load_config` all'ID `ui_ui_config_bridge_defaults`. La relazione tecnica esplicita è la seguente: `load_config() [ui_ui_config_bridge_load_config]` chiama `_defaults()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 990 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 990 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 990 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 990 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 990 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L18`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 990 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 990 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo del codice RDG, l'arco 990 registra come `source` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, l'arco 990 registra come `target` l'identificatore `ui_ui_config_bridge_defaults` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 990 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=991 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [ui_ui_config_bridge_py_any]`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge_load_config` all'ID `ui_ui_config_bridge_py_any`. La relazione tecnica esplicita è la seguente: `load_config() [ui_ui_config_bridge_load_config]` fa riferimento a `Any [ui_ui_config_bridge_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 991 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 991 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 991 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 991 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 991 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 991 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 991 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo del codice RDG, l'arco 991 registra come `source` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, l'arco 991 registra come `target` l'identificatore `ui_ui_config_bridge_py_any` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 991 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=992 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è la sorgente `source` della relazione `references` verso il nodo canonico `Path [ui_ui_config_bridge_py_path]`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge_load_config` all'ID `ui_ui_config_bridge_py_path`. La relazione tecnica esplicita è la seguente: `load_config() [ui_ui_config_bridge_load_config]` fa riferimento a `Path [ui_ui_config_bridge_py_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 992 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 992 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 992 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 992 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 992 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 992 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 992 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo del codice RDG, l'arco 992 registra come `source` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, l'arco 992 registra come `target` l'identificatore `ui_ui_config_bridge_py_path` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 992 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=885 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `dashboard_tab.py`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab` all'ID `ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `dashboard_tab.py` importa `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 885 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 885 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 885 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 885 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 885 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L11`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 885 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 885 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo del codice RDG, l'arco 885 registra come `source` l'identificatore `ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, l'arco 885 registra come `target` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 885 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=921 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._read_metrics()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_read_metrics` all'ID `ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `._read_metrics()` chiama `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 921 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 921 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 921 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 921 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 921 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 921 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L307`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 921 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 921 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo del codice RDG, l'arco 921 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_read_metrics` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, l'arco 921 registra come `target` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`.
<!-- GRAPH_EDGE graph=codice index=923 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._report_path()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_report_path` all'ID `ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `._report_path()` chiama `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 923 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 923 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 923 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 923 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 923 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 923 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L324`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 923 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 923 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo del codice RDG, l'arco 923 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_report_path` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, l'arco 923 registra come `target` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`.
<!-- GRAPH_EDGE graph=codice index=926 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `run_tab.py`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab` all'ID `ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `run_tab.py` importa `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 926 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 926 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 926 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 926 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 926 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 926 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 926 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo del codice RDG, l'arco 926 registra come `source` l'identificatore `ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, l'arco 926 registra come `target` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 926 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=978 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._run_step_1()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_run_step_1` all'ID `ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `._run_step_1()` chiama `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 978 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 978 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 978 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 978 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 978 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 978 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L347`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 978 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 978 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo del codice RDG, l'arco 978 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_run_step_1` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, l'arco 978 registra come `target` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`.
<!-- GRAPH_EDGE graph=codice index=980 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._run_step_2()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_run_step_2` all'ID `ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `._run_step_2()` chiama `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 980 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 980 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 980 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 980 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 980 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 980 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L354`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 980 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 980 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo del codice RDG, l'arco 980 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_run_step_2` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, l'arco 980 registra come `target` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`.
<!-- GRAPH_EDGE graph=codice index=985 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._write_mock_outputs()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_write_mock_outputs` all'ID `ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `._write_mock_outputs()` chiama `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 985 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 985 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 985 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 985 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 985 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 985 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L445`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 985 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 985 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo del codice RDG, l'arco 985 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_write_mock_outputs` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, l'arco 985 registra come `target` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`.
<!-- GRAPH_EDGE graph=codice index=987 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `ui_config_bridge.py`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge` all'ID `ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `ui_config_bridge.py` contiene `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 987 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 987 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 987 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 987 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 987 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 987 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo del codice RDG, l'arco 987 registra come `source` l'identificatore `ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, l'arco 987 registra come `target` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 987 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=993 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge_rationale_16` all'ID `ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `Carica il file json. Se non esiste, o non e' valido, restituisce i default.` costituisce una motivazione per `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 993 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 993 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 993 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 993 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 993 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 993 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo del codice RDG, l'arco 993 registra come `source` l'identificatore `ui_ui_config_bridge_rationale_16` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, l'arco 993 registra come `target` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 993 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=496 id=Request-Dataset-Generator::ui_ui_config_bridge_load_config -->
Nel grafo unificato, l'etichetta originale del nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `load_config()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L15`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `load_config()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui_ui_config_bridge_load_config`. Nel grafo unificato, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` possiede l'identificatore esatto `Request-Dataset-Generator::ui_ui_config_bridge_load_config`.
<!-- GRAPH_EDGE graph=unificata index=990 role=outgoing -->
Nel grafo unificato, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_defaults()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_defaults`. La relazione tecnica esplicita è la seguente: `load_config() [ui_ui_config_bridge_load_config]` chiama `_defaults()`. Nel grafo unificato, il tipo dichiarato dell'arco 990 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 990 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 990 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 990 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 990 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L18`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 990 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 990 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 990 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, l'arco 990 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, l'arco 990 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_defaults` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`.
<!-- GRAPH_EDGE graph=unificata index=991 role=outgoing -->
Nel grafo unificato, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [ui_ui_config_bridge_py_any]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_py_any`. La relazione tecnica esplicita è la seguente: `load_config() [ui_ui_config_bridge_load_config]` fa riferimento a `Any [ui_ui_config_bridge_py_any]`. Nel grafo unificato, il tipo dichiarato dell'arco 991 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 991 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 991 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 991 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 991 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 991 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 991 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 991 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, l'arco 991 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, l'arco 991 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_py_any` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`.
<!-- GRAPH_EDGE graph=unificata index=992 role=outgoing -->
Nel grafo unificato, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è la sorgente `source` della relazione `references` verso il nodo canonico `Path [ui_ui_config_bridge_py_path]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_py_path`. La relazione tecnica esplicita è la seguente: `load_config() [ui_ui_config_bridge_load_config]` fa riferimento a `Path [ui_ui_config_bridge_py_path]`. Nel grafo unificato, il tipo dichiarato dell'arco 992 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 992 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 992 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 992 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 992 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 992 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 992 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 992 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, l'arco 992 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, l'arco 992 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_py_path` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`.
<!-- GRAPH_EDGE graph=unificata index=993 role=outgoing -->
Nel grafo unificato, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Carica il file json. Se non esiste, o non e' valido, restituisce i default.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_rationale_16`. La relazione tecnica esplicita è la seguente: `load_config() [ui_ui_config_bridge_load_config]` costituisce una motivazione per `Carica il file json. Se non esiste, o non e' valido, restituisce i default.`. Nel grafo unificato, il tipo dichiarato dell'arco 993 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 993 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 993 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 993 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L16`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 993 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 993 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 993 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, l'arco 993 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, l'arco 993 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_rationale_16` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`.
<!-- GRAPH_EDGE graph=unificata index=885 role=incoming -->
Nel grafo unificato, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `dashboard_tab.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `dashboard_tab.py` importa `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 885 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 885 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 885 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 885 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 885 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L11`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 885 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 885 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 885 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, l'arco 885 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, l'arco 885 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`.
<!-- GRAPH_EDGE graph=unificata index=921 role=incoming -->
Nel grafo unificato, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._read_metrics()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `._read_metrics()` chiama `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 921 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 921 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 921 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 921 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 921 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 921 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L307`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 921 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 921 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo unificato, l'arco 921 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, l'arco 921 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`.
<!-- GRAPH_EDGE graph=unificata index=923 role=incoming -->
Nel grafo unificato, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._report_path()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_report_path` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `._report_path()` chiama `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 923 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 923 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 923 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 923 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 923 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 923 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L324`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 923 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 923 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo unificato, l'arco 923 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_report_path` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, l'arco 923 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`.
<!-- GRAPH_EDGE graph=unificata index=926 role=incoming -->
Nel grafo unificato, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `run_tab.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `run_tab.py` importa `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 926 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 926 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 926 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 926 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 926 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 926 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 926 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 926 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, l'arco 926 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, l'arco 926 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`.
<!-- GRAPH_EDGE graph=unificata index=978 role=incoming -->
Nel grafo unificato, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._run_step_1()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_1` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `._run_step_1()` chiama `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 978 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 978 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 978 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 978 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 978 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 978 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L347`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 978 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 978 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo unificato, l'arco 978 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_1` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, l'arco 978 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`.
<!-- GRAPH_EDGE graph=unificata index=980 role=incoming -->
Nel grafo unificato, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._run_step_2()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_2` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `._run_step_2()` chiama `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 980 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 980 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 980 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 980 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 980 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 980 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L354`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 980 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 980 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo unificato, l'arco 980 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_2` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, l'arco 980 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`.
<!-- GRAPH_EDGE graph=unificata index=985 role=incoming -->
Nel grafo unificato, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._write_mock_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_write_mock_outputs` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `._write_mock_outputs()` chiama `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 985 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 985 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 985 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 985 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 985 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 985 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L445`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 985 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 985 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo unificato, l'arco 985 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_write_mock_outputs` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, l'arco 985 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`.
<!-- GRAPH_EDGE graph=unificata index=987 role=incoming -->
Nel grafo unificato, il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `ui_config_bridge.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `ui_config_bridge.py` contiene `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 987 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 987 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 987 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 987 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 987 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 987 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 987 che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]` è `1.0`. Nel grafo unificato, l'arco 987 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, l'arco 987 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`.

### main() [ui_app_main]

<!-- GRAPH_NODE graph=codice index=419 id=ui_app_main -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `main() [ui_app_main]` è `main()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `main() [ui_app_main]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `main() [ui_app_main]` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `main() [ui_app_main]` è `L136`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `main() [ui_app_main]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `main() [ui_app_main]` possiede l'identificatore esatto `ui_app_main`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `main() [ui_app_main]` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `main() [ui_app_main]` è `main()`.
<!-- GRAPH_EDGE graph=codice index=830 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main() [ui_app_main]` è la sorgente `source` della relazione `calls` verso il nodo canonico `RDGApp`; l'orientamento registrato nel JSON va dall'ID `ui_app_main` all'ID `ui_app_rdgapp`. La relazione tecnica esplicita è la seguente: `main() [ui_app_main]` chiama `RDGApp`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 830 che coinvolge il nodo canonico `main() [ui_app_main]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 830 che coinvolge il nodo canonico `main() [ui_app_main]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 830 che coinvolge il nodo canonico `main() [ui_app_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 830 che coinvolge il nodo canonico `main() [ui_app_main]` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 830 che coinvolge il nodo canonico `main() [ui_app_main]` è `L137`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 830 che coinvolge il nodo canonico `main() [ui_app_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 830 che coinvolge il nodo canonico `main() [ui_app_main]` è `ast`. Nel grafo del codice RDG, l'arco 830 registra come `source` l'identificatore `ui_app_main` per la relazione che coinvolge il nodo canonico `main() [ui_app_main]`. Nel grafo del codice RDG, l'arco 830 registra come `target` l'identificatore `ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `main() [ui_app_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 830 che coinvolge il nodo canonico `main() [ui_app_main]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=820 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `main() [ui_app_main]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `app.py`; l'orientamento registrato nel JSON va dall'ID `ui_app` all'ID `ui_app_main`. La relazione tecnica esplicita è la seguente: `app.py` contiene `main() [ui_app_main]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 820 che coinvolge il nodo canonico `main() [ui_app_main]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 820 che coinvolge il nodo canonico `main() [ui_app_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 820 che coinvolge il nodo canonico `main() [ui_app_main]` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 820 che coinvolge il nodo canonico `main() [ui_app_main]` è `L136`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 820 che coinvolge il nodo canonico `main() [ui_app_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 820 che coinvolge il nodo canonico `main() [ui_app_main]` è `ast`. Nel grafo del codice RDG, l'arco 820 registra come `source` l'identificatore `ui_app` per la relazione che coinvolge il nodo canonico `main() [ui_app_main]`. Nel grafo del codice RDG, l'arco 820 registra come `target` l'identificatore `ui_app_main` per la relazione che coinvolge il nodo canonico `main() [ui_app_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 820 che coinvolge il nodo canonico `main() [ui_app_main]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=419 id=Request-Dataset-Generator::ui_app_main -->
Nel grafo unificato, l'etichetta originale del nodo canonico `main() [ui_app_main]` è `main()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `main() [ui_app_main]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `main() [ui_app_main]` è `ui/app.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `main() [ui_app_main]` è `L136`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `main() [ui_app_main]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `main() [ui_app_main]` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `main() [ui_app_main]` è `main()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `main() [ui_app_main]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `main() [ui_app_main]` è `ui_app_main`. Nel grafo unificato, il nodo canonico `main() [ui_app_main]` possiede l'identificatore esatto `Request-Dataset-Generator::ui_app_main`.
Nel grafo unificato, il nodo canonico `main() [ui_app_main]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=820 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [ui_app_main]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `app.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app` all'ID `Request-Dataset-Generator::ui_app_main`. La relazione tecnica esplicita è la seguente: `app.py` contiene `main() [ui_app_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 820 che coinvolge il nodo canonico `main() [ui_app_main]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 820 che coinvolge il nodo canonico `main() [ui_app_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 820 che coinvolge il nodo canonico `main() [ui_app_main]` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 820 che coinvolge il nodo canonico `main() [ui_app_main]` è `L136`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 820 che coinvolge il nodo canonico `main() [ui_app_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 820 che coinvolge il nodo canonico `main() [ui_app_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 820 che coinvolge il nodo canonico `main() [ui_app_main]` è `1.0`. Nel grafo unificato, l'arco 820 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app` per la relazione che coinvolge il nodo canonico `main() [ui_app_main]`. Nel grafo unificato, l'arco 820 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_main` per la relazione che coinvolge il nodo canonico `main() [ui_app_main]`.
<!-- GRAPH_EDGE graph=unificata index=830 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [ui_app_main]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `RDGApp`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp` all'ID `Request-Dataset-Generator::ui_app_main`. La relazione tecnica esplicita è la seguente: `RDGApp` chiama `main() [ui_app_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 830 che coinvolge il nodo canonico `main() [ui_app_main]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 830 che coinvolge il nodo canonico `main() [ui_app_main]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 830 che coinvolge il nodo canonico `main() [ui_app_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 830 che coinvolge il nodo canonico `main() [ui_app_main]` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 830 che coinvolge il nodo canonico `main() [ui_app_main]` è `L137`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 830 che coinvolge il nodo canonico `main() [ui_app_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 830 che coinvolge il nodo canonico `main() [ui_app_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 830 che coinvolge il nodo canonico `main() [ui_app_main]` è `1.0`. Nel grafo unificato, l'arco 830 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `main() [ui_app_main]`. Nel grafo unificato, l'arco 830 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_main` per la relazione che coinvolge il nodo canonico `main() [ui_app_main]`.

### Metriche, grafico latenza e report finale.

<!-- GRAPH_NODE graph=codice index=464 id=ui_tabs_dashboard_tab_rationale_42 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Metriche, grafico latenza e report finale.` è `Metriche, grafico latenza e report finale.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Metriche, grafico latenza e report finale.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Metriche, grafico latenza e report finale.` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Metriche, grafico latenza e report finale.` è `L42`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Metriche, grafico latenza e report finale.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Metriche, grafico latenza e report finale.` possiede l'identificatore esatto `ui_tabs_dashboard_tab_rationale_42`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Metriche, grafico latenza e report finale.` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Metriche, grafico latenza e report finale.` è `metriche, grafico latenza e report finale.`.
<!-- GRAPH_EDGE graph=codice index=901 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Metriche, grafico latenza e report finale.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_rationale_42` all'ID `ui_tabs_dashboard_tab_dashboardtab`. La relazione tecnica esplicita è la seguente: `Metriche, grafico latenza e report finale.` costituisce una motivazione per `DashboardTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 901 che coinvolge il nodo canonico `Metriche, grafico latenza e report finale.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 901 che coinvolge il nodo canonico `Metriche, grafico latenza e report finale.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 901 che coinvolge il nodo canonico `Metriche, grafico latenza e report finale.` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 901 che coinvolge il nodo canonico `Metriche, grafico latenza e report finale.` è `L42`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 901 che coinvolge il nodo canonico `Metriche, grafico latenza e report finale.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 901 che coinvolge il nodo canonico `Metriche, grafico latenza e report finale.` è `ast`. Nel grafo del codice RDG, l'arco 901 registra come `source` l'identificatore `ui_tabs_dashboard_tab_rationale_42` per la relazione che coinvolge il nodo canonico `Metriche, grafico latenza e report finale.`. Nel grafo del codice RDG, l'arco 901 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `Metriche, grafico latenza e report finale.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 901 che coinvolge il nodo canonico `Metriche, grafico latenza e report finale.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Metriche, grafico latenza e report finale.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=464 id=Request-Dataset-Generator::ui_tabs_dashboard_tab_rationale_42 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Metriche, grafico latenza e report finale.` è `Metriche, grafico latenza e report finale.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Metriche, grafico latenza e report finale.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Metriche, grafico latenza e report finale.` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Metriche, grafico latenza e report finale.` è `L42`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Metriche, grafico latenza e report finale.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Metriche, grafico latenza e report finale.` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Metriche, grafico latenza e report finale.` è `metriche, grafico latenza e report finale.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Metriche, grafico latenza e report finale.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Metriche, grafico latenza e report finale.` è `ui_tabs_dashboard_tab_rationale_42`. Nel grafo unificato, il nodo canonico `Metriche, grafico latenza e report finale.` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_dashboard_tab_rationale_42`.
Nel grafo unificato, il nodo canonico `Metriche, grafico latenza e report finale.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=901 role=incoming -->
Nel grafo unificato, il nodo canonico `Metriche, grafico latenza e report finale.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_rationale_42`. La relazione tecnica esplicita è la seguente: `DashboardTab` costituisce una motivazione per `Metriche, grafico latenza e report finale.`. Nel grafo unificato, il tipo dichiarato dell'arco 901 che coinvolge il nodo canonico `Metriche, grafico latenza e report finale.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 901 che coinvolge il nodo canonico `Metriche, grafico latenza e report finale.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 901 che coinvolge il nodo canonico `Metriche, grafico latenza e report finale.` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 901 che coinvolge il nodo canonico `Metriche, grafico latenza e report finale.` è `L42`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 901 che coinvolge il nodo canonico `Metriche, grafico latenza e report finale.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 901 che coinvolge il nodo canonico `Metriche, grafico latenza e report finale.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 901 che coinvolge il nodo canonico `Metriche, grafico latenza e report finale.` è `1.0`. Nel grafo unificato, l'arco 901 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `Metriche, grafico latenza e report finale.`. Nel grafo unificato, l'arco 901 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_rationale_42` per la relazione che coinvolge il nodo canonico `Metriche, grafico latenza e report finale.`.

### Path [ui_tabs_dashboard_tab_py_path]

<!-- GRAPH_NODE graph=codice index=460 id=ui_tabs_dashboard_tab_py_path -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `Path`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è ``. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` possiede l'identificatore esatto `ui_tabs_dashboard_tab_py_path`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `path`.
Nel grafo del codice RDG, il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=920 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._read_metrics()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_read_metrics` all'ID `ui_tabs_dashboard_tab_py_path`. La relazione tecnica esplicita è la seguente: `._read_metrics()` chiama `Path [ui_tabs_dashboard_tab_py_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 920 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 920 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 920 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 920 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 920 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `L310`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 920 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 920 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `ast`. Nel grafo del codice RDG, l'arco 920 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_read_metrics` per la relazione che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]`. Nel grafo del codice RDG, l'arco 920 registra come `target` l'identificatore `ui_tabs_dashboard_tab_py_path` per la relazione che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 920 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=922 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `._report_path()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab_dashboardtab_report_path` all'ID `ui_tabs_dashboard_tab_py_path`. La relazione tecnica esplicita è la seguente: `._report_path()` fa riferimento a `Path [ui_tabs_dashboard_tab_py_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 922 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 922 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `return_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 922 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 922 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 922 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `L323`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 922 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 922 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `ast`. Nel grafo del codice RDG, l'arco 922 registra come `source` l'identificatore `ui_tabs_dashboard_tab_dashboardtab_report_path` per la relazione che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]`. Nel grafo del codice RDG, l'arco 922 registra come `target` l'identificatore `ui_tabs_dashboard_tab_py_path` per la relazione che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 922 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=460 id=Request-Dataset-Generator::ui_tabs_dashboard_tab_py_path -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `Path`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è ``. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `path`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `ui_tabs_dashboard_tab_py_path`. Nel grafo unificato, il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_path`.
Nel grafo unificato, il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=920 role=incoming -->
Nel grafo unificato, il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._read_metrics()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_path`. La relazione tecnica esplicita è la seguente: `._read_metrics()` chiama `Path [ui_tabs_dashboard_tab_py_path]`. Nel grafo unificato, il tipo dichiarato dell'arco 920 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 920 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 920 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 920 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 920 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `L310`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 920 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 920 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 920 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `1.0`. Nel grafo unificato, l'arco 920 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_read_metrics` per la relazione che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]`. Nel grafo unificato, l'arco 920 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_path` per la relazione che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]`.
<!-- GRAPH_EDGE graph=unificata index=922 role=incoming -->
Nel grafo unificato, il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `._report_path()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_report_path` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_path`. La relazione tecnica esplicita è la seguente: `._report_path()` fa riferimento a `Path [ui_tabs_dashboard_tab_py_path]`. Nel grafo unificato, il tipo dichiarato dell'arco 922 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 922 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `return_type`. Nel grafo unificato, la confidenza categoriale dell'arco 922 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 922 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 922 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `L323`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 922 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 922 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 922 che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]` è `1.0`. Nel grafo unificato, l'arco 922 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab_report_path` per la relazione che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]`. Nel grafo unificato, l'arco 922 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_py_path` per la relazione che coinvolge il nodo canonico `Path [ui_tabs_dashboard_tab_py_path]`.

### Path [ui_ui_config_bridge_py_path]

<!-- GRAPH_NODE graph=codice index=497 id=ui_ui_config_bridge_py_path -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Path [ui_ui_config_bridge_py_path]` è `Path`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Path [ui_ui_config_bridge_py_path]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Path [ui_ui_config_bridge_py_path]` è ``. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Path [ui_ui_config_bridge_py_path]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Path [ui_ui_config_bridge_py_path]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Path [ui_ui_config_bridge_py_path]` possiede l'identificatore esatto `ui_ui_config_bridge_py_path`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Path [ui_ui_config_bridge_py_path]` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Path [ui_ui_config_bridge_py_path]` è `path`.
Nel grafo del codice RDG, il nodo canonico `Path [ui_ui_config_bridge_py_path]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=992 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Path [ui_ui_config_bridge_py_path]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge_load_config` all'ID `ui_ui_config_bridge_py_path`. La relazione tecnica esplicita è la seguente: `load_config() [ui_ui_config_bridge_load_config]` fa riferimento a `Path [ui_ui_config_bridge_py_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 992 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 992 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 992 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 992 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 992 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 992 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 992 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `ast`. Nel grafo del codice RDG, l'arco 992 registra come `source` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]`. Nel grafo del codice RDG, l'arco 992 registra come `target` l'identificatore `ui_ui_config_bridge_py_path` per la relazione che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 992 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=994 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Path [ui_ui_config_bridge_py_path]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `save_config()`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge_save_config` all'ID `ui_ui_config_bridge_py_path`. La relazione tecnica esplicita è la seguente: `save_config()` fa riferimento a `Path [ui_ui_config_bridge_py_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 994 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 994 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 994 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 994 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 994 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `L34`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 994 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 994 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `ast`. Nel grafo del codice RDG, l'arco 994 registra come `source` l'identificatore `ui_ui_config_bridge_save_config` per la relazione che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]`. Nel grafo del codice RDG, l'arco 994 registra come `target` l'identificatore `ui_ui_config_bridge_py_path` per la relazione che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 994 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=497 id=Request-Dataset-Generator::ui_ui_config_bridge_py_path -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Path [ui_ui_config_bridge_py_path]` è `Path`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Path [ui_ui_config_bridge_py_path]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Path [ui_ui_config_bridge_py_path]` è ``. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Path [ui_ui_config_bridge_py_path]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Path [ui_ui_config_bridge_py_path]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Path [ui_ui_config_bridge_py_path]` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Path [ui_ui_config_bridge_py_path]` è `path`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Path [ui_ui_config_bridge_py_path]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `ui_ui_config_bridge_py_path`. Nel grafo unificato, il nodo canonico `Path [ui_ui_config_bridge_py_path]` possiede l'identificatore esatto `Request-Dataset-Generator::ui_ui_config_bridge_py_path`.
<!-- GRAPH_EDGE graph=unificata index=994 role=outgoing -->
Nel grafo unificato, il nodo canonico `Path [ui_ui_config_bridge_py_path]` è la sorgente `source` della relazione `references` verso il nodo canonico `save_config()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge_py_path` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_save_config`. La relazione tecnica esplicita è la seguente: `Path [ui_ui_config_bridge_py_path]` fa riferimento a `save_config()`. Nel grafo unificato, il tipo dichiarato dell'arco 994 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 994 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 994 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 994 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 994 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `L34`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 994 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 994 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 994 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `1.0`. Nel grafo unificato, l'arco 994 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_py_path` per la relazione che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]`. Nel grafo unificato, l'arco 994 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_save_config` per la relazione che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]`.
<!-- GRAPH_EDGE graph=unificata index=992 role=incoming -->
Nel grafo unificato, il nodo canonico `Path [ui_ui_config_bridge_py_path]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_py_path`. La relazione tecnica esplicita è la seguente: `load_config() [ui_ui_config_bridge_load_config]` fa riferimento a `Path [ui_ui_config_bridge_py_path]`. Nel grafo unificato, il tipo dichiarato dell'arco 992 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 992 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 992 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 992 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 992 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 992 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 992 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 992 che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]` è `1.0`. Nel grafo unificato, l'arco 992 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]`. Nel grafo unificato, l'arco 992 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_py_path` per la relazione che coinvolge il nodo canonico `Path [ui_ui_config_bridge_py_path]`.

### RDGApp

<!-- GRAPH_NODE graph=codice index=414 id=ui_app_rdgapp -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `RDGApp` è `RDGApp`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `RDGApp` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `RDGApp` è `L31`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `RDGApp` vale `ast`. Nel grafo del codice RDG, il nodo canonico `RDGApp` possiede l'identificatore esatto `ui_app_rdgapp`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `RDGApp` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `RDGApp` è `rdgapp`.
<!-- GRAPH_EDGE graph=codice index=832 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RDGApp` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_tabs()`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp` all'ID `ui_app_rdgapp_build_tabs`. La relazione tecnica esplicita è la seguente: `RDGApp` registra come proprio metodo `._build_tabs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 832 che coinvolge il nodo canonico `RDGApp` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 832 che coinvolge il nodo canonico `RDGApp` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 832 che coinvolge il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 832 che coinvolge il nodo canonico `RDGApp` è `L85`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 832 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 832 che coinvolge il nodo canonico `RDGApp` è `ast`. Nel grafo del codice RDG, l'arco 832 registra come `source` l'identificatore `ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo del codice RDG, l'arco 832 registra come `target` l'identificatore `ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 832 che coinvolge il nodo canonico `RDGApp` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=833 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RDGApp` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_titlebar()`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp` all'ID `ui_app_rdgapp_build_titlebar`. La relazione tecnica esplicita è la seguente: `RDGApp` registra come proprio metodo `._build_titlebar()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 833 che coinvolge il nodo canonico `RDGApp` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 833 che coinvolge il nodo canonico `RDGApp` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 833 che coinvolge il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 833 che coinvolge il nodo canonico `RDGApp` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 833 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 833 che coinvolge il nodo canonico `RDGApp` è `ast`. Nel grafo del codice RDG, l'arco 833 registra come `source` l'identificatore `ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo del codice RDG, l'arco 833 registra come `target` l'identificatore `ui_app_rdgapp_build_titlebar` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 833 che coinvolge il nodo canonico `RDGApp` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=834 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RDGApp` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [ui_app_rdgapp_init]`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp` all'ID `ui_app_rdgapp_init`. La relazione tecnica esplicita è la seguente: `RDGApp` registra come proprio metodo `.__init__() [ui_app_rdgapp_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 834 che coinvolge il nodo canonico `RDGApp` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 834 che coinvolge il nodo canonico `RDGApp` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 834 che coinvolge il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 834 che coinvolge il nodo canonico `RDGApp` è `L36`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 834 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 834 che coinvolge il nodo canonico `RDGApp` è `ast`. Nel grafo del codice RDG, l'arco 834 registra come `source` l'identificatore `ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo del codice RDG, l'arco 834 registra come `target` l'identificatore `ui_app_rdgapp_init` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 834 che coinvolge il nodo canonico `RDGApp` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=835 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RDGApp` è la sorgente `source` della relazione `method` verso il nodo canonico `._style_segmented_tabs()`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp` all'ID `ui_app_rdgapp_style_segmented_tabs`. La relazione tecnica esplicita è la seguente: `RDGApp` registra come proprio metodo `._style_segmented_tabs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 835 che coinvolge il nodo canonico `RDGApp` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 835 che coinvolge il nodo canonico `RDGApp` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 835 che coinvolge il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 835 che coinvolge il nodo canonico `RDGApp` è `L126`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 835 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 835 che coinvolge il nodo canonico `RDGApp` è `ast`. Nel grafo del codice RDG, l'arco 835 registra come `source` l'identificatore `ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo del codice RDG, l'arco 835 registra come `target` l'identificatore `ui_app_rdgapp_style_segmented_tabs` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 835 che coinvolge il nodo canonico `RDGApp` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=836 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RDGApp` è la sorgente `source` della relazione `uses` verso il nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp` all'ID `ui_tabs_config_tab_configtab`. La relazione tecnica esplicita è la seguente: `RDGApp` usa `ConfigTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 836 che coinvolge il nodo canonico `RDGApp` è `uses`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 836 che coinvolge il nodo canonico `RDGApp` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 836 che coinvolge il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 836 che coinvolge il nodo canonico `RDGApp` è `L26`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 836 che coinvolge il nodo canonico `RDGApp` è `0.8`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 836 che coinvolge il nodo canonico `RDGApp` è `ast`. Nel grafo del codice RDG, l'arco 836 registra come `source` l'identificatore `ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo del codice RDG, l'arco 836 registra come `target` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 836 che coinvolge il nodo canonico `RDGApp` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=837 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RDGApp` è la sorgente `source` della relazione `uses` verso il nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp` all'ID `ui_tabs_dashboard_tab_dashboardtab`. La relazione tecnica esplicita è la seguente: `RDGApp` usa `DashboardTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 837 che coinvolge il nodo canonico `RDGApp` è `uses`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 837 che coinvolge il nodo canonico `RDGApp` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 837 che coinvolge il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 837 che coinvolge il nodo canonico `RDGApp` è `L27`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 837 che coinvolge il nodo canonico `RDGApp` è `0.8`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 837 che coinvolge il nodo canonico `RDGApp` è `ast`. Nel grafo del codice RDG, l'arco 837 registra come `source` l'identificatore `ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo del codice RDG, l'arco 837 registra come `target` l'identificatore `ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 837 che coinvolge il nodo canonico `RDGApp` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=838 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RDGApp` è la sorgente `source` della relazione `uses` verso il nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp` all'ID `ui_tabs_run_tab_runtab`. La relazione tecnica esplicita è la seguente: `RDGApp` usa `RunTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 838 che coinvolge il nodo canonico `RDGApp` è `uses`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 838 che coinvolge il nodo canonico `RDGApp` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 838 che coinvolge il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 838 che coinvolge il nodo canonico `RDGApp` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 838 che coinvolge il nodo canonico `RDGApp` è `0.8`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 838 che coinvolge il nodo canonico `RDGApp` è `ast`. Nel grafo del codice RDG, l'arco 838 registra come `source` l'identificatore `ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo del codice RDG, l'arco 838 registra come `target` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 838 che coinvolge il nodo canonico `RDGApp` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=821 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `RDGApp` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `app.py`; l'orientamento registrato nel JSON va dall'ID `ui_app` all'ID `ui_app_rdgapp`. La relazione tecnica esplicita è la seguente: `app.py` contiene `RDGApp`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 821 che coinvolge il nodo canonico `RDGApp` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 821 che coinvolge il nodo canonico `RDGApp` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 821 che coinvolge il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 821 che coinvolge il nodo canonico `RDGApp` è `L31`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 821 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 821 che coinvolge il nodo canonico `RDGApp` è `ast`. Nel grafo del codice RDG, l'arco 821 registra come `source` l'identificatore `ui_app` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo del codice RDG, l'arco 821 registra come `target` l'identificatore `ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 821 che coinvolge il nodo canonico `RDGApp` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=830 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `RDGApp` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `main() [ui_app_main]`; l'orientamento registrato nel JSON va dall'ID `ui_app_main` all'ID `ui_app_rdgapp`. La relazione tecnica esplicita è la seguente: `main() [ui_app_main]` chiama `RDGApp`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 830 che coinvolge il nodo canonico `RDGApp` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 830 che coinvolge il nodo canonico `RDGApp` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 830 che coinvolge il nodo canonico `RDGApp` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 830 che coinvolge il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 830 che coinvolge il nodo canonico `RDGApp` è `L137`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 830 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 830 che coinvolge il nodo canonico `RDGApp` è `ast`. Nel grafo del codice RDG, l'arco 830 registra come `source` l'identificatore `ui_app_main` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo del codice RDG, l'arco 830 registra come `target` l'identificatore `ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 830 che coinvolge il nodo canonico `RDGApp` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=831 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `RDGApp` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Shell principale con titlebar simulata e tabview.`; l'orientamento registrato nel JSON va dall'ID `ui_app_rationale_32` all'ID `ui_app_rdgapp`. La relazione tecnica esplicita è la seguente: `Shell principale con titlebar simulata e tabview.` costituisce una motivazione per `RDGApp`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 831 che coinvolge il nodo canonico `RDGApp` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 831 che coinvolge il nodo canonico `RDGApp` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 831 che coinvolge il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 831 che coinvolge il nodo canonico `RDGApp` è `L32`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 831 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 831 che coinvolge il nodo canonico `RDGApp` è `ast`. Nel grafo del codice RDG, l'arco 831 registra come `source` l'identificatore `ui_app_rationale_32` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo del codice RDG, l'arco 831 registra come `target` l'identificatore `ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 831 che coinvolge il nodo canonico `RDGApp` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=414 id=Request-Dataset-Generator::ui_app_rdgapp -->
Nel grafo unificato, l'etichetta originale del nodo canonico `RDGApp` è `RDGApp`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `RDGApp` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `RDGApp` è `L31`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `RDGApp` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `RDGApp` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `RDGApp` è `rdgapp`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `RDGApp` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `RDGApp` è `ui_app_rdgapp`. Nel grafo unificato, il nodo canonico `RDGApp` possiede l'identificatore esatto `Request-Dataset-Generator::ui_app_rdgapp`.
<!-- GRAPH_EDGE graph=unificata index=830 role=outgoing -->
Nel grafo unificato, il nodo canonico `RDGApp` è la sorgente `source` della relazione `calls` verso il nodo canonico `main() [ui_app_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp` all'ID `Request-Dataset-Generator::ui_app_main`. La relazione tecnica esplicita è la seguente: `RDGApp` chiama `main() [ui_app_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 830 che coinvolge il nodo canonico `RDGApp` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 830 che coinvolge il nodo canonico `RDGApp` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 830 che coinvolge il nodo canonico `RDGApp` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 830 che coinvolge il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 830 che coinvolge il nodo canonico `RDGApp` è `L137`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 830 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 830 che coinvolge il nodo canonico `RDGApp` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 830 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo unificato, l'arco 830 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo unificato, l'arco 830 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_main` per la relazione che coinvolge il nodo canonico `RDGApp`.
<!-- GRAPH_EDGE graph=unificata index=831 role=outgoing -->
Nel grafo unificato, il nodo canonico `RDGApp` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Shell principale con titlebar simulata e tabview.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp` all'ID `Request-Dataset-Generator::ui_app_rationale_32`. La relazione tecnica esplicita è la seguente: `RDGApp` costituisce una motivazione per `Shell principale con titlebar simulata e tabview.`. Nel grafo unificato, il tipo dichiarato dell'arco 831 che coinvolge il nodo canonico `RDGApp` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 831 che coinvolge il nodo canonico `RDGApp` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 831 che coinvolge il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 831 che coinvolge il nodo canonico `RDGApp` è `L32`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 831 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 831 che coinvolge il nodo canonico `RDGApp` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 831 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo unificato, l'arco 831 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo unificato, l'arco 831 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_rationale_32` per la relazione che coinvolge il nodo canonico `RDGApp`.
<!-- GRAPH_EDGE graph=unificata index=832 role=outgoing -->
Nel grafo unificato, il nodo canonico `RDGApp` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_tabs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp` all'ID `Request-Dataset-Generator::ui_app_rdgapp_build_tabs`. La relazione tecnica esplicita è la seguente: `RDGApp` registra come proprio metodo `._build_tabs()`. Nel grafo unificato, il tipo dichiarato dell'arco 832 che coinvolge il nodo canonico `RDGApp` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 832 che coinvolge il nodo canonico `RDGApp` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 832 che coinvolge il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 832 che coinvolge il nodo canonico `RDGApp` è `L85`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 832 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 832 che coinvolge il nodo canonico `RDGApp` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 832 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo unificato, l'arco 832 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo unificato, l'arco 832 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `RDGApp`.
<!-- GRAPH_EDGE graph=unificata index=833 role=outgoing -->
Nel grafo unificato, il nodo canonico `RDGApp` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_titlebar()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp` all'ID `Request-Dataset-Generator::ui_app_rdgapp_build_titlebar`. La relazione tecnica esplicita è la seguente: `RDGApp` registra come proprio metodo `._build_titlebar()`. Nel grafo unificato, il tipo dichiarato dell'arco 833 che coinvolge il nodo canonico `RDGApp` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 833 che coinvolge il nodo canonico `RDGApp` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 833 che coinvolge il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 833 che coinvolge il nodo canonico `RDGApp` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 833 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 833 che coinvolge il nodo canonico `RDGApp` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 833 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo unificato, l'arco 833 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo unificato, l'arco 833 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_build_titlebar` per la relazione che coinvolge il nodo canonico `RDGApp`.
<!-- GRAPH_EDGE graph=unificata index=834 role=outgoing -->
Nel grafo unificato, il nodo canonico `RDGApp` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [ui_app_rdgapp_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp` all'ID `Request-Dataset-Generator::ui_app_rdgapp_init`. La relazione tecnica esplicita è la seguente: `RDGApp` registra come proprio metodo `.__init__() [ui_app_rdgapp_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 834 che coinvolge il nodo canonico `RDGApp` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 834 che coinvolge il nodo canonico `RDGApp` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 834 che coinvolge il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 834 che coinvolge il nodo canonico `RDGApp` è `L36`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 834 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 834 che coinvolge il nodo canonico `RDGApp` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 834 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo unificato, l'arco 834 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo unificato, l'arco 834 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_init` per la relazione che coinvolge il nodo canonico `RDGApp`.
<!-- GRAPH_EDGE graph=unificata index=835 role=outgoing -->
Nel grafo unificato, il nodo canonico `RDGApp` è la sorgente `source` della relazione `method` verso il nodo canonico `._style_segmented_tabs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp` all'ID `Request-Dataset-Generator::ui_app_rdgapp_style_segmented_tabs`. La relazione tecnica esplicita è la seguente: `RDGApp` registra come proprio metodo `._style_segmented_tabs()`. Nel grafo unificato, il tipo dichiarato dell'arco 835 che coinvolge il nodo canonico `RDGApp` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 835 che coinvolge il nodo canonico `RDGApp` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 835 che coinvolge il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 835 che coinvolge il nodo canonico `RDGApp` è `L126`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 835 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 835 che coinvolge il nodo canonico `RDGApp` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 835 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo unificato, l'arco 835 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo unificato, l'arco 835 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_style_segmented_tabs` per la relazione che coinvolge il nodo canonico `RDGApp`.
<!-- GRAPH_EDGE graph=unificata index=836 role=outgoing -->
Nel grafo unificato, il nodo canonico `RDGApp` è la sorgente `source` della relazione `uses` verso il nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab`. La relazione tecnica esplicita è la seguente: `RDGApp` usa `ConfigTab`. Nel grafo unificato, il tipo dichiarato dell'arco 836 che coinvolge il nodo canonico `RDGApp` è `uses`. Nel grafo unificato, la confidenza categoriale dell'arco 836 che coinvolge il nodo canonico `RDGApp` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 836 che coinvolge il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 836 che coinvolge il nodo canonico `RDGApp` è `L26`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 836 che coinvolge il nodo canonico `RDGApp` è `0.8`. Nel grafo unificato, la provenienza `_origin` dell'arco 836 che coinvolge il nodo canonico `RDGApp` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 836 che coinvolge il nodo canonico `RDGApp` è `0.5`. Nel grafo unificato, l'arco 836 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo unificato, l'arco 836 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `RDGApp`.
<!-- GRAPH_EDGE graph=unificata index=837 role=outgoing -->
Nel grafo unificato, il nodo canonico `RDGApp` è la sorgente `source` della relazione `uses` verso il nodo canonico `DashboardTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab`. La relazione tecnica esplicita è la seguente: `RDGApp` usa `DashboardTab`. Nel grafo unificato, il tipo dichiarato dell'arco 837 che coinvolge il nodo canonico `RDGApp` è `uses`. Nel grafo unificato, la confidenza categoriale dell'arco 837 che coinvolge il nodo canonico `RDGApp` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 837 che coinvolge il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 837 che coinvolge il nodo canonico `RDGApp` è `L27`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 837 che coinvolge il nodo canonico `RDGApp` è `0.8`. Nel grafo unificato, la provenienza `_origin` dell'arco 837 che coinvolge il nodo canonico `RDGApp` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 837 che coinvolge il nodo canonico `RDGApp` è `0.5`. Nel grafo unificato, l'arco 837 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo unificato, l'arco 837 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab_dashboardtab` per la relazione che coinvolge il nodo canonico `RDGApp`.
<!-- GRAPH_EDGE graph=unificata index=838 role=outgoing -->
Nel grafo unificato, il nodo canonico `RDGApp` è la sorgente `source` della relazione `uses` verso il nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab`. La relazione tecnica esplicita è la seguente: `RDGApp` usa `RunTab`. Nel grafo unificato, il tipo dichiarato dell'arco 838 che coinvolge il nodo canonico `RDGApp` è `uses`. Nel grafo unificato, la confidenza categoriale dell'arco 838 che coinvolge il nodo canonico `RDGApp` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 838 che coinvolge il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 838 che coinvolge il nodo canonico `RDGApp` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 838 che coinvolge il nodo canonico `RDGApp` è `0.8`. Nel grafo unificato, la provenienza `_origin` dell'arco 838 che coinvolge il nodo canonico `RDGApp` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 838 che coinvolge il nodo canonico `RDGApp` è `0.5`. Nel grafo unificato, l'arco 838 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo unificato, l'arco 838 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RDGApp`.
<!-- GRAPH_EDGE graph=unificata index=821 role=incoming -->
Nel grafo unificato, il nodo canonico `RDGApp` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `app.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app` all'ID `Request-Dataset-Generator::ui_app_rdgapp`. La relazione tecnica esplicita è la seguente: `app.py` contiene `RDGApp`. Nel grafo unificato, il tipo dichiarato dell'arco 821 che coinvolge il nodo canonico `RDGApp` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 821 che coinvolge il nodo canonico `RDGApp` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 821 che coinvolge il nodo canonico `RDGApp` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 821 che coinvolge il nodo canonico `RDGApp` è `L31`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 821 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 821 che coinvolge il nodo canonico `RDGApp` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 821 che coinvolge il nodo canonico `RDGApp` è `1.0`. Nel grafo unificato, l'arco 821 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app` per la relazione che coinvolge il nodo canonico `RDGApp`. Nel grafo unificato, l'arco 821 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RDGApp`.

### run_tab.py

<!-- GRAPH_NODE graph=codice index=465 id=ui_tabs_run_tab -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `run_tab.py` è `run_tab.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `run_tab.py` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `run_tab.py` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `run_tab.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `run_tab.py` vale `ast`. Nel grafo del codice RDG, il nodo canonico `run_tab.py` possiede l'identificatore esatto `ui_tabs_run_tab`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `run_tab.py` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `run_tab.py` è `run_tab.py`.
<!-- GRAPH_EDGE graph=codice index=848 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `run_tab.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `style.py`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab` all'ID `ui_style`. La relazione tecnica esplicita è la seguente: `run_tab.py` importa da `style.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 848 che coinvolge il nodo canonico `run_tab.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 848 che coinvolge il nodo canonico `run_tab.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 848 che coinvolge il nodo canonico `run_tab.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 848 che coinvolge il nodo canonico `run_tab.py` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 848 che coinvolge il nodo canonico `run_tab.py` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 848 che coinvolge il nodo canonico `run_tab.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 848 che coinvolge il nodo canonico `run_tab.py` è `ast`. Nel grafo del codice RDG, l'arco 848 registra come `source` l'identificatore `ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `run_tab.py`. Nel grafo del codice RDG, l'arco 848 registra come `target` l'identificatore `ui_style` per la relazione che coinvolge il nodo canonico `run_tab.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 848 che coinvolge il nodo canonico `run_tab.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=924 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `run_tab.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab` all'ID `ui_tabs_run_tab_runtab`. La relazione tecnica esplicita è la seguente: `run_tab.py` contiene `RunTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 924 che coinvolge il nodo canonico `run_tab.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 924 che coinvolge il nodo canonico `run_tab.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 924 che coinvolge il nodo canonico `run_tab.py` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 924 che coinvolge il nodo canonico `run_tab.py` è `L42`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 924 che coinvolge il nodo canonico `run_tab.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 924 che coinvolge il nodo canonico `run_tab.py` è `ast`. Nel grafo del codice RDG, l'arco 924 registra come `source` l'identificatore `ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `run_tab.py`. Nel grafo del codice RDG, l'arco 924 registra come `target` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `run_tab.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 924 che coinvolge il nodo canonico `run_tab.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=925 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `run_tab.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `ui_config_bridge.py`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab` all'ID `ui_ui_config_bridge`. La relazione tecnica esplicita è la seguente: `run_tab.py` importa da `ui_config_bridge.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 925 che coinvolge il nodo canonico `run_tab.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 925 che coinvolge il nodo canonico `run_tab.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 925 che coinvolge il nodo canonico `run_tab.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 925 che coinvolge il nodo canonico `run_tab.py` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 925 che coinvolge il nodo canonico `run_tab.py` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 925 che coinvolge il nodo canonico `run_tab.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 925 che coinvolge il nodo canonico `run_tab.py` è `ast`. Nel grafo del codice RDG, l'arco 925 registra come `source` l'identificatore `ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `run_tab.py`. Nel grafo del codice RDG, l'arco 925 registra come `target` l'identificatore `ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `run_tab.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 925 che coinvolge il nodo canonico `run_tab.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=926 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `run_tab.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab` all'ID `ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `run_tab.py` importa `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 926 che coinvolge il nodo canonico `run_tab.py` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 926 che coinvolge il nodo canonico `run_tab.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 926 che coinvolge il nodo canonico `run_tab.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 926 che coinvolge il nodo canonico `run_tab.py` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 926 che coinvolge il nodo canonico `run_tab.py` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 926 che coinvolge il nodo canonico `run_tab.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 926 che coinvolge il nodo canonico `run_tab.py` è `ast`. Nel grafo del codice RDG, l'arco 926 registra come `source` l'identificatore `ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `run_tab.py`. Nel grafo del codice RDG, l'arco 926 registra come `target` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `run_tab.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 926 che coinvolge il nodo canonico `run_tab.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=827 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `run_tab.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `app.py`; l'orientamento registrato nel JSON va dall'ID `ui_app` all'ID `ui_tabs_run_tab`. La relazione tecnica esplicita è la seguente: `app.py` importa da `run_tab.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 827 che coinvolge il nodo canonico `run_tab.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 827 che coinvolge il nodo canonico `run_tab.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 827 che coinvolge il nodo canonico `run_tab.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 827 che coinvolge il nodo canonico `run_tab.py` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 827 che coinvolge il nodo canonico `run_tab.py` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 827 che coinvolge il nodo canonico `run_tab.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 827 che coinvolge il nodo canonico `run_tab.py` è `ast`. Nel grafo del codice RDG, l'arco 827 registra come `source` l'identificatore `ui_app` per la relazione che coinvolge il nodo canonico `run_tab.py`. Nel grafo del codice RDG, l'arco 827 registra come `target` l'identificatore `ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `run_tab.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 827 che coinvolge il nodo canonico `run_tab.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=927 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `run_tab.py` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Tab di esecuzione pipeline RDG.`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_rationale_1` all'ID `ui_tabs_run_tab`. La relazione tecnica esplicita è la seguente: `Tab di esecuzione pipeline RDG.` costituisce una motivazione per `run_tab.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 927 che coinvolge il nodo canonico `run_tab.py` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 927 che coinvolge il nodo canonico `run_tab.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 927 che coinvolge il nodo canonico `run_tab.py` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 927 che coinvolge il nodo canonico `run_tab.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 927 che coinvolge il nodo canonico `run_tab.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 927 che coinvolge il nodo canonico `run_tab.py` è `ast`. Nel grafo del codice RDG, l'arco 927 registra come `source` l'identificatore `ui_tabs_run_tab_rationale_1` per la relazione che coinvolge il nodo canonico `run_tab.py`. Nel grafo del codice RDG, l'arco 927 registra come `target` l'identificatore `ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `run_tab.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 927 che coinvolge il nodo canonico `run_tab.py` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=465 id=Request-Dataset-Generator::ui_tabs_run_tab -->
Nel grafo unificato, l'etichetta originale del nodo canonico `run_tab.py` è `run_tab.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `run_tab.py` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `run_tab.py` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `run_tab.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `run_tab.py` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `run_tab.py` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `run_tab.py` è `run_tab.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `run_tab.py` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `run_tab.py` è `ui_tabs_run_tab`. Nel grafo unificato, il nodo canonico `run_tab.py` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab`.
<!-- GRAPH_EDGE graph=unificata index=924 role=outgoing -->
Nel grafo unificato, il nodo canonico `run_tab.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab`. La relazione tecnica esplicita è la seguente: `run_tab.py` contiene `RunTab`. Nel grafo unificato, il tipo dichiarato dell'arco 924 che coinvolge il nodo canonico `run_tab.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 924 che coinvolge il nodo canonico `run_tab.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 924 che coinvolge il nodo canonico `run_tab.py` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 924 che coinvolge il nodo canonico `run_tab.py` è `L42`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 924 che coinvolge il nodo canonico `run_tab.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 924 che coinvolge il nodo canonico `run_tab.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 924 che coinvolge il nodo canonico `run_tab.py` è `1.0`. Nel grafo unificato, l'arco 924 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `run_tab.py`. Nel grafo unificato, l'arco 924 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `run_tab.py`.
<!-- GRAPH_EDGE graph=unificata index=925 role=outgoing -->
Nel grafo unificato, il nodo canonico `run_tab.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `ui_config_bridge.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab` all'ID `Request-Dataset-Generator::ui_ui_config_bridge`. La relazione tecnica esplicita è la seguente: `run_tab.py` importa da `ui_config_bridge.py`. Nel grafo unificato, il tipo dichiarato dell'arco 925 che coinvolge il nodo canonico `run_tab.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 925 che coinvolge il nodo canonico `run_tab.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 925 che coinvolge il nodo canonico `run_tab.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 925 che coinvolge il nodo canonico `run_tab.py` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 925 che coinvolge il nodo canonico `run_tab.py` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 925 che coinvolge il nodo canonico `run_tab.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 925 che coinvolge il nodo canonico `run_tab.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 925 che coinvolge il nodo canonico `run_tab.py` è `1.0`. Nel grafo unificato, l'arco 925 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `run_tab.py`. Nel grafo unificato, l'arco 925 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `run_tab.py`.
<!-- GRAPH_EDGE graph=unificata index=926 role=outgoing -->
Nel grafo unificato, il nodo canonico `run_tab.py` è la sorgente `source` della relazione `imports` verso il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `run_tab.py` importa `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 926 che coinvolge il nodo canonico `run_tab.py` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 926 che coinvolge il nodo canonico `run_tab.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 926 che coinvolge il nodo canonico `run_tab.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 926 che coinvolge il nodo canonico `run_tab.py` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 926 che coinvolge il nodo canonico `run_tab.py` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 926 che coinvolge il nodo canonico `run_tab.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 926 che coinvolge il nodo canonico `run_tab.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 926 che coinvolge il nodo canonico `run_tab.py` è `1.0`. Nel grafo unificato, l'arco 926 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `run_tab.py`. Nel grafo unificato, l'arco 926 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `run_tab.py`.
<!-- GRAPH_EDGE graph=unificata index=927 role=outgoing -->
Nel grafo unificato, il nodo canonico `run_tab.py` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Tab di esecuzione pipeline RDG.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_rationale_1`. La relazione tecnica esplicita è la seguente: `run_tab.py` costituisce una motivazione per `Tab di esecuzione pipeline RDG.`. Nel grafo unificato, il tipo dichiarato dell'arco 927 che coinvolge il nodo canonico `run_tab.py` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 927 che coinvolge il nodo canonico `run_tab.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 927 che coinvolge il nodo canonico `run_tab.py` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 927 che coinvolge il nodo canonico `run_tab.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 927 che coinvolge il nodo canonico `run_tab.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 927 che coinvolge il nodo canonico `run_tab.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 927 che coinvolge il nodo canonico `run_tab.py` è `1.0`. Nel grafo unificato, l'arco 927 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `run_tab.py`. Nel grafo unificato, l'arco 927 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_rationale_1` per la relazione che coinvolge il nodo canonico `run_tab.py`.
<!-- GRAPH_EDGE graph=unificata index=827 role=incoming -->
Nel grafo unificato, il nodo canonico `run_tab.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `app.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app` all'ID `Request-Dataset-Generator::ui_tabs_run_tab`. La relazione tecnica esplicita è la seguente: `app.py` importa da `run_tab.py`. Nel grafo unificato, il tipo dichiarato dell'arco 827 che coinvolge il nodo canonico `run_tab.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 827 che coinvolge il nodo canonico `run_tab.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 827 che coinvolge il nodo canonico `run_tab.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 827 che coinvolge il nodo canonico `run_tab.py` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 827 che coinvolge il nodo canonico `run_tab.py` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 827 che coinvolge il nodo canonico `run_tab.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 827 che coinvolge il nodo canonico `run_tab.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 827 che coinvolge il nodo canonico `run_tab.py` è `1.0`. Nel grafo unificato, l'arco 827 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app` per la relazione che coinvolge il nodo canonico `run_tab.py`. Nel grafo unificato, l'arco 827 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `run_tab.py`.
<!-- GRAPH_EDGE graph=unificata index=848 role=incoming -->
Nel grafo unificato, il nodo canonico `run_tab.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `style.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_style` all'ID `Request-Dataset-Generator::ui_tabs_run_tab`. La relazione tecnica esplicita è la seguente: `style.py` importa da `run_tab.py`. Nel grafo unificato, il tipo dichiarato dell'arco 848 che coinvolge il nodo canonico `run_tab.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 848 che coinvolge il nodo canonico `run_tab.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 848 che coinvolge il nodo canonico `run_tab.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 848 che coinvolge il nodo canonico `run_tab.py` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 848 che coinvolge il nodo canonico `run_tab.py` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 848 che coinvolge il nodo canonico `run_tab.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 848 che coinvolge il nodo canonico `run_tab.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 848 che coinvolge il nodo canonico `run_tab.py` è `1.0`. Nel grafo unificato, l'arco 848 registra come `source` l'identificatore `Request-Dataset-Generator::ui_style` per la relazione che coinvolge il nodo canonico `run_tab.py`. Nel grafo unificato, l'arco 848 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `run_tab.py`.

### save_config()

<!-- GRAPH_NODE graph=codice index=499 id=ui_ui_config_bridge_save_config -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `save_config()` è `save_config()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `save_config()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `save_config()` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `save_config()` è `L34`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `save_config()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `save_config()` possiede l'identificatore esatto `ui_ui_config_bridge_save_config`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `save_config()` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `save_config()` è `save_config()`.
<!-- GRAPH_EDGE graph=codice index=994 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `save_config()` è la sorgente `source` della relazione `references` verso il nodo canonico `Path [ui_ui_config_bridge_py_path]`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge_save_config` all'ID `ui_ui_config_bridge_py_path`. La relazione tecnica esplicita è la seguente: `save_config()` fa riferimento a `Path [ui_ui_config_bridge_py_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 994 che coinvolge il nodo canonico `save_config()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 994 che coinvolge il nodo canonico `save_config()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 994 che coinvolge il nodo canonico `save_config()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 994 che coinvolge il nodo canonico `save_config()` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 994 che coinvolge il nodo canonico `save_config()` è `L34`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 994 che coinvolge il nodo canonico `save_config()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 994 che coinvolge il nodo canonico `save_config()` è `ast`. Nel grafo del codice RDG, l'arco 994 registra come `source` l'identificatore `ui_ui_config_bridge_save_config` per la relazione che coinvolge il nodo canonico `save_config()`. Nel grafo del codice RDG, l'arco 994 registra come `target` l'identificatore `ui_ui_config_bridge_py_path` per la relazione che coinvolge il nodo canonico `save_config()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 994 che coinvolge il nodo canonico `save_config()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=996 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `save_config()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [ui_ui_config_bridge_py_any]`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge_save_config` all'ID `ui_ui_config_bridge_py_any`. La relazione tecnica esplicita è la seguente: `save_config()` fa riferimento a `Any [ui_ui_config_bridge_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 996 che coinvolge il nodo canonico `save_config()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 996 che coinvolge il nodo canonico `save_config()` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 996 che coinvolge il nodo canonico `save_config()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 996 che coinvolge il nodo canonico `save_config()` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 996 che coinvolge il nodo canonico `save_config()` è `L34`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 996 che coinvolge il nodo canonico `save_config()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 996 che coinvolge il nodo canonico `save_config()` è `ast`. Nel grafo del codice RDG, l'arco 996 registra come `source` l'identificatore `ui_ui_config_bridge_save_config` per la relazione che coinvolge il nodo canonico `save_config()`. Nel grafo del codice RDG, l'arco 996 registra come `target` l'identificatore `ui_ui_config_bridge_py_any` per la relazione che coinvolge il nodo canonico `save_config()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 996 che coinvolge il nodo canonico `save_config()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=988 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `save_config()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `ui_config_bridge.py`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge` all'ID `ui_ui_config_bridge_save_config`. La relazione tecnica esplicita è la seguente: `ui_config_bridge.py` contiene `save_config()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 988 che coinvolge il nodo canonico `save_config()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 988 che coinvolge il nodo canonico `save_config()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 988 che coinvolge il nodo canonico `save_config()` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 988 che coinvolge il nodo canonico `save_config()` è `L34`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 988 che coinvolge il nodo canonico `save_config()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 988 che coinvolge il nodo canonico `save_config()` è `ast`. Nel grafo del codice RDG, l'arco 988 registra come `source` l'identificatore `ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `save_config()`. Nel grafo del codice RDG, l'arco 988 registra come `target` l'identificatore `ui_ui_config_bridge_save_config` per la relazione che coinvolge il nodo canonico `save_config()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 988 che coinvolge il nodo canonico `save_config()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=997 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `save_config()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Sovrascrive il file json con i valori dal form UI.`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge_rationale_35` all'ID `ui_ui_config_bridge_save_config`. La relazione tecnica esplicita è la seguente: `Sovrascrive il file json con i valori dal form UI.` costituisce una motivazione per `save_config()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 997 che coinvolge il nodo canonico `save_config()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 997 che coinvolge il nodo canonico `save_config()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 997 che coinvolge il nodo canonico `save_config()` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 997 che coinvolge il nodo canonico `save_config()` è `L35`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 997 che coinvolge il nodo canonico `save_config()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 997 che coinvolge il nodo canonico `save_config()` è `ast`. Nel grafo del codice RDG, l'arco 997 registra come `source` l'identificatore `ui_ui_config_bridge_rationale_35` per la relazione che coinvolge il nodo canonico `save_config()`. Nel grafo del codice RDG, l'arco 997 registra come `target` l'identificatore `ui_ui_config_bridge_save_config` per la relazione che coinvolge il nodo canonico `save_config()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 997 che coinvolge il nodo canonico `save_config()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=499 id=Request-Dataset-Generator::ui_ui_config_bridge_save_config -->
Nel grafo unificato, l'etichetta originale del nodo canonico `save_config()` è `save_config()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `save_config()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `save_config()` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `save_config()` è `L34`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `save_config()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `save_config()` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `save_config()` è `save_config()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `save_config()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `save_config()` è `ui_ui_config_bridge_save_config`. Nel grafo unificato, il nodo canonico `save_config()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_ui_config_bridge_save_config`.
<!-- GRAPH_EDGE graph=unificata index=997 role=outgoing -->
Nel grafo unificato, il nodo canonico `save_config()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Sovrascrive il file json con i valori dal form UI.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge_save_config` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_rationale_35`. La relazione tecnica esplicita è la seguente: `save_config()` costituisce una motivazione per `Sovrascrive il file json con i valori dal form UI.`. Nel grafo unificato, il tipo dichiarato dell'arco 997 che coinvolge il nodo canonico `save_config()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 997 che coinvolge il nodo canonico `save_config()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 997 che coinvolge il nodo canonico `save_config()` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 997 che coinvolge il nodo canonico `save_config()` è `L35`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 997 che coinvolge il nodo canonico `save_config()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 997 che coinvolge il nodo canonico `save_config()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 997 che coinvolge il nodo canonico `save_config()` è `1.0`. Nel grafo unificato, l'arco 997 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_save_config` per la relazione che coinvolge il nodo canonico `save_config()`. Nel grafo unificato, l'arco 997 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_rationale_35` per la relazione che coinvolge il nodo canonico `save_config()`.
<!-- GRAPH_EDGE graph=unificata index=988 role=incoming -->
Nel grafo unificato, il nodo canonico `save_config()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `ui_config_bridge.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_save_config`. La relazione tecnica esplicita è la seguente: `ui_config_bridge.py` contiene `save_config()`. Nel grafo unificato, il tipo dichiarato dell'arco 988 che coinvolge il nodo canonico `save_config()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 988 che coinvolge il nodo canonico `save_config()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 988 che coinvolge il nodo canonico `save_config()` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 988 che coinvolge il nodo canonico `save_config()` è `L34`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 988 che coinvolge il nodo canonico `save_config()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 988 che coinvolge il nodo canonico `save_config()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 988 che coinvolge il nodo canonico `save_config()` è `1.0`. Nel grafo unificato, l'arco 988 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `save_config()`. Nel grafo unificato, l'arco 988 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_save_config` per la relazione che coinvolge il nodo canonico `save_config()`.
<!-- GRAPH_EDGE graph=unificata index=994 role=incoming -->
Nel grafo unificato, il nodo canonico `save_config()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Path [ui_ui_config_bridge_py_path]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge_py_path` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_save_config`. La relazione tecnica esplicita è la seguente: `Path [ui_ui_config_bridge_py_path]` fa riferimento a `save_config()`. Nel grafo unificato, il tipo dichiarato dell'arco 994 che coinvolge il nodo canonico `save_config()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 994 che coinvolge il nodo canonico `save_config()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 994 che coinvolge il nodo canonico `save_config()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 994 che coinvolge il nodo canonico `save_config()` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 994 che coinvolge il nodo canonico `save_config()` è `L34`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 994 che coinvolge il nodo canonico `save_config()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 994 che coinvolge il nodo canonico `save_config()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 994 che coinvolge il nodo canonico `save_config()` è `1.0`. Nel grafo unificato, l'arco 994 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_py_path` per la relazione che coinvolge il nodo canonico `save_config()`. Nel grafo unificato, l'arco 994 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_save_config` per la relazione che coinvolge il nodo canonico `save_config()`.
<!-- GRAPH_EDGE graph=unificata index=996 role=incoming -->
Nel grafo unificato, il nodo canonico `save_config()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Any [ui_ui_config_bridge_py_any]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge_py_any` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_save_config`. La relazione tecnica esplicita è la seguente: `Any [ui_ui_config_bridge_py_any]` fa riferimento a `save_config()`. Nel grafo unificato, il tipo dichiarato dell'arco 996 che coinvolge il nodo canonico `save_config()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 996 che coinvolge il nodo canonico `save_config()` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 996 che coinvolge il nodo canonico `save_config()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 996 che coinvolge il nodo canonico `save_config()` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 996 che coinvolge il nodo canonico `save_config()` è `L34`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 996 che coinvolge il nodo canonico `save_config()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 996 che coinvolge il nodo canonico `save_config()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 996 che coinvolge il nodo canonico `save_config()` è `1.0`. Nel grafo unificato, l'arco 996 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_py_any` per la relazione che coinvolge il nodo canonico `save_config()`. Nel grafo unificato, l'arco 996 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_save_config` per la relazione che coinvolge il nodo canonico `save_config()`.

### Shell principale con titlebar simulata e tabview.

<!-- GRAPH_NODE graph=codice index=421 id=ui_app_rationale_32 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Shell principale con titlebar simulata e tabview.` è `Shell principale con titlebar simulata e tabview.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Shell principale con titlebar simulata e tabview.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Shell principale con titlebar simulata e tabview.` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Shell principale con titlebar simulata e tabview.` è `L32`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Shell principale con titlebar simulata e tabview.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Shell principale con titlebar simulata e tabview.` possiede l'identificatore esatto `ui_app_rationale_32`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Shell principale con titlebar simulata e tabview.` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Shell principale con titlebar simulata e tabview.` è `shell principale con titlebar simulata e tabview.`.
<!-- GRAPH_EDGE graph=codice index=831 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Shell principale con titlebar simulata e tabview.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `RDGApp`; l'orientamento registrato nel JSON va dall'ID `ui_app_rationale_32` all'ID `ui_app_rdgapp`. La relazione tecnica esplicita è la seguente: `Shell principale con titlebar simulata e tabview.` costituisce una motivazione per `RDGApp`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 831 che coinvolge il nodo canonico `Shell principale con titlebar simulata e tabview.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 831 che coinvolge il nodo canonico `Shell principale con titlebar simulata e tabview.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 831 che coinvolge il nodo canonico `Shell principale con titlebar simulata e tabview.` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 831 che coinvolge il nodo canonico `Shell principale con titlebar simulata e tabview.` è `L32`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 831 che coinvolge il nodo canonico `Shell principale con titlebar simulata e tabview.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 831 che coinvolge il nodo canonico `Shell principale con titlebar simulata e tabview.` è `ast`. Nel grafo del codice RDG, l'arco 831 registra come `source` l'identificatore `ui_app_rationale_32` per la relazione che coinvolge il nodo canonico `Shell principale con titlebar simulata e tabview.`. Nel grafo del codice RDG, l'arco 831 registra come `target` l'identificatore `ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `Shell principale con titlebar simulata e tabview.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 831 che coinvolge il nodo canonico `Shell principale con titlebar simulata e tabview.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Shell principale con titlebar simulata e tabview.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=421 id=Request-Dataset-Generator::ui_app_rationale_32 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Shell principale con titlebar simulata e tabview.` è `Shell principale con titlebar simulata e tabview.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Shell principale con titlebar simulata e tabview.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Shell principale con titlebar simulata e tabview.` è `ui/app.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Shell principale con titlebar simulata e tabview.` è `L32`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Shell principale con titlebar simulata e tabview.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Shell principale con titlebar simulata e tabview.` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Shell principale con titlebar simulata e tabview.` è `shell principale con titlebar simulata e tabview.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Shell principale con titlebar simulata e tabview.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Shell principale con titlebar simulata e tabview.` è `ui_app_rationale_32`. Nel grafo unificato, il nodo canonico `Shell principale con titlebar simulata e tabview.` possiede l'identificatore esatto `Request-Dataset-Generator::ui_app_rationale_32`.
Nel grafo unificato, il nodo canonico `Shell principale con titlebar simulata e tabview.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=831 role=incoming -->
Nel grafo unificato, il nodo canonico `Shell principale con titlebar simulata e tabview.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `RDGApp`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp` all'ID `Request-Dataset-Generator::ui_app_rationale_32`. La relazione tecnica esplicita è la seguente: `RDGApp` costituisce una motivazione per `Shell principale con titlebar simulata e tabview.`. Nel grafo unificato, il tipo dichiarato dell'arco 831 che coinvolge il nodo canonico `Shell principale con titlebar simulata e tabview.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 831 che coinvolge il nodo canonico `Shell principale con titlebar simulata e tabview.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 831 che coinvolge il nodo canonico `Shell principale con titlebar simulata e tabview.` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 831 che coinvolge il nodo canonico `Shell principale con titlebar simulata e tabview.` è `L32`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 831 che coinvolge il nodo canonico `Shell principale con titlebar simulata e tabview.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 831 che coinvolge il nodo canonico `Shell principale con titlebar simulata e tabview.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 831 che coinvolge il nodo canonico `Shell principale con titlebar simulata e tabview.` è `1.0`. Nel grafo unificato, l'arco 831 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `Shell principale con titlebar simulata e tabview.`. Nel grafo unificato, l'arco 831 registra come `target` l'identificatore `Request-Dataset-Generator::ui_app_rationale_32` per la relazione che coinvolge il nodo canonico `Shell principale con titlebar simulata e tabview.`.

### Sovrascrive il file json con i valori dal form UI.

<!-- GRAPH_NODE graph=codice index=503 id=ui_ui_config_bridge_rationale_35 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `Sovrascrive il file json con i valori dal form UI.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Sovrascrive il file json con i valori dal form UI.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `L35`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Sovrascrive il file json con i valori dal form UI.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Sovrascrive il file json con i valori dal form UI.` possiede l'identificatore esatto `ui_ui_config_bridge_rationale_35`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Sovrascrive il file json con i valori dal form UI.` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `sovrascrive il file json con i valori dal form ui.`.
<!-- GRAPH_EDGE graph=codice index=997 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `save_config()`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge_rationale_35` all'ID `ui_ui_config_bridge_save_config`. La relazione tecnica esplicita è la seguente: `Sovrascrive il file json con i valori dal form UI.` costituisce una motivazione per `save_config()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 997 che coinvolge il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 997 che coinvolge il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 997 che coinvolge il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 997 che coinvolge il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `L35`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 997 che coinvolge il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 997 che coinvolge il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `ast`. Nel grafo del codice RDG, l'arco 997 registra come `source` l'identificatore `ui_ui_config_bridge_rationale_35` per la relazione che coinvolge il nodo canonico `Sovrascrive il file json con i valori dal form UI.`. Nel grafo del codice RDG, l'arco 997 registra come `target` l'identificatore `ui_ui_config_bridge_save_config` per la relazione che coinvolge il nodo canonico `Sovrascrive il file json con i valori dal form UI.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 997 che coinvolge il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Sovrascrive il file json con i valori dal form UI.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=503 id=Request-Dataset-Generator::ui_ui_config_bridge_rationale_35 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `Sovrascrive il file json con i valori dal form UI.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Sovrascrive il file json con i valori dal form UI.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `L35`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Sovrascrive il file json con i valori dal form UI.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Sovrascrive il file json con i valori dal form UI.` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `sovrascrive il file json con i valori dal form ui.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Sovrascrive il file json con i valori dal form UI.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `ui_ui_config_bridge_rationale_35`. Nel grafo unificato, il nodo canonico `Sovrascrive il file json con i valori dal form UI.` possiede l'identificatore esatto `Request-Dataset-Generator::ui_ui_config_bridge_rationale_35`.
Nel grafo unificato, il nodo canonico `Sovrascrive il file json con i valori dal form UI.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=997 role=incoming -->
Nel grafo unificato, il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `save_config()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge_save_config` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_rationale_35`. La relazione tecnica esplicita è la seguente: `save_config()` costituisce una motivazione per `Sovrascrive il file json con i valori dal form UI.`. Nel grafo unificato, il tipo dichiarato dell'arco 997 che coinvolge il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 997 che coinvolge il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 997 che coinvolge il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 997 che coinvolge il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `L35`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 997 che coinvolge il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 997 che coinvolge il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 997 che coinvolge il nodo canonico `Sovrascrive il file json con i valori dal form UI.` è `1.0`. Nel grafo unificato, l'arco 997 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_save_config` per la relazione che coinvolge il nodo canonico `Sovrascrive il file json con i valori dal form UI.`. Nel grafo unificato, l'arco 997 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_rationale_35` per la relazione che coinvolge il nodo canonico `Sovrascrive il file json con i valori dal form UI.`.

### style.py

<!-- GRAPH_NODE graph=codice index=422 id=ui_style -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `style.py` è `style.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `style.py` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `style.py` è `ui/style.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `style.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `style.py` vale `ast`. Nel grafo del codice RDG, il nodo canonico `style.py` possiede l'identificatore esatto `ui_style`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `style.py` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `style.py` è `style.py`.
Nel grafo del codice RDG, il nodo canonico `style.py` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=822 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `style.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `app.py`; l'orientamento registrato nel JSON va dall'ID `ui_app` all'ID `ui_style`. La relazione tecnica esplicita è la seguente: `app.py` importa da `style.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 822 che coinvolge il nodo canonico `style.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 822 che coinvolge il nodo canonico `style.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 822 che coinvolge il nodo canonico `style.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 822 che coinvolge il nodo canonico `style.py` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 822 che coinvolge il nodo canonico `style.py` è `L13`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 822 che coinvolge il nodo canonico `style.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 822 che coinvolge il nodo canonico `style.py` è `ast`. Nel grafo del codice RDG, l'arco 822 registra come `source` l'identificatore `ui_app` per la relazione che coinvolge il nodo canonico `style.py`. Nel grafo del codice RDG, l'arco 822 registra come `target` l'identificatore `ui_style` per la relazione che coinvolge il nodo canonico `style.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 822 che coinvolge il nodo canonico `style.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=845 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `style.py` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.`; l'orientamento registrato nel JSON va dall'ID `ui_style_rationale_1` all'ID `ui_style`. La relazione tecnica esplicita è la seguente: `Costanti visuali per la UI CustomTkinter di RDG.` costituisce una motivazione per `style.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 845 che coinvolge il nodo canonico `style.py` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 845 che coinvolge il nodo canonico `style.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 845 che coinvolge il nodo canonico `style.py` è `ui/style.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 845 che coinvolge il nodo canonico `style.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 845 che coinvolge il nodo canonico `style.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 845 che coinvolge il nodo canonico `style.py` è `ast`. Nel grafo del codice RDG, l'arco 845 registra come `source` l'identificatore `ui_style_rationale_1` per la relazione che coinvolge il nodo canonico `style.py`. Nel grafo del codice RDG, l'arco 845 registra come `target` l'identificatore `ui_style` per la relazione che coinvolge il nodo canonico `style.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 845 che coinvolge il nodo canonico `style.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=846 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `style.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `config_tab.py`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab` all'ID `ui_style`. La relazione tecnica esplicita è la seguente: `config_tab.py` importa da `style.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 846 che coinvolge il nodo canonico `style.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 846 che coinvolge il nodo canonico `style.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 846 che coinvolge il nodo canonico `style.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 846 che coinvolge il nodo canonico `style.py` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 846 che coinvolge il nodo canonico `style.py` è `L8`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 846 che coinvolge il nodo canonico `style.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 846 che coinvolge il nodo canonico `style.py` è `ast`. Nel grafo del codice RDG, l'arco 846 registra come `source` l'identificatore `ui_tabs_config_tab` per la relazione che coinvolge il nodo canonico `style.py`. Nel grafo del codice RDG, l'arco 846 registra come `target` l'identificatore `ui_style` per la relazione che coinvolge il nodo canonico `style.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 846 che coinvolge il nodo canonico `style.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=847 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `style.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `dashboard_tab.py`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab` all'ID `ui_style`. La relazione tecnica esplicita è la seguente: `dashboard_tab.py` importa da `style.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 847 che coinvolge il nodo canonico `style.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 847 che coinvolge il nodo canonico `style.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 847 che coinvolge il nodo canonico `style.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 847 che coinvolge il nodo canonico `style.py` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 847 che coinvolge il nodo canonico `style.py` è `L12`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 847 che coinvolge il nodo canonico `style.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 847 che coinvolge il nodo canonico `style.py` è `ast`. Nel grafo del codice RDG, l'arco 847 registra come `source` l'identificatore `ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `style.py`. Nel grafo del codice RDG, l'arco 847 registra come `target` l'identificatore `ui_style` per la relazione che coinvolge il nodo canonico `style.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 847 che coinvolge il nodo canonico `style.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=848 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `style.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `run_tab.py`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab` all'ID `ui_style`. La relazione tecnica esplicita è la seguente: `run_tab.py` importa da `style.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 848 che coinvolge il nodo canonico `style.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 848 che coinvolge il nodo canonico `style.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 848 che coinvolge il nodo canonico `style.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 848 che coinvolge il nodo canonico `style.py` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 848 che coinvolge il nodo canonico `style.py` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 848 che coinvolge il nodo canonico `style.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 848 che coinvolge il nodo canonico `style.py` è `ast`. Nel grafo del codice RDG, l'arco 848 registra come `source` l'identificatore `ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `style.py`. Nel grafo del codice RDG, l'arco 848 registra come `target` l'identificatore `ui_style` per la relazione che coinvolge il nodo canonico `style.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 848 che coinvolge il nodo canonico `style.py` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=422 id=Request-Dataset-Generator::ui_style -->
Nel grafo unificato, l'etichetta originale del nodo canonico `style.py` è `style.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `style.py` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `style.py` è `ui/style.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `style.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `style.py` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `style.py` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `style.py` è `style.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `style.py` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `style.py` è `ui_style`. Nel grafo unificato, il nodo canonico `style.py` possiede l'identificatore esatto `Request-Dataset-Generator::ui_style`.
<!-- GRAPH_EDGE graph=unificata index=845 role=outgoing -->
Nel grafo unificato, il nodo canonico `style.py` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Costanti visuali per la UI CustomTkinter di RDG.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_style` all'ID `Request-Dataset-Generator::ui_style_rationale_1`. La relazione tecnica esplicita è la seguente: `style.py` costituisce una motivazione per `Costanti visuali per la UI CustomTkinter di RDG.`. Nel grafo unificato, il tipo dichiarato dell'arco 845 che coinvolge il nodo canonico `style.py` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 845 che coinvolge il nodo canonico `style.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 845 che coinvolge il nodo canonico `style.py` è `ui/style.py`. Nel grafo unificato, la posizione sorgente dell'arco 845 che coinvolge il nodo canonico `style.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 845 che coinvolge il nodo canonico `style.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 845 che coinvolge il nodo canonico `style.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 845 che coinvolge il nodo canonico `style.py` è `1.0`. Nel grafo unificato, l'arco 845 registra come `source` l'identificatore `Request-Dataset-Generator::ui_style` per la relazione che coinvolge il nodo canonico `style.py`. Nel grafo unificato, l'arco 845 registra come `target` l'identificatore `Request-Dataset-Generator::ui_style_rationale_1` per la relazione che coinvolge il nodo canonico `style.py`.
<!-- GRAPH_EDGE graph=unificata index=846 role=outgoing -->
Nel grafo unificato, il nodo canonico `style.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `config_tab.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_style` all'ID `Request-Dataset-Generator::ui_tabs_config_tab`. La relazione tecnica esplicita è la seguente: `style.py` importa da `config_tab.py`. Nel grafo unificato, il tipo dichiarato dell'arco 846 che coinvolge il nodo canonico `style.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 846 che coinvolge il nodo canonico `style.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 846 che coinvolge il nodo canonico `style.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 846 che coinvolge il nodo canonico `style.py` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 846 che coinvolge il nodo canonico `style.py` è `L8`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 846 che coinvolge il nodo canonico `style.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 846 che coinvolge il nodo canonico `style.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 846 che coinvolge il nodo canonico `style.py` è `1.0`. Nel grafo unificato, l'arco 846 registra come `source` l'identificatore `Request-Dataset-Generator::ui_style` per la relazione che coinvolge il nodo canonico `style.py`. Nel grafo unificato, l'arco 846 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab` per la relazione che coinvolge il nodo canonico `style.py`.
<!-- GRAPH_EDGE graph=unificata index=847 role=outgoing -->
Nel grafo unificato, il nodo canonico `style.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `dashboard_tab.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_style` all'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab`. La relazione tecnica esplicita è la seguente: `style.py` importa da `dashboard_tab.py`. Nel grafo unificato, il tipo dichiarato dell'arco 847 che coinvolge il nodo canonico `style.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 847 che coinvolge il nodo canonico `style.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 847 che coinvolge il nodo canonico `style.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 847 che coinvolge il nodo canonico `style.py` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 847 che coinvolge il nodo canonico `style.py` è `L12`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 847 che coinvolge il nodo canonico `style.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 847 che coinvolge il nodo canonico `style.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 847 che coinvolge il nodo canonico `style.py` è `1.0`. Nel grafo unificato, l'arco 847 registra come `source` l'identificatore `Request-Dataset-Generator::ui_style` per la relazione che coinvolge il nodo canonico `style.py`. Nel grafo unificato, l'arco 847 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `style.py`.
<!-- GRAPH_EDGE graph=unificata index=848 role=outgoing -->
Nel grafo unificato, il nodo canonico `style.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `run_tab.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_style` all'ID `Request-Dataset-Generator::ui_tabs_run_tab`. La relazione tecnica esplicita è la seguente: `style.py` importa da `run_tab.py`. Nel grafo unificato, il tipo dichiarato dell'arco 848 che coinvolge il nodo canonico `style.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 848 che coinvolge il nodo canonico `style.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 848 che coinvolge il nodo canonico `style.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 848 che coinvolge il nodo canonico `style.py` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 848 che coinvolge il nodo canonico `style.py` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 848 che coinvolge il nodo canonico `style.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 848 che coinvolge il nodo canonico `style.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 848 che coinvolge il nodo canonico `style.py` è `1.0`. Nel grafo unificato, l'arco 848 registra come `source` l'identificatore `Request-Dataset-Generator::ui_style` per la relazione che coinvolge il nodo canonico `style.py`. Nel grafo unificato, l'arco 848 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `style.py`.
<!-- GRAPH_EDGE graph=unificata index=822 role=incoming -->
Nel grafo unificato, il nodo canonico `style.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `app.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app` all'ID `Request-Dataset-Generator::ui_style`. La relazione tecnica esplicita è la seguente: `app.py` importa da `style.py`. Nel grafo unificato, il tipo dichiarato dell'arco 822 che coinvolge il nodo canonico `style.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 822 che coinvolge il nodo canonico `style.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 822 che coinvolge il nodo canonico `style.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 822 che coinvolge il nodo canonico `style.py` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 822 che coinvolge il nodo canonico `style.py` è `L13`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 822 che coinvolge il nodo canonico `style.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 822 che coinvolge il nodo canonico `style.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 822 che coinvolge il nodo canonico `style.py` è `1.0`. Nel grafo unificato, l'arco 822 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app` per la relazione che coinvolge il nodo canonico `style.py`. Nel grafo unificato, l'arco 822 registra come `target` l'identificatore `Request-Dataset-Generator::ui_style` per la relazione che coinvolge il nodo canonico `style.py`.

### Tab di configurazione esperimento RDG.

<!-- GRAPH_NODE graph=codice index=443 id=ui_tabs_config_tab_rationale_1 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Tab di configurazione esperimento RDG.` è `Tab di configurazione esperimento RDG.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Tab di configurazione esperimento RDG.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Tab di configurazione esperimento RDG.` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Tab di configurazione esperimento RDG.` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Tab di configurazione esperimento RDG.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Tab di configurazione esperimento RDG.` possiede l'identificatore esatto `ui_tabs_config_tab_rationale_1`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Tab di configurazione esperimento RDG.` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Tab di configurazione esperimento RDG.` è `tab di configurazione esperimento rdg.`.
<!-- GRAPH_EDGE graph=codice index=852 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Tab di configurazione esperimento RDG.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `config_tab.py`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_rationale_1` all'ID `ui_tabs_config_tab`. La relazione tecnica esplicita è la seguente: `Tab di configurazione esperimento RDG.` costituisce una motivazione per `config_tab.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 852 che coinvolge il nodo canonico `Tab di configurazione esperimento RDG.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 852 che coinvolge il nodo canonico `Tab di configurazione esperimento RDG.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 852 che coinvolge il nodo canonico `Tab di configurazione esperimento RDG.` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 852 che coinvolge il nodo canonico `Tab di configurazione esperimento RDG.` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 852 che coinvolge il nodo canonico `Tab di configurazione esperimento RDG.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 852 che coinvolge il nodo canonico `Tab di configurazione esperimento RDG.` è `ast`. Nel grafo del codice RDG, l'arco 852 registra come `source` l'identificatore `ui_tabs_config_tab_rationale_1` per la relazione che coinvolge il nodo canonico `Tab di configurazione esperimento RDG.`. Nel grafo del codice RDG, l'arco 852 registra come `target` l'identificatore `ui_tabs_config_tab` per la relazione che coinvolge il nodo canonico `Tab di configurazione esperimento RDG.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 852 che coinvolge il nodo canonico `Tab di configurazione esperimento RDG.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Tab di configurazione esperimento RDG.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=443 id=Request-Dataset-Generator::ui_tabs_config_tab_rationale_1 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Tab di configurazione esperimento RDG.` è `Tab di configurazione esperimento RDG.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Tab di configurazione esperimento RDG.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Tab di configurazione esperimento RDG.` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Tab di configurazione esperimento RDG.` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Tab di configurazione esperimento RDG.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Tab di configurazione esperimento RDG.` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Tab di configurazione esperimento RDG.` è `tab di configurazione esperimento rdg.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Tab di configurazione esperimento RDG.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Tab di configurazione esperimento RDG.` è `ui_tabs_config_tab_rationale_1`. Nel grafo unificato, il nodo canonico `Tab di configurazione esperimento RDG.` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_config_tab_rationale_1`.
Nel grafo unificato, il nodo canonico `Tab di configurazione esperimento RDG.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=852 role=incoming -->
Nel grafo unificato, il nodo canonico `Tab di configurazione esperimento RDG.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `config_tab.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_rationale_1`. La relazione tecnica esplicita è la seguente: `config_tab.py` costituisce una motivazione per `Tab di configurazione esperimento RDG.`. Nel grafo unificato, il tipo dichiarato dell'arco 852 che coinvolge il nodo canonico `Tab di configurazione esperimento RDG.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 852 che coinvolge il nodo canonico `Tab di configurazione esperimento RDG.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 852 che coinvolge il nodo canonico `Tab di configurazione esperimento RDG.` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 852 che coinvolge il nodo canonico `Tab di configurazione esperimento RDG.` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 852 che coinvolge il nodo canonico `Tab di configurazione esperimento RDG.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 852 che coinvolge il nodo canonico `Tab di configurazione esperimento RDG.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 852 che coinvolge il nodo canonico `Tab di configurazione esperimento RDG.` è `1.0`. Nel grafo unificato, l'arco 852 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab` per la relazione che coinvolge il nodo canonico `Tab di configurazione esperimento RDG.`. Nel grafo unificato, l'arco 852 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_rationale_1` per la relazione che coinvolge il nodo canonico `Tab di configurazione esperimento RDG.`.

### Tab di esecuzione pipeline RDG.

<!-- GRAPH_NODE graph=codice index=492 id=ui_tabs_run_tab_rationale_1 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Tab di esecuzione pipeline RDG.` è `Tab di esecuzione pipeline RDG.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Tab di esecuzione pipeline RDG.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Tab di esecuzione pipeline RDG.` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Tab di esecuzione pipeline RDG.` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Tab di esecuzione pipeline RDG.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Tab di esecuzione pipeline RDG.` possiede l'identificatore esatto `ui_tabs_run_tab_rationale_1`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Tab di esecuzione pipeline RDG.` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Tab di esecuzione pipeline RDG.` è `tab di esecuzione pipeline rdg.`.
<!-- GRAPH_EDGE graph=codice index=927 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Tab di esecuzione pipeline RDG.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `run_tab.py`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_rationale_1` all'ID `ui_tabs_run_tab`. La relazione tecnica esplicita è la seguente: `Tab di esecuzione pipeline RDG.` costituisce una motivazione per `run_tab.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 927 che coinvolge il nodo canonico `Tab di esecuzione pipeline RDG.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 927 che coinvolge il nodo canonico `Tab di esecuzione pipeline RDG.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 927 che coinvolge il nodo canonico `Tab di esecuzione pipeline RDG.` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 927 che coinvolge il nodo canonico `Tab di esecuzione pipeline RDG.` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 927 che coinvolge il nodo canonico `Tab di esecuzione pipeline RDG.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 927 che coinvolge il nodo canonico `Tab di esecuzione pipeline RDG.` è `ast`. Nel grafo del codice RDG, l'arco 927 registra come `source` l'identificatore `ui_tabs_run_tab_rationale_1` per la relazione che coinvolge il nodo canonico `Tab di esecuzione pipeline RDG.`. Nel grafo del codice RDG, l'arco 927 registra come `target` l'identificatore `ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `Tab di esecuzione pipeline RDG.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 927 che coinvolge il nodo canonico `Tab di esecuzione pipeline RDG.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Tab di esecuzione pipeline RDG.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=492 id=Request-Dataset-Generator::ui_tabs_run_tab_rationale_1 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Tab di esecuzione pipeline RDG.` è `Tab di esecuzione pipeline RDG.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Tab di esecuzione pipeline RDG.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Tab di esecuzione pipeline RDG.` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Tab di esecuzione pipeline RDG.` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Tab di esecuzione pipeline RDG.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Tab di esecuzione pipeline RDG.` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Tab di esecuzione pipeline RDG.` è `tab di esecuzione pipeline rdg.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Tab di esecuzione pipeline RDG.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Tab di esecuzione pipeline RDG.` è `ui_tabs_run_tab_rationale_1`. Nel grafo unificato, il nodo canonico `Tab di esecuzione pipeline RDG.` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_rationale_1`.
Nel grafo unificato, il nodo canonico `Tab di esecuzione pipeline RDG.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=927 role=incoming -->
Nel grafo unificato, il nodo canonico `Tab di esecuzione pipeline RDG.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `run_tab.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_rationale_1`. La relazione tecnica esplicita è la seguente: `run_tab.py` costituisce una motivazione per `Tab di esecuzione pipeline RDG.`. Nel grafo unificato, il tipo dichiarato dell'arco 927 che coinvolge il nodo canonico `Tab di esecuzione pipeline RDG.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 927 che coinvolge il nodo canonico `Tab di esecuzione pipeline RDG.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 927 che coinvolge il nodo canonico `Tab di esecuzione pipeline RDG.` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 927 che coinvolge il nodo canonico `Tab di esecuzione pipeline RDG.` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 927 che coinvolge il nodo canonico `Tab di esecuzione pipeline RDG.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 927 che coinvolge il nodo canonico `Tab di esecuzione pipeline RDG.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 927 che coinvolge il nodo canonico `Tab di esecuzione pipeline RDG.` è `1.0`. Nel grafo unificato, l'arco 927 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `Tab di esecuzione pipeline RDG.`. Nel grafo unificato, l'arco 927 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_rationale_1` per la relazione che coinvolge il nodo canonico `Tab di esecuzione pipeline RDG.`.

### ui_config_bridge.py

<!-- GRAPH_NODE graph=codice index=495 id=ui_ui_config_bridge -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `ui_config_bridge.py` è `ui_config_bridge.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `ui_config_bridge.py` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `ui_config_bridge.py` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `ui_config_bridge.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `ui_config_bridge.py` vale `ast`. Nel grafo del codice RDG, il nodo canonico `ui_config_bridge.py` possiede l'identificatore esatto `ui_ui_config_bridge`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `ui_config_bridge.py` alla comunità numerica `3`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `ui_config_bridge.py` è `ui_config_bridge.py`.
<!-- GRAPH_EDGE graph=codice index=986 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ui_config_bridge.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_defaults()`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge` all'ID `ui_ui_config_bridge_defaults`. La relazione tecnica esplicita è la seguente: `ui_config_bridge.py` contiene `_defaults()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 986 che coinvolge il nodo canonico `ui_config_bridge.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 986 che coinvolge il nodo canonico `ui_config_bridge.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 986 che coinvolge il nodo canonico `ui_config_bridge.py` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 986 che coinvolge il nodo canonico `ui_config_bridge.py` è `L42`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 986 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 986 che coinvolge il nodo canonico `ui_config_bridge.py` è `ast`. Nel grafo del codice RDG, l'arco 986 registra come `source` l'identificatore `ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo del codice RDG, l'arco 986 registra come `target` l'identificatore `ui_ui_config_bridge_defaults` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 986 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=987 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ui_config_bridge.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge` all'ID `ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `ui_config_bridge.py` contiene `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 987 che coinvolge il nodo canonico `ui_config_bridge.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 987 che coinvolge il nodo canonico `ui_config_bridge.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 987 che coinvolge il nodo canonico `ui_config_bridge.py` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 987 che coinvolge il nodo canonico `ui_config_bridge.py` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 987 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 987 che coinvolge il nodo canonico `ui_config_bridge.py` è `ast`. Nel grafo del codice RDG, l'arco 987 registra come `source` l'identificatore `ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo del codice RDG, l'arco 987 registra come `target` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 987 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=988 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ui_config_bridge.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `save_config()`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge` all'ID `ui_ui_config_bridge_save_config`. La relazione tecnica esplicita è la seguente: `ui_config_bridge.py` contiene `save_config()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 988 che coinvolge il nodo canonico `ui_config_bridge.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 988 che coinvolge il nodo canonico `ui_config_bridge.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 988 che coinvolge il nodo canonico `ui_config_bridge.py` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 988 che coinvolge il nodo canonico `ui_config_bridge.py` è `L34`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 988 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 988 che coinvolge il nodo canonico `ui_config_bridge.py` è `ast`. Nel grafo del codice RDG, l'arco 988 registra come `source` l'identificatore `ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo del codice RDG, l'arco 988 registra come `target` l'identificatore `ui_ui_config_bridge_save_config` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 988 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=851 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ui_config_bridge.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `config_tab.py`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab` all'ID `ui_ui_config_bridge`. La relazione tecnica esplicita è la seguente: `config_tab.py` importa da `ui_config_bridge.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 851 che coinvolge il nodo canonico `ui_config_bridge.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 851 che coinvolge il nodo canonico `ui_config_bridge.py` è `submodule_import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 851 che coinvolge il nodo canonico `ui_config_bridge.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 851 che coinvolge il nodo canonico `ui_config_bridge.py` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 851 che coinvolge il nodo canonico `ui_config_bridge.py` è `L7`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 851 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 851 che coinvolge il nodo canonico `ui_config_bridge.py` è `ast`. Nel grafo del codice RDG, l'arco 851 registra come `source` l'identificatore `ui_tabs_config_tab` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo del codice RDG, l'arco 851 registra come `target` l'identificatore `ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 851 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=884 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ui_config_bridge.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `dashboard_tab.py`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_dashboard_tab` all'ID `ui_ui_config_bridge`. La relazione tecnica esplicita è la seguente: `dashboard_tab.py` importa da `ui_config_bridge.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 884 che coinvolge il nodo canonico `ui_config_bridge.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 884 che coinvolge il nodo canonico `ui_config_bridge.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 884 che coinvolge il nodo canonico `ui_config_bridge.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 884 che coinvolge il nodo canonico `ui_config_bridge.py` è `ui/tabs/dashboard_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 884 che coinvolge il nodo canonico `ui_config_bridge.py` è `L11`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 884 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 884 che coinvolge il nodo canonico `ui_config_bridge.py` è `ast`. Nel grafo del codice RDG, l'arco 884 registra come `source` l'identificatore `ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo del codice RDG, l'arco 884 registra come `target` l'identificatore `ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 884 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=925 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ui_config_bridge.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `run_tab.py`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab` all'ID `ui_ui_config_bridge`. La relazione tecnica esplicita è la seguente: `run_tab.py` importa da `ui_config_bridge.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 925 che coinvolge il nodo canonico `ui_config_bridge.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 925 che coinvolge il nodo canonico `ui_config_bridge.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 925 che coinvolge il nodo canonico `ui_config_bridge.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 925 che coinvolge il nodo canonico `ui_config_bridge.py` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 925 che coinvolge il nodo canonico `ui_config_bridge.py` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 925 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 925 che coinvolge il nodo canonico `ui_config_bridge.py` è `ast`. Nel grafo del codice RDG, l'arco 925 registra come `source` l'identificatore `ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo del codice RDG, l'arco 925 registra come `target` l'identificatore `ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 925 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=989 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ui_config_bridge.py` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla`; l'orientamento registrato nel JSON va dall'ID `ui_ui_config_bridge_rationale_1` all'ID `ui_ui_config_bridge`. La relazione tecnica esplicita è la seguente: `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla` costituisce una motivazione per `ui_config_bridge.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 989 che coinvolge il nodo canonico `ui_config_bridge.py` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 989 che coinvolge il nodo canonico `ui_config_bridge.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 989 che coinvolge il nodo canonico `ui_config_bridge.py` è `ui/ui_config_bridge.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 989 che coinvolge il nodo canonico `ui_config_bridge.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 989 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 989 che coinvolge il nodo canonico `ui_config_bridge.py` è `ast`. Nel grafo del codice RDG, l'arco 989 registra come `source` l'identificatore `ui_ui_config_bridge_rationale_1` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo del codice RDG, l'arco 989 registra come `target` l'identificatore `ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 989 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=495 id=Request-Dataset-Generator::ui_ui_config_bridge -->
Nel grafo unificato, l'etichetta originale del nodo canonico `ui_config_bridge.py` è `ui_config_bridge.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `ui_config_bridge.py` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `ui_config_bridge.py` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `ui_config_bridge.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `ui_config_bridge.py` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `ui_config_bridge.py` alla comunità numerica `3`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `ui_config_bridge.py` è `ui_config_bridge.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `ui_config_bridge.py` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `ui_config_bridge.py` è `ui_ui_config_bridge`. Nel grafo unificato, il nodo canonico `ui_config_bridge.py` possiede l'identificatore esatto `Request-Dataset-Generator::ui_ui_config_bridge`.
<!-- GRAPH_EDGE graph=unificata index=986 role=outgoing -->
Nel grafo unificato, il nodo canonico `ui_config_bridge.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_defaults()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_defaults`. La relazione tecnica esplicita è la seguente: `ui_config_bridge.py` contiene `_defaults()`. Nel grafo unificato, il tipo dichiarato dell'arco 986 che coinvolge il nodo canonico `ui_config_bridge.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 986 che coinvolge il nodo canonico `ui_config_bridge.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 986 che coinvolge il nodo canonico `ui_config_bridge.py` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 986 che coinvolge il nodo canonico `ui_config_bridge.py` è `L42`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 986 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 986 che coinvolge il nodo canonico `ui_config_bridge.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 986 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo unificato, l'arco 986 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo unificato, l'arco 986 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_defaults` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`.
<!-- GRAPH_EDGE graph=unificata index=987 role=outgoing -->
Nel grafo unificato, il nodo canonico `ui_config_bridge.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `ui_config_bridge.py` contiene `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 987 che coinvolge il nodo canonico `ui_config_bridge.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 987 che coinvolge il nodo canonico `ui_config_bridge.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 987 che coinvolge il nodo canonico `ui_config_bridge.py` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 987 che coinvolge il nodo canonico `ui_config_bridge.py` è `L15`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 987 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 987 che coinvolge il nodo canonico `ui_config_bridge.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 987 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo unificato, l'arco 987 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo unificato, l'arco 987 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`.
<!-- GRAPH_EDGE graph=unificata index=988 role=outgoing -->
Nel grafo unificato, il nodo canonico `ui_config_bridge.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `save_config()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_save_config`. La relazione tecnica esplicita è la seguente: `ui_config_bridge.py` contiene `save_config()`. Nel grafo unificato, il tipo dichiarato dell'arco 988 che coinvolge il nodo canonico `ui_config_bridge.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 988 che coinvolge il nodo canonico `ui_config_bridge.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 988 che coinvolge il nodo canonico `ui_config_bridge.py` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 988 che coinvolge il nodo canonico `ui_config_bridge.py` è `L34`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 988 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 988 che coinvolge il nodo canonico `ui_config_bridge.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 988 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo unificato, l'arco 988 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo unificato, l'arco 988 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_save_config` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`.
<!-- GRAPH_EDGE graph=unificata index=989 role=outgoing -->
Nel grafo unificato, il nodo canonico `ui_config_bridge.py` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_ui_config_bridge` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_rationale_1`. La relazione tecnica esplicita è la seguente: `ui_config_bridge.py` costituisce una motivazione per `Bridge tra UI e file di configurazione json dell'esperimento. Non importa nulla`. Nel grafo unificato, il tipo dichiarato dell'arco 989 che coinvolge il nodo canonico `ui_config_bridge.py` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 989 che coinvolge il nodo canonico `ui_config_bridge.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 989 che coinvolge il nodo canonico `ui_config_bridge.py` è `ui/ui_config_bridge.py`. Nel grafo unificato, la posizione sorgente dell'arco 989 che coinvolge il nodo canonico `ui_config_bridge.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 989 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 989 che coinvolge il nodo canonico `ui_config_bridge.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 989 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo unificato, l'arco 989 registra come `source` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo unificato, l'arco 989 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_rationale_1` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`.
<!-- GRAPH_EDGE graph=unificata index=851 role=incoming -->
Nel grafo unificato, il nodo canonico `ui_config_bridge.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `config_tab.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab` all'ID `Request-Dataset-Generator::ui_ui_config_bridge`. La relazione tecnica esplicita è la seguente: `config_tab.py` importa da `ui_config_bridge.py`. Nel grafo unificato, il tipo dichiarato dell'arco 851 che coinvolge il nodo canonico `ui_config_bridge.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 851 che coinvolge il nodo canonico `ui_config_bridge.py` è `submodule_import`. Nel grafo unificato, la confidenza categoriale dell'arco 851 che coinvolge il nodo canonico `ui_config_bridge.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 851 che coinvolge il nodo canonico `ui_config_bridge.py` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 851 che coinvolge il nodo canonico `ui_config_bridge.py` è `L7`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 851 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 851 che coinvolge il nodo canonico `ui_config_bridge.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 851 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo unificato, l'arco 851 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo unificato, l'arco 851 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`.
<!-- GRAPH_EDGE graph=unificata index=884 role=incoming -->
Nel grafo unificato, il nodo canonico `ui_config_bridge.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `dashboard_tab.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_dashboard_tab` all'ID `Request-Dataset-Generator::ui_ui_config_bridge`. La relazione tecnica esplicita è la seguente: `dashboard_tab.py` importa da `ui_config_bridge.py`. Nel grafo unificato, il tipo dichiarato dell'arco 884 che coinvolge il nodo canonico `ui_config_bridge.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 884 che coinvolge il nodo canonico `ui_config_bridge.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 884 che coinvolge il nodo canonico `ui_config_bridge.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 884 che coinvolge il nodo canonico `ui_config_bridge.py` è `ui/tabs/dashboard_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 884 che coinvolge il nodo canonico `ui_config_bridge.py` è `L11`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 884 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 884 che coinvolge il nodo canonico `ui_config_bridge.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 884 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo unificato, l'arco 884 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_dashboard_tab` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo unificato, l'arco 884 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`.
<!-- GRAPH_EDGE graph=unificata index=925 role=incoming -->
Nel grafo unificato, il nodo canonico `ui_config_bridge.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `run_tab.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab` all'ID `Request-Dataset-Generator::ui_ui_config_bridge`. La relazione tecnica esplicita è la seguente: `run_tab.py` importa da `ui_config_bridge.py`. Nel grafo unificato, il tipo dichiarato dell'arco 925 che coinvolge il nodo canonico `ui_config_bridge.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 925 che coinvolge il nodo canonico `ui_config_bridge.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 925 che coinvolge il nodo canonico `ui_config_bridge.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 925 che coinvolge il nodo canonico `ui_config_bridge.py` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 925 che coinvolge il nodo canonico `ui_config_bridge.py` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 925 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 925 che coinvolge il nodo canonico `ui_config_bridge.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 925 che coinvolge il nodo canonico `ui_config_bridge.py` è `1.0`. Nel grafo unificato, l'arco 925 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`. Nel grafo unificato, l'arco 925 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge` per la relazione che coinvolge il nodo canonico `ui_config_bridge.py`.
