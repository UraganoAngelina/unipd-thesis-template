# Architettura e codice del framework RDG — parte 5

## Metadati

- Dominio: Architettura e codice del framework RDG.
- Data di generazione: 2026-07-31.
- Versione dei grafi sorgente: KB documentale `5f9434fc0f01de5d74dd3c6d72fffc217b7f8629`; KB codice RDG `06d5dd91cef2e12574b63a36d73b049a3588cdf8`; KB unificata `null`.
- Nodi coperti: 87 nodi canonici e 174 occorrenze fisiche; tipi canonici: code: 50, concept: 23, document: 4, rationale: 10.
- Archi coperti: 304 occorrenze fisiche incidenti ai nodi del documento; tipi: calls: 104, conceptually_related_to: 4, contains: 28, implements: 2, imports: 6, imports_from: 4, method: 66, rationale_for: 20, references: 54, semantically_similar_to: 14, uses: 2.
- Iperarchi coperti: 2 occorrenze fisiche, replicate anche negli altri file coinvolti quando attraversano la segmentazione editoriale.
- Nota sulla direzione: tutti e tre i grafi dichiarano `directed=false`; la prosa conserva comunque l'orientamento dei campi JSON `source` e `target` senza trasformarlo in causalità.

## Glossario

- EXTRACTED — Valore della proprietà `confidence` che il grafo assegna a una relazione estratta.
- INFERRED — Valore della proprietà `confidence` che il grafo assegna a una relazione inferita; la prosa non promuove una relazione INFERRED a fatto estratto.
- RDG — Nome editoriale canonico del repository Request-Dataset-Generator.
- AST — Valore di provenienza `_origin=ast` registrato dal grafo.
- FHIR — Nome canonico dell'acronimo usato negli identificatori RDG.
- RVE — Nome canonico del prefisso usato per i flussi RDG.
- UI — Nome canonico dell'acronimo usato nei moduli di interfaccia.

### Mappa dei nomi canonici e delle varianti

La mappa seguente non fonde nodi solo perché semanticamente simili. Le occorrenze del grafo sorgente e del grafo unificato sono ricondotte allo stesso nome soltanto tramite `repo` e `local_id`; le etichette duplicate di nodi distinti sono disambiguate con l'identificatore locale.

- Il nome canonico `.__init__() [log_collector_frameworklogparser_init]` identifica il nodo `Request-Dataset-Generator::log_collector_frameworklogparser_init`; le etichette originali conservate sono `.__init__()`.
- Il nome canonico `.__init__() [log_collector_middlewarelogparser_init]` identifica il nodo `Request-Dataset-Generator::log_collector_middlewarelogparser_init`; le etichette originali conservate sono `.__init__()`.
- Il nome canonico `.aggregate()` identifica il nodo `Request-Dataset-Generator::log_collector_timeseriesaggregator_aggregate`; le etichette originali conservate sono `.aggregate()`.
- Il nome canonico `.build()` identifica il nodo `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build`; le etichette originali conservate sono `.build()`.
- Il nome canonico `.build_flows()` identifica il nodo `Request-Dataset-Generator::log_collector_logjoiner_build_flows`; le etichette originali conservate sono `.build_flows()`.
- Il nome canonico `.join()` identifica il nodo `Request-Dataset-Generator::log_collector_logjoiner_join`; le etichette originali conservate sono `.join()`.
- Il nome canonico `.parse() [log_collector_frameworklogparser_parse]` identifica il nodo `Request-Dataset-Generator::log_collector_frameworklogparser_parse`; le etichette originali conservate sono `.parse()`.
- Il nome canonico `.parse() [log_collector_middlewarelogparser_parse]` identifica il nodo `Request-Dataset-Generator::log_collector_middlewarelogparser_parse`; le etichette originali conservate sono `.parse()`.
- Il nome canonico `_build_parser() [log_collector_build_parser]` identifica il nodo `Request-Dataset-Generator::log_collector_build_parser`; le etichette originali conservate sono `_build_parser()`.
- Il nome canonico `_first_present()` identifica il nodo `Request-Dataset-Generator::log_collector_first_present`; le etichette originali conservate sono `_first_present()`.
- Il nome canonico `_framework_event_key()` identifica il nodo `Request-Dataset-Generator::log_collector_framework_event_key`; le etichette originali conservate sono `_framework_event_key()`.
- Il nome canonico `_load_config() [log_collector_load_config]` identifica il nodo `Request-Dataset-Generator::log_collector_load_config`; le etichette originali conservate sono `_load_config()`.
- Il nome canonico `_percentile()` identifica il nodo `Request-Dataset-Generator::log_collector_percentile`; le etichette originali conservate sono `_percentile()`.
- Il nome canonico `_resolve_output_path() [log_collector_resolve_output_path]` identifica il nodo `Request-Dataset-Generator::log_collector_resolve_output_path`; le etichette originali conservate sono `_resolve_output_path()`.
- Il nome canonico `Aggrega i record normalizzati in bucket temporali.` identifica il nodo `Request-Dataset-Generator::log_collector_rationale_324`; le etichette originali conservate sono `Aggrega i record normalizzati in bucket temporali.`.
- Il nome canonico `Any [log_collector_py_any]` identifica il nodo `Request-Dataset-Generator::log_collector_py_any`; le etichette originali conservate sono `Any`.
- Il nome canonico `ArgumentParser [log_collector_py_argumentparser]` identifica il nodo `Request-Dataset-Generator::log_collector_py_argumentparser`; le etichette originali conservate sono `ArgumentParser`.
- Il nome canonico `Assembla il JSON finale del Normalized Metrics Dataset.` identifica il nodo `Request-Dataset-Generator::log_collector_rationale_498`; le etichette originali conservate sono `Assembla il JSON finale del Normalized Metrics Dataset.`.
- Il nome canonico `Calcola il percentile p da una lista già ordinata.` identifica il nodo `Request-Dataset-Generator::log_collector_rationale_482`; le etichette originali conservate sono `Calcola il percentile p da una lista già ordinata.`.
- Il nome canonico `collect_logs()` identifica il nodo `Request-Dataset-Generator::log_collector_collect_logs`; le etichette originali conservate sono `collect_logs()`.
- Il nome canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` identifica il nodo `Request-Dataset-Generator::log_collector_rationale_254`; le etichette originali conservate sono `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con`.
- Il nome canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` identifica il nodo `Request-Dataset-Generator::log_collector_rationale_591`; le etichette originali conservate sono `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.`.
- Il nome canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` identifica il nodo `Request-Dataset-Generator::log_collector_rationale_161`; le etichette originali conservate sono `Fa il join tra framework log e middleware log su request_id.      Produce la lis`.
- Il nome canonico `FrameworkLogParser` identifica il nodo `Request-Dataset-Generator::log_collector_frameworklogparser`; le etichette originali conservate sono `FrameworkLogParser`.
- Il nome canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` identifica il nodo `Request-Dataset-Generator::log_collector_rationale_127`; le etichette originali conservate sono `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke`.
- Il nome canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` identifica il nodo `Request-Dataset-Generator::log_collector_rationale_54`; le etichette originali conservate sono `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r`.
- Il nome canonico `log_collector.py` identifica il nodo `Request-Dataset-Generator::log_collector`; le etichette originali conservate sono `log_collector.py`.
- Il nome canonico `LogJoiner` identifica il nodo `Request-Dataset-Generator::log_collector_logjoiner`; le etichette originali conservate sono `LogJoiner`.
- Il nome canonico `main() [log_collector_main]` identifica il nodo `Request-Dataset-Generator::log_collector_main`; le etichette originali conservate sono `main()`.
- Il nome canonico `MiddlewareLogParser` identifica il nodo `Request-Dataset-Generator::log_collector_middlewarelogparser`; le etichette originali conservate sono `MiddlewareLogParser`.
- Il nome canonico `NormalizedDatasetBuilder` identifica il nodo `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder`; le etichette originali conservate sono `NormalizedDatasetBuilder`.
- Il nome canonico `TimeseriesAggregator` identifica il nodo `Request-Dataset-Generator::log_collector_timeseriesaggregator`; le etichette originali conservate sono `TimeseriesAggregator`.
- Il nome canonico `.__init__() [ui_tabs_run_tab_runtab_init]` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_init`; le etichette originali conservate sono `.__init__()`.
- Il nome canonico `._build() [ui_tabs_run_tab_runtab_build]` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build`; le etichette originali conservate sono `._build()`.
- Il nome canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_buttons`; le etichette originali conservate sono `._build_buttons()`.
- Il nome canonico `._build_log()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_log`; le etichette originali conservate sono `._build_log()`.
- Il nome canonico `._build_progress()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_progress`; le etichette originali conservate sono `._build_progress()`.
- Il nome canonico `._build_step_card()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_step_card`; le etichette originali conservate sono `._build_step_card()`.
- Il nome canonico `._clear_log()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_clear_log`; le etichette originali conservate sono `._clear_log()`.
- Il nome canonico `._configure_log_tags()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_configure_log_tags`; le etichette originali conservate sono `._configure_log_tags()`.
- Il nome canonico `._pipeline_worker()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker`; le etichette originali conservate sono `._pipeline_worker()`.
- Il nome canonico `._prepare_run()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_prepare_run`; le etichette originali conservate sono `._prepare_run()`.
- Il nome canonico `._run_step_1()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_1`; le etichette originali conservate sono `._run_step_1()`.
- Il nome canonico `._run_step_2()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_2`; le etichette originali conservate sono `._run_step_2()`.
- Il nome canonico `._run_step_3()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_3`; le etichette originali conservate sono `._run_step_3()`.
- Il nome canonico `._run_step_4()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_4`; le etichette originali conservate sono `._run_step_4()`.
- Il nome canonico `._set_log_placeholder()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_log_placeholder`; le etichette originali conservate sono `._set_log_placeholder()`.
- Il nome canonico `._set_status_now()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_status_now`; le etichette originali conservate sono `._set_status_now()`.
- Il nome canonico `._single_step_worker()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker`; le etichette originali conservate sono `._single_step_worker()`.
- Il nome canonico `._start_single_step()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_single_step`; le etichette originali conservate sono `._start_single_step()`.
- Il nome canonico `._ui_finish()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_finish`; le etichette originali conservate sono `._ui_finish()`.
- Il nome canonico `._ui_log()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log`; le etichette originali conservate sono `._ui_log()`.
- Il nome canonico `._ui_set_progress()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_progress`; le etichette originali conservate sono `._ui_set_progress()`.
- Il nome canonico `._ui_set_status()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_status`; le etichette originali conservate sono `._ui_set_status()`.
- Il nome canonico `._write_mock_outputs()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_write_mock_outputs`; le etichette originali conservate sono `._write_mock_outputs()`.
- Il nome canonico `.reset()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_reset`; le etichette originali conservate sono `.reset()`.
- Il nome canonico `.start_pipeline()` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_pipeline`; le etichette originali conservate sono `.start_pipeline()`.
- Il nome canonico `Accoda l'aggiornamento progress nel main thread Tk.` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_rationale_377`; le etichette originali conservate sono `Accoda l'aggiornamento progress nel main thread Tk.`.
- Il nome canonico `Controllo pipeline con progress bar e log thread-safe.` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_rationale_43`; le etichette originali conservate sono `Controllo pipeline con progress bar e log thread-safe.`.
- Il nome canonico `RunTab` identifica il nodo `Request-Dataset-Generator::ui_tabs_run_tab_runtab`; le etichette originali conservate sono `RunTab`.
- Il nome canonico `aiohttp` identifica il nodo `Request-Dataset-Generator::requirements_aiohttp`; le etichette originali conservate sono `aiohttp`.
- Il nome canonico `cryptography` identifica il nodo `Request-Dataset-Generator::requirements_cryptography`; le etichette originali conservate sono `cryptography`.
- Il nome canonico `customtkinter` identifica il nodo `Request-Dataset-Generator::ui_requirements_customtkinter`; le etichette originali conservate sono `customtkinter`.
- Il nome canonico `Direct Integrations` identifica il nodo `Request-Dataset-Generator::usage_direct_integrations`; le etichette originali conservate sono `Direct Integrations`.
- Il nome canonico `Fault Injection` identifica il nodo `Request-Dataset-Generator::usage_fault_injection`; le etichette originali conservate sono `Fault Injection`.
- Il nome canonico `FHIR Synthetic Patients` identifica il nodo `Request-Dataset-Generator::readme_fhir_synthetic_patients`; le etichette originali conservate sono `FHIR Synthetic Patients`.
- Il nome canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` identifica il nodo `Request-Dataset-Generator::readme_flow_context_call`; le etichette originali conservate sono `FLOW_CONTEXT_CALL`.
- Il nome canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` identifica il nodo `Request-Dataset-Generator::usage_flow_context_call`; le etichette originali conservate sono `FLOW_CONTEXT_CALL`.
- Il nome canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` identifica il nodo `Request-Dataset-Generator::readme_flow_patient_create`; le etichette originali conservate sono `FLOW_PATIENT_CREATE`.
- Il nome canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` identifica il nodo `Request-Dataset-Generator::usage_flow_patient_create`; le etichette originali conservate sono `FLOW_PATIENT_CREATE`.
- Il nome canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` identifica il nodo `Request-Dataset-Generator::readme_flow_patient_query`; le etichette originali conservate sono `FLOW_PATIENT_QUERY`.
- Il nome canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` identifica il nodo `Request-Dataset-Generator::usage_flow_patient_query`; le etichette originali conservate sono `FLOW_PATIENT_QUERY`.
- Il nome canonico `main.py Command Dispatch` identifica il nodo `Request-Dataset-Generator::usage_command_dispatch`; le etichette originali conservate sono `main.py Command Dispatch`.
- Il nome canonico `matplotlib [requirements_matplotlib]` identifica il nodo `Request-Dataset-Generator::requirements_matplotlib`; le etichette originali conservate sono `matplotlib`.
- Il nome canonico `matplotlib [ui_requirements_matplotlib]` identifica il nodo `Request-Dataset-Generator::ui_requirements_matplotlib`; le etichette originali conservate sono `matplotlib`.
- Il nome canonico `Middleware Integrations` identifica il nodo `Request-Dataset-Generator::usage_middleware_integrations`; le etichette originali conservate sono `Middleware Integrations`.
- Il nome canonico `mTLS IAP` identifica il nodo `Request-Dataset-Generator::usage_mtls_iap`; le etichette originali conservate sono `mTLS IAP`.
- Il nome canonico `mTLS with IAP` identifica il nodo `Request-Dataset-Generator::readme_mtls_iap`; le etichette originali conservate sono `mTLS with IAP`.
- Il nome canonico `Pipeline [readme_pipeline]` identifica il nodo `Request-Dataset-Generator::readme_pipeline`; le etichette originali conservate sono `Pipeline`.
- Il nome canonico `Pipeline [usage_pipeline]` identifica il nodo `Request-Dataset-Generator::usage_pipeline`; le etichette originali conservate sono `Pipeline`.
- Il nome canonico `Placeholder Resolution` identifica il nodo `Request-Dataset-Generator::usage_placeholder_resolution`; le etichette originali conservate sono `Placeholder Resolution`.
- Il nome canonico `Request Dataset Generator RVE` identifica il nodo `Request-Dataset-Generator::readme_request_dataset_generator_rve`; le etichette originali conservate sono `Request Dataset Generator RVE`.
- Il nome canonico `Request Dataset Generator RVE Guide` identifica il nodo `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide`; le etichette originali conservate sono `Request Dataset Generator RVE Guide`.
- Il nome canonico `Runtime Dependencies` identifica il nodo `Request-Dataset-Generator::requirements_runtime_dependencies`; le etichette originali conservate sono `Runtime Dependencies`.
- Il nome canonico `RVE Flows [readme_rve_flows]` identifica il nodo `Request-Dataset-Generator::readme_rve_flows`; le etichette originali conservate sono `RVE Flows`.
- Il nome canonico `RVE Flows [usage_rve_flows]` identifica il nodo `Request-Dataset-Generator::usage_rve_flows`; le etichette originali conservate sono `RVE Flows`.
- Il nome canonico `UI Dependencies` identifica il nodo `Request-Dataset-Generator::ui_requirements_ui_dependencies`; le etichette originali conservate sono `UI Dependencies`.

## Iperarchi del dominio

<!-- GRAPH_HYPEREDGE graph=codice index=0 copy=07_architettura_rdg_parte5.md -->
Nel grafo del codice RDG, l'iperarco `Supported Core RVE Flows` con identificatore `supported_core_rve_flows` comprende esattamente i nodi `FLOW_CONTEXT_CALL [readme_flow_context_call]` (ID locale `readme_flow_context_call`), `FLOW_PATIENT_QUERY [readme_flow_patient_query]` (ID locale `readme_flow_patient_query`), `FLOW_PATIENT_CREATE [readme_flow_patient_create]` (ID locale `readme_flow_patient_create`), `FLOW_CONTEXT_CALL [usage_flow_context_call]` (ID locale `usage_flow_context_call`), `FLOW_PATIENT_QUERY [usage_flow_patient_query]` (ID locale `usage_flow_patient_query`), `FLOW_PATIENT_CREATE [usage_flow_patient_create]` (ID locale `usage_flow_patient_create`). Nel grafo del codice RDG, l'iperarco `Supported Core RVE Flows` con identificatore `supported_core_rve_flows` dichiara la relazione di gruppo `form`; il grafo associa tutti i membri elencati all'etichetta dell'iperarco senza specificare una coppia `source`-`target`. Nel grafo del codice RDG, la proprietà `id` di l'iperarco `Supported Core RVE Flows` con identificatore `supported_core_rve_flows` vale `supported_core_rve_flows`. Nel grafo del codice RDG, la proprietà `label` di l'iperarco `Supported Core RVE Flows` con identificatore `supported_core_rve_flows` vale `Supported Core RVE Flows`. Nel grafo del codice RDG, la proprietà `nodes` di l'iperarco `Supported Core RVE Flows` con identificatore `supported_core_rve_flows` conserva nell'ordine il valore esatto `["readme_flow_context_call", "readme_flow_patient_query", "readme_flow_patient_create", "usage_flow_context_call", "usage_flow_patient_query", "usage_flow_patient_create"]`. Nel grafo del codice RDG, la proprietà `relation` di l'iperarco `Supported Core RVE Flows` con identificatore `supported_core_rve_flows` vale `form`. Nel grafo del codice RDG, la confidenza categoriale di l'iperarco `Supported Core RVE Flows` con identificatore `supported_core_rve_flows` vale `EXTRACTED`. Nel grafo del codice RDG, il punteggio di confidenza di l'iperarco `Supported Core RVE Flows` con identificatore `supported_core_rve_flows` vale `1.0`. Nel grafo del codice RDG, il file sorgente di l'iperarco `Supported Core RVE Flows` con identificatore `supported_core_rve_flows` è `README.md`.

<!-- GRAPH_HYPEREDGE graph=codice index=1 copy=07_architettura_rdg_parte5.md -->
Nel grafo del codice RDG, l'iperarco `Complete Pipeline Stages` con identificatore `complete_pipeline_stages` comprende esattamente i nodi `Pipeline [readme_pipeline]` (ID locale `readme_pipeline`), `Pipeline [usage_pipeline]` (ID locale `usage_pipeline`), `main.py Command Dispatch` (ID locale `usage_command_dispatch`). Nel grafo del codice RDG, l'iperarco `Complete Pipeline Stages` con identificatore `complete_pipeline_stages` dichiara la relazione di gruppo `implement`; il grafo associa tutti i membri elencati all'etichetta dell'iperarco senza specificare una coppia `source`-`target`. Nel grafo del codice RDG, la proprietà `id` di l'iperarco `Complete Pipeline Stages` con identificatore `complete_pipeline_stages` vale `complete_pipeline_stages`. Nel grafo del codice RDG, la proprietà `label` di l'iperarco `Complete Pipeline Stages` con identificatore `complete_pipeline_stages` vale `Complete Pipeline Stages`. Nel grafo del codice RDG, la proprietà `nodes` di l'iperarco `Complete Pipeline Stages` con identificatore `complete_pipeline_stages` conserva nell'ordine il valore esatto `["readme_pipeline", "usage_pipeline", "usage_command_dispatch"]`. Nel grafo del codice RDG, la proprietà `relation` di l'iperarco `Complete Pipeline Stages` con identificatore `complete_pipeline_stages` vale `implement`. Nel grafo del codice RDG, la confidenza categoriale di l'iperarco `Complete Pipeline Stages` con identificatore `complete_pipeline_stages` vale `EXTRACTED`. Nel grafo del codice RDG, il punteggio di confidenza di l'iperarco `Complete Pipeline Stages` con identificatore `complete_pipeline_stages` vale `1.0`. Nel grafo del codice RDG, il file sorgente di l'iperarco `Complete Pipeline Stages` con identificatore `complete_pipeline_stages` è `usage.txt`.

## Cluster Graphify 7 — Log Collection

Il cluster editoriale Graphify 7 del dominio Architettura e codice del framework RDG contiene 32 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### .__init__() [log_collector_frameworklogparser_init]

<!-- GRAPH_NODE graph=codice index=30 id=log_collector_frameworklogparser_init -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `.__init__()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `L64`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` possiede l'identificatore esatto `log_collector_frameworklogparser_init`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `.__init__()`.
Nel grafo del codice RDG, il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=77 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FrameworkLogParser`; l'orientamento registrato nel JSON va dall'ID `log_collector_frameworklogparser` all'ID `log_collector_frameworklogparser_init`. La relazione tecnica esplicita è la seguente: `FrameworkLogParser` registra come proprio metodo `.__init__() [log_collector_frameworklogparser_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 77 che coinvolge il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 77 che coinvolge il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 77 che coinvolge il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 77 che coinvolge il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `L64`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 77 che coinvolge il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 77 che coinvolge il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `ast`. Nel grafo del codice RDG, l'arco 77 registra come `source` l'identificatore `log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `.__init__() [log_collector_frameworklogparser_init]`. Nel grafo del codice RDG, l'arco 77 registra come `target` l'identificatore `log_collector_frameworklogparser_init` per la relazione che coinvolge il nodo canonico `.__init__() [log_collector_frameworklogparser_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 77 che coinvolge il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=30 id=Request-Dataset-Generator::log_collector_frameworklogparser_init -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `.__init__()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `L64`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `.__init__()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `log_collector_frameworklogparser_init`. Nel grafo unificato, il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_frameworklogparser_init`.
Nel grafo unificato, il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=77 role=incoming -->
Nel grafo unificato, il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FrameworkLogParser`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_frameworklogparser` all'ID `Request-Dataset-Generator::log_collector_frameworklogparser_init`. La relazione tecnica esplicita è la seguente: `FrameworkLogParser` registra come proprio metodo `.__init__() [log_collector_frameworklogparser_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 77 che coinvolge il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 77 che coinvolge il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 77 che coinvolge il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 77 che coinvolge il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `L64`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 77 che coinvolge il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 77 che coinvolge il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 77 che coinvolge il nodo canonico `.__init__() [log_collector_frameworklogparser_init]` è `1.0`. Nel grafo unificato, l'arco 77 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `.__init__() [log_collector_frameworklogparser_init]`. Nel grafo unificato, l'arco 77 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser_init` per la relazione che coinvolge il nodo canonico `.__init__() [log_collector_frameworklogparser_init]`.

### .__init__() [log_collector_middlewarelogparser_init]

<!-- GRAPH_NODE graph=codice index=34 id=log_collector_middlewarelogparser_init -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `.__init__()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `L133`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` possiede l'identificatore esatto `log_collector_middlewarelogparser_init`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `.__init__()`.
Nel grafo del codice RDG, il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=86 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MiddlewareLogParser`; l'orientamento registrato nel JSON va dall'ID `log_collector_middlewarelogparser` all'ID `log_collector_middlewarelogparser_init`. La relazione tecnica esplicita è la seguente: `MiddlewareLogParser` registra come proprio metodo `.__init__() [log_collector_middlewarelogparser_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 86 che coinvolge il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 86 che coinvolge il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 86 che coinvolge il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 86 che coinvolge il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `L133`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 86 che coinvolge il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 86 che coinvolge il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `ast`. Nel grafo del codice RDG, l'arco 86 registra come `source` l'identificatore `log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]`. Nel grafo del codice RDG, l'arco 86 registra come `target` l'identificatore `log_collector_middlewarelogparser_init` per la relazione che coinvolge il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 86 che coinvolge il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=34 id=Request-Dataset-Generator::log_collector_middlewarelogparser_init -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `.__init__()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `L133`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `.__init__()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `log_collector_middlewarelogparser_init`. Nel grafo unificato, il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_middlewarelogparser_init`.
Nel grafo unificato, il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=86 role=incoming -->
Nel grafo unificato, il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MiddlewareLogParser`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_middlewarelogparser` all'ID `Request-Dataset-Generator::log_collector_middlewarelogparser_init`. La relazione tecnica esplicita è la seguente: `MiddlewareLogParser` registra come proprio metodo `.__init__() [log_collector_middlewarelogparser_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 86 che coinvolge il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 86 che coinvolge il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 86 che coinvolge il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 86 che coinvolge il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `L133`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 86 che coinvolge il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 86 che coinvolge il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 86 che coinvolge il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]` è `1.0`. Nel grafo unificato, l'arco 86 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]`. Nel grafo unificato, l'arco 86 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_middlewarelogparser_init` per la relazione che coinvolge il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]`.

### .aggregate()

<!-- GRAPH_NODE graph=codice index=40 id=log_collector_timeseriesaggregator_aggregate -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.aggregate()` è `.aggregate()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.aggregate()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.aggregate()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.aggregate()` è `L329`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.aggregate()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.aggregate()` possiede l'identificatore esatto `log_collector_timeseriesaggregator_aggregate`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.aggregate()` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.aggregate()` è `.aggregate()`.
<!-- GRAPH_EDGE graph=codice index=100 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.aggregate()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_percentile()`; l'orientamento registrato nel JSON va dall'ID `log_collector_timeseriesaggregator_aggregate` all'ID `log_collector_percentile`. La relazione tecnica esplicita è la seguente: `.aggregate()` chiama `_percentile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 100 che coinvolge il nodo canonico `.aggregate()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 100 che coinvolge il nodo canonico `.aggregate()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 100 che coinvolge il nodo canonico `.aggregate()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 100 che coinvolge il nodo canonico `.aggregate()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 100 che coinvolge il nodo canonico `.aggregate()` è `L422`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 100 che coinvolge il nodo canonico `.aggregate()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 100 che coinvolge il nodo canonico `.aggregate()` è `ast`. Nel grafo del codice RDG, l'arco 100 registra come `source` l'identificatore `log_collector_timeseriesaggregator_aggregate` per la relazione che coinvolge il nodo canonico `.aggregate()`. Nel grafo del codice RDG, l'arco 100 registra come `target` l'identificatore `log_collector_percentile` per la relazione che coinvolge il nodo canonico `.aggregate()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 100 che coinvolge il nodo canonico `.aggregate()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=98 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.aggregate()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `TimeseriesAggregator`; l'orientamento registrato nel JSON va dall'ID `log_collector_timeseriesaggregator` all'ID `log_collector_timeseriesaggregator_aggregate`. La relazione tecnica esplicita è la seguente: `TimeseriesAggregator` registra come proprio metodo `.aggregate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 98 che coinvolge il nodo canonico `.aggregate()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 98 che coinvolge il nodo canonico `.aggregate()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 98 che coinvolge il nodo canonico `.aggregate()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 98 che coinvolge il nodo canonico `.aggregate()` è `L329`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 98 che coinvolge il nodo canonico `.aggregate()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 98 che coinvolge il nodo canonico `.aggregate()` è `ast`. Nel grafo del codice RDG, l'arco 98 registra come `source` l'identificatore `log_collector_timeseriesaggregator` per la relazione che coinvolge il nodo canonico `.aggregate()`. Nel grafo del codice RDG, l'arco 98 registra come `target` l'identificatore `log_collector_timeseriesaggregator_aggregate` per la relazione che coinvolge il nodo canonico `.aggregate()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 98 che coinvolge il nodo canonico `.aggregate()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=99 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.aggregate()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `log_collector_collect_logs` all'ID `log_collector_timeseriesaggregator_aggregate`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `.aggregate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 99 che coinvolge il nodo canonico `.aggregate()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 99 che coinvolge il nodo canonico `.aggregate()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 99 che coinvolge il nodo canonico `.aggregate()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 99 che coinvolge il nodo canonico `.aggregate()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 99 che coinvolge il nodo canonico `.aggregate()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 99 che coinvolge il nodo canonico `.aggregate()` è `L623`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 99 che coinvolge il nodo canonico `.aggregate()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 99 che coinvolge il nodo canonico `.aggregate()` è `ast`. Nel grafo del codice RDG, l'arco 99 registra come `source` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `.aggregate()`. Nel grafo del codice RDG, l'arco 99 registra come `target` l'identificatore `log_collector_timeseriesaggregator_aggregate` per la relazione che coinvolge il nodo canonico `.aggregate()`.

<!-- GRAPH_NODE graph=unificata index=40 id=Request-Dataset-Generator::log_collector_timeseriesaggregator_aggregate -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.aggregate()` è `.aggregate()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.aggregate()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.aggregate()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.aggregate()` è `L329`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.aggregate()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.aggregate()` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.aggregate()` è `.aggregate()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.aggregate()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.aggregate()` è `log_collector_timeseriesaggregator_aggregate`. Nel grafo unificato, il nodo canonico `.aggregate()` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_timeseriesaggregator_aggregate`.
<!-- GRAPH_EDGE graph=unificata index=99 role=outgoing -->
Nel grafo unificato, il nodo canonico `.aggregate()` è la sorgente `source` della relazione `calls` verso il nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_timeseriesaggregator_aggregate` all'ID `Request-Dataset-Generator::log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `.aggregate()` chiama `collect_logs()`. Nel grafo unificato, il tipo dichiarato dell'arco 99 che coinvolge il nodo canonico `.aggregate()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 99 che coinvolge il nodo canonico `.aggregate()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 99 che coinvolge il nodo canonico `.aggregate()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 99 che coinvolge il nodo canonico `.aggregate()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 99 che coinvolge il nodo canonico `.aggregate()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 99 che coinvolge il nodo canonico `.aggregate()` è `L623`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 99 che coinvolge il nodo canonico `.aggregate()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 99 che coinvolge il nodo canonico `.aggregate()` è `ast`. Nel grafo unificato, l'arco 99 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_timeseriesaggregator_aggregate` per la relazione che coinvolge il nodo canonico `.aggregate()`. Nel grafo unificato, l'arco 99 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `.aggregate()`.
<!-- GRAPH_EDGE graph=unificata index=100 role=outgoing -->
Nel grafo unificato, il nodo canonico `.aggregate()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_percentile()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_timeseriesaggregator_aggregate` all'ID `Request-Dataset-Generator::log_collector_percentile`. La relazione tecnica esplicita è la seguente: `.aggregate()` chiama `_percentile()`. Nel grafo unificato, il tipo dichiarato dell'arco 100 che coinvolge il nodo canonico `.aggregate()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 100 che coinvolge il nodo canonico `.aggregate()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 100 che coinvolge il nodo canonico `.aggregate()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 100 che coinvolge il nodo canonico `.aggregate()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 100 che coinvolge il nodo canonico `.aggregate()` è `L422`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 100 che coinvolge il nodo canonico `.aggregate()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 100 che coinvolge il nodo canonico `.aggregate()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 100 che coinvolge il nodo canonico `.aggregate()` è `1.0`. Nel grafo unificato, l'arco 100 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_timeseriesaggregator_aggregate` per la relazione che coinvolge il nodo canonico `.aggregate()`. Nel grafo unificato, l'arco 100 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_percentile` per la relazione che coinvolge il nodo canonico `.aggregate()`.
<!-- GRAPH_EDGE graph=unificata index=98 role=incoming -->
Nel grafo unificato, il nodo canonico `.aggregate()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `TimeseriesAggregator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_timeseriesaggregator` all'ID `Request-Dataset-Generator::log_collector_timeseriesaggregator_aggregate`. La relazione tecnica esplicita è la seguente: `TimeseriesAggregator` registra come proprio metodo `.aggregate()`. Nel grafo unificato, il tipo dichiarato dell'arco 98 che coinvolge il nodo canonico `.aggregate()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 98 che coinvolge il nodo canonico `.aggregate()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 98 che coinvolge il nodo canonico `.aggregate()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 98 che coinvolge il nodo canonico `.aggregate()` è `L329`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 98 che coinvolge il nodo canonico `.aggregate()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 98 che coinvolge il nodo canonico `.aggregate()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 98 che coinvolge il nodo canonico `.aggregate()` è `1.0`. Nel grafo unificato, l'arco 98 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_timeseriesaggregator` per la relazione che coinvolge il nodo canonico `.aggregate()`. Nel grafo unificato, l'arco 98 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_timeseriesaggregator_aggregate` per la relazione che coinvolge il nodo canonico `.aggregate()`.

### .build()

<!-- GRAPH_NODE graph=codice index=43 id=log_collector_normalizeddatasetbuilder_build -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.build()` è `.build()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.build()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.build()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.build()` è `L503`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.build()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.build()` possiede l'identificatore esatto `log_collector_normalizeddatasetbuilder_build`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.build()` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.build()` è `.build()`.
<!-- GRAPH_EDGE graph=codice index=81 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.build()` è la sorgente `source` della relazione `references` verso il nodo canonico `FrameworkLogParser`; l'orientamento registrato nel JSON va dall'ID `log_collector_normalizeddatasetbuilder_build` all'ID `log_collector_frameworklogparser`. La relazione tecnica esplicita è la seguente: `.build()` fa riferimento a `FrameworkLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 81 che coinvolge il nodo canonico `.build()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 81 che coinvolge il nodo canonico `.build()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 81 che coinvolge il nodo canonico `.build()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 81 che coinvolge il nodo canonico `.build()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 81 che coinvolge il nodo canonico `.build()` è `L503`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 81 che coinvolge il nodo canonico `.build()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 81 che coinvolge il nodo canonico `.build()` è `ast`. Nel grafo del codice RDG, l'arco 81 registra come `source` l'identificatore `log_collector_normalizeddatasetbuilder_build` per la relazione che coinvolge il nodo canonico `.build()`. Nel grafo del codice RDG, l'arco 81 registra come `target` l'identificatore `log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `.build()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 81 che coinvolge il nodo canonico `.build()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=88 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.build()` è la sorgente `source` della relazione `references` verso il nodo canonico `MiddlewareLogParser`; l'orientamento registrato nel JSON va dall'ID `log_collector_normalizeddatasetbuilder_build` all'ID `log_collector_middlewarelogparser`. La relazione tecnica esplicita è la seguente: `.build()` fa riferimento a `MiddlewareLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 88 che coinvolge il nodo canonico `.build()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 88 che coinvolge il nodo canonico `.build()` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 88 che coinvolge il nodo canonico `.build()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 88 che coinvolge il nodo canonico `.build()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 88 che coinvolge il nodo canonico `.build()` è `L503`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 88 che coinvolge il nodo canonico `.build()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 88 che coinvolge il nodo canonico `.build()` è `ast`. Nel grafo del codice RDG, l'arco 88 registra come `source` l'identificatore `log_collector_normalizeddatasetbuilder_build` per la relazione che coinvolge il nodo canonico `.build()`. Nel grafo del codice RDG, l'arco 88 registra come `target` l'identificatore `log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `.build()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 88 che coinvolge il nodo canonico `.build()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=105 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.build()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_first_present()`; l'orientamento registrato nel JSON va dall'ID `log_collector_normalizeddatasetbuilder_build` all'ID `log_collector_first_present`. La relazione tecnica esplicita è la seguente: `.build()` chiama `_first_present()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 105 che coinvolge il nodo canonico `.build()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 105 che coinvolge il nodo canonico `.build()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 105 che coinvolge il nodo canonico `.build()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 105 che coinvolge il nodo canonico `.build()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 105 che coinvolge il nodo canonico `.build()` è `L512`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 105 che coinvolge il nodo canonico `.build()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 105 che coinvolge il nodo canonico `.build()` è `ast`. Nel grafo del codice RDG, l'arco 105 registra come `source` l'identificatore `log_collector_normalizeddatasetbuilder_build` per la relazione che coinvolge il nodo canonico `.build()`. Nel grafo del codice RDG, l'arco 105 registra come `target` l'identificatore `log_collector_first_present` per la relazione che coinvolge il nodo canonico `.build()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 105 che coinvolge il nodo canonico `.build()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=102 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.build()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `NormalizedDatasetBuilder`; l'orientamento registrato nel JSON va dall'ID `log_collector_normalizeddatasetbuilder` all'ID `log_collector_normalizeddatasetbuilder_build`. La relazione tecnica esplicita è la seguente: `NormalizedDatasetBuilder` registra come proprio metodo `.build()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 102 che coinvolge il nodo canonico `.build()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 102 che coinvolge il nodo canonico `.build()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 102 che coinvolge il nodo canonico `.build()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 102 che coinvolge il nodo canonico `.build()` è `L503`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 102 che coinvolge il nodo canonico `.build()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 102 che coinvolge il nodo canonico `.build()` è `ast`. Nel grafo del codice RDG, l'arco 102 registra come `source` l'identificatore `log_collector_normalizeddatasetbuilder` per la relazione che coinvolge il nodo canonico `.build()`. Nel grafo del codice RDG, l'arco 102 registra come `target` l'identificatore `log_collector_normalizeddatasetbuilder_build` per la relazione che coinvolge il nodo canonico `.build()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 102 che coinvolge il nodo canonico `.build()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=104 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.build()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `log_collector_collect_logs` all'ID `log_collector_normalizeddatasetbuilder_build`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `.build()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 104 che coinvolge il nodo canonico `.build()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 104 che coinvolge il nodo canonico `.build()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 104 che coinvolge il nodo canonico `.build()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 104 che coinvolge il nodo canonico `.build()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 104 che coinvolge il nodo canonico `.build()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 104 che coinvolge il nodo canonico `.build()` è `L627`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 104 che coinvolge il nodo canonico `.build()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 104 che coinvolge il nodo canonico `.build()` è `ast`. Nel grafo del codice RDG, l'arco 104 registra come `source` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `.build()`. Nel grafo del codice RDG, l'arco 104 registra come `target` l'identificatore `log_collector_normalizeddatasetbuilder_build` per la relazione che coinvolge il nodo canonico `.build()`.

<!-- GRAPH_NODE graph=unificata index=43 id=Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.build()` è `.build()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.build()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.build()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.build()` è `L503`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.build()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.build()` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.build()` è `.build()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.build()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.build()` è `log_collector_normalizeddatasetbuilder_build`. Nel grafo unificato, il nodo canonico `.build()` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build`.
<!-- GRAPH_EDGE graph=unificata index=104 role=outgoing -->
Nel grafo unificato, il nodo canonico `.build()` è la sorgente `source` della relazione `calls` verso il nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build` all'ID `Request-Dataset-Generator::log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `.build()` chiama `collect_logs()`. Nel grafo unificato, il tipo dichiarato dell'arco 104 che coinvolge il nodo canonico `.build()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 104 che coinvolge il nodo canonico `.build()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 104 che coinvolge il nodo canonico `.build()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 104 che coinvolge il nodo canonico `.build()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 104 che coinvolge il nodo canonico `.build()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 104 che coinvolge il nodo canonico `.build()` è `L627`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 104 che coinvolge il nodo canonico `.build()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 104 che coinvolge il nodo canonico `.build()` è `ast`. Nel grafo unificato, l'arco 104 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build` per la relazione che coinvolge il nodo canonico `.build()`. Nel grafo unificato, l'arco 104 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `.build()`.
<!-- GRAPH_EDGE graph=unificata index=105 role=outgoing -->
Nel grafo unificato, il nodo canonico `.build()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_first_present()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build` all'ID `Request-Dataset-Generator::log_collector_first_present`. La relazione tecnica esplicita è la seguente: `.build()` chiama `_first_present()`. Nel grafo unificato, il tipo dichiarato dell'arco 105 che coinvolge il nodo canonico `.build()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 105 che coinvolge il nodo canonico `.build()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 105 che coinvolge il nodo canonico `.build()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 105 che coinvolge il nodo canonico `.build()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 105 che coinvolge il nodo canonico `.build()` è `L512`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 105 che coinvolge il nodo canonico `.build()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 105 che coinvolge il nodo canonico `.build()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 105 che coinvolge il nodo canonico `.build()` è `1.0`. Nel grafo unificato, l'arco 105 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build` per la relazione che coinvolge il nodo canonico `.build()`. Nel grafo unificato, l'arco 105 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_first_present` per la relazione che coinvolge il nodo canonico `.build()`.
<!-- GRAPH_EDGE graph=unificata index=81 role=incoming -->
Nel grafo unificato, il nodo canonico `.build()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `FrameworkLogParser`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_frameworklogparser` all'ID `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build`. La relazione tecnica esplicita è la seguente: `FrameworkLogParser` fa riferimento a `.build()`. Nel grafo unificato, il tipo dichiarato dell'arco 81 che coinvolge il nodo canonico `.build()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 81 che coinvolge il nodo canonico `.build()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 81 che coinvolge il nodo canonico `.build()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 81 che coinvolge il nodo canonico `.build()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 81 che coinvolge il nodo canonico `.build()` è `L503`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 81 che coinvolge il nodo canonico `.build()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 81 che coinvolge il nodo canonico `.build()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 81 che coinvolge il nodo canonico `.build()` è `1.0`. Nel grafo unificato, l'arco 81 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `.build()`. Nel grafo unificato, l'arco 81 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build` per la relazione che coinvolge il nodo canonico `.build()`.
<!-- GRAPH_EDGE graph=unificata index=88 role=incoming -->
Nel grafo unificato, il nodo canonico `.build()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `MiddlewareLogParser`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_middlewarelogparser` all'ID `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build`. La relazione tecnica esplicita è la seguente: `MiddlewareLogParser` fa riferimento a `.build()`. Nel grafo unificato, il tipo dichiarato dell'arco 88 che coinvolge il nodo canonico `.build()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 88 che coinvolge il nodo canonico `.build()` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 88 che coinvolge il nodo canonico `.build()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 88 che coinvolge il nodo canonico `.build()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 88 che coinvolge il nodo canonico `.build()` è `L503`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 88 che coinvolge il nodo canonico `.build()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 88 che coinvolge il nodo canonico `.build()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 88 che coinvolge il nodo canonico `.build()` è `1.0`. Nel grafo unificato, l'arco 88 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `.build()`. Nel grafo unificato, l'arco 88 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build` per la relazione che coinvolge il nodo canonico `.build()`.
<!-- GRAPH_EDGE graph=unificata index=102 role=incoming -->
Nel grafo unificato, il nodo canonico `.build()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `NormalizedDatasetBuilder`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder` all'ID `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build`. La relazione tecnica esplicita è la seguente: `NormalizedDatasetBuilder` registra come proprio metodo `.build()`. Nel grafo unificato, il tipo dichiarato dell'arco 102 che coinvolge il nodo canonico `.build()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 102 che coinvolge il nodo canonico `.build()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 102 che coinvolge il nodo canonico `.build()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 102 che coinvolge il nodo canonico `.build()` è `L503`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 102 che coinvolge il nodo canonico `.build()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 102 che coinvolge il nodo canonico `.build()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 102 che coinvolge il nodo canonico `.build()` è `1.0`. Nel grafo unificato, l'arco 102 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder` per la relazione che coinvolge il nodo canonico `.build()`. Nel grafo unificato, l'arco 102 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build` per la relazione che coinvolge il nodo canonico `.build()`.

### .build_flows()

<!-- GRAPH_NODE graph=codice index=38 id=log_collector_logjoiner_build_flows -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.build_flows()` è `.build_flows()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.build_flows()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.build_flows()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.build_flows()` è `L250`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.build_flows()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.build_flows()` possiede l'identificatore esatto `log_collector_logjoiner_build_flows`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.build_flows()` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.build_flows()` è `.build_flows()`.
<!-- GRAPH_EDGE graph=codice index=79 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.build_flows()` è la sorgente `source` della relazione `references` verso il nodo canonico `FrameworkLogParser`; l'orientamento registrato nel JSON va dall'ID `log_collector_logjoiner_build_flows` all'ID `log_collector_frameworklogparser`. La relazione tecnica esplicita è la seguente: `.build_flows()` fa riferimento a `FrameworkLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 79 che coinvolge il nodo canonico `.build_flows()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 79 che coinvolge il nodo canonico `.build_flows()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 79 che coinvolge il nodo canonico `.build_flows()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 79 che coinvolge il nodo canonico `.build_flows()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 79 che coinvolge il nodo canonico `.build_flows()` è `L250`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 79 che coinvolge il nodo canonico `.build_flows()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 79 che coinvolge il nodo canonico `.build_flows()` è `ast`. Nel grafo del codice RDG, l'arco 79 registra come `source` l'identificatore `log_collector_logjoiner_build_flows` per la relazione che coinvolge il nodo canonico `.build_flows()`. Nel grafo del codice RDG, l'arco 79 registra come `target` l'identificatore `log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `.build_flows()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 79 che coinvolge il nodo canonico `.build_flows()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=91 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.build_flows()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `LogJoiner`; l'orientamento registrato nel JSON va dall'ID `log_collector_logjoiner` all'ID `log_collector_logjoiner_build_flows`. La relazione tecnica esplicita è la seguente: `LogJoiner` registra come proprio metodo `.build_flows()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 91 che coinvolge il nodo canonico `.build_flows()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 91 che coinvolge il nodo canonico `.build_flows()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 91 che coinvolge il nodo canonico `.build_flows()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 91 che coinvolge il nodo canonico `.build_flows()` è `L250`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 91 che coinvolge il nodo canonico `.build_flows()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 91 che coinvolge il nodo canonico `.build_flows()` è `ast`. Nel grafo del codice RDG, l'arco 91 registra come `source` l'identificatore `log_collector_logjoiner` per la relazione che coinvolge il nodo canonico `.build_flows()`. Nel grafo del codice RDG, l'arco 91 registra come `target` l'identificatore `log_collector_logjoiner_build_flows` per la relazione che coinvolge il nodo canonico `.build_flows()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 91 che coinvolge il nodo canonico `.build_flows()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=95 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.build_flows()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `log_collector_collect_logs` all'ID `log_collector_logjoiner_build_flows`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `.build_flows()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 95 che coinvolge il nodo canonico `.build_flows()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 95 che coinvolge il nodo canonico `.build_flows()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 95 che coinvolge il nodo canonico `.build_flows()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 95 che coinvolge il nodo canonico `.build_flows()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 95 che coinvolge il nodo canonico `.build_flows()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 95 che coinvolge il nodo canonico `.build_flows()` è `L619`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 95 che coinvolge il nodo canonico `.build_flows()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 95 che coinvolge il nodo canonico `.build_flows()` è `ast`. Nel grafo del codice RDG, l'arco 95 registra come `source` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `.build_flows()`. Nel grafo del codice RDG, l'arco 95 registra come `target` l'identificatore `log_collector_logjoiner_build_flows` per la relazione che coinvolge il nodo canonico `.build_flows()`.
<!-- GRAPH_EDGE graph=codice index=96 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.build_flows()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con`; l'orientamento registrato nel JSON va dall'ID `log_collector_rationale_254` all'ID `log_collector_logjoiner_build_flows`. La relazione tecnica esplicita è la seguente: `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` costituisce una motivazione per `.build_flows()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 96 che coinvolge il nodo canonico `.build_flows()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 96 che coinvolge il nodo canonico `.build_flows()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 96 che coinvolge il nodo canonico `.build_flows()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 96 che coinvolge il nodo canonico `.build_flows()` è `L254`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 96 che coinvolge il nodo canonico `.build_flows()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 96 che coinvolge il nodo canonico `.build_flows()` è `ast`. Nel grafo del codice RDG, l'arco 96 registra come `source` l'identificatore `log_collector_rationale_254` per la relazione che coinvolge il nodo canonico `.build_flows()`. Nel grafo del codice RDG, l'arco 96 registra come `target` l'identificatore `log_collector_logjoiner_build_flows` per la relazione che coinvolge il nodo canonico `.build_flows()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 96 che coinvolge il nodo canonico `.build_flows()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=38 id=Request-Dataset-Generator::log_collector_logjoiner_build_flows -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.build_flows()` è `.build_flows()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.build_flows()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.build_flows()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.build_flows()` è `L250`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.build_flows()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.build_flows()` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.build_flows()` è `.build_flows()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.build_flows()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.build_flows()` è `log_collector_logjoiner_build_flows`. Nel grafo unificato, il nodo canonico `.build_flows()` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_logjoiner_build_flows`.
<!-- GRAPH_EDGE graph=unificata index=95 role=outgoing -->
Nel grafo unificato, il nodo canonico `.build_flows()` è la sorgente `source` della relazione `calls` verso il nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_logjoiner_build_flows` all'ID `Request-Dataset-Generator::log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `.build_flows()` chiama `collect_logs()`. Nel grafo unificato, il tipo dichiarato dell'arco 95 che coinvolge il nodo canonico `.build_flows()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 95 che coinvolge il nodo canonico `.build_flows()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 95 che coinvolge il nodo canonico `.build_flows()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 95 che coinvolge il nodo canonico `.build_flows()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 95 che coinvolge il nodo canonico `.build_flows()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 95 che coinvolge il nodo canonico `.build_flows()` è `L619`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 95 che coinvolge il nodo canonico `.build_flows()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 95 che coinvolge il nodo canonico `.build_flows()` è `ast`. Nel grafo unificato, l'arco 95 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner_build_flows` per la relazione che coinvolge il nodo canonico `.build_flows()`. Nel grafo unificato, l'arco 95 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `.build_flows()`.
<!-- GRAPH_EDGE graph=unificata index=96 role=outgoing -->
Nel grafo unificato, il nodo canonico `.build_flows()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_logjoiner_build_flows` all'ID `Request-Dataset-Generator::log_collector_rationale_254`. La relazione tecnica esplicita è la seguente: `.build_flows()` costituisce una motivazione per `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con`. Nel grafo unificato, il tipo dichiarato dell'arco 96 che coinvolge il nodo canonico `.build_flows()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 96 che coinvolge il nodo canonico `.build_flows()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 96 che coinvolge il nodo canonico `.build_flows()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 96 che coinvolge il nodo canonico `.build_flows()` è `L254`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 96 che coinvolge il nodo canonico `.build_flows()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 96 che coinvolge il nodo canonico `.build_flows()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 96 che coinvolge il nodo canonico `.build_flows()` è `1.0`. Nel grafo unificato, l'arco 96 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner_build_flows` per la relazione che coinvolge il nodo canonico `.build_flows()`. Nel grafo unificato, l'arco 96 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_rationale_254` per la relazione che coinvolge il nodo canonico `.build_flows()`.
<!-- GRAPH_EDGE graph=unificata index=79 role=incoming -->
Nel grafo unificato, il nodo canonico `.build_flows()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `FrameworkLogParser`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_frameworklogparser` all'ID `Request-Dataset-Generator::log_collector_logjoiner_build_flows`. La relazione tecnica esplicita è la seguente: `FrameworkLogParser` fa riferimento a `.build_flows()`. Nel grafo unificato, il tipo dichiarato dell'arco 79 che coinvolge il nodo canonico `.build_flows()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 79 che coinvolge il nodo canonico `.build_flows()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 79 che coinvolge il nodo canonico `.build_flows()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 79 che coinvolge il nodo canonico `.build_flows()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 79 che coinvolge il nodo canonico `.build_flows()` è `L250`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 79 che coinvolge il nodo canonico `.build_flows()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 79 che coinvolge il nodo canonico `.build_flows()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 79 che coinvolge il nodo canonico `.build_flows()` è `1.0`. Nel grafo unificato, l'arco 79 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `.build_flows()`. Nel grafo unificato, l'arco 79 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner_build_flows` per la relazione che coinvolge il nodo canonico `.build_flows()`.
<!-- GRAPH_EDGE graph=unificata index=91 role=incoming -->
Nel grafo unificato, il nodo canonico `.build_flows()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `LogJoiner`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_logjoiner` all'ID `Request-Dataset-Generator::log_collector_logjoiner_build_flows`. La relazione tecnica esplicita è la seguente: `LogJoiner` registra come proprio metodo `.build_flows()`. Nel grafo unificato, il tipo dichiarato dell'arco 91 che coinvolge il nodo canonico `.build_flows()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 91 che coinvolge il nodo canonico `.build_flows()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 91 che coinvolge il nodo canonico `.build_flows()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 91 che coinvolge il nodo canonico `.build_flows()` è `L250`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 91 che coinvolge il nodo canonico `.build_flows()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 91 che coinvolge il nodo canonico `.build_flows()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 91 che coinvolge il nodo canonico `.build_flows()` è `1.0`. Nel grafo unificato, l'arco 91 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner` per la relazione che coinvolge il nodo canonico `.build_flows()`. Nel grafo unificato, l'arco 91 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner_build_flows` per la relazione che coinvolge il nodo canonico `.build_flows()`.

### .join()

<!-- GRAPH_NODE graph=codice index=37 id=log_collector_logjoiner_join -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.join()` è `.join()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.join()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.join()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.join()` è `L168`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.join()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.join()` possiede l'identificatore esatto `log_collector_logjoiner_join`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.join()` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.join()` è `.join()`.
<!-- GRAPH_EDGE graph=codice index=80 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.join()` è la sorgente `source` della relazione `references` verso il nodo canonico `FrameworkLogParser`; l'orientamento registrato nel JSON va dall'ID `log_collector_logjoiner_join` all'ID `log_collector_frameworklogparser`. La relazione tecnica esplicita è la seguente: `.join()` fa riferimento a `FrameworkLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 80 che coinvolge il nodo canonico `.join()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 80 che coinvolge il nodo canonico `.join()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 80 che coinvolge il nodo canonico `.join()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 80 che coinvolge il nodo canonico `.join()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 80 che coinvolge il nodo canonico `.join()` è `L168`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 80 che coinvolge il nodo canonico `.join()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 80 che coinvolge il nodo canonico `.join()` è `ast`. Nel grafo del codice RDG, l'arco 80 registra come `source` l'identificatore `log_collector_logjoiner_join` per la relazione che coinvolge il nodo canonico `.join()`. Nel grafo del codice RDG, l'arco 80 registra come `target` l'identificatore `log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `.join()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 80 che coinvolge il nodo canonico `.join()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=85 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.join()` è la sorgente `source` della relazione `references` verso il nodo canonico `MiddlewareLogParser`; l'orientamento registrato nel JSON va dall'ID `log_collector_logjoiner_join` all'ID `log_collector_middlewarelogparser`. La relazione tecnica esplicita è la seguente: `.join()` fa riferimento a `MiddlewareLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 85 che coinvolge il nodo canonico `.join()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 85 che coinvolge il nodo canonico `.join()` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 85 che coinvolge il nodo canonico `.join()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 85 che coinvolge il nodo canonico `.join()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 85 che coinvolge il nodo canonico `.join()` è `L168`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 85 che coinvolge il nodo canonico `.join()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 85 che coinvolge il nodo canonico `.join()` è `ast`. Nel grafo del codice RDG, l'arco 85 registra come `source` l'identificatore `log_collector_logjoiner_join` per la relazione che coinvolge il nodo canonico `.join()`. Nel grafo del codice RDG, l'arco 85 registra come `target` l'identificatore `log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `.join()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 85 che coinvolge il nodo canonico `.join()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=74 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.join()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]`; l'orientamento registrato nel JSON va dall'ID `log_collector_resolve_output_path` all'ID `log_collector_logjoiner_join`. La relazione tecnica esplicita è la seguente: `_resolve_output_path() [log_collector_resolve_output_path]` chiama `.join()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 74 che coinvolge il nodo canonico `.join()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 74 che coinvolge il nodo canonico `.join()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 74 che coinvolge il nodo canonico `.join()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 74 che coinvolge il nodo canonico `.join()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 74 che coinvolge il nodo canonico `.join()` è `L46`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 74 che coinvolge il nodo canonico `.join()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 74 che coinvolge il nodo canonico `.join()` è `ast`. Nel grafo del codice RDG, l'arco 74 registra come `source` l'identificatore `log_collector_resolve_output_path` per la relazione che coinvolge il nodo canonico `.join()`. Nel grafo del codice RDG, l'arco 74 registra come `target` l'identificatore `log_collector_logjoiner_join` per la relazione che coinvolge il nodo canonico `.join()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 74 che coinvolge il nodo canonico `.join()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=92 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.join()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `LogJoiner`; l'orientamento registrato nel JSON va dall'ID `log_collector_logjoiner` all'ID `log_collector_logjoiner_join`. La relazione tecnica esplicita è la seguente: `LogJoiner` registra come proprio metodo `.join()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 92 che coinvolge il nodo canonico `.join()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 92 che coinvolge il nodo canonico `.join()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 92 che coinvolge il nodo canonico `.join()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 92 che coinvolge il nodo canonico `.join()` è `L168`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 92 che coinvolge il nodo canonico `.join()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 92 che coinvolge il nodo canonico `.join()` è `ast`. Nel grafo del codice RDG, l'arco 92 registra come `source` l'identificatore `log_collector_logjoiner` per la relazione che coinvolge il nodo canonico `.join()`. Nel grafo del codice RDG, l'arco 92 registra come `target` l'identificatore `log_collector_logjoiner_join` per la relazione che coinvolge il nodo canonico `.join()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 92 che coinvolge il nodo canonico `.join()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=94 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.join()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `log_collector_collect_logs` all'ID `log_collector_logjoiner_join`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `.join()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 94 che coinvolge il nodo canonico `.join()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 94 che coinvolge il nodo canonico `.join()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 94 che coinvolge il nodo canonico `.join()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 94 che coinvolge il nodo canonico `.join()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 94 che coinvolge il nodo canonico `.join()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 94 che coinvolge il nodo canonico `.join()` è `L615`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 94 che coinvolge il nodo canonico `.join()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 94 che coinvolge il nodo canonico `.join()` è `ast`. Nel grafo del codice RDG, l'arco 94 registra come `source` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `.join()`. Nel grafo del codice RDG, l'arco 94 registra come `target` l'identificatore `log_collector_logjoiner_join` per la relazione che coinvolge il nodo canonico `.join()`.

<!-- GRAPH_NODE graph=unificata index=37 id=Request-Dataset-Generator::log_collector_logjoiner_join -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.join()` è `.join()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.join()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.join()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.join()` è `L168`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.join()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.join()` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.join()` è `.join()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.join()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.join()` è `log_collector_logjoiner_join`. Nel grafo unificato, il nodo canonico `.join()` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_logjoiner_join`.
<!-- GRAPH_EDGE graph=unificata index=94 role=outgoing -->
Nel grafo unificato, il nodo canonico `.join()` è la sorgente `source` della relazione `calls` verso il nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_logjoiner_join` all'ID `Request-Dataset-Generator::log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `.join()` chiama `collect_logs()`. Nel grafo unificato, il tipo dichiarato dell'arco 94 che coinvolge il nodo canonico `.join()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 94 che coinvolge il nodo canonico `.join()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 94 che coinvolge il nodo canonico `.join()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 94 che coinvolge il nodo canonico `.join()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 94 che coinvolge il nodo canonico `.join()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 94 che coinvolge il nodo canonico `.join()` è `L615`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 94 che coinvolge il nodo canonico `.join()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 94 che coinvolge il nodo canonico `.join()` è `ast`. Nel grafo unificato, l'arco 94 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner_join` per la relazione che coinvolge il nodo canonico `.join()`. Nel grafo unificato, l'arco 94 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `.join()`.
<!-- GRAPH_EDGE graph=unificata index=74 role=incoming -->
Nel grafo unificato, il nodo canonico `.join()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_resolve_output_path` all'ID `Request-Dataset-Generator::log_collector_logjoiner_join`. La relazione tecnica esplicita è la seguente: `_resolve_output_path() [log_collector_resolve_output_path]` chiama `.join()`. Nel grafo unificato, il tipo dichiarato dell'arco 74 che coinvolge il nodo canonico `.join()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 74 che coinvolge il nodo canonico `.join()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 74 che coinvolge il nodo canonico `.join()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 74 che coinvolge il nodo canonico `.join()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 74 che coinvolge il nodo canonico `.join()` è `L46`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 74 che coinvolge il nodo canonico `.join()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 74 che coinvolge il nodo canonico `.join()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 74 che coinvolge il nodo canonico `.join()` è `1.0`. Nel grafo unificato, l'arco 74 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_resolve_output_path` per la relazione che coinvolge il nodo canonico `.join()`. Nel grafo unificato, l'arco 74 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner_join` per la relazione che coinvolge il nodo canonico `.join()`.
<!-- GRAPH_EDGE graph=unificata index=80 role=incoming -->
Nel grafo unificato, il nodo canonico `.join()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `FrameworkLogParser`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_frameworklogparser` all'ID `Request-Dataset-Generator::log_collector_logjoiner_join`. La relazione tecnica esplicita è la seguente: `FrameworkLogParser` fa riferimento a `.join()`. Nel grafo unificato, il tipo dichiarato dell'arco 80 che coinvolge il nodo canonico `.join()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 80 che coinvolge il nodo canonico `.join()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 80 che coinvolge il nodo canonico `.join()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 80 che coinvolge il nodo canonico `.join()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 80 che coinvolge il nodo canonico `.join()` è `L168`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 80 che coinvolge il nodo canonico `.join()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 80 che coinvolge il nodo canonico `.join()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 80 che coinvolge il nodo canonico `.join()` è `1.0`. Nel grafo unificato, l'arco 80 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `.join()`. Nel grafo unificato, l'arco 80 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner_join` per la relazione che coinvolge il nodo canonico `.join()`.
<!-- GRAPH_EDGE graph=unificata index=85 role=incoming -->
Nel grafo unificato, il nodo canonico `.join()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `MiddlewareLogParser`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_middlewarelogparser` all'ID `Request-Dataset-Generator::log_collector_logjoiner_join`. La relazione tecnica esplicita è la seguente: `MiddlewareLogParser` fa riferimento a `.join()`. Nel grafo unificato, il tipo dichiarato dell'arco 85 che coinvolge il nodo canonico `.join()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 85 che coinvolge il nodo canonico `.join()` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 85 che coinvolge il nodo canonico `.join()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 85 che coinvolge il nodo canonico `.join()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 85 che coinvolge il nodo canonico `.join()` è `L168`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 85 che coinvolge il nodo canonico `.join()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 85 che coinvolge il nodo canonico `.join()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 85 che coinvolge il nodo canonico `.join()` è `1.0`. Nel grafo unificato, l'arco 85 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `.join()`. Nel grafo unificato, l'arco 85 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner_join` per la relazione che coinvolge il nodo canonico `.join()`.
<!-- GRAPH_EDGE graph=unificata index=92 role=incoming -->
Nel grafo unificato, il nodo canonico `.join()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `LogJoiner`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_logjoiner` all'ID `Request-Dataset-Generator::log_collector_logjoiner_join`. La relazione tecnica esplicita è la seguente: `LogJoiner` registra come proprio metodo `.join()`. Nel grafo unificato, il tipo dichiarato dell'arco 92 che coinvolge il nodo canonico `.join()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 92 che coinvolge il nodo canonico `.join()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 92 che coinvolge il nodo canonico `.join()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 92 che coinvolge il nodo canonico `.join()` è `L168`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 92 che coinvolge il nodo canonico `.join()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 92 che coinvolge il nodo canonico `.join()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 92 che coinvolge il nodo canonico `.join()` è `1.0`. Nel grafo unificato, l'arco 92 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner` per la relazione che coinvolge il nodo canonico `.join()`. Nel grafo unificato, l'arco 92 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner_join` per la relazione che coinvolge il nodo canonico `.join()`.

### .parse() [log_collector_frameworklogparser_parse]

<!-- GRAPH_NODE graph=codice index=31 id=log_collector_frameworklogparser_parse -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `.parse()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `L73`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` possiede l'identificatore esatto `log_collector_frameworklogparser_parse`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `.parse()`.
<!-- GRAPH_EDGE graph=codice index=83 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_framework_event_key()`; l'orientamento registrato nel JSON va dall'ID `log_collector_frameworklogparser_parse` all'ID `log_collector_framework_event_key`. La relazione tecnica esplicita è la seguente: `.parse() [log_collector_frameworklogparser_parse]` chiama `_framework_event_key()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 83 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 83 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 83 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 83 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 83 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `L92`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 83 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 83 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `ast`. Nel grafo del codice RDG, l'arco 83 registra come `source` l'identificatore `log_collector_frameworklogparser_parse` per la relazione che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]`. Nel grafo del codice RDG, l'arco 83 registra come `target` l'identificatore `log_collector_framework_event_key` per la relazione che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 83 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=78 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FrameworkLogParser`; l'orientamento registrato nel JSON va dall'ID `log_collector_frameworklogparser` all'ID `log_collector_frameworklogparser_parse`. La relazione tecnica esplicita è la seguente: `FrameworkLogParser` registra come proprio metodo `.parse() [log_collector_frameworklogparser_parse]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 78 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 78 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 78 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 78 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `L73`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 78 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 78 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `ast`. Nel grafo del codice RDG, l'arco 78 registra come `source` l'identificatore `log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]`. Nel grafo del codice RDG, l'arco 78 registra come `target` l'identificatore `log_collector_frameworklogparser_parse` per la relazione che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 78 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=31 id=Request-Dataset-Generator::log_collector_frameworklogparser_parse -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `.parse()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `L73`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `.parse()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `log_collector_frameworklogparser_parse`. Nel grafo unificato, il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_frameworklogparser_parse`.
<!-- GRAPH_EDGE graph=unificata index=83 role=outgoing -->
Nel grafo unificato, il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_framework_event_key()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_frameworklogparser_parse` all'ID `Request-Dataset-Generator::log_collector_framework_event_key`. La relazione tecnica esplicita è la seguente: `.parse() [log_collector_frameworklogparser_parse]` chiama `_framework_event_key()`. Nel grafo unificato, il tipo dichiarato dell'arco 83 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 83 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 83 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 83 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 83 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `L92`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 83 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 83 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 83 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `1.0`. Nel grafo unificato, l'arco 83 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser_parse` per la relazione che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]`. Nel grafo unificato, l'arco 83 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_framework_event_key` per la relazione che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]`.
<!-- GRAPH_EDGE graph=unificata index=78 role=incoming -->
Nel grafo unificato, il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FrameworkLogParser`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_frameworklogparser` all'ID `Request-Dataset-Generator::log_collector_frameworklogparser_parse`. La relazione tecnica esplicita è la seguente: `FrameworkLogParser` registra come proprio metodo `.parse() [log_collector_frameworklogparser_parse]`. Nel grafo unificato, il tipo dichiarato dell'arco 78 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 78 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 78 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 78 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `L73`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 78 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 78 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 78 che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]` è `1.0`. Nel grafo unificato, l'arco 78 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]`. Nel grafo unificato, l'arco 78 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser_parse` per la relazione che coinvolge il nodo canonico `.parse() [log_collector_frameworklogparser_parse]`.

### .parse() [log_collector_middlewarelogparser_parse]

<!-- GRAPH_NODE graph=codice index=35 id=log_collector_middlewarelogparser_parse -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `.parse()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `L137`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` possiede l'identificatore esatto `log_collector_middlewarelogparser_parse`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `.parse()`.
Nel grafo del codice RDG, il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=87 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MiddlewareLogParser`; l'orientamento registrato nel JSON va dall'ID `log_collector_middlewarelogparser` all'ID `log_collector_middlewarelogparser_parse`. La relazione tecnica esplicita è la seguente: `MiddlewareLogParser` registra come proprio metodo `.parse() [log_collector_middlewarelogparser_parse]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 87 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 87 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 87 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 87 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `L137`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 87 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 87 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `ast`. Nel grafo del codice RDG, l'arco 87 registra come `source` l'identificatore `log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]`. Nel grafo del codice RDG, l'arco 87 registra come `target` l'identificatore `log_collector_middlewarelogparser_parse` per la relazione che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 87 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=90 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `log_collector_collect_logs` all'ID `log_collector_middlewarelogparser_parse`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `.parse() [log_collector_middlewarelogparser_parse]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 90 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 90 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 90 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 90 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 90 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `L595`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 90 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 90 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `ast`. Nel grafo del codice RDG, l'arco 90 registra come `source` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]`. Nel grafo del codice RDG, l'arco 90 registra come `target` l'identificatore `log_collector_middlewarelogparser_parse` per la relazione che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 90 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=35 id=Request-Dataset-Generator::log_collector_middlewarelogparser_parse -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `.parse()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `L137`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `.parse()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `log_collector_middlewarelogparser_parse`. Nel grafo unificato, il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_middlewarelogparser_parse`.
<!-- GRAPH_EDGE graph=unificata index=90 role=outgoing -->
Nel grafo unificato, il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è la sorgente `source` della relazione `calls` verso il nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_middlewarelogparser_parse` all'ID `Request-Dataset-Generator::log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `.parse() [log_collector_middlewarelogparser_parse]` chiama `collect_logs()`. Nel grafo unificato, il tipo dichiarato dell'arco 90 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 90 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 90 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 90 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 90 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `L595`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 90 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 90 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 90 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `1.0`. Nel grafo unificato, l'arco 90 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_middlewarelogparser_parse` per la relazione che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]`. Nel grafo unificato, l'arco 90 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]`.
<!-- GRAPH_EDGE graph=unificata index=87 role=incoming -->
Nel grafo unificato, il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MiddlewareLogParser`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_middlewarelogparser` all'ID `Request-Dataset-Generator::log_collector_middlewarelogparser_parse`. La relazione tecnica esplicita è la seguente: `MiddlewareLogParser` registra come proprio metodo `.parse() [log_collector_middlewarelogparser_parse]`. Nel grafo unificato, il tipo dichiarato dell'arco 87 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 87 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 87 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 87 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `L137`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 87 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 87 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 87 che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]` è `1.0`. Nel grafo unificato, l'arco 87 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]`. Nel grafo unificato, l'arco 87 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_middlewarelogparser_parse` per la relazione che coinvolge il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]`.

### _build_parser() [log_collector_build_parser]

<!-- GRAPH_NODE graph=codice index=48 id=log_collector_build_parser -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_build_parser() [log_collector_build_parser]` è `_build_parser()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_build_parser() [log_collector_build_parser]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_build_parser() [log_collector_build_parser]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_build_parser() [log_collector_build_parser]` è `L663`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_build_parser() [log_collector_build_parser]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_build_parser() [log_collector_build_parser]` possiede l'identificatore esatto `log_collector_build_parser`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_build_parser() [log_collector_build_parser]` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_build_parser() [log_collector_build_parser]` è `_build_parser()`.
<!-- GRAPH_EDGE graph=codice index=117 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_build_parser() [log_collector_build_parser]` è la sorgente `source` della relazione `references` verso il nodo canonico `ArgumentParser [log_collector_py_argumentparser]`; l'orientamento registrato nel JSON va dall'ID `log_collector_build_parser` all'ID `log_collector_py_argumentparser`. La relazione tecnica esplicita è la seguente: `_build_parser() [log_collector_build_parser]` fa riferimento a `ArgumentParser [log_collector_py_argumentparser]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 117 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 117 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `return_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 117 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 117 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 117 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `L663`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 117 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 117 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `ast`. Nel grafo del codice RDG, l'arco 117 registra come `source` l'identificatore `log_collector_build_parser` per la relazione che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]`. Nel grafo del codice RDG, l'arco 117 registra come `target` l'identificatore `log_collector_py_argumentparser` per la relazione che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 117 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=58 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_parser() [log_collector_build_parser]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_build_parser`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_build_parser() [log_collector_build_parser]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 58 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 58 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 58 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 58 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `L663`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 58 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 58 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `ast`. Nel grafo del codice RDG, l'arco 58 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]`. Nel grafo del codice RDG, l'arco 58 registra come `target` l'identificatore `log_collector_build_parser` per la relazione che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 58 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=118 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_parser() [log_collector_build_parser]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `main() [log_collector_main]`; l'orientamento registrato nel JSON va dall'ID `log_collector_main` all'ID `log_collector_build_parser`. La relazione tecnica esplicita è la seguente: `main() [log_collector_main]` chiama `_build_parser() [log_collector_build_parser]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 118 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 118 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 118 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 118 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 118 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `L677`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 118 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 118 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `ast`. Nel grafo del codice RDG, l'arco 118 registra come `source` l'identificatore `log_collector_main` per la relazione che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]`. Nel grafo del codice RDG, l'arco 118 registra come `target` l'identificatore `log_collector_build_parser` per la relazione che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 118 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=48 id=Request-Dataset-Generator::log_collector_build_parser -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_build_parser() [log_collector_build_parser]` è `_build_parser()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_build_parser() [log_collector_build_parser]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_build_parser() [log_collector_build_parser]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_build_parser() [log_collector_build_parser]` è `L663`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_build_parser() [log_collector_build_parser]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_build_parser() [log_collector_build_parser]` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_build_parser() [log_collector_build_parser]` è `_build_parser()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_build_parser() [log_collector_build_parser]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_build_parser() [log_collector_build_parser]` è `log_collector_build_parser`. Nel grafo unificato, il nodo canonico `_build_parser() [log_collector_build_parser]` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_build_parser`.
<!-- GRAPH_EDGE graph=unificata index=117 role=outgoing -->
Nel grafo unificato, il nodo canonico `_build_parser() [log_collector_build_parser]` è la sorgente `source` della relazione `references` verso il nodo canonico `ArgumentParser [log_collector_py_argumentparser]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_build_parser` all'ID `Request-Dataset-Generator::log_collector_py_argumentparser`. La relazione tecnica esplicita è la seguente: `_build_parser() [log_collector_build_parser]` fa riferimento a `ArgumentParser [log_collector_py_argumentparser]`. Nel grafo unificato, il tipo dichiarato dell'arco 117 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 117 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `return_type`. Nel grafo unificato, la confidenza categoriale dell'arco 117 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 117 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 117 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `L663`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 117 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 117 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 117 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `1.0`. Nel grafo unificato, l'arco 117 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_build_parser` per la relazione che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]`. Nel grafo unificato, l'arco 117 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_py_argumentparser` per la relazione che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]`.
<!-- GRAPH_EDGE graph=unificata index=118 role=outgoing -->
Nel grafo unificato, il nodo canonico `_build_parser() [log_collector_build_parser]` è la sorgente `source` della relazione `calls` verso il nodo canonico `main() [log_collector_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_build_parser` all'ID `Request-Dataset-Generator::log_collector_main`. La relazione tecnica esplicita è la seguente: `_build_parser() [log_collector_build_parser]` chiama `main() [log_collector_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 118 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 118 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 118 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 118 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 118 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `L677`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 118 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 118 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 118 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `1.0`. Nel grafo unificato, l'arco 118 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_build_parser` per la relazione che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]`. Nel grafo unificato, l'arco 118 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_main` per la relazione che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]`.
<!-- GRAPH_EDGE graph=unificata index=58 role=incoming -->
Nel grafo unificato, il nodo canonico `_build_parser() [log_collector_build_parser]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_build_parser`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_build_parser() [log_collector_build_parser]`. Nel grafo unificato, il tipo dichiarato dell'arco 58 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 58 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 58 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 58 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `L663`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 58 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 58 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 58 che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]` è `1.0`. Nel grafo unificato, l'arco 58 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]`. Nel grafo unificato, l'arco 58 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_build_parser` per la relazione che coinvolge il nodo canonico `_build_parser() [log_collector_build_parser]`.

### _first_present()

<!-- GRAPH_NODE graph=codice index=44 id=log_collector_first_present -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_first_present()` è `_first_present()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_first_present()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_first_present()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_first_present()` è `L571`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_first_present()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_first_present()` possiede l'identificatore esatto `log_collector_first_present`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_first_present()` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_first_present()` è `_first_present()`.
<!-- GRAPH_EDGE graph=codice index=76 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_first_present()` è la sorgente `source` della relazione `references` verso il nodo canonico `FrameworkLogParser`; l'orientamento registrato nel JSON va dall'ID `log_collector_first_present` all'ID `log_collector_frameworklogparser`. La relazione tecnica esplicita è la seguente: `_first_present()` fa riferimento a `FrameworkLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 76 che coinvolge il nodo canonico `_first_present()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 76 che coinvolge il nodo canonico `_first_present()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 76 che coinvolge il nodo canonico `_first_present()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 76 che coinvolge il nodo canonico `_first_present()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 76 che coinvolge il nodo canonico `_first_present()` è `L571`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 76 che coinvolge il nodo canonico `_first_present()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 76 che coinvolge il nodo canonico `_first_present()` è `ast`. Nel grafo del codice RDG, l'arco 76 registra come `source` l'identificatore `log_collector_first_present` per la relazione che coinvolge il nodo canonico `_first_present()`. Nel grafo del codice RDG, l'arco 76 registra come `target` l'identificatore `log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `_first_present()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 76 che coinvolge il nodo canonico `_first_present()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=106 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_first_present()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [log_collector_py_any]`; l'orientamento registrato nel JSON va dall'ID `log_collector_first_present` all'ID `log_collector_py_any`. La relazione tecnica esplicita è la seguente: `_first_present()` fa riferimento a `Any [log_collector_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 106 che coinvolge il nodo canonico `_first_present()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 106 che coinvolge il nodo canonico `_first_present()` è `return_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 106 che coinvolge il nodo canonico `_first_present()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 106 che coinvolge il nodo canonico `_first_present()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 106 che coinvolge il nodo canonico `_first_present()` è `L571`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 106 che coinvolge il nodo canonico `_first_present()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 106 che coinvolge il nodo canonico `_first_present()` è `ast`. Nel grafo del codice RDG, l'arco 106 registra come `source` l'identificatore `log_collector_first_present` per la relazione che coinvolge il nodo canonico `_first_present()`. Nel grafo del codice RDG, l'arco 106 registra come `target` l'identificatore `log_collector_py_any` per la relazione che coinvolge il nodo canonico `_first_present()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 106 che coinvolge il nodo canonico `_first_present()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=60 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_first_present()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_first_present`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_first_present()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 60 che coinvolge il nodo canonico `_first_present()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 60 che coinvolge il nodo canonico `_first_present()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 60 che coinvolge il nodo canonico `_first_present()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 60 che coinvolge il nodo canonico `_first_present()` è `L571`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 60 che coinvolge il nodo canonico `_first_present()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 60 che coinvolge il nodo canonico `_first_present()` è `ast`. Nel grafo del codice RDG, l'arco 60 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `_first_present()`. Nel grafo del codice RDG, l'arco 60 registra come `target` l'identificatore `log_collector_first_present` per la relazione che coinvolge il nodo canonico `_first_present()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 60 che coinvolge il nodo canonico `_first_present()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=105 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_first_present()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.build()`; l'orientamento registrato nel JSON va dall'ID `log_collector_normalizeddatasetbuilder_build` all'ID `log_collector_first_present`. La relazione tecnica esplicita è la seguente: `.build()` chiama `_first_present()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 105 che coinvolge il nodo canonico `_first_present()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 105 che coinvolge il nodo canonico `_first_present()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 105 che coinvolge il nodo canonico `_first_present()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 105 che coinvolge il nodo canonico `_first_present()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 105 che coinvolge il nodo canonico `_first_present()` è `L512`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 105 che coinvolge il nodo canonico `_first_present()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 105 che coinvolge il nodo canonico `_first_present()` è `ast`. Nel grafo del codice RDG, l'arco 105 registra come `source` l'identificatore `log_collector_normalizeddatasetbuilder_build` per la relazione che coinvolge il nodo canonico `_first_present()`. Nel grafo del codice RDG, l'arco 105 registra come `target` l'identificatore `log_collector_first_present` per la relazione che coinvolge il nodo canonico `_first_present()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 105 che coinvolge il nodo canonico `_first_present()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=44 id=Request-Dataset-Generator::log_collector_first_present -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_first_present()` è `_first_present()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_first_present()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_first_present()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_first_present()` è `L571`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_first_present()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_first_present()` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_first_present()` è `_first_present()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_first_present()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_first_present()` è `log_collector_first_present`. Nel grafo unificato, il nodo canonico `_first_present()` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_first_present`.
<!-- GRAPH_EDGE graph=unificata index=106 role=outgoing -->
Nel grafo unificato, il nodo canonico `_first_present()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [log_collector_py_any]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_first_present` all'ID `Request-Dataset-Generator::log_collector_py_any`. La relazione tecnica esplicita è la seguente: `_first_present()` fa riferimento a `Any [log_collector_py_any]`. Nel grafo unificato, il tipo dichiarato dell'arco 106 che coinvolge il nodo canonico `_first_present()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 106 che coinvolge il nodo canonico `_first_present()` è `return_type`. Nel grafo unificato, la confidenza categoriale dell'arco 106 che coinvolge il nodo canonico `_first_present()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 106 che coinvolge il nodo canonico `_first_present()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 106 che coinvolge il nodo canonico `_first_present()` è `L571`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 106 che coinvolge il nodo canonico `_first_present()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 106 che coinvolge il nodo canonico `_first_present()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 106 che coinvolge il nodo canonico `_first_present()` è `1.0`. Nel grafo unificato, l'arco 106 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_first_present` per la relazione che coinvolge il nodo canonico `_first_present()`. Nel grafo unificato, l'arco 106 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_py_any` per la relazione che coinvolge il nodo canonico `_first_present()`.
<!-- GRAPH_EDGE graph=unificata index=60 role=incoming -->
Nel grafo unificato, il nodo canonico `_first_present()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_first_present`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_first_present()`. Nel grafo unificato, il tipo dichiarato dell'arco 60 che coinvolge il nodo canonico `_first_present()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 60 che coinvolge il nodo canonico `_first_present()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 60 che coinvolge il nodo canonico `_first_present()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 60 che coinvolge il nodo canonico `_first_present()` è `L571`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 60 che coinvolge il nodo canonico `_first_present()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 60 che coinvolge il nodo canonico `_first_present()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 60 che coinvolge il nodo canonico `_first_present()` è `1.0`. Nel grafo unificato, l'arco 60 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `_first_present()`. Nel grafo unificato, l'arco 60 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_first_present` per la relazione che coinvolge il nodo canonico `_first_present()`.
<!-- GRAPH_EDGE graph=unificata index=76 role=incoming -->
Nel grafo unificato, il nodo canonico `_first_present()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `FrameworkLogParser`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_frameworklogparser` all'ID `Request-Dataset-Generator::log_collector_first_present`. La relazione tecnica esplicita è la seguente: `FrameworkLogParser` fa riferimento a `_first_present()`. Nel grafo unificato, il tipo dichiarato dell'arco 76 che coinvolge il nodo canonico `_first_present()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 76 che coinvolge il nodo canonico `_first_present()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 76 che coinvolge il nodo canonico `_first_present()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 76 che coinvolge il nodo canonico `_first_present()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 76 che coinvolge il nodo canonico `_first_present()` è `L571`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 76 che coinvolge il nodo canonico `_first_present()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 76 che coinvolge il nodo canonico `_first_present()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 76 che coinvolge il nodo canonico `_first_present()` è `1.0`. Nel grafo unificato, l'arco 76 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `_first_present()`. Nel grafo unificato, l'arco 76 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_first_present` per la relazione che coinvolge il nodo canonico `_first_present()`.
<!-- GRAPH_EDGE graph=unificata index=105 role=incoming -->
Nel grafo unificato, il nodo canonico `_first_present()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.build()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build` all'ID `Request-Dataset-Generator::log_collector_first_present`. La relazione tecnica esplicita è la seguente: `.build()` chiama `_first_present()`. Nel grafo unificato, il tipo dichiarato dell'arco 105 che coinvolge il nodo canonico `_first_present()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 105 che coinvolge il nodo canonico `_first_present()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 105 che coinvolge il nodo canonico `_first_present()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 105 che coinvolge il nodo canonico `_first_present()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 105 che coinvolge il nodo canonico `_first_present()` è `L512`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 105 che coinvolge il nodo canonico `_first_present()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 105 che coinvolge il nodo canonico `_first_present()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 105 che coinvolge il nodo canonico `_first_present()` è `1.0`. Nel grafo unificato, l'arco 105 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build` per la relazione che coinvolge il nodo canonico `_first_present()`. Nel grafo unificato, l'arco 105 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_first_present` per la relazione che coinvolge il nodo canonico `_first_present()`.

### _framework_event_key()

<!-- GRAPH_NODE graph=codice index=32 id=log_collector_framework_event_key -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_framework_event_key()` è `_framework_event_key()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_framework_event_key()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_framework_event_key()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_framework_event_key()` è `L113`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_framework_event_key()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_framework_event_key()` possiede l'identificatore esatto `log_collector_framework_event_key`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_framework_event_key()` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_framework_event_key()` è `_framework_event_key()`.
Nel grafo del codice RDG, il nodo canonico `_framework_event_key()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=61 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_framework_event_key()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_framework_event_key`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_framework_event_key()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 61 che coinvolge il nodo canonico `_framework_event_key()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 61 che coinvolge il nodo canonico `_framework_event_key()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 61 che coinvolge il nodo canonico `_framework_event_key()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 61 che coinvolge il nodo canonico `_framework_event_key()` è `L113`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 61 che coinvolge il nodo canonico `_framework_event_key()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 61 che coinvolge il nodo canonico `_framework_event_key()` è `ast`. Nel grafo del codice RDG, l'arco 61 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `_framework_event_key()`. Nel grafo del codice RDG, l'arco 61 registra come `target` l'identificatore `log_collector_framework_event_key` per la relazione che coinvolge il nodo canonico `_framework_event_key()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 61 che coinvolge il nodo canonico `_framework_event_key()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=83 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_framework_event_key()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.parse() [log_collector_frameworklogparser_parse]`; l'orientamento registrato nel JSON va dall'ID `log_collector_frameworklogparser_parse` all'ID `log_collector_framework_event_key`. La relazione tecnica esplicita è la seguente: `.parse() [log_collector_frameworklogparser_parse]` chiama `_framework_event_key()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 83 che coinvolge il nodo canonico `_framework_event_key()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 83 che coinvolge il nodo canonico `_framework_event_key()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 83 che coinvolge il nodo canonico `_framework_event_key()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 83 che coinvolge il nodo canonico `_framework_event_key()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 83 che coinvolge il nodo canonico `_framework_event_key()` è `L92`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 83 che coinvolge il nodo canonico `_framework_event_key()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 83 che coinvolge il nodo canonico `_framework_event_key()` è `ast`. Nel grafo del codice RDG, l'arco 83 registra come `source` l'identificatore `log_collector_frameworklogparser_parse` per la relazione che coinvolge il nodo canonico `_framework_event_key()`. Nel grafo del codice RDG, l'arco 83 registra come `target` l'identificatore `log_collector_framework_event_key` per la relazione che coinvolge il nodo canonico `_framework_event_key()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 83 che coinvolge il nodo canonico `_framework_event_key()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=32 id=Request-Dataset-Generator::log_collector_framework_event_key -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_framework_event_key()` è `_framework_event_key()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_framework_event_key()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_framework_event_key()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_framework_event_key()` è `L113`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_framework_event_key()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_framework_event_key()` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_framework_event_key()` è `_framework_event_key()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_framework_event_key()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_framework_event_key()` è `log_collector_framework_event_key`. Nel grafo unificato, il nodo canonico `_framework_event_key()` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_framework_event_key`.
Nel grafo unificato, il nodo canonico `_framework_event_key()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=61 role=incoming -->
Nel grafo unificato, il nodo canonico `_framework_event_key()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_framework_event_key`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_framework_event_key()`. Nel grafo unificato, il tipo dichiarato dell'arco 61 che coinvolge il nodo canonico `_framework_event_key()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 61 che coinvolge il nodo canonico `_framework_event_key()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 61 che coinvolge il nodo canonico `_framework_event_key()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 61 che coinvolge il nodo canonico `_framework_event_key()` è `L113`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 61 che coinvolge il nodo canonico `_framework_event_key()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 61 che coinvolge il nodo canonico `_framework_event_key()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 61 che coinvolge il nodo canonico `_framework_event_key()` è `1.0`. Nel grafo unificato, l'arco 61 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `_framework_event_key()`. Nel grafo unificato, l'arco 61 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_framework_event_key` per la relazione che coinvolge il nodo canonico `_framework_event_key()`.
<!-- GRAPH_EDGE graph=unificata index=83 role=incoming -->
Nel grafo unificato, il nodo canonico `_framework_event_key()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.parse() [log_collector_frameworklogparser_parse]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_frameworklogparser_parse` all'ID `Request-Dataset-Generator::log_collector_framework_event_key`. La relazione tecnica esplicita è la seguente: `.parse() [log_collector_frameworklogparser_parse]` chiama `_framework_event_key()`. Nel grafo unificato, il tipo dichiarato dell'arco 83 che coinvolge il nodo canonico `_framework_event_key()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 83 che coinvolge il nodo canonico `_framework_event_key()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 83 che coinvolge il nodo canonico `_framework_event_key()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 83 che coinvolge il nodo canonico `_framework_event_key()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 83 che coinvolge il nodo canonico `_framework_event_key()` è `L92`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 83 che coinvolge il nodo canonico `_framework_event_key()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 83 che coinvolge il nodo canonico `_framework_event_key()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 83 che coinvolge il nodo canonico `_framework_event_key()` è `1.0`. Nel grafo unificato, l'arco 83 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser_parse` per la relazione che coinvolge il nodo canonico `_framework_event_key()`. Nel grafo unificato, l'arco 83 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_framework_event_key` per la relazione che coinvolge il nodo canonico `_framework_event_key()`.

### _load_config() [log_collector_load_config]

<!-- GRAPH_NODE graph=codice index=47 id=log_collector_load_config -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_load_config() [log_collector_load_config]` è `_load_config()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_load_config() [log_collector_load_config]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_load_config() [log_collector_load_config]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_load_config() [log_collector_load_config]` è `L655`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_load_config() [log_collector_load_config]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_load_config() [log_collector_load_config]` possiede l'identificatore esatto `log_collector_load_config`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_load_config() [log_collector_load_config]` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_load_config() [log_collector_load_config]` è `_load_config()`.
Nel grafo del codice RDG, il nodo canonico `_load_config() [log_collector_load_config]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=63 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_load_config() [log_collector_load_config]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_load_config`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_load_config() [log_collector_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 63 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 63 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 63 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 63 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `L655`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 63 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 63 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `ast`. Nel grafo del codice RDG, l'arco 63 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `_load_config() [log_collector_load_config]`. Nel grafo del codice RDG, l'arco 63 registra come `target` l'identificatore `log_collector_load_config` per la relazione che coinvolge il nodo canonico `_load_config() [log_collector_load_config]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 63 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=116 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_load_config() [log_collector_load_config]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `main() [log_collector_main]`; l'orientamento registrato nel JSON va dall'ID `log_collector_main` all'ID `log_collector_load_config`. La relazione tecnica esplicita è la seguente: `main() [log_collector_main]` chiama `_load_config() [log_collector_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 116 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 116 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 116 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 116 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 116 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `L678`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 116 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 116 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `ast`. Nel grafo del codice RDG, l'arco 116 registra come `source` l'identificatore `log_collector_main` per la relazione che coinvolge il nodo canonico `_load_config() [log_collector_load_config]`. Nel grafo del codice RDG, l'arco 116 registra come `target` l'identificatore `log_collector_load_config` per la relazione che coinvolge il nodo canonico `_load_config() [log_collector_load_config]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 116 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=47 id=Request-Dataset-Generator::log_collector_load_config -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_load_config() [log_collector_load_config]` è `_load_config()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_load_config() [log_collector_load_config]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_load_config() [log_collector_load_config]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_load_config() [log_collector_load_config]` è `L655`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_load_config() [log_collector_load_config]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_load_config() [log_collector_load_config]` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_load_config() [log_collector_load_config]` è `_load_config()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_load_config() [log_collector_load_config]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_load_config() [log_collector_load_config]` è `log_collector_load_config`. Nel grafo unificato, il nodo canonico `_load_config() [log_collector_load_config]` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_load_config`.
<!-- GRAPH_EDGE graph=unificata index=116 role=outgoing -->
Nel grafo unificato, il nodo canonico `_load_config() [log_collector_load_config]` è la sorgente `source` della relazione `calls` verso il nodo canonico `main() [log_collector_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_load_config` all'ID `Request-Dataset-Generator::log_collector_main`. La relazione tecnica esplicita è la seguente: `_load_config() [log_collector_load_config]` chiama `main() [log_collector_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 116 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 116 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 116 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 116 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 116 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `L678`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 116 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 116 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 116 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `1.0`. Nel grafo unificato, l'arco 116 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_load_config` per la relazione che coinvolge il nodo canonico `_load_config() [log_collector_load_config]`. Nel grafo unificato, l'arco 116 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_main` per la relazione che coinvolge il nodo canonico `_load_config() [log_collector_load_config]`.
<!-- GRAPH_EDGE graph=unificata index=63 role=incoming -->
Nel grafo unificato, il nodo canonico `_load_config() [log_collector_load_config]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_load_config`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_load_config() [log_collector_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 63 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 63 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 63 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 63 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `L655`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 63 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 63 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 63 che coinvolge il nodo canonico `_load_config() [log_collector_load_config]` è `1.0`. Nel grafo unificato, l'arco 63 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `_load_config() [log_collector_load_config]`. Nel grafo unificato, l'arco 63 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_load_config` per la relazione che coinvolge il nodo canonico `_load_config() [log_collector_load_config]`.

### _percentile()

<!-- GRAPH_NODE graph=codice index=41 id=log_collector_percentile -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_percentile()` è `_percentile()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_percentile()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_percentile()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_percentile()` è `L481`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_percentile()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_percentile()` possiede l'identificatore esatto `log_collector_percentile`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_percentile()` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_percentile()` è `_percentile()`.
Nel grafo del codice RDG, il nodo canonico `_percentile()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=68 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_percentile()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_percentile`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_percentile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 68 che coinvolge il nodo canonico `_percentile()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 68 che coinvolge il nodo canonico `_percentile()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 68 che coinvolge il nodo canonico `_percentile()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 68 che coinvolge il nodo canonico `_percentile()` è `L481`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 68 che coinvolge il nodo canonico `_percentile()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 68 che coinvolge il nodo canonico `_percentile()` è `ast`. Nel grafo del codice RDG, l'arco 68 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `_percentile()`. Nel grafo del codice RDG, l'arco 68 registra come `target` l'identificatore `log_collector_percentile` per la relazione che coinvolge il nodo canonico `_percentile()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 68 che coinvolge il nodo canonico `_percentile()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=100 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_percentile()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.aggregate()`; l'orientamento registrato nel JSON va dall'ID `log_collector_timeseriesaggregator_aggregate` all'ID `log_collector_percentile`. La relazione tecnica esplicita è la seguente: `.aggregate()` chiama `_percentile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 100 che coinvolge il nodo canonico `_percentile()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 100 che coinvolge il nodo canonico `_percentile()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 100 che coinvolge il nodo canonico `_percentile()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 100 che coinvolge il nodo canonico `_percentile()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 100 che coinvolge il nodo canonico `_percentile()` è `L422`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 100 che coinvolge il nodo canonico `_percentile()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 100 che coinvolge il nodo canonico `_percentile()` è `ast`. Nel grafo del codice RDG, l'arco 100 registra come `source` l'identificatore `log_collector_timeseriesaggregator_aggregate` per la relazione che coinvolge il nodo canonico `_percentile()`. Nel grafo del codice RDG, l'arco 100 registra come `target` l'identificatore `log_collector_percentile` per la relazione che coinvolge il nodo canonico `_percentile()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 100 che coinvolge il nodo canonico `_percentile()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=101 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_percentile()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Calcola il percentile p da una lista già ordinata.`; l'orientamento registrato nel JSON va dall'ID `log_collector_rationale_482` all'ID `log_collector_percentile`. La relazione tecnica esplicita è la seguente: `Calcola il percentile p da una lista già ordinata.` costituisce una motivazione per `_percentile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 101 che coinvolge il nodo canonico `_percentile()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 101 che coinvolge il nodo canonico `_percentile()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 101 che coinvolge il nodo canonico `_percentile()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 101 che coinvolge il nodo canonico `_percentile()` è `L482`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 101 che coinvolge il nodo canonico `_percentile()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 101 che coinvolge il nodo canonico `_percentile()` è `ast`. Nel grafo del codice RDG, l'arco 101 registra come `source` l'identificatore `log_collector_rationale_482` per la relazione che coinvolge il nodo canonico `_percentile()`. Nel grafo del codice RDG, l'arco 101 registra come `target` l'identificatore `log_collector_percentile` per la relazione che coinvolge il nodo canonico `_percentile()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 101 che coinvolge il nodo canonico `_percentile()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=41 id=Request-Dataset-Generator::log_collector_percentile -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_percentile()` è `_percentile()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_percentile()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_percentile()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_percentile()` è `L481`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_percentile()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_percentile()` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_percentile()` è `_percentile()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_percentile()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_percentile()` è `log_collector_percentile`. Nel grafo unificato, il nodo canonico `_percentile()` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_percentile`.
<!-- GRAPH_EDGE graph=unificata index=101 role=outgoing -->
Nel grafo unificato, il nodo canonico `_percentile()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Calcola il percentile p da una lista già ordinata.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_percentile` all'ID `Request-Dataset-Generator::log_collector_rationale_482`. La relazione tecnica esplicita è la seguente: `_percentile()` costituisce una motivazione per `Calcola il percentile p da una lista già ordinata.`. Nel grafo unificato, il tipo dichiarato dell'arco 101 che coinvolge il nodo canonico `_percentile()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 101 che coinvolge il nodo canonico `_percentile()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 101 che coinvolge il nodo canonico `_percentile()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 101 che coinvolge il nodo canonico `_percentile()` è `L482`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 101 che coinvolge il nodo canonico `_percentile()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 101 che coinvolge il nodo canonico `_percentile()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 101 che coinvolge il nodo canonico `_percentile()` è `1.0`. Nel grafo unificato, l'arco 101 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_percentile` per la relazione che coinvolge il nodo canonico `_percentile()`. Nel grafo unificato, l'arco 101 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_rationale_482` per la relazione che coinvolge il nodo canonico `_percentile()`.
<!-- GRAPH_EDGE graph=unificata index=68 role=incoming -->
Nel grafo unificato, il nodo canonico `_percentile()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_percentile`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_percentile()`. Nel grafo unificato, il tipo dichiarato dell'arco 68 che coinvolge il nodo canonico `_percentile()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 68 che coinvolge il nodo canonico `_percentile()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 68 che coinvolge il nodo canonico `_percentile()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 68 che coinvolge il nodo canonico `_percentile()` è `L481`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 68 che coinvolge il nodo canonico `_percentile()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 68 che coinvolge il nodo canonico `_percentile()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 68 che coinvolge il nodo canonico `_percentile()` è `1.0`. Nel grafo unificato, l'arco 68 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `_percentile()`. Nel grafo unificato, l'arco 68 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_percentile` per la relazione che coinvolge il nodo canonico `_percentile()`.
<!-- GRAPH_EDGE graph=unificata index=100 role=incoming -->
Nel grafo unificato, il nodo canonico `_percentile()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.aggregate()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_timeseriesaggregator_aggregate` all'ID `Request-Dataset-Generator::log_collector_percentile`. La relazione tecnica esplicita è la seguente: `.aggregate()` chiama `_percentile()`. Nel grafo unificato, il tipo dichiarato dell'arco 100 che coinvolge il nodo canonico `_percentile()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 100 che coinvolge il nodo canonico `_percentile()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 100 che coinvolge il nodo canonico `_percentile()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 100 che coinvolge il nodo canonico `_percentile()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 100 che coinvolge il nodo canonico `_percentile()` è `L422`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 100 che coinvolge il nodo canonico `_percentile()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 100 che coinvolge il nodo canonico `_percentile()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 100 che coinvolge il nodo canonico `_percentile()` è `1.0`. Nel grafo unificato, l'arco 100 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_timeseriesaggregator_aggregate` per la relazione che coinvolge il nodo canonico `_percentile()`. Nel grafo unificato, l'arco 100 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_percentile` per la relazione che coinvolge il nodo canonico `_percentile()`.

### _resolve_output_path() [log_collector_resolve_output_path]

<!-- GRAPH_NODE graph=codice index=28 id=log_collector_resolve_output_path -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `_resolve_output_path()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `L41`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` possiede l'identificatore esatto `log_collector_resolve_output_path`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `_resolve_output_path()`.
<!-- GRAPH_EDGE graph=codice index=74 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è la sorgente `source` della relazione `calls` verso il nodo canonico `.join()`; l'orientamento registrato nel JSON va dall'ID `log_collector_resolve_output_path` all'ID `log_collector_logjoiner_join`. La relazione tecnica esplicita è la seguente: `_resolve_output_path() [log_collector_resolve_output_path]` chiama `.join()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 74 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 74 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 74 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 74 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 74 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `L46`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 74 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 74 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `ast`. Nel grafo del codice RDG, l'arco 74 registra come `source` l'identificatore `log_collector_resolve_output_path` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]`. Nel grafo del codice RDG, l'arco 74 registra come `target` l'identificatore `log_collector_logjoiner_join` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 74 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=69 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_resolve_output_path`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_resolve_output_path() [log_collector_resolve_output_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 69 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 69 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 69 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 69 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `L41`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 69 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 69 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `ast`. Nel grafo del codice RDG, l'arco 69 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]`. Nel grafo del codice RDG, l'arco 69 registra come `target` l'identificatore `log_collector_resolve_output_path` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 69 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=73 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `main() [log_collector_main]`; l'orientamento registrato nel JSON va dall'ID `log_collector_main` all'ID `log_collector_resolve_output_path`. La relazione tecnica esplicita è la seguente: `main() [log_collector_main]` chiama `_resolve_output_path() [log_collector_resolve_output_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 73 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 73 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 73 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 73 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 73 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `L688`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 73 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 73 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `ast`. Nel grafo del codice RDG, l'arco 73 registra come `source` l'identificatore `log_collector_main` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]`. Nel grafo del codice RDG, l'arco 73 registra come `target` l'identificatore `log_collector_resolve_output_path` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 73 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=28 id=Request-Dataset-Generator::log_collector_resolve_output_path -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `_resolve_output_path()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `L41`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `_resolve_output_path()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `log_collector_resolve_output_path`. Nel grafo unificato, il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_resolve_output_path`.
<!-- GRAPH_EDGE graph=unificata index=73 role=outgoing -->
Nel grafo unificato, il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è la sorgente `source` della relazione `calls` verso il nodo canonico `main() [log_collector_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_resolve_output_path` all'ID `Request-Dataset-Generator::log_collector_main`. La relazione tecnica esplicita è la seguente: `_resolve_output_path() [log_collector_resolve_output_path]` chiama `main() [log_collector_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 73 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 73 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 73 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 73 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 73 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `L688`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 73 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 73 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 73 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `1.0`. Nel grafo unificato, l'arco 73 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_resolve_output_path` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]`. Nel grafo unificato, l'arco 73 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_main` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]`.
<!-- GRAPH_EDGE graph=unificata index=74 role=outgoing -->
Nel grafo unificato, il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è la sorgente `source` della relazione `calls` verso il nodo canonico `.join()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_resolve_output_path` all'ID `Request-Dataset-Generator::log_collector_logjoiner_join`. La relazione tecnica esplicita è la seguente: `_resolve_output_path() [log_collector_resolve_output_path]` chiama `.join()`. Nel grafo unificato, il tipo dichiarato dell'arco 74 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 74 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 74 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 74 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 74 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `L46`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 74 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 74 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 74 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `1.0`. Nel grafo unificato, l'arco 74 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_resolve_output_path` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]`. Nel grafo unificato, l'arco 74 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner_join` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]`.
<!-- GRAPH_EDGE graph=unificata index=69 role=incoming -->
Nel grafo unificato, il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_resolve_output_path`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_resolve_output_path() [log_collector_resolve_output_path]`. Nel grafo unificato, il tipo dichiarato dell'arco 69 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 69 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 69 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 69 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `L41`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 69 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 69 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 69 che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]` è `1.0`. Nel grafo unificato, l'arco 69 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]`. Nel grafo unificato, l'arco 69 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_resolve_output_path` per la relazione che coinvolge il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]`.

### Aggrega i record normalizzati in bucket temporali.

<!-- GRAPH_NODE graph=codice index=55 id=log_collector_rationale_324 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `Aggrega i record normalizzati in bucket temporali.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Aggrega i record normalizzati in bucket temporali.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `L324`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Aggrega i record normalizzati in bucket temporali.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Aggrega i record normalizzati in bucket temporali.` possiede l'identificatore esatto `log_collector_rationale_324`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Aggrega i record normalizzati in bucket temporali.` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `aggrega i record normalizzati in bucket temporali.`.
<!-- GRAPH_EDGE graph=codice index=97 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `TimeseriesAggregator`; l'orientamento registrato nel JSON va dall'ID `log_collector_rationale_324` all'ID `log_collector_timeseriesaggregator`. La relazione tecnica esplicita è la seguente: `Aggrega i record normalizzati in bucket temporali.` costituisce una motivazione per `TimeseriesAggregator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 97 che coinvolge il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 97 che coinvolge il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 97 che coinvolge il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 97 che coinvolge il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `L324`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 97 che coinvolge il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 97 che coinvolge il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `ast`. Nel grafo del codice RDG, l'arco 97 registra come `source` l'identificatore `log_collector_rationale_324` per la relazione che coinvolge il nodo canonico `Aggrega i record normalizzati in bucket temporali.`. Nel grafo del codice RDG, l'arco 97 registra come `target` l'identificatore `log_collector_timeseriesaggregator` per la relazione che coinvolge il nodo canonico `Aggrega i record normalizzati in bucket temporali.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 97 che coinvolge il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Aggrega i record normalizzati in bucket temporali.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=55 id=Request-Dataset-Generator::log_collector_rationale_324 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `Aggrega i record normalizzati in bucket temporali.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Aggrega i record normalizzati in bucket temporali.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `L324`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Aggrega i record normalizzati in bucket temporali.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Aggrega i record normalizzati in bucket temporali.` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `aggrega i record normalizzati in bucket temporali.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Aggrega i record normalizzati in bucket temporali.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `log_collector_rationale_324`. Nel grafo unificato, il nodo canonico `Aggrega i record normalizzati in bucket temporali.` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_rationale_324`.
Nel grafo unificato, il nodo canonico `Aggrega i record normalizzati in bucket temporali.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=97 role=incoming -->
Nel grafo unificato, il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `TimeseriesAggregator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_timeseriesaggregator` all'ID `Request-Dataset-Generator::log_collector_rationale_324`. La relazione tecnica esplicita è la seguente: `TimeseriesAggregator` costituisce una motivazione per `Aggrega i record normalizzati in bucket temporali.`. Nel grafo unificato, il tipo dichiarato dell'arco 97 che coinvolge il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 97 che coinvolge il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 97 che coinvolge il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 97 che coinvolge il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `L324`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 97 che coinvolge il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 97 che coinvolge il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 97 che coinvolge il nodo canonico `Aggrega i record normalizzati in bucket temporali.` è `1.0`. Nel grafo unificato, l'arco 97 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_timeseriesaggregator` per la relazione che coinvolge il nodo canonico `Aggrega i record normalizzati in bucket temporali.`. Nel grafo unificato, l'arco 97 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_rationale_324` per la relazione che coinvolge il nodo canonico `Aggrega i record normalizzati in bucket temporali.`.

### Any [log_collector_py_any]

<!-- GRAPH_NODE graph=codice index=45 id=log_collector_py_any -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Any [log_collector_py_any]` è `Any`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Any [log_collector_py_any]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Any [log_collector_py_any]` è ``. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Any [log_collector_py_any]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Any [log_collector_py_any]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Any [log_collector_py_any]` possiede l'identificatore esatto `log_collector_py_any`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Any [log_collector_py_any]` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Any [log_collector_py_any]` è `any`.
Nel grafo del codice RDG, il nodo canonico `Any [log_collector_py_any]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=106 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Any [log_collector_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_first_present()`; l'orientamento registrato nel JSON va dall'ID `log_collector_first_present` all'ID `log_collector_py_any`. La relazione tecnica esplicita è la seguente: `_first_present()` fa riferimento a `Any [log_collector_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 106 che coinvolge il nodo canonico `Any [log_collector_py_any]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 106 che coinvolge il nodo canonico `Any [log_collector_py_any]` è `return_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 106 che coinvolge il nodo canonico `Any [log_collector_py_any]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 106 che coinvolge il nodo canonico `Any [log_collector_py_any]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 106 che coinvolge il nodo canonico `Any [log_collector_py_any]` è `L571`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 106 che coinvolge il nodo canonico `Any [log_collector_py_any]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 106 che coinvolge il nodo canonico `Any [log_collector_py_any]` è `ast`. Nel grafo del codice RDG, l'arco 106 registra come `source` l'identificatore `log_collector_first_present` per la relazione che coinvolge il nodo canonico `Any [log_collector_py_any]`. Nel grafo del codice RDG, l'arco 106 registra come `target` l'identificatore `log_collector_py_any` per la relazione che coinvolge il nodo canonico `Any [log_collector_py_any]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 106 che coinvolge il nodo canonico `Any [log_collector_py_any]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=45 id=Request-Dataset-Generator::log_collector_py_any -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Any [log_collector_py_any]` è `Any`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Any [log_collector_py_any]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Any [log_collector_py_any]` è ``. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Any [log_collector_py_any]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Any [log_collector_py_any]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Any [log_collector_py_any]` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Any [log_collector_py_any]` è `any`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Any [log_collector_py_any]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Any [log_collector_py_any]` è `log_collector_py_any`. Nel grafo unificato, il nodo canonico `Any [log_collector_py_any]` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_py_any`.
Nel grafo unificato, il nodo canonico `Any [log_collector_py_any]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=106 role=incoming -->
Nel grafo unificato, il nodo canonico `Any [log_collector_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_first_present()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_first_present` all'ID `Request-Dataset-Generator::log_collector_py_any`. La relazione tecnica esplicita è la seguente: `_first_present()` fa riferimento a `Any [log_collector_py_any]`. Nel grafo unificato, il tipo dichiarato dell'arco 106 che coinvolge il nodo canonico `Any [log_collector_py_any]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 106 che coinvolge il nodo canonico `Any [log_collector_py_any]` è `return_type`. Nel grafo unificato, la confidenza categoriale dell'arco 106 che coinvolge il nodo canonico `Any [log_collector_py_any]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 106 che coinvolge il nodo canonico `Any [log_collector_py_any]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 106 che coinvolge il nodo canonico `Any [log_collector_py_any]` è `L571`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 106 che coinvolge il nodo canonico `Any [log_collector_py_any]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 106 che coinvolge il nodo canonico `Any [log_collector_py_any]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 106 che coinvolge il nodo canonico `Any [log_collector_py_any]` è `1.0`. Nel grafo unificato, l'arco 106 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_first_present` per la relazione che coinvolge il nodo canonico `Any [log_collector_py_any]`. Nel grafo unificato, l'arco 106 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_py_any` per la relazione che coinvolge il nodo canonico `Any [log_collector_py_any]`.

### ArgumentParser [log_collector_py_argumentparser]

<!-- GRAPH_NODE graph=codice index=49 id=log_collector_py_argumentparser -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `ArgumentParser`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è ``. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` possiede l'identificatore esatto `log_collector_py_argumentparser`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `argumentparser`.
Nel grafo del codice RDG, il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=117 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_build_parser() [log_collector_build_parser]`; l'orientamento registrato nel JSON va dall'ID `log_collector_build_parser` all'ID `log_collector_py_argumentparser`. La relazione tecnica esplicita è la seguente: `_build_parser() [log_collector_build_parser]` fa riferimento a `ArgumentParser [log_collector_py_argumentparser]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 117 che coinvolge il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 117 che coinvolge il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `return_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 117 che coinvolge il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 117 che coinvolge il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 117 che coinvolge il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `L663`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 117 che coinvolge il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 117 che coinvolge il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `ast`. Nel grafo del codice RDG, l'arco 117 registra come `source` l'identificatore `log_collector_build_parser` per la relazione che coinvolge il nodo canonico `ArgumentParser [log_collector_py_argumentparser]`. Nel grafo del codice RDG, l'arco 117 registra come `target` l'identificatore `log_collector_py_argumentparser` per la relazione che coinvolge il nodo canonico `ArgumentParser [log_collector_py_argumentparser]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 117 che coinvolge il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=49 id=Request-Dataset-Generator::log_collector_py_argumentparser -->
Nel grafo unificato, l'etichetta originale del nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `ArgumentParser`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è ``. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `argumentparser`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `log_collector_py_argumentparser`. Nel grafo unificato, il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_py_argumentparser`.
Nel grafo unificato, il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=117 role=incoming -->
Nel grafo unificato, il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_build_parser() [log_collector_build_parser]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_build_parser` all'ID `Request-Dataset-Generator::log_collector_py_argumentparser`. La relazione tecnica esplicita è la seguente: `_build_parser() [log_collector_build_parser]` fa riferimento a `ArgumentParser [log_collector_py_argumentparser]`. Nel grafo unificato, il tipo dichiarato dell'arco 117 che coinvolge il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 117 che coinvolge il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `return_type`. Nel grafo unificato, la confidenza categoriale dell'arco 117 che coinvolge il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 117 che coinvolge il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 117 che coinvolge il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `L663`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 117 che coinvolge il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 117 che coinvolge il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 117 che coinvolge il nodo canonico `ArgumentParser [log_collector_py_argumentparser]` è `1.0`. Nel grafo unificato, l'arco 117 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_build_parser` per la relazione che coinvolge il nodo canonico `ArgumentParser [log_collector_py_argumentparser]`. Nel grafo unificato, l'arco 117 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_py_argumentparser` per la relazione che coinvolge il nodo canonico `ArgumentParser [log_collector_py_argumentparser]`.

### Assembla il JSON finale del Normalized Metrics Dataset.

<!-- GRAPH_NODE graph=codice index=57 id=log_collector_rationale_498 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `Assembla il JSON finale del Normalized Metrics Dataset.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `L498`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` possiede l'identificatore esatto `log_collector_rationale_498`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `assembla il json finale del normalized metrics dataset.`.
<!-- GRAPH_EDGE graph=codice index=103 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `NormalizedDatasetBuilder`; l'orientamento registrato nel JSON va dall'ID `log_collector_rationale_498` all'ID `log_collector_normalizeddatasetbuilder`. La relazione tecnica esplicita è la seguente: `Assembla il JSON finale del Normalized Metrics Dataset.` costituisce una motivazione per `NormalizedDatasetBuilder`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 103 che coinvolge il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 103 che coinvolge il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 103 che coinvolge il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 103 che coinvolge il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `L498`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 103 che coinvolge il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 103 che coinvolge il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `ast`. Nel grafo del codice RDG, l'arco 103 registra come `source` l'identificatore `log_collector_rationale_498` per la relazione che coinvolge il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.`. Nel grafo del codice RDG, l'arco 103 registra come `target` l'identificatore `log_collector_normalizeddatasetbuilder` per la relazione che coinvolge il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 103 che coinvolge il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=57 id=Request-Dataset-Generator::log_collector_rationale_498 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `Assembla il JSON finale del Normalized Metrics Dataset.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `L498`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `assembla il json finale del normalized metrics dataset.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `log_collector_rationale_498`. Nel grafo unificato, il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_rationale_498`.
Nel grafo unificato, il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=103 role=incoming -->
Nel grafo unificato, il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `NormalizedDatasetBuilder`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder` all'ID `Request-Dataset-Generator::log_collector_rationale_498`. La relazione tecnica esplicita è la seguente: `NormalizedDatasetBuilder` costituisce una motivazione per `Assembla il JSON finale del Normalized Metrics Dataset.`. Nel grafo unificato, il tipo dichiarato dell'arco 103 che coinvolge il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 103 che coinvolge il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 103 che coinvolge il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 103 che coinvolge il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `L498`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 103 che coinvolge il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 103 che coinvolge il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 103 che coinvolge il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.` è `1.0`. Nel grafo unificato, l'arco 103 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder` per la relazione che coinvolge il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.`. Nel grafo unificato, l'arco 103 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_rationale_498` per la relazione che coinvolge il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.`.

### Calcola il percentile p da una lista già ordinata.

<!-- GRAPH_NODE graph=codice index=56 id=log_collector_rationale_482 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Calcola il percentile p da una lista già ordinata.` è `Calcola il percentile p da una lista già ordinata.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Calcola il percentile p da una lista già ordinata.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Calcola il percentile p da una lista già ordinata.` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Calcola il percentile p da una lista già ordinata.` è `L482`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Calcola il percentile p da una lista già ordinata.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Calcola il percentile p da una lista già ordinata.` possiede l'identificatore esatto `log_collector_rationale_482`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Calcola il percentile p da una lista già ordinata.` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Calcola il percentile p da una lista già ordinata.` è `calcola il percentile p da una lista gia ordinata.`.
<!-- GRAPH_EDGE graph=codice index=101 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Calcola il percentile p da una lista già ordinata.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `_percentile()`; l'orientamento registrato nel JSON va dall'ID `log_collector_rationale_482` all'ID `log_collector_percentile`. La relazione tecnica esplicita è la seguente: `Calcola il percentile p da una lista già ordinata.` costituisce una motivazione per `_percentile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 101 che coinvolge il nodo canonico `Calcola il percentile p da una lista già ordinata.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 101 che coinvolge il nodo canonico `Calcola il percentile p da una lista già ordinata.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 101 che coinvolge il nodo canonico `Calcola il percentile p da una lista già ordinata.` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 101 che coinvolge il nodo canonico `Calcola il percentile p da una lista già ordinata.` è `L482`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 101 che coinvolge il nodo canonico `Calcola il percentile p da una lista già ordinata.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 101 che coinvolge il nodo canonico `Calcola il percentile p da una lista già ordinata.` è `ast`. Nel grafo del codice RDG, l'arco 101 registra come `source` l'identificatore `log_collector_rationale_482` per la relazione che coinvolge il nodo canonico `Calcola il percentile p da una lista già ordinata.`. Nel grafo del codice RDG, l'arco 101 registra come `target` l'identificatore `log_collector_percentile` per la relazione che coinvolge il nodo canonico `Calcola il percentile p da una lista già ordinata.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 101 che coinvolge il nodo canonico `Calcola il percentile p da una lista già ordinata.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Calcola il percentile p da una lista già ordinata.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=56 id=Request-Dataset-Generator::log_collector_rationale_482 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Calcola il percentile p da una lista già ordinata.` è `Calcola il percentile p da una lista già ordinata.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Calcola il percentile p da una lista già ordinata.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Calcola il percentile p da una lista già ordinata.` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Calcola il percentile p da una lista già ordinata.` è `L482`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Calcola il percentile p da una lista già ordinata.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Calcola il percentile p da una lista già ordinata.` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Calcola il percentile p da una lista già ordinata.` è `calcola il percentile p da una lista gia ordinata.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Calcola il percentile p da una lista già ordinata.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Calcola il percentile p da una lista già ordinata.` è `log_collector_rationale_482`. Nel grafo unificato, il nodo canonico `Calcola il percentile p da una lista già ordinata.` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_rationale_482`.
Nel grafo unificato, il nodo canonico `Calcola il percentile p da una lista già ordinata.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=101 role=incoming -->
Nel grafo unificato, il nodo canonico `Calcola il percentile p da una lista già ordinata.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `_percentile()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_percentile` all'ID `Request-Dataset-Generator::log_collector_rationale_482`. La relazione tecnica esplicita è la seguente: `_percentile()` costituisce una motivazione per `Calcola il percentile p da una lista già ordinata.`. Nel grafo unificato, il tipo dichiarato dell'arco 101 che coinvolge il nodo canonico `Calcola il percentile p da una lista già ordinata.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 101 che coinvolge il nodo canonico `Calcola il percentile p da una lista già ordinata.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 101 che coinvolge il nodo canonico `Calcola il percentile p da una lista già ordinata.` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 101 che coinvolge il nodo canonico `Calcola il percentile p da una lista già ordinata.` è `L482`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 101 che coinvolge il nodo canonico `Calcola il percentile p da una lista già ordinata.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 101 che coinvolge il nodo canonico `Calcola il percentile p da una lista già ordinata.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 101 che coinvolge il nodo canonico `Calcola il percentile p da una lista già ordinata.` è `1.0`. Nel grafo unificato, l'arco 101 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_percentile` per la relazione che coinvolge il nodo canonico `Calcola il percentile p da una lista già ordinata.`. Nel grafo unificato, l'arco 101 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_rationale_482` per la relazione che coinvolge il nodo canonico `Calcola il percentile p da una lista già ordinata.`.

### collect_logs()

<!-- GRAPH_NODE graph=codice index=46 id=log_collector_collect_logs -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `collect_logs()` è `collect_logs()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `collect_logs()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `collect_logs()` è `L584`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `collect_logs()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `collect_logs()` possiede l'identificatore esatto `log_collector_collect_logs`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `collect_logs()` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `collect_logs()` è `collect_logs()`.
<!-- GRAPH_EDGE graph=codice index=75 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `collect_logs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `FrameworkLogParser`; l'orientamento registrato nel JSON va dall'ID `log_collector_collect_logs` all'ID `log_collector_frameworklogparser`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `FrameworkLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 75 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 75 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 75 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 75 che coinvolge il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 75 che coinvolge il nodo canonico `collect_logs()` è `L594`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 75 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 75 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo del codice RDG, l'arco 75 registra come `source` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, l'arco 75 registra come `target` l'identificatore `log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 75 che coinvolge il nodo canonico `collect_logs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=84 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `collect_logs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `MiddlewareLogParser`; l'orientamento registrato nel JSON va dall'ID `log_collector_collect_logs` all'ID `log_collector_middlewarelogparser`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `MiddlewareLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 84 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 84 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 84 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 84 che coinvolge il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 84 che coinvolge il nodo canonico `collect_logs()` è `L608`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 84 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 84 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo del codice RDG, l'arco 84 registra come `source` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, l'arco 84 registra come `target` l'identificatore `log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 84 che coinvolge il nodo canonico `collect_logs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=90 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `collect_logs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]`; l'orientamento registrato nel JSON va dall'ID `log_collector_collect_logs` all'ID `log_collector_middlewarelogparser_parse`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `.parse() [log_collector_middlewarelogparser_parse]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 90 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 90 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 90 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 90 che coinvolge il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 90 che coinvolge il nodo canonico `collect_logs()` è `L595`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 90 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 90 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo del codice RDG, l'arco 90 registra come `source` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, l'arco 90 registra come `target` l'identificatore `log_collector_middlewarelogparser_parse` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 90 che coinvolge il nodo canonico `collect_logs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=94 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `collect_logs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.join()`; l'orientamento registrato nel JSON va dall'ID `log_collector_collect_logs` all'ID `log_collector_logjoiner_join`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `.join()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 94 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 94 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 94 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 94 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 94 che coinvolge il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 94 che coinvolge il nodo canonico `collect_logs()` è `L615`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 94 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 94 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo del codice RDG, l'arco 94 registra come `source` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, l'arco 94 registra come `target` l'identificatore `log_collector_logjoiner_join` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=codice index=95 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `collect_logs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.build_flows()`; l'orientamento registrato nel JSON va dall'ID `log_collector_collect_logs` all'ID `log_collector_logjoiner_build_flows`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `.build_flows()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 95 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 95 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 95 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 95 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 95 che coinvolge il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 95 che coinvolge il nodo canonico `collect_logs()` è `L619`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 95 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 95 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo del codice RDG, l'arco 95 registra come `source` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, l'arco 95 registra come `target` l'identificatore `log_collector_logjoiner_build_flows` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=codice index=99 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `collect_logs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.aggregate()`; l'orientamento registrato nel JSON va dall'ID `log_collector_collect_logs` all'ID `log_collector_timeseriesaggregator_aggregate`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `.aggregate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 99 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 99 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 99 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 99 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 99 che coinvolge il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 99 che coinvolge il nodo canonico `collect_logs()` è `L623`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 99 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 99 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo del codice RDG, l'arco 99 registra come `source` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, l'arco 99 registra come `target` l'identificatore `log_collector_timeseriesaggregator_aggregate` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=codice index=104 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `collect_logs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.build()`; l'orientamento registrato nel JSON va dall'ID `log_collector_collect_logs` all'ID `log_collector_normalizeddatasetbuilder_build`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `.build()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 104 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 104 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 104 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 104 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 104 che coinvolge il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 104 che coinvolge il nodo canonico `collect_logs()` è `L627`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 104 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 104 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo del codice RDG, l'arco 104 registra come `source` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, l'arco 104 registra come `target` l'identificatore `log_collector_normalizeddatasetbuilder_build` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=codice index=59 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `collect_logs()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `collect_logs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 59 che coinvolge il nodo canonico `collect_logs()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 59 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 59 che coinvolge il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 59 che coinvolge il nodo canonico `collect_logs()` è `L584`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 59 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 59 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo del codice RDG, l'arco 59 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, l'arco 59 registra come `target` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 59 che coinvolge il nodo canonico `collect_logs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=107 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `collect_logs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `main() [log_collector_main]`; l'orientamento registrato nel JSON va dall'ID `log_collector_main` all'ID `log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `main() [log_collector_main]` chiama `collect_logs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 107 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 107 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 107 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 107 che coinvolge il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 107 che coinvolge il nodo canonico `collect_logs()` è `L687`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 107 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 107 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo del codice RDG, l'arco 107 registra come `source` l'identificatore `log_collector_main` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, l'arco 107 registra come `target` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 107 che coinvolge il nodo canonico `collect_logs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=108 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `collect_logs()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.`; l'orientamento registrato nel JSON va dall'ID `log_collector_rationale_591` all'ID `log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` costituisce una motivazione per `collect_logs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 108 che coinvolge il nodo canonico `collect_logs()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 108 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 108 che coinvolge il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 108 che coinvolge il nodo canonico `collect_logs()` è `L591`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 108 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 108 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo del codice RDG, l'arco 108 registra come `source` l'identificatore `log_collector_rationale_591` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, l'arco 108 registra come `target` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 108 che coinvolge il nodo canonico `collect_logs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=109 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `collect_logs()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `main.py` importa `collect_logs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 109 che coinvolge il nodo canonico `collect_logs()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 109 che coinvolge il nodo canonico `collect_logs()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 109 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 109 che coinvolge il nodo canonico `collect_logs()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 109 che coinvolge il nodo canonico `collect_logs()` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 109 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 109 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo del codice RDG, l'arco 109 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, l'arco 109 registra come `target` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 109 che coinvolge il nodo canonico `collect_logs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=110 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `collect_logs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_collect()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_collect` all'ID `log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `cmd_collect()` chiama `collect_logs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 110 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 110 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 110 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 110 che coinvolge il nodo canonico `collect_logs()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 110 che coinvolge il nodo canonico `collect_logs()` è `L485`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 110 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 110 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo del codice RDG, l'arco 110 registra come `source` l'identificatore `main_cmd_collect` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, l'arco 110 registra come `target` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 110 che coinvolge il nodo canonico `collect_logs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=111 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `collect_logs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `cmd_pipeline()`; l'orientamento registrato nel JSON va dall'ID `main_cmd_pipeline` all'ID `log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `cmd_pipeline()` chiama `collect_logs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 111 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 111 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 111 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 111 che coinvolge il nodo canonico `collect_logs()` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 111 che coinvolge il nodo canonico `collect_logs()` è `L531`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 111 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 111 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo del codice RDG, l'arco 111 registra come `source` l'identificatore `main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, l'arco 111 registra come `target` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 111 che coinvolge il nodo canonico `collect_logs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=112 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `collect_logs()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `collect_logs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 112 che coinvolge il nodo canonico `collect_logs()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 112 che coinvolge il nodo canonico `collect_logs()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 112 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 112 che coinvolge il nodo canonico `collect_logs()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 112 che coinvolge il nodo canonico `collect_logs()` è `L10`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 112 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 112 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo del codice RDG, l'arco 112 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, l'arco 112 registra come `target` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 112 che coinvolge il nodo canonico `collect_logs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=113 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `collect_logs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` all'ID `log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `.test_error_injection_produces_measurable_failures_and_skipped_steps()` chiama `collect_logs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 113 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 113 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 113 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 113 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 113 che coinvolge il nodo canonico `collect_logs()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 113 che coinvolge il nodo canonico `collect_logs()` è `L695`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 113 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 113 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo del codice RDG, l'arco 113 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, l'arco 113 registra come `target` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=codice index=114 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `collect_logs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` all'ID `log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `.test_framework_log_is_sufficient_for_normalized_dataset()` chiama `collect_logs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 114 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 114 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 114 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 114 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 114 che coinvolge il nodo canonico `collect_logs()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 114 che coinvolge il nodo canonico `collect_logs()` è `L637`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 114 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 114 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo del codice RDG, l'arco 114 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, l'arco 114 registra come `target` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=codice index=115 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `collect_logs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_metric_engine_writes_csv_tables()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` all'ID `log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `.test_metric_engine_writes_csv_tables()` chiama `collect_logs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 115 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 115 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 115 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 115 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 115 che coinvolge il nodo canonico `collect_logs()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 115 che coinvolge il nodo canonico `collect_logs()` è `L749`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 115 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 115 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo del codice RDG, l'arco 115 registra come `source` l'identificatore `tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo del codice RDG, l'arco 115 registra come `target` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`.

<!-- GRAPH_NODE graph=unificata index=46 id=Request-Dataset-Generator::log_collector_collect_logs -->
Nel grafo unificato, l'etichetta originale del nodo canonico `collect_logs()` è `collect_logs()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `collect_logs()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `collect_logs()` è `L584`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `collect_logs()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `collect_logs()` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `collect_logs()` è `collect_logs()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `collect_logs()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `collect_logs()` è `log_collector_collect_logs`. Nel grafo unificato, il nodo canonico `collect_logs()` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_collect_logs`.
<!-- GRAPH_EDGE graph=unificata index=107 role=outgoing -->
Nel grafo unificato, il nodo canonico `collect_logs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `main() [log_collector_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_collect_logs` all'ID `Request-Dataset-Generator::log_collector_main`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `main() [log_collector_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 107 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 107 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 107 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 107 che coinvolge il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 107 che coinvolge il nodo canonico `collect_logs()` è `L687`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 107 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 107 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 107 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, l'arco 107 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo unificato, l'arco 107 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_main` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=unificata index=108 role=outgoing -->
Nel grafo unificato, il nodo canonico `collect_logs()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_collect_logs` all'ID `Request-Dataset-Generator::log_collector_rationale_591`. La relazione tecnica esplicita è la seguente: `collect_logs()` costituisce una motivazione per `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.`. Nel grafo unificato, il tipo dichiarato dell'arco 108 che coinvolge il nodo canonico `collect_logs()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 108 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 108 che coinvolge il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 108 che coinvolge il nodo canonico `collect_logs()` è `L591`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 108 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 108 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 108 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, l'arco 108 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo unificato, l'arco 108 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_rationale_591` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=unificata index=109 role=outgoing -->
Nel grafo unificato, il nodo canonico `collect_logs()` è la sorgente `source` della relazione `imports` verso il nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_collect_logs` all'ID `Request-Dataset-Generator::main`. La relazione tecnica esplicita è la seguente: `collect_logs()` importa `main.py`. Nel grafo unificato, il tipo dichiarato dell'arco 109 che coinvolge il nodo canonico `collect_logs()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 109 che coinvolge il nodo canonico `collect_logs()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 109 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 109 che coinvolge il nodo canonico `collect_logs()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 109 che coinvolge il nodo canonico `collect_logs()` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 109 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 109 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 109 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, l'arco 109 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo unificato, l'arco 109 registra come `target` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=unificata index=110 role=outgoing -->
Nel grafo unificato, il nodo canonico `collect_logs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_collect()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_collect_logs` all'ID `Request-Dataset-Generator::main_cmd_collect`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `cmd_collect()`. Nel grafo unificato, il tipo dichiarato dell'arco 110 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 110 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 110 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 110 che coinvolge il nodo canonico `collect_logs()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 110 che coinvolge il nodo canonico `collect_logs()` è `L485`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 110 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 110 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 110 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, l'arco 110 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo unificato, l'arco 110 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_collect` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=unificata index=111 role=outgoing -->
Nel grafo unificato, il nodo canonico `collect_logs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `cmd_pipeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_collect_logs` all'ID `Request-Dataset-Generator::main_cmd_pipeline`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `cmd_pipeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 111 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 111 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 111 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 111 che coinvolge il nodo canonico `collect_logs()` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 111 che coinvolge il nodo canonico `collect_logs()` è `L531`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 111 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 111 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 111 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, l'arco 111 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo unificato, l'arco 111 registra come `target` l'identificatore `Request-Dataset-Generator::main_cmd_pipeline` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=unificata index=112 role=outgoing -->
Nel grafo unificato, il nodo canonico `collect_logs()` è la sorgente `source` della relazione `imports` verso il nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_collect_logs` all'ID `Request-Dataset-Generator::tests_test_core`. La relazione tecnica esplicita è la seguente: `collect_logs()` importa `test_core.py`. Nel grafo unificato, il tipo dichiarato dell'arco 112 che coinvolge il nodo canonico `collect_logs()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 112 che coinvolge il nodo canonico `collect_logs()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 112 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 112 che coinvolge il nodo canonico `collect_logs()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 112 che coinvolge il nodo canonico `collect_logs()` è `L10`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 112 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 112 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 112 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, l'arco 112 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo unificato, l'arco 112 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=unificata index=113 role=outgoing -->
Nel grafo unificato, il nodo canonico `collect_logs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.test_error_injection_produces_measurable_failures_and_skipped_steps()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_collect_logs` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `.test_error_injection_produces_measurable_failures_and_skipped_steps()`. Nel grafo unificato, il tipo dichiarato dell'arco 113 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 113 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 113 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 113 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 113 che coinvolge il nodo canonico `collect_logs()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 113 che coinvolge il nodo canonico `collect_logs()` è `L695`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 113 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 113 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo unificato, l'arco 113 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo unificato, l'arco 113 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_error_injection_produces_measurable_failures_and_skipped_steps` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=unificata index=114 role=outgoing -->
Nel grafo unificato, il nodo canonico `collect_logs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.test_framework_log_is_sufficient_for_normalized_dataset()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_collect_logs` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `.test_framework_log_is_sufficient_for_normalized_dataset()`. Nel grafo unificato, il tipo dichiarato dell'arco 114 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 114 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 114 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 114 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 114 che coinvolge il nodo canonico `collect_logs()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 114 che coinvolge il nodo canonico `collect_logs()` è `L637`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 114 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 114 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo unificato, l'arco 114 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo unificato, l'arco 114 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_framework_log_is_sufficient_for_normalized_dataset` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=unificata index=115 role=outgoing -->
Nel grafo unificato, il nodo canonico `collect_logs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.test_metric_engine_writes_csv_tables()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_collect_logs` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `.test_metric_engine_writes_csv_tables()`. Nel grafo unificato, il tipo dichiarato dell'arco 115 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 115 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 115 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 115 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 115 che coinvolge il nodo canonico `collect_logs()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 115 che coinvolge il nodo canonico `collect_logs()` è `L749`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 115 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 115 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo unificato, l'arco 115 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo unificato, l'arco 115 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest_test_metric_engine_writes_csv_tables` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=unificata index=59 role=incoming -->
Nel grafo unificato, il nodo canonico `collect_logs()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `collect_logs()`. Nel grafo unificato, il tipo dichiarato dell'arco 59 che coinvolge il nodo canonico `collect_logs()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 59 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 59 che coinvolge il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 59 che coinvolge il nodo canonico `collect_logs()` è `L584`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 59 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 59 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 59 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, l'arco 59 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo unificato, l'arco 59 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=unificata index=75 role=incoming -->
Nel grafo unificato, il nodo canonico `collect_logs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `FrameworkLogParser`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_frameworklogparser` all'ID `Request-Dataset-Generator::log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `FrameworkLogParser` chiama `collect_logs()`. Nel grafo unificato, il tipo dichiarato dell'arco 75 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 75 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 75 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 75 che coinvolge il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 75 che coinvolge il nodo canonico `collect_logs()` è `L594`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 75 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 75 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 75 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, l'arco 75 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo unificato, l'arco 75 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=unificata index=84 role=incoming -->
Nel grafo unificato, il nodo canonico `collect_logs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `MiddlewareLogParser`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_middlewarelogparser` all'ID `Request-Dataset-Generator::log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `MiddlewareLogParser` chiama `collect_logs()`. Nel grafo unificato, il tipo dichiarato dell'arco 84 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 84 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 84 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 84 che coinvolge il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 84 che coinvolge il nodo canonico `collect_logs()` è `L608`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 84 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 84 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 84 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, l'arco 84 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo unificato, l'arco 84 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=unificata index=90 role=incoming -->
Nel grafo unificato, il nodo canonico `collect_logs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.parse() [log_collector_middlewarelogparser_parse]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_middlewarelogparser_parse` all'ID `Request-Dataset-Generator::log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `.parse() [log_collector_middlewarelogparser_parse]` chiama `collect_logs()`. Nel grafo unificato, il tipo dichiarato dell'arco 90 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 90 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 90 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 90 che coinvolge il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 90 che coinvolge il nodo canonico `collect_logs()` è `L595`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 90 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 90 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 90 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, l'arco 90 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_middlewarelogparser_parse` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo unificato, l'arco 90 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=unificata index=94 role=incoming -->
Nel grafo unificato, il nodo canonico `collect_logs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.join()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_logjoiner_join` all'ID `Request-Dataset-Generator::log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `.join()` chiama `collect_logs()`. Nel grafo unificato, il tipo dichiarato dell'arco 94 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 94 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 94 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 94 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 94 che coinvolge il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 94 che coinvolge il nodo canonico `collect_logs()` è `L615`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 94 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 94 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo unificato, l'arco 94 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner_join` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo unificato, l'arco 94 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=unificata index=95 role=incoming -->
Nel grafo unificato, il nodo canonico `collect_logs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.build_flows()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_logjoiner_build_flows` all'ID `Request-Dataset-Generator::log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `.build_flows()` chiama `collect_logs()`. Nel grafo unificato, il tipo dichiarato dell'arco 95 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 95 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 95 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 95 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 95 che coinvolge il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 95 che coinvolge il nodo canonico `collect_logs()` è `L619`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 95 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 95 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo unificato, l'arco 95 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner_build_flows` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo unificato, l'arco 95 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=unificata index=99 role=incoming -->
Nel grafo unificato, il nodo canonico `collect_logs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.aggregate()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_timeseriesaggregator_aggregate` all'ID `Request-Dataset-Generator::log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `.aggregate()` chiama `collect_logs()`. Nel grafo unificato, il tipo dichiarato dell'arco 99 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 99 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 99 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 99 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 99 che coinvolge il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 99 che coinvolge il nodo canonico `collect_logs()` è `L623`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 99 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 99 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo unificato, l'arco 99 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_timeseriesaggregator_aggregate` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo unificato, l'arco 99 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`.
<!-- GRAPH_EDGE graph=unificata index=104 role=incoming -->
Nel grafo unificato, il nodo canonico `collect_logs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.build()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build` all'ID `Request-Dataset-Generator::log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `.build()` chiama `collect_logs()`. Nel grafo unificato, il tipo dichiarato dell'arco 104 che coinvolge il nodo canonico `collect_logs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 104 che coinvolge il nodo canonico `collect_logs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 104 che coinvolge il nodo canonico `collect_logs()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 104 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 104 che coinvolge il nodo canonico `collect_logs()` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 104 che coinvolge il nodo canonico `collect_logs()` è `L627`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 104 che coinvolge il nodo canonico `collect_logs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 104 che coinvolge il nodo canonico `collect_logs()` è `ast`. Nel grafo unificato, l'arco 104 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build` per la relazione che coinvolge il nodo canonico `collect_logs()`. Nel grafo unificato, l'arco 104 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `collect_logs()`.

### Costruisce la sezione &quot;flows&quot; dagli eventi flow_completed         arricchiti con

<!-- GRAPH_NODE graph=codice index=54 id=log_collector_rationale_254 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `L254`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` possiede l'identificatore esatto `log_collector_rationale_254`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con`.
<!-- GRAPH_EDGE graph=codice index=96 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `.build_flows()`; l'orientamento registrato nel JSON va dall'ID `log_collector_rationale_254` all'ID `log_collector_logjoiner_build_flows`. La relazione tecnica esplicita è la seguente: `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` costituisce una motivazione per `.build_flows()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 96 che coinvolge il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 96 che coinvolge il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 96 che coinvolge il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 96 che coinvolge il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `L254`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 96 che coinvolge il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 96 che coinvolge il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `ast`. Nel grafo del codice RDG, l'arco 96 registra come `source` l'identificatore `log_collector_rationale_254` per la relazione che coinvolge il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con`. Nel grafo del codice RDG, l'arco 96 registra come `target` l'identificatore `log_collector_logjoiner_build_flows` per la relazione che coinvolge il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 96 che coinvolge il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=54 id=Request-Dataset-Generator::log_collector_rationale_254 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `L254`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `log_collector_rationale_254`. Nel grafo unificato, il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_rationale_254`.
Nel grafo unificato, il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=96 role=incoming -->
Nel grafo unificato, il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `.build_flows()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_logjoiner_build_flows` all'ID `Request-Dataset-Generator::log_collector_rationale_254`. La relazione tecnica esplicita è la seguente: `.build_flows()` costituisce una motivazione per `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con`. Nel grafo unificato, il tipo dichiarato dell'arco 96 che coinvolge il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 96 che coinvolge il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 96 che coinvolge il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 96 che coinvolge il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `L254`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 96 che coinvolge il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 96 che coinvolge il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 96 che coinvolge il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con` è `1.0`. Nel grafo unificato, l'arco 96 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner_build_flows` per la relazione che coinvolge il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con`. Nel grafo unificato, l'arco 96 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_rationale_254` per la relazione che coinvolge il nodo canonico `Costruisce la sezione "flows" dagli eventi flow_completed         arricchiti con`.

### Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.

<!-- GRAPH_NODE graph=codice index=58 id=log_collector_rationale_591 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `L591`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` possiede l'identificatore esatto `log_collector_rationale_591`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `esegue la normalizzazione log ed eventualmente scrive il dataset json.`.
<!-- GRAPH_EDGE graph=codice index=108 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `log_collector_rationale_591` all'ID `log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` costituisce una motivazione per `collect_logs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 108 che coinvolge il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 108 che coinvolge il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 108 che coinvolge il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 108 che coinvolge il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `L591`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 108 che coinvolge il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 108 che coinvolge il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `ast`. Nel grafo del codice RDG, l'arco 108 registra come `source` l'identificatore `log_collector_rationale_591` per la relazione che coinvolge il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.`. Nel grafo del codice RDG, l'arco 108 registra come `target` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 108 che coinvolge il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=58 id=Request-Dataset-Generator::log_collector_rationale_591 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `L591`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `esegue la normalizzazione log ed eventualmente scrive il dataset json.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `log_collector_rationale_591`. Nel grafo unificato, il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_rationale_591`.
Nel grafo unificato, il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=108 role=incoming -->
Nel grafo unificato, il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_collect_logs` all'ID `Request-Dataset-Generator::log_collector_rationale_591`. La relazione tecnica esplicita è la seguente: `collect_logs()` costituisce una motivazione per `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.`. Nel grafo unificato, il tipo dichiarato dell'arco 108 che coinvolge il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 108 che coinvolge il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 108 che coinvolge il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 108 che coinvolge il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `L591`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 108 che coinvolge il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 108 che coinvolge il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 108 che coinvolge il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.` è `1.0`. Nel grafo unificato, l'arco 108 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.`. Nel grafo unificato, l'arco 108 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_rationale_591` per la relazione che coinvolge il nodo canonico `Esegue la normalizzazione log ed eventualmente scrive il dataset JSON.`.

### Fa il join tra framework log e middleware log su request_id.      Produce la lis

<!-- GRAPH_NODE graph=codice index=53 id=log_collector_rationale_161 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `Fa il join tra framework log e middleware log su request_id.      Produce la lis`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `L161`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` possiede l'identificatore esatto `log_collector_rationale_161`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `fa il join tra framework log e middleware log su request_id.      produce la lis`.
<!-- GRAPH_EDGE graph=codice index=93 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `LogJoiner`; l'orientamento registrato nel JSON va dall'ID `log_collector_rationale_161` all'ID `log_collector_logjoiner`. La relazione tecnica esplicita è la seguente: `Fa il join tra framework log e middleware log su request_id.      Produce la lis` costituisce una motivazione per `LogJoiner`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 93 che coinvolge il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 93 che coinvolge il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 93 che coinvolge il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 93 che coinvolge il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `L161`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 93 che coinvolge il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 93 che coinvolge il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `ast`. Nel grafo del codice RDG, l'arco 93 registra come `source` l'identificatore `log_collector_rationale_161` per la relazione che coinvolge il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis`. Nel grafo del codice RDG, l'arco 93 registra come `target` l'identificatore `log_collector_logjoiner` per la relazione che coinvolge il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 93 che coinvolge il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=53 id=Request-Dataset-Generator::log_collector_rationale_161 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `Fa il join tra framework log e middleware log su request_id.      Produce la lis`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `L161`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `fa il join tra framework log e middleware log su request_id.      produce la lis`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `log_collector_rationale_161`. Nel grafo unificato, il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_rationale_161`.
Nel grafo unificato, il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=93 role=incoming -->
Nel grafo unificato, il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `LogJoiner`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_logjoiner` all'ID `Request-Dataset-Generator::log_collector_rationale_161`. La relazione tecnica esplicita è la seguente: `LogJoiner` costituisce una motivazione per `Fa il join tra framework log e middleware log su request_id.      Produce la lis`. Nel grafo unificato, il tipo dichiarato dell'arco 93 che coinvolge il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 93 che coinvolge il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 93 che coinvolge il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 93 che coinvolge il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `L161`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 93 che coinvolge il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 93 che coinvolge il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 93 che coinvolge il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis` è `1.0`. Nel grafo unificato, l'arco 93 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner` per la relazione che coinvolge il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis`. Nel grafo unificato, l'arco 93 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_rationale_161` per la relazione che coinvolge il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis`.

### FrameworkLogParser

<!-- GRAPH_NODE graph=codice index=29 id=log_collector_frameworklogparser -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `FrameworkLogParser` è `FrameworkLogParser`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `FrameworkLogParser` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `FrameworkLogParser` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `FrameworkLogParser` è `L53`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `FrameworkLogParser` vale `ast`. Nel grafo del codice RDG, il nodo canonico `FrameworkLogParser` possiede l'identificatore esatto `log_collector_frameworklogparser`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `FrameworkLogParser` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `FrameworkLogParser` è `frameworklogparser`.
<!-- GRAPH_EDGE graph=codice index=77 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FrameworkLogParser` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [log_collector_frameworklogparser_init]`; l'orientamento registrato nel JSON va dall'ID `log_collector_frameworklogparser` all'ID `log_collector_frameworklogparser_init`. La relazione tecnica esplicita è la seguente: `FrameworkLogParser` registra come proprio metodo `.__init__() [log_collector_frameworklogparser_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 77 che coinvolge il nodo canonico `FrameworkLogParser` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 77 che coinvolge il nodo canonico `FrameworkLogParser` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 77 che coinvolge il nodo canonico `FrameworkLogParser` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 77 che coinvolge il nodo canonico `FrameworkLogParser` è `L64`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 77 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 77 che coinvolge il nodo canonico `FrameworkLogParser` è `ast`. Nel grafo del codice RDG, l'arco 77 registra come `source` l'identificatore `log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo del codice RDG, l'arco 77 registra come `target` l'identificatore `log_collector_frameworklogparser_init` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 77 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=78 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FrameworkLogParser` è la sorgente `source` della relazione `method` verso il nodo canonico `.parse() [log_collector_frameworklogparser_parse]`; l'orientamento registrato nel JSON va dall'ID `log_collector_frameworklogparser` all'ID `log_collector_frameworklogparser_parse`. La relazione tecnica esplicita è la seguente: `FrameworkLogParser` registra come proprio metodo `.parse() [log_collector_frameworklogparser_parse]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 78 che coinvolge il nodo canonico `FrameworkLogParser` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 78 che coinvolge il nodo canonico `FrameworkLogParser` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 78 che coinvolge il nodo canonico `FrameworkLogParser` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 78 che coinvolge il nodo canonico `FrameworkLogParser` è `L73`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 78 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 78 che coinvolge il nodo canonico `FrameworkLogParser` è `ast`. Nel grafo del codice RDG, l'arco 78 registra come `source` l'identificatore `log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo del codice RDG, l'arco 78 registra come `target` l'identificatore `log_collector_frameworklogparser_parse` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 78 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=62 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FrameworkLogParser` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_frameworklogparser`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `FrameworkLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 62 che coinvolge il nodo canonico `FrameworkLogParser` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 62 che coinvolge il nodo canonico `FrameworkLogParser` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 62 che coinvolge il nodo canonico `FrameworkLogParser` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 62 che coinvolge il nodo canonico `FrameworkLogParser` è `L53`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 62 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 62 che coinvolge il nodo canonico `FrameworkLogParser` è `ast`. Nel grafo del codice RDG, l'arco 62 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo del codice RDG, l'arco 62 registra come `target` l'identificatore `log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 62 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=75 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FrameworkLogParser` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `log_collector_collect_logs` all'ID `log_collector_frameworklogparser`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `FrameworkLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 75 che coinvolge il nodo canonico `FrameworkLogParser` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 75 che coinvolge il nodo canonico `FrameworkLogParser` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 75 che coinvolge il nodo canonico `FrameworkLogParser` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 75 che coinvolge il nodo canonico `FrameworkLogParser` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 75 che coinvolge il nodo canonico `FrameworkLogParser` è `L594`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 75 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 75 che coinvolge il nodo canonico `FrameworkLogParser` è `ast`. Nel grafo del codice RDG, l'arco 75 registra come `source` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo del codice RDG, l'arco 75 registra come `target` l'identificatore `log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 75 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=76 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FrameworkLogParser` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_first_present()`; l'orientamento registrato nel JSON va dall'ID `log_collector_first_present` all'ID `log_collector_frameworklogparser`. La relazione tecnica esplicita è la seguente: `_first_present()` fa riferimento a `FrameworkLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 76 che coinvolge il nodo canonico `FrameworkLogParser` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 76 che coinvolge il nodo canonico `FrameworkLogParser` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 76 che coinvolge il nodo canonico `FrameworkLogParser` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 76 che coinvolge il nodo canonico `FrameworkLogParser` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 76 che coinvolge il nodo canonico `FrameworkLogParser` è `L571`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 76 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 76 che coinvolge il nodo canonico `FrameworkLogParser` è `ast`. Nel grafo del codice RDG, l'arco 76 registra come `source` l'identificatore `log_collector_first_present` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo del codice RDG, l'arco 76 registra come `target` l'identificatore `log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 76 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=79 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FrameworkLogParser` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `.build_flows()`; l'orientamento registrato nel JSON va dall'ID `log_collector_logjoiner_build_flows` all'ID `log_collector_frameworklogparser`. La relazione tecnica esplicita è la seguente: `.build_flows()` fa riferimento a `FrameworkLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 79 che coinvolge il nodo canonico `FrameworkLogParser` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 79 che coinvolge il nodo canonico `FrameworkLogParser` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 79 che coinvolge il nodo canonico `FrameworkLogParser` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 79 che coinvolge il nodo canonico `FrameworkLogParser` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 79 che coinvolge il nodo canonico `FrameworkLogParser` è `L250`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 79 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 79 che coinvolge il nodo canonico `FrameworkLogParser` è `ast`. Nel grafo del codice RDG, l'arco 79 registra come `source` l'identificatore `log_collector_logjoiner_build_flows` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo del codice RDG, l'arco 79 registra come `target` l'identificatore `log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 79 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=80 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FrameworkLogParser` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `.join()`; l'orientamento registrato nel JSON va dall'ID `log_collector_logjoiner_join` all'ID `log_collector_frameworklogparser`. La relazione tecnica esplicita è la seguente: `.join()` fa riferimento a `FrameworkLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 80 che coinvolge il nodo canonico `FrameworkLogParser` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 80 che coinvolge il nodo canonico `FrameworkLogParser` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 80 che coinvolge il nodo canonico `FrameworkLogParser` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 80 che coinvolge il nodo canonico `FrameworkLogParser` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 80 che coinvolge il nodo canonico `FrameworkLogParser` è `L168`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 80 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 80 che coinvolge il nodo canonico `FrameworkLogParser` è `ast`. Nel grafo del codice RDG, l'arco 80 registra come `source` l'identificatore `log_collector_logjoiner_join` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo del codice RDG, l'arco 80 registra come `target` l'identificatore `log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 80 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=81 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FrameworkLogParser` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `.build()`; l'orientamento registrato nel JSON va dall'ID `log_collector_normalizeddatasetbuilder_build` all'ID `log_collector_frameworklogparser`. La relazione tecnica esplicita è la seguente: `.build()` fa riferimento a `FrameworkLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 81 che coinvolge il nodo canonico `FrameworkLogParser` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 81 che coinvolge il nodo canonico `FrameworkLogParser` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 81 che coinvolge il nodo canonico `FrameworkLogParser` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 81 che coinvolge il nodo canonico `FrameworkLogParser` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 81 che coinvolge il nodo canonico `FrameworkLogParser` è `L503`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 81 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 81 che coinvolge il nodo canonico `FrameworkLogParser` è `ast`. Nel grafo del codice RDG, l'arco 81 registra come `source` l'identificatore `log_collector_normalizeddatasetbuilder_build` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo del codice RDG, l'arco 81 registra come `target` l'identificatore `log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 81 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=82 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FrameworkLogParser` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r`; l'orientamento registrato nel JSON va dall'ID `log_collector_rationale_54` all'ID `log_collector_frameworklogparser`. La relazione tecnica esplicita è la seguente: `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` costituisce una motivazione per `FrameworkLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 82 che coinvolge il nodo canonico `FrameworkLogParser` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 82 che coinvolge il nodo canonico `FrameworkLogParser` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 82 che coinvolge il nodo canonico `FrameworkLogParser` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 82 che coinvolge il nodo canonico `FrameworkLogParser` è `L54`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 82 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 82 che coinvolge il nodo canonico `FrameworkLogParser` è `ast`. Nel grafo del codice RDG, l'arco 82 registra come `source` l'identificatore `log_collector_rationale_54` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo del codice RDG, l'arco 82 registra come `target` l'identificatore `log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 82 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=29 id=Request-Dataset-Generator::log_collector_frameworklogparser -->
Nel grafo unificato, l'etichetta originale del nodo canonico `FrameworkLogParser` è `FrameworkLogParser`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `FrameworkLogParser` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `FrameworkLogParser` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `FrameworkLogParser` è `L53`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `FrameworkLogParser` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `FrameworkLogParser` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `FrameworkLogParser` è `frameworklogparser`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `FrameworkLogParser` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `FrameworkLogParser` è `log_collector_frameworklogparser`. Nel grafo unificato, il nodo canonico `FrameworkLogParser` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_frameworklogparser`.
<!-- GRAPH_EDGE graph=unificata index=75 role=outgoing -->
Nel grafo unificato, il nodo canonico `FrameworkLogParser` è la sorgente `source` della relazione `calls` verso il nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_frameworklogparser` all'ID `Request-Dataset-Generator::log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `FrameworkLogParser` chiama `collect_logs()`. Nel grafo unificato, il tipo dichiarato dell'arco 75 che coinvolge il nodo canonico `FrameworkLogParser` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 75 che coinvolge il nodo canonico `FrameworkLogParser` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 75 che coinvolge il nodo canonico `FrameworkLogParser` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 75 che coinvolge il nodo canonico `FrameworkLogParser` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 75 che coinvolge il nodo canonico `FrameworkLogParser` è `L594`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 75 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 75 che coinvolge il nodo canonico `FrameworkLogParser` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 75 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo unificato, l'arco 75 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo unificato, l'arco 75 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`.
<!-- GRAPH_EDGE graph=unificata index=76 role=outgoing -->
Nel grafo unificato, il nodo canonico `FrameworkLogParser` è la sorgente `source` della relazione `references` verso il nodo canonico `_first_present()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_frameworklogparser` all'ID `Request-Dataset-Generator::log_collector_first_present`. La relazione tecnica esplicita è la seguente: `FrameworkLogParser` fa riferimento a `_first_present()`. Nel grafo unificato, il tipo dichiarato dell'arco 76 che coinvolge il nodo canonico `FrameworkLogParser` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 76 che coinvolge il nodo canonico `FrameworkLogParser` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 76 che coinvolge il nodo canonico `FrameworkLogParser` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 76 che coinvolge il nodo canonico `FrameworkLogParser` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 76 che coinvolge il nodo canonico `FrameworkLogParser` è `L571`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 76 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 76 che coinvolge il nodo canonico `FrameworkLogParser` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 76 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo unificato, l'arco 76 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo unificato, l'arco 76 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_first_present` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`.
<!-- GRAPH_EDGE graph=unificata index=77 role=outgoing -->
Nel grafo unificato, il nodo canonico `FrameworkLogParser` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [log_collector_frameworklogparser_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_frameworklogparser` all'ID `Request-Dataset-Generator::log_collector_frameworklogparser_init`. La relazione tecnica esplicita è la seguente: `FrameworkLogParser` registra come proprio metodo `.__init__() [log_collector_frameworklogparser_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 77 che coinvolge il nodo canonico `FrameworkLogParser` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 77 che coinvolge il nodo canonico `FrameworkLogParser` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 77 che coinvolge il nodo canonico `FrameworkLogParser` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 77 che coinvolge il nodo canonico `FrameworkLogParser` è `L64`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 77 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 77 che coinvolge il nodo canonico `FrameworkLogParser` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 77 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo unificato, l'arco 77 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo unificato, l'arco 77 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser_init` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`.
<!-- GRAPH_EDGE graph=unificata index=78 role=outgoing -->
Nel grafo unificato, il nodo canonico `FrameworkLogParser` è la sorgente `source` della relazione `method` verso il nodo canonico `.parse() [log_collector_frameworklogparser_parse]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_frameworklogparser` all'ID `Request-Dataset-Generator::log_collector_frameworklogparser_parse`. La relazione tecnica esplicita è la seguente: `FrameworkLogParser` registra come proprio metodo `.parse() [log_collector_frameworklogparser_parse]`. Nel grafo unificato, il tipo dichiarato dell'arco 78 che coinvolge il nodo canonico `FrameworkLogParser` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 78 che coinvolge il nodo canonico `FrameworkLogParser` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 78 che coinvolge il nodo canonico `FrameworkLogParser` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 78 che coinvolge il nodo canonico `FrameworkLogParser` è `L73`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 78 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 78 che coinvolge il nodo canonico `FrameworkLogParser` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 78 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo unificato, l'arco 78 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo unificato, l'arco 78 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser_parse` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`.
<!-- GRAPH_EDGE graph=unificata index=79 role=outgoing -->
Nel grafo unificato, il nodo canonico `FrameworkLogParser` è la sorgente `source` della relazione `references` verso il nodo canonico `.build_flows()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_frameworklogparser` all'ID `Request-Dataset-Generator::log_collector_logjoiner_build_flows`. La relazione tecnica esplicita è la seguente: `FrameworkLogParser` fa riferimento a `.build_flows()`. Nel grafo unificato, il tipo dichiarato dell'arco 79 che coinvolge il nodo canonico `FrameworkLogParser` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 79 che coinvolge il nodo canonico `FrameworkLogParser` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 79 che coinvolge il nodo canonico `FrameworkLogParser` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 79 che coinvolge il nodo canonico `FrameworkLogParser` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 79 che coinvolge il nodo canonico `FrameworkLogParser` è `L250`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 79 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 79 che coinvolge il nodo canonico `FrameworkLogParser` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 79 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo unificato, l'arco 79 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo unificato, l'arco 79 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner_build_flows` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`.
<!-- GRAPH_EDGE graph=unificata index=80 role=outgoing -->
Nel grafo unificato, il nodo canonico `FrameworkLogParser` è la sorgente `source` della relazione `references` verso il nodo canonico `.join()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_frameworklogparser` all'ID `Request-Dataset-Generator::log_collector_logjoiner_join`. La relazione tecnica esplicita è la seguente: `FrameworkLogParser` fa riferimento a `.join()`. Nel grafo unificato, il tipo dichiarato dell'arco 80 che coinvolge il nodo canonico `FrameworkLogParser` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 80 che coinvolge il nodo canonico `FrameworkLogParser` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 80 che coinvolge il nodo canonico `FrameworkLogParser` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 80 che coinvolge il nodo canonico `FrameworkLogParser` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 80 che coinvolge il nodo canonico `FrameworkLogParser` è `L168`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 80 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 80 che coinvolge il nodo canonico `FrameworkLogParser` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 80 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo unificato, l'arco 80 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo unificato, l'arco 80 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner_join` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`.
<!-- GRAPH_EDGE graph=unificata index=81 role=outgoing -->
Nel grafo unificato, il nodo canonico `FrameworkLogParser` è la sorgente `source` della relazione `references` verso il nodo canonico `.build()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_frameworklogparser` all'ID `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build`. La relazione tecnica esplicita è la seguente: `FrameworkLogParser` fa riferimento a `.build()`. Nel grafo unificato, il tipo dichiarato dell'arco 81 che coinvolge il nodo canonico `FrameworkLogParser` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 81 che coinvolge il nodo canonico `FrameworkLogParser` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 81 che coinvolge il nodo canonico `FrameworkLogParser` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 81 che coinvolge il nodo canonico `FrameworkLogParser` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 81 che coinvolge il nodo canonico `FrameworkLogParser` è `L503`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 81 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 81 che coinvolge il nodo canonico `FrameworkLogParser` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 81 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo unificato, l'arco 81 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo unificato, l'arco 81 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`.
<!-- GRAPH_EDGE graph=unificata index=82 role=outgoing -->
Nel grafo unificato, il nodo canonico `FrameworkLogParser` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_frameworklogparser` all'ID `Request-Dataset-Generator::log_collector_rationale_54`. La relazione tecnica esplicita è la seguente: `FrameworkLogParser` costituisce una motivazione per `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r`. Nel grafo unificato, il tipo dichiarato dell'arco 82 che coinvolge il nodo canonico `FrameworkLogParser` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 82 che coinvolge il nodo canonico `FrameworkLogParser` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 82 che coinvolge il nodo canonico `FrameworkLogParser` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 82 che coinvolge il nodo canonico `FrameworkLogParser` è `L54`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 82 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 82 che coinvolge il nodo canonico `FrameworkLogParser` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 82 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo unificato, l'arco 82 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo unificato, l'arco 82 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_rationale_54` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`.
<!-- GRAPH_EDGE graph=unificata index=62 role=incoming -->
Nel grafo unificato, il nodo canonico `FrameworkLogParser` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_frameworklogparser`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `FrameworkLogParser`. Nel grafo unificato, il tipo dichiarato dell'arco 62 che coinvolge il nodo canonico `FrameworkLogParser` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 62 che coinvolge il nodo canonico `FrameworkLogParser` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 62 che coinvolge il nodo canonico `FrameworkLogParser` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 62 che coinvolge il nodo canonico `FrameworkLogParser` è `L53`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 62 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 62 che coinvolge il nodo canonico `FrameworkLogParser` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 62 che coinvolge il nodo canonico `FrameworkLogParser` è `1.0`. Nel grafo unificato, l'arco 62 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`. Nel grafo unificato, l'arco 62 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `FrameworkLogParser`.

### Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke

<!-- GRAPH_NODE graph=codice index=52 id=log_collector_rationale_127 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `L127`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` possiede l'identificatore esatto `log_collector_rationale_127`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `legge il log jsonl del middleware in streaming.      indicizza per occurrence_ke`.
<!-- GRAPH_EDGE graph=codice index=89 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `MiddlewareLogParser`; l'orientamento registrato nel JSON va dall'ID `log_collector_rationale_127` all'ID `log_collector_middlewarelogparser`. La relazione tecnica esplicita è la seguente: `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` costituisce una motivazione per `MiddlewareLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 89 che coinvolge il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 89 che coinvolge il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 89 che coinvolge il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 89 che coinvolge il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `L127`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 89 che coinvolge il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 89 che coinvolge il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `ast`. Nel grafo del codice RDG, l'arco 89 registra come `source` l'identificatore `log_collector_rationale_127` per la relazione che coinvolge il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke`. Nel grafo del codice RDG, l'arco 89 registra come `target` l'identificatore `log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 89 che coinvolge il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=52 id=Request-Dataset-Generator::log_collector_rationale_127 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `L127`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `legge il log jsonl del middleware in streaming.      indicizza per occurrence_ke`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `log_collector_rationale_127`. Nel grafo unificato, il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_rationale_127`.
Nel grafo unificato, il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=89 role=incoming -->
Nel grafo unificato, il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `MiddlewareLogParser`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_middlewarelogparser` all'ID `Request-Dataset-Generator::log_collector_rationale_127`. La relazione tecnica esplicita è la seguente: `MiddlewareLogParser` costituisce una motivazione per `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke`. Nel grafo unificato, il tipo dichiarato dell'arco 89 che coinvolge il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 89 che coinvolge il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 89 che coinvolge il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 89 che coinvolge il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `L127`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 89 che coinvolge il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 89 che coinvolge il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 89 che coinvolge il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` è `1.0`. Nel grafo unificato, l'arco 89 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke`. Nel grafo unificato, l'arco 89 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_rationale_127` per la relazione che coinvolge il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke`.

### Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r

<!-- GRAPH_NODE graph=codice index=51 id=log_collector_rationale_54 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `L54`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` possiede l'identificatore esatto `log_collector_rationale_54`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `legge il log jsonl del traffic engine in streaming.      il traffic engine puo r`.
<!-- GRAPH_EDGE graph=codice index=82 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `FrameworkLogParser`; l'orientamento registrato nel JSON va dall'ID `log_collector_rationale_54` all'ID `log_collector_frameworklogparser`. La relazione tecnica esplicita è la seguente: `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` costituisce una motivazione per `FrameworkLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 82 che coinvolge il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 82 che coinvolge il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 82 che coinvolge il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 82 che coinvolge il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `L54`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 82 che coinvolge il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 82 che coinvolge il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `ast`. Nel grafo del codice RDG, l'arco 82 registra come `source` l'identificatore `log_collector_rationale_54` per la relazione che coinvolge il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r`. Nel grafo del codice RDG, l'arco 82 registra come `target` l'identificatore `log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 82 che coinvolge il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=51 id=Request-Dataset-Generator::log_collector_rationale_54 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `L54`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `legge il log jsonl del traffic engine in streaming.      il traffic engine puo r`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `log_collector_rationale_54`. Nel grafo unificato, il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_rationale_54`.
Nel grafo unificato, il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=82 role=incoming -->
Nel grafo unificato, il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `FrameworkLogParser`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_frameworklogparser` all'ID `Request-Dataset-Generator::log_collector_rationale_54`. La relazione tecnica esplicita è la seguente: `FrameworkLogParser` costituisce una motivazione per `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r`. Nel grafo unificato, il tipo dichiarato dell'arco 82 che coinvolge il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 82 che coinvolge il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 82 che coinvolge il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 82 che coinvolge il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `L54`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 82 che coinvolge il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 82 che coinvolge il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 82 che coinvolge il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r` è `1.0`. Nel grafo unificato, l'arco 82 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r`. Nel grafo unificato, l'arco 82 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_rationale_54` per la relazione che coinvolge il nodo canonico `Legge il log JSONL del traffic engine in streaming.      Il traffic engine può r`.

### log_collector.py

<!-- GRAPH_NODE graph=codice index=27 id=log_collector -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `log_collector.py` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `log_collector.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `log_collector.py` vale `ast`. Nel grafo del codice RDG, il nodo canonico `log_collector.py` possiede l'identificatore esatto `log_collector`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `log_collector.py` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `log_collector.py` è `log_collector.py`.
<!-- GRAPH_EDGE graph=codice index=58 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_build_parser() [log_collector_build_parser]`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_build_parser`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_build_parser() [log_collector_build_parser]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 58 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 58 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 58 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 58 che coinvolge il nodo canonico `log_collector.py` è `L663`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 58 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 58 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo del codice RDG, l'arco 58 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, l'arco 58 registra come `target` l'identificatore `log_collector_build_parser` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 58 che coinvolge il nodo canonico `log_collector.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=59 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `collect_logs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 59 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 59 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 59 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 59 che coinvolge il nodo canonico `log_collector.py` è `L584`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 59 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 59 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo del codice RDG, l'arco 59 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, l'arco 59 registra come `target` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 59 che coinvolge il nodo canonico `log_collector.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=60 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_first_present()`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_first_present`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_first_present()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 60 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 60 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 60 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 60 che coinvolge il nodo canonico `log_collector.py` è `L571`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 60 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 60 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo del codice RDG, l'arco 60 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, l'arco 60 registra come `target` l'identificatore `log_collector_first_present` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 60 che coinvolge il nodo canonico `log_collector.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=61 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_framework_event_key()`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_framework_event_key`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_framework_event_key()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 61 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 61 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 61 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 61 che coinvolge il nodo canonico `log_collector.py` è `L113`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 61 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 61 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo del codice RDG, l'arco 61 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, l'arco 61 registra come `target` l'identificatore `log_collector_framework_event_key` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 61 che coinvolge il nodo canonico `log_collector.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=62 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `FrameworkLogParser`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_frameworklogparser`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `FrameworkLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 62 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 62 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 62 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 62 che coinvolge il nodo canonico `log_collector.py` è `L53`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 62 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 62 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo del codice RDG, l'arco 62 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, l'arco 62 registra come `target` l'identificatore `log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 62 che coinvolge il nodo canonico `log_collector.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=63 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_load_config() [log_collector_load_config]`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_load_config`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_load_config() [log_collector_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 63 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 63 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 63 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 63 che coinvolge il nodo canonico `log_collector.py` è `L655`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 63 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 63 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo del codice RDG, l'arco 63 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, l'arco 63 registra come `target` l'identificatore `log_collector_load_config` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 63 che coinvolge il nodo canonico `log_collector.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=64 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `LogJoiner`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_logjoiner`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `LogJoiner`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 64 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 64 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 64 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 64 che coinvolge il nodo canonico `log_collector.py` è `L160`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 64 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 64 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo del codice RDG, l'arco 64 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, l'arco 64 registra come `target` l'identificatore `log_collector_logjoiner` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 64 che coinvolge il nodo canonico `log_collector.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=65 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `main() [log_collector_main]`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_main`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `main() [log_collector_main]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 65 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 65 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 65 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 65 che coinvolge il nodo canonico `log_collector.py` è `L676`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 65 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 65 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo del codice RDG, l'arco 65 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, l'arco 65 registra come `target` l'identificatore `log_collector_main` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 65 che coinvolge il nodo canonico `log_collector.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=66 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `MiddlewareLogParser`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_middlewarelogparser`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `MiddlewareLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 66 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 66 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 66 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 66 che coinvolge il nodo canonico `log_collector.py` è `L126`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 66 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 66 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo del codice RDG, l'arco 66 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, l'arco 66 registra come `target` l'identificatore `log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 66 che coinvolge il nodo canonico `log_collector.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=67 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `NormalizedDatasetBuilder`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_normalizeddatasetbuilder`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `NormalizedDatasetBuilder`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 67 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 67 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 67 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 67 che coinvolge il nodo canonico `log_collector.py` è `L497`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 67 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 67 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo del codice RDG, l'arco 67 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, l'arco 67 registra come `target` l'identificatore `log_collector_normalizeddatasetbuilder` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 67 che coinvolge il nodo canonico `log_collector.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=68 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_percentile()`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_percentile`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_percentile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 68 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 68 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 68 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 68 che coinvolge il nodo canonico `log_collector.py` è `L481`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 68 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 68 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo del codice RDG, l'arco 68 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, l'arco 68 registra come `target` l'identificatore `log_collector_percentile` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 68 che coinvolge il nodo canonico `log_collector.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=69 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_resolve_output_path`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_resolve_output_path() [log_collector_resolve_output_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 69 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 69 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 69 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 69 che coinvolge il nodo canonico `log_collector.py` è `L41`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 69 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 69 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo del codice RDG, l'arco 69 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, l'arco 69 registra come `target` l'identificatore `log_collector_resolve_output_path` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 69 che coinvolge il nodo canonico `log_collector.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=70 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `TimeseriesAggregator`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_timeseriesaggregator`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `TimeseriesAggregator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 70 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 70 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 70 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 70 che coinvolge il nodo canonico `log_collector.py` è `L323`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 70 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 70 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo del codice RDG, l'arco 70 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, l'arco 70 registra come `target` l'identificatore `log_collector_timeseriesaggregator` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 70 che coinvolge il nodo canonico `log_collector.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=71 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `log_collector.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `log_collector`. La relazione tecnica esplicita è la seguente: `main.py` importa da `log_collector.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 71 che coinvolge il nodo canonico `log_collector.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 71 che coinvolge il nodo canonico `log_collector.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 71 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 71 che coinvolge il nodo canonico `log_collector.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 71 che coinvolge il nodo canonico `log_collector.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 71 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 71 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo del codice RDG, l'arco 71 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, l'arco 71 registra come `target` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 71 che coinvolge il nodo canonico `log_collector.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=72 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `log_collector.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `log_collector`. La relazione tecnica esplicita è la seguente: `test_core.py` importa da `log_collector.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 72 che coinvolge il nodo canonico `log_collector.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 72 che coinvolge il nodo canonico `log_collector.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 72 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 72 che coinvolge il nodo canonico `log_collector.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 72 che coinvolge il nodo canonico `log_collector.py` è `L10`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 72 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 72 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo del codice RDG, l'arco 72 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, l'arco 72 registra come `target` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 72 che coinvolge il nodo canonico `log_collector.py` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=27 id=Request-Dataset-Generator::log_collector -->
Nel grafo unificato, l'etichetta originale del nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `log_collector.py` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `log_collector.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `log_collector.py` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `log_collector.py` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `log_collector.py` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `log_collector.py` è `log_collector`. Nel grafo unificato, il nodo canonico `log_collector.py` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector`.
<!-- GRAPH_EDGE graph=unificata index=58 role=outgoing -->
Nel grafo unificato, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_build_parser() [log_collector_build_parser]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_build_parser`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_build_parser() [log_collector_build_parser]`. Nel grafo unificato, il tipo dichiarato dell'arco 58 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 58 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 58 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 58 che coinvolge il nodo canonico `log_collector.py` è `L663`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 58 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 58 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 58 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, l'arco 58 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo unificato, l'arco 58 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_build_parser` per la relazione che coinvolge il nodo canonico `log_collector.py`.
<!-- GRAPH_EDGE graph=unificata index=59 role=outgoing -->
Nel grafo unificato, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `collect_logs()`. Nel grafo unificato, il tipo dichiarato dell'arco 59 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 59 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 59 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 59 che coinvolge il nodo canonico `log_collector.py` è `L584`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 59 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 59 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 59 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, l'arco 59 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo unificato, l'arco 59 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `log_collector.py`.
<!-- GRAPH_EDGE graph=unificata index=60 role=outgoing -->
Nel grafo unificato, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_first_present()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_first_present`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_first_present()`. Nel grafo unificato, il tipo dichiarato dell'arco 60 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 60 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 60 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 60 che coinvolge il nodo canonico `log_collector.py` è `L571`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 60 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 60 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 60 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, l'arco 60 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo unificato, l'arco 60 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_first_present` per la relazione che coinvolge il nodo canonico `log_collector.py`.
<!-- GRAPH_EDGE graph=unificata index=61 role=outgoing -->
Nel grafo unificato, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_framework_event_key()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_framework_event_key`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_framework_event_key()`. Nel grafo unificato, il tipo dichiarato dell'arco 61 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 61 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 61 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 61 che coinvolge il nodo canonico `log_collector.py` è `L113`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 61 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 61 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 61 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, l'arco 61 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo unificato, l'arco 61 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_framework_event_key` per la relazione che coinvolge il nodo canonico `log_collector.py`.
<!-- GRAPH_EDGE graph=unificata index=62 role=outgoing -->
Nel grafo unificato, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `FrameworkLogParser`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_frameworklogparser`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `FrameworkLogParser`. Nel grafo unificato, il tipo dichiarato dell'arco 62 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 62 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 62 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 62 che coinvolge il nodo canonico `log_collector.py` è `L53`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 62 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 62 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 62 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, l'arco 62 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo unificato, l'arco 62 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_frameworklogparser` per la relazione che coinvolge il nodo canonico `log_collector.py`.
<!-- GRAPH_EDGE graph=unificata index=63 role=outgoing -->
Nel grafo unificato, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_load_config() [log_collector_load_config]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_load_config`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_load_config() [log_collector_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 63 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 63 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 63 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 63 che coinvolge il nodo canonico `log_collector.py` è `L655`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 63 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 63 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 63 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, l'arco 63 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo unificato, l'arco 63 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_load_config` per la relazione che coinvolge il nodo canonico `log_collector.py`.
<!-- GRAPH_EDGE graph=unificata index=64 role=outgoing -->
Nel grafo unificato, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `LogJoiner`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_logjoiner`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `LogJoiner`. Nel grafo unificato, il tipo dichiarato dell'arco 64 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 64 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 64 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 64 che coinvolge il nodo canonico `log_collector.py` è `L160`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 64 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 64 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 64 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, l'arco 64 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo unificato, l'arco 64 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner` per la relazione che coinvolge il nodo canonico `log_collector.py`.
<!-- GRAPH_EDGE graph=unificata index=65 role=outgoing -->
Nel grafo unificato, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `main() [log_collector_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_main`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `main() [log_collector_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 65 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 65 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 65 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 65 che coinvolge il nodo canonico `log_collector.py` è `L676`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 65 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 65 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 65 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, l'arco 65 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo unificato, l'arco 65 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_main` per la relazione che coinvolge il nodo canonico `log_collector.py`.
<!-- GRAPH_EDGE graph=unificata index=66 role=outgoing -->
Nel grafo unificato, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `MiddlewareLogParser`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_middlewarelogparser`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `MiddlewareLogParser`. Nel grafo unificato, il tipo dichiarato dell'arco 66 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 66 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 66 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 66 che coinvolge il nodo canonico `log_collector.py` è `L126`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 66 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 66 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 66 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, l'arco 66 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo unificato, l'arco 66 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `log_collector.py`.
<!-- GRAPH_EDGE graph=unificata index=67 role=outgoing -->
Nel grafo unificato, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `NormalizedDatasetBuilder`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `NormalizedDatasetBuilder`. Nel grafo unificato, il tipo dichiarato dell'arco 67 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 67 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 67 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 67 che coinvolge il nodo canonico `log_collector.py` è `L497`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 67 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 67 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 67 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, l'arco 67 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo unificato, l'arco 67 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder` per la relazione che coinvolge il nodo canonico `log_collector.py`.
<!-- GRAPH_EDGE graph=unificata index=68 role=outgoing -->
Nel grafo unificato, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_percentile()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_percentile`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_percentile()`. Nel grafo unificato, il tipo dichiarato dell'arco 68 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 68 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 68 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 68 che coinvolge il nodo canonico `log_collector.py` è `L481`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 68 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 68 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 68 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, l'arco 68 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo unificato, l'arco 68 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_percentile` per la relazione che coinvolge il nodo canonico `log_collector.py`.
<!-- GRAPH_EDGE graph=unificata index=69 role=outgoing -->
Nel grafo unificato, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_resolve_output_path`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `_resolve_output_path() [log_collector_resolve_output_path]`. Nel grafo unificato, il tipo dichiarato dell'arco 69 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 69 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 69 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 69 che coinvolge il nodo canonico `log_collector.py` è `L41`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 69 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 69 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 69 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, l'arco 69 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo unificato, l'arco 69 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_resolve_output_path` per la relazione che coinvolge il nodo canonico `log_collector.py`.
<!-- GRAPH_EDGE graph=unificata index=70 role=outgoing -->
Nel grafo unificato, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `TimeseriesAggregator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_timeseriesaggregator`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `TimeseriesAggregator`. Nel grafo unificato, il tipo dichiarato dell'arco 70 che coinvolge il nodo canonico `log_collector.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 70 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 70 che coinvolge il nodo canonico `log_collector.py` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 70 che coinvolge il nodo canonico `log_collector.py` è `L323`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 70 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 70 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 70 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, l'arco 70 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo unificato, l'arco 70 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_timeseriesaggregator` per la relazione che coinvolge il nodo canonico `log_collector.py`.
<!-- GRAPH_EDGE graph=unificata index=71 role=outgoing -->
Nel grafo unificato, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::main`. La relazione tecnica esplicita è la seguente: `log_collector.py` importa da `main.py`. Nel grafo unificato, il tipo dichiarato dell'arco 71 che coinvolge il nodo canonico `log_collector.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 71 che coinvolge il nodo canonico `log_collector.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 71 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 71 che coinvolge il nodo canonico `log_collector.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 71 che coinvolge il nodo canonico `log_collector.py` è `L21`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 71 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 71 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 71 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, l'arco 71 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo unificato, l'arco 71 registra come `target` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `log_collector.py`.
<!-- GRAPH_EDGE graph=unificata index=72 role=outgoing -->
Nel grafo unificato, il nodo canonico `log_collector.py` è la sorgente `source` della relazione `imports_from` verso il nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::tests_test_core`. La relazione tecnica esplicita è la seguente: `log_collector.py` importa da `test_core.py`. Nel grafo unificato, il tipo dichiarato dell'arco 72 che coinvolge il nodo canonico `log_collector.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 72 che coinvolge il nodo canonico `log_collector.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 72 che coinvolge il nodo canonico `log_collector.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 72 che coinvolge il nodo canonico `log_collector.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 72 che coinvolge il nodo canonico `log_collector.py` è `L10`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 72 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 72 che coinvolge il nodo canonico `log_collector.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 72 che coinvolge il nodo canonico `log_collector.py` è `1.0`. Nel grafo unificato, l'arco 72 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `log_collector.py`. Nel grafo unificato, l'arco 72 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `log_collector.py`.
Nel grafo unificato, il nodo canonico `log_collector.py` non possiede archi entranti registrati nel campo `links`.

### LogJoiner

<!-- GRAPH_NODE graph=codice index=36 id=log_collector_logjoiner -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `LogJoiner` è `LogJoiner`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `LogJoiner` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `LogJoiner` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `LogJoiner` è `L160`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `LogJoiner` vale `ast`. Nel grafo del codice RDG, il nodo canonico `LogJoiner` possiede l'identificatore esatto `log_collector_logjoiner`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `LogJoiner` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `LogJoiner` è `logjoiner`.
<!-- GRAPH_EDGE graph=codice index=91 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `LogJoiner` è la sorgente `source` della relazione `method` verso il nodo canonico `.build_flows()`; l'orientamento registrato nel JSON va dall'ID `log_collector_logjoiner` all'ID `log_collector_logjoiner_build_flows`. La relazione tecnica esplicita è la seguente: `LogJoiner` registra come proprio metodo `.build_flows()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 91 che coinvolge il nodo canonico `LogJoiner` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 91 che coinvolge il nodo canonico `LogJoiner` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 91 che coinvolge il nodo canonico `LogJoiner` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 91 che coinvolge il nodo canonico `LogJoiner` è `L250`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 91 che coinvolge il nodo canonico `LogJoiner` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 91 che coinvolge il nodo canonico `LogJoiner` è `ast`. Nel grafo del codice RDG, l'arco 91 registra come `source` l'identificatore `log_collector_logjoiner` per la relazione che coinvolge il nodo canonico `LogJoiner`. Nel grafo del codice RDG, l'arco 91 registra come `target` l'identificatore `log_collector_logjoiner_build_flows` per la relazione che coinvolge il nodo canonico `LogJoiner`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 91 che coinvolge il nodo canonico `LogJoiner` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=92 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `LogJoiner` è la sorgente `source` della relazione `method` verso il nodo canonico `.join()`; l'orientamento registrato nel JSON va dall'ID `log_collector_logjoiner` all'ID `log_collector_logjoiner_join`. La relazione tecnica esplicita è la seguente: `LogJoiner` registra come proprio metodo `.join()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 92 che coinvolge il nodo canonico `LogJoiner` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 92 che coinvolge il nodo canonico `LogJoiner` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 92 che coinvolge il nodo canonico `LogJoiner` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 92 che coinvolge il nodo canonico `LogJoiner` è `L168`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 92 che coinvolge il nodo canonico `LogJoiner` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 92 che coinvolge il nodo canonico `LogJoiner` è `ast`. Nel grafo del codice RDG, l'arco 92 registra come `source` l'identificatore `log_collector_logjoiner` per la relazione che coinvolge il nodo canonico `LogJoiner`. Nel grafo del codice RDG, l'arco 92 registra come `target` l'identificatore `log_collector_logjoiner_join` per la relazione che coinvolge il nodo canonico `LogJoiner`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 92 che coinvolge il nodo canonico `LogJoiner` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=64 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `LogJoiner` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_logjoiner`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `LogJoiner`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 64 che coinvolge il nodo canonico `LogJoiner` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 64 che coinvolge il nodo canonico `LogJoiner` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 64 che coinvolge il nodo canonico `LogJoiner` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 64 che coinvolge il nodo canonico `LogJoiner` è `L160`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 64 che coinvolge il nodo canonico `LogJoiner` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 64 che coinvolge il nodo canonico `LogJoiner` è `ast`. Nel grafo del codice RDG, l'arco 64 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `LogJoiner`. Nel grafo del codice RDG, l'arco 64 registra come `target` l'identificatore `log_collector_logjoiner` per la relazione che coinvolge il nodo canonico `LogJoiner`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 64 che coinvolge il nodo canonico `LogJoiner` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=93 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `LogJoiner` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis`; l'orientamento registrato nel JSON va dall'ID `log_collector_rationale_161` all'ID `log_collector_logjoiner`. La relazione tecnica esplicita è la seguente: `Fa il join tra framework log e middleware log su request_id.      Produce la lis` costituisce una motivazione per `LogJoiner`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 93 che coinvolge il nodo canonico `LogJoiner` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 93 che coinvolge il nodo canonico `LogJoiner` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 93 che coinvolge il nodo canonico `LogJoiner` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 93 che coinvolge il nodo canonico `LogJoiner` è `L161`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 93 che coinvolge il nodo canonico `LogJoiner` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 93 che coinvolge il nodo canonico `LogJoiner` è `ast`. Nel grafo del codice RDG, l'arco 93 registra come `source` l'identificatore `log_collector_rationale_161` per la relazione che coinvolge il nodo canonico `LogJoiner`. Nel grafo del codice RDG, l'arco 93 registra come `target` l'identificatore `log_collector_logjoiner` per la relazione che coinvolge il nodo canonico `LogJoiner`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 93 che coinvolge il nodo canonico `LogJoiner` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=36 id=Request-Dataset-Generator::log_collector_logjoiner -->
Nel grafo unificato, l'etichetta originale del nodo canonico `LogJoiner` è `LogJoiner`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `LogJoiner` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `LogJoiner` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `LogJoiner` è `L160`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `LogJoiner` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `LogJoiner` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `LogJoiner` è `logjoiner`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `LogJoiner` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `LogJoiner` è `log_collector_logjoiner`. Nel grafo unificato, il nodo canonico `LogJoiner` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_logjoiner`.
<!-- GRAPH_EDGE graph=unificata index=91 role=outgoing -->
Nel grafo unificato, il nodo canonico `LogJoiner` è la sorgente `source` della relazione `method` verso il nodo canonico `.build_flows()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_logjoiner` all'ID `Request-Dataset-Generator::log_collector_logjoiner_build_flows`. La relazione tecnica esplicita è la seguente: `LogJoiner` registra come proprio metodo `.build_flows()`. Nel grafo unificato, il tipo dichiarato dell'arco 91 che coinvolge il nodo canonico `LogJoiner` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 91 che coinvolge il nodo canonico `LogJoiner` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 91 che coinvolge il nodo canonico `LogJoiner` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 91 che coinvolge il nodo canonico `LogJoiner` è `L250`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 91 che coinvolge il nodo canonico `LogJoiner` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 91 che coinvolge il nodo canonico `LogJoiner` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 91 che coinvolge il nodo canonico `LogJoiner` è `1.0`. Nel grafo unificato, l'arco 91 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner` per la relazione che coinvolge il nodo canonico `LogJoiner`. Nel grafo unificato, l'arco 91 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner_build_flows` per la relazione che coinvolge il nodo canonico `LogJoiner`.
<!-- GRAPH_EDGE graph=unificata index=92 role=outgoing -->
Nel grafo unificato, il nodo canonico `LogJoiner` è la sorgente `source` della relazione `method` verso il nodo canonico `.join()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_logjoiner` all'ID `Request-Dataset-Generator::log_collector_logjoiner_join`. La relazione tecnica esplicita è la seguente: `LogJoiner` registra come proprio metodo `.join()`. Nel grafo unificato, il tipo dichiarato dell'arco 92 che coinvolge il nodo canonico `LogJoiner` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 92 che coinvolge il nodo canonico `LogJoiner` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 92 che coinvolge il nodo canonico `LogJoiner` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 92 che coinvolge il nodo canonico `LogJoiner` è `L168`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 92 che coinvolge il nodo canonico `LogJoiner` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 92 che coinvolge il nodo canonico `LogJoiner` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 92 che coinvolge il nodo canonico `LogJoiner` è `1.0`. Nel grafo unificato, l'arco 92 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner` per la relazione che coinvolge il nodo canonico `LogJoiner`. Nel grafo unificato, l'arco 92 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner_join` per la relazione che coinvolge il nodo canonico `LogJoiner`.
<!-- GRAPH_EDGE graph=unificata index=93 role=outgoing -->
Nel grafo unificato, il nodo canonico `LogJoiner` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Fa il join tra framework log e middleware log su request_id.      Produce la lis`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_logjoiner` all'ID `Request-Dataset-Generator::log_collector_rationale_161`. La relazione tecnica esplicita è la seguente: `LogJoiner` costituisce una motivazione per `Fa il join tra framework log e middleware log su request_id.      Produce la lis`. Nel grafo unificato, il tipo dichiarato dell'arco 93 che coinvolge il nodo canonico `LogJoiner` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 93 che coinvolge il nodo canonico `LogJoiner` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 93 che coinvolge il nodo canonico `LogJoiner` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 93 che coinvolge il nodo canonico `LogJoiner` è `L161`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 93 che coinvolge il nodo canonico `LogJoiner` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 93 che coinvolge il nodo canonico `LogJoiner` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 93 che coinvolge il nodo canonico `LogJoiner` è `1.0`. Nel grafo unificato, l'arco 93 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner` per la relazione che coinvolge il nodo canonico `LogJoiner`. Nel grafo unificato, l'arco 93 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_rationale_161` per la relazione che coinvolge il nodo canonico `LogJoiner`.
<!-- GRAPH_EDGE graph=unificata index=64 role=incoming -->
Nel grafo unificato, il nodo canonico `LogJoiner` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_logjoiner`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `LogJoiner`. Nel grafo unificato, il tipo dichiarato dell'arco 64 che coinvolge il nodo canonico `LogJoiner` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 64 che coinvolge il nodo canonico `LogJoiner` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 64 che coinvolge il nodo canonico `LogJoiner` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 64 che coinvolge il nodo canonico `LogJoiner` è `L160`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 64 che coinvolge il nodo canonico `LogJoiner` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 64 che coinvolge il nodo canonico `LogJoiner` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 64 che coinvolge il nodo canonico `LogJoiner` è `1.0`. Nel grafo unificato, l'arco 64 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `LogJoiner`. Nel grafo unificato, l'arco 64 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner` per la relazione che coinvolge il nodo canonico `LogJoiner`.

### main() [log_collector_main]

<!-- GRAPH_NODE graph=codice index=50 id=log_collector_main -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `main() [log_collector_main]` è `main()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `main() [log_collector_main]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `main() [log_collector_main]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `main() [log_collector_main]` è `L676`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `main() [log_collector_main]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `main() [log_collector_main]` possiede l'identificatore esatto `log_collector_main`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `main() [log_collector_main]` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `main() [log_collector_main]` è `main()`.
<!-- GRAPH_EDGE graph=codice index=73 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main() [log_collector_main]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]`; l'orientamento registrato nel JSON va dall'ID `log_collector_main` all'ID `log_collector_resolve_output_path`. La relazione tecnica esplicita è la seguente: `main() [log_collector_main]` chiama `_resolve_output_path() [log_collector_resolve_output_path]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 73 che coinvolge il nodo canonico `main() [log_collector_main]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 73 che coinvolge il nodo canonico `main() [log_collector_main]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 73 che coinvolge il nodo canonico `main() [log_collector_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 73 che coinvolge il nodo canonico `main() [log_collector_main]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 73 che coinvolge il nodo canonico `main() [log_collector_main]` è `L688`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 73 che coinvolge il nodo canonico `main() [log_collector_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 73 che coinvolge il nodo canonico `main() [log_collector_main]` è `ast`. Nel grafo del codice RDG, l'arco 73 registra come `source` l'identificatore `log_collector_main` per la relazione che coinvolge il nodo canonico `main() [log_collector_main]`. Nel grafo del codice RDG, l'arco 73 registra come `target` l'identificatore `log_collector_resolve_output_path` per la relazione che coinvolge il nodo canonico `main() [log_collector_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 73 che coinvolge il nodo canonico `main() [log_collector_main]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=107 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main() [log_collector_main]` è la sorgente `source` della relazione `calls` verso il nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `log_collector_main` all'ID `log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `main() [log_collector_main]` chiama `collect_logs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 107 che coinvolge il nodo canonico `main() [log_collector_main]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 107 che coinvolge il nodo canonico `main() [log_collector_main]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 107 che coinvolge il nodo canonico `main() [log_collector_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 107 che coinvolge il nodo canonico `main() [log_collector_main]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 107 che coinvolge il nodo canonico `main() [log_collector_main]` è `L687`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 107 che coinvolge il nodo canonico `main() [log_collector_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 107 che coinvolge il nodo canonico `main() [log_collector_main]` è `ast`. Nel grafo del codice RDG, l'arco 107 registra come `source` l'identificatore `log_collector_main` per la relazione che coinvolge il nodo canonico `main() [log_collector_main]`. Nel grafo del codice RDG, l'arco 107 registra come `target` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `main() [log_collector_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 107 che coinvolge il nodo canonico `main() [log_collector_main]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=116 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main() [log_collector_main]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_load_config() [log_collector_load_config]`; l'orientamento registrato nel JSON va dall'ID `log_collector_main` all'ID `log_collector_load_config`. La relazione tecnica esplicita è la seguente: `main() [log_collector_main]` chiama `_load_config() [log_collector_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 116 che coinvolge il nodo canonico `main() [log_collector_main]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 116 che coinvolge il nodo canonico `main() [log_collector_main]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 116 che coinvolge il nodo canonico `main() [log_collector_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 116 che coinvolge il nodo canonico `main() [log_collector_main]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 116 che coinvolge il nodo canonico `main() [log_collector_main]` è `L678`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 116 che coinvolge il nodo canonico `main() [log_collector_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 116 che coinvolge il nodo canonico `main() [log_collector_main]` è `ast`. Nel grafo del codice RDG, l'arco 116 registra come `source` l'identificatore `log_collector_main` per la relazione che coinvolge il nodo canonico `main() [log_collector_main]`. Nel grafo del codice RDG, l'arco 116 registra come `target` l'identificatore `log_collector_load_config` per la relazione che coinvolge il nodo canonico `main() [log_collector_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 116 che coinvolge il nodo canonico `main() [log_collector_main]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=118 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main() [log_collector_main]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_build_parser() [log_collector_build_parser]`; l'orientamento registrato nel JSON va dall'ID `log_collector_main` all'ID `log_collector_build_parser`. La relazione tecnica esplicita è la seguente: `main() [log_collector_main]` chiama `_build_parser() [log_collector_build_parser]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 118 che coinvolge il nodo canonico `main() [log_collector_main]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 118 che coinvolge il nodo canonico `main() [log_collector_main]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 118 che coinvolge il nodo canonico `main() [log_collector_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 118 che coinvolge il nodo canonico `main() [log_collector_main]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 118 che coinvolge il nodo canonico `main() [log_collector_main]` è `L677`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 118 che coinvolge il nodo canonico `main() [log_collector_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 118 che coinvolge il nodo canonico `main() [log_collector_main]` è `ast`. Nel grafo del codice RDG, l'arco 118 registra come `source` l'identificatore `log_collector_main` per la relazione che coinvolge il nodo canonico `main() [log_collector_main]`. Nel grafo del codice RDG, l'arco 118 registra come `target` l'identificatore `log_collector_build_parser` per la relazione che coinvolge il nodo canonico `main() [log_collector_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 118 che coinvolge il nodo canonico `main() [log_collector_main]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=65 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `main() [log_collector_main]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_main`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `main() [log_collector_main]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 65 che coinvolge il nodo canonico `main() [log_collector_main]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 65 che coinvolge il nodo canonico `main() [log_collector_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 65 che coinvolge il nodo canonico `main() [log_collector_main]` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 65 che coinvolge il nodo canonico `main() [log_collector_main]` è `L676`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 65 che coinvolge il nodo canonico `main() [log_collector_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 65 che coinvolge il nodo canonico `main() [log_collector_main]` è `ast`. Nel grafo del codice RDG, l'arco 65 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `main() [log_collector_main]`. Nel grafo del codice RDG, l'arco 65 registra come `target` l'identificatore `log_collector_main` per la relazione che coinvolge il nodo canonico `main() [log_collector_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 65 che coinvolge il nodo canonico `main() [log_collector_main]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=50 id=Request-Dataset-Generator::log_collector_main -->
Nel grafo unificato, l'etichetta originale del nodo canonico `main() [log_collector_main]` è `main()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `main() [log_collector_main]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `main() [log_collector_main]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `main() [log_collector_main]` è `L676`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `main() [log_collector_main]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `main() [log_collector_main]` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `main() [log_collector_main]` è `main()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `main() [log_collector_main]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `main() [log_collector_main]` è `log_collector_main`. Nel grafo unificato, il nodo canonico `main() [log_collector_main]` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_main`.
Nel grafo unificato, il nodo canonico `main() [log_collector_main]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=65 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [log_collector_main]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_main`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `main() [log_collector_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 65 che coinvolge il nodo canonico `main() [log_collector_main]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 65 che coinvolge il nodo canonico `main() [log_collector_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 65 che coinvolge il nodo canonico `main() [log_collector_main]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 65 che coinvolge il nodo canonico `main() [log_collector_main]` è `L676`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 65 che coinvolge il nodo canonico `main() [log_collector_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 65 che coinvolge il nodo canonico `main() [log_collector_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 65 che coinvolge il nodo canonico `main() [log_collector_main]` è `1.0`. Nel grafo unificato, l'arco 65 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `main() [log_collector_main]`. Nel grafo unificato, l'arco 65 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_main` per la relazione che coinvolge il nodo canonico `main() [log_collector_main]`.
<!-- GRAPH_EDGE graph=unificata index=73 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [log_collector_main]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_resolve_output_path() [log_collector_resolve_output_path]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_resolve_output_path` all'ID `Request-Dataset-Generator::log_collector_main`. La relazione tecnica esplicita è la seguente: `_resolve_output_path() [log_collector_resolve_output_path]` chiama `main() [log_collector_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 73 che coinvolge il nodo canonico `main() [log_collector_main]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 73 che coinvolge il nodo canonico `main() [log_collector_main]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 73 che coinvolge il nodo canonico `main() [log_collector_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 73 che coinvolge il nodo canonico `main() [log_collector_main]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 73 che coinvolge il nodo canonico `main() [log_collector_main]` è `L688`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 73 che coinvolge il nodo canonico `main() [log_collector_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 73 che coinvolge il nodo canonico `main() [log_collector_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 73 che coinvolge il nodo canonico `main() [log_collector_main]` è `1.0`. Nel grafo unificato, l'arco 73 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_resolve_output_path` per la relazione che coinvolge il nodo canonico `main() [log_collector_main]`. Nel grafo unificato, l'arco 73 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_main` per la relazione che coinvolge il nodo canonico `main() [log_collector_main]`.
<!-- GRAPH_EDGE graph=unificata index=107 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [log_collector_main]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_collect_logs` all'ID `Request-Dataset-Generator::log_collector_main`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `main() [log_collector_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 107 che coinvolge il nodo canonico `main() [log_collector_main]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 107 che coinvolge il nodo canonico `main() [log_collector_main]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 107 che coinvolge il nodo canonico `main() [log_collector_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 107 che coinvolge il nodo canonico `main() [log_collector_main]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 107 che coinvolge il nodo canonico `main() [log_collector_main]` è `L687`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 107 che coinvolge il nodo canonico `main() [log_collector_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 107 che coinvolge il nodo canonico `main() [log_collector_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 107 che coinvolge il nodo canonico `main() [log_collector_main]` è `1.0`. Nel grafo unificato, l'arco 107 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `main() [log_collector_main]`. Nel grafo unificato, l'arco 107 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_main` per la relazione che coinvolge il nodo canonico `main() [log_collector_main]`.
<!-- GRAPH_EDGE graph=unificata index=116 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [log_collector_main]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_load_config() [log_collector_load_config]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_load_config` all'ID `Request-Dataset-Generator::log_collector_main`. La relazione tecnica esplicita è la seguente: `_load_config() [log_collector_load_config]` chiama `main() [log_collector_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 116 che coinvolge il nodo canonico `main() [log_collector_main]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 116 che coinvolge il nodo canonico `main() [log_collector_main]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 116 che coinvolge il nodo canonico `main() [log_collector_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 116 che coinvolge il nodo canonico `main() [log_collector_main]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 116 che coinvolge il nodo canonico `main() [log_collector_main]` è `L678`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 116 che coinvolge il nodo canonico `main() [log_collector_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 116 che coinvolge il nodo canonico `main() [log_collector_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 116 che coinvolge il nodo canonico `main() [log_collector_main]` è `1.0`. Nel grafo unificato, l'arco 116 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_load_config` per la relazione che coinvolge il nodo canonico `main() [log_collector_main]`. Nel grafo unificato, l'arco 116 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_main` per la relazione che coinvolge il nodo canonico `main() [log_collector_main]`.
<!-- GRAPH_EDGE graph=unificata index=118 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [log_collector_main]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_build_parser() [log_collector_build_parser]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_build_parser` all'ID `Request-Dataset-Generator::log_collector_main`. La relazione tecnica esplicita è la seguente: `_build_parser() [log_collector_build_parser]` chiama `main() [log_collector_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 118 che coinvolge il nodo canonico `main() [log_collector_main]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 118 che coinvolge il nodo canonico `main() [log_collector_main]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 118 che coinvolge il nodo canonico `main() [log_collector_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 118 che coinvolge il nodo canonico `main() [log_collector_main]` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 118 che coinvolge il nodo canonico `main() [log_collector_main]` è `L677`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 118 che coinvolge il nodo canonico `main() [log_collector_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 118 che coinvolge il nodo canonico `main() [log_collector_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 118 che coinvolge il nodo canonico `main() [log_collector_main]` è `1.0`. Nel grafo unificato, l'arco 118 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_build_parser` per la relazione che coinvolge il nodo canonico `main() [log_collector_main]`. Nel grafo unificato, l'arco 118 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_main` per la relazione che coinvolge il nodo canonico `main() [log_collector_main]`.

### MiddlewareLogParser

<!-- GRAPH_NODE graph=codice index=33 id=log_collector_middlewarelogparser -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `MiddlewareLogParser` è `MiddlewareLogParser`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `MiddlewareLogParser` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `MiddlewareLogParser` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `MiddlewareLogParser` è `L126`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `MiddlewareLogParser` vale `ast`. Nel grafo del codice RDG, il nodo canonico `MiddlewareLogParser` possiede l'identificatore esatto `log_collector_middlewarelogparser`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `MiddlewareLogParser` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `MiddlewareLogParser` è `middlewarelogparser`.
<!-- GRAPH_EDGE graph=codice index=86 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `MiddlewareLogParser` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]`; l'orientamento registrato nel JSON va dall'ID `log_collector_middlewarelogparser` all'ID `log_collector_middlewarelogparser_init`. La relazione tecnica esplicita è la seguente: `MiddlewareLogParser` registra come proprio metodo `.__init__() [log_collector_middlewarelogparser_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 86 che coinvolge il nodo canonico `MiddlewareLogParser` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 86 che coinvolge il nodo canonico `MiddlewareLogParser` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 86 che coinvolge il nodo canonico `MiddlewareLogParser` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 86 che coinvolge il nodo canonico `MiddlewareLogParser` è `L133`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 86 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 86 che coinvolge il nodo canonico `MiddlewareLogParser` è `ast`. Nel grafo del codice RDG, l'arco 86 registra come `source` l'identificatore `log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo del codice RDG, l'arco 86 registra come `target` l'identificatore `log_collector_middlewarelogparser_init` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 86 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=87 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `MiddlewareLogParser` è la sorgente `source` della relazione `method` verso il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]`; l'orientamento registrato nel JSON va dall'ID `log_collector_middlewarelogparser` all'ID `log_collector_middlewarelogparser_parse`. La relazione tecnica esplicita è la seguente: `MiddlewareLogParser` registra come proprio metodo `.parse() [log_collector_middlewarelogparser_parse]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 87 che coinvolge il nodo canonico `MiddlewareLogParser` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 87 che coinvolge il nodo canonico `MiddlewareLogParser` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 87 che coinvolge il nodo canonico `MiddlewareLogParser` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 87 che coinvolge il nodo canonico `MiddlewareLogParser` è `L137`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 87 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 87 che coinvolge il nodo canonico `MiddlewareLogParser` è `ast`. Nel grafo del codice RDG, l'arco 87 registra come `source` l'identificatore `log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo del codice RDG, l'arco 87 registra come `target` l'identificatore `log_collector_middlewarelogparser_parse` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 87 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=66 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `MiddlewareLogParser` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_middlewarelogparser`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `MiddlewareLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 66 che coinvolge il nodo canonico `MiddlewareLogParser` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 66 che coinvolge il nodo canonico `MiddlewareLogParser` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 66 che coinvolge il nodo canonico `MiddlewareLogParser` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 66 che coinvolge il nodo canonico `MiddlewareLogParser` è `L126`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 66 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 66 che coinvolge il nodo canonico `MiddlewareLogParser` è `ast`. Nel grafo del codice RDG, l'arco 66 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo del codice RDG, l'arco 66 registra come `target` l'identificatore `log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 66 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=84 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `MiddlewareLogParser` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `log_collector_collect_logs` all'ID `log_collector_middlewarelogparser`. La relazione tecnica esplicita è la seguente: `collect_logs()` chiama `MiddlewareLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 84 che coinvolge il nodo canonico `MiddlewareLogParser` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 84 che coinvolge il nodo canonico `MiddlewareLogParser` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 84 che coinvolge il nodo canonico `MiddlewareLogParser` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 84 che coinvolge il nodo canonico `MiddlewareLogParser` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 84 che coinvolge il nodo canonico `MiddlewareLogParser` è `L608`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 84 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 84 che coinvolge il nodo canonico `MiddlewareLogParser` è `ast`. Nel grafo del codice RDG, l'arco 84 registra come `source` l'identificatore `log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo del codice RDG, l'arco 84 registra come `target` l'identificatore `log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 84 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=85 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `MiddlewareLogParser` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `.join()`; l'orientamento registrato nel JSON va dall'ID `log_collector_logjoiner_join` all'ID `log_collector_middlewarelogparser`. La relazione tecnica esplicita è la seguente: `.join()` fa riferimento a `MiddlewareLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 85 che coinvolge il nodo canonico `MiddlewareLogParser` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 85 che coinvolge il nodo canonico `MiddlewareLogParser` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 85 che coinvolge il nodo canonico `MiddlewareLogParser` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 85 che coinvolge il nodo canonico `MiddlewareLogParser` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 85 che coinvolge il nodo canonico `MiddlewareLogParser` è `L168`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 85 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 85 che coinvolge il nodo canonico `MiddlewareLogParser` è `ast`. Nel grafo del codice RDG, l'arco 85 registra come `source` l'identificatore `log_collector_logjoiner_join` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo del codice RDG, l'arco 85 registra come `target` l'identificatore `log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 85 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=88 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `MiddlewareLogParser` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `.build()`; l'orientamento registrato nel JSON va dall'ID `log_collector_normalizeddatasetbuilder_build` all'ID `log_collector_middlewarelogparser`. La relazione tecnica esplicita è la seguente: `.build()` fa riferimento a `MiddlewareLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 88 che coinvolge il nodo canonico `MiddlewareLogParser` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 88 che coinvolge il nodo canonico `MiddlewareLogParser` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 88 che coinvolge il nodo canonico `MiddlewareLogParser` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 88 che coinvolge il nodo canonico `MiddlewareLogParser` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 88 che coinvolge il nodo canonico `MiddlewareLogParser` è `L503`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 88 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 88 che coinvolge il nodo canonico `MiddlewareLogParser` è `ast`. Nel grafo del codice RDG, l'arco 88 registra come `source` l'identificatore `log_collector_normalizeddatasetbuilder_build` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo del codice RDG, l'arco 88 registra come `target` l'identificatore `log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 88 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=89 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `MiddlewareLogParser` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke`; l'orientamento registrato nel JSON va dall'ID `log_collector_rationale_127` all'ID `log_collector_middlewarelogparser`. La relazione tecnica esplicita è la seguente: `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke` costituisce una motivazione per `MiddlewareLogParser`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 89 che coinvolge il nodo canonico `MiddlewareLogParser` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 89 che coinvolge il nodo canonico `MiddlewareLogParser` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 89 che coinvolge il nodo canonico `MiddlewareLogParser` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 89 che coinvolge il nodo canonico `MiddlewareLogParser` è `L127`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 89 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 89 che coinvolge il nodo canonico `MiddlewareLogParser` è `ast`. Nel grafo del codice RDG, l'arco 89 registra come `source` l'identificatore `log_collector_rationale_127` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo del codice RDG, l'arco 89 registra come `target` l'identificatore `log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 89 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=33 id=Request-Dataset-Generator::log_collector_middlewarelogparser -->
Nel grafo unificato, l'etichetta originale del nodo canonico `MiddlewareLogParser` è `MiddlewareLogParser`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `MiddlewareLogParser` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `MiddlewareLogParser` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `MiddlewareLogParser` è `L126`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `MiddlewareLogParser` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `MiddlewareLogParser` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `MiddlewareLogParser` è `middlewarelogparser`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `MiddlewareLogParser` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `MiddlewareLogParser` è `log_collector_middlewarelogparser`. Nel grafo unificato, il nodo canonico `MiddlewareLogParser` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_middlewarelogparser`.
<!-- GRAPH_EDGE graph=unificata index=84 role=outgoing -->
Nel grafo unificato, il nodo canonico `MiddlewareLogParser` è la sorgente `source` della relazione `calls` verso il nodo canonico `collect_logs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_middlewarelogparser` all'ID `Request-Dataset-Generator::log_collector_collect_logs`. La relazione tecnica esplicita è la seguente: `MiddlewareLogParser` chiama `collect_logs()`. Nel grafo unificato, il tipo dichiarato dell'arco 84 che coinvolge il nodo canonico `MiddlewareLogParser` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 84 che coinvolge il nodo canonico `MiddlewareLogParser` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 84 che coinvolge il nodo canonico `MiddlewareLogParser` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 84 che coinvolge il nodo canonico `MiddlewareLogParser` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 84 che coinvolge il nodo canonico `MiddlewareLogParser` è `L608`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 84 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 84 che coinvolge il nodo canonico `MiddlewareLogParser` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 84 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo unificato, l'arco 84 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo unificato, l'arco 84 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_collect_logs` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`.
<!-- GRAPH_EDGE graph=unificata index=85 role=outgoing -->
Nel grafo unificato, il nodo canonico `MiddlewareLogParser` è la sorgente `source` della relazione `references` verso il nodo canonico `.join()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_middlewarelogparser` all'ID `Request-Dataset-Generator::log_collector_logjoiner_join`. La relazione tecnica esplicita è la seguente: `MiddlewareLogParser` fa riferimento a `.join()`. Nel grafo unificato, il tipo dichiarato dell'arco 85 che coinvolge il nodo canonico `MiddlewareLogParser` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 85 che coinvolge il nodo canonico `MiddlewareLogParser` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 85 che coinvolge il nodo canonico `MiddlewareLogParser` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 85 che coinvolge il nodo canonico `MiddlewareLogParser` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 85 che coinvolge il nodo canonico `MiddlewareLogParser` è `L168`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 85 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 85 che coinvolge il nodo canonico `MiddlewareLogParser` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 85 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo unificato, l'arco 85 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo unificato, l'arco 85 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_logjoiner_join` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`.
<!-- GRAPH_EDGE graph=unificata index=86 role=outgoing -->
Nel grafo unificato, il nodo canonico `MiddlewareLogParser` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [log_collector_middlewarelogparser_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_middlewarelogparser` all'ID `Request-Dataset-Generator::log_collector_middlewarelogparser_init`. La relazione tecnica esplicita è la seguente: `MiddlewareLogParser` registra come proprio metodo `.__init__() [log_collector_middlewarelogparser_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 86 che coinvolge il nodo canonico `MiddlewareLogParser` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 86 che coinvolge il nodo canonico `MiddlewareLogParser` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 86 che coinvolge il nodo canonico `MiddlewareLogParser` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 86 che coinvolge il nodo canonico `MiddlewareLogParser` è `L133`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 86 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 86 che coinvolge il nodo canonico `MiddlewareLogParser` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 86 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo unificato, l'arco 86 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo unificato, l'arco 86 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_middlewarelogparser_init` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`.
<!-- GRAPH_EDGE graph=unificata index=87 role=outgoing -->
Nel grafo unificato, il nodo canonico `MiddlewareLogParser` è la sorgente `source` della relazione `method` verso il nodo canonico `.parse() [log_collector_middlewarelogparser_parse]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_middlewarelogparser` all'ID `Request-Dataset-Generator::log_collector_middlewarelogparser_parse`. La relazione tecnica esplicita è la seguente: `MiddlewareLogParser` registra come proprio metodo `.parse() [log_collector_middlewarelogparser_parse]`. Nel grafo unificato, il tipo dichiarato dell'arco 87 che coinvolge il nodo canonico `MiddlewareLogParser` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 87 che coinvolge il nodo canonico `MiddlewareLogParser` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 87 che coinvolge il nodo canonico `MiddlewareLogParser` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 87 che coinvolge il nodo canonico `MiddlewareLogParser` è `L137`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 87 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 87 che coinvolge il nodo canonico `MiddlewareLogParser` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 87 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo unificato, l'arco 87 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo unificato, l'arco 87 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_middlewarelogparser_parse` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`.
<!-- GRAPH_EDGE graph=unificata index=88 role=outgoing -->
Nel grafo unificato, il nodo canonico `MiddlewareLogParser` è la sorgente `source` della relazione `references` verso il nodo canonico `.build()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_middlewarelogparser` all'ID `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build`. La relazione tecnica esplicita è la seguente: `MiddlewareLogParser` fa riferimento a `.build()`. Nel grafo unificato, il tipo dichiarato dell'arco 88 che coinvolge il nodo canonico `MiddlewareLogParser` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 88 che coinvolge il nodo canonico `MiddlewareLogParser` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 88 che coinvolge il nodo canonico `MiddlewareLogParser` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 88 che coinvolge il nodo canonico `MiddlewareLogParser` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 88 che coinvolge il nodo canonico `MiddlewareLogParser` è `L503`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 88 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 88 che coinvolge il nodo canonico `MiddlewareLogParser` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 88 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo unificato, l'arco 88 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo unificato, l'arco 88 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`.
<!-- GRAPH_EDGE graph=unificata index=89 role=outgoing -->
Nel grafo unificato, il nodo canonico `MiddlewareLogParser` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_middlewarelogparser` all'ID `Request-Dataset-Generator::log_collector_rationale_127`. La relazione tecnica esplicita è la seguente: `MiddlewareLogParser` costituisce una motivazione per `Legge il log JSONL del middleware in streaming.      Indicizza per occurrence_ke`. Nel grafo unificato, il tipo dichiarato dell'arco 89 che coinvolge il nodo canonico `MiddlewareLogParser` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 89 che coinvolge il nodo canonico `MiddlewareLogParser` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 89 che coinvolge il nodo canonico `MiddlewareLogParser` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 89 che coinvolge il nodo canonico `MiddlewareLogParser` è `L127`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 89 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 89 che coinvolge il nodo canonico `MiddlewareLogParser` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 89 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo unificato, l'arco 89 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo unificato, l'arco 89 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_rationale_127` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`.
<!-- GRAPH_EDGE graph=unificata index=66 role=incoming -->
Nel grafo unificato, il nodo canonico `MiddlewareLogParser` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_middlewarelogparser`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `MiddlewareLogParser`. Nel grafo unificato, il tipo dichiarato dell'arco 66 che coinvolge il nodo canonico `MiddlewareLogParser` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 66 che coinvolge il nodo canonico `MiddlewareLogParser` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 66 che coinvolge il nodo canonico `MiddlewareLogParser` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 66 che coinvolge il nodo canonico `MiddlewareLogParser` è `L126`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 66 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 66 che coinvolge il nodo canonico `MiddlewareLogParser` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 66 che coinvolge il nodo canonico `MiddlewareLogParser` è `1.0`. Nel grafo unificato, l'arco 66 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`. Nel grafo unificato, l'arco 66 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_middlewarelogparser` per la relazione che coinvolge il nodo canonico `MiddlewareLogParser`.

### NormalizedDatasetBuilder

<!-- GRAPH_NODE graph=codice index=42 id=log_collector_normalizeddatasetbuilder -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `NormalizedDatasetBuilder` è `NormalizedDatasetBuilder`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `NormalizedDatasetBuilder` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `NormalizedDatasetBuilder` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `NormalizedDatasetBuilder` è `L497`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `NormalizedDatasetBuilder` vale `ast`. Nel grafo del codice RDG, il nodo canonico `NormalizedDatasetBuilder` possiede l'identificatore esatto `log_collector_normalizeddatasetbuilder`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `NormalizedDatasetBuilder` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `NormalizedDatasetBuilder` è `normalizeddatasetbuilder`.
<!-- GRAPH_EDGE graph=codice index=102 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `NormalizedDatasetBuilder` è la sorgente `source` della relazione `method` verso il nodo canonico `.build()`; l'orientamento registrato nel JSON va dall'ID `log_collector_normalizeddatasetbuilder` all'ID `log_collector_normalizeddatasetbuilder_build`. La relazione tecnica esplicita è la seguente: `NormalizedDatasetBuilder` registra come proprio metodo `.build()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 102 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 102 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 102 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 102 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `L503`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 102 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 102 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `ast`. Nel grafo del codice RDG, l'arco 102 registra come `source` l'identificatore `log_collector_normalizeddatasetbuilder` per la relazione che coinvolge il nodo canonico `NormalizedDatasetBuilder`. Nel grafo del codice RDG, l'arco 102 registra come `target` l'identificatore `log_collector_normalizeddatasetbuilder_build` per la relazione che coinvolge il nodo canonico `NormalizedDatasetBuilder`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 102 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=67 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `NormalizedDatasetBuilder` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_normalizeddatasetbuilder`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `NormalizedDatasetBuilder`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 67 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 67 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 67 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 67 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `L497`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 67 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 67 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `ast`. Nel grafo del codice RDG, l'arco 67 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `NormalizedDatasetBuilder`. Nel grafo del codice RDG, l'arco 67 registra come `target` l'identificatore `log_collector_normalizeddatasetbuilder` per la relazione che coinvolge il nodo canonico `NormalizedDatasetBuilder`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 67 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=103 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `NormalizedDatasetBuilder` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.`; l'orientamento registrato nel JSON va dall'ID `log_collector_rationale_498` all'ID `log_collector_normalizeddatasetbuilder`. La relazione tecnica esplicita è la seguente: `Assembla il JSON finale del Normalized Metrics Dataset.` costituisce una motivazione per `NormalizedDatasetBuilder`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 103 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 103 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 103 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 103 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `L498`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 103 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 103 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `ast`. Nel grafo del codice RDG, l'arco 103 registra come `source` l'identificatore `log_collector_rationale_498` per la relazione che coinvolge il nodo canonico `NormalizedDatasetBuilder`. Nel grafo del codice RDG, l'arco 103 registra come `target` l'identificatore `log_collector_normalizeddatasetbuilder` per la relazione che coinvolge il nodo canonico `NormalizedDatasetBuilder`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 103 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=42 id=Request-Dataset-Generator::log_collector_normalizeddatasetbuilder -->
Nel grafo unificato, l'etichetta originale del nodo canonico `NormalizedDatasetBuilder` è `NormalizedDatasetBuilder`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `NormalizedDatasetBuilder` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `NormalizedDatasetBuilder` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `NormalizedDatasetBuilder` è `L497`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `NormalizedDatasetBuilder` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `NormalizedDatasetBuilder` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `NormalizedDatasetBuilder` è `normalizeddatasetbuilder`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `NormalizedDatasetBuilder` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `NormalizedDatasetBuilder` è `log_collector_normalizeddatasetbuilder`. Nel grafo unificato, il nodo canonico `NormalizedDatasetBuilder` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder`.
<!-- GRAPH_EDGE graph=unificata index=102 role=outgoing -->
Nel grafo unificato, il nodo canonico `NormalizedDatasetBuilder` è la sorgente `source` della relazione `method` verso il nodo canonico `.build()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder` all'ID `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build`. La relazione tecnica esplicita è la seguente: `NormalizedDatasetBuilder` registra come proprio metodo `.build()`. Nel grafo unificato, il tipo dichiarato dell'arco 102 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 102 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 102 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 102 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `L503`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 102 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 102 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 102 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `1.0`. Nel grafo unificato, l'arco 102 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder` per la relazione che coinvolge il nodo canonico `NormalizedDatasetBuilder`. Nel grafo unificato, l'arco 102 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder_build` per la relazione che coinvolge il nodo canonico `NormalizedDatasetBuilder`.
<!-- GRAPH_EDGE graph=unificata index=103 role=outgoing -->
Nel grafo unificato, il nodo canonico `NormalizedDatasetBuilder` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Assembla il JSON finale del Normalized Metrics Dataset.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder` all'ID `Request-Dataset-Generator::log_collector_rationale_498`. La relazione tecnica esplicita è la seguente: `NormalizedDatasetBuilder` costituisce una motivazione per `Assembla il JSON finale del Normalized Metrics Dataset.`. Nel grafo unificato, il tipo dichiarato dell'arco 103 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 103 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 103 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 103 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `L498`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 103 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 103 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 103 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `1.0`. Nel grafo unificato, l'arco 103 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder` per la relazione che coinvolge il nodo canonico `NormalizedDatasetBuilder`. Nel grafo unificato, l'arco 103 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_rationale_498` per la relazione che coinvolge il nodo canonico `NormalizedDatasetBuilder`.
<!-- GRAPH_EDGE graph=unificata index=67 role=incoming -->
Nel grafo unificato, il nodo canonico `NormalizedDatasetBuilder` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `NormalizedDatasetBuilder`. Nel grafo unificato, il tipo dichiarato dell'arco 67 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 67 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 67 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 67 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `L497`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 67 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 67 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 67 che coinvolge il nodo canonico `NormalizedDatasetBuilder` è `1.0`. Nel grafo unificato, l'arco 67 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `NormalizedDatasetBuilder`. Nel grafo unificato, l'arco 67 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_normalizeddatasetbuilder` per la relazione che coinvolge il nodo canonico `NormalizedDatasetBuilder`.

### TimeseriesAggregator

<!-- GRAPH_NODE graph=codice index=39 id=log_collector_timeseriesaggregator -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `TimeseriesAggregator` è `TimeseriesAggregator`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `TimeseriesAggregator` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `TimeseriesAggregator` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `TimeseriesAggregator` è `L323`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `TimeseriesAggregator` vale `ast`. Nel grafo del codice RDG, il nodo canonico `TimeseriesAggregator` possiede l'identificatore esatto `log_collector_timeseriesaggregator`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `TimeseriesAggregator` alla comunità numerica `7`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `TimeseriesAggregator` è `timeseriesaggregator`.
<!-- GRAPH_EDGE graph=codice index=98 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `TimeseriesAggregator` è la sorgente `source` della relazione `method` verso il nodo canonico `.aggregate()`; l'orientamento registrato nel JSON va dall'ID `log_collector_timeseriesaggregator` all'ID `log_collector_timeseriesaggregator_aggregate`. La relazione tecnica esplicita è la seguente: `TimeseriesAggregator` registra come proprio metodo `.aggregate()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 98 che coinvolge il nodo canonico `TimeseriesAggregator` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 98 che coinvolge il nodo canonico `TimeseriesAggregator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 98 che coinvolge il nodo canonico `TimeseriesAggregator` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 98 che coinvolge il nodo canonico `TimeseriesAggregator` è `L329`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 98 che coinvolge il nodo canonico `TimeseriesAggregator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 98 che coinvolge il nodo canonico `TimeseriesAggregator` è `ast`. Nel grafo del codice RDG, l'arco 98 registra come `source` l'identificatore `log_collector_timeseriesaggregator` per la relazione che coinvolge il nodo canonico `TimeseriesAggregator`. Nel grafo del codice RDG, l'arco 98 registra come `target` l'identificatore `log_collector_timeseriesaggregator_aggregate` per la relazione che coinvolge il nodo canonico `TimeseriesAggregator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 98 che coinvolge il nodo canonico `TimeseriesAggregator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=70 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `TimeseriesAggregator` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `log_collector` all'ID `log_collector_timeseriesaggregator`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `TimeseriesAggregator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 70 che coinvolge il nodo canonico `TimeseriesAggregator` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 70 che coinvolge il nodo canonico `TimeseriesAggregator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 70 che coinvolge il nodo canonico `TimeseriesAggregator` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 70 che coinvolge il nodo canonico `TimeseriesAggregator` è `L323`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 70 che coinvolge il nodo canonico `TimeseriesAggregator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 70 che coinvolge il nodo canonico `TimeseriesAggregator` è `ast`. Nel grafo del codice RDG, l'arco 70 registra come `source` l'identificatore `log_collector` per la relazione che coinvolge il nodo canonico `TimeseriesAggregator`. Nel grafo del codice RDG, l'arco 70 registra come `target` l'identificatore `log_collector_timeseriesaggregator` per la relazione che coinvolge il nodo canonico `TimeseriesAggregator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 70 che coinvolge il nodo canonico `TimeseriesAggregator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=97 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `TimeseriesAggregator` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Aggrega i record normalizzati in bucket temporali.`; l'orientamento registrato nel JSON va dall'ID `log_collector_rationale_324` all'ID `log_collector_timeseriesaggregator`. La relazione tecnica esplicita è la seguente: `Aggrega i record normalizzati in bucket temporali.` costituisce una motivazione per `TimeseriesAggregator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 97 che coinvolge il nodo canonico `TimeseriesAggregator` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 97 che coinvolge il nodo canonico `TimeseriesAggregator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 97 che coinvolge il nodo canonico `TimeseriesAggregator` è `log_collector.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 97 che coinvolge il nodo canonico `TimeseriesAggregator` è `L324`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 97 che coinvolge il nodo canonico `TimeseriesAggregator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 97 che coinvolge il nodo canonico `TimeseriesAggregator` è `ast`. Nel grafo del codice RDG, l'arco 97 registra come `source` l'identificatore `log_collector_rationale_324` per la relazione che coinvolge il nodo canonico `TimeseriesAggregator`. Nel grafo del codice RDG, l'arco 97 registra come `target` l'identificatore `log_collector_timeseriesaggregator` per la relazione che coinvolge il nodo canonico `TimeseriesAggregator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 97 che coinvolge il nodo canonico `TimeseriesAggregator` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=39 id=Request-Dataset-Generator::log_collector_timeseriesaggregator -->
Nel grafo unificato, l'etichetta originale del nodo canonico `TimeseriesAggregator` è `TimeseriesAggregator`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `TimeseriesAggregator` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `TimeseriesAggregator` è `log_collector.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `TimeseriesAggregator` è `L323`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `TimeseriesAggregator` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `TimeseriesAggregator` alla comunità numerica `7`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `TimeseriesAggregator` è `timeseriesaggregator`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `TimeseriesAggregator` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `TimeseriesAggregator` è `log_collector_timeseriesaggregator`. Nel grafo unificato, il nodo canonico `TimeseriesAggregator` possiede l'identificatore esatto `Request-Dataset-Generator::log_collector_timeseriesaggregator`.
<!-- GRAPH_EDGE graph=unificata index=97 role=outgoing -->
Nel grafo unificato, il nodo canonico `TimeseriesAggregator` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Aggrega i record normalizzati in bucket temporali.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_timeseriesaggregator` all'ID `Request-Dataset-Generator::log_collector_rationale_324`. La relazione tecnica esplicita è la seguente: `TimeseriesAggregator` costituisce una motivazione per `Aggrega i record normalizzati in bucket temporali.`. Nel grafo unificato, il tipo dichiarato dell'arco 97 che coinvolge il nodo canonico `TimeseriesAggregator` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 97 che coinvolge il nodo canonico `TimeseriesAggregator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 97 che coinvolge il nodo canonico `TimeseriesAggregator` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 97 che coinvolge il nodo canonico `TimeseriesAggregator` è `L324`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 97 che coinvolge il nodo canonico `TimeseriesAggregator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 97 che coinvolge il nodo canonico `TimeseriesAggregator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 97 che coinvolge il nodo canonico `TimeseriesAggregator` è `1.0`. Nel grafo unificato, l'arco 97 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_timeseriesaggregator` per la relazione che coinvolge il nodo canonico `TimeseriesAggregator`. Nel grafo unificato, l'arco 97 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_rationale_324` per la relazione che coinvolge il nodo canonico `TimeseriesAggregator`.
<!-- GRAPH_EDGE graph=unificata index=98 role=outgoing -->
Nel grafo unificato, il nodo canonico `TimeseriesAggregator` è la sorgente `source` della relazione `method` verso il nodo canonico `.aggregate()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector_timeseriesaggregator` all'ID `Request-Dataset-Generator::log_collector_timeseriesaggregator_aggregate`. La relazione tecnica esplicita è la seguente: `TimeseriesAggregator` registra come proprio metodo `.aggregate()`. Nel grafo unificato, il tipo dichiarato dell'arco 98 che coinvolge il nodo canonico `TimeseriesAggregator` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 98 che coinvolge il nodo canonico `TimeseriesAggregator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 98 che coinvolge il nodo canonico `TimeseriesAggregator` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 98 che coinvolge il nodo canonico `TimeseriesAggregator` è `L329`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 98 che coinvolge il nodo canonico `TimeseriesAggregator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 98 che coinvolge il nodo canonico `TimeseriesAggregator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 98 che coinvolge il nodo canonico `TimeseriesAggregator` è `1.0`. Nel grafo unificato, l'arco 98 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector_timeseriesaggregator` per la relazione che coinvolge il nodo canonico `TimeseriesAggregator`. Nel grafo unificato, l'arco 98 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_timeseriesaggregator_aggregate` per la relazione che coinvolge il nodo canonico `TimeseriesAggregator`.
<!-- GRAPH_EDGE graph=unificata index=70 role=incoming -->
Nel grafo unificato, il nodo canonico `TimeseriesAggregator` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `log_collector.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::log_collector` all'ID `Request-Dataset-Generator::log_collector_timeseriesaggregator`. La relazione tecnica esplicita è la seguente: `log_collector.py` contiene `TimeseriesAggregator`. Nel grafo unificato, il tipo dichiarato dell'arco 70 che coinvolge il nodo canonico `TimeseriesAggregator` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 70 che coinvolge il nodo canonico `TimeseriesAggregator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 70 che coinvolge il nodo canonico `TimeseriesAggregator` è `log_collector.py`. Nel grafo unificato, la posizione sorgente dell'arco 70 che coinvolge il nodo canonico `TimeseriesAggregator` è `L323`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 70 che coinvolge il nodo canonico `TimeseriesAggregator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 70 che coinvolge il nodo canonico `TimeseriesAggregator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 70 che coinvolge il nodo canonico `TimeseriesAggregator` è `1.0`. Nel grafo unificato, l'arco 70 registra come `source` l'identificatore `Request-Dataset-Generator::log_collector` per la relazione che coinvolge il nodo canonico `TimeseriesAggregator`. Nel grafo unificato, l'arco 70 registra come `target` l'identificatore `Request-Dataset-Generator::log_collector_timeseriesaggregator` per la relazione che coinvolge il nodo canonico `TimeseriesAggregator`.

## Cluster Graphify 8 — Run Tab Pipeline

Il cluster editoriale Graphify 8 del dominio Architettura e codice del framework RDG contiene 28 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### .__init__() [ui_tabs_run_tab_runtab_init]

<!-- GRAPH_NODE graph=codice index=467 id=ui_tabs_run_tab_runtab_init -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `.__init__()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `L80`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_init`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `.__init__()`.
<!-- GRAPH_EDGE graph=codice index=954 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_init` all'ID `ui_tabs_run_tab_runtab_build`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_tabs_run_tab_runtab_init]` chiama `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 954 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 954 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 954 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 954 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 954 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `L89`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 954 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 954 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `ast`. Nel grafo del codice RDG, l'arco 954 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]`. Nel grafo del codice RDG, l'arco 954 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 954 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=936 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_init`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `.__init__() [ui_tabs_run_tab_runtab_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 936 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 936 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 936 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 936 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `L80`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 936 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 936 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `ast`. Nel grafo del codice RDG, l'arco 936 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]`. Nel grafo del codice RDG, l'arco 936 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 936 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=467 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_init -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `.__init__()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `L80`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `.__init__()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `ui_tabs_run_tab_runtab_init`. Nel grafo unificato, il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_init`.
<!-- GRAPH_EDGE graph=unificata index=954 role=outgoing -->
Nel grafo unificato, il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_init` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_tabs_run_tab_runtab_init]` chiama `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo unificato, il tipo dichiarato dell'arco 954 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 954 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 954 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 954 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 954 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `L89`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 954 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 954 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 954 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `1.0`. Nel grafo unificato, l'arco 954 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]`. Nel grafo unificato, l'arco 954 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]`.
<!-- GRAPH_EDGE graph=unificata index=936 role=incoming -->
Nel grafo unificato, il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_init`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `.__init__() [ui_tabs_run_tab_runtab_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 936 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 936 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 936 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 936 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `L80`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 936 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 936 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 936 che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]` è `1.0`. Nel grafo unificato, l'arco 936 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]`. Nel grafo unificato, l'arco 936 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]`.

### ._build() [ui_tabs_run_tab_runtab_build]

<!-- GRAPH_NODE graph=codice index=468 id=ui_tabs_run_tab_runtab_build -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `._build()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `L92`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_build`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `._build()`.
<!-- GRAPH_EDGE graph=codice index=955 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_build` all'ID `ui_tabs_run_tab_runtab_build_buttons`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_run_tab_runtab_build]` chiama `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 955 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 955 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 955 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 955 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 955 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `L110`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 955 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 955 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ast`. Nel grafo del codice RDG, l'arco 955 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo del codice RDG, l'arco 955 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_build_buttons` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 955 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=956 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_log()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_build` all'ID `ui_tabs_run_tab_runtab_build_log`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_run_tab_runtab_build]` chiama `._build_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 956 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 956 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 956 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 956 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 956 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `L105`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 956 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 956 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ast`. Nel grafo del codice RDG, l'arco 956 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo del codice RDG, l'arco 956 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_build_log` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 956 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=957 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_progress()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_build` all'ID `ui_tabs_run_tab_runtab_build_progress`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_run_tab_runtab_build]` chiama `._build_progress()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 957 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 957 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 957 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 957 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 957 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `L104`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 957 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 957 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ast`. Nel grafo del codice RDG, l'arco 957 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo del codice RDG, l'arco 957 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_build_progress` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 957 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=958 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_step_card()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_build` all'ID `ui_tabs_run_tab_runtab_build_step_card`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_run_tab_runtab_build]` chiama `._build_step_card()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 958 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 958 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 958 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 958 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 958 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `L102`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 958 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 958 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ast`. Nel grafo del codice RDG, l'arco 958 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo del codice RDG, l'arco 958 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_build_step_card` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 958 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=929 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_build`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 929 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 929 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 929 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 929 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `L92`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 929 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 929 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ast`. Nel grafo del codice RDG, l'arco 929 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo del codice RDG, l'arco 929 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 929 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=954 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_init` all'ID `ui_tabs_run_tab_runtab_build`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_tabs_run_tab_runtab_init]` chiama `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 954 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 954 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 954 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 954 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 954 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `L89`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 954 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 954 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ast`. Nel grafo del codice RDG, l'arco 954 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_init` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo del codice RDG, l'arco 954 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 954 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=468 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_build -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `._build()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `L92`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `._build()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ui_tabs_run_tab_runtab_build`. Nel grafo unificato, il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build`.
<!-- GRAPH_EDGE graph=unificata index=955 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_buttons`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_run_tab_runtab_build]` chiama `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]`. Nel grafo unificato, il tipo dichiarato dell'arco 955 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 955 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 955 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 955 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 955 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `L110`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 955 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 955 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 955 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`. Nel grafo unificato, l'arco 955 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo unificato, l'arco 955 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_buttons` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`.
<!-- GRAPH_EDGE graph=unificata index=956 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_log()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_log`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_run_tab_runtab_build]` chiama `._build_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 956 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 956 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 956 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 956 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 956 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `L105`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 956 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 956 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 956 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`. Nel grafo unificato, l'arco 956 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo unificato, l'arco 956 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_log` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`.
<!-- GRAPH_EDGE graph=unificata index=957 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_progress()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_progress`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_run_tab_runtab_build]` chiama `._build_progress()`. Nel grafo unificato, il tipo dichiarato dell'arco 957 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 957 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 957 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 957 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 957 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `L104`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 957 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 957 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 957 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`. Nel grafo unificato, l'arco 957 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo unificato, l'arco 957 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_progress` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`.
<!-- GRAPH_EDGE graph=unificata index=958 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_step_card()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_step_card`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_run_tab_runtab_build]` chiama `._build_step_card()`. Nel grafo unificato, il tipo dichiarato dell'arco 958 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 958 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 958 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 958 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 958 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `L102`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 958 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 958 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 958 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`. Nel grafo unificato, l'arco 958 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo unificato, l'arco 958 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_step_card` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`.
<!-- GRAPH_EDGE graph=unificata index=929 role=incoming -->
Nel grafo unificato, il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo unificato, il tipo dichiarato dell'arco 929 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 929 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 929 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 929 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `L92`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 929 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 929 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 929 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`. Nel grafo unificato, l'arco 929 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo unificato, l'arco 929 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`.
<!-- GRAPH_EDGE graph=unificata index=954 role=incoming -->
Nel grafo unificato, il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_init` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_tabs_run_tab_runtab_init]` chiama `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo unificato, il tipo dichiarato dell'arco 954 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 954 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 954 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 954 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 954 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `L89`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 954 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 954 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 954 che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]` è `1.0`. Nel grafo unificato, l'arco 954 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_init` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo unificato, l'arco 954 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`.

### ._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]

<!-- GRAPH_NODE graph=codice index=472 id=ui_tabs_run_tab_runtab_build_buttons -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `._build_buttons()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `L238`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_build_buttons`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `._build_buttons()`.
Nel grafo del codice RDG, il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=930 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_build_buttons`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 930 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 930 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 930 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 930 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `L238`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 930 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 930 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `ast`. Nel grafo del codice RDG, l'arco 930 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]`. Nel grafo del codice RDG, l'arco 930 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_build_buttons` per la relazione che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 930 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=955 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_build` all'ID `ui_tabs_run_tab_runtab_build_buttons`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_run_tab_runtab_build]` chiama `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 955 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 955 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 955 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 955 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 955 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `L110`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 955 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 955 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `ast`. Nel grafo del codice RDG, l'arco 955 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]`. Nel grafo del codice RDG, l'arco 955 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_build_buttons` per la relazione che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 955 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=472 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_buttons -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `._build_buttons()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `L238`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `._build_buttons()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `ui_tabs_run_tab_runtab_build_buttons`. Nel grafo unificato, il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_buttons`.
Nel grafo unificato, il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=930 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_buttons`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]`. Nel grafo unificato, il tipo dichiarato dell'arco 930 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 930 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 930 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 930 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `L238`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 930 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 930 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 930 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `1.0`. Nel grafo unificato, l'arco 930 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]`. Nel grafo unificato, l'arco 930 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_buttons` per la relazione che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]`.
<!-- GRAPH_EDGE graph=unificata index=955 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_buttons`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_run_tab_runtab_build]` chiama `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]`. Nel grafo unificato, il tipo dichiarato dell'arco 955 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 955 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 955 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 955 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 955 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `L110`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 955 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 955 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 955 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]` è `1.0`. Nel grafo unificato, l'arco 955 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]`. Nel grafo unificato, l'arco 955 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_buttons` per la relazione che coinvolge il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]`.

### ._build_log()

<!-- GRAPH_NODE graph=codice index=471 id=ui_tabs_run_tab_runtab_build_log -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._build_log()` è `._build_log()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._build_log()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._build_log()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._build_log()` è `L221`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._build_log()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._build_log()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_build_log`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._build_log()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._build_log()` è `._build_log()`.
<!-- GRAPH_EDGE graph=codice index=960 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build_log()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._configure_log_tags()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_build_log` all'ID `ui_tabs_run_tab_runtab_configure_log_tags`. La relazione tecnica esplicita è la seguente: `._build_log()` chiama `._configure_log_tags()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 960 che coinvolge il nodo canonico `._build_log()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 960 che coinvolge il nodo canonico `._build_log()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 960 che coinvolge il nodo canonico `._build_log()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 960 che coinvolge il nodo canonico `._build_log()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 960 che coinvolge il nodo canonico `._build_log()` è `L235`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 960 che coinvolge il nodo canonico `._build_log()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 960 che coinvolge il nodo canonico `._build_log()` è `ast`. Nel grafo del codice RDG, l'arco 960 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_build_log` per la relazione che coinvolge il nodo canonico `._build_log()`. Nel grafo del codice RDG, l'arco 960 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_configure_log_tags` per la relazione che coinvolge il nodo canonico `._build_log()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 960 che coinvolge il nodo canonico `._build_log()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=961 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build_log()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._set_log_placeholder()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_build_log` all'ID `ui_tabs_run_tab_runtab_set_log_placeholder`. La relazione tecnica esplicita è la seguente: `._build_log()` chiama `._set_log_placeholder()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 961 che coinvolge il nodo canonico `._build_log()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 961 che coinvolge il nodo canonico `._build_log()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 961 che coinvolge il nodo canonico `._build_log()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 961 che coinvolge il nodo canonico `._build_log()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 961 che coinvolge il nodo canonico `._build_log()` è `L236`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 961 che coinvolge il nodo canonico `._build_log()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 961 che coinvolge il nodo canonico `._build_log()` è `ast`. Nel grafo del codice RDG, l'arco 961 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_build_log` per la relazione che coinvolge il nodo canonico `._build_log()`. Nel grafo del codice RDG, l'arco 961 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_set_log_placeholder` per la relazione che coinvolge il nodo canonico `._build_log()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 961 che coinvolge il nodo canonico `._build_log()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=931 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_log()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_build_log`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._build_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 931 che coinvolge il nodo canonico `._build_log()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 931 che coinvolge il nodo canonico `._build_log()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 931 che coinvolge il nodo canonico `._build_log()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 931 che coinvolge il nodo canonico `._build_log()` è `L221`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 931 che coinvolge il nodo canonico `._build_log()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 931 che coinvolge il nodo canonico `._build_log()` è `ast`. Nel grafo del codice RDG, l'arco 931 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._build_log()`. Nel grafo del codice RDG, l'arco 931 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_build_log` per la relazione che coinvolge il nodo canonico `._build_log()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 931 che coinvolge il nodo canonico `._build_log()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=956 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_log()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_build` all'ID `ui_tabs_run_tab_runtab_build_log`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_run_tab_runtab_build]` chiama `._build_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 956 che coinvolge il nodo canonico `._build_log()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 956 che coinvolge il nodo canonico `._build_log()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 956 che coinvolge il nodo canonico `._build_log()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 956 che coinvolge il nodo canonico `._build_log()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 956 che coinvolge il nodo canonico `._build_log()` è `L105`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 956 che coinvolge il nodo canonico `._build_log()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 956 che coinvolge il nodo canonico `._build_log()` è `ast`. Nel grafo del codice RDG, l'arco 956 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `._build_log()`. Nel grafo del codice RDG, l'arco 956 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_build_log` per la relazione che coinvolge il nodo canonico `._build_log()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 956 che coinvolge il nodo canonico `._build_log()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=471 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_log -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._build_log()` è `._build_log()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._build_log()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._build_log()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._build_log()` è `L221`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._build_log()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._build_log()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._build_log()` è `._build_log()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._build_log()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._build_log()` è `ui_tabs_run_tab_runtab_build_log`. Nel grafo unificato, il nodo canonico `._build_log()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_log`.
<!-- GRAPH_EDGE graph=unificata index=960 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build_log()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._configure_log_tags()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_log` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_configure_log_tags`. La relazione tecnica esplicita è la seguente: `._build_log()` chiama `._configure_log_tags()`. Nel grafo unificato, il tipo dichiarato dell'arco 960 che coinvolge il nodo canonico `._build_log()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 960 che coinvolge il nodo canonico `._build_log()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 960 che coinvolge il nodo canonico `._build_log()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 960 che coinvolge il nodo canonico `._build_log()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 960 che coinvolge il nodo canonico `._build_log()` è `L235`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 960 che coinvolge il nodo canonico `._build_log()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 960 che coinvolge il nodo canonico `._build_log()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 960 che coinvolge il nodo canonico `._build_log()` è `1.0`. Nel grafo unificato, l'arco 960 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_log` per la relazione che coinvolge il nodo canonico `._build_log()`. Nel grafo unificato, l'arco 960 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_configure_log_tags` per la relazione che coinvolge il nodo canonico `._build_log()`.
<!-- GRAPH_EDGE graph=unificata index=961 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build_log()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._set_log_placeholder()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_log` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_log_placeholder`. La relazione tecnica esplicita è la seguente: `._build_log()` chiama `._set_log_placeholder()`. Nel grafo unificato, il tipo dichiarato dell'arco 961 che coinvolge il nodo canonico `._build_log()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 961 che coinvolge il nodo canonico `._build_log()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 961 che coinvolge il nodo canonico `._build_log()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 961 che coinvolge il nodo canonico `._build_log()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 961 che coinvolge il nodo canonico `._build_log()` è `L236`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 961 che coinvolge il nodo canonico `._build_log()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 961 che coinvolge il nodo canonico `._build_log()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 961 che coinvolge il nodo canonico `._build_log()` è `1.0`. Nel grafo unificato, l'arco 961 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_log` per la relazione che coinvolge il nodo canonico `._build_log()`. Nel grafo unificato, l'arco 961 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_log_placeholder` per la relazione che coinvolge il nodo canonico `._build_log()`.
<!-- GRAPH_EDGE graph=unificata index=931 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_log()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_log`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._build_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 931 che coinvolge il nodo canonico `._build_log()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 931 che coinvolge il nodo canonico `._build_log()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 931 che coinvolge il nodo canonico `._build_log()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 931 che coinvolge il nodo canonico `._build_log()` è `L221`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 931 che coinvolge il nodo canonico `._build_log()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 931 che coinvolge il nodo canonico `._build_log()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 931 che coinvolge il nodo canonico `._build_log()` è `1.0`. Nel grafo unificato, l'arco 931 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._build_log()`. Nel grafo unificato, l'arco 931 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_log` per la relazione che coinvolge il nodo canonico `._build_log()`.
<!-- GRAPH_EDGE graph=unificata index=956 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_log()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_log`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_run_tab_runtab_build]` chiama `._build_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 956 che coinvolge il nodo canonico `._build_log()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 956 che coinvolge il nodo canonico `._build_log()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 956 che coinvolge il nodo canonico `._build_log()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 956 che coinvolge il nodo canonico `._build_log()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 956 che coinvolge il nodo canonico `._build_log()` è `L105`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 956 che coinvolge il nodo canonico `._build_log()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 956 che coinvolge il nodo canonico `._build_log()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 956 che coinvolge il nodo canonico `._build_log()` è `1.0`. Nel grafo unificato, l'arco 956 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `._build_log()`. Nel grafo unificato, l'arco 956 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_log` per la relazione che coinvolge il nodo canonico `._build_log()`.

### ._build_progress()

<!-- GRAPH_NODE graph=codice index=470 id=ui_tabs_run_tab_runtab_build_progress -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._build_progress()` è `._build_progress()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._build_progress()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._build_progress()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._build_progress()` è `L176`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._build_progress()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._build_progress()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_build_progress`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._build_progress()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._build_progress()` è `._build_progress()`.
Nel grafo del codice RDG, il nodo canonico `._build_progress()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=932 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_progress()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_build_progress`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._build_progress()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 932 che coinvolge il nodo canonico `._build_progress()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 932 che coinvolge il nodo canonico `._build_progress()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 932 che coinvolge il nodo canonico `._build_progress()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 932 che coinvolge il nodo canonico `._build_progress()` è `L176`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 932 che coinvolge il nodo canonico `._build_progress()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 932 che coinvolge il nodo canonico `._build_progress()` è `ast`. Nel grafo del codice RDG, l'arco 932 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._build_progress()`. Nel grafo del codice RDG, l'arco 932 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_build_progress` per la relazione che coinvolge il nodo canonico `._build_progress()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 932 che coinvolge il nodo canonico `._build_progress()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=957 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_progress()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_build` all'ID `ui_tabs_run_tab_runtab_build_progress`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_run_tab_runtab_build]` chiama `._build_progress()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 957 che coinvolge il nodo canonico `._build_progress()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 957 che coinvolge il nodo canonico `._build_progress()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 957 che coinvolge il nodo canonico `._build_progress()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 957 che coinvolge il nodo canonico `._build_progress()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 957 che coinvolge il nodo canonico `._build_progress()` è `L104`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 957 che coinvolge il nodo canonico `._build_progress()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 957 che coinvolge il nodo canonico `._build_progress()` è `ast`. Nel grafo del codice RDG, l'arco 957 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `._build_progress()`. Nel grafo del codice RDG, l'arco 957 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_build_progress` per la relazione che coinvolge il nodo canonico `._build_progress()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 957 che coinvolge il nodo canonico `._build_progress()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=470 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_progress -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._build_progress()` è `._build_progress()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._build_progress()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._build_progress()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._build_progress()` è `L176`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._build_progress()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._build_progress()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._build_progress()` è `._build_progress()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._build_progress()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._build_progress()` è `ui_tabs_run_tab_runtab_build_progress`. Nel grafo unificato, il nodo canonico `._build_progress()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_progress`.
Nel grafo unificato, il nodo canonico `._build_progress()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=932 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_progress()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_progress`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._build_progress()`. Nel grafo unificato, il tipo dichiarato dell'arco 932 che coinvolge il nodo canonico `._build_progress()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 932 che coinvolge il nodo canonico `._build_progress()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 932 che coinvolge il nodo canonico `._build_progress()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 932 che coinvolge il nodo canonico `._build_progress()` è `L176`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 932 che coinvolge il nodo canonico `._build_progress()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 932 che coinvolge il nodo canonico `._build_progress()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 932 che coinvolge il nodo canonico `._build_progress()` è `1.0`. Nel grafo unificato, l'arco 932 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._build_progress()`. Nel grafo unificato, l'arco 932 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_progress` per la relazione che coinvolge il nodo canonico `._build_progress()`.
<!-- GRAPH_EDGE graph=unificata index=957 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_progress()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_progress`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_run_tab_runtab_build]` chiama `._build_progress()`. Nel grafo unificato, il tipo dichiarato dell'arco 957 che coinvolge il nodo canonico `._build_progress()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 957 che coinvolge il nodo canonico `._build_progress()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 957 che coinvolge il nodo canonico `._build_progress()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 957 che coinvolge il nodo canonico `._build_progress()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 957 che coinvolge il nodo canonico `._build_progress()` è `L104`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 957 che coinvolge il nodo canonico `._build_progress()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 957 che coinvolge il nodo canonico `._build_progress()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 957 che coinvolge il nodo canonico `._build_progress()` è `1.0`. Nel grafo unificato, l'arco 957 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `._build_progress()`. Nel grafo unificato, l'arco 957 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_progress` per la relazione che coinvolge il nodo canonico `._build_progress()`.

### ._build_step_card()

<!-- GRAPH_NODE graph=codice index=469 id=ui_tabs_run_tab_runtab_build_step_card -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._build_step_card()` è `._build_step_card()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._build_step_card()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._build_step_card()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._build_step_card()` è `L112`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._build_step_card()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._build_step_card()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_build_step_card`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._build_step_card()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._build_step_card()` è `._build_step_card()`.
<!-- GRAPH_EDGE graph=codice index=959 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build_step_card()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._start_single_step()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_build_step_card` all'ID `ui_tabs_run_tab_runtab_start_single_step`. La relazione tecnica esplicita è la seguente: `._build_step_card()` chiama `._start_single_step()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 959 che coinvolge il nodo canonico `._build_step_card()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 959 che coinvolge il nodo canonico `._build_step_card()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 959 che coinvolge il nodo canonico `._build_step_card()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 959 che coinvolge il nodo canonico `._build_step_card()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 959 che coinvolge il nodo canonico `._build_step_card()` è `L174`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 959 che coinvolge il nodo canonico `._build_step_card()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 959 che coinvolge il nodo canonico `._build_step_card()` è `ast`. Nel grafo del codice RDG, l'arco 959 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_build_step_card` per la relazione che coinvolge il nodo canonico `._build_step_card()`. Nel grafo del codice RDG, l'arco 959 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_start_single_step` per la relazione che coinvolge il nodo canonico `._build_step_card()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 959 che coinvolge il nodo canonico `._build_step_card()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=933 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_step_card()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_build_step_card`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._build_step_card()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 933 che coinvolge il nodo canonico `._build_step_card()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 933 che coinvolge il nodo canonico `._build_step_card()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 933 che coinvolge il nodo canonico `._build_step_card()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 933 che coinvolge il nodo canonico `._build_step_card()` è `L112`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 933 che coinvolge il nodo canonico `._build_step_card()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 933 che coinvolge il nodo canonico `._build_step_card()` è `ast`. Nel grafo del codice RDG, l'arco 933 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._build_step_card()`. Nel grafo del codice RDG, l'arco 933 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_build_step_card` per la relazione che coinvolge il nodo canonico `._build_step_card()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 933 che coinvolge il nodo canonico `._build_step_card()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=958 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_step_card()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_build` all'ID `ui_tabs_run_tab_runtab_build_step_card`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_run_tab_runtab_build]` chiama `._build_step_card()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 958 che coinvolge il nodo canonico `._build_step_card()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 958 che coinvolge il nodo canonico `._build_step_card()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 958 che coinvolge il nodo canonico `._build_step_card()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 958 che coinvolge il nodo canonico `._build_step_card()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 958 che coinvolge il nodo canonico `._build_step_card()` è `L102`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 958 che coinvolge il nodo canonico `._build_step_card()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 958 che coinvolge il nodo canonico `._build_step_card()` è `ast`. Nel grafo del codice RDG, l'arco 958 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `._build_step_card()`. Nel grafo del codice RDG, l'arco 958 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_build_step_card` per la relazione che coinvolge il nodo canonico `._build_step_card()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 958 che coinvolge il nodo canonico `._build_step_card()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=469 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_step_card -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._build_step_card()` è `._build_step_card()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._build_step_card()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._build_step_card()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._build_step_card()` è `L112`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._build_step_card()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._build_step_card()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._build_step_card()` è `._build_step_card()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._build_step_card()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._build_step_card()` è `ui_tabs_run_tab_runtab_build_step_card`. Nel grafo unificato, il nodo canonico `._build_step_card()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_step_card`.
<!-- GRAPH_EDGE graph=unificata index=959 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build_step_card()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._start_single_step()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_step_card` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_single_step`. La relazione tecnica esplicita è la seguente: `._build_step_card()` chiama `._start_single_step()`. Nel grafo unificato, il tipo dichiarato dell'arco 959 che coinvolge il nodo canonico `._build_step_card()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 959 che coinvolge il nodo canonico `._build_step_card()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 959 che coinvolge il nodo canonico `._build_step_card()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 959 che coinvolge il nodo canonico `._build_step_card()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 959 che coinvolge il nodo canonico `._build_step_card()` è `L174`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 959 che coinvolge il nodo canonico `._build_step_card()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 959 che coinvolge il nodo canonico `._build_step_card()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 959 che coinvolge il nodo canonico `._build_step_card()` è `1.0`. Nel grafo unificato, l'arco 959 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_step_card` per la relazione che coinvolge il nodo canonico `._build_step_card()`. Nel grafo unificato, l'arco 959 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_single_step` per la relazione che coinvolge il nodo canonico `._build_step_card()`.
<!-- GRAPH_EDGE graph=unificata index=933 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_step_card()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_step_card`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._build_step_card()`. Nel grafo unificato, il tipo dichiarato dell'arco 933 che coinvolge il nodo canonico `._build_step_card()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 933 che coinvolge il nodo canonico `._build_step_card()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 933 che coinvolge il nodo canonico `._build_step_card()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 933 che coinvolge il nodo canonico `._build_step_card()` è `L112`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 933 che coinvolge il nodo canonico `._build_step_card()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 933 che coinvolge il nodo canonico `._build_step_card()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 933 che coinvolge il nodo canonico `._build_step_card()` è `1.0`. Nel grafo unificato, l'arco 933 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._build_step_card()`. Nel grafo unificato, l'arco 933 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_step_card` per la relazione che coinvolge il nodo canonico `._build_step_card()`.
<!-- GRAPH_EDGE graph=unificata index=958 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_step_card()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_step_card`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_run_tab_runtab_build]` chiama `._build_step_card()`. Nel grafo unificato, il tipo dichiarato dell'arco 958 che coinvolge il nodo canonico `._build_step_card()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 958 che coinvolge il nodo canonico `._build_step_card()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 958 che coinvolge il nodo canonico `._build_step_card()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 958 che coinvolge il nodo canonico `._build_step_card()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 958 che coinvolge il nodo canonico `._build_step_card()` è `L102`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 958 che coinvolge il nodo canonico `._build_step_card()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 958 che coinvolge il nodo canonico `._build_step_card()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 958 che coinvolge il nodo canonico `._build_step_card()` è `1.0`. Nel grafo unificato, l'arco 958 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `._build_step_card()`. Nel grafo unificato, l'arco 958 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_step_card` per la relazione che coinvolge il nodo canonico `._build_step_card()`.

### ._clear_log()

<!-- GRAPH_NODE graph=codice index=489 id=ui_tabs_run_tab_runtab_clear_log -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._clear_log()` è `._clear_log()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._clear_log()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._clear_log()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._clear_log()` è `L433`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._clear_log()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._clear_log()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_clear_log`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._clear_log()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._clear_log()` è `._clear_log()`.
Nel grafo del codice RDG, il nodo canonico `._clear_log()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=934 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._clear_log()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_clear_log`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._clear_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 934 che coinvolge il nodo canonico `._clear_log()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 934 che coinvolge il nodo canonico `._clear_log()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 934 che coinvolge il nodo canonico `._clear_log()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 934 che coinvolge il nodo canonico `._clear_log()` è `L433`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 934 che coinvolge il nodo canonico `._clear_log()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 934 che coinvolge il nodo canonico `._clear_log()` è `ast`. Nel grafo del codice RDG, l'arco 934 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._clear_log()`. Nel grafo del codice RDG, l'arco 934 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_clear_log` per la relazione che coinvolge il nodo canonico `._clear_log()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 934 che coinvolge il nodo canonico `._clear_log()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=966 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._clear_log()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._prepare_run()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_prepare_run` all'ID `ui_tabs_run_tab_runtab_clear_log`. La relazione tecnica esplicita è la seguente: `._prepare_run()` chiama `._clear_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 966 che coinvolge il nodo canonico `._clear_log()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 966 che coinvolge il nodo canonico `._clear_log()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 966 che coinvolge il nodo canonico `._clear_log()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 966 che coinvolge il nodo canonico `._clear_log()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 966 che coinvolge il nodo canonico `._clear_log()` è `L300`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 966 che coinvolge il nodo canonico `._clear_log()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 966 che coinvolge il nodo canonico `._clear_log()` è `ast`. Nel grafo del codice RDG, l'arco 966 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_prepare_run` per la relazione che coinvolge il nodo canonico `._clear_log()`. Nel grafo del codice RDG, l'arco 966 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_clear_log` per la relazione che coinvolge il nodo canonico `._clear_log()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 966 che coinvolge il nodo canonico `._clear_log()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=489 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_clear_log -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._clear_log()` è `._clear_log()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._clear_log()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._clear_log()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._clear_log()` è `L433`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._clear_log()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._clear_log()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._clear_log()` è `._clear_log()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._clear_log()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._clear_log()` è `ui_tabs_run_tab_runtab_clear_log`. Nel grafo unificato, il nodo canonico `._clear_log()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_clear_log`.
Nel grafo unificato, il nodo canonico `._clear_log()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=934 role=incoming -->
Nel grafo unificato, il nodo canonico `._clear_log()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_clear_log`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._clear_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 934 che coinvolge il nodo canonico `._clear_log()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 934 che coinvolge il nodo canonico `._clear_log()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 934 che coinvolge il nodo canonico `._clear_log()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 934 che coinvolge il nodo canonico `._clear_log()` è `L433`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 934 che coinvolge il nodo canonico `._clear_log()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 934 che coinvolge il nodo canonico `._clear_log()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 934 che coinvolge il nodo canonico `._clear_log()` è `1.0`. Nel grafo unificato, l'arco 934 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._clear_log()`. Nel grafo unificato, l'arco 934 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_clear_log` per la relazione che coinvolge il nodo canonico `._clear_log()`.
<!-- GRAPH_EDGE graph=unificata index=966 role=incoming -->
Nel grafo unificato, il nodo canonico `._clear_log()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._prepare_run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_prepare_run` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_clear_log`. La relazione tecnica esplicita è la seguente: `._prepare_run()` chiama `._clear_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 966 che coinvolge il nodo canonico `._clear_log()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 966 che coinvolge il nodo canonico `._clear_log()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 966 che coinvolge il nodo canonico `._clear_log()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 966 che coinvolge il nodo canonico `._clear_log()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 966 che coinvolge il nodo canonico `._clear_log()` è `L300`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 966 che coinvolge il nodo canonico `._clear_log()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 966 che coinvolge il nodo canonico `._clear_log()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 966 che coinvolge il nodo canonico `._clear_log()` è `1.0`. Nel grafo unificato, l'arco 966 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_prepare_run` per la relazione che coinvolge il nodo canonico `._clear_log()`. Nel grafo unificato, l'arco 966 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_clear_log` per la relazione che coinvolge il nodo canonico `._clear_log()`.

### ._configure_log_tags()

<!-- GRAPH_NODE graph=codice index=488 id=ui_tabs_run_tab_runtab_configure_log_tags -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._configure_log_tags()` è `._configure_log_tags()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._configure_log_tags()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._configure_log_tags()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._configure_log_tags()` è `L419`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._configure_log_tags()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._configure_log_tags()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_configure_log_tags`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._configure_log_tags()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._configure_log_tags()` è `._configure_log_tags()`.
Nel grafo del codice RDG, il nodo canonico `._configure_log_tags()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=935 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._configure_log_tags()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_configure_log_tags`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._configure_log_tags()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 935 che coinvolge il nodo canonico `._configure_log_tags()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 935 che coinvolge il nodo canonico `._configure_log_tags()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 935 che coinvolge il nodo canonico `._configure_log_tags()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 935 che coinvolge il nodo canonico `._configure_log_tags()` è `L419`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 935 che coinvolge il nodo canonico `._configure_log_tags()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 935 che coinvolge il nodo canonico `._configure_log_tags()` è `ast`. Nel grafo del codice RDG, l'arco 935 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._configure_log_tags()`. Nel grafo del codice RDG, l'arco 935 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_configure_log_tags` per la relazione che coinvolge il nodo canonico `._configure_log_tags()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 935 che coinvolge il nodo canonico `._configure_log_tags()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=960 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._configure_log_tags()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build_log()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_build_log` all'ID `ui_tabs_run_tab_runtab_configure_log_tags`. La relazione tecnica esplicita è la seguente: `._build_log()` chiama `._configure_log_tags()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 960 che coinvolge il nodo canonico `._configure_log_tags()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 960 che coinvolge il nodo canonico `._configure_log_tags()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 960 che coinvolge il nodo canonico `._configure_log_tags()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 960 che coinvolge il nodo canonico `._configure_log_tags()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 960 che coinvolge il nodo canonico `._configure_log_tags()` è `L235`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 960 che coinvolge il nodo canonico `._configure_log_tags()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 960 che coinvolge il nodo canonico `._configure_log_tags()` è `ast`. Nel grafo del codice RDG, l'arco 960 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_build_log` per la relazione che coinvolge il nodo canonico `._configure_log_tags()`. Nel grafo del codice RDG, l'arco 960 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_configure_log_tags` per la relazione che coinvolge il nodo canonico `._configure_log_tags()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 960 che coinvolge il nodo canonico `._configure_log_tags()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=488 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_configure_log_tags -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._configure_log_tags()` è `._configure_log_tags()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._configure_log_tags()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._configure_log_tags()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._configure_log_tags()` è `L419`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._configure_log_tags()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._configure_log_tags()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._configure_log_tags()` è `._configure_log_tags()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._configure_log_tags()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._configure_log_tags()` è `ui_tabs_run_tab_runtab_configure_log_tags`. Nel grafo unificato, il nodo canonico `._configure_log_tags()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_configure_log_tags`.
Nel grafo unificato, il nodo canonico `._configure_log_tags()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=935 role=incoming -->
Nel grafo unificato, il nodo canonico `._configure_log_tags()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_configure_log_tags`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._configure_log_tags()`. Nel grafo unificato, il tipo dichiarato dell'arco 935 che coinvolge il nodo canonico `._configure_log_tags()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 935 che coinvolge il nodo canonico `._configure_log_tags()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 935 che coinvolge il nodo canonico `._configure_log_tags()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 935 che coinvolge il nodo canonico `._configure_log_tags()` è `L419`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 935 che coinvolge il nodo canonico `._configure_log_tags()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 935 che coinvolge il nodo canonico `._configure_log_tags()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 935 che coinvolge il nodo canonico `._configure_log_tags()` è `1.0`. Nel grafo unificato, l'arco 935 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._configure_log_tags()`. Nel grafo unificato, l'arco 935 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_configure_log_tags` per la relazione che coinvolge il nodo canonico `._configure_log_tags()`.
<!-- GRAPH_EDGE graph=unificata index=960 role=incoming -->
Nel grafo unificato, il nodo canonico `._configure_log_tags()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build_log()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_log` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_configure_log_tags`. La relazione tecnica esplicita è la seguente: `._build_log()` chiama `._configure_log_tags()`. Nel grafo unificato, il tipo dichiarato dell'arco 960 che coinvolge il nodo canonico `._configure_log_tags()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 960 che coinvolge il nodo canonico `._configure_log_tags()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 960 che coinvolge il nodo canonico `._configure_log_tags()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 960 che coinvolge il nodo canonico `._configure_log_tags()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 960 che coinvolge il nodo canonico `._configure_log_tags()` è `L235`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 960 che coinvolge il nodo canonico `._configure_log_tags()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 960 che coinvolge il nodo canonico `._configure_log_tags()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 960 che coinvolge il nodo canonico `._configure_log_tags()` è `1.0`. Nel grafo unificato, l'arco 960 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_log` per la relazione che coinvolge il nodo canonico `._configure_log_tags()`. Nel grafo unificato, l'arco 960 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_configure_log_tags` per la relazione che coinvolge il nodo canonico `._configure_log_tags()`.

### ._pipeline_worker()

<!-- GRAPH_NODE graph=codice index=477 id=ui_tabs_run_tab_runtab_pipeline_worker -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._pipeline_worker()` è `._pipeline_worker()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._pipeline_worker()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._pipeline_worker()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._pipeline_worker()` è `L303`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._pipeline_worker()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._pipeline_worker()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_pipeline_worker`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._pipeline_worker()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._pipeline_worker()` è `._pipeline_worker()`.
<!-- GRAPH_EDGE graph=codice index=967 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._pipeline_worker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_finish()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_pipeline_worker` all'ID `ui_tabs_run_tab_runtab_ui_finish`. La relazione tecnica esplicita è la seguente: `._pipeline_worker()` chiama `._ui_finish()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 967 che coinvolge il nodo canonico `._pipeline_worker()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 967 che coinvolge il nodo canonico `._pipeline_worker()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 967 che coinvolge il nodo canonico `._pipeline_worker()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 967 che coinvolge il nodo canonico `._pipeline_worker()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 967 che coinvolge il nodo canonico `._pipeline_worker()` è `L325`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 967 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 967 che coinvolge il nodo canonico `._pipeline_worker()` è `ast`. Nel grafo del codice RDG, l'arco 967 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`. Nel grafo del codice RDG, l'arco 967 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_finish` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 967 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=968 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._pipeline_worker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_log()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_pipeline_worker` all'ID `ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._pipeline_worker()` chiama `._ui_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 968 che coinvolge il nodo canonico `._pipeline_worker()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 968 che coinvolge il nodo canonico `._pipeline_worker()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 968 che coinvolge il nodo canonico `._pipeline_worker()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 968 che coinvolge il nodo canonico `._pipeline_worker()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 968 che coinvolge il nodo canonico `._pipeline_worker()` è `L315`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 968 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 968 che coinvolge il nodo canonico `._pipeline_worker()` è `ast`. Nel grafo del codice RDG, l'arco 968 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`. Nel grafo del codice RDG, l'arco 968 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 968 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=969 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._pipeline_worker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_set_progress()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_pipeline_worker` all'ID `ui_tabs_run_tab_runtab_ui_set_progress`. La relazione tecnica esplicita è la seguente: `._pipeline_worker()` chiama `._ui_set_progress()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 969 che coinvolge il nodo canonico `._pipeline_worker()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 969 che coinvolge il nodo canonico `._pipeline_worker()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 969 che coinvolge il nodo canonico `._pipeline_worker()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 969 che coinvolge il nodo canonico `._pipeline_worker()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 969 che coinvolge il nodo canonico `._pipeline_worker()` è `L314`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 969 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 969 che coinvolge il nodo canonico `._pipeline_worker()` è `ast`. Nel grafo del codice RDG, l'arco 969 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`. Nel grafo del codice RDG, l'arco 969 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_set_progress` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 969 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=970 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._pipeline_worker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_set_status()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_pipeline_worker` all'ID `ui_tabs_run_tab_runtab_ui_set_status`. La relazione tecnica esplicita è la seguente: `._pipeline_worker()` chiama `._ui_set_status()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 970 che coinvolge il nodo canonico `._pipeline_worker()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 970 che coinvolge il nodo canonico `._pipeline_worker()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 970 che coinvolge il nodo canonico `._pipeline_worker()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 970 che coinvolge il nodo canonico `._pipeline_worker()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 970 che coinvolge il nodo canonico `._pipeline_worker()` è `L313`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 970 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 970 che coinvolge il nodo canonico `._pipeline_worker()` è `ast`. Nel grafo del codice RDG, l'arco 970 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`. Nel grafo del codice RDG, l'arco 970 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_set_status` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 970 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=971 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._pipeline_worker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._write_mock_outputs()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_pipeline_worker` all'ID `ui_tabs_run_tab_runtab_write_mock_outputs`. La relazione tecnica esplicita è la seguente: `._pipeline_worker()` chiama `._write_mock_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 971 che coinvolge il nodo canonico `._pipeline_worker()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 971 che coinvolge il nodo canonico `._pipeline_worker()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 971 che coinvolge il nodo canonico `._pipeline_worker()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 971 che coinvolge il nodo canonico `._pipeline_worker()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 971 che coinvolge il nodo canonico `._pipeline_worker()` è `L320`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 971 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 971 che coinvolge il nodo canonico `._pipeline_worker()` è `ast`. Nel grafo del codice RDG, l'arco 971 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`. Nel grafo del codice RDG, l'arco 971 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_write_mock_outputs` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 971 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=937 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._pipeline_worker()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_pipeline_worker`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._pipeline_worker()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 937 che coinvolge il nodo canonico `._pipeline_worker()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 937 che coinvolge il nodo canonico `._pipeline_worker()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 937 che coinvolge il nodo canonico `._pipeline_worker()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 937 che coinvolge il nodo canonico `._pipeline_worker()` è `L303`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 937 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 937 che coinvolge il nodo canonico `._pipeline_worker()` è `ast`. Nel grafo del codice RDG, l'arco 937 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`. Nel grafo del codice RDG, l'arco 937 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 937 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=477 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._pipeline_worker()` è `._pipeline_worker()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._pipeline_worker()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._pipeline_worker()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._pipeline_worker()` è `L303`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._pipeline_worker()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._pipeline_worker()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._pipeline_worker()` è `._pipeline_worker()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._pipeline_worker()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._pipeline_worker()` è `ui_tabs_run_tab_runtab_pipeline_worker`. Nel grafo unificato, il nodo canonico `._pipeline_worker()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker`.
<!-- GRAPH_EDGE graph=unificata index=967 role=outgoing -->
Nel grafo unificato, il nodo canonico `._pipeline_worker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_finish()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_finish`. La relazione tecnica esplicita è la seguente: `._pipeline_worker()` chiama `._ui_finish()`. Nel grafo unificato, il tipo dichiarato dell'arco 967 che coinvolge il nodo canonico `._pipeline_worker()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 967 che coinvolge il nodo canonico `._pipeline_worker()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 967 che coinvolge il nodo canonico `._pipeline_worker()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 967 che coinvolge il nodo canonico `._pipeline_worker()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 967 che coinvolge il nodo canonico `._pipeline_worker()` è `L325`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 967 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 967 che coinvolge il nodo canonico `._pipeline_worker()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 967 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`. Nel grafo unificato, l'arco 967 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`. Nel grafo unificato, l'arco 967 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_finish` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`.
<!-- GRAPH_EDGE graph=unificata index=968 role=outgoing -->
Nel grafo unificato, il nodo canonico `._pipeline_worker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_log()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._pipeline_worker()` chiama `._ui_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 968 che coinvolge il nodo canonico `._pipeline_worker()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 968 che coinvolge il nodo canonico `._pipeline_worker()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 968 che coinvolge il nodo canonico `._pipeline_worker()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 968 che coinvolge il nodo canonico `._pipeline_worker()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 968 che coinvolge il nodo canonico `._pipeline_worker()` è `L315`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 968 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 968 che coinvolge il nodo canonico `._pipeline_worker()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 968 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`. Nel grafo unificato, l'arco 968 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`. Nel grafo unificato, l'arco 968 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`.
<!-- GRAPH_EDGE graph=unificata index=969 role=outgoing -->
Nel grafo unificato, il nodo canonico `._pipeline_worker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_set_progress()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_progress`. La relazione tecnica esplicita è la seguente: `._pipeline_worker()` chiama `._ui_set_progress()`. Nel grafo unificato, il tipo dichiarato dell'arco 969 che coinvolge il nodo canonico `._pipeline_worker()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 969 che coinvolge il nodo canonico `._pipeline_worker()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 969 che coinvolge il nodo canonico `._pipeline_worker()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 969 che coinvolge il nodo canonico `._pipeline_worker()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 969 che coinvolge il nodo canonico `._pipeline_worker()` è `L314`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 969 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 969 che coinvolge il nodo canonico `._pipeline_worker()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 969 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`. Nel grafo unificato, l'arco 969 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`. Nel grafo unificato, l'arco 969 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_progress` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`.
<!-- GRAPH_EDGE graph=unificata index=970 role=outgoing -->
Nel grafo unificato, il nodo canonico `._pipeline_worker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_set_status()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_status`. La relazione tecnica esplicita è la seguente: `._pipeline_worker()` chiama `._ui_set_status()`. Nel grafo unificato, il tipo dichiarato dell'arco 970 che coinvolge il nodo canonico `._pipeline_worker()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 970 che coinvolge il nodo canonico `._pipeline_worker()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 970 che coinvolge il nodo canonico `._pipeline_worker()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 970 che coinvolge il nodo canonico `._pipeline_worker()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 970 che coinvolge il nodo canonico `._pipeline_worker()` è `L313`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 970 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 970 che coinvolge il nodo canonico `._pipeline_worker()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 970 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`. Nel grafo unificato, l'arco 970 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`. Nel grafo unificato, l'arco 970 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_status` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`.
<!-- GRAPH_EDGE graph=unificata index=971 role=outgoing -->
Nel grafo unificato, il nodo canonico `._pipeline_worker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._write_mock_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_write_mock_outputs`. La relazione tecnica esplicita è la seguente: `._pipeline_worker()` chiama `._write_mock_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 971 che coinvolge il nodo canonico `._pipeline_worker()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 971 che coinvolge il nodo canonico `._pipeline_worker()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 971 che coinvolge il nodo canonico `._pipeline_worker()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 971 che coinvolge il nodo canonico `._pipeline_worker()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 971 che coinvolge il nodo canonico `._pipeline_worker()` è `L320`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 971 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 971 che coinvolge il nodo canonico `._pipeline_worker()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 971 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`. Nel grafo unificato, l'arco 971 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`. Nel grafo unificato, l'arco 971 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_write_mock_outputs` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`.
<!-- GRAPH_EDGE graph=unificata index=937 role=incoming -->
Nel grafo unificato, il nodo canonico `._pipeline_worker()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._pipeline_worker()`. Nel grafo unificato, il tipo dichiarato dell'arco 937 che coinvolge il nodo canonico `._pipeline_worker()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 937 che coinvolge il nodo canonico `._pipeline_worker()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 937 che coinvolge il nodo canonico `._pipeline_worker()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 937 che coinvolge il nodo canonico `._pipeline_worker()` è `L303`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 937 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 937 che coinvolge il nodo canonico `._pipeline_worker()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 937 che coinvolge il nodo canonico `._pipeline_worker()` è `1.0`. Nel grafo unificato, l'arco 937 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`. Nel grafo unificato, l'arco 937 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._pipeline_worker()`.

### ._prepare_run()

<!-- GRAPH_NODE graph=codice index=476 id=ui_tabs_run_tab_runtab_prepare_run -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._prepare_run()` è `._prepare_run()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._prepare_run()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._prepare_run()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._prepare_run()` è `L294`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._prepare_run()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._prepare_run()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_prepare_run`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._prepare_run()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._prepare_run()` è `._prepare_run()`.
<!-- GRAPH_EDGE graph=codice index=966 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._prepare_run()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._clear_log()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_prepare_run` all'ID `ui_tabs_run_tab_runtab_clear_log`. La relazione tecnica esplicita è la seguente: `._prepare_run()` chiama `._clear_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 966 che coinvolge il nodo canonico `._prepare_run()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 966 che coinvolge il nodo canonico `._prepare_run()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 966 che coinvolge il nodo canonico `._prepare_run()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 966 che coinvolge il nodo canonico `._prepare_run()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 966 che coinvolge il nodo canonico `._prepare_run()` è `L300`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 966 che coinvolge il nodo canonico `._prepare_run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 966 che coinvolge il nodo canonico `._prepare_run()` è `ast`. Nel grafo del codice RDG, l'arco 966 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_prepare_run` per la relazione che coinvolge il nodo canonico `._prepare_run()`. Nel grafo del codice RDG, l'arco 966 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_clear_log` per la relazione che coinvolge il nodo canonico `._prepare_run()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 966 che coinvolge il nodo canonico `._prepare_run()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=938 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._prepare_run()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_prepare_run`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._prepare_run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 938 che coinvolge il nodo canonico `._prepare_run()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 938 che coinvolge il nodo canonico `._prepare_run()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 938 che coinvolge il nodo canonico `._prepare_run()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 938 che coinvolge il nodo canonico `._prepare_run()` è `L294`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 938 che coinvolge il nodo canonico `._prepare_run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 938 che coinvolge il nodo canonico `._prepare_run()` è `ast`. Nel grafo del codice RDG, l'arco 938 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._prepare_run()`. Nel grafo del codice RDG, l'arco 938 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_prepare_run` per la relazione che coinvolge il nodo canonico `._prepare_run()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 938 che coinvolge il nodo canonico `._prepare_run()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=962 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._prepare_run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.start_pipeline()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_start_pipeline` all'ID `ui_tabs_run_tab_runtab_prepare_run`. La relazione tecnica esplicita è la seguente: `.start_pipeline()` chiama `._prepare_run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 962 che coinvolge il nodo canonico `._prepare_run()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 962 che coinvolge il nodo canonico `._prepare_run()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 962 che coinvolge il nodo canonico `._prepare_run()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 962 che coinvolge il nodo canonico `._prepare_run()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 962 che coinvolge il nodo canonico `._prepare_run()` è `L274`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 962 che coinvolge il nodo canonico `._prepare_run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 962 che coinvolge il nodo canonico `._prepare_run()` è `ast`. Nel grafo del codice RDG, l'arco 962 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_start_pipeline` per la relazione che coinvolge il nodo canonico `._prepare_run()`. Nel grafo del codice RDG, l'arco 962 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_prepare_run` per la relazione che coinvolge il nodo canonico `._prepare_run()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 962 che coinvolge il nodo canonico `._prepare_run()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=963 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._prepare_run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._start_single_step()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_start_single_step` all'ID `ui_tabs_run_tab_runtab_prepare_run`. La relazione tecnica esplicita è la seguente: `._start_single_step()` chiama `._prepare_run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 963 che coinvolge il nodo canonico `._prepare_run()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 963 che coinvolge il nodo canonico `._prepare_run()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 963 che coinvolge il nodo canonico `._prepare_run()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 963 che coinvolge il nodo canonico `._prepare_run()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 963 che coinvolge il nodo canonico `._prepare_run()` è `L280`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 963 che coinvolge il nodo canonico `._prepare_run()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 963 che coinvolge il nodo canonico `._prepare_run()` è `ast`. Nel grafo del codice RDG, l'arco 963 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_start_single_step` per la relazione che coinvolge il nodo canonico `._prepare_run()`. Nel grafo del codice RDG, l'arco 963 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_prepare_run` per la relazione che coinvolge il nodo canonico `._prepare_run()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 963 che coinvolge il nodo canonico `._prepare_run()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=476 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_prepare_run -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._prepare_run()` è `._prepare_run()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._prepare_run()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._prepare_run()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._prepare_run()` è `L294`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._prepare_run()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._prepare_run()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._prepare_run()` è `._prepare_run()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._prepare_run()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._prepare_run()` è `ui_tabs_run_tab_runtab_prepare_run`. Nel grafo unificato, il nodo canonico `._prepare_run()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_prepare_run`.
<!-- GRAPH_EDGE graph=unificata index=966 role=outgoing -->
Nel grafo unificato, il nodo canonico `._prepare_run()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._clear_log()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_prepare_run` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_clear_log`. La relazione tecnica esplicita è la seguente: `._prepare_run()` chiama `._clear_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 966 che coinvolge il nodo canonico `._prepare_run()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 966 che coinvolge il nodo canonico `._prepare_run()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 966 che coinvolge il nodo canonico `._prepare_run()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 966 che coinvolge il nodo canonico `._prepare_run()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 966 che coinvolge il nodo canonico `._prepare_run()` è `L300`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 966 che coinvolge il nodo canonico `._prepare_run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 966 che coinvolge il nodo canonico `._prepare_run()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 966 che coinvolge il nodo canonico `._prepare_run()` è `1.0`. Nel grafo unificato, l'arco 966 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_prepare_run` per la relazione che coinvolge il nodo canonico `._prepare_run()`. Nel grafo unificato, l'arco 966 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_clear_log` per la relazione che coinvolge il nodo canonico `._prepare_run()`.
<!-- GRAPH_EDGE graph=unificata index=938 role=incoming -->
Nel grafo unificato, il nodo canonico `._prepare_run()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_prepare_run`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._prepare_run()`. Nel grafo unificato, il tipo dichiarato dell'arco 938 che coinvolge il nodo canonico `._prepare_run()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 938 che coinvolge il nodo canonico `._prepare_run()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 938 che coinvolge il nodo canonico `._prepare_run()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 938 che coinvolge il nodo canonico `._prepare_run()` è `L294`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 938 che coinvolge il nodo canonico `._prepare_run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 938 che coinvolge il nodo canonico `._prepare_run()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 938 che coinvolge il nodo canonico `._prepare_run()` è `1.0`. Nel grafo unificato, l'arco 938 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._prepare_run()`. Nel grafo unificato, l'arco 938 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_prepare_run` per la relazione che coinvolge il nodo canonico `._prepare_run()`.
<!-- GRAPH_EDGE graph=unificata index=962 role=incoming -->
Nel grafo unificato, il nodo canonico `._prepare_run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.start_pipeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_pipeline` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_prepare_run`. La relazione tecnica esplicita è la seguente: `.start_pipeline()` chiama `._prepare_run()`. Nel grafo unificato, il tipo dichiarato dell'arco 962 che coinvolge il nodo canonico `._prepare_run()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 962 che coinvolge il nodo canonico `._prepare_run()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 962 che coinvolge il nodo canonico `._prepare_run()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 962 che coinvolge il nodo canonico `._prepare_run()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 962 che coinvolge il nodo canonico `._prepare_run()` è `L274`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 962 che coinvolge il nodo canonico `._prepare_run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 962 che coinvolge il nodo canonico `._prepare_run()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 962 che coinvolge il nodo canonico `._prepare_run()` è `1.0`. Nel grafo unificato, l'arco 962 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_pipeline` per la relazione che coinvolge il nodo canonico `._prepare_run()`. Nel grafo unificato, l'arco 962 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_prepare_run` per la relazione che coinvolge il nodo canonico `._prepare_run()`.
<!-- GRAPH_EDGE graph=unificata index=963 role=incoming -->
Nel grafo unificato, il nodo canonico `._prepare_run()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._start_single_step()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_single_step` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_prepare_run`. La relazione tecnica esplicita è la seguente: `._start_single_step()` chiama `._prepare_run()`. Nel grafo unificato, il tipo dichiarato dell'arco 963 che coinvolge il nodo canonico `._prepare_run()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 963 che coinvolge il nodo canonico `._prepare_run()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 963 che coinvolge il nodo canonico `._prepare_run()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 963 che coinvolge il nodo canonico `._prepare_run()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 963 che coinvolge il nodo canonico `._prepare_run()` è `L280`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 963 che coinvolge il nodo canonico `._prepare_run()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 963 che coinvolge il nodo canonico `._prepare_run()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 963 che coinvolge il nodo canonico `._prepare_run()` è `1.0`. Nel grafo unificato, l'arco 963 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_single_step` per la relazione che coinvolge il nodo canonico `._prepare_run()`. Nel grafo unificato, l'arco 963 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_prepare_run` per la relazione che coinvolge il nodo canonico `._prepare_run()`.

### ._run_step_1()

<!-- GRAPH_NODE graph=codice index=479 id=ui_tabs_run_tab_runtab_run_step_1 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._run_step_1()` è `._run_step_1()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._run_step_1()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._run_step_1()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._run_step_1()` è `L346`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._run_step_1()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._run_step_1()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_run_step_1`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._run_step_1()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._run_step_1()` è `._run_step_1()`.
<!-- GRAPH_EDGE graph=codice index=977 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._run_step_1()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_log()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_run_step_1` all'ID `ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._run_step_1()` chiama `._ui_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 977 che coinvolge il nodo canonico `._run_step_1()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 977 che coinvolge il nodo canonico `._run_step_1()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 977 che coinvolge il nodo canonico `._run_step_1()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 977 che coinvolge il nodo canonico `._run_step_1()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 977 che coinvolge il nodo canonico `._run_step_1()` è `L349`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 977 che coinvolge il nodo canonico `._run_step_1()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 977 che coinvolge il nodo canonico `._run_step_1()` è `ast`. Nel grafo del codice RDG, l'arco 977 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_run_step_1` per la relazione che coinvolge il nodo canonico `._run_step_1()`. Nel grafo del codice RDG, l'arco 977 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._run_step_1()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 977 che coinvolge il nodo canonico `._run_step_1()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=978 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._run_step_1()` è la sorgente `source` della relazione `calls` verso il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_run_step_1` all'ID `ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `._run_step_1()` chiama `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 978 che coinvolge il nodo canonico `._run_step_1()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 978 che coinvolge il nodo canonico `._run_step_1()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 978 che coinvolge il nodo canonico `._run_step_1()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 978 che coinvolge il nodo canonico `._run_step_1()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 978 che coinvolge il nodo canonico `._run_step_1()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 978 che coinvolge il nodo canonico `._run_step_1()` è `L347`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 978 che coinvolge il nodo canonico `._run_step_1()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 978 che coinvolge il nodo canonico `._run_step_1()` è `ast`. Nel grafo del codice RDG, l'arco 978 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_run_step_1` per la relazione che coinvolge il nodo canonico `._run_step_1()`. Nel grafo del codice RDG, l'arco 978 registra come `target` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `._run_step_1()`.
<!-- GRAPH_EDGE graph=codice index=940 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._run_step_1()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_run_step_1`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._run_step_1()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 940 che coinvolge il nodo canonico `._run_step_1()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 940 che coinvolge il nodo canonico `._run_step_1()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 940 che coinvolge il nodo canonico `._run_step_1()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 940 che coinvolge il nodo canonico `._run_step_1()` è `L346`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 940 che coinvolge il nodo canonico `._run_step_1()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 940 che coinvolge il nodo canonico `._run_step_1()` è `ast`. Nel grafo del codice RDG, l'arco 940 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._run_step_1()`. Nel grafo del codice RDG, l'arco 940 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_run_step_1` per la relazione che coinvolge il nodo canonico `._run_step_1()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 940 che coinvolge il nodo canonico `._run_step_1()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=479 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_1 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._run_step_1()` è `._run_step_1()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._run_step_1()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._run_step_1()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._run_step_1()` è `L346`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._run_step_1()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._run_step_1()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._run_step_1()` è `._run_step_1()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._run_step_1()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._run_step_1()` è `ui_tabs_run_tab_runtab_run_step_1`. Nel grafo unificato, il nodo canonico `._run_step_1()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_1`.
<!-- GRAPH_EDGE graph=unificata index=977 role=outgoing -->
Nel grafo unificato, il nodo canonico `._run_step_1()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_log()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_1` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._run_step_1()` chiama `._ui_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 977 che coinvolge il nodo canonico `._run_step_1()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 977 che coinvolge il nodo canonico `._run_step_1()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 977 che coinvolge il nodo canonico `._run_step_1()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 977 che coinvolge il nodo canonico `._run_step_1()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 977 che coinvolge il nodo canonico `._run_step_1()` è `L349`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 977 che coinvolge il nodo canonico `._run_step_1()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 977 che coinvolge il nodo canonico `._run_step_1()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 977 che coinvolge il nodo canonico `._run_step_1()` è `1.0`. Nel grafo unificato, l'arco 977 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_1` per la relazione che coinvolge il nodo canonico `._run_step_1()`. Nel grafo unificato, l'arco 977 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._run_step_1()`.
<!-- GRAPH_EDGE graph=unificata index=978 role=outgoing -->
Nel grafo unificato, il nodo canonico `._run_step_1()` è la sorgente `source` della relazione `calls` verso il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_1` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `._run_step_1()` chiama `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 978 che coinvolge il nodo canonico `._run_step_1()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 978 che coinvolge il nodo canonico `._run_step_1()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 978 che coinvolge il nodo canonico `._run_step_1()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 978 che coinvolge il nodo canonico `._run_step_1()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 978 che coinvolge il nodo canonico `._run_step_1()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 978 che coinvolge il nodo canonico `._run_step_1()` è `L347`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 978 che coinvolge il nodo canonico `._run_step_1()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 978 che coinvolge il nodo canonico `._run_step_1()` è `ast`. Nel grafo unificato, l'arco 978 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_1` per la relazione che coinvolge il nodo canonico `._run_step_1()`. Nel grafo unificato, l'arco 978 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `._run_step_1()`.
<!-- GRAPH_EDGE graph=unificata index=940 role=incoming -->
Nel grafo unificato, il nodo canonico `._run_step_1()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_1`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._run_step_1()`. Nel grafo unificato, il tipo dichiarato dell'arco 940 che coinvolge il nodo canonico `._run_step_1()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 940 che coinvolge il nodo canonico `._run_step_1()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 940 che coinvolge il nodo canonico `._run_step_1()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 940 che coinvolge il nodo canonico `._run_step_1()` è `L346`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 940 che coinvolge il nodo canonico `._run_step_1()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 940 che coinvolge il nodo canonico `._run_step_1()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 940 che coinvolge il nodo canonico `._run_step_1()` è `1.0`. Nel grafo unificato, l'arco 940 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._run_step_1()`. Nel grafo unificato, l'arco 940 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_1` per la relazione che coinvolge il nodo canonico `._run_step_1()`.

### ._run_step_2()

<!-- GRAPH_NODE graph=codice index=480 id=ui_tabs_run_tab_runtab_run_step_2 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._run_step_2()` è `._run_step_2()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._run_step_2()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._run_step_2()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._run_step_2()` è `L353`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._run_step_2()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._run_step_2()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_run_step_2`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._run_step_2()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._run_step_2()` è `._run_step_2()`.
<!-- GRAPH_EDGE graph=codice index=979 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._run_step_2()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_log()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_run_step_2` all'ID `ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._run_step_2()` chiama `._ui_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 979 che coinvolge il nodo canonico `._run_step_2()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 979 che coinvolge il nodo canonico `._run_step_2()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 979 che coinvolge il nodo canonico `._run_step_2()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 979 che coinvolge il nodo canonico `._run_step_2()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 979 che coinvolge il nodo canonico `._run_step_2()` è `L356`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 979 che coinvolge il nodo canonico `._run_step_2()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 979 che coinvolge il nodo canonico `._run_step_2()` è `ast`. Nel grafo del codice RDG, l'arco 979 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_run_step_2` per la relazione che coinvolge il nodo canonico `._run_step_2()`. Nel grafo del codice RDG, l'arco 979 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._run_step_2()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 979 che coinvolge il nodo canonico `._run_step_2()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=980 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._run_step_2()` è la sorgente `source` della relazione `calls` verso il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_run_step_2` all'ID `ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `._run_step_2()` chiama `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 980 che coinvolge il nodo canonico `._run_step_2()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 980 che coinvolge il nodo canonico `._run_step_2()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 980 che coinvolge il nodo canonico `._run_step_2()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 980 che coinvolge il nodo canonico `._run_step_2()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 980 che coinvolge il nodo canonico `._run_step_2()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 980 che coinvolge il nodo canonico `._run_step_2()` è `L354`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 980 che coinvolge il nodo canonico `._run_step_2()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 980 che coinvolge il nodo canonico `._run_step_2()` è `ast`. Nel grafo del codice RDG, l'arco 980 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_run_step_2` per la relazione che coinvolge il nodo canonico `._run_step_2()`. Nel grafo del codice RDG, l'arco 980 registra come `target` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `._run_step_2()`.
<!-- GRAPH_EDGE graph=codice index=941 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._run_step_2()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_run_step_2`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._run_step_2()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 941 che coinvolge il nodo canonico `._run_step_2()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 941 che coinvolge il nodo canonico `._run_step_2()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 941 che coinvolge il nodo canonico `._run_step_2()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 941 che coinvolge il nodo canonico `._run_step_2()` è `L353`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 941 che coinvolge il nodo canonico `._run_step_2()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 941 che coinvolge il nodo canonico `._run_step_2()` è `ast`. Nel grafo del codice RDG, l'arco 941 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._run_step_2()`. Nel grafo del codice RDG, l'arco 941 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_run_step_2` per la relazione che coinvolge il nodo canonico `._run_step_2()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 941 che coinvolge il nodo canonico `._run_step_2()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=480 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_2 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._run_step_2()` è `._run_step_2()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._run_step_2()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._run_step_2()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._run_step_2()` è `L353`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._run_step_2()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._run_step_2()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._run_step_2()` è `._run_step_2()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._run_step_2()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._run_step_2()` è `ui_tabs_run_tab_runtab_run_step_2`. Nel grafo unificato, il nodo canonico `._run_step_2()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_2`.
<!-- GRAPH_EDGE graph=unificata index=979 role=outgoing -->
Nel grafo unificato, il nodo canonico `._run_step_2()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_log()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_2` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._run_step_2()` chiama `._ui_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 979 che coinvolge il nodo canonico `._run_step_2()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 979 che coinvolge il nodo canonico `._run_step_2()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 979 che coinvolge il nodo canonico `._run_step_2()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 979 che coinvolge il nodo canonico `._run_step_2()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 979 che coinvolge il nodo canonico `._run_step_2()` è `L356`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 979 che coinvolge il nodo canonico `._run_step_2()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 979 che coinvolge il nodo canonico `._run_step_2()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 979 che coinvolge il nodo canonico `._run_step_2()` è `1.0`. Nel grafo unificato, l'arco 979 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_2` per la relazione che coinvolge il nodo canonico `._run_step_2()`. Nel grafo unificato, l'arco 979 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._run_step_2()`.
<!-- GRAPH_EDGE graph=unificata index=980 role=outgoing -->
Nel grafo unificato, il nodo canonico `._run_step_2()` è la sorgente `source` della relazione `calls` verso il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_2` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `._run_step_2()` chiama `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 980 che coinvolge il nodo canonico `._run_step_2()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 980 che coinvolge il nodo canonico `._run_step_2()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 980 che coinvolge il nodo canonico `._run_step_2()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 980 che coinvolge il nodo canonico `._run_step_2()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 980 che coinvolge il nodo canonico `._run_step_2()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 980 che coinvolge il nodo canonico `._run_step_2()` è `L354`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 980 che coinvolge il nodo canonico `._run_step_2()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 980 che coinvolge il nodo canonico `._run_step_2()` è `ast`. Nel grafo unificato, l'arco 980 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_2` per la relazione che coinvolge il nodo canonico `._run_step_2()`. Nel grafo unificato, l'arco 980 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `._run_step_2()`.
<!-- GRAPH_EDGE graph=unificata index=941 role=incoming -->
Nel grafo unificato, il nodo canonico `._run_step_2()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_2`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._run_step_2()`. Nel grafo unificato, il tipo dichiarato dell'arco 941 che coinvolge il nodo canonico `._run_step_2()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 941 che coinvolge il nodo canonico `._run_step_2()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 941 che coinvolge il nodo canonico `._run_step_2()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 941 che coinvolge il nodo canonico `._run_step_2()` è `L353`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 941 che coinvolge il nodo canonico `._run_step_2()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 941 che coinvolge il nodo canonico `._run_step_2()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 941 che coinvolge il nodo canonico `._run_step_2()` è `1.0`. Nel grafo unificato, l'arco 941 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._run_step_2()`. Nel grafo unificato, l'arco 941 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_2` per la relazione che coinvolge il nodo canonico `._run_step_2()`.

### ._run_step_3()

<!-- GRAPH_NODE graph=codice index=481 id=ui_tabs_run_tab_runtab_run_step_3 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._run_step_3()` è `._run_step_3()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._run_step_3()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._run_step_3()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._run_step_3()` è `L361`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._run_step_3()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._run_step_3()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_run_step_3`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._run_step_3()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._run_step_3()` è `._run_step_3()`.
<!-- GRAPH_EDGE graph=codice index=981 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._run_step_3()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_log()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_run_step_3` all'ID `ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._run_step_3()` chiama `._ui_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 981 che coinvolge il nodo canonico `._run_step_3()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 981 che coinvolge il nodo canonico `._run_step_3()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 981 che coinvolge il nodo canonico `._run_step_3()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 981 che coinvolge il nodo canonico `._run_step_3()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 981 che coinvolge il nodo canonico `._run_step_3()` è `L363`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 981 che coinvolge il nodo canonico `._run_step_3()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 981 che coinvolge il nodo canonico `._run_step_3()` è `ast`. Nel grafo del codice RDG, l'arco 981 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_run_step_3` per la relazione che coinvolge il nodo canonico `._run_step_3()`. Nel grafo del codice RDG, l'arco 981 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._run_step_3()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 981 che coinvolge il nodo canonico `._run_step_3()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=942 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._run_step_3()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_run_step_3`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._run_step_3()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 942 che coinvolge il nodo canonico `._run_step_3()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 942 che coinvolge il nodo canonico `._run_step_3()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 942 che coinvolge il nodo canonico `._run_step_3()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 942 che coinvolge il nodo canonico `._run_step_3()` è `L361`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 942 che coinvolge il nodo canonico `._run_step_3()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 942 che coinvolge il nodo canonico `._run_step_3()` è `ast`. Nel grafo del codice RDG, l'arco 942 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._run_step_3()`. Nel grafo del codice RDG, l'arco 942 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_run_step_3` per la relazione che coinvolge il nodo canonico `._run_step_3()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 942 che coinvolge il nodo canonico `._run_step_3()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=481 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_3 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._run_step_3()` è `._run_step_3()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._run_step_3()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._run_step_3()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._run_step_3()` è `L361`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._run_step_3()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._run_step_3()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._run_step_3()` è `._run_step_3()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._run_step_3()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._run_step_3()` è `ui_tabs_run_tab_runtab_run_step_3`. Nel grafo unificato, il nodo canonico `._run_step_3()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_3`.
<!-- GRAPH_EDGE graph=unificata index=981 role=outgoing -->
Nel grafo unificato, il nodo canonico `._run_step_3()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_log()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_3` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._run_step_3()` chiama `._ui_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 981 che coinvolge il nodo canonico `._run_step_3()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 981 che coinvolge il nodo canonico `._run_step_3()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 981 che coinvolge il nodo canonico `._run_step_3()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 981 che coinvolge il nodo canonico `._run_step_3()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 981 che coinvolge il nodo canonico `._run_step_3()` è `L363`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 981 che coinvolge il nodo canonico `._run_step_3()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 981 che coinvolge il nodo canonico `._run_step_3()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 981 che coinvolge il nodo canonico `._run_step_3()` è `1.0`. Nel grafo unificato, l'arco 981 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_3` per la relazione che coinvolge il nodo canonico `._run_step_3()`. Nel grafo unificato, l'arco 981 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._run_step_3()`.
<!-- GRAPH_EDGE graph=unificata index=942 role=incoming -->
Nel grafo unificato, il nodo canonico `._run_step_3()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_3`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._run_step_3()`. Nel grafo unificato, il tipo dichiarato dell'arco 942 che coinvolge il nodo canonico `._run_step_3()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 942 che coinvolge il nodo canonico `._run_step_3()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 942 che coinvolge il nodo canonico `._run_step_3()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 942 che coinvolge il nodo canonico `._run_step_3()` è `L361`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 942 che coinvolge il nodo canonico `._run_step_3()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 942 che coinvolge il nodo canonico `._run_step_3()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 942 che coinvolge il nodo canonico `._run_step_3()` è `1.0`. Nel grafo unificato, l'arco 942 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._run_step_3()`. Nel grafo unificato, l'arco 942 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_3` per la relazione che coinvolge il nodo canonico `._run_step_3()`.

### ._run_step_4()

<!-- GRAPH_NODE graph=codice index=482 id=ui_tabs_run_tab_runtab_run_step_4 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._run_step_4()` è `._run_step_4()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._run_step_4()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._run_step_4()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._run_step_4()` è `L368`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._run_step_4()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._run_step_4()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_run_step_4`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._run_step_4()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._run_step_4()` è `._run_step_4()`.
<!-- GRAPH_EDGE graph=codice index=982 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._run_step_4()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_log()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_run_step_4` all'ID `ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._run_step_4()` chiama `._ui_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 982 che coinvolge il nodo canonico `._run_step_4()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 982 che coinvolge il nodo canonico `._run_step_4()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 982 che coinvolge il nodo canonico `._run_step_4()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 982 che coinvolge il nodo canonico `._run_step_4()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 982 che coinvolge il nodo canonico `._run_step_4()` è `L370`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 982 che coinvolge il nodo canonico `._run_step_4()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 982 che coinvolge il nodo canonico `._run_step_4()` è `ast`. Nel grafo del codice RDG, l'arco 982 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_run_step_4` per la relazione che coinvolge il nodo canonico `._run_step_4()`. Nel grafo del codice RDG, l'arco 982 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._run_step_4()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 982 che coinvolge il nodo canonico `._run_step_4()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=943 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._run_step_4()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_run_step_4`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._run_step_4()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 943 che coinvolge il nodo canonico `._run_step_4()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 943 che coinvolge il nodo canonico `._run_step_4()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 943 che coinvolge il nodo canonico `._run_step_4()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 943 che coinvolge il nodo canonico `._run_step_4()` è `L368`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 943 che coinvolge il nodo canonico `._run_step_4()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 943 che coinvolge il nodo canonico `._run_step_4()` è `ast`. Nel grafo del codice RDG, l'arco 943 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._run_step_4()`. Nel grafo del codice RDG, l'arco 943 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_run_step_4` per la relazione che coinvolge il nodo canonico `._run_step_4()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 943 che coinvolge il nodo canonico `._run_step_4()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=482 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_4 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._run_step_4()` è `._run_step_4()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._run_step_4()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._run_step_4()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._run_step_4()` è `L368`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._run_step_4()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._run_step_4()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._run_step_4()` è `._run_step_4()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._run_step_4()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._run_step_4()` è `ui_tabs_run_tab_runtab_run_step_4`. Nel grafo unificato, il nodo canonico `._run_step_4()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_4`.
<!-- GRAPH_EDGE graph=unificata index=982 role=outgoing -->
Nel grafo unificato, il nodo canonico `._run_step_4()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_log()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_4` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._run_step_4()` chiama `._ui_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 982 che coinvolge il nodo canonico `._run_step_4()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 982 che coinvolge il nodo canonico `._run_step_4()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 982 che coinvolge il nodo canonico `._run_step_4()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 982 che coinvolge il nodo canonico `._run_step_4()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 982 che coinvolge il nodo canonico `._run_step_4()` è `L370`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 982 che coinvolge il nodo canonico `._run_step_4()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 982 che coinvolge il nodo canonico `._run_step_4()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 982 che coinvolge il nodo canonico `._run_step_4()` è `1.0`. Nel grafo unificato, l'arco 982 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_4` per la relazione che coinvolge il nodo canonico `._run_step_4()`. Nel grafo unificato, l'arco 982 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._run_step_4()`.
<!-- GRAPH_EDGE graph=unificata index=943 role=incoming -->
Nel grafo unificato, il nodo canonico `._run_step_4()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_4`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._run_step_4()`. Nel grafo unificato, il tipo dichiarato dell'arco 943 che coinvolge il nodo canonico `._run_step_4()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 943 che coinvolge il nodo canonico `._run_step_4()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 943 che coinvolge il nodo canonico `._run_step_4()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 943 che coinvolge il nodo canonico `._run_step_4()` è `L368`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 943 che coinvolge il nodo canonico `._run_step_4()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 943 che coinvolge il nodo canonico `._run_step_4()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 943 che coinvolge il nodo canonico `._run_step_4()` è `1.0`. Nel grafo unificato, l'arco 943 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._run_step_4()`. Nel grafo unificato, l'arco 943 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_4` per la relazione che coinvolge il nodo canonico `._run_step_4()`.

### ._set_log_placeholder()

<!-- GRAPH_NODE graph=codice index=490 id=ui_tabs_run_tab_runtab_set_log_placeholder -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._set_log_placeholder()` è `._set_log_placeholder()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._set_log_placeholder()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._set_log_placeholder()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._set_log_placeholder()` è `L438`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._set_log_placeholder()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._set_log_placeholder()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_set_log_placeholder`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._set_log_placeholder()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._set_log_placeholder()` è `._set_log_placeholder()`.
Nel grafo del codice RDG, il nodo canonico `._set_log_placeholder()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=944 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._set_log_placeholder()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_set_log_placeholder`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._set_log_placeholder()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 944 che coinvolge il nodo canonico `._set_log_placeholder()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 944 che coinvolge il nodo canonico `._set_log_placeholder()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 944 che coinvolge il nodo canonico `._set_log_placeholder()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 944 che coinvolge il nodo canonico `._set_log_placeholder()` è `L438`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 944 che coinvolge il nodo canonico `._set_log_placeholder()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 944 che coinvolge il nodo canonico `._set_log_placeholder()` è `ast`. Nel grafo del codice RDG, l'arco 944 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._set_log_placeholder()`. Nel grafo del codice RDG, l'arco 944 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_set_log_placeholder` per la relazione che coinvolge il nodo canonico `._set_log_placeholder()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 944 che coinvolge il nodo canonico `._set_log_placeholder()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=961 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._set_log_placeholder()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build_log()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_build_log` all'ID `ui_tabs_run_tab_runtab_set_log_placeholder`. La relazione tecnica esplicita è la seguente: `._build_log()` chiama `._set_log_placeholder()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 961 che coinvolge il nodo canonico `._set_log_placeholder()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 961 che coinvolge il nodo canonico `._set_log_placeholder()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 961 che coinvolge il nodo canonico `._set_log_placeholder()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 961 che coinvolge il nodo canonico `._set_log_placeholder()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 961 che coinvolge il nodo canonico `._set_log_placeholder()` è `L236`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 961 che coinvolge il nodo canonico `._set_log_placeholder()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 961 che coinvolge il nodo canonico `._set_log_placeholder()` è `ast`. Nel grafo del codice RDG, l'arco 961 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_build_log` per la relazione che coinvolge il nodo canonico `._set_log_placeholder()`. Nel grafo del codice RDG, l'arco 961 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_set_log_placeholder` per la relazione che coinvolge il nodo canonico `._set_log_placeholder()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 961 che coinvolge il nodo canonico `._set_log_placeholder()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=964 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._set_log_placeholder()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.reset()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_reset` all'ID `ui_tabs_run_tab_runtab_set_log_placeholder`. La relazione tecnica esplicita è la seguente: `.reset()` chiama `._set_log_placeholder()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 964 che coinvolge il nodo canonico `._set_log_placeholder()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 964 che coinvolge il nodo canonico `._set_log_placeholder()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 964 che coinvolge il nodo canonico `._set_log_placeholder()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 964 che coinvolge il nodo canonico `._set_log_placeholder()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 964 che coinvolge il nodo canonico `._set_log_placeholder()` è `L292`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 964 che coinvolge il nodo canonico `._set_log_placeholder()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 964 che coinvolge il nodo canonico `._set_log_placeholder()` è `ast`. Nel grafo del codice RDG, l'arco 964 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_reset` per la relazione che coinvolge il nodo canonico `._set_log_placeholder()`. Nel grafo del codice RDG, l'arco 964 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_set_log_placeholder` per la relazione che coinvolge il nodo canonico `._set_log_placeholder()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 964 che coinvolge il nodo canonico `._set_log_placeholder()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=490 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_log_placeholder -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._set_log_placeholder()` è `._set_log_placeholder()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._set_log_placeholder()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._set_log_placeholder()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._set_log_placeholder()` è `L438`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._set_log_placeholder()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._set_log_placeholder()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._set_log_placeholder()` è `._set_log_placeholder()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._set_log_placeholder()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._set_log_placeholder()` è `ui_tabs_run_tab_runtab_set_log_placeholder`. Nel grafo unificato, il nodo canonico `._set_log_placeholder()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_log_placeholder`.
Nel grafo unificato, il nodo canonico `._set_log_placeholder()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=944 role=incoming -->
Nel grafo unificato, il nodo canonico `._set_log_placeholder()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_log_placeholder`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._set_log_placeholder()`. Nel grafo unificato, il tipo dichiarato dell'arco 944 che coinvolge il nodo canonico `._set_log_placeholder()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 944 che coinvolge il nodo canonico `._set_log_placeholder()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 944 che coinvolge il nodo canonico `._set_log_placeholder()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 944 che coinvolge il nodo canonico `._set_log_placeholder()` è `L438`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 944 che coinvolge il nodo canonico `._set_log_placeholder()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 944 che coinvolge il nodo canonico `._set_log_placeholder()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 944 che coinvolge il nodo canonico `._set_log_placeholder()` è `1.0`. Nel grafo unificato, l'arco 944 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._set_log_placeholder()`. Nel grafo unificato, l'arco 944 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_log_placeholder` per la relazione che coinvolge il nodo canonico `._set_log_placeholder()`.
<!-- GRAPH_EDGE graph=unificata index=961 role=incoming -->
Nel grafo unificato, il nodo canonico `._set_log_placeholder()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build_log()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_log` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_log_placeholder`. La relazione tecnica esplicita è la seguente: `._build_log()` chiama `._set_log_placeholder()`. Nel grafo unificato, il tipo dichiarato dell'arco 961 che coinvolge il nodo canonico `._set_log_placeholder()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 961 che coinvolge il nodo canonico `._set_log_placeholder()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 961 che coinvolge il nodo canonico `._set_log_placeholder()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 961 che coinvolge il nodo canonico `._set_log_placeholder()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 961 che coinvolge il nodo canonico `._set_log_placeholder()` è `L236`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 961 che coinvolge il nodo canonico `._set_log_placeholder()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 961 che coinvolge il nodo canonico `._set_log_placeholder()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 961 che coinvolge il nodo canonico `._set_log_placeholder()` è `1.0`. Nel grafo unificato, l'arco 961 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_log` per la relazione che coinvolge il nodo canonico `._set_log_placeholder()`. Nel grafo unificato, l'arco 961 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_log_placeholder` per la relazione che coinvolge il nodo canonico `._set_log_placeholder()`.
<!-- GRAPH_EDGE graph=unificata index=964 role=incoming -->
Nel grafo unificato, il nodo canonico `._set_log_placeholder()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.reset()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_reset` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_log_placeholder`. La relazione tecnica esplicita è la seguente: `.reset()` chiama `._set_log_placeholder()`. Nel grafo unificato, il tipo dichiarato dell'arco 964 che coinvolge il nodo canonico `._set_log_placeholder()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 964 che coinvolge il nodo canonico `._set_log_placeholder()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 964 che coinvolge il nodo canonico `._set_log_placeholder()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 964 che coinvolge il nodo canonico `._set_log_placeholder()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 964 che coinvolge il nodo canonico `._set_log_placeholder()` è `L292`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 964 che coinvolge il nodo canonico `._set_log_placeholder()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 964 che coinvolge il nodo canonico `._set_log_placeholder()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 964 che coinvolge il nodo canonico `._set_log_placeholder()` è `1.0`. Nel grafo unificato, l'arco 964 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_reset` per la relazione che coinvolge il nodo canonico `._set_log_placeholder()`. Nel grafo unificato, l'arco 964 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_log_placeholder` per la relazione che coinvolge il nodo canonico `._set_log_placeholder()`.

### ._set_status_now()

<!-- GRAPH_NODE graph=codice index=487 id=ui_tabs_run_tab_runtab_set_status_now -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._set_status_now()` è `._set_status_now()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._set_status_now()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._set_status_now()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._set_status_now()` è `L410`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._set_status_now()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._set_status_now()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_set_status_now`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._set_status_now()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._set_status_now()` è `._set_status_now()`.
Nel grafo del codice RDG, il nodo canonico `._set_status_now()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=945 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._set_status_now()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_set_status_now`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._set_status_now()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 945 che coinvolge il nodo canonico `._set_status_now()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 945 che coinvolge il nodo canonico `._set_status_now()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 945 che coinvolge il nodo canonico `._set_status_now()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 945 che coinvolge il nodo canonico `._set_status_now()` è `L410`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 945 che coinvolge il nodo canonico `._set_status_now()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 945 che coinvolge il nodo canonico `._set_status_now()` è `ast`. Nel grafo del codice RDG, l'arco 945 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._set_status_now()`. Nel grafo del codice RDG, l'arco 945 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_set_status_now` per la relazione che coinvolge il nodo canonico `._set_status_now()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 945 che coinvolge il nodo canonico `._set_status_now()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=965 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._set_status_now()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.reset()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_reset` all'ID `ui_tabs_run_tab_runtab_set_status_now`. La relazione tecnica esplicita è la seguente: `.reset()` chiama `._set_status_now()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 965 che coinvolge il nodo canonico `._set_status_now()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 965 che coinvolge il nodo canonico `._set_status_now()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 965 che coinvolge il nodo canonico `._set_status_now()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 965 che coinvolge il nodo canonico `._set_status_now()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 965 che coinvolge il nodo canonico `._set_status_now()` è `L287`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 965 che coinvolge il nodo canonico `._set_status_now()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 965 che coinvolge il nodo canonico `._set_status_now()` è `ast`. Nel grafo del codice RDG, l'arco 965 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_reset` per la relazione che coinvolge il nodo canonico `._set_status_now()`. Nel grafo del codice RDG, l'arco 965 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_set_status_now` per la relazione che coinvolge il nodo canonico `._set_status_now()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 965 che coinvolge il nodo canonico `._set_status_now()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=984 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._set_status_now()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._ui_set_status()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_ui_set_status` all'ID `ui_tabs_run_tab_runtab_set_status_now`. La relazione tecnica esplicita è la seguente: `._ui_set_status()` chiama `._set_status_now()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 984 che coinvolge il nodo canonico `._set_status_now()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 984 che coinvolge il nodo canonico `._set_status_now()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 984 che coinvolge il nodo canonico `._set_status_now()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 984 che coinvolge il nodo canonico `._set_status_now()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 984 che coinvolge il nodo canonico `._set_status_now()` è `L389`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 984 che coinvolge il nodo canonico `._set_status_now()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 984 che coinvolge il nodo canonico `._set_status_now()` è `ast`. Nel grafo del codice RDG, l'arco 984 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_ui_set_status` per la relazione che coinvolge il nodo canonico `._set_status_now()`. Nel grafo del codice RDG, l'arco 984 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_set_status_now` per la relazione che coinvolge il nodo canonico `._set_status_now()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 984 che coinvolge il nodo canonico `._set_status_now()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=487 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_status_now -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._set_status_now()` è `._set_status_now()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._set_status_now()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._set_status_now()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._set_status_now()` è `L410`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._set_status_now()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._set_status_now()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._set_status_now()` è `._set_status_now()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._set_status_now()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._set_status_now()` è `ui_tabs_run_tab_runtab_set_status_now`. Nel grafo unificato, il nodo canonico `._set_status_now()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_status_now`.
Nel grafo unificato, il nodo canonico `._set_status_now()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=945 role=incoming -->
Nel grafo unificato, il nodo canonico `._set_status_now()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_status_now`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._set_status_now()`. Nel grafo unificato, il tipo dichiarato dell'arco 945 che coinvolge il nodo canonico `._set_status_now()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 945 che coinvolge il nodo canonico `._set_status_now()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 945 che coinvolge il nodo canonico `._set_status_now()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 945 che coinvolge il nodo canonico `._set_status_now()` è `L410`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 945 che coinvolge il nodo canonico `._set_status_now()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 945 che coinvolge il nodo canonico `._set_status_now()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 945 che coinvolge il nodo canonico `._set_status_now()` è `1.0`. Nel grafo unificato, l'arco 945 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._set_status_now()`. Nel grafo unificato, l'arco 945 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_status_now` per la relazione che coinvolge il nodo canonico `._set_status_now()`.
<!-- GRAPH_EDGE graph=unificata index=965 role=incoming -->
Nel grafo unificato, il nodo canonico `._set_status_now()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.reset()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_reset` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_status_now`. La relazione tecnica esplicita è la seguente: `.reset()` chiama `._set_status_now()`. Nel grafo unificato, il tipo dichiarato dell'arco 965 che coinvolge il nodo canonico `._set_status_now()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 965 che coinvolge il nodo canonico `._set_status_now()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 965 che coinvolge il nodo canonico `._set_status_now()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 965 che coinvolge il nodo canonico `._set_status_now()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 965 che coinvolge il nodo canonico `._set_status_now()` è `L287`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 965 che coinvolge il nodo canonico `._set_status_now()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 965 che coinvolge il nodo canonico `._set_status_now()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 965 che coinvolge il nodo canonico `._set_status_now()` è `1.0`. Nel grafo unificato, l'arco 965 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_reset` per la relazione che coinvolge il nodo canonico `._set_status_now()`. Nel grafo unificato, l'arco 965 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_status_now` per la relazione che coinvolge il nodo canonico `._set_status_now()`.
<!-- GRAPH_EDGE graph=unificata index=984 role=incoming -->
Nel grafo unificato, il nodo canonico `._set_status_now()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._ui_set_status()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_status` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_status_now`. La relazione tecnica esplicita è la seguente: `._ui_set_status()` chiama `._set_status_now()`. Nel grafo unificato, il tipo dichiarato dell'arco 984 che coinvolge il nodo canonico `._set_status_now()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 984 che coinvolge il nodo canonico `._set_status_now()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 984 che coinvolge il nodo canonico `._set_status_now()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 984 che coinvolge il nodo canonico `._set_status_now()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 984 che coinvolge il nodo canonico `._set_status_now()` è `L389`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 984 che coinvolge il nodo canonico `._set_status_now()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 984 che coinvolge il nodo canonico `._set_status_now()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 984 che coinvolge il nodo canonico `._set_status_now()` è `1.0`. Nel grafo unificato, l'arco 984 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_status` per la relazione che coinvolge il nodo canonico `._set_status_now()`. Nel grafo unificato, l'arco 984 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_status_now` per la relazione che coinvolge il nodo canonico `._set_status_now()`.

### ._single_step_worker()

<!-- GRAPH_NODE graph=codice index=478 id=ui_tabs_run_tab_runtab_single_step_worker -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._single_step_worker()` è `._single_step_worker()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._single_step_worker()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._single_step_worker()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._single_step_worker()` è `L327`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._single_step_worker()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._single_step_worker()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_single_step_worker`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._single_step_worker()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._single_step_worker()` è `._single_step_worker()`.
<!-- GRAPH_EDGE graph=codice index=972 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._single_step_worker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_finish()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_single_step_worker` all'ID `ui_tabs_run_tab_runtab_ui_finish`. La relazione tecnica esplicita è la seguente: `._single_step_worker()` chiama `._ui_finish()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 972 che coinvolge il nodo canonico `._single_step_worker()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 972 che coinvolge il nodo canonico `._single_step_worker()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 972 che coinvolge il nodo canonico `._single_step_worker()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 972 che coinvolge il nodo canonico `._single_step_worker()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 972 che coinvolge il nodo canonico `._single_step_worker()` è `L343`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 972 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 972 che coinvolge il nodo canonico `._single_step_worker()` è `ast`. Nel grafo del codice RDG, l'arco 972 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._single_step_worker()`. Nel grafo del codice RDG, l'arco 972 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_finish` per la relazione che coinvolge il nodo canonico `._single_step_worker()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 972 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=973 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._single_step_worker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_log()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_single_step_worker` all'ID `ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._single_step_worker()` chiama `._ui_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 973 che coinvolge il nodo canonico `._single_step_worker()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 973 che coinvolge il nodo canonico `._single_step_worker()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 973 che coinvolge il nodo canonico `._single_step_worker()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 973 che coinvolge il nodo canonico `._single_step_worker()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 973 che coinvolge il nodo canonico `._single_step_worker()` è `L333`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 973 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 973 che coinvolge il nodo canonico `._single_step_worker()` è `ast`. Nel grafo del codice RDG, l'arco 973 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._single_step_worker()`. Nel grafo del codice RDG, l'arco 973 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._single_step_worker()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 973 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=974 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._single_step_worker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_set_progress()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_single_step_worker` all'ID `ui_tabs_run_tab_runtab_ui_set_progress`. La relazione tecnica esplicita è la seguente: `._single_step_worker()` chiama `._ui_set_progress()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 974 che coinvolge il nodo canonico `._single_step_worker()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 974 che coinvolge il nodo canonico `._single_step_worker()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 974 che coinvolge il nodo canonico `._single_step_worker()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 974 che coinvolge il nodo canonico `._single_step_worker()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 974 che coinvolge il nodo canonico `._single_step_worker()` è `L332`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 974 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 974 che coinvolge il nodo canonico `._single_step_worker()` è `ast`. Nel grafo del codice RDG, l'arco 974 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._single_step_worker()`. Nel grafo del codice RDG, l'arco 974 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_set_progress` per la relazione che coinvolge il nodo canonico `._single_step_worker()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 974 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=975 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._single_step_worker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_set_status()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_single_step_worker` all'ID `ui_tabs_run_tab_runtab_ui_set_status`. La relazione tecnica esplicita è la seguente: `._single_step_worker()` chiama `._ui_set_status()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 975 che coinvolge il nodo canonico `._single_step_worker()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 975 che coinvolge il nodo canonico `._single_step_worker()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 975 che coinvolge il nodo canonico `._single_step_worker()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 975 che coinvolge il nodo canonico `._single_step_worker()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 975 che coinvolge il nodo canonico `._single_step_worker()` è `L331`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 975 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 975 che coinvolge il nodo canonico `._single_step_worker()` è `ast`. Nel grafo del codice RDG, l'arco 975 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._single_step_worker()`. Nel grafo del codice RDG, l'arco 975 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_set_status` per la relazione che coinvolge il nodo canonico `._single_step_worker()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 975 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=976 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._single_step_worker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._write_mock_outputs()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_single_step_worker` all'ID `ui_tabs_run_tab_runtab_write_mock_outputs`. La relazione tecnica esplicita è la seguente: `._single_step_worker()` chiama `._write_mock_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 976 che coinvolge il nodo canonico `._single_step_worker()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 976 che coinvolge il nodo canonico `._single_step_worker()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 976 che coinvolge il nodo canonico `._single_step_worker()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 976 che coinvolge il nodo canonico `._single_step_worker()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 976 che coinvolge il nodo canonico `._single_step_worker()` è `L339`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 976 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 976 che coinvolge il nodo canonico `._single_step_worker()` è `ast`. Nel grafo del codice RDG, l'arco 976 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._single_step_worker()`. Nel grafo del codice RDG, l'arco 976 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_write_mock_outputs` per la relazione che coinvolge il nodo canonico `._single_step_worker()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 976 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=946 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._single_step_worker()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_single_step_worker`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._single_step_worker()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 946 che coinvolge il nodo canonico `._single_step_worker()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 946 che coinvolge il nodo canonico `._single_step_worker()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 946 che coinvolge il nodo canonico `._single_step_worker()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 946 che coinvolge il nodo canonico `._single_step_worker()` è `L327`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 946 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 946 che coinvolge il nodo canonico `._single_step_worker()` è `ast`. Nel grafo del codice RDG, l'arco 946 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._single_step_worker()`. Nel grafo del codice RDG, l'arco 946 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._single_step_worker()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 946 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=478 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._single_step_worker()` è `._single_step_worker()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._single_step_worker()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._single_step_worker()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._single_step_worker()` è `L327`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._single_step_worker()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._single_step_worker()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._single_step_worker()` è `._single_step_worker()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._single_step_worker()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._single_step_worker()` è `ui_tabs_run_tab_runtab_single_step_worker`. Nel grafo unificato, il nodo canonico `._single_step_worker()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker`.
<!-- GRAPH_EDGE graph=unificata index=972 role=outgoing -->
Nel grafo unificato, il nodo canonico `._single_step_worker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_finish()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_finish`. La relazione tecnica esplicita è la seguente: `._single_step_worker()` chiama `._ui_finish()`. Nel grafo unificato, il tipo dichiarato dell'arco 972 che coinvolge il nodo canonico `._single_step_worker()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 972 che coinvolge il nodo canonico `._single_step_worker()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 972 che coinvolge il nodo canonico `._single_step_worker()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 972 che coinvolge il nodo canonico `._single_step_worker()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 972 che coinvolge il nodo canonico `._single_step_worker()` è `L343`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 972 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 972 che coinvolge il nodo canonico `._single_step_worker()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 972 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`. Nel grafo unificato, l'arco 972 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._single_step_worker()`. Nel grafo unificato, l'arco 972 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_finish` per la relazione che coinvolge il nodo canonico `._single_step_worker()`.
<!-- GRAPH_EDGE graph=unificata index=973 role=outgoing -->
Nel grafo unificato, il nodo canonico `._single_step_worker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_log()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._single_step_worker()` chiama `._ui_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 973 che coinvolge il nodo canonico `._single_step_worker()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 973 che coinvolge il nodo canonico `._single_step_worker()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 973 che coinvolge il nodo canonico `._single_step_worker()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 973 che coinvolge il nodo canonico `._single_step_worker()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 973 che coinvolge il nodo canonico `._single_step_worker()` è `L333`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 973 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 973 che coinvolge il nodo canonico `._single_step_worker()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 973 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`. Nel grafo unificato, l'arco 973 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._single_step_worker()`. Nel grafo unificato, l'arco 973 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._single_step_worker()`.
<!-- GRAPH_EDGE graph=unificata index=974 role=outgoing -->
Nel grafo unificato, il nodo canonico `._single_step_worker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_set_progress()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_progress`. La relazione tecnica esplicita è la seguente: `._single_step_worker()` chiama `._ui_set_progress()`. Nel grafo unificato, il tipo dichiarato dell'arco 974 che coinvolge il nodo canonico `._single_step_worker()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 974 che coinvolge il nodo canonico `._single_step_worker()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 974 che coinvolge il nodo canonico `._single_step_worker()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 974 che coinvolge il nodo canonico `._single_step_worker()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 974 che coinvolge il nodo canonico `._single_step_worker()` è `L332`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 974 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 974 che coinvolge il nodo canonico `._single_step_worker()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 974 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`. Nel grafo unificato, l'arco 974 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._single_step_worker()`. Nel grafo unificato, l'arco 974 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_progress` per la relazione che coinvolge il nodo canonico `._single_step_worker()`.
<!-- GRAPH_EDGE graph=unificata index=975 role=outgoing -->
Nel grafo unificato, il nodo canonico `._single_step_worker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._ui_set_status()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_status`. La relazione tecnica esplicita è la seguente: `._single_step_worker()` chiama `._ui_set_status()`. Nel grafo unificato, il tipo dichiarato dell'arco 975 che coinvolge il nodo canonico `._single_step_worker()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 975 che coinvolge il nodo canonico `._single_step_worker()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 975 che coinvolge il nodo canonico `._single_step_worker()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 975 che coinvolge il nodo canonico `._single_step_worker()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 975 che coinvolge il nodo canonico `._single_step_worker()` è `L331`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 975 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 975 che coinvolge il nodo canonico `._single_step_worker()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 975 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`. Nel grafo unificato, l'arco 975 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._single_step_worker()`. Nel grafo unificato, l'arco 975 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_status` per la relazione che coinvolge il nodo canonico `._single_step_worker()`.
<!-- GRAPH_EDGE graph=unificata index=976 role=outgoing -->
Nel grafo unificato, il nodo canonico `._single_step_worker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._write_mock_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_write_mock_outputs`. La relazione tecnica esplicita è la seguente: `._single_step_worker()` chiama `._write_mock_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 976 che coinvolge il nodo canonico `._single_step_worker()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 976 che coinvolge il nodo canonico `._single_step_worker()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 976 che coinvolge il nodo canonico `._single_step_worker()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 976 che coinvolge il nodo canonico `._single_step_worker()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 976 che coinvolge il nodo canonico `._single_step_worker()` è `L339`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 976 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 976 che coinvolge il nodo canonico `._single_step_worker()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 976 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`. Nel grafo unificato, l'arco 976 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._single_step_worker()`. Nel grafo unificato, l'arco 976 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_write_mock_outputs` per la relazione che coinvolge il nodo canonico `._single_step_worker()`.
<!-- GRAPH_EDGE graph=unificata index=946 role=incoming -->
Nel grafo unificato, il nodo canonico `._single_step_worker()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._single_step_worker()`. Nel grafo unificato, il tipo dichiarato dell'arco 946 che coinvolge il nodo canonico `._single_step_worker()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 946 che coinvolge il nodo canonico `._single_step_worker()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 946 che coinvolge il nodo canonico `._single_step_worker()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 946 che coinvolge il nodo canonico `._single_step_worker()` è `L327`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 946 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 946 che coinvolge il nodo canonico `._single_step_worker()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 946 che coinvolge il nodo canonico `._single_step_worker()` è `1.0`. Nel grafo unificato, l'arco 946 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._single_step_worker()`. Nel grafo unificato, l'arco 946 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._single_step_worker()`.

### ._start_single_step()

<!-- GRAPH_NODE graph=codice index=474 id=ui_tabs_run_tab_runtab_start_single_step -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._start_single_step()` è `._start_single_step()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._start_single_step()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._start_single_step()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._start_single_step()` è `L277`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._start_single_step()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._start_single_step()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_start_single_step`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._start_single_step()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._start_single_step()` è `._start_single_step()`.
<!-- GRAPH_EDGE graph=codice index=963 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._start_single_step()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._prepare_run()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_start_single_step` all'ID `ui_tabs_run_tab_runtab_prepare_run`. La relazione tecnica esplicita è la seguente: `._start_single_step()` chiama `._prepare_run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 963 che coinvolge il nodo canonico `._start_single_step()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 963 che coinvolge il nodo canonico `._start_single_step()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 963 che coinvolge il nodo canonico `._start_single_step()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 963 che coinvolge il nodo canonico `._start_single_step()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 963 che coinvolge il nodo canonico `._start_single_step()` è `L280`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 963 che coinvolge il nodo canonico `._start_single_step()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 963 che coinvolge il nodo canonico `._start_single_step()` è `ast`. Nel grafo del codice RDG, l'arco 963 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_start_single_step` per la relazione che coinvolge il nodo canonico `._start_single_step()`. Nel grafo del codice RDG, l'arco 963 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_prepare_run` per la relazione che coinvolge il nodo canonico `._start_single_step()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 963 che coinvolge il nodo canonico `._start_single_step()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=948 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._start_single_step()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_start_single_step`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._start_single_step()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 948 che coinvolge il nodo canonico `._start_single_step()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 948 che coinvolge il nodo canonico `._start_single_step()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 948 che coinvolge il nodo canonico `._start_single_step()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 948 che coinvolge il nodo canonico `._start_single_step()` è `L277`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 948 che coinvolge il nodo canonico `._start_single_step()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 948 che coinvolge il nodo canonico `._start_single_step()` è `ast`. Nel grafo del codice RDG, l'arco 948 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._start_single_step()`. Nel grafo del codice RDG, l'arco 948 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_start_single_step` per la relazione che coinvolge il nodo canonico `._start_single_step()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 948 che coinvolge il nodo canonico `._start_single_step()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=959 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._start_single_step()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build_step_card()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_build_step_card` all'ID `ui_tabs_run_tab_runtab_start_single_step`. La relazione tecnica esplicita è la seguente: `._build_step_card()` chiama `._start_single_step()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 959 che coinvolge il nodo canonico `._start_single_step()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 959 che coinvolge il nodo canonico `._start_single_step()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 959 che coinvolge il nodo canonico `._start_single_step()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 959 che coinvolge il nodo canonico `._start_single_step()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 959 che coinvolge il nodo canonico `._start_single_step()` è `L174`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 959 che coinvolge il nodo canonico `._start_single_step()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 959 che coinvolge il nodo canonico `._start_single_step()` è `ast`. Nel grafo del codice RDG, l'arco 959 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_build_step_card` per la relazione che coinvolge il nodo canonico `._start_single_step()`. Nel grafo del codice RDG, l'arco 959 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_start_single_step` per la relazione che coinvolge il nodo canonico `._start_single_step()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 959 che coinvolge il nodo canonico `._start_single_step()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=474 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_single_step -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._start_single_step()` è `._start_single_step()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._start_single_step()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._start_single_step()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._start_single_step()` è `L277`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._start_single_step()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._start_single_step()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._start_single_step()` è `._start_single_step()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._start_single_step()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._start_single_step()` è `ui_tabs_run_tab_runtab_start_single_step`. Nel grafo unificato, il nodo canonico `._start_single_step()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_single_step`.
<!-- GRAPH_EDGE graph=unificata index=963 role=outgoing -->
Nel grafo unificato, il nodo canonico `._start_single_step()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._prepare_run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_single_step` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_prepare_run`. La relazione tecnica esplicita è la seguente: `._start_single_step()` chiama `._prepare_run()`. Nel grafo unificato, il tipo dichiarato dell'arco 963 che coinvolge il nodo canonico `._start_single_step()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 963 che coinvolge il nodo canonico `._start_single_step()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 963 che coinvolge il nodo canonico `._start_single_step()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 963 che coinvolge il nodo canonico `._start_single_step()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 963 che coinvolge il nodo canonico `._start_single_step()` è `L280`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 963 che coinvolge il nodo canonico `._start_single_step()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 963 che coinvolge il nodo canonico `._start_single_step()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 963 che coinvolge il nodo canonico `._start_single_step()` è `1.0`. Nel grafo unificato, l'arco 963 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_single_step` per la relazione che coinvolge il nodo canonico `._start_single_step()`. Nel grafo unificato, l'arco 963 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_prepare_run` per la relazione che coinvolge il nodo canonico `._start_single_step()`.
<!-- GRAPH_EDGE graph=unificata index=948 role=incoming -->
Nel grafo unificato, il nodo canonico `._start_single_step()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_single_step`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._start_single_step()`. Nel grafo unificato, il tipo dichiarato dell'arco 948 che coinvolge il nodo canonico `._start_single_step()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 948 che coinvolge il nodo canonico `._start_single_step()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 948 che coinvolge il nodo canonico `._start_single_step()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 948 che coinvolge il nodo canonico `._start_single_step()` è `L277`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 948 che coinvolge il nodo canonico `._start_single_step()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 948 che coinvolge il nodo canonico `._start_single_step()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 948 che coinvolge il nodo canonico `._start_single_step()` è `1.0`. Nel grafo unificato, l'arco 948 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._start_single_step()`. Nel grafo unificato, l'arco 948 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_single_step` per la relazione che coinvolge il nodo canonico `._start_single_step()`.
<!-- GRAPH_EDGE graph=unificata index=959 role=incoming -->
Nel grafo unificato, il nodo canonico `._start_single_step()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build_step_card()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_step_card` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_single_step`. La relazione tecnica esplicita è la seguente: `._build_step_card()` chiama `._start_single_step()`. Nel grafo unificato, il tipo dichiarato dell'arco 959 che coinvolge il nodo canonico `._start_single_step()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 959 che coinvolge il nodo canonico `._start_single_step()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 959 che coinvolge il nodo canonico `._start_single_step()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 959 che coinvolge il nodo canonico `._start_single_step()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 959 che coinvolge il nodo canonico `._start_single_step()` è `L174`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 959 che coinvolge il nodo canonico `._start_single_step()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 959 che coinvolge il nodo canonico `._start_single_step()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 959 che coinvolge il nodo canonico `._start_single_step()` è `1.0`. Nel grafo unificato, l'arco 959 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_step_card` per la relazione che coinvolge il nodo canonico `._start_single_step()`. Nel grafo unificato, l'arco 959 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_single_step` per la relazione che coinvolge il nodo canonico `._start_single_step()`.

### ._ui_finish()

<!-- GRAPH_NODE graph=codice index=486 id=ui_tabs_run_tab_runtab_ui_finish -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._ui_finish()` è `._ui_finish()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._ui_finish()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._ui_finish()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._ui_finish()` è `L400`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._ui_finish()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._ui_finish()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_ui_finish`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._ui_finish()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._ui_finish()` è `._ui_finish()`.
Nel grafo del codice RDG, il nodo canonico `._ui_finish()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=949 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._ui_finish()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_ui_finish`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._ui_finish()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 949 che coinvolge il nodo canonico `._ui_finish()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 949 che coinvolge il nodo canonico `._ui_finish()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 949 che coinvolge il nodo canonico `._ui_finish()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 949 che coinvolge il nodo canonico `._ui_finish()` è `L400`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 949 che coinvolge il nodo canonico `._ui_finish()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 949 che coinvolge il nodo canonico `._ui_finish()` è `ast`. Nel grafo del codice RDG, l'arco 949 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._ui_finish()`. Nel grafo del codice RDG, l'arco 949 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_finish` per la relazione che coinvolge il nodo canonico `._ui_finish()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 949 che coinvolge il nodo canonico `._ui_finish()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=967 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._ui_finish()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._pipeline_worker()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_pipeline_worker` all'ID `ui_tabs_run_tab_runtab_ui_finish`. La relazione tecnica esplicita è la seguente: `._pipeline_worker()` chiama `._ui_finish()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 967 che coinvolge il nodo canonico `._ui_finish()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 967 che coinvolge il nodo canonico `._ui_finish()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 967 che coinvolge il nodo canonico `._ui_finish()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 967 che coinvolge il nodo canonico `._ui_finish()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 967 che coinvolge il nodo canonico `._ui_finish()` è `L325`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 967 che coinvolge il nodo canonico `._ui_finish()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 967 che coinvolge il nodo canonico `._ui_finish()` è `ast`. Nel grafo del codice RDG, l'arco 967 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._ui_finish()`. Nel grafo del codice RDG, l'arco 967 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_finish` per la relazione che coinvolge il nodo canonico `._ui_finish()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 967 che coinvolge il nodo canonico `._ui_finish()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=972 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._ui_finish()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._single_step_worker()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_single_step_worker` all'ID `ui_tabs_run_tab_runtab_ui_finish`. La relazione tecnica esplicita è la seguente: `._single_step_worker()` chiama `._ui_finish()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 972 che coinvolge il nodo canonico `._ui_finish()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 972 che coinvolge il nodo canonico `._ui_finish()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 972 che coinvolge il nodo canonico `._ui_finish()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 972 che coinvolge il nodo canonico `._ui_finish()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 972 che coinvolge il nodo canonico `._ui_finish()` è `L343`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 972 che coinvolge il nodo canonico `._ui_finish()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 972 che coinvolge il nodo canonico `._ui_finish()` è `ast`. Nel grafo del codice RDG, l'arco 972 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._ui_finish()`. Nel grafo del codice RDG, l'arco 972 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_finish` per la relazione che coinvolge il nodo canonico `._ui_finish()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 972 che coinvolge il nodo canonico `._ui_finish()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=486 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_finish -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._ui_finish()` è `._ui_finish()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._ui_finish()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._ui_finish()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._ui_finish()` è `L400`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._ui_finish()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._ui_finish()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._ui_finish()` è `._ui_finish()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._ui_finish()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._ui_finish()` è `ui_tabs_run_tab_runtab_ui_finish`. Nel grafo unificato, il nodo canonico `._ui_finish()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_finish`.
Nel grafo unificato, il nodo canonico `._ui_finish()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=949 role=incoming -->
Nel grafo unificato, il nodo canonico `._ui_finish()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_finish`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._ui_finish()`. Nel grafo unificato, il tipo dichiarato dell'arco 949 che coinvolge il nodo canonico `._ui_finish()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 949 che coinvolge il nodo canonico `._ui_finish()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 949 che coinvolge il nodo canonico `._ui_finish()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 949 che coinvolge il nodo canonico `._ui_finish()` è `L400`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 949 che coinvolge il nodo canonico `._ui_finish()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 949 che coinvolge il nodo canonico `._ui_finish()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 949 che coinvolge il nodo canonico `._ui_finish()` è `1.0`. Nel grafo unificato, l'arco 949 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._ui_finish()`. Nel grafo unificato, l'arco 949 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_finish` per la relazione che coinvolge il nodo canonico `._ui_finish()`.
<!-- GRAPH_EDGE graph=unificata index=967 role=incoming -->
Nel grafo unificato, il nodo canonico `._ui_finish()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._pipeline_worker()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_finish`. La relazione tecnica esplicita è la seguente: `._pipeline_worker()` chiama `._ui_finish()`. Nel grafo unificato, il tipo dichiarato dell'arco 967 che coinvolge il nodo canonico `._ui_finish()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 967 che coinvolge il nodo canonico `._ui_finish()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 967 che coinvolge il nodo canonico `._ui_finish()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 967 che coinvolge il nodo canonico `._ui_finish()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 967 che coinvolge il nodo canonico `._ui_finish()` è `L325`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 967 che coinvolge il nodo canonico `._ui_finish()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 967 che coinvolge il nodo canonico `._ui_finish()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 967 che coinvolge il nodo canonico `._ui_finish()` è `1.0`. Nel grafo unificato, l'arco 967 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._ui_finish()`. Nel grafo unificato, l'arco 967 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_finish` per la relazione che coinvolge il nodo canonico `._ui_finish()`.
<!-- GRAPH_EDGE graph=unificata index=972 role=incoming -->
Nel grafo unificato, il nodo canonico `._ui_finish()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._single_step_worker()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_finish`. La relazione tecnica esplicita è la seguente: `._single_step_worker()` chiama `._ui_finish()`. Nel grafo unificato, il tipo dichiarato dell'arco 972 che coinvolge il nodo canonico `._ui_finish()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 972 che coinvolge il nodo canonico `._ui_finish()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 972 che coinvolge il nodo canonico `._ui_finish()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 972 che coinvolge il nodo canonico `._ui_finish()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 972 che coinvolge il nodo canonico `._ui_finish()` è `L343`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 972 che coinvolge il nodo canonico `._ui_finish()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 972 che coinvolge il nodo canonico `._ui_finish()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 972 che coinvolge il nodo canonico `._ui_finish()` è `1.0`. Nel grafo unificato, l'arco 972 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._ui_finish()`. Nel grafo unificato, l'arco 972 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_finish` per la relazione che coinvolge il nodo canonico `._ui_finish()`.

### ._ui_log()

<!-- GRAPH_NODE graph=codice index=485 id=ui_tabs_run_tab_runtab_ui_log -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._ui_log()` è `._ui_log()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._ui_log()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._ui_log()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._ui_log()` è `L391`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._ui_log()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._ui_log()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_ui_log`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._ui_log()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._ui_log()` è `._ui_log()`.
Nel grafo del codice RDG, il nodo canonico `._ui_log()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=950 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._ui_log()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._ui_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 950 che coinvolge il nodo canonico `._ui_log()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 950 che coinvolge il nodo canonico `._ui_log()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 950 che coinvolge il nodo canonico `._ui_log()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 950 che coinvolge il nodo canonico `._ui_log()` è `L391`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 950 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 950 che coinvolge il nodo canonico `._ui_log()` è `ast`. Nel grafo del codice RDG, l'arco 950 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo del codice RDG, l'arco 950 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 950 che coinvolge il nodo canonico `._ui_log()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=968 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._ui_log()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._pipeline_worker()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_pipeline_worker` all'ID `ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._pipeline_worker()` chiama `._ui_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 968 che coinvolge il nodo canonico `._ui_log()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 968 che coinvolge il nodo canonico `._ui_log()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 968 che coinvolge il nodo canonico `._ui_log()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 968 che coinvolge il nodo canonico `._ui_log()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 968 che coinvolge il nodo canonico `._ui_log()` è `L315`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 968 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 968 che coinvolge il nodo canonico `._ui_log()` è `ast`. Nel grafo del codice RDG, l'arco 968 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo del codice RDG, l'arco 968 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 968 che coinvolge il nodo canonico `._ui_log()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=973 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._ui_log()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._single_step_worker()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_single_step_worker` all'ID `ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._single_step_worker()` chiama `._ui_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 973 che coinvolge il nodo canonico `._ui_log()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 973 che coinvolge il nodo canonico `._ui_log()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 973 che coinvolge il nodo canonico `._ui_log()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 973 che coinvolge il nodo canonico `._ui_log()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 973 che coinvolge il nodo canonico `._ui_log()` è `L333`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 973 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 973 che coinvolge il nodo canonico `._ui_log()` è `ast`. Nel grafo del codice RDG, l'arco 973 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo del codice RDG, l'arco 973 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 973 che coinvolge il nodo canonico `._ui_log()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=977 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._ui_log()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._run_step_1()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_run_step_1` all'ID `ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._run_step_1()` chiama `._ui_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 977 che coinvolge il nodo canonico `._ui_log()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 977 che coinvolge il nodo canonico `._ui_log()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 977 che coinvolge il nodo canonico `._ui_log()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 977 che coinvolge il nodo canonico `._ui_log()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 977 che coinvolge il nodo canonico `._ui_log()` è `L349`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 977 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 977 che coinvolge il nodo canonico `._ui_log()` è `ast`. Nel grafo del codice RDG, l'arco 977 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_run_step_1` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo del codice RDG, l'arco 977 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 977 che coinvolge il nodo canonico `._ui_log()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=979 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._ui_log()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._run_step_2()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_run_step_2` all'ID `ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._run_step_2()` chiama `._ui_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 979 che coinvolge il nodo canonico `._ui_log()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 979 che coinvolge il nodo canonico `._ui_log()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 979 che coinvolge il nodo canonico `._ui_log()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 979 che coinvolge il nodo canonico `._ui_log()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 979 che coinvolge il nodo canonico `._ui_log()` è `L356`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 979 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 979 che coinvolge il nodo canonico `._ui_log()` è `ast`. Nel grafo del codice RDG, l'arco 979 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_run_step_2` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo del codice RDG, l'arco 979 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 979 che coinvolge il nodo canonico `._ui_log()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=981 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._ui_log()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._run_step_3()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_run_step_3` all'ID `ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._run_step_3()` chiama `._ui_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 981 che coinvolge il nodo canonico `._ui_log()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 981 che coinvolge il nodo canonico `._ui_log()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 981 che coinvolge il nodo canonico `._ui_log()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 981 che coinvolge il nodo canonico `._ui_log()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 981 che coinvolge il nodo canonico `._ui_log()` è `L363`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 981 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 981 che coinvolge il nodo canonico `._ui_log()` è `ast`. Nel grafo del codice RDG, l'arco 981 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_run_step_3` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo del codice RDG, l'arco 981 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 981 che coinvolge il nodo canonico `._ui_log()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=982 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._ui_log()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._run_step_4()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_run_step_4` all'ID `ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._run_step_4()` chiama `._ui_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 982 che coinvolge il nodo canonico `._ui_log()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 982 che coinvolge il nodo canonico `._ui_log()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 982 che coinvolge il nodo canonico `._ui_log()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 982 che coinvolge il nodo canonico `._ui_log()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 982 che coinvolge il nodo canonico `._ui_log()` è `L370`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 982 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 982 che coinvolge il nodo canonico `._ui_log()` è `ast`. Nel grafo del codice RDG, l'arco 982 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_run_step_4` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo del codice RDG, l'arco 982 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 982 che coinvolge il nodo canonico `._ui_log()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=485 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._ui_log()` è `._ui_log()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._ui_log()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._ui_log()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._ui_log()` è `L391`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._ui_log()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._ui_log()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._ui_log()` è `._ui_log()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._ui_log()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._ui_log()` è `ui_tabs_run_tab_runtab_ui_log`. Nel grafo unificato, il nodo canonico `._ui_log()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log`.
Nel grafo unificato, il nodo canonico `._ui_log()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=950 role=incoming -->
Nel grafo unificato, il nodo canonico `._ui_log()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._ui_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 950 che coinvolge il nodo canonico `._ui_log()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 950 che coinvolge il nodo canonico `._ui_log()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 950 che coinvolge il nodo canonico `._ui_log()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 950 che coinvolge il nodo canonico `._ui_log()` è `L391`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 950 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 950 che coinvolge il nodo canonico `._ui_log()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 950 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo unificato, l'arco 950 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo unificato, l'arco 950 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._ui_log()`.
<!-- GRAPH_EDGE graph=unificata index=968 role=incoming -->
Nel grafo unificato, il nodo canonico `._ui_log()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._pipeline_worker()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._pipeline_worker()` chiama `._ui_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 968 che coinvolge il nodo canonico `._ui_log()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 968 che coinvolge il nodo canonico `._ui_log()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 968 che coinvolge il nodo canonico `._ui_log()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 968 che coinvolge il nodo canonico `._ui_log()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 968 che coinvolge il nodo canonico `._ui_log()` è `L315`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 968 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 968 che coinvolge il nodo canonico `._ui_log()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 968 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo unificato, l'arco 968 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo unificato, l'arco 968 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._ui_log()`.
<!-- GRAPH_EDGE graph=unificata index=973 role=incoming -->
Nel grafo unificato, il nodo canonico `._ui_log()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._single_step_worker()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._single_step_worker()` chiama `._ui_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 973 che coinvolge il nodo canonico `._ui_log()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 973 che coinvolge il nodo canonico `._ui_log()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 973 che coinvolge il nodo canonico `._ui_log()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 973 che coinvolge il nodo canonico `._ui_log()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 973 che coinvolge il nodo canonico `._ui_log()` è `L333`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 973 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 973 che coinvolge il nodo canonico `._ui_log()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 973 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo unificato, l'arco 973 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo unificato, l'arco 973 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._ui_log()`.
<!-- GRAPH_EDGE graph=unificata index=977 role=incoming -->
Nel grafo unificato, il nodo canonico `._ui_log()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._run_step_1()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_1` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._run_step_1()` chiama `._ui_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 977 che coinvolge il nodo canonico `._ui_log()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 977 che coinvolge il nodo canonico `._ui_log()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 977 che coinvolge il nodo canonico `._ui_log()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 977 che coinvolge il nodo canonico `._ui_log()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 977 che coinvolge il nodo canonico `._ui_log()` è `L349`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 977 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 977 che coinvolge il nodo canonico `._ui_log()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 977 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo unificato, l'arco 977 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_1` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo unificato, l'arco 977 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._ui_log()`.
<!-- GRAPH_EDGE graph=unificata index=979 role=incoming -->
Nel grafo unificato, il nodo canonico `._ui_log()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._run_step_2()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_2` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._run_step_2()` chiama `._ui_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 979 che coinvolge il nodo canonico `._ui_log()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 979 che coinvolge il nodo canonico `._ui_log()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 979 che coinvolge il nodo canonico `._ui_log()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 979 che coinvolge il nodo canonico `._ui_log()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 979 che coinvolge il nodo canonico `._ui_log()` è `L356`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 979 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 979 che coinvolge il nodo canonico `._ui_log()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 979 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo unificato, l'arco 979 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_2` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo unificato, l'arco 979 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._ui_log()`.
<!-- GRAPH_EDGE graph=unificata index=981 role=incoming -->
Nel grafo unificato, il nodo canonico `._ui_log()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._run_step_3()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_3` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._run_step_3()` chiama `._ui_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 981 che coinvolge il nodo canonico `._ui_log()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 981 che coinvolge il nodo canonico `._ui_log()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 981 che coinvolge il nodo canonico `._ui_log()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 981 che coinvolge il nodo canonico `._ui_log()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 981 che coinvolge il nodo canonico `._ui_log()` è `L363`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 981 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 981 che coinvolge il nodo canonico `._ui_log()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 981 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo unificato, l'arco 981 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_3` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo unificato, l'arco 981 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._ui_log()`.
<!-- GRAPH_EDGE graph=unificata index=982 role=incoming -->
Nel grafo unificato, il nodo canonico `._ui_log()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._run_step_4()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_4` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `._run_step_4()` chiama `._ui_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 982 che coinvolge il nodo canonico `._ui_log()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 982 che coinvolge il nodo canonico `._ui_log()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 982 che coinvolge il nodo canonico `._ui_log()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 982 che coinvolge il nodo canonico `._ui_log()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 982 che coinvolge il nodo canonico `._ui_log()` è `L370`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 982 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 982 che coinvolge il nodo canonico `._ui_log()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 982 che coinvolge il nodo canonico `._ui_log()` è `1.0`. Nel grafo unificato, l'arco 982 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_4` per la relazione che coinvolge il nodo canonico `._ui_log()`. Nel grafo unificato, l'arco 982 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `._ui_log()`.

### ._ui_set_progress()

<!-- GRAPH_NODE graph=codice index=483 id=ui_tabs_run_tab_runtab_ui_set_progress -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._ui_set_progress()` è `._ui_set_progress()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._ui_set_progress()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._ui_set_progress()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._ui_set_progress()` è `L376`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._ui_set_progress()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._ui_set_progress()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_ui_set_progress`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._ui_set_progress()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._ui_set_progress()` è `._ui_set_progress()`.
Nel grafo del codice RDG, il nodo canonico `._ui_set_progress()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=951 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._ui_set_progress()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_ui_set_progress`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._ui_set_progress()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 951 che coinvolge il nodo canonico `._ui_set_progress()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 951 che coinvolge il nodo canonico `._ui_set_progress()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 951 che coinvolge il nodo canonico `._ui_set_progress()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 951 che coinvolge il nodo canonico `._ui_set_progress()` è `L376`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 951 che coinvolge il nodo canonico `._ui_set_progress()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 951 che coinvolge il nodo canonico `._ui_set_progress()` è `ast`. Nel grafo del codice RDG, l'arco 951 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._ui_set_progress()`. Nel grafo del codice RDG, l'arco 951 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_set_progress` per la relazione che coinvolge il nodo canonico `._ui_set_progress()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 951 che coinvolge il nodo canonico `._ui_set_progress()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=969 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._ui_set_progress()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._pipeline_worker()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_pipeline_worker` all'ID `ui_tabs_run_tab_runtab_ui_set_progress`. La relazione tecnica esplicita è la seguente: `._pipeline_worker()` chiama `._ui_set_progress()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 969 che coinvolge il nodo canonico `._ui_set_progress()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 969 che coinvolge il nodo canonico `._ui_set_progress()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 969 che coinvolge il nodo canonico `._ui_set_progress()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 969 che coinvolge il nodo canonico `._ui_set_progress()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 969 che coinvolge il nodo canonico `._ui_set_progress()` è `L314`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 969 che coinvolge il nodo canonico `._ui_set_progress()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 969 che coinvolge il nodo canonico `._ui_set_progress()` è `ast`. Nel grafo del codice RDG, l'arco 969 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._ui_set_progress()`. Nel grafo del codice RDG, l'arco 969 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_set_progress` per la relazione che coinvolge il nodo canonico `._ui_set_progress()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 969 che coinvolge il nodo canonico `._ui_set_progress()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=974 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._ui_set_progress()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._single_step_worker()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_single_step_worker` all'ID `ui_tabs_run_tab_runtab_ui_set_progress`. La relazione tecnica esplicita è la seguente: `._single_step_worker()` chiama `._ui_set_progress()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 974 che coinvolge il nodo canonico `._ui_set_progress()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 974 che coinvolge il nodo canonico `._ui_set_progress()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 974 che coinvolge il nodo canonico `._ui_set_progress()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 974 che coinvolge il nodo canonico `._ui_set_progress()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 974 che coinvolge il nodo canonico `._ui_set_progress()` è `L332`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 974 che coinvolge il nodo canonico `._ui_set_progress()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 974 che coinvolge il nodo canonico `._ui_set_progress()` è `ast`. Nel grafo del codice RDG, l'arco 974 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._ui_set_progress()`. Nel grafo del codice RDG, l'arco 974 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_set_progress` per la relazione che coinvolge il nodo canonico `._ui_set_progress()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 974 che coinvolge il nodo canonico `._ui_set_progress()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=983 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._ui_set_progress()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_rationale_377` all'ID `ui_tabs_run_tab_runtab_ui_set_progress`. La relazione tecnica esplicita è la seguente: `Accoda l'aggiornamento progress nel main thread Tk.` costituisce una motivazione per `._ui_set_progress()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 983 che coinvolge il nodo canonico `._ui_set_progress()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 983 che coinvolge il nodo canonico `._ui_set_progress()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 983 che coinvolge il nodo canonico `._ui_set_progress()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 983 che coinvolge il nodo canonico `._ui_set_progress()` è `L377`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 983 che coinvolge il nodo canonico `._ui_set_progress()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 983 che coinvolge il nodo canonico `._ui_set_progress()` è `ast`. Nel grafo del codice RDG, l'arco 983 registra come `source` l'identificatore `ui_tabs_run_tab_rationale_377` per la relazione che coinvolge il nodo canonico `._ui_set_progress()`. Nel grafo del codice RDG, l'arco 983 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_set_progress` per la relazione che coinvolge il nodo canonico `._ui_set_progress()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 983 che coinvolge il nodo canonico `._ui_set_progress()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=483 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_progress -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._ui_set_progress()` è `._ui_set_progress()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._ui_set_progress()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._ui_set_progress()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._ui_set_progress()` è `L376`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._ui_set_progress()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._ui_set_progress()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._ui_set_progress()` è `._ui_set_progress()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._ui_set_progress()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._ui_set_progress()` è `ui_tabs_run_tab_runtab_ui_set_progress`. Nel grafo unificato, il nodo canonico `._ui_set_progress()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_progress`.
<!-- GRAPH_EDGE graph=unificata index=983 role=outgoing -->
Nel grafo unificato, il nodo canonico `._ui_set_progress()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_progress` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_rationale_377`. La relazione tecnica esplicita è la seguente: `._ui_set_progress()` costituisce una motivazione per `Accoda l'aggiornamento progress nel main thread Tk.`. Nel grafo unificato, il tipo dichiarato dell'arco 983 che coinvolge il nodo canonico `._ui_set_progress()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 983 che coinvolge il nodo canonico `._ui_set_progress()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 983 che coinvolge il nodo canonico `._ui_set_progress()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 983 che coinvolge il nodo canonico `._ui_set_progress()` è `L377`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 983 che coinvolge il nodo canonico `._ui_set_progress()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 983 che coinvolge il nodo canonico `._ui_set_progress()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 983 che coinvolge il nodo canonico `._ui_set_progress()` è `1.0`. Nel grafo unificato, l'arco 983 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_progress` per la relazione che coinvolge il nodo canonico `._ui_set_progress()`. Nel grafo unificato, l'arco 983 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_rationale_377` per la relazione che coinvolge il nodo canonico `._ui_set_progress()`.
<!-- GRAPH_EDGE graph=unificata index=951 role=incoming -->
Nel grafo unificato, il nodo canonico `._ui_set_progress()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_progress`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._ui_set_progress()`. Nel grafo unificato, il tipo dichiarato dell'arco 951 che coinvolge il nodo canonico `._ui_set_progress()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 951 che coinvolge il nodo canonico `._ui_set_progress()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 951 che coinvolge il nodo canonico `._ui_set_progress()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 951 che coinvolge il nodo canonico `._ui_set_progress()` è `L376`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 951 che coinvolge il nodo canonico `._ui_set_progress()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 951 che coinvolge il nodo canonico `._ui_set_progress()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 951 che coinvolge il nodo canonico `._ui_set_progress()` è `1.0`. Nel grafo unificato, l'arco 951 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._ui_set_progress()`. Nel grafo unificato, l'arco 951 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_progress` per la relazione che coinvolge il nodo canonico `._ui_set_progress()`.
<!-- GRAPH_EDGE graph=unificata index=969 role=incoming -->
Nel grafo unificato, il nodo canonico `._ui_set_progress()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._pipeline_worker()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_progress`. La relazione tecnica esplicita è la seguente: `._pipeline_worker()` chiama `._ui_set_progress()`. Nel grafo unificato, il tipo dichiarato dell'arco 969 che coinvolge il nodo canonico `._ui_set_progress()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 969 che coinvolge il nodo canonico `._ui_set_progress()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 969 che coinvolge il nodo canonico `._ui_set_progress()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 969 che coinvolge il nodo canonico `._ui_set_progress()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 969 che coinvolge il nodo canonico `._ui_set_progress()` è `L314`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 969 che coinvolge il nodo canonico `._ui_set_progress()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 969 che coinvolge il nodo canonico `._ui_set_progress()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 969 che coinvolge il nodo canonico `._ui_set_progress()` è `1.0`. Nel grafo unificato, l'arco 969 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._ui_set_progress()`. Nel grafo unificato, l'arco 969 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_progress` per la relazione che coinvolge il nodo canonico `._ui_set_progress()`.
<!-- GRAPH_EDGE graph=unificata index=974 role=incoming -->
Nel grafo unificato, il nodo canonico `._ui_set_progress()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._single_step_worker()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_progress`. La relazione tecnica esplicita è la seguente: `._single_step_worker()` chiama `._ui_set_progress()`. Nel grafo unificato, il tipo dichiarato dell'arco 974 che coinvolge il nodo canonico `._ui_set_progress()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 974 che coinvolge il nodo canonico `._ui_set_progress()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 974 che coinvolge il nodo canonico `._ui_set_progress()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 974 che coinvolge il nodo canonico `._ui_set_progress()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 974 che coinvolge il nodo canonico `._ui_set_progress()` è `L332`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 974 che coinvolge il nodo canonico `._ui_set_progress()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 974 che coinvolge il nodo canonico `._ui_set_progress()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 974 che coinvolge il nodo canonico `._ui_set_progress()` è `1.0`. Nel grafo unificato, l'arco 974 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._ui_set_progress()`. Nel grafo unificato, l'arco 974 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_progress` per la relazione che coinvolge il nodo canonico `._ui_set_progress()`.

### ._ui_set_status()

<!-- GRAPH_NODE graph=codice index=484 id=ui_tabs_run_tab_runtab_ui_set_status -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._ui_set_status()` è `._ui_set_status()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._ui_set_status()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._ui_set_status()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._ui_set_status()` è `L388`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._ui_set_status()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._ui_set_status()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_ui_set_status`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._ui_set_status()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._ui_set_status()` è `._ui_set_status()`.
<!-- GRAPH_EDGE graph=codice index=984 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._ui_set_status()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._set_status_now()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_ui_set_status` all'ID `ui_tabs_run_tab_runtab_set_status_now`. La relazione tecnica esplicita è la seguente: `._ui_set_status()` chiama `._set_status_now()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 984 che coinvolge il nodo canonico `._ui_set_status()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 984 che coinvolge il nodo canonico `._ui_set_status()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 984 che coinvolge il nodo canonico `._ui_set_status()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 984 che coinvolge il nodo canonico `._ui_set_status()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 984 che coinvolge il nodo canonico `._ui_set_status()` è `L389`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 984 che coinvolge il nodo canonico `._ui_set_status()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 984 che coinvolge il nodo canonico `._ui_set_status()` è `ast`. Nel grafo del codice RDG, l'arco 984 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_ui_set_status` per la relazione che coinvolge il nodo canonico `._ui_set_status()`. Nel grafo del codice RDG, l'arco 984 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_set_status_now` per la relazione che coinvolge il nodo canonico `._ui_set_status()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 984 che coinvolge il nodo canonico `._ui_set_status()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=952 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._ui_set_status()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_ui_set_status`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._ui_set_status()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 952 che coinvolge il nodo canonico `._ui_set_status()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 952 che coinvolge il nodo canonico `._ui_set_status()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 952 che coinvolge il nodo canonico `._ui_set_status()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 952 che coinvolge il nodo canonico `._ui_set_status()` è `L388`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 952 che coinvolge il nodo canonico `._ui_set_status()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 952 che coinvolge il nodo canonico `._ui_set_status()` è `ast`. Nel grafo del codice RDG, l'arco 952 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._ui_set_status()`. Nel grafo del codice RDG, l'arco 952 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_set_status` per la relazione che coinvolge il nodo canonico `._ui_set_status()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 952 che coinvolge il nodo canonico `._ui_set_status()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=970 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._ui_set_status()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._pipeline_worker()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_pipeline_worker` all'ID `ui_tabs_run_tab_runtab_ui_set_status`. La relazione tecnica esplicita è la seguente: `._pipeline_worker()` chiama `._ui_set_status()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 970 che coinvolge il nodo canonico `._ui_set_status()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 970 che coinvolge il nodo canonico `._ui_set_status()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 970 che coinvolge il nodo canonico `._ui_set_status()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 970 che coinvolge il nodo canonico `._ui_set_status()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 970 che coinvolge il nodo canonico `._ui_set_status()` è `L313`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 970 che coinvolge il nodo canonico `._ui_set_status()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 970 che coinvolge il nodo canonico `._ui_set_status()` è `ast`. Nel grafo del codice RDG, l'arco 970 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._ui_set_status()`. Nel grafo del codice RDG, l'arco 970 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_set_status` per la relazione che coinvolge il nodo canonico `._ui_set_status()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 970 che coinvolge il nodo canonico `._ui_set_status()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=975 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._ui_set_status()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._single_step_worker()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_single_step_worker` all'ID `ui_tabs_run_tab_runtab_ui_set_status`. La relazione tecnica esplicita è la seguente: `._single_step_worker()` chiama `._ui_set_status()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 975 che coinvolge il nodo canonico `._ui_set_status()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 975 che coinvolge il nodo canonico `._ui_set_status()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 975 che coinvolge il nodo canonico `._ui_set_status()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 975 che coinvolge il nodo canonico `._ui_set_status()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 975 che coinvolge il nodo canonico `._ui_set_status()` è `L331`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 975 che coinvolge il nodo canonico `._ui_set_status()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 975 che coinvolge il nodo canonico `._ui_set_status()` è `ast`. Nel grafo del codice RDG, l'arco 975 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._ui_set_status()`. Nel grafo del codice RDG, l'arco 975 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_set_status` per la relazione che coinvolge il nodo canonico `._ui_set_status()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 975 che coinvolge il nodo canonico `._ui_set_status()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=484 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_status -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._ui_set_status()` è `._ui_set_status()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._ui_set_status()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._ui_set_status()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._ui_set_status()` è `L388`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._ui_set_status()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._ui_set_status()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._ui_set_status()` è `._ui_set_status()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._ui_set_status()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._ui_set_status()` è `ui_tabs_run_tab_runtab_ui_set_status`. Nel grafo unificato, il nodo canonico `._ui_set_status()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_status`.
<!-- GRAPH_EDGE graph=unificata index=984 role=outgoing -->
Nel grafo unificato, il nodo canonico `._ui_set_status()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._set_status_now()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_status` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_status_now`. La relazione tecnica esplicita è la seguente: `._ui_set_status()` chiama `._set_status_now()`. Nel grafo unificato, il tipo dichiarato dell'arco 984 che coinvolge il nodo canonico `._ui_set_status()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 984 che coinvolge il nodo canonico `._ui_set_status()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 984 che coinvolge il nodo canonico `._ui_set_status()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 984 che coinvolge il nodo canonico `._ui_set_status()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 984 che coinvolge il nodo canonico `._ui_set_status()` è `L389`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 984 che coinvolge il nodo canonico `._ui_set_status()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 984 che coinvolge il nodo canonico `._ui_set_status()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 984 che coinvolge il nodo canonico `._ui_set_status()` è `1.0`. Nel grafo unificato, l'arco 984 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_status` per la relazione che coinvolge il nodo canonico `._ui_set_status()`. Nel grafo unificato, l'arco 984 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_status_now` per la relazione che coinvolge il nodo canonico `._ui_set_status()`.
<!-- GRAPH_EDGE graph=unificata index=952 role=incoming -->
Nel grafo unificato, il nodo canonico `._ui_set_status()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_status`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._ui_set_status()`. Nel grafo unificato, il tipo dichiarato dell'arco 952 che coinvolge il nodo canonico `._ui_set_status()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 952 che coinvolge il nodo canonico `._ui_set_status()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 952 che coinvolge il nodo canonico `._ui_set_status()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 952 che coinvolge il nodo canonico `._ui_set_status()` è `L388`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 952 che coinvolge il nodo canonico `._ui_set_status()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 952 che coinvolge il nodo canonico `._ui_set_status()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 952 che coinvolge il nodo canonico `._ui_set_status()` è `1.0`. Nel grafo unificato, l'arco 952 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._ui_set_status()`. Nel grafo unificato, l'arco 952 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_status` per la relazione che coinvolge il nodo canonico `._ui_set_status()`.
<!-- GRAPH_EDGE graph=unificata index=970 role=incoming -->
Nel grafo unificato, il nodo canonico `._ui_set_status()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._pipeline_worker()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_status`. La relazione tecnica esplicita è la seguente: `._pipeline_worker()` chiama `._ui_set_status()`. Nel grafo unificato, il tipo dichiarato dell'arco 970 che coinvolge il nodo canonico `._ui_set_status()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 970 che coinvolge il nodo canonico `._ui_set_status()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 970 che coinvolge il nodo canonico `._ui_set_status()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 970 che coinvolge il nodo canonico `._ui_set_status()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 970 che coinvolge il nodo canonico `._ui_set_status()` è `L313`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 970 che coinvolge il nodo canonico `._ui_set_status()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 970 che coinvolge il nodo canonico `._ui_set_status()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 970 che coinvolge il nodo canonico `._ui_set_status()` è `1.0`. Nel grafo unificato, l'arco 970 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._ui_set_status()`. Nel grafo unificato, l'arco 970 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_status` per la relazione che coinvolge il nodo canonico `._ui_set_status()`.
<!-- GRAPH_EDGE graph=unificata index=975 role=incoming -->
Nel grafo unificato, il nodo canonico `._ui_set_status()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._single_step_worker()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_status`. La relazione tecnica esplicita è la seguente: `._single_step_worker()` chiama `._ui_set_status()`. Nel grafo unificato, il tipo dichiarato dell'arco 975 che coinvolge il nodo canonico `._ui_set_status()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 975 che coinvolge il nodo canonico `._ui_set_status()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 975 che coinvolge il nodo canonico `._ui_set_status()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 975 che coinvolge il nodo canonico `._ui_set_status()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 975 che coinvolge il nodo canonico `._ui_set_status()` è `L331`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 975 che coinvolge il nodo canonico `._ui_set_status()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 975 che coinvolge il nodo canonico `._ui_set_status()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 975 che coinvolge il nodo canonico `._ui_set_status()` è `1.0`. Nel grafo unificato, l'arco 975 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._ui_set_status()`. Nel grafo unificato, l'arco 975 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_status` per la relazione che coinvolge il nodo canonico `._ui_set_status()`.

### ._write_mock_outputs()

<!-- GRAPH_NODE graph=codice index=491 id=ui_tabs_run_tab_runtab_write_mock_outputs -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._write_mock_outputs()` è `._write_mock_outputs()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._write_mock_outputs()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._write_mock_outputs()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._write_mock_outputs()` è `L444`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._write_mock_outputs()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._write_mock_outputs()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_write_mock_outputs`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._write_mock_outputs()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._write_mock_outputs()` è `._write_mock_outputs()`.
<!-- GRAPH_EDGE graph=codice index=985 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._write_mock_outputs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_write_mock_outputs` all'ID `ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `._write_mock_outputs()` chiama `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 985 che coinvolge il nodo canonico `._write_mock_outputs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 985 che coinvolge il nodo canonico `._write_mock_outputs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 985 che coinvolge il nodo canonico `._write_mock_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 985 che coinvolge il nodo canonico `._write_mock_outputs()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 985 che coinvolge il nodo canonico `._write_mock_outputs()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 985 che coinvolge il nodo canonico `._write_mock_outputs()` è `L445`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 985 che coinvolge il nodo canonico `._write_mock_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 985 che coinvolge il nodo canonico `._write_mock_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 985 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_write_mock_outputs` per la relazione che coinvolge il nodo canonico `._write_mock_outputs()`. Nel grafo del codice RDG, l'arco 985 registra come `target` l'identificatore `ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `._write_mock_outputs()`.
<!-- GRAPH_EDGE graph=codice index=953 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._write_mock_outputs()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_write_mock_outputs`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._write_mock_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 953 che coinvolge il nodo canonico `._write_mock_outputs()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 953 che coinvolge il nodo canonico `._write_mock_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 953 che coinvolge il nodo canonico `._write_mock_outputs()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 953 che coinvolge il nodo canonico `._write_mock_outputs()` è `L444`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 953 che coinvolge il nodo canonico `._write_mock_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 953 che coinvolge il nodo canonico `._write_mock_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 953 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._write_mock_outputs()`. Nel grafo del codice RDG, l'arco 953 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_write_mock_outputs` per la relazione che coinvolge il nodo canonico `._write_mock_outputs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 953 che coinvolge il nodo canonico `._write_mock_outputs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=971 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._write_mock_outputs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._pipeline_worker()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_pipeline_worker` all'ID `ui_tabs_run_tab_runtab_write_mock_outputs`. La relazione tecnica esplicita è la seguente: `._pipeline_worker()` chiama `._write_mock_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 971 che coinvolge il nodo canonico `._write_mock_outputs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 971 che coinvolge il nodo canonico `._write_mock_outputs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 971 che coinvolge il nodo canonico `._write_mock_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 971 che coinvolge il nodo canonico `._write_mock_outputs()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 971 che coinvolge il nodo canonico `._write_mock_outputs()` è `L320`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 971 che coinvolge il nodo canonico `._write_mock_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 971 che coinvolge il nodo canonico `._write_mock_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 971 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._write_mock_outputs()`. Nel grafo del codice RDG, l'arco 971 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_write_mock_outputs` per la relazione che coinvolge il nodo canonico `._write_mock_outputs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 971 che coinvolge il nodo canonico `._write_mock_outputs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=976 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._write_mock_outputs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._single_step_worker()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_single_step_worker` all'ID `ui_tabs_run_tab_runtab_write_mock_outputs`. La relazione tecnica esplicita è la seguente: `._single_step_worker()` chiama `._write_mock_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 976 che coinvolge il nodo canonico `._write_mock_outputs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 976 che coinvolge il nodo canonico `._write_mock_outputs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 976 che coinvolge il nodo canonico `._write_mock_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 976 che coinvolge il nodo canonico `._write_mock_outputs()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 976 che coinvolge il nodo canonico `._write_mock_outputs()` è `L339`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 976 che coinvolge il nodo canonico `._write_mock_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 976 che coinvolge il nodo canonico `._write_mock_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 976 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._write_mock_outputs()`. Nel grafo del codice RDG, l'arco 976 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_write_mock_outputs` per la relazione che coinvolge il nodo canonico `._write_mock_outputs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 976 che coinvolge il nodo canonico `._write_mock_outputs()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=491 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_write_mock_outputs -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._write_mock_outputs()` è `._write_mock_outputs()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._write_mock_outputs()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._write_mock_outputs()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._write_mock_outputs()` è `L444`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._write_mock_outputs()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._write_mock_outputs()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._write_mock_outputs()` è `._write_mock_outputs()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._write_mock_outputs()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._write_mock_outputs()` è `ui_tabs_run_tab_runtab_write_mock_outputs`. Nel grafo unificato, il nodo canonico `._write_mock_outputs()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_write_mock_outputs`.
<!-- GRAPH_EDGE graph=unificata index=985 role=outgoing -->
Nel grafo unificato, il nodo canonico `._write_mock_outputs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `load_config() [ui_ui_config_bridge_load_config]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_write_mock_outputs` all'ID `Request-Dataset-Generator::ui_ui_config_bridge_load_config`. La relazione tecnica esplicita è la seguente: `._write_mock_outputs()` chiama `load_config() [ui_ui_config_bridge_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 985 che coinvolge il nodo canonico `._write_mock_outputs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 985 che coinvolge il nodo canonico `._write_mock_outputs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 985 che coinvolge il nodo canonico `._write_mock_outputs()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 985 che coinvolge il nodo canonico `._write_mock_outputs()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 985 che coinvolge il nodo canonico `._write_mock_outputs()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 985 che coinvolge il nodo canonico `._write_mock_outputs()` è `L445`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 985 che coinvolge il nodo canonico `._write_mock_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 985 che coinvolge il nodo canonico `._write_mock_outputs()` è `ast`. Nel grafo unificato, l'arco 985 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_write_mock_outputs` per la relazione che coinvolge il nodo canonico `._write_mock_outputs()`. Nel grafo unificato, l'arco 985 registra come `target` l'identificatore `Request-Dataset-Generator::ui_ui_config_bridge_load_config` per la relazione che coinvolge il nodo canonico `._write_mock_outputs()`.
<!-- GRAPH_EDGE graph=unificata index=953 role=incoming -->
Nel grafo unificato, il nodo canonico `._write_mock_outputs()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_write_mock_outputs`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._write_mock_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 953 che coinvolge il nodo canonico `._write_mock_outputs()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 953 che coinvolge il nodo canonico `._write_mock_outputs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 953 che coinvolge il nodo canonico `._write_mock_outputs()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 953 che coinvolge il nodo canonico `._write_mock_outputs()` è `L444`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 953 che coinvolge il nodo canonico `._write_mock_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 953 che coinvolge il nodo canonico `._write_mock_outputs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 953 che coinvolge il nodo canonico `._write_mock_outputs()` è `1.0`. Nel grafo unificato, l'arco 953 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `._write_mock_outputs()`. Nel grafo unificato, l'arco 953 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_write_mock_outputs` per la relazione che coinvolge il nodo canonico `._write_mock_outputs()`.
<!-- GRAPH_EDGE graph=unificata index=971 role=incoming -->
Nel grafo unificato, il nodo canonico `._write_mock_outputs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._pipeline_worker()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_write_mock_outputs`. La relazione tecnica esplicita è la seguente: `._pipeline_worker()` chiama `._write_mock_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 971 che coinvolge il nodo canonico `._write_mock_outputs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 971 che coinvolge il nodo canonico `._write_mock_outputs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 971 che coinvolge il nodo canonico `._write_mock_outputs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 971 che coinvolge il nodo canonico `._write_mock_outputs()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 971 che coinvolge il nodo canonico `._write_mock_outputs()` è `L320`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 971 che coinvolge il nodo canonico `._write_mock_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 971 che coinvolge il nodo canonico `._write_mock_outputs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 971 che coinvolge il nodo canonico `._write_mock_outputs()` è `1.0`. Nel grafo unificato, l'arco 971 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `._write_mock_outputs()`. Nel grafo unificato, l'arco 971 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_write_mock_outputs` per la relazione che coinvolge il nodo canonico `._write_mock_outputs()`.
<!-- GRAPH_EDGE graph=unificata index=976 role=incoming -->
Nel grafo unificato, il nodo canonico `._write_mock_outputs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._single_step_worker()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_write_mock_outputs`. La relazione tecnica esplicita è la seguente: `._single_step_worker()` chiama `._write_mock_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 976 che coinvolge il nodo canonico `._write_mock_outputs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 976 che coinvolge il nodo canonico `._write_mock_outputs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 976 che coinvolge il nodo canonico `._write_mock_outputs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 976 che coinvolge il nodo canonico `._write_mock_outputs()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 976 che coinvolge il nodo canonico `._write_mock_outputs()` è `L339`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 976 che coinvolge il nodo canonico `._write_mock_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 976 che coinvolge il nodo canonico `._write_mock_outputs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 976 che coinvolge il nodo canonico `._write_mock_outputs()` è `1.0`. Nel grafo unificato, l'arco 976 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `._write_mock_outputs()`. Nel grafo unificato, l'arco 976 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_write_mock_outputs` per la relazione che coinvolge il nodo canonico `._write_mock_outputs()`.

### .reset()

<!-- GRAPH_NODE graph=codice index=475 id=ui_tabs_run_tab_runtab_reset -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.reset()` è `.reset()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.reset()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.reset()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.reset()` è `L283`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.reset()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.reset()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_reset`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.reset()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.reset()` è `.reset()`.
<!-- GRAPH_EDGE graph=codice index=964 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.reset()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._set_log_placeholder()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_reset` all'ID `ui_tabs_run_tab_runtab_set_log_placeholder`. La relazione tecnica esplicita è la seguente: `.reset()` chiama `._set_log_placeholder()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 964 che coinvolge il nodo canonico `.reset()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 964 che coinvolge il nodo canonico `.reset()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 964 che coinvolge il nodo canonico `.reset()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 964 che coinvolge il nodo canonico `.reset()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 964 che coinvolge il nodo canonico `.reset()` è `L292`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 964 che coinvolge il nodo canonico `.reset()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 964 che coinvolge il nodo canonico `.reset()` è `ast`. Nel grafo del codice RDG, l'arco 964 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_reset` per la relazione che coinvolge il nodo canonico `.reset()`. Nel grafo del codice RDG, l'arco 964 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_set_log_placeholder` per la relazione che coinvolge il nodo canonico `.reset()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 964 che coinvolge il nodo canonico `.reset()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=965 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.reset()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._set_status_now()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_reset` all'ID `ui_tabs_run_tab_runtab_set_status_now`. La relazione tecnica esplicita è la seguente: `.reset()` chiama `._set_status_now()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 965 che coinvolge il nodo canonico `.reset()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 965 che coinvolge il nodo canonico `.reset()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 965 che coinvolge il nodo canonico `.reset()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 965 che coinvolge il nodo canonico `.reset()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 965 che coinvolge il nodo canonico `.reset()` è `L287`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 965 che coinvolge il nodo canonico `.reset()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 965 che coinvolge il nodo canonico `.reset()` è `ast`. Nel grafo del codice RDG, l'arco 965 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_reset` per la relazione che coinvolge il nodo canonico `.reset()`. Nel grafo del codice RDG, l'arco 965 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_set_status_now` per la relazione che coinvolge il nodo canonico `.reset()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 965 che coinvolge il nodo canonico `.reset()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=939 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.reset()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_reset`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `.reset()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 939 che coinvolge il nodo canonico `.reset()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 939 che coinvolge il nodo canonico `.reset()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 939 che coinvolge il nodo canonico `.reset()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 939 che coinvolge il nodo canonico `.reset()` è `L283`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 939 che coinvolge il nodo canonico `.reset()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 939 che coinvolge il nodo canonico `.reset()` è `ast`. Nel grafo del codice RDG, l'arco 939 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `.reset()`. Nel grafo del codice RDG, l'arco 939 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_reset` per la relazione che coinvolge il nodo canonico `.reset()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 939 che coinvolge il nodo canonico `.reset()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=475 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_reset -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.reset()` è `.reset()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.reset()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.reset()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.reset()` è `L283`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.reset()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.reset()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.reset()` è `.reset()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.reset()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.reset()` è `ui_tabs_run_tab_runtab_reset`. Nel grafo unificato, il nodo canonico `.reset()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_reset`.
<!-- GRAPH_EDGE graph=unificata index=964 role=outgoing -->
Nel grafo unificato, il nodo canonico `.reset()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._set_log_placeholder()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_reset` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_log_placeholder`. La relazione tecnica esplicita è la seguente: `.reset()` chiama `._set_log_placeholder()`. Nel grafo unificato, il tipo dichiarato dell'arco 964 che coinvolge il nodo canonico `.reset()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 964 che coinvolge il nodo canonico `.reset()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 964 che coinvolge il nodo canonico `.reset()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 964 che coinvolge il nodo canonico `.reset()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 964 che coinvolge il nodo canonico `.reset()` è `L292`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 964 che coinvolge il nodo canonico `.reset()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 964 che coinvolge il nodo canonico `.reset()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 964 che coinvolge il nodo canonico `.reset()` è `1.0`. Nel grafo unificato, l'arco 964 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_reset` per la relazione che coinvolge il nodo canonico `.reset()`. Nel grafo unificato, l'arco 964 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_log_placeholder` per la relazione che coinvolge il nodo canonico `.reset()`.
<!-- GRAPH_EDGE graph=unificata index=965 role=outgoing -->
Nel grafo unificato, il nodo canonico `.reset()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._set_status_now()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_reset` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_status_now`. La relazione tecnica esplicita è la seguente: `.reset()` chiama `._set_status_now()`. Nel grafo unificato, il tipo dichiarato dell'arco 965 che coinvolge il nodo canonico `.reset()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 965 che coinvolge il nodo canonico `.reset()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 965 che coinvolge il nodo canonico `.reset()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 965 che coinvolge il nodo canonico `.reset()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 965 che coinvolge il nodo canonico `.reset()` è `L287`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 965 che coinvolge il nodo canonico `.reset()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 965 che coinvolge il nodo canonico `.reset()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 965 che coinvolge il nodo canonico `.reset()` è `1.0`. Nel grafo unificato, l'arco 965 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_reset` per la relazione che coinvolge il nodo canonico `.reset()`. Nel grafo unificato, l'arco 965 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_status_now` per la relazione che coinvolge il nodo canonico `.reset()`.
<!-- GRAPH_EDGE graph=unificata index=939 role=incoming -->
Nel grafo unificato, il nodo canonico `.reset()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_reset`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `.reset()`. Nel grafo unificato, il tipo dichiarato dell'arco 939 che coinvolge il nodo canonico `.reset()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 939 che coinvolge il nodo canonico `.reset()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 939 che coinvolge il nodo canonico `.reset()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 939 che coinvolge il nodo canonico `.reset()` è `L283`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 939 che coinvolge il nodo canonico `.reset()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 939 che coinvolge il nodo canonico `.reset()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 939 che coinvolge il nodo canonico `.reset()` è `1.0`. Nel grafo unificato, l'arco 939 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `.reset()`. Nel grafo unificato, l'arco 939 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_reset` per la relazione che coinvolge il nodo canonico `.reset()`.

### .start_pipeline()

<!-- GRAPH_NODE graph=codice index=473 id=ui_tabs_run_tab_runtab_start_pipeline -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.start_pipeline()` è `.start_pipeline()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.start_pipeline()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.start_pipeline()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.start_pipeline()` è `L271`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.start_pipeline()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.start_pipeline()` possiede l'identificatore esatto `ui_tabs_run_tab_runtab_start_pipeline`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.start_pipeline()` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.start_pipeline()` è `.start_pipeline()`.
<!-- GRAPH_EDGE graph=codice index=962 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.start_pipeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._prepare_run()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab_start_pipeline` all'ID `ui_tabs_run_tab_runtab_prepare_run`. La relazione tecnica esplicita è la seguente: `.start_pipeline()` chiama `._prepare_run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 962 che coinvolge il nodo canonico `.start_pipeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 962 che coinvolge il nodo canonico `.start_pipeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 962 che coinvolge il nodo canonico `.start_pipeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 962 che coinvolge il nodo canonico `.start_pipeline()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 962 che coinvolge il nodo canonico `.start_pipeline()` è `L274`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 962 che coinvolge il nodo canonico `.start_pipeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 962 che coinvolge il nodo canonico `.start_pipeline()` è `ast`. Nel grafo del codice RDG, l'arco 962 registra come `source` l'identificatore `ui_tabs_run_tab_runtab_start_pipeline` per la relazione che coinvolge il nodo canonico `.start_pipeline()`. Nel grafo del codice RDG, l'arco 962 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_prepare_run` per la relazione che coinvolge il nodo canonico `.start_pipeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 962 che coinvolge il nodo canonico `.start_pipeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=947 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.start_pipeline()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_start_pipeline`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `.start_pipeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 947 che coinvolge il nodo canonico `.start_pipeline()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 947 che coinvolge il nodo canonico `.start_pipeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 947 che coinvolge il nodo canonico `.start_pipeline()` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 947 che coinvolge il nodo canonico `.start_pipeline()` è `L271`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 947 che coinvolge il nodo canonico `.start_pipeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 947 che coinvolge il nodo canonico `.start_pipeline()` è `ast`. Nel grafo del codice RDG, l'arco 947 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `.start_pipeline()`. Nel grafo del codice RDG, l'arco 947 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_start_pipeline` per la relazione che coinvolge il nodo canonico `.start_pipeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 947 che coinvolge il nodo canonico `.start_pipeline()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=473 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_pipeline -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.start_pipeline()` è `.start_pipeline()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.start_pipeline()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.start_pipeline()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.start_pipeline()` è `L271`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.start_pipeline()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.start_pipeline()` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.start_pipeline()` è `.start_pipeline()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.start_pipeline()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.start_pipeline()` è `ui_tabs_run_tab_runtab_start_pipeline`. Nel grafo unificato, il nodo canonico `.start_pipeline()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_pipeline`.
<!-- GRAPH_EDGE graph=unificata index=962 role=outgoing -->
Nel grafo unificato, il nodo canonico `.start_pipeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._prepare_run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_pipeline` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_prepare_run`. La relazione tecnica esplicita è la seguente: `.start_pipeline()` chiama `._prepare_run()`. Nel grafo unificato, il tipo dichiarato dell'arco 962 che coinvolge il nodo canonico `.start_pipeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 962 che coinvolge il nodo canonico `.start_pipeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 962 che coinvolge il nodo canonico `.start_pipeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 962 che coinvolge il nodo canonico `.start_pipeline()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 962 che coinvolge il nodo canonico `.start_pipeline()` è `L274`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 962 che coinvolge il nodo canonico `.start_pipeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 962 che coinvolge il nodo canonico `.start_pipeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 962 che coinvolge il nodo canonico `.start_pipeline()` è `1.0`. Nel grafo unificato, l'arco 962 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_pipeline` per la relazione che coinvolge il nodo canonico `.start_pipeline()`. Nel grafo unificato, l'arco 962 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_prepare_run` per la relazione che coinvolge il nodo canonico `.start_pipeline()`.
<!-- GRAPH_EDGE graph=unificata index=947 role=incoming -->
Nel grafo unificato, il nodo canonico `.start_pipeline()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_pipeline`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `.start_pipeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 947 che coinvolge il nodo canonico `.start_pipeline()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 947 che coinvolge il nodo canonico `.start_pipeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 947 che coinvolge il nodo canonico `.start_pipeline()` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 947 che coinvolge il nodo canonico `.start_pipeline()` è `L271`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 947 che coinvolge il nodo canonico `.start_pipeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 947 che coinvolge il nodo canonico `.start_pipeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 947 che coinvolge il nodo canonico `.start_pipeline()` è `1.0`. Nel grafo unificato, l'arco 947 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `.start_pipeline()`. Nel grafo unificato, l'arco 947 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_pipeline` per la relazione che coinvolge il nodo canonico `.start_pipeline()`.

### Accoda l&#x27;aggiornamento progress nel main thread Tk.

<!-- GRAPH_NODE graph=codice index=494 id=ui_tabs_run_tab_rationale_377 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `Accoda l'aggiornamento progress nel main thread Tk.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `L377`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` possiede l'identificatore esatto `ui_tabs_run_tab_rationale_377`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `accoda l'aggiornamento progress nel main thread tk.`.
<!-- GRAPH_EDGE graph=codice index=983 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `._ui_set_progress()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_rationale_377` all'ID `ui_tabs_run_tab_runtab_ui_set_progress`. La relazione tecnica esplicita è la seguente: `Accoda l'aggiornamento progress nel main thread Tk.` costituisce una motivazione per `._ui_set_progress()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 983 che coinvolge il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 983 che coinvolge il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 983 che coinvolge il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 983 che coinvolge il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `L377`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 983 che coinvolge il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 983 che coinvolge il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `ast`. Nel grafo del codice RDG, l'arco 983 registra come `source` l'identificatore `ui_tabs_run_tab_rationale_377` per la relazione che coinvolge il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.`. Nel grafo del codice RDG, l'arco 983 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_set_progress` per la relazione che coinvolge il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 983 che coinvolge il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=494 id=Request-Dataset-Generator::ui_tabs_run_tab_rationale_377 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `Accoda l'aggiornamento progress nel main thread Tk.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `L377`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `accoda l'aggiornamento progress nel main thread tk.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `ui_tabs_run_tab_rationale_377`. Nel grafo unificato, il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_rationale_377`.
Nel grafo unificato, il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=983 role=incoming -->
Nel grafo unificato, il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `._ui_set_progress()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_progress` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_rationale_377`. La relazione tecnica esplicita è la seguente: `._ui_set_progress()` costituisce una motivazione per `Accoda l'aggiornamento progress nel main thread Tk.`. Nel grafo unificato, il tipo dichiarato dell'arco 983 che coinvolge il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 983 che coinvolge il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 983 che coinvolge il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 983 che coinvolge il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `L377`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 983 che coinvolge il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 983 che coinvolge il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 983 che coinvolge il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.` è `1.0`. Nel grafo unificato, l'arco 983 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_progress` per la relazione che coinvolge il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.`. Nel grafo unificato, l'arco 983 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_rationale_377` per la relazione che coinvolge il nodo canonico `Accoda l'aggiornamento progress nel main thread Tk.`.

### Controllo pipeline con progress bar e log thread-safe.

<!-- GRAPH_NODE graph=codice index=493 id=ui_tabs_run_tab_rationale_43 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `Controllo pipeline con progress bar e log thread-safe.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `L43`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` possiede l'identificatore esatto `ui_tabs_run_tab_rationale_43`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `controllo pipeline con progress bar e log thread-safe.`.
<!-- GRAPH_EDGE graph=codice index=928 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_rationale_43` all'ID `ui_tabs_run_tab_runtab`. La relazione tecnica esplicita è la seguente: `Controllo pipeline con progress bar e log thread-safe.` costituisce una motivazione per `RunTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 928 che coinvolge il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 928 che coinvolge il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 928 che coinvolge il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 928 che coinvolge il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `L43`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 928 che coinvolge il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 928 che coinvolge il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `ast`. Nel grafo del codice RDG, l'arco 928 registra come `source` l'identificatore `ui_tabs_run_tab_rationale_43` per la relazione che coinvolge il nodo canonico `Controllo pipeline con progress bar e log thread-safe.`. Nel grafo del codice RDG, l'arco 928 registra come `target` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `Controllo pipeline con progress bar e log thread-safe.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 928 che coinvolge il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=493 id=Request-Dataset-Generator::ui_tabs_run_tab_rationale_43 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `Controllo pipeline con progress bar e log thread-safe.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `L43`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `controllo pipeline con progress bar e log thread-safe.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `ui_tabs_run_tab_rationale_43`. Nel grafo unificato, il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_rationale_43`.
Nel grafo unificato, il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=928 role=incoming -->
Nel grafo unificato, il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `RunTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_rationale_43`. La relazione tecnica esplicita è la seguente: `RunTab` costituisce una motivazione per `Controllo pipeline con progress bar e log thread-safe.`. Nel grafo unificato, il tipo dichiarato dell'arco 928 che coinvolge il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 928 che coinvolge il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 928 che coinvolge il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 928 che coinvolge il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `L43`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 928 che coinvolge il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 928 che coinvolge il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 928 che coinvolge il nodo canonico `Controllo pipeline con progress bar e log thread-safe.` è `1.0`. Nel grafo unificato, l'arco 928 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `Controllo pipeline con progress bar e log thread-safe.`. Nel grafo unificato, l'arco 928 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_rationale_43` per la relazione che coinvolge il nodo canonico `Controllo pipeline con progress bar e log thread-safe.`.

### RunTab

<!-- GRAPH_NODE graph=codice index=466 id=ui_tabs_run_tab_runtab -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `RunTab` è `RunTab`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `RunTab` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `RunTab` è `L42`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `RunTab` vale `ast`. Nel grafo del codice RDG, il nodo canonico `RunTab` possiede l'identificatore esatto `ui_tabs_run_tab_runtab`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `RunTab` alla comunità numerica `8`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `RunTab` è `runtab`.
<!-- GRAPH_EDGE graph=codice index=929 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_build`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 929 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 929 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 929 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 929 che coinvolge il nodo canonico `RunTab` è `L92`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 929 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 929 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 929 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 929 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 929 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=930 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_build_buttons`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 930 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 930 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 930 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 930 che coinvolge il nodo canonico `RunTab` è `L238`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 930 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 930 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 930 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 930 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_build_buttons` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 930 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=931 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_log()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_build_log`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._build_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 931 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 931 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 931 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 931 che coinvolge il nodo canonico `RunTab` è `L221`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 931 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 931 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 931 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 931 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_build_log` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 931 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=932 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_progress()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_build_progress`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._build_progress()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 932 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 932 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 932 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 932 che coinvolge il nodo canonico `RunTab` è `L176`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 932 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 932 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 932 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 932 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_build_progress` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 932 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=933 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_step_card()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_build_step_card`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._build_step_card()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 933 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 933 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 933 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 933 che coinvolge il nodo canonico `RunTab` è `L112`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 933 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 933 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 933 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 933 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_build_step_card` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 933 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=934 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._clear_log()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_clear_log`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._clear_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 934 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 934 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 934 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 934 che coinvolge il nodo canonico `RunTab` è `L433`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 934 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 934 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 934 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 934 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_clear_log` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 934 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=935 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._configure_log_tags()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_configure_log_tags`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._configure_log_tags()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 935 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 935 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 935 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 935 che coinvolge il nodo canonico `RunTab` è `L419`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 935 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 935 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 935 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 935 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_configure_log_tags` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 935 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=936 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_init`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `.__init__() [ui_tabs_run_tab_runtab_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 936 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 936 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 936 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 936 che coinvolge il nodo canonico `RunTab` è `L80`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 936 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 936 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 936 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 936 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_init` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 936 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=937 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._pipeline_worker()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_pipeline_worker`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._pipeline_worker()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 937 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 937 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 937 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 937 che coinvolge il nodo canonico `RunTab` è `L303`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 937 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 937 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 937 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 937 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 937 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=938 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._prepare_run()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_prepare_run`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._prepare_run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 938 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 938 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 938 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 938 che coinvolge il nodo canonico `RunTab` è `L294`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 938 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 938 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 938 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 938 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_prepare_run` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 938 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=939 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `.reset()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_reset`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `.reset()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 939 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 939 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 939 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 939 che coinvolge il nodo canonico `RunTab` è `L283`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 939 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 939 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 939 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 939 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_reset` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 939 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=940 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._run_step_1()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_run_step_1`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._run_step_1()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 940 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 940 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 940 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 940 che coinvolge il nodo canonico `RunTab` è `L346`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 940 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 940 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 940 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 940 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_run_step_1` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 940 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=941 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._run_step_2()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_run_step_2`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._run_step_2()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 941 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 941 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 941 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 941 che coinvolge il nodo canonico `RunTab` è `L353`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 941 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 941 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 941 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 941 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_run_step_2` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 941 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=942 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._run_step_3()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_run_step_3`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._run_step_3()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 942 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 942 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 942 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 942 che coinvolge il nodo canonico `RunTab` è `L361`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 942 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 942 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 942 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 942 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_run_step_3` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 942 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=943 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._run_step_4()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_run_step_4`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._run_step_4()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 943 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 943 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 943 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 943 che coinvolge il nodo canonico `RunTab` è `L368`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 943 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 943 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 943 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 943 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_run_step_4` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 943 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=944 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._set_log_placeholder()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_set_log_placeholder`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._set_log_placeholder()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 944 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 944 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 944 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 944 che coinvolge il nodo canonico `RunTab` è `L438`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 944 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 944 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 944 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 944 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_set_log_placeholder` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 944 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=945 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._set_status_now()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_set_status_now`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._set_status_now()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 945 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 945 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 945 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 945 che coinvolge il nodo canonico `RunTab` è `L410`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 945 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 945 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 945 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 945 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_set_status_now` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 945 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=946 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._single_step_worker()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_single_step_worker`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._single_step_worker()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 946 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 946 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 946 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 946 che coinvolge il nodo canonico `RunTab` è `L327`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 946 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 946 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 946 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 946 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 946 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=947 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `.start_pipeline()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_start_pipeline`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `.start_pipeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 947 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 947 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 947 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 947 che coinvolge il nodo canonico `RunTab` è `L271`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 947 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 947 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 947 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 947 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_start_pipeline` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 947 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=948 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._start_single_step()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_start_single_step`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._start_single_step()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 948 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 948 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 948 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 948 che coinvolge il nodo canonico `RunTab` è `L277`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 948 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 948 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 948 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 948 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_start_single_step` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 948 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=949 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._ui_finish()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_ui_finish`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._ui_finish()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 949 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 949 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 949 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 949 che coinvolge il nodo canonico `RunTab` è `L400`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 949 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 949 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 949 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 949 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_finish` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 949 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=950 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._ui_log()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._ui_log()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 950 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 950 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 950 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 950 che coinvolge il nodo canonico `RunTab` è `L391`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 950 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 950 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 950 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 950 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 950 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=951 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._ui_set_progress()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_ui_set_progress`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._ui_set_progress()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 951 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 951 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 951 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 951 che coinvolge il nodo canonico `RunTab` è `L376`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 951 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 951 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 951 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 951 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_set_progress` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 951 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=952 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._ui_set_status()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_ui_set_status`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._ui_set_status()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 952 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 952 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 952 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 952 che coinvolge il nodo canonico `RunTab` è `L388`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 952 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 952 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 952 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 952 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_ui_set_status` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 952 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=953 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._write_mock_outputs()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_runtab` all'ID `ui_tabs_run_tab_runtab_write_mock_outputs`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._write_mock_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 953 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 953 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 953 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 953 che coinvolge il nodo canonico `RunTab` è `L444`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 953 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 953 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 953 registra come `source` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 953 registra come `target` l'identificatore `ui_tabs_run_tab_runtab_write_mock_outputs` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 953 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=828 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `app.py`; l'orientamento registrato nel JSON va dall'ID `ui_app` all'ID `ui_tabs_run_tab_runtab`. La relazione tecnica esplicita è la seguente: `app.py` importa `RunTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 828 che coinvolge il nodo canonico `RunTab` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 828 che coinvolge il nodo canonico `RunTab` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 828 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 828 che coinvolge il nodo canonico `RunTab` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 828 che coinvolge il nodo canonico `RunTab` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 828 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 828 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 828 registra come `source` l'identificatore `ui_app` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 828 registra come `target` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 828 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=838 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è il destinatario `target` della relazione `uses` proveniente dal nodo canonico `RDGApp`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp` all'ID `ui_tabs_run_tab_runtab`. La relazione tecnica esplicita è la seguente: `RDGApp` usa `RunTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 838 che coinvolge il nodo canonico `RunTab` è `uses`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 838 che coinvolge il nodo canonico `RunTab` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 838 che coinvolge il nodo canonico `RunTab` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 838 che coinvolge il nodo canonico `RunTab` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 838 che coinvolge il nodo canonico `RunTab` è `0.8`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 838 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 838 registra come `source` l'identificatore `ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 838 registra come `target` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 838 che coinvolge il nodo canonico `RunTab` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=844 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build_tabs()`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp_build_tabs` all'ID `ui_tabs_run_tab_runtab`. La relazione tecnica esplicita è la seguente: `._build_tabs()` chiama `RunTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 844 che coinvolge il nodo canonico `RunTab` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 844 che coinvolge il nodo canonico `RunTab` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 844 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 844 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 844 che coinvolge il nodo canonico `RunTab` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 844 che coinvolge il nodo canonico `RunTab` è `L117`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 844 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 844 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 844 registra come `source` l'identificatore `ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 844 registra come `target` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=codice index=924 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `run_tab.py`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab` all'ID `ui_tabs_run_tab_runtab`. La relazione tecnica esplicita è la seguente: `run_tab.py` contiene `RunTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 924 che coinvolge il nodo canonico `RunTab` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 924 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 924 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 924 che coinvolge il nodo canonico `RunTab` è `L42`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 924 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 924 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 924 registra come `source` l'identificatore `ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 924 registra come `target` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 924 che coinvolge il nodo canonico `RunTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=928 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `RunTab` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Controllo pipeline con progress bar e log thread-safe.`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_run_tab_rationale_43` all'ID `ui_tabs_run_tab_runtab`. La relazione tecnica esplicita è la seguente: `Controllo pipeline con progress bar e log thread-safe.` costituisce una motivazione per `RunTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 928 che coinvolge il nodo canonico `RunTab` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 928 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 928 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 928 che coinvolge il nodo canonico `RunTab` è `L43`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 928 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 928 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo del codice RDG, l'arco 928 registra come `source` l'identificatore `ui_tabs_run_tab_rationale_43` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, l'arco 928 registra come `target` l'identificatore `ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 928 che coinvolge il nodo canonico `RunTab` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=466 id=Request-Dataset-Generator::ui_tabs_run_tab_runtab -->
Nel grafo unificato, l'etichetta originale del nodo canonico `RunTab` è `RunTab`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `RunTab` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `RunTab` è `L42`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `RunTab` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `RunTab` alla comunità numerica `8`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `RunTab` è `runtab`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `RunTab` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `RunTab` è `ui_tabs_run_tab_runtab`. Nel grafo unificato, il nodo canonico `RunTab` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_run_tab_runtab`.
<!-- GRAPH_EDGE graph=unificata index=928 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Controllo pipeline con progress bar e log thread-safe.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_rationale_43`. La relazione tecnica esplicita è la seguente: `RunTab` costituisce una motivazione per `Controllo pipeline con progress bar e log thread-safe.`. Nel grafo unificato, il tipo dichiarato dell'arco 928 che coinvolge il nodo canonico `RunTab` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 928 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 928 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 928 che coinvolge il nodo canonico `RunTab` è `L43`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 928 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 928 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 928 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 928 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 928 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_rationale_43` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=929 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build() [ui_tabs_run_tab_runtab_build]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._build() [ui_tabs_run_tab_runtab_build]`. Nel grafo unificato, il tipo dichiarato dell'arco 929 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 929 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 929 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 929 che coinvolge il nodo canonico `RunTab` è `L92`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 929 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 929 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 929 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 929 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 929 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=930 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_buttons`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._build_buttons() [ui_tabs_run_tab_runtab_build_buttons]`. Nel grafo unificato, il tipo dichiarato dell'arco 930 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 930 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 930 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 930 che coinvolge il nodo canonico `RunTab` è `L238`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 930 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 930 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 930 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 930 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 930 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_buttons` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=931 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_log()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_log`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._build_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 931 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 931 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 931 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 931 che coinvolge il nodo canonico `RunTab` è `L221`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 931 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 931 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 931 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 931 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 931 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_log` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=932 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_progress()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_progress`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._build_progress()`. Nel grafo unificato, il tipo dichiarato dell'arco 932 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 932 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 932 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 932 che coinvolge il nodo canonico `RunTab` è `L176`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 932 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 932 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 932 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 932 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 932 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_progress` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=933 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_step_card()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_step_card`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._build_step_card()`. Nel grafo unificato, il tipo dichiarato dell'arco 933 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 933 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 933 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 933 che coinvolge il nodo canonico `RunTab` è `L112`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 933 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 933 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 933 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 933 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 933 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_build_step_card` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=934 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._clear_log()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_clear_log`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._clear_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 934 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 934 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 934 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 934 che coinvolge il nodo canonico `RunTab` è `L433`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 934 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 934 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 934 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 934 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 934 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_clear_log` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=935 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._configure_log_tags()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_configure_log_tags`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._configure_log_tags()`. Nel grafo unificato, il tipo dichiarato dell'arco 935 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 935 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 935 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 935 che coinvolge il nodo canonico `RunTab` è `L419`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 935 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 935 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 935 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 935 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 935 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_configure_log_tags` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=936 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [ui_tabs_run_tab_runtab_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_init`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `.__init__() [ui_tabs_run_tab_runtab_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 936 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 936 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 936 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 936 che coinvolge il nodo canonico `RunTab` è `L80`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 936 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 936 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 936 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 936 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 936 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_init` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=937 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._pipeline_worker()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._pipeline_worker()`. Nel grafo unificato, il tipo dichiarato dell'arco 937 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 937 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 937 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 937 che coinvolge il nodo canonico `RunTab` è `L303`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 937 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 937 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 937 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 937 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 937 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_pipeline_worker` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=938 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._prepare_run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_prepare_run`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._prepare_run()`. Nel grafo unificato, il tipo dichiarato dell'arco 938 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 938 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 938 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 938 che coinvolge il nodo canonico `RunTab` è `L294`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 938 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 938 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 938 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 938 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 938 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_prepare_run` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=939 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `.reset()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_reset`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `.reset()`. Nel grafo unificato, il tipo dichiarato dell'arco 939 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 939 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 939 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 939 che coinvolge il nodo canonico `RunTab` è `L283`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 939 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 939 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 939 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 939 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 939 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_reset` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=940 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._run_step_1()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_1`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._run_step_1()`. Nel grafo unificato, il tipo dichiarato dell'arco 940 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 940 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 940 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 940 che coinvolge il nodo canonico `RunTab` è `L346`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 940 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 940 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 940 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 940 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 940 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_1` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=941 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._run_step_2()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_2`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._run_step_2()`. Nel grafo unificato, il tipo dichiarato dell'arco 941 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 941 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 941 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 941 che coinvolge il nodo canonico `RunTab` è `L353`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 941 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 941 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 941 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 941 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 941 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_2` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=942 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._run_step_3()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_3`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._run_step_3()`. Nel grafo unificato, il tipo dichiarato dell'arco 942 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 942 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 942 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 942 che coinvolge il nodo canonico `RunTab` è `L361`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 942 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 942 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 942 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 942 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 942 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_3` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=943 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._run_step_4()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_4`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._run_step_4()`. Nel grafo unificato, il tipo dichiarato dell'arco 943 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 943 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 943 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 943 che coinvolge il nodo canonico `RunTab` è `L368`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 943 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 943 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 943 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 943 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 943 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_run_step_4` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=944 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._set_log_placeholder()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_log_placeholder`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._set_log_placeholder()`. Nel grafo unificato, il tipo dichiarato dell'arco 944 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 944 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 944 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 944 che coinvolge il nodo canonico `RunTab` è `L438`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 944 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 944 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 944 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 944 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 944 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_log_placeholder` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=945 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._set_status_now()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_status_now`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._set_status_now()`. Nel grafo unificato, il tipo dichiarato dell'arco 945 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 945 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 945 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 945 che coinvolge il nodo canonico `RunTab` è `L410`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 945 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 945 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 945 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 945 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 945 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_set_status_now` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=946 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._single_step_worker()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._single_step_worker()`. Nel grafo unificato, il tipo dichiarato dell'arco 946 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 946 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 946 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 946 che coinvolge il nodo canonico `RunTab` è `L327`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 946 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 946 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 946 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 946 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 946 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_single_step_worker` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=947 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `.start_pipeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_pipeline`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `.start_pipeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 947 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 947 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 947 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 947 che coinvolge il nodo canonico `RunTab` è `L271`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 947 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 947 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 947 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 947 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 947 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_pipeline` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=948 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._start_single_step()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_single_step`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._start_single_step()`. Nel grafo unificato, il tipo dichiarato dell'arco 948 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 948 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 948 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 948 che coinvolge il nodo canonico `RunTab` è `L277`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 948 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 948 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 948 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 948 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 948 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_start_single_step` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=949 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._ui_finish()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_finish`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._ui_finish()`. Nel grafo unificato, il tipo dichiarato dell'arco 949 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 949 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 949 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 949 che coinvolge il nodo canonico `RunTab` è `L400`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 949 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 949 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 949 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 949 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 949 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_finish` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=950 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._ui_log()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._ui_log()`. Nel grafo unificato, il tipo dichiarato dell'arco 950 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 950 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 950 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 950 che coinvolge il nodo canonico `RunTab` è `L391`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 950 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 950 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 950 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 950 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 950 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_log` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=951 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._ui_set_progress()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_progress`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._ui_set_progress()`. Nel grafo unificato, il tipo dichiarato dell'arco 951 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 951 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 951 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 951 che coinvolge il nodo canonico `RunTab` è `L376`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 951 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 951 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 951 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 951 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 951 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_progress` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=952 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._ui_set_status()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_status`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._ui_set_status()`. Nel grafo unificato, il tipo dichiarato dell'arco 952 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 952 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 952 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 952 che coinvolge il nodo canonico `RunTab` è `L388`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 952 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 952 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 952 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 952 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 952 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_ui_set_status` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=953 role=outgoing -->
Nel grafo unificato, il nodo canonico `RunTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._write_mock_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab_write_mock_outputs`. La relazione tecnica esplicita è la seguente: `RunTab` registra come proprio metodo `._write_mock_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 953 che coinvolge il nodo canonico `RunTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 953 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 953 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 953 che coinvolge il nodo canonico `RunTab` è `L444`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 953 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 953 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 953 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 953 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 953 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab_write_mock_outputs` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=828 role=incoming -->
Nel grafo unificato, il nodo canonico `RunTab` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `app.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab`. La relazione tecnica esplicita è la seguente: `app.py` importa `RunTab`. Nel grafo unificato, il tipo dichiarato dell'arco 828 che coinvolge il nodo canonico `RunTab` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 828 che coinvolge il nodo canonico `RunTab` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 828 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 828 che coinvolge il nodo canonico `RunTab` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 828 che coinvolge il nodo canonico `RunTab` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 828 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 828 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 828 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 828 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 828 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=838 role=incoming -->
Nel grafo unificato, il nodo canonico `RunTab` è il destinatario `target` della relazione `uses` proveniente dal nodo canonico `RDGApp`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab`. La relazione tecnica esplicita è la seguente: `RDGApp` usa `RunTab`. Nel grafo unificato, il tipo dichiarato dell'arco 838 che coinvolge il nodo canonico `RunTab` è `uses`. Nel grafo unificato, la confidenza categoriale dell'arco 838 che coinvolge il nodo canonico `RunTab` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 838 che coinvolge il nodo canonico `RunTab` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 838 che coinvolge il nodo canonico `RunTab` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 838 che coinvolge il nodo canonico `RunTab` è `0.8`. Nel grafo unificato, la provenienza `_origin` dell'arco 838 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 838 che coinvolge il nodo canonico `RunTab` è `0.5`. Nel grafo unificato, l'arco 838 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 838 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=844 role=incoming -->
Nel grafo unificato, il nodo canonico `RunTab` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build_tabs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp_build_tabs` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab`. La relazione tecnica esplicita è la seguente: `._build_tabs()` chiama `RunTab`. Nel grafo unificato, il tipo dichiarato dell'arco 844 che coinvolge il nodo canonico `RunTab` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 844 che coinvolge il nodo canonico `RunTab` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 844 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 844 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 844 che coinvolge il nodo canonico `RunTab` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 844 che coinvolge il nodo canonico `RunTab` è `L117`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 844 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 844 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, l'arco 844 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 844 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`.
<!-- GRAPH_EDGE graph=unificata index=924 role=incoming -->
Nel grafo unificato, il nodo canonico `RunTab` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `run_tab.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_run_tab` all'ID `Request-Dataset-Generator::ui_tabs_run_tab_runtab`. La relazione tecnica esplicita è la seguente: `run_tab.py` contiene `RunTab`. Nel grafo unificato, il tipo dichiarato dell'arco 924 che coinvolge il nodo canonico `RunTab` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 924 che coinvolge il nodo canonico `RunTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 924 che coinvolge il nodo canonico `RunTab` è `ui/tabs/run_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 924 che coinvolge il nodo canonico `RunTab` è `L42`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 924 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 924 che coinvolge il nodo canonico `RunTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 924 che coinvolge il nodo canonico `RunTab` è `1.0`. Nel grafo unificato, l'arco 924 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab` per la relazione che coinvolge il nodo canonico `RunTab`. Nel grafo unificato, l'arco 924 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_run_tab_runtab` per la relazione che coinvolge il nodo canonico `RunTab`.

## Cluster Graphify 9 — RVE Documentation Concepts

Il cluster editoriale Graphify 9 del dominio Architettura e codice del framework RDG contiene 27 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### aiohttp

<!-- GRAPH_NODE graph=codice index=515 id=requirements_aiohttp -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `aiohttp` è `aiohttp`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `aiohttp` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `aiohttp` è `requirements.txt`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `aiohttp` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `aiohttp` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `aiohttp` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `aiohttp` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `aiohttp` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `aiohttp` possiede l'identificatore esatto `requirements_aiohttp`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `aiohttp` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `aiohttp` è `aiohttp`.
Nel grafo del codice RDG, il nodo canonico `aiohttp` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=1011 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `aiohttp` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Runtime Dependencies`; l'orientamento registrato nel JSON va dall'ID `requirements_runtime_dependencies` all'ID `requirements_aiohttp`. La relazione tecnica esplicita è la seguente: `Runtime Dependencies` fa riferimento a `aiohttp`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1011 che coinvolge il nodo canonico `aiohttp` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1011 che coinvolge il nodo canonico `aiohttp` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1011 che coinvolge il nodo canonico `aiohttp` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1011 che coinvolge il nodo canonico `aiohttp` è `requirements.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1011 che coinvolge il nodo canonico `aiohttp` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1011 che coinvolge il nodo canonico `aiohttp` è `1.0`. Nel grafo del codice RDG, l'arco 1011 registra come `source` l'identificatore `requirements_runtime_dependencies` per la relazione che coinvolge il nodo canonico `aiohttp`. Nel grafo del codice RDG, l'arco 1011 registra come `target` l'identificatore `requirements_aiohttp` per la relazione che coinvolge il nodo canonico `aiohttp`.

<!-- GRAPH_NODE graph=unificata index=515 id=Request-Dataset-Generator::requirements_aiohttp -->
Nel grafo unificato, l'etichetta originale del nodo canonico `aiohttp` è `aiohttp`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `aiohttp` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `aiohttp` è `requirements.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `aiohttp` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `aiohttp` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `aiohttp` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `aiohttp` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `aiohttp` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `aiohttp` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `aiohttp` è `aiohttp`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `aiohttp` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `aiohttp` è `requirements_aiohttp`. Nel grafo unificato, il nodo canonico `aiohttp` possiede l'identificatore esatto `Request-Dataset-Generator::requirements_aiohttp`.
Nel grafo unificato, il nodo canonico `aiohttp` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1011 role=incoming -->
Nel grafo unificato, il nodo canonico `aiohttp` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Runtime Dependencies`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::requirements_runtime_dependencies` all'ID `Request-Dataset-Generator::requirements_aiohttp`. La relazione tecnica esplicita è la seguente: `Runtime Dependencies` fa riferimento a `aiohttp`. Nel grafo unificato, il tipo dichiarato dell'arco 1011 che coinvolge il nodo canonico `aiohttp` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1011 che coinvolge il nodo canonico `aiohttp` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1011 che coinvolge il nodo canonico `aiohttp` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1011 che coinvolge il nodo canonico `aiohttp` è `requirements.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1011 che coinvolge il nodo canonico `aiohttp` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1011 che coinvolge il nodo canonico `aiohttp` è `1.0`. Nel grafo unificato, l'arco 1011 registra come `source` l'identificatore `Request-Dataset-Generator::requirements_runtime_dependencies` per la relazione che coinvolge il nodo canonico `aiohttp`. Nel grafo unificato, l'arco 1011 registra come `target` l'identificatore `Request-Dataset-Generator::requirements_aiohttp` per la relazione che coinvolge il nodo canonico `aiohttp`.

### cryptography

<!-- GRAPH_NODE graph=codice index=516 id=requirements_cryptography -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `cryptography` è `cryptography`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `cryptography` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `cryptography` è `requirements.txt`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `cryptography` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `cryptography` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `cryptography` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `cryptography` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `cryptography` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `cryptography` possiede l'identificatore esatto `requirements_cryptography`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `cryptography` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `cryptography` è `cryptography`.
<!-- GRAPH_EDGE graph=codice index=1009 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `cryptography` è la sorgente `source` della relazione `conceptually_related_to` verso il nodo canonico `mTLS with IAP`; l'orientamento registrato nel JSON va dall'ID `requirements_cryptography` all'ID `readme_mtls_iap`. La relazione tecnica esplicita è la seguente: `cryptography` è concettualmente correlato a `mTLS with IAP`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1009 che coinvolge il nodo canonico `cryptography` è `conceptually_related_to`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1009 che coinvolge il nodo canonico `cryptography` è `INFERRED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1009 che coinvolge il nodo canonico `cryptography` è `0.85`. Nel grafo del codice RDG, il file sorgente dell'arco 1009 che coinvolge il nodo canonico `cryptography` è `requirements.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1009 che coinvolge il nodo canonico `cryptography` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1009 che coinvolge il nodo canonico `cryptography` è `1.0`. Nel grafo del codice RDG, l'arco 1009 registra come `source` l'identificatore `requirements_cryptography` per la relazione che coinvolge il nodo canonico `cryptography`. Nel grafo del codice RDG, l'arco 1009 registra come `target` l'identificatore `readme_mtls_iap` per la relazione che coinvolge il nodo canonico `cryptography`.
<!-- GRAPH_EDGE graph=codice index=1012 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `cryptography` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Runtime Dependencies`; l'orientamento registrato nel JSON va dall'ID `requirements_runtime_dependencies` all'ID `requirements_cryptography`. La relazione tecnica esplicita è la seguente: `Runtime Dependencies` fa riferimento a `cryptography`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1012 che coinvolge il nodo canonico `cryptography` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1012 che coinvolge il nodo canonico `cryptography` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1012 che coinvolge il nodo canonico `cryptography` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1012 che coinvolge il nodo canonico `cryptography` è `requirements.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1012 che coinvolge il nodo canonico `cryptography` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1012 che coinvolge il nodo canonico `cryptography` è `1.0`. Nel grafo del codice RDG, l'arco 1012 registra come `source` l'identificatore `requirements_runtime_dependencies` per la relazione che coinvolge il nodo canonico `cryptography`. Nel grafo del codice RDG, l'arco 1012 registra come `target` l'identificatore `requirements_cryptography` per la relazione che coinvolge il nodo canonico `cryptography`.

<!-- GRAPH_NODE graph=unificata index=516 id=Request-Dataset-Generator::requirements_cryptography -->
Nel grafo unificato, l'etichetta originale del nodo canonico `cryptography` è `cryptography`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `cryptography` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `cryptography` è `requirements.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `cryptography` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `cryptography` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `cryptography` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `cryptography` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `cryptography` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `cryptography` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `cryptography` è `cryptography`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `cryptography` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `cryptography` è `requirements_cryptography`. Nel grafo unificato, il nodo canonico `cryptography` possiede l'identificatore esatto `Request-Dataset-Generator::requirements_cryptography`.
Nel grafo unificato, il nodo canonico `cryptography` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1009 role=incoming -->
Nel grafo unificato, il nodo canonico `cryptography` è il destinatario `target` della relazione `conceptually_related_to` proveniente dal nodo canonico `mTLS with IAP`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_mtls_iap` all'ID `Request-Dataset-Generator::requirements_cryptography`. La relazione tecnica esplicita è la seguente: `mTLS with IAP` è concettualmente correlato a `cryptography`. Nel grafo unificato, il tipo dichiarato dell'arco 1009 che coinvolge il nodo canonico `cryptography` è `conceptually_related_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1009 che coinvolge il nodo canonico `cryptography` è `INFERRED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1009 che coinvolge il nodo canonico `cryptography` è `0.85`. Nel grafo unificato, il file sorgente dell'arco 1009 che coinvolge il nodo canonico `cryptography` è `requirements.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1009 che coinvolge il nodo canonico `cryptography` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1009 che coinvolge il nodo canonico `cryptography` è `1.0`. Nel grafo unificato, l'arco 1009 registra come `source` l'identificatore `Request-Dataset-Generator::readme_mtls_iap` per la relazione che coinvolge il nodo canonico `cryptography`. Nel grafo unificato, l'arco 1009 registra come `target` l'identificatore `Request-Dataset-Generator::requirements_cryptography` per la relazione che coinvolge il nodo canonico `cryptography`.
<!-- GRAPH_EDGE graph=unificata index=1012 role=incoming -->
Nel grafo unificato, il nodo canonico `cryptography` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Runtime Dependencies`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::requirements_runtime_dependencies` all'ID `Request-Dataset-Generator::requirements_cryptography`. La relazione tecnica esplicita è la seguente: `Runtime Dependencies` fa riferimento a `cryptography`. Nel grafo unificato, il tipo dichiarato dell'arco 1012 che coinvolge il nodo canonico `cryptography` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1012 che coinvolge il nodo canonico `cryptography` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1012 che coinvolge il nodo canonico `cryptography` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1012 che coinvolge il nodo canonico `cryptography` è `requirements.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1012 che coinvolge il nodo canonico `cryptography` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1012 che coinvolge il nodo canonico `cryptography` è `1.0`. Nel grafo unificato, l'arco 1012 registra come `source` l'identificatore `Request-Dataset-Generator::requirements_runtime_dependencies` per la relazione che coinvolge il nodo canonico `cryptography`. Nel grafo unificato, l'arco 1012 registra come `target` l'identificatore `Request-Dataset-Generator::requirements_cryptography` per la relazione che coinvolge il nodo canonico `cryptography`.

### customtkinter

<!-- GRAPH_NODE graph=codice index=519 id=ui_requirements_customtkinter -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `customtkinter` è `customtkinter`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `customtkinter` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `customtkinter` è `ui/requirements.txt`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `customtkinter` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `customtkinter` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `customtkinter` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `customtkinter` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `customtkinter` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `customtkinter` possiede l'identificatore esatto `ui_requirements_customtkinter`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `customtkinter` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `customtkinter` è `customtkinter`.
Nel grafo del codice RDG, il nodo canonico `customtkinter` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=1015 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `customtkinter` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `UI Dependencies`; l'orientamento registrato nel JSON va dall'ID `ui_requirements_ui_dependencies` all'ID `ui_requirements_customtkinter`. La relazione tecnica esplicita è la seguente: `UI Dependencies` fa riferimento a `customtkinter`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1015 che coinvolge il nodo canonico `customtkinter` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1015 che coinvolge il nodo canonico `customtkinter` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1015 che coinvolge il nodo canonico `customtkinter` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1015 che coinvolge il nodo canonico `customtkinter` è `ui/requirements.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1015 che coinvolge il nodo canonico `customtkinter` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1015 che coinvolge il nodo canonico `customtkinter` è `1.0`. Nel grafo del codice RDG, l'arco 1015 registra come `source` l'identificatore `ui_requirements_ui_dependencies` per la relazione che coinvolge il nodo canonico `customtkinter`. Nel grafo del codice RDG, l'arco 1015 registra come `target` l'identificatore `ui_requirements_customtkinter` per la relazione che coinvolge il nodo canonico `customtkinter`.

<!-- GRAPH_NODE graph=unificata index=519 id=Request-Dataset-Generator::ui_requirements_customtkinter -->
Nel grafo unificato, l'etichetta originale del nodo canonico `customtkinter` è `customtkinter`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `customtkinter` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `customtkinter` è `ui/requirements.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `customtkinter` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `customtkinter` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `customtkinter` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `customtkinter` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `customtkinter` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `customtkinter` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `customtkinter` è `customtkinter`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `customtkinter` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `customtkinter` è `ui_requirements_customtkinter`. Nel grafo unificato, il nodo canonico `customtkinter` possiede l'identificatore esatto `Request-Dataset-Generator::ui_requirements_customtkinter`.
Nel grafo unificato, il nodo canonico `customtkinter` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1015 role=incoming -->
Nel grafo unificato, il nodo canonico `customtkinter` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `UI Dependencies`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_requirements_ui_dependencies` all'ID `Request-Dataset-Generator::ui_requirements_customtkinter`. La relazione tecnica esplicita è la seguente: `UI Dependencies` fa riferimento a `customtkinter`. Nel grafo unificato, il tipo dichiarato dell'arco 1015 che coinvolge il nodo canonico `customtkinter` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1015 che coinvolge il nodo canonico `customtkinter` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1015 che coinvolge il nodo canonico `customtkinter` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1015 che coinvolge il nodo canonico `customtkinter` è `ui/requirements.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1015 che coinvolge il nodo canonico `customtkinter` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1015 che coinvolge il nodo canonico `customtkinter` è `1.0`. Nel grafo unificato, l'arco 1015 registra come `source` l'identificatore `Request-Dataset-Generator::ui_requirements_ui_dependencies` per la relazione che coinvolge il nodo canonico `customtkinter`. Nel grafo unificato, l'arco 1015 registra come `target` l'identificatore `Request-Dataset-Generator::ui_requirements_customtkinter` per la relazione che coinvolge il nodo canonico `customtkinter`.

### Direct Integrations

<!-- GRAPH_NODE graph=codice index=531 id=usage_direct_integrations -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Direct Integrations` è `Direct Integrations`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Direct Integrations` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Direct Integrations` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Direct Integrations` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `Direct Integrations` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `Direct Integrations` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `Direct Integrations` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `Direct Integrations` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `Direct Integrations` possiede l'identificatore esatto `usage_direct_integrations`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Direct Integrations` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Direct Integrations` è `direct integrations`.
Nel grafo del codice RDG, il nodo canonico `Direct Integrations` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=1018 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Direct Integrations` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Request Dataset Generator RVE Guide`; l'orientamento registrato nel JSON va dall'ID `usage_request_dataset_generator_rve_guide` all'ID `usage_direct_integrations`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `Direct Integrations`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1018 che coinvolge il nodo canonico `Direct Integrations` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1018 che coinvolge il nodo canonico `Direct Integrations` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1018 che coinvolge il nodo canonico `Direct Integrations` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1018 che coinvolge il nodo canonico `Direct Integrations` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1018 che coinvolge il nodo canonico `Direct Integrations` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1018 che coinvolge il nodo canonico `Direct Integrations` è `1.0`. Nel grafo del codice RDG, l'arco 1018 registra come `source` l'identificatore `usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `Direct Integrations`. Nel grafo del codice RDG, l'arco 1018 registra come `target` l'identificatore `usage_direct_integrations` per la relazione che coinvolge il nodo canonico `Direct Integrations`.

<!-- GRAPH_NODE graph=unificata index=531 id=Request-Dataset-Generator::usage_direct_integrations -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Direct Integrations` è `Direct Integrations`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Direct Integrations` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Direct Integrations` è `usage.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Direct Integrations` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Direct Integrations` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Direct Integrations` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Direct Integrations` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Direct Integrations` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Direct Integrations` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Direct Integrations` è `direct integrations`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Direct Integrations` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Direct Integrations` è `usage_direct_integrations`. Nel grafo unificato, il nodo canonico `Direct Integrations` possiede l'identificatore esatto `Request-Dataset-Generator::usage_direct_integrations`.
Nel grafo unificato, il nodo canonico `Direct Integrations` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1018 role=incoming -->
Nel grafo unificato, il nodo canonico `Direct Integrations` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Request Dataset Generator RVE Guide`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` all'ID `Request-Dataset-Generator::usage_direct_integrations`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `Direct Integrations`. Nel grafo unificato, il tipo dichiarato dell'arco 1018 che coinvolge il nodo canonico `Direct Integrations` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1018 che coinvolge il nodo canonico `Direct Integrations` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1018 che coinvolge il nodo canonico `Direct Integrations` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1018 che coinvolge il nodo canonico `Direct Integrations` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1018 che coinvolge il nodo canonico `Direct Integrations` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1018 che coinvolge il nodo canonico `Direct Integrations` è `1.0`. Nel grafo unificato, l'arco 1018 registra come `source` l'identificatore `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `Direct Integrations`. Nel grafo unificato, l'arco 1018 registra come `target` l'identificatore `Request-Dataset-Generator::usage_direct_integrations` per la relazione che coinvolge il nodo canonico `Direct Integrations`.

### Fault Injection

<!-- GRAPH_NODE graph=codice index=528 id=usage_fault_injection -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Fault Injection` è `Fault Injection`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Fault Injection` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Fault Injection` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Fault Injection` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `Fault Injection` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `Fault Injection` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `Fault Injection` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `Fault Injection` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `Fault Injection` possiede l'identificatore esatto `usage_fault_injection`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Fault Injection` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Fault Injection` è `fault injection`.
Nel grafo del codice RDG, il nodo canonico `Fault Injection` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=1019 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Fault Injection` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Request Dataset Generator RVE Guide`; l'orientamento registrato nel JSON va dall'ID `usage_request_dataset_generator_rve_guide` all'ID `usage_fault_injection`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `Fault Injection`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1019 che coinvolge il nodo canonico `Fault Injection` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1019 che coinvolge il nodo canonico `Fault Injection` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1019 che coinvolge il nodo canonico `Fault Injection` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1019 che coinvolge il nodo canonico `Fault Injection` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1019 che coinvolge il nodo canonico `Fault Injection` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1019 che coinvolge il nodo canonico `Fault Injection` è `1.0`. Nel grafo del codice RDG, l'arco 1019 registra come `source` l'identificatore `usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `Fault Injection`. Nel grafo del codice RDG, l'arco 1019 registra come `target` l'identificatore `usage_fault_injection` per la relazione che coinvolge il nodo canonico `Fault Injection`.

<!-- GRAPH_NODE graph=unificata index=528 id=Request-Dataset-Generator::usage_fault_injection -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Fault Injection` è `Fault Injection`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Fault Injection` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Fault Injection` è `usage.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Fault Injection` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Fault Injection` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Fault Injection` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Fault Injection` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Fault Injection` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Fault Injection` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Fault Injection` è `fault injection`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Fault Injection` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Fault Injection` è `usage_fault_injection`. Nel grafo unificato, il nodo canonico `Fault Injection` possiede l'identificatore esatto `Request-Dataset-Generator::usage_fault_injection`.
Nel grafo unificato, il nodo canonico `Fault Injection` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1019 role=incoming -->
Nel grafo unificato, il nodo canonico `Fault Injection` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Request Dataset Generator RVE Guide`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` all'ID `Request-Dataset-Generator::usage_fault_injection`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `Fault Injection`. Nel grafo unificato, il tipo dichiarato dell'arco 1019 che coinvolge il nodo canonico `Fault Injection` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1019 che coinvolge il nodo canonico `Fault Injection` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1019 che coinvolge il nodo canonico `Fault Injection` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1019 che coinvolge il nodo canonico `Fault Injection` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1019 che coinvolge il nodo canonico `Fault Injection` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1019 che coinvolge il nodo canonico `Fault Injection` è `1.0`. Nel grafo unificato, l'arco 1019 registra come `source` l'identificatore `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `Fault Injection`. Nel grafo unificato, l'arco 1019 registra come `target` l'identificatore `Request-Dataset-Generator::usage_fault_injection` per la relazione che coinvolge il nodo canonico `Fault Injection`.

### FHIR Synthetic Patients

<!-- GRAPH_NODE graph=codice index=505 id=readme_fhir_synthetic_patients -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `FHIR Synthetic Patients` è `FHIR Synthetic Patients`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `FHIR Synthetic Patients` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `FHIR Synthetic Patients` è `README.md`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `FHIR Synthetic Patients` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `FHIR Synthetic Patients` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `FHIR Synthetic Patients` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `FHIR Synthetic Patients` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `FHIR Synthetic Patients` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `FHIR Synthetic Patients` possiede l'identificatore esatto `readme_fhir_synthetic_patients`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `FHIR Synthetic Patients` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `FHIR Synthetic Patients` è `fhir synthetic patients`.
Nel grafo del codice RDG, il nodo canonico `FHIR Synthetic Patients` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=998 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FHIR Synthetic Patients` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Request Dataset Generator RVE`; l'orientamento registrato nel JSON va dall'ID `readme_request_dataset_generator_rve` all'ID `readme_fhir_synthetic_patients`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE` fa riferimento a `FHIR Synthetic Patients`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 998 che coinvolge il nodo canonico `FHIR Synthetic Patients` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 998 che coinvolge il nodo canonico `FHIR Synthetic Patients` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 998 che coinvolge il nodo canonico `FHIR Synthetic Patients` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 998 che coinvolge il nodo canonico `FHIR Synthetic Patients` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 998 che coinvolge il nodo canonico `FHIR Synthetic Patients` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 998 che coinvolge il nodo canonico `FHIR Synthetic Patients` è `1.0`. Nel grafo del codice RDG, l'arco 998 registra come `source` l'identificatore `readme_request_dataset_generator_rve` per la relazione che coinvolge il nodo canonico `FHIR Synthetic Patients`. Nel grafo del codice RDG, l'arco 998 registra come `target` l'identificatore `readme_fhir_synthetic_patients` per la relazione che coinvolge il nodo canonico `FHIR Synthetic Patients`.

<!-- GRAPH_NODE graph=unificata index=505 id=Request-Dataset-Generator::readme_fhir_synthetic_patients -->
Nel grafo unificato, l'etichetta originale del nodo canonico `FHIR Synthetic Patients` è `FHIR Synthetic Patients`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `FHIR Synthetic Patients` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `FHIR Synthetic Patients` è `README.md`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `FHIR Synthetic Patients` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `FHIR Synthetic Patients` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `FHIR Synthetic Patients` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `FHIR Synthetic Patients` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `FHIR Synthetic Patients` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `FHIR Synthetic Patients` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `FHIR Synthetic Patients` è `fhir synthetic patients`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `FHIR Synthetic Patients` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `FHIR Synthetic Patients` è `readme_fhir_synthetic_patients`. Nel grafo unificato, il nodo canonico `FHIR Synthetic Patients` possiede l'identificatore esatto `Request-Dataset-Generator::readme_fhir_synthetic_patients`.
Nel grafo unificato, il nodo canonico `FHIR Synthetic Patients` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=998 role=incoming -->
Nel grafo unificato, il nodo canonico `FHIR Synthetic Patients` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Request Dataset Generator RVE`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_request_dataset_generator_rve` all'ID `Request-Dataset-Generator::readme_fhir_synthetic_patients`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE` fa riferimento a `FHIR Synthetic Patients`. Nel grafo unificato, il tipo dichiarato dell'arco 998 che coinvolge il nodo canonico `FHIR Synthetic Patients` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 998 che coinvolge il nodo canonico `FHIR Synthetic Patients` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 998 che coinvolge il nodo canonico `FHIR Synthetic Patients` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 998 che coinvolge il nodo canonico `FHIR Synthetic Patients` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 998 che coinvolge il nodo canonico `FHIR Synthetic Patients` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 998 che coinvolge il nodo canonico `FHIR Synthetic Patients` è `1.0`. Nel grafo unificato, l'arco 998 registra come `source` l'identificatore `Request-Dataset-Generator::readme_request_dataset_generator_rve` per la relazione che coinvolge il nodo canonico `FHIR Synthetic Patients`. Nel grafo unificato, l'arco 998 registra come `target` l'identificatore `Request-Dataset-Generator::readme_fhir_synthetic_patients` per la relazione che coinvolge il nodo canonico `FHIR Synthetic Patients`.

### FLOW_CONTEXT_CALL [readme_flow_context_call]

<!-- GRAPH_NODE graph=codice index=507 id=readme_flow_context_call -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `FLOW_CONTEXT_CALL`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` possiede l'identificatore esatto `readme_flow_context_call`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `flow_context_call`.
<!-- GRAPH_EDGE graph=codice index=1004 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è la sorgente `source` della relazione `semantically_similar_to` verso il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]`; l'orientamento registrato nel JSON va dall'ID `readme_flow_context_call` all'ID `usage_flow_context_call`. La relazione tecnica esplicita è la seguente: `FLOW_CONTEXT_CALL [readme_flow_context_call]` è semanticamente simile a `FLOW_CONTEXT_CALL [usage_flow_context_call]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `semantically_similar_to`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `INFERRED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `0.95`. Nel grafo del codice RDG, il file sorgente dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `1.0`. Nel grafo del codice RDG, l'arco 1004 registra come `source` l'identificatore `readme_flow_context_call` per la relazione che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]`. Nel grafo del codice RDG, l'arco 1004 registra come `target` l'identificatore `usage_flow_context_call` per la relazione che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]`.
<!-- GRAPH_EDGE graph=codice index=1000 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `RVE Flows [readme_rve_flows]`; l'orientamento registrato nel JSON va dall'ID `readme_rve_flows` all'ID `readme_flow_context_call`. La relazione tecnica esplicita è la seguente: `RVE Flows [readme_rve_flows]` fa riferimento a `FLOW_CONTEXT_CALL [readme_flow_context_call]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1000 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1000 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1000 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1000 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1000 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1000 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `1.0`. Nel grafo del codice RDG, l'arco 1000 registra come `source` l'identificatore `readme_rve_flows` per la relazione che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]`. Nel grafo del codice RDG, l'arco 1000 registra come `target` l'identificatore `readme_flow_context_call` per la relazione che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]`.

<!-- GRAPH_NODE graph=unificata index=507 id=Request-Dataset-Generator::readme_flow_context_call -->
Nel grafo unificato, l'etichetta originale del nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `FLOW_CONTEXT_CALL`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `README.md`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `flow_context_call`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `readme_flow_context_call`. Nel grafo unificato, il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` possiede l'identificatore esatto `Request-Dataset-Generator::readme_flow_context_call`.
<!-- GRAPH_EDGE graph=unificata index=1004 role=outgoing -->
Nel grafo unificato, il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è la sorgente `source` della relazione `semantically_similar_to` verso il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_flow_context_call` all'ID `Request-Dataset-Generator::usage_flow_context_call`. La relazione tecnica esplicita è la seguente: `FLOW_CONTEXT_CALL [readme_flow_context_call]` è semanticamente simile a `FLOW_CONTEXT_CALL [usage_flow_context_call]`. Nel grafo unificato, il tipo dichiarato dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `semantically_similar_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `INFERRED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `0.95`. Nel grafo unificato, il file sorgente dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `1.0`. Nel grafo unificato, l'arco 1004 registra come `source` l'identificatore `Request-Dataset-Generator::readme_flow_context_call` per la relazione che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]`. Nel grafo unificato, l'arco 1004 registra come `target` l'identificatore `Request-Dataset-Generator::usage_flow_context_call` per la relazione che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]`.
<!-- GRAPH_EDGE graph=unificata index=1000 role=incoming -->
Nel grafo unificato, il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `RVE Flows [readme_rve_flows]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_rve_flows` all'ID `Request-Dataset-Generator::readme_flow_context_call`. La relazione tecnica esplicita è la seguente: `RVE Flows [readme_rve_flows]` fa riferimento a `FLOW_CONTEXT_CALL [readme_flow_context_call]`. Nel grafo unificato, il tipo dichiarato dell'arco 1000 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1000 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1000 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1000 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1000 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1000 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]` è `1.0`. Nel grafo unificato, l'arco 1000 registra come `source` l'identificatore `Request-Dataset-Generator::readme_rve_flows` per la relazione che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]`. Nel grafo unificato, l'arco 1000 registra come `target` l'identificatore `Request-Dataset-Generator::readme_flow_context_call` per la relazione che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]`.

### FLOW_CONTEXT_CALL [usage_flow_context_call]

<!-- GRAPH_NODE graph=codice index=523 id=usage_flow_context_call -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `FLOW_CONTEXT_CALL`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` possiede l'identificatore esatto `usage_flow_context_call`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `flow_context_call`.
Nel grafo del codice RDG, il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=1004 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è il destinatario `target` della relazione `semantically_similar_to` proveniente dal nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]`; l'orientamento registrato nel JSON va dall'ID `readme_flow_context_call` all'ID `usage_flow_context_call`. La relazione tecnica esplicita è la seguente: `FLOW_CONTEXT_CALL [readme_flow_context_call]` è semanticamente simile a `FLOW_CONTEXT_CALL [usage_flow_context_call]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `semantically_similar_to`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `INFERRED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `0.95`. Nel grafo del codice RDG, il file sorgente dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `1.0`. Nel grafo del codice RDG, l'arco 1004 registra come `source` l'identificatore `readme_flow_context_call` per la relazione che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]`. Nel grafo del codice RDG, l'arco 1004 registra come `target` l'identificatore `usage_flow_context_call` per la relazione che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]`.
<!-- GRAPH_EDGE graph=codice index=1023 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `RVE Flows [usage_rve_flows]`; l'orientamento registrato nel JSON va dall'ID `usage_rve_flows` all'ID `usage_flow_context_call`. La relazione tecnica esplicita è la seguente: `RVE Flows [usage_rve_flows]` fa riferimento a `FLOW_CONTEXT_CALL [usage_flow_context_call]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1023 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1023 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1023 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1023 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1023 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1023 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `1.0`. Nel grafo del codice RDG, l'arco 1023 registra come `source` l'identificatore `usage_rve_flows` per la relazione che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]`. Nel grafo del codice RDG, l'arco 1023 registra come `target` l'identificatore `usage_flow_context_call` per la relazione che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]`.

<!-- GRAPH_NODE graph=unificata index=523 id=Request-Dataset-Generator::usage_flow_context_call -->
Nel grafo unificato, l'etichetta originale del nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `FLOW_CONTEXT_CALL`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `usage.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `flow_context_call`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `usage_flow_context_call`. Nel grafo unificato, il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` possiede l'identificatore esatto `Request-Dataset-Generator::usage_flow_context_call`.
Nel grafo unificato, il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1004 role=incoming -->
Nel grafo unificato, il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è il destinatario `target` della relazione `semantically_similar_to` proveniente dal nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_flow_context_call` all'ID `Request-Dataset-Generator::usage_flow_context_call`. La relazione tecnica esplicita è la seguente: `FLOW_CONTEXT_CALL [readme_flow_context_call]` è semanticamente simile a `FLOW_CONTEXT_CALL [usage_flow_context_call]`. Nel grafo unificato, il tipo dichiarato dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `semantically_similar_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `INFERRED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `0.95`. Nel grafo unificato, il file sorgente dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1004 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `1.0`. Nel grafo unificato, l'arco 1004 registra come `source` l'identificatore `Request-Dataset-Generator::readme_flow_context_call` per la relazione che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]`. Nel grafo unificato, l'arco 1004 registra come `target` l'identificatore `Request-Dataset-Generator::usage_flow_context_call` per la relazione che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]`.
<!-- GRAPH_EDGE graph=unificata index=1023 role=incoming -->
Nel grafo unificato, il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `RVE Flows [usage_rve_flows]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_rve_flows` all'ID `Request-Dataset-Generator::usage_flow_context_call`. La relazione tecnica esplicita è la seguente: `RVE Flows [usage_rve_flows]` fa riferimento a `FLOW_CONTEXT_CALL [usage_flow_context_call]`. Nel grafo unificato, il tipo dichiarato dell'arco 1023 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1023 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1023 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1023 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1023 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1023 che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]` è `1.0`. Nel grafo unificato, l'arco 1023 registra come `source` l'identificatore `Request-Dataset-Generator::usage_rve_flows` per la relazione che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]`. Nel grafo unificato, l'arco 1023 registra come `target` l'identificatore `Request-Dataset-Generator::usage_flow_context_call` per la relazione che coinvolge il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]`.

### FLOW_PATIENT_CREATE [readme_flow_patient_create]

<!-- GRAPH_NODE graph=codice index=509 id=readme_flow_patient_create -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `FLOW_PATIENT_CREATE`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` possiede l'identificatore esatto `readme_flow_patient_create`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `flow_patient_create`.
<!-- GRAPH_EDGE graph=codice index=1006 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è la sorgente `source` della relazione `semantically_similar_to` verso il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]`; l'orientamento registrato nel JSON va dall'ID `readme_flow_patient_create` all'ID `usage_flow_patient_create`. La relazione tecnica esplicita è la seguente: `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è semanticamente simile a `FLOW_PATIENT_CREATE [usage_flow_patient_create]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `semantically_similar_to`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `INFERRED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `0.95`. Nel grafo del codice RDG, il file sorgente dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `1.0`. Nel grafo del codice RDG, l'arco 1006 registra come `source` l'identificatore `readme_flow_patient_create` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]`. Nel grafo del codice RDG, l'arco 1006 registra come `target` l'identificatore `usage_flow_patient_create` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]`.
<!-- GRAPH_EDGE graph=codice index=1001 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `RVE Flows [readme_rve_flows]`; l'orientamento registrato nel JSON va dall'ID `readme_rve_flows` all'ID `readme_flow_patient_create`. La relazione tecnica esplicita è la seguente: `RVE Flows [readme_rve_flows]` fa riferimento a `FLOW_PATIENT_CREATE [readme_flow_patient_create]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1001 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1001 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1001 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1001 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1001 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1001 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `1.0`. Nel grafo del codice RDG, l'arco 1001 registra come `source` l'identificatore `readme_rve_flows` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]`. Nel grafo del codice RDG, l'arco 1001 registra come `target` l'identificatore `readme_flow_patient_create` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]`.

<!-- GRAPH_NODE graph=unificata index=509 id=Request-Dataset-Generator::readme_flow_patient_create -->
Nel grafo unificato, l'etichetta originale del nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `FLOW_PATIENT_CREATE`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `README.md`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `flow_patient_create`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `readme_flow_patient_create`. Nel grafo unificato, il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` possiede l'identificatore esatto `Request-Dataset-Generator::readme_flow_patient_create`.
<!-- GRAPH_EDGE graph=unificata index=1006 role=outgoing -->
Nel grafo unificato, il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è la sorgente `source` della relazione `semantically_similar_to` verso il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_flow_patient_create` all'ID `Request-Dataset-Generator::usage_flow_patient_create`. La relazione tecnica esplicita è la seguente: `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è semanticamente simile a `FLOW_PATIENT_CREATE [usage_flow_patient_create]`. Nel grafo unificato, il tipo dichiarato dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `semantically_similar_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `INFERRED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `0.95`. Nel grafo unificato, il file sorgente dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `1.0`. Nel grafo unificato, l'arco 1006 registra come `source` l'identificatore `Request-Dataset-Generator::readme_flow_patient_create` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]`. Nel grafo unificato, l'arco 1006 registra come `target` l'identificatore `Request-Dataset-Generator::usage_flow_patient_create` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]`.
<!-- GRAPH_EDGE graph=unificata index=1001 role=incoming -->
Nel grafo unificato, il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `RVE Flows [readme_rve_flows]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_rve_flows` all'ID `Request-Dataset-Generator::readme_flow_patient_create`. La relazione tecnica esplicita è la seguente: `RVE Flows [readme_rve_flows]` fa riferimento a `FLOW_PATIENT_CREATE [readme_flow_patient_create]`. Nel grafo unificato, il tipo dichiarato dell'arco 1001 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1001 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1001 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1001 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1001 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1001 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è `1.0`. Nel grafo unificato, l'arco 1001 registra come `source` l'identificatore `Request-Dataset-Generator::readme_rve_flows` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]`. Nel grafo unificato, l'arco 1001 registra come `target` l'identificatore `Request-Dataset-Generator::readme_flow_patient_create` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]`.

### FLOW_PATIENT_CREATE [usage_flow_patient_create]

<!-- GRAPH_NODE graph=codice index=525 id=usage_flow_patient_create -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `FLOW_PATIENT_CREATE`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` possiede l'identificatore esatto `usage_flow_patient_create`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `flow_patient_create`.
Nel grafo del codice RDG, il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=1006 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è il destinatario `target` della relazione `semantically_similar_to` proveniente dal nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]`; l'orientamento registrato nel JSON va dall'ID `readme_flow_patient_create` all'ID `usage_flow_patient_create`. La relazione tecnica esplicita è la seguente: `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è semanticamente simile a `FLOW_PATIENT_CREATE [usage_flow_patient_create]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `semantically_similar_to`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `INFERRED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `0.95`. Nel grafo del codice RDG, il file sorgente dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `1.0`. Nel grafo del codice RDG, l'arco 1006 registra come `source` l'identificatore `readme_flow_patient_create` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]`. Nel grafo del codice RDG, l'arco 1006 registra come `target` l'identificatore `usage_flow_patient_create` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]`.
<!-- GRAPH_EDGE graph=codice index=1024 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `RVE Flows [usage_rve_flows]`; l'orientamento registrato nel JSON va dall'ID `usage_rve_flows` all'ID `usage_flow_patient_create`. La relazione tecnica esplicita è la seguente: `RVE Flows [usage_rve_flows]` fa riferimento a `FLOW_PATIENT_CREATE [usage_flow_patient_create]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1024 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1024 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1024 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1024 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1024 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1024 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `1.0`. Nel grafo del codice RDG, l'arco 1024 registra come `source` l'identificatore `usage_rve_flows` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]`. Nel grafo del codice RDG, l'arco 1024 registra come `target` l'identificatore `usage_flow_patient_create` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]`.

<!-- GRAPH_NODE graph=unificata index=525 id=Request-Dataset-Generator::usage_flow_patient_create -->
Nel grafo unificato, l'etichetta originale del nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `FLOW_PATIENT_CREATE`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `usage.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `flow_patient_create`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `usage_flow_patient_create`. Nel grafo unificato, il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` possiede l'identificatore esatto `Request-Dataset-Generator::usage_flow_patient_create`.
Nel grafo unificato, il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1006 role=incoming -->
Nel grafo unificato, il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è il destinatario `target` della relazione `semantically_similar_to` proveniente dal nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_flow_patient_create` all'ID `Request-Dataset-Generator::usage_flow_patient_create`. La relazione tecnica esplicita è la seguente: `FLOW_PATIENT_CREATE [readme_flow_patient_create]` è semanticamente simile a `FLOW_PATIENT_CREATE [usage_flow_patient_create]`. Nel grafo unificato, il tipo dichiarato dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `semantically_similar_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `INFERRED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `0.95`. Nel grafo unificato, il file sorgente dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1006 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `1.0`. Nel grafo unificato, l'arco 1006 registra come `source` l'identificatore `Request-Dataset-Generator::readme_flow_patient_create` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]`. Nel grafo unificato, l'arco 1006 registra come `target` l'identificatore `Request-Dataset-Generator::usage_flow_patient_create` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]`.
<!-- GRAPH_EDGE graph=unificata index=1024 role=incoming -->
Nel grafo unificato, il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `RVE Flows [usage_rve_flows]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_rve_flows` all'ID `Request-Dataset-Generator::usage_flow_patient_create`. La relazione tecnica esplicita è la seguente: `RVE Flows [usage_rve_flows]` fa riferimento a `FLOW_PATIENT_CREATE [usage_flow_patient_create]`. Nel grafo unificato, il tipo dichiarato dell'arco 1024 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1024 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1024 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1024 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1024 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1024 che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]` è `1.0`. Nel grafo unificato, l'arco 1024 registra come `source` l'identificatore `Request-Dataset-Generator::usage_rve_flows` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]`. Nel grafo unificato, l'arco 1024 registra come `target` l'identificatore `Request-Dataset-Generator::usage_flow_patient_create` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]`.

### FLOW_PATIENT_QUERY [readme_flow_patient_query]

<!-- GRAPH_NODE graph=codice index=508 id=readme_flow_patient_query -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `FLOW_PATIENT_QUERY`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` possiede l'identificatore esatto `readme_flow_patient_query`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `flow_patient_query`.
<!-- GRAPH_EDGE graph=codice index=1005 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è la sorgente `source` della relazione `semantically_similar_to` verso il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]`; l'orientamento registrato nel JSON va dall'ID `readme_flow_patient_query` all'ID `usage_flow_patient_query`. La relazione tecnica esplicita è la seguente: `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è semanticamente simile a `FLOW_PATIENT_QUERY [usage_flow_patient_query]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `semantically_similar_to`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `INFERRED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `0.95`. Nel grafo del codice RDG, il file sorgente dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `1.0`. Nel grafo del codice RDG, l'arco 1005 registra come `source` l'identificatore `readme_flow_patient_query` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]`. Nel grafo del codice RDG, l'arco 1005 registra come `target` l'identificatore `usage_flow_patient_query` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]`.
<!-- GRAPH_EDGE graph=codice index=1002 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `RVE Flows [readme_rve_flows]`; l'orientamento registrato nel JSON va dall'ID `readme_rve_flows` all'ID `readme_flow_patient_query`. La relazione tecnica esplicita è la seguente: `RVE Flows [readme_rve_flows]` fa riferimento a `FLOW_PATIENT_QUERY [readme_flow_patient_query]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1002 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1002 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1002 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1002 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1002 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1002 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `1.0`. Nel grafo del codice RDG, l'arco 1002 registra come `source` l'identificatore `readme_rve_flows` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]`. Nel grafo del codice RDG, l'arco 1002 registra come `target` l'identificatore `readme_flow_patient_query` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]`.

<!-- GRAPH_NODE graph=unificata index=508 id=Request-Dataset-Generator::readme_flow_patient_query -->
Nel grafo unificato, l'etichetta originale del nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `FLOW_PATIENT_QUERY`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `README.md`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `flow_patient_query`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `readme_flow_patient_query`. Nel grafo unificato, il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` possiede l'identificatore esatto `Request-Dataset-Generator::readme_flow_patient_query`.
<!-- GRAPH_EDGE graph=unificata index=1005 role=outgoing -->
Nel grafo unificato, il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è la sorgente `source` della relazione `semantically_similar_to` verso il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_flow_patient_query` all'ID `Request-Dataset-Generator::usage_flow_patient_query`. La relazione tecnica esplicita è la seguente: `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è semanticamente simile a `FLOW_PATIENT_QUERY [usage_flow_patient_query]`. Nel grafo unificato, il tipo dichiarato dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `semantically_similar_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `INFERRED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `0.95`. Nel grafo unificato, il file sorgente dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `1.0`. Nel grafo unificato, l'arco 1005 registra come `source` l'identificatore `Request-Dataset-Generator::readme_flow_patient_query` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]`. Nel grafo unificato, l'arco 1005 registra come `target` l'identificatore `Request-Dataset-Generator::usage_flow_patient_query` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]`.
<!-- GRAPH_EDGE graph=unificata index=1002 role=incoming -->
Nel grafo unificato, il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `RVE Flows [readme_rve_flows]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_rve_flows` all'ID `Request-Dataset-Generator::readme_flow_patient_query`. La relazione tecnica esplicita è la seguente: `RVE Flows [readme_rve_flows]` fa riferimento a `FLOW_PATIENT_QUERY [readme_flow_patient_query]`. Nel grafo unificato, il tipo dichiarato dell'arco 1002 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1002 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1002 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1002 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1002 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1002 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è `1.0`. Nel grafo unificato, l'arco 1002 registra come `source` l'identificatore `Request-Dataset-Generator::readme_rve_flows` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]`. Nel grafo unificato, l'arco 1002 registra come `target` l'identificatore `Request-Dataset-Generator::readme_flow_patient_query` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]`.

### FLOW_PATIENT_QUERY [usage_flow_patient_query]

<!-- GRAPH_NODE graph=codice index=524 id=usage_flow_patient_query -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `FLOW_PATIENT_QUERY`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` possiede l'identificatore esatto `usage_flow_patient_query`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `flow_patient_query`.
Nel grafo del codice RDG, il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=1005 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è il destinatario `target` della relazione `semantically_similar_to` proveniente dal nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]`; l'orientamento registrato nel JSON va dall'ID `readme_flow_patient_query` all'ID `usage_flow_patient_query`. La relazione tecnica esplicita è la seguente: `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è semanticamente simile a `FLOW_PATIENT_QUERY [usage_flow_patient_query]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `semantically_similar_to`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `INFERRED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `0.95`. Nel grafo del codice RDG, il file sorgente dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `1.0`. Nel grafo del codice RDG, l'arco 1005 registra come `source` l'identificatore `readme_flow_patient_query` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]`. Nel grafo del codice RDG, l'arco 1005 registra come `target` l'identificatore `usage_flow_patient_query` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]`.
<!-- GRAPH_EDGE graph=codice index=1025 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `RVE Flows [usage_rve_flows]`; l'orientamento registrato nel JSON va dall'ID `usage_rve_flows` all'ID `usage_flow_patient_query`. La relazione tecnica esplicita è la seguente: `RVE Flows [usage_rve_flows]` fa riferimento a `FLOW_PATIENT_QUERY [usage_flow_patient_query]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1025 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1025 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1025 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1025 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1025 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1025 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `1.0`. Nel grafo del codice RDG, l'arco 1025 registra come `source` l'identificatore `usage_rve_flows` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]`. Nel grafo del codice RDG, l'arco 1025 registra come `target` l'identificatore `usage_flow_patient_query` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]`.

<!-- GRAPH_NODE graph=unificata index=524 id=Request-Dataset-Generator::usage_flow_patient_query -->
Nel grafo unificato, l'etichetta originale del nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `FLOW_PATIENT_QUERY`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `usage.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `flow_patient_query`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `usage_flow_patient_query`. Nel grafo unificato, il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` possiede l'identificatore esatto `Request-Dataset-Generator::usage_flow_patient_query`.
Nel grafo unificato, il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1005 role=incoming -->
Nel grafo unificato, il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è il destinatario `target` della relazione `semantically_similar_to` proveniente dal nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_flow_patient_query` all'ID `Request-Dataset-Generator::usage_flow_patient_query`. La relazione tecnica esplicita è la seguente: `FLOW_PATIENT_QUERY [readme_flow_patient_query]` è semanticamente simile a `FLOW_PATIENT_QUERY [usage_flow_patient_query]`. Nel grafo unificato, il tipo dichiarato dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `semantically_similar_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `INFERRED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `0.95`. Nel grafo unificato, il file sorgente dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1005 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `1.0`. Nel grafo unificato, l'arco 1005 registra come `source` l'identificatore `Request-Dataset-Generator::readme_flow_patient_query` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]`. Nel grafo unificato, l'arco 1005 registra come `target` l'identificatore `Request-Dataset-Generator::usage_flow_patient_query` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]`.
<!-- GRAPH_EDGE graph=unificata index=1025 role=incoming -->
Nel grafo unificato, il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `RVE Flows [usage_rve_flows]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_rve_flows` all'ID `Request-Dataset-Generator::usage_flow_patient_query`. La relazione tecnica esplicita è la seguente: `RVE Flows [usage_rve_flows]` fa riferimento a `FLOW_PATIENT_QUERY [usage_flow_patient_query]`. Nel grafo unificato, il tipo dichiarato dell'arco 1025 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1025 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1025 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1025 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1025 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1025 che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]` è `1.0`. Nel grafo unificato, l'arco 1025 registra come `source` l'identificatore `Request-Dataset-Generator::usage_rve_flows` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]`. Nel grafo unificato, l'arco 1025 registra come `target` l'identificatore `Request-Dataset-Generator::usage_flow_patient_query` per la relazione che coinvolge il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]`.

### main.py Command Dispatch

<!-- GRAPH_NODE graph=codice index=526 id=usage_command_dispatch -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `main.py Command Dispatch` è `main.py Command Dispatch`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `main.py Command Dispatch` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `main.py Command Dispatch` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `main.py Command Dispatch` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `main.py Command Dispatch` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `main.py Command Dispatch` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `main.py Command Dispatch` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `main.py Command Dispatch` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `main.py Command Dispatch` possiede l'identificatore esatto `usage_command_dispatch`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `main.py Command Dispatch` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `main.py Command Dispatch` è `main.py command dispatch`.
<!-- GRAPH_EDGE graph=codice index=1026 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main.py Command Dispatch` è la sorgente `source` della relazione `implements` verso il nodo canonico `Pipeline [usage_pipeline]`; l'orientamento registrato nel JSON va dall'ID `usage_command_dispatch` all'ID `usage_pipeline`. La relazione tecnica esplicita è la seguente: `main.py Command Dispatch` implementa `Pipeline [usage_pipeline]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1026 che coinvolge il nodo canonico `main.py Command Dispatch` è `implements`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1026 che coinvolge il nodo canonico `main.py Command Dispatch` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1026 che coinvolge il nodo canonico `main.py Command Dispatch` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1026 che coinvolge il nodo canonico `main.py Command Dispatch` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1026 che coinvolge il nodo canonico `main.py Command Dispatch` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1026 che coinvolge il nodo canonico `main.py Command Dispatch` è `1.0`. Nel grafo del codice RDG, l'arco 1026 registra come `source` l'identificatore `usage_command_dispatch` per la relazione che coinvolge il nodo canonico `main.py Command Dispatch`. Nel grafo del codice RDG, l'arco 1026 registra come `target` l'identificatore `usage_pipeline` per la relazione che coinvolge il nodo canonico `main.py Command Dispatch`.
<!-- GRAPH_EDGE graph=codice index=1017 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `main.py Command Dispatch` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Request Dataset Generator RVE Guide`; l'orientamento registrato nel JSON va dall'ID `usage_request_dataset_generator_rve_guide` all'ID `usage_command_dispatch`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `main.py Command Dispatch`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1017 che coinvolge il nodo canonico `main.py Command Dispatch` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1017 che coinvolge il nodo canonico `main.py Command Dispatch` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1017 che coinvolge il nodo canonico `main.py Command Dispatch` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1017 che coinvolge il nodo canonico `main.py Command Dispatch` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1017 che coinvolge il nodo canonico `main.py Command Dispatch` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1017 che coinvolge il nodo canonico `main.py Command Dispatch` è `1.0`. Nel grafo del codice RDG, l'arco 1017 registra come `source` l'identificatore `usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `main.py Command Dispatch`. Nel grafo del codice RDG, l'arco 1017 registra come `target` l'identificatore `usage_command_dispatch` per la relazione che coinvolge il nodo canonico `main.py Command Dispatch`.

<!-- GRAPH_NODE graph=unificata index=526 id=Request-Dataset-Generator::usage_command_dispatch -->
Nel grafo unificato, l'etichetta originale del nodo canonico `main.py Command Dispatch` è `main.py Command Dispatch`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `main.py Command Dispatch` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `main.py Command Dispatch` è `usage.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `main.py Command Dispatch` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `main.py Command Dispatch` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `main.py Command Dispatch` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `main.py Command Dispatch` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `main.py Command Dispatch` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `main.py Command Dispatch` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `main.py Command Dispatch` è `main.py command dispatch`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `main.py Command Dispatch` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `main.py Command Dispatch` è `usage_command_dispatch`. Nel grafo unificato, il nodo canonico `main.py Command Dispatch` possiede l'identificatore esatto `Request-Dataset-Generator::usage_command_dispatch`.
<!-- GRAPH_EDGE graph=unificata index=1026 role=outgoing -->
Nel grafo unificato, il nodo canonico `main.py Command Dispatch` è la sorgente `source` della relazione `implements` verso il nodo canonico `Pipeline [usage_pipeline]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_command_dispatch` all'ID `Request-Dataset-Generator::usage_pipeline`. La relazione tecnica esplicita è la seguente: `main.py Command Dispatch` implementa `Pipeline [usage_pipeline]`. Nel grafo unificato, il tipo dichiarato dell'arco 1026 che coinvolge il nodo canonico `main.py Command Dispatch` è `implements`. Nel grafo unificato, la confidenza categoriale dell'arco 1026 che coinvolge il nodo canonico `main.py Command Dispatch` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1026 che coinvolge il nodo canonico `main.py Command Dispatch` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1026 che coinvolge il nodo canonico `main.py Command Dispatch` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1026 che coinvolge il nodo canonico `main.py Command Dispatch` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1026 che coinvolge il nodo canonico `main.py Command Dispatch` è `1.0`. Nel grafo unificato, l'arco 1026 registra come `source` l'identificatore `Request-Dataset-Generator::usage_command_dispatch` per la relazione che coinvolge il nodo canonico `main.py Command Dispatch`. Nel grafo unificato, l'arco 1026 registra come `target` l'identificatore `Request-Dataset-Generator::usage_pipeline` per la relazione che coinvolge il nodo canonico `main.py Command Dispatch`.
<!-- GRAPH_EDGE graph=unificata index=1017 role=incoming -->
Nel grafo unificato, il nodo canonico `main.py Command Dispatch` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Request Dataset Generator RVE Guide`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` all'ID `Request-Dataset-Generator::usage_command_dispatch`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `main.py Command Dispatch`. Nel grafo unificato, il tipo dichiarato dell'arco 1017 che coinvolge il nodo canonico `main.py Command Dispatch` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1017 che coinvolge il nodo canonico `main.py Command Dispatch` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1017 che coinvolge il nodo canonico `main.py Command Dispatch` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1017 che coinvolge il nodo canonico `main.py Command Dispatch` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1017 che coinvolge il nodo canonico `main.py Command Dispatch` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1017 che coinvolge il nodo canonico `main.py Command Dispatch` è `1.0`. Nel grafo unificato, l'arco 1017 registra come `source` l'identificatore `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `main.py Command Dispatch`. Nel grafo unificato, l'arco 1017 registra come `target` l'identificatore `Request-Dataset-Generator::usage_command_dispatch` per la relazione che coinvolge il nodo canonico `main.py Command Dispatch`.

### matplotlib [requirements_matplotlib]

<!-- GRAPH_NODE graph=codice index=517 id=requirements_matplotlib -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `matplotlib [requirements_matplotlib]` è `matplotlib`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `matplotlib [requirements_matplotlib]` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `matplotlib [requirements_matplotlib]` è `requirements.txt`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `matplotlib [requirements_matplotlib]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `matplotlib [requirements_matplotlib]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `matplotlib [requirements_matplotlib]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `matplotlib [requirements_matplotlib]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `matplotlib [requirements_matplotlib]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `matplotlib [requirements_matplotlib]` possiede l'identificatore esatto `requirements_matplotlib`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `matplotlib [requirements_matplotlib]` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `matplotlib [requirements_matplotlib]` è `matplotlib`.
<!-- GRAPH_EDGE graph=codice index=1014 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `matplotlib [requirements_matplotlib]` è la sorgente `source` della relazione `semantically_similar_to` verso il nodo canonico `matplotlib [ui_requirements_matplotlib]`; l'orientamento registrato nel JSON va dall'ID `requirements_matplotlib` all'ID `ui_requirements_matplotlib`. La relazione tecnica esplicita è la seguente: `matplotlib [requirements_matplotlib]` è semanticamente simile a `matplotlib [ui_requirements_matplotlib]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1014 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `semantically_similar_to`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1014 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `INFERRED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1014 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `0.95`. Nel grafo del codice RDG, il file sorgente dell'arco 1014 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `requirements.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1014 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1014 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `1.0`. Nel grafo del codice RDG, l'arco 1014 registra come `source` l'identificatore `requirements_matplotlib` per la relazione che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]`. Nel grafo del codice RDG, l'arco 1014 registra come `target` l'identificatore `ui_requirements_matplotlib` per la relazione che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]`.
<!-- GRAPH_EDGE graph=codice index=1013 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `matplotlib [requirements_matplotlib]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Runtime Dependencies`; l'orientamento registrato nel JSON va dall'ID `requirements_runtime_dependencies` all'ID `requirements_matplotlib`. La relazione tecnica esplicita è la seguente: `Runtime Dependencies` fa riferimento a `matplotlib [requirements_matplotlib]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1013 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1013 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1013 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1013 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `requirements.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1013 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1013 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `1.0`. Nel grafo del codice RDG, l'arco 1013 registra come `source` l'identificatore `requirements_runtime_dependencies` per la relazione che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]`. Nel grafo del codice RDG, l'arco 1013 registra come `target` l'identificatore `requirements_matplotlib` per la relazione che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]`.

<!-- GRAPH_NODE graph=unificata index=517 id=Request-Dataset-Generator::requirements_matplotlib -->
Nel grafo unificato, l'etichetta originale del nodo canonico `matplotlib [requirements_matplotlib]` è `matplotlib`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `matplotlib [requirements_matplotlib]` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `matplotlib [requirements_matplotlib]` è `requirements.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `matplotlib [requirements_matplotlib]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `matplotlib [requirements_matplotlib]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `matplotlib [requirements_matplotlib]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `matplotlib [requirements_matplotlib]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `matplotlib [requirements_matplotlib]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `matplotlib [requirements_matplotlib]` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `matplotlib [requirements_matplotlib]` è `matplotlib`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `matplotlib [requirements_matplotlib]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `matplotlib [requirements_matplotlib]` è `requirements_matplotlib`. Nel grafo unificato, il nodo canonico `matplotlib [requirements_matplotlib]` possiede l'identificatore esatto `Request-Dataset-Generator::requirements_matplotlib`.
<!-- GRAPH_EDGE graph=unificata index=1014 role=outgoing -->
Nel grafo unificato, il nodo canonico `matplotlib [requirements_matplotlib]` è la sorgente `source` della relazione `semantically_similar_to` verso il nodo canonico `matplotlib [ui_requirements_matplotlib]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::requirements_matplotlib` all'ID `Request-Dataset-Generator::ui_requirements_matplotlib`. La relazione tecnica esplicita è la seguente: `matplotlib [requirements_matplotlib]` è semanticamente simile a `matplotlib [ui_requirements_matplotlib]`. Nel grafo unificato, il tipo dichiarato dell'arco 1014 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `semantically_similar_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1014 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `INFERRED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1014 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `0.95`. Nel grafo unificato, il file sorgente dell'arco 1014 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `requirements.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1014 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1014 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `1.0`. Nel grafo unificato, l'arco 1014 registra come `source` l'identificatore `Request-Dataset-Generator::requirements_matplotlib` per la relazione che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]`. Nel grafo unificato, l'arco 1014 registra come `target` l'identificatore `Request-Dataset-Generator::ui_requirements_matplotlib` per la relazione che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]`.
<!-- GRAPH_EDGE graph=unificata index=1013 role=incoming -->
Nel grafo unificato, il nodo canonico `matplotlib [requirements_matplotlib]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Runtime Dependencies`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::requirements_runtime_dependencies` all'ID `Request-Dataset-Generator::requirements_matplotlib`. La relazione tecnica esplicita è la seguente: `Runtime Dependencies` fa riferimento a `matplotlib [requirements_matplotlib]`. Nel grafo unificato, il tipo dichiarato dell'arco 1013 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1013 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1013 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1013 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `requirements.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1013 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1013 che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]` è `1.0`. Nel grafo unificato, l'arco 1013 registra come `source` l'identificatore `Request-Dataset-Generator::requirements_runtime_dependencies` per la relazione che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]`. Nel grafo unificato, l'arco 1013 registra come `target` l'identificatore `Request-Dataset-Generator::requirements_matplotlib` per la relazione che coinvolge il nodo canonico `matplotlib [requirements_matplotlib]`.

### matplotlib [ui_requirements_matplotlib]

<!-- GRAPH_NODE graph=codice index=520 id=ui_requirements_matplotlib -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `matplotlib [ui_requirements_matplotlib]` è `matplotlib`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `matplotlib [ui_requirements_matplotlib]` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `ui/requirements.txt`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `matplotlib [ui_requirements_matplotlib]` possiede l'identificatore esatto `ui_requirements_matplotlib`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `matplotlib [ui_requirements_matplotlib]` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `matplotlib [ui_requirements_matplotlib]` è `matplotlib`.
Nel grafo del codice RDG, il nodo canonico `matplotlib [ui_requirements_matplotlib]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=1014 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `matplotlib [ui_requirements_matplotlib]` è il destinatario `target` della relazione `semantically_similar_to` proveniente dal nodo canonico `matplotlib [requirements_matplotlib]`; l'orientamento registrato nel JSON va dall'ID `requirements_matplotlib` all'ID `ui_requirements_matplotlib`. La relazione tecnica esplicita è la seguente: `matplotlib [requirements_matplotlib]` è semanticamente simile a `matplotlib [ui_requirements_matplotlib]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1014 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `semantically_similar_to`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1014 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `INFERRED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1014 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `0.95`. Nel grafo del codice RDG, il file sorgente dell'arco 1014 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `requirements.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1014 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1014 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `1.0`. Nel grafo del codice RDG, l'arco 1014 registra come `source` l'identificatore `requirements_matplotlib` per la relazione che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]`. Nel grafo del codice RDG, l'arco 1014 registra come `target` l'identificatore `ui_requirements_matplotlib` per la relazione che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]`.
<!-- GRAPH_EDGE graph=codice index=1016 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `matplotlib [ui_requirements_matplotlib]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `UI Dependencies`; l'orientamento registrato nel JSON va dall'ID `ui_requirements_ui_dependencies` all'ID `ui_requirements_matplotlib`. La relazione tecnica esplicita è la seguente: `UI Dependencies` fa riferimento a `matplotlib [ui_requirements_matplotlib]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1016 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1016 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1016 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1016 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `ui/requirements.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1016 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1016 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `1.0`. Nel grafo del codice RDG, l'arco 1016 registra come `source` l'identificatore `ui_requirements_ui_dependencies` per la relazione che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]`. Nel grafo del codice RDG, l'arco 1016 registra come `target` l'identificatore `ui_requirements_matplotlib` per la relazione che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]`.

<!-- GRAPH_NODE graph=unificata index=520 id=Request-Dataset-Generator::ui_requirements_matplotlib -->
Nel grafo unificato, l'etichetta originale del nodo canonico `matplotlib [ui_requirements_matplotlib]` è `matplotlib`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `matplotlib [ui_requirements_matplotlib]` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `ui/requirements.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `matplotlib [ui_requirements_matplotlib]` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `matplotlib [ui_requirements_matplotlib]` è `matplotlib`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `matplotlib [ui_requirements_matplotlib]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `ui_requirements_matplotlib`. Nel grafo unificato, il nodo canonico `matplotlib [ui_requirements_matplotlib]` possiede l'identificatore esatto `Request-Dataset-Generator::ui_requirements_matplotlib`.
Nel grafo unificato, il nodo canonico `matplotlib [ui_requirements_matplotlib]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1014 role=incoming -->
Nel grafo unificato, il nodo canonico `matplotlib [ui_requirements_matplotlib]` è il destinatario `target` della relazione `semantically_similar_to` proveniente dal nodo canonico `matplotlib [requirements_matplotlib]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::requirements_matplotlib` all'ID `Request-Dataset-Generator::ui_requirements_matplotlib`. La relazione tecnica esplicita è la seguente: `matplotlib [requirements_matplotlib]` è semanticamente simile a `matplotlib [ui_requirements_matplotlib]`. Nel grafo unificato, il tipo dichiarato dell'arco 1014 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `semantically_similar_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1014 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `INFERRED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1014 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `0.95`. Nel grafo unificato, il file sorgente dell'arco 1014 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `requirements.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1014 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1014 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `1.0`. Nel grafo unificato, l'arco 1014 registra come `source` l'identificatore `Request-Dataset-Generator::requirements_matplotlib` per la relazione che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]`. Nel grafo unificato, l'arco 1014 registra come `target` l'identificatore `Request-Dataset-Generator::ui_requirements_matplotlib` per la relazione che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]`.
<!-- GRAPH_EDGE graph=unificata index=1016 role=incoming -->
Nel grafo unificato, il nodo canonico `matplotlib [ui_requirements_matplotlib]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `UI Dependencies`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_requirements_ui_dependencies` all'ID `Request-Dataset-Generator::ui_requirements_matplotlib`. La relazione tecnica esplicita è la seguente: `UI Dependencies` fa riferimento a `matplotlib [ui_requirements_matplotlib]`. Nel grafo unificato, il tipo dichiarato dell'arco 1016 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1016 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1016 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1016 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `ui/requirements.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1016 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1016 che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]` è `1.0`. Nel grafo unificato, l'arco 1016 registra come `source` l'identificatore `Request-Dataset-Generator::ui_requirements_ui_dependencies` per la relazione che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]`. Nel grafo unificato, l'arco 1016 registra come `target` l'identificatore `Request-Dataset-Generator::ui_requirements_matplotlib` per la relazione che coinvolge il nodo canonico `matplotlib [ui_requirements_matplotlib]`.

### Middleware Integrations

<!-- GRAPH_NODE graph=codice index=532 id=usage_middleware_integrations -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Middleware Integrations` è `Middleware Integrations`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Middleware Integrations` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Middleware Integrations` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Middleware Integrations` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `Middleware Integrations` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `Middleware Integrations` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `Middleware Integrations` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `Middleware Integrations` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `Middleware Integrations` possiede l'identificatore esatto `usage_middleware_integrations`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Middleware Integrations` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Middleware Integrations` è `middleware integrations`.
Nel grafo del codice RDG, il nodo canonico `Middleware Integrations` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=1020 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Middleware Integrations` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Request Dataset Generator RVE Guide`; l'orientamento registrato nel JSON va dall'ID `usage_request_dataset_generator_rve_guide` all'ID `usage_middleware_integrations`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `Middleware Integrations`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1020 che coinvolge il nodo canonico `Middleware Integrations` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1020 che coinvolge il nodo canonico `Middleware Integrations` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1020 che coinvolge il nodo canonico `Middleware Integrations` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1020 che coinvolge il nodo canonico `Middleware Integrations` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1020 che coinvolge il nodo canonico `Middleware Integrations` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1020 che coinvolge il nodo canonico `Middleware Integrations` è `1.0`. Nel grafo del codice RDG, l'arco 1020 registra come `source` l'identificatore `usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `Middleware Integrations`. Nel grafo del codice RDG, l'arco 1020 registra come `target` l'identificatore `usage_middleware_integrations` per la relazione che coinvolge il nodo canonico `Middleware Integrations`.

<!-- GRAPH_NODE graph=unificata index=532 id=Request-Dataset-Generator::usage_middleware_integrations -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Middleware Integrations` è `Middleware Integrations`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Middleware Integrations` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Middleware Integrations` è `usage.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Middleware Integrations` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Middleware Integrations` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Middleware Integrations` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Middleware Integrations` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Middleware Integrations` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Middleware Integrations` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Middleware Integrations` è `middleware integrations`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Middleware Integrations` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Middleware Integrations` è `usage_middleware_integrations`. Nel grafo unificato, il nodo canonico `Middleware Integrations` possiede l'identificatore esatto `Request-Dataset-Generator::usage_middleware_integrations`.
Nel grafo unificato, il nodo canonico `Middleware Integrations` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1020 role=incoming -->
Nel grafo unificato, il nodo canonico `Middleware Integrations` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Request Dataset Generator RVE Guide`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` all'ID `Request-Dataset-Generator::usage_middleware_integrations`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `Middleware Integrations`. Nel grafo unificato, il tipo dichiarato dell'arco 1020 che coinvolge il nodo canonico `Middleware Integrations` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1020 che coinvolge il nodo canonico `Middleware Integrations` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1020 che coinvolge il nodo canonico `Middleware Integrations` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1020 che coinvolge il nodo canonico `Middleware Integrations` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1020 che coinvolge il nodo canonico `Middleware Integrations` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1020 che coinvolge il nodo canonico `Middleware Integrations` è `1.0`. Nel grafo unificato, l'arco 1020 registra come `source` l'identificatore `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `Middleware Integrations`. Nel grafo unificato, l'arco 1020 registra come `target` l'identificatore `Request-Dataset-Generator::usage_middleware_integrations` per la relazione che coinvolge il nodo canonico `Middleware Integrations`.

### mTLS IAP

<!-- GRAPH_NODE graph=codice index=529 id=usage_mtls_iap -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `mTLS IAP` è `mTLS IAP`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `mTLS IAP` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `mTLS IAP` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `mTLS IAP` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `mTLS IAP` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `mTLS IAP` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `mTLS IAP` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `mTLS IAP` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `mTLS IAP` possiede l'identificatore esatto `usage_mtls_iap`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `mTLS IAP` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `mTLS IAP` è `mtls iap`.
<!-- GRAPH_EDGE graph=codice index=1027 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `mTLS IAP` è la sorgente `source` della relazione `conceptually_related_to` verso il nodo canonico `Placeholder Resolution`; l'orientamento registrato nel JSON va dall'ID `usage_mtls_iap` all'ID `usage_placeholder_resolution`. La relazione tecnica esplicita è la seguente: `mTLS IAP` è concettualmente correlato a `Placeholder Resolution`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1027 che coinvolge il nodo canonico `mTLS IAP` è `conceptually_related_to`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1027 che coinvolge il nodo canonico `mTLS IAP` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1027 che coinvolge il nodo canonico `mTLS IAP` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1027 che coinvolge il nodo canonico `mTLS IAP` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1027 che coinvolge il nodo canonico `mTLS IAP` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1027 che coinvolge il nodo canonico `mTLS IAP` è `1.0`. Nel grafo del codice RDG, l'arco 1027 registra come `source` l'identificatore `usage_mtls_iap` per la relazione che coinvolge il nodo canonico `mTLS IAP`. Nel grafo del codice RDG, l'arco 1027 registra come `target` l'identificatore `usage_placeholder_resolution` per la relazione che coinvolge il nodo canonico `mTLS IAP`.
<!-- GRAPH_EDGE graph=codice index=1008 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `mTLS IAP` è il destinatario `target` della relazione `semantically_similar_to` proveniente dal nodo canonico `mTLS with IAP`; l'orientamento registrato nel JSON va dall'ID `readme_mtls_iap` all'ID `usage_mtls_iap`. La relazione tecnica esplicita è la seguente: `mTLS with IAP` è semanticamente simile a `mTLS IAP`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1008 che coinvolge il nodo canonico `mTLS IAP` è `semantically_similar_to`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1008 che coinvolge il nodo canonico `mTLS IAP` è `INFERRED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1008 che coinvolge il nodo canonico `mTLS IAP` è `0.95`. Nel grafo del codice RDG, il file sorgente dell'arco 1008 che coinvolge il nodo canonico `mTLS IAP` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1008 che coinvolge il nodo canonico `mTLS IAP` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1008 che coinvolge il nodo canonico `mTLS IAP` è `1.0`. Nel grafo del codice RDG, l'arco 1008 registra come `source` l'identificatore `readme_mtls_iap` per la relazione che coinvolge il nodo canonico `mTLS IAP`. Nel grafo del codice RDG, l'arco 1008 registra come `target` l'identificatore `usage_mtls_iap` per la relazione che coinvolge il nodo canonico `mTLS IAP`.
<!-- GRAPH_EDGE graph=codice index=1021 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `mTLS IAP` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Request Dataset Generator RVE Guide`; l'orientamento registrato nel JSON va dall'ID `usage_request_dataset_generator_rve_guide` all'ID `usage_mtls_iap`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `mTLS IAP`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1021 che coinvolge il nodo canonico `mTLS IAP` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1021 che coinvolge il nodo canonico `mTLS IAP` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1021 che coinvolge il nodo canonico `mTLS IAP` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1021 che coinvolge il nodo canonico `mTLS IAP` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1021 che coinvolge il nodo canonico `mTLS IAP` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1021 che coinvolge il nodo canonico `mTLS IAP` è `1.0`. Nel grafo del codice RDG, l'arco 1021 registra come `source` l'identificatore `usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `mTLS IAP`. Nel grafo del codice RDG, l'arco 1021 registra come `target` l'identificatore `usage_mtls_iap` per la relazione che coinvolge il nodo canonico `mTLS IAP`.

<!-- GRAPH_NODE graph=unificata index=529 id=Request-Dataset-Generator::usage_mtls_iap -->
Nel grafo unificato, l'etichetta originale del nodo canonico `mTLS IAP` è `mTLS IAP`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `mTLS IAP` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `mTLS IAP` è `usage.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `mTLS IAP` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `mTLS IAP` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `mTLS IAP` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `mTLS IAP` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `mTLS IAP` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `mTLS IAP` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `mTLS IAP` è `mtls iap`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `mTLS IAP` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `mTLS IAP` è `usage_mtls_iap`. Nel grafo unificato, il nodo canonico `mTLS IAP` possiede l'identificatore esatto `Request-Dataset-Generator::usage_mtls_iap`.
<!-- GRAPH_EDGE graph=unificata index=1027 role=outgoing -->
Nel grafo unificato, il nodo canonico `mTLS IAP` è la sorgente `source` della relazione `conceptually_related_to` verso il nodo canonico `Placeholder Resolution`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_mtls_iap` all'ID `Request-Dataset-Generator::usage_placeholder_resolution`. La relazione tecnica esplicita è la seguente: `mTLS IAP` è concettualmente correlato a `Placeholder Resolution`. Nel grafo unificato, il tipo dichiarato dell'arco 1027 che coinvolge il nodo canonico `mTLS IAP` è `conceptually_related_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1027 che coinvolge il nodo canonico `mTLS IAP` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1027 che coinvolge il nodo canonico `mTLS IAP` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1027 che coinvolge il nodo canonico `mTLS IAP` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1027 che coinvolge il nodo canonico `mTLS IAP` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1027 che coinvolge il nodo canonico `mTLS IAP` è `1.0`. Nel grafo unificato, l'arco 1027 registra come `source` l'identificatore `Request-Dataset-Generator::usage_mtls_iap` per la relazione che coinvolge il nodo canonico `mTLS IAP`. Nel grafo unificato, l'arco 1027 registra come `target` l'identificatore `Request-Dataset-Generator::usage_placeholder_resolution` per la relazione che coinvolge il nodo canonico `mTLS IAP`.
<!-- GRAPH_EDGE graph=unificata index=1008 role=incoming -->
Nel grafo unificato, il nodo canonico `mTLS IAP` è il destinatario `target` della relazione `semantically_similar_to` proveniente dal nodo canonico `mTLS with IAP`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_mtls_iap` all'ID `Request-Dataset-Generator::usage_mtls_iap`. La relazione tecnica esplicita è la seguente: `mTLS with IAP` è semanticamente simile a `mTLS IAP`. Nel grafo unificato, il tipo dichiarato dell'arco 1008 che coinvolge il nodo canonico `mTLS IAP` è `semantically_similar_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1008 che coinvolge il nodo canonico `mTLS IAP` è `INFERRED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1008 che coinvolge il nodo canonico `mTLS IAP` è `0.95`. Nel grafo unificato, il file sorgente dell'arco 1008 che coinvolge il nodo canonico `mTLS IAP` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1008 che coinvolge il nodo canonico `mTLS IAP` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1008 che coinvolge il nodo canonico `mTLS IAP` è `1.0`. Nel grafo unificato, l'arco 1008 registra come `source` l'identificatore `Request-Dataset-Generator::readme_mtls_iap` per la relazione che coinvolge il nodo canonico `mTLS IAP`. Nel grafo unificato, l'arco 1008 registra come `target` l'identificatore `Request-Dataset-Generator::usage_mtls_iap` per la relazione che coinvolge il nodo canonico `mTLS IAP`.
<!-- GRAPH_EDGE graph=unificata index=1021 role=incoming -->
Nel grafo unificato, il nodo canonico `mTLS IAP` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Request Dataset Generator RVE Guide`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` all'ID `Request-Dataset-Generator::usage_mtls_iap`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `mTLS IAP`. Nel grafo unificato, il tipo dichiarato dell'arco 1021 che coinvolge il nodo canonico `mTLS IAP` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1021 che coinvolge il nodo canonico `mTLS IAP` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1021 che coinvolge il nodo canonico `mTLS IAP` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1021 che coinvolge il nodo canonico `mTLS IAP` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1021 che coinvolge il nodo canonico `mTLS IAP` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1021 che coinvolge il nodo canonico `mTLS IAP` è `1.0`. Nel grafo unificato, l'arco 1021 registra come `source` l'identificatore `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `mTLS IAP`. Nel grafo unificato, l'arco 1021 registra come `target` l'identificatore `Request-Dataset-Generator::usage_mtls_iap` per la relazione che coinvolge il nodo canonico `mTLS IAP`.

### mTLS with IAP

<!-- GRAPH_NODE graph=codice index=511 id=readme_mtls_iap -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `mTLS with IAP` è `mTLS with IAP`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `mTLS with IAP` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `mTLS with IAP` è `README.md`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `mTLS with IAP` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `mTLS with IAP` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `mTLS with IAP` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `mTLS with IAP` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `mTLS with IAP` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `mTLS with IAP` possiede l'identificatore esatto `readme_mtls_iap`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `mTLS with IAP` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `mTLS with IAP` è `mtls with iap`.
<!-- GRAPH_EDGE graph=codice index=1008 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `mTLS with IAP` è la sorgente `source` della relazione `semantically_similar_to` verso il nodo canonico `mTLS IAP`; l'orientamento registrato nel JSON va dall'ID `readme_mtls_iap` all'ID `usage_mtls_iap`. La relazione tecnica esplicita è la seguente: `mTLS with IAP` è semanticamente simile a `mTLS IAP`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1008 che coinvolge il nodo canonico `mTLS with IAP` è `semantically_similar_to`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1008 che coinvolge il nodo canonico `mTLS with IAP` è `INFERRED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1008 che coinvolge il nodo canonico `mTLS with IAP` è `0.95`. Nel grafo del codice RDG, il file sorgente dell'arco 1008 che coinvolge il nodo canonico `mTLS with IAP` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1008 che coinvolge il nodo canonico `mTLS with IAP` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1008 che coinvolge il nodo canonico `mTLS with IAP` è `1.0`. Nel grafo del codice RDG, l'arco 1008 registra come `source` l'identificatore `readme_mtls_iap` per la relazione che coinvolge il nodo canonico `mTLS with IAP`. Nel grafo del codice RDG, l'arco 1008 registra come `target` l'identificatore `usage_mtls_iap` per la relazione che coinvolge il nodo canonico `mTLS with IAP`.
<!-- GRAPH_EDGE graph=codice index=1009 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `mTLS with IAP` è il destinatario `target` della relazione `conceptually_related_to` proveniente dal nodo canonico `cryptography`; l'orientamento registrato nel JSON va dall'ID `requirements_cryptography` all'ID `readme_mtls_iap`. La relazione tecnica esplicita è la seguente: `cryptography` è concettualmente correlato a `mTLS with IAP`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1009 che coinvolge il nodo canonico `mTLS with IAP` è `conceptually_related_to`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1009 che coinvolge il nodo canonico `mTLS with IAP` è `INFERRED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1009 che coinvolge il nodo canonico `mTLS with IAP` è `0.85`. Nel grafo del codice RDG, il file sorgente dell'arco 1009 che coinvolge il nodo canonico `mTLS with IAP` è `requirements.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1009 che coinvolge il nodo canonico `mTLS with IAP` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1009 che coinvolge il nodo canonico `mTLS with IAP` è `1.0`. Nel grafo del codice RDG, l'arco 1009 registra come `source` l'identificatore `requirements_cryptography` per la relazione che coinvolge il nodo canonico `mTLS with IAP`. Nel grafo del codice RDG, l'arco 1009 registra come `target` l'identificatore `readme_mtls_iap` per la relazione che coinvolge il nodo canonico `mTLS with IAP`.

<!-- GRAPH_NODE graph=unificata index=511 id=Request-Dataset-Generator::readme_mtls_iap -->
Nel grafo unificato, l'etichetta originale del nodo canonico `mTLS with IAP` è `mTLS with IAP`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `mTLS with IAP` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `mTLS with IAP` è `README.md`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `mTLS with IAP` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `mTLS with IAP` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `mTLS with IAP` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `mTLS with IAP` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `mTLS with IAP` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `mTLS with IAP` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `mTLS with IAP` è `mtls with iap`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `mTLS with IAP` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `mTLS with IAP` è `readme_mtls_iap`. Nel grafo unificato, il nodo canonico `mTLS with IAP` possiede l'identificatore esatto `Request-Dataset-Generator::readme_mtls_iap`.
<!-- GRAPH_EDGE graph=unificata index=1008 role=outgoing -->
Nel grafo unificato, il nodo canonico `mTLS with IAP` è la sorgente `source` della relazione `semantically_similar_to` verso il nodo canonico `mTLS IAP`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_mtls_iap` all'ID `Request-Dataset-Generator::usage_mtls_iap`. La relazione tecnica esplicita è la seguente: `mTLS with IAP` è semanticamente simile a `mTLS IAP`. Nel grafo unificato, il tipo dichiarato dell'arco 1008 che coinvolge il nodo canonico `mTLS with IAP` è `semantically_similar_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1008 che coinvolge il nodo canonico `mTLS with IAP` è `INFERRED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1008 che coinvolge il nodo canonico `mTLS with IAP` è `0.95`. Nel grafo unificato, il file sorgente dell'arco 1008 che coinvolge il nodo canonico `mTLS with IAP` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1008 che coinvolge il nodo canonico `mTLS with IAP` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1008 che coinvolge il nodo canonico `mTLS with IAP` è `1.0`. Nel grafo unificato, l'arco 1008 registra come `source` l'identificatore `Request-Dataset-Generator::readme_mtls_iap` per la relazione che coinvolge il nodo canonico `mTLS with IAP`. Nel grafo unificato, l'arco 1008 registra come `target` l'identificatore `Request-Dataset-Generator::usage_mtls_iap` per la relazione che coinvolge il nodo canonico `mTLS with IAP`.
<!-- GRAPH_EDGE graph=unificata index=1009 role=outgoing -->
Nel grafo unificato, il nodo canonico `mTLS with IAP` è la sorgente `source` della relazione `conceptually_related_to` verso il nodo canonico `cryptography`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_mtls_iap` all'ID `Request-Dataset-Generator::requirements_cryptography`. La relazione tecnica esplicita è la seguente: `mTLS with IAP` è concettualmente correlato a `cryptography`. Nel grafo unificato, il tipo dichiarato dell'arco 1009 che coinvolge il nodo canonico `mTLS with IAP` è `conceptually_related_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1009 che coinvolge il nodo canonico `mTLS with IAP` è `INFERRED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1009 che coinvolge il nodo canonico `mTLS with IAP` è `0.85`. Nel grafo unificato, il file sorgente dell'arco 1009 che coinvolge il nodo canonico `mTLS with IAP` è `requirements.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1009 che coinvolge il nodo canonico `mTLS with IAP` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1009 che coinvolge il nodo canonico `mTLS with IAP` è `1.0`. Nel grafo unificato, l'arco 1009 registra come `source` l'identificatore `Request-Dataset-Generator::readme_mtls_iap` per la relazione che coinvolge il nodo canonico `mTLS with IAP`. Nel grafo unificato, l'arco 1009 registra come `target` l'identificatore `Request-Dataset-Generator::requirements_cryptography` per la relazione che coinvolge il nodo canonico `mTLS with IAP`.
Nel grafo unificato, il nodo canonico `mTLS with IAP` non possiede archi entranti registrati nel campo `links`.

### Pipeline [readme_pipeline]

<!-- GRAPH_NODE graph=codice index=512 id=readme_pipeline -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Pipeline [readme_pipeline]` è `Pipeline`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Pipeline [readme_pipeline]` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Pipeline [readme_pipeline]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Pipeline [readme_pipeline]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `Pipeline [readme_pipeline]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `Pipeline [readme_pipeline]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `Pipeline [readme_pipeline]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `Pipeline [readme_pipeline]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `Pipeline [readme_pipeline]` possiede l'identificatore esatto `readme_pipeline`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Pipeline [readme_pipeline]` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Pipeline [readme_pipeline]` è `pipeline`.
<!-- GRAPH_EDGE graph=codice index=1010 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Pipeline [readme_pipeline]` è la sorgente `source` della relazione `semantically_similar_to` verso il nodo canonico `Pipeline [usage_pipeline]`; l'orientamento registrato nel JSON va dall'ID `readme_pipeline` all'ID `usage_pipeline`. La relazione tecnica esplicita è la seguente: `Pipeline [readme_pipeline]` è semanticamente simile a `Pipeline [usage_pipeline]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1010 che coinvolge il nodo canonico `Pipeline [readme_pipeline]` è `semantically_similar_to`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1010 che coinvolge il nodo canonico `Pipeline [readme_pipeline]` è `INFERRED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1010 che coinvolge il nodo canonico `Pipeline [readme_pipeline]` è `0.95`. Nel grafo del codice RDG, il file sorgente dell'arco 1010 che coinvolge il nodo canonico `Pipeline [readme_pipeline]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1010 che coinvolge il nodo canonico `Pipeline [readme_pipeline]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1010 che coinvolge il nodo canonico `Pipeline [readme_pipeline]` è `1.0`. Nel grafo del codice RDG, l'arco 1010 registra come `source` l'identificatore `readme_pipeline` per la relazione che coinvolge il nodo canonico `Pipeline [readme_pipeline]`. Nel grafo del codice RDG, l'arco 1010 registra come `target` l'identificatore `usage_pipeline` per la relazione che coinvolge il nodo canonico `Pipeline [readme_pipeline]`.
Nel grafo del codice RDG, il nodo canonico `Pipeline [readme_pipeline]` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=512 id=Request-Dataset-Generator::readme_pipeline -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Pipeline [readme_pipeline]` è `Pipeline`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Pipeline [readme_pipeline]` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Pipeline [readme_pipeline]` è `README.md`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Pipeline [readme_pipeline]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Pipeline [readme_pipeline]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Pipeline [readme_pipeline]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Pipeline [readme_pipeline]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Pipeline [readme_pipeline]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Pipeline [readme_pipeline]` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Pipeline [readme_pipeline]` è `pipeline`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Pipeline [readme_pipeline]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Pipeline [readme_pipeline]` è `readme_pipeline`. Nel grafo unificato, il nodo canonico `Pipeline [readme_pipeline]` possiede l'identificatore esatto `Request-Dataset-Generator::readme_pipeline`.
<!-- GRAPH_EDGE graph=unificata index=1010 role=outgoing -->
Nel grafo unificato, il nodo canonico `Pipeline [readme_pipeline]` è la sorgente `source` della relazione `semantically_similar_to` verso il nodo canonico `Pipeline [usage_pipeline]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_pipeline` all'ID `Request-Dataset-Generator::usage_pipeline`. La relazione tecnica esplicita è la seguente: `Pipeline [readme_pipeline]` è semanticamente simile a `Pipeline [usage_pipeline]`. Nel grafo unificato, il tipo dichiarato dell'arco 1010 che coinvolge il nodo canonico `Pipeline [readme_pipeline]` è `semantically_similar_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1010 che coinvolge il nodo canonico `Pipeline [readme_pipeline]` è `INFERRED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1010 che coinvolge il nodo canonico `Pipeline [readme_pipeline]` è `0.95`. Nel grafo unificato, il file sorgente dell'arco 1010 che coinvolge il nodo canonico `Pipeline [readme_pipeline]` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1010 che coinvolge il nodo canonico `Pipeline [readme_pipeline]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1010 che coinvolge il nodo canonico `Pipeline [readme_pipeline]` è `1.0`. Nel grafo unificato, l'arco 1010 registra come `source` l'identificatore `Request-Dataset-Generator::readme_pipeline` per la relazione che coinvolge il nodo canonico `Pipeline [readme_pipeline]`. Nel grafo unificato, l'arco 1010 registra come `target` l'identificatore `Request-Dataset-Generator::usage_pipeline` per la relazione che coinvolge il nodo canonico `Pipeline [readme_pipeline]`.
Nel grafo unificato, il nodo canonico `Pipeline [readme_pipeline]` non possiede archi entranti registrati nel campo `links`.

### Pipeline [usage_pipeline]

<!-- GRAPH_NODE graph=codice index=527 id=usage_pipeline -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Pipeline [usage_pipeline]` è `Pipeline`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Pipeline [usage_pipeline]` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Pipeline [usage_pipeline]` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Pipeline [usage_pipeline]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `Pipeline [usage_pipeline]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `Pipeline [usage_pipeline]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `Pipeline [usage_pipeline]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `Pipeline [usage_pipeline]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `Pipeline [usage_pipeline]` possiede l'identificatore esatto `usage_pipeline`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Pipeline [usage_pipeline]` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Pipeline [usage_pipeline]` è `pipeline`.
Nel grafo del codice RDG, il nodo canonico `Pipeline [usage_pipeline]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=1010 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Pipeline [usage_pipeline]` è il destinatario `target` della relazione `semantically_similar_to` proveniente dal nodo canonico `Pipeline [readme_pipeline]`; l'orientamento registrato nel JSON va dall'ID `readme_pipeline` all'ID `usage_pipeline`. La relazione tecnica esplicita è la seguente: `Pipeline [readme_pipeline]` è semanticamente simile a `Pipeline [usage_pipeline]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1010 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `semantically_similar_to`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1010 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `INFERRED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1010 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `0.95`. Nel grafo del codice RDG, il file sorgente dell'arco 1010 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1010 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1010 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `1.0`. Nel grafo del codice RDG, l'arco 1010 registra come `source` l'identificatore `readme_pipeline` per la relazione che coinvolge il nodo canonico `Pipeline [usage_pipeline]`. Nel grafo del codice RDG, l'arco 1010 registra come `target` l'identificatore `usage_pipeline` per la relazione che coinvolge il nodo canonico `Pipeline [usage_pipeline]`.
<!-- GRAPH_EDGE graph=codice index=1026 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Pipeline [usage_pipeline]` è il destinatario `target` della relazione `implements` proveniente dal nodo canonico `main.py Command Dispatch`; l'orientamento registrato nel JSON va dall'ID `usage_command_dispatch` all'ID `usage_pipeline`. La relazione tecnica esplicita è la seguente: `main.py Command Dispatch` implementa `Pipeline [usage_pipeline]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1026 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `implements`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1026 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1026 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1026 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1026 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1026 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `1.0`. Nel grafo del codice RDG, l'arco 1026 registra come `source` l'identificatore `usage_command_dispatch` per la relazione che coinvolge il nodo canonico `Pipeline [usage_pipeline]`. Nel grafo del codice RDG, l'arco 1026 registra come `target` l'identificatore `usage_pipeline` per la relazione che coinvolge il nodo canonico `Pipeline [usage_pipeline]`.

<!-- GRAPH_NODE graph=unificata index=527 id=Request-Dataset-Generator::usage_pipeline -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Pipeline [usage_pipeline]` è `Pipeline`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Pipeline [usage_pipeline]` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Pipeline [usage_pipeline]` è `usage.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Pipeline [usage_pipeline]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Pipeline [usage_pipeline]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Pipeline [usage_pipeline]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Pipeline [usage_pipeline]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Pipeline [usage_pipeline]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Pipeline [usage_pipeline]` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Pipeline [usage_pipeline]` è `pipeline`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Pipeline [usage_pipeline]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Pipeline [usage_pipeline]` è `usage_pipeline`. Nel grafo unificato, il nodo canonico `Pipeline [usage_pipeline]` possiede l'identificatore esatto `Request-Dataset-Generator::usage_pipeline`.
Nel grafo unificato, il nodo canonico `Pipeline [usage_pipeline]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1010 role=incoming -->
Nel grafo unificato, il nodo canonico `Pipeline [usage_pipeline]` è il destinatario `target` della relazione `semantically_similar_to` proveniente dal nodo canonico `Pipeline [readme_pipeline]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_pipeline` all'ID `Request-Dataset-Generator::usage_pipeline`. La relazione tecnica esplicita è la seguente: `Pipeline [readme_pipeline]` è semanticamente simile a `Pipeline [usage_pipeline]`. Nel grafo unificato, il tipo dichiarato dell'arco 1010 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `semantically_similar_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1010 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `INFERRED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1010 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `0.95`. Nel grafo unificato, il file sorgente dell'arco 1010 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1010 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1010 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `1.0`. Nel grafo unificato, l'arco 1010 registra come `source` l'identificatore `Request-Dataset-Generator::readme_pipeline` per la relazione che coinvolge il nodo canonico `Pipeline [usage_pipeline]`. Nel grafo unificato, l'arco 1010 registra come `target` l'identificatore `Request-Dataset-Generator::usage_pipeline` per la relazione che coinvolge il nodo canonico `Pipeline [usage_pipeline]`.
<!-- GRAPH_EDGE graph=unificata index=1026 role=incoming -->
Nel grafo unificato, il nodo canonico `Pipeline [usage_pipeline]` è il destinatario `target` della relazione `implements` proveniente dal nodo canonico `main.py Command Dispatch`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_command_dispatch` all'ID `Request-Dataset-Generator::usage_pipeline`. La relazione tecnica esplicita è la seguente: `main.py Command Dispatch` implementa `Pipeline [usage_pipeline]`. Nel grafo unificato, il tipo dichiarato dell'arco 1026 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `implements`. Nel grafo unificato, la confidenza categoriale dell'arco 1026 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1026 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1026 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1026 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1026 che coinvolge il nodo canonico `Pipeline [usage_pipeline]` è `1.0`. Nel grafo unificato, l'arco 1026 registra come `source` l'identificatore `Request-Dataset-Generator::usage_command_dispatch` per la relazione che coinvolge il nodo canonico `Pipeline [usage_pipeline]`. Nel grafo unificato, l'arco 1026 registra come `target` l'identificatore `Request-Dataset-Generator::usage_pipeline` per la relazione che coinvolge il nodo canonico `Pipeline [usage_pipeline]`.

### Placeholder Resolution

<!-- GRAPH_NODE graph=codice index=530 id=usage_placeholder_resolution -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Placeholder Resolution` è `Placeholder Resolution`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Placeholder Resolution` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Placeholder Resolution` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Placeholder Resolution` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `Placeholder Resolution` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `Placeholder Resolution` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `Placeholder Resolution` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `Placeholder Resolution` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `Placeholder Resolution` possiede l'identificatore esatto `usage_placeholder_resolution`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Placeholder Resolution` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Placeholder Resolution` è `placeholder resolution`.
Nel grafo del codice RDG, il nodo canonico `Placeholder Resolution` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=1027 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Placeholder Resolution` è il destinatario `target` della relazione `conceptually_related_to` proveniente dal nodo canonico `mTLS IAP`; l'orientamento registrato nel JSON va dall'ID `usage_mtls_iap` all'ID `usage_placeholder_resolution`. La relazione tecnica esplicita è la seguente: `mTLS IAP` è concettualmente correlato a `Placeholder Resolution`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1027 che coinvolge il nodo canonico `Placeholder Resolution` è `conceptually_related_to`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1027 che coinvolge il nodo canonico `Placeholder Resolution` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1027 che coinvolge il nodo canonico `Placeholder Resolution` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1027 che coinvolge il nodo canonico `Placeholder Resolution` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1027 che coinvolge il nodo canonico `Placeholder Resolution` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1027 che coinvolge il nodo canonico `Placeholder Resolution` è `1.0`. Nel grafo del codice RDG, l'arco 1027 registra come `source` l'identificatore `usage_mtls_iap` per la relazione che coinvolge il nodo canonico `Placeholder Resolution`. Nel grafo del codice RDG, l'arco 1027 registra come `target` l'identificatore `usage_placeholder_resolution` per la relazione che coinvolge il nodo canonico `Placeholder Resolution`.

<!-- GRAPH_NODE graph=unificata index=530 id=Request-Dataset-Generator::usage_placeholder_resolution -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Placeholder Resolution` è `Placeholder Resolution`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Placeholder Resolution` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Placeholder Resolution` è `usage.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Placeholder Resolution` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Placeholder Resolution` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Placeholder Resolution` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Placeholder Resolution` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Placeholder Resolution` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Placeholder Resolution` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Placeholder Resolution` è `placeholder resolution`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Placeholder Resolution` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Placeholder Resolution` è `usage_placeholder_resolution`. Nel grafo unificato, il nodo canonico `Placeholder Resolution` possiede l'identificatore esatto `Request-Dataset-Generator::usage_placeholder_resolution`.
Nel grafo unificato, il nodo canonico `Placeholder Resolution` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1027 role=incoming -->
Nel grafo unificato, il nodo canonico `Placeholder Resolution` è il destinatario `target` della relazione `conceptually_related_to` proveniente dal nodo canonico `mTLS IAP`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_mtls_iap` all'ID `Request-Dataset-Generator::usage_placeholder_resolution`. La relazione tecnica esplicita è la seguente: `mTLS IAP` è concettualmente correlato a `Placeholder Resolution`. Nel grafo unificato, il tipo dichiarato dell'arco 1027 che coinvolge il nodo canonico `Placeholder Resolution` è `conceptually_related_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1027 che coinvolge il nodo canonico `Placeholder Resolution` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1027 che coinvolge il nodo canonico `Placeholder Resolution` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1027 che coinvolge il nodo canonico `Placeholder Resolution` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1027 che coinvolge il nodo canonico `Placeholder Resolution` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1027 che coinvolge il nodo canonico `Placeholder Resolution` è `1.0`. Nel grafo unificato, l'arco 1027 registra come `source` l'identificatore `Request-Dataset-Generator::usage_mtls_iap` per la relazione che coinvolge il nodo canonico `Placeholder Resolution`. Nel grafo unificato, l'arco 1027 registra come `target` l'identificatore `Request-Dataset-Generator::usage_placeholder_resolution` per la relazione che coinvolge il nodo canonico `Placeholder Resolution`.

### Request Dataset Generator RVE

<!-- GRAPH_NODE graph=codice index=504 id=readme_request_dataset_generator_rve -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Request Dataset Generator RVE` è `Request Dataset Generator RVE`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Request Dataset Generator RVE` come `document`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Request Dataset Generator RVE` è `README.md`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Request Dataset Generator RVE` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `Request Dataset Generator RVE` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `Request Dataset Generator RVE` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `Request Dataset Generator RVE` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `Request Dataset Generator RVE` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `Request Dataset Generator RVE` possiede l'identificatore esatto `readme_request_dataset_generator_rve`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Request Dataset Generator RVE` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Request Dataset Generator RVE` è `request dataset generator rve`.
<!-- GRAPH_EDGE graph=codice index=998 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Request Dataset Generator RVE` è la sorgente `source` della relazione `references` verso il nodo canonico `FHIR Synthetic Patients`; l'orientamento registrato nel JSON va dall'ID `readme_request_dataset_generator_rve` all'ID `readme_fhir_synthetic_patients`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE` fa riferimento a `FHIR Synthetic Patients`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 998 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 998 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 998 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 998 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 998 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 998 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `1.0`. Nel grafo del codice RDG, l'arco 998 registra come `source` l'identificatore `readme_request_dataset_generator_rve` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE`. Nel grafo del codice RDG, l'arco 998 registra come `target` l'identificatore `readme_fhir_synthetic_patients` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE`.
<!-- GRAPH_EDGE graph=codice index=999 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Request Dataset Generator RVE` è la sorgente `source` della relazione `references` verso il nodo canonico `RVE Flows [readme_rve_flows]`; l'orientamento registrato nel JSON va dall'ID `readme_request_dataset_generator_rve` all'ID `readme_rve_flows`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE` fa riferimento a `RVE Flows [readme_rve_flows]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 999 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 999 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 999 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 999 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 999 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 999 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `1.0`. Nel grafo del codice RDG, l'arco 999 registra come `source` l'identificatore `readme_request_dataset_generator_rve` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE`. Nel grafo del codice RDG, l'arco 999 registra come `target` l'identificatore `readme_rve_flows` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE`.
Nel grafo del codice RDG, il nodo canonico `Request Dataset Generator RVE` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=504 id=Request-Dataset-Generator::readme_request_dataset_generator_rve -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Request Dataset Generator RVE` è `Request Dataset Generator RVE`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Request Dataset Generator RVE` come `document`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Request Dataset Generator RVE` è `README.md`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Request Dataset Generator RVE` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Request Dataset Generator RVE` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Request Dataset Generator RVE` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Request Dataset Generator RVE` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Request Dataset Generator RVE` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Request Dataset Generator RVE` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Request Dataset Generator RVE` è `request dataset generator rve`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Request Dataset Generator RVE` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Request Dataset Generator RVE` è `readme_request_dataset_generator_rve`. Nel grafo unificato, il nodo canonico `Request Dataset Generator RVE` possiede l'identificatore esatto `Request-Dataset-Generator::readme_request_dataset_generator_rve`.
<!-- GRAPH_EDGE graph=unificata index=998 role=outgoing -->
Nel grafo unificato, il nodo canonico `Request Dataset Generator RVE` è la sorgente `source` della relazione `references` verso il nodo canonico `FHIR Synthetic Patients`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_request_dataset_generator_rve` all'ID `Request-Dataset-Generator::readme_fhir_synthetic_patients`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE` fa riferimento a `FHIR Synthetic Patients`. Nel grafo unificato, il tipo dichiarato dell'arco 998 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 998 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 998 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 998 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 998 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 998 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `1.0`. Nel grafo unificato, l'arco 998 registra come `source` l'identificatore `Request-Dataset-Generator::readme_request_dataset_generator_rve` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE`. Nel grafo unificato, l'arco 998 registra come `target` l'identificatore `Request-Dataset-Generator::readme_fhir_synthetic_patients` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE`.
<!-- GRAPH_EDGE graph=unificata index=999 role=outgoing -->
Nel grafo unificato, il nodo canonico `Request Dataset Generator RVE` è la sorgente `source` della relazione `references` verso il nodo canonico `RVE Flows [readme_rve_flows]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_request_dataset_generator_rve` all'ID `Request-Dataset-Generator::readme_rve_flows`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE` fa riferimento a `RVE Flows [readme_rve_flows]`. Nel grafo unificato, il tipo dichiarato dell'arco 999 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 999 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 999 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 999 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 999 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 999 che coinvolge il nodo canonico `Request Dataset Generator RVE` è `1.0`. Nel grafo unificato, l'arco 999 registra come `source` l'identificatore `Request-Dataset-Generator::readme_request_dataset_generator_rve` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE`. Nel grafo unificato, l'arco 999 registra come `target` l'identificatore `Request-Dataset-Generator::readme_rve_flows` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE`.
Nel grafo unificato, il nodo canonico `Request Dataset Generator RVE` non possiede archi entranti registrati nel campo `links`.

### Request Dataset Generator RVE Guide

<!-- GRAPH_NODE graph=codice index=521 id=usage_request_dataset_generator_rve_guide -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Request Dataset Generator RVE Guide` è `Request Dataset Generator RVE Guide`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Request Dataset Generator RVE Guide` come `document`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Request Dataset Generator RVE Guide` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Request Dataset Generator RVE Guide` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `Request Dataset Generator RVE Guide` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `Request Dataset Generator RVE Guide` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `Request Dataset Generator RVE Guide` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `Request Dataset Generator RVE Guide` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `Request Dataset Generator RVE Guide` possiede l'identificatore esatto `usage_request_dataset_generator_rve_guide`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Request Dataset Generator RVE Guide` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Request Dataset Generator RVE Guide` è `request dataset generator rve guide`.
<!-- GRAPH_EDGE graph=codice index=1017 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Request Dataset Generator RVE Guide` è la sorgente `source` della relazione `references` verso il nodo canonico `main.py Command Dispatch`; l'orientamento registrato nel JSON va dall'ID `usage_request_dataset_generator_rve_guide` all'ID `usage_command_dispatch`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `main.py Command Dispatch`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1017 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1017 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1017 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1017 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1017 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1017 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo del codice RDG, l'arco 1017 registra come `source` l'identificatore `usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`. Nel grafo del codice RDG, l'arco 1017 registra come `target` l'identificatore `usage_command_dispatch` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`.
<!-- GRAPH_EDGE graph=codice index=1018 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Request Dataset Generator RVE Guide` è la sorgente `source` della relazione `references` verso il nodo canonico `Direct Integrations`; l'orientamento registrato nel JSON va dall'ID `usage_request_dataset_generator_rve_guide` all'ID `usage_direct_integrations`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `Direct Integrations`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1018 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1018 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1018 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1018 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1018 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1018 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo del codice RDG, l'arco 1018 registra come `source` l'identificatore `usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`. Nel grafo del codice RDG, l'arco 1018 registra come `target` l'identificatore `usage_direct_integrations` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`.
<!-- GRAPH_EDGE graph=codice index=1019 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Request Dataset Generator RVE Guide` è la sorgente `source` della relazione `references` verso il nodo canonico `Fault Injection`; l'orientamento registrato nel JSON va dall'ID `usage_request_dataset_generator_rve_guide` all'ID `usage_fault_injection`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `Fault Injection`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1019 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1019 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1019 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1019 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1019 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1019 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo del codice RDG, l'arco 1019 registra come `source` l'identificatore `usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`. Nel grafo del codice RDG, l'arco 1019 registra come `target` l'identificatore `usage_fault_injection` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`.
<!-- GRAPH_EDGE graph=codice index=1020 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Request Dataset Generator RVE Guide` è la sorgente `source` della relazione `references` verso il nodo canonico `Middleware Integrations`; l'orientamento registrato nel JSON va dall'ID `usage_request_dataset_generator_rve_guide` all'ID `usage_middleware_integrations`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `Middleware Integrations`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1020 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1020 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1020 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1020 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1020 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1020 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo del codice RDG, l'arco 1020 registra come `source` l'identificatore `usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`. Nel grafo del codice RDG, l'arco 1020 registra come `target` l'identificatore `usage_middleware_integrations` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`.
<!-- GRAPH_EDGE graph=codice index=1021 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Request Dataset Generator RVE Guide` è la sorgente `source` della relazione `references` verso il nodo canonico `mTLS IAP`; l'orientamento registrato nel JSON va dall'ID `usage_request_dataset_generator_rve_guide` all'ID `usage_mtls_iap`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `mTLS IAP`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1021 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1021 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1021 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1021 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1021 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1021 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo del codice RDG, l'arco 1021 registra come `source` l'identificatore `usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`. Nel grafo del codice RDG, l'arco 1021 registra come `target` l'identificatore `usage_mtls_iap` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`.
<!-- GRAPH_EDGE graph=codice index=1022 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Request Dataset Generator RVE Guide` è la sorgente `source` della relazione `references` verso il nodo canonico `RVE Flows [usage_rve_flows]`; l'orientamento registrato nel JSON va dall'ID `usage_request_dataset_generator_rve_guide` all'ID `usage_rve_flows`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `RVE Flows [usage_rve_flows]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1022 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1022 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1022 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1022 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1022 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1022 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo del codice RDG, l'arco 1022 registra come `source` l'identificatore `usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`. Nel grafo del codice RDG, l'arco 1022 registra come `target` l'identificatore `usage_rve_flows` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`.
Nel grafo del codice RDG, il nodo canonico `Request Dataset Generator RVE Guide` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=521 id=Request-Dataset-Generator::usage_request_dataset_generator_rve_guide -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Request Dataset Generator RVE Guide` è `Request Dataset Generator RVE Guide`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Request Dataset Generator RVE Guide` come `document`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Request Dataset Generator RVE Guide` è `usage.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Request Dataset Generator RVE Guide` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Request Dataset Generator RVE Guide` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Request Dataset Generator RVE Guide` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Request Dataset Generator RVE Guide` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Request Dataset Generator RVE Guide` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Request Dataset Generator RVE Guide` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Request Dataset Generator RVE Guide` è `request dataset generator rve guide`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Request Dataset Generator RVE Guide` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Request Dataset Generator RVE Guide` è `usage_request_dataset_generator_rve_guide`. Nel grafo unificato, il nodo canonico `Request Dataset Generator RVE Guide` possiede l'identificatore esatto `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide`.
<!-- GRAPH_EDGE graph=unificata index=1017 role=outgoing -->
Nel grafo unificato, il nodo canonico `Request Dataset Generator RVE Guide` è la sorgente `source` della relazione `references` verso il nodo canonico `main.py Command Dispatch`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` all'ID `Request-Dataset-Generator::usage_command_dispatch`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `main.py Command Dispatch`. Nel grafo unificato, il tipo dichiarato dell'arco 1017 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1017 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1017 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1017 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1017 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1017 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo unificato, l'arco 1017 registra come `source` l'identificatore `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`. Nel grafo unificato, l'arco 1017 registra come `target` l'identificatore `Request-Dataset-Generator::usage_command_dispatch` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`.
<!-- GRAPH_EDGE graph=unificata index=1018 role=outgoing -->
Nel grafo unificato, il nodo canonico `Request Dataset Generator RVE Guide` è la sorgente `source` della relazione `references` verso il nodo canonico `Direct Integrations`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` all'ID `Request-Dataset-Generator::usage_direct_integrations`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `Direct Integrations`. Nel grafo unificato, il tipo dichiarato dell'arco 1018 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1018 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1018 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1018 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1018 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1018 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo unificato, l'arco 1018 registra come `source` l'identificatore `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`. Nel grafo unificato, l'arco 1018 registra come `target` l'identificatore `Request-Dataset-Generator::usage_direct_integrations` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`.
<!-- GRAPH_EDGE graph=unificata index=1019 role=outgoing -->
Nel grafo unificato, il nodo canonico `Request Dataset Generator RVE Guide` è la sorgente `source` della relazione `references` verso il nodo canonico `Fault Injection`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` all'ID `Request-Dataset-Generator::usage_fault_injection`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `Fault Injection`. Nel grafo unificato, il tipo dichiarato dell'arco 1019 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1019 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1019 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1019 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1019 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1019 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo unificato, l'arco 1019 registra come `source` l'identificatore `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`. Nel grafo unificato, l'arco 1019 registra come `target` l'identificatore `Request-Dataset-Generator::usage_fault_injection` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`.
<!-- GRAPH_EDGE graph=unificata index=1020 role=outgoing -->
Nel grafo unificato, il nodo canonico `Request Dataset Generator RVE Guide` è la sorgente `source` della relazione `references` verso il nodo canonico `Middleware Integrations`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` all'ID `Request-Dataset-Generator::usage_middleware_integrations`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `Middleware Integrations`. Nel grafo unificato, il tipo dichiarato dell'arco 1020 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1020 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1020 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1020 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1020 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1020 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo unificato, l'arco 1020 registra come `source` l'identificatore `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`. Nel grafo unificato, l'arco 1020 registra come `target` l'identificatore `Request-Dataset-Generator::usage_middleware_integrations` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`.
<!-- GRAPH_EDGE graph=unificata index=1021 role=outgoing -->
Nel grafo unificato, il nodo canonico `Request Dataset Generator RVE Guide` è la sorgente `source` della relazione `references` verso il nodo canonico `mTLS IAP`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` all'ID `Request-Dataset-Generator::usage_mtls_iap`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `mTLS IAP`. Nel grafo unificato, il tipo dichiarato dell'arco 1021 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1021 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1021 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1021 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1021 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1021 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo unificato, l'arco 1021 registra come `source` l'identificatore `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`. Nel grafo unificato, l'arco 1021 registra come `target` l'identificatore `Request-Dataset-Generator::usage_mtls_iap` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`.
<!-- GRAPH_EDGE graph=unificata index=1022 role=outgoing -->
Nel grafo unificato, il nodo canonico `Request Dataset Generator RVE Guide` è la sorgente `source` della relazione `references` verso il nodo canonico `RVE Flows [usage_rve_flows]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` all'ID `Request-Dataset-Generator::usage_rve_flows`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `RVE Flows [usage_rve_flows]`. Nel grafo unificato, il tipo dichiarato dell'arco 1022 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1022 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1022 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1022 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1022 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1022 che coinvolge il nodo canonico `Request Dataset Generator RVE Guide` è `1.0`. Nel grafo unificato, l'arco 1022 registra come `source` l'identificatore `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`. Nel grafo unificato, l'arco 1022 registra come `target` l'identificatore `Request-Dataset-Generator::usage_rve_flows` per la relazione che coinvolge il nodo canonico `Request Dataset Generator RVE Guide`.
Nel grafo unificato, il nodo canonico `Request Dataset Generator RVE Guide` non possiede archi entranti registrati nel campo `links`.

### Runtime Dependencies

<!-- GRAPH_NODE graph=codice index=514 id=requirements_runtime_dependencies -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Runtime Dependencies` è `Runtime Dependencies`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Runtime Dependencies` come `document`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Runtime Dependencies` è `requirements.txt`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Runtime Dependencies` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `Runtime Dependencies` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `Runtime Dependencies` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `Runtime Dependencies` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `Runtime Dependencies` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `Runtime Dependencies` possiede l'identificatore esatto `requirements_runtime_dependencies`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Runtime Dependencies` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Runtime Dependencies` è `runtime dependencies`.
<!-- GRAPH_EDGE graph=codice index=1011 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Runtime Dependencies` è la sorgente `source` della relazione `references` verso il nodo canonico `aiohttp`; l'orientamento registrato nel JSON va dall'ID `requirements_runtime_dependencies` all'ID `requirements_aiohttp`. La relazione tecnica esplicita è la seguente: `Runtime Dependencies` fa riferimento a `aiohttp`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1011 che coinvolge il nodo canonico `Runtime Dependencies` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1011 che coinvolge il nodo canonico `Runtime Dependencies` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1011 che coinvolge il nodo canonico `Runtime Dependencies` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1011 che coinvolge il nodo canonico `Runtime Dependencies` è `requirements.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1011 che coinvolge il nodo canonico `Runtime Dependencies` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1011 che coinvolge il nodo canonico `Runtime Dependencies` è `1.0`. Nel grafo del codice RDG, l'arco 1011 registra come `source` l'identificatore `requirements_runtime_dependencies` per la relazione che coinvolge il nodo canonico `Runtime Dependencies`. Nel grafo del codice RDG, l'arco 1011 registra come `target` l'identificatore `requirements_aiohttp` per la relazione che coinvolge il nodo canonico `Runtime Dependencies`.
<!-- GRAPH_EDGE graph=codice index=1012 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Runtime Dependencies` è la sorgente `source` della relazione `references` verso il nodo canonico `cryptography`; l'orientamento registrato nel JSON va dall'ID `requirements_runtime_dependencies` all'ID `requirements_cryptography`. La relazione tecnica esplicita è la seguente: `Runtime Dependencies` fa riferimento a `cryptography`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1012 che coinvolge il nodo canonico `Runtime Dependencies` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1012 che coinvolge il nodo canonico `Runtime Dependencies` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1012 che coinvolge il nodo canonico `Runtime Dependencies` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1012 che coinvolge il nodo canonico `Runtime Dependencies` è `requirements.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1012 che coinvolge il nodo canonico `Runtime Dependencies` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1012 che coinvolge il nodo canonico `Runtime Dependencies` è `1.0`. Nel grafo del codice RDG, l'arco 1012 registra come `source` l'identificatore `requirements_runtime_dependencies` per la relazione che coinvolge il nodo canonico `Runtime Dependencies`. Nel grafo del codice RDG, l'arco 1012 registra come `target` l'identificatore `requirements_cryptography` per la relazione che coinvolge il nodo canonico `Runtime Dependencies`.
<!-- GRAPH_EDGE graph=codice index=1013 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Runtime Dependencies` è la sorgente `source` della relazione `references` verso il nodo canonico `matplotlib [requirements_matplotlib]`; l'orientamento registrato nel JSON va dall'ID `requirements_runtime_dependencies` all'ID `requirements_matplotlib`. La relazione tecnica esplicita è la seguente: `Runtime Dependencies` fa riferimento a `matplotlib [requirements_matplotlib]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1013 che coinvolge il nodo canonico `Runtime Dependencies` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1013 che coinvolge il nodo canonico `Runtime Dependencies` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1013 che coinvolge il nodo canonico `Runtime Dependencies` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1013 che coinvolge il nodo canonico `Runtime Dependencies` è `requirements.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1013 che coinvolge il nodo canonico `Runtime Dependencies` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1013 che coinvolge il nodo canonico `Runtime Dependencies` è `1.0`. Nel grafo del codice RDG, l'arco 1013 registra come `source` l'identificatore `requirements_runtime_dependencies` per la relazione che coinvolge il nodo canonico `Runtime Dependencies`. Nel grafo del codice RDG, l'arco 1013 registra come `target` l'identificatore `requirements_matplotlib` per la relazione che coinvolge il nodo canonico `Runtime Dependencies`.
Nel grafo del codice RDG, il nodo canonico `Runtime Dependencies` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=514 id=Request-Dataset-Generator::requirements_runtime_dependencies -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Runtime Dependencies` è `Runtime Dependencies`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Runtime Dependencies` come `document`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Runtime Dependencies` è `requirements.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Runtime Dependencies` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Runtime Dependencies` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Runtime Dependencies` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Runtime Dependencies` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Runtime Dependencies` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Runtime Dependencies` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Runtime Dependencies` è `runtime dependencies`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Runtime Dependencies` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Runtime Dependencies` è `requirements_runtime_dependencies`. Nel grafo unificato, il nodo canonico `Runtime Dependencies` possiede l'identificatore esatto `Request-Dataset-Generator::requirements_runtime_dependencies`.
<!-- GRAPH_EDGE graph=unificata index=1011 role=outgoing -->
Nel grafo unificato, il nodo canonico `Runtime Dependencies` è la sorgente `source` della relazione `references` verso il nodo canonico `aiohttp`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::requirements_runtime_dependencies` all'ID `Request-Dataset-Generator::requirements_aiohttp`. La relazione tecnica esplicita è la seguente: `Runtime Dependencies` fa riferimento a `aiohttp`. Nel grafo unificato, il tipo dichiarato dell'arco 1011 che coinvolge il nodo canonico `Runtime Dependencies` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1011 che coinvolge il nodo canonico `Runtime Dependencies` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1011 che coinvolge il nodo canonico `Runtime Dependencies` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1011 che coinvolge il nodo canonico `Runtime Dependencies` è `requirements.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1011 che coinvolge il nodo canonico `Runtime Dependencies` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1011 che coinvolge il nodo canonico `Runtime Dependencies` è `1.0`. Nel grafo unificato, l'arco 1011 registra come `source` l'identificatore `Request-Dataset-Generator::requirements_runtime_dependencies` per la relazione che coinvolge il nodo canonico `Runtime Dependencies`. Nel grafo unificato, l'arco 1011 registra come `target` l'identificatore `Request-Dataset-Generator::requirements_aiohttp` per la relazione che coinvolge il nodo canonico `Runtime Dependencies`.
<!-- GRAPH_EDGE graph=unificata index=1012 role=outgoing -->
Nel grafo unificato, il nodo canonico `Runtime Dependencies` è la sorgente `source` della relazione `references` verso il nodo canonico `cryptography`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::requirements_runtime_dependencies` all'ID `Request-Dataset-Generator::requirements_cryptography`. La relazione tecnica esplicita è la seguente: `Runtime Dependencies` fa riferimento a `cryptography`. Nel grafo unificato, il tipo dichiarato dell'arco 1012 che coinvolge il nodo canonico `Runtime Dependencies` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1012 che coinvolge il nodo canonico `Runtime Dependencies` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1012 che coinvolge il nodo canonico `Runtime Dependencies` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1012 che coinvolge il nodo canonico `Runtime Dependencies` è `requirements.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1012 che coinvolge il nodo canonico `Runtime Dependencies` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1012 che coinvolge il nodo canonico `Runtime Dependencies` è `1.0`. Nel grafo unificato, l'arco 1012 registra come `source` l'identificatore `Request-Dataset-Generator::requirements_runtime_dependencies` per la relazione che coinvolge il nodo canonico `Runtime Dependencies`. Nel grafo unificato, l'arco 1012 registra come `target` l'identificatore `Request-Dataset-Generator::requirements_cryptography` per la relazione che coinvolge il nodo canonico `Runtime Dependencies`.
<!-- GRAPH_EDGE graph=unificata index=1013 role=outgoing -->
Nel grafo unificato, il nodo canonico `Runtime Dependencies` è la sorgente `source` della relazione `references` verso il nodo canonico `matplotlib [requirements_matplotlib]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::requirements_runtime_dependencies` all'ID `Request-Dataset-Generator::requirements_matplotlib`. La relazione tecnica esplicita è la seguente: `Runtime Dependencies` fa riferimento a `matplotlib [requirements_matplotlib]`. Nel grafo unificato, il tipo dichiarato dell'arco 1013 che coinvolge il nodo canonico `Runtime Dependencies` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1013 che coinvolge il nodo canonico `Runtime Dependencies` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1013 che coinvolge il nodo canonico `Runtime Dependencies` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1013 che coinvolge il nodo canonico `Runtime Dependencies` è `requirements.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1013 che coinvolge il nodo canonico `Runtime Dependencies` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1013 che coinvolge il nodo canonico `Runtime Dependencies` è `1.0`. Nel grafo unificato, l'arco 1013 registra come `source` l'identificatore `Request-Dataset-Generator::requirements_runtime_dependencies` per la relazione che coinvolge il nodo canonico `Runtime Dependencies`. Nel grafo unificato, l'arco 1013 registra come `target` l'identificatore `Request-Dataset-Generator::requirements_matplotlib` per la relazione che coinvolge il nodo canonico `Runtime Dependencies`.
Nel grafo unificato, il nodo canonico `Runtime Dependencies` non possiede archi entranti registrati nel campo `links`.

### RVE Flows [readme_rve_flows]

<!-- GRAPH_NODE graph=codice index=506 id=readme_rve_flows -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `RVE Flows [readme_rve_flows]` è `RVE Flows`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `RVE Flows [readme_rve_flows]` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `RVE Flows [readme_rve_flows]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `RVE Flows [readme_rve_flows]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `RVE Flows [readme_rve_flows]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `RVE Flows [readme_rve_flows]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `RVE Flows [readme_rve_flows]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `RVE Flows [readme_rve_flows]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `RVE Flows [readme_rve_flows]` possiede l'identificatore esatto `readme_rve_flows`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `RVE Flows [readme_rve_flows]` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `RVE Flows [readme_rve_flows]` è `rve flows`.
<!-- GRAPH_EDGE graph=codice index=1000 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RVE Flows [readme_rve_flows]` è la sorgente `source` della relazione `references` verso il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]`; l'orientamento registrato nel JSON va dall'ID `readme_rve_flows` all'ID `readme_flow_context_call`. La relazione tecnica esplicita è la seguente: `RVE Flows [readme_rve_flows]` fa riferimento a `FLOW_CONTEXT_CALL [readme_flow_context_call]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1000 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1000 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1000 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1000 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1000 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1000 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `1.0`. Nel grafo del codice RDG, l'arco 1000 registra come `source` l'identificatore `readme_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]`. Nel grafo del codice RDG, l'arco 1000 registra come `target` l'identificatore `readme_flow_context_call` per la relazione che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]`.
<!-- GRAPH_EDGE graph=codice index=1001 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RVE Flows [readme_rve_flows]` è la sorgente `source` della relazione `references` verso il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]`; l'orientamento registrato nel JSON va dall'ID `readme_rve_flows` all'ID `readme_flow_patient_create`. La relazione tecnica esplicita è la seguente: `RVE Flows [readme_rve_flows]` fa riferimento a `FLOW_PATIENT_CREATE [readme_flow_patient_create]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1001 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1001 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1001 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1001 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1001 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1001 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `1.0`. Nel grafo del codice RDG, l'arco 1001 registra come `source` l'identificatore `readme_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]`. Nel grafo del codice RDG, l'arco 1001 registra come `target` l'identificatore `readme_flow_patient_create` per la relazione che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]`.
<!-- GRAPH_EDGE graph=codice index=1002 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RVE Flows [readme_rve_flows]` è la sorgente `source` della relazione `references` verso il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]`; l'orientamento registrato nel JSON va dall'ID `readme_rve_flows` all'ID `readme_flow_patient_query`. La relazione tecnica esplicita è la seguente: `RVE Flows [readme_rve_flows]` fa riferimento a `FLOW_PATIENT_QUERY [readme_flow_patient_query]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1002 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1002 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1002 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1002 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1002 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1002 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `1.0`. Nel grafo del codice RDG, l'arco 1002 registra come `source` l'identificatore `readme_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]`. Nel grafo del codice RDG, l'arco 1002 registra come `target` l'identificatore `readme_flow_patient_query` per la relazione che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]`.
<!-- GRAPH_EDGE graph=codice index=1003 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RVE Flows [readme_rve_flows]` è la sorgente `source` della relazione `semantically_similar_to` verso il nodo canonico `RVE Flows [usage_rve_flows]`; l'orientamento registrato nel JSON va dall'ID `readme_rve_flows` all'ID `usage_rve_flows`. La relazione tecnica esplicita è la seguente: `RVE Flows [readme_rve_flows]` è semanticamente simile a `RVE Flows [usage_rve_flows]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `semantically_similar_to`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `INFERRED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `0.95`. Nel grafo del codice RDG, il file sorgente dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `1.0`. Nel grafo del codice RDG, l'arco 1003 registra come `source` l'identificatore `readme_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]`. Nel grafo del codice RDG, l'arco 1003 registra come `target` l'identificatore `usage_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]`.
<!-- GRAPH_EDGE graph=codice index=999 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `RVE Flows [readme_rve_flows]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Request Dataset Generator RVE`; l'orientamento registrato nel JSON va dall'ID `readme_request_dataset_generator_rve` all'ID `readme_rve_flows`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE` fa riferimento a `RVE Flows [readme_rve_flows]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 999 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 999 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 999 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 999 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 999 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 999 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `1.0`. Nel grafo del codice RDG, l'arco 999 registra come `source` l'identificatore `readme_request_dataset_generator_rve` per la relazione che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]`. Nel grafo del codice RDG, l'arco 999 registra come `target` l'identificatore `readme_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]`.

<!-- GRAPH_NODE graph=unificata index=506 id=Request-Dataset-Generator::readme_rve_flows -->
Nel grafo unificato, l'etichetta originale del nodo canonico `RVE Flows [readme_rve_flows]` è `RVE Flows`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `RVE Flows [readme_rve_flows]` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `RVE Flows [readme_rve_flows]` è `README.md`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `RVE Flows [readme_rve_flows]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `RVE Flows [readme_rve_flows]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `RVE Flows [readme_rve_flows]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `RVE Flows [readme_rve_flows]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `RVE Flows [readme_rve_flows]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `RVE Flows [readme_rve_flows]` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `RVE Flows [readme_rve_flows]` è `rve flows`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `RVE Flows [readme_rve_flows]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `RVE Flows [readme_rve_flows]` è `readme_rve_flows`. Nel grafo unificato, il nodo canonico `RVE Flows [readme_rve_flows]` possiede l'identificatore esatto `Request-Dataset-Generator::readme_rve_flows`.
<!-- GRAPH_EDGE graph=unificata index=1000 role=outgoing -->
Nel grafo unificato, il nodo canonico `RVE Flows [readme_rve_flows]` è la sorgente `source` della relazione `references` verso il nodo canonico `FLOW_CONTEXT_CALL [readme_flow_context_call]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_rve_flows` all'ID `Request-Dataset-Generator::readme_flow_context_call`. La relazione tecnica esplicita è la seguente: `RVE Flows [readme_rve_flows]` fa riferimento a `FLOW_CONTEXT_CALL [readme_flow_context_call]`. Nel grafo unificato, il tipo dichiarato dell'arco 1000 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1000 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1000 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1000 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1000 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1000 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `1.0`. Nel grafo unificato, l'arco 1000 registra come `source` l'identificatore `Request-Dataset-Generator::readme_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]`. Nel grafo unificato, l'arco 1000 registra come `target` l'identificatore `Request-Dataset-Generator::readme_flow_context_call` per la relazione che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]`.
<!-- GRAPH_EDGE graph=unificata index=1001 role=outgoing -->
Nel grafo unificato, il nodo canonico `RVE Flows [readme_rve_flows]` è la sorgente `source` della relazione `references` verso il nodo canonico `FLOW_PATIENT_CREATE [readme_flow_patient_create]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_rve_flows` all'ID `Request-Dataset-Generator::readme_flow_patient_create`. La relazione tecnica esplicita è la seguente: `RVE Flows [readme_rve_flows]` fa riferimento a `FLOW_PATIENT_CREATE [readme_flow_patient_create]`. Nel grafo unificato, il tipo dichiarato dell'arco 1001 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1001 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1001 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1001 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1001 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1001 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `1.0`. Nel grafo unificato, l'arco 1001 registra come `source` l'identificatore `Request-Dataset-Generator::readme_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]`. Nel grafo unificato, l'arco 1001 registra come `target` l'identificatore `Request-Dataset-Generator::readme_flow_patient_create` per la relazione che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]`.
<!-- GRAPH_EDGE graph=unificata index=1002 role=outgoing -->
Nel grafo unificato, il nodo canonico `RVE Flows [readme_rve_flows]` è la sorgente `source` della relazione `references` verso il nodo canonico `FLOW_PATIENT_QUERY [readme_flow_patient_query]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_rve_flows` all'ID `Request-Dataset-Generator::readme_flow_patient_query`. La relazione tecnica esplicita è la seguente: `RVE Flows [readme_rve_flows]` fa riferimento a `FLOW_PATIENT_QUERY [readme_flow_patient_query]`. Nel grafo unificato, il tipo dichiarato dell'arco 1002 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1002 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1002 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1002 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1002 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1002 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `1.0`. Nel grafo unificato, l'arco 1002 registra come `source` l'identificatore `Request-Dataset-Generator::readme_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]`. Nel grafo unificato, l'arco 1002 registra come `target` l'identificatore `Request-Dataset-Generator::readme_flow_patient_query` per la relazione che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]`.
<!-- GRAPH_EDGE graph=unificata index=1003 role=outgoing -->
Nel grafo unificato, il nodo canonico `RVE Flows [readme_rve_flows]` è la sorgente `source` della relazione `semantically_similar_to` verso il nodo canonico `RVE Flows [usage_rve_flows]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_rve_flows` all'ID `Request-Dataset-Generator::usage_rve_flows`. La relazione tecnica esplicita è la seguente: `RVE Flows [readme_rve_flows]` è semanticamente simile a `RVE Flows [usage_rve_flows]`. Nel grafo unificato, il tipo dichiarato dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `semantically_similar_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `INFERRED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `0.95`. Nel grafo unificato, il file sorgente dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `1.0`. Nel grafo unificato, l'arco 1003 registra come `source` l'identificatore `Request-Dataset-Generator::readme_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]`. Nel grafo unificato, l'arco 1003 registra come `target` l'identificatore `Request-Dataset-Generator::usage_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]`.
<!-- GRAPH_EDGE graph=unificata index=999 role=incoming -->
Nel grafo unificato, il nodo canonico `RVE Flows [readme_rve_flows]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Request Dataset Generator RVE`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_request_dataset_generator_rve` all'ID `Request-Dataset-Generator::readme_rve_flows`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE` fa riferimento a `RVE Flows [readme_rve_flows]`. Nel grafo unificato, il tipo dichiarato dell'arco 999 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 999 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 999 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 999 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 999 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 999 che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]` è `1.0`. Nel grafo unificato, l'arco 999 registra come `source` l'identificatore `Request-Dataset-Generator::readme_request_dataset_generator_rve` per la relazione che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]`. Nel grafo unificato, l'arco 999 registra come `target` l'identificatore `Request-Dataset-Generator::readme_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [readme_rve_flows]`.

### RVE Flows [usage_rve_flows]

<!-- GRAPH_NODE graph=codice index=522 id=usage_rve_flows -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `RVE Flows [usage_rve_flows]` è `RVE Flows`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `RVE Flows [usage_rve_flows]` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `RVE Flows [usage_rve_flows]` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `RVE Flows [usage_rve_flows]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `RVE Flows [usage_rve_flows]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `RVE Flows [usage_rve_flows]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `RVE Flows [usage_rve_flows]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `RVE Flows [usage_rve_flows]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `RVE Flows [usage_rve_flows]` possiede l'identificatore esatto `usage_rve_flows`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `RVE Flows [usage_rve_flows]` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `RVE Flows [usage_rve_flows]` è `rve flows`.
<!-- GRAPH_EDGE graph=codice index=1023 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RVE Flows [usage_rve_flows]` è la sorgente `source` della relazione `references` verso il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]`; l'orientamento registrato nel JSON va dall'ID `usage_rve_flows` all'ID `usage_flow_context_call`. La relazione tecnica esplicita è la seguente: `RVE Flows [usage_rve_flows]` fa riferimento a `FLOW_CONTEXT_CALL [usage_flow_context_call]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1023 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1023 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1023 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1023 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1023 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1023 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `1.0`. Nel grafo del codice RDG, l'arco 1023 registra come `source` l'identificatore `usage_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]`. Nel grafo del codice RDG, l'arco 1023 registra come `target` l'identificatore `usage_flow_context_call` per la relazione che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]`.
<!-- GRAPH_EDGE graph=codice index=1024 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RVE Flows [usage_rve_flows]` è la sorgente `source` della relazione `references` verso il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]`; l'orientamento registrato nel JSON va dall'ID `usage_rve_flows` all'ID `usage_flow_patient_create`. La relazione tecnica esplicita è la seguente: `RVE Flows [usage_rve_flows]` fa riferimento a `FLOW_PATIENT_CREATE [usage_flow_patient_create]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1024 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1024 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1024 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1024 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1024 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1024 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `1.0`. Nel grafo del codice RDG, l'arco 1024 registra come `source` l'identificatore `usage_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]`. Nel grafo del codice RDG, l'arco 1024 registra come `target` l'identificatore `usage_flow_patient_create` per la relazione che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]`.
<!-- GRAPH_EDGE graph=codice index=1025 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RVE Flows [usage_rve_flows]` è la sorgente `source` della relazione `references` verso il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]`; l'orientamento registrato nel JSON va dall'ID `usage_rve_flows` all'ID `usage_flow_patient_query`. La relazione tecnica esplicita è la seguente: `RVE Flows [usage_rve_flows]` fa riferimento a `FLOW_PATIENT_QUERY [usage_flow_patient_query]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1025 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1025 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1025 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1025 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1025 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1025 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `1.0`. Nel grafo del codice RDG, l'arco 1025 registra come `source` l'identificatore `usage_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]`. Nel grafo del codice RDG, l'arco 1025 registra come `target` l'identificatore `usage_flow_patient_query` per la relazione che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]`.
<!-- GRAPH_EDGE graph=codice index=1003 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `RVE Flows [usage_rve_flows]` è il destinatario `target` della relazione `semantically_similar_to` proveniente dal nodo canonico `RVE Flows [readme_rve_flows]`; l'orientamento registrato nel JSON va dall'ID `readme_rve_flows` all'ID `usage_rve_flows`. La relazione tecnica esplicita è la seguente: `RVE Flows [readme_rve_flows]` è semanticamente simile a `RVE Flows [usage_rve_flows]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `semantically_similar_to`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `INFERRED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `0.95`. Nel grafo del codice RDG, il file sorgente dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `1.0`. Nel grafo del codice RDG, l'arco 1003 registra come `source` l'identificatore `readme_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]`. Nel grafo del codice RDG, l'arco 1003 registra come `target` l'identificatore `usage_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]`.
<!-- GRAPH_EDGE graph=codice index=1022 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `RVE Flows [usage_rve_flows]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Request Dataset Generator RVE Guide`; l'orientamento registrato nel JSON va dall'ID `usage_request_dataset_generator_rve_guide` all'ID `usage_rve_flows`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `RVE Flows [usage_rve_flows]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1022 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1022 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1022 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1022 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `usage.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1022 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1022 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `1.0`. Nel grafo del codice RDG, l'arco 1022 registra come `source` l'identificatore `usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]`. Nel grafo del codice RDG, l'arco 1022 registra come `target` l'identificatore `usage_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]`.

<!-- GRAPH_NODE graph=unificata index=522 id=Request-Dataset-Generator::usage_rve_flows -->
Nel grafo unificato, l'etichetta originale del nodo canonico `RVE Flows [usage_rve_flows]` è `RVE Flows`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `RVE Flows [usage_rve_flows]` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `RVE Flows [usage_rve_flows]` è `usage.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `RVE Flows [usage_rve_flows]` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `RVE Flows [usage_rve_flows]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `RVE Flows [usage_rve_flows]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `RVE Flows [usage_rve_flows]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `RVE Flows [usage_rve_flows]` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `RVE Flows [usage_rve_flows]` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `RVE Flows [usage_rve_flows]` è `rve flows`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `RVE Flows [usage_rve_flows]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `RVE Flows [usage_rve_flows]` è `usage_rve_flows`. Nel grafo unificato, il nodo canonico `RVE Flows [usage_rve_flows]` possiede l'identificatore esatto `Request-Dataset-Generator::usage_rve_flows`.
<!-- GRAPH_EDGE graph=unificata index=1023 role=outgoing -->
Nel grafo unificato, il nodo canonico `RVE Flows [usage_rve_flows]` è la sorgente `source` della relazione `references` verso il nodo canonico `FLOW_CONTEXT_CALL [usage_flow_context_call]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_rve_flows` all'ID `Request-Dataset-Generator::usage_flow_context_call`. La relazione tecnica esplicita è la seguente: `RVE Flows [usage_rve_flows]` fa riferimento a `FLOW_CONTEXT_CALL [usage_flow_context_call]`. Nel grafo unificato, il tipo dichiarato dell'arco 1023 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1023 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1023 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1023 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1023 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1023 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `1.0`. Nel grafo unificato, l'arco 1023 registra come `source` l'identificatore `Request-Dataset-Generator::usage_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]`. Nel grafo unificato, l'arco 1023 registra come `target` l'identificatore `Request-Dataset-Generator::usage_flow_context_call` per la relazione che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]`.
<!-- GRAPH_EDGE graph=unificata index=1024 role=outgoing -->
Nel grafo unificato, il nodo canonico `RVE Flows [usage_rve_flows]` è la sorgente `source` della relazione `references` verso il nodo canonico `FLOW_PATIENT_CREATE [usage_flow_patient_create]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_rve_flows` all'ID `Request-Dataset-Generator::usage_flow_patient_create`. La relazione tecnica esplicita è la seguente: `RVE Flows [usage_rve_flows]` fa riferimento a `FLOW_PATIENT_CREATE [usage_flow_patient_create]`. Nel grafo unificato, il tipo dichiarato dell'arco 1024 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1024 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1024 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1024 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1024 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1024 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `1.0`. Nel grafo unificato, l'arco 1024 registra come `source` l'identificatore `Request-Dataset-Generator::usage_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]`. Nel grafo unificato, l'arco 1024 registra come `target` l'identificatore `Request-Dataset-Generator::usage_flow_patient_create` per la relazione che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]`.
<!-- GRAPH_EDGE graph=unificata index=1025 role=outgoing -->
Nel grafo unificato, il nodo canonico `RVE Flows [usage_rve_flows]` è la sorgente `source` della relazione `references` verso il nodo canonico `FLOW_PATIENT_QUERY [usage_flow_patient_query]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_rve_flows` all'ID `Request-Dataset-Generator::usage_flow_patient_query`. La relazione tecnica esplicita è la seguente: `RVE Flows [usage_rve_flows]` fa riferimento a `FLOW_PATIENT_QUERY [usage_flow_patient_query]`. Nel grafo unificato, il tipo dichiarato dell'arco 1025 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1025 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1025 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1025 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1025 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1025 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `1.0`. Nel grafo unificato, l'arco 1025 registra come `source` l'identificatore `Request-Dataset-Generator::usage_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]`. Nel grafo unificato, l'arco 1025 registra come `target` l'identificatore `Request-Dataset-Generator::usage_flow_patient_query` per la relazione che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]`.
<!-- GRAPH_EDGE graph=unificata index=1003 role=incoming -->
Nel grafo unificato, il nodo canonico `RVE Flows [usage_rve_flows]` è il destinatario `target` della relazione `semantically_similar_to` proveniente dal nodo canonico `RVE Flows [readme_rve_flows]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_rve_flows` all'ID `Request-Dataset-Generator::usage_rve_flows`. La relazione tecnica esplicita è la seguente: `RVE Flows [readme_rve_flows]` è semanticamente simile a `RVE Flows [usage_rve_flows]`. Nel grafo unificato, il tipo dichiarato dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `semantically_similar_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `INFERRED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `0.95`. Nel grafo unificato, il file sorgente dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1003 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `1.0`. Nel grafo unificato, l'arco 1003 registra come `source` l'identificatore `Request-Dataset-Generator::readme_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]`. Nel grafo unificato, l'arco 1003 registra come `target` l'identificatore `Request-Dataset-Generator::usage_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]`.
<!-- GRAPH_EDGE graph=unificata index=1022 role=incoming -->
Nel grafo unificato, il nodo canonico `RVE Flows [usage_rve_flows]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Request Dataset Generator RVE Guide`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` all'ID `Request-Dataset-Generator::usage_rve_flows`. La relazione tecnica esplicita è la seguente: `Request Dataset Generator RVE Guide` fa riferimento a `RVE Flows [usage_rve_flows]`. Nel grafo unificato, il tipo dichiarato dell'arco 1022 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1022 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1022 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1022 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `usage.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1022 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1022 che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]` è `1.0`. Nel grafo unificato, l'arco 1022 registra come `source` l'identificatore `Request-Dataset-Generator::usage_request_dataset_generator_rve_guide` per la relazione che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]`. Nel grafo unificato, l'arco 1022 registra come `target` l'identificatore `Request-Dataset-Generator::usage_rve_flows` per la relazione che coinvolge il nodo canonico `RVE Flows [usage_rve_flows]`.

### UI Dependencies

<!-- GRAPH_NODE graph=codice index=518 id=ui_requirements_ui_dependencies -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `UI Dependencies` è `UI Dependencies`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `UI Dependencies` come `document`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `UI Dependencies` è `ui/requirements.txt`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `UI Dependencies` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `UI Dependencies` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `UI Dependencies` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `UI Dependencies` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `UI Dependencies` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `UI Dependencies` possiede l'identificatore esatto `ui_requirements_ui_dependencies`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `UI Dependencies` alla comunità numerica `9`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `UI Dependencies` è `ui dependencies`.
<!-- GRAPH_EDGE graph=codice index=1015 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `UI Dependencies` è la sorgente `source` della relazione `references` verso il nodo canonico `customtkinter`; l'orientamento registrato nel JSON va dall'ID `ui_requirements_ui_dependencies` all'ID `ui_requirements_customtkinter`. La relazione tecnica esplicita è la seguente: `UI Dependencies` fa riferimento a `customtkinter`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1015 che coinvolge il nodo canonico `UI Dependencies` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1015 che coinvolge il nodo canonico `UI Dependencies` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1015 che coinvolge il nodo canonico `UI Dependencies` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1015 che coinvolge il nodo canonico `UI Dependencies` è `ui/requirements.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1015 che coinvolge il nodo canonico `UI Dependencies` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1015 che coinvolge il nodo canonico `UI Dependencies` è `1.0`. Nel grafo del codice RDG, l'arco 1015 registra come `source` l'identificatore `ui_requirements_ui_dependencies` per la relazione che coinvolge il nodo canonico `UI Dependencies`. Nel grafo del codice RDG, l'arco 1015 registra come `target` l'identificatore `ui_requirements_customtkinter` per la relazione che coinvolge il nodo canonico `UI Dependencies`.
<!-- GRAPH_EDGE graph=codice index=1016 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `UI Dependencies` è la sorgente `source` della relazione `references` verso il nodo canonico `matplotlib [ui_requirements_matplotlib]`; l'orientamento registrato nel JSON va dall'ID `ui_requirements_ui_dependencies` all'ID `ui_requirements_matplotlib`. La relazione tecnica esplicita è la seguente: `UI Dependencies` fa riferimento a `matplotlib [ui_requirements_matplotlib]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1016 che coinvolge il nodo canonico `UI Dependencies` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1016 che coinvolge il nodo canonico `UI Dependencies` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1016 che coinvolge il nodo canonico `UI Dependencies` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1016 che coinvolge il nodo canonico `UI Dependencies` è `ui/requirements.txt`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1016 che coinvolge il nodo canonico `UI Dependencies` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1016 che coinvolge il nodo canonico `UI Dependencies` è `1.0`. Nel grafo del codice RDG, l'arco 1016 registra come `source` l'identificatore `ui_requirements_ui_dependencies` per la relazione che coinvolge il nodo canonico `UI Dependencies`. Nel grafo del codice RDG, l'arco 1016 registra come `target` l'identificatore `ui_requirements_matplotlib` per la relazione che coinvolge il nodo canonico `UI Dependencies`.
Nel grafo del codice RDG, il nodo canonico `UI Dependencies` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=518 id=Request-Dataset-Generator::ui_requirements_ui_dependencies -->
Nel grafo unificato, l'etichetta originale del nodo canonico `UI Dependencies` è `UI Dependencies`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `UI Dependencies` come `document`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `UI Dependencies` è `ui/requirements.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `UI Dependencies` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `UI Dependencies` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `UI Dependencies` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `UI Dependencies` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `UI Dependencies` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `UI Dependencies` alla comunità numerica `9`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `UI Dependencies` è `ui dependencies`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `UI Dependencies` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `UI Dependencies` è `ui_requirements_ui_dependencies`. Nel grafo unificato, il nodo canonico `UI Dependencies` possiede l'identificatore esatto `Request-Dataset-Generator::ui_requirements_ui_dependencies`.
<!-- GRAPH_EDGE graph=unificata index=1015 role=outgoing -->
Nel grafo unificato, il nodo canonico `UI Dependencies` è la sorgente `source` della relazione `references` verso il nodo canonico `customtkinter`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_requirements_ui_dependencies` all'ID `Request-Dataset-Generator::ui_requirements_customtkinter`. La relazione tecnica esplicita è la seguente: `UI Dependencies` fa riferimento a `customtkinter`. Nel grafo unificato, il tipo dichiarato dell'arco 1015 che coinvolge il nodo canonico `UI Dependencies` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1015 che coinvolge il nodo canonico `UI Dependencies` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1015 che coinvolge il nodo canonico `UI Dependencies` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1015 che coinvolge il nodo canonico `UI Dependencies` è `ui/requirements.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1015 che coinvolge il nodo canonico `UI Dependencies` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1015 che coinvolge il nodo canonico `UI Dependencies` è `1.0`. Nel grafo unificato, l'arco 1015 registra come `source` l'identificatore `Request-Dataset-Generator::ui_requirements_ui_dependencies` per la relazione che coinvolge il nodo canonico `UI Dependencies`. Nel grafo unificato, l'arco 1015 registra come `target` l'identificatore `Request-Dataset-Generator::ui_requirements_customtkinter` per la relazione che coinvolge il nodo canonico `UI Dependencies`.
<!-- GRAPH_EDGE graph=unificata index=1016 role=outgoing -->
Nel grafo unificato, il nodo canonico `UI Dependencies` è la sorgente `source` della relazione `references` verso il nodo canonico `matplotlib [ui_requirements_matplotlib]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_requirements_ui_dependencies` all'ID `Request-Dataset-Generator::ui_requirements_matplotlib`. La relazione tecnica esplicita è la seguente: `UI Dependencies` fa riferimento a `matplotlib [ui_requirements_matplotlib]`. Nel grafo unificato, il tipo dichiarato dell'arco 1016 che coinvolge il nodo canonico `UI Dependencies` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1016 che coinvolge il nodo canonico `UI Dependencies` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1016 che coinvolge il nodo canonico `UI Dependencies` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1016 che coinvolge il nodo canonico `UI Dependencies` è `ui/requirements.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1016 che coinvolge il nodo canonico `UI Dependencies` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1016 che coinvolge il nodo canonico `UI Dependencies` è `1.0`. Nel grafo unificato, l'arco 1016 registra come `source` l'identificatore `Request-Dataset-Generator::ui_requirements_ui_dependencies` per la relazione che coinvolge il nodo canonico `UI Dependencies`. Nel grafo unificato, l'arco 1016 registra come `target` l'identificatore `Request-Dataset-Generator::ui_requirements_matplotlib` per la relazione che coinvolge il nodo canonico `UI Dependencies`.
Nel grafo unificato, il nodo canonico `UI Dependencies` non possiede archi entranti registrati nel campo `links`.
