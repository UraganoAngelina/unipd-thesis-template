# Interoperabilità tecnico-normativa — parte 2

## Metadati

- Dominio: Interoperabilità tecnico-normativa.
- Data di generazione: 2026-07-31.
- Versione dei grafi sorgente: KB documentale `5f9434fc0f01de5d74dd3c6d72fffc217b7f8629`; KB codice RDG `06d5dd91cef2e12574b63a36d73b049a3588cdf8`; KB unificata `null`.
- Nodi coperti: 30 nodi canonici e 60 occorrenze fisiche; tipi canonici: concept: 23, document: 5, paper: 1, rationale: 1.
- Archi coperti: 40 occorrenze fisiche incidenti ai nodi del documento; tipi: conceptually_related_to: 4, references: 32, shares_data_with: 4.
- Iperarchi coperti: 4 occorrenze fisiche, replicate anche negli altri file coinvolti quando attraversano la segmentazione editoriale.
- Nota sulla direzione: tutti e tre i grafi dichiarano `directed=false`; la prosa conserva comunque l'orientamento dei campi JSON `source` e `target` senza trasformarlo in causalità.

## Glossario

- EXTRACTED — Valore della proprietà `confidence` che il grafo assegna a una relazione estratta.
- INFERRED — Valore della proprietà `confidence` che il grafo assegna a una relazione inferita; la prosa non promuove una relazione INFERRED a fatto estratto.
- APMS — Acronimo presente nel grafo; l'espansione non è registrata. [DA VERIFICARE: espansione assente dal grafo]
- COT — Acronimo presente nel grafo; l'espansione non è registrata. [DA VERIFICARE: espansione assente dal grafo]
- FHIR — Nome canonico dell'acronimo usato nelle etichette del grafo.
- FSE — Acronimo presente nel grafo; l'espansione non è registrata. [DA VERIFICARE: espansione assente dal grafo]
- IHE — Nome canonico dell'acronimo usato nelle etichette del grafo.
- IUA — Nome canonico dell'acronimo usato nelle etichette del grafo.
- JWT — Nome canonico dell'acronimo usato nelle etichette del grafo.
- PUA — Acronimo presente nel grafo; l'espansione non è registrata. [DA VERIFICARE: espansione assente dal grafo]
- RVE — Nome canonico del prefisso delle transazioni regionali nel grafo.
- SAML — Nome canonico dell'acronimo usato nelle etichette del grafo.
- SI.Ter — Nome canonico adottato dalle etichette del grafo.
- XDS — Nome canonico dell'acronimo usato nelle etichette del grafo.

### Mappa dei nomi canonici e delle varianti

La mappa seguente non fonde nodi solo perché semanticamente simili. Le occorrenze del grafo sorgente e del grafo unificato sono ricondotte allo stesso nome soltanto tramite `repo` e `local_id`; le etichette duplicate di nodi distinti sono disambiguate con l'identificatore locale.

- Il nome canonico `Componenti Accenture DIH` identifica il nodo `graphify_kb::dimensionamento_accenture_dih`; le etichette originali conservate sono `Componenti Accenture DIH`.
- Il nome canonico `Componenti Almaviva ePersonam` identifica il nodo `graphify_kb::dimensionamento_almaviva_epersonam`; le etichette originali conservate sono `Componenti Almaviva ePersonam`.
- Il nome canonico `Componenti GPI SisTer` identifica il nodo `graphify_kb::dimensionamento_gpi_sister`; le etichette originali conservate sono `Componenti GPI SisTer`.
- Il nome canonico `SI.Ter Dimensionamento Computazionale` identifica il nodo `graphify_kb::dimensionamento_siter`; le etichette originali conservate sono `SI.Ter Dimensionamento Computazionale`.
- Il nome canonico `Camunda BPMN Rules` identifica il nodo `graphify_kb::pianificazione_camunda_bpmn_rules`; le etichette originali conservate sono `Camunda BPMN Rules`.
- Il nome canonico `Dimissione Protetta End To End` identifica il nodo `graphify_kb::pianificazione_dimissione_protetta_e2e`; le etichette originali conservate sono `Dimissione Protetta End To End`.
- Il nome canonico `FSEr XDS Layer` identifica il nodo `graphify_kb::pianificazione_fser_xds_layer`; le etichette originali conservate sono `FSEr XDS Layer`.
- Il nome canonico `RabbitMQ Queues` identifica il nodo `graphify_kb::pianificazione_rabbitmq_queues`; le etichette originali conservate sono `RabbitMQ Queues`.
- Il nome canonico `Impact of Latency on Applications Performance` identifica il nodo `graphify_kb::srg_latency_performance`; le etichette originali conservate sono `Impact of Latency on Applications Performance`.
- Il nome canonico `MPI/Pro` identifica il nodo `graphify_kb::srg_mpi_pro`; le etichette originali conservate sono `MPI/Pro`.
- Il nome canonico `Ping-Pong Tests` identifica il nodo `graphify_kb::srg_ping_pong_tests`; le etichette originali conservate sono `Ping-Pong Tests`.
- Il nome canonico `Point-to-Point Latency` identifica il nodo `graphify_kb::srg_point_to_point_latency`; le etichette originali conservate sono `Point-to-Point Latency`.
- Il nome canonico `Document Source` identifica il nodo `graphify_kb::xvalue_document_source`; le etichette originali conservate sono `Document Source`.
- Il nome canonico `Specifiche integrazione XValue` identifica il nodo `graphify_kb::xvalue_specifiche`; le etichette originali conservate sono `Specifiche integrazione XValue`.
- Il nome canonico `XDS Document Registry` identifica il nodo `graphify_kb::xvalue_xds_registry`; le etichette originali conservate sono `XDS Document Registry`.
- Il nome canonico `XDS Document Repository` identifica il nodo `graphify_kb::xvalue_xds_repository`; le etichette originali conservate sono `XDS Document Repository`.
- Il nome canonico `Data Governance` identifica il nodo `graphify_kb::data_governance`; le etichette originali conservate sono `Data Governance`.
- Il nome canonico `Data Reference Architecture` identifica il nodo `graphify_kb::data_reference_architecture`; le etichette originali conservate sono `Data Reference Architecture`.
- Il nome canonico `Data Virtualization` identifica il nodo `graphify_kb::data_virtualization`; le etichette originali conservate sono `Data Virtualization`.
- Il nome canonico `Camunda v7` identifica il nodo `graphify_kb::rti_camunda`; le etichette originali conservate sono `Camunda v7`.
- Il nome canonico `Layer Interoperabilita` identifica il nodo `graphify_kb::rti_layer_interoperabilita`; le etichette originali conservate sono `Layer Interoperabilita`.
- Il nome canonico `RabbitMQ` identifica il nodo `graphify_kb::rti_rabbitmq`; le etichette originali conservate sono `RabbitMQ`.
- Il nome canonico `Authorization Code Flow` identifica il nodo `graphify_kb::apms_authorization_code_flow`; le etichette originali conservate sono `Authorization Code Flow`.
- Il nome canonico `ID Token` identifica il nodo `graphify_kb::apms_id_token`; le etichette originali conservate sono `ID Token`.
- Il nome canonico `eIDAS` identifica il nodo `graphify_kb::eidas`; le etichette originali conservate sono `eIDAS`.
- Il nome canonico `Firme e Sigilli Elettronici` identifica il nodo `graphify_kb::eidas_signatures`; le etichette originali conservate sono `Firme e Sigilli Elettronici`.
- Il nome canonico `Firma Elettronica Qualificata` identifica il nodo `graphify_kb::feq`; le etichette originali conservate sono `Firma Elettronica Qualificata`.
- Il nome canonico `Qualified Trust Service Provider` identifica il nodo `graphify_kb::qtsp`; le etichette originali conservate sono `Qualified Trust Service Provider`.
- Il nome canonico `Flussi NSIS` identifica il nodo `graphify_kb::percorsi_flussi_nsis`; le etichette originali conservate sono `Flussi NSIS`.
- Il nome canonico `Percorsi Sistemi e Flussi Sanitari` identifica il nodo `graphify_kb::percorsi_flussi_sanitari`; le etichette originali conservate sono `Percorsi Sistemi e Flussi Sanitari`.

## Iperarchi del dominio

<!-- GRAPH_HYPEREDGE graph=documentale index=0 copy=02_interoperabilita_tecnico_normativa_parte2.md -->
Nel grafo documentale, l'iperarco `Sanita Territoriale Operating Model` con identificatore `sanita_territoriale_operating_model` comprende esattamente i nodi `Decreto Ministeriale 77/2022` (ID locale `dm77_decreto`), `CO 116117` (ID locale `analisi_co116117`), `Punto Unico di Accesso` (ID locale `analisi_pua`), `Centrale Operativa Territoriale` (ID locale `analisi_cot`), `Progetto SI.Ter` (ID locale `analisi_progetto_siter`), `Flussi NSIS` (ID locale `percorsi_flussi_nsis`). Nel grafo documentale, l'iperarco `Sanita Territoriale Operating Model` con identificatore `sanita_territoriale_operating_model` dichiara la relazione di gruppo `form`; il grafo associa tutti i membri elencati all'etichetta dell'iperarco senza specificare una coppia `source`-`target`. Nel grafo documentale, la proprietà `id` di l'iperarco `Sanita Territoriale Operating Model` con identificatore `sanita_territoriale_operating_model` vale `sanita_territoriale_operating_model`. Nel grafo documentale, la proprietà `label` di l'iperarco `Sanita Territoriale Operating Model` con identificatore `sanita_territoriale_operating_model` vale `Sanita Territoriale Operating Model`. Nel grafo documentale, la proprietà `nodes` di l'iperarco `Sanita Territoriale Operating Model` con identificatore `sanita_territoriale_operating_model` conserva nell'ordine il valore esatto `["dm77_decreto", "analisi_co116117", "analisi_pua", "analisi_cot", "analisi_progetto_siter", "percorsi_flussi_nsis"]`. Nel grafo documentale, la proprietà `relation` di l'iperarco `Sanita Territoriale Operating Model` con identificatore `sanita_territoriale_operating_model` vale `form`. Nel grafo documentale, la confidenza categoriale di l'iperarco `Sanita Territoriale Operating Model` con identificatore `sanita_territoriale_operating_model` vale `INFERRED`. Nel grafo documentale, il punteggio di confidenza di l'iperarco `Sanita Territoriale Operating Model` con identificatore `sanita_territoriale_operating_model` vale `0.85`. Nel grafo documentale, il file sorgente di l'iperarco `Sanita Territoriale Operating Model` con identificatore `sanita_territoriale_operating_model` è `analisi_completa.md`.

<!-- GRAPH_HYPEREDGE graph=documentale index=1 copy=02_interoperabilita_tecnico_normativa_parte2.md -->
Nel grafo documentale, l'iperarco `SI.Ter Integration Stack` con identificatore `siter_integration_stack` comprende esattamente i nodi `APMS` (ID locale `analisi_apms`), `Anagrafe Zero` (ID locale `analisi_anagrafe_zero`), `FSEr XDS` (ID locale `analisi_fser_xds`), `RabbitMQ` (ID locale `rti_rabbitmq`), `Camunda v7` (ID locale `rti_camunda`), `ePersonam [rti_epersonam]` (ID locale `rti_epersonam`), `SisTer [rti_sister]` (ID locale `rti_sister`). Nel grafo documentale, l'iperarco `SI.Ter Integration Stack` con identificatore `siter_integration_stack` dichiara la relazione di gruppo `form`; il grafo associa tutti i membri elencati all'etichetta dell'iperarco senza specificare una coppia `source`-`target`. Nel grafo documentale, la proprietà `id` di l'iperarco `SI.Ter Integration Stack` con identificatore `siter_integration_stack` vale `siter_integration_stack`. Nel grafo documentale, la proprietà `label` di l'iperarco `SI.Ter Integration Stack` con identificatore `siter_integration_stack` vale `SI.Ter Integration Stack`. Nel grafo documentale, la proprietà `nodes` di l'iperarco `SI.Ter Integration Stack` con identificatore `siter_integration_stack` conserva nell'ordine il valore esatto `["analisi_apms", "analisi_anagrafe_zero", "analisi_fser_xds", "rti_rabbitmq", "rti_camunda", "rti_epersonam", "rti_sister"]`. Nel grafo documentale, la proprietà `relation` di l'iperarco `SI.Ter Integration Stack` con identificatore `siter_integration_stack` vale `form`. Nel grafo documentale, la confidenza categoriale di l'iperarco `SI.Ter Integration Stack` con identificatore `siter_integration_stack` vale `EXTRACTED`. Nel grafo documentale, il punteggio di confidenza di l'iperarco `SI.Ter Integration Stack` con identificatore `siter_integration_stack` vale `1.0`. Nel grafo documentale, il file sorgente di l'iperarco `SI.Ter Integration Stack` con identificatore `siter_integration_stack` è `RTI AlmavivA Relazione Tecnica Sanità digitale ID 2365 Lotto 3 - Azienda Zero Veneto SIT _RdO 5521029 1.pdf`.

<!-- GRAPH_HYPEREDGE graph=documentale index=2 copy=02_interoperabilita_tecnico_normativa_parte2.md -->
Nel grafo documentale, l'iperarco `XDS Document Sharing Metadata Pattern` con identificatore `xds_document_sharing_metadata_pattern` comprende esattamente i nodi `IHE XDS.b` (ID locale `affinity_xdsb`), `XDSDocumentEntry Metadata` (ID locale `affinity_xdsdocumententry_metadata`), `XDS.b Transactions` (ID locale `xvalue_xds_transactions`), `XDS Document Repository` (ID locale `xvalue_xds_repository`), `XDS Document Registry` (ID locale `xvalue_xds_registry`). Nel grafo documentale, l'iperarco `XDS Document Sharing Metadata Pattern` con identificatore `xds_document_sharing_metadata_pattern` dichiara la relazione di gruppo `form`; il grafo associa tutti i membri elencati all'etichetta dell'iperarco senza specificare una coppia `source`-`target`. Nel grafo documentale, la proprietà `id` di l'iperarco `XDS Document Sharing Metadata Pattern` con identificatore `xds_document_sharing_metadata_pattern` vale `xds_document_sharing_metadata_pattern`. Nel grafo documentale, la proprietà `label` di l'iperarco `XDS Document Sharing Metadata Pattern` con identificatore `xds_document_sharing_metadata_pattern` vale `XDS Document Sharing Metadata Pattern`. Nel grafo documentale, la proprietà `nodes` di l'iperarco `XDS Document Sharing Metadata Pattern` con identificatore `xds_document_sharing_metadata_pattern` conserva nell'ordine il valore esatto `["affinity_xdsb", "affinity_xdsdocumententry_metadata", "xvalue_xds_transactions", "xvalue_xds_repository", "xvalue_xds_registry"]`. Nel grafo documentale, la proprietà `relation` di l'iperarco `XDS Document Sharing Metadata Pattern` con identificatore `xds_document_sharing_metadata_pattern` vale `form`. Nel grafo documentale, la confidenza categoriale di l'iperarco `XDS Document Sharing Metadata Pattern` con identificatore `xds_document_sharing_metadata_pattern` vale `INFERRED`. Nel grafo documentale, il punteggio di confidenza di l'iperarco `XDS Document Sharing Metadata Pattern` con identificatore `xds_document_sharing_metadata_pattern` vale `0.85`. Nel grafo documentale, il file sorgente di l'iperarco `XDS Document Sharing Metadata Pattern` con identificatore `xds_document_sharing_metadata_pattern` è `Specifiche tecniche per l’interoperabilità 2.6.3 - Affinity Domain.pdf`.

<!-- GRAPH_HYPEREDGE graph=documentale index=4 copy=02_interoperabilita_tecnico_normativa_parte2.md -->
Nel grafo documentale, l'iperarco `Latency Aware Distributed Systems` con identificatore `latency_aware_distributed_systems` comprende esattamente i nodi `Edge Computing Latency` (ID locale `latency_edge_computing_latency`), `Persistent Connections` (ID locale `latency_persistent_connections`), `Load-Sharing Window` (ID locale `network_load_sharing_window`), `Beta Quantile Rule` (ID locale `network_beta_quantile_rule`), `Point-to-Point Latency` (ID locale `srg_point_to_point_latency`). Nel grafo documentale, l'iperarco `Latency Aware Distributed Systems` con identificatore `latency_aware_distributed_systems` dichiara la relazione di gruppo `form`; il grafo associa tutti i membri elencati all'etichetta dell'iperarco senza specificare una coppia `source`-`target`. Nel grafo documentale, la proprietà `id` di l'iperarco `Latency Aware Distributed Systems` con identificatore `latency_aware_distributed_systems` vale `latency_aware_distributed_systems`. Nel grafo documentale, la proprietà `label` di l'iperarco `Latency Aware Distributed Systems` con identificatore `latency_aware_distributed_systems` vale `Latency Aware Distributed Systems`. Nel grafo documentale, la proprietà `nodes` di l'iperarco `Latency Aware Distributed Systems` con identificatore `latency_aware_distributed_systems` conserva nell'ordine il valore esatto `["latency_edge_computing_latency", "latency_persistent_connections", "network_load_sharing_window", "network_beta_quantile_rule", "srg_point_to_point_latency"]`. Nel grafo documentale, la proprietà `relation` di l'iperarco `Latency Aware Distributed Systems` con identificatore `latency_aware_distributed_systems` vale `form`. Nel grafo documentale, la confidenza categoriale di l'iperarco `Latency Aware Distributed Systems` con identificatore `latency_aware_distributed_systems` vale `INFERRED`. Nel grafo documentale, il punteggio di confidenza di l'iperarco `Latency Aware Distributed Systems` con identificatore `latency_aware_distributed_systems` vale `0.75`. Nel grafo documentale, il file sorgente di l'iperarco `Latency Aware Distributed Systems` con identificatore `latency_aware_distributed_systems` è `3503954.3503956.pdf`.

## Cluster Graphify 10 — SI.Ter Sizing

Il cluster editoriale Graphify 10 del dominio Interoperabilità tecnico-normativa contiene 4 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### Componenti Accenture DIH

<!-- GRAPH_NODE graph=documentale index=22 id=dimensionamento_accenture_dih -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Componenti Accenture DIH` è `Componenti Accenture DIH`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Componenti Accenture DIH` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Componenti Accenture DIH` è `AQ2365-L3 _Si.Ter_DIMENSIONAMENTO INFRASTRUTTURA_V1.4.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Componenti Accenture DIH` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Componenti Accenture DIH` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Componenti Accenture DIH` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Componenti Accenture DIH` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Componenti Accenture DIH` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Componenti Accenture DIH` possiede l'identificatore esatto `dimensionamento_accenture_dih`. Nel grafo documentale, Graphify assegna il nodo canonico `Componenti Accenture DIH` alla comunità numerica `10`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Componenti Accenture DIH` è `componenti accenture dih`.
Nel grafo documentale, il nodo canonico `Componenti Accenture DIH` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=documentale index=24 role=incoming -->
Nel grafo documentale, il nodo canonico `Componenti Accenture DIH` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `SI.Ter Dimensionamento Computazionale`; l'orientamento registrato nel JSON va dall'ID `dimensionamento_siter` all'ID `dimensionamento_accenture_dih`. La relazione tecnica esplicita è la seguente: `SI.Ter Dimensionamento Computazionale` fa riferimento a `Componenti Accenture DIH`. Nel grafo documentale, il tipo dichiarato dell'arco 24 che coinvolge il nodo canonico `Componenti Accenture DIH` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 24 che coinvolge il nodo canonico `Componenti Accenture DIH` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 24 che coinvolge il nodo canonico `Componenti Accenture DIH` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 24 che coinvolge il nodo canonico `Componenti Accenture DIH` è `AQ2365-L3 _Si.Ter_DIMENSIONAMENTO INFRASTRUTTURA_V1.4.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 24 che coinvolge il nodo canonico `Componenti Accenture DIH` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 24 che coinvolge il nodo canonico `Componenti Accenture DIH` è `1.0`. Nel grafo documentale, l'arco 24 registra come `source` l'identificatore `dimensionamento_siter` per la relazione che coinvolge il nodo canonico `Componenti Accenture DIH`. Nel grafo documentale, l'arco 24 registra come `target` l'identificatore `dimensionamento_accenture_dih` per la relazione che coinvolge il nodo canonico `Componenti Accenture DIH`.

<!-- GRAPH_NODE graph=unificata index=555 id=graphify_kb::dimensionamento_accenture_dih -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Componenti Accenture DIH` è `Componenti Accenture DIH`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Componenti Accenture DIH` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Componenti Accenture DIH` è `AQ2365-L3 _Si.Ter_DIMENSIONAMENTO INFRASTRUTTURA_V1.4.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Componenti Accenture DIH` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Componenti Accenture DIH` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Componenti Accenture DIH` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Componenti Accenture DIH` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Componenti Accenture DIH` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Componenti Accenture DIH` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Componenti Accenture DIH` è `componenti accenture dih`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Componenti Accenture DIH` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Componenti Accenture DIH` è `dimensionamento_accenture_dih`. Nel grafo unificato, il nodo canonico `Componenti Accenture DIH` possiede l'identificatore esatto `graphify_kb::dimensionamento_accenture_dih`.
Nel grafo unificato, il nodo canonico `Componenti Accenture DIH` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1052 role=incoming -->
Nel grafo unificato, il nodo canonico `Componenti Accenture DIH` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `SI.Ter Dimensionamento Computazionale`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::dimensionamento_siter` all'ID `graphify_kb::dimensionamento_accenture_dih`. La relazione tecnica esplicita è la seguente: `SI.Ter Dimensionamento Computazionale` fa riferimento a `Componenti Accenture DIH`. Nel grafo unificato, il tipo dichiarato dell'arco 1052 che coinvolge il nodo canonico `Componenti Accenture DIH` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1052 che coinvolge il nodo canonico `Componenti Accenture DIH` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1052 che coinvolge il nodo canonico `Componenti Accenture DIH` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1052 che coinvolge il nodo canonico `Componenti Accenture DIH` è `AQ2365-L3 _Si.Ter_DIMENSIONAMENTO INFRASTRUTTURA_V1.4.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1052 che coinvolge il nodo canonico `Componenti Accenture DIH` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1052 che coinvolge il nodo canonico `Componenti Accenture DIH` è `1.0`. Nel grafo unificato, l'arco 1052 registra come `source` l'identificatore `graphify_kb::dimensionamento_siter` per la relazione che coinvolge il nodo canonico `Componenti Accenture DIH`. Nel grafo unificato, l'arco 1052 registra come `target` l'identificatore `graphify_kb::dimensionamento_accenture_dih` per la relazione che coinvolge il nodo canonico `Componenti Accenture DIH`.

### Componenti Almaviva ePersonam

<!-- GRAPH_NODE graph=documentale index=23 id=dimensionamento_almaviva_epersonam -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Componenti Almaviva ePersonam` è `Componenti Almaviva ePersonam`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Componenti Almaviva ePersonam` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Componenti Almaviva ePersonam` è `AQ2365-L3 _Si.Ter_DIMENSIONAMENTO INFRASTRUTTURA_V1.4.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Componenti Almaviva ePersonam` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Componenti Almaviva ePersonam` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Componenti Almaviva ePersonam` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Componenti Almaviva ePersonam` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Componenti Almaviva ePersonam` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Componenti Almaviva ePersonam` possiede l'identificatore esatto `dimensionamento_almaviva_epersonam`. Nel grafo documentale, Graphify assegna il nodo canonico `Componenti Almaviva ePersonam` alla comunità numerica `10`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Componenti Almaviva ePersonam` è `componenti almaviva epersonam`.
Nel grafo documentale, il nodo canonico `Componenti Almaviva ePersonam` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=documentale index=25 role=incoming -->
Nel grafo documentale, il nodo canonico `Componenti Almaviva ePersonam` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `SI.Ter Dimensionamento Computazionale`; l'orientamento registrato nel JSON va dall'ID `dimensionamento_siter` all'ID `dimensionamento_almaviva_epersonam`. La relazione tecnica esplicita è la seguente: `SI.Ter Dimensionamento Computazionale` fa riferimento a `Componenti Almaviva ePersonam`. Nel grafo documentale, il tipo dichiarato dell'arco 25 che coinvolge il nodo canonico `Componenti Almaviva ePersonam` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 25 che coinvolge il nodo canonico `Componenti Almaviva ePersonam` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 25 che coinvolge il nodo canonico `Componenti Almaviva ePersonam` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 25 che coinvolge il nodo canonico `Componenti Almaviva ePersonam` è `AQ2365-L3 _Si.Ter_DIMENSIONAMENTO INFRASTRUTTURA_V1.4.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 25 che coinvolge il nodo canonico `Componenti Almaviva ePersonam` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 25 che coinvolge il nodo canonico `Componenti Almaviva ePersonam` è `1.0`. Nel grafo documentale, l'arco 25 registra come `source` l'identificatore `dimensionamento_siter` per la relazione che coinvolge il nodo canonico `Componenti Almaviva ePersonam`. Nel grafo documentale, l'arco 25 registra come `target` l'identificatore `dimensionamento_almaviva_epersonam` per la relazione che coinvolge il nodo canonico `Componenti Almaviva ePersonam`.

<!-- GRAPH_NODE graph=unificata index=556 id=graphify_kb::dimensionamento_almaviva_epersonam -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Componenti Almaviva ePersonam` è `Componenti Almaviva ePersonam`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Componenti Almaviva ePersonam` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Componenti Almaviva ePersonam` è `AQ2365-L3 _Si.Ter_DIMENSIONAMENTO INFRASTRUTTURA_V1.4.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Componenti Almaviva ePersonam` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Componenti Almaviva ePersonam` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Componenti Almaviva ePersonam` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Componenti Almaviva ePersonam` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Componenti Almaviva ePersonam` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Componenti Almaviva ePersonam` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Componenti Almaviva ePersonam` è `componenti almaviva epersonam`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Componenti Almaviva ePersonam` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Componenti Almaviva ePersonam` è `dimensionamento_almaviva_epersonam`. Nel grafo unificato, il nodo canonico `Componenti Almaviva ePersonam` possiede l'identificatore esatto `graphify_kb::dimensionamento_almaviva_epersonam`.
Nel grafo unificato, il nodo canonico `Componenti Almaviva ePersonam` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1053 role=incoming -->
Nel grafo unificato, il nodo canonico `Componenti Almaviva ePersonam` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `SI.Ter Dimensionamento Computazionale`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::dimensionamento_siter` all'ID `graphify_kb::dimensionamento_almaviva_epersonam`. La relazione tecnica esplicita è la seguente: `SI.Ter Dimensionamento Computazionale` fa riferimento a `Componenti Almaviva ePersonam`. Nel grafo unificato, il tipo dichiarato dell'arco 1053 che coinvolge il nodo canonico `Componenti Almaviva ePersonam` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1053 che coinvolge il nodo canonico `Componenti Almaviva ePersonam` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1053 che coinvolge il nodo canonico `Componenti Almaviva ePersonam` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1053 che coinvolge il nodo canonico `Componenti Almaviva ePersonam` è `AQ2365-L3 _Si.Ter_DIMENSIONAMENTO INFRASTRUTTURA_V1.4.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1053 che coinvolge il nodo canonico `Componenti Almaviva ePersonam` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1053 che coinvolge il nodo canonico `Componenti Almaviva ePersonam` è `1.0`. Nel grafo unificato, l'arco 1053 registra come `source` l'identificatore `graphify_kb::dimensionamento_siter` per la relazione che coinvolge il nodo canonico `Componenti Almaviva ePersonam`. Nel grafo unificato, l'arco 1053 registra come `target` l'identificatore `graphify_kb::dimensionamento_almaviva_epersonam` per la relazione che coinvolge il nodo canonico `Componenti Almaviva ePersonam`.

### Componenti GPI SisTer

<!-- GRAPH_NODE graph=documentale index=21 id=dimensionamento_gpi_sister -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Componenti GPI SisTer` è `Componenti GPI SisTer`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Componenti GPI SisTer` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Componenti GPI SisTer` è `AQ2365-L3 _Si.Ter_DIMENSIONAMENTO INFRASTRUTTURA_V1.4.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Componenti GPI SisTer` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Componenti GPI SisTer` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Componenti GPI SisTer` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Componenti GPI SisTer` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Componenti GPI SisTer` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Componenti GPI SisTer` possiede l'identificatore esatto `dimensionamento_gpi_sister`. Nel grafo documentale, Graphify assegna il nodo canonico `Componenti GPI SisTer` alla comunità numerica `10`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Componenti GPI SisTer` è `componenti gpi sister`.
Nel grafo documentale, il nodo canonico `Componenti GPI SisTer` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=documentale index=26 role=incoming -->
Nel grafo documentale, il nodo canonico `Componenti GPI SisTer` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `SI.Ter Dimensionamento Computazionale`; l'orientamento registrato nel JSON va dall'ID `dimensionamento_siter` all'ID `dimensionamento_gpi_sister`. La relazione tecnica esplicita è la seguente: `SI.Ter Dimensionamento Computazionale` fa riferimento a `Componenti GPI SisTer`. Nel grafo documentale, il tipo dichiarato dell'arco 26 che coinvolge il nodo canonico `Componenti GPI SisTer` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 26 che coinvolge il nodo canonico `Componenti GPI SisTer` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 26 che coinvolge il nodo canonico `Componenti GPI SisTer` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 26 che coinvolge il nodo canonico `Componenti GPI SisTer` è `AQ2365-L3 _Si.Ter_DIMENSIONAMENTO INFRASTRUTTURA_V1.4.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 26 che coinvolge il nodo canonico `Componenti GPI SisTer` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 26 che coinvolge il nodo canonico `Componenti GPI SisTer` è `1.0`. Nel grafo documentale, l'arco 26 registra come `source` l'identificatore `dimensionamento_siter` per la relazione che coinvolge il nodo canonico `Componenti GPI SisTer`. Nel grafo documentale, l'arco 26 registra come `target` l'identificatore `dimensionamento_gpi_sister` per la relazione che coinvolge il nodo canonico `Componenti GPI SisTer`.

<!-- GRAPH_NODE graph=unificata index=554 id=graphify_kb::dimensionamento_gpi_sister -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Componenti GPI SisTer` è `Componenti GPI SisTer`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Componenti GPI SisTer` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Componenti GPI SisTer` è `AQ2365-L3 _Si.Ter_DIMENSIONAMENTO INFRASTRUTTURA_V1.4.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Componenti GPI SisTer` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Componenti GPI SisTer` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Componenti GPI SisTer` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Componenti GPI SisTer` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Componenti GPI SisTer` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Componenti GPI SisTer` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Componenti GPI SisTer` è `componenti gpi sister`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Componenti GPI SisTer` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Componenti GPI SisTer` è `dimensionamento_gpi_sister`. Nel grafo unificato, il nodo canonico `Componenti GPI SisTer` possiede l'identificatore esatto `graphify_kb::dimensionamento_gpi_sister`.
Nel grafo unificato, il nodo canonico `Componenti GPI SisTer` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1054 role=incoming -->
Nel grafo unificato, il nodo canonico `Componenti GPI SisTer` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `SI.Ter Dimensionamento Computazionale`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::dimensionamento_siter` all'ID `graphify_kb::dimensionamento_gpi_sister`. La relazione tecnica esplicita è la seguente: `SI.Ter Dimensionamento Computazionale` fa riferimento a `Componenti GPI SisTer`. Nel grafo unificato, il tipo dichiarato dell'arco 1054 che coinvolge il nodo canonico `Componenti GPI SisTer` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1054 che coinvolge il nodo canonico `Componenti GPI SisTer` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1054 che coinvolge il nodo canonico `Componenti GPI SisTer` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1054 che coinvolge il nodo canonico `Componenti GPI SisTer` è `AQ2365-L3 _Si.Ter_DIMENSIONAMENTO INFRASTRUTTURA_V1.4.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1054 che coinvolge il nodo canonico `Componenti GPI SisTer` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1054 che coinvolge il nodo canonico `Componenti GPI SisTer` è `1.0`. Nel grafo unificato, l'arco 1054 registra come `source` l'identificatore `graphify_kb::dimensionamento_siter` per la relazione che coinvolge il nodo canonico `Componenti GPI SisTer`. Nel grafo unificato, l'arco 1054 registra come `target` l'identificatore `graphify_kb::dimensionamento_gpi_sister` per la relazione che coinvolge il nodo canonico `Componenti GPI SisTer`.

### SI.Ter Dimensionamento Computazionale

<!-- GRAPH_NODE graph=documentale index=20 id=dimensionamento_siter -->
Nel grafo documentale, l'etichetta originale del nodo canonico `SI.Ter Dimensionamento Computazionale` è `SI.Ter Dimensionamento Computazionale`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `SI.Ter Dimensionamento Computazionale` come `document`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `SI.Ter Dimensionamento Computazionale` è `AQ2365-L3 _Si.Ter_DIMENSIONAMENTO INFRASTRUTTURA_V1.4.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `SI.Ter Dimensionamento Computazionale` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `SI.Ter Dimensionamento Computazionale` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `SI.Ter Dimensionamento Computazionale` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `SI.Ter Dimensionamento Computazionale` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `SI.Ter Dimensionamento Computazionale` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `SI.Ter Dimensionamento Computazionale` possiede l'identificatore esatto `dimensionamento_siter`. Nel grafo documentale, Graphify assegna il nodo canonico `SI.Ter Dimensionamento Computazionale` alla comunità numerica `10`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `SI.Ter Dimensionamento Computazionale` è `si.ter dimensionamento computazionale`.
<!-- GRAPH_EDGE graph=documentale index=24 role=outgoing -->
Nel grafo documentale, il nodo canonico `SI.Ter Dimensionamento Computazionale` è la sorgente `source` della relazione `references` verso il nodo canonico `Componenti Accenture DIH`; l'orientamento registrato nel JSON va dall'ID `dimensionamento_siter` all'ID `dimensionamento_accenture_dih`. La relazione tecnica esplicita è la seguente: `SI.Ter Dimensionamento Computazionale` fa riferimento a `Componenti Accenture DIH`. Nel grafo documentale, il tipo dichiarato dell'arco 24 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 24 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 24 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 24 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `AQ2365-L3 _Si.Ter_DIMENSIONAMENTO INFRASTRUTTURA_V1.4.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 24 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 24 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `1.0`. Nel grafo documentale, l'arco 24 registra come `source` l'identificatore `dimensionamento_siter` per la relazione che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale`. Nel grafo documentale, l'arco 24 registra come `target` l'identificatore `dimensionamento_accenture_dih` per la relazione che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale`.
<!-- GRAPH_EDGE graph=documentale index=25 role=outgoing -->
Nel grafo documentale, il nodo canonico `SI.Ter Dimensionamento Computazionale` è la sorgente `source` della relazione `references` verso il nodo canonico `Componenti Almaviva ePersonam`; l'orientamento registrato nel JSON va dall'ID `dimensionamento_siter` all'ID `dimensionamento_almaviva_epersonam`. La relazione tecnica esplicita è la seguente: `SI.Ter Dimensionamento Computazionale` fa riferimento a `Componenti Almaviva ePersonam`. Nel grafo documentale, il tipo dichiarato dell'arco 25 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 25 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 25 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 25 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `AQ2365-L3 _Si.Ter_DIMENSIONAMENTO INFRASTRUTTURA_V1.4.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 25 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 25 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `1.0`. Nel grafo documentale, l'arco 25 registra come `source` l'identificatore `dimensionamento_siter` per la relazione che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale`. Nel grafo documentale, l'arco 25 registra come `target` l'identificatore `dimensionamento_almaviva_epersonam` per la relazione che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale`.
<!-- GRAPH_EDGE graph=documentale index=26 role=outgoing -->
Nel grafo documentale, il nodo canonico `SI.Ter Dimensionamento Computazionale` è la sorgente `source` della relazione `references` verso il nodo canonico `Componenti GPI SisTer`; l'orientamento registrato nel JSON va dall'ID `dimensionamento_siter` all'ID `dimensionamento_gpi_sister`. La relazione tecnica esplicita è la seguente: `SI.Ter Dimensionamento Computazionale` fa riferimento a `Componenti GPI SisTer`. Nel grafo documentale, il tipo dichiarato dell'arco 26 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 26 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 26 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 26 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `AQ2365-L3 _Si.Ter_DIMENSIONAMENTO INFRASTRUTTURA_V1.4.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 26 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 26 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `1.0`. Nel grafo documentale, l'arco 26 registra come `source` l'identificatore `dimensionamento_siter` per la relazione che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale`. Nel grafo documentale, l'arco 26 registra come `target` l'identificatore `dimensionamento_gpi_sister` per la relazione che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale`.
Nel grafo documentale, il nodo canonico `SI.Ter Dimensionamento Computazionale` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=553 id=graphify_kb::dimensionamento_siter -->
Nel grafo unificato, l'etichetta originale del nodo canonico `SI.Ter Dimensionamento Computazionale` è `SI.Ter Dimensionamento Computazionale`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `SI.Ter Dimensionamento Computazionale` come `document`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `SI.Ter Dimensionamento Computazionale` è `AQ2365-L3 _Si.Ter_DIMENSIONAMENTO INFRASTRUTTURA_V1.4.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `SI.Ter Dimensionamento Computazionale` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `SI.Ter Dimensionamento Computazionale` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `SI.Ter Dimensionamento Computazionale` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `SI.Ter Dimensionamento Computazionale` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `SI.Ter Dimensionamento Computazionale` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `SI.Ter Dimensionamento Computazionale` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `SI.Ter Dimensionamento Computazionale` è `si.ter dimensionamento computazionale`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `SI.Ter Dimensionamento Computazionale` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `SI.Ter Dimensionamento Computazionale` è `dimensionamento_siter`. Nel grafo unificato, il nodo canonico `SI.Ter Dimensionamento Computazionale` possiede l'identificatore esatto `graphify_kb::dimensionamento_siter`.
<!-- GRAPH_EDGE graph=unificata index=1052 role=outgoing -->
Nel grafo unificato, il nodo canonico `SI.Ter Dimensionamento Computazionale` è la sorgente `source` della relazione `references` verso il nodo canonico `Componenti Accenture DIH`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::dimensionamento_siter` all'ID `graphify_kb::dimensionamento_accenture_dih`. La relazione tecnica esplicita è la seguente: `SI.Ter Dimensionamento Computazionale` fa riferimento a `Componenti Accenture DIH`. Nel grafo unificato, il tipo dichiarato dell'arco 1052 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1052 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1052 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1052 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `AQ2365-L3 _Si.Ter_DIMENSIONAMENTO INFRASTRUTTURA_V1.4.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1052 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1052 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `1.0`. Nel grafo unificato, l'arco 1052 registra come `source` l'identificatore `graphify_kb::dimensionamento_siter` per la relazione che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale`. Nel grafo unificato, l'arco 1052 registra come `target` l'identificatore `graphify_kb::dimensionamento_accenture_dih` per la relazione che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale`.
<!-- GRAPH_EDGE graph=unificata index=1053 role=outgoing -->
Nel grafo unificato, il nodo canonico `SI.Ter Dimensionamento Computazionale` è la sorgente `source` della relazione `references` verso il nodo canonico `Componenti Almaviva ePersonam`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::dimensionamento_siter` all'ID `graphify_kb::dimensionamento_almaviva_epersonam`. La relazione tecnica esplicita è la seguente: `SI.Ter Dimensionamento Computazionale` fa riferimento a `Componenti Almaviva ePersonam`. Nel grafo unificato, il tipo dichiarato dell'arco 1053 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1053 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1053 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1053 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `AQ2365-L3 _Si.Ter_DIMENSIONAMENTO INFRASTRUTTURA_V1.4.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1053 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1053 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `1.0`. Nel grafo unificato, l'arco 1053 registra come `source` l'identificatore `graphify_kb::dimensionamento_siter` per la relazione che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale`. Nel grafo unificato, l'arco 1053 registra come `target` l'identificatore `graphify_kb::dimensionamento_almaviva_epersonam` per la relazione che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale`.
<!-- GRAPH_EDGE graph=unificata index=1054 role=outgoing -->
Nel grafo unificato, il nodo canonico `SI.Ter Dimensionamento Computazionale` è la sorgente `source` della relazione `references` verso il nodo canonico `Componenti GPI SisTer`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::dimensionamento_siter` all'ID `graphify_kb::dimensionamento_gpi_sister`. La relazione tecnica esplicita è la seguente: `SI.Ter Dimensionamento Computazionale` fa riferimento a `Componenti GPI SisTer`. Nel grafo unificato, il tipo dichiarato dell'arco 1054 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1054 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1054 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1054 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `AQ2365-L3 _Si.Ter_DIMENSIONAMENTO INFRASTRUTTURA_V1.4.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1054 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1054 che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale` è `1.0`. Nel grafo unificato, l'arco 1054 registra come `source` l'identificatore `graphify_kb::dimensionamento_siter` per la relazione che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale`. Nel grafo unificato, l'arco 1054 registra come `target` l'identificatore `graphify_kb::dimensionamento_gpi_sister` per la relazione che coinvolge il nodo canonico `SI.Ter Dimensionamento Computazionale`.
Nel grafo unificato, il nodo canonico `SI.Ter Dimensionamento Computazionale` non possiede archi entranti registrati nel campo `links`.

## Cluster Graphify 11 — Protected Discharge Flow

Il cluster editoriale Graphify 11 del dominio Interoperabilità tecnico-normativa contiene 4 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### Camunda BPMN Rules

<!-- GRAPH_NODE graph=documentale index=15 id=pianificazione_camunda_bpmn_rules -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Camunda BPMN Rules` è `Camunda BPMN Rules`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Camunda BPMN Rules` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Camunda BPMN Rules` è `pianificazione.txt`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Camunda BPMN Rules` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Camunda BPMN Rules` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Camunda BPMN Rules` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Camunda BPMN Rules` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Camunda BPMN Rules` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Camunda BPMN Rules` possiede l'identificatore esatto `pianificazione_camunda_bpmn_rules`. Nel grafo documentale, Graphify assegna il nodo canonico `Camunda BPMN Rules` alla comunità numerica `11`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Camunda BPMN Rules` è `camunda bpmn rules`.
Nel grafo documentale, il nodo canonico `Camunda BPMN Rules` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=documentale index=17 role=incoming -->
Nel grafo documentale, il nodo canonico `Camunda BPMN Rules` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Dimissione Protetta End To End`; l'orientamento registrato nel JSON va dall'ID `pianificazione_dimissione_protetta_e2e` all'ID `pianificazione_camunda_bpmn_rules`. La relazione tecnica esplicita è la seguente: `Dimissione Protetta End To End` fa riferimento a `Camunda BPMN Rules`. Nel grafo documentale, il tipo dichiarato dell'arco 17 che coinvolge il nodo canonico `Camunda BPMN Rules` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 17 che coinvolge il nodo canonico `Camunda BPMN Rules` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 17 che coinvolge il nodo canonico `Camunda BPMN Rules` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 17 che coinvolge il nodo canonico `Camunda BPMN Rules` è `pianificazione.txt`. Nel grafo documentale, la posizione sorgente dell'arco 17 che coinvolge il nodo canonico `Camunda BPMN Rules` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 17 che coinvolge il nodo canonico `Camunda BPMN Rules` è `1.0`. Nel grafo documentale, l'arco 17 registra come `source` l'identificatore `pianificazione_dimissione_protetta_e2e` per la relazione che coinvolge il nodo canonico `Camunda BPMN Rules`. Nel grafo documentale, l'arco 17 registra come `target` l'identificatore `pianificazione_camunda_bpmn_rules` per la relazione che coinvolge il nodo canonico `Camunda BPMN Rules`.

<!-- GRAPH_NODE graph=unificata index=548 id=graphify_kb::pianificazione_camunda_bpmn_rules -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Camunda BPMN Rules` è `Camunda BPMN Rules`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Camunda BPMN Rules` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Camunda BPMN Rules` è `pianificazione.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Camunda BPMN Rules` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Camunda BPMN Rules` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Camunda BPMN Rules` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Camunda BPMN Rules` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Camunda BPMN Rules` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Camunda BPMN Rules` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Camunda BPMN Rules` è `camunda bpmn rules`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Camunda BPMN Rules` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Camunda BPMN Rules` è `pianificazione_camunda_bpmn_rules`. Nel grafo unificato, il nodo canonico `Camunda BPMN Rules` possiede l'identificatore esatto `graphify_kb::pianificazione_camunda_bpmn_rules`.
Nel grafo unificato, il nodo canonico `Camunda BPMN Rules` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1045 role=incoming -->
Nel grafo unificato, il nodo canonico `Camunda BPMN Rules` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Dimissione Protetta End To End`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::pianificazione_dimissione_protetta_e2e` all'ID `graphify_kb::pianificazione_camunda_bpmn_rules`. La relazione tecnica esplicita è la seguente: `Dimissione Protetta End To End` fa riferimento a `Camunda BPMN Rules`. Nel grafo unificato, il tipo dichiarato dell'arco 1045 che coinvolge il nodo canonico `Camunda BPMN Rules` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1045 che coinvolge il nodo canonico `Camunda BPMN Rules` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1045 che coinvolge il nodo canonico `Camunda BPMN Rules` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1045 che coinvolge il nodo canonico `Camunda BPMN Rules` è `pianificazione.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1045 che coinvolge il nodo canonico `Camunda BPMN Rules` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1045 che coinvolge il nodo canonico `Camunda BPMN Rules` è `1.0`. Nel grafo unificato, l'arco 1045 registra come `source` l'identificatore `graphify_kb::pianificazione_dimissione_protetta_e2e` per la relazione che coinvolge il nodo canonico `Camunda BPMN Rules`. Nel grafo unificato, l'arco 1045 registra come `target` l'identificatore `graphify_kb::pianificazione_camunda_bpmn_rules` per la relazione che coinvolge il nodo canonico `Camunda BPMN Rules`.

### Dimissione Protetta End To End

<!-- GRAPH_NODE graph=documentale index=12 id=pianificazione_dimissione_protetta_e2e -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Dimissione Protetta End To End` è `Dimissione Protetta End To End`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Dimissione Protetta End To End` come `rationale`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Dimissione Protetta End To End` è `pianificazione.txt`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Dimissione Protetta End To End` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Dimissione Protetta End To End` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Dimissione Protetta End To End` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Dimissione Protetta End To End` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Dimissione Protetta End To End` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Dimissione Protetta End To End` possiede l'identificatore esatto `pianificazione_dimissione_protetta_e2e`. Nel grafo documentale, Graphify assegna il nodo canonico `Dimissione Protetta End To End` alla comunità numerica `11`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Dimissione Protetta End To End` è `dimissione protetta end to end`.
<!-- GRAPH_EDGE graph=documentale index=17 role=outgoing -->
Nel grafo documentale, il nodo canonico `Dimissione Protetta End To End` è la sorgente `source` della relazione `references` verso il nodo canonico `Camunda BPMN Rules`; l'orientamento registrato nel JSON va dall'ID `pianificazione_dimissione_protetta_e2e` all'ID `pianificazione_camunda_bpmn_rules`. La relazione tecnica esplicita è la seguente: `Dimissione Protetta End To End` fa riferimento a `Camunda BPMN Rules`. Nel grafo documentale, il tipo dichiarato dell'arco 17 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 17 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 17 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 17 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `pianificazione.txt`. Nel grafo documentale, la posizione sorgente dell'arco 17 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 17 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `1.0`. Nel grafo documentale, l'arco 17 registra come `source` l'identificatore `pianificazione_dimissione_protetta_e2e` per la relazione che coinvolge il nodo canonico `Dimissione Protetta End To End`. Nel grafo documentale, l'arco 17 registra come `target` l'identificatore `pianificazione_camunda_bpmn_rules` per la relazione che coinvolge il nodo canonico `Dimissione Protetta End To End`.
<!-- GRAPH_EDGE graph=documentale index=18 role=outgoing -->
Nel grafo documentale, il nodo canonico `Dimissione Protetta End To End` è la sorgente `source` della relazione `references` verso il nodo canonico `FSEr XDS Layer`; l'orientamento registrato nel JSON va dall'ID `pianificazione_dimissione_protetta_e2e` all'ID `pianificazione_fser_xds_layer`. La relazione tecnica esplicita è la seguente: `Dimissione Protetta End To End` fa riferimento a `FSEr XDS Layer`. Nel grafo documentale, il tipo dichiarato dell'arco 18 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 18 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 18 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 18 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `pianificazione.txt`. Nel grafo documentale, la posizione sorgente dell'arco 18 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 18 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `1.0`. Nel grafo documentale, l'arco 18 registra come `source` l'identificatore `pianificazione_dimissione_protetta_e2e` per la relazione che coinvolge il nodo canonico `Dimissione Protetta End To End`. Nel grafo documentale, l'arco 18 registra come `target` l'identificatore `pianificazione_fser_xds_layer` per la relazione che coinvolge il nodo canonico `Dimissione Protetta End To End`.
<!-- GRAPH_EDGE graph=documentale index=19 role=outgoing -->
Nel grafo documentale, il nodo canonico `Dimissione Protetta End To End` è la sorgente `source` della relazione `references` verso il nodo canonico `RabbitMQ Queues`; l'orientamento registrato nel JSON va dall'ID `pianificazione_dimissione_protetta_e2e` all'ID `pianificazione_rabbitmq_queues`. La relazione tecnica esplicita è la seguente: `Dimissione Protetta End To End` fa riferimento a `RabbitMQ Queues`. Nel grafo documentale, il tipo dichiarato dell'arco 19 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 19 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 19 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 19 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `pianificazione.txt`. Nel grafo documentale, la posizione sorgente dell'arco 19 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 19 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `1.0`. Nel grafo documentale, l'arco 19 registra come `source` l'identificatore `pianificazione_dimissione_protetta_e2e` per la relazione che coinvolge il nodo canonico `Dimissione Protetta End To End`. Nel grafo documentale, l'arco 19 registra come `target` l'identificatore `pianificazione_rabbitmq_queues` per la relazione che coinvolge il nodo canonico `Dimissione Protetta End To End`.
Nel grafo documentale, il nodo canonico `Dimissione Protetta End To End` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=545 id=graphify_kb::pianificazione_dimissione_protetta_e2e -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Dimissione Protetta End To End` è `Dimissione Protetta End To End`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Dimissione Protetta End To End` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Dimissione Protetta End To End` è `pianificazione.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Dimissione Protetta End To End` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Dimissione Protetta End To End` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Dimissione Protetta End To End` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Dimissione Protetta End To End` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Dimissione Protetta End To End` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Dimissione Protetta End To End` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Dimissione Protetta End To End` è `dimissione protetta end to end`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Dimissione Protetta End To End` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Dimissione Protetta End To End` è `pianificazione_dimissione_protetta_e2e`. Nel grafo unificato, il nodo canonico `Dimissione Protetta End To End` possiede l'identificatore esatto `graphify_kb::pianificazione_dimissione_protetta_e2e`.
<!-- GRAPH_EDGE graph=unificata index=1045 role=outgoing -->
Nel grafo unificato, il nodo canonico `Dimissione Protetta End To End` è la sorgente `source` della relazione `references` verso il nodo canonico `Camunda BPMN Rules`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::pianificazione_dimissione_protetta_e2e` all'ID `graphify_kb::pianificazione_camunda_bpmn_rules`. La relazione tecnica esplicita è la seguente: `Dimissione Protetta End To End` fa riferimento a `Camunda BPMN Rules`. Nel grafo unificato, il tipo dichiarato dell'arco 1045 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1045 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1045 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1045 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `pianificazione.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1045 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1045 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `1.0`. Nel grafo unificato, l'arco 1045 registra come `source` l'identificatore `graphify_kb::pianificazione_dimissione_protetta_e2e` per la relazione che coinvolge il nodo canonico `Dimissione Protetta End To End`. Nel grafo unificato, l'arco 1045 registra come `target` l'identificatore `graphify_kb::pianificazione_camunda_bpmn_rules` per la relazione che coinvolge il nodo canonico `Dimissione Protetta End To End`.
<!-- GRAPH_EDGE graph=unificata index=1046 role=outgoing -->
Nel grafo unificato, il nodo canonico `Dimissione Protetta End To End` è la sorgente `source` della relazione `references` verso il nodo canonico `FSEr XDS Layer`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::pianificazione_dimissione_protetta_e2e` all'ID `graphify_kb::pianificazione_fser_xds_layer`. La relazione tecnica esplicita è la seguente: `Dimissione Protetta End To End` fa riferimento a `FSEr XDS Layer`. Nel grafo unificato, il tipo dichiarato dell'arco 1046 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1046 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1046 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1046 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `pianificazione.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1046 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1046 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `1.0`. Nel grafo unificato, l'arco 1046 registra come `source` l'identificatore `graphify_kb::pianificazione_dimissione_protetta_e2e` per la relazione che coinvolge il nodo canonico `Dimissione Protetta End To End`. Nel grafo unificato, l'arco 1046 registra come `target` l'identificatore `graphify_kb::pianificazione_fser_xds_layer` per la relazione che coinvolge il nodo canonico `Dimissione Protetta End To End`.
<!-- GRAPH_EDGE graph=unificata index=1047 role=outgoing -->
Nel grafo unificato, il nodo canonico `Dimissione Protetta End To End` è la sorgente `source` della relazione `references` verso il nodo canonico `RabbitMQ Queues`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::pianificazione_dimissione_protetta_e2e` all'ID `graphify_kb::pianificazione_rabbitmq_queues`. La relazione tecnica esplicita è la seguente: `Dimissione Protetta End To End` fa riferimento a `RabbitMQ Queues`. Nel grafo unificato, il tipo dichiarato dell'arco 1047 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1047 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1047 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1047 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `pianificazione.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1047 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1047 che coinvolge il nodo canonico `Dimissione Protetta End To End` è `1.0`. Nel grafo unificato, l'arco 1047 registra come `source` l'identificatore `graphify_kb::pianificazione_dimissione_protetta_e2e` per la relazione che coinvolge il nodo canonico `Dimissione Protetta End To End`. Nel grafo unificato, l'arco 1047 registra come `target` l'identificatore `graphify_kb::pianificazione_rabbitmq_queues` per la relazione che coinvolge il nodo canonico `Dimissione Protetta End To End`.
Nel grafo unificato, il nodo canonico `Dimissione Protetta End To End` non possiede archi entranti registrati nel campo `links`.

### FSEr XDS Layer

<!-- GRAPH_NODE graph=documentale index=13 id=pianificazione_fser_xds_layer -->
Nel grafo documentale, l'etichetta originale del nodo canonico `FSEr XDS Layer` è `FSEr XDS Layer`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `FSEr XDS Layer` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `FSEr XDS Layer` è `pianificazione.txt`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `FSEr XDS Layer` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `FSEr XDS Layer` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `FSEr XDS Layer` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `FSEr XDS Layer` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `FSEr XDS Layer` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `FSEr XDS Layer` possiede l'identificatore esatto `pianificazione_fser_xds_layer`. Nel grafo documentale, Graphify assegna il nodo canonico `FSEr XDS Layer` alla comunità numerica `11`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `FSEr XDS Layer` è `fser xds layer`.
Nel grafo documentale, il nodo canonico `FSEr XDS Layer` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=documentale index=18 role=incoming -->
Nel grafo documentale, il nodo canonico `FSEr XDS Layer` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Dimissione Protetta End To End`; l'orientamento registrato nel JSON va dall'ID `pianificazione_dimissione_protetta_e2e` all'ID `pianificazione_fser_xds_layer`. La relazione tecnica esplicita è la seguente: `Dimissione Protetta End To End` fa riferimento a `FSEr XDS Layer`. Nel grafo documentale, il tipo dichiarato dell'arco 18 che coinvolge il nodo canonico `FSEr XDS Layer` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 18 che coinvolge il nodo canonico `FSEr XDS Layer` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 18 che coinvolge il nodo canonico `FSEr XDS Layer` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 18 che coinvolge il nodo canonico `FSEr XDS Layer` è `pianificazione.txt`. Nel grafo documentale, la posizione sorgente dell'arco 18 che coinvolge il nodo canonico `FSEr XDS Layer` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 18 che coinvolge il nodo canonico `FSEr XDS Layer` è `1.0`. Nel grafo documentale, l'arco 18 registra come `source` l'identificatore `pianificazione_dimissione_protetta_e2e` per la relazione che coinvolge il nodo canonico `FSEr XDS Layer`. Nel grafo documentale, l'arco 18 registra come `target` l'identificatore `pianificazione_fser_xds_layer` per la relazione che coinvolge il nodo canonico `FSEr XDS Layer`.

<!-- GRAPH_NODE graph=unificata index=546 id=graphify_kb::pianificazione_fser_xds_layer -->
Nel grafo unificato, l'etichetta originale del nodo canonico `FSEr XDS Layer` è `FSEr XDS Layer`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `FSEr XDS Layer` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `FSEr XDS Layer` è `pianificazione.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `FSEr XDS Layer` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `FSEr XDS Layer` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `FSEr XDS Layer` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `FSEr XDS Layer` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `FSEr XDS Layer` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `FSEr XDS Layer` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `FSEr XDS Layer` è `fser xds layer`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `FSEr XDS Layer` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `FSEr XDS Layer` è `pianificazione_fser_xds_layer`. Nel grafo unificato, il nodo canonico `FSEr XDS Layer` possiede l'identificatore esatto `graphify_kb::pianificazione_fser_xds_layer`.
Nel grafo unificato, il nodo canonico `FSEr XDS Layer` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1046 role=incoming -->
Nel grafo unificato, il nodo canonico `FSEr XDS Layer` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Dimissione Protetta End To End`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::pianificazione_dimissione_protetta_e2e` all'ID `graphify_kb::pianificazione_fser_xds_layer`. La relazione tecnica esplicita è la seguente: `Dimissione Protetta End To End` fa riferimento a `FSEr XDS Layer`. Nel grafo unificato, il tipo dichiarato dell'arco 1046 che coinvolge il nodo canonico `FSEr XDS Layer` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1046 che coinvolge il nodo canonico `FSEr XDS Layer` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1046 che coinvolge il nodo canonico `FSEr XDS Layer` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1046 che coinvolge il nodo canonico `FSEr XDS Layer` è `pianificazione.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1046 che coinvolge il nodo canonico `FSEr XDS Layer` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1046 che coinvolge il nodo canonico `FSEr XDS Layer` è `1.0`. Nel grafo unificato, l'arco 1046 registra come `source` l'identificatore `graphify_kb::pianificazione_dimissione_protetta_e2e` per la relazione che coinvolge il nodo canonico `FSEr XDS Layer`. Nel grafo unificato, l'arco 1046 registra come `target` l'identificatore `graphify_kb::pianificazione_fser_xds_layer` per la relazione che coinvolge il nodo canonico `FSEr XDS Layer`.

### RabbitMQ Queues

<!-- GRAPH_NODE graph=documentale index=14 id=pianificazione_rabbitmq_queues -->
Nel grafo documentale, l'etichetta originale del nodo canonico `RabbitMQ Queues` è `RabbitMQ Queues`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `RabbitMQ Queues` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `RabbitMQ Queues` è `pianificazione.txt`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `RabbitMQ Queues` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `RabbitMQ Queues` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `RabbitMQ Queues` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `RabbitMQ Queues` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `RabbitMQ Queues` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `RabbitMQ Queues` possiede l'identificatore esatto `pianificazione_rabbitmq_queues`. Nel grafo documentale, Graphify assegna il nodo canonico `RabbitMQ Queues` alla comunità numerica `11`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `RabbitMQ Queues` è `rabbitmq queues`.
Nel grafo documentale, il nodo canonico `RabbitMQ Queues` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=documentale index=19 role=incoming -->
Nel grafo documentale, il nodo canonico `RabbitMQ Queues` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Dimissione Protetta End To End`; l'orientamento registrato nel JSON va dall'ID `pianificazione_dimissione_protetta_e2e` all'ID `pianificazione_rabbitmq_queues`. La relazione tecnica esplicita è la seguente: `Dimissione Protetta End To End` fa riferimento a `RabbitMQ Queues`. Nel grafo documentale, il tipo dichiarato dell'arco 19 che coinvolge il nodo canonico `RabbitMQ Queues` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 19 che coinvolge il nodo canonico `RabbitMQ Queues` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 19 che coinvolge il nodo canonico `RabbitMQ Queues` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 19 che coinvolge il nodo canonico `RabbitMQ Queues` è `pianificazione.txt`. Nel grafo documentale, la posizione sorgente dell'arco 19 che coinvolge il nodo canonico `RabbitMQ Queues` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 19 che coinvolge il nodo canonico `RabbitMQ Queues` è `1.0`. Nel grafo documentale, l'arco 19 registra come `source` l'identificatore `pianificazione_dimissione_protetta_e2e` per la relazione che coinvolge il nodo canonico `RabbitMQ Queues`. Nel grafo documentale, l'arco 19 registra come `target` l'identificatore `pianificazione_rabbitmq_queues` per la relazione che coinvolge il nodo canonico `RabbitMQ Queues`.

<!-- GRAPH_NODE graph=unificata index=547 id=graphify_kb::pianificazione_rabbitmq_queues -->
Nel grafo unificato, l'etichetta originale del nodo canonico `RabbitMQ Queues` è `RabbitMQ Queues`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `RabbitMQ Queues` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `RabbitMQ Queues` è `pianificazione.txt`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `RabbitMQ Queues` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `RabbitMQ Queues` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `RabbitMQ Queues` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `RabbitMQ Queues` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `RabbitMQ Queues` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `RabbitMQ Queues` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `RabbitMQ Queues` è `rabbitmq queues`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `RabbitMQ Queues` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `RabbitMQ Queues` è `pianificazione_rabbitmq_queues`. Nel grafo unificato, il nodo canonico `RabbitMQ Queues` possiede l'identificatore esatto `graphify_kb::pianificazione_rabbitmq_queues`.
Nel grafo unificato, il nodo canonico `RabbitMQ Queues` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1047 role=incoming -->
Nel grafo unificato, il nodo canonico `RabbitMQ Queues` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Dimissione Protetta End To End`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::pianificazione_dimissione_protetta_e2e` all'ID `graphify_kb::pianificazione_rabbitmq_queues`. La relazione tecnica esplicita è la seguente: `Dimissione Protetta End To End` fa riferimento a `RabbitMQ Queues`. Nel grafo unificato, il tipo dichiarato dell'arco 1047 che coinvolge il nodo canonico `RabbitMQ Queues` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1047 che coinvolge il nodo canonico `RabbitMQ Queues` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1047 che coinvolge il nodo canonico `RabbitMQ Queues` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1047 che coinvolge il nodo canonico `RabbitMQ Queues` è `pianificazione.txt`. Nel grafo unificato, la posizione sorgente dell'arco 1047 che coinvolge il nodo canonico `RabbitMQ Queues` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1047 che coinvolge il nodo canonico `RabbitMQ Queues` è `1.0`. Nel grafo unificato, l'arco 1047 registra come `source` l'identificatore `graphify_kb::pianificazione_dimissione_protetta_e2e` per la relazione che coinvolge il nodo canonico `RabbitMQ Queues`. Nel grafo unificato, l'arco 1047 registra come `target` l'identificatore `graphify_kb::pianificazione_rabbitmq_queues` per la relazione che coinvolge il nodo canonico `RabbitMQ Queues`.

## Cluster Graphify 12 — Network Latency Benchmarks

Il cluster editoriale Graphify 12 del dominio Interoperabilità tecnico-normativa contiene 4 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### Impact of Latency on Applications Performance

<!-- GRAPH_NODE graph=documentale index=104 id=srg_latency_performance -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Impact of Latency on Applications Performance` è `Impact of Latency on Applications Performance`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Impact of Latency on Applications Performance` come `paper`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Impact of Latency on Applications Performance` è `srg_2.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Impact of Latency on Applications Performance` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Impact of Latency on Applications Performance` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Impact of Latency on Applications Performance` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Impact of Latency on Applications Performance` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Impact of Latency on Applications Performance` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Impact of Latency on Applications Performance` possiede l'identificatore esatto `srg_latency_performance`. Nel grafo documentale, Graphify assegna il nodo canonico `Impact of Latency on Applications Performance` alla comunità numerica `12`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Impact of Latency on Applications Performance` è `impact of latency on applications performance`.
<!-- GRAPH_EDGE graph=documentale index=92 role=outgoing -->
Nel grafo documentale, il nodo canonico `Impact of Latency on Applications Performance` è la sorgente `source` della relazione `references` verso il nodo canonico `Ping-Pong Tests`; l'orientamento registrato nel JSON va dall'ID `srg_latency_performance` all'ID `srg_ping_pong_tests`. La relazione tecnica esplicita è la seguente: `Impact of Latency on Applications Performance` fa riferimento a `Ping-Pong Tests`. Nel grafo documentale, il tipo dichiarato dell'arco 92 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 92 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 92 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 92 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `srg_2.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 92 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 92 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `1.0`. Nel grafo documentale, l'arco 92 registra come `source` l'identificatore `srg_latency_performance` per la relazione che coinvolge il nodo canonico `Impact of Latency on Applications Performance`. Nel grafo documentale, l'arco 92 registra come `target` l'identificatore `srg_ping_pong_tests` per la relazione che coinvolge il nodo canonico `Impact of Latency on Applications Performance`.
<!-- GRAPH_EDGE graph=documentale index=93 role=outgoing -->
Nel grafo documentale, il nodo canonico `Impact of Latency on Applications Performance` è la sorgente `source` della relazione `references` verso il nodo canonico `Point-to-Point Latency`; l'orientamento registrato nel JSON va dall'ID `srg_latency_performance` all'ID `srg_point_to_point_latency`. La relazione tecnica esplicita è la seguente: `Impact of Latency on Applications Performance` fa riferimento a `Point-to-Point Latency`. Nel grafo documentale, il tipo dichiarato dell'arco 93 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 93 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 93 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 93 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `srg_2.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 93 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 93 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `1.0`. Nel grafo documentale, l'arco 93 registra come `source` l'identificatore `srg_latency_performance` per la relazione che coinvolge il nodo canonico `Impact of Latency on Applications Performance`. Nel grafo documentale, l'arco 93 registra come `target` l'identificatore `srg_point_to_point_latency` per la relazione che coinvolge il nodo canonico `Impact of Latency on Applications Performance`.
Nel grafo documentale, il nodo canonico `Impact of Latency on Applications Performance` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=637 id=graphify_kb::srg_latency_performance -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Impact of Latency on Applications Performance` è `Impact of Latency on Applications Performance`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Impact of Latency on Applications Performance` come `paper`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Impact of Latency on Applications Performance` è `srg_2.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Impact of Latency on Applications Performance` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Impact of Latency on Applications Performance` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Impact of Latency on Applications Performance` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Impact of Latency on Applications Performance` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Impact of Latency on Applications Performance` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Impact of Latency on Applications Performance` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Impact of Latency on Applications Performance` è `impact of latency on applications performance`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Impact of Latency on Applications Performance` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Impact of Latency on Applications Performance` è `srg_latency_performance`. Nel grafo unificato, il nodo canonico `Impact of Latency on Applications Performance` possiede l'identificatore esatto `graphify_kb::srg_latency_performance`.
<!-- GRAPH_EDGE graph=unificata index=1120 role=outgoing -->
Nel grafo unificato, il nodo canonico `Impact of Latency on Applications Performance` è la sorgente `source` della relazione `references` verso il nodo canonico `Ping-Pong Tests`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::srg_latency_performance` all'ID `graphify_kb::srg_ping_pong_tests`. La relazione tecnica esplicita è la seguente: `Impact of Latency on Applications Performance` fa riferimento a `Ping-Pong Tests`. Nel grafo unificato, il tipo dichiarato dell'arco 1120 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1120 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1120 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1120 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `srg_2.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1120 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1120 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `1.0`. Nel grafo unificato, l'arco 1120 registra come `source` l'identificatore `graphify_kb::srg_latency_performance` per la relazione che coinvolge il nodo canonico `Impact of Latency on Applications Performance`. Nel grafo unificato, l'arco 1120 registra come `target` l'identificatore `graphify_kb::srg_ping_pong_tests` per la relazione che coinvolge il nodo canonico `Impact of Latency on Applications Performance`.
<!-- GRAPH_EDGE graph=unificata index=1121 role=outgoing -->
Nel grafo unificato, il nodo canonico `Impact of Latency on Applications Performance` è la sorgente `source` della relazione `references` verso il nodo canonico `Point-to-Point Latency`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::srg_latency_performance` all'ID `graphify_kb::srg_point_to_point_latency`. La relazione tecnica esplicita è la seguente: `Impact of Latency on Applications Performance` fa riferimento a `Point-to-Point Latency`. Nel grafo unificato, il tipo dichiarato dell'arco 1121 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1121 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1121 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1121 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `srg_2.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1121 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1121 che coinvolge il nodo canonico `Impact of Latency on Applications Performance` è `1.0`. Nel grafo unificato, l'arco 1121 registra come `source` l'identificatore `graphify_kb::srg_latency_performance` per la relazione che coinvolge il nodo canonico `Impact of Latency on Applications Performance`. Nel grafo unificato, l'arco 1121 registra come `target` l'identificatore `graphify_kb::srg_point_to_point_latency` per la relazione che coinvolge il nodo canonico `Impact of Latency on Applications Performance`.
Nel grafo unificato, il nodo canonico `Impact of Latency on Applications Performance` non possiede archi entranti registrati nel campo `links`.

### MPI/Pro

<!-- GRAPH_NODE graph=documentale index=107 id=srg_mpi_pro -->
Nel grafo documentale, l'etichetta originale del nodo canonico `MPI/Pro` è `MPI/Pro`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `MPI/Pro` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `MPI/Pro` è `srg_2.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `MPI/Pro` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `MPI/Pro` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `MPI/Pro` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `MPI/Pro` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `MPI/Pro` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `MPI/Pro` possiede l'identificatore esatto `srg_mpi_pro`. Nel grafo documentale, Graphify assegna il nodo canonico `MPI/Pro` alla comunità numerica `12`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `MPI/Pro` è `mpi/pro`.
<!-- GRAPH_EDGE graph=documentale index=94 role=outgoing -->
Nel grafo documentale, il nodo canonico `MPI/Pro` è la sorgente `source` della relazione `conceptually_related_to` verso il nodo canonico `Point-to-Point Latency`; l'orientamento registrato nel JSON va dall'ID `srg_mpi_pro` all'ID `srg_point_to_point_latency`. La relazione tecnica esplicita è la seguente: `MPI/Pro` è concettualmente correlato a `Point-to-Point Latency`. Nel grafo documentale, il tipo dichiarato dell'arco 94 che coinvolge il nodo canonico `MPI/Pro` è `conceptually_related_to`. Nel grafo documentale, la confidenza categoriale dell'arco 94 che coinvolge il nodo canonico `MPI/Pro` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 94 che coinvolge il nodo canonico `MPI/Pro` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 94 che coinvolge il nodo canonico `MPI/Pro` è `srg_2.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 94 che coinvolge il nodo canonico `MPI/Pro` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 94 che coinvolge il nodo canonico `MPI/Pro` è `1.0`. Nel grafo documentale, l'arco 94 registra come `source` l'identificatore `srg_mpi_pro` per la relazione che coinvolge il nodo canonico `MPI/Pro`. Nel grafo documentale, l'arco 94 registra come `target` l'identificatore `srg_point_to_point_latency` per la relazione che coinvolge il nodo canonico `MPI/Pro`.
Nel grafo documentale, il nodo canonico `MPI/Pro` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=640 id=graphify_kb::srg_mpi_pro -->
Nel grafo unificato, l'etichetta originale del nodo canonico `MPI/Pro` è `MPI/Pro`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `MPI/Pro` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `MPI/Pro` è `srg_2.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `MPI/Pro` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `MPI/Pro` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `MPI/Pro` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `MPI/Pro` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `MPI/Pro` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `MPI/Pro` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `MPI/Pro` è `mpi/pro`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `MPI/Pro` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `MPI/Pro` è `srg_mpi_pro`. Nel grafo unificato, il nodo canonico `MPI/Pro` possiede l'identificatore esatto `graphify_kb::srg_mpi_pro`.
Nel grafo unificato, il nodo canonico `MPI/Pro` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1122 role=incoming -->
Nel grafo unificato, il nodo canonico `MPI/Pro` è il destinatario `target` della relazione `conceptually_related_to` proveniente dal nodo canonico `Point-to-Point Latency`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::srg_point_to_point_latency` all'ID `graphify_kb::srg_mpi_pro`. La relazione tecnica esplicita è la seguente: `Point-to-Point Latency` è concettualmente correlato a `MPI/Pro`. Nel grafo unificato, il tipo dichiarato dell'arco 1122 che coinvolge il nodo canonico `MPI/Pro` è `conceptually_related_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1122 che coinvolge il nodo canonico `MPI/Pro` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1122 che coinvolge il nodo canonico `MPI/Pro` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1122 che coinvolge il nodo canonico `MPI/Pro` è `srg_2.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1122 che coinvolge il nodo canonico `MPI/Pro` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1122 che coinvolge il nodo canonico `MPI/Pro` è `1.0`. Nel grafo unificato, l'arco 1122 registra come `source` l'identificatore `graphify_kb::srg_point_to_point_latency` per la relazione che coinvolge il nodo canonico `MPI/Pro`. Nel grafo unificato, l'arco 1122 registra come `target` l'identificatore `graphify_kb::srg_mpi_pro` per la relazione che coinvolge il nodo canonico `MPI/Pro`.

### Ping-Pong Tests

<!-- GRAPH_NODE graph=documentale index=106 id=srg_ping_pong_tests -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Ping-Pong Tests` è `Ping-Pong Tests`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Ping-Pong Tests` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Ping-Pong Tests` è `srg_2.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Ping-Pong Tests` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Ping-Pong Tests` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Ping-Pong Tests` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Ping-Pong Tests` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Ping-Pong Tests` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Ping-Pong Tests` possiede l'identificatore esatto `srg_ping_pong_tests`. Nel grafo documentale, Graphify assegna il nodo canonico `Ping-Pong Tests` alla comunità numerica `12`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Ping-Pong Tests` è `ping-pong tests`.
Nel grafo documentale, il nodo canonico `Ping-Pong Tests` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=documentale index=92 role=incoming -->
Nel grafo documentale, il nodo canonico `Ping-Pong Tests` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Impact of Latency on Applications Performance`; l'orientamento registrato nel JSON va dall'ID `srg_latency_performance` all'ID `srg_ping_pong_tests`. La relazione tecnica esplicita è la seguente: `Impact of Latency on Applications Performance` fa riferimento a `Ping-Pong Tests`. Nel grafo documentale, il tipo dichiarato dell'arco 92 che coinvolge il nodo canonico `Ping-Pong Tests` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 92 che coinvolge il nodo canonico `Ping-Pong Tests` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 92 che coinvolge il nodo canonico `Ping-Pong Tests` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 92 che coinvolge il nodo canonico `Ping-Pong Tests` è `srg_2.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 92 che coinvolge il nodo canonico `Ping-Pong Tests` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 92 che coinvolge il nodo canonico `Ping-Pong Tests` è `1.0`. Nel grafo documentale, l'arco 92 registra come `source` l'identificatore `srg_latency_performance` per la relazione che coinvolge il nodo canonico `Ping-Pong Tests`. Nel grafo documentale, l'arco 92 registra come `target` l'identificatore `srg_ping_pong_tests` per la relazione che coinvolge il nodo canonico `Ping-Pong Tests`.

<!-- GRAPH_NODE graph=unificata index=639 id=graphify_kb::srg_ping_pong_tests -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Ping-Pong Tests` è `Ping-Pong Tests`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Ping-Pong Tests` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Ping-Pong Tests` è `srg_2.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Ping-Pong Tests` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Ping-Pong Tests` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Ping-Pong Tests` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Ping-Pong Tests` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Ping-Pong Tests` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Ping-Pong Tests` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Ping-Pong Tests` è `ping-pong tests`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Ping-Pong Tests` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Ping-Pong Tests` è `srg_ping_pong_tests`. Nel grafo unificato, il nodo canonico `Ping-Pong Tests` possiede l'identificatore esatto `graphify_kb::srg_ping_pong_tests`.
Nel grafo unificato, il nodo canonico `Ping-Pong Tests` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1120 role=incoming -->
Nel grafo unificato, il nodo canonico `Ping-Pong Tests` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Impact of Latency on Applications Performance`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::srg_latency_performance` all'ID `graphify_kb::srg_ping_pong_tests`. La relazione tecnica esplicita è la seguente: `Impact of Latency on Applications Performance` fa riferimento a `Ping-Pong Tests`. Nel grafo unificato, il tipo dichiarato dell'arco 1120 che coinvolge il nodo canonico `Ping-Pong Tests` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1120 che coinvolge il nodo canonico `Ping-Pong Tests` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1120 che coinvolge il nodo canonico `Ping-Pong Tests` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1120 che coinvolge il nodo canonico `Ping-Pong Tests` è `srg_2.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1120 che coinvolge il nodo canonico `Ping-Pong Tests` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1120 che coinvolge il nodo canonico `Ping-Pong Tests` è `1.0`. Nel grafo unificato, l'arco 1120 registra come `source` l'identificatore `graphify_kb::srg_latency_performance` per la relazione che coinvolge il nodo canonico `Ping-Pong Tests`. Nel grafo unificato, l'arco 1120 registra come `target` l'identificatore `graphify_kb::srg_ping_pong_tests` per la relazione che coinvolge il nodo canonico `Ping-Pong Tests`.

### Point-to-Point Latency

<!-- GRAPH_NODE graph=documentale index=105 id=srg_point_to_point_latency -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Point-to-Point Latency` è `Point-to-Point Latency`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Point-to-Point Latency` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Point-to-Point Latency` è `srg_2.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Point-to-Point Latency` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Point-to-Point Latency` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Point-to-Point Latency` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Point-to-Point Latency` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Point-to-Point Latency` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Point-to-Point Latency` possiede l'identificatore esatto `srg_point_to_point_latency`. Nel grafo documentale, Graphify assegna il nodo canonico `Point-to-Point Latency` alla comunità numerica `12`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Point-to-Point Latency` è `point-to-point latency`.
Nel grafo documentale, il nodo canonico `Point-to-Point Latency` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=documentale index=93 role=incoming -->
Nel grafo documentale, il nodo canonico `Point-to-Point Latency` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Impact of Latency on Applications Performance`; l'orientamento registrato nel JSON va dall'ID `srg_latency_performance` all'ID `srg_point_to_point_latency`. La relazione tecnica esplicita è la seguente: `Impact of Latency on Applications Performance` fa riferimento a `Point-to-Point Latency`. Nel grafo documentale, il tipo dichiarato dell'arco 93 che coinvolge il nodo canonico `Point-to-Point Latency` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 93 che coinvolge il nodo canonico `Point-to-Point Latency` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 93 che coinvolge il nodo canonico `Point-to-Point Latency` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 93 che coinvolge il nodo canonico `Point-to-Point Latency` è `srg_2.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 93 che coinvolge il nodo canonico `Point-to-Point Latency` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 93 che coinvolge il nodo canonico `Point-to-Point Latency` è `1.0`. Nel grafo documentale, l'arco 93 registra come `source` l'identificatore `srg_latency_performance` per la relazione che coinvolge il nodo canonico `Point-to-Point Latency`. Nel grafo documentale, l'arco 93 registra come `target` l'identificatore `srg_point_to_point_latency` per la relazione che coinvolge il nodo canonico `Point-to-Point Latency`.
<!-- GRAPH_EDGE graph=documentale index=94 role=incoming -->
Nel grafo documentale, il nodo canonico `Point-to-Point Latency` è il destinatario `target` della relazione `conceptually_related_to` proveniente dal nodo canonico `MPI/Pro`; l'orientamento registrato nel JSON va dall'ID `srg_mpi_pro` all'ID `srg_point_to_point_latency`. La relazione tecnica esplicita è la seguente: `MPI/Pro` è concettualmente correlato a `Point-to-Point Latency`. Nel grafo documentale, il tipo dichiarato dell'arco 94 che coinvolge il nodo canonico `Point-to-Point Latency` è `conceptually_related_to`. Nel grafo documentale, la confidenza categoriale dell'arco 94 che coinvolge il nodo canonico `Point-to-Point Latency` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 94 che coinvolge il nodo canonico `Point-to-Point Latency` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 94 che coinvolge il nodo canonico `Point-to-Point Latency` è `srg_2.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 94 che coinvolge il nodo canonico `Point-to-Point Latency` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 94 che coinvolge il nodo canonico `Point-to-Point Latency` è `1.0`. Nel grafo documentale, l'arco 94 registra come `source` l'identificatore `srg_mpi_pro` per la relazione che coinvolge il nodo canonico `Point-to-Point Latency`. Nel grafo documentale, l'arco 94 registra come `target` l'identificatore `srg_point_to_point_latency` per la relazione che coinvolge il nodo canonico `Point-to-Point Latency`.

<!-- GRAPH_NODE graph=unificata index=638 id=graphify_kb::srg_point_to_point_latency -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Point-to-Point Latency` è `Point-to-Point Latency`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Point-to-Point Latency` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Point-to-Point Latency` è `srg_2.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Point-to-Point Latency` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Point-to-Point Latency` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Point-to-Point Latency` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Point-to-Point Latency` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Point-to-Point Latency` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Point-to-Point Latency` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Point-to-Point Latency` è `point-to-point latency`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Point-to-Point Latency` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Point-to-Point Latency` è `srg_point_to_point_latency`. Nel grafo unificato, il nodo canonico `Point-to-Point Latency` possiede l'identificatore esatto `graphify_kb::srg_point_to_point_latency`.
<!-- GRAPH_EDGE graph=unificata index=1122 role=outgoing -->
Nel grafo unificato, il nodo canonico `Point-to-Point Latency` è la sorgente `source` della relazione `conceptually_related_to` verso il nodo canonico `MPI/Pro`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::srg_point_to_point_latency` all'ID `graphify_kb::srg_mpi_pro`. La relazione tecnica esplicita è la seguente: `Point-to-Point Latency` è concettualmente correlato a `MPI/Pro`. Nel grafo unificato, il tipo dichiarato dell'arco 1122 che coinvolge il nodo canonico `Point-to-Point Latency` è `conceptually_related_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1122 che coinvolge il nodo canonico `Point-to-Point Latency` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1122 che coinvolge il nodo canonico `Point-to-Point Latency` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1122 che coinvolge il nodo canonico `Point-to-Point Latency` è `srg_2.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1122 che coinvolge il nodo canonico `Point-to-Point Latency` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1122 che coinvolge il nodo canonico `Point-to-Point Latency` è `1.0`. Nel grafo unificato, l'arco 1122 registra come `source` l'identificatore `graphify_kb::srg_point_to_point_latency` per la relazione che coinvolge il nodo canonico `Point-to-Point Latency`. Nel grafo unificato, l'arco 1122 registra come `target` l'identificatore `graphify_kb::srg_mpi_pro` per la relazione che coinvolge il nodo canonico `Point-to-Point Latency`.
<!-- GRAPH_EDGE graph=unificata index=1121 role=incoming -->
Nel grafo unificato, il nodo canonico `Point-to-Point Latency` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Impact of Latency on Applications Performance`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::srg_latency_performance` all'ID `graphify_kb::srg_point_to_point_latency`. La relazione tecnica esplicita è la seguente: `Impact of Latency on Applications Performance` fa riferimento a `Point-to-Point Latency`. Nel grafo unificato, il tipo dichiarato dell'arco 1121 che coinvolge il nodo canonico `Point-to-Point Latency` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1121 che coinvolge il nodo canonico `Point-to-Point Latency` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1121 che coinvolge il nodo canonico `Point-to-Point Latency` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1121 che coinvolge il nodo canonico `Point-to-Point Latency` è `srg_2.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1121 che coinvolge il nodo canonico `Point-to-Point Latency` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1121 che coinvolge il nodo canonico `Point-to-Point Latency` è `1.0`. Nel grafo unificato, l'arco 1121 registra come `source` l'identificatore `graphify_kb::srg_latency_performance` per la relazione che coinvolge il nodo canonico `Point-to-Point Latency`. Nel grafo unificato, l'arco 1121 registra come `target` l'identificatore `graphify_kb::srg_point_to_point_latency` per la relazione che coinvolge il nodo canonico `Point-to-Point Latency`.

## Cluster Graphify 13 — XValue Document Sharing

Il cluster editoriale Graphify 13 del dominio Interoperabilità tecnico-normativa contiene 4 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### Document Source

<!-- GRAPH_NODE graph=documentale index=95 id=xvalue_document_source -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Document Source` è `Document Source`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Document Source` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Document Source` è `ULSS 3 Serenissima - Specifiche tecniche di integrazione con XValue v2.10.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Document Source` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Document Source` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Document Source` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Document Source` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Document Source` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Document Source` possiede l'identificatore esatto `xvalue_document_source`. Nel grafo documentale, Graphify assegna il nodo canonico `Document Source` alla comunità numerica `13`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Document Source` è `document source`.
<!-- GRAPH_EDGE graph=documentale index=84 role=outgoing -->
Nel grafo documentale, il nodo canonico `Document Source` è la sorgente `source` della relazione `shares_data_with` verso il nodo canonico `XDS Document Repository`; l'orientamento registrato nel JSON va dall'ID `xvalue_document_source` all'ID `xvalue_xds_repository`. La relazione tecnica esplicita è la seguente: `Document Source` condivide dati con `XDS Document Repository`. Nel grafo documentale, il tipo dichiarato dell'arco 84 che coinvolge il nodo canonico `Document Source` è `shares_data_with`. Nel grafo documentale, la confidenza categoriale dell'arco 84 che coinvolge il nodo canonico `Document Source` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 84 che coinvolge il nodo canonico `Document Source` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 84 che coinvolge il nodo canonico `Document Source` è `ULSS 3 Serenissima - Specifiche tecniche di integrazione con XValue v2.10.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 84 che coinvolge il nodo canonico `Document Source` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 84 che coinvolge il nodo canonico `Document Source` è `1.0`. Nel grafo documentale, l'arco 84 registra come `source` l'identificatore `xvalue_document_source` per la relazione che coinvolge il nodo canonico `Document Source`. Nel grafo documentale, l'arco 84 registra come `target` l'identificatore `xvalue_xds_repository` per la relazione che coinvolge il nodo canonico `Document Source`.
<!-- GRAPH_EDGE graph=documentale index=83 role=incoming -->
Nel grafo documentale, il nodo canonico `Document Source` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Specifiche integrazione XValue`; l'orientamento registrato nel JSON va dall'ID `xvalue_specifiche` all'ID `xvalue_document_source`. La relazione tecnica esplicita è la seguente: `Specifiche integrazione XValue` fa riferimento a `Document Source`. Nel grafo documentale, il tipo dichiarato dell'arco 83 che coinvolge il nodo canonico `Document Source` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 83 che coinvolge il nodo canonico `Document Source` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 83 che coinvolge il nodo canonico `Document Source` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 83 che coinvolge il nodo canonico `Document Source` è `ULSS 3 Serenissima - Specifiche tecniche di integrazione con XValue v2.10.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 83 che coinvolge il nodo canonico `Document Source` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 83 che coinvolge il nodo canonico `Document Source` è `1.0`. Nel grafo documentale, l'arco 83 registra come `source` l'identificatore `xvalue_specifiche` per la relazione che coinvolge il nodo canonico `Document Source`. Nel grafo documentale, l'arco 83 registra come `target` l'identificatore `xvalue_document_source` per la relazione che coinvolge il nodo canonico `Document Source`.

<!-- GRAPH_NODE graph=unificata index=628 id=graphify_kb::xvalue_document_source -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Document Source` è `Document Source`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Document Source` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Document Source` è `ULSS 3 Serenissima - Specifiche tecniche di integrazione con XValue v2.10.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Document Source` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Document Source` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Document Source` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Document Source` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Document Source` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Document Source` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Document Source` è `document source`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Document Source` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Document Source` è `xvalue_document_source`. Nel grafo unificato, il nodo canonico `Document Source` possiede l'identificatore esatto `graphify_kb::xvalue_document_source`.
<!-- GRAPH_EDGE graph=unificata index=1112 role=outgoing -->
Nel grafo unificato, il nodo canonico `Document Source` è la sorgente `source` della relazione `shares_data_with` verso il nodo canonico `XDS Document Repository`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::xvalue_document_source` all'ID `graphify_kb::xvalue_xds_repository`. La relazione tecnica esplicita è la seguente: `Document Source` condivide dati con `XDS Document Repository`. Nel grafo unificato, il tipo dichiarato dell'arco 1112 che coinvolge il nodo canonico `Document Source` è `shares_data_with`. Nel grafo unificato, la confidenza categoriale dell'arco 1112 che coinvolge il nodo canonico `Document Source` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1112 che coinvolge il nodo canonico `Document Source` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1112 che coinvolge il nodo canonico `Document Source` è `ULSS 3 Serenissima - Specifiche tecniche di integrazione con XValue v2.10.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1112 che coinvolge il nodo canonico `Document Source` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1112 che coinvolge il nodo canonico `Document Source` è `1.0`. Nel grafo unificato, l'arco 1112 registra come `source` l'identificatore `graphify_kb::xvalue_document_source` per la relazione che coinvolge il nodo canonico `Document Source`. Nel grafo unificato, l'arco 1112 registra come `target` l'identificatore `graphify_kb::xvalue_xds_repository` per la relazione che coinvolge il nodo canonico `Document Source`.
<!-- GRAPH_EDGE graph=unificata index=1111 role=incoming -->
Nel grafo unificato, il nodo canonico `Document Source` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Specifiche integrazione XValue`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::xvalue_specifiche` all'ID `graphify_kb::xvalue_document_source`. La relazione tecnica esplicita è la seguente: `Specifiche integrazione XValue` fa riferimento a `Document Source`. Nel grafo unificato, il tipo dichiarato dell'arco 1111 che coinvolge il nodo canonico `Document Source` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1111 che coinvolge il nodo canonico `Document Source` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1111 che coinvolge il nodo canonico `Document Source` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1111 che coinvolge il nodo canonico `Document Source` è `ULSS 3 Serenissima - Specifiche tecniche di integrazione con XValue v2.10.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1111 che coinvolge il nodo canonico `Document Source` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1111 che coinvolge il nodo canonico `Document Source` è `1.0`. Nel grafo unificato, l'arco 1111 registra come `source` l'identificatore `graphify_kb::xvalue_specifiche` per la relazione che coinvolge il nodo canonico `Document Source`. Nel grafo unificato, l'arco 1111 registra come `target` l'identificatore `graphify_kb::xvalue_document_source` per la relazione che coinvolge il nodo canonico `Document Source`.

### Specifiche integrazione XValue

<!-- GRAPH_NODE graph=documentale index=94 id=xvalue_specifiche -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Specifiche integrazione XValue` è `Specifiche integrazione XValue`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Specifiche integrazione XValue` come `document`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Specifiche integrazione XValue` è `ULSS 3 Serenissima - Specifiche tecniche di integrazione con XValue v2.10.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Specifiche integrazione XValue` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Specifiche integrazione XValue` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Specifiche integrazione XValue` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Specifiche integrazione XValue` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Specifiche integrazione XValue` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Specifiche integrazione XValue` possiede l'identificatore esatto `xvalue_specifiche`. Nel grafo documentale, Graphify assegna il nodo canonico `Specifiche integrazione XValue` alla comunità numerica `13`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Specifiche integrazione XValue` è `specifiche integrazione xvalue`.
<!-- GRAPH_EDGE graph=documentale index=83 role=outgoing -->
Nel grafo documentale, il nodo canonico `Specifiche integrazione XValue` è la sorgente `source` della relazione `references` verso il nodo canonico `Document Source`; l'orientamento registrato nel JSON va dall'ID `xvalue_specifiche` all'ID `xvalue_document_source`. La relazione tecnica esplicita è la seguente: `Specifiche integrazione XValue` fa riferimento a `Document Source`. Nel grafo documentale, il tipo dichiarato dell'arco 83 che coinvolge il nodo canonico `Specifiche integrazione XValue` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 83 che coinvolge il nodo canonico `Specifiche integrazione XValue` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 83 che coinvolge il nodo canonico `Specifiche integrazione XValue` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 83 che coinvolge il nodo canonico `Specifiche integrazione XValue` è `ULSS 3 Serenissima - Specifiche tecniche di integrazione con XValue v2.10.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 83 che coinvolge il nodo canonico `Specifiche integrazione XValue` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 83 che coinvolge il nodo canonico `Specifiche integrazione XValue` è `1.0`. Nel grafo documentale, l'arco 83 registra come `source` l'identificatore `xvalue_specifiche` per la relazione che coinvolge il nodo canonico `Specifiche integrazione XValue`. Nel grafo documentale, l'arco 83 registra come `target` l'identificatore `xvalue_document_source` per la relazione che coinvolge il nodo canonico `Specifiche integrazione XValue`.
Nel grafo documentale, il nodo canonico `Specifiche integrazione XValue` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=627 id=graphify_kb::xvalue_specifiche -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Specifiche integrazione XValue` è `Specifiche integrazione XValue`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Specifiche integrazione XValue` come `document`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Specifiche integrazione XValue` è `ULSS 3 Serenissima - Specifiche tecniche di integrazione con XValue v2.10.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Specifiche integrazione XValue` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Specifiche integrazione XValue` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Specifiche integrazione XValue` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Specifiche integrazione XValue` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Specifiche integrazione XValue` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Specifiche integrazione XValue` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Specifiche integrazione XValue` è `specifiche integrazione xvalue`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Specifiche integrazione XValue` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Specifiche integrazione XValue` è `xvalue_specifiche`. Nel grafo unificato, il nodo canonico `Specifiche integrazione XValue` possiede l'identificatore esatto `graphify_kb::xvalue_specifiche`.
<!-- GRAPH_EDGE graph=unificata index=1111 role=outgoing -->
Nel grafo unificato, il nodo canonico `Specifiche integrazione XValue` è la sorgente `source` della relazione `references` verso il nodo canonico `Document Source`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::xvalue_specifiche` all'ID `graphify_kb::xvalue_document_source`. La relazione tecnica esplicita è la seguente: `Specifiche integrazione XValue` fa riferimento a `Document Source`. Nel grafo unificato, il tipo dichiarato dell'arco 1111 che coinvolge il nodo canonico `Specifiche integrazione XValue` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1111 che coinvolge il nodo canonico `Specifiche integrazione XValue` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1111 che coinvolge il nodo canonico `Specifiche integrazione XValue` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1111 che coinvolge il nodo canonico `Specifiche integrazione XValue` è `ULSS 3 Serenissima - Specifiche tecniche di integrazione con XValue v2.10.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1111 che coinvolge il nodo canonico `Specifiche integrazione XValue` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1111 che coinvolge il nodo canonico `Specifiche integrazione XValue` è `1.0`. Nel grafo unificato, l'arco 1111 registra come `source` l'identificatore `graphify_kb::xvalue_specifiche` per la relazione che coinvolge il nodo canonico `Specifiche integrazione XValue`. Nel grafo unificato, l'arco 1111 registra come `target` l'identificatore `graphify_kb::xvalue_document_source` per la relazione che coinvolge il nodo canonico `Specifiche integrazione XValue`.
Nel grafo unificato, il nodo canonico `Specifiche integrazione XValue` non possiede archi entranti registrati nel campo `links`.

### XDS Document Registry

<!-- GRAPH_NODE graph=documentale index=97 id=xvalue_xds_registry -->
Nel grafo documentale, l'etichetta originale del nodo canonico `XDS Document Registry` è `XDS Document Registry`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `XDS Document Registry` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `XDS Document Registry` è `ULSS 3 Serenissima - Specifiche tecniche di integrazione con XValue v2.10.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `XDS Document Registry` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `XDS Document Registry` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `XDS Document Registry` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `XDS Document Registry` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `XDS Document Registry` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `XDS Document Registry` possiede l'identificatore esatto `xvalue_xds_registry`. Nel grafo documentale, Graphify assegna il nodo canonico `XDS Document Registry` alla comunità numerica `13`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `XDS Document Registry` è `xds document registry`.
Nel grafo documentale, il nodo canonico `XDS Document Registry` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=documentale index=85 role=incoming -->
Nel grafo documentale, il nodo canonico `XDS Document Registry` è il destinatario `target` della relazione `shares_data_with` proveniente dal nodo canonico `XDS Document Repository`; l'orientamento registrato nel JSON va dall'ID `xvalue_xds_repository` all'ID `xvalue_xds_registry`. La relazione tecnica esplicita è la seguente: `XDS Document Repository` condivide dati con `XDS Document Registry`. Nel grafo documentale, il tipo dichiarato dell'arco 85 che coinvolge il nodo canonico `XDS Document Registry` è `shares_data_with`. Nel grafo documentale, la confidenza categoriale dell'arco 85 che coinvolge il nodo canonico `XDS Document Registry` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 85 che coinvolge il nodo canonico `XDS Document Registry` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 85 che coinvolge il nodo canonico `XDS Document Registry` è `ULSS 3 Serenissima - Specifiche tecniche di integrazione con XValue v2.10.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 85 che coinvolge il nodo canonico `XDS Document Registry` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 85 che coinvolge il nodo canonico `XDS Document Registry` è `1.0`. Nel grafo documentale, l'arco 85 registra come `source` l'identificatore `xvalue_xds_repository` per la relazione che coinvolge il nodo canonico `XDS Document Registry`. Nel grafo documentale, l'arco 85 registra come `target` l'identificatore `xvalue_xds_registry` per la relazione che coinvolge il nodo canonico `XDS Document Registry`.

<!-- GRAPH_NODE graph=unificata index=630 id=graphify_kb::xvalue_xds_registry -->
Nel grafo unificato, l'etichetta originale del nodo canonico `XDS Document Registry` è `XDS Document Registry`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `XDS Document Registry` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `XDS Document Registry` è `ULSS 3 Serenissima - Specifiche tecniche di integrazione con XValue v2.10.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `XDS Document Registry` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `XDS Document Registry` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `XDS Document Registry` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `XDS Document Registry` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `XDS Document Registry` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `XDS Document Registry` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `XDS Document Registry` è `xds document registry`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `XDS Document Registry` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `XDS Document Registry` è `xvalue_xds_registry`. Nel grafo unificato, il nodo canonico `XDS Document Registry` possiede l'identificatore esatto `graphify_kb::xvalue_xds_registry`.
Nel grafo unificato, il nodo canonico `XDS Document Registry` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1113 role=incoming -->
Nel grafo unificato, il nodo canonico `XDS Document Registry` è il destinatario `target` della relazione `shares_data_with` proveniente dal nodo canonico `XDS Document Repository`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::xvalue_xds_repository` all'ID `graphify_kb::xvalue_xds_registry`. La relazione tecnica esplicita è la seguente: `XDS Document Repository` condivide dati con `XDS Document Registry`. Nel grafo unificato, il tipo dichiarato dell'arco 1113 che coinvolge il nodo canonico `XDS Document Registry` è `shares_data_with`. Nel grafo unificato, la confidenza categoriale dell'arco 1113 che coinvolge il nodo canonico `XDS Document Registry` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1113 che coinvolge il nodo canonico `XDS Document Registry` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1113 che coinvolge il nodo canonico `XDS Document Registry` è `ULSS 3 Serenissima - Specifiche tecniche di integrazione con XValue v2.10.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1113 che coinvolge il nodo canonico `XDS Document Registry` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1113 che coinvolge il nodo canonico `XDS Document Registry` è `1.0`. Nel grafo unificato, l'arco 1113 registra come `source` l'identificatore `graphify_kb::xvalue_xds_repository` per la relazione che coinvolge il nodo canonico `XDS Document Registry`. Nel grafo unificato, l'arco 1113 registra come `target` l'identificatore `graphify_kb::xvalue_xds_registry` per la relazione che coinvolge il nodo canonico `XDS Document Registry`.

### XDS Document Repository

<!-- GRAPH_NODE graph=documentale index=96 id=xvalue_xds_repository -->
Nel grafo documentale, l'etichetta originale del nodo canonico `XDS Document Repository` è `XDS Document Repository`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `XDS Document Repository` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `XDS Document Repository` è `ULSS 3 Serenissima - Specifiche tecniche di integrazione con XValue v2.10.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `XDS Document Repository` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `XDS Document Repository` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `XDS Document Repository` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `XDS Document Repository` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `XDS Document Repository` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `XDS Document Repository` possiede l'identificatore esatto `xvalue_xds_repository`. Nel grafo documentale, Graphify assegna il nodo canonico `XDS Document Repository` alla comunità numerica `13`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `XDS Document Repository` è `xds document repository`.
<!-- GRAPH_EDGE graph=documentale index=85 role=outgoing -->
Nel grafo documentale, il nodo canonico `XDS Document Repository` è la sorgente `source` della relazione `shares_data_with` verso il nodo canonico `XDS Document Registry`; l'orientamento registrato nel JSON va dall'ID `xvalue_xds_repository` all'ID `xvalue_xds_registry`. La relazione tecnica esplicita è la seguente: `XDS Document Repository` condivide dati con `XDS Document Registry`. Nel grafo documentale, il tipo dichiarato dell'arco 85 che coinvolge il nodo canonico `XDS Document Repository` è `shares_data_with`. Nel grafo documentale, la confidenza categoriale dell'arco 85 che coinvolge il nodo canonico `XDS Document Repository` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 85 che coinvolge il nodo canonico `XDS Document Repository` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 85 che coinvolge il nodo canonico `XDS Document Repository` è `ULSS 3 Serenissima - Specifiche tecniche di integrazione con XValue v2.10.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 85 che coinvolge il nodo canonico `XDS Document Repository` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 85 che coinvolge il nodo canonico `XDS Document Repository` è `1.0`. Nel grafo documentale, l'arco 85 registra come `source` l'identificatore `xvalue_xds_repository` per la relazione che coinvolge il nodo canonico `XDS Document Repository`. Nel grafo documentale, l'arco 85 registra come `target` l'identificatore `xvalue_xds_registry` per la relazione che coinvolge il nodo canonico `XDS Document Repository`.
<!-- GRAPH_EDGE graph=documentale index=84 role=incoming -->
Nel grafo documentale, il nodo canonico `XDS Document Repository` è il destinatario `target` della relazione `shares_data_with` proveniente dal nodo canonico `Document Source`; l'orientamento registrato nel JSON va dall'ID `xvalue_document_source` all'ID `xvalue_xds_repository`. La relazione tecnica esplicita è la seguente: `Document Source` condivide dati con `XDS Document Repository`. Nel grafo documentale, il tipo dichiarato dell'arco 84 che coinvolge il nodo canonico `XDS Document Repository` è `shares_data_with`. Nel grafo documentale, la confidenza categoriale dell'arco 84 che coinvolge il nodo canonico `XDS Document Repository` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 84 che coinvolge il nodo canonico `XDS Document Repository` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 84 che coinvolge il nodo canonico `XDS Document Repository` è `ULSS 3 Serenissima - Specifiche tecniche di integrazione con XValue v2.10.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 84 che coinvolge il nodo canonico `XDS Document Repository` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 84 che coinvolge il nodo canonico `XDS Document Repository` è `1.0`. Nel grafo documentale, l'arco 84 registra come `source` l'identificatore `xvalue_document_source` per la relazione che coinvolge il nodo canonico `XDS Document Repository`. Nel grafo documentale, l'arco 84 registra come `target` l'identificatore `xvalue_xds_repository` per la relazione che coinvolge il nodo canonico `XDS Document Repository`.

<!-- GRAPH_NODE graph=unificata index=629 id=graphify_kb::xvalue_xds_repository -->
Nel grafo unificato, l'etichetta originale del nodo canonico `XDS Document Repository` è `XDS Document Repository`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `XDS Document Repository` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `XDS Document Repository` è `ULSS 3 Serenissima - Specifiche tecniche di integrazione con XValue v2.10.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `XDS Document Repository` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `XDS Document Repository` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `XDS Document Repository` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `XDS Document Repository` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `XDS Document Repository` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `XDS Document Repository` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `XDS Document Repository` è `xds document repository`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `XDS Document Repository` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `XDS Document Repository` è `xvalue_xds_repository`. Nel grafo unificato, il nodo canonico `XDS Document Repository` possiede l'identificatore esatto `graphify_kb::xvalue_xds_repository`.
<!-- GRAPH_EDGE graph=unificata index=1113 role=outgoing -->
Nel grafo unificato, il nodo canonico `XDS Document Repository` è la sorgente `source` della relazione `shares_data_with` verso il nodo canonico `XDS Document Registry`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::xvalue_xds_repository` all'ID `graphify_kb::xvalue_xds_registry`. La relazione tecnica esplicita è la seguente: `XDS Document Repository` condivide dati con `XDS Document Registry`. Nel grafo unificato, il tipo dichiarato dell'arco 1113 che coinvolge il nodo canonico `XDS Document Repository` è `shares_data_with`. Nel grafo unificato, la confidenza categoriale dell'arco 1113 che coinvolge il nodo canonico `XDS Document Repository` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1113 che coinvolge il nodo canonico `XDS Document Repository` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1113 che coinvolge il nodo canonico `XDS Document Repository` è `ULSS 3 Serenissima - Specifiche tecniche di integrazione con XValue v2.10.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1113 che coinvolge il nodo canonico `XDS Document Repository` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1113 che coinvolge il nodo canonico `XDS Document Repository` è `1.0`. Nel grafo unificato, l'arco 1113 registra come `source` l'identificatore `graphify_kb::xvalue_xds_repository` per la relazione che coinvolge il nodo canonico `XDS Document Repository`. Nel grafo unificato, l'arco 1113 registra come `target` l'identificatore `graphify_kb::xvalue_xds_registry` per la relazione che coinvolge il nodo canonico `XDS Document Repository`.
<!-- GRAPH_EDGE graph=unificata index=1112 role=incoming -->
Nel grafo unificato, il nodo canonico `XDS Document Repository` è il destinatario `target` della relazione `shares_data_with` proveniente dal nodo canonico `Document Source`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::xvalue_document_source` all'ID `graphify_kb::xvalue_xds_repository`. La relazione tecnica esplicita è la seguente: `Document Source` condivide dati con `XDS Document Repository`. Nel grafo unificato, il tipo dichiarato dell'arco 1112 che coinvolge il nodo canonico `XDS Document Repository` è `shares_data_with`. Nel grafo unificato, la confidenza categoriale dell'arco 1112 che coinvolge il nodo canonico `XDS Document Repository` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1112 che coinvolge il nodo canonico `XDS Document Repository` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1112 che coinvolge il nodo canonico `XDS Document Repository` è `ULSS 3 Serenissima - Specifiche tecniche di integrazione con XValue v2.10.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1112 che coinvolge il nodo canonico `XDS Document Repository` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1112 che coinvolge il nodo canonico `XDS Document Repository` è `1.0`. Nel grafo unificato, l'arco 1112 registra come `source` l'identificatore `graphify_kb::xvalue_document_source` per la relazione che coinvolge il nodo canonico `XDS Document Repository`. Nel grafo unificato, l'arco 1112 registra come `target` l'identificatore `graphify_kb::xvalue_xds_repository` per la relazione che coinvolge il nodo canonico `XDS Document Repository`.

## Cluster Graphify 14 — Data Architecture

Il cluster editoriale Graphify 14 del dominio Interoperabilità tecnico-normativa contiene 3 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### Data Governance

<!-- GRAPH_NODE graph=documentale index=38 id=data_governance -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Data Governance` è `Data Governance`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Data Governance` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Data Governance` è `Data Reference Architecture-v1.1-Nuova.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Data Governance` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Data Governance` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Data Governance` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Data Governance` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Data Governance` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Data Governance` possiede l'identificatore esatto `data_governance`. Nel grafo documentale, Graphify assegna il nodo canonico `Data Governance` alla comunità numerica `14`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Data Governance` è `data governance`.
Nel grafo documentale, il nodo canonico `Data Governance` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=documentale index=39 role=incoming -->
Nel grafo documentale, il nodo canonico `Data Governance` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Data Reference Architecture`; l'orientamento registrato nel JSON va dall'ID `data_reference_architecture` all'ID `data_governance`. La relazione tecnica esplicita è la seguente: `Data Reference Architecture` fa riferimento a `Data Governance`. Nel grafo documentale, il tipo dichiarato dell'arco 39 che coinvolge il nodo canonico `Data Governance` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 39 che coinvolge il nodo canonico `Data Governance` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 39 che coinvolge il nodo canonico `Data Governance` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 39 che coinvolge il nodo canonico `Data Governance` è `Data Reference Architecture-v1.1-Nuova.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 39 che coinvolge il nodo canonico `Data Governance` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 39 che coinvolge il nodo canonico `Data Governance` è `1.0`. Nel grafo documentale, l'arco 39 registra come `source` l'identificatore `data_reference_architecture` per la relazione che coinvolge il nodo canonico `Data Governance`. Nel grafo documentale, l'arco 39 registra come `target` l'identificatore `data_governance` per la relazione che coinvolge il nodo canonico `Data Governance`.

<!-- GRAPH_NODE graph=unificata index=571 id=graphify_kb::data_governance -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Data Governance` è `Data Governance`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Data Governance` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Data Governance` è `Data Reference Architecture-v1.1-Nuova.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Data Governance` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Data Governance` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Data Governance` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Data Governance` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Data Governance` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Data Governance` alla comunità numerica `14`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Data Governance` è `data governance`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Data Governance` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Data Governance` è `data_governance`. Nel grafo unificato, il nodo canonico `Data Governance` possiede l'identificatore esatto `graphify_kb::data_governance`.
Nel grafo unificato, il nodo canonico `Data Governance` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1067 role=incoming -->
Nel grafo unificato, il nodo canonico `Data Governance` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Data Reference Architecture`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::data_reference_architecture` all'ID `graphify_kb::data_governance`. La relazione tecnica esplicita è la seguente: `Data Reference Architecture` fa riferimento a `Data Governance`. Nel grafo unificato, il tipo dichiarato dell'arco 1067 che coinvolge il nodo canonico `Data Governance` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1067 che coinvolge il nodo canonico `Data Governance` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1067 che coinvolge il nodo canonico `Data Governance` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1067 che coinvolge il nodo canonico `Data Governance` è `Data Reference Architecture-v1.1-Nuova.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1067 che coinvolge il nodo canonico `Data Governance` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1067 che coinvolge il nodo canonico `Data Governance` è `1.0`. Nel grafo unificato, l'arco 1067 registra come `source` l'identificatore `graphify_kb::data_reference_architecture` per la relazione che coinvolge il nodo canonico `Data Governance`. Nel grafo unificato, l'arco 1067 registra come `target` l'identificatore `graphify_kb::data_governance` per la relazione che coinvolge il nodo canonico `Data Governance`.

### Data Reference Architecture

<!-- GRAPH_NODE graph=documentale index=37 id=data_reference_architecture -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Data Reference Architecture` è `Data Reference Architecture`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Data Reference Architecture` come `document`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Data Reference Architecture` è `Data Reference Architecture-v1.1-Nuova.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Data Reference Architecture` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Data Reference Architecture` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Data Reference Architecture` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Data Reference Architecture` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Data Reference Architecture` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Data Reference Architecture` possiede l'identificatore esatto `data_reference_architecture`. Nel grafo documentale, Graphify assegna il nodo canonico `Data Reference Architecture` alla comunità numerica `14`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Data Reference Architecture` è `data reference architecture`.
<!-- GRAPH_EDGE graph=documentale index=39 role=outgoing -->
Nel grafo documentale, il nodo canonico `Data Reference Architecture` è la sorgente `source` della relazione `references` verso il nodo canonico `Data Governance`; l'orientamento registrato nel JSON va dall'ID `data_reference_architecture` all'ID `data_governance`. La relazione tecnica esplicita è la seguente: `Data Reference Architecture` fa riferimento a `Data Governance`. Nel grafo documentale, il tipo dichiarato dell'arco 39 che coinvolge il nodo canonico `Data Reference Architecture` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 39 che coinvolge il nodo canonico `Data Reference Architecture` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 39 che coinvolge il nodo canonico `Data Reference Architecture` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 39 che coinvolge il nodo canonico `Data Reference Architecture` è `Data Reference Architecture-v1.1-Nuova.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 39 che coinvolge il nodo canonico `Data Reference Architecture` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 39 che coinvolge il nodo canonico `Data Reference Architecture` è `1.0`. Nel grafo documentale, l'arco 39 registra come `source` l'identificatore `data_reference_architecture` per la relazione che coinvolge il nodo canonico `Data Reference Architecture`. Nel grafo documentale, l'arco 39 registra come `target` l'identificatore `data_governance` per la relazione che coinvolge il nodo canonico `Data Reference Architecture`.
<!-- GRAPH_EDGE graph=documentale index=40 role=outgoing -->
Nel grafo documentale, il nodo canonico `Data Reference Architecture` è la sorgente `source` della relazione `references` verso il nodo canonico `Data Virtualization`; l'orientamento registrato nel JSON va dall'ID `data_reference_architecture` all'ID `data_virtualization`. La relazione tecnica esplicita è la seguente: `Data Reference Architecture` fa riferimento a `Data Virtualization`. Nel grafo documentale, il tipo dichiarato dell'arco 40 che coinvolge il nodo canonico `Data Reference Architecture` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 40 che coinvolge il nodo canonico `Data Reference Architecture` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 40 che coinvolge il nodo canonico `Data Reference Architecture` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 40 che coinvolge il nodo canonico `Data Reference Architecture` è `Data Reference Architecture-v1.1-Nuova.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 40 che coinvolge il nodo canonico `Data Reference Architecture` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 40 che coinvolge il nodo canonico `Data Reference Architecture` è `1.0`. Nel grafo documentale, l'arco 40 registra come `source` l'identificatore `data_reference_architecture` per la relazione che coinvolge il nodo canonico `Data Reference Architecture`. Nel grafo documentale, l'arco 40 registra come `target` l'identificatore `data_virtualization` per la relazione che coinvolge il nodo canonico `Data Reference Architecture`.
Nel grafo documentale, il nodo canonico `Data Reference Architecture` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=570 id=graphify_kb::data_reference_architecture -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Data Reference Architecture` è `Data Reference Architecture`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Data Reference Architecture` come `document`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Data Reference Architecture` è `Data Reference Architecture-v1.1-Nuova.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Data Reference Architecture` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Data Reference Architecture` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Data Reference Architecture` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Data Reference Architecture` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Data Reference Architecture` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Data Reference Architecture` alla comunità numerica `14`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Data Reference Architecture` è `data reference architecture`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Data Reference Architecture` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Data Reference Architecture` è `data_reference_architecture`. Nel grafo unificato, il nodo canonico `Data Reference Architecture` possiede l'identificatore esatto `graphify_kb::data_reference_architecture`.
<!-- GRAPH_EDGE graph=unificata index=1067 role=outgoing -->
Nel grafo unificato, il nodo canonico `Data Reference Architecture` è la sorgente `source` della relazione `references` verso il nodo canonico `Data Governance`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::data_reference_architecture` all'ID `graphify_kb::data_governance`. La relazione tecnica esplicita è la seguente: `Data Reference Architecture` fa riferimento a `Data Governance`. Nel grafo unificato, il tipo dichiarato dell'arco 1067 che coinvolge il nodo canonico `Data Reference Architecture` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1067 che coinvolge il nodo canonico `Data Reference Architecture` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1067 che coinvolge il nodo canonico `Data Reference Architecture` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1067 che coinvolge il nodo canonico `Data Reference Architecture` è `Data Reference Architecture-v1.1-Nuova.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1067 che coinvolge il nodo canonico `Data Reference Architecture` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1067 che coinvolge il nodo canonico `Data Reference Architecture` è `1.0`. Nel grafo unificato, l'arco 1067 registra come `source` l'identificatore `graphify_kb::data_reference_architecture` per la relazione che coinvolge il nodo canonico `Data Reference Architecture`. Nel grafo unificato, l'arco 1067 registra come `target` l'identificatore `graphify_kb::data_governance` per la relazione che coinvolge il nodo canonico `Data Reference Architecture`.
<!-- GRAPH_EDGE graph=unificata index=1068 role=outgoing -->
Nel grafo unificato, il nodo canonico `Data Reference Architecture` è la sorgente `source` della relazione `references` verso il nodo canonico `Data Virtualization`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::data_reference_architecture` all'ID `graphify_kb::data_virtualization`. La relazione tecnica esplicita è la seguente: `Data Reference Architecture` fa riferimento a `Data Virtualization`. Nel grafo unificato, il tipo dichiarato dell'arco 1068 che coinvolge il nodo canonico `Data Reference Architecture` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1068 che coinvolge il nodo canonico `Data Reference Architecture` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1068 che coinvolge il nodo canonico `Data Reference Architecture` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1068 che coinvolge il nodo canonico `Data Reference Architecture` è `Data Reference Architecture-v1.1-Nuova.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1068 che coinvolge il nodo canonico `Data Reference Architecture` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1068 che coinvolge il nodo canonico `Data Reference Architecture` è `1.0`. Nel grafo unificato, l'arco 1068 registra come `source` l'identificatore `graphify_kb::data_reference_architecture` per la relazione che coinvolge il nodo canonico `Data Reference Architecture`. Nel grafo unificato, l'arco 1068 registra come `target` l'identificatore `graphify_kb::data_virtualization` per la relazione che coinvolge il nodo canonico `Data Reference Architecture`.
Nel grafo unificato, il nodo canonico `Data Reference Architecture` non possiede archi entranti registrati nel campo `links`.

### Data Virtualization

<!-- GRAPH_NODE graph=documentale index=39 id=data_virtualization -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Data Virtualization` è `Data Virtualization`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Data Virtualization` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Data Virtualization` è `Data Reference Architecture-v1.1-Nuova.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Data Virtualization` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Data Virtualization` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Data Virtualization` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Data Virtualization` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Data Virtualization` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Data Virtualization` possiede l'identificatore esatto `data_virtualization`. Nel grafo documentale, Graphify assegna il nodo canonico `Data Virtualization` alla comunità numerica `14`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Data Virtualization` è `data virtualization`.
Nel grafo documentale, il nodo canonico `Data Virtualization` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=documentale index=40 role=incoming -->
Nel grafo documentale, il nodo canonico `Data Virtualization` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Data Reference Architecture`; l'orientamento registrato nel JSON va dall'ID `data_reference_architecture` all'ID `data_virtualization`. La relazione tecnica esplicita è la seguente: `Data Reference Architecture` fa riferimento a `Data Virtualization`. Nel grafo documentale, il tipo dichiarato dell'arco 40 che coinvolge il nodo canonico `Data Virtualization` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 40 che coinvolge il nodo canonico `Data Virtualization` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 40 che coinvolge il nodo canonico `Data Virtualization` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 40 che coinvolge il nodo canonico `Data Virtualization` è `Data Reference Architecture-v1.1-Nuova.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 40 che coinvolge il nodo canonico `Data Virtualization` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 40 che coinvolge il nodo canonico `Data Virtualization` è `1.0`. Nel grafo documentale, l'arco 40 registra come `source` l'identificatore `data_reference_architecture` per la relazione che coinvolge il nodo canonico `Data Virtualization`. Nel grafo documentale, l'arco 40 registra come `target` l'identificatore `data_virtualization` per la relazione che coinvolge il nodo canonico `Data Virtualization`.

<!-- GRAPH_NODE graph=unificata index=572 id=graphify_kb::data_virtualization -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Data Virtualization` è `Data Virtualization`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Data Virtualization` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Data Virtualization` è `Data Reference Architecture-v1.1-Nuova.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Data Virtualization` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Data Virtualization` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Data Virtualization` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Data Virtualization` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Data Virtualization` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Data Virtualization` alla comunità numerica `14`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Data Virtualization` è `data virtualization`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Data Virtualization` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Data Virtualization` è `data_virtualization`. Nel grafo unificato, il nodo canonico `Data Virtualization` possiede l'identificatore esatto `graphify_kb::data_virtualization`.
Nel grafo unificato, il nodo canonico `Data Virtualization` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1068 role=incoming -->
Nel grafo unificato, il nodo canonico `Data Virtualization` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Data Reference Architecture`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::data_reference_architecture` all'ID `graphify_kb::data_virtualization`. La relazione tecnica esplicita è la seguente: `Data Reference Architecture` fa riferimento a `Data Virtualization`. Nel grafo unificato, il tipo dichiarato dell'arco 1068 che coinvolge il nodo canonico `Data Virtualization` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1068 che coinvolge il nodo canonico `Data Virtualization` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1068 che coinvolge il nodo canonico `Data Virtualization` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1068 che coinvolge il nodo canonico `Data Virtualization` è `Data Reference Architecture-v1.1-Nuova.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1068 che coinvolge il nodo canonico `Data Virtualization` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1068 che coinvolge il nodo canonico `Data Virtualization` è `1.0`. Nel grafo unificato, l'arco 1068 registra come `source` l'identificatore `graphify_kb::data_reference_architecture` per la relazione che coinvolge il nodo canonico `Data Virtualization`. Nel grafo unificato, l'arco 1068 registra come `target` l'identificatore `graphify_kb::data_virtualization` per la relazione che coinvolge il nodo canonico `Data Virtualization`.

## Cluster Graphify 15 — Integration Middleware

Il cluster editoriale Graphify 15 del dominio Interoperabilità tecnico-normativa contiene 3 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### Camunda v7

<!-- GRAPH_NODE graph=documentale index=65 id=rti_camunda -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Camunda v7` è `Camunda v7`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Camunda v7` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Camunda v7` è `RTI AlmavivA Relazione Tecnica Sanità digitale ID 2365 Lotto 3 - Azienda Zero Veneto SIT _RdO 5521029 1.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Camunda v7` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Camunda v7` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Camunda v7` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Camunda v7` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Camunda v7` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Camunda v7` possiede l'identificatore esatto `rti_camunda`. Nel grafo documentale, Graphify assegna il nodo canonico `Camunda v7` alla comunità numerica `15`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Camunda v7` è `camunda v7`.
Nel grafo documentale, il nodo canonico `Camunda v7` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=documentale index=58 role=incoming -->
Nel grafo documentale, il nodo canonico `Camunda v7` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Layer Interoperabilita`; l'orientamento registrato nel JSON va dall'ID `rti_layer_interoperabilita` all'ID `rti_camunda`. La relazione tecnica esplicita è la seguente: `Layer Interoperabilita` fa riferimento a `Camunda v7`. Nel grafo documentale, il tipo dichiarato dell'arco 58 che coinvolge il nodo canonico `Camunda v7` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 58 che coinvolge il nodo canonico `Camunda v7` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 58 che coinvolge il nodo canonico `Camunda v7` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 58 che coinvolge il nodo canonico `Camunda v7` è `RTI AlmavivA Relazione Tecnica Sanità digitale ID 2365 Lotto 3 - Azienda Zero Veneto SIT _RdO 5521029 1.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 58 che coinvolge il nodo canonico `Camunda v7` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 58 che coinvolge il nodo canonico `Camunda v7` è `1.0`. Nel grafo documentale, l'arco 58 registra come `source` l'identificatore `rti_layer_interoperabilita` per la relazione che coinvolge il nodo canonico `Camunda v7`. Nel grafo documentale, l'arco 58 registra come `target` l'identificatore `rti_camunda` per la relazione che coinvolge il nodo canonico `Camunda v7`.

<!-- GRAPH_NODE graph=unificata index=598 id=graphify_kb::rti_camunda -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Camunda v7` è `Camunda v7`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Camunda v7` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Camunda v7` è `RTI AlmavivA Relazione Tecnica Sanità digitale ID 2365 Lotto 3 - Azienda Zero Veneto SIT _RdO 5521029 1.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Camunda v7` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Camunda v7` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Camunda v7` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Camunda v7` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Camunda v7` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Camunda v7` alla comunità numerica `15`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Camunda v7` è `camunda v7`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Camunda v7` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Camunda v7` è `rti_camunda`. Nel grafo unificato, il nodo canonico `Camunda v7` possiede l'identificatore esatto `graphify_kb::rti_camunda`.
Nel grafo unificato, il nodo canonico `Camunda v7` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1086 role=incoming -->
Nel grafo unificato, il nodo canonico `Camunda v7` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Layer Interoperabilita`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::rti_layer_interoperabilita` all'ID `graphify_kb::rti_camunda`. La relazione tecnica esplicita è la seguente: `Layer Interoperabilita` fa riferimento a `Camunda v7`. Nel grafo unificato, il tipo dichiarato dell'arco 1086 che coinvolge il nodo canonico `Camunda v7` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1086 che coinvolge il nodo canonico `Camunda v7` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1086 che coinvolge il nodo canonico `Camunda v7` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1086 che coinvolge il nodo canonico `Camunda v7` è `RTI AlmavivA Relazione Tecnica Sanità digitale ID 2365 Lotto 3 - Azienda Zero Veneto SIT _RdO 5521029 1.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1086 che coinvolge il nodo canonico `Camunda v7` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1086 che coinvolge il nodo canonico `Camunda v7` è `1.0`. Nel grafo unificato, l'arco 1086 registra come `source` l'identificatore `graphify_kb::rti_layer_interoperabilita` per la relazione che coinvolge il nodo canonico `Camunda v7`. Nel grafo unificato, l'arco 1086 registra come `target` l'identificatore `graphify_kb::rti_camunda` per la relazione che coinvolge il nodo canonico `Camunda v7`.

### Layer Interoperabilita

<!-- GRAPH_NODE graph=documentale index=64 id=rti_layer_interoperabilita -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Layer Interoperabilita` è `Layer Interoperabilita`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Layer Interoperabilita` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Layer Interoperabilita` è `RTI AlmavivA Relazione Tecnica Sanità digitale ID 2365 Lotto 3 - Azienda Zero Veneto SIT _RdO 5521029 1.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Layer Interoperabilita` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Layer Interoperabilita` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Layer Interoperabilita` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Layer Interoperabilita` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Layer Interoperabilita` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Layer Interoperabilita` possiede l'identificatore esatto `rti_layer_interoperabilita`. Nel grafo documentale, Graphify assegna il nodo canonico `Layer Interoperabilita` alla comunità numerica `15`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Layer Interoperabilita` è `layer interoperabilita`.
<!-- GRAPH_EDGE graph=documentale index=58 role=outgoing -->
Nel grafo documentale, il nodo canonico `Layer Interoperabilita` è la sorgente `source` della relazione `references` verso il nodo canonico `Camunda v7`; l'orientamento registrato nel JSON va dall'ID `rti_layer_interoperabilita` all'ID `rti_camunda`. La relazione tecnica esplicita è la seguente: `Layer Interoperabilita` fa riferimento a `Camunda v7`. Nel grafo documentale, il tipo dichiarato dell'arco 58 che coinvolge il nodo canonico `Layer Interoperabilita` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 58 che coinvolge il nodo canonico `Layer Interoperabilita` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 58 che coinvolge il nodo canonico `Layer Interoperabilita` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 58 che coinvolge il nodo canonico `Layer Interoperabilita` è `RTI AlmavivA Relazione Tecnica Sanità digitale ID 2365 Lotto 3 - Azienda Zero Veneto SIT _RdO 5521029 1.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 58 che coinvolge il nodo canonico `Layer Interoperabilita` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 58 che coinvolge il nodo canonico `Layer Interoperabilita` è `1.0`. Nel grafo documentale, l'arco 58 registra come `source` l'identificatore `rti_layer_interoperabilita` per la relazione che coinvolge il nodo canonico `Layer Interoperabilita`. Nel grafo documentale, l'arco 58 registra come `target` l'identificatore `rti_camunda` per la relazione che coinvolge il nodo canonico `Layer Interoperabilita`.
<!-- GRAPH_EDGE graph=documentale index=59 role=outgoing -->
Nel grafo documentale, il nodo canonico `Layer Interoperabilita` è la sorgente `source` della relazione `references` verso il nodo canonico `RabbitMQ`; l'orientamento registrato nel JSON va dall'ID `rti_layer_interoperabilita` all'ID `rti_rabbitmq`. La relazione tecnica esplicita è la seguente: `Layer Interoperabilita` fa riferimento a `RabbitMQ`. Nel grafo documentale, il tipo dichiarato dell'arco 59 che coinvolge il nodo canonico `Layer Interoperabilita` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 59 che coinvolge il nodo canonico `Layer Interoperabilita` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 59 che coinvolge il nodo canonico `Layer Interoperabilita` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 59 che coinvolge il nodo canonico `Layer Interoperabilita` è `RTI AlmavivA Relazione Tecnica Sanità digitale ID 2365 Lotto 3 - Azienda Zero Veneto SIT _RdO 5521029 1.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 59 che coinvolge il nodo canonico `Layer Interoperabilita` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 59 che coinvolge il nodo canonico `Layer Interoperabilita` è `1.0`. Nel grafo documentale, l'arco 59 registra come `source` l'identificatore `rti_layer_interoperabilita` per la relazione che coinvolge il nodo canonico `Layer Interoperabilita`. Nel grafo documentale, l'arco 59 registra come `target` l'identificatore `rti_rabbitmq` per la relazione che coinvolge il nodo canonico `Layer Interoperabilita`.
Nel grafo documentale, il nodo canonico `Layer Interoperabilita` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=597 id=graphify_kb::rti_layer_interoperabilita -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Layer Interoperabilita` è `Layer Interoperabilita`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Layer Interoperabilita` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Layer Interoperabilita` è `RTI AlmavivA Relazione Tecnica Sanità digitale ID 2365 Lotto 3 - Azienda Zero Veneto SIT _RdO 5521029 1.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Layer Interoperabilita` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Layer Interoperabilita` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Layer Interoperabilita` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Layer Interoperabilita` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Layer Interoperabilita` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Layer Interoperabilita` alla comunità numerica `15`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Layer Interoperabilita` è `layer interoperabilita`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Layer Interoperabilita` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Layer Interoperabilita` è `rti_layer_interoperabilita`. Nel grafo unificato, il nodo canonico `Layer Interoperabilita` possiede l'identificatore esatto `graphify_kb::rti_layer_interoperabilita`.
<!-- GRAPH_EDGE graph=unificata index=1086 role=outgoing -->
Nel grafo unificato, il nodo canonico `Layer Interoperabilita` è la sorgente `source` della relazione `references` verso il nodo canonico `Camunda v7`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::rti_layer_interoperabilita` all'ID `graphify_kb::rti_camunda`. La relazione tecnica esplicita è la seguente: `Layer Interoperabilita` fa riferimento a `Camunda v7`. Nel grafo unificato, il tipo dichiarato dell'arco 1086 che coinvolge il nodo canonico `Layer Interoperabilita` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1086 che coinvolge il nodo canonico `Layer Interoperabilita` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1086 che coinvolge il nodo canonico `Layer Interoperabilita` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1086 che coinvolge il nodo canonico `Layer Interoperabilita` è `RTI AlmavivA Relazione Tecnica Sanità digitale ID 2365 Lotto 3 - Azienda Zero Veneto SIT _RdO 5521029 1.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1086 che coinvolge il nodo canonico `Layer Interoperabilita` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1086 che coinvolge il nodo canonico `Layer Interoperabilita` è `1.0`. Nel grafo unificato, l'arco 1086 registra come `source` l'identificatore `graphify_kb::rti_layer_interoperabilita` per la relazione che coinvolge il nodo canonico `Layer Interoperabilita`. Nel grafo unificato, l'arco 1086 registra come `target` l'identificatore `graphify_kb::rti_camunda` per la relazione che coinvolge il nodo canonico `Layer Interoperabilita`.
<!-- GRAPH_EDGE graph=unificata index=1087 role=outgoing -->
Nel grafo unificato, il nodo canonico `Layer Interoperabilita` è la sorgente `source` della relazione `references` verso il nodo canonico `RabbitMQ`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::rti_layer_interoperabilita` all'ID `graphify_kb::rti_rabbitmq`. La relazione tecnica esplicita è la seguente: `Layer Interoperabilita` fa riferimento a `RabbitMQ`. Nel grafo unificato, il tipo dichiarato dell'arco 1087 che coinvolge il nodo canonico `Layer Interoperabilita` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1087 che coinvolge il nodo canonico `Layer Interoperabilita` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1087 che coinvolge il nodo canonico `Layer Interoperabilita` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1087 che coinvolge il nodo canonico `Layer Interoperabilita` è `RTI AlmavivA Relazione Tecnica Sanità digitale ID 2365 Lotto 3 - Azienda Zero Veneto SIT _RdO 5521029 1.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1087 che coinvolge il nodo canonico `Layer Interoperabilita` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1087 che coinvolge il nodo canonico `Layer Interoperabilita` è `1.0`. Nel grafo unificato, l'arco 1087 registra come `source` l'identificatore `graphify_kb::rti_layer_interoperabilita` per la relazione che coinvolge il nodo canonico `Layer Interoperabilita`. Nel grafo unificato, l'arco 1087 registra come `target` l'identificatore `graphify_kb::rti_rabbitmq` per la relazione che coinvolge il nodo canonico `Layer Interoperabilita`.
Nel grafo unificato, il nodo canonico `Layer Interoperabilita` non possiede archi entranti registrati nel campo `links`.

### RabbitMQ

<!-- GRAPH_NODE graph=documentale index=66 id=rti_rabbitmq -->
Nel grafo documentale, l'etichetta originale del nodo canonico `RabbitMQ` è `RabbitMQ`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `RabbitMQ` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `RabbitMQ` è `RTI AlmavivA Relazione Tecnica Sanità digitale ID 2365 Lotto 3 - Azienda Zero Veneto SIT _RdO 5521029 1.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `RabbitMQ` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `RabbitMQ` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `RabbitMQ` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `RabbitMQ` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `RabbitMQ` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `RabbitMQ` possiede l'identificatore esatto `rti_rabbitmq`. Nel grafo documentale, Graphify assegna il nodo canonico `RabbitMQ` alla comunità numerica `15`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `RabbitMQ` è `rabbitmq`.
Nel grafo documentale, il nodo canonico `RabbitMQ` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=documentale index=59 role=incoming -->
Nel grafo documentale, il nodo canonico `RabbitMQ` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Layer Interoperabilita`; l'orientamento registrato nel JSON va dall'ID `rti_layer_interoperabilita` all'ID `rti_rabbitmq`. La relazione tecnica esplicita è la seguente: `Layer Interoperabilita` fa riferimento a `RabbitMQ`. Nel grafo documentale, il tipo dichiarato dell'arco 59 che coinvolge il nodo canonico `RabbitMQ` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 59 che coinvolge il nodo canonico `RabbitMQ` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 59 che coinvolge il nodo canonico `RabbitMQ` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 59 che coinvolge il nodo canonico `RabbitMQ` è `RTI AlmavivA Relazione Tecnica Sanità digitale ID 2365 Lotto 3 - Azienda Zero Veneto SIT _RdO 5521029 1.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 59 che coinvolge il nodo canonico `RabbitMQ` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 59 che coinvolge il nodo canonico `RabbitMQ` è `1.0`. Nel grafo documentale, l'arco 59 registra come `source` l'identificatore `rti_layer_interoperabilita` per la relazione che coinvolge il nodo canonico `RabbitMQ`. Nel grafo documentale, l'arco 59 registra come `target` l'identificatore `rti_rabbitmq` per la relazione che coinvolge il nodo canonico `RabbitMQ`.

<!-- GRAPH_NODE graph=unificata index=599 id=graphify_kb::rti_rabbitmq -->
Nel grafo unificato, l'etichetta originale del nodo canonico `RabbitMQ` è `RabbitMQ`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `RabbitMQ` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `RabbitMQ` è `RTI AlmavivA Relazione Tecnica Sanità digitale ID 2365 Lotto 3 - Azienda Zero Veneto SIT _RdO 5521029 1.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `RabbitMQ` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `RabbitMQ` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `RabbitMQ` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `RabbitMQ` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `RabbitMQ` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `RabbitMQ` alla comunità numerica `15`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `RabbitMQ` è `rabbitmq`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `RabbitMQ` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `RabbitMQ` è `rti_rabbitmq`. Nel grafo unificato, il nodo canonico `RabbitMQ` possiede l'identificatore esatto `graphify_kb::rti_rabbitmq`.
Nel grafo unificato, il nodo canonico `RabbitMQ` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1087 role=incoming -->
Nel grafo unificato, il nodo canonico `RabbitMQ` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Layer Interoperabilita`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::rti_layer_interoperabilita` all'ID `graphify_kb::rti_rabbitmq`. La relazione tecnica esplicita è la seguente: `Layer Interoperabilita` fa riferimento a `RabbitMQ`. Nel grafo unificato, il tipo dichiarato dell'arco 1087 che coinvolge il nodo canonico `RabbitMQ` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1087 che coinvolge il nodo canonico `RabbitMQ` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1087 che coinvolge il nodo canonico `RabbitMQ` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1087 che coinvolge il nodo canonico `RabbitMQ` è `RTI AlmavivA Relazione Tecnica Sanità digitale ID 2365 Lotto 3 - Azienda Zero Veneto SIT _RdO 5521029 1.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1087 che coinvolge il nodo canonico `RabbitMQ` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1087 che coinvolge il nodo canonico `RabbitMQ` è `1.0`. Nel grafo unificato, l'arco 1087 registra come `source` l'identificatore `graphify_kb::rti_layer_interoperabilita` per la relazione che coinvolge il nodo canonico `RabbitMQ`. Nel grafo unificato, l'arco 1087 registra come `target` l'identificatore `graphify_kb::rti_rabbitmq` per la relazione che coinvolge il nodo canonico `RabbitMQ`.

## Cluster Graphify 16 — OAuth Token Flow

Il cluster editoriale Graphify 16 del dominio Interoperabilità tecnico-normativa contiene 2 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### Authorization Code Flow

<!-- GRAPH_NODE graph=documentale index=69 id=apms_authorization_code_flow -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Authorization Code Flow` è `Authorization Code Flow`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Authorization Code Flow` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Authorization Code Flow` è `SGI-WP1-Specifiche di integrazione autenticazione APMS-Team di sviluppo-v1.4.1.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Authorization Code Flow` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Authorization Code Flow` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Authorization Code Flow` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Authorization Code Flow` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Authorization Code Flow` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Authorization Code Flow` possiede l'identificatore esatto `apms_authorization_code_flow`. Nel grafo documentale, Graphify assegna il nodo canonico `Authorization Code Flow` alla comunità numerica `16`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Authorization Code Flow` è `authorization code flow`.
<!-- GRAPH_EDGE graph=documentale index=61 role=outgoing -->
Nel grafo documentale, il nodo canonico `Authorization Code Flow` è la sorgente `source` della relazione `conceptually_related_to` verso il nodo canonico `ID Token`; l'orientamento registrato nel JSON va dall'ID `apms_authorization_code_flow` all'ID `apms_id_token`. La relazione tecnica esplicita è la seguente: `Authorization Code Flow` è concettualmente correlato a `ID Token`. Nel grafo documentale, il tipo dichiarato dell'arco 61 che coinvolge il nodo canonico `Authorization Code Flow` è `conceptually_related_to`. Nel grafo documentale, la confidenza categoriale dell'arco 61 che coinvolge il nodo canonico `Authorization Code Flow` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 61 che coinvolge il nodo canonico `Authorization Code Flow` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 61 che coinvolge il nodo canonico `Authorization Code Flow` è `SGI-WP1-Specifiche di integrazione autenticazione APMS-Team di sviluppo-v1.4.1.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 61 che coinvolge il nodo canonico `Authorization Code Flow` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 61 che coinvolge il nodo canonico `Authorization Code Flow` è `1.0`. Nel grafo documentale, l'arco 61 registra come `source` l'identificatore `apms_authorization_code_flow` per la relazione che coinvolge il nodo canonico `Authorization Code Flow`. Nel grafo documentale, l'arco 61 registra come `target` l'identificatore `apms_id_token` per la relazione che coinvolge il nodo canonico `Authorization Code Flow`.
Nel grafo documentale, il nodo canonico `Authorization Code Flow` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=602 id=graphify_kb::apms_authorization_code_flow -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Authorization Code Flow` è `Authorization Code Flow`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Authorization Code Flow` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Authorization Code Flow` è `SGI-WP1-Specifiche di integrazione autenticazione APMS-Team di sviluppo-v1.4.1.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Authorization Code Flow` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Authorization Code Flow` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Authorization Code Flow` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Authorization Code Flow` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Authorization Code Flow` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Authorization Code Flow` alla comunità numerica `16`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Authorization Code Flow` è `authorization code flow`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Authorization Code Flow` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Authorization Code Flow` è `apms_authorization_code_flow`. Nel grafo unificato, il nodo canonico `Authorization Code Flow` possiede l'identificatore esatto `graphify_kb::apms_authorization_code_flow`.
<!-- GRAPH_EDGE graph=unificata index=1089 role=outgoing -->
Nel grafo unificato, il nodo canonico `Authorization Code Flow` è la sorgente `source` della relazione `conceptually_related_to` verso il nodo canonico `ID Token`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::apms_authorization_code_flow` all'ID `graphify_kb::apms_id_token`. La relazione tecnica esplicita è la seguente: `Authorization Code Flow` è concettualmente correlato a `ID Token`. Nel grafo unificato, il tipo dichiarato dell'arco 1089 che coinvolge il nodo canonico `Authorization Code Flow` è `conceptually_related_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1089 che coinvolge il nodo canonico `Authorization Code Flow` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1089 che coinvolge il nodo canonico `Authorization Code Flow` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1089 che coinvolge il nodo canonico `Authorization Code Flow` è `SGI-WP1-Specifiche di integrazione autenticazione APMS-Team di sviluppo-v1.4.1.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1089 che coinvolge il nodo canonico `Authorization Code Flow` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1089 che coinvolge il nodo canonico `Authorization Code Flow` è `1.0`. Nel grafo unificato, l'arco 1089 registra come `source` l'identificatore `graphify_kb::apms_authorization_code_flow` per la relazione che coinvolge il nodo canonico `Authorization Code Flow`. Nel grafo unificato, l'arco 1089 registra come `target` l'identificatore `graphify_kb::apms_id_token` per la relazione che coinvolge il nodo canonico `Authorization Code Flow`.
Nel grafo unificato, il nodo canonico `Authorization Code Flow` non possiede archi entranti registrati nel campo `links`.

### ID Token

<!-- GRAPH_NODE graph=documentale index=70 id=apms_id_token -->
Nel grafo documentale, l'etichetta originale del nodo canonico `ID Token` è `ID Token`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `ID Token` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `ID Token` è `SGI-WP1-Specifiche di integrazione autenticazione APMS-Team di sviluppo-v1.4.1.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `ID Token` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `ID Token` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `ID Token` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `ID Token` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `ID Token` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `ID Token` possiede l'identificatore esatto `apms_id_token`. Nel grafo documentale, Graphify assegna il nodo canonico `ID Token` alla comunità numerica `16`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `ID Token` è `id token`.
Nel grafo documentale, il nodo canonico `ID Token` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=documentale index=61 role=incoming -->
Nel grafo documentale, il nodo canonico `ID Token` è il destinatario `target` della relazione `conceptually_related_to` proveniente dal nodo canonico `Authorization Code Flow`; l'orientamento registrato nel JSON va dall'ID `apms_authorization_code_flow` all'ID `apms_id_token`. La relazione tecnica esplicita è la seguente: `Authorization Code Flow` è concettualmente correlato a `ID Token`. Nel grafo documentale, il tipo dichiarato dell'arco 61 che coinvolge il nodo canonico `ID Token` è `conceptually_related_to`. Nel grafo documentale, la confidenza categoriale dell'arco 61 che coinvolge il nodo canonico `ID Token` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 61 che coinvolge il nodo canonico `ID Token` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 61 che coinvolge il nodo canonico `ID Token` è `SGI-WP1-Specifiche di integrazione autenticazione APMS-Team di sviluppo-v1.4.1.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 61 che coinvolge il nodo canonico `ID Token` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 61 che coinvolge il nodo canonico `ID Token` è `1.0`. Nel grafo documentale, l'arco 61 registra come `source` l'identificatore `apms_authorization_code_flow` per la relazione che coinvolge il nodo canonico `ID Token`. Nel grafo documentale, l'arco 61 registra come `target` l'identificatore `apms_id_token` per la relazione che coinvolge il nodo canonico `ID Token`.

<!-- GRAPH_NODE graph=unificata index=603 id=graphify_kb::apms_id_token -->
Nel grafo unificato, l'etichetta originale del nodo canonico `ID Token` è `ID Token`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `ID Token` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `ID Token` è `SGI-WP1-Specifiche di integrazione autenticazione APMS-Team di sviluppo-v1.4.1.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `ID Token` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `ID Token` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `ID Token` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `ID Token` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `ID Token` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `ID Token` alla comunità numerica `16`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `ID Token` è `id token`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `ID Token` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `ID Token` è `apms_id_token`. Nel grafo unificato, il nodo canonico `ID Token` possiede l'identificatore esatto `graphify_kb::apms_id_token`.
Nel grafo unificato, il nodo canonico `ID Token` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1089 role=incoming -->
Nel grafo unificato, il nodo canonico `ID Token` è il destinatario `target` della relazione `conceptually_related_to` proveniente dal nodo canonico `Authorization Code Flow`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::apms_authorization_code_flow` all'ID `graphify_kb::apms_id_token`. La relazione tecnica esplicita è la seguente: `Authorization Code Flow` è concettualmente correlato a `ID Token`. Nel grafo unificato, il tipo dichiarato dell'arco 1089 che coinvolge il nodo canonico `ID Token` è `conceptually_related_to`. Nel grafo unificato, la confidenza categoriale dell'arco 1089 che coinvolge il nodo canonico `ID Token` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1089 che coinvolge il nodo canonico `ID Token` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1089 che coinvolge il nodo canonico `ID Token` è `SGI-WP1-Specifiche di integrazione autenticazione APMS-Team di sviluppo-v1.4.1.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1089 che coinvolge il nodo canonico `ID Token` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1089 che coinvolge il nodo canonico `ID Token` è `1.0`. Nel grafo unificato, l'arco 1089 registra come `source` l'identificatore `graphify_kb::apms_authorization_code_flow` per la relazione che coinvolge il nodo canonico `ID Token`. Nel grafo unificato, l'arco 1089 registra come `target` l'identificatore `graphify_kb::apms_id_token` per la relazione che coinvolge il nodo canonico `ID Token`.

## Cluster Graphify 17 — eIDAS Signatures

Il cluster editoriale Graphify 17 del dominio Interoperabilità tecnico-normativa contiene 2 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### eIDAS

<!-- GRAPH_NODE graph=documentale index=41 id=eidas -->
Nel grafo documentale, l'etichetta originale del nodo canonico `eIDAS` è `eIDAS`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `eIDAS` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `eIDAS` è `Firme e sigilli elettronici 1.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `eIDAS` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `eIDAS` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `eIDAS` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `eIDAS` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `eIDAS` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `eIDAS` possiede l'identificatore esatto `eidas`. Nel grafo documentale, Graphify assegna il nodo canonico `eIDAS` alla comunità numerica `17`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `eIDAS` è `eidas`.
Nel grafo documentale, il nodo canonico `eIDAS` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=documentale index=41 role=incoming -->
Nel grafo documentale, il nodo canonico `eIDAS` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Firme e Sigilli Elettronici`; l'orientamento registrato nel JSON va dall'ID `eidas_signatures` all'ID `eidas`. La relazione tecnica esplicita è la seguente: `Firme e Sigilli Elettronici` fa riferimento a `eIDAS`. Nel grafo documentale, il tipo dichiarato dell'arco 41 che coinvolge il nodo canonico `eIDAS` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 41 che coinvolge il nodo canonico `eIDAS` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 41 che coinvolge il nodo canonico `eIDAS` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 41 che coinvolge il nodo canonico `eIDAS` è `Firme e sigilli elettronici 1.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 41 che coinvolge il nodo canonico `eIDAS` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 41 che coinvolge il nodo canonico `eIDAS` è `1.0`. Nel grafo documentale, l'arco 41 registra come `source` l'identificatore `eidas_signatures` per la relazione che coinvolge il nodo canonico `eIDAS`. Nel grafo documentale, l'arco 41 registra come `target` l'identificatore `eidas` per la relazione che coinvolge il nodo canonico `eIDAS`.

<!-- GRAPH_NODE graph=unificata index=574 id=graphify_kb::eidas -->
Nel grafo unificato, l'etichetta originale del nodo canonico `eIDAS` è `eIDAS`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `eIDAS` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `eIDAS` è `Firme e sigilli elettronici 1.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `eIDAS` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `eIDAS` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `eIDAS` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `eIDAS` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `eIDAS` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `eIDAS` alla comunità numerica `17`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `eIDAS` è `eidas`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `eIDAS` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `eIDAS` è `eidas`. Nel grafo unificato, il nodo canonico `eIDAS` possiede l'identificatore esatto `graphify_kb::eidas`.
Nel grafo unificato, il nodo canonico `eIDAS` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1069 role=incoming -->
Nel grafo unificato, il nodo canonico `eIDAS` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Firme e Sigilli Elettronici`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::eidas_signatures` all'ID `graphify_kb::eidas`. La relazione tecnica esplicita è la seguente: `Firme e Sigilli Elettronici` fa riferimento a `eIDAS`. Nel grafo unificato, il tipo dichiarato dell'arco 1069 che coinvolge il nodo canonico `eIDAS` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1069 che coinvolge il nodo canonico `eIDAS` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1069 che coinvolge il nodo canonico `eIDAS` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1069 che coinvolge il nodo canonico `eIDAS` è `Firme e sigilli elettronici 1.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1069 che coinvolge il nodo canonico `eIDAS` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1069 che coinvolge il nodo canonico `eIDAS` è `1.0`. Nel grafo unificato, l'arco 1069 registra come `source` l'identificatore `graphify_kb::eidas_signatures` per la relazione che coinvolge il nodo canonico `eIDAS`. Nel grafo unificato, l'arco 1069 registra come `target` l'identificatore `graphify_kb::eidas` per la relazione che coinvolge il nodo canonico `eIDAS`.

### Firme e Sigilli Elettronici

<!-- GRAPH_NODE graph=documentale index=40 id=eidas_signatures -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Firme e Sigilli Elettronici` è `Firme e Sigilli Elettronici`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Firme e Sigilli Elettronici` come `document`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Firme e Sigilli Elettronici` è `Firme e sigilli elettronici 1.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Firme e Sigilli Elettronici` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Firme e Sigilli Elettronici` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Firme e Sigilli Elettronici` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Firme e Sigilli Elettronici` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Firme e Sigilli Elettronici` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Firme e Sigilli Elettronici` possiede l'identificatore esatto `eidas_signatures`. Nel grafo documentale, Graphify assegna il nodo canonico `Firme e Sigilli Elettronici` alla comunità numerica `17`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Firme e Sigilli Elettronici` è `firme e sigilli elettronici`.
<!-- GRAPH_EDGE graph=documentale index=41 role=outgoing -->
Nel grafo documentale, il nodo canonico `Firme e Sigilli Elettronici` è la sorgente `source` della relazione `references` verso il nodo canonico `eIDAS`; l'orientamento registrato nel JSON va dall'ID `eidas_signatures` all'ID `eidas`. La relazione tecnica esplicita è la seguente: `Firme e Sigilli Elettronici` fa riferimento a `eIDAS`. Nel grafo documentale, il tipo dichiarato dell'arco 41 che coinvolge il nodo canonico `Firme e Sigilli Elettronici` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 41 che coinvolge il nodo canonico `Firme e Sigilli Elettronici` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 41 che coinvolge il nodo canonico `Firme e Sigilli Elettronici` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 41 che coinvolge il nodo canonico `Firme e Sigilli Elettronici` è `Firme e sigilli elettronici 1.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 41 che coinvolge il nodo canonico `Firme e Sigilli Elettronici` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 41 che coinvolge il nodo canonico `Firme e Sigilli Elettronici` è `1.0`. Nel grafo documentale, l'arco 41 registra come `source` l'identificatore `eidas_signatures` per la relazione che coinvolge il nodo canonico `Firme e Sigilli Elettronici`. Nel grafo documentale, l'arco 41 registra come `target` l'identificatore `eidas` per la relazione che coinvolge il nodo canonico `Firme e Sigilli Elettronici`.
Nel grafo documentale, il nodo canonico `Firme e Sigilli Elettronici` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=573 id=graphify_kb::eidas_signatures -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Firme e Sigilli Elettronici` è `Firme e Sigilli Elettronici`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Firme e Sigilli Elettronici` come `document`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Firme e Sigilli Elettronici` è `Firme e sigilli elettronici 1.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Firme e Sigilli Elettronici` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Firme e Sigilli Elettronici` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Firme e Sigilli Elettronici` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Firme e Sigilli Elettronici` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Firme e Sigilli Elettronici` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Firme e Sigilli Elettronici` alla comunità numerica `17`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Firme e Sigilli Elettronici` è `firme e sigilli elettronici`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Firme e Sigilli Elettronici` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Firme e Sigilli Elettronici` è `eidas_signatures`. Nel grafo unificato, il nodo canonico `Firme e Sigilli Elettronici` possiede l'identificatore esatto `graphify_kb::eidas_signatures`.
<!-- GRAPH_EDGE graph=unificata index=1069 role=outgoing -->
Nel grafo unificato, il nodo canonico `Firme e Sigilli Elettronici` è la sorgente `source` della relazione `references` verso il nodo canonico `eIDAS`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::eidas_signatures` all'ID `graphify_kb::eidas`. La relazione tecnica esplicita è la seguente: `Firme e Sigilli Elettronici` fa riferimento a `eIDAS`. Nel grafo unificato, il tipo dichiarato dell'arco 1069 che coinvolge il nodo canonico `Firme e Sigilli Elettronici` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1069 che coinvolge il nodo canonico `Firme e Sigilli Elettronici` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1069 che coinvolge il nodo canonico `Firme e Sigilli Elettronici` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1069 che coinvolge il nodo canonico `Firme e Sigilli Elettronici` è `Firme e sigilli elettronici 1.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1069 che coinvolge il nodo canonico `Firme e Sigilli Elettronici` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1069 che coinvolge il nodo canonico `Firme e Sigilli Elettronici` è `1.0`. Nel grafo unificato, l'arco 1069 registra come `source` l'identificatore `graphify_kb::eidas_signatures` per la relazione che coinvolge il nodo canonico `Firme e Sigilli Elettronici`. Nel grafo unificato, l'arco 1069 registra come `target` l'identificatore `graphify_kb::eidas` per la relazione che coinvolge il nodo canonico `Firme e Sigilli Elettronici`.
Nel grafo unificato, il nodo canonico `Firme e Sigilli Elettronici` non possiede archi entranti registrati nel campo `links`.

## Cluster Graphify 18 — Qualified Trust Services

Il cluster editoriale Graphify 18 del dominio Interoperabilità tecnico-normativa contiene 2 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### Firma Elettronica Qualificata

<!-- GRAPH_NODE graph=documentale index=42 id=feq -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Firma Elettronica Qualificata` è `Firma Elettronica Qualificata`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Firma Elettronica Qualificata` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Firma Elettronica Qualificata` è `Firme e sigilli elettronici 1.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Firma Elettronica Qualificata` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Firma Elettronica Qualificata` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Firma Elettronica Qualificata` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Firma Elettronica Qualificata` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Firma Elettronica Qualificata` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Firma Elettronica Qualificata` possiede l'identificatore esatto `feq`. Nel grafo documentale, Graphify assegna il nodo canonico `Firma Elettronica Qualificata` alla comunità numerica `18`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Firma Elettronica Qualificata` è `firma elettronica qualificata`.
<!-- GRAPH_EDGE graph=documentale index=42 role=outgoing -->
Nel grafo documentale, il nodo canonico `Firma Elettronica Qualificata` è la sorgente `source` della relazione `references` verso il nodo canonico `Qualified Trust Service Provider`; l'orientamento registrato nel JSON va dall'ID `feq` all'ID `qtsp`. La relazione tecnica esplicita è la seguente: `Firma Elettronica Qualificata` fa riferimento a `Qualified Trust Service Provider`. Nel grafo documentale, il tipo dichiarato dell'arco 42 che coinvolge il nodo canonico `Firma Elettronica Qualificata` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 42 che coinvolge il nodo canonico `Firma Elettronica Qualificata` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 42 che coinvolge il nodo canonico `Firma Elettronica Qualificata` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 42 che coinvolge il nodo canonico `Firma Elettronica Qualificata` è `Firme e sigilli elettronici 1.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 42 che coinvolge il nodo canonico `Firma Elettronica Qualificata` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 42 che coinvolge il nodo canonico `Firma Elettronica Qualificata` è `1.0`. Nel grafo documentale, l'arco 42 registra come `source` l'identificatore `feq` per la relazione che coinvolge il nodo canonico `Firma Elettronica Qualificata`. Nel grafo documentale, l'arco 42 registra come `target` l'identificatore `qtsp` per la relazione che coinvolge il nodo canonico `Firma Elettronica Qualificata`.
Nel grafo documentale, il nodo canonico `Firma Elettronica Qualificata` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=575 id=graphify_kb::feq -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Firma Elettronica Qualificata` è `Firma Elettronica Qualificata`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Firma Elettronica Qualificata` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Firma Elettronica Qualificata` è `Firme e sigilli elettronici 1.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Firma Elettronica Qualificata` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Firma Elettronica Qualificata` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Firma Elettronica Qualificata` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Firma Elettronica Qualificata` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Firma Elettronica Qualificata` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Firma Elettronica Qualificata` alla comunità numerica `18`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Firma Elettronica Qualificata` è `firma elettronica qualificata`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Firma Elettronica Qualificata` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Firma Elettronica Qualificata` è `feq`. Nel grafo unificato, il nodo canonico `Firma Elettronica Qualificata` possiede l'identificatore esatto `graphify_kb::feq`.
<!-- GRAPH_EDGE graph=unificata index=1070 role=outgoing -->
Nel grafo unificato, il nodo canonico `Firma Elettronica Qualificata` è la sorgente `source` della relazione `references` verso il nodo canonico `Qualified Trust Service Provider`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::feq` all'ID `graphify_kb::qtsp`. La relazione tecnica esplicita è la seguente: `Firma Elettronica Qualificata` fa riferimento a `Qualified Trust Service Provider`. Nel grafo unificato, il tipo dichiarato dell'arco 1070 che coinvolge il nodo canonico `Firma Elettronica Qualificata` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1070 che coinvolge il nodo canonico `Firma Elettronica Qualificata` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1070 che coinvolge il nodo canonico `Firma Elettronica Qualificata` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1070 che coinvolge il nodo canonico `Firma Elettronica Qualificata` è `Firme e sigilli elettronici 1.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1070 che coinvolge il nodo canonico `Firma Elettronica Qualificata` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1070 che coinvolge il nodo canonico `Firma Elettronica Qualificata` è `1.0`. Nel grafo unificato, l'arco 1070 registra come `source` l'identificatore `graphify_kb::feq` per la relazione che coinvolge il nodo canonico `Firma Elettronica Qualificata`. Nel grafo unificato, l'arco 1070 registra come `target` l'identificatore `graphify_kb::qtsp` per la relazione che coinvolge il nodo canonico `Firma Elettronica Qualificata`.
Nel grafo unificato, il nodo canonico `Firma Elettronica Qualificata` non possiede archi entranti registrati nel campo `links`.

### Qualified Trust Service Provider

<!-- GRAPH_NODE graph=documentale index=43 id=qtsp -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Qualified Trust Service Provider` è `Qualified Trust Service Provider`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Qualified Trust Service Provider` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Qualified Trust Service Provider` è `Firme e sigilli elettronici 1.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Qualified Trust Service Provider` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Qualified Trust Service Provider` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Qualified Trust Service Provider` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Qualified Trust Service Provider` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Qualified Trust Service Provider` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Qualified Trust Service Provider` possiede l'identificatore esatto `qtsp`. Nel grafo documentale, Graphify assegna il nodo canonico `Qualified Trust Service Provider` alla comunità numerica `18`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Qualified Trust Service Provider` è `qualified trust service provider`.
Nel grafo documentale, il nodo canonico `Qualified Trust Service Provider` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=documentale index=42 role=incoming -->
Nel grafo documentale, il nodo canonico `Qualified Trust Service Provider` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Firma Elettronica Qualificata`; l'orientamento registrato nel JSON va dall'ID `feq` all'ID `qtsp`. La relazione tecnica esplicita è la seguente: `Firma Elettronica Qualificata` fa riferimento a `Qualified Trust Service Provider`. Nel grafo documentale, il tipo dichiarato dell'arco 42 che coinvolge il nodo canonico `Qualified Trust Service Provider` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 42 che coinvolge il nodo canonico `Qualified Trust Service Provider` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 42 che coinvolge il nodo canonico `Qualified Trust Service Provider` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 42 che coinvolge il nodo canonico `Qualified Trust Service Provider` è `Firme e sigilli elettronici 1.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 42 che coinvolge il nodo canonico `Qualified Trust Service Provider` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 42 che coinvolge il nodo canonico `Qualified Trust Service Provider` è `1.0`. Nel grafo documentale, l'arco 42 registra come `source` l'identificatore `feq` per la relazione che coinvolge il nodo canonico `Qualified Trust Service Provider`. Nel grafo documentale, l'arco 42 registra come `target` l'identificatore `qtsp` per la relazione che coinvolge il nodo canonico `Qualified Trust Service Provider`.

<!-- GRAPH_NODE graph=unificata index=576 id=graphify_kb::qtsp -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Qualified Trust Service Provider` è `Qualified Trust Service Provider`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Qualified Trust Service Provider` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Qualified Trust Service Provider` è `Firme e sigilli elettronici 1.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Qualified Trust Service Provider` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Qualified Trust Service Provider` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Qualified Trust Service Provider` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Qualified Trust Service Provider` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Qualified Trust Service Provider` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Qualified Trust Service Provider` alla comunità numerica `18`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Qualified Trust Service Provider` è `qualified trust service provider`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Qualified Trust Service Provider` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Qualified Trust Service Provider` è `qtsp`. Nel grafo unificato, il nodo canonico `Qualified Trust Service Provider` possiede l'identificatore esatto `graphify_kb::qtsp`.
Nel grafo unificato, il nodo canonico `Qualified Trust Service Provider` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1070 role=incoming -->
Nel grafo unificato, il nodo canonico `Qualified Trust Service Provider` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Firma Elettronica Qualificata`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::feq` all'ID `graphify_kb::qtsp`. La relazione tecnica esplicita è la seguente: `Firma Elettronica Qualificata` fa riferimento a `Qualified Trust Service Provider`. Nel grafo unificato, il tipo dichiarato dell'arco 1070 che coinvolge il nodo canonico `Qualified Trust Service Provider` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1070 che coinvolge il nodo canonico `Qualified Trust Service Provider` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1070 che coinvolge il nodo canonico `Qualified Trust Service Provider` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1070 che coinvolge il nodo canonico `Qualified Trust Service Provider` è `Firme e sigilli elettronici 1.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1070 che coinvolge il nodo canonico `Qualified Trust Service Provider` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1070 che coinvolge il nodo canonico `Qualified Trust Service Provider` è `1.0`. Nel grafo unificato, l'arco 1070 registra come `source` l'identificatore `graphify_kb::feq` per la relazione che coinvolge il nodo canonico `Qualified Trust Service Provider`. Nel grafo unificato, l'arco 1070 registra come `target` l'identificatore `graphify_kb::qtsp` per la relazione che coinvolge il nodo canonico `Qualified Trust Service Provider`.

## Cluster Graphify 19 — Territorial Health Flows

Il cluster editoriale Graphify 19 del dominio Interoperabilità tecnico-normativa contiene 2 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### Flussi NSIS

<!-- GRAPH_NODE graph=documentale index=58 id=percorsi_flussi_nsis -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Flussi NSIS` è `Flussi NSIS`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Flussi NSIS` come `concept`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Flussi NSIS` è `Percorsi, Sistemi e Flussi Sanitari dei Servizi Territoriali_v3.0.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Flussi NSIS` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Flussi NSIS` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Flussi NSIS` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Flussi NSIS` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Flussi NSIS` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Flussi NSIS` possiede l'identificatore esatto `percorsi_flussi_nsis`. Nel grafo documentale, Graphify assegna il nodo canonico `Flussi NSIS` alla comunità numerica `19`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Flussi NSIS` è `flussi nsis`.
Nel grafo documentale, il nodo canonico `Flussi NSIS` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=documentale index=54 role=incoming -->
Nel grafo documentale, il nodo canonico `Flussi NSIS` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Percorsi Sistemi e Flussi Sanitari`; l'orientamento registrato nel JSON va dall'ID `percorsi_flussi_sanitari` all'ID `percorsi_flussi_nsis`. La relazione tecnica esplicita è la seguente: `Percorsi Sistemi e Flussi Sanitari` fa riferimento a `Flussi NSIS`. Nel grafo documentale, il tipo dichiarato dell'arco 54 che coinvolge il nodo canonico `Flussi NSIS` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 54 che coinvolge il nodo canonico `Flussi NSIS` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 54 che coinvolge il nodo canonico `Flussi NSIS` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 54 che coinvolge il nodo canonico `Flussi NSIS` è `Percorsi, Sistemi e Flussi Sanitari dei Servizi Territoriali_v3.0.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 54 che coinvolge il nodo canonico `Flussi NSIS` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 54 che coinvolge il nodo canonico `Flussi NSIS` è `1.0`. Nel grafo documentale, l'arco 54 registra come `source` l'identificatore `percorsi_flussi_sanitari` per la relazione che coinvolge il nodo canonico `Flussi NSIS`. Nel grafo documentale, l'arco 54 registra come `target` l'identificatore `percorsi_flussi_nsis` per la relazione che coinvolge il nodo canonico `Flussi NSIS`.

<!-- GRAPH_NODE graph=unificata index=591 id=graphify_kb::percorsi_flussi_nsis -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Flussi NSIS` è `Flussi NSIS`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Flussi NSIS` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Flussi NSIS` è `Percorsi, Sistemi e Flussi Sanitari dei Servizi Territoriali_v3.0.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Flussi NSIS` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Flussi NSIS` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Flussi NSIS` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Flussi NSIS` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Flussi NSIS` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Flussi NSIS` alla comunità numerica `19`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Flussi NSIS` è `flussi nsis`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Flussi NSIS` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Flussi NSIS` è `percorsi_flussi_nsis`. Nel grafo unificato, il nodo canonico `Flussi NSIS` possiede l'identificatore esatto `graphify_kb::percorsi_flussi_nsis`.
Nel grafo unificato, il nodo canonico `Flussi NSIS` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1082 role=incoming -->
Nel grafo unificato, il nodo canonico `Flussi NSIS` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Percorsi Sistemi e Flussi Sanitari`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::percorsi_flussi_sanitari` all'ID `graphify_kb::percorsi_flussi_nsis`. La relazione tecnica esplicita è la seguente: `Percorsi Sistemi e Flussi Sanitari` fa riferimento a `Flussi NSIS`. Nel grafo unificato, il tipo dichiarato dell'arco 1082 che coinvolge il nodo canonico `Flussi NSIS` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1082 che coinvolge il nodo canonico `Flussi NSIS` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1082 che coinvolge il nodo canonico `Flussi NSIS` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1082 che coinvolge il nodo canonico `Flussi NSIS` è `Percorsi, Sistemi e Flussi Sanitari dei Servizi Territoriali_v3.0.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1082 che coinvolge il nodo canonico `Flussi NSIS` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1082 che coinvolge il nodo canonico `Flussi NSIS` è `1.0`. Nel grafo unificato, l'arco 1082 registra come `source` l'identificatore `graphify_kb::percorsi_flussi_sanitari` per la relazione che coinvolge il nodo canonico `Flussi NSIS`. Nel grafo unificato, l'arco 1082 registra come `target` l'identificatore `graphify_kb::percorsi_flussi_nsis` per la relazione che coinvolge il nodo canonico `Flussi NSIS`.

### Percorsi Sistemi e Flussi Sanitari

<!-- GRAPH_NODE graph=documentale index=57 id=percorsi_flussi_sanitari -->
Nel grafo documentale, l'etichetta originale del nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `Percorsi Sistemi e Flussi Sanitari`. Nel grafo documentale, la proprietà `file_type` tipizza il nodo canonico `Percorsi Sistemi e Flussi Sanitari` come `document`. Nel grafo documentale, il file sorgente registrato per il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `Percorsi, Sistemi e Flussi Sanitari dei Servizi Territoriali_v3.0.pdf`. Nel grafo documentale, la posizione sorgente registrata per il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo documentale, l'URL sorgente registrato per il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'istante di acquisizione registrato per il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, l'autore registrato per il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il contributore registrato per il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo documentale, il nodo canonico `Percorsi Sistemi e Flussi Sanitari` possiede l'identificatore esatto `percorsi_flussi_sanitari`. Nel grafo documentale, Graphify assegna il nodo canonico `Percorsi Sistemi e Flussi Sanitari` alla comunità numerica `19`. Nel grafo documentale, l'etichetta normalizzata del nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `percorsi sistemi e flussi sanitari`.
<!-- GRAPH_EDGE graph=documentale index=54 role=outgoing -->
Nel grafo documentale, il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è la sorgente `source` della relazione `references` verso il nodo canonico `Flussi NSIS`; l'orientamento registrato nel JSON va dall'ID `percorsi_flussi_sanitari` all'ID `percorsi_flussi_nsis`. La relazione tecnica esplicita è la seguente: `Percorsi Sistemi e Flussi Sanitari` fa riferimento a `Flussi NSIS`. Nel grafo documentale, il tipo dichiarato dell'arco 54 che coinvolge il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `references`. Nel grafo documentale, la confidenza categoriale dell'arco 54 che coinvolge il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `EXTRACTED`. Nel grafo documentale, il punteggio numerico di confidenza dell'arco 54 che coinvolge il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `1.0`. Nel grafo documentale, il file sorgente dell'arco 54 che coinvolge il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `Percorsi, Sistemi e Flussi Sanitari dei Servizi Territoriali_v3.0.pdf`. Nel grafo documentale, la posizione sorgente dell'arco 54 che coinvolge il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `null`; `null` segnala una posizione non valorizzata. Nel grafo documentale, il peso dell'arco 54 che coinvolge il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `1.0`. Nel grafo documentale, l'arco 54 registra come `source` l'identificatore `percorsi_flussi_sanitari` per la relazione che coinvolge il nodo canonico `Percorsi Sistemi e Flussi Sanitari`. Nel grafo documentale, l'arco 54 registra come `target` l'identificatore `percorsi_flussi_nsis` per la relazione che coinvolge il nodo canonico `Percorsi Sistemi e Flussi Sanitari`.
Nel grafo documentale, il nodo canonico `Percorsi Sistemi e Flussi Sanitari` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=590 id=graphify_kb::percorsi_flussi_sanitari -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `Percorsi Sistemi e Flussi Sanitari`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Percorsi Sistemi e Flussi Sanitari` come `document`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `Percorsi, Sistemi e Flussi Sanitari dei Servizi Territoriali_v3.0.pdf`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `Percorsi Sistemi e Flussi Sanitari` alla comunità numerica `19`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `percorsi sistemi e flussi sanitari`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Percorsi Sistemi e Flussi Sanitari` al repository `graphify_kb`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `percorsi_flussi_sanitari`. Nel grafo unificato, il nodo canonico `Percorsi Sistemi e Flussi Sanitari` possiede l'identificatore esatto `graphify_kb::percorsi_flussi_sanitari`.
<!-- GRAPH_EDGE graph=unificata index=1082 role=outgoing -->
Nel grafo unificato, il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è la sorgente `source` della relazione `references` verso il nodo canonico `Flussi NSIS`; l'orientamento registrato nel JSON va dall'ID `graphify_kb::percorsi_flussi_sanitari` all'ID `graphify_kb::percorsi_flussi_nsis`. La relazione tecnica esplicita è la seguente: `Percorsi Sistemi e Flussi Sanitari` fa riferimento a `Flussi NSIS`. Nel grafo unificato, il tipo dichiarato dell'arco 1082 che coinvolge il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1082 che coinvolge il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1082 che coinvolge il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1082 che coinvolge il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `Percorsi, Sistemi e Flussi Sanitari dei Servizi Territoriali_v3.0.pdf`. Nel grafo unificato, la posizione sorgente dell'arco 1082 che coinvolge il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1082 che coinvolge il nodo canonico `Percorsi Sistemi e Flussi Sanitari` è `1.0`. Nel grafo unificato, l'arco 1082 registra come `source` l'identificatore `graphify_kb::percorsi_flussi_sanitari` per la relazione che coinvolge il nodo canonico `Percorsi Sistemi e Flussi Sanitari`. Nel grafo unificato, l'arco 1082 registra come `target` l'identificatore `graphify_kb::percorsi_flussi_nsis` per la relazione che coinvolge il nodo canonico `Percorsi Sistemi e Flussi Sanitari`.
Nel grafo unificato, il nodo canonico `Percorsi Sistemi e Flussi Sanitari` non possiede archi entranti registrati nel campo `links`.
