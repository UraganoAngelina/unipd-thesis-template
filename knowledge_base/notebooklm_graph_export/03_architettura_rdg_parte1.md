# Architettura e codice del framework RDG — parte 1

## Metadati

- Dominio: Architettura e codice del framework RDG.
- Data di generazione: 2026-07-31.
- Versione dei grafi sorgente: KB documentale `5f9434fc0f01de5d74dd3c6d72fffc217b7f8629`; KB codice RDG `06d5dd91cef2e12574b63a36d73b049a3588cdf8`; KB unificata `null`.
- Nodi coperti: 78 nodi canonici e 156 occorrenze fisiche; tipi canonici: code: 63, rationale: 15.
- Archi coperti: 354 occorrenze fisiche incidenti ai nodi del documento; tipi: calls: 152, contains: 66, imports: 6, imports_from: 4, inherits: 4, method: 48, rationale_for: 30, references: 40, uses: 4.
- Iperarchi coperti: 0 occorrenze fisiche, replicate anche negli altri file coinvolti quando attraversano la segmentazione editoriale.
- Nota sulla direzione: tutti e tre i grafi dichiarano `directed=false`; la prosa conserva comunque l'orientamento dei campi JSON `source` e `target` senza trasformarlo in causalità.

## Glossario

- EXTRACTED — Valore della proprietà `confidence` che il grafo assegna a una relazione estratta.
- INFERRED — Valore della proprietà `confidence` che il grafo assegna a una relazione inferita; la prosa non promuove una relazione INFERRED a fatto estratto.
- RDG — Nome editoriale canonico del repository Request-Dataset-Generator.
- AST — Valore di provenienza `_origin=ast` registrato dal grafo.
- FHIR — Nome canonico dell'acronimo usato negli identificatori RDG.
- RVE — Nome canonico del prefisso usato per i flussi RDG.
- UI — Nome canonico dell'acronimo usato nei moduli di interfaccia.

### Mappa dei nomi canonici e delle varianti

La mappa seguente non fonde nodi solo perché semanticamente simili. Le occorrenze del grafo sorgente e del grafo unificato sono ricondotte allo stesso nome soltanto tramite `repo` e `local_id`; le etichette duplicate di nodi distinti sono disambiguate con l'identificatore locale.

- Il nome canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_init`; le etichette originali conservate sono `.__init__()`.
- Il nome canonico `.__init__() [traffic_exec_engine_executionlogger_init]` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_executionlogger_init`; le etichette originali conservate sono `.__init__()`.
- Il nome canonico `.__init__() [traffic_exec_engine_faultinjector_init]` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_faultinjector_init`; le etichette originali conservate sono `.__init__()`.
- Il nome canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_init`; le etichette originali conservate sono `.__init__()`.
- Il nome canonico `._emit()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit`; le etichette originali conservate sono `._emit()`.
- Il nome canonico `._load_pkcs12()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_load_pkcs12`; le etichette originali conservate sono `._load_pkcs12()`.
- Il nome canonico `._matches()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_faultinjector_matches`; le etichette originali conservate sono `._matches()`.
- Il nome canonico `.flow_finished()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_finished`; le etichette originali conservate sono `.flow_finished()`.
- Il nome canonico `.flow_started()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_started`; le etichette originali conservate sono `.flow_started()`.
- Il nome canonico `.flush()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_executionlogger_flush`; le etichette originali conservate sono `.flush()`.
- Il nome canonico `.get()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`; le etichette originali conservate sono `.get()`.
- Il nome canonico `.log_flow_completed()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_flow_completed`; le etichette originali conservate sono `.log_flow_completed()`.
- Il nome canonico `.log_request_sent()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent`; le etichette originali conservate sono `.log_request_sent()`.
- Il nome canonico `.log_response_received()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received`; le etichette originali conservate sono `.log_response_received()`.
- Il nome canonico `.plan()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan`; le etichette originali conservate sono `.plan()`.
- Il nome canonico `.step_finished()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_finished`; le etichette originali conservate sono `.step_finished()`.
- Il nome canonico `.step_started()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_started`; le etichette originali conservate sono `.step_started()`.
- Il nome canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_summary`; le etichette originali conservate sono `.summary()`.
- Il nome canonico `.summary() [traffic_exec_engine_executionlogger_summary]` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_executionlogger_summary`; le etichette originali conservate sono `.summary()`.
- Il nome canonico `.test_detects_unresolved_placeholders()` identifica il nodo `Request-Dataset-Generator::tests_test_core_placeholdertest_test_detects_unresolved_placeholders`; le etichette originali conservate sono `.test_detects_unresolved_placeholders()`.
- Il nome canonico `.test_extracts_saml_assertion_as_base64()` identifica il nodo `Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64`; le etichette originali conservate sono `.test_extracts_saml_assertion_as_base64()`.
- Il nome canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` identifica il nodo `Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64`; le etichette originali conservate sono `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`.
- Il nome canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` identifica il nodo `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback`; le etichette originali conservate sono `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`.
- Il nome canonico `.test_resolves_placeholders_in_url_headers_and_body()` identifica il nodo `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body`; le etichette originali conservate sono `.test_resolves_placeholders_in_url_headers_and_body()`.
- Il nome canonico `_build_parser() [traffic_exec_engine_build_parser]` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_build_parser`; le etichette originali conservate sono `_build_parser()`.
- Il nome canonico `_correlation_headers()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_correlation_headers`; le etichette originali conservate sono `_correlation_headers()`.
- Il nome canonico `_extract_json_output()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_extract_json_output`; le etichette originali conservate sono `_extract_json_output()`.
- Il nome canonico `_extract_xml_output()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output`; le etichette originali conservate sono `_extract_xml_output()`.
- Il nome canonico `_extract_xml_output_stdlib()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib`; le etichette originali conservate sono `_extract_xml_output_stdlib()`.
- Il nome canonico `_find_first_by_local_name()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_find_first_by_local_name`; le etichette originali conservate sono `_find_first_by_local_name()`.
- Il nome canonico `_linearize_xml()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_linearize_xml`; le etichette originali conservate sono `_linearize_xml()`.
- Il nome canonico `_now_epoch_ms()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_now_epoch_ms`; le etichette originali conservate sono `_now_epoch_ms()`.
- Il nome canonico `_now_iso()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_now_iso`; le etichette originali conservate sono `_now_iso()`.
- Il nome canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_replace_newlines_inside_tags`; le etichette originali conservate sono `_replace_newlines_inside_tags()`.
- Il nome canonico `_resolve_secret()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_resolve_secret`; le etichette originali conservate sono `_resolve_secret()`.
- Il nome canonico `_response_size()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_response_size`; le etichette originali conservate sono `_response_size()`.
- Il nome canonico `_stable_unit_interval()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_stable_unit_interval`; le etichette originali conservate sono `_stable_unit_interval()`.
- Il nome canonico `_xml_raw_string()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string`; le etichette originali conservate sono `_xml_raw_string()`.
- Il nome canonico `Any [traffic_exec_engine_py_any]` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_py_any`; le etichette originali conservate sono `Any`.
- Il nome canonico `Applica fault injection deterministica a livello step.` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_rationale_472`; le etichette originali conservate sono `Applica fault injection deterministica a livello step.`.
- Il nome canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_py_argumentparser`; le etichette originali conservate sono `ArgumentParser`.
- Il nome canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_rationale_1098`; le etichette originali conservate sono `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t`.
- Il nome canonico `ConcurrencyTracker` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker`; le etichette originali conservate sono `ConcurrencyTracker`.
- Il nome canonico `Controlla gli output marcati required nello step.` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_rationale_332`; le etichette originali conservate sono `Controlla gli output marcati required nello step.`.
- Il nome canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_rationale_351`; le etichette originali conservate sono `Costruisce gli header di correlazione da iniettare in ogni request live.      No`.
- Il nome canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_rationale_374`; le etichette originali conservate sono `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co`.
- Il nome canonico `Element [traffic_exec_engine_py_element]` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_py_element`; le etichette originali conservate sono `Element`.
- Il nome canonico `Errore esplicito nella fase di estrazione output da response.` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_rationale_48`; le etichette originali conservate sono `Errore esplicito nella fase di estrazione output da response.`.
- Il nome canonico `Errore esplicito quando un placeholder live resta non risolto.` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_rationale_52`; le etichette originali conservate sono `Errore esplicito quando un placeholder live resta non risolto.`.
- Il nome canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_rationale_824`; le etichette originali conservate sono `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol`.
- Il nome canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_rationale_159`; le etichette originali conservate sono `Estrae valori dalla response secondo le specifiche output_extract dello step.`.
- Il nome canonico `execute_flow()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_execute_flow`; le etichette originali conservate sono `execute_flow()`.
- Il nome canonico `ExecutionLogger` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_executionlogger`; le etichette originali conservate sono `ExecutionLogger`.
- Il nome canonico `extract_outputs()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_extract_outputs`; le etichette originali conservate sono `extract_outputs()`.
- Il nome canonico `ExtractionTest` identifica il nodo `Request-Dataset-Generator::tests_test_core_extractiontest`; le etichette originali conservate sono `ExtractionTest`.
- Il nome canonico `FaultInjector` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_faultinjector`; le etichette originali conservate sono `FaultInjector`.
- Il nome canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_rationale_549`; le etichette originali conservate sono `Invia la HTTP request definita nello step.      Inietta automaticamente gli head`.
- Il nome canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_rationale_367`; le etichette originali conservate sono `Legge un segreto da env var se configurata, altrimenti dal config locale.`.
- Il nome canonico `load_config() [traffic_exec_engine_load_config]` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_load_config`; le etichette originali conservate sono `load_config()`.
- Il nome canonico `load_dataset_as_timeline()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_load_dataset_as_timeline`; le etichette originali conservate sono `load_dataset_as_timeline()`.
- Il nome canonico `load_timeline()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_load_timeline`; le etichette originali conservate sono `load_timeline()`.
- Il nome canonico `main() [traffic_exec_engine_main]` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_main`; le etichette originali conservate sono `main()`.
- Il nome canonico `MtlsContextFactory` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory`; le etichette originali conservate sono `MtlsContextFactory`.
- Il nome canonico `OutputExtractionError` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror`; le etichette originali conservate sono `OutputExtractionError`.
- Il nome canonico `PlaceholderTest` identifica il nodo `Request-Dataset-Generator::tests_test_core_placeholdertest`; le etichette originali conservate sono `PlaceholderTest`.
- Il nome canonico `resolve_placeholders()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders`; le etichette originali conservate sono `resolve_placeholders()`.
- Il nome canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_rationale_286`; le etichette originali conservate sono `Rimuove solo whitespace tra tag, preservando testo e attributi.`.
- Il nome canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_rationale_88`; le etichette originali conservate sono `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand`.
- Il nome canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_rationale_135`; le etichette originali conservate sono `Ritorna i placeholder ancora presenti nei campi inviati su rete.`.
- Il nome canonico `RuntimeError` identifica il nodo `Request-Dataset-Generator::runtimeerror`; le etichette originali conservate sono `RuntimeError`.
- Il nome canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_rationale_621`; le etichette originali conservate sono `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di`.
- Il nome canonico `Semaphore` identifica il nodo `Request-Dataset-Generator::semaphore`; le etichette originali conservate sono `Semaphore`.
- Il nome canonico `send_request()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_send_request`; le etichette originali conservate sono `send_request()`.
- Il nome canonico `SSLContext` identifica il nodo `Request-Dataset-Generator::sslcontext`; le etichette originali conservate sono `SSLContext`.
- Il nome canonico `traffic_exec_engine.py` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine`; le etichette originali conservate sono `traffic_exec_engine.py`.
- Il nome canonico `unresolved_placeholders()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders`; le etichette originali conservate sono `unresolved_placeholders()`.
- Il nome canonico `UnresolvedPlaceholderError` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_unresolvedplaceholdererror`; le etichette originali conservate sono `UnresolvedPlaceholderError`.
- Il nome canonico `validate_required_outputs()` identifica il nodo `Request-Dataset-Generator::traffic_exec_engine_validate_required_outputs`; le etichette originali conservate sono `validate_required_outputs()`.

## Cluster Graphify 0 — Traffic Execution Tests

Il cluster editoriale Graphify 0 del dominio Architettura e codice del framework RDG contiene 78 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### .__init__() [traffic_exec_engine_concurrencytracker_init]

<!-- GRAPH_NODE graph=codice index=380 id=traffic_exec_engine_concurrencytracker_init -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `.__init__()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `L773`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` possiede l'identificatore esatto `traffic_exec_engine_concurrencytracker_init`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `.__init__()`.
Nel grafo del codice RDG, il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=797 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConcurrencyTracker`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_concurrencytracker` all'ID `traffic_exec_engine_concurrencytracker_init`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.__init__() [traffic_exec_engine_concurrencytracker_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 797 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 797 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 797 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 797 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `L773`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 797 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 797 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `ast`. Nel grafo del codice RDG, l'arco 797 registra come `source` l'identificatore `traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]`. Nel grafo del codice RDG, l'arco 797 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_init` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 797 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=380 id=Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_init -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `.__init__()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `L773`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `.__init__()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `traffic_exec_engine_concurrencytracker_init`. Nel grafo unificato, il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_init`.
Nel grafo unificato, il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=797 role=incoming -->
Nel grafo unificato, il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConcurrencyTracker`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` all'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_init`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.__init__() [traffic_exec_engine_concurrencytracker_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 797 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 797 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 797 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 797 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `L773`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 797 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 797 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 797 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]` è `1.0`. Nel grafo unificato, l'arco 797 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]`. Nel grafo unificato, l'arco 797 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_init` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]`.

### .__init__() [traffic_exec_engine_executionlogger_init]

<!-- GRAPH_NODE graph=codice index=372 id=traffic_exec_engine_executionlogger_init -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `.__init__()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `L628`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` possiede l'identificatore esatto `traffic_exec_engine_executionlogger_init`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `.__init__()`.
Nel grafo del codice RDG, il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=781 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger` all'ID `traffic_exec_engine_executionlogger_init`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.__init__() [traffic_exec_engine_executionlogger_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 781 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 781 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 781 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 781 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `L628`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 781 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 781 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `ast`. Nel grafo del codice RDG, l'arco 781 registra come `source` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]`. Nel grafo del codice RDG, l'arco 781 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_init` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 781 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=372 id=Request-Dataset-Generator::traffic_exec_engine_executionlogger_init -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `.__init__()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `L628`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `.__init__()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `traffic_exec_engine_executionlogger_init`. Nel grafo unificato, il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_executionlogger_init`.
Nel grafo unificato, il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=781 role=incoming -->
Nel grafo unificato, il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_init`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.__init__() [traffic_exec_engine_executionlogger_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 781 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 781 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 781 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 781 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `L628`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 781 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 781 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 781 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]` è `1.0`. Nel grafo unificato, l'arco 781 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]`. Nel grafo unificato, l'arco 781 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_init` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]`.

### .__init__() [traffic_exec_engine_faultinjector_init]

<!-- GRAPH_NODE graph=codice index=367 id=traffic_exec_engine_faultinjector_init -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `.__init__()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `L474`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` possiede l'identificatore esatto `traffic_exec_engine_faultinjector_init`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `.__init__()`.
<!-- GRAPH_EDGE graph=codice index=758 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_faultinjector_init` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `.__init__() [traffic_exec_engine_faultinjector_init]` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 758 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 758 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 758 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 758 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 758 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `L475`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 758 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 758 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `ast`. Nel grafo del codice RDG, l'arco 758 registra come `source` l'identificatore `traffic_exec_engine_faultinjector_init` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]`. Nel grafo del codice RDG, l'arco 758 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 758 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=769 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FaultInjector`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_faultinjector` all'ID `traffic_exec_engine_faultinjector_init`. La relazione tecnica esplicita è la seguente: `FaultInjector` registra come proprio metodo `.__init__() [traffic_exec_engine_faultinjector_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 769 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 769 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 769 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 769 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `L474`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 769 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 769 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `ast`. Nel grafo del codice RDG, l'arco 769 registra come `source` l'identificatore `traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]`. Nel grafo del codice RDG, l'arco 769 registra come `target` l'identificatore `traffic_exec_engine_faultinjector_init` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 769 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=367 id=Request-Dataset-Generator::traffic_exec_engine_faultinjector_init -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `.__init__()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `L474`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `.__init__()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `traffic_exec_engine_faultinjector_init`. Nel grafo unificato, il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_faultinjector_init`.
Nel grafo unificato, il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=758 role=incoming -->
Nel grafo unificato, il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector_init`. La relazione tecnica esplicita è la seguente: `.get()` chiama `.__init__() [traffic_exec_engine_faultinjector_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 758 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 758 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 758 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 758 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 758 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `L475`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 758 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 758 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 758 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `1.0`. Nel grafo unificato, l'arco 758 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]`. Nel grafo unificato, l'arco 758 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector_init` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]`.
<!-- GRAPH_EDGE graph=unificata index=769 role=incoming -->
Nel grafo unificato, il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FaultInjector`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector` all'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector_init`. La relazione tecnica esplicita è la seguente: `FaultInjector` registra come proprio metodo `.__init__() [traffic_exec_engine_faultinjector_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 769 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 769 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 769 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 769 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `L474`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 769 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 769 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 769 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]` è `1.0`. Nel grafo unificato, l'arco 769 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]`. Nel grafo unificato, l'arco 769 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector_init` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]`.

### .__init__() [traffic_exec_engine_mtlscontextfactory_init]

<!-- GRAPH_NODE graph=codice index=362 id=traffic_exec_engine_mtlscontextfactory_init -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `.__init__()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `L381`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` possiede l'identificatore esatto `traffic_exec_engine_mtlscontextfactory_init`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `.__init__()`.
<!-- GRAPH_EDGE graph=codice index=752 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_mtlscontextfactory_init` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 752 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 752 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 752 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 752 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 752 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `L382`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 752 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 752 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `ast`. Nel grafo del codice RDG, l'arco 752 registra come `source` l'identificatore `traffic_exec_engine_mtlscontextfactory_init` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]`. Nel grafo del codice RDG, l'arco 752 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 752 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=747 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MtlsContextFactory`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_mtlscontextfactory` all'ID `traffic_exec_engine_mtlscontextfactory_init`. La relazione tecnica esplicita è la seguente: `MtlsContextFactory` registra come proprio metodo `.__init__() [traffic_exec_engine_mtlscontextfactory_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 747 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 747 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 747 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 747 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `L381`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 747 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 747 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `ast`. Nel grafo del codice RDG, l'arco 747 registra come `source` l'identificatore `traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]`. Nel grafo del codice RDG, l'arco 747 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_init` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 747 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=362 id=Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_init -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `.__init__()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `L381`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `.__init__()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `traffic_exec_engine_mtlscontextfactory_init`. Nel grafo unificato, il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_init`.
<!-- GRAPH_EDGE graph=unificata index=752 role=outgoing -->
Nel grafo unificato, il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_init` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` chiama `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 752 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 752 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 752 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 752 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 752 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `L382`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 752 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 752 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 752 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `1.0`. Nel grafo unificato, l'arco 752 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_init` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]`. Nel grafo unificato, l'arco 752 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]`.
<!-- GRAPH_EDGE graph=unificata index=747 role=incoming -->
Nel grafo unificato, il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MtlsContextFactory`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_init`. La relazione tecnica esplicita è la seguente: `MtlsContextFactory` registra come proprio metodo `.__init__() [traffic_exec_engine_mtlscontextfactory_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 747 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 747 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 747 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 747 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `L381`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 747 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 747 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 747 che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` è `1.0`. Nel grafo unificato, l'arco 747 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]`. Nel grafo unificato, l'arco 747 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_init` per la relazione che coinvolge il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]`.

### ._emit()

<!-- GRAPH_NODE graph=codice index=373 id=traffic_exec_engine_executionlogger_emit -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._emit()` è `._emit()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._emit()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._emit()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._emit()` è `L641`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._emit()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._emit()` possiede l'identificatore esatto `traffic_exec_engine_executionlogger_emit`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._emit()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._emit()` è `._emit()`.
<!-- GRAPH_EDGE graph=codice index=754 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._emit()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_emit` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `._emit()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 754 che coinvolge il nodo canonico `._emit()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 754 che coinvolge il nodo canonico `._emit()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 754 che coinvolge il nodo canonico `._emit()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 754 che coinvolge il nodo canonico `._emit()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 754 che coinvolge il nodo canonico `._emit()` è `L643`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 754 che coinvolge il nodo canonico `._emit()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 754 che coinvolge il nodo canonico `._emit()` è `ast`. Nel grafo del codice RDG, l'arco 754 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_emit` per la relazione che coinvolge il nodo canonico `._emit()`. Nel grafo del codice RDG, l'arco 754 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `._emit()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 754 che coinvolge il nodo canonico `._emit()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=779 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._emit()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger` all'ID `traffic_exec_engine_executionlogger_emit`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `._emit()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 779 che coinvolge il nodo canonico `._emit()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 779 che coinvolge il nodo canonico `._emit()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 779 che coinvolge il nodo canonico `._emit()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 779 che coinvolge il nodo canonico `._emit()` è `L641`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 779 che coinvolge il nodo canonico `._emit()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 779 che coinvolge il nodo canonico `._emit()` è `ast`. Nel grafo del codice RDG, l'arco 779 registra come `source` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `._emit()`. Nel grafo del codice RDG, l'arco 779 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_emit` per la relazione che coinvolge il nodo canonico `._emit()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 779 che coinvolge il nodo canonico `._emit()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=788 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._emit()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.log_flow_completed()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_flow_completed` all'ID `traffic_exec_engine_executionlogger_emit`. La relazione tecnica esplicita è la seguente: `.log_flow_completed()` chiama `._emit()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 788 che coinvolge il nodo canonico `._emit()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 788 che coinvolge il nodo canonico `._emit()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 788 che coinvolge il nodo canonico `._emit()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 788 che coinvolge il nodo canonico `._emit()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 788 che coinvolge il nodo canonico `._emit()` è `L745`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 788 che coinvolge il nodo canonico `._emit()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 788 che coinvolge il nodo canonico `._emit()` è `ast`. Nel grafo del codice RDG, l'arco 788 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_flow_completed` per la relazione che coinvolge il nodo canonico `._emit()`. Nel grafo del codice RDG, l'arco 788 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_emit` per la relazione che coinvolge il nodo canonico `._emit()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 788 che coinvolge il nodo canonico `._emit()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=789 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._emit()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.log_request_sent()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_request_sent` all'ID `traffic_exec_engine_executionlogger_emit`. La relazione tecnica esplicita è la seguente: `.log_request_sent()` chiama `._emit()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 789 che coinvolge il nodo canonico `._emit()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 789 che coinvolge il nodo canonico `._emit()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 789 che coinvolge il nodo canonico `._emit()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 789 che coinvolge il nodo canonico `._emit()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 789 che coinvolge il nodo canonico `._emit()` è `L684`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 789 che coinvolge il nodo canonico `._emit()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 789 che coinvolge il nodo canonico `._emit()` è `ast`. Nel grafo del codice RDG, l'arco 789 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `._emit()`. Nel grafo del codice RDG, l'arco 789 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_emit` per la relazione che coinvolge il nodo canonico `._emit()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 789 che coinvolge il nodo canonico `._emit()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=790 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._emit()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.log_response_received()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_response_received` all'ID `traffic_exec_engine_executionlogger_emit`. La relazione tecnica esplicita è la seguente: `.log_response_received()` chiama `._emit()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 790 che coinvolge il nodo canonico `._emit()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 790 che coinvolge il nodo canonico `._emit()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 790 che coinvolge il nodo canonico `._emit()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 790 che coinvolge il nodo canonico `._emit()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 790 che coinvolge il nodo canonico `._emit()` è `L719`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 790 che coinvolge il nodo canonico `._emit()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 790 che coinvolge il nodo canonico `._emit()` è `ast`. Nel grafo del codice RDG, l'arco 790 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `._emit()`. Nel grafo del codice RDG, l'arco 790 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_emit` per la relazione che coinvolge il nodo canonico `._emit()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 790 che coinvolge il nodo canonico `._emit()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=373 id=Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._emit()` è `._emit()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._emit()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._emit()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._emit()` è `L641`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._emit()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._emit()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._emit()` è `._emit()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._emit()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._emit()` è `traffic_exec_engine_executionlogger_emit`. Nel grafo unificato, il nodo canonico `._emit()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit`.
<!-- GRAPH_EDGE graph=unificata index=788 role=outgoing -->
Nel grafo unificato, il nodo canonico `._emit()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.log_flow_completed()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_flow_completed`. La relazione tecnica esplicita è la seguente: `._emit()` chiama `.log_flow_completed()`. Nel grafo unificato, il tipo dichiarato dell'arco 788 che coinvolge il nodo canonico `._emit()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 788 che coinvolge il nodo canonico `._emit()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 788 che coinvolge il nodo canonico `._emit()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 788 che coinvolge il nodo canonico `._emit()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 788 che coinvolge il nodo canonico `._emit()` è `L745`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 788 che coinvolge il nodo canonico `._emit()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 788 che coinvolge il nodo canonico `._emit()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 788 che coinvolge il nodo canonico `._emit()` è `1.0`. Nel grafo unificato, l'arco 788 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit` per la relazione che coinvolge il nodo canonico `._emit()`. Nel grafo unificato, l'arco 788 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_flow_completed` per la relazione che coinvolge il nodo canonico `._emit()`.
<!-- GRAPH_EDGE graph=unificata index=789 role=outgoing -->
Nel grafo unificato, il nodo canonico `._emit()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.log_request_sent()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent`. La relazione tecnica esplicita è la seguente: `._emit()` chiama `.log_request_sent()`. Nel grafo unificato, il tipo dichiarato dell'arco 789 che coinvolge il nodo canonico `._emit()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 789 che coinvolge il nodo canonico `._emit()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 789 che coinvolge il nodo canonico `._emit()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 789 che coinvolge il nodo canonico `._emit()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 789 che coinvolge il nodo canonico `._emit()` è `L684`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 789 che coinvolge il nodo canonico `._emit()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 789 che coinvolge il nodo canonico `._emit()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 789 che coinvolge il nodo canonico `._emit()` è `1.0`. Nel grafo unificato, l'arco 789 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit` per la relazione che coinvolge il nodo canonico `._emit()`. Nel grafo unificato, l'arco 789 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `._emit()`.
<!-- GRAPH_EDGE graph=unificata index=790 role=outgoing -->
Nel grafo unificato, il nodo canonico `._emit()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.log_response_received()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received`. La relazione tecnica esplicita è la seguente: `._emit()` chiama `.log_response_received()`. Nel grafo unificato, il tipo dichiarato dell'arco 790 che coinvolge il nodo canonico `._emit()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 790 che coinvolge il nodo canonico `._emit()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 790 che coinvolge il nodo canonico `._emit()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 790 che coinvolge il nodo canonico `._emit()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 790 che coinvolge il nodo canonico `._emit()` è `L719`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 790 che coinvolge il nodo canonico `._emit()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 790 che coinvolge il nodo canonico `._emit()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 790 che coinvolge il nodo canonico `._emit()` è `1.0`. Nel grafo unificato, l'arco 790 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit` per la relazione che coinvolge il nodo canonico `._emit()`. Nel grafo unificato, l'arco 790 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `._emit()`.
<!-- GRAPH_EDGE graph=unificata index=754 role=incoming -->
Nel grafo unificato, il nodo canonico `._emit()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit`. La relazione tecnica esplicita è la seguente: `.get()` chiama `._emit()`. Nel grafo unificato, il tipo dichiarato dell'arco 754 che coinvolge il nodo canonico `._emit()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 754 che coinvolge il nodo canonico `._emit()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 754 che coinvolge il nodo canonico `._emit()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 754 che coinvolge il nodo canonico `._emit()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 754 che coinvolge il nodo canonico `._emit()` è `L643`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 754 che coinvolge il nodo canonico `._emit()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 754 che coinvolge il nodo canonico `._emit()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 754 che coinvolge il nodo canonico `._emit()` è `1.0`. Nel grafo unificato, l'arco 754 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `._emit()`. Nel grafo unificato, l'arco 754 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit` per la relazione che coinvolge il nodo canonico `._emit()`.
<!-- GRAPH_EDGE graph=unificata index=779 role=incoming -->
Nel grafo unificato, il nodo canonico `._emit()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `._emit()`. Nel grafo unificato, il tipo dichiarato dell'arco 779 che coinvolge il nodo canonico `._emit()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 779 che coinvolge il nodo canonico `._emit()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 779 che coinvolge il nodo canonico `._emit()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 779 che coinvolge il nodo canonico `._emit()` è `L641`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 779 che coinvolge il nodo canonico `._emit()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 779 che coinvolge il nodo canonico `._emit()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 779 che coinvolge il nodo canonico `._emit()` è `1.0`. Nel grafo unificato, l'arco 779 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `._emit()`. Nel grafo unificato, l'arco 779 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit` per la relazione che coinvolge il nodo canonico `._emit()`.

### ._load_pkcs12()

<!-- GRAPH_NODE graph=codice index=365 id=traffic_exec_engine_mtlscontextfactory_load_pkcs12 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._load_pkcs12()` è `._load_pkcs12()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._load_pkcs12()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._load_pkcs12()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._load_pkcs12()` è `L429`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._load_pkcs12()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._load_pkcs12()` possiede l'identificatore esatto `traffic_exec_engine_mtlscontextfactory_load_pkcs12`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._load_pkcs12()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._load_pkcs12()` è `._load_pkcs12()`.
<!-- GRAPH_EDGE graph=codice index=766 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._load_pkcs12()` è la sorgente `source` della relazione `references` verso il nodo canonico `SSLContext`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_mtlscontextfactory_load_pkcs12` all'ID `sslcontext`. La relazione tecnica esplicita è la seguente: `._load_pkcs12()` fa riferimento a `SSLContext`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 766 che coinvolge il nodo canonico `._load_pkcs12()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 766 che coinvolge il nodo canonico `._load_pkcs12()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 766 che coinvolge il nodo canonico `._load_pkcs12()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 766 che coinvolge il nodo canonico `._load_pkcs12()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 766 che coinvolge il nodo canonico `._load_pkcs12()` è `L429`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 766 che coinvolge il nodo canonico `._load_pkcs12()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 766 che coinvolge il nodo canonico `._load_pkcs12()` è `ast`. Nel grafo del codice RDG, l'arco 766 registra come `source` l'identificatore `traffic_exec_engine_mtlscontextfactory_load_pkcs12` per la relazione che coinvolge il nodo canonico `._load_pkcs12()`. Nel grafo del codice RDG, l'arco 766 registra come `target` l'identificatore `sslcontext` per la relazione che coinvolge il nodo canonico `._load_pkcs12()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 766 che coinvolge il nodo canonico `._load_pkcs12()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=767 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._load_pkcs12()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.flush()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_mtlscontextfactory_load_pkcs12` all'ID `traffic_exec_engine_executionlogger_flush`. La relazione tecnica esplicita è la seguente: `._load_pkcs12()` chiama `.flush()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 767 che coinvolge il nodo canonico `._load_pkcs12()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 767 che coinvolge il nodo canonico `._load_pkcs12()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 767 che coinvolge il nodo canonico `._load_pkcs12()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 767 che coinvolge il nodo canonico `._load_pkcs12()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 767 che coinvolge il nodo canonico `._load_pkcs12()` è `L463`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 767 che coinvolge il nodo canonico `._load_pkcs12()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 767 che coinvolge il nodo canonico `._load_pkcs12()` è `ast`. Nel grafo del codice RDG, l'arco 767 registra come `source` l'identificatore `traffic_exec_engine_mtlscontextfactory_load_pkcs12` per la relazione che coinvolge il nodo canonico `._load_pkcs12()`. Nel grafo del codice RDG, l'arco 767 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_flush` per la relazione che coinvolge il nodo canonico `._load_pkcs12()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 767 che coinvolge il nodo canonico `._load_pkcs12()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=748 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._load_pkcs12()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MtlsContextFactory`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_mtlscontextfactory` all'ID `traffic_exec_engine_mtlscontextfactory_load_pkcs12`. La relazione tecnica esplicita è la seguente: `MtlsContextFactory` registra come proprio metodo `._load_pkcs12()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 748 che coinvolge il nodo canonico `._load_pkcs12()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 748 che coinvolge il nodo canonico `._load_pkcs12()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 748 che coinvolge il nodo canonico `._load_pkcs12()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 748 che coinvolge il nodo canonico `._load_pkcs12()` è `L429`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 748 che coinvolge il nodo canonico `._load_pkcs12()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 748 che coinvolge il nodo canonico `._load_pkcs12()` è `ast`. Nel grafo del codice RDG, l'arco 748 registra come `source` l'identificatore `traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `._load_pkcs12()`. Nel grafo del codice RDG, l'arco 748 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_load_pkcs12` per la relazione che coinvolge il nodo canonico `._load_pkcs12()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 748 che coinvolge il nodo canonico `._load_pkcs12()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=763 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._load_pkcs12()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_mtlscontextfactory_get` all'ID `traffic_exec_engine_mtlscontextfactory_load_pkcs12`. La relazione tecnica esplicita è la seguente: `.get()` chiama `._load_pkcs12()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 763 che coinvolge il nodo canonico `._load_pkcs12()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 763 che coinvolge il nodo canonico `._load_pkcs12()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 763 che coinvolge il nodo canonico `._load_pkcs12()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 763 che coinvolge il nodo canonico `._load_pkcs12()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 763 che coinvolge il nodo canonico `._load_pkcs12()` è `L416`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 763 che coinvolge il nodo canonico `._load_pkcs12()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 763 che coinvolge il nodo canonico `._load_pkcs12()` è `ast`. Nel grafo del codice RDG, l'arco 763 registra come `source` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `._load_pkcs12()`. Nel grafo del codice RDG, l'arco 763 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_load_pkcs12` per la relazione che coinvolge il nodo canonico `._load_pkcs12()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 763 che coinvolge il nodo canonico `._load_pkcs12()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=365 id=Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_load_pkcs12 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._load_pkcs12()` è `._load_pkcs12()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._load_pkcs12()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._load_pkcs12()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._load_pkcs12()` è `L429`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._load_pkcs12()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._load_pkcs12()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._load_pkcs12()` è `._load_pkcs12()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._load_pkcs12()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._load_pkcs12()` è `traffic_exec_engine_mtlscontextfactory_load_pkcs12`. Nel grafo unificato, il nodo canonico `._load_pkcs12()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_load_pkcs12`.
<!-- GRAPH_EDGE graph=unificata index=767 role=outgoing -->
Nel grafo unificato, il nodo canonico `._load_pkcs12()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.flush()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_load_pkcs12` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_flush`. La relazione tecnica esplicita è la seguente: `._load_pkcs12()` chiama `.flush()`. Nel grafo unificato, il tipo dichiarato dell'arco 767 che coinvolge il nodo canonico `._load_pkcs12()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 767 che coinvolge il nodo canonico `._load_pkcs12()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 767 che coinvolge il nodo canonico `._load_pkcs12()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 767 che coinvolge il nodo canonico `._load_pkcs12()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 767 che coinvolge il nodo canonico `._load_pkcs12()` è `L463`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 767 che coinvolge il nodo canonico `._load_pkcs12()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 767 che coinvolge il nodo canonico `._load_pkcs12()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 767 che coinvolge il nodo canonico `._load_pkcs12()` è `1.0`. Nel grafo unificato, l'arco 767 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_load_pkcs12` per la relazione che coinvolge il nodo canonico `._load_pkcs12()`. Nel grafo unificato, l'arco 767 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_flush` per la relazione che coinvolge il nodo canonico `._load_pkcs12()`.
<!-- GRAPH_EDGE graph=unificata index=748 role=incoming -->
Nel grafo unificato, il nodo canonico `._load_pkcs12()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MtlsContextFactory`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_load_pkcs12`. La relazione tecnica esplicita è la seguente: `MtlsContextFactory` registra come proprio metodo `._load_pkcs12()`. Nel grafo unificato, il tipo dichiarato dell'arco 748 che coinvolge il nodo canonico `._load_pkcs12()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 748 che coinvolge il nodo canonico `._load_pkcs12()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 748 che coinvolge il nodo canonico `._load_pkcs12()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 748 che coinvolge il nodo canonico `._load_pkcs12()` è `L429`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 748 che coinvolge il nodo canonico `._load_pkcs12()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 748 che coinvolge il nodo canonico `._load_pkcs12()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 748 che coinvolge il nodo canonico `._load_pkcs12()` è `1.0`. Nel grafo unificato, l'arco 748 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `._load_pkcs12()`. Nel grafo unificato, l'arco 748 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_load_pkcs12` per la relazione che coinvolge il nodo canonico `._load_pkcs12()`.
<!-- GRAPH_EDGE graph=unificata index=763 role=incoming -->
Nel grafo unificato, il nodo canonico `._load_pkcs12()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_load_pkcs12`. La relazione tecnica esplicita è la seguente: `.get()` chiama `._load_pkcs12()`. Nel grafo unificato, il tipo dichiarato dell'arco 763 che coinvolge il nodo canonico `._load_pkcs12()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 763 che coinvolge il nodo canonico `._load_pkcs12()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 763 che coinvolge il nodo canonico `._load_pkcs12()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 763 che coinvolge il nodo canonico `._load_pkcs12()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 763 che coinvolge il nodo canonico `._load_pkcs12()` è `L416`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 763 che coinvolge il nodo canonico `._load_pkcs12()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 763 che coinvolge il nodo canonico `._load_pkcs12()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 763 che coinvolge il nodo canonico `._load_pkcs12()` è `1.0`. Nel grafo unificato, l'arco 763 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `._load_pkcs12()`. Nel grafo unificato, l'arco 763 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_load_pkcs12` per la relazione che coinvolge il nodo canonico `._load_pkcs12()`.
<!-- GRAPH_EDGE graph=unificata index=766 role=incoming -->
Nel grafo unificato, il nodo canonico `._load_pkcs12()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `SSLContext`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::sslcontext` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_load_pkcs12`. La relazione tecnica esplicita è la seguente: `SSLContext` fa riferimento a `._load_pkcs12()`. Nel grafo unificato, il tipo dichiarato dell'arco 766 che coinvolge il nodo canonico `._load_pkcs12()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 766 che coinvolge il nodo canonico `._load_pkcs12()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 766 che coinvolge il nodo canonico `._load_pkcs12()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 766 che coinvolge il nodo canonico `._load_pkcs12()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 766 che coinvolge il nodo canonico `._load_pkcs12()` è `L429`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 766 che coinvolge il nodo canonico `._load_pkcs12()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 766 che coinvolge il nodo canonico `._load_pkcs12()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 766 che coinvolge il nodo canonico `._load_pkcs12()` è `1.0`. Nel grafo unificato, l'arco 766 registra come `source` l'identificatore `Request-Dataset-Generator::sslcontext` per la relazione che coinvolge il nodo canonico `._load_pkcs12()`. Nel grafo unificato, l'arco 766 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_load_pkcs12` per la relazione che coinvolge il nodo canonico `._load_pkcs12()`.

### ._matches()

<!-- GRAPH_NODE graph=codice index=369 id=traffic_exec_engine_faultinjector_matches -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._matches()` è `._matches()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._matches()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._matches()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._matches()` è `L525`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._matches()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._matches()` possiede l'identificatore esatto `traffic_exec_engine_faultinjector_matches`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._matches()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._matches()` è `._matches()`.
<!-- GRAPH_EDGE graph=codice index=759 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._matches()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_faultinjector_matches` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `._matches()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 759 che coinvolge il nodo canonico `._matches()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 759 che coinvolge il nodo canonico `._matches()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 759 che coinvolge il nodo canonico `._matches()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 759 che coinvolge il nodo canonico `._matches()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 759 che coinvolge il nodo canonico `._matches()` è `L527`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 759 che coinvolge il nodo canonico `._matches()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 759 che coinvolge il nodo canonico `._matches()` è `ast`. Nel grafo del codice RDG, l'arco 759 registra come `source` l'identificatore `traffic_exec_engine_faultinjector_matches` per la relazione che coinvolge il nodo canonico `._matches()`. Nel grafo del codice RDG, l'arco 759 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `._matches()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 759 che coinvolge il nodo canonico `._matches()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=770 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._matches()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FaultInjector`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_faultinjector` all'ID `traffic_exec_engine_faultinjector_matches`. La relazione tecnica esplicita è la seguente: `FaultInjector` registra come proprio metodo `._matches()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 770 che coinvolge il nodo canonico `._matches()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 770 che coinvolge il nodo canonico `._matches()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 770 che coinvolge il nodo canonico `._matches()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 770 che coinvolge il nodo canonico `._matches()` è `L525`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 770 che coinvolge il nodo canonico `._matches()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 770 che coinvolge il nodo canonico `._matches()` è `ast`. Nel grafo del codice RDG, l'arco 770 registra come `source` l'identificatore `traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `._matches()`. Nel grafo del codice RDG, l'arco 770 registra come `target` l'identificatore `traffic_exec_engine_faultinjector_matches` per la relazione che coinvolge il nodo canonico `._matches()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 770 che coinvolge il nodo canonico `._matches()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=775 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._matches()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.plan()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_faultinjector_plan` all'ID `traffic_exec_engine_faultinjector_matches`. La relazione tecnica esplicita è la seguente: `.plan()` chiama `._matches()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 775 che coinvolge il nodo canonico `._matches()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 775 che coinvolge il nodo canonico `._matches()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 775 che coinvolge il nodo canonico `._matches()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 775 che coinvolge il nodo canonico `._matches()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 775 che coinvolge il nodo canonico `._matches()` è `L485`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 775 che coinvolge il nodo canonico `._matches()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 775 che coinvolge il nodo canonico `._matches()` è `ast`. Nel grafo del codice RDG, l'arco 775 registra come `source` l'identificatore `traffic_exec_engine_faultinjector_plan` per la relazione che coinvolge il nodo canonico `._matches()`. Nel grafo del codice RDG, l'arco 775 registra come `target` l'identificatore `traffic_exec_engine_faultinjector_matches` per la relazione che coinvolge il nodo canonico `._matches()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 775 che coinvolge il nodo canonico `._matches()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=369 id=Request-Dataset-Generator::traffic_exec_engine_faultinjector_matches -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._matches()` è `._matches()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._matches()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._matches()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._matches()` è `L525`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._matches()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._matches()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._matches()` è `._matches()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._matches()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._matches()` è `traffic_exec_engine_faultinjector_matches`. Nel grafo unificato, il nodo canonico `._matches()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_faultinjector_matches`.
Nel grafo unificato, il nodo canonico `._matches()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=759 role=incoming -->
Nel grafo unificato, il nodo canonico `._matches()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector_matches`. La relazione tecnica esplicita è la seguente: `.get()` chiama `._matches()`. Nel grafo unificato, il tipo dichiarato dell'arco 759 che coinvolge il nodo canonico `._matches()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 759 che coinvolge il nodo canonico `._matches()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 759 che coinvolge il nodo canonico `._matches()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 759 che coinvolge il nodo canonico `._matches()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 759 che coinvolge il nodo canonico `._matches()` è `L527`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 759 che coinvolge il nodo canonico `._matches()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 759 che coinvolge il nodo canonico `._matches()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 759 che coinvolge il nodo canonico `._matches()` è `1.0`. Nel grafo unificato, l'arco 759 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `._matches()`. Nel grafo unificato, l'arco 759 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector_matches` per la relazione che coinvolge il nodo canonico `._matches()`.
<!-- GRAPH_EDGE graph=unificata index=770 role=incoming -->
Nel grafo unificato, il nodo canonico `._matches()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FaultInjector`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector` all'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector_matches`. La relazione tecnica esplicita è la seguente: `FaultInjector` registra come proprio metodo `._matches()`. Nel grafo unificato, il tipo dichiarato dell'arco 770 che coinvolge il nodo canonico `._matches()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 770 che coinvolge il nodo canonico `._matches()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 770 che coinvolge il nodo canonico `._matches()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 770 che coinvolge il nodo canonico `._matches()` è `L525`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 770 che coinvolge il nodo canonico `._matches()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 770 che coinvolge il nodo canonico `._matches()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 770 che coinvolge il nodo canonico `._matches()` è `1.0`. Nel grafo unificato, l'arco 770 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `._matches()`. Nel grafo unificato, l'arco 770 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector_matches` per la relazione che coinvolge il nodo canonico `._matches()`.
<!-- GRAPH_EDGE graph=unificata index=775 role=incoming -->
Nel grafo unificato, il nodo canonico `._matches()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.plan()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan` all'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector_matches`. La relazione tecnica esplicita è la seguente: `.plan()` chiama `._matches()`. Nel grafo unificato, il tipo dichiarato dell'arco 775 che coinvolge il nodo canonico `._matches()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 775 che coinvolge il nodo canonico `._matches()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 775 che coinvolge il nodo canonico `._matches()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 775 che coinvolge il nodo canonico `._matches()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 775 che coinvolge il nodo canonico `._matches()` è `L485`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 775 che coinvolge il nodo canonico `._matches()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 775 che coinvolge il nodo canonico `._matches()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 775 che coinvolge il nodo canonico `._matches()` è `1.0`. Nel grafo unificato, l'arco 775 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan` per la relazione che coinvolge il nodo canonico `._matches()`. Nel grafo unificato, l'arco 775 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector_matches` per la relazione che coinvolge il nodo canonico `._matches()`.

### .flow_finished()

<!-- GRAPH_NODE graph=codice index=382 id=traffic_exec_engine_concurrencytracker_flow_finished -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.flow_finished()` è `.flow_finished()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.flow_finished()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.flow_finished()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.flow_finished()` è `L783`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.flow_finished()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.flow_finished()` possiede l'identificatore esatto `traffic_exec_engine_concurrencytracker_flow_finished`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.flow_finished()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.flow_finished()` è `.flow_finished()`.
Nel grafo del codice RDG, il nodo canonico `.flow_finished()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=795 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.flow_finished()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConcurrencyTracker`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_concurrencytracker` all'ID `traffic_exec_engine_concurrencytracker_flow_finished`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.flow_finished()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 795 che coinvolge il nodo canonico `.flow_finished()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 795 che coinvolge il nodo canonico `.flow_finished()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 795 che coinvolge il nodo canonico `.flow_finished()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 795 che coinvolge il nodo canonico `.flow_finished()` è `L783`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 795 che coinvolge il nodo canonico `.flow_finished()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 795 che coinvolge il nodo canonico `.flow_finished()` è `ast`. Nel grafo del codice RDG, l'arco 795 registra come `source` l'identificatore `traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `.flow_finished()`. Nel grafo del codice RDG, l'arco 795 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_flow_finished` per la relazione che coinvolge il nodo canonico `.flow_finished()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 795 che coinvolge il nodo canonico `.flow_finished()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=804 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.flow_finished()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_concurrencytracker_flow_finished`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `.flow_finished()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 804 che coinvolge il nodo canonico `.flow_finished()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 804 che coinvolge il nodo canonico `.flow_finished()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 804 che coinvolge il nodo canonico `.flow_finished()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 804 che coinvolge il nodo canonico `.flow_finished()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 804 che coinvolge il nodo canonico `.flow_finished()` è `L960`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 804 che coinvolge il nodo canonico `.flow_finished()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 804 che coinvolge il nodo canonico `.flow_finished()` è `ast`. Nel grafo del codice RDG, l'arco 804 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `.flow_finished()`. Nel grafo del codice RDG, l'arco 804 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_flow_finished` per la relazione che coinvolge il nodo canonico `.flow_finished()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 804 che coinvolge il nodo canonico `.flow_finished()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=382 id=Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_finished -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.flow_finished()` è `.flow_finished()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.flow_finished()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.flow_finished()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.flow_finished()` è `L783`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.flow_finished()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.flow_finished()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.flow_finished()` è `.flow_finished()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.flow_finished()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.flow_finished()` è `traffic_exec_engine_concurrencytracker_flow_finished`. Nel grafo unificato, il nodo canonico `.flow_finished()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_finished`.
<!-- GRAPH_EDGE graph=unificata index=804 role=outgoing -->
Nel grafo unificato, il nodo canonico `.flow_finished()` è la sorgente `source` della relazione `calls` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_finished` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `.flow_finished()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 804 che coinvolge il nodo canonico `.flow_finished()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 804 che coinvolge il nodo canonico `.flow_finished()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 804 che coinvolge il nodo canonico `.flow_finished()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 804 che coinvolge il nodo canonico `.flow_finished()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 804 che coinvolge il nodo canonico `.flow_finished()` è `L960`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 804 che coinvolge il nodo canonico `.flow_finished()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 804 che coinvolge il nodo canonico `.flow_finished()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 804 che coinvolge il nodo canonico `.flow_finished()` è `1.0`. Nel grafo unificato, l'arco 804 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_finished` per la relazione che coinvolge il nodo canonico `.flow_finished()`. Nel grafo unificato, l'arco 804 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `.flow_finished()`.
<!-- GRAPH_EDGE graph=unificata index=795 role=incoming -->
Nel grafo unificato, il nodo canonico `.flow_finished()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConcurrencyTracker`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` all'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_finished`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.flow_finished()`. Nel grafo unificato, il tipo dichiarato dell'arco 795 che coinvolge il nodo canonico `.flow_finished()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 795 che coinvolge il nodo canonico `.flow_finished()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 795 che coinvolge il nodo canonico `.flow_finished()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 795 che coinvolge il nodo canonico `.flow_finished()` è `L783`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 795 che coinvolge il nodo canonico `.flow_finished()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 795 che coinvolge il nodo canonico `.flow_finished()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 795 che coinvolge il nodo canonico `.flow_finished()` è `1.0`. Nel grafo unificato, l'arco 795 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `.flow_finished()`. Nel grafo unificato, l'arco 795 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_finished` per la relazione che coinvolge il nodo canonico `.flow_finished()`.

### .flow_started()

<!-- GRAPH_NODE graph=codice index=381 id=traffic_exec_engine_concurrencytracker_flow_started -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.flow_started()` è `.flow_started()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.flow_started()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.flow_started()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.flow_started()` è `L779`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.flow_started()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.flow_started()` possiede l'identificatore esatto `traffic_exec_engine_concurrencytracker_flow_started`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.flow_started()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.flow_started()` è `.flow_started()`.
Nel grafo del codice RDG, il nodo canonico `.flow_started()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=796 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.flow_started()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConcurrencyTracker`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_concurrencytracker` all'ID `traffic_exec_engine_concurrencytracker_flow_started`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.flow_started()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 796 che coinvolge il nodo canonico `.flow_started()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 796 che coinvolge il nodo canonico `.flow_started()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 796 che coinvolge il nodo canonico `.flow_started()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 796 che coinvolge il nodo canonico `.flow_started()` è `L779`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 796 che coinvolge il nodo canonico `.flow_started()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 796 che coinvolge il nodo canonico `.flow_started()` è `ast`. Nel grafo del codice RDG, l'arco 796 registra come `source` l'identificatore `traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `.flow_started()`. Nel grafo del codice RDG, l'arco 796 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_flow_started` per la relazione che coinvolge il nodo canonico `.flow_started()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 796 che coinvolge il nodo canonico `.flow_started()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=803 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.flow_started()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_concurrencytracker_flow_started`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `.flow_started()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 803 che coinvolge il nodo canonico `.flow_started()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 803 che coinvolge il nodo canonico `.flow_started()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 803 che coinvolge il nodo canonico `.flow_started()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 803 che coinvolge il nodo canonico `.flow_started()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 803 che coinvolge il nodo canonico `.flow_started()` è `L846`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 803 che coinvolge il nodo canonico `.flow_started()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 803 che coinvolge il nodo canonico `.flow_started()` è `ast`. Nel grafo del codice RDG, l'arco 803 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `.flow_started()`. Nel grafo del codice RDG, l'arco 803 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_flow_started` per la relazione che coinvolge il nodo canonico `.flow_started()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 803 che coinvolge il nodo canonico `.flow_started()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=381 id=Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_started -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.flow_started()` è `.flow_started()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.flow_started()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.flow_started()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.flow_started()` è `L779`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.flow_started()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.flow_started()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.flow_started()` è `.flow_started()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.flow_started()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.flow_started()` è `traffic_exec_engine_concurrencytracker_flow_started`. Nel grafo unificato, il nodo canonico `.flow_started()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_started`.
<!-- GRAPH_EDGE graph=unificata index=803 role=outgoing -->
Nel grafo unificato, il nodo canonico `.flow_started()` è la sorgente `source` della relazione `calls` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_started` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `.flow_started()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 803 che coinvolge il nodo canonico `.flow_started()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 803 che coinvolge il nodo canonico `.flow_started()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 803 che coinvolge il nodo canonico `.flow_started()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 803 che coinvolge il nodo canonico `.flow_started()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 803 che coinvolge il nodo canonico `.flow_started()` è `L846`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 803 che coinvolge il nodo canonico `.flow_started()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 803 che coinvolge il nodo canonico `.flow_started()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 803 che coinvolge il nodo canonico `.flow_started()` è `1.0`. Nel grafo unificato, l'arco 803 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_started` per la relazione che coinvolge il nodo canonico `.flow_started()`. Nel grafo unificato, l'arco 803 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `.flow_started()`.
<!-- GRAPH_EDGE graph=unificata index=796 role=incoming -->
Nel grafo unificato, il nodo canonico `.flow_started()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConcurrencyTracker`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` all'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_started`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.flow_started()`. Nel grafo unificato, il tipo dichiarato dell'arco 796 che coinvolge il nodo canonico `.flow_started()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 796 che coinvolge il nodo canonico `.flow_started()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 796 che coinvolge il nodo canonico `.flow_started()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 796 che coinvolge il nodo canonico `.flow_started()` è `L779`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 796 che coinvolge il nodo canonico `.flow_started()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 796 che coinvolge il nodo canonico `.flow_started()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 796 che coinvolge il nodo canonico `.flow_started()` è `1.0`. Nel grafo unificato, l'arco 796 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `.flow_started()`. Nel grafo unificato, l'arco 796 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_started` per la relazione che coinvolge il nodo canonico `.flow_started()`.

### .flush()

<!-- GRAPH_NODE graph=codice index=377 id=traffic_exec_engine_executionlogger_flush -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.flush()` è `.flush()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.flush()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.flush()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.flush()` è `L760`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.flush()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.flush()` possiede l'identificatore esatto `traffic_exec_engine_executionlogger_flush`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.flush()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.flush()` è `.flush()`.
Nel grafo del codice RDG, il nodo canonico `.flush()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=767 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.flush()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._load_pkcs12()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_mtlscontextfactory_load_pkcs12` all'ID `traffic_exec_engine_executionlogger_flush`. La relazione tecnica esplicita è la seguente: `._load_pkcs12()` chiama `.flush()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 767 che coinvolge il nodo canonico `.flush()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 767 che coinvolge il nodo canonico `.flush()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 767 che coinvolge il nodo canonico `.flush()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 767 che coinvolge il nodo canonico `.flush()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 767 che coinvolge il nodo canonico `.flush()` è `L463`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 767 che coinvolge il nodo canonico `.flush()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 767 che coinvolge il nodo canonico `.flush()` è `ast`. Nel grafo del codice RDG, l'arco 767 registra come `source` l'identificatore `traffic_exec_engine_mtlscontextfactory_load_pkcs12` per la relazione che coinvolge il nodo canonico `.flush()`. Nel grafo del codice RDG, l'arco 767 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_flush` per la relazione che coinvolge il nodo canonico `.flush()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 767 che coinvolge il nodo canonico `.flush()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=780 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.flush()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger` all'ID `traffic_exec_engine_executionlogger_flush`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.flush()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 780 che coinvolge il nodo canonico `.flush()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 780 che coinvolge il nodo canonico `.flush()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 780 che coinvolge il nodo canonico `.flush()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 780 che coinvolge il nodo canonico `.flush()` è `L760`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 780 che coinvolge il nodo canonico `.flush()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 780 che coinvolge il nodo canonico `.flush()` è `ast`. Nel grafo del codice RDG, l'arco 780 registra come `source` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `.flush()`. Nel grafo del codice RDG, l'arco 780 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_flush` per la relazione che coinvolge il nodo canonico `.flush()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 780 che coinvolge il nodo canonico `.flush()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=794 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.flush()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_run` all'ID `traffic_exec_engine_executionlogger_flush`. La relazione tecnica esplicita è la seguente: `run()` chiama `.flush()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 794 che coinvolge il nodo canonico `.flush()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 794 che coinvolge il nodo canonico `.flush()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 794 che coinvolge il nodo canonico `.flush()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 794 che coinvolge il nodo canonico `.flush()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 794 che coinvolge il nodo canonico `.flush()` è `L1075`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 794 che coinvolge il nodo canonico `.flush()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 794 che coinvolge il nodo canonico `.flush()` è `ast`. Nel grafo del codice RDG, l'arco 794 registra come `source` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `.flush()`. Nel grafo del codice RDG, l'arco 794 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_flush` per la relazione che coinvolge il nodo canonico `.flush()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 794 che coinvolge il nodo canonico `.flush()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=377 id=Request-Dataset-Generator::traffic_exec_engine_executionlogger_flush -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.flush()` è `.flush()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.flush()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.flush()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.flush()` è `L760`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.flush()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.flush()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.flush()` è `.flush()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.flush()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.flush()` è `traffic_exec_engine_executionlogger_flush`. Nel grafo unificato, il nodo canonico `.flush()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_executionlogger_flush`.
<!-- GRAPH_EDGE graph=unificata index=794 role=outgoing -->
Nel grafo unificato, il nodo canonico `.flush()` è la sorgente `source` della relazione `calls` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_flush` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.flush()` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 794 che coinvolge il nodo canonico `.flush()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 794 che coinvolge il nodo canonico `.flush()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 794 che coinvolge il nodo canonico `.flush()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 794 che coinvolge il nodo canonico `.flush()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 794 che coinvolge il nodo canonico `.flush()` è `L1075`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 794 che coinvolge il nodo canonico `.flush()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 794 che coinvolge il nodo canonico `.flush()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 794 che coinvolge il nodo canonico `.flush()` è `1.0`. Nel grafo unificato, l'arco 794 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_flush` per la relazione che coinvolge il nodo canonico `.flush()`. Nel grafo unificato, l'arco 794 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `.flush()`.
<!-- GRAPH_EDGE graph=unificata index=767 role=incoming -->
Nel grafo unificato, il nodo canonico `.flush()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._load_pkcs12()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_load_pkcs12` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_flush`. La relazione tecnica esplicita è la seguente: `._load_pkcs12()` chiama `.flush()`. Nel grafo unificato, il tipo dichiarato dell'arco 767 che coinvolge il nodo canonico `.flush()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 767 che coinvolge il nodo canonico `.flush()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 767 che coinvolge il nodo canonico `.flush()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 767 che coinvolge il nodo canonico `.flush()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 767 che coinvolge il nodo canonico `.flush()` è `L463`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 767 che coinvolge il nodo canonico `.flush()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 767 che coinvolge il nodo canonico `.flush()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 767 che coinvolge il nodo canonico `.flush()` è `1.0`. Nel grafo unificato, l'arco 767 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_load_pkcs12` per la relazione che coinvolge il nodo canonico `.flush()`. Nel grafo unificato, l'arco 767 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_flush` per la relazione che coinvolge il nodo canonico `.flush()`.
<!-- GRAPH_EDGE graph=unificata index=780 role=incoming -->
Nel grafo unificato, il nodo canonico `.flush()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_flush`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.flush()`. Nel grafo unificato, il tipo dichiarato dell'arco 780 che coinvolge il nodo canonico `.flush()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 780 che coinvolge il nodo canonico `.flush()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 780 che coinvolge il nodo canonico `.flush()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 780 che coinvolge il nodo canonico `.flush()` è `L760`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 780 che coinvolge il nodo canonico `.flush()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 780 che coinvolge il nodo canonico `.flush()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 780 che coinvolge il nodo canonico `.flush()` è `1.0`. Nel grafo unificato, l'arco 780 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `.flush()`. Nel grafo unificato, l'arco 780 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_flush` per la relazione che coinvolge il nodo canonico `.flush()`.

### .get()

<!-- GRAPH_NODE graph=codice index=363 id=traffic_exec_engine_mtlscontextfactory_get -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.get()` è `.get()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.get()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.get()` è `L385`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.get()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.get()` possiede l'identificatore esatto `traffic_exec_engine_mtlscontextfactory_get`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.get()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.get()` è `.get()`.
<!-- GRAPH_EDGE graph=codice index=743 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.get()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_resolve_secret()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_mtlscontextfactory_get` all'ID `traffic_exec_engine_resolve_secret`. La relazione tecnica esplicita è la seguente: `.get()` chiama `_resolve_secret()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 743 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 743 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 743 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 743 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 743 che coinvolge il nodo canonico `.get()` è `L397`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 743 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 743 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 743 registra come `source` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 743 registra come `target` l'identificatore `traffic_exec_engine_resolve_secret` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 743 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=762 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.get()` è la sorgente `source` della relazione `references` verso il nodo canonico `SSLContext`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_mtlscontextfactory_get` all'ID `sslcontext`. La relazione tecnica esplicita è la seguente: `.get()` fa riferimento a `SSLContext`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 762 che coinvolge il nodo canonico `.get()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 762 che coinvolge il nodo canonico `.get()` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 762 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 762 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 762 che coinvolge il nodo canonico `.get()` è `L385`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 762 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 762 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 762 registra come `source` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 762 registra come `target` l'identificatore `sslcontext` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 762 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=763 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.get()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._load_pkcs12()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_mtlscontextfactory_get` all'ID `traffic_exec_engine_mtlscontextfactory_load_pkcs12`. La relazione tecnica esplicita è la seguente: `.get()` chiama `._load_pkcs12()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 763 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 763 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 763 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 763 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 763 che coinvolge il nodo canonico `.get()` è `L416`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 763 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 763 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 763 registra come `source` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 763 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_load_pkcs12` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 763 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=716 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `resolve_placeholders()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_resolve_placeholders` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `resolve_placeholders()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 716 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 716 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 716 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 716 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 716 che coinvolge il nodo canonico `.get()` è `L114`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 716 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 716 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 716 registra come `source` l'identificatore `traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 716 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 716 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=719 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `unresolved_placeholders()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_unresolved_placeholders` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `unresolved_placeholders()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 719 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 719 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 719 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 719 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 719 che coinvolge il nodo canonico `.get()` è `L148`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 719 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 719 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 719 registra come `source` l'identificatore `traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 719 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 719 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=723 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `extract_outputs()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_outputs` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `extract_outputs()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 723 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 723 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 723 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 723 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 723 che coinvolge il nodo canonico `.get()` è `L177`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 723 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 723 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 723 registra come `source` l'identificatore `traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 723 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 723 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=725 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_extract_json_output()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_json_output` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `_extract_json_output()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 725 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 725 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 725 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 725 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 725 che coinvolge il nodo canonico `.get()` è `L194`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 725 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 725 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 725 registra come `source` l'identificatore `traffic_exec_engine_extract_json_output` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 725 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 725 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=728 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_extract_xml_output()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `_extract_xml_output()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 728 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 728 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 728 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 728 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 728 che coinvolge il nodo canonico `.get()` è `L212`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 728 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 728 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 728 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 728 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 728 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=732 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_extract_xml_output_stdlib()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output_stdlib` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `_extract_xml_output_stdlib()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 732 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 732 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 732 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 732 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 732 che coinvolge il nodo canonico `.get()` è `L243`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 732 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 732 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 732 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 732 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 732 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=739 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `validate_required_outputs()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_validate_required_outputs` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `validate_required_outputs()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 739 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 739 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 739 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 739 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 739 che coinvolge il nodo canonico `.get()` è `L334`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 739 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 739 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 739 registra come `source` l'identificatore `traffic_exec_engine_validate_required_outputs` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 739 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 739 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=740 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_correlation_headers()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_correlation_headers` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `_correlation_headers()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 740 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 740 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 740 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 740 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 740 che coinvolge il nodo canonico `.get()` è `L358`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 740 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 740 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 740 registra come `source` l'identificatore `traffic_exec_engine_correlation_headers` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 740 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 740 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=746 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MtlsContextFactory`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_mtlscontextfactory` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `MtlsContextFactory` registra come proprio metodo `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 746 che coinvolge il nodo canonico `.get()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 746 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 746 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 746 che coinvolge il nodo canonico `.get()` è `L385`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 746 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 746 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 746 registra come `source` l'identificatore `traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 746 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 746 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=752 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_mtlscontextfactory_init` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 752 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 752 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 752 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 752 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 752 che coinvolge il nodo canonico `.get()` è `L382`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 752 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 752 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 752 registra come `source` l'identificatore `traffic_exec_engine_mtlscontextfactory_init` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 752 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 752 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=753 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 753 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 753 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 753 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 753 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 753 che coinvolge il nodo canonico `.get()` è `L840`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 753 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 753 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 753 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 753 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 753 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=754 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._emit()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_emit` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `._emit()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 754 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 754 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 754 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 754 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 754 che coinvolge il nodo canonico `.get()` è `L643`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 754 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 754 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 754 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_emit` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 754 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 754 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=755 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.log_flow_completed()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_flow_completed` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `.log_flow_completed()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 755 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 755 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 755 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 755 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 755 che coinvolge il nodo canonico `.get()` è `L747`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 755 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 755 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 755 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_flow_completed` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 755 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 755 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=756 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.log_request_sent()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_request_sent` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `.log_request_sent()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 756 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 756 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 756 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 756 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 756 che coinvolge il nodo canonico `.get()` è `L688`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 756 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 756 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 756 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 756 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 756 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=757 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.log_response_received()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_response_received` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `.log_response_received()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 757 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 757 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 757 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 757 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 757 che coinvolge il nodo canonico `.get()` è `L723`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 757 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 757 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 757 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 757 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 757 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=758 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_faultinjector_init` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `.__init__() [traffic_exec_engine_faultinjector_init]` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 758 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 758 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 758 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 758 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 758 che coinvolge il nodo canonico `.get()` è `L475`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 758 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 758 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 758 registra come `source` l'identificatore `traffic_exec_engine_faultinjector_init` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 758 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 758 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=759 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._matches()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_faultinjector_matches` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `._matches()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 759 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 759 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 759 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 759 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 759 che coinvolge il nodo canonico `.get()` è `L527`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 759 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 759 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 759 registra come `source` l'identificatore `traffic_exec_engine_faultinjector_matches` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 759 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 759 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=760 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.plan()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_faultinjector_plan` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `.plan()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 760 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 760 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 760 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 760 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 760 che coinvolge il nodo canonico `.get()` è `L491`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 760 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 760 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 760 registra come `source` l'identificatore `traffic_exec_engine_faultinjector_plan` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 760 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 760 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=761 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `load_dataset_as_timeline()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_load_dataset_as_timeline` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `load_dataset_as_timeline()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 761 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 761 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 761 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 761 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 761 che coinvolge il nodo canonico `.get()` è `L1109`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 761 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 761 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 761 registra come `source` l'identificatore `traffic_exec_engine_load_dataset_as_timeline` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 761 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 761 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=764 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_run` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `run()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 764 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 764 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 764 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 764 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 764 che coinvolge il nodo canonico `.get()` è `L982`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 764 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 764 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 764 registra come `source` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 764 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 764 che coinvolge il nodo canonico `.get()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=765 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `send_request()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_send_request` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `send_request()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 765 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 765 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 765 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 765 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 765 che coinvolge il nodo canonico `.get()` è `L569`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 765 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 765 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo del codice RDG, l'arco 765 registra come `source` l'identificatore `traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, l'arco 765 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 765 che coinvolge il nodo canonico `.get()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=363 id=Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.get()` è `.get()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.get()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.get()` è `L385`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.get()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.get()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.get()` è `.get()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.get()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.get()` è `traffic_exec_engine_mtlscontextfactory_get`. Nel grafo unificato, il nodo canonico `.get()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`.
<!-- GRAPH_EDGE graph=unificata index=753 role=outgoing -->
Nel grafo unificato, il nodo canonico `.get()` è la sorgente `source` della relazione `calls` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `.get()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 753 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 753 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 753 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 753 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 753 che coinvolge il nodo canonico `.get()` è `L840`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 753 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 753 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 753 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 753 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 753 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=754 role=outgoing -->
Nel grafo unificato, il nodo canonico `.get()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._emit()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit`. La relazione tecnica esplicita è la seguente: `.get()` chiama `._emit()`. Nel grafo unificato, il tipo dichiarato dell'arco 754 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 754 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 754 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 754 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 754 che coinvolge il nodo canonico `.get()` è `L643`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 754 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 754 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 754 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 754 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 754 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=755 role=outgoing -->
Nel grafo unificato, il nodo canonico `.get()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.log_flow_completed()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_flow_completed`. La relazione tecnica esplicita è la seguente: `.get()` chiama `.log_flow_completed()`. Nel grafo unificato, il tipo dichiarato dell'arco 755 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 755 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 755 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 755 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 755 che coinvolge il nodo canonico `.get()` è `L747`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 755 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 755 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 755 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 755 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 755 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_flow_completed` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=756 role=outgoing -->
Nel grafo unificato, il nodo canonico `.get()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.log_request_sent()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent`. La relazione tecnica esplicita è la seguente: `.get()` chiama `.log_request_sent()`. Nel grafo unificato, il tipo dichiarato dell'arco 756 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 756 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 756 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 756 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 756 che coinvolge il nodo canonico `.get()` è `L688`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 756 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 756 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 756 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 756 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 756 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=757 role=outgoing -->
Nel grafo unificato, il nodo canonico `.get()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.log_response_received()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received`. La relazione tecnica esplicita è la seguente: `.get()` chiama `.log_response_received()`. Nel grafo unificato, il tipo dichiarato dell'arco 757 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 757 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 757 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 757 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 757 che coinvolge il nodo canonico `.get()` è `L723`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 757 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 757 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 757 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 757 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 757 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=758 role=outgoing -->
Nel grafo unificato, il nodo canonico `.get()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector_init`. La relazione tecnica esplicita è la seguente: `.get()` chiama `.__init__() [traffic_exec_engine_faultinjector_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 758 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 758 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 758 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 758 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 758 che coinvolge il nodo canonico `.get()` è `L475`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 758 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 758 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 758 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 758 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 758 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector_init` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=759 role=outgoing -->
Nel grafo unificato, il nodo canonico `.get()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._matches()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector_matches`. La relazione tecnica esplicita è la seguente: `.get()` chiama `._matches()`. Nel grafo unificato, il tipo dichiarato dell'arco 759 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 759 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 759 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 759 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 759 che coinvolge il nodo canonico `.get()` è `L527`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 759 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 759 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 759 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 759 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 759 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector_matches` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=760 role=outgoing -->
Nel grafo unificato, il nodo canonico `.get()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.plan()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan`. La relazione tecnica esplicita è la seguente: `.get()` chiama `.plan()`. Nel grafo unificato, il tipo dichiarato dell'arco 760 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 760 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 760 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 760 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 760 che coinvolge il nodo canonico `.get()` è `L491`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 760 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 760 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 760 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 760 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 760 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=761 role=outgoing -->
Nel grafo unificato, il nodo canonico `.get()` è la sorgente `source` della relazione `calls` verso il nodo canonico `load_dataset_as_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_load_dataset_as_timeline`. La relazione tecnica esplicita è la seguente: `.get()` chiama `load_dataset_as_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 761 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 761 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 761 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 761 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 761 che coinvolge il nodo canonico `.get()` è `L1109`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 761 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 761 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 761 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 761 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 761 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_load_dataset_as_timeline` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=762 role=outgoing -->
Nel grafo unificato, il nodo canonico `.get()` è la sorgente `source` della relazione `references` verso il nodo canonico `SSLContext`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::sslcontext`. La relazione tecnica esplicita è la seguente: `.get()` fa riferimento a `SSLContext`. Nel grafo unificato, il tipo dichiarato dell'arco 762 che coinvolge il nodo canonico `.get()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 762 che coinvolge il nodo canonico `.get()` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 762 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 762 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 762 che coinvolge il nodo canonico `.get()` è `L385`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 762 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 762 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 762 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 762 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 762 registra come `target` l'identificatore `Request-Dataset-Generator::sslcontext` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=763 role=outgoing -->
Nel grafo unificato, il nodo canonico `.get()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._load_pkcs12()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_load_pkcs12`. La relazione tecnica esplicita è la seguente: `.get()` chiama `._load_pkcs12()`. Nel grafo unificato, il tipo dichiarato dell'arco 763 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 763 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 763 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 763 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 763 che coinvolge il nodo canonico `.get()` è `L416`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 763 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 763 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 763 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 763 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 763 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_load_pkcs12` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=764 role=outgoing -->
Nel grafo unificato, il nodo canonico `.get()` è la sorgente `source` della relazione `calls` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.get()` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 764 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 764 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 764 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 764 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 764 che coinvolge il nodo canonico `.get()` è `L982`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 764 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 764 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 764 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 764 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 764 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=765 role=outgoing -->
Nel grafo unificato, il nodo canonico `.get()` è la sorgente `source` della relazione `calls` verso il nodo canonico `send_request()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_send_request`. La relazione tecnica esplicita è la seguente: `.get()` chiama `send_request()`. Nel grafo unificato, il tipo dichiarato dell'arco 765 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 765 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 765 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 765 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 765 che coinvolge il nodo canonico `.get()` è `L569`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 765 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 765 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 765 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 765 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 765 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=716 role=incoming -->
Nel grafo unificato, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `resolve_placeholders()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `resolve_placeholders()` chiama `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 716 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 716 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 716 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 716 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 716 che coinvolge il nodo canonico `.get()` è `L114`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 716 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 716 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 716 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 716 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 716 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=719 role=incoming -->
Nel grafo unificato, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `unresolved_placeholders()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `unresolved_placeholders()` chiama `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 719 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 719 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 719 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 719 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 719 che coinvolge il nodo canonico `.get()` è `L148`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 719 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 719 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 719 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 719 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 719 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=723 role=incoming -->
Nel grafo unificato, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `extract_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `extract_outputs()` chiama `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 723 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 723 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 723 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 723 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 723 che coinvolge il nodo canonico `.get()` è `L177`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 723 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 723 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 723 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 723 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 723 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=725 role=incoming -->
Nel grafo unificato, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_extract_json_output()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_json_output` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `_extract_json_output()` chiama `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 725 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 725 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 725 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 725 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 725 che coinvolge il nodo canonico `.get()` è `L194`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 725 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 725 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 725 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 725 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_json_output` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 725 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=728 role=incoming -->
Nel grafo unificato, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_extract_xml_output()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `_extract_xml_output()` chiama `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 728 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 728 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 728 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 728 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 728 che coinvolge il nodo canonico `.get()` è `L212`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 728 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 728 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 728 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 728 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 728 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=732 role=incoming -->
Nel grafo unificato, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_extract_xml_output_stdlib()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `_extract_xml_output_stdlib()` chiama `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 732 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 732 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 732 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 732 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 732 che coinvolge il nodo canonico `.get()` è `L243`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 732 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 732 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 732 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 732 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 732 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=739 role=incoming -->
Nel grafo unificato, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `validate_required_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_validate_required_outputs` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `validate_required_outputs()` chiama `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 739 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 739 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 739 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 739 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 739 che coinvolge il nodo canonico `.get()` è `L334`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 739 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 739 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 739 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 739 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_validate_required_outputs` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 739 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=740 role=incoming -->
Nel grafo unificato, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_correlation_headers()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_correlation_headers` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `_correlation_headers()` chiama `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 740 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 740 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 740 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 740 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 740 che coinvolge il nodo canonico `.get()` è `L358`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 740 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 740 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 740 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 740 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_correlation_headers` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 740 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=743 role=incoming -->
Nel grafo unificato, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_resolve_secret()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_resolve_secret` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `_resolve_secret()` chiama `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 743 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 743 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 743 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 743 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 743 che coinvolge il nodo canonico `.get()` è `L397`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 743 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 743 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 743 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 743 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_resolve_secret` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 743 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=746 role=incoming -->
Nel grafo unificato, il nodo canonico `.get()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MtlsContextFactory`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `MtlsContextFactory` registra come proprio metodo `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 746 che coinvolge il nodo canonico `.get()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 746 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 746 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 746 che coinvolge il nodo canonico `.get()` è `L385`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 746 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 746 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 746 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 746 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 746 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`.
<!-- GRAPH_EDGE graph=unificata index=752 role=incoming -->
Nel grafo unificato, il nodo canonico `.get()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_init` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `.__init__() [traffic_exec_engine_mtlscontextfactory_init]` chiama `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 752 che coinvolge il nodo canonico `.get()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 752 che coinvolge il nodo canonico `.get()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 752 che coinvolge il nodo canonico `.get()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 752 che coinvolge il nodo canonico `.get()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 752 che coinvolge il nodo canonico `.get()` è `L382`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 752 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 752 che coinvolge il nodo canonico `.get()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 752 che coinvolge il nodo canonico `.get()` è `1.0`. Nel grafo unificato, l'arco 752 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_init` per la relazione che coinvolge il nodo canonico `.get()`. Nel grafo unificato, l'arco 752 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.get()`.

### .log_flow_completed()

<!-- GRAPH_NODE graph=codice index=376 id=traffic_exec_engine_executionlogger_log_flow_completed -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.log_flow_completed()` è `.log_flow_completed()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.log_flow_completed()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.log_flow_completed()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.log_flow_completed()` è `L738`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.log_flow_completed()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.log_flow_completed()` possiede l'identificatore esatto `traffic_exec_engine_executionlogger_log_flow_completed`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.log_flow_completed()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.log_flow_completed()` è `.log_flow_completed()`.
<!-- GRAPH_EDGE graph=codice index=755 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.log_flow_completed()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_flow_completed` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `.log_flow_completed()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 755 che coinvolge il nodo canonico `.log_flow_completed()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 755 che coinvolge il nodo canonico `.log_flow_completed()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 755 che coinvolge il nodo canonico `.log_flow_completed()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 755 che coinvolge il nodo canonico `.log_flow_completed()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 755 che coinvolge il nodo canonico `.log_flow_completed()` è `L747`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 755 che coinvolge il nodo canonico `.log_flow_completed()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 755 che coinvolge il nodo canonico `.log_flow_completed()` è `ast`. Nel grafo del codice RDG, l'arco 755 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_flow_completed` per la relazione che coinvolge il nodo canonico `.log_flow_completed()`. Nel grafo del codice RDG, l'arco 755 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.log_flow_completed()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 755 che coinvolge il nodo canonico `.log_flow_completed()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=788 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.log_flow_completed()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._emit()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_flow_completed` all'ID `traffic_exec_engine_executionlogger_emit`. La relazione tecnica esplicita è la seguente: `.log_flow_completed()` chiama `._emit()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 788 che coinvolge il nodo canonico `.log_flow_completed()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 788 che coinvolge il nodo canonico `.log_flow_completed()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 788 che coinvolge il nodo canonico `.log_flow_completed()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 788 che coinvolge il nodo canonico `.log_flow_completed()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 788 che coinvolge il nodo canonico `.log_flow_completed()` è `L745`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 788 che coinvolge il nodo canonico `.log_flow_completed()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 788 che coinvolge il nodo canonico `.log_flow_completed()` è `ast`. Nel grafo del codice RDG, l'arco 788 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_flow_completed` per la relazione che coinvolge il nodo canonico `.log_flow_completed()`. Nel grafo del codice RDG, l'arco 788 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_emit` per la relazione che coinvolge il nodo canonico `.log_flow_completed()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 788 che coinvolge il nodo canonico `.log_flow_completed()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=782 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.log_flow_completed()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger` all'ID `traffic_exec_engine_executionlogger_log_flow_completed`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.log_flow_completed()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 782 che coinvolge il nodo canonico `.log_flow_completed()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 782 che coinvolge il nodo canonico `.log_flow_completed()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 782 che coinvolge il nodo canonico `.log_flow_completed()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 782 che coinvolge il nodo canonico `.log_flow_completed()` è `L738`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 782 che coinvolge il nodo canonico `.log_flow_completed()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 782 che coinvolge il nodo canonico `.log_flow_completed()` è `ast`. Nel grafo del codice RDG, l'arco 782 registra come `source` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `.log_flow_completed()`. Nel grafo del codice RDG, l'arco 782 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_log_flow_completed` per la relazione che coinvolge il nodo canonico `.log_flow_completed()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 782 che coinvolge il nodo canonico `.log_flow_completed()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=793 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.log_flow_completed()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_executionlogger_log_flow_completed`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `.log_flow_completed()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 793 che coinvolge il nodo canonico `.log_flow_completed()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 793 che coinvolge il nodo canonico `.log_flow_completed()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 793 che coinvolge il nodo canonico `.log_flow_completed()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 793 che coinvolge il nodo canonico `.log_flow_completed()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 793 che coinvolge il nodo canonico `.log_flow_completed()` è `L954`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 793 che coinvolge il nodo canonico `.log_flow_completed()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 793 che coinvolge il nodo canonico `.log_flow_completed()` è `ast`. Nel grafo del codice RDG, l'arco 793 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `.log_flow_completed()`. Nel grafo del codice RDG, l'arco 793 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_log_flow_completed` per la relazione che coinvolge il nodo canonico `.log_flow_completed()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 793 che coinvolge il nodo canonico `.log_flow_completed()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=376 id=Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_flow_completed -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.log_flow_completed()` è `.log_flow_completed()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.log_flow_completed()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.log_flow_completed()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.log_flow_completed()` è `L738`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.log_flow_completed()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.log_flow_completed()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.log_flow_completed()` è `.log_flow_completed()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.log_flow_completed()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.log_flow_completed()` è `traffic_exec_engine_executionlogger_log_flow_completed`. Nel grafo unificato, il nodo canonico `.log_flow_completed()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_flow_completed`.
<!-- GRAPH_EDGE graph=unificata index=793 role=outgoing -->
Nel grafo unificato, il nodo canonico `.log_flow_completed()` è la sorgente `source` della relazione `calls` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_flow_completed` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `.log_flow_completed()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 793 che coinvolge il nodo canonico `.log_flow_completed()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 793 che coinvolge il nodo canonico `.log_flow_completed()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 793 che coinvolge il nodo canonico `.log_flow_completed()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 793 che coinvolge il nodo canonico `.log_flow_completed()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 793 che coinvolge il nodo canonico `.log_flow_completed()` è `L954`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 793 che coinvolge il nodo canonico `.log_flow_completed()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 793 che coinvolge il nodo canonico `.log_flow_completed()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 793 che coinvolge il nodo canonico `.log_flow_completed()` è `1.0`. Nel grafo unificato, l'arco 793 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_flow_completed` per la relazione che coinvolge il nodo canonico `.log_flow_completed()`. Nel grafo unificato, l'arco 793 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `.log_flow_completed()`.
<!-- GRAPH_EDGE graph=unificata index=755 role=incoming -->
Nel grafo unificato, il nodo canonico `.log_flow_completed()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_flow_completed`. La relazione tecnica esplicita è la seguente: `.get()` chiama `.log_flow_completed()`. Nel grafo unificato, il tipo dichiarato dell'arco 755 che coinvolge il nodo canonico `.log_flow_completed()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 755 che coinvolge il nodo canonico `.log_flow_completed()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 755 che coinvolge il nodo canonico `.log_flow_completed()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 755 che coinvolge il nodo canonico `.log_flow_completed()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 755 che coinvolge il nodo canonico `.log_flow_completed()` è `L747`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 755 che coinvolge il nodo canonico `.log_flow_completed()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 755 che coinvolge il nodo canonico `.log_flow_completed()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 755 che coinvolge il nodo canonico `.log_flow_completed()` è `1.0`. Nel grafo unificato, l'arco 755 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.log_flow_completed()`. Nel grafo unificato, l'arco 755 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_flow_completed` per la relazione che coinvolge il nodo canonico `.log_flow_completed()`.
<!-- GRAPH_EDGE graph=unificata index=782 role=incoming -->
Nel grafo unificato, il nodo canonico `.log_flow_completed()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_flow_completed`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.log_flow_completed()`. Nel grafo unificato, il tipo dichiarato dell'arco 782 che coinvolge il nodo canonico `.log_flow_completed()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 782 che coinvolge il nodo canonico `.log_flow_completed()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 782 che coinvolge il nodo canonico `.log_flow_completed()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 782 che coinvolge il nodo canonico `.log_flow_completed()` è `L738`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 782 che coinvolge il nodo canonico `.log_flow_completed()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 782 che coinvolge il nodo canonico `.log_flow_completed()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 782 che coinvolge il nodo canonico `.log_flow_completed()` è `1.0`. Nel grafo unificato, l'arco 782 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `.log_flow_completed()`. Nel grafo unificato, l'arco 782 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_flow_completed` per la relazione che coinvolge il nodo canonico `.log_flow_completed()`.
<!-- GRAPH_EDGE graph=unificata index=788 role=incoming -->
Nel grafo unificato, il nodo canonico `.log_flow_completed()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._emit()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_flow_completed`. La relazione tecnica esplicita è la seguente: `._emit()` chiama `.log_flow_completed()`. Nel grafo unificato, il tipo dichiarato dell'arco 788 che coinvolge il nodo canonico `.log_flow_completed()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 788 che coinvolge il nodo canonico `.log_flow_completed()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 788 che coinvolge il nodo canonico `.log_flow_completed()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 788 che coinvolge il nodo canonico `.log_flow_completed()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 788 che coinvolge il nodo canonico `.log_flow_completed()` è `L745`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 788 che coinvolge il nodo canonico `.log_flow_completed()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 788 che coinvolge il nodo canonico `.log_flow_completed()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 788 che coinvolge il nodo canonico `.log_flow_completed()` è `1.0`. Nel grafo unificato, l'arco 788 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit` per la relazione che coinvolge il nodo canonico `.log_flow_completed()`. Nel grafo unificato, l'arco 788 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_flow_completed` per la relazione che coinvolge il nodo canonico `.log_flow_completed()`.

### .log_request_sent()

<!-- GRAPH_NODE graph=codice index=374 id=traffic_exec_engine_executionlogger_log_request_sent -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.log_request_sent()` è `.log_request_sent()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.log_request_sent()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.log_request_sent()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.log_request_sent()` è `L674`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.log_request_sent()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.log_request_sent()` possiede l'identificatore esatto `traffic_exec_engine_executionlogger_log_request_sent`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.log_request_sent()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.log_request_sent()` è `.log_request_sent()`.
<!-- GRAPH_EDGE graph=codice index=698 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.log_request_sent()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_now_iso()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_request_sent` all'ID `traffic_exec_engine_now_iso`. La relazione tecnica esplicita è la seguente: `.log_request_sent()` chiama `_now_iso()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 698 che coinvolge il nodo canonico `.log_request_sent()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 698 che coinvolge il nodo canonico `.log_request_sent()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 698 che coinvolge il nodo canonico `.log_request_sent()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 698 che coinvolge il nodo canonico `.log_request_sent()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 698 che coinvolge il nodo canonico `.log_request_sent()` è `L686`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 698 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 698 che coinvolge il nodo canonico `.log_request_sent()` è `ast`. Nel grafo del codice RDG, l'arco 698 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `.log_request_sent()`. Nel grafo del codice RDG, l'arco 698 registra come `target` l'identificatore `traffic_exec_engine_now_iso` per la relazione che coinvolge il nodo canonico `.log_request_sent()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 698 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=700 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.log_request_sent()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_now_epoch_ms()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_request_sent` all'ID `traffic_exec_engine_now_epoch_ms`. La relazione tecnica esplicita è la seguente: `.log_request_sent()` chiama `_now_epoch_ms()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 700 che coinvolge il nodo canonico `.log_request_sent()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 700 che coinvolge il nodo canonico `.log_request_sent()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 700 che coinvolge il nodo canonico `.log_request_sent()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 700 che coinvolge il nodo canonico `.log_request_sent()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 700 che coinvolge il nodo canonico `.log_request_sent()` è `L687`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 700 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 700 che coinvolge il nodo canonico `.log_request_sent()` è `ast`. Nel grafo del codice RDG, l'arco 700 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `.log_request_sent()`. Nel grafo del codice RDG, l'arco 700 registra come `target` l'identificatore `traffic_exec_engine_now_epoch_ms` per la relazione che coinvolge il nodo canonico `.log_request_sent()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 700 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=756 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.log_request_sent()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_request_sent` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `.log_request_sent()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 756 che coinvolge il nodo canonico `.log_request_sent()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 756 che coinvolge il nodo canonico `.log_request_sent()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 756 che coinvolge il nodo canonico `.log_request_sent()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 756 che coinvolge il nodo canonico `.log_request_sent()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 756 che coinvolge il nodo canonico `.log_request_sent()` è `L688`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 756 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 756 che coinvolge il nodo canonico `.log_request_sent()` è `ast`. Nel grafo del codice RDG, l'arco 756 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `.log_request_sent()`. Nel grafo del codice RDG, l'arco 756 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.log_request_sent()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 756 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=789 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.log_request_sent()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._emit()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_request_sent` all'ID `traffic_exec_engine_executionlogger_emit`. La relazione tecnica esplicita è la seguente: `.log_request_sent()` chiama `._emit()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 789 che coinvolge il nodo canonico `.log_request_sent()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 789 che coinvolge il nodo canonico `.log_request_sent()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 789 che coinvolge il nodo canonico `.log_request_sent()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 789 che coinvolge il nodo canonico `.log_request_sent()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 789 che coinvolge il nodo canonico `.log_request_sent()` è `L684`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 789 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 789 che coinvolge il nodo canonico `.log_request_sent()` è `ast`. Nel grafo del codice RDG, l'arco 789 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `.log_request_sent()`. Nel grafo del codice RDG, l'arco 789 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_emit` per la relazione che coinvolge il nodo canonico `.log_request_sent()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 789 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=783 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.log_request_sent()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger` all'ID `traffic_exec_engine_executionlogger_log_request_sent`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.log_request_sent()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 783 che coinvolge il nodo canonico `.log_request_sent()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 783 che coinvolge il nodo canonico `.log_request_sent()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 783 che coinvolge il nodo canonico `.log_request_sent()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 783 che coinvolge il nodo canonico `.log_request_sent()` è `L674`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 783 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 783 che coinvolge il nodo canonico `.log_request_sent()` è `ast`. Nel grafo del codice RDG, l'arco 783 registra come `source` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `.log_request_sent()`. Nel grafo del codice RDG, l'arco 783 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `.log_request_sent()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 783 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=791 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.log_request_sent()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_executionlogger_log_request_sent`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `.log_request_sent()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 791 che coinvolge il nodo canonico `.log_request_sent()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 791 che coinvolge il nodo canonico `.log_request_sent()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 791 che coinvolge il nodo canonico `.log_request_sent()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 791 che coinvolge il nodo canonico `.log_request_sent()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 791 che coinvolge il nodo canonico `.log_request_sent()` è `L859`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 791 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 791 che coinvolge il nodo canonico `.log_request_sent()` è `ast`. Nel grafo del codice RDG, l'arco 791 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `.log_request_sent()`. Nel grafo del codice RDG, l'arco 791 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `.log_request_sent()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 791 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=374 id=Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.log_request_sent()` è `.log_request_sent()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.log_request_sent()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.log_request_sent()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.log_request_sent()` è `L674`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.log_request_sent()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.log_request_sent()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.log_request_sent()` è `.log_request_sent()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.log_request_sent()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.log_request_sent()` è `traffic_exec_engine_executionlogger_log_request_sent`. Nel grafo unificato, il nodo canonico `.log_request_sent()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent`.
<!-- GRAPH_EDGE graph=unificata index=791 role=outgoing -->
Nel grafo unificato, il nodo canonico `.log_request_sent()` è la sorgente `source` della relazione `calls` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `.log_request_sent()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 791 che coinvolge il nodo canonico `.log_request_sent()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 791 che coinvolge il nodo canonico `.log_request_sent()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 791 che coinvolge il nodo canonico `.log_request_sent()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 791 che coinvolge il nodo canonico `.log_request_sent()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 791 che coinvolge il nodo canonico `.log_request_sent()` è `L859`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 791 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 791 che coinvolge il nodo canonico `.log_request_sent()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 791 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`. Nel grafo unificato, l'arco 791 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `.log_request_sent()`. Nel grafo unificato, l'arco 791 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `.log_request_sent()`.
<!-- GRAPH_EDGE graph=unificata index=698 role=incoming -->
Nel grafo unificato, il nodo canonico `.log_request_sent()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_now_iso()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_now_iso` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent`. La relazione tecnica esplicita è la seguente: `_now_iso()` chiama `.log_request_sent()`. Nel grafo unificato, il tipo dichiarato dell'arco 698 che coinvolge il nodo canonico `.log_request_sent()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 698 che coinvolge il nodo canonico `.log_request_sent()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 698 che coinvolge il nodo canonico `.log_request_sent()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 698 che coinvolge il nodo canonico `.log_request_sent()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 698 che coinvolge il nodo canonico `.log_request_sent()` è `L686`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 698 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 698 che coinvolge il nodo canonico `.log_request_sent()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 698 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`. Nel grafo unificato, l'arco 698 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_now_iso` per la relazione che coinvolge il nodo canonico `.log_request_sent()`. Nel grafo unificato, l'arco 698 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `.log_request_sent()`.
<!-- GRAPH_EDGE graph=unificata index=700 role=incoming -->
Nel grafo unificato, il nodo canonico `.log_request_sent()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_now_epoch_ms()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_now_epoch_ms` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent`. La relazione tecnica esplicita è la seguente: `_now_epoch_ms()` chiama `.log_request_sent()`. Nel grafo unificato, il tipo dichiarato dell'arco 700 che coinvolge il nodo canonico `.log_request_sent()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 700 che coinvolge il nodo canonico `.log_request_sent()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 700 che coinvolge il nodo canonico `.log_request_sent()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 700 che coinvolge il nodo canonico `.log_request_sent()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 700 che coinvolge il nodo canonico `.log_request_sent()` è `L687`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 700 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 700 che coinvolge il nodo canonico `.log_request_sent()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 700 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`. Nel grafo unificato, l'arco 700 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_now_epoch_ms` per la relazione che coinvolge il nodo canonico `.log_request_sent()`. Nel grafo unificato, l'arco 700 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `.log_request_sent()`.
<!-- GRAPH_EDGE graph=unificata index=756 role=incoming -->
Nel grafo unificato, il nodo canonico `.log_request_sent()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent`. La relazione tecnica esplicita è la seguente: `.get()` chiama `.log_request_sent()`. Nel grafo unificato, il tipo dichiarato dell'arco 756 che coinvolge il nodo canonico `.log_request_sent()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 756 che coinvolge il nodo canonico `.log_request_sent()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 756 che coinvolge il nodo canonico `.log_request_sent()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 756 che coinvolge il nodo canonico `.log_request_sent()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 756 che coinvolge il nodo canonico `.log_request_sent()` è `L688`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 756 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 756 che coinvolge il nodo canonico `.log_request_sent()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 756 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`. Nel grafo unificato, l'arco 756 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.log_request_sent()`. Nel grafo unificato, l'arco 756 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `.log_request_sent()`.
<!-- GRAPH_EDGE graph=unificata index=783 role=incoming -->
Nel grafo unificato, il nodo canonico `.log_request_sent()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.log_request_sent()`. Nel grafo unificato, il tipo dichiarato dell'arco 783 che coinvolge il nodo canonico `.log_request_sent()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 783 che coinvolge il nodo canonico `.log_request_sent()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 783 che coinvolge il nodo canonico `.log_request_sent()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 783 che coinvolge il nodo canonico `.log_request_sent()` è `L674`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 783 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 783 che coinvolge il nodo canonico `.log_request_sent()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 783 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`. Nel grafo unificato, l'arco 783 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `.log_request_sent()`. Nel grafo unificato, l'arco 783 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `.log_request_sent()`.
<!-- GRAPH_EDGE graph=unificata index=789 role=incoming -->
Nel grafo unificato, il nodo canonico `.log_request_sent()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._emit()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent`. La relazione tecnica esplicita è la seguente: `._emit()` chiama `.log_request_sent()`. Nel grafo unificato, il tipo dichiarato dell'arco 789 che coinvolge il nodo canonico `.log_request_sent()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 789 che coinvolge il nodo canonico `.log_request_sent()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 789 che coinvolge il nodo canonico `.log_request_sent()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 789 che coinvolge il nodo canonico `.log_request_sent()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 789 che coinvolge il nodo canonico `.log_request_sent()` è `L684`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 789 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 789 che coinvolge il nodo canonico `.log_request_sent()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 789 che coinvolge il nodo canonico `.log_request_sent()` è `1.0`. Nel grafo unificato, l'arco 789 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit` per la relazione che coinvolge il nodo canonico `.log_request_sent()`. Nel grafo unificato, l'arco 789 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `.log_request_sent()`.

### .log_response_received()

<!-- GRAPH_NODE graph=codice index=375 id=traffic_exec_engine_executionlogger_log_response_received -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.log_response_received()` è `.log_response_received()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.log_response_received()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.log_response_received()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.log_response_received()` è `L708`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.log_response_received()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.log_response_received()` possiede l'identificatore esatto `traffic_exec_engine_executionlogger_log_response_received`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.log_response_received()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.log_response_received()` è `.log_response_received()`.
<!-- GRAPH_EDGE graph=codice index=699 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.log_response_received()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_now_iso()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_response_received` all'ID `traffic_exec_engine_now_iso`. La relazione tecnica esplicita è la seguente: `.log_response_received()` chiama `_now_iso()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 699 che coinvolge il nodo canonico `.log_response_received()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 699 che coinvolge il nodo canonico `.log_response_received()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 699 che coinvolge il nodo canonico `.log_response_received()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 699 che coinvolge il nodo canonico `.log_response_received()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 699 che coinvolge il nodo canonico `.log_response_received()` è `L721`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 699 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 699 che coinvolge il nodo canonico `.log_response_received()` è `ast`. Nel grafo del codice RDG, l'arco 699 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo del codice RDG, l'arco 699 registra come `target` l'identificatore `traffic_exec_engine_now_iso` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 699 che coinvolge il nodo canonico `.log_response_received()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=701 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.log_response_received()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_now_epoch_ms()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_response_received` all'ID `traffic_exec_engine_now_epoch_ms`. La relazione tecnica esplicita è la seguente: `.log_response_received()` chiama `_now_epoch_ms()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 701 che coinvolge il nodo canonico `.log_response_received()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 701 che coinvolge il nodo canonico `.log_response_received()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 701 che coinvolge il nodo canonico `.log_response_received()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 701 che coinvolge il nodo canonico `.log_response_received()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 701 che coinvolge il nodo canonico `.log_response_received()` è `L722`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 701 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 701 che coinvolge il nodo canonico `.log_response_received()` è `ast`. Nel grafo del codice RDG, l'arco 701 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo del codice RDG, l'arco 701 registra come `target` l'identificatore `traffic_exec_engine_now_epoch_ms` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 701 che coinvolge il nodo canonico `.log_response_received()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=702 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.log_response_received()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_response_size()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_response_received` all'ID `traffic_exec_engine_response_size`. La relazione tecnica esplicita è la seguente: `.log_response_received()` chiama `_response_size()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 702 che coinvolge il nodo canonico `.log_response_received()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 702 che coinvolge il nodo canonico `.log_response_received()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 702 che coinvolge il nodo canonico `.log_response_received()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 702 che coinvolge il nodo canonico `.log_response_received()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 702 che coinvolge il nodo canonico `.log_response_received()` è `L730`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 702 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 702 che coinvolge il nodo canonico `.log_response_received()` è `ast`. Nel grafo del codice RDG, l'arco 702 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo del codice RDG, l'arco 702 registra come `target` l'identificatore `traffic_exec_engine_response_size` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 702 che coinvolge il nodo canonico `.log_response_received()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=705 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.log_response_received()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [traffic_exec_engine_py_any]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_response_received` all'ID `traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `.log_response_received()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 705 che coinvolge il nodo canonico `.log_response_received()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 705 che coinvolge il nodo canonico `.log_response_received()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 705 che coinvolge il nodo canonico `.log_response_received()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 705 che coinvolge il nodo canonico `.log_response_received()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 705 che coinvolge il nodo canonico `.log_response_received()` è `L708`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 705 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 705 che coinvolge il nodo canonico `.log_response_received()` è `ast`. Nel grafo del codice RDG, l'arco 705 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo del codice RDG, l'arco 705 registra come `target` l'identificatore `traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 705 che coinvolge il nodo canonico `.log_response_received()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=757 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.log_response_received()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_response_received` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `.log_response_received()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 757 che coinvolge il nodo canonico `.log_response_received()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 757 che coinvolge il nodo canonico `.log_response_received()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 757 che coinvolge il nodo canonico `.log_response_received()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 757 che coinvolge il nodo canonico `.log_response_received()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 757 che coinvolge il nodo canonico `.log_response_received()` è `L723`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 757 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 757 che coinvolge il nodo canonico `.log_response_received()` è `ast`. Nel grafo del codice RDG, l'arco 757 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo del codice RDG, l'arco 757 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 757 che coinvolge il nodo canonico `.log_response_received()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=790 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.log_response_received()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._emit()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_response_received` all'ID `traffic_exec_engine_executionlogger_emit`. La relazione tecnica esplicita è la seguente: `.log_response_received()` chiama `._emit()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 790 che coinvolge il nodo canonico `.log_response_received()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 790 che coinvolge il nodo canonico `.log_response_received()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 790 che coinvolge il nodo canonico `.log_response_received()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 790 che coinvolge il nodo canonico `.log_response_received()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 790 che coinvolge il nodo canonico `.log_response_received()` è `L719`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 790 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 790 che coinvolge il nodo canonico `.log_response_received()` è `ast`. Nel grafo del codice RDG, l'arco 790 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo del codice RDG, l'arco 790 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_emit` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 790 che coinvolge il nodo canonico `.log_response_received()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=784 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.log_response_received()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger` all'ID `traffic_exec_engine_executionlogger_log_response_received`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.log_response_received()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 784 che coinvolge il nodo canonico `.log_response_received()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 784 che coinvolge il nodo canonico `.log_response_received()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 784 che coinvolge il nodo canonico `.log_response_received()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 784 che coinvolge il nodo canonico `.log_response_received()` è `L708`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 784 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 784 che coinvolge il nodo canonico `.log_response_received()` è `ast`. Nel grafo del codice RDG, l'arco 784 registra come `source` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo del codice RDG, l'arco 784 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 784 che coinvolge il nodo canonico `.log_response_received()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=792 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.log_response_received()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_executionlogger_log_response_received`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `.log_response_received()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 792 che coinvolge il nodo canonico `.log_response_received()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 792 che coinvolge il nodo canonico `.log_response_received()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 792 che coinvolge il nodo canonico `.log_response_received()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 792 che coinvolge il nodo canonico `.log_response_received()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 792 che coinvolge il nodo canonico `.log_response_received()` è `L931`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 792 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 792 che coinvolge il nodo canonico `.log_response_received()` è `ast`. Nel grafo del codice RDG, l'arco 792 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo del codice RDG, l'arco 792 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 792 che coinvolge il nodo canonico `.log_response_received()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=375 id=Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.log_response_received()` è `.log_response_received()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.log_response_received()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.log_response_received()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.log_response_received()` è `L708`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.log_response_received()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.log_response_received()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.log_response_received()` è `.log_response_received()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.log_response_received()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.log_response_received()` è `traffic_exec_engine_executionlogger_log_response_received`. Nel grafo unificato, il nodo canonico `.log_response_received()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received`.
<!-- GRAPH_EDGE graph=unificata index=792 role=outgoing -->
Nel grafo unificato, il nodo canonico `.log_response_received()` è la sorgente `source` della relazione `calls` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `.log_response_received()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 792 che coinvolge il nodo canonico `.log_response_received()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 792 che coinvolge il nodo canonico `.log_response_received()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 792 che coinvolge il nodo canonico `.log_response_received()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 792 che coinvolge il nodo canonico `.log_response_received()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 792 che coinvolge il nodo canonico `.log_response_received()` è `L931`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 792 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 792 che coinvolge il nodo canonico `.log_response_received()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 792 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo unificato, l'arco 792 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo unificato, l'arco 792 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `.log_response_received()`.
<!-- GRAPH_EDGE graph=unificata index=699 role=incoming -->
Nel grafo unificato, il nodo canonico `.log_response_received()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_now_iso()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_now_iso` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received`. La relazione tecnica esplicita è la seguente: `_now_iso()` chiama `.log_response_received()`. Nel grafo unificato, il tipo dichiarato dell'arco 699 che coinvolge il nodo canonico `.log_response_received()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 699 che coinvolge il nodo canonico `.log_response_received()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 699 che coinvolge il nodo canonico `.log_response_received()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 699 che coinvolge il nodo canonico `.log_response_received()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 699 che coinvolge il nodo canonico `.log_response_received()` è `L721`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 699 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 699 che coinvolge il nodo canonico `.log_response_received()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 699 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo unificato, l'arco 699 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_now_iso` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo unificato, l'arco 699 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `.log_response_received()`.
<!-- GRAPH_EDGE graph=unificata index=701 role=incoming -->
Nel grafo unificato, il nodo canonico `.log_response_received()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_now_epoch_ms()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_now_epoch_ms` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received`. La relazione tecnica esplicita è la seguente: `_now_epoch_ms()` chiama `.log_response_received()`. Nel grafo unificato, il tipo dichiarato dell'arco 701 che coinvolge il nodo canonico `.log_response_received()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 701 che coinvolge il nodo canonico `.log_response_received()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 701 che coinvolge il nodo canonico `.log_response_received()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 701 che coinvolge il nodo canonico `.log_response_received()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 701 che coinvolge il nodo canonico `.log_response_received()` è `L722`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 701 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 701 che coinvolge il nodo canonico `.log_response_received()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 701 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo unificato, l'arco 701 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_now_epoch_ms` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo unificato, l'arco 701 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `.log_response_received()`.
<!-- GRAPH_EDGE graph=unificata index=702 role=incoming -->
Nel grafo unificato, il nodo canonico `.log_response_received()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_response_size()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_response_size` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received`. La relazione tecnica esplicita è la seguente: `_response_size()` chiama `.log_response_received()`. Nel grafo unificato, il tipo dichiarato dell'arco 702 che coinvolge il nodo canonico `.log_response_received()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 702 che coinvolge il nodo canonico `.log_response_received()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 702 che coinvolge il nodo canonico `.log_response_received()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 702 che coinvolge il nodo canonico `.log_response_received()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 702 che coinvolge il nodo canonico `.log_response_received()` è `L730`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 702 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 702 che coinvolge il nodo canonico `.log_response_received()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 702 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo unificato, l'arco 702 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_response_size` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo unificato, l'arco 702 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `.log_response_received()`.
<!-- GRAPH_EDGE graph=unificata index=705 role=incoming -->
Nel grafo unificato, il nodo canonico `.log_response_received()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Any [traffic_exec_engine_py_any]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_py_any` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received`. La relazione tecnica esplicita è la seguente: `Any [traffic_exec_engine_py_any]` fa riferimento a `.log_response_received()`. Nel grafo unificato, il tipo dichiarato dell'arco 705 che coinvolge il nodo canonico `.log_response_received()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 705 che coinvolge il nodo canonico `.log_response_received()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 705 che coinvolge il nodo canonico `.log_response_received()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 705 che coinvolge il nodo canonico `.log_response_received()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 705 che coinvolge il nodo canonico `.log_response_received()` è `L708`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 705 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 705 che coinvolge il nodo canonico `.log_response_received()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 705 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo unificato, l'arco 705 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo unificato, l'arco 705 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `.log_response_received()`.
<!-- GRAPH_EDGE graph=unificata index=757 role=incoming -->
Nel grafo unificato, il nodo canonico `.log_response_received()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received`. La relazione tecnica esplicita è la seguente: `.get()` chiama `.log_response_received()`. Nel grafo unificato, il tipo dichiarato dell'arco 757 che coinvolge il nodo canonico `.log_response_received()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 757 che coinvolge il nodo canonico `.log_response_received()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 757 che coinvolge il nodo canonico `.log_response_received()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 757 che coinvolge il nodo canonico `.log_response_received()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 757 che coinvolge il nodo canonico `.log_response_received()` è `L723`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 757 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 757 che coinvolge il nodo canonico `.log_response_received()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 757 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo unificato, l'arco 757 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo unificato, l'arco 757 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `.log_response_received()`.
<!-- GRAPH_EDGE graph=unificata index=784 role=incoming -->
Nel grafo unificato, il nodo canonico `.log_response_received()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.log_response_received()`. Nel grafo unificato, il tipo dichiarato dell'arco 784 che coinvolge il nodo canonico `.log_response_received()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 784 che coinvolge il nodo canonico `.log_response_received()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 784 che coinvolge il nodo canonico `.log_response_received()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 784 che coinvolge il nodo canonico `.log_response_received()` è `L708`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 784 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 784 che coinvolge il nodo canonico `.log_response_received()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 784 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo unificato, l'arco 784 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo unificato, l'arco 784 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `.log_response_received()`.
<!-- GRAPH_EDGE graph=unificata index=790 role=incoming -->
Nel grafo unificato, il nodo canonico `.log_response_received()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._emit()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received`. La relazione tecnica esplicita è la seguente: `._emit()` chiama `.log_response_received()`. Nel grafo unificato, il tipo dichiarato dell'arco 790 che coinvolge il nodo canonico `.log_response_received()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 790 che coinvolge il nodo canonico `.log_response_received()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 790 che coinvolge il nodo canonico `.log_response_received()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 790 che coinvolge il nodo canonico `.log_response_received()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 790 che coinvolge il nodo canonico `.log_response_received()` è `L719`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 790 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 790 che coinvolge il nodo canonico `.log_response_received()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 790 che coinvolge il nodo canonico `.log_response_received()` è `1.0`. Nel grafo unificato, l'arco 790 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit` per la relazione che coinvolge il nodo canonico `.log_response_received()`. Nel grafo unificato, l'arco 790 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `.log_response_received()`.

### .plan()

<!-- GRAPH_NODE graph=codice index=368 id=traffic_exec_engine_faultinjector_plan -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.plan()` è `.plan()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.plan()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.plan()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.plan()` è `L480`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.plan()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.plan()` possiede l'identificatore esatto `traffic_exec_engine_faultinjector_plan`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.plan()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.plan()` è `.plan()`.
<!-- GRAPH_EDGE graph=codice index=713 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.plan()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_stable_unit_interval()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_faultinjector_plan` all'ID `traffic_exec_engine_stable_unit_interval`. La relazione tecnica esplicita è la seguente: `.plan()` chiama `_stable_unit_interval()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 713 che coinvolge il nodo canonico `.plan()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 713 che coinvolge il nodo canonico `.plan()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 713 che coinvolge il nodo canonico `.plan()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 713 che coinvolge il nodo canonico `.plan()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 713 che coinvolge il nodo canonico `.plan()` è `L498`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 713 che coinvolge il nodo canonico `.plan()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 713 che coinvolge il nodo canonico `.plan()` è `ast`. Nel grafo del codice RDG, l'arco 713 registra come `source` l'identificatore `traffic_exec_engine_faultinjector_plan` per la relazione che coinvolge il nodo canonico `.plan()`. Nel grafo del codice RDG, l'arco 713 registra come `target` l'identificatore `traffic_exec_engine_stable_unit_interval` per la relazione che coinvolge il nodo canonico `.plan()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 713 che coinvolge il nodo canonico `.plan()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=760 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.plan()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_faultinjector_plan` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `.plan()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 760 che coinvolge il nodo canonico `.plan()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 760 che coinvolge il nodo canonico `.plan()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 760 che coinvolge il nodo canonico `.plan()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 760 che coinvolge il nodo canonico `.plan()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 760 che coinvolge il nodo canonico `.plan()` è `L491`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 760 che coinvolge il nodo canonico `.plan()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 760 che coinvolge il nodo canonico `.plan()` è `ast`. Nel grafo del codice RDG, l'arco 760 registra come `source` l'identificatore `traffic_exec_engine_faultinjector_plan` per la relazione che coinvolge il nodo canonico `.plan()`. Nel grafo del codice RDG, l'arco 760 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.plan()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 760 che coinvolge il nodo canonico `.plan()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=775 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.plan()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._matches()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_faultinjector_plan` all'ID `traffic_exec_engine_faultinjector_matches`. La relazione tecnica esplicita è la seguente: `.plan()` chiama `._matches()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 775 che coinvolge il nodo canonico `.plan()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 775 che coinvolge il nodo canonico `.plan()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 775 che coinvolge il nodo canonico `.plan()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 775 che coinvolge il nodo canonico `.plan()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 775 che coinvolge il nodo canonico `.plan()` è `L485`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 775 che coinvolge il nodo canonico `.plan()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 775 che coinvolge il nodo canonico `.plan()` è `ast`. Nel grafo del codice RDG, l'arco 775 registra come `source` l'identificatore `traffic_exec_engine_faultinjector_plan` per la relazione che coinvolge il nodo canonico `.plan()`. Nel grafo del codice RDG, l'arco 775 registra come `target` l'identificatore `traffic_exec_engine_faultinjector_matches` per la relazione che coinvolge il nodo canonico `.plan()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 775 che coinvolge il nodo canonico `.plan()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=771 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.plan()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FaultInjector`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_faultinjector` all'ID `traffic_exec_engine_faultinjector_plan`. La relazione tecnica esplicita è la seguente: `FaultInjector` registra come proprio metodo `.plan()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 771 che coinvolge il nodo canonico `.plan()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 771 che coinvolge il nodo canonico `.plan()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 771 che coinvolge il nodo canonico `.plan()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 771 che coinvolge il nodo canonico `.plan()` è `L480`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 771 che coinvolge il nodo canonico `.plan()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 771 che coinvolge il nodo canonico `.plan()` è `ast`. Nel grafo del codice RDG, l'arco 771 registra come `source` l'identificatore `traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `.plan()`. Nel grafo del codice RDG, l'arco 771 registra come `target` l'identificatore `traffic_exec_engine_faultinjector_plan` per la relazione che coinvolge il nodo canonico `.plan()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 771 che coinvolge il nodo canonico `.plan()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=774 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.plan()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_faultinjector_plan`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `.plan()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 774 che coinvolge il nodo canonico `.plan()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 774 che coinvolge il nodo canonico `.plan()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 774 che coinvolge il nodo canonico `.plan()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 774 che coinvolge il nodo canonico `.plan()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 774 che coinvolge il nodo canonico `.plan()` è `L871`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 774 che coinvolge il nodo canonico `.plan()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 774 che coinvolge il nodo canonico `.plan()` è `ast`. Nel grafo del codice RDG, l'arco 774 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `.plan()`. Nel grafo del codice RDG, l'arco 774 registra come `target` l'identificatore `traffic_exec_engine_faultinjector_plan` per la relazione che coinvolge il nodo canonico `.plan()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 774 che coinvolge il nodo canonico `.plan()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=368 id=Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.plan()` è `.plan()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.plan()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.plan()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.plan()` è `L480`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.plan()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.plan()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.plan()` è `.plan()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.plan()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.plan()` è `traffic_exec_engine_faultinjector_plan`. Nel grafo unificato, il nodo canonico `.plan()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan`.
<!-- GRAPH_EDGE graph=unificata index=774 role=outgoing -->
Nel grafo unificato, il nodo canonico `.plan()` è la sorgente `source` della relazione `calls` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `.plan()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 774 che coinvolge il nodo canonico `.plan()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 774 che coinvolge il nodo canonico `.plan()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 774 che coinvolge il nodo canonico `.plan()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 774 che coinvolge il nodo canonico `.plan()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 774 che coinvolge il nodo canonico `.plan()` è `L871`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 774 che coinvolge il nodo canonico `.plan()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 774 che coinvolge il nodo canonico `.plan()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 774 che coinvolge il nodo canonico `.plan()` è `1.0`. Nel grafo unificato, l'arco 774 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan` per la relazione che coinvolge il nodo canonico `.plan()`. Nel grafo unificato, l'arco 774 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `.plan()`.
<!-- GRAPH_EDGE graph=unificata index=775 role=outgoing -->
Nel grafo unificato, il nodo canonico `.plan()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._matches()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan` all'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector_matches`. La relazione tecnica esplicita è la seguente: `.plan()` chiama `._matches()`. Nel grafo unificato, il tipo dichiarato dell'arco 775 che coinvolge il nodo canonico `.plan()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 775 che coinvolge il nodo canonico `.plan()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 775 che coinvolge il nodo canonico `.plan()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 775 che coinvolge il nodo canonico `.plan()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 775 che coinvolge il nodo canonico `.plan()` è `L485`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 775 che coinvolge il nodo canonico `.plan()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 775 che coinvolge il nodo canonico `.plan()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 775 che coinvolge il nodo canonico `.plan()` è `1.0`. Nel grafo unificato, l'arco 775 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan` per la relazione che coinvolge il nodo canonico `.plan()`. Nel grafo unificato, l'arco 775 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector_matches` per la relazione che coinvolge il nodo canonico `.plan()`.
<!-- GRAPH_EDGE graph=unificata index=713 role=incoming -->
Nel grafo unificato, il nodo canonico `.plan()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_stable_unit_interval()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_stable_unit_interval` all'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan`. La relazione tecnica esplicita è la seguente: `_stable_unit_interval()` chiama `.plan()`. Nel grafo unificato, il tipo dichiarato dell'arco 713 che coinvolge il nodo canonico `.plan()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 713 che coinvolge il nodo canonico `.plan()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 713 che coinvolge il nodo canonico `.plan()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 713 che coinvolge il nodo canonico `.plan()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 713 che coinvolge il nodo canonico `.plan()` è `L498`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 713 che coinvolge il nodo canonico `.plan()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 713 che coinvolge il nodo canonico `.plan()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 713 che coinvolge il nodo canonico `.plan()` è `1.0`. Nel grafo unificato, l'arco 713 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_stable_unit_interval` per la relazione che coinvolge il nodo canonico `.plan()`. Nel grafo unificato, l'arco 713 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan` per la relazione che coinvolge il nodo canonico `.plan()`.
<!-- GRAPH_EDGE graph=unificata index=760 role=incoming -->
Nel grafo unificato, il nodo canonico `.plan()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan`. La relazione tecnica esplicita è la seguente: `.get()` chiama `.plan()`. Nel grafo unificato, il tipo dichiarato dell'arco 760 che coinvolge il nodo canonico `.plan()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 760 che coinvolge il nodo canonico `.plan()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 760 che coinvolge il nodo canonico `.plan()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 760 che coinvolge il nodo canonico `.plan()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 760 che coinvolge il nodo canonico `.plan()` è `L491`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 760 che coinvolge il nodo canonico `.plan()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 760 che coinvolge il nodo canonico `.plan()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 760 che coinvolge il nodo canonico `.plan()` è `1.0`. Nel grafo unificato, l'arco 760 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `.plan()`. Nel grafo unificato, l'arco 760 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan` per la relazione che coinvolge il nodo canonico `.plan()`.
<!-- GRAPH_EDGE graph=unificata index=771 role=incoming -->
Nel grafo unificato, il nodo canonico `.plan()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FaultInjector`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector` all'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan`. La relazione tecnica esplicita è la seguente: `FaultInjector` registra come proprio metodo `.plan()`. Nel grafo unificato, il tipo dichiarato dell'arco 771 che coinvolge il nodo canonico `.plan()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 771 che coinvolge il nodo canonico `.plan()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 771 che coinvolge il nodo canonico `.plan()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 771 che coinvolge il nodo canonico `.plan()` è `L480`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 771 che coinvolge il nodo canonico `.plan()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 771 che coinvolge il nodo canonico `.plan()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 771 che coinvolge il nodo canonico `.plan()` è `1.0`. Nel grafo unificato, l'arco 771 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `.plan()`. Nel grafo unificato, l'arco 771 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan` per la relazione che coinvolge il nodo canonico `.plan()`.

### .step_finished()

<!-- GRAPH_NODE graph=codice index=384 id=traffic_exec_engine_concurrencytracker_step_finished -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.step_finished()` è `.step_finished()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.step_finished()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.step_finished()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.step_finished()` è `L791`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.step_finished()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.step_finished()` possiede l'identificatore esatto `traffic_exec_engine_concurrencytracker_step_finished`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.step_finished()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.step_finished()` è `.step_finished()`.
Nel grafo del codice RDG, il nodo canonico `.step_finished()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=798 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.step_finished()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConcurrencyTracker`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_concurrencytracker` all'ID `traffic_exec_engine_concurrencytracker_step_finished`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.step_finished()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 798 che coinvolge il nodo canonico `.step_finished()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 798 che coinvolge il nodo canonico `.step_finished()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 798 che coinvolge il nodo canonico `.step_finished()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 798 che coinvolge il nodo canonico `.step_finished()` è `L791`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 798 che coinvolge il nodo canonico `.step_finished()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 798 che coinvolge il nodo canonico `.step_finished()` è `ast`. Nel grafo del codice RDG, l'arco 798 registra come `source` l'identificatore `traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `.step_finished()`. Nel grafo del codice RDG, l'arco 798 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_step_finished` per la relazione che coinvolge il nodo canonico `.step_finished()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 798 che coinvolge il nodo canonico `.step_finished()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=806 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.step_finished()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_concurrencytracker_step_finished`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `.step_finished()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 806 che coinvolge il nodo canonico `.step_finished()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 806 che coinvolge il nodo canonico `.step_finished()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 806 che coinvolge il nodo canonico `.step_finished()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 806 che coinvolge il nodo canonico `.step_finished()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 806 che coinvolge il nodo canonico `.step_finished()` è `L941`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 806 che coinvolge il nodo canonico `.step_finished()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 806 che coinvolge il nodo canonico `.step_finished()` è `ast`. Nel grafo del codice RDG, l'arco 806 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `.step_finished()`. Nel grafo del codice RDG, l'arco 806 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_step_finished` per la relazione che coinvolge il nodo canonico `.step_finished()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 806 che coinvolge il nodo canonico `.step_finished()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=384 id=Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_finished -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.step_finished()` è `.step_finished()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.step_finished()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.step_finished()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.step_finished()` è `L791`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.step_finished()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.step_finished()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.step_finished()` è `.step_finished()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.step_finished()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.step_finished()` è `traffic_exec_engine_concurrencytracker_step_finished`. Nel grafo unificato, il nodo canonico `.step_finished()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_finished`.
<!-- GRAPH_EDGE graph=unificata index=806 role=outgoing -->
Nel grafo unificato, il nodo canonico `.step_finished()` è la sorgente `source` della relazione `calls` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_finished` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `.step_finished()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 806 che coinvolge il nodo canonico `.step_finished()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 806 che coinvolge il nodo canonico `.step_finished()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 806 che coinvolge il nodo canonico `.step_finished()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 806 che coinvolge il nodo canonico `.step_finished()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 806 che coinvolge il nodo canonico `.step_finished()` è `L941`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 806 che coinvolge il nodo canonico `.step_finished()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 806 che coinvolge il nodo canonico `.step_finished()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 806 che coinvolge il nodo canonico `.step_finished()` è `1.0`. Nel grafo unificato, l'arco 806 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_finished` per la relazione che coinvolge il nodo canonico `.step_finished()`. Nel grafo unificato, l'arco 806 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `.step_finished()`.
<!-- GRAPH_EDGE graph=unificata index=798 role=incoming -->
Nel grafo unificato, il nodo canonico `.step_finished()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConcurrencyTracker`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` all'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_finished`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.step_finished()`. Nel grafo unificato, il tipo dichiarato dell'arco 798 che coinvolge il nodo canonico `.step_finished()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 798 che coinvolge il nodo canonico `.step_finished()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 798 che coinvolge il nodo canonico `.step_finished()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 798 che coinvolge il nodo canonico `.step_finished()` è `L791`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 798 che coinvolge il nodo canonico `.step_finished()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 798 che coinvolge il nodo canonico `.step_finished()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 798 che coinvolge il nodo canonico `.step_finished()` è `1.0`. Nel grafo unificato, l'arco 798 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `.step_finished()`. Nel grafo unificato, l'arco 798 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_finished` per la relazione che coinvolge il nodo canonico `.step_finished()`.

### .step_started()

<!-- GRAPH_NODE graph=codice index=383 id=traffic_exec_engine_concurrencytracker_step_started -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.step_started()` è `.step_started()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.step_started()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.step_started()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.step_started()` è `L786`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.step_started()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.step_started()` possiede l'identificatore esatto `traffic_exec_engine_concurrencytracker_step_started`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.step_started()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.step_started()` è `.step_started()`.
Nel grafo del codice RDG, il nodo canonico `.step_started()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=799 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.step_started()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConcurrencyTracker`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_concurrencytracker` all'ID `traffic_exec_engine_concurrencytracker_step_started`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.step_started()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 799 che coinvolge il nodo canonico `.step_started()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 799 che coinvolge il nodo canonico `.step_started()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 799 che coinvolge il nodo canonico `.step_started()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 799 che coinvolge il nodo canonico `.step_started()` è `L786`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 799 che coinvolge il nodo canonico `.step_started()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 799 che coinvolge il nodo canonico `.step_started()` è `ast`. Nel grafo del codice RDG, l'arco 799 registra come `source` l'identificatore `traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `.step_started()`. Nel grafo del codice RDG, l'arco 799 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_step_started` per la relazione che coinvolge il nodo canonico `.step_started()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 799 che coinvolge il nodo canonico `.step_started()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=805 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.step_started()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_concurrencytracker_step_started`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `.step_started()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 805 che coinvolge il nodo canonico `.step_started()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 805 che coinvolge il nodo canonico `.step_started()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 805 che coinvolge il nodo canonico `.step_started()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 805 che coinvolge il nodo canonico `.step_started()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 805 che coinvolge il nodo canonico `.step_started()` è `L857`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 805 che coinvolge il nodo canonico `.step_started()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 805 che coinvolge il nodo canonico `.step_started()` è `ast`. Nel grafo del codice RDG, l'arco 805 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `.step_started()`. Nel grafo del codice RDG, l'arco 805 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_step_started` per la relazione che coinvolge il nodo canonico `.step_started()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 805 che coinvolge il nodo canonico `.step_started()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=383 id=Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_started -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.step_started()` è `.step_started()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.step_started()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.step_started()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.step_started()` è `L786`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.step_started()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.step_started()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.step_started()` è `.step_started()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.step_started()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.step_started()` è `traffic_exec_engine_concurrencytracker_step_started`. Nel grafo unificato, il nodo canonico `.step_started()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_started`.
<!-- GRAPH_EDGE graph=unificata index=805 role=outgoing -->
Nel grafo unificato, il nodo canonico `.step_started()` è la sorgente `source` della relazione `calls` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_started` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `.step_started()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 805 che coinvolge il nodo canonico `.step_started()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 805 che coinvolge il nodo canonico `.step_started()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 805 che coinvolge il nodo canonico `.step_started()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 805 che coinvolge il nodo canonico `.step_started()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 805 che coinvolge il nodo canonico `.step_started()` è `L857`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 805 che coinvolge il nodo canonico `.step_started()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 805 che coinvolge il nodo canonico `.step_started()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 805 che coinvolge il nodo canonico `.step_started()` è `1.0`. Nel grafo unificato, l'arco 805 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_started` per la relazione che coinvolge il nodo canonico `.step_started()`. Nel grafo unificato, l'arco 805 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `.step_started()`.
<!-- GRAPH_EDGE graph=unificata index=799 role=incoming -->
Nel grafo unificato, il nodo canonico `.step_started()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConcurrencyTracker`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` all'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_started`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.step_started()`. Nel grafo unificato, il tipo dichiarato dell'arco 799 che coinvolge il nodo canonico `.step_started()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 799 che coinvolge il nodo canonico `.step_started()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 799 che coinvolge il nodo canonico `.step_started()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 799 che coinvolge il nodo canonico `.step_started()` è `L786`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 799 che coinvolge il nodo canonico `.step_started()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 799 che coinvolge il nodo canonico `.step_started()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 799 che coinvolge il nodo canonico `.step_started()` è `1.0`. Nel grafo unificato, l'arco 799 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `.step_started()`. Nel grafo unificato, l'arco 799 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_started` per la relazione che coinvolge il nodo canonico `.step_started()`.

### .summary() [traffic_exec_engine_concurrencytracker_summary]

<!-- GRAPH_NODE graph=codice index=385 id=traffic_exec_engine_concurrencytracker_summary -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `.summary()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `L794`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` possiede l'identificatore esatto `traffic_exec_engine_concurrencytracker_summary`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `.summary()`.
Nel grafo del codice RDG, il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=800 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConcurrencyTracker`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_concurrencytracker` all'ID `traffic_exec_engine_concurrencytracker_summary`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.summary() [traffic_exec_engine_concurrencytracker_summary]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 800 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 800 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 800 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 800 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `L794`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 800 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 800 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `ast`. Nel grafo del codice RDG, l'arco 800 registra come `source` l'identificatore `traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]`. Nel grafo del codice RDG, l'arco 800 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_summary` per la relazione che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 800 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=807 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_run` all'ID `traffic_exec_engine_concurrencytracker_summary`. La relazione tecnica esplicita è la seguente: `run()` chiama `.summary() [traffic_exec_engine_concurrencytracker_summary]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 807 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 807 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 807 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 807 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 807 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `L1077`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 807 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 807 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `ast`. Nel grafo del codice RDG, l'arco 807 registra come `source` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]`. Nel grafo del codice RDG, l'arco 807 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_summary` per la relazione che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 807 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=385 id=Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_summary -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `.summary()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `L794`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `.summary()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `traffic_exec_engine_concurrencytracker_summary`. Nel grafo unificato, il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_summary`.
<!-- GRAPH_EDGE graph=unificata index=807 role=outgoing -->
Nel grafo unificato, il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è la sorgente `source` della relazione `calls` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_summary` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `.summary() [traffic_exec_engine_concurrencytracker_summary]` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 807 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 807 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 807 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 807 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 807 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `L1077`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 807 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 807 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 807 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `1.0`. Nel grafo unificato, l'arco 807 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_summary` per la relazione che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]`. Nel grafo unificato, l'arco 807 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]`.
<!-- GRAPH_EDGE graph=unificata index=800 role=incoming -->
Nel grafo unificato, il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConcurrencyTracker`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` all'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_summary`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.summary() [traffic_exec_engine_concurrencytracker_summary]`. Nel grafo unificato, il tipo dichiarato dell'arco 800 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 800 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 800 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 800 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `L794`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 800 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 800 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 800 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]` è `1.0`. Nel grafo unificato, l'arco 800 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]`. Nel grafo unificato, l'arco 800 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_summary` per la relazione che coinvolge il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]`.

### .summary() [traffic_exec_engine_executionlogger_summary]

<!-- GRAPH_NODE graph=codice index=378 id=traffic_exec_engine_executionlogger_summary -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `.summary()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `L768`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` possiede l'identificatore esatto `traffic_exec_engine_executionlogger_summary`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `.summary()`.
Nel grafo del codice RDG, il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=785 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger` all'ID `traffic_exec_engine_executionlogger_summary`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.summary() [traffic_exec_engine_executionlogger_summary]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 785 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 785 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 785 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 785 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `L768`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 785 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 785 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `ast`. Nel grafo del codice RDG, l'arco 785 registra come `source` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]`. Nel grafo del codice RDG, l'arco 785 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_summary` per la relazione che coinvolge il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 785 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=378 id=Request-Dataset-Generator::traffic_exec_engine_executionlogger_summary -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `.summary()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `L768`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `.summary()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `traffic_exec_engine_executionlogger_summary`. Nel grafo unificato, il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_executionlogger_summary`.
Nel grafo unificato, il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=785 role=incoming -->
Nel grafo unificato, il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_summary`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.summary() [traffic_exec_engine_executionlogger_summary]`. Nel grafo unificato, il tipo dichiarato dell'arco 785 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 785 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 785 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 785 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `L768`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 785 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 785 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 785 che coinvolge il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]` è `1.0`. Nel grafo unificato, l'arco 785 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]`. Nel grafo unificato, l'arco 785 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_summary` per la relazione che coinvolge il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]`.

### .test_detects_unresolved_placeholders()

<!-- GRAPH_NODE graph=codice index=291 id=tests_test_core_placeholdertest_test_detects_unresolved_placeholders -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_detects_unresolved_placeholders()` è `.test_detects_unresolved_placeholders()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_detects_unresolved_placeholders()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_detects_unresolved_placeholders()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_detects_unresolved_placeholders()` è `L272`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_detects_unresolved_placeholders()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_detects_unresolved_placeholders()` possiede l'identificatore esatto `tests_test_core_placeholdertest_test_detects_unresolved_placeholders`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_detects_unresolved_placeholders()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_detects_unresolved_placeholders()` è `.test_detects_unresolved_placeholders()`.
<!-- GRAPH_EDGE graph=codice index=603 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_detects_unresolved_placeholders()` è la sorgente `source` della relazione `calls` verso il nodo canonico `unresolved_placeholders()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_placeholdertest_test_detects_unresolved_placeholders` all'ID `traffic_exec_engine_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `.test_detects_unresolved_placeholders()` chiama `unresolved_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 603 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 603 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 603 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 603 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 603 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 603 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `L275`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 603 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 603 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `ast`. Nel grafo del codice RDG, l'arco 603 registra come `source` l'identificatore `tests_test_core_placeholdertest_test_detects_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()`. Nel grafo del codice RDG, l'arco 603 registra come `target` l'identificatore `traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()`.
<!-- GRAPH_EDGE graph=codice index=597 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_detects_unresolved_placeholders()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `PlaceholderTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_placeholdertest` all'ID `tests_test_core_placeholdertest_test_detects_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `PlaceholderTest` registra come proprio metodo `.test_detects_unresolved_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 597 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 597 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 597 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 597 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `L272`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 597 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 597 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `ast`. Nel grafo del codice RDG, l'arco 597 registra come `source` l'identificatore `tests_test_core_placeholdertest` per la relazione che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()`. Nel grafo del codice RDG, l'arco 597 registra come `target` l'identificatore `tests_test_core_placeholdertest_test_detects_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 597 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=291 id=Request-Dataset-Generator::tests_test_core_placeholdertest_test_detects_unresolved_placeholders -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_detects_unresolved_placeholders()` è `.test_detects_unresolved_placeholders()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_detects_unresolved_placeholders()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_detects_unresolved_placeholders()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_detects_unresolved_placeholders()` è `L272`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_detects_unresolved_placeholders()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_detects_unresolved_placeholders()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_detects_unresolved_placeholders()` è `.test_detects_unresolved_placeholders()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_detects_unresolved_placeholders()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_detects_unresolved_placeholders()` è `tests_test_core_placeholdertest_test_detects_unresolved_placeholders`. Nel grafo unificato, il nodo canonico `.test_detects_unresolved_placeholders()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_placeholdertest_test_detects_unresolved_placeholders`.
<!-- GRAPH_EDGE graph=unificata index=603 role=outgoing -->
Nel grafo unificato, il nodo canonico `.test_detects_unresolved_placeholders()` è la sorgente `source` della relazione `calls` verso il nodo canonico `unresolved_placeholders()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_placeholdertest_test_detects_unresolved_placeholders` all'ID `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `.test_detects_unresolved_placeholders()` chiama `unresolved_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 603 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 603 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 603 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 603 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 603 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 603 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `L275`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 603 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 603 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `ast`. Nel grafo unificato, l'arco 603 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest_test_detects_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()`. Nel grafo unificato, l'arco 603 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()`.
<!-- GRAPH_EDGE graph=unificata index=597 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_detects_unresolved_placeholders()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `PlaceholderTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_placeholdertest` all'ID `Request-Dataset-Generator::tests_test_core_placeholdertest_test_detects_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `PlaceholderTest` registra come proprio metodo `.test_detects_unresolved_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 597 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 597 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 597 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 597 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `L272`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 597 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 597 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 597 che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()` è `1.0`. Nel grafo unificato, l'arco 597 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest` per la relazione che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()`. Nel grafo unificato, l'arco 597 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest_test_detects_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `.test_detects_unresolved_placeholders()`.

### .test_extracts_saml_assertion_as_base64()

<!-- GRAPH_NODE graph=codice index=286 id=tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_extracts_saml_assertion_as_base64()` è `.test_extracts_saml_assertion_as_base64()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_extracts_saml_assertion_as_base64()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `L153`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_extracts_saml_assertion_as_base64()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_extracts_saml_assertion_as_base64()` possiede l'identificatore esatto `tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_extracts_saml_assertion_as_base64()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_extracts_saml_assertion_as_base64()` è `.test_extracts_saml_assertion_as_base64()`.
<!-- GRAPH_EDGE graph=codice index=595 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_extracts_saml_assertion_as_base64()` è la sorgente `source` della relazione `calls` verso il nodo canonico `extract_outputs()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64` all'ID `traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `.test_extracts_saml_assertion_as_base64()` chiama `extract_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 595 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 595 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 595 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 595 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 595 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 595 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `L184`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 595 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 595 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `ast`. Nel grafo del codice RDG, l'arco 595 registra come `source` l'identificatore `tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64` per la relazione che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()`. Nel grafo del codice RDG, l'arco 595 registra come `target` l'identificatore `traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()`.
<!-- GRAPH_EDGE graph=codice index=593 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_extracts_saml_assertion_as_base64()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ExtractionTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_extractiontest` all'ID `tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64`. La relazione tecnica esplicita è la seguente: `ExtractionTest` registra come proprio metodo `.test_extracts_saml_assertion_as_base64()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 593 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 593 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 593 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 593 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `L153`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 593 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 593 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `ast`. Nel grafo del codice RDG, l'arco 593 registra come `source` l'identificatore `tests_test_core_extractiontest` per la relazione che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()`. Nel grafo del codice RDG, l'arco 593 registra come `target` l'identificatore `tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64` per la relazione che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 593 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=286 id=Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_extracts_saml_assertion_as_base64()` è `.test_extracts_saml_assertion_as_base64()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_extracts_saml_assertion_as_base64()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `L153`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_extracts_saml_assertion_as_base64()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_extracts_saml_assertion_as_base64()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_extracts_saml_assertion_as_base64()` è `.test_extracts_saml_assertion_as_base64()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_extracts_saml_assertion_as_base64()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64`. Nel grafo unificato, il nodo canonico `.test_extracts_saml_assertion_as_base64()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64`.
<!-- GRAPH_EDGE graph=unificata index=595 role=outgoing -->
Nel grafo unificato, il nodo canonico `.test_extracts_saml_assertion_as_base64()` è la sorgente `source` della relazione `calls` verso il nodo canonico `extract_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `.test_extracts_saml_assertion_as_base64()` chiama `extract_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 595 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 595 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 595 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 595 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 595 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 595 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `L184`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 595 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 595 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `ast`. Nel grafo unificato, l'arco 595 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64` per la relazione che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()`. Nel grafo unificato, l'arco 595 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()`.
<!-- GRAPH_EDGE graph=unificata index=593 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_extracts_saml_assertion_as_base64()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ExtractionTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_extractiontest` all'ID `Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64`. La relazione tecnica esplicita è la seguente: `ExtractionTest` registra come proprio metodo `.test_extracts_saml_assertion_as_base64()`. Nel grafo unificato, il tipo dichiarato dell'arco 593 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 593 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 593 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 593 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `L153`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 593 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 593 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 593 che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()` è `1.0`. Nel grafo unificato, l'arco 593 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_extractiontest` per la relazione che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()`. Nel grafo unificato, l'arco 593 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64` per la relazione che coinvolge il nodo canonico `.test_extracts_saml_assertion_as_base64()`.

### .test_extracts_saml_assertion_string_linearized_and_linearized_base64()

<!-- GRAPH_NODE graph=codice index=287 id=tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `L191`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` possiede l'identificatore esatto `tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`.
<!-- GRAPH_EDGE graph=codice index=596 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è la sorgente `source` della relazione `calls` verso il nodo canonico `extract_outputs()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64` all'ID `traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` chiama `extract_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 596 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 596 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 596 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 596 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 596 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 596 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `L226`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 596 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 596 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `ast`. Nel grafo del codice RDG, l'arco 596 registra come `source` l'identificatore `tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64` per la relazione che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`. Nel grafo del codice RDG, l'arco 596 registra come `target` l'identificatore `traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`.
<!-- GRAPH_EDGE graph=codice index=594 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ExtractionTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_extractiontest` all'ID `tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64`. La relazione tecnica esplicita è la seguente: `ExtractionTest` registra come proprio metodo `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 594 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 594 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 594 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 594 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `L191`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 594 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 594 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `ast`. Nel grafo del codice RDG, l'arco 594 registra come `source` l'identificatore `tests_test_core_extractiontest` per la relazione che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`. Nel grafo del codice RDG, l'arco 594 registra come `target` l'identificatore `tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64` per la relazione che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 594 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=287 id=Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `L191`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64`. Nel grafo unificato, il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64`.
<!-- GRAPH_EDGE graph=unificata index=596 role=outgoing -->
Nel grafo unificato, il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è la sorgente `source` della relazione `calls` verso il nodo canonico `extract_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` chiama `extract_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 596 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 596 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 596 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 596 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 596 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 596 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `L226`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 596 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 596 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `ast`. Nel grafo unificato, l'arco 596 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64` per la relazione che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`. Nel grafo unificato, l'arco 596 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`.
<!-- GRAPH_EDGE graph=unificata index=594 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ExtractionTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_extractiontest` all'ID `Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64`. La relazione tecnica esplicita è la seguente: `ExtractionTest` registra come proprio metodo `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`. Nel grafo unificato, il tipo dichiarato dell'arco 594 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 594 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 594 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 594 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `L191`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 594 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 594 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 594 che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` è `1.0`. Nel grafo unificato, l'arco 594 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_extractiontest` per la relazione che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`. Nel grafo unificato, l'arco 594 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64` per la relazione che coinvolge il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`.

### .test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()

<!-- GRAPH_NODE graph=codice index=290 id=tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `L253`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` possiede l'identificatore esatto `tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`.
<!-- GRAPH_EDGE graph=codice index=602 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è la sorgente `source` della relazione `calls` verso il nodo canonico `resolve_placeholders()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback` all'ID `traffic_exec_engine_resolve_placeholders`. La relazione tecnica esplicita è la seguente: `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` chiama `resolve_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 602 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 602 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 602 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 602 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 602 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 602 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `L260`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 602 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 602 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `ast`. Nel grafo del codice RDG, l'arco 602 registra come `source` l'identificatore `tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback` per la relazione che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`. Nel grafo del codice RDG, l'arco 602 registra come `target` l'identificatore `traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`.
<!-- GRAPH_EDGE graph=codice index=598 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `PlaceholderTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_placeholdertest` all'ID `tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback`. La relazione tecnica esplicita è la seguente: `PlaceholderTest` registra come proprio metodo `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 598 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 598 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 598 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 598 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `L253`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 598 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 598 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `ast`. Nel grafo del codice RDG, l'arco 598 registra come `source` l'identificatore `tests_test_core_placeholdertest` per la relazione che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`. Nel grafo del codice RDG, l'arco 598 registra come `target` l'identificatore `tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback` per la relazione che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 598 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=290 id=Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `L253`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback`. Nel grafo unificato, il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback`.
<!-- GRAPH_EDGE graph=unificata index=602 role=outgoing -->
Nel grafo unificato, il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è la sorgente `source` della relazione `calls` verso il nodo canonico `resolve_placeholders()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback` all'ID `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders`. La relazione tecnica esplicita è la seguente: `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` chiama `resolve_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 602 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 602 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 602 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 602 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 602 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 602 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `L260`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 602 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 602 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `ast`. Nel grafo unificato, l'arco 602 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback` per la relazione che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`. Nel grafo unificato, l'arco 602 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`.
<!-- GRAPH_EDGE graph=unificata index=598 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `PlaceholderTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_placeholdertest` all'ID `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback`. La relazione tecnica esplicita è la seguente: `PlaceholderTest` registra come proprio metodo `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`. Nel grafo unificato, il tipo dichiarato dell'arco 598 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 598 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 598 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 598 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `L253`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 598 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 598 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 598 che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` è `1.0`. Nel grafo unificato, l'arco 598 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest` per la relazione che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`. Nel grafo unificato, l'arco 598 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback` per la relazione che coinvolge il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`.

### .test_resolves_placeholders_in_url_headers_and_body()

<!-- GRAPH_NODE graph=codice index=289 id=tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `.test_resolves_placeholders_in_url_headers_and_body()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `L240`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` possiede l'identificatore esatto `tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `.test_resolves_placeholders_in_url_headers_and_body()`.
<!-- GRAPH_EDGE graph=codice index=600 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è la sorgente `source` della relazione `calls` verso il nodo canonico `resolve_placeholders()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body` all'ID `traffic_exec_engine_resolve_placeholders`. La relazione tecnica esplicita è la seguente: `.test_resolves_placeholders_in_url_headers_and_body()` chiama `resolve_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 600 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 600 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 600 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 600 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 600 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 600 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `L246`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 600 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 600 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `ast`. Nel grafo del codice RDG, l'arco 600 registra come `source` l'identificatore `tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body` per la relazione che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()`. Nel grafo del codice RDG, l'arco 600 registra come `target` l'identificatore `traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()`.
<!-- GRAPH_EDGE graph=codice index=601 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è la sorgente `source` della relazione `calls` verso il nodo canonico `unresolved_placeholders()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body` all'ID `traffic_exec_engine_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `.test_resolves_placeholders_in_url_headers_and_body()` chiama `unresolved_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 601 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 601 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 601 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 601 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 601 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 601 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `L251`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 601 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 601 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `ast`. Nel grafo del codice RDG, l'arco 601 registra come `source` l'identificatore `tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body` per la relazione che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()`. Nel grafo del codice RDG, l'arco 601 registra come `target` l'identificatore `traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()`.
<!-- GRAPH_EDGE graph=codice index=599 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `PlaceholderTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_placeholdertest` all'ID `tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body`. La relazione tecnica esplicita è la seguente: `PlaceholderTest` registra come proprio metodo `.test_resolves_placeholders_in_url_headers_and_body()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 599 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 599 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 599 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 599 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `L240`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 599 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 599 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `ast`. Nel grafo del codice RDG, l'arco 599 registra come `source` l'identificatore `tests_test_core_placeholdertest` per la relazione che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()`. Nel grafo del codice RDG, l'arco 599 registra come `target` l'identificatore `tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body` per la relazione che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 599 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=289 id=Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `.test_resolves_placeholders_in_url_headers_and_body()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `L240`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `.test_resolves_placeholders_in_url_headers_and_body()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body`. Nel grafo unificato, il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body`.
<!-- GRAPH_EDGE graph=unificata index=600 role=outgoing -->
Nel grafo unificato, il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è la sorgente `source` della relazione `calls` verso il nodo canonico `resolve_placeholders()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body` all'ID `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders`. La relazione tecnica esplicita è la seguente: `.test_resolves_placeholders_in_url_headers_and_body()` chiama `resolve_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 600 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 600 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 600 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 600 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 600 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 600 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `L246`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 600 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 600 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `ast`. Nel grafo unificato, l'arco 600 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body` per la relazione che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()`. Nel grafo unificato, l'arco 600 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()`.
<!-- GRAPH_EDGE graph=unificata index=601 role=outgoing -->
Nel grafo unificato, il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è la sorgente `source` della relazione `calls` verso il nodo canonico `unresolved_placeholders()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body` all'ID `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `.test_resolves_placeholders_in_url_headers_and_body()` chiama `unresolved_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 601 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 601 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 601 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 601 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 601 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 601 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `L251`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 601 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 601 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `ast`. Nel grafo unificato, l'arco 601 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body` per la relazione che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()`. Nel grafo unificato, l'arco 601 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()`.
<!-- GRAPH_EDGE graph=unificata index=599 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `PlaceholderTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_placeholdertest` all'ID `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body`. La relazione tecnica esplicita è la seguente: `PlaceholderTest` registra come proprio metodo `.test_resolves_placeholders_in_url_headers_and_body()`. Nel grafo unificato, il tipo dichiarato dell'arco 599 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 599 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 599 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 599 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `L240`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 599 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 599 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 599 che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()` è `1.0`. Nel grafo unificato, l'arco 599 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest` per la relazione che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()`. Nel grafo unificato, l'arco 599 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body` per la relazione che coinvolge il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()`.

### _build_parser() [traffic_exec_engine_build_parser]

<!-- GRAPH_NODE graph=codice index=392 id=traffic_exec_engine_build_parser -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `_build_parser()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `L1127`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` possiede l'identificatore esatto `traffic_exec_engine_build_parser`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `_build_parser()`.
<!-- GRAPH_EDGE graph=codice index=817 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è la sorgente `source` della relazione `references` verso il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_build_parser` all'ID `traffic_exec_engine_py_argumentparser`. La relazione tecnica esplicita è la seguente: `_build_parser() [traffic_exec_engine_build_parser]` fa riferimento a `ArgumentParser [traffic_exec_engine_py_argumentparser]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 817 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 817 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `return_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 817 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 817 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 817 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `L1127`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 817 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 817 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `ast`. Nel grafo del codice RDG, l'arco 817 registra come `source` l'identificatore `traffic_exec_engine_build_parser` per la relazione che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]`. Nel grafo del codice RDG, l'arco 817 registra come `target` l'identificatore `traffic_exec_engine_py_argumentparser` per la relazione che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 817 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=661 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_build_parser`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_build_parser() [traffic_exec_engine_build_parser]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 661 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 661 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 661 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 661 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `L1127`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 661 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 661 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `ast`. Nel grafo del codice RDG, l'arco 661 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]`. Nel grafo del codice RDG, l'arco 661 registra come `target` l'identificatore `traffic_exec_engine_build_parser` per la relazione che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 661 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=818 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `main() [traffic_exec_engine_main]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_main` all'ID `traffic_exec_engine_build_parser`. La relazione tecnica esplicita è la seguente: `main() [traffic_exec_engine_main]` chiama `_build_parser() [traffic_exec_engine_build_parser]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 818 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 818 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 818 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 818 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 818 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `L1186`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 818 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 818 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `ast`. Nel grafo del codice RDG, l'arco 818 registra come `source` l'identificatore `traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]`. Nel grafo del codice RDG, l'arco 818 registra come `target` l'identificatore `traffic_exec_engine_build_parser` per la relazione che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 818 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=392 id=Request-Dataset-Generator::traffic_exec_engine_build_parser -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `_build_parser()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `L1127`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `_build_parser()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `traffic_exec_engine_build_parser`. Nel grafo unificato, il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_build_parser`.
<!-- GRAPH_EDGE graph=unificata index=817 role=outgoing -->
Nel grafo unificato, il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è la sorgente `source` della relazione `references` verso il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_build_parser` all'ID `Request-Dataset-Generator::traffic_exec_engine_py_argumentparser`. La relazione tecnica esplicita è la seguente: `_build_parser() [traffic_exec_engine_build_parser]` fa riferimento a `ArgumentParser [traffic_exec_engine_py_argumentparser]`. Nel grafo unificato, il tipo dichiarato dell'arco 817 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 817 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `return_type`. Nel grafo unificato, la confidenza categoriale dell'arco 817 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 817 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 817 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `L1127`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 817 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 817 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 817 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `1.0`. Nel grafo unificato, l'arco 817 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_build_parser` per la relazione che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]`. Nel grafo unificato, l'arco 817 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_argumentparser` per la relazione che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]`.
<!-- GRAPH_EDGE graph=unificata index=818 role=outgoing -->
Nel grafo unificato, il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è la sorgente `source` della relazione `calls` verso il nodo canonico `main() [traffic_exec_engine_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_build_parser` all'ID `Request-Dataset-Generator::traffic_exec_engine_main`. La relazione tecnica esplicita è la seguente: `_build_parser() [traffic_exec_engine_build_parser]` chiama `main() [traffic_exec_engine_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 818 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 818 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 818 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 818 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 818 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `L1186`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 818 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 818 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 818 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `1.0`. Nel grafo unificato, l'arco 818 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_build_parser` per la relazione che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]`. Nel grafo unificato, l'arco 818 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]`.
<!-- GRAPH_EDGE graph=unificata index=661 role=incoming -->
Nel grafo unificato, il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_build_parser`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_build_parser() [traffic_exec_engine_build_parser]`. Nel grafo unificato, il tipo dichiarato dell'arco 661 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 661 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 661 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 661 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `L1127`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 661 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 661 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 661 che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]` è `1.0`. Nel grafo unificato, l'arco 661 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]`. Nel grafo unificato, l'arco 661 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_build_parser` per la relazione che coinvolge il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]`.

### _correlation_headers()

<!-- GRAPH_NODE graph=codice index=359 id=traffic_exec_engine_correlation_headers -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_correlation_headers()` è `_correlation_headers()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_correlation_headers()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_correlation_headers()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_correlation_headers()` è `L350`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_correlation_headers()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_correlation_headers()` possiede l'identificatore esatto `traffic_exec_engine_correlation_headers`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_correlation_headers()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_correlation_headers()` è `_correlation_headers()`.
<!-- GRAPH_EDGE graph=codice index=740 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_correlation_headers()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_correlation_headers` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `_correlation_headers()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 740 che coinvolge il nodo canonico `_correlation_headers()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 740 che coinvolge il nodo canonico `_correlation_headers()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 740 che coinvolge il nodo canonico `_correlation_headers()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 740 che coinvolge il nodo canonico `_correlation_headers()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 740 che coinvolge il nodo canonico `_correlation_headers()` è `L358`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 740 che coinvolge il nodo canonico `_correlation_headers()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 740 che coinvolge il nodo canonico `_correlation_headers()` è `ast`. Nel grafo del codice RDG, l'arco 740 registra come `source` l'identificatore `traffic_exec_engine_correlation_headers` per la relazione che coinvolge il nodo canonico `_correlation_headers()`. Nel grafo del codice RDG, l'arco 740 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `_correlation_headers()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 740 che coinvolge il nodo canonico `_correlation_headers()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=663 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_correlation_headers()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_correlation_headers`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_correlation_headers()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 663 che coinvolge il nodo canonico `_correlation_headers()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 663 che coinvolge il nodo canonico `_correlation_headers()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 663 che coinvolge il nodo canonico `_correlation_headers()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 663 che coinvolge il nodo canonico `_correlation_headers()` è `L350`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 663 che coinvolge il nodo canonico `_correlation_headers()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 663 che coinvolge il nodo canonico `_correlation_headers()` è `ast`. Nel grafo del codice RDG, l'arco 663 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_correlation_headers()`. Nel grafo del codice RDG, l'arco 663 registra come `target` l'identificatore `traffic_exec_engine_correlation_headers` per la relazione che coinvolge il nodo canonico `_correlation_headers()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 663 che coinvolge il nodo canonico `_correlation_headers()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=741 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_correlation_headers()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_351` all'ID `traffic_exec_engine_correlation_headers`. La relazione tecnica esplicita è la seguente: `Costruisce gli header di correlazione da iniettare in ogni request live.      No` costituisce una motivazione per `_correlation_headers()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 741 che coinvolge il nodo canonico `_correlation_headers()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 741 che coinvolge il nodo canonico `_correlation_headers()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 741 che coinvolge il nodo canonico `_correlation_headers()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 741 che coinvolge il nodo canonico `_correlation_headers()` è `L351`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 741 che coinvolge il nodo canonico `_correlation_headers()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 741 che coinvolge il nodo canonico `_correlation_headers()` è `ast`. Nel grafo del codice RDG, l'arco 741 registra come `source` l'identificatore `traffic_exec_engine_rationale_351` per la relazione che coinvolge il nodo canonico `_correlation_headers()`. Nel grafo del codice RDG, l'arco 741 registra come `target` l'identificatore `traffic_exec_engine_correlation_headers` per la relazione che coinvolge il nodo canonico `_correlation_headers()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 741 che coinvolge il nodo canonico `_correlation_headers()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=742 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_correlation_headers()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `send_request()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_send_request` all'ID `traffic_exec_engine_correlation_headers`. La relazione tecnica esplicita è la seguente: `send_request()` chiama `_correlation_headers()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 742 che coinvolge il nodo canonico `_correlation_headers()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 742 che coinvolge il nodo canonico `_correlation_headers()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 742 che coinvolge il nodo canonico `_correlation_headers()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 742 che coinvolge il nodo canonico `_correlation_headers()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 742 che coinvolge il nodo canonico `_correlation_headers()` è `L572`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 742 che coinvolge il nodo canonico `_correlation_headers()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 742 che coinvolge il nodo canonico `_correlation_headers()` è `ast`. Nel grafo del codice RDG, l'arco 742 registra come `source` l'identificatore `traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `_correlation_headers()`. Nel grafo del codice RDG, l'arco 742 registra come `target` l'identificatore `traffic_exec_engine_correlation_headers` per la relazione che coinvolge il nodo canonico `_correlation_headers()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 742 che coinvolge il nodo canonico `_correlation_headers()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=359 id=Request-Dataset-Generator::traffic_exec_engine_correlation_headers -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_correlation_headers()` è `_correlation_headers()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_correlation_headers()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_correlation_headers()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_correlation_headers()` è `L350`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_correlation_headers()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_correlation_headers()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_correlation_headers()` è `_correlation_headers()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_correlation_headers()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_correlation_headers()` è `traffic_exec_engine_correlation_headers`. Nel grafo unificato, il nodo canonico `_correlation_headers()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_correlation_headers`.
<!-- GRAPH_EDGE graph=unificata index=740 role=outgoing -->
Nel grafo unificato, il nodo canonico `_correlation_headers()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_correlation_headers` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `_correlation_headers()` chiama `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 740 che coinvolge il nodo canonico `_correlation_headers()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 740 che coinvolge il nodo canonico `_correlation_headers()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 740 che coinvolge il nodo canonico `_correlation_headers()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 740 che coinvolge il nodo canonico `_correlation_headers()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 740 che coinvolge il nodo canonico `_correlation_headers()` è `L358`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 740 che coinvolge il nodo canonico `_correlation_headers()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 740 che coinvolge il nodo canonico `_correlation_headers()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 740 che coinvolge il nodo canonico `_correlation_headers()` è `1.0`. Nel grafo unificato, l'arco 740 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_correlation_headers` per la relazione che coinvolge il nodo canonico `_correlation_headers()`. Nel grafo unificato, l'arco 740 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `_correlation_headers()`.
<!-- GRAPH_EDGE graph=unificata index=741 role=outgoing -->
Nel grafo unificato, il nodo canonico `_correlation_headers()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_correlation_headers` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_351`. La relazione tecnica esplicita è la seguente: `_correlation_headers()` costituisce una motivazione per `Costruisce gli header di correlazione da iniettare in ogni request live.      No`. Nel grafo unificato, il tipo dichiarato dell'arco 741 che coinvolge il nodo canonico `_correlation_headers()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 741 che coinvolge il nodo canonico `_correlation_headers()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 741 che coinvolge il nodo canonico `_correlation_headers()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 741 che coinvolge il nodo canonico `_correlation_headers()` è `L351`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 741 che coinvolge il nodo canonico `_correlation_headers()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 741 che coinvolge il nodo canonico `_correlation_headers()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 741 che coinvolge il nodo canonico `_correlation_headers()` è `1.0`. Nel grafo unificato, l'arco 741 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_correlation_headers` per la relazione che coinvolge il nodo canonico `_correlation_headers()`. Nel grafo unificato, l'arco 741 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_351` per la relazione che coinvolge il nodo canonico `_correlation_headers()`.
<!-- GRAPH_EDGE graph=unificata index=742 role=outgoing -->
Nel grafo unificato, il nodo canonico `_correlation_headers()` è la sorgente `source` della relazione `calls` verso il nodo canonico `send_request()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_correlation_headers` all'ID `Request-Dataset-Generator::traffic_exec_engine_send_request`. La relazione tecnica esplicita è la seguente: `_correlation_headers()` chiama `send_request()`. Nel grafo unificato, il tipo dichiarato dell'arco 742 che coinvolge il nodo canonico `_correlation_headers()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 742 che coinvolge il nodo canonico `_correlation_headers()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 742 che coinvolge il nodo canonico `_correlation_headers()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 742 che coinvolge il nodo canonico `_correlation_headers()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 742 che coinvolge il nodo canonico `_correlation_headers()` è `L572`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 742 che coinvolge il nodo canonico `_correlation_headers()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 742 che coinvolge il nodo canonico `_correlation_headers()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 742 che coinvolge il nodo canonico `_correlation_headers()` è `1.0`. Nel grafo unificato, l'arco 742 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_correlation_headers` per la relazione che coinvolge il nodo canonico `_correlation_headers()`. Nel grafo unificato, l'arco 742 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `_correlation_headers()`.
<!-- GRAPH_EDGE graph=unificata index=663 role=incoming -->
Nel grafo unificato, il nodo canonico `_correlation_headers()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_correlation_headers`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_correlation_headers()`. Nel grafo unificato, il tipo dichiarato dell'arco 663 che coinvolge il nodo canonico `_correlation_headers()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 663 che coinvolge il nodo canonico `_correlation_headers()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 663 che coinvolge il nodo canonico `_correlation_headers()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 663 che coinvolge il nodo canonico `_correlation_headers()` è `L350`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 663 che coinvolge il nodo canonico `_correlation_headers()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 663 che coinvolge il nodo canonico `_correlation_headers()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 663 che coinvolge il nodo canonico `_correlation_headers()` è `1.0`. Nel grafo unificato, l'arco 663 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_correlation_headers()`. Nel grafo unificato, l'arco 663 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_correlation_headers` per la relazione che coinvolge il nodo canonico `_correlation_headers()`.

### _extract_json_output()

<!-- GRAPH_NODE graph=codice index=350 id=traffic_exec_engine_extract_json_output -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_extract_json_output()` è `_extract_json_output()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_extract_json_output()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_extract_json_output()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_extract_json_output()` è `L189`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_extract_json_output()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_extract_json_output()` possiede l'identificatore esatto `traffic_exec_engine_extract_json_output`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_extract_json_output()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_extract_json_output()` è `_extract_json_output()`.
<!-- GRAPH_EDGE graph=codice index=706 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_extract_json_output()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [traffic_exec_engine_py_any]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_json_output` all'ID `traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `_extract_json_output()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 706 che coinvolge il nodo canonico `_extract_json_output()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 706 che coinvolge il nodo canonico `_extract_json_output()` è `return_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 706 che coinvolge il nodo canonico `_extract_json_output()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 706 che coinvolge il nodo canonico `_extract_json_output()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 706 che coinvolge il nodo canonico `_extract_json_output()` è `L189`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 706 che coinvolge il nodo canonico `_extract_json_output()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 706 che coinvolge il nodo canonico `_extract_json_output()` è `ast`. Nel grafo del codice RDG, l'arco 706 registra come `source` l'identificatore `traffic_exec_engine_extract_json_output` per la relazione che coinvolge il nodo canonico `_extract_json_output()`. Nel grafo del codice RDG, l'arco 706 registra come `target` l'identificatore `traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `_extract_json_output()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 706 che coinvolge il nodo canonico `_extract_json_output()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=725 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_extract_json_output()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_json_output` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `_extract_json_output()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 725 che coinvolge il nodo canonico `_extract_json_output()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 725 che coinvolge il nodo canonico `_extract_json_output()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 725 che coinvolge il nodo canonico `_extract_json_output()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 725 che coinvolge il nodo canonico `_extract_json_output()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 725 che coinvolge il nodo canonico `_extract_json_output()` è `L194`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 725 che coinvolge il nodo canonico `_extract_json_output()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 725 che coinvolge il nodo canonico `_extract_json_output()` è `ast`. Nel grafo del codice RDG, l'arco 725 registra come `source` l'identificatore `traffic_exec_engine_extract_json_output` per la relazione che coinvolge il nodo canonico `_extract_json_output()`. Nel grafo del codice RDG, l'arco 725 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `_extract_json_output()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 725 che coinvolge il nodo canonico `_extract_json_output()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=666 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_extract_json_output()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_extract_json_output`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_extract_json_output()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 666 che coinvolge il nodo canonico `_extract_json_output()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 666 che coinvolge il nodo canonico `_extract_json_output()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 666 che coinvolge il nodo canonico `_extract_json_output()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 666 che coinvolge il nodo canonico `_extract_json_output()` è `L189`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 666 che coinvolge il nodo canonico `_extract_json_output()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 666 che coinvolge il nodo canonico `_extract_json_output()` è `ast`. Nel grafo del codice RDG, l'arco 666 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_extract_json_output()`. Nel grafo del codice RDG, l'arco 666 registra come `target` l'identificatore `traffic_exec_engine_extract_json_output` per la relazione che coinvolge il nodo canonico `_extract_json_output()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 666 che coinvolge il nodo canonico `_extract_json_output()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=721 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_extract_json_output()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `extract_outputs()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_outputs` all'ID `traffic_exec_engine_extract_json_output`. La relazione tecnica esplicita è la seguente: `extract_outputs()` chiama `_extract_json_output()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 721 che coinvolge il nodo canonico `_extract_json_output()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 721 che coinvolge il nodo canonico `_extract_json_output()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 721 che coinvolge il nodo canonico `_extract_json_output()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 721 che coinvolge il nodo canonico `_extract_json_output()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 721 che coinvolge il nodo canonico `_extract_json_output()` è `L182`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 721 che coinvolge il nodo canonico `_extract_json_output()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 721 che coinvolge il nodo canonico `_extract_json_output()` è `ast`. Nel grafo del codice RDG, l'arco 721 registra come `source` l'identificatore `traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `_extract_json_output()`. Nel grafo del codice RDG, l'arco 721 registra come `target` l'identificatore `traffic_exec_engine_extract_json_output` per la relazione che coinvolge il nodo canonico `_extract_json_output()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 721 che coinvolge il nodo canonico `_extract_json_output()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=350 id=Request-Dataset-Generator::traffic_exec_engine_extract_json_output -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_extract_json_output()` è `_extract_json_output()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_extract_json_output()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_extract_json_output()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_extract_json_output()` è `L189`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_extract_json_output()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_extract_json_output()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_extract_json_output()` è `_extract_json_output()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_extract_json_output()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_extract_json_output()` è `traffic_exec_engine_extract_json_output`. Nel grafo unificato, il nodo canonico `_extract_json_output()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_extract_json_output`.
<!-- GRAPH_EDGE graph=unificata index=725 role=outgoing -->
Nel grafo unificato, il nodo canonico `_extract_json_output()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_json_output` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `_extract_json_output()` chiama `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 725 che coinvolge il nodo canonico `_extract_json_output()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 725 che coinvolge il nodo canonico `_extract_json_output()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 725 che coinvolge il nodo canonico `_extract_json_output()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 725 che coinvolge il nodo canonico `_extract_json_output()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 725 che coinvolge il nodo canonico `_extract_json_output()` è `L194`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 725 che coinvolge il nodo canonico `_extract_json_output()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 725 che coinvolge il nodo canonico `_extract_json_output()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 725 che coinvolge il nodo canonico `_extract_json_output()` è `1.0`. Nel grafo unificato, l'arco 725 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_json_output` per la relazione che coinvolge il nodo canonico `_extract_json_output()`. Nel grafo unificato, l'arco 725 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `_extract_json_output()`.
<!-- GRAPH_EDGE graph=unificata index=666 role=incoming -->
Nel grafo unificato, il nodo canonico `_extract_json_output()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_json_output`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_extract_json_output()`. Nel grafo unificato, il tipo dichiarato dell'arco 666 che coinvolge il nodo canonico `_extract_json_output()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 666 che coinvolge il nodo canonico `_extract_json_output()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 666 che coinvolge il nodo canonico `_extract_json_output()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 666 che coinvolge il nodo canonico `_extract_json_output()` è `L189`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 666 che coinvolge il nodo canonico `_extract_json_output()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 666 che coinvolge il nodo canonico `_extract_json_output()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 666 che coinvolge il nodo canonico `_extract_json_output()` è `1.0`. Nel grafo unificato, l'arco 666 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_extract_json_output()`. Nel grafo unificato, l'arco 666 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_json_output` per la relazione che coinvolge il nodo canonico `_extract_json_output()`.
<!-- GRAPH_EDGE graph=unificata index=706 role=incoming -->
Nel grafo unificato, il nodo canonico `_extract_json_output()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Any [traffic_exec_engine_py_any]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_py_any` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_json_output`. La relazione tecnica esplicita è la seguente: `Any [traffic_exec_engine_py_any]` fa riferimento a `_extract_json_output()`. Nel grafo unificato, il tipo dichiarato dell'arco 706 che coinvolge il nodo canonico `_extract_json_output()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 706 che coinvolge il nodo canonico `_extract_json_output()` è `return_type`. Nel grafo unificato, la confidenza categoriale dell'arco 706 che coinvolge il nodo canonico `_extract_json_output()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 706 che coinvolge il nodo canonico `_extract_json_output()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 706 che coinvolge il nodo canonico `_extract_json_output()` è `L189`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 706 che coinvolge il nodo canonico `_extract_json_output()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 706 che coinvolge il nodo canonico `_extract_json_output()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 706 che coinvolge il nodo canonico `_extract_json_output()` è `1.0`. Nel grafo unificato, l'arco 706 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `_extract_json_output()`. Nel grafo unificato, l'arco 706 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_json_output` per la relazione che coinvolge il nodo canonico `_extract_json_output()`.
<!-- GRAPH_EDGE graph=unificata index=721 role=incoming -->
Nel grafo unificato, il nodo canonico `_extract_json_output()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `extract_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_json_output`. La relazione tecnica esplicita è la seguente: `extract_outputs()` chiama `_extract_json_output()`. Nel grafo unificato, il tipo dichiarato dell'arco 721 che coinvolge il nodo canonico `_extract_json_output()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 721 che coinvolge il nodo canonico `_extract_json_output()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 721 che coinvolge il nodo canonico `_extract_json_output()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 721 che coinvolge il nodo canonico `_extract_json_output()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 721 che coinvolge il nodo canonico `_extract_json_output()` è `L182`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 721 che coinvolge il nodo canonico `_extract_json_output()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 721 che coinvolge il nodo canonico `_extract_json_output()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 721 che coinvolge il nodo canonico `_extract_json_output()` è `1.0`. Nel grafo unificato, l'arco 721 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `_extract_json_output()`. Nel grafo unificato, l'arco 721 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_json_output` per la relazione che coinvolge il nodo canonico `_extract_json_output()`.

### _extract_xml_output()

<!-- GRAPH_NODE graph=codice index=351 id=traffic_exec_engine_extract_xml_output -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_extract_xml_output()` è `_extract_xml_output()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_extract_xml_output()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_extract_xml_output()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_extract_xml_output()` è `L203`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_extract_xml_output()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_extract_xml_output()` possiede l'identificatore esatto `traffic_exec_engine_extract_xml_output`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_extract_xml_output()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_extract_xml_output()` è `_extract_xml_output()`.
<!-- GRAPH_EDGE graph=codice index=708 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_extract_xml_output()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [traffic_exec_engine_py_any]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output` all'ID `traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `_extract_xml_output()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 708 che coinvolge il nodo canonico `_extract_xml_output()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 708 che coinvolge il nodo canonico `_extract_xml_output()` è `return_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 708 che coinvolge il nodo canonico `_extract_xml_output()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 708 che coinvolge il nodo canonico `_extract_xml_output()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 708 che coinvolge il nodo canonico `_extract_xml_output()` è `L203`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 708 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 708 che coinvolge il nodo canonico `_extract_xml_output()` è `ast`. Nel grafo del codice RDG, l'arco 708 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo del codice RDG, l'arco 708 registra come `target` l'identificatore `traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 708 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=726 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_extract_xml_output()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_extract_xml_output_stdlib()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output` all'ID `traffic_exec_engine_extract_xml_output_stdlib`. La relazione tecnica esplicita è la seguente: `_extract_xml_output()` chiama `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 726 che coinvolge il nodo canonico `_extract_xml_output()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output()` è `L207`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output()` è `ast`. Nel grafo del codice RDG, l'arco 726 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo del codice RDG, l'arco 726 registra come `target` l'identificatore `traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=727 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_extract_xml_output()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_linearize_xml()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output` all'ID `traffic_exec_engine_linearize_xml`. La relazione tecnica esplicita è la seguente: `_extract_xml_output()` chiama `_linearize_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 727 che coinvolge il nodo canonico `_extract_xml_output()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 727 che coinvolge il nodo canonico `_extract_xml_output()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 727 che coinvolge il nodo canonico `_extract_xml_output()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 727 che coinvolge il nodo canonico `_extract_xml_output()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 727 che coinvolge il nodo canonico `_extract_xml_output()` è `L231`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 727 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 727 che coinvolge il nodo canonico `_extract_xml_output()` è `ast`. Nel grafo del codice RDG, l'arco 727 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo del codice RDG, l'arco 727 registra come `target` l'identificatore `traffic_exec_engine_linearize_xml` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 727 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=728 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_extract_xml_output()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `_extract_xml_output()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 728 che coinvolge il nodo canonico `_extract_xml_output()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 728 che coinvolge il nodo canonico `_extract_xml_output()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 728 che coinvolge il nodo canonico `_extract_xml_output()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 728 che coinvolge il nodo canonico `_extract_xml_output()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 728 che coinvolge il nodo canonico `_extract_xml_output()` è `L212`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 728 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 728 che coinvolge il nodo canonico `_extract_xml_output()` è `ast`. Nel grafo del codice RDG, l'arco 728 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo del codice RDG, l'arco 728 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 728 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=729 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_extract_xml_output()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_xml_raw_string()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output` all'ID `traffic_exec_engine_xml_raw_string`. La relazione tecnica esplicita è la seguente: `_extract_xml_output()` chiama `_xml_raw_string()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 729 che coinvolge il nodo canonico `_extract_xml_output()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 729 che coinvolge il nodo canonico `_extract_xml_output()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 729 che coinvolge il nodo canonico `_extract_xml_output()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 729 che coinvolge il nodo canonico `_extract_xml_output()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 729 che coinvolge il nodo canonico `_extract_xml_output()` è `L209`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 729 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 729 che coinvolge il nodo canonico `_extract_xml_output()` è `ast`. Nel grafo del codice RDG, l'arco 729 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo del codice RDG, l'arco 729 registra come `target` l'identificatore `traffic_exec_engine_xml_raw_string` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 729 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=668 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_extract_xml_output()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_extract_xml_output`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_extract_xml_output()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 668 che coinvolge il nodo canonico `_extract_xml_output()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 668 che coinvolge il nodo canonico `_extract_xml_output()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 668 che coinvolge il nodo canonico `_extract_xml_output()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 668 che coinvolge il nodo canonico `_extract_xml_output()` è `L203`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 668 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 668 che coinvolge il nodo canonico `_extract_xml_output()` è `ast`. Nel grafo del codice RDG, l'arco 668 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo del codice RDG, l'arco 668 registra come `target` l'identificatore `traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 668 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=722 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_extract_xml_output()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `extract_outputs()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_outputs` all'ID `traffic_exec_engine_extract_xml_output`. La relazione tecnica esplicita è la seguente: `extract_outputs()` chiama `_extract_xml_output()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 722 che coinvolge il nodo canonico `_extract_xml_output()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 722 che coinvolge il nodo canonico `_extract_xml_output()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 722 che coinvolge il nodo canonico `_extract_xml_output()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 722 che coinvolge il nodo canonico `_extract_xml_output()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 722 che coinvolge il nodo canonico `_extract_xml_output()` è `L180`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 722 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 722 che coinvolge il nodo canonico `_extract_xml_output()` è `ast`. Nel grafo del codice RDG, l'arco 722 registra come `source` l'identificatore `traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo del codice RDG, l'arco 722 registra come `target` l'identificatore `traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 722 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=351 id=Request-Dataset-Generator::traffic_exec_engine_extract_xml_output -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_extract_xml_output()` è `_extract_xml_output()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_extract_xml_output()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_extract_xml_output()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_extract_xml_output()` è `L203`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_extract_xml_output()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_extract_xml_output()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_extract_xml_output()` è `_extract_xml_output()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_extract_xml_output()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_extract_xml_output()` è `traffic_exec_engine_extract_xml_output`. Nel grafo unificato, il nodo canonico `_extract_xml_output()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output`.
<!-- GRAPH_EDGE graph=unificata index=726 role=outgoing -->
Nel grafo unificato, il nodo canonico `_extract_xml_output()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_extract_xml_output_stdlib()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib`. La relazione tecnica esplicita è la seguente: `_extract_xml_output()` chiama `_extract_xml_output_stdlib()`. Nel grafo unificato, il tipo dichiarato dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 726 che coinvolge il nodo canonico `_extract_xml_output()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output()` è `L207`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo unificato, l'arco 726 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo unificato, l'arco 726 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`.
<!-- GRAPH_EDGE graph=unificata index=727 role=outgoing -->
Nel grafo unificato, il nodo canonico `_extract_xml_output()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_linearize_xml()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` all'ID `Request-Dataset-Generator::traffic_exec_engine_linearize_xml`. La relazione tecnica esplicita è la seguente: `_extract_xml_output()` chiama `_linearize_xml()`. Nel grafo unificato, il tipo dichiarato dell'arco 727 che coinvolge il nodo canonico `_extract_xml_output()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 727 che coinvolge il nodo canonico `_extract_xml_output()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 727 che coinvolge il nodo canonico `_extract_xml_output()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 727 che coinvolge il nodo canonico `_extract_xml_output()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 727 che coinvolge il nodo canonico `_extract_xml_output()` è `L231`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 727 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 727 che coinvolge il nodo canonico `_extract_xml_output()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 727 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo unificato, l'arco 727 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo unificato, l'arco 727 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_linearize_xml` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`.
<!-- GRAPH_EDGE graph=unificata index=728 role=outgoing -->
Nel grafo unificato, il nodo canonico `_extract_xml_output()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `_extract_xml_output()` chiama `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 728 che coinvolge il nodo canonico `_extract_xml_output()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 728 che coinvolge il nodo canonico `_extract_xml_output()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 728 che coinvolge il nodo canonico `_extract_xml_output()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 728 che coinvolge il nodo canonico `_extract_xml_output()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 728 che coinvolge il nodo canonico `_extract_xml_output()` è `L212`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 728 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 728 che coinvolge il nodo canonico `_extract_xml_output()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 728 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo unificato, l'arco 728 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo unificato, l'arco 728 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`.
<!-- GRAPH_EDGE graph=unificata index=729 role=outgoing -->
Nel grafo unificato, il nodo canonico `_extract_xml_output()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_xml_raw_string()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` all'ID `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string`. La relazione tecnica esplicita è la seguente: `_extract_xml_output()` chiama `_xml_raw_string()`. Nel grafo unificato, il tipo dichiarato dell'arco 729 che coinvolge il nodo canonico `_extract_xml_output()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 729 che coinvolge il nodo canonico `_extract_xml_output()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 729 che coinvolge il nodo canonico `_extract_xml_output()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 729 che coinvolge il nodo canonico `_extract_xml_output()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 729 che coinvolge il nodo canonico `_extract_xml_output()` è `L209`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 729 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 729 che coinvolge il nodo canonico `_extract_xml_output()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 729 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo unificato, l'arco 729 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo unificato, l'arco 729 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`.
<!-- GRAPH_EDGE graph=unificata index=668 role=incoming -->
Nel grafo unificato, il nodo canonico `_extract_xml_output()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_extract_xml_output()`. Nel grafo unificato, il tipo dichiarato dell'arco 668 che coinvolge il nodo canonico `_extract_xml_output()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 668 che coinvolge il nodo canonico `_extract_xml_output()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 668 che coinvolge il nodo canonico `_extract_xml_output()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 668 che coinvolge il nodo canonico `_extract_xml_output()` è `L203`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 668 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 668 che coinvolge il nodo canonico `_extract_xml_output()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 668 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo unificato, l'arco 668 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo unificato, l'arco 668 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`.
<!-- GRAPH_EDGE graph=unificata index=708 role=incoming -->
Nel grafo unificato, il nodo canonico `_extract_xml_output()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Any [traffic_exec_engine_py_any]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_py_any` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output`. La relazione tecnica esplicita è la seguente: `Any [traffic_exec_engine_py_any]` fa riferimento a `_extract_xml_output()`. Nel grafo unificato, il tipo dichiarato dell'arco 708 che coinvolge il nodo canonico `_extract_xml_output()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 708 che coinvolge il nodo canonico `_extract_xml_output()` è `return_type`. Nel grafo unificato, la confidenza categoriale dell'arco 708 che coinvolge il nodo canonico `_extract_xml_output()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 708 che coinvolge il nodo canonico `_extract_xml_output()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 708 che coinvolge il nodo canonico `_extract_xml_output()` è `L203`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 708 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 708 che coinvolge il nodo canonico `_extract_xml_output()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 708 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo unificato, l'arco 708 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo unificato, l'arco 708 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`.
<!-- GRAPH_EDGE graph=unificata index=722 role=incoming -->
Nel grafo unificato, il nodo canonico `_extract_xml_output()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `extract_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output`. La relazione tecnica esplicita è la seguente: `extract_outputs()` chiama `_extract_xml_output()`. Nel grafo unificato, il tipo dichiarato dell'arco 722 che coinvolge il nodo canonico `_extract_xml_output()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 722 che coinvolge il nodo canonico `_extract_xml_output()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 722 che coinvolge il nodo canonico `_extract_xml_output()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 722 che coinvolge il nodo canonico `_extract_xml_output()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 722 che coinvolge il nodo canonico `_extract_xml_output()` è `L180`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 722 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 722 che coinvolge il nodo canonico `_extract_xml_output()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 722 che coinvolge il nodo canonico `_extract_xml_output()` è `1.0`. Nel grafo unificato, l'arco 722 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`. Nel grafo unificato, l'arco 722 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `_extract_xml_output()`.

### _extract_xml_output_stdlib()

<!-- GRAPH_NODE graph=codice index=352 id=traffic_exec_engine_extract_xml_output_stdlib -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_extract_xml_output_stdlib()` è `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_extract_xml_output_stdlib()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_extract_xml_output_stdlib()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_extract_xml_output_stdlib()` è `L239`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_extract_xml_output_stdlib()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_extract_xml_output_stdlib()` possiede l'identificatore esatto `traffic_exec_engine_extract_xml_output_stdlib`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_extract_xml_output_stdlib()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_extract_xml_output_stdlib()` è `_extract_xml_output_stdlib()`.
<!-- GRAPH_EDGE graph=codice index=692 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_extract_xml_output_stdlib()` è la sorgente `source` della relazione `calls` verso il nodo canonico `OutputExtractionError`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output_stdlib` all'ID `traffic_exec_engine_outputextractionerror`. La relazione tecnica esplicita è la seguente: `_extract_xml_output_stdlib()` chiama `OutputExtractionError`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 692 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 692 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 692 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 692 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 692 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `L270`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 692 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 692 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `ast`. Nel grafo del codice RDG, l'arco 692 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, l'arco 692 registra come `target` l'identificatore `traffic_exec_engine_outputextractionerror` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 692 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=709 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_extract_xml_output_stdlib()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [traffic_exec_engine_py_any]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output_stdlib` all'ID `traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `_extract_xml_output_stdlib()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 709 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 709 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `return_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 709 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 709 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 709 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `L239`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 709 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 709 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `ast`. Nel grafo del codice RDG, l'arco 709 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, l'arco 709 registra come `target` l'identificatore `traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 709 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=730 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_extract_xml_output_stdlib()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_find_first_by_local_name()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output_stdlib` all'ID `traffic_exec_engine_find_first_by_local_name`. La relazione tecnica esplicita è la seguente: `_extract_xml_output_stdlib()` chiama `_find_first_by_local_name()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 730 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 730 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 730 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 730 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 730 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `L247`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 730 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 730 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `ast`. Nel grafo del codice RDG, l'arco 730 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, l'arco 730 registra come `target` l'identificatore `traffic_exec_engine_find_first_by_local_name` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 730 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=731 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_extract_xml_output_stdlib()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_linearize_xml()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output_stdlib` all'ID `traffic_exec_engine_linearize_xml`. La relazione tecnica esplicita è la seguente: `_extract_xml_output_stdlib()` chiama `_linearize_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 731 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 731 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 731 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 731 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 731 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `L256`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 731 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 731 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `ast`. Nel grafo del codice RDG, l'arco 731 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, l'arco 731 registra come `target` l'identificatore `traffic_exec_engine_linearize_xml` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 731 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=732 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_extract_xml_output_stdlib()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output_stdlib` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `_extract_xml_output_stdlib()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 732 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 732 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 732 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 732 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 732 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `L243`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 732 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 732 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `ast`. Nel grafo del codice RDG, l'arco 732 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, l'arco 732 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 732 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=733 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_extract_xml_output_stdlib()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_xml_raw_string()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output_stdlib` all'ID `traffic_exec_engine_xml_raw_string`. La relazione tecnica esplicita è la seguente: `_extract_xml_output_stdlib()` chiama `_xml_raw_string()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 733 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 733 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 733 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 733 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 733 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `L240`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 733 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 733 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `ast`. Nel grafo del codice RDG, l'arco 733 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, l'arco 733 registra come `target` l'identificatore `traffic_exec_engine_xml_raw_string` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 733 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=669 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_extract_xml_output_stdlib()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_extract_xml_output_stdlib`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 669 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 669 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 669 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 669 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `L239`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 669 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 669 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `ast`. Nel grafo del codice RDG, l'arco 669 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, l'arco 669 registra come `target` l'identificatore `traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 669 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=726 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_extract_xml_output_stdlib()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_extract_xml_output()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output` all'ID `traffic_exec_engine_extract_xml_output_stdlib`. La relazione tecnica esplicita è la seguente: `_extract_xml_output()` chiama `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 726 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `L207`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `ast`. Nel grafo del codice RDG, l'arco 726 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, l'arco 726 registra come `target` l'identificatore `traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=352 id=Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_extract_xml_output_stdlib()` è `_extract_xml_output_stdlib()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_extract_xml_output_stdlib()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_extract_xml_output_stdlib()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_extract_xml_output_stdlib()` è `L239`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_extract_xml_output_stdlib()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_extract_xml_output_stdlib()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_extract_xml_output_stdlib()` è `_extract_xml_output_stdlib()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_extract_xml_output_stdlib()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_extract_xml_output_stdlib()` è `traffic_exec_engine_extract_xml_output_stdlib`. Nel grafo unificato, il nodo canonico `_extract_xml_output_stdlib()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib`.
<!-- GRAPH_EDGE graph=unificata index=730 role=outgoing -->
Nel grafo unificato, il nodo canonico `_extract_xml_output_stdlib()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_find_first_by_local_name()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` all'ID `Request-Dataset-Generator::traffic_exec_engine_find_first_by_local_name`. La relazione tecnica esplicita è la seguente: `_extract_xml_output_stdlib()` chiama `_find_first_by_local_name()`. Nel grafo unificato, il tipo dichiarato dell'arco 730 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 730 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 730 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 730 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 730 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `L247`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 730 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 730 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 730 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo unificato, l'arco 730 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo unificato, l'arco 730 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_find_first_by_local_name` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`.
<!-- GRAPH_EDGE graph=unificata index=731 role=outgoing -->
Nel grafo unificato, il nodo canonico `_extract_xml_output_stdlib()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_linearize_xml()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` all'ID `Request-Dataset-Generator::traffic_exec_engine_linearize_xml`. La relazione tecnica esplicita è la seguente: `_extract_xml_output_stdlib()` chiama `_linearize_xml()`. Nel grafo unificato, il tipo dichiarato dell'arco 731 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 731 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 731 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 731 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 731 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `L256`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 731 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 731 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 731 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo unificato, l'arco 731 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo unificato, l'arco 731 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_linearize_xml` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`.
<!-- GRAPH_EDGE graph=unificata index=732 role=outgoing -->
Nel grafo unificato, il nodo canonico `_extract_xml_output_stdlib()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `_extract_xml_output_stdlib()` chiama `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 732 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 732 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 732 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 732 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 732 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `L243`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 732 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 732 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 732 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo unificato, l'arco 732 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo unificato, l'arco 732 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`.
<!-- GRAPH_EDGE graph=unificata index=733 role=outgoing -->
Nel grafo unificato, il nodo canonico `_extract_xml_output_stdlib()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_xml_raw_string()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` all'ID `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string`. La relazione tecnica esplicita è la seguente: `_extract_xml_output_stdlib()` chiama `_xml_raw_string()`. Nel grafo unificato, il tipo dichiarato dell'arco 733 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 733 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 733 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 733 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 733 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `L240`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 733 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 733 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 733 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo unificato, l'arco 733 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo unificato, l'arco 733 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`.
<!-- GRAPH_EDGE graph=unificata index=669 role=incoming -->
Nel grafo unificato, il nodo canonico `_extract_xml_output_stdlib()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_extract_xml_output_stdlib()`. Nel grafo unificato, il tipo dichiarato dell'arco 669 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 669 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 669 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 669 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `L239`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 669 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 669 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 669 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo unificato, l'arco 669 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo unificato, l'arco 669 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`.
<!-- GRAPH_EDGE graph=unificata index=692 role=incoming -->
Nel grafo unificato, il nodo canonico `_extract_xml_output_stdlib()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `OutputExtractionError`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib`. La relazione tecnica esplicita è la seguente: `OutputExtractionError` chiama `_extract_xml_output_stdlib()`. Nel grafo unificato, il tipo dichiarato dell'arco 692 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 692 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 692 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 692 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 692 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `L270`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 692 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 692 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 692 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo unificato, l'arco 692 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo unificato, l'arco 692 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`.
<!-- GRAPH_EDGE graph=unificata index=709 role=incoming -->
Nel grafo unificato, il nodo canonico `_extract_xml_output_stdlib()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Any [traffic_exec_engine_py_any]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_py_any` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib`. La relazione tecnica esplicita è la seguente: `Any [traffic_exec_engine_py_any]` fa riferimento a `_extract_xml_output_stdlib()`. Nel grafo unificato, il tipo dichiarato dell'arco 709 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 709 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `return_type`. Nel grafo unificato, la confidenza categoriale dell'arco 709 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 709 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 709 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `L239`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 709 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 709 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 709 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo unificato, l'arco 709 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo unificato, l'arco 709 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`.
<!-- GRAPH_EDGE graph=unificata index=726 role=incoming -->
Nel grafo unificato, il nodo canonico `_extract_xml_output_stdlib()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_extract_xml_output()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib`. La relazione tecnica esplicita è la seguente: `_extract_xml_output()` chiama `_extract_xml_output_stdlib()`. Nel grafo unificato, il tipo dichiarato dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 726 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `L207`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 726 che coinvolge il nodo canonico `_extract_xml_output_stdlib()` è `1.0`. Nel grafo unificato, l'arco 726 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`. Nel grafo unificato, l'arco 726 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_extract_xml_output_stdlib()`.

### _find_first_by_local_name()

<!-- GRAPH_NODE graph=codice index=356 id=traffic_exec_engine_find_first_by_local_name -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_find_first_by_local_name()` è `_find_first_by_local_name()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_find_first_by_local_name()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_find_first_by_local_name()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_find_first_by_local_name()` è `L321`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_find_first_by_local_name()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_find_first_by_local_name()` possiede l'identificatore esatto `traffic_exec_engine_find_first_by_local_name`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_find_first_by_local_name()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_find_first_by_local_name()` è `_find_first_by_local_name()`.
<!-- GRAPH_EDGE graph=codice index=736 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_find_first_by_local_name()` è la sorgente `source` della relazione `references` verso il nodo canonico `Element [traffic_exec_engine_py_element]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_find_first_by_local_name` all'ID `traffic_exec_engine_py_element`. La relazione tecnica esplicita è la seguente: `_find_first_by_local_name()` fa riferimento a `Element [traffic_exec_engine_py_element]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 736 che coinvolge il nodo canonico `_find_first_by_local_name()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 736 che coinvolge il nodo canonico `_find_first_by_local_name()` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 736 che coinvolge il nodo canonico `_find_first_by_local_name()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 736 che coinvolge il nodo canonico `_find_first_by_local_name()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 736 che coinvolge il nodo canonico `_find_first_by_local_name()` è `L321`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 736 che coinvolge il nodo canonico `_find_first_by_local_name()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 736 che coinvolge il nodo canonico `_find_first_by_local_name()` è `ast`. Nel grafo del codice RDG, l'arco 736 registra come `source` l'identificatore `traffic_exec_engine_find_first_by_local_name` per la relazione che coinvolge il nodo canonico `_find_first_by_local_name()`. Nel grafo del codice RDG, l'arco 736 registra come `target` l'identificatore `traffic_exec_engine_py_element` per la relazione che coinvolge il nodo canonico `_find_first_by_local_name()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 736 che coinvolge il nodo canonico `_find_first_by_local_name()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=671 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_find_first_by_local_name()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_find_first_by_local_name`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_find_first_by_local_name()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 671 che coinvolge il nodo canonico `_find_first_by_local_name()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 671 che coinvolge il nodo canonico `_find_first_by_local_name()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 671 che coinvolge il nodo canonico `_find_first_by_local_name()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 671 che coinvolge il nodo canonico `_find_first_by_local_name()` è `L321`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 671 che coinvolge il nodo canonico `_find_first_by_local_name()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 671 che coinvolge il nodo canonico `_find_first_by_local_name()` è `ast`. Nel grafo del codice RDG, l'arco 671 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_find_first_by_local_name()`. Nel grafo del codice RDG, l'arco 671 registra come `target` l'identificatore `traffic_exec_engine_find_first_by_local_name` per la relazione che coinvolge il nodo canonico `_find_first_by_local_name()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 671 che coinvolge il nodo canonico `_find_first_by_local_name()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=730 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_find_first_by_local_name()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_extract_xml_output_stdlib()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output_stdlib` all'ID `traffic_exec_engine_find_first_by_local_name`. La relazione tecnica esplicita è la seguente: `_extract_xml_output_stdlib()` chiama `_find_first_by_local_name()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 730 che coinvolge il nodo canonico `_find_first_by_local_name()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 730 che coinvolge il nodo canonico `_find_first_by_local_name()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 730 che coinvolge il nodo canonico `_find_first_by_local_name()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 730 che coinvolge il nodo canonico `_find_first_by_local_name()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 730 che coinvolge il nodo canonico `_find_first_by_local_name()` è `L247`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 730 che coinvolge il nodo canonico `_find_first_by_local_name()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 730 che coinvolge il nodo canonico `_find_first_by_local_name()` è `ast`. Nel grafo del codice RDG, l'arco 730 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_find_first_by_local_name()`. Nel grafo del codice RDG, l'arco 730 registra come `target` l'identificatore `traffic_exec_engine_find_first_by_local_name` per la relazione che coinvolge il nodo canonico `_find_first_by_local_name()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 730 che coinvolge il nodo canonico `_find_first_by_local_name()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=356 id=Request-Dataset-Generator::traffic_exec_engine_find_first_by_local_name -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_find_first_by_local_name()` è `_find_first_by_local_name()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_find_first_by_local_name()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_find_first_by_local_name()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_find_first_by_local_name()` è `L321`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_find_first_by_local_name()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_find_first_by_local_name()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_find_first_by_local_name()` è `_find_first_by_local_name()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_find_first_by_local_name()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_find_first_by_local_name()` è `traffic_exec_engine_find_first_by_local_name`. Nel grafo unificato, il nodo canonico `_find_first_by_local_name()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_find_first_by_local_name`.
<!-- GRAPH_EDGE graph=unificata index=736 role=outgoing -->
Nel grafo unificato, il nodo canonico `_find_first_by_local_name()` è la sorgente `source` della relazione `references` verso il nodo canonico `Element [traffic_exec_engine_py_element]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_find_first_by_local_name` all'ID `Request-Dataset-Generator::traffic_exec_engine_py_element`. La relazione tecnica esplicita è la seguente: `_find_first_by_local_name()` fa riferimento a `Element [traffic_exec_engine_py_element]`. Nel grafo unificato, il tipo dichiarato dell'arco 736 che coinvolge il nodo canonico `_find_first_by_local_name()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 736 che coinvolge il nodo canonico `_find_first_by_local_name()` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 736 che coinvolge il nodo canonico `_find_first_by_local_name()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 736 che coinvolge il nodo canonico `_find_first_by_local_name()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 736 che coinvolge il nodo canonico `_find_first_by_local_name()` è `L321`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 736 che coinvolge il nodo canonico `_find_first_by_local_name()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 736 che coinvolge il nodo canonico `_find_first_by_local_name()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 736 che coinvolge il nodo canonico `_find_first_by_local_name()` è `1.0`. Nel grafo unificato, l'arco 736 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_find_first_by_local_name` per la relazione che coinvolge il nodo canonico `_find_first_by_local_name()`. Nel grafo unificato, l'arco 736 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_element` per la relazione che coinvolge il nodo canonico `_find_first_by_local_name()`.
<!-- GRAPH_EDGE graph=unificata index=671 role=incoming -->
Nel grafo unificato, il nodo canonico `_find_first_by_local_name()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_find_first_by_local_name`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_find_first_by_local_name()`. Nel grafo unificato, il tipo dichiarato dell'arco 671 che coinvolge il nodo canonico `_find_first_by_local_name()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 671 che coinvolge il nodo canonico `_find_first_by_local_name()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 671 che coinvolge il nodo canonico `_find_first_by_local_name()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 671 che coinvolge il nodo canonico `_find_first_by_local_name()` è `L321`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 671 che coinvolge il nodo canonico `_find_first_by_local_name()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 671 che coinvolge il nodo canonico `_find_first_by_local_name()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 671 che coinvolge il nodo canonico `_find_first_by_local_name()` è `1.0`. Nel grafo unificato, l'arco 671 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_find_first_by_local_name()`. Nel grafo unificato, l'arco 671 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_find_first_by_local_name` per la relazione che coinvolge il nodo canonico `_find_first_by_local_name()`.
<!-- GRAPH_EDGE graph=unificata index=730 role=incoming -->
Nel grafo unificato, il nodo canonico `_find_first_by_local_name()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_extract_xml_output_stdlib()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` all'ID `Request-Dataset-Generator::traffic_exec_engine_find_first_by_local_name`. La relazione tecnica esplicita è la seguente: `_extract_xml_output_stdlib()` chiama `_find_first_by_local_name()`. Nel grafo unificato, il tipo dichiarato dell'arco 730 che coinvolge il nodo canonico `_find_first_by_local_name()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 730 che coinvolge il nodo canonico `_find_first_by_local_name()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 730 che coinvolge il nodo canonico `_find_first_by_local_name()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 730 che coinvolge il nodo canonico `_find_first_by_local_name()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 730 che coinvolge il nodo canonico `_find_first_by_local_name()` è `L247`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 730 che coinvolge il nodo canonico `_find_first_by_local_name()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 730 che coinvolge il nodo canonico `_find_first_by_local_name()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 730 che coinvolge il nodo canonico `_find_first_by_local_name()` è `1.0`. Nel grafo unificato, l'arco 730 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_find_first_by_local_name()`. Nel grafo unificato, l'arco 730 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_find_first_by_local_name` per la relazione che coinvolge il nodo canonico `_find_first_by_local_name()`.

### _linearize_xml()

<!-- GRAPH_NODE graph=codice index=354 id=traffic_exec_engine_linearize_xml -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_linearize_xml()` è `_linearize_xml()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_linearize_xml()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_linearize_xml()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_linearize_xml()` è `L285`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_linearize_xml()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_linearize_xml()` possiede l'identificatore esatto `traffic_exec_engine_linearize_xml`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_linearize_xml()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_linearize_xml()` è `_linearize_xml()`.
<!-- GRAPH_EDGE graph=codice index=734 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_linearize_xml()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_linearize_xml` all'ID `traffic_exec_engine_replace_newlines_inside_tags`. La relazione tecnica esplicita è la seguente: `_linearize_xml()` chiama `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 734 che coinvolge il nodo canonico `_linearize_xml()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 734 che coinvolge il nodo canonico `_linearize_xml()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 734 che coinvolge il nodo canonico `_linearize_xml()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 734 che coinvolge il nodo canonico `_linearize_xml()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 734 che coinvolge il nodo canonico `_linearize_xml()` è `L294`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 734 che coinvolge il nodo canonico `_linearize_xml()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 734 che coinvolge il nodo canonico `_linearize_xml()` è `ast`. Nel grafo del codice RDG, l'arco 734 registra come `source` l'identificatore `traffic_exec_engine_linearize_xml` per la relazione che coinvolge il nodo canonico `_linearize_xml()`. Nel grafo del codice RDG, l'arco 734 registra come `target` l'identificatore `traffic_exec_engine_replace_newlines_inside_tags` per la relazione che coinvolge il nodo canonico `_linearize_xml()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 734 che coinvolge il nodo canonico `_linearize_xml()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=672 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_linearize_xml()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_linearize_xml`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_linearize_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 672 che coinvolge il nodo canonico `_linearize_xml()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 672 che coinvolge il nodo canonico `_linearize_xml()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 672 che coinvolge il nodo canonico `_linearize_xml()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 672 che coinvolge il nodo canonico `_linearize_xml()` è `L285`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 672 che coinvolge il nodo canonico `_linearize_xml()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 672 che coinvolge il nodo canonico `_linearize_xml()` è `ast`. Nel grafo del codice RDG, l'arco 672 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_linearize_xml()`. Nel grafo del codice RDG, l'arco 672 registra come `target` l'identificatore `traffic_exec_engine_linearize_xml` per la relazione che coinvolge il nodo canonico `_linearize_xml()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 672 che coinvolge il nodo canonico `_linearize_xml()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=727 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_linearize_xml()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_extract_xml_output()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output` all'ID `traffic_exec_engine_linearize_xml`. La relazione tecnica esplicita è la seguente: `_extract_xml_output()` chiama `_linearize_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 727 che coinvolge il nodo canonico `_linearize_xml()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 727 che coinvolge il nodo canonico `_linearize_xml()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 727 che coinvolge il nodo canonico `_linearize_xml()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 727 che coinvolge il nodo canonico `_linearize_xml()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 727 che coinvolge il nodo canonico `_linearize_xml()` è `L231`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 727 che coinvolge il nodo canonico `_linearize_xml()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 727 che coinvolge il nodo canonico `_linearize_xml()` è `ast`. Nel grafo del codice RDG, l'arco 727 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `_linearize_xml()`. Nel grafo del codice RDG, l'arco 727 registra come `target` l'identificatore `traffic_exec_engine_linearize_xml` per la relazione che coinvolge il nodo canonico `_linearize_xml()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 727 che coinvolge il nodo canonico `_linearize_xml()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=731 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_linearize_xml()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_extract_xml_output_stdlib()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output_stdlib` all'ID `traffic_exec_engine_linearize_xml`. La relazione tecnica esplicita è la seguente: `_extract_xml_output_stdlib()` chiama `_linearize_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 731 che coinvolge il nodo canonico `_linearize_xml()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 731 che coinvolge il nodo canonico `_linearize_xml()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 731 che coinvolge il nodo canonico `_linearize_xml()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 731 che coinvolge il nodo canonico `_linearize_xml()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 731 che coinvolge il nodo canonico `_linearize_xml()` è `L256`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 731 che coinvolge il nodo canonico `_linearize_xml()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 731 che coinvolge il nodo canonico `_linearize_xml()` è `ast`. Nel grafo del codice RDG, l'arco 731 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_linearize_xml()`. Nel grafo del codice RDG, l'arco 731 registra come `target` l'identificatore `traffic_exec_engine_linearize_xml` per la relazione che coinvolge il nodo canonico `_linearize_xml()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 731 che coinvolge il nodo canonico `_linearize_xml()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=735 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_linearize_xml()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_286` all'ID `traffic_exec_engine_linearize_xml`. La relazione tecnica esplicita è la seguente: `Rimuove solo whitespace tra tag, preservando testo e attributi.` costituisce una motivazione per `_linearize_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 735 che coinvolge il nodo canonico `_linearize_xml()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 735 che coinvolge il nodo canonico `_linearize_xml()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 735 che coinvolge il nodo canonico `_linearize_xml()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 735 che coinvolge il nodo canonico `_linearize_xml()` è `L286`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 735 che coinvolge il nodo canonico `_linearize_xml()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 735 che coinvolge il nodo canonico `_linearize_xml()` è `ast`. Nel grafo del codice RDG, l'arco 735 registra come `source` l'identificatore `traffic_exec_engine_rationale_286` per la relazione che coinvolge il nodo canonico `_linearize_xml()`. Nel grafo del codice RDG, l'arco 735 registra come `target` l'identificatore `traffic_exec_engine_linearize_xml` per la relazione che coinvolge il nodo canonico `_linearize_xml()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 735 che coinvolge il nodo canonico `_linearize_xml()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=354 id=Request-Dataset-Generator::traffic_exec_engine_linearize_xml -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_linearize_xml()` è `_linearize_xml()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_linearize_xml()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_linearize_xml()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_linearize_xml()` è `L285`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_linearize_xml()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_linearize_xml()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_linearize_xml()` è `_linearize_xml()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_linearize_xml()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_linearize_xml()` è `traffic_exec_engine_linearize_xml`. Nel grafo unificato, il nodo canonico `_linearize_xml()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_linearize_xml`.
<!-- GRAPH_EDGE graph=unificata index=734 role=outgoing -->
Nel grafo unificato, il nodo canonico `_linearize_xml()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_linearize_xml` all'ID `Request-Dataset-Generator::traffic_exec_engine_replace_newlines_inside_tags`. La relazione tecnica esplicita è la seguente: `_linearize_xml()` chiama `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]`. Nel grafo unificato, il tipo dichiarato dell'arco 734 che coinvolge il nodo canonico `_linearize_xml()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 734 che coinvolge il nodo canonico `_linearize_xml()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 734 che coinvolge il nodo canonico `_linearize_xml()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 734 che coinvolge il nodo canonico `_linearize_xml()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 734 che coinvolge il nodo canonico `_linearize_xml()` è `L294`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 734 che coinvolge il nodo canonico `_linearize_xml()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 734 che coinvolge il nodo canonico `_linearize_xml()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 734 che coinvolge il nodo canonico `_linearize_xml()` è `1.0`. Nel grafo unificato, l'arco 734 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_linearize_xml` per la relazione che coinvolge il nodo canonico `_linearize_xml()`. Nel grafo unificato, l'arco 734 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_replace_newlines_inside_tags` per la relazione che coinvolge il nodo canonico `_linearize_xml()`.
<!-- GRAPH_EDGE graph=unificata index=735 role=outgoing -->
Nel grafo unificato, il nodo canonico `_linearize_xml()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_linearize_xml` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_286`. La relazione tecnica esplicita è la seguente: `_linearize_xml()` costituisce una motivazione per `Rimuove solo whitespace tra tag, preservando testo e attributi.`. Nel grafo unificato, il tipo dichiarato dell'arco 735 che coinvolge il nodo canonico `_linearize_xml()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 735 che coinvolge il nodo canonico `_linearize_xml()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 735 che coinvolge il nodo canonico `_linearize_xml()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 735 che coinvolge il nodo canonico `_linearize_xml()` è `L286`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 735 che coinvolge il nodo canonico `_linearize_xml()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 735 che coinvolge il nodo canonico `_linearize_xml()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 735 che coinvolge il nodo canonico `_linearize_xml()` è `1.0`. Nel grafo unificato, l'arco 735 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_linearize_xml` per la relazione che coinvolge il nodo canonico `_linearize_xml()`. Nel grafo unificato, l'arco 735 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_286` per la relazione che coinvolge il nodo canonico `_linearize_xml()`.
<!-- GRAPH_EDGE graph=unificata index=672 role=incoming -->
Nel grafo unificato, il nodo canonico `_linearize_xml()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_linearize_xml`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_linearize_xml()`. Nel grafo unificato, il tipo dichiarato dell'arco 672 che coinvolge il nodo canonico `_linearize_xml()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 672 che coinvolge il nodo canonico `_linearize_xml()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 672 che coinvolge il nodo canonico `_linearize_xml()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 672 che coinvolge il nodo canonico `_linearize_xml()` è `L285`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 672 che coinvolge il nodo canonico `_linearize_xml()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 672 che coinvolge il nodo canonico `_linearize_xml()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 672 che coinvolge il nodo canonico `_linearize_xml()` è `1.0`. Nel grafo unificato, l'arco 672 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_linearize_xml()`. Nel grafo unificato, l'arco 672 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_linearize_xml` per la relazione che coinvolge il nodo canonico `_linearize_xml()`.
<!-- GRAPH_EDGE graph=unificata index=727 role=incoming -->
Nel grafo unificato, il nodo canonico `_linearize_xml()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_extract_xml_output()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` all'ID `Request-Dataset-Generator::traffic_exec_engine_linearize_xml`. La relazione tecnica esplicita è la seguente: `_extract_xml_output()` chiama `_linearize_xml()`. Nel grafo unificato, il tipo dichiarato dell'arco 727 che coinvolge il nodo canonico `_linearize_xml()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 727 che coinvolge il nodo canonico `_linearize_xml()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 727 che coinvolge il nodo canonico `_linearize_xml()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 727 che coinvolge il nodo canonico `_linearize_xml()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 727 che coinvolge il nodo canonico `_linearize_xml()` è `L231`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 727 che coinvolge il nodo canonico `_linearize_xml()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 727 che coinvolge il nodo canonico `_linearize_xml()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 727 che coinvolge il nodo canonico `_linearize_xml()` è `1.0`. Nel grafo unificato, l'arco 727 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `_linearize_xml()`. Nel grafo unificato, l'arco 727 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_linearize_xml` per la relazione che coinvolge il nodo canonico `_linearize_xml()`.
<!-- GRAPH_EDGE graph=unificata index=731 role=incoming -->
Nel grafo unificato, il nodo canonico `_linearize_xml()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_extract_xml_output_stdlib()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` all'ID `Request-Dataset-Generator::traffic_exec_engine_linearize_xml`. La relazione tecnica esplicita è la seguente: `_extract_xml_output_stdlib()` chiama `_linearize_xml()`. Nel grafo unificato, il tipo dichiarato dell'arco 731 che coinvolge il nodo canonico `_linearize_xml()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 731 che coinvolge il nodo canonico `_linearize_xml()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 731 che coinvolge il nodo canonico `_linearize_xml()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 731 che coinvolge il nodo canonico `_linearize_xml()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 731 che coinvolge il nodo canonico `_linearize_xml()` è `L256`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 731 che coinvolge il nodo canonico `_linearize_xml()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 731 che coinvolge il nodo canonico `_linearize_xml()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 731 che coinvolge il nodo canonico `_linearize_xml()` è `1.0`. Nel grafo unificato, l'arco 731 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_linearize_xml()`. Nel grafo unificato, l'arco 731 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_linearize_xml` per la relazione che coinvolge il nodo canonico `_linearize_xml()`.

### _now_epoch_ms()

<!-- GRAPH_NODE graph=codice index=343 id=traffic_exec_engine_now_epoch_ms -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_now_epoch_ms()` è `_now_epoch_ms()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_now_epoch_ms()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_now_epoch_ms()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_now_epoch_ms()` è `L63`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_now_epoch_ms()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_now_epoch_ms()` possiede l'identificatore esatto `traffic_exec_engine_now_epoch_ms`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_now_epoch_ms()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_now_epoch_ms()` è `_now_epoch_ms()`.
Nel grafo del codice RDG, il nodo canonico `_now_epoch_ms()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=678 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_now_epoch_ms()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_now_epoch_ms`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_now_epoch_ms()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 678 che coinvolge il nodo canonico `_now_epoch_ms()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 678 che coinvolge il nodo canonico `_now_epoch_ms()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 678 che coinvolge il nodo canonico `_now_epoch_ms()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 678 che coinvolge il nodo canonico `_now_epoch_ms()` è `L63`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 678 che coinvolge il nodo canonico `_now_epoch_ms()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 678 che coinvolge il nodo canonico `_now_epoch_ms()` è `ast`. Nel grafo del codice RDG, l'arco 678 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_now_epoch_ms()`. Nel grafo del codice RDG, l'arco 678 registra come `target` l'identificatore `traffic_exec_engine_now_epoch_ms` per la relazione che coinvolge il nodo canonico `_now_epoch_ms()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 678 che coinvolge il nodo canonico `_now_epoch_ms()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=700 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_now_epoch_ms()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.log_request_sent()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_request_sent` all'ID `traffic_exec_engine_now_epoch_ms`. La relazione tecnica esplicita è la seguente: `.log_request_sent()` chiama `_now_epoch_ms()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 700 che coinvolge il nodo canonico `_now_epoch_ms()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 700 che coinvolge il nodo canonico `_now_epoch_ms()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 700 che coinvolge il nodo canonico `_now_epoch_ms()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 700 che coinvolge il nodo canonico `_now_epoch_ms()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 700 che coinvolge il nodo canonico `_now_epoch_ms()` è `L687`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 700 che coinvolge il nodo canonico `_now_epoch_ms()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 700 che coinvolge il nodo canonico `_now_epoch_ms()` è `ast`. Nel grafo del codice RDG, l'arco 700 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `_now_epoch_ms()`. Nel grafo del codice RDG, l'arco 700 registra come `target` l'identificatore `traffic_exec_engine_now_epoch_ms` per la relazione che coinvolge il nodo canonico `_now_epoch_ms()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 700 che coinvolge il nodo canonico `_now_epoch_ms()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=701 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_now_epoch_ms()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.log_response_received()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_response_received` all'ID `traffic_exec_engine_now_epoch_ms`. La relazione tecnica esplicita è la seguente: `.log_response_received()` chiama `_now_epoch_ms()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 701 che coinvolge il nodo canonico `_now_epoch_ms()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 701 che coinvolge il nodo canonico `_now_epoch_ms()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 701 che coinvolge il nodo canonico `_now_epoch_ms()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 701 che coinvolge il nodo canonico `_now_epoch_ms()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 701 che coinvolge il nodo canonico `_now_epoch_ms()` è `L722`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 701 che coinvolge il nodo canonico `_now_epoch_ms()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 701 che coinvolge il nodo canonico `_now_epoch_ms()` è `ast`. Nel grafo del codice RDG, l'arco 701 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `_now_epoch_ms()`. Nel grafo del codice RDG, l'arco 701 registra come `target` l'identificatore `traffic_exec_engine_now_epoch_ms` per la relazione che coinvolge il nodo canonico `_now_epoch_ms()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 701 che coinvolge il nodo canonico `_now_epoch_ms()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=343 id=Request-Dataset-Generator::traffic_exec_engine_now_epoch_ms -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_now_epoch_ms()` è `_now_epoch_ms()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_now_epoch_ms()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_now_epoch_ms()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_now_epoch_ms()` è `L63`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_now_epoch_ms()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_now_epoch_ms()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_now_epoch_ms()` è `_now_epoch_ms()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_now_epoch_ms()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_now_epoch_ms()` è `traffic_exec_engine_now_epoch_ms`. Nel grafo unificato, il nodo canonico `_now_epoch_ms()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_now_epoch_ms`.
<!-- GRAPH_EDGE graph=unificata index=700 role=outgoing -->
Nel grafo unificato, il nodo canonico `_now_epoch_ms()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.log_request_sent()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_now_epoch_ms` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent`. La relazione tecnica esplicita è la seguente: `_now_epoch_ms()` chiama `.log_request_sent()`. Nel grafo unificato, il tipo dichiarato dell'arco 700 che coinvolge il nodo canonico `_now_epoch_ms()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 700 che coinvolge il nodo canonico `_now_epoch_ms()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 700 che coinvolge il nodo canonico `_now_epoch_ms()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 700 che coinvolge il nodo canonico `_now_epoch_ms()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 700 che coinvolge il nodo canonico `_now_epoch_ms()` è `L687`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 700 che coinvolge il nodo canonico `_now_epoch_ms()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 700 che coinvolge il nodo canonico `_now_epoch_ms()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 700 che coinvolge il nodo canonico `_now_epoch_ms()` è `1.0`. Nel grafo unificato, l'arco 700 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_now_epoch_ms` per la relazione che coinvolge il nodo canonico `_now_epoch_ms()`. Nel grafo unificato, l'arco 700 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `_now_epoch_ms()`.
<!-- GRAPH_EDGE graph=unificata index=701 role=outgoing -->
Nel grafo unificato, il nodo canonico `_now_epoch_ms()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.log_response_received()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_now_epoch_ms` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received`. La relazione tecnica esplicita è la seguente: `_now_epoch_ms()` chiama `.log_response_received()`. Nel grafo unificato, il tipo dichiarato dell'arco 701 che coinvolge il nodo canonico `_now_epoch_ms()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 701 che coinvolge il nodo canonico `_now_epoch_ms()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 701 che coinvolge il nodo canonico `_now_epoch_ms()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 701 che coinvolge il nodo canonico `_now_epoch_ms()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 701 che coinvolge il nodo canonico `_now_epoch_ms()` è `L722`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 701 che coinvolge il nodo canonico `_now_epoch_ms()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 701 che coinvolge il nodo canonico `_now_epoch_ms()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 701 che coinvolge il nodo canonico `_now_epoch_ms()` è `1.0`. Nel grafo unificato, l'arco 701 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_now_epoch_ms` per la relazione che coinvolge il nodo canonico `_now_epoch_ms()`. Nel grafo unificato, l'arco 701 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `_now_epoch_ms()`.
<!-- GRAPH_EDGE graph=unificata index=678 role=incoming -->
Nel grafo unificato, il nodo canonico `_now_epoch_ms()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_now_epoch_ms`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_now_epoch_ms()`. Nel grafo unificato, il tipo dichiarato dell'arco 678 che coinvolge il nodo canonico `_now_epoch_ms()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 678 che coinvolge il nodo canonico `_now_epoch_ms()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 678 che coinvolge il nodo canonico `_now_epoch_ms()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 678 che coinvolge il nodo canonico `_now_epoch_ms()` è `L63`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 678 che coinvolge il nodo canonico `_now_epoch_ms()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 678 che coinvolge il nodo canonico `_now_epoch_ms()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 678 che coinvolge il nodo canonico `_now_epoch_ms()` è `1.0`. Nel grafo unificato, l'arco 678 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_now_epoch_ms()`. Nel grafo unificato, l'arco 678 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_now_epoch_ms` per la relazione che coinvolge il nodo canonico `_now_epoch_ms()`.

### _now_iso()

<!-- GRAPH_NODE graph=codice index=342 id=traffic_exec_engine_now_iso -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_now_iso()` è `_now_iso()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_now_iso()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_now_iso()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_now_iso()` è `L59`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_now_iso()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_now_iso()` possiede l'identificatore esatto `traffic_exec_engine_now_iso`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_now_iso()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_now_iso()` è `_now_iso()`.
Nel grafo del codice RDG, il nodo canonico `_now_iso()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=679 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_now_iso()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_now_iso`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_now_iso()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 679 che coinvolge il nodo canonico `_now_iso()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 679 che coinvolge il nodo canonico `_now_iso()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 679 che coinvolge il nodo canonico `_now_iso()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 679 che coinvolge il nodo canonico `_now_iso()` è `L59`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 679 che coinvolge il nodo canonico `_now_iso()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 679 che coinvolge il nodo canonico `_now_iso()` è `ast`. Nel grafo del codice RDG, l'arco 679 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_now_iso()`. Nel grafo del codice RDG, l'arco 679 registra come `target` l'identificatore `traffic_exec_engine_now_iso` per la relazione che coinvolge il nodo canonico `_now_iso()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 679 che coinvolge il nodo canonico `_now_iso()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=698 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_now_iso()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.log_request_sent()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_request_sent` all'ID `traffic_exec_engine_now_iso`. La relazione tecnica esplicita è la seguente: `.log_request_sent()` chiama `_now_iso()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 698 che coinvolge il nodo canonico `_now_iso()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 698 che coinvolge il nodo canonico `_now_iso()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 698 che coinvolge il nodo canonico `_now_iso()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 698 che coinvolge il nodo canonico `_now_iso()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 698 che coinvolge il nodo canonico `_now_iso()` è `L686`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 698 che coinvolge il nodo canonico `_now_iso()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 698 che coinvolge il nodo canonico `_now_iso()` è `ast`. Nel grafo del codice RDG, l'arco 698 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `_now_iso()`. Nel grafo del codice RDG, l'arco 698 registra come `target` l'identificatore `traffic_exec_engine_now_iso` per la relazione che coinvolge il nodo canonico `_now_iso()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 698 che coinvolge il nodo canonico `_now_iso()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=699 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_now_iso()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.log_response_received()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_response_received` all'ID `traffic_exec_engine_now_iso`. La relazione tecnica esplicita è la seguente: `.log_response_received()` chiama `_now_iso()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 699 che coinvolge il nodo canonico `_now_iso()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 699 che coinvolge il nodo canonico `_now_iso()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 699 che coinvolge il nodo canonico `_now_iso()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 699 che coinvolge il nodo canonico `_now_iso()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 699 che coinvolge il nodo canonico `_now_iso()` è `L721`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 699 che coinvolge il nodo canonico `_now_iso()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 699 che coinvolge il nodo canonico `_now_iso()` è `ast`. Nel grafo del codice RDG, l'arco 699 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `_now_iso()`. Nel grafo del codice RDG, l'arco 699 registra come `target` l'identificatore `traffic_exec_engine_now_iso` per la relazione che coinvolge il nodo canonico `_now_iso()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 699 che coinvolge il nodo canonico `_now_iso()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=342 id=Request-Dataset-Generator::traffic_exec_engine_now_iso -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_now_iso()` è `_now_iso()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_now_iso()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_now_iso()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_now_iso()` è `L59`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_now_iso()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_now_iso()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_now_iso()` è `_now_iso()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_now_iso()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_now_iso()` è `traffic_exec_engine_now_iso`. Nel grafo unificato, il nodo canonico `_now_iso()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_now_iso`.
<!-- GRAPH_EDGE graph=unificata index=698 role=outgoing -->
Nel grafo unificato, il nodo canonico `_now_iso()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.log_request_sent()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_now_iso` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent`. La relazione tecnica esplicita è la seguente: `_now_iso()` chiama `.log_request_sent()`. Nel grafo unificato, il tipo dichiarato dell'arco 698 che coinvolge il nodo canonico `_now_iso()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 698 che coinvolge il nodo canonico `_now_iso()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 698 che coinvolge il nodo canonico `_now_iso()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 698 che coinvolge il nodo canonico `_now_iso()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 698 che coinvolge il nodo canonico `_now_iso()` è `L686`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 698 che coinvolge il nodo canonico `_now_iso()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 698 che coinvolge il nodo canonico `_now_iso()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 698 che coinvolge il nodo canonico `_now_iso()` è `1.0`. Nel grafo unificato, l'arco 698 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_now_iso` per la relazione che coinvolge il nodo canonico `_now_iso()`. Nel grafo unificato, l'arco 698 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `_now_iso()`.
<!-- GRAPH_EDGE graph=unificata index=699 role=outgoing -->
Nel grafo unificato, il nodo canonico `_now_iso()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.log_response_received()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_now_iso` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received`. La relazione tecnica esplicita è la seguente: `_now_iso()` chiama `.log_response_received()`. Nel grafo unificato, il tipo dichiarato dell'arco 699 che coinvolge il nodo canonico `_now_iso()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 699 che coinvolge il nodo canonico `_now_iso()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 699 che coinvolge il nodo canonico `_now_iso()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 699 che coinvolge il nodo canonico `_now_iso()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 699 che coinvolge il nodo canonico `_now_iso()` è `L721`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 699 che coinvolge il nodo canonico `_now_iso()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 699 che coinvolge il nodo canonico `_now_iso()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 699 che coinvolge il nodo canonico `_now_iso()` è `1.0`. Nel grafo unificato, l'arco 699 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_now_iso` per la relazione che coinvolge il nodo canonico `_now_iso()`. Nel grafo unificato, l'arco 699 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `_now_iso()`.
<!-- GRAPH_EDGE graph=unificata index=679 role=incoming -->
Nel grafo unificato, il nodo canonico `_now_iso()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_now_iso`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_now_iso()`. Nel grafo unificato, il tipo dichiarato dell'arco 679 che coinvolge il nodo canonico `_now_iso()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 679 che coinvolge il nodo canonico `_now_iso()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 679 che coinvolge il nodo canonico `_now_iso()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 679 che coinvolge il nodo canonico `_now_iso()` è `L59`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 679 che coinvolge il nodo canonico `_now_iso()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 679 che coinvolge il nodo canonico `_now_iso()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 679 che coinvolge il nodo canonico `_now_iso()` è `1.0`. Nel grafo unificato, l'arco 679 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_now_iso()`. Nel grafo unificato, l'arco 679 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_now_iso` per la relazione che coinvolge il nodo canonico `_now_iso()`.

### _replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]

<!-- GRAPH_NODE graph=codice index=355 id=traffic_exec_engine_replace_newlines_inside_tags -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `_replace_newlines_inside_tags()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `L298`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` possiede l'identificatore esatto `traffic_exec_engine_replace_newlines_inside_tags`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `_replace_newlines_inside_tags()`.
Nel grafo del codice RDG, il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=681 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_replace_newlines_inside_tags`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 681 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 681 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 681 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 681 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `L298`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 681 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 681 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `ast`. Nel grafo del codice RDG, l'arco 681 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]`. Nel grafo del codice RDG, l'arco 681 registra come `target` l'identificatore `traffic_exec_engine_replace_newlines_inside_tags` per la relazione che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 681 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=734 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_linearize_xml()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_linearize_xml` all'ID `traffic_exec_engine_replace_newlines_inside_tags`. La relazione tecnica esplicita è la seguente: `_linearize_xml()` chiama `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 734 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 734 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 734 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 734 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 734 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `L294`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 734 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 734 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `ast`. Nel grafo del codice RDG, l'arco 734 registra come `source` l'identificatore `traffic_exec_engine_linearize_xml` per la relazione che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]`. Nel grafo del codice RDG, l'arco 734 registra come `target` l'identificatore `traffic_exec_engine_replace_newlines_inside_tags` per la relazione che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 734 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=355 id=Request-Dataset-Generator::traffic_exec_engine_replace_newlines_inside_tags -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `_replace_newlines_inside_tags()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `L298`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `_replace_newlines_inside_tags()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `traffic_exec_engine_replace_newlines_inside_tags`. Nel grafo unificato, il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_replace_newlines_inside_tags`.
Nel grafo unificato, il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=681 role=incoming -->
Nel grafo unificato, il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_replace_newlines_inside_tags`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]`. Nel grafo unificato, il tipo dichiarato dell'arco 681 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 681 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 681 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 681 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `L298`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 681 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 681 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 681 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `1.0`. Nel grafo unificato, l'arco 681 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]`. Nel grafo unificato, l'arco 681 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_replace_newlines_inside_tags` per la relazione che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]`.
<!-- GRAPH_EDGE graph=unificata index=734 role=incoming -->
Nel grafo unificato, il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_linearize_xml()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_linearize_xml` all'ID `Request-Dataset-Generator::traffic_exec_engine_replace_newlines_inside_tags`. La relazione tecnica esplicita è la seguente: `_linearize_xml()` chiama `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]`. Nel grafo unificato, il tipo dichiarato dell'arco 734 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 734 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 734 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 734 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 734 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `L294`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 734 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 734 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 734 che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]` è `1.0`. Nel grafo unificato, l'arco 734 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_linearize_xml` per la relazione che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]`. Nel grafo unificato, l'arco 734 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_replace_newlines_inside_tags` per la relazione che coinvolge il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]`.

### _resolve_secret()

<!-- GRAPH_NODE graph=codice index=360 id=traffic_exec_engine_resolve_secret -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_resolve_secret()` è `_resolve_secret()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_resolve_secret()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_resolve_secret()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_resolve_secret()` è `L366`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_resolve_secret()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_resolve_secret()` possiede l'identificatore esatto `traffic_exec_engine_resolve_secret`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_resolve_secret()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_resolve_secret()` è `_resolve_secret()`.
Nel grafo del codice RDG, il nodo canonico `_resolve_secret()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=683 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_resolve_secret()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_resolve_secret`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_resolve_secret()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 683 che coinvolge il nodo canonico `_resolve_secret()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 683 che coinvolge il nodo canonico `_resolve_secret()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 683 che coinvolge il nodo canonico `_resolve_secret()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 683 che coinvolge il nodo canonico `_resolve_secret()` è `L366`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 683 che coinvolge il nodo canonico `_resolve_secret()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 683 che coinvolge il nodo canonico `_resolve_secret()` è `ast`. Nel grafo del codice RDG, l'arco 683 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_resolve_secret()`. Nel grafo del codice RDG, l'arco 683 registra come `target` l'identificatore `traffic_exec_engine_resolve_secret` per la relazione che coinvolge il nodo canonico `_resolve_secret()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 683 che coinvolge il nodo canonico `_resolve_secret()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=743 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_resolve_secret()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_mtlscontextfactory_get` all'ID `traffic_exec_engine_resolve_secret`. La relazione tecnica esplicita è la seguente: `.get()` chiama `_resolve_secret()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 743 che coinvolge il nodo canonico `_resolve_secret()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 743 che coinvolge il nodo canonico `_resolve_secret()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 743 che coinvolge il nodo canonico `_resolve_secret()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 743 che coinvolge il nodo canonico `_resolve_secret()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 743 che coinvolge il nodo canonico `_resolve_secret()` è `L397`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 743 che coinvolge il nodo canonico `_resolve_secret()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 743 che coinvolge il nodo canonico `_resolve_secret()` è `ast`. Nel grafo del codice RDG, l'arco 743 registra come `source` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `_resolve_secret()`. Nel grafo del codice RDG, l'arco 743 registra come `target` l'identificatore `traffic_exec_engine_resolve_secret` per la relazione che coinvolge il nodo canonico `_resolve_secret()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 743 che coinvolge il nodo canonico `_resolve_secret()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=744 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_resolve_secret()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_367` all'ID `traffic_exec_engine_resolve_secret`. La relazione tecnica esplicita è la seguente: `Legge un segreto da env var se configurata, altrimenti dal config locale.` costituisce una motivazione per `_resolve_secret()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 744 che coinvolge il nodo canonico `_resolve_secret()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 744 che coinvolge il nodo canonico `_resolve_secret()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 744 che coinvolge il nodo canonico `_resolve_secret()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 744 che coinvolge il nodo canonico `_resolve_secret()` è `L367`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 744 che coinvolge il nodo canonico `_resolve_secret()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 744 che coinvolge il nodo canonico `_resolve_secret()` è `ast`. Nel grafo del codice RDG, l'arco 744 registra come `source` l'identificatore `traffic_exec_engine_rationale_367` per la relazione che coinvolge il nodo canonico `_resolve_secret()`. Nel grafo del codice RDG, l'arco 744 registra come `target` l'identificatore `traffic_exec_engine_resolve_secret` per la relazione che coinvolge il nodo canonico `_resolve_secret()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 744 che coinvolge il nodo canonico `_resolve_secret()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=360 id=Request-Dataset-Generator::traffic_exec_engine_resolve_secret -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_resolve_secret()` è `_resolve_secret()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_resolve_secret()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_resolve_secret()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_resolve_secret()` è `L366`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_resolve_secret()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_resolve_secret()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_resolve_secret()` è `_resolve_secret()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_resolve_secret()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_resolve_secret()` è `traffic_exec_engine_resolve_secret`. Nel grafo unificato, il nodo canonico `_resolve_secret()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_resolve_secret`.
<!-- GRAPH_EDGE graph=unificata index=743 role=outgoing -->
Nel grafo unificato, il nodo canonico `_resolve_secret()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_resolve_secret` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `_resolve_secret()` chiama `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 743 che coinvolge il nodo canonico `_resolve_secret()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 743 che coinvolge il nodo canonico `_resolve_secret()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 743 che coinvolge il nodo canonico `_resolve_secret()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 743 che coinvolge il nodo canonico `_resolve_secret()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 743 che coinvolge il nodo canonico `_resolve_secret()` è `L397`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 743 che coinvolge il nodo canonico `_resolve_secret()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 743 che coinvolge il nodo canonico `_resolve_secret()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 743 che coinvolge il nodo canonico `_resolve_secret()` è `1.0`. Nel grafo unificato, l'arco 743 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_resolve_secret` per la relazione che coinvolge il nodo canonico `_resolve_secret()`. Nel grafo unificato, l'arco 743 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `_resolve_secret()`.
<!-- GRAPH_EDGE graph=unificata index=744 role=outgoing -->
Nel grafo unificato, il nodo canonico `_resolve_secret()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_resolve_secret` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_367`. La relazione tecnica esplicita è la seguente: `_resolve_secret()` costituisce una motivazione per `Legge un segreto da env var se configurata, altrimenti dal config locale.`. Nel grafo unificato, il tipo dichiarato dell'arco 744 che coinvolge il nodo canonico `_resolve_secret()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 744 che coinvolge il nodo canonico `_resolve_secret()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 744 che coinvolge il nodo canonico `_resolve_secret()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 744 che coinvolge il nodo canonico `_resolve_secret()` è `L367`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 744 che coinvolge il nodo canonico `_resolve_secret()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 744 che coinvolge il nodo canonico `_resolve_secret()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 744 che coinvolge il nodo canonico `_resolve_secret()` è `1.0`. Nel grafo unificato, l'arco 744 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_resolve_secret` per la relazione che coinvolge il nodo canonico `_resolve_secret()`. Nel grafo unificato, l'arco 744 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_367` per la relazione che coinvolge il nodo canonico `_resolve_secret()`.
<!-- GRAPH_EDGE graph=unificata index=683 role=incoming -->
Nel grafo unificato, il nodo canonico `_resolve_secret()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_resolve_secret`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_resolve_secret()`. Nel grafo unificato, il tipo dichiarato dell'arco 683 che coinvolge il nodo canonico `_resolve_secret()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 683 che coinvolge il nodo canonico `_resolve_secret()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 683 che coinvolge il nodo canonico `_resolve_secret()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 683 che coinvolge il nodo canonico `_resolve_secret()` è `L366`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 683 che coinvolge il nodo canonico `_resolve_secret()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 683 che coinvolge il nodo canonico `_resolve_secret()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 683 che coinvolge il nodo canonico `_resolve_secret()` è `1.0`. Nel grafo unificato, l'arco 683 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_resolve_secret()`. Nel grafo unificato, l'arco 683 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_resolve_secret` per la relazione che coinvolge il nodo canonico `_resolve_secret()`.

### _response_size()

<!-- GRAPH_NODE graph=codice index=344 id=traffic_exec_engine_response_size -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_response_size()` è `_response_size()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_response_size()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_response_size()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_response_size()` è `L67`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_response_size()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_response_size()` possiede l'identificatore esatto `traffic_exec_engine_response_size`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_response_size()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_response_size()` è `_response_size()`.
<!-- GRAPH_EDGE graph=codice index=703 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_response_size()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [traffic_exec_engine_py_any]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_response_size` all'ID `traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `_response_size()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 703 che coinvolge il nodo canonico `_response_size()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 703 che coinvolge il nodo canonico `_response_size()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 703 che coinvolge il nodo canonico `_response_size()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 703 che coinvolge il nodo canonico `_response_size()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 703 che coinvolge il nodo canonico `_response_size()` è `L67`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 703 che coinvolge il nodo canonico `_response_size()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 703 che coinvolge il nodo canonico `_response_size()` è `ast`. Nel grafo del codice RDG, l'arco 703 registra come `source` l'identificatore `traffic_exec_engine_response_size` per la relazione che coinvolge il nodo canonico `_response_size()`. Nel grafo del codice RDG, l'arco 703 registra come `target` l'identificatore `traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `_response_size()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 703 che coinvolge il nodo canonico `_response_size()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=684 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_response_size()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_response_size`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_response_size()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 684 che coinvolge il nodo canonico `_response_size()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 684 che coinvolge il nodo canonico `_response_size()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 684 che coinvolge il nodo canonico `_response_size()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 684 che coinvolge il nodo canonico `_response_size()` è `L67`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 684 che coinvolge il nodo canonico `_response_size()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 684 che coinvolge il nodo canonico `_response_size()` è `ast`. Nel grafo del codice RDG, l'arco 684 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_response_size()`. Nel grafo del codice RDG, l'arco 684 registra come `target` l'identificatore `traffic_exec_engine_response_size` per la relazione che coinvolge il nodo canonico `_response_size()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 684 che coinvolge il nodo canonico `_response_size()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=702 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_response_size()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.log_response_received()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_response_received` all'ID `traffic_exec_engine_response_size`. La relazione tecnica esplicita è la seguente: `.log_response_received()` chiama `_response_size()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 702 che coinvolge il nodo canonico `_response_size()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 702 che coinvolge il nodo canonico `_response_size()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 702 che coinvolge il nodo canonico `_response_size()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 702 che coinvolge il nodo canonico `_response_size()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 702 che coinvolge il nodo canonico `_response_size()` è `L730`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 702 che coinvolge il nodo canonico `_response_size()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 702 che coinvolge il nodo canonico `_response_size()` è `ast`. Nel grafo del codice RDG, l'arco 702 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `_response_size()`. Nel grafo del codice RDG, l'arco 702 registra come `target` l'identificatore `traffic_exec_engine_response_size` per la relazione che coinvolge il nodo canonico `_response_size()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 702 che coinvolge il nodo canonico `_response_size()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=344 id=Request-Dataset-Generator::traffic_exec_engine_response_size -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_response_size()` è `_response_size()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_response_size()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_response_size()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_response_size()` è `L67`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_response_size()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_response_size()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_response_size()` è `_response_size()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_response_size()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_response_size()` è `traffic_exec_engine_response_size`. Nel grafo unificato, il nodo canonico `_response_size()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_response_size`.
<!-- GRAPH_EDGE graph=unificata index=702 role=outgoing -->
Nel grafo unificato, il nodo canonico `_response_size()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.log_response_received()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_response_size` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received`. La relazione tecnica esplicita è la seguente: `_response_size()` chiama `.log_response_received()`. Nel grafo unificato, il tipo dichiarato dell'arco 702 che coinvolge il nodo canonico `_response_size()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 702 che coinvolge il nodo canonico `_response_size()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 702 che coinvolge il nodo canonico `_response_size()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 702 che coinvolge il nodo canonico `_response_size()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 702 che coinvolge il nodo canonico `_response_size()` è `L730`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 702 che coinvolge il nodo canonico `_response_size()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 702 che coinvolge il nodo canonico `_response_size()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 702 che coinvolge il nodo canonico `_response_size()` è `1.0`. Nel grafo unificato, l'arco 702 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_response_size` per la relazione che coinvolge il nodo canonico `_response_size()`. Nel grafo unificato, l'arco 702 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `_response_size()`.
<!-- GRAPH_EDGE graph=unificata index=703 role=outgoing -->
Nel grafo unificato, il nodo canonico `_response_size()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [traffic_exec_engine_py_any]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_response_size` all'ID `Request-Dataset-Generator::traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `_response_size()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo unificato, il tipo dichiarato dell'arco 703 che coinvolge il nodo canonico `_response_size()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 703 che coinvolge il nodo canonico `_response_size()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 703 che coinvolge il nodo canonico `_response_size()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 703 che coinvolge il nodo canonico `_response_size()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 703 che coinvolge il nodo canonico `_response_size()` è `L67`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 703 che coinvolge il nodo canonico `_response_size()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 703 che coinvolge il nodo canonico `_response_size()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 703 che coinvolge il nodo canonico `_response_size()` è `1.0`. Nel grafo unificato, l'arco 703 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_response_size` per la relazione che coinvolge il nodo canonico `_response_size()`. Nel grafo unificato, l'arco 703 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `_response_size()`.
<!-- GRAPH_EDGE graph=unificata index=684 role=incoming -->
Nel grafo unificato, il nodo canonico `_response_size()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_response_size`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_response_size()`. Nel grafo unificato, il tipo dichiarato dell'arco 684 che coinvolge il nodo canonico `_response_size()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 684 che coinvolge il nodo canonico `_response_size()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 684 che coinvolge il nodo canonico `_response_size()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 684 che coinvolge il nodo canonico `_response_size()` è `L67`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 684 che coinvolge il nodo canonico `_response_size()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 684 che coinvolge il nodo canonico `_response_size()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 684 che coinvolge il nodo canonico `_response_size()` è `1.0`. Nel grafo unificato, l'arco 684 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_response_size()`. Nel grafo unificato, l'arco 684 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_response_size` per la relazione che coinvolge il nodo canonico `_response_size()`.

### _stable_unit_interval()

<!-- GRAPH_NODE graph=codice index=346 id=traffic_exec_engine_stable_unit_interval -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_stable_unit_interval()` è `_stable_unit_interval()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_stable_unit_interval()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_stable_unit_interval()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_stable_unit_interval()` è `L77`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_stable_unit_interval()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_stable_unit_interval()` possiede l'identificatore esatto `traffic_exec_engine_stable_unit_interval`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_stable_unit_interval()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_stable_unit_interval()` è `_stable_unit_interval()`.
<!-- GRAPH_EDGE graph=codice index=711 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_stable_unit_interval()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [traffic_exec_engine_py_any]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_stable_unit_interval` all'ID `traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `_stable_unit_interval()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 711 che coinvolge il nodo canonico `_stable_unit_interval()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 711 che coinvolge il nodo canonico `_stable_unit_interval()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 711 che coinvolge il nodo canonico `_stable_unit_interval()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 711 che coinvolge il nodo canonico `_stable_unit_interval()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 711 che coinvolge il nodo canonico `_stable_unit_interval()` è `L77`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 711 che coinvolge il nodo canonico `_stable_unit_interval()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 711 che coinvolge il nodo canonico `_stable_unit_interval()` è `ast`. Nel grafo del codice RDG, l'arco 711 registra come `source` l'identificatore `traffic_exec_engine_stable_unit_interval` per la relazione che coinvolge il nodo canonico `_stable_unit_interval()`. Nel grafo del codice RDG, l'arco 711 registra come `target` l'identificatore `traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `_stable_unit_interval()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 711 che coinvolge il nodo canonico `_stable_unit_interval()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=687 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_stable_unit_interval()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_stable_unit_interval`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_stable_unit_interval()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 687 che coinvolge il nodo canonico `_stable_unit_interval()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 687 che coinvolge il nodo canonico `_stable_unit_interval()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 687 che coinvolge il nodo canonico `_stable_unit_interval()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 687 che coinvolge il nodo canonico `_stable_unit_interval()` è `L77`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 687 che coinvolge il nodo canonico `_stable_unit_interval()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 687 che coinvolge il nodo canonico `_stable_unit_interval()` è `ast`. Nel grafo del codice RDG, l'arco 687 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_stable_unit_interval()`. Nel grafo del codice RDG, l'arco 687 registra come `target` l'identificatore `traffic_exec_engine_stable_unit_interval` per la relazione che coinvolge il nodo canonico `_stable_unit_interval()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 687 che coinvolge il nodo canonico `_stable_unit_interval()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=713 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_stable_unit_interval()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.plan()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_faultinjector_plan` all'ID `traffic_exec_engine_stable_unit_interval`. La relazione tecnica esplicita è la seguente: `.plan()` chiama `_stable_unit_interval()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 713 che coinvolge il nodo canonico `_stable_unit_interval()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 713 che coinvolge il nodo canonico `_stable_unit_interval()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 713 che coinvolge il nodo canonico `_stable_unit_interval()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 713 che coinvolge il nodo canonico `_stable_unit_interval()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 713 che coinvolge il nodo canonico `_stable_unit_interval()` è `L498`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 713 che coinvolge il nodo canonico `_stable_unit_interval()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 713 che coinvolge il nodo canonico `_stable_unit_interval()` è `ast`. Nel grafo del codice RDG, l'arco 713 registra come `source` l'identificatore `traffic_exec_engine_faultinjector_plan` per la relazione che coinvolge il nodo canonico `_stable_unit_interval()`. Nel grafo del codice RDG, l'arco 713 registra come `target` l'identificatore `traffic_exec_engine_stable_unit_interval` per la relazione che coinvolge il nodo canonico `_stable_unit_interval()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 713 che coinvolge il nodo canonico `_stable_unit_interval()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=346 id=Request-Dataset-Generator::traffic_exec_engine_stable_unit_interval -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_stable_unit_interval()` è `_stable_unit_interval()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_stable_unit_interval()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_stable_unit_interval()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_stable_unit_interval()` è `L77`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_stable_unit_interval()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_stable_unit_interval()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_stable_unit_interval()` è `_stable_unit_interval()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_stable_unit_interval()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_stable_unit_interval()` è `traffic_exec_engine_stable_unit_interval`. Nel grafo unificato, il nodo canonico `_stable_unit_interval()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_stable_unit_interval`.
<!-- GRAPH_EDGE graph=unificata index=713 role=outgoing -->
Nel grafo unificato, il nodo canonico `_stable_unit_interval()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.plan()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_stable_unit_interval` all'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan`. La relazione tecnica esplicita è la seguente: `_stable_unit_interval()` chiama `.plan()`. Nel grafo unificato, il tipo dichiarato dell'arco 713 che coinvolge il nodo canonico `_stable_unit_interval()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 713 che coinvolge il nodo canonico `_stable_unit_interval()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 713 che coinvolge il nodo canonico `_stable_unit_interval()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 713 che coinvolge il nodo canonico `_stable_unit_interval()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 713 che coinvolge il nodo canonico `_stable_unit_interval()` è `L498`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 713 che coinvolge il nodo canonico `_stable_unit_interval()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 713 che coinvolge il nodo canonico `_stable_unit_interval()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 713 che coinvolge il nodo canonico `_stable_unit_interval()` è `1.0`. Nel grafo unificato, l'arco 713 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_stable_unit_interval` per la relazione che coinvolge il nodo canonico `_stable_unit_interval()`. Nel grafo unificato, l'arco 713 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan` per la relazione che coinvolge il nodo canonico `_stable_unit_interval()`.
<!-- GRAPH_EDGE graph=unificata index=687 role=incoming -->
Nel grafo unificato, il nodo canonico `_stable_unit_interval()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_stable_unit_interval`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_stable_unit_interval()`. Nel grafo unificato, il tipo dichiarato dell'arco 687 che coinvolge il nodo canonico `_stable_unit_interval()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 687 che coinvolge il nodo canonico `_stable_unit_interval()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 687 che coinvolge il nodo canonico `_stable_unit_interval()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 687 che coinvolge il nodo canonico `_stable_unit_interval()` è `L77`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 687 che coinvolge il nodo canonico `_stable_unit_interval()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 687 che coinvolge il nodo canonico `_stable_unit_interval()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 687 che coinvolge il nodo canonico `_stable_unit_interval()` è `1.0`. Nel grafo unificato, l'arco 687 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_stable_unit_interval()`. Nel grafo unificato, l'arco 687 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_stable_unit_interval` per la relazione che coinvolge il nodo canonico `_stable_unit_interval()`.
<!-- GRAPH_EDGE graph=unificata index=711 role=incoming -->
Nel grafo unificato, il nodo canonico `_stable_unit_interval()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Any [traffic_exec_engine_py_any]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_py_any` all'ID `Request-Dataset-Generator::traffic_exec_engine_stable_unit_interval`. La relazione tecnica esplicita è la seguente: `Any [traffic_exec_engine_py_any]` fa riferimento a `_stable_unit_interval()`. Nel grafo unificato, il tipo dichiarato dell'arco 711 che coinvolge il nodo canonico `_stable_unit_interval()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 711 che coinvolge il nodo canonico `_stable_unit_interval()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 711 che coinvolge il nodo canonico `_stable_unit_interval()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 711 che coinvolge il nodo canonico `_stable_unit_interval()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 711 che coinvolge il nodo canonico `_stable_unit_interval()` è `L77`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 711 che coinvolge il nodo canonico `_stable_unit_interval()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 711 che coinvolge il nodo canonico `_stable_unit_interval()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 711 che coinvolge il nodo canonico `_stable_unit_interval()` è `1.0`. Nel grafo unificato, l'arco 711 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `_stable_unit_interval()`. Nel grafo unificato, l'arco 711 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_stable_unit_interval` per la relazione che coinvolge il nodo canonico `_stable_unit_interval()`.

### _xml_raw_string()

<!-- GRAPH_NODE graph=codice index=353 id=traffic_exec_engine_xml_raw_string -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_xml_raw_string()` è `_xml_raw_string()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_xml_raw_string()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_xml_raw_string()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_xml_raw_string()` è `L275`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_xml_raw_string()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_xml_raw_string()` possiede l'identificatore esatto `traffic_exec_engine_xml_raw_string`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_xml_raw_string()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_xml_raw_string()` è `_xml_raw_string()`.
<!-- GRAPH_EDGE graph=codice index=695 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_xml_raw_string()` è la sorgente `source` della relazione `calls` verso il nodo canonico `OutputExtractionError`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_xml_raw_string` all'ID `traffic_exec_engine_outputextractionerror`. La relazione tecnica esplicita è la seguente: `_xml_raw_string()` chiama `OutputExtractionError`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 695 che coinvolge il nodo canonico `_xml_raw_string()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 695 che coinvolge il nodo canonico `_xml_raw_string()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 695 che coinvolge il nodo canonico `_xml_raw_string()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 695 che coinvolge il nodo canonico `_xml_raw_string()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 695 che coinvolge il nodo canonico `_xml_raw_string()` è `L277`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 695 che coinvolge il nodo canonico `_xml_raw_string()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 695 che coinvolge il nodo canonico `_xml_raw_string()` è `ast`. Nel grafo del codice RDG, l'arco 695 registra come `source` l'identificatore `traffic_exec_engine_xml_raw_string` per la relazione che coinvolge il nodo canonico `_xml_raw_string()`. Nel grafo del codice RDG, l'arco 695 registra come `target` l'identificatore `traffic_exec_engine_outputextractionerror` per la relazione che coinvolge il nodo canonico `_xml_raw_string()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 695 che coinvolge il nodo canonico `_xml_raw_string()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=712 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_xml_raw_string()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [traffic_exec_engine_py_any]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_xml_raw_string` all'ID `traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `_xml_raw_string()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 712 che coinvolge il nodo canonico `_xml_raw_string()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 712 che coinvolge il nodo canonico `_xml_raw_string()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 712 che coinvolge il nodo canonico `_xml_raw_string()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 712 che coinvolge il nodo canonico `_xml_raw_string()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 712 che coinvolge il nodo canonico `_xml_raw_string()` è `L275`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 712 che coinvolge il nodo canonico `_xml_raw_string()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 712 che coinvolge il nodo canonico `_xml_raw_string()` è `ast`. Nel grafo del codice RDG, l'arco 712 registra come `source` l'identificatore `traffic_exec_engine_xml_raw_string` per la relazione che coinvolge il nodo canonico `_xml_raw_string()`. Nel grafo del codice RDG, l'arco 712 registra come `target` l'identificatore `traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `_xml_raw_string()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 712 che coinvolge il nodo canonico `_xml_raw_string()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=691 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_xml_raw_string()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_xml_raw_string`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_xml_raw_string()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 691 che coinvolge il nodo canonico `_xml_raw_string()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 691 che coinvolge il nodo canonico `_xml_raw_string()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 691 che coinvolge il nodo canonico `_xml_raw_string()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 691 che coinvolge il nodo canonico `_xml_raw_string()` è `L275`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 691 che coinvolge il nodo canonico `_xml_raw_string()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 691 che coinvolge il nodo canonico `_xml_raw_string()` è `ast`. Nel grafo del codice RDG, l'arco 691 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_xml_raw_string()`. Nel grafo del codice RDG, l'arco 691 registra come `target` l'identificatore `traffic_exec_engine_xml_raw_string` per la relazione che coinvolge il nodo canonico `_xml_raw_string()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 691 che coinvolge il nodo canonico `_xml_raw_string()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=729 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_xml_raw_string()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_extract_xml_output()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output` all'ID `traffic_exec_engine_xml_raw_string`. La relazione tecnica esplicita è la seguente: `_extract_xml_output()` chiama `_xml_raw_string()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 729 che coinvolge il nodo canonico `_xml_raw_string()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 729 che coinvolge il nodo canonico `_xml_raw_string()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 729 che coinvolge il nodo canonico `_xml_raw_string()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 729 che coinvolge il nodo canonico `_xml_raw_string()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 729 che coinvolge il nodo canonico `_xml_raw_string()` è `L209`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 729 che coinvolge il nodo canonico `_xml_raw_string()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 729 che coinvolge il nodo canonico `_xml_raw_string()` è `ast`. Nel grafo del codice RDG, l'arco 729 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `_xml_raw_string()`. Nel grafo del codice RDG, l'arco 729 registra come `target` l'identificatore `traffic_exec_engine_xml_raw_string` per la relazione che coinvolge il nodo canonico `_xml_raw_string()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 729 che coinvolge il nodo canonico `_xml_raw_string()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=733 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_xml_raw_string()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_extract_xml_output_stdlib()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output_stdlib` all'ID `traffic_exec_engine_xml_raw_string`. La relazione tecnica esplicita è la seguente: `_extract_xml_output_stdlib()` chiama `_xml_raw_string()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 733 che coinvolge il nodo canonico `_xml_raw_string()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 733 che coinvolge il nodo canonico `_xml_raw_string()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 733 che coinvolge il nodo canonico `_xml_raw_string()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 733 che coinvolge il nodo canonico `_xml_raw_string()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 733 che coinvolge il nodo canonico `_xml_raw_string()` è `L240`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 733 che coinvolge il nodo canonico `_xml_raw_string()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 733 che coinvolge il nodo canonico `_xml_raw_string()` è `ast`. Nel grafo del codice RDG, l'arco 733 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_xml_raw_string()`. Nel grafo del codice RDG, l'arco 733 registra come `target` l'identificatore `traffic_exec_engine_xml_raw_string` per la relazione che coinvolge il nodo canonico `_xml_raw_string()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 733 che coinvolge il nodo canonico `_xml_raw_string()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=353 id=Request-Dataset-Generator::traffic_exec_engine_xml_raw_string -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_xml_raw_string()` è `_xml_raw_string()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_xml_raw_string()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_xml_raw_string()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_xml_raw_string()` è `L275`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_xml_raw_string()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_xml_raw_string()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_xml_raw_string()` è `_xml_raw_string()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_xml_raw_string()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_xml_raw_string()` è `traffic_exec_engine_xml_raw_string`. Nel grafo unificato, il nodo canonico `_xml_raw_string()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string`.
Nel grafo unificato, il nodo canonico `_xml_raw_string()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=691 role=incoming -->
Nel grafo unificato, il nodo canonico `_xml_raw_string()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_xml_raw_string()`. Nel grafo unificato, il tipo dichiarato dell'arco 691 che coinvolge il nodo canonico `_xml_raw_string()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 691 che coinvolge il nodo canonico `_xml_raw_string()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 691 che coinvolge il nodo canonico `_xml_raw_string()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 691 che coinvolge il nodo canonico `_xml_raw_string()` è `L275`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 691 che coinvolge il nodo canonico `_xml_raw_string()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 691 che coinvolge il nodo canonico `_xml_raw_string()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 691 che coinvolge il nodo canonico `_xml_raw_string()` è `1.0`. Nel grafo unificato, l'arco 691 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `_xml_raw_string()`. Nel grafo unificato, l'arco 691 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string` per la relazione che coinvolge il nodo canonico `_xml_raw_string()`.
<!-- GRAPH_EDGE graph=unificata index=695 role=incoming -->
Nel grafo unificato, il nodo canonico `_xml_raw_string()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `OutputExtractionError`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror` all'ID `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string`. La relazione tecnica esplicita è la seguente: `OutputExtractionError` chiama `_xml_raw_string()`. Nel grafo unificato, il tipo dichiarato dell'arco 695 che coinvolge il nodo canonico `_xml_raw_string()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 695 che coinvolge il nodo canonico `_xml_raw_string()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 695 che coinvolge il nodo canonico `_xml_raw_string()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 695 che coinvolge il nodo canonico `_xml_raw_string()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 695 che coinvolge il nodo canonico `_xml_raw_string()` è `L277`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 695 che coinvolge il nodo canonico `_xml_raw_string()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 695 che coinvolge il nodo canonico `_xml_raw_string()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 695 che coinvolge il nodo canonico `_xml_raw_string()` è `1.0`. Nel grafo unificato, l'arco 695 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror` per la relazione che coinvolge il nodo canonico `_xml_raw_string()`. Nel grafo unificato, l'arco 695 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string` per la relazione che coinvolge il nodo canonico `_xml_raw_string()`.
<!-- GRAPH_EDGE graph=unificata index=712 role=incoming -->
Nel grafo unificato, il nodo canonico `_xml_raw_string()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Any [traffic_exec_engine_py_any]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_py_any` all'ID `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string`. La relazione tecnica esplicita è la seguente: `Any [traffic_exec_engine_py_any]` fa riferimento a `_xml_raw_string()`. Nel grafo unificato, il tipo dichiarato dell'arco 712 che coinvolge il nodo canonico `_xml_raw_string()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 712 che coinvolge il nodo canonico `_xml_raw_string()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 712 che coinvolge il nodo canonico `_xml_raw_string()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 712 che coinvolge il nodo canonico `_xml_raw_string()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 712 che coinvolge il nodo canonico `_xml_raw_string()` è `L275`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 712 che coinvolge il nodo canonico `_xml_raw_string()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 712 che coinvolge il nodo canonico `_xml_raw_string()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 712 che coinvolge il nodo canonico `_xml_raw_string()` è `1.0`. Nel grafo unificato, l'arco 712 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `_xml_raw_string()`. Nel grafo unificato, l'arco 712 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string` per la relazione che coinvolge il nodo canonico `_xml_raw_string()`.
<!-- GRAPH_EDGE graph=unificata index=729 role=incoming -->
Nel grafo unificato, il nodo canonico `_xml_raw_string()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_extract_xml_output()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` all'ID `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string`. La relazione tecnica esplicita è la seguente: `_extract_xml_output()` chiama `_xml_raw_string()`. Nel grafo unificato, il tipo dichiarato dell'arco 729 che coinvolge il nodo canonico `_xml_raw_string()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 729 che coinvolge il nodo canonico `_xml_raw_string()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 729 che coinvolge il nodo canonico `_xml_raw_string()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 729 che coinvolge il nodo canonico `_xml_raw_string()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 729 che coinvolge il nodo canonico `_xml_raw_string()` è `L209`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 729 che coinvolge il nodo canonico `_xml_raw_string()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 729 che coinvolge il nodo canonico `_xml_raw_string()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 729 che coinvolge il nodo canonico `_xml_raw_string()` è `1.0`. Nel grafo unificato, l'arco 729 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `_xml_raw_string()`. Nel grafo unificato, l'arco 729 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string` per la relazione che coinvolge il nodo canonico `_xml_raw_string()`.
<!-- GRAPH_EDGE graph=unificata index=733 role=incoming -->
Nel grafo unificato, il nodo canonico `_xml_raw_string()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_extract_xml_output_stdlib()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` all'ID `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string`. La relazione tecnica esplicita è la seguente: `_extract_xml_output_stdlib()` chiama `_xml_raw_string()`. Nel grafo unificato, il tipo dichiarato dell'arco 733 che coinvolge il nodo canonico `_xml_raw_string()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 733 che coinvolge il nodo canonico `_xml_raw_string()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 733 che coinvolge il nodo canonico `_xml_raw_string()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 733 che coinvolge il nodo canonico `_xml_raw_string()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 733 che coinvolge il nodo canonico `_xml_raw_string()` è `L240`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 733 che coinvolge il nodo canonico `_xml_raw_string()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 733 che coinvolge il nodo canonico `_xml_raw_string()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 733 che coinvolge il nodo canonico `_xml_raw_string()` è `1.0`. Nel grafo unificato, l'arco 733 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `_xml_raw_string()`. Nel grafo unificato, l'arco 733 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string` per la relazione che coinvolge il nodo canonico `_xml_raw_string()`.

### Any [traffic_exec_engine_py_any]

<!-- GRAPH_NODE graph=codice index=345 id=traffic_exec_engine_py_any -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Any [traffic_exec_engine_py_any]` è `Any`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Any [traffic_exec_engine_py_any]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Any [traffic_exec_engine_py_any]` è ``. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Any [traffic_exec_engine_py_any]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Any [traffic_exec_engine_py_any]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Any [traffic_exec_engine_py_any]` possiede l'identificatore esatto `traffic_exec_engine_py_any`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Any [traffic_exec_engine_py_any]` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Any [traffic_exec_engine_py_any]` è `any`.
Nel grafo del codice RDG, il nodo canonico `Any [traffic_exec_engine_py_any]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=703 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Any [traffic_exec_engine_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_response_size()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_response_size` all'ID `traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `_response_size()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 703 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 703 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 703 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 703 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 703 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `L67`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 703 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 703 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `ast`. Nel grafo del codice RDG, l'arco 703 registra come `source` l'identificatore `traffic_exec_engine_response_size` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, l'arco 703 registra come `target` l'identificatore `traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 703 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=704 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Any [traffic_exec_engine_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `execute_flow()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 704 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 704 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 704 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 704 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 704 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 704 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 704 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `ast`. Nel grafo del codice RDG, l'arco 704 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, l'arco 704 registra come `target` l'identificatore `traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 704 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=705 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Any [traffic_exec_engine_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `.log_response_received()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger_log_response_received` all'ID `traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `.log_response_received()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 705 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 705 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 705 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 705 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 705 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `L708`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 705 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 705 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `ast`. Nel grafo del codice RDG, l'arco 705 registra come `source` l'identificatore `traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, l'arco 705 registra come `target` l'identificatore `traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 705 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=706 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Any [traffic_exec_engine_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_extract_json_output()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_json_output` all'ID `traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `_extract_json_output()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 706 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 706 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `return_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 706 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 706 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 706 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `L189`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 706 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 706 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `ast`. Nel grafo del codice RDG, l'arco 706 registra come `source` l'identificatore `traffic_exec_engine_extract_json_output` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, l'arco 706 registra come `target` l'identificatore `traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 706 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=707 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Any [traffic_exec_engine_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `extract_outputs()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_outputs` all'ID `traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `extract_outputs()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 707 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 707 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 707 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 707 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 707 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `L158`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 707 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 707 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `ast`. Nel grafo del codice RDG, l'arco 707 registra come `source` l'identificatore `traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, l'arco 707 registra come `target` l'identificatore `traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 707 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=708 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Any [traffic_exec_engine_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_extract_xml_output()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output` all'ID `traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `_extract_xml_output()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 708 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 708 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `return_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 708 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 708 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 708 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `L203`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 708 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 708 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `ast`. Nel grafo del codice RDG, l'arco 708 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, l'arco 708 registra come `target` l'identificatore `traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 708 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=709 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Any [traffic_exec_engine_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_extract_xml_output_stdlib()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output_stdlib` all'ID `traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `_extract_xml_output_stdlib()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 709 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 709 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `return_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 709 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 709 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 709 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `L239`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 709 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 709 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `ast`. Nel grafo del codice RDG, l'arco 709 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, l'arco 709 registra come `target` l'identificatore `traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 709 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=710 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Any [traffic_exec_engine_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `send_request()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_send_request` all'ID `traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `send_request()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 710 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 710 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 710 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 710 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 710 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `L542`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 710 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 710 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `ast`. Nel grafo del codice RDG, l'arco 710 registra come `source` l'identificatore `traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, l'arco 710 registra come `target` l'identificatore `traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 710 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=711 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Any [traffic_exec_engine_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_stable_unit_interval()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_stable_unit_interval` all'ID `traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `_stable_unit_interval()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 711 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 711 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 711 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 711 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 711 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `L77`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 711 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 711 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `ast`. Nel grafo del codice RDG, l'arco 711 registra come `source` l'identificatore `traffic_exec_engine_stable_unit_interval` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, l'arco 711 registra come `target` l'identificatore `traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 711 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=712 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Any [traffic_exec_engine_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_xml_raw_string()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_xml_raw_string` all'ID `traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `_xml_raw_string()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 712 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 712 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 712 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 712 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 712 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `L275`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 712 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 712 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `ast`. Nel grafo del codice RDG, l'arco 712 registra come `source` l'identificatore `traffic_exec_engine_xml_raw_string` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, l'arco 712 registra come `target` l'identificatore `traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 712 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=345 id=Request-Dataset-Generator::traffic_exec_engine_py_any -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Any [traffic_exec_engine_py_any]` è `Any`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Any [traffic_exec_engine_py_any]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Any [traffic_exec_engine_py_any]` è ``. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Any [traffic_exec_engine_py_any]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Any [traffic_exec_engine_py_any]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Any [traffic_exec_engine_py_any]` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Any [traffic_exec_engine_py_any]` è `any`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Any [traffic_exec_engine_py_any]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine_py_any`. Nel grafo unificato, il nodo canonico `Any [traffic_exec_engine_py_any]` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_py_any`.
<!-- GRAPH_EDGE graph=unificata index=704 role=outgoing -->
Nel grafo unificato, il nodo canonico `Any [traffic_exec_engine_py_any]` è la sorgente `source` della relazione `references` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_py_any` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `Any [traffic_exec_engine_py_any]` fa riferimento a `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 704 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 704 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 704 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 704 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 704 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 704 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 704 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 704 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo unificato, l'arco 704 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo unificato, l'arco 704 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`.
<!-- GRAPH_EDGE graph=unificata index=705 role=outgoing -->
Nel grafo unificato, il nodo canonico `Any [traffic_exec_engine_py_any]` è la sorgente `source` della relazione `references` verso il nodo canonico `.log_response_received()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_py_any` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received`. La relazione tecnica esplicita è la seguente: `Any [traffic_exec_engine_py_any]` fa riferimento a `.log_response_received()`. Nel grafo unificato, il tipo dichiarato dell'arco 705 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 705 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 705 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 705 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 705 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `L708`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 705 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 705 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 705 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo unificato, l'arco 705 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo unificato, l'arco 705 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`.
<!-- GRAPH_EDGE graph=unificata index=706 role=outgoing -->
Nel grafo unificato, il nodo canonico `Any [traffic_exec_engine_py_any]` è la sorgente `source` della relazione `references` verso il nodo canonico `_extract_json_output()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_py_any` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_json_output`. La relazione tecnica esplicita è la seguente: `Any [traffic_exec_engine_py_any]` fa riferimento a `_extract_json_output()`. Nel grafo unificato, il tipo dichiarato dell'arco 706 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 706 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `return_type`. Nel grafo unificato, la confidenza categoriale dell'arco 706 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 706 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 706 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `L189`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 706 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 706 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 706 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo unificato, l'arco 706 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo unificato, l'arco 706 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_json_output` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`.
<!-- GRAPH_EDGE graph=unificata index=707 role=outgoing -->
Nel grafo unificato, il nodo canonico `Any [traffic_exec_engine_py_any]` è la sorgente `source` della relazione `references` verso il nodo canonico `extract_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_py_any` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `Any [traffic_exec_engine_py_any]` fa riferimento a `extract_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 707 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 707 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 707 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 707 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 707 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `L158`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 707 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 707 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 707 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo unificato, l'arco 707 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo unificato, l'arco 707 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`.
<!-- GRAPH_EDGE graph=unificata index=708 role=outgoing -->
Nel grafo unificato, il nodo canonico `Any [traffic_exec_engine_py_any]` è la sorgente `source` della relazione `references` verso il nodo canonico `_extract_xml_output()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_py_any` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output`. La relazione tecnica esplicita è la seguente: `Any [traffic_exec_engine_py_any]` fa riferimento a `_extract_xml_output()`. Nel grafo unificato, il tipo dichiarato dell'arco 708 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 708 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `return_type`. Nel grafo unificato, la confidenza categoriale dell'arco 708 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 708 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 708 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `L203`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 708 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 708 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 708 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo unificato, l'arco 708 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo unificato, l'arco 708 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`.
<!-- GRAPH_EDGE graph=unificata index=709 role=outgoing -->
Nel grafo unificato, il nodo canonico `Any [traffic_exec_engine_py_any]` è la sorgente `source` della relazione `references` verso il nodo canonico `_extract_xml_output_stdlib()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_py_any` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib`. La relazione tecnica esplicita è la seguente: `Any [traffic_exec_engine_py_any]` fa riferimento a `_extract_xml_output_stdlib()`. Nel grafo unificato, il tipo dichiarato dell'arco 709 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 709 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `return_type`. Nel grafo unificato, la confidenza categoriale dell'arco 709 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 709 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 709 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `L239`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 709 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 709 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 709 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo unificato, l'arco 709 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo unificato, l'arco 709 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`.
<!-- GRAPH_EDGE graph=unificata index=710 role=outgoing -->
Nel grafo unificato, il nodo canonico `Any [traffic_exec_engine_py_any]` è la sorgente `source` della relazione `references` verso il nodo canonico `send_request()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_py_any` all'ID `Request-Dataset-Generator::traffic_exec_engine_send_request`. La relazione tecnica esplicita è la seguente: `Any [traffic_exec_engine_py_any]` fa riferimento a `send_request()`. Nel grafo unificato, il tipo dichiarato dell'arco 710 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 710 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 710 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 710 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 710 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `L542`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 710 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 710 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 710 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo unificato, l'arco 710 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo unificato, l'arco 710 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`.
<!-- GRAPH_EDGE graph=unificata index=711 role=outgoing -->
Nel grafo unificato, il nodo canonico `Any [traffic_exec_engine_py_any]` è la sorgente `source` della relazione `references` verso il nodo canonico `_stable_unit_interval()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_py_any` all'ID `Request-Dataset-Generator::traffic_exec_engine_stable_unit_interval`. La relazione tecnica esplicita è la seguente: `Any [traffic_exec_engine_py_any]` fa riferimento a `_stable_unit_interval()`. Nel grafo unificato, il tipo dichiarato dell'arco 711 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 711 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 711 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 711 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 711 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `L77`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 711 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 711 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 711 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo unificato, l'arco 711 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo unificato, l'arco 711 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_stable_unit_interval` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`.
<!-- GRAPH_EDGE graph=unificata index=712 role=outgoing -->
Nel grafo unificato, il nodo canonico `Any [traffic_exec_engine_py_any]` è la sorgente `source` della relazione `references` verso il nodo canonico `_xml_raw_string()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_py_any` all'ID `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string`. La relazione tecnica esplicita è la seguente: `Any [traffic_exec_engine_py_any]` fa riferimento a `_xml_raw_string()`. Nel grafo unificato, il tipo dichiarato dell'arco 712 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 712 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 712 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 712 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 712 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `L275`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 712 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 712 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 712 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo unificato, l'arco 712 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo unificato, l'arco 712 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`.
<!-- GRAPH_EDGE graph=unificata index=703 role=incoming -->
Nel grafo unificato, il nodo canonico `Any [traffic_exec_engine_py_any]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_response_size()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_response_size` all'ID `Request-Dataset-Generator::traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `_response_size()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo unificato, il tipo dichiarato dell'arco 703 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 703 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 703 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 703 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 703 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `L67`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 703 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 703 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 703 che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]` è `1.0`. Nel grafo unificato, l'arco 703 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_response_size` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`. Nel grafo unificato, l'arco 703 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `Any [traffic_exec_engine_py_any]`.

### Applica fault injection deterministica a livello step.

<!-- GRAPH_NODE graph=codice index=405 id=traffic_exec_engine_rationale_472 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Applica fault injection deterministica a livello step.` è `Applica fault injection deterministica a livello step.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Applica fault injection deterministica a livello step.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Applica fault injection deterministica a livello step.` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Applica fault injection deterministica a livello step.` è `L472`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Applica fault injection deterministica a livello step.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Applica fault injection deterministica a livello step.` possiede l'identificatore esatto `traffic_exec_engine_rationale_472`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Applica fault injection deterministica a livello step.` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Applica fault injection deterministica a livello step.` è `applica fault injection deterministica a livello step.`.
<!-- GRAPH_EDGE graph=codice index=772 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Applica fault injection deterministica a livello step.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `FaultInjector`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_472` all'ID `traffic_exec_engine_faultinjector`. La relazione tecnica esplicita è la seguente: `Applica fault injection deterministica a livello step.` costituisce una motivazione per `FaultInjector`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 772 che coinvolge il nodo canonico `Applica fault injection deterministica a livello step.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 772 che coinvolge il nodo canonico `Applica fault injection deterministica a livello step.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 772 che coinvolge il nodo canonico `Applica fault injection deterministica a livello step.` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 772 che coinvolge il nodo canonico `Applica fault injection deterministica a livello step.` è `L472`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 772 che coinvolge il nodo canonico `Applica fault injection deterministica a livello step.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 772 che coinvolge il nodo canonico `Applica fault injection deterministica a livello step.` è `ast`. Nel grafo del codice RDG, l'arco 772 registra come `source` l'identificatore `traffic_exec_engine_rationale_472` per la relazione che coinvolge il nodo canonico `Applica fault injection deterministica a livello step.`. Nel grafo del codice RDG, l'arco 772 registra come `target` l'identificatore `traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `Applica fault injection deterministica a livello step.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 772 che coinvolge il nodo canonico `Applica fault injection deterministica a livello step.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Applica fault injection deterministica a livello step.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=405 id=Request-Dataset-Generator::traffic_exec_engine_rationale_472 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Applica fault injection deterministica a livello step.` è `Applica fault injection deterministica a livello step.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Applica fault injection deterministica a livello step.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Applica fault injection deterministica a livello step.` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Applica fault injection deterministica a livello step.` è `L472`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Applica fault injection deterministica a livello step.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Applica fault injection deterministica a livello step.` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Applica fault injection deterministica a livello step.` è `applica fault injection deterministica a livello step.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Applica fault injection deterministica a livello step.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Applica fault injection deterministica a livello step.` è `traffic_exec_engine_rationale_472`. Nel grafo unificato, il nodo canonico `Applica fault injection deterministica a livello step.` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_rationale_472`.
Nel grafo unificato, il nodo canonico `Applica fault injection deterministica a livello step.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=772 role=incoming -->
Nel grafo unificato, il nodo canonico `Applica fault injection deterministica a livello step.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `FaultInjector`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_472`. La relazione tecnica esplicita è la seguente: `FaultInjector` costituisce una motivazione per `Applica fault injection deterministica a livello step.`. Nel grafo unificato, il tipo dichiarato dell'arco 772 che coinvolge il nodo canonico `Applica fault injection deterministica a livello step.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 772 che coinvolge il nodo canonico `Applica fault injection deterministica a livello step.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 772 che coinvolge il nodo canonico `Applica fault injection deterministica a livello step.` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 772 che coinvolge il nodo canonico `Applica fault injection deterministica a livello step.` è `L472`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 772 che coinvolge il nodo canonico `Applica fault injection deterministica a livello step.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 772 che coinvolge il nodo canonico `Applica fault injection deterministica a livello step.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 772 che coinvolge il nodo canonico `Applica fault injection deterministica a livello step.` è `1.0`. Nel grafo unificato, l'arco 772 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `Applica fault injection deterministica a livello step.`. Nel grafo unificato, l'arco 772 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_472` per la relazione che coinvolge il nodo canonico `Applica fault injection deterministica a livello step.`.

### ArgumentParser [traffic_exec_engine_py_argumentparser]

<!-- GRAPH_NODE graph=codice index=393 id=traffic_exec_engine_py_argumentparser -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `ArgumentParser`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è ``. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` possiede l'identificatore esatto `traffic_exec_engine_py_argumentparser`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `argumentparser`.
Nel grafo del codice RDG, il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=817 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_build_parser() [traffic_exec_engine_build_parser]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_build_parser` all'ID `traffic_exec_engine_py_argumentparser`. La relazione tecnica esplicita è la seguente: `_build_parser() [traffic_exec_engine_build_parser]` fa riferimento a `ArgumentParser [traffic_exec_engine_py_argumentparser]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 817 che coinvolge il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 817 che coinvolge il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `return_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 817 che coinvolge il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 817 che coinvolge il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 817 che coinvolge il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `L1127`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 817 che coinvolge il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 817 che coinvolge il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `ast`. Nel grafo del codice RDG, l'arco 817 registra come `source` l'identificatore `traffic_exec_engine_build_parser` per la relazione che coinvolge il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]`. Nel grafo del codice RDG, l'arco 817 registra come `target` l'identificatore `traffic_exec_engine_py_argumentparser` per la relazione che coinvolge il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 817 che coinvolge il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=393 id=Request-Dataset-Generator::traffic_exec_engine_py_argumentparser -->
Nel grafo unificato, l'etichetta originale del nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `ArgumentParser`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è ``. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `argumentparser`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `traffic_exec_engine_py_argumentparser`. Nel grafo unificato, il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_py_argumentparser`.
Nel grafo unificato, il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=817 role=incoming -->
Nel grafo unificato, il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_build_parser() [traffic_exec_engine_build_parser]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_build_parser` all'ID `Request-Dataset-Generator::traffic_exec_engine_py_argumentparser`. La relazione tecnica esplicita è la seguente: `_build_parser() [traffic_exec_engine_build_parser]` fa riferimento a `ArgumentParser [traffic_exec_engine_py_argumentparser]`. Nel grafo unificato, il tipo dichiarato dell'arco 817 che coinvolge il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 817 che coinvolge il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `return_type`. Nel grafo unificato, la confidenza categoriale dell'arco 817 che coinvolge il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 817 che coinvolge il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 817 che coinvolge il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `L1127`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 817 che coinvolge il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 817 che coinvolge il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 817 che coinvolge il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]` è `1.0`. Nel grafo unificato, l'arco 817 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_build_parser` per la relazione che coinvolge il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]`. Nel grafo unificato, l'arco 817 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_argumentparser` per la relazione che coinvolge il nodo canonico `ArgumentParser [traffic_exec_engine_py_argumentparser]`.

### Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t

<!-- GRAPH_NODE graph=codice index=410 id=traffic_exec_engine_rationale_1098 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `L1098`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` possiede l'identificatore esatto `traffic_exec_engine_rationale_1098`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t`.
<!-- GRAPH_EDGE graph=codice index=815 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `load_dataset_as_timeline()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_1098` all'ID `traffic_exec_engine_load_dataset_as_timeline`. La relazione tecnica esplicita è la seguente: `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` costituisce una motivazione per `load_dataset_as_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 815 che coinvolge il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 815 che coinvolge il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 815 che coinvolge il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 815 che coinvolge il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `L1098`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 815 che coinvolge il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 815 che coinvolge il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `ast`. Nel grafo del codice RDG, l'arco 815 registra come `source` l'identificatore `traffic_exec_engine_rationale_1098` per la relazione che coinvolge il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t`. Nel grafo del codice RDG, l'arco 815 registra come `target` l'identificatore `traffic_exec_engine_load_dataset_as_timeline` per la relazione che coinvolge il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 815 che coinvolge il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=410 id=Request-Dataset-Generator::traffic_exec_engine_rationale_1098 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `L1098`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `traffic_exec_engine_rationale_1098`. Nel grafo unificato, il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_rationale_1098`.
Nel grafo unificato, il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=815 role=incoming -->
Nel grafo unificato, il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `load_dataset_as_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_load_dataset_as_timeline` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_1098`. La relazione tecnica esplicita è la seguente: `load_dataset_as_timeline()` costituisce una motivazione per `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t`. Nel grafo unificato, il tipo dichiarato dell'arco 815 che coinvolge il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 815 che coinvolge il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 815 che coinvolge il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 815 che coinvolge il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `L1098`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 815 che coinvolge il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 815 che coinvolge il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 815 che coinvolge il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` è `1.0`. Nel grafo unificato, l'arco 815 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_load_dataset_as_timeline` per la relazione che coinvolge il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t`. Nel grafo unificato, l'arco 815 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_1098` per la relazione che coinvolge il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t`.

### ConcurrencyTracker

<!-- GRAPH_NODE graph=codice index=379 id=traffic_exec_engine_concurrencytracker -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `ConcurrencyTracker` è `ConcurrencyTracker`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `ConcurrencyTracker` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `ConcurrencyTracker` è `L772`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `ConcurrencyTracker` vale `ast`. Nel grafo del codice RDG, il nodo canonico `ConcurrencyTracker` possiede l'identificatore esatto `traffic_exec_engine_concurrencytracker`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `ConcurrencyTracker` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `ConcurrencyTracker` è `concurrencytracker`.
<!-- GRAPH_EDGE graph=codice index=795 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConcurrencyTracker` è la sorgente `source` della relazione `method` verso il nodo canonico `.flow_finished()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_concurrencytracker` all'ID `traffic_exec_engine_concurrencytracker_flow_finished`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.flow_finished()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 795 che coinvolge il nodo canonico `ConcurrencyTracker` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 795 che coinvolge il nodo canonico `ConcurrencyTracker` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 795 che coinvolge il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 795 che coinvolge il nodo canonico `ConcurrencyTracker` è `L783`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 795 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 795 che coinvolge il nodo canonico `ConcurrencyTracker` è `ast`. Nel grafo del codice RDG, l'arco 795 registra come `source` l'identificatore `traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo del codice RDG, l'arco 795 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_flow_finished` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 795 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=796 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConcurrencyTracker` è la sorgente `source` della relazione `method` verso il nodo canonico `.flow_started()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_concurrencytracker` all'ID `traffic_exec_engine_concurrencytracker_flow_started`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.flow_started()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 796 che coinvolge il nodo canonico `ConcurrencyTracker` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 796 che coinvolge il nodo canonico `ConcurrencyTracker` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 796 che coinvolge il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 796 che coinvolge il nodo canonico `ConcurrencyTracker` è `L779`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 796 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 796 che coinvolge il nodo canonico `ConcurrencyTracker` è `ast`. Nel grafo del codice RDG, l'arco 796 registra come `source` l'identificatore `traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo del codice RDG, l'arco 796 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_flow_started` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 796 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=797 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConcurrencyTracker` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_concurrencytracker` all'ID `traffic_exec_engine_concurrencytracker_init`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.__init__() [traffic_exec_engine_concurrencytracker_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 797 che coinvolge il nodo canonico `ConcurrencyTracker` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 797 che coinvolge il nodo canonico `ConcurrencyTracker` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 797 che coinvolge il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 797 che coinvolge il nodo canonico `ConcurrencyTracker` è `L773`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 797 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 797 che coinvolge il nodo canonico `ConcurrencyTracker` è `ast`. Nel grafo del codice RDG, l'arco 797 registra come `source` l'identificatore `traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo del codice RDG, l'arco 797 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_init` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 797 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=798 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConcurrencyTracker` è la sorgente `source` della relazione `method` verso il nodo canonico `.step_finished()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_concurrencytracker` all'ID `traffic_exec_engine_concurrencytracker_step_finished`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.step_finished()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 798 che coinvolge il nodo canonico `ConcurrencyTracker` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 798 che coinvolge il nodo canonico `ConcurrencyTracker` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 798 che coinvolge il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 798 che coinvolge il nodo canonico `ConcurrencyTracker` è `L791`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 798 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 798 che coinvolge il nodo canonico `ConcurrencyTracker` è `ast`. Nel grafo del codice RDG, l'arco 798 registra come `source` l'identificatore `traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo del codice RDG, l'arco 798 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_step_finished` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 798 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=799 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConcurrencyTracker` è la sorgente `source` della relazione `method` verso il nodo canonico `.step_started()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_concurrencytracker` all'ID `traffic_exec_engine_concurrencytracker_step_started`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.step_started()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 799 che coinvolge il nodo canonico `ConcurrencyTracker` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 799 che coinvolge il nodo canonico `ConcurrencyTracker` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 799 che coinvolge il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 799 che coinvolge il nodo canonico `ConcurrencyTracker` è `L786`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 799 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 799 che coinvolge il nodo canonico `ConcurrencyTracker` è `ast`. Nel grafo del codice RDG, l'arco 799 registra come `source` l'identificatore `traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo del codice RDG, l'arco 799 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_step_started` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 799 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=800 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConcurrencyTracker` è la sorgente `source` della relazione `method` verso il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_concurrencytracker` all'ID `traffic_exec_engine_concurrencytracker_summary`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.summary() [traffic_exec_engine_concurrencytracker_summary]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 800 che coinvolge il nodo canonico `ConcurrencyTracker` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 800 che coinvolge il nodo canonico `ConcurrencyTracker` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 800 che coinvolge il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 800 che coinvolge il nodo canonico `ConcurrencyTracker` è `L794`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 800 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 800 che coinvolge il nodo canonico `ConcurrencyTracker` è `ast`. Nel grafo del codice RDG, l'arco 800 registra come `source` l'identificatore `traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo del codice RDG, l'arco 800 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_summary` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 800 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=662 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ConcurrencyTracker` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_concurrencytracker`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `ConcurrencyTracker`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 662 che coinvolge il nodo canonico `ConcurrencyTracker` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 662 che coinvolge il nodo canonico `ConcurrencyTracker` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 662 che coinvolge il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 662 che coinvolge il nodo canonico `ConcurrencyTracker` è `L772`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 662 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 662 che coinvolge il nodo canonico `ConcurrencyTracker` è `ast`. Nel grafo del codice RDG, l'arco 662 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo del codice RDG, l'arco 662 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 662 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=801 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ConcurrencyTracker` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_concurrencytracker`. La relazione tecnica esplicita è la seguente: `execute_flow()` fa riferimento a `ConcurrencyTracker`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 801 che coinvolge il nodo canonico `ConcurrencyTracker` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 801 che coinvolge il nodo canonico `ConcurrencyTracker` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 801 che coinvolge il nodo canonico `ConcurrencyTracker` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 801 che coinvolge il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 801 che coinvolge il nodo canonico `ConcurrencyTracker` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 801 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 801 che coinvolge il nodo canonico `ConcurrencyTracker` è `ast`. Nel grafo del codice RDG, l'arco 801 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo del codice RDG, l'arco 801 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 801 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=802 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ConcurrencyTracker` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_run` all'ID `traffic_exec_engine_concurrencytracker`. La relazione tecnica esplicita è la seguente: `run()` chiama `ConcurrencyTracker`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 802 che coinvolge il nodo canonico `ConcurrencyTracker` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 802 che coinvolge il nodo canonico `ConcurrencyTracker` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 802 che coinvolge il nodo canonico `ConcurrencyTracker` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 802 che coinvolge il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 802 che coinvolge il nodo canonico `ConcurrencyTracker` è `L995`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 802 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 802 che coinvolge il nodo canonico `ConcurrencyTracker` è `ast`. Nel grafo del codice RDG, l'arco 802 registra come `source` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo del codice RDG, l'arco 802 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 802 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=379 id=Request-Dataset-Generator::traffic_exec_engine_concurrencytracker -->
Nel grafo unificato, l'etichetta originale del nodo canonico `ConcurrencyTracker` è `ConcurrencyTracker`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `ConcurrencyTracker` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `ConcurrencyTracker` è `L772`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `ConcurrencyTracker` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `ConcurrencyTracker` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `ConcurrencyTracker` è `concurrencytracker`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `ConcurrencyTracker` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine_concurrencytracker`. Nel grafo unificato, il nodo canonico `ConcurrencyTracker` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker`.
<!-- GRAPH_EDGE graph=unificata index=795 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConcurrencyTracker` è la sorgente `source` della relazione `method` verso il nodo canonico `.flow_finished()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` all'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_finished`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.flow_finished()`. Nel grafo unificato, il tipo dichiarato dell'arco 795 che coinvolge il nodo canonico `ConcurrencyTracker` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 795 che coinvolge il nodo canonico `ConcurrencyTracker` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 795 che coinvolge il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 795 che coinvolge il nodo canonico `ConcurrencyTracker` è `L783`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 795 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 795 che coinvolge il nodo canonico `ConcurrencyTracker` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 795 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo unificato, l'arco 795 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo unificato, l'arco 795 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_finished` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`.
<!-- GRAPH_EDGE graph=unificata index=796 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConcurrencyTracker` è la sorgente `source` della relazione `method` verso il nodo canonico `.flow_started()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` all'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_started`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.flow_started()`. Nel grafo unificato, il tipo dichiarato dell'arco 796 che coinvolge il nodo canonico `ConcurrencyTracker` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 796 che coinvolge il nodo canonico `ConcurrencyTracker` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 796 che coinvolge il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 796 che coinvolge il nodo canonico `ConcurrencyTracker` è `L779`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 796 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 796 che coinvolge il nodo canonico `ConcurrencyTracker` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 796 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo unificato, l'arco 796 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo unificato, l'arco 796 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_started` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`.
<!-- GRAPH_EDGE graph=unificata index=797 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConcurrencyTracker` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [traffic_exec_engine_concurrencytracker_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` all'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_init`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.__init__() [traffic_exec_engine_concurrencytracker_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 797 che coinvolge il nodo canonico `ConcurrencyTracker` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 797 che coinvolge il nodo canonico `ConcurrencyTracker` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 797 che coinvolge il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 797 che coinvolge il nodo canonico `ConcurrencyTracker` è `L773`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 797 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 797 che coinvolge il nodo canonico `ConcurrencyTracker` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 797 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo unificato, l'arco 797 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo unificato, l'arco 797 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_init` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`.
<!-- GRAPH_EDGE graph=unificata index=798 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConcurrencyTracker` è la sorgente `source` della relazione `method` verso il nodo canonico `.step_finished()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` all'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_finished`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.step_finished()`. Nel grafo unificato, il tipo dichiarato dell'arco 798 che coinvolge il nodo canonico `ConcurrencyTracker` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 798 che coinvolge il nodo canonico `ConcurrencyTracker` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 798 che coinvolge il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 798 che coinvolge il nodo canonico `ConcurrencyTracker` è `L791`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 798 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 798 che coinvolge il nodo canonico `ConcurrencyTracker` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 798 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo unificato, l'arco 798 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo unificato, l'arco 798 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_finished` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`.
<!-- GRAPH_EDGE graph=unificata index=799 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConcurrencyTracker` è la sorgente `source` della relazione `method` verso il nodo canonico `.step_started()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` all'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_started`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.step_started()`. Nel grafo unificato, il tipo dichiarato dell'arco 799 che coinvolge il nodo canonico `ConcurrencyTracker` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 799 che coinvolge il nodo canonico `ConcurrencyTracker` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 799 che coinvolge il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 799 che coinvolge il nodo canonico `ConcurrencyTracker` è `L786`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 799 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 799 che coinvolge il nodo canonico `ConcurrencyTracker` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 799 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo unificato, l'arco 799 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo unificato, l'arco 799 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_started` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`.
<!-- GRAPH_EDGE graph=unificata index=800 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConcurrencyTracker` è la sorgente `source` della relazione `method` verso il nodo canonico `.summary() [traffic_exec_engine_concurrencytracker_summary]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` all'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_summary`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` registra come proprio metodo `.summary() [traffic_exec_engine_concurrencytracker_summary]`. Nel grafo unificato, il tipo dichiarato dell'arco 800 che coinvolge il nodo canonico `ConcurrencyTracker` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 800 che coinvolge il nodo canonico `ConcurrencyTracker` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 800 che coinvolge il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 800 che coinvolge il nodo canonico `ConcurrencyTracker` è `L794`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 800 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 800 che coinvolge il nodo canonico `ConcurrencyTracker` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 800 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo unificato, l'arco 800 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo unificato, l'arco 800 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_summary` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`.
<!-- GRAPH_EDGE graph=unificata index=801 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConcurrencyTracker` è la sorgente `source` della relazione `references` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` fa riferimento a `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 801 che coinvolge il nodo canonico `ConcurrencyTracker` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 801 che coinvolge il nodo canonico `ConcurrencyTracker` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 801 che coinvolge il nodo canonico `ConcurrencyTracker` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 801 che coinvolge il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 801 che coinvolge il nodo canonico `ConcurrencyTracker` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 801 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 801 che coinvolge il nodo canonico `ConcurrencyTracker` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 801 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo unificato, l'arco 801 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo unificato, l'arco 801 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`.
<!-- GRAPH_EDGE graph=unificata index=802 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConcurrencyTracker` è la sorgente `source` della relazione `calls` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 802 che coinvolge il nodo canonico `ConcurrencyTracker` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 802 che coinvolge il nodo canonico `ConcurrencyTracker` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 802 che coinvolge il nodo canonico `ConcurrencyTracker` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 802 che coinvolge il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 802 che coinvolge il nodo canonico `ConcurrencyTracker` è `L995`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 802 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 802 che coinvolge il nodo canonico `ConcurrencyTracker` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 802 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo unificato, l'arco 802 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo unificato, l'arco 802 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`.
<!-- GRAPH_EDGE graph=unificata index=662 role=incoming -->
Nel grafo unificato, il nodo canonico `ConcurrencyTracker` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `ConcurrencyTracker`. Nel grafo unificato, il tipo dichiarato dell'arco 662 che coinvolge il nodo canonico `ConcurrencyTracker` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 662 che coinvolge il nodo canonico `ConcurrencyTracker` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 662 che coinvolge il nodo canonico `ConcurrencyTracker` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 662 che coinvolge il nodo canonico `ConcurrencyTracker` è `L772`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 662 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 662 che coinvolge il nodo canonico `ConcurrencyTracker` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 662 che coinvolge il nodo canonico `ConcurrencyTracker` è `1.0`. Nel grafo unificato, l'arco 662 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`. Nel grafo unificato, l'arco 662 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `ConcurrencyTracker`.

### Controlla gli output marcati required nello step.

<!-- GRAPH_NODE graph=codice index=401 id=traffic_exec_engine_rationale_332 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Controlla gli output marcati required nello step.` è `Controlla gli output marcati required nello step.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Controlla gli output marcati required nello step.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Controlla gli output marcati required nello step.` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Controlla gli output marcati required nello step.` è `L332`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Controlla gli output marcati required nello step.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Controlla gli output marcati required nello step.` possiede l'identificatore esatto `traffic_exec_engine_rationale_332`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Controlla gli output marcati required nello step.` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Controlla gli output marcati required nello step.` è `controlla gli output marcati required nello step.`.
<!-- GRAPH_EDGE graph=codice index=738 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Controlla gli output marcati required nello step.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `validate_required_outputs()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_332` all'ID `traffic_exec_engine_validate_required_outputs`. La relazione tecnica esplicita è la seguente: `Controlla gli output marcati required nello step.` costituisce una motivazione per `validate_required_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 738 che coinvolge il nodo canonico `Controlla gli output marcati required nello step.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 738 che coinvolge il nodo canonico `Controlla gli output marcati required nello step.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 738 che coinvolge il nodo canonico `Controlla gli output marcati required nello step.` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 738 che coinvolge il nodo canonico `Controlla gli output marcati required nello step.` è `L332`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 738 che coinvolge il nodo canonico `Controlla gli output marcati required nello step.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 738 che coinvolge il nodo canonico `Controlla gli output marcati required nello step.` è `ast`. Nel grafo del codice RDG, l'arco 738 registra come `source` l'identificatore `traffic_exec_engine_rationale_332` per la relazione che coinvolge il nodo canonico `Controlla gli output marcati required nello step.`. Nel grafo del codice RDG, l'arco 738 registra come `target` l'identificatore `traffic_exec_engine_validate_required_outputs` per la relazione che coinvolge il nodo canonico `Controlla gli output marcati required nello step.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 738 che coinvolge il nodo canonico `Controlla gli output marcati required nello step.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Controlla gli output marcati required nello step.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=401 id=Request-Dataset-Generator::traffic_exec_engine_rationale_332 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Controlla gli output marcati required nello step.` è `Controlla gli output marcati required nello step.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Controlla gli output marcati required nello step.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Controlla gli output marcati required nello step.` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Controlla gli output marcati required nello step.` è `L332`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Controlla gli output marcati required nello step.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Controlla gli output marcati required nello step.` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Controlla gli output marcati required nello step.` è `controlla gli output marcati required nello step.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Controlla gli output marcati required nello step.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Controlla gli output marcati required nello step.` è `traffic_exec_engine_rationale_332`. Nel grafo unificato, il nodo canonico `Controlla gli output marcati required nello step.` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_rationale_332`.
Nel grafo unificato, il nodo canonico `Controlla gli output marcati required nello step.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=738 role=incoming -->
Nel grafo unificato, il nodo canonico `Controlla gli output marcati required nello step.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `validate_required_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_validate_required_outputs` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_332`. La relazione tecnica esplicita è la seguente: `validate_required_outputs()` costituisce una motivazione per `Controlla gli output marcati required nello step.`. Nel grafo unificato, il tipo dichiarato dell'arco 738 che coinvolge il nodo canonico `Controlla gli output marcati required nello step.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 738 che coinvolge il nodo canonico `Controlla gli output marcati required nello step.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 738 che coinvolge il nodo canonico `Controlla gli output marcati required nello step.` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 738 che coinvolge il nodo canonico `Controlla gli output marcati required nello step.` è `L332`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 738 che coinvolge il nodo canonico `Controlla gli output marcati required nello step.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 738 che coinvolge il nodo canonico `Controlla gli output marcati required nello step.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 738 che coinvolge il nodo canonico `Controlla gli output marcati required nello step.` è `1.0`. Nel grafo unificato, l'arco 738 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_validate_required_outputs` per la relazione che coinvolge il nodo canonico `Controlla gli output marcati required nello step.`. Nel grafo unificato, l'arco 738 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_332` per la relazione che coinvolge il nodo canonico `Controlla gli output marcati required nello step.`.

### Costruisce gli header di correlazione da iniettare in ogni request live.      No

<!-- GRAPH_NODE graph=codice index=402 id=traffic_exec_engine_rationale_351 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `Costruisce gli header di correlazione da iniettare in ogni request live.      No`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `L351`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` possiede l'identificatore esatto `traffic_exec_engine_rationale_351`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `costruisce gli header di correlazione da iniettare in ogni request live.      no`.
<!-- GRAPH_EDGE graph=codice index=741 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `_correlation_headers()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_351` all'ID `traffic_exec_engine_correlation_headers`. La relazione tecnica esplicita è la seguente: `Costruisce gli header di correlazione da iniettare in ogni request live.      No` costituisce una motivazione per `_correlation_headers()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 741 che coinvolge il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 741 che coinvolge il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 741 che coinvolge il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 741 che coinvolge il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `L351`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 741 che coinvolge il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 741 che coinvolge il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `ast`. Nel grafo del codice RDG, l'arco 741 registra come `source` l'identificatore `traffic_exec_engine_rationale_351` per la relazione che coinvolge il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No`. Nel grafo del codice RDG, l'arco 741 registra come `target` l'identificatore `traffic_exec_engine_correlation_headers` per la relazione che coinvolge il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 741 che coinvolge il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=402 id=Request-Dataset-Generator::traffic_exec_engine_rationale_351 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `Costruisce gli header di correlazione da iniettare in ogni request live.      No`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `L351`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `costruisce gli header di correlazione da iniettare in ogni request live.      no`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `traffic_exec_engine_rationale_351`. Nel grafo unificato, il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_rationale_351`.
Nel grafo unificato, il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=741 role=incoming -->
Nel grafo unificato, il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `_correlation_headers()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_correlation_headers` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_351`. La relazione tecnica esplicita è la seguente: `_correlation_headers()` costituisce una motivazione per `Costruisce gli header di correlazione da iniettare in ogni request live.      No`. Nel grafo unificato, il tipo dichiarato dell'arco 741 che coinvolge il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 741 che coinvolge il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 741 che coinvolge il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 741 che coinvolge il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `L351`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 741 che coinvolge il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 741 che coinvolge il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 741 che coinvolge il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No` è `1.0`. Nel grafo unificato, l'arco 741 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_correlation_headers` per la relazione che coinvolge il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No`. Nel grafo unificato, l'arco 741 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_351` per la relazione che coinvolge il nodo canonico `Costruisce gli header di correlazione da iniettare in ogni request live.      No`.

### Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co

<!-- GRAPH_NODE graph=codice index=404 id=traffic_exec_engine_rationale_374 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `L374`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` possiede l'identificatore esatto `traffic_exec_engine_rationale_374`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `crea e conserva sslcontext per profili mtls.      la configurazione resta nel co`.
<!-- GRAPH_EDGE graph=codice index=749 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `MtlsContextFactory`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_374` all'ID `traffic_exec_engine_mtlscontextfactory`. La relazione tecnica esplicita è la seguente: `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` costituisce una motivazione per `MtlsContextFactory`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 749 che coinvolge il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 749 che coinvolge il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 749 che coinvolge il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 749 che coinvolge il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `L374`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 749 che coinvolge il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 749 che coinvolge il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `ast`. Nel grafo del codice RDG, l'arco 749 registra come `source` l'identificatore `traffic_exec_engine_rationale_374` per la relazione che coinvolge il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co`. Nel grafo del codice RDG, l'arco 749 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 749 che coinvolge il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=404 id=Request-Dataset-Generator::traffic_exec_engine_rationale_374 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `L374`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `crea e conserva sslcontext per profili mtls.      la configurazione resta nel co`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `traffic_exec_engine_rationale_374`. Nel grafo unificato, il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_rationale_374`.
Nel grafo unificato, il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=749 role=incoming -->
Nel grafo unificato, il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `MtlsContextFactory`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_374`. La relazione tecnica esplicita è la seguente: `MtlsContextFactory` costituisce una motivazione per `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co`. Nel grafo unificato, il tipo dichiarato dell'arco 749 che coinvolge il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 749 che coinvolge il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 749 che coinvolge il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 749 che coinvolge il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `L374`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 749 che coinvolge il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 749 che coinvolge il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 749 che coinvolge il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` è `1.0`. Nel grafo unificato, l'arco 749 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co`. Nel grafo unificato, l'arco 749 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_374` per la relazione che coinvolge il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co`.

### Element [traffic_exec_engine_py_element]

<!-- GRAPH_NODE graph=codice index=357 id=traffic_exec_engine_py_element -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Element [traffic_exec_engine_py_element]` è `Element`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Element [traffic_exec_engine_py_element]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Element [traffic_exec_engine_py_element]` è ``. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Element [traffic_exec_engine_py_element]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Element [traffic_exec_engine_py_element]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Element [traffic_exec_engine_py_element]` possiede l'identificatore esatto `traffic_exec_engine_py_element`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Element [traffic_exec_engine_py_element]` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Element [traffic_exec_engine_py_element]` è `element`.
Nel grafo del codice RDG, il nodo canonico `Element [traffic_exec_engine_py_element]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=736 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Element [traffic_exec_engine_py_element]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_find_first_by_local_name()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_find_first_by_local_name` all'ID `traffic_exec_engine_py_element`. La relazione tecnica esplicita è la seguente: `_find_first_by_local_name()` fa riferimento a `Element [traffic_exec_engine_py_element]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 736 che coinvolge il nodo canonico `Element [traffic_exec_engine_py_element]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 736 che coinvolge il nodo canonico `Element [traffic_exec_engine_py_element]` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 736 che coinvolge il nodo canonico `Element [traffic_exec_engine_py_element]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 736 che coinvolge il nodo canonico `Element [traffic_exec_engine_py_element]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 736 che coinvolge il nodo canonico `Element [traffic_exec_engine_py_element]` è `L321`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 736 che coinvolge il nodo canonico `Element [traffic_exec_engine_py_element]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 736 che coinvolge il nodo canonico `Element [traffic_exec_engine_py_element]` è `ast`. Nel grafo del codice RDG, l'arco 736 registra come `source` l'identificatore `traffic_exec_engine_find_first_by_local_name` per la relazione che coinvolge il nodo canonico `Element [traffic_exec_engine_py_element]`. Nel grafo del codice RDG, l'arco 736 registra come `target` l'identificatore `traffic_exec_engine_py_element` per la relazione che coinvolge il nodo canonico `Element [traffic_exec_engine_py_element]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 736 che coinvolge il nodo canonico `Element [traffic_exec_engine_py_element]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=357 id=Request-Dataset-Generator::traffic_exec_engine_py_element -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Element [traffic_exec_engine_py_element]` è `Element`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Element [traffic_exec_engine_py_element]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Element [traffic_exec_engine_py_element]` è ``. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Element [traffic_exec_engine_py_element]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Element [traffic_exec_engine_py_element]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Element [traffic_exec_engine_py_element]` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Element [traffic_exec_engine_py_element]` è `element`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Element [traffic_exec_engine_py_element]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Element [traffic_exec_engine_py_element]` è `traffic_exec_engine_py_element`. Nel grafo unificato, il nodo canonico `Element [traffic_exec_engine_py_element]` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_py_element`.
Nel grafo unificato, il nodo canonico `Element [traffic_exec_engine_py_element]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=736 role=incoming -->
Nel grafo unificato, il nodo canonico `Element [traffic_exec_engine_py_element]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_find_first_by_local_name()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_find_first_by_local_name` all'ID `Request-Dataset-Generator::traffic_exec_engine_py_element`. La relazione tecnica esplicita è la seguente: `_find_first_by_local_name()` fa riferimento a `Element [traffic_exec_engine_py_element]`. Nel grafo unificato, il tipo dichiarato dell'arco 736 che coinvolge il nodo canonico `Element [traffic_exec_engine_py_element]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 736 che coinvolge il nodo canonico `Element [traffic_exec_engine_py_element]` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 736 che coinvolge il nodo canonico `Element [traffic_exec_engine_py_element]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 736 che coinvolge il nodo canonico `Element [traffic_exec_engine_py_element]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 736 che coinvolge il nodo canonico `Element [traffic_exec_engine_py_element]` è `L321`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 736 che coinvolge il nodo canonico `Element [traffic_exec_engine_py_element]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 736 che coinvolge il nodo canonico `Element [traffic_exec_engine_py_element]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 736 che coinvolge il nodo canonico `Element [traffic_exec_engine_py_element]` è `1.0`. Nel grafo unificato, l'arco 736 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_find_first_by_local_name` per la relazione che coinvolge il nodo canonico `Element [traffic_exec_engine_py_element]`. Nel grafo unificato, l'arco 736 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_element` per la relazione che coinvolge il nodo canonico `Element [traffic_exec_engine_py_element]`.

### Errore esplicito nella fase di estrazione output da response.

<!-- GRAPH_NODE graph=codice index=395 id=traffic_exec_engine_rationale_48 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `Errore esplicito nella fase di estrazione output da response.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Errore esplicito nella fase di estrazione output da response.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `L48`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Errore esplicito nella fase di estrazione output da response.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Errore esplicito nella fase di estrazione output da response.` possiede l'identificatore esatto `traffic_exec_engine_rationale_48`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Errore esplicito nella fase di estrazione output da response.` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `errore esplicito nella fase di estrazione output da response.`.
<!-- GRAPH_EDGE graph=codice index=694 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `OutputExtractionError`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_48` all'ID `traffic_exec_engine_outputextractionerror`. La relazione tecnica esplicita è la seguente: `Errore esplicito nella fase di estrazione output da response.` costituisce una motivazione per `OutputExtractionError`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 694 che coinvolge il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 694 che coinvolge il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 694 che coinvolge il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 694 che coinvolge il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `L48`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 694 che coinvolge il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 694 che coinvolge il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `ast`. Nel grafo del codice RDG, l'arco 694 registra come `source` l'identificatore `traffic_exec_engine_rationale_48` per la relazione che coinvolge il nodo canonico `Errore esplicito nella fase di estrazione output da response.`. Nel grafo del codice RDG, l'arco 694 registra come `target` l'identificatore `traffic_exec_engine_outputextractionerror` per la relazione che coinvolge il nodo canonico `Errore esplicito nella fase di estrazione output da response.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 694 che coinvolge il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Errore esplicito nella fase di estrazione output da response.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=395 id=Request-Dataset-Generator::traffic_exec_engine_rationale_48 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `Errore esplicito nella fase di estrazione output da response.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Errore esplicito nella fase di estrazione output da response.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `L48`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Errore esplicito nella fase di estrazione output da response.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Errore esplicito nella fase di estrazione output da response.` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `errore esplicito nella fase di estrazione output da response.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Errore esplicito nella fase di estrazione output da response.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `traffic_exec_engine_rationale_48`. Nel grafo unificato, il nodo canonico `Errore esplicito nella fase di estrazione output da response.` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_rationale_48`.
Nel grafo unificato, il nodo canonico `Errore esplicito nella fase di estrazione output da response.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=694 role=incoming -->
Nel grafo unificato, il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `OutputExtractionError`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_48`. La relazione tecnica esplicita è la seguente: `OutputExtractionError` costituisce una motivazione per `Errore esplicito nella fase di estrazione output da response.`. Nel grafo unificato, il tipo dichiarato dell'arco 694 che coinvolge il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 694 che coinvolge il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 694 che coinvolge il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 694 che coinvolge il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `L48`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 694 che coinvolge il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 694 che coinvolge il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 694 che coinvolge il nodo canonico `Errore esplicito nella fase di estrazione output da response.` è `1.0`. Nel grafo unificato, l'arco 694 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror` per la relazione che coinvolge il nodo canonico `Errore esplicito nella fase di estrazione output da response.`. Nel grafo unificato, l'arco 694 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_48` per la relazione che coinvolge il nodo canonico `Errore esplicito nella fase di estrazione output da response.`.

### Errore esplicito quando un placeholder live resta non risolto.

<!-- GRAPH_NODE graph=codice index=396 id=traffic_exec_engine_rationale_52 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `Errore esplicito quando un placeholder live resta non risolto.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `L52`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` possiede l'identificatore esatto `traffic_exec_engine_rationale_52`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `errore esplicito quando un placeholder live resta non risolto.`.
<!-- GRAPH_EDGE graph=codice index=697 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `UnresolvedPlaceholderError`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_52` all'ID `traffic_exec_engine_unresolvedplaceholdererror`. La relazione tecnica esplicita è la seguente: `Errore esplicito quando un placeholder live resta non risolto.` costituisce una motivazione per `UnresolvedPlaceholderError`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 697 che coinvolge il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 697 che coinvolge il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 697 che coinvolge il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 697 che coinvolge il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `L52`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 697 che coinvolge il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 697 che coinvolge il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `ast`. Nel grafo del codice RDG, l'arco 697 registra come `source` l'identificatore `traffic_exec_engine_rationale_52` per la relazione che coinvolge il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.`. Nel grafo del codice RDG, l'arco 697 registra come `target` l'identificatore `traffic_exec_engine_unresolvedplaceholdererror` per la relazione che coinvolge il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 697 che coinvolge il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=396 id=Request-Dataset-Generator::traffic_exec_engine_rationale_52 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `Errore esplicito quando un placeholder live resta non risolto.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `L52`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `errore esplicito quando un placeholder live resta non risolto.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `traffic_exec_engine_rationale_52`. Nel grafo unificato, il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_rationale_52`.
Nel grafo unificato, il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=697 role=incoming -->
Nel grafo unificato, il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `UnresolvedPlaceholderError`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_unresolvedplaceholdererror` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_52`. La relazione tecnica esplicita è la seguente: `UnresolvedPlaceholderError` costituisce una motivazione per `Errore esplicito quando un placeholder live resta non risolto.`. Nel grafo unificato, il tipo dichiarato dell'arco 697 che coinvolge il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 697 che coinvolge il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 697 che coinvolge il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 697 che coinvolge il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `L52`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 697 che coinvolge il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 697 che coinvolge il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 697 che coinvolge il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.` è `1.0`. Nel grafo unificato, l'arco 697 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_unresolvedplaceholdererror` per la relazione che coinvolge il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.`. Nel grafo unificato, l'arco 697 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_52` per la relazione che coinvolge il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.`.

### Esegue un singolo flusso.      Il semaforo wrappa l&#x27;INTERO flusso (non il singol

<!-- GRAPH_NODE graph=codice index=408 id=traffic_exec_engine_rationale_824 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `L824`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` possiede l'identificatore esatto `traffic_exec_engine_rationale_824`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `esegue un singolo flusso.      il semaforo wrappa l'intero flusso (non il singol`.
<!-- GRAPH_EDGE graph=codice index=809 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_824` all'ID `traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` costituisce una motivazione per `execute_flow()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 809 che coinvolge il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 809 che coinvolge il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 809 che coinvolge il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 809 che coinvolge il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `L824`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 809 che coinvolge il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 809 che coinvolge il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `ast`. Nel grafo del codice RDG, l'arco 809 registra come `source` l'identificatore `traffic_exec_engine_rationale_824` per la relazione che coinvolge il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol`. Nel grafo del codice RDG, l'arco 809 registra come `target` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 809 che coinvolge il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=408 id=Request-Dataset-Generator::traffic_exec_engine_rationale_824 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `L824`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `esegue un singolo flusso.      il semaforo wrappa l'intero flusso (non il singol`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `traffic_exec_engine_rationale_824`. Nel grafo unificato, il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_rationale_824`.
Nel grafo unificato, il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=809 role=incoming -->
Nel grafo unificato, il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_824`. La relazione tecnica esplicita è la seguente: `execute_flow()` costituisce una motivazione per `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol`. Nel grafo unificato, il tipo dichiarato dell'arco 809 che coinvolge il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 809 che coinvolge il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 809 che coinvolge il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 809 che coinvolge il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `L824`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 809 che coinvolge il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 809 che coinvolge il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 809 che coinvolge il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` è `1.0`. Nel grafo unificato, l'arco 809 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol`. Nel grafo unificato, l'arco 809 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_824` per la relazione che coinvolge il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol`.

### Estrae valori dalla response secondo le specifiche output_extract dello step.

<!-- GRAPH_NODE graph=codice index=399 id=traffic_exec_engine_rationale_159 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `Estrae valori dalla response secondo le specifiche output_extract dello step.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `L159`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` possiede l'identificatore esatto `traffic_exec_engine_rationale_159`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `estrae valori dalla response secondo le specifiche output_extract dello step.`.
<!-- GRAPH_EDGE graph=codice index=724 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `extract_outputs()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_159` all'ID `traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `Estrae valori dalla response secondo le specifiche output_extract dello step.` costituisce una motivazione per `extract_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 724 che coinvolge il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 724 che coinvolge il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 724 che coinvolge il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 724 che coinvolge il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `L159`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 724 che coinvolge il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 724 che coinvolge il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `ast`. Nel grafo del codice RDG, l'arco 724 registra come `source` l'identificatore `traffic_exec_engine_rationale_159` per la relazione che coinvolge il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.`. Nel grafo del codice RDG, l'arco 724 registra come `target` l'identificatore `traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 724 che coinvolge il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=399 id=Request-Dataset-Generator::traffic_exec_engine_rationale_159 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `Estrae valori dalla response secondo le specifiche output_extract dello step.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `L159`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `estrae valori dalla response secondo le specifiche output_extract dello step.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `traffic_exec_engine_rationale_159`. Nel grafo unificato, il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_rationale_159`.
Nel grafo unificato, il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=724 role=incoming -->
Nel grafo unificato, il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `extract_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_159`. La relazione tecnica esplicita è la seguente: `extract_outputs()` costituisce una motivazione per `Estrae valori dalla response secondo le specifiche output_extract dello step.`. Nel grafo unificato, il tipo dichiarato dell'arco 724 che coinvolge il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 724 che coinvolge il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 724 che coinvolge il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 724 che coinvolge il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `L159`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 724 che coinvolge il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 724 che coinvolge il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 724 che coinvolge il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.` è `1.0`. Nel grafo unificato, l'arco 724 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.`. Nel grafo unificato, l'arco 724 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_159` per la relazione che coinvolge il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.`.

### execute_flow()

<!-- GRAPH_NODE graph=codice index=386 id=traffic_exec_engine_execute_flow -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `execute_flow()` è `execute_flow()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `execute_flow()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `execute_flow()` è `L805`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `execute_flow()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `execute_flow()` possiede l'identificatore esatto `traffic_exec_engine_execute_flow`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `execute_flow()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `execute_flow()` è `execute_flow()`.
<!-- GRAPH_EDGE graph=codice index=704 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [traffic_exec_engine_py_any]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `execute_flow()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 704 che coinvolge il nodo canonico `execute_flow()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 704 che coinvolge il nodo canonico `execute_flow()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 704 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 704 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 704 che coinvolge il nodo canonico `execute_flow()` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 704 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 704 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 704 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 704 registra come `target` l'identificatore `traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 704 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=714 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `calls` verso il nodo canonico `resolve_placeholders()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_resolve_placeholders`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `resolve_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 714 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 714 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 714 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 714 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 714 che coinvolge il nodo canonico `execute_flow()` è `L854`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 714 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 714 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 714 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 714 registra come `target` l'identificatore `traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 714 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=717 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `calls` verso il nodo canonico `unresolved_placeholders()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `unresolved_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 717 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 717 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 717 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 717 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 717 che coinvolge il nodo canonico `execute_flow()` è `L882`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 717 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 717 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 717 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 717 registra come `target` l'identificatore `traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 717 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=720 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `calls` verso il nodo canonico `extract_outputs()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `extract_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 720 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 720 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 720 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 720 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 720 che coinvolge il nodo canonico `execute_flow()` è `L897`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 720 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 720 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 720 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 720 registra come `target` l'identificatore `traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 720 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=737 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `calls` verso il nodo canonico `validate_required_outputs()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_validate_required_outputs`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `validate_required_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 737 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 737 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 737 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 737 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 737 che coinvolge il nodo canonico `execute_flow()` è `L898`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 737 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 737 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 737 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 737 registra come `target` l'identificatore `traffic_exec_engine_validate_required_outputs` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 737 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=745 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `references` verso il nodo canonico `MtlsContextFactory`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_mtlscontextfactory`. La relazione tecnica esplicita è la seguente: `execute_flow()` fa riferimento a `MtlsContextFactory`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 745 che coinvolge il nodo canonico `execute_flow()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 745 che coinvolge il nodo canonico `execute_flow()` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 745 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 745 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 745 che coinvolge il nodo canonico `execute_flow()` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 745 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 745 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 745 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 745 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 745 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=753 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 753 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 753 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 753 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 753 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 753 che coinvolge il nodo canonico `execute_flow()` è `L840`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 753 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 753 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 753 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 753 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 753 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=768 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `references` verso il nodo canonico `FaultInjector`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_faultinjector`. La relazione tecnica esplicita è la seguente: `execute_flow()` fa riferimento a `FaultInjector`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 768 che coinvolge il nodo canonico `execute_flow()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 768 che coinvolge il nodo canonico `execute_flow()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 768 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 768 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 768 che coinvolge il nodo canonico `execute_flow()` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 768 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 768 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 768 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 768 registra come `target` l'identificatore `traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 768 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=774 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.plan()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_faultinjector_plan`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `.plan()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 774 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 774 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 774 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 774 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 774 che coinvolge il nodo canonico `execute_flow()` è `L871`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 774 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 774 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 774 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 774 registra come `target` l'identificatore `traffic_exec_engine_faultinjector_plan` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 774 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=776 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `calls` verso il nodo canonico `send_request()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_send_request`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `send_request()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 776 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 776 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 776 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 776 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 776 che coinvolge il nodo canonico `execute_flow()` è `L892`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 776 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 776 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 776 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 776 registra come `target` l'identificatore `traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 776 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=778 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `references` verso il nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_executionlogger`. La relazione tecnica esplicita è la seguente: `execute_flow()` fa riferimento a `ExecutionLogger`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 778 che coinvolge il nodo canonico `execute_flow()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 778 che coinvolge il nodo canonico `execute_flow()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 778 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 778 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 778 che coinvolge il nodo canonico `execute_flow()` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 778 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 778 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 778 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 778 registra come `target` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 778 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=791 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.log_request_sent()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_executionlogger_log_request_sent`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `.log_request_sent()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 791 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 791 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 791 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 791 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 791 che coinvolge il nodo canonico `execute_flow()` è `L859`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 791 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 791 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 791 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 791 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 791 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=792 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.log_response_received()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_executionlogger_log_response_received`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `.log_response_received()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 792 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 792 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 792 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 792 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 792 che coinvolge il nodo canonico `execute_flow()` è `L931`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 792 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 792 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 792 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 792 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 792 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=793 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.log_flow_completed()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_executionlogger_log_flow_completed`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `.log_flow_completed()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 793 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 793 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 793 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 793 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 793 che coinvolge il nodo canonico `execute_flow()` è `L954`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 793 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 793 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 793 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 793 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_log_flow_completed` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 793 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=801 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `references` verso il nodo canonico `ConcurrencyTracker`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_concurrencytracker`. La relazione tecnica esplicita è la seguente: `execute_flow()` fa riferimento a `ConcurrencyTracker`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 801 che coinvolge il nodo canonico `execute_flow()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 801 che coinvolge il nodo canonico `execute_flow()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 801 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 801 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 801 che coinvolge il nodo canonico `execute_flow()` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 801 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 801 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 801 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 801 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 801 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=803 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.flow_started()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_concurrencytracker_flow_started`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `.flow_started()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 803 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 803 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 803 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 803 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 803 che coinvolge il nodo canonico `execute_flow()` è `L846`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 803 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 803 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 803 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 803 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_flow_started` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 803 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=804 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.flow_finished()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_concurrencytracker_flow_finished`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `.flow_finished()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 804 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 804 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 804 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 804 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 804 che coinvolge il nodo canonico `execute_flow()` è `L960`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 804 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 804 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 804 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 804 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_flow_finished` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 804 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=805 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.step_started()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_concurrencytracker_step_started`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `.step_started()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 805 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 805 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 805 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 805 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 805 che coinvolge il nodo canonico `execute_flow()` è `L857`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 805 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 805 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 805 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 805 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_step_started` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 805 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=806 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.step_finished()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_concurrencytracker_step_finished`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `.step_finished()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 806 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 806 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 806 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 806 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 806 che coinvolge il nodo canonico `execute_flow()` è `L941`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 806 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 806 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 806 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 806 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker_step_finished` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 806 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=808 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `references` verso il nodo canonico `Semaphore`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `semaphore`. La relazione tecnica esplicita è la seguente: `execute_flow()` fa riferimento a `Semaphore`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 808 che coinvolge il nodo canonico `execute_flow()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 808 che coinvolge il nodo canonico `execute_flow()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 808 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 808 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 808 che coinvolge il nodo canonico `execute_flow()` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 808 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 808 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 808 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 808 registra come `target` l'identificatore `semaphore` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 808 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=664 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `execute_flow()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 664 che coinvolge il nodo canonico `execute_flow()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 664 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 664 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 664 che coinvolge il nodo canonico `execute_flow()` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 664 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 664 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 664 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 664 registra come `target` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 664 che coinvolge il nodo canonico `execute_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=809 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_824` all'ID `traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol` costituisce una motivazione per `execute_flow()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 809 che coinvolge il nodo canonico `execute_flow()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 809 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 809 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 809 che coinvolge il nodo canonico `execute_flow()` è `L824`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 809 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 809 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo del codice RDG, l'arco 809 registra come `source` l'identificatore `traffic_exec_engine_rationale_824` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, l'arco 809 registra come `target` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 809 che coinvolge il nodo canonico `execute_flow()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=386 id=Request-Dataset-Generator::traffic_exec_engine_execute_flow -->
Nel grafo unificato, l'etichetta originale del nodo canonico `execute_flow()` è `execute_flow()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `execute_flow()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `execute_flow()` è `L805`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `execute_flow()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `execute_flow()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `execute_flow()` è `execute_flow()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `execute_flow()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `execute_flow()` è `traffic_exec_engine_execute_flow`. Nel grafo unificato, il nodo canonico `execute_flow()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_execute_flow`.
<!-- GRAPH_EDGE graph=unificata index=808 role=outgoing -->
Nel grafo unificato, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `references` verso il nodo canonico `Semaphore`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow` all'ID `Request-Dataset-Generator::semaphore`. La relazione tecnica esplicita è la seguente: `execute_flow()` fa riferimento a `Semaphore`. Nel grafo unificato, il tipo dichiarato dell'arco 808 che coinvolge il nodo canonico `execute_flow()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 808 che coinvolge il nodo canonico `execute_flow()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 808 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 808 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 808 che coinvolge il nodo canonico `execute_flow()` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 808 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 808 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 808 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 808 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 808 registra come `target` l'identificatore `Request-Dataset-Generator::semaphore` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=809 role=outgoing -->
Nel grafo unificato, il nodo canonico `execute_flow()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_824`. La relazione tecnica esplicita è la seguente: `execute_flow()` costituisce una motivazione per `Esegue un singolo flusso.      Il semaforo wrappa l'INTERO flusso (non il singol`. Nel grafo unificato, il tipo dichiarato dell'arco 809 che coinvolge il nodo canonico `execute_flow()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 809 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 809 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 809 che coinvolge il nodo canonico `execute_flow()` è `L824`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 809 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 809 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 809 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 809 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 809 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_824` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=664 role=incoming -->
Nel grafo unificato, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 664 che coinvolge il nodo canonico `execute_flow()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 664 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 664 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 664 che coinvolge il nodo canonico `execute_flow()` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 664 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 664 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 664 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 664 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 664 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=704 role=incoming -->
Nel grafo unificato, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Any [traffic_exec_engine_py_any]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_py_any` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `Any [traffic_exec_engine_py_any]` fa riferimento a `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 704 che coinvolge il nodo canonico `execute_flow()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 704 che coinvolge il nodo canonico `execute_flow()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 704 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 704 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 704 che coinvolge il nodo canonico `execute_flow()` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 704 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 704 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 704 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 704 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 704 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=714 role=incoming -->
Nel grafo unificato, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `resolve_placeholders()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `resolve_placeholders()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 714 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 714 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 714 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 714 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 714 che coinvolge il nodo canonico `execute_flow()` è `L854`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 714 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 714 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 714 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 714 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 714 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=717 role=incoming -->
Nel grafo unificato, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `unresolved_placeholders()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `unresolved_placeholders()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 717 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 717 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 717 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 717 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 717 che coinvolge il nodo canonico `execute_flow()` è `L882`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 717 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 717 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 717 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 717 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 717 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=720 role=incoming -->
Nel grafo unificato, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `extract_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `extract_outputs()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 720 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 720 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 720 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 720 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 720 che coinvolge il nodo canonico `execute_flow()` è `L897`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 720 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 720 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 720 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 720 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 720 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=737 role=incoming -->
Nel grafo unificato, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `validate_required_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_validate_required_outputs` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `validate_required_outputs()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 737 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 737 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 737 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 737 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 737 che coinvolge il nodo canonico `execute_flow()` è `L898`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 737 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 737 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 737 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 737 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_validate_required_outputs` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 737 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=745 role=incoming -->
Nel grafo unificato, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `MtlsContextFactory`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `MtlsContextFactory` fa riferimento a `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 745 che coinvolge il nodo canonico `execute_flow()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 745 che coinvolge il nodo canonico `execute_flow()` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 745 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 745 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 745 che coinvolge il nodo canonico `execute_flow()` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 745 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 745 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 745 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 745 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 745 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=753 role=incoming -->
Nel grafo unificato, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `.get()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 753 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 753 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 753 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 753 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 753 che coinvolge il nodo canonico `execute_flow()` è `L840`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 753 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 753 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 753 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 753 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 753 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=768 role=incoming -->
Nel grafo unificato, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `FaultInjector`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `FaultInjector` fa riferimento a `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 768 che coinvolge il nodo canonico `execute_flow()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 768 che coinvolge il nodo canonico `execute_flow()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 768 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 768 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 768 che coinvolge il nodo canonico `execute_flow()` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 768 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 768 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 768 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 768 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 768 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=774 role=incoming -->
Nel grafo unificato, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.plan()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `.plan()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 774 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 774 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 774 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 774 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 774 che coinvolge il nodo canonico `execute_flow()` è `L871`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 774 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 774 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 774 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 774 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 774 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=776 role=incoming -->
Nel grafo unificato, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `send_request()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_send_request` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `send_request()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 776 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 776 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 776 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 776 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 776 che coinvolge il nodo canonico `execute_flow()` è `L892`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 776 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 776 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 776 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 776 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 776 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=778 role=incoming -->
Nel grafo unificato, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` fa riferimento a `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 778 che coinvolge il nodo canonico `execute_flow()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 778 che coinvolge il nodo canonico `execute_flow()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 778 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 778 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 778 che coinvolge il nodo canonico `execute_flow()` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 778 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 778 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 778 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 778 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 778 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=791 role=incoming -->
Nel grafo unificato, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.log_request_sent()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `.log_request_sent()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 791 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 791 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 791 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 791 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 791 che coinvolge il nodo canonico `execute_flow()` è `L859`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 791 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 791 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 791 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 791 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 791 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=792 role=incoming -->
Nel grafo unificato, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.log_response_received()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `.log_response_received()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 792 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 792 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 792 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 792 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 792 che coinvolge il nodo canonico `execute_flow()` è `L931`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 792 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 792 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 792 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 792 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 792 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=793 role=incoming -->
Nel grafo unificato, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.log_flow_completed()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_flow_completed` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `.log_flow_completed()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 793 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 793 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 793 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 793 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 793 che coinvolge il nodo canonico `execute_flow()` è `L954`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 793 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 793 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 793 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 793 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_flow_completed` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 793 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=801 role=incoming -->
Nel grafo unificato, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `ConcurrencyTracker`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `ConcurrencyTracker` fa riferimento a `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 801 che coinvolge il nodo canonico `execute_flow()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 801 che coinvolge il nodo canonico `execute_flow()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 801 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 801 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 801 che coinvolge il nodo canonico `execute_flow()` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 801 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 801 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 801 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 801 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 801 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=803 role=incoming -->
Nel grafo unificato, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.flow_started()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_started` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `.flow_started()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 803 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 803 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 803 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 803 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 803 che coinvolge il nodo canonico `execute_flow()` è `L846`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 803 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 803 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 803 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 803 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_started` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 803 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=804 role=incoming -->
Nel grafo unificato, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.flow_finished()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_finished` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `.flow_finished()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 804 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 804 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 804 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 804 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 804 che coinvolge il nodo canonico `execute_flow()` è `L960`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 804 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 804 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 804 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 804 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_flow_finished` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 804 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=805 role=incoming -->
Nel grafo unificato, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.step_started()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_started` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `.step_started()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 805 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 805 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 805 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 805 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 805 che coinvolge il nodo canonico `execute_flow()` è `L857`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 805 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 805 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 805 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 805 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_started` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 805 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`.
<!-- GRAPH_EDGE graph=unificata index=806 role=incoming -->
Nel grafo unificato, il nodo canonico `execute_flow()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.step_finished()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_finished` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `.step_finished()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 806 che coinvolge il nodo canonico `execute_flow()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 806 che coinvolge il nodo canonico `execute_flow()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 806 che coinvolge il nodo canonico `execute_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 806 che coinvolge il nodo canonico `execute_flow()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 806 che coinvolge il nodo canonico `execute_flow()` è `L941`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 806 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 806 che coinvolge il nodo canonico `execute_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 806 che coinvolge il nodo canonico `execute_flow()` è `1.0`. Nel grafo unificato, l'arco 806 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker_step_finished` per la relazione che coinvolge il nodo canonico `execute_flow()`. Nel grafo unificato, l'arco 806 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `execute_flow()`.

### ExecutionLogger

<!-- GRAPH_NODE graph=codice index=371 id=traffic_exec_engine_executionlogger -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `ExecutionLogger` è `ExecutionLogger`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `ExecutionLogger` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `ExecutionLogger` è `L620`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `ExecutionLogger` vale `ast`. Nel grafo del codice RDG, il nodo canonico `ExecutionLogger` possiede l'identificatore esatto `traffic_exec_engine_executionlogger`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `ExecutionLogger` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `ExecutionLogger` è `executionlogger`.
<!-- GRAPH_EDGE graph=codice index=779 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ExecutionLogger` è la sorgente `source` della relazione `method` verso il nodo canonico `._emit()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger` all'ID `traffic_exec_engine_executionlogger_emit`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `._emit()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 779 che coinvolge il nodo canonico `ExecutionLogger` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 779 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 779 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 779 che coinvolge il nodo canonico `ExecutionLogger` è `L641`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 779 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 779 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo del codice RDG, l'arco 779 registra come `source` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, l'arco 779 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_emit` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 779 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=780 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ExecutionLogger` è la sorgente `source` della relazione `method` verso il nodo canonico `.flush()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger` all'ID `traffic_exec_engine_executionlogger_flush`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.flush()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 780 che coinvolge il nodo canonico `ExecutionLogger` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 780 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 780 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 780 che coinvolge il nodo canonico `ExecutionLogger` è `L760`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 780 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 780 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo del codice RDG, l'arco 780 registra come `source` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, l'arco 780 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_flush` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 780 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=781 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ExecutionLogger` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger` all'ID `traffic_exec_engine_executionlogger_init`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.__init__() [traffic_exec_engine_executionlogger_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 781 che coinvolge il nodo canonico `ExecutionLogger` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 781 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 781 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 781 che coinvolge il nodo canonico `ExecutionLogger` è `L628`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 781 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 781 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo del codice RDG, l'arco 781 registra come `source` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, l'arco 781 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_init` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 781 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=782 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ExecutionLogger` è la sorgente `source` della relazione `method` verso il nodo canonico `.log_flow_completed()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger` all'ID `traffic_exec_engine_executionlogger_log_flow_completed`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.log_flow_completed()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 782 che coinvolge il nodo canonico `ExecutionLogger` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 782 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 782 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 782 che coinvolge il nodo canonico `ExecutionLogger` è `L738`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 782 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 782 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo del codice RDG, l'arco 782 registra come `source` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, l'arco 782 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_log_flow_completed` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 782 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=783 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ExecutionLogger` è la sorgente `source` della relazione `method` verso il nodo canonico `.log_request_sent()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger` all'ID `traffic_exec_engine_executionlogger_log_request_sent`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.log_request_sent()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 783 che coinvolge il nodo canonico `ExecutionLogger` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 783 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 783 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 783 che coinvolge il nodo canonico `ExecutionLogger` è `L674`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 783 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 783 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo del codice RDG, l'arco 783 registra come `source` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, l'arco 783 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 783 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=784 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ExecutionLogger` è la sorgente `source` della relazione `method` verso il nodo canonico `.log_response_received()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger` all'ID `traffic_exec_engine_executionlogger_log_response_received`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.log_response_received()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 784 che coinvolge il nodo canonico `ExecutionLogger` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 784 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 784 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 784 che coinvolge il nodo canonico `ExecutionLogger` è `L708`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 784 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 784 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo del codice RDG, l'arco 784 registra come `source` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, l'arco 784 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 784 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=785 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ExecutionLogger` è la sorgente `source` della relazione `method` verso il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_executionlogger` all'ID `traffic_exec_engine_executionlogger_summary`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.summary() [traffic_exec_engine_executionlogger_summary]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 785 che coinvolge il nodo canonico `ExecutionLogger` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 785 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 785 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 785 che coinvolge il nodo canonico `ExecutionLogger` è `L768`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 785 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 785 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo del codice RDG, l'arco 785 registra come `source` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, l'arco 785 registra come `target` l'identificatore `traffic_exec_engine_executionlogger_summary` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 785 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=665 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ExecutionLogger` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_executionlogger`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `ExecutionLogger`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 665 che coinvolge il nodo canonico `ExecutionLogger` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 665 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 665 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 665 che coinvolge il nodo canonico `ExecutionLogger` è `L620`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 665 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 665 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo del codice RDG, l'arco 665 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, l'arco 665 registra come `target` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 665 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=778 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ExecutionLogger` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_executionlogger`. La relazione tecnica esplicita è la seguente: `execute_flow()` fa riferimento a `ExecutionLogger`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 778 che coinvolge il nodo canonico `ExecutionLogger` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 778 che coinvolge il nodo canonico `ExecutionLogger` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 778 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 778 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 778 che coinvolge il nodo canonico `ExecutionLogger` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 778 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 778 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo del codice RDG, l'arco 778 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, l'arco 778 registra come `target` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 778 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=786 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ExecutionLogger` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_621` all'ID `traffic_exec_engine_executionlogger`. La relazione tecnica esplicita è la seguente: `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` costituisce una motivazione per `ExecutionLogger`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 786 che coinvolge il nodo canonico `ExecutionLogger` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 786 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 786 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 786 che coinvolge il nodo canonico `ExecutionLogger` è `L621`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 786 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 786 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo del codice RDG, l'arco 786 registra come `source` l'identificatore `traffic_exec_engine_rationale_621` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, l'arco 786 registra come `target` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 786 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=787 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ExecutionLogger` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_run` all'ID `traffic_exec_engine_executionlogger`. La relazione tecnica esplicita è la seguente: `run()` chiama `ExecutionLogger`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 787 che coinvolge il nodo canonico `ExecutionLogger` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 787 che coinvolge il nodo canonico `ExecutionLogger` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 787 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 787 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 787 che coinvolge il nodo canonico `ExecutionLogger` è `L993`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 787 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 787 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo del codice RDG, l'arco 787 registra come `source` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, l'arco 787 registra come `target` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 787 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=371 id=Request-Dataset-Generator::traffic_exec_engine_executionlogger -->
Nel grafo unificato, l'etichetta originale del nodo canonico `ExecutionLogger` è `ExecutionLogger`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `ExecutionLogger` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `ExecutionLogger` è `L620`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `ExecutionLogger` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `ExecutionLogger` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `ExecutionLogger` è `executionlogger`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `ExecutionLogger` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `ExecutionLogger` è `traffic_exec_engine_executionlogger`. Nel grafo unificato, il nodo canonico `ExecutionLogger` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_executionlogger`.
<!-- GRAPH_EDGE graph=unificata index=778 role=outgoing -->
Nel grafo unificato, il nodo canonico `ExecutionLogger` è la sorgente `source` della relazione `references` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` fa riferimento a `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 778 che coinvolge il nodo canonico `ExecutionLogger` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 778 che coinvolge il nodo canonico `ExecutionLogger` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 778 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 778 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 778 che coinvolge il nodo canonico `ExecutionLogger` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 778 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 778 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 778 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, l'arco 778 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo unificato, l'arco 778 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `ExecutionLogger`.
<!-- GRAPH_EDGE graph=unificata index=779 role=outgoing -->
Nel grafo unificato, il nodo canonico `ExecutionLogger` è la sorgente `source` della relazione `method` verso il nodo canonico `._emit()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `._emit()`. Nel grafo unificato, il tipo dichiarato dell'arco 779 che coinvolge il nodo canonico `ExecutionLogger` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 779 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 779 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 779 che coinvolge il nodo canonico `ExecutionLogger` è `L641`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 779 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 779 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 779 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, l'arco 779 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo unificato, l'arco 779 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_emit` per la relazione che coinvolge il nodo canonico `ExecutionLogger`.
<!-- GRAPH_EDGE graph=unificata index=780 role=outgoing -->
Nel grafo unificato, il nodo canonico `ExecutionLogger` è la sorgente `source` della relazione `method` verso il nodo canonico `.flush()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_flush`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.flush()`. Nel grafo unificato, il tipo dichiarato dell'arco 780 che coinvolge il nodo canonico `ExecutionLogger` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 780 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 780 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 780 che coinvolge il nodo canonico `ExecutionLogger` è `L760`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 780 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 780 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 780 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, l'arco 780 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo unificato, l'arco 780 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_flush` per la relazione che coinvolge il nodo canonico `ExecutionLogger`.
<!-- GRAPH_EDGE graph=unificata index=781 role=outgoing -->
Nel grafo unificato, il nodo canonico `ExecutionLogger` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [traffic_exec_engine_executionlogger_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_init`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.__init__() [traffic_exec_engine_executionlogger_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 781 che coinvolge il nodo canonico `ExecutionLogger` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 781 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 781 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 781 che coinvolge il nodo canonico `ExecutionLogger` è `L628`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 781 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 781 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 781 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, l'arco 781 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo unificato, l'arco 781 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_init` per la relazione che coinvolge il nodo canonico `ExecutionLogger`.
<!-- GRAPH_EDGE graph=unificata index=782 role=outgoing -->
Nel grafo unificato, il nodo canonico `ExecutionLogger` è la sorgente `source` della relazione `method` verso il nodo canonico `.log_flow_completed()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_flow_completed`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.log_flow_completed()`. Nel grafo unificato, il tipo dichiarato dell'arco 782 che coinvolge il nodo canonico `ExecutionLogger` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 782 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 782 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 782 che coinvolge il nodo canonico `ExecutionLogger` è `L738`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 782 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 782 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 782 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, l'arco 782 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo unificato, l'arco 782 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_flow_completed` per la relazione che coinvolge il nodo canonico `ExecutionLogger`.
<!-- GRAPH_EDGE graph=unificata index=783 role=outgoing -->
Nel grafo unificato, il nodo canonico `ExecutionLogger` è la sorgente `source` della relazione `method` verso il nodo canonico `.log_request_sent()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.log_request_sent()`. Nel grafo unificato, il tipo dichiarato dell'arco 783 che coinvolge il nodo canonico `ExecutionLogger` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 783 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 783 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 783 che coinvolge il nodo canonico `ExecutionLogger` è `L674`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 783 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 783 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 783 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, l'arco 783 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo unificato, l'arco 783 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_request_sent` per la relazione che coinvolge il nodo canonico `ExecutionLogger`.
<!-- GRAPH_EDGE graph=unificata index=784 role=outgoing -->
Nel grafo unificato, il nodo canonico `ExecutionLogger` è la sorgente `source` della relazione `method` verso il nodo canonico `.log_response_received()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.log_response_received()`. Nel grafo unificato, il tipo dichiarato dell'arco 784 che coinvolge il nodo canonico `ExecutionLogger` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 784 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 784 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 784 che coinvolge il nodo canonico `ExecutionLogger` è `L708`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 784 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 784 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 784 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, l'arco 784 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo unificato, l'arco 784 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_log_response_received` per la relazione che coinvolge il nodo canonico `ExecutionLogger`.
<!-- GRAPH_EDGE graph=unificata index=785 role=outgoing -->
Nel grafo unificato, il nodo canonico `ExecutionLogger` è la sorgente `source` della relazione `method` verso il nodo canonico `.summary() [traffic_exec_engine_executionlogger_summary]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger_summary`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` registra come proprio metodo `.summary() [traffic_exec_engine_executionlogger_summary]`. Nel grafo unificato, il tipo dichiarato dell'arco 785 che coinvolge il nodo canonico `ExecutionLogger` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 785 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 785 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 785 che coinvolge il nodo canonico `ExecutionLogger` è `L768`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 785 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 785 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 785 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, l'arco 785 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo unificato, l'arco 785 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger_summary` per la relazione che coinvolge il nodo canonico `ExecutionLogger`.
<!-- GRAPH_EDGE graph=unificata index=786 role=outgoing -->
Nel grafo unificato, il nodo canonico `ExecutionLogger` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_621`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` costituisce una motivazione per `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di`. Nel grafo unificato, il tipo dichiarato dell'arco 786 che coinvolge il nodo canonico `ExecutionLogger` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 786 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 786 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 786 che coinvolge il nodo canonico `ExecutionLogger` è `L621`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 786 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 786 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 786 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, l'arco 786 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo unificato, l'arco 786 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_621` per la relazione che coinvolge il nodo canonico `ExecutionLogger`.
<!-- GRAPH_EDGE graph=unificata index=787 role=outgoing -->
Nel grafo unificato, il nodo canonico `ExecutionLogger` è la sorgente `source` della relazione `calls` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 787 che coinvolge il nodo canonico `ExecutionLogger` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 787 che coinvolge il nodo canonico `ExecutionLogger` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 787 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 787 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 787 che coinvolge il nodo canonico `ExecutionLogger` è `L993`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 787 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 787 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 787 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, l'arco 787 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo unificato, l'arco 787 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `ExecutionLogger`.
<!-- GRAPH_EDGE graph=unificata index=665 role=incoming -->
Nel grafo unificato, il nodo canonico `ExecutionLogger` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `ExecutionLogger`. Nel grafo unificato, il tipo dichiarato dell'arco 665 che coinvolge il nodo canonico `ExecutionLogger` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 665 che coinvolge il nodo canonico `ExecutionLogger` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 665 che coinvolge il nodo canonico `ExecutionLogger` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 665 che coinvolge il nodo canonico `ExecutionLogger` è `L620`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 665 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 665 che coinvolge il nodo canonico `ExecutionLogger` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 665 che coinvolge il nodo canonico `ExecutionLogger` è `1.0`. Nel grafo unificato, l'arco 665 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `ExecutionLogger`. Nel grafo unificato, l'arco 665 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `ExecutionLogger`.

### extract_outputs()

<!-- GRAPH_NODE graph=codice index=349 id=traffic_exec_engine_extract_outputs -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `extract_outputs()` è `extract_outputs()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `extract_outputs()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `extract_outputs()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `extract_outputs()` è `L158`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `extract_outputs()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `extract_outputs()` possiede l'identificatore esatto `traffic_exec_engine_extract_outputs`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `extract_outputs()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `extract_outputs()` è `extract_outputs()`.
<!-- GRAPH_EDGE graph=codice index=707 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `extract_outputs()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [traffic_exec_engine_py_any]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_outputs` all'ID `traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `extract_outputs()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 707 che coinvolge il nodo canonico `extract_outputs()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 707 che coinvolge il nodo canonico `extract_outputs()` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 707 che coinvolge il nodo canonico `extract_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 707 che coinvolge il nodo canonico `extract_outputs()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 707 che coinvolge il nodo canonico `extract_outputs()` è `L158`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 707 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 707 che coinvolge il nodo canonico `extract_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 707 registra come `source` l'identificatore `traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo del codice RDG, l'arco 707 registra come `target` l'identificatore `traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 707 che coinvolge il nodo canonico `extract_outputs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=721 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `extract_outputs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_extract_json_output()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_outputs` all'ID `traffic_exec_engine_extract_json_output`. La relazione tecnica esplicita è la seguente: `extract_outputs()` chiama `_extract_json_output()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 721 che coinvolge il nodo canonico `extract_outputs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 721 che coinvolge il nodo canonico `extract_outputs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 721 che coinvolge il nodo canonico `extract_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 721 che coinvolge il nodo canonico `extract_outputs()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 721 che coinvolge il nodo canonico `extract_outputs()` è `L182`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 721 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 721 che coinvolge il nodo canonico `extract_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 721 registra come `source` l'identificatore `traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo del codice RDG, l'arco 721 registra come `target` l'identificatore `traffic_exec_engine_extract_json_output` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 721 che coinvolge il nodo canonico `extract_outputs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=722 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `extract_outputs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_extract_xml_output()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_outputs` all'ID `traffic_exec_engine_extract_xml_output`. La relazione tecnica esplicita è la seguente: `extract_outputs()` chiama `_extract_xml_output()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 722 che coinvolge il nodo canonico `extract_outputs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 722 che coinvolge il nodo canonico `extract_outputs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 722 che coinvolge il nodo canonico `extract_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 722 che coinvolge il nodo canonico `extract_outputs()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 722 che coinvolge il nodo canonico `extract_outputs()` è `L180`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 722 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 722 che coinvolge il nodo canonico `extract_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 722 registra come `source` l'identificatore `traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo del codice RDG, l'arco 722 registra come `target` l'identificatore `traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 722 che coinvolge il nodo canonico `extract_outputs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=723 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `extract_outputs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_outputs` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `extract_outputs()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 723 che coinvolge il nodo canonico `extract_outputs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 723 che coinvolge il nodo canonico `extract_outputs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 723 che coinvolge il nodo canonico `extract_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 723 che coinvolge il nodo canonico `extract_outputs()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 723 che coinvolge il nodo canonico `extract_outputs()` è `L177`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 723 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 723 che coinvolge il nodo canonico `extract_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 723 registra come `source` l'identificatore `traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo del codice RDG, l'arco 723 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 723 che coinvolge il nodo canonico `extract_outputs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=589 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `extract_outputs()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `extract_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 589 che coinvolge il nodo canonico `extract_outputs()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 589 che coinvolge il nodo canonico `extract_outputs()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 589 che coinvolge il nodo canonico `extract_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 589 che coinvolge il nodo canonico `extract_outputs()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 589 che coinvolge il nodo canonico `extract_outputs()` è `L25`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 589 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 589 che coinvolge il nodo canonico `extract_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 589 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo del codice RDG, l'arco 589 registra come `target` l'identificatore `traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 589 che coinvolge il nodo canonico `extract_outputs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=595 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `extract_outputs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_extracts_saml_assertion_as_base64()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64` all'ID `traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `.test_extracts_saml_assertion_as_base64()` chiama `extract_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 595 che coinvolge il nodo canonico `extract_outputs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 595 che coinvolge il nodo canonico `extract_outputs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 595 che coinvolge il nodo canonico `extract_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 595 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 595 che coinvolge il nodo canonico `extract_outputs()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 595 che coinvolge il nodo canonico `extract_outputs()` è `L184`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 595 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 595 che coinvolge il nodo canonico `extract_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 595 registra come `source` l'identificatore `tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo del codice RDG, l'arco 595 registra come `target` l'identificatore `traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `extract_outputs()`.
<!-- GRAPH_EDGE graph=codice index=596 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `extract_outputs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64` all'ID `traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` chiama `extract_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 596 che coinvolge il nodo canonico `extract_outputs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 596 che coinvolge il nodo canonico `extract_outputs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 596 che coinvolge il nodo canonico `extract_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 596 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 596 che coinvolge il nodo canonico `extract_outputs()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 596 che coinvolge il nodo canonico `extract_outputs()` è `L226`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 596 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 596 che coinvolge il nodo canonico `extract_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 596 registra come `source` l'identificatore `tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo del codice RDG, l'arco 596 registra come `target` l'identificatore `traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `extract_outputs()`.
<!-- GRAPH_EDGE graph=codice index=667 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `extract_outputs()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `extract_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 667 che coinvolge il nodo canonico `extract_outputs()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 667 che coinvolge il nodo canonico `extract_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 667 che coinvolge il nodo canonico `extract_outputs()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 667 che coinvolge il nodo canonico `extract_outputs()` è `L158`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 667 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 667 che coinvolge il nodo canonico `extract_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 667 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo del codice RDG, l'arco 667 registra come `target` l'identificatore `traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 667 che coinvolge il nodo canonico `extract_outputs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=720 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `extract_outputs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `extract_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 720 che coinvolge il nodo canonico `extract_outputs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 720 che coinvolge il nodo canonico `extract_outputs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 720 che coinvolge il nodo canonico `extract_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 720 che coinvolge il nodo canonico `extract_outputs()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 720 che coinvolge il nodo canonico `extract_outputs()` è `L897`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 720 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 720 che coinvolge il nodo canonico `extract_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 720 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo del codice RDG, l'arco 720 registra come `target` l'identificatore `traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 720 che coinvolge il nodo canonico `extract_outputs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=724 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `extract_outputs()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_159` all'ID `traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `Estrae valori dalla response secondo le specifiche output_extract dello step.` costituisce una motivazione per `extract_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 724 che coinvolge il nodo canonico `extract_outputs()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 724 che coinvolge il nodo canonico `extract_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 724 che coinvolge il nodo canonico `extract_outputs()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 724 che coinvolge il nodo canonico `extract_outputs()` è `L159`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 724 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 724 che coinvolge il nodo canonico `extract_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 724 registra come `source` l'identificatore `traffic_exec_engine_rationale_159` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo del codice RDG, l'arco 724 registra come `target` l'identificatore `traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 724 che coinvolge il nodo canonico `extract_outputs()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=349 id=Request-Dataset-Generator::traffic_exec_engine_extract_outputs -->
Nel grafo unificato, l'etichetta originale del nodo canonico `extract_outputs()` è `extract_outputs()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `extract_outputs()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `extract_outputs()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `extract_outputs()` è `L158`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `extract_outputs()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `extract_outputs()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `extract_outputs()` è `extract_outputs()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `extract_outputs()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `extract_outputs()` è `traffic_exec_engine_extract_outputs`. Nel grafo unificato, il nodo canonico `extract_outputs()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_extract_outputs`.
<!-- GRAPH_EDGE graph=unificata index=720 role=outgoing -->
Nel grafo unificato, il nodo canonico `extract_outputs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `extract_outputs()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 720 che coinvolge il nodo canonico `extract_outputs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 720 che coinvolge il nodo canonico `extract_outputs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 720 che coinvolge il nodo canonico `extract_outputs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 720 che coinvolge il nodo canonico `extract_outputs()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 720 che coinvolge il nodo canonico `extract_outputs()` è `L897`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 720 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 720 che coinvolge il nodo canonico `extract_outputs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 720 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo unificato, l'arco 720 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo unificato, l'arco 720 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `extract_outputs()`.
<!-- GRAPH_EDGE graph=unificata index=721 role=outgoing -->
Nel grafo unificato, il nodo canonico `extract_outputs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_extract_json_output()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_json_output`. La relazione tecnica esplicita è la seguente: `extract_outputs()` chiama `_extract_json_output()`. Nel grafo unificato, il tipo dichiarato dell'arco 721 che coinvolge il nodo canonico `extract_outputs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 721 che coinvolge il nodo canonico `extract_outputs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 721 che coinvolge il nodo canonico `extract_outputs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 721 che coinvolge il nodo canonico `extract_outputs()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 721 che coinvolge il nodo canonico `extract_outputs()` è `L182`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 721 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 721 che coinvolge il nodo canonico `extract_outputs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 721 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo unificato, l'arco 721 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo unificato, l'arco 721 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_json_output` per la relazione che coinvolge il nodo canonico `extract_outputs()`.
<!-- GRAPH_EDGE graph=unificata index=722 role=outgoing -->
Nel grafo unificato, il nodo canonico `extract_outputs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_extract_xml_output()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output`. La relazione tecnica esplicita è la seguente: `extract_outputs()` chiama `_extract_xml_output()`. Nel grafo unificato, il tipo dichiarato dell'arco 722 che coinvolge il nodo canonico `extract_outputs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 722 che coinvolge il nodo canonico `extract_outputs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 722 che coinvolge il nodo canonico `extract_outputs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 722 che coinvolge il nodo canonico `extract_outputs()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 722 che coinvolge il nodo canonico `extract_outputs()` è `L180`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 722 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 722 che coinvolge il nodo canonico `extract_outputs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 722 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo unificato, l'arco 722 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo unificato, l'arco 722 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `extract_outputs()`.
<!-- GRAPH_EDGE graph=unificata index=723 role=outgoing -->
Nel grafo unificato, il nodo canonico `extract_outputs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `extract_outputs()` chiama `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 723 che coinvolge il nodo canonico `extract_outputs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 723 che coinvolge il nodo canonico `extract_outputs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 723 che coinvolge il nodo canonico `extract_outputs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 723 che coinvolge il nodo canonico `extract_outputs()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 723 che coinvolge il nodo canonico `extract_outputs()` è `L177`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 723 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 723 che coinvolge il nodo canonico `extract_outputs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 723 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo unificato, l'arco 723 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo unificato, l'arco 723 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `extract_outputs()`.
<!-- GRAPH_EDGE graph=unificata index=724 role=outgoing -->
Nel grafo unificato, il nodo canonico `extract_outputs()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Estrae valori dalla response secondo le specifiche output_extract dello step.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_159`. La relazione tecnica esplicita è la seguente: `extract_outputs()` costituisce una motivazione per `Estrae valori dalla response secondo le specifiche output_extract dello step.`. Nel grafo unificato, il tipo dichiarato dell'arco 724 che coinvolge il nodo canonico `extract_outputs()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 724 che coinvolge il nodo canonico `extract_outputs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 724 che coinvolge il nodo canonico `extract_outputs()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 724 che coinvolge il nodo canonico `extract_outputs()` è `L159`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 724 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 724 che coinvolge il nodo canonico `extract_outputs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 724 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo unificato, l'arco 724 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo unificato, l'arco 724 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_159` per la relazione che coinvolge il nodo canonico `extract_outputs()`.
<!-- GRAPH_EDGE graph=unificata index=589 role=incoming -->
Nel grafo unificato, il nodo canonico `extract_outputs()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `extract_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 589 che coinvolge il nodo canonico `extract_outputs()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 589 che coinvolge il nodo canonico `extract_outputs()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 589 che coinvolge il nodo canonico `extract_outputs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 589 che coinvolge il nodo canonico `extract_outputs()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 589 che coinvolge il nodo canonico `extract_outputs()` è `L25`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 589 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 589 che coinvolge il nodo canonico `extract_outputs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 589 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo unificato, l'arco 589 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo unificato, l'arco 589 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `extract_outputs()`.
<!-- GRAPH_EDGE graph=unificata index=595 role=incoming -->
Nel grafo unificato, il nodo canonico `extract_outputs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_extracts_saml_assertion_as_base64()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `.test_extracts_saml_assertion_as_base64()` chiama `extract_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 595 che coinvolge il nodo canonico `extract_outputs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 595 che coinvolge il nodo canonico `extract_outputs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 595 che coinvolge il nodo canonico `extract_outputs()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 595 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 595 che coinvolge il nodo canonico `extract_outputs()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 595 che coinvolge il nodo canonico `extract_outputs()` è `L184`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 595 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 595 che coinvolge il nodo canonico `extract_outputs()` è `ast`. Nel grafo unificato, l'arco 595 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo unificato, l'arco 595 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `extract_outputs()`.
<!-- GRAPH_EDGE graph=unificata index=596 role=incoming -->
Nel grafo unificato, il nodo canonico `extract_outputs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()` chiama `extract_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 596 che coinvolge il nodo canonico `extract_outputs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 596 che coinvolge il nodo canonico `extract_outputs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 596 che coinvolge il nodo canonico `extract_outputs()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 596 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 596 che coinvolge il nodo canonico `extract_outputs()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 596 che coinvolge il nodo canonico `extract_outputs()` è `L226`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 596 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 596 che coinvolge il nodo canonico `extract_outputs()` è `ast`. Nel grafo unificato, l'arco 596 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo unificato, l'arco 596 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `extract_outputs()`.
<!-- GRAPH_EDGE graph=unificata index=667 role=incoming -->
Nel grafo unificato, il nodo canonico `extract_outputs()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `extract_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 667 che coinvolge il nodo canonico `extract_outputs()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 667 che coinvolge il nodo canonico `extract_outputs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 667 che coinvolge il nodo canonico `extract_outputs()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 667 che coinvolge il nodo canonico `extract_outputs()` è `L158`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 667 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 667 che coinvolge il nodo canonico `extract_outputs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 667 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo unificato, l'arco 667 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo unificato, l'arco 667 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `extract_outputs()`.
<!-- GRAPH_EDGE graph=unificata index=707 role=incoming -->
Nel grafo unificato, il nodo canonico `extract_outputs()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Any [traffic_exec_engine_py_any]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_py_any` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `Any [traffic_exec_engine_py_any]` fa riferimento a `extract_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 707 che coinvolge il nodo canonico `extract_outputs()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 707 che coinvolge il nodo canonico `extract_outputs()` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 707 che coinvolge il nodo canonico `extract_outputs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 707 che coinvolge il nodo canonico `extract_outputs()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 707 che coinvolge il nodo canonico `extract_outputs()` è `L158`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 707 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 707 che coinvolge il nodo canonico `extract_outputs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 707 che coinvolge il nodo canonico `extract_outputs()` è `1.0`. Nel grafo unificato, l'arco 707 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `extract_outputs()`. Nel grafo unificato, l'arco 707 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `extract_outputs()`.

### ExtractionTest

<!-- GRAPH_NODE graph=codice index=285 id=tests_test_core_extractiontest -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `ExtractionTest` è `ExtractionTest`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `ExtractionTest` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `ExtractionTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `ExtractionTest` è `L152`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `ExtractionTest` vale `ast`. Nel grafo del codice RDG, il nodo canonico `ExtractionTest` possiede l'identificatore esatto `tests_test_core_extractiontest`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `ExtractionTest` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `ExtractionTest` è `extractiontest`.
<!-- GRAPH_EDGE graph=codice index=435 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ExtractionTest` è la sorgente `source` della relazione `uses` verso il nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_extractiontest` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `ExtractionTest` usa `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 435 che coinvolge il nodo canonico `ExtractionTest` è `uses`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 435 che coinvolge il nodo canonico `ExtractionTest` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 435 che coinvolge il nodo canonico `ExtractionTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 435 che coinvolge il nodo canonico `ExtractionTest` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 435 che coinvolge il nodo canonico `ExtractionTest` è `0.8`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 435 che coinvolge il nodo canonico `ExtractionTest` è `ast`. Nel grafo del codice RDG, l'arco 435 registra come `source` l'identificatore `tests_test_core_extractiontest` per la relazione che coinvolge il nodo canonico `ExtractionTest`. Nel grafo del codice RDG, l'arco 435 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `ExtractionTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 435 che coinvolge il nodo canonico `ExtractionTest` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=593 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ExtractionTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_extracts_saml_assertion_as_base64()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_extractiontest` all'ID `tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64`. La relazione tecnica esplicita è la seguente: `ExtractionTest` registra come proprio metodo `.test_extracts_saml_assertion_as_base64()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 593 che coinvolge il nodo canonico `ExtractionTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 593 che coinvolge il nodo canonico `ExtractionTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 593 che coinvolge il nodo canonico `ExtractionTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 593 che coinvolge il nodo canonico `ExtractionTest` è `L153`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 593 che coinvolge il nodo canonico `ExtractionTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 593 che coinvolge il nodo canonico `ExtractionTest` è `ast`. Nel grafo del codice RDG, l'arco 593 registra come `source` l'identificatore `tests_test_core_extractiontest` per la relazione che coinvolge il nodo canonico `ExtractionTest`. Nel grafo del codice RDG, l'arco 593 registra come `target` l'identificatore `tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64` per la relazione che coinvolge il nodo canonico `ExtractionTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 593 che coinvolge il nodo canonico `ExtractionTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=594 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ExtractionTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_extractiontest` all'ID `tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64`. La relazione tecnica esplicita è la seguente: `ExtractionTest` registra come proprio metodo `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 594 che coinvolge il nodo canonico `ExtractionTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 594 che coinvolge il nodo canonico `ExtractionTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 594 che coinvolge il nodo canonico `ExtractionTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 594 che coinvolge il nodo canonico `ExtractionTest` è `L191`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 594 che coinvolge il nodo canonico `ExtractionTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 594 che coinvolge il nodo canonico `ExtractionTest` è `ast`. Nel grafo del codice RDG, l'arco 594 registra come `source` l'identificatore `tests_test_core_extractiontest` per la relazione che coinvolge il nodo canonico `ExtractionTest`. Nel grafo del codice RDG, l'arco 594 registra come `target` l'identificatore `tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64` per la relazione che coinvolge il nodo canonico `ExtractionTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 594 che coinvolge il nodo canonico `ExtractionTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=580 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ExtractionTest` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `tests_test_core_extractiontest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `ExtractionTest`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 580 che coinvolge il nodo canonico `ExtractionTest` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 580 che coinvolge il nodo canonico `ExtractionTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 580 che coinvolge il nodo canonico `ExtractionTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 580 che coinvolge il nodo canonico `ExtractionTest` è `L152`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 580 che coinvolge il nodo canonico `ExtractionTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 580 che coinvolge il nodo canonico `ExtractionTest` è `ast`. Nel grafo del codice RDG, l'arco 580 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `ExtractionTest`. Nel grafo del codice RDG, l'arco 580 registra come `target` l'identificatore `tests_test_core_extractiontest` per la relazione che coinvolge il nodo canonico `ExtractionTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 580 che coinvolge il nodo canonico `ExtractionTest` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=285 id=Request-Dataset-Generator::tests_test_core_extractiontest -->
Nel grafo unificato, l'etichetta originale del nodo canonico `ExtractionTest` è `ExtractionTest`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `ExtractionTest` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `ExtractionTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `ExtractionTest` è `L152`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `ExtractionTest` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `ExtractionTest` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `ExtractionTest` è `extractiontest`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `ExtractionTest` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `ExtractionTest` è `tests_test_core_extractiontest`. Nel grafo unificato, il nodo canonico `ExtractionTest` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_extractiontest`.
<!-- GRAPH_EDGE graph=unificata index=593 role=outgoing -->
Nel grafo unificato, il nodo canonico `ExtractionTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_extracts_saml_assertion_as_base64()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_extractiontest` all'ID `Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64`. La relazione tecnica esplicita è la seguente: `ExtractionTest` registra come proprio metodo `.test_extracts_saml_assertion_as_base64()`. Nel grafo unificato, il tipo dichiarato dell'arco 593 che coinvolge il nodo canonico `ExtractionTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 593 che coinvolge il nodo canonico `ExtractionTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 593 che coinvolge il nodo canonico `ExtractionTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 593 che coinvolge il nodo canonico `ExtractionTest` è `L153`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 593 che coinvolge il nodo canonico `ExtractionTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 593 che coinvolge il nodo canonico `ExtractionTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 593 che coinvolge il nodo canonico `ExtractionTest` è `1.0`. Nel grafo unificato, l'arco 593 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_extractiontest` per la relazione che coinvolge il nodo canonico `ExtractionTest`. Nel grafo unificato, l'arco 593 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_as_base64` per la relazione che coinvolge il nodo canonico `ExtractionTest`.
<!-- GRAPH_EDGE graph=unificata index=594 role=outgoing -->
Nel grafo unificato, il nodo canonico `ExtractionTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_extractiontest` all'ID `Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64`. La relazione tecnica esplicita è la seguente: `ExtractionTest` registra come proprio metodo `.test_extracts_saml_assertion_string_linearized_and_linearized_base64()`. Nel grafo unificato, il tipo dichiarato dell'arco 594 che coinvolge il nodo canonico `ExtractionTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 594 che coinvolge il nodo canonico `ExtractionTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 594 che coinvolge il nodo canonico `ExtractionTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 594 che coinvolge il nodo canonico `ExtractionTest` è `L191`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 594 che coinvolge il nodo canonico `ExtractionTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 594 che coinvolge il nodo canonico `ExtractionTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 594 che coinvolge il nodo canonico `ExtractionTest` è `1.0`. Nel grafo unificato, l'arco 594 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_extractiontest` per la relazione che coinvolge il nodo canonico `ExtractionTest`. Nel grafo unificato, l'arco 594 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_extractiontest_test_extracts_saml_assertion_string_linearized_and_linearized_base64` per la relazione che coinvolge il nodo canonico `ExtractionTest`.
<!-- GRAPH_EDGE graph=unificata index=435 role=incoming -->
Nel grafo unificato, il nodo canonico `ExtractionTest` è il destinatario `target` della relazione `uses` proveniente dal nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_extractiontest`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` usa `ExtractionTest`. Nel grafo unificato, il tipo dichiarato dell'arco 435 che coinvolge il nodo canonico `ExtractionTest` è `uses`. Nel grafo unificato, la confidenza categoriale dell'arco 435 che coinvolge il nodo canonico `ExtractionTest` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 435 che coinvolge il nodo canonico `ExtractionTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 435 che coinvolge il nodo canonico `ExtractionTest` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 435 che coinvolge il nodo canonico `ExtractionTest` è `0.8`. Nel grafo unificato, la provenienza `_origin` dell'arco 435 che coinvolge il nodo canonico `ExtractionTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 435 che coinvolge il nodo canonico `ExtractionTest` è `0.5`. Nel grafo unificato, l'arco 435 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `ExtractionTest`. Nel grafo unificato, l'arco 435 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_extractiontest` per la relazione che coinvolge il nodo canonico `ExtractionTest`.
<!-- GRAPH_EDGE graph=unificata index=580 role=incoming -->
Nel grafo unificato, il nodo canonico `ExtractionTest` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::tests_test_core_extractiontest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `ExtractionTest`. Nel grafo unificato, il tipo dichiarato dell'arco 580 che coinvolge il nodo canonico `ExtractionTest` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 580 che coinvolge il nodo canonico `ExtractionTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 580 che coinvolge il nodo canonico `ExtractionTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 580 che coinvolge il nodo canonico `ExtractionTest` è `L152`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 580 che coinvolge il nodo canonico `ExtractionTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 580 che coinvolge il nodo canonico `ExtractionTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 580 che coinvolge il nodo canonico `ExtractionTest` è `1.0`. Nel grafo unificato, l'arco 580 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `ExtractionTest`. Nel grafo unificato, l'arco 580 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_extractiontest` per la relazione che coinvolge il nodo canonico `ExtractionTest`.

### FaultInjector

<!-- GRAPH_NODE graph=codice index=366 id=traffic_exec_engine_faultinjector -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `FaultInjector` è `FaultInjector`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `FaultInjector` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `FaultInjector` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `FaultInjector` è `L471`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `FaultInjector` vale `ast`. Nel grafo del codice RDG, il nodo canonico `FaultInjector` possiede l'identificatore esatto `traffic_exec_engine_faultinjector`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `FaultInjector` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `FaultInjector` è `faultinjector`.
<!-- GRAPH_EDGE graph=codice index=769 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FaultInjector` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_faultinjector` all'ID `traffic_exec_engine_faultinjector_init`. La relazione tecnica esplicita è la seguente: `FaultInjector` registra come proprio metodo `.__init__() [traffic_exec_engine_faultinjector_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 769 che coinvolge il nodo canonico `FaultInjector` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 769 che coinvolge il nodo canonico `FaultInjector` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 769 che coinvolge il nodo canonico `FaultInjector` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 769 che coinvolge il nodo canonico `FaultInjector` è `L474`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 769 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 769 che coinvolge il nodo canonico `FaultInjector` è `ast`. Nel grafo del codice RDG, l'arco 769 registra come `source` l'identificatore `traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo del codice RDG, l'arco 769 registra come `target` l'identificatore `traffic_exec_engine_faultinjector_init` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 769 che coinvolge il nodo canonico `FaultInjector` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=770 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FaultInjector` è la sorgente `source` della relazione `method` verso il nodo canonico `._matches()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_faultinjector` all'ID `traffic_exec_engine_faultinjector_matches`. La relazione tecnica esplicita è la seguente: `FaultInjector` registra come proprio metodo `._matches()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 770 che coinvolge il nodo canonico `FaultInjector` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 770 che coinvolge il nodo canonico `FaultInjector` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 770 che coinvolge il nodo canonico `FaultInjector` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 770 che coinvolge il nodo canonico `FaultInjector` è `L525`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 770 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 770 che coinvolge il nodo canonico `FaultInjector` è `ast`. Nel grafo del codice RDG, l'arco 770 registra come `source` l'identificatore `traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo del codice RDG, l'arco 770 registra come `target` l'identificatore `traffic_exec_engine_faultinjector_matches` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 770 che coinvolge il nodo canonico `FaultInjector` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=771 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FaultInjector` è la sorgente `source` della relazione `method` verso il nodo canonico `.plan()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_faultinjector` all'ID `traffic_exec_engine_faultinjector_plan`. La relazione tecnica esplicita è la seguente: `FaultInjector` registra come proprio metodo `.plan()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 771 che coinvolge il nodo canonico `FaultInjector` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 771 che coinvolge il nodo canonico `FaultInjector` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 771 che coinvolge il nodo canonico `FaultInjector` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 771 che coinvolge il nodo canonico `FaultInjector` è `L480`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 771 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 771 che coinvolge il nodo canonico `FaultInjector` è `ast`. Nel grafo del codice RDG, l'arco 771 registra come `source` l'identificatore `traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo del codice RDG, l'arco 771 registra come `target` l'identificatore `traffic_exec_engine_faultinjector_plan` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 771 che coinvolge il nodo canonico `FaultInjector` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=670 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FaultInjector` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_faultinjector`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `FaultInjector`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 670 che coinvolge il nodo canonico `FaultInjector` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 670 che coinvolge il nodo canonico `FaultInjector` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 670 che coinvolge il nodo canonico `FaultInjector` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 670 che coinvolge il nodo canonico `FaultInjector` è `L471`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 670 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 670 che coinvolge il nodo canonico `FaultInjector` è `ast`. Nel grafo del codice RDG, l'arco 670 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo del codice RDG, l'arco 670 registra come `target` l'identificatore `traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 670 che coinvolge il nodo canonico `FaultInjector` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=768 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FaultInjector` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_faultinjector`. La relazione tecnica esplicita è la seguente: `execute_flow()` fa riferimento a `FaultInjector`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 768 che coinvolge il nodo canonico `FaultInjector` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 768 che coinvolge il nodo canonico `FaultInjector` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 768 che coinvolge il nodo canonico `FaultInjector` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 768 che coinvolge il nodo canonico `FaultInjector` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 768 che coinvolge il nodo canonico `FaultInjector` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 768 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 768 che coinvolge il nodo canonico `FaultInjector` è `ast`. Nel grafo del codice RDG, l'arco 768 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo del codice RDG, l'arco 768 registra come `target` l'identificatore `traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 768 che coinvolge il nodo canonico `FaultInjector` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=772 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FaultInjector` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Applica fault injection deterministica a livello step.`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_472` all'ID `traffic_exec_engine_faultinjector`. La relazione tecnica esplicita è la seguente: `Applica fault injection deterministica a livello step.` costituisce una motivazione per `FaultInjector`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 772 che coinvolge il nodo canonico `FaultInjector` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 772 che coinvolge il nodo canonico `FaultInjector` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 772 che coinvolge il nodo canonico `FaultInjector` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 772 che coinvolge il nodo canonico `FaultInjector` è `L472`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 772 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 772 che coinvolge il nodo canonico `FaultInjector` è `ast`. Nel grafo del codice RDG, l'arco 772 registra come `source` l'identificatore `traffic_exec_engine_rationale_472` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo del codice RDG, l'arco 772 registra come `target` l'identificatore `traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 772 che coinvolge il nodo canonico `FaultInjector` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=773 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FaultInjector` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_run` all'ID `traffic_exec_engine_faultinjector`. La relazione tecnica esplicita è la seguente: `run()` chiama `FaultInjector`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 773 che coinvolge il nodo canonico `FaultInjector` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 773 che coinvolge il nodo canonico `FaultInjector` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 773 che coinvolge il nodo canonico `FaultInjector` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 773 che coinvolge il nodo canonico `FaultInjector` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 773 che coinvolge il nodo canonico `FaultInjector` è `L996`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 773 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 773 che coinvolge il nodo canonico `FaultInjector` è `ast`. Nel grafo del codice RDG, l'arco 773 registra come `source` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo del codice RDG, l'arco 773 registra come `target` l'identificatore `traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 773 che coinvolge il nodo canonico `FaultInjector` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=366 id=Request-Dataset-Generator::traffic_exec_engine_faultinjector -->
Nel grafo unificato, l'etichetta originale del nodo canonico `FaultInjector` è `FaultInjector`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `FaultInjector` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `FaultInjector` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `FaultInjector` è `L471`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `FaultInjector` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `FaultInjector` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `FaultInjector` è `faultinjector`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `FaultInjector` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `FaultInjector` è `traffic_exec_engine_faultinjector`. Nel grafo unificato, il nodo canonico `FaultInjector` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_faultinjector`.
<!-- GRAPH_EDGE graph=unificata index=768 role=outgoing -->
Nel grafo unificato, il nodo canonico `FaultInjector` è la sorgente `source` della relazione `references` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `FaultInjector` fa riferimento a `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 768 che coinvolge il nodo canonico `FaultInjector` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 768 che coinvolge il nodo canonico `FaultInjector` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 768 che coinvolge il nodo canonico `FaultInjector` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 768 che coinvolge il nodo canonico `FaultInjector` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 768 che coinvolge il nodo canonico `FaultInjector` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 768 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 768 che coinvolge il nodo canonico `FaultInjector` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 768 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo unificato, l'arco 768 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo unificato, l'arco 768 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `FaultInjector`.
<!-- GRAPH_EDGE graph=unificata index=769 role=outgoing -->
Nel grafo unificato, il nodo canonico `FaultInjector` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [traffic_exec_engine_faultinjector_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector` all'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector_init`. La relazione tecnica esplicita è la seguente: `FaultInjector` registra come proprio metodo `.__init__() [traffic_exec_engine_faultinjector_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 769 che coinvolge il nodo canonico `FaultInjector` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 769 che coinvolge il nodo canonico `FaultInjector` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 769 che coinvolge il nodo canonico `FaultInjector` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 769 che coinvolge il nodo canonico `FaultInjector` è `L474`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 769 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 769 che coinvolge il nodo canonico `FaultInjector` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 769 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo unificato, l'arco 769 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo unificato, l'arco 769 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector_init` per la relazione che coinvolge il nodo canonico `FaultInjector`.
<!-- GRAPH_EDGE graph=unificata index=770 role=outgoing -->
Nel grafo unificato, il nodo canonico `FaultInjector` è la sorgente `source` della relazione `method` verso il nodo canonico `._matches()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector` all'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector_matches`. La relazione tecnica esplicita è la seguente: `FaultInjector` registra come proprio metodo `._matches()`. Nel grafo unificato, il tipo dichiarato dell'arco 770 che coinvolge il nodo canonico `FaultInjector` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 770 che coinvolge il nodo canonico `FaultInjector` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 770 che coinvolge il nodo canonico `FaultInjector` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 770 che coinvolge il nodo canonico `FaultInjector` è `L525`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 770 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 770 che coinvolge il nodo canonico `FaultInjector` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 770 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo unificato, l'arco 770 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo unificato, l'arco 770 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector_matches` per la relazione che coinvolge il nodo canonico `FaultInjector`.
<!-- GRAPH_EDGE graph=unificata index=771 role=outgoing -->
Nel grafo unificato, il nodo canonico `FaultInjector` è la sorgente `source` della relazione `method` verso il nodo canonico `.plan()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector` all'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan`. La relazione tecnica esplicita è la seguente: `FaultInjector` registra come proprio metodo `.plan()`. Nel grafo unificato, il tipo dichiarato dell'arco 771 che coinvolge il nodo canonico `FaultInjector` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 771 che coinvolge il nodo canonico `FaultInjector` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 771 che coinvolge il nodo canonico `FaultInjector` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 771 che coinvolge il nodo canonico `FaultInjector` è `L480`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 771 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 771 che coinvolge il nodo canonico `FaultInjector` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 771 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo unificato, l'arco 771 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo unificato, l'arco 771 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector_plan` per la relazione che coinvolge il nodo canonico `FaultInjector`.
<!-- GRAPH_EDGE graph=unificata index=772 role=outgoing -->
Nel grafo unificato, il nodo canonico `FaultInjector` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Applica fault injection deterministica a livello step.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_472`. La relazione tecnica esplicita è la seguente: `FaultInjector` costituisce una motivazione per `Applica fault injection deterministica a livello step.`. Nel grafo unificato, il tipo dichiarato dell'arco 772 che coinvolge il nodo canonico `FaultInjector` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 772 che coinvolge il nodo canonico `FaultInjector` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 772 che coinvolge il nodo canonico `FaultInjector` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 772 che coinvolge il nodo canonico `FaultInjector` è `L472`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 772 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 772 che coinvolge il nodo canonico `FaultInjector` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 772 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo unificato, l'arco 772 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo unificato, l'arco 772 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_472` per la relazione che coinvolge il nodo canonico `FaultInjector`.
<!-- GRAPH_EDGE graph=unificata index=773 role=outgoing -->
Nel grafo unificato, il nodo canonico `FaultInjector` è la sorgente `source` della relazione `calls` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `FaultInjector` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 773 che coinvolge il nodo canonico `FaultInjector` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 773 che coinvolge il nodo canonico `FaultInjector` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 773 che coinvolge il nodo canonico `FaultInjector` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 773 che coinvolge il nodo canonico `FaultInjector` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 773 che coinvolge il nodo canonico `FaultInjector` è `L996`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 773 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 773 che coinvolge il nodo canonico `FaultInjector` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 773 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo unificato, l'arco 773 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo unificato, l'arco 773 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `FaultInjector`.
<!-- GRAPH_EDGE graph=unificata index=670 role=incoming -->
Nel grafo unificato, il nodo canonico `FaultInjector` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `FaultInjector`. Nel grafo unificato, il tipo dichiarato dell'arco 670 che coinvolge il nodo canonico `FaultInjector` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 670 che coinvolge il nodo canonico `FaultInjector` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 670 che coinvolge il nodo canonico `FaultInjector` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 670 che coinvolge il nodo canonico `FaultInjector` è `L471`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 670 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 670 che coinvolge il nodo canonico `FaultInjector` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 670 che coinvolge il nodo canonico `FaultInjector` è `1.0`. Nel grafo unificato, l'arco 670 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `FaultInjector`. Nel grafo unificato, l'arco 670 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `FaultInjector`.

### Invia la HTTP request definita nello step.      Inietta automaticamente gli head

<!-- GRAPH_NODE graph=codice index=406 id=traffic_exec_engine_rationale_549 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `Invia la HTTP request definita nello step.      Inietta automaticamente gli head`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `L549`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` possiede l'identificatore esatto `traffic_exec_engine_rationale_549`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `invia la http request definita nello step.      inietta automaticamente gli head`.
<!-- GRAPH_EDGE graph=codice index=777 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `send_request()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_549` all'ID `traffic_exec_engine_send_request`. La relazione tecnica esplicita è la seguente: `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` costituisce una motivazione per `send_request()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 777 che coinvolge il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 777 che coinvolge il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 777 che coinvolge il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 777 che coinvolge il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `L549`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 777 che coinvolge il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 777 che coinvolge il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `ast`. Nel grafo del codice RDG, l'arco 777 registra come `source` l'identificatore `traffic_exec_engine_rationale_549` per la relazione che coinvolge il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head`. Nel grafo del codice RDG, l'arco 777 registra come `target` l'identificatore `traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 777 che coinvolge il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=406 id=Request-Dataset-Generator::traffic_exec_engine_rationale_549 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `Invia la HTTP request definita nello step.      Inietta automaticamente gli head`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `L549`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `invia la http request definita nello step.      inietta automaticamente gli head`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `traffic_exec_engine_rationale_549`. Nel grafo unificato, il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_rationale_549`.
Nel grafo unificato, il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=777 role=incoming -->
Nel grafo unificato, il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `send_request()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_send_request` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_549`. La relazione tecnica esplicita è la seguente: `send_request()` costituisce una motivazione per `Invia la HTTP request definita nello step.      Inietta automaticamente gli head`. Nel grafo unificato, il tipo dichiarato dell'arco 777 che coinvolge il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 777 che coinvolge il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 777 che coinvolge il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 777 che coinvolge il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `L549`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 777 che coinvolge il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 777 che coinvolge il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 777 che coinvolge il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` è `1.0`. Nel grafo unificato, l'arco 777 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head`. Nel grafo unificato, l'arco 777 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_549` per la relazione che coinvolge il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head`.

### Legge un segreto da env var se configurata, altrimenti dal config locale.

<!-- GRAPH_NODE graph=codice index=403 id=traffic_exec_engine_rationale_367 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `Legge un segreto da env var se configurata, altrimenti dal config locale.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `L367`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` possiede l'identificatore esatto `traffic_exec_engine_rationale_367`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `legge un segreto da env var se configurata, altrimenti dal config locale.`.
<!-- GRAPH_EDGE graph=codice index=744 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `_resolve_secret()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_367` all'ID `traffic_exec_engine_resolve_secret`. La relazione tecnica esplicita è la seguente: `Legge un segreto da env var se configurata, altrimenti dal config locale.` costituisce una motivazione per `_resolve_secret()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 744 che coinvolge il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 744 che coinvolge il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 744 che coinvolge il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 744 che coinvolge il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `L367`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 744 che coinvolge il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 744 che coinvolge il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `ast`. Nel grafo del codice RDG, l'arco 744 registra come `source` l'identificatore `traffic_exec_engine_rationale_367` per la relazione che coinvolge il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.`. Nel grafo del codice RDG, l'arco 744 registra come `target` l'identificatore `traffic_exec_engine_resolve_secret` per la relazione che coinvolge il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 744 che coinvolge il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=403 id=Request-Dataset-Generator::traffic_exec_engine_rationale_367 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `Legge un segreto da env var se configurata, altrimenti dal config locale.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `L367`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `legge un segreto da env var se configurata, altrimenti dal config locale.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `traffic_exec_engine_rationale_367`. Nel grafo unificato, il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_rationale_367`.
Nel grafo unificato, il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=744 role=incoming -->
Nel grafo unificato, il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `_resolve_secret()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_resolve_secret` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_367`. La relazione tecnica esplicita è la seguente: `_resolve_secret()` costituisce una motivazione per `Legge un segreto da env var se configurata, altrimenti dal config locale.`. Nel grafo unificato, il tipo dichiarato dell'arco 744 che coinvolge il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 744 che coinvolge il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 744 che coinvolge il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 744 che coinvolge il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `L367`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 744 che coinvolge il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 744 che coinvolge il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 744 che coinvolge il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.` è `1.0`. Nel grafo unificato, l'arco 744 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_resolve_secret` per la relazione che coinvolge il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.`. Nel grafo unificato, l'arco 744 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_367` per la relazione che coinvolge il nodo canonico `Legge un segreto da env var se configurata, altrimenti dal config locale.`.

### load_config() [traffic_exec_engine_load_config]

<!-- GRAPH_NODE graph=codice index=391 id=traffic_exec_engine_load_config -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `load_config() [traffic_exec_engine_load_config]` è `load_config()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `load_config() [traffic_exec_engine_load_config]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `L1118`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `load_config() [traffic_exec_engine_load_config]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `load_config() [traffic_exec_engine_load_config]` possiede l'identificatore esatto `traffic_exec_engine_load_config`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `load_config() [traffic_exec_engine_load_config]` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `load_config() [traffic_exec_engine_load_config]` è `load_config()`.
Nel grafo del codice RDG, il nodo canonico `load_config() [traffic_exec_engine_load_config]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=673 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `load_config() [traffic_exec_engine_load_config]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_load_config`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `load_config() [traffic_exec_engine_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 673 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 673 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 673 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 673 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `L1118`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 673 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 673 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `ast`. Nel grafo del codice RDG, l'arco 673 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]`. Nel grafo del codice RDG, l'arco 673 registra come `target` l'identificatore `traffic_exec_engine_load_config` per la relazione che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 673 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=816 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `load_config() [traffic_exec_engine_load_config]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `main() [traffic_exec_engine_main]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_main` all'ID `traffic_exec_engine_load_config`. La relazione tecnica esplicita è la seguente: `main() [traffic_exec_engine_main]` chiama `load_config() [traffic_exec_engine_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 816 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 816 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 816 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 816 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 816 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `L1191`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 816 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 816 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `ast`. Nel grafo del codice RDG, l'arco 816 registra come `source` l'identificatore `traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]`. Nel grafo del codice RDG, l'arco 816 registra come `target` l'identificatore `traffic_exec_engine_load_config` per la relazione che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 816 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=391 id=Request-Dataset-Generator::traffic_exec_engine_load_config -->
Nel grafo unificato, l'etichetta originale del nodo canonico `load_config() [traffic_exec_engine_load_config]` è `load_config()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `load_config() [traffic_exec_engine_load_config]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `L1118`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `load_config() [traffic_exec_engine_load_config]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `load_config() [traffic_exec_engine_load_config]` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `load_config() [traffic_exec_engine_load_config]` è `load_config()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `load_config() [traffic_exec_engine_load_config]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `traffic_exec_engine_load_config`. Nel grafo unificato, il nodo canonico `load_config() [traffic_exec_engine_load_config]` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_load_config`.
<!-- GRAPH_EDGE graph=unificata index=816 role=outgoing -->
Nel grafo unificato, il nodo canonico `load_config() [traffic_exec_engine_load_config]` è la sorgente `source` della relazione `calls` verso il nodo canonico `main() [traffic_exec_engine_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_load_config` all'ID `Request-Dataset-Generator::traffic_exec_engine_main`. La relazione tecnica esplicita è la seguente: `load_config() [traffic_exec_engine_load_config]` chiama `main() [traffic_exec_engine_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 816 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 816 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 816 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 816 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 816 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `L1191`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 816 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 816 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 816 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `1.0`. Nel grafo unificato, l'arco 816 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_load_config` per la relazione che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]`. Nel grafo unificato, l'arco 816 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]`.
<!-- GRAPH_EDGE graph=unificata index=673 role=incoming -->
Nel grafo unificato, il nodo canonico `load_config() [traffic_exec_engine_load_config]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_load_config`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `load_config() [traffic_exec_engine_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 673 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 673 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 673 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 673 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `L1118`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 673 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 673 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 673 che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]` è `1.0`. Nel grafo unificato, l'arco 673 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]`. Nel grafo unificato, l'arco 673 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_load_config` per la relazione che coinvolge il nodo canonico `load_config() [traffic_exec_engine_load_config]`.

### load_dataset_as_timeline()

<!-- GRAPH_NODE graph=codice index=390 id=traffic_exec_engine_load_dataset_as_timeline -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `load_dataset_as_timeline()` è `load_dataset_as_timeline()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `load_dataset_as_timeline()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `load_dataset_as_timeline()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `load_dataset_as_timeline()` è `L1097`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `load_dataset_as_timeline()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `load_dataset_as_timeline()` possiede l'identificatore esatto `traffic_exec_engine_load_dataset_as_timeline`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `load_dataset_as_timeline()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `load_dataset_as_timeline()` è `load_dataset_as_timeline()`.
<!-- GRAPH_EDGE graph=codice index=761 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `load_dataset_as_timeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_load_dataset_as_timeline` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `load_dataset_as_timeline()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 761 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 761 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 761 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 761 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 761 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `L1109`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 761 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 761 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 761 registra come `source` l'identificatore `traffic_exec_engine_load_dataset_as_timeline` per la relazione che coinvolge il nodo canonico `load_dataset_as_timeline()`. Nel grafo del codice RDG, l'arco 761 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `load_dataset_as_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 761 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=674 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `load_dataset_as_timeline()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_load_dataset_as_timeline`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `load_dataset_as_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 674 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 674 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 674 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 674 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `L1097`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 674 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 674 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 674 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `load_dataset_as_timeline()`. Nel grafo del codice RDG, l'arco 674 registra come `target` l'identificatore `traffic_exec_engine_load_dataset_as_timeline` per la relazione che coinvolge il nodo canonico `load_dataset_as_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 674 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=814 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `load_dataset_as_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `main() [traffic_exec_engine_main]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_main` all'ID `traffic_exec_engine_load_dataset_as_timeline`. La relazione tecnica esplicita è la seguente: `main() [traffic_exec_engine_main]` chiama `load_dataset_as_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 814 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 814 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 814 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 814 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 814 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `L1202`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 814 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 814 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 814 registra come `source` l'identificatore `traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `load_dataset_as_timeline()`. Nel grafo del codice RDG, l'arco 814 registra come `target` l'identificatore `traffic_exec_engine_load_dataset_as_timeline` per la relazione che coinvolge il nodo canonico `load_dataset_as_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 814 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=815 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `load_dataset_as_timeline()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_1098` all'ID `traffic_exec_engine_load_dataset_as_timeline`. La relazione tecnica esplicita è la seguente: `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t` costituisce una motivazione per `load_dataset_as_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 815 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 815 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 815 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 815 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `L1098`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 815 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 815 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 815 registra come `source` l'identificatore `traffic_exec_engine_rationale_1098` per la relazione che coinvolge il nodo canonico `load_dataset_as_timeline()`. Nel grafo del codice RDG, l'arco 815 registra come `target` l'identificatore `traffic_exec_engine_load_dataset_as_timeline` per la relazione che coinvolge il nodo canonico `load_dataset_as_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 815 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=390 id=Request-Dataset-Generator::traffic_exec_engine_load_dataset_as_timeline -->
Nel grafo unificato, l'etichetta originale del nodo canonico `load_dataset_as_timeline()` è `load_dataset_as_timeline()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `load_dataset_as_timeline()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `load_dataset_as_timeline()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `load_dataset_as_timeline()` è `L1097`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `load_dataset_as_timeline()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `load_dataset_as_timeline()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `load_dataset_as_timeline()` è `load_dataset_as_timeline()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `load_dataset_as_timeline()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `load_dataset_as_timeline()` è `traffic_exec_engine_load_dataset_as_timeline`. Nel grafo unificato, il nodo canonico `load_dataset_as_timeline()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_load_dataset_as_timeline`.
<!-- GRAPH_EDGE graph=unificata index=814 role=outgoing -->
Nel grafo unificato, il nodo canonico `load_dataset_as_timeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `main() [traffic_exec_engine_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_load_dataset_as_timeline` all'ID `Request-Dataset-Generator::traffic_exec_engine_main`. La relazione tecnica esplicita è la seguente: `load_dataset_as_timeline()` chiama `main() [traffic_exec_engine_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 814 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 814 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 814 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 814 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 814 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `L1202`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 814 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 814 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 814 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `1.0`. Nel grafo unificato, l'arco 814 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_load_dataset_as_timeline` per la relazione che coinvolge il nodo canonico `load_dataset_as_timeline()`. Nel grafo unificato, l'arco 814 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `load_dataset_as_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=815 role=outgoing -->
Nel grafo unificato, il nodo canonico `load_dataset_as_timeline()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_load_dataset_as_timeline` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_1098`. La relazione tecnica esplicita è la seguente: `load_dataset_as_timeline()` costituisce una motivazione per `Carica un dataset e lo converte in una timeline piatta     con timestamp=0 per t`. Nel grafo unificato, il tipo dichiarato dell'arco 815 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 815 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 815 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 815 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `L1098`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 815 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 815 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 815 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `1.0`. Nel grafo unificato, l'arco 815 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_load_dataset_as_timeline` per la relazione che coinvolge il nodo canonico `load_dataset_as_timeline()`. Nel grafo unificato, l'arco 815 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_1098` per la relazione che coinvolge il nodo canonico `load_dataset_as_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=674 role=incoming -->
Nel grafo unificato, il nodo canonico `load_dataset_as_timeline()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_load_dataset_as_timeline`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `load_dataset_as_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 674 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 674 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 674 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 674 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `L1097`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 674 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 674 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 674 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `1.0`. Nel grafo unificato, l'arco 674 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `load_dataset_as_timeline()`. Nel grafo unificato, l'arco 674 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_load_dataset_as_timeline` per la relazione che coinvolge il nodo canonico `load_dataset_as_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=761 role=incoming -->
Nel grafo unificato, il nodo canonico `load_dataset_as_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_load_dataset_as_timeline`. La relazione tecnica esplicita è la seguente: `.get()` chiama `load_dataset_as_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 761 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 761 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 761 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 761 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 761 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `L1109`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 761 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 761 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 761 che coinvolge il nodo canonico `load_dataset_as_timeline()` è `1.0`. Nel grafo unificato, l'arco 761 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `load_dataset_as_timeline()`. Nel grafo unificato, l'arco 761 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_load_dataset_as_timeline` per la relazione che coinvolge il nodo canonico `load_dataset_as_timeline()`.

### load_timeline()

<!-- GRAPH_NODE graph=codice index=389 id=traffic_exec_engine_load_timeline -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `load_timeline()` è `load_timeline()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `load_timeline()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `load_timeline()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `load_timeline()` è `L1089`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `load_timeline()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `load_timeline()` possiede l'identificatore esatto `traffic_exec_engine_load_timeline`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `load_timeline()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `load_timeline()` è `load_timeline()`.
Nel grafo del codice RDG, il nodo canonico `load_timeline()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=675 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `load_timeline()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_load_timeline`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `load_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 675 che coinvolge il nodo canonico `load_timeline()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 675 che coinvolge il nodo canonico `load_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 675 che coinvolge il nodo canonico `load_timeline()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 675 che coinvolge il nodo canonico `load_timeline()` è `L1089`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 675 che coinvolge il nodo canonico `load_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 675 che coinvolge il nodo canonico `load_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 675 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `load_timeline()`. Nel grafo del codice RDG, l'arco 675 registra come `target` l'identificatore `traffic_exec_engine_load_timeline` per la relazione che coinvolge il nodo canonico `load_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 675 che coinvolge il nodo canonico `load_timeline()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=813 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `load_timeline()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `main() [traffic_exec_engine_main]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_main` all'ID `traffic_exec_engine_load_timeline`. La relazione tecnica esplicita è la seguente: `main() [traffic_exec_engine_main]` chiama `load_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 813 che coinvolge il nodo canonico `load_timeline()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 813 che coinvolge il nodo canonico `load_timeline()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 813 che coinvolge il nodo canonico `load_timeline()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 813 che coinvolge il nodo canonico `load_timeline()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 813 che coinvolge il nodo canonico `load_timeline()` è `L1199`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 813 che coinvolge il nodo canonico `load_timeline()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 813 che coinvolge il nodo canonico `load_timeline()` è `ast`. Nel grafo del codice RDG, l'arco 813 registra come `source` l'identificatore `traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `load_timeline()`. Nel grafo del codice RDG, l'arco 813 registra come `target` l'identificatore `traffic_exec_engine_load_timeline` per la relazione che coinvolge il nodo canonico `load_timeline()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 813 che coinvolge il nodo canonico `load_timeline()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=389 id=Request-Dataset-Generator::traffic_exec_engine_load_timeline -->
Nel grafo unificato, l'etichetta originale del nodo canonico `load_timeline()` è `load_timeline()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `load_timeline()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `load_timeline()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `load_timeline()` è `L1089`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `load_timeline()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `load_timeline()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `load_timeline()` è `load_timeline()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `load_timeline()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `load_timeline()` è `traffic_exec_engine_load_timeline`. Nel grafo unificato, il nodo canonico `load_timeline()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_load_timeline`.
<!-- GRAPH_EDGE graph=unificata index=813 role=outgoing -->
Nel grafo unificato, il nodo canonico `load_timeline()` è la sorgente `source` della relazione `calls` verso il nodo canonico `main() [traffic_exec_engine_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_load_timeline` all'ID `Request-Dataset-Generator::traffic_exec_engine_main`. La relazione tecnica esplicita è la seguente: `load_timeline()` chiama `main() [traffic_exec_engine_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 813 che coinvolge il nodo canonico `load_timeline()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 813 che coinvolge il nodo canonico `load_timeline()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 813 che coinvolge il nodo canonico `load_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 813 che coinvolge il nodo canonico `load_timeline()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 813 che coinvolge il nodo canonico `load_timeline()` è `L1199`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 813 che coinvolge il nodo canonico `load_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 813 che coinvolge il nodo canonico `load_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 813 che coinvolge il nodo canonico `load_timeline()` è `1.0`. Nel grafo unificato, l'arco 813 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_load_timeline` per la relazione che coinvolge il nodo canonico `load_timeline()`. Nel grafo unificato, l'arco 813 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `load_timeline()`.
<!-- GRAPH_EDGE graph=unificata index=675 role=incoming -->
Nel grafo unificato, il nodo canonico `load_timeline()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_load_timeline`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `load_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 675 che coinvolge il nodo canonico `load_timeline()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 675 che coinvolge il nodo canonico `load_timeline()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 675 che coinvolge il nodo canonico `load_timeline()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 675 che coinvolge il nodo canonico `load_timeline()` è `L1089`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 675 che coinvolge il nodo canonico `load_timeline()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 675 che coinvolge il nodo canonico `load_timeline()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 675 che coinvolge il nodo canonico `load_timeline()` è `1.0`. Nel grafo unificato, l'arco 675 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `load_timeline()`. Nel grafo unificato, l'arco 675 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_load_timeline` per la relazione che coinvolge il nodo canonico `load_timeline()`.

### main() [traffic_exec_engine_main]

<!-- GRAPH_NODE graph=codice index=394 id=traffic_exec_engine_main -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `main() [traffic_exec_engine_main]` è `main()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `main() [traffic_exec_engine_main]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `main() [traffic_exec_engine_main]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `main() [traffic_exec_engine_main]` è `L1185`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `main() [traffic_exec_engine_main]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `main() [traffic_exec_engine_main]` possiede l'identificatore esatto `traffic_exec_engine_main`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `main() [traffic_exec_engine_main]` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `main() [traffic_exec_engine_main]` è `main()`.
<!-- GRAPH_EDGE graph=codice index=811 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main() [traffic_exec_engine_main]` è la sorgente `source` della relazione `calls` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_main` all'ID `traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `main() [traffic_exec_engine_main]` chiama `run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 811 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 811 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 811 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 811 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 811 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `L1213`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 811 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 811 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `ast`. Nel grafo del codice RDG, l'arco 811 registra come `source` l'identificatore `traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`. Nel grafo del codice RDG, l'arco 811 registra come `target` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 811 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=813 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main() [traffic_exec_engine_main]` è la sorgente `source` della relazione `calls` verso il nodo canonico `load_timeline()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_main` all'ID `traffic_exec_engine_load_timeline`. La relazione tecnica esplicita è la seguente: `main() [traffic_exec_engine_main]` chiama `load_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 813 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 813 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 813 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 813 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 813 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `L1199`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 813 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 813 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `ast`. Nel grafo del codice RDG, l'arco 813 registra come `source` l'identificatore `traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`. Nel grafo del codice RDG, l'arco 813 registra come `target` l'identificatore `traffic_exec_engine_load_timeline` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 813 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=814 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main() [traffic_exec_engine_main]` è la sorgente `source` della relazione `calls` verso il nodo canonico `load_dataset_as_timeline()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_main` all'ID `traffic_exec_engine_load_dataset_as_timeline`. La relazione tecnica esplicita è la seguente: `main() [traffic_exec_engine_main]` chiama `load_dataset_as_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 814 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 814 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 814 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 814 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 814 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `L1202`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 814 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 814 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `ast`. Nel grafo del codice RDG, l'arco 814 registra come `source` l'identificatore `traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`. Nel grafo del codice RDG, l'arco 814 registra come `target` l'identificatore `traffic_exec_engine_load_dataset_as_timeline` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 814 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=816 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main() [traffic_exec_engine_main]` è la sorgente `source` della relazione `calls` verso il nodo canonico `load_config() [traffic_exec_engine_load_config]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_main` all'ID `traffic_exec_engine_load_config`. La relazione tecnica esplicita è la seguente: `main() [traffic_exec_engine_main]` chiama `load_config() [traffic_exec_engine_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 816 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 816 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 816 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 816 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 816 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `L1191`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 816 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 816 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `ast`. Nel grafo del codice RDG, l'arco 816 registra come `source` l'identificatore `traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`. Nel grafo del codice RDG, l'arco 816 registra come `target` l'identificatore `traffic_exec_engine_load_config` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 816 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=818 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main() [traffic_exec_engine_main]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_main` all'ID `traffic_exec_engine_build_parser`. La relazione tecnica esplicita è la seguente: `main() [traffic_exec_engine_main]` chiama `_build_parser() [traffic_exec_engine_build_parser]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 818 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 818 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 818 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 818 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 818 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `L1186`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 818 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 818 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `ast`. Nel grafo del codice RDG, l'arco 818 registra come `source` l'identificatore `traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`. Nel grafo del codice RDG, l'arco 818 registra come `target` l'identificatore `traffic_exec_engine_build_parser` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 818 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=676 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `main() [traffic_exec_engine_main]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_main`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `main() [traffic_exec_engine_main]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 676 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 676 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 676 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 676 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `L1185`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 676 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 676 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `ast`. Nel grafo del codice RDG, l'arco 676 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`. Nel grafo del codice RDG, l'arco 676 registra come `target` l'identificatore `traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 676 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=394 id=Request-Dataset-Generator::traffic_exec_engine_main -->
Nel grafo unificato, l'etichetta originale del nodo canonico `main() [traffic_exec_engine_main]` è `main()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `main() [traffic_exec_engine_main]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `main() [traffic_exec_engine_main]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `main() [traffic_exec_engine_main]` è `L1185`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `main() [traffic_exec_engine_main]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `main() [traffic_exec_engine_main]` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `main() [traffic_exec_engine_main]` è `main()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `main() [traffic_exec_engine_main]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `main() [traffic_exec_engine_main]` è `traffic_exec_engine_main`. Nel grafo unificato, il nodo canonico `main() [traffic_exec_engine_main]` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_main`.
Nel grafo unificato, il nodo canonico `main() [traffic_exec_engine_main]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=676 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [traffic_exec_engine_main]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_main`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `main() [traffic_exec_engine_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 676 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 676 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 676 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 676 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `L1185`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 676 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 676 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 676 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`. Nel grafo unificato, l'arco 676 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`. Nel grafo unificato, l'arco 676 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`.
<!-- GRAPH_EDGE graph=unificata index=811 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [traffic_exec_engine_main]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_run` all'ID `Request-Dataset-Generator::traffic_exec_engine_main`. La relazione tecnica esplicita è la seguente: `run()` chiama `main() [traffic_exec_engine_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 811 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 811 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 811 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 811 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 811 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `L1213`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 811 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 811 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 811 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`. Nel grafo unificato, l'arco 811 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`. Nel grafo unificato, l'arco 811 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`.
<!-- GRAPH_EDGE graph=unificata index=813 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [traffic_exec_engine_main]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `load_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_load_timeline` all'ID `Request-Dataset-Generator::traffic_exec_engine_main`. La relazione tecnica esplicita è la seguente: `load_timeline()` chiama `main() [traffic_exec_engine_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 813 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 813 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 813 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 813 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 813 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `L1199`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 813 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 813 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 813 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`. Nel grafo unificato, l'arco 813 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_load_timeline` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`. Nel grafo unificato, l'arco 813 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`.
<!-- GRAPH_EDGE graph=unificata index=814 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [traffic_exec_engine_main]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `load_dataset_as_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_load_dataset_as_timeline` all'ID `Request-Dataset-Generator::traffic_exec_engine_main`. La relazione tecnica esplicita è la seguente: `load_dataset_as_timeline()` chiama `main() [traffic_exec_engine_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 814 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 814 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 814 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 814 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 814 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `L1202`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 814 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 814 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 814 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`. Nel grafo unificato, l'arco 814 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_load_dataset_as_timeline` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`. Nel grafo unificato, l'arco 814 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`.
<!-- GRAPH_EDGE graph=unificata index=816 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [traffic_exec_engine_main]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `load_config() [traffic_exec_engine_load_config]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_load_config` all'ID `Request-Dataset-Generator::traffic_exec_engine_main`. La relazione tecnica esplicita è la seguente: `load_config() [traffic_exec_engine_load_config]` chiama `main() [traffic_exec_engine_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 816 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 816 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 816 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 816 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 816 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `L1191`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 816 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 816 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 816 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`. Nel grafo unificato, l'arco 816 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_load_config` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`. Nel grafo unificato, l'arco 816 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`.
<!-- GRAPH_EDGE graph=unificata index=818 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [traffic_exec_engine_main]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_build_parser() [traffic_exec_engine_build_parser]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_build_parser` all'ID `Request-Dataset-Generator::traffic_exec_engine_main`. La relazione tecnica esplicita è la seguente: `_build_parser() [traffic_exec_engine_build_parser]` chiama `main() [traffic_exec_engine_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 818 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 818 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 818 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 818 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 818 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `L1186`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 818 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 818 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 818 che coinvolge il nodo canonico `main() [traffic_exec_engine_main]` è `1.0`. Nel grafo unificato, l'arco 818 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_build_parser` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`. Nel grafo unificato, l'arco 818 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `main() [traffic_exec_engine_main]`.

### MtlsContextFactory

<!-- GRAPH_NODE graph=codice index=361 id=traffic_exec_engine_mtlscontextfactory -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `MtlsContextFactory` è `MtlsContextFactory`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `MtlsContextFactory` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `MtlsContextFactory` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `MtlsContextFactory` è `L373`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `MtlsContextFactory` vale `ast`. Nel grafo del codice RDG, il nodo canonico `MtlsContextFactory` possiede l'identificatore esatto `traffic_exec_engine_mtlscontextfactory`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `MtlsContextFactory` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `MtlsContextFactory` è `mtlscontextfactory`.
<!-- GRAPH_EDGE graph=codice index=746 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `MtlsContextFactory` è la sorgente `source` della relazione `method` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_mtlscontextfactory` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `MtlsContextFactory` registra come proprio metodo `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 746 che coinvolge il nodo canonico `MtlsContextFactory` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 746 che coinvolge il nodo canonico `MtlsContextFactory` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 746 che coinvolge il nodo canonico `MtlsContextFactory` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 746 che coinvolge il nodo canonico `MtlsContextFactory` è `L385`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 746 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 746 che coinvolge il nodo canonico `MtlsContextFactory` è `ast`. Nel grafo del codice RDG, l'arco 746 registra come `source` l'identificatore `traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo del codice RDG, l'arco 746 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 746 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=747 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `MtlsContextFactory` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_mtlscontextfactory` all'ID `traffic_exec_engine_mtlscontextfactory_init`. La relazione tecnica esplicita è la seguente: `MtlsContextFactory` registra come proprio metodo `.__init__() [traffic_exec_engine_mtlscontextfactory_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 747 che coinvolge il nodo canonico `MtlsContextFactory` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 747 che coinvolge il nodo canonico `MtlsContextFactory` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 747 che coinvolge il nodo canonico `MtlsContextFactory` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 747 che coinvolge il nodo canonico `MtlsContextFactory` è `L381`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 747 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 747 che coinvolge il nodo canonico `MtlsContextFactory` è `ast`. Nel grafo del codice RDG, l'arco 747 registra come `source` l'identificatore `traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo del codice RDG, l'arco 747 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_init` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 747 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=748 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `MtlsContextFactory` è la sorgente `source` della relazione `method` verso il nodo canonico `._load_pkcs12()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_mtlscontextfactory` all'ID `traffic_exec_engine_mtlscontextfactory_load_pkcs12`. La relazione tecnica esplicita è la seguente: `MtlsContextFactory` registra come proprio metodo `._load_pkcs12()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 748 che coinvolge il nodo canonico `MtlsContextFactory` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 748 che coinvolge il nodo canonico `MtlsContextFactory` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 748 che coinvolge il nodo canonico `MtlsContextFactory` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 748 che coinvolge il nodo canonico `MtlsContextFactory` è `L429`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 748 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 748 che coinvolge il nodo canonico `MtlsContextFactory` è `ast`. Nel grafo del codice RDG, l'arco 748 registra come `source` l'identificatore `traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo del codice RDG, l'arco 748 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_load_pkcs12` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 748 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=677 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `MtlsContextFactory` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_mtlscontextfactory`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `MtlsContextFactory`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 677 che coinvolge il nodo canonico `MtlsContextFactory` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 677 che coinvolge il nodo canonico `MtlsContextFactory` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 677 che coinvolge il nodo canonico `MtlsContextFactory` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 677 che coinvolge il nodo canonico `MtlsContextFactory` è `L373`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 677 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 677 che coinvolge il nodo canonico `MtlsContextFactory` è `ast`. Nel grafo del codice RDG, l'arco 677 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo del codice RDG, l'arco 677 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 677 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=745 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `MtlsContextFactory` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_mtlscontextfactory`. La relazione tecnica esplicita è la seguente: `execute_flow()` fa riferimento a `MtlsContextFactory`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 745 che coinvolge il nodo canonico `MtlsContextFactory` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 745 che coinvolge il nodo canonico `MtlsContextFactory` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 745 che coinvolge il nodo canonico `MtlsContextFactory` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 745 che coinvolge il nodo canonico `MtlsContextFactory` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 745 che coinvolge il nodo canonico `MtlsContextFactory` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 745 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 745 che coinvolge il nodo canonico `MtlsContextFactory` è `ast`. Nel grafo del codice RDG, l'arco 745 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo del codice RDG, l'arco 745 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 745 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=749 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `MtlsContextFactory` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_374` all'ID `traffic_exec_engine_mtlscontextfactory`. La relazione tecnica esplicita è la seguente: `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co` costituisce una motivazione per `MtlsContextFactory`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 749 che coinvolge il nodo canonico `MtlsContextFactory` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 749 che coinvolge il nodo canonico `MtlsContextFactory` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 749 che coinvolge il nodo canonico `MtlsContextFactory` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 749 che coinvolge il nodo canonico `MtlsContextFactory` è `L374`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 749 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 749 che coinvolge il nodo canonico `MtlsContextFactory` è `ast`. Nel grafo del codice RDG, l'arco 749 registra come `source` l'identificatore `traffic_exec_engine_rationale_374` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo del codice RDG, l'arco 749 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 749 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=750 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `MtlsContextFactory` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_run` all'ID `traffic_exec_engine_mtlscontextfactory`. La relazione tecnica esplicita è la seguente: `run()` chiama `MtlsContextFactory`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 750 che coinvolge il nodo canonico `MtlsContextFactory` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 750 che coinvolge il nodo canonico `MtlsContextFactory` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 750 che coinvolge il nodo canonico `MtlsContextFactory` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 750 che coinvolge il nodo canonico `MtlsContextFactory` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 750 che coinvolge il nodo canonico `MtlsContextFactory` è `L997`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 750 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 750 che coinvolge il nodo canonico `MtlsContextFactory` è `ast`. Nel grafo del codice RDG, l'arco 750 registra come `source` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo del codice RDG, l'arco 750 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 750 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=751 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `MtlsContextFactory` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `send_request()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_send_request` all'ID `traffic_exec_engine_mtlscontextfactory`. La relazione tecnica esplicita è la seguente: `send_request()` fa riferimento a `MtlsContextFactory`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 751 che coinvolge il nodo canonico `MtlsContextFactory` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 751 che coinvolge il nodo canonico `MtlsContextFactory` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 751 che coinvolge il nodo canonico `MtlsContextFactory` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 751 che coinvolge il nodo canonico `MtlsContextFactory` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 751 che coinvolge il nodo canonico `MtlsContextFactory` è `L542`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 751 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 751 che coinvolge il nodo canonico `MtlsContextFactory` è `ast`. Nel grafo del codice RDG, l'arco 751 registra come `source` l'identificatore `traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo del codice RDG, l'arco 751 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 751 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=361 id=Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory -->
Nel grafo unificato, l'etichetta originale del nodo canonico `MtlsContextFactory` è `MtlsContextFactory`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `MtlsContextFactory` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `MtlsContextFactory` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `MtlsContextFactory` è `L373`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `MtlsContextFactory` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `MtlsContextFactory` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `MtlsContextFactory` è `mtlscontextfactory`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `MtlsContextFactory` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `MtlsContextFactory` è `traffic_exec_engine_mtlscontextfactory`. Nel grafo unificato, il nodo canonico `MtlsContextFactory` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory`.
<!-- GRAPH_EDGE graph=unificata index=745 role=outgoing -->
Nel grafo unificato, il nodo canonico `MtlsContextFactory` è la sorgente `source` della relazione `references` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `MtlsContextFactory` fa riferimento a `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 745 che coinvolge il nodo canonico `MtlsContextFactory` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 745 che coinvolge il nodo canonico `MtlsContextFactory` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 745 che coinvolge il nodo canonico `MtlsContextFactory` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 745 che coinvolge il nodo canonico `MtlsContextFactory` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 745 che coinvolge il nodo canonico `MtlsContextFactory` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 745 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 745 che coinvolge il nodo canonico `MtlsContextFactory` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 745 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo unificato, l'arco 745 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo unificato, l'arco 745 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`.
<!-- GRAPH_EDGE graph=unificata index=746 role=outgoing -->
Nel grafo unificato, il nodo canonico `MtlsContextFactory` è la sorgente `source` della relazione `method` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `MtlsContextFactory` registra come proprio metodo `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 746 che coinvolge il nodo canonico `MtlsContextFactory` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 746 che coinvolge il nodo canonico `MtlsContextFactory` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 746 che coinvolge il nodo canonico `MtlsContextFactory` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 746 che coinvolge il nodo canonico `MtlsContextFactory` è `L385`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 746 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 746 che coinvolge il nodo canonico `MtlsContextFactory` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 746 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo unificato, l'arco 746 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo unificato, l'arco 746 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`.
<!-- GRAPH_EDGE graph=unificata index=747 role=outgoing -->
Nel grafo unificato, il nodo canonico `MtlsContextFactory` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [traffic_exec_engine_mtlscontextfactory_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_init`. La relazione tecnica esplicita è la seguente: `MtlsContextFactory` registra come proprio metodo `.__init__() [traffic_exec_engine_mtlscontextfactory_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 747 che coinvolge il nodo canonico `MtlsContextFactory` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 747 che coinvolge il nodo canonico `MtlsContextFactory` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 747 che coinvolge il nodo canonico `MtlsContextFactory` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 747 che coinvolge il nodo canonico `MtlsContextFactory` è `L381`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 747 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 747 che coinvolge il nodo canonico `MtlsContextFactory` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 747 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo unificato, l'arco 747 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo unificato, l'arco 747 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_init` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`.
<!-- GRAPH_EDGE graph=unificata index=748 role=outgoing -->
Nel grafo unificato, il nodo canonico `MtlsContextFactory` è la sorgente `source` della relazione `method` verso il nodo canonico `._load_pkcs12()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_load_pkcs12`. La relazione tecnica esplicita è la seguente: `MtlsContextFactory` registra come proprio metodo `._load_pkcs12()`. Nel grafo unificato, il tipo dichiarato dell'arco 748 che coinvolge il nodo canonico `MtlsContextFactory` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 748 che coinvolge il nodo canonico `MtlsContextFactory` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 748 che coinvolge il nodo canonico `MtlsContextFactory` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 748 che coinvolge il nodo canonico `MtlsContextFactory` è `L429`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 748 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 748 che coinvolge il nodo canonico `MtlsContextFactory` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 748 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo unificato, l'arco 748 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo unificato, l'arco 748 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_load_pkcs12` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`.
<!-- GRAPH_EDGE graph=unificata index=749 role=outgoing -->
Nel grafo unificato, il nodo canonico `MtlsContextFactory` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_374`. La relazione tecnica esplicita è la seguente: `MtlsContextFactory` costituisce una motivazione per `Crea e conserva SSLContext per profili mTLS.      La configurazione resta nel co`. Nel grafo unificato, il tipo dichiarato dell'arco 749 che coinvolge il nodo canonico `MtlsContextFactory` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 749 che coinvolge il nodo canonico `MtlsContextFactory` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 749 che coinvolge il nodo canonico `MtlsContextFactory` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 749 che coinvolge il nodo canonico `MtlsContextFactory` è `L374`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 749 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 749 che coinvolge il nodo canonico `MtlsContextFactory` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 749 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo unificato, l'arco 749 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo unificato, l'arco 749 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_374` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`.
<!-- GRAPH_EDGE graph=unificata index=750 role=outgoing -->
Nel grafo unificato, il nodo canonico `MtlsContextFactory` è la sorgente `source` della relazione `calls` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `MtlsContextFactory` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 750 che coinvolge il nodo canonico `MtlsContextFactory` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 750 che coinvolge il nodo canonico `MtlsContextFactory` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 750 che coinvolge il nodo canonico `MtlsContextFactory` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 750 che coinvolge il nodo canonico `MtlsContextFactory` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 750 che coinvolge il nodo canonico `MtlsContextFactory` è `L997`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 750 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 750 che coinvolge il nodo canonico `MtlsContextFactory` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 750 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo unificato, l'arco 750 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo unificato, l'arco 750 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`.
<!-- GRAPH_EDGE graph=unificata index=751 role=outgoing -->
Nel grafo unificato, il nodo canonico `MtlsContextFactory` è la sorgente `source` della relazione `references` verso il nodo canonico `send_request()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` all'ID `Request-Dataset-Generator::traffic_exec_engine_send_request`. La relazione tecnica esplicita è la seguente: `MtlsContextFactory` fa riferimento a `send_request()`. Nel grafo unificato, il tipo dichiarato dell'arco 751 che coinvolge il nodo canonico `MtlsContextFactory` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 751 che coinvolge il nodo canonico `MtlsContextFactory` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 751 che coinvolge il nodo canonico `MtlsContextFactory` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 751 che coinvolge il nodo canonico `MtlsContextFactory` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 751 che coinvolge il nodo canonico `MtlsContextFactory` è `L542`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 751 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 751 che coinvolge il nodo canonico `MtlsContextFactory` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 751 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo unificato, l'arco 751 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo unificato, l'arco 751 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`.
<!-- GRAPH_EDGE graph=unificata index=677 role=incoming -->
Nel grafo unificato, il nodo canonico `MtlsContextFactory` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `MtlsContextFactory`. Nel grafo unificato, il tipo dichiarato dell'arco 677 che coinvolge il nodo canonico `MtlsContextFactory` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 677 che coinvolge il nodo canonico `MtlsContextFactory` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 677 che coinvolge il nodo canonico `MtlsContextFactory` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 677 che coinvolge il nodo canonico `MtlsContextFactory` è `L373`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 677 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 677 che coinvolge il nodo canonico `MtlsContextFactory` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 677 che coinvolge il nodo canonico `MtlsContextFactory` è `1.0`. Nel grafo unificato, l'arco 677 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`. Nel grafo unificato, l'arco 677 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `MtlsContextFactory`.

### OutputExtractionError

<!-- GRAPH_NODE graph=codice index=339 id=traffic_exec_engine_outputextractionerror -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `OutputExtractionError` è `OutputExtractionError`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `OutputExtractionError` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `OutputExtractionError` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `OutputExtractionError` è `L47`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `OutputExtractionError` vale `ast`. Nel grafo del codice RDG, il nodo canonico `OutputExtractionError` possiede l'identificatore esatto `traffic_exec_engine_outputextractionerror`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `OutputExtractionError` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `OutputExtractionError` è `outputextractionerror`.
<!-- GRAPH_EDGE graph=codice index=693 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `OutputExtractionError` è la sorgente `source` della relazione `inherits` verso il nodo canonico `RuntimeError`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_outputextractionerror` all'ID `runtimeerror`. La relazione tecnica esplicita è la seguente: `OutputExtractionError` eredita da `RuntimeError`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 693 che coinvolge il nodo canonico `OutputExtractionError` è `inherits`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 693 che coinvolge il nodo canonico `OutputExtractionError` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 693 che coinvolge il nodo canonico `OutputExtractionError` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 693 che coinvolge il nodo canonico `OutputExtractionError` è `L47`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 693 che coinvolge il nodo canonico `OutputExtractionError` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 693 che coinvolge il nodo canonico `OutputExtractionError` è `ast`. Nel grafo del codice RDG, l'arco 693 registra come `source` l'identificatore `traffic_exec_engine_outputextractionerror` per la relazione che coinvolge il nodo canonico `OutputExtractionError`. Nel grafo del codice RDG, l'arco 693 registra come `target` l'identificatore `runtimeerror` per la relazione che coinvolge il nodo canonico `OutputExtractionError`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 693 che coinvolge il nodo canonico `OutputExtractionError` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=680 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `OutputExtractionError` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_outputextractionerror`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `OutputExtractionError`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 680 che coinvolge il nodo canonico `OutputExtractionError` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 680 che coinvolge il nodo canonico `OutputExtractionError` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 680 che coinvolge il nodo canonico `OutputExtractionError` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 680 che coinvolge il nodo canonico `OutputExtractionError` è `L47`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 680 che coinvolge il nodo canonico `OutputExtractionError` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 680 che coinvolge il nodo canonico `OutputExtractionError` è `ast`. Nel grafo del codice RDG, l'arco 680 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `OutputExtractionError`. Nel grafo del codice RDG, l'arco 680 registra come `target` l'identificatore `traffic_exec_engine_outputextractionerror` per la relazione che coinvolge il nodo canonico `OutputExtractionError`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 680 che coinvolge il nodo canonico `OutputExtractionError` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=692 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `OutputExtractionError` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_extract_xml_output_stdlib()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_extract_xml_output_stdlib` all'ID `traffic_exec_engine_outputextractionerror`. La relazione tecnica esplicita è la seguente: `_extract_xml_output_stdlib()` chiama `OutputExtractionError`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 692 che coinvolge il nodo canonico `OutputExtractionError` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 692 che coinvolge il nodo canonico `OutputExtractionError` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 692 che coinvolge il nodo canonico `OutputExtractionError` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 692 che coinvolge il nodo canonico `OutputExtractionError` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 692 che coinvolge il nodo canonico `OutputExtractionError` è `L270`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 692 che coinvolge il nodo canonico `OutputExtractionError` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 692 che coinvolge il nodo canonico `OutputExtractionError` è `ast`. Nel grafo del codice RDG, l'arco 692 registra come `source` l'identificatore `traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `OutputExtractionError`. Nel grafo del codice RDG, l'arco 692 registra come `target` l'identificatore `traffic_exec_engine_outputextractionerror` per la relazione che coinvolge il nodo canonico `OutputExtractionError`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 692 che coinvolge il nodo canonico `OutputExtractionError` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=694 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `OutputExtractionError` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Errore esplicito nella fase di estrazione output da response.`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_48` all'ID `traffic_exec_engine_outputextractionerror`. La relazione tecnica esplicita è la seguente: `Errore esplicito nella fase di estrazione output da response.` costituisce una motivazione per `OutputExtractionError`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 694 che coinvolge il nodo canonico `OutputExtractionError` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 694 che coinvolge il nodo canonico `OutputExtractionError` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 694 che coinvolge il nodo canonico `OutputExtractionError` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 694 che coinvolge il nodo canonico `OutputExtractionError` è `L48`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 694 che coinvolge il nodo canonico `OutputExtractionError` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 694 che coinvolge il nodo canonico `OutputExtractionError` è `ast`. Nel grafo del codice RDG, l'arco 694 registra come `source` l'identificatore `traffic_exec_engine_rationale_48` per la relazione che coinvolge il nodo canonico `OutputExtractionError`. Nel grafo del codice RDG, l'arco 694 registra come `target` l'identificatore `traffic_exec_engine_outputextractionerror` per la relazione che coinvolge il nodo canonico `OutputExtractionError`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 694 che coinvolge il nodo canonico `OutputExtractionError` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=695 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `OutputExtractionError` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_xml_raw_string()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_xml_raw_string` all'ID `traffic_exec_engine_outputextractionerror`. La relazione tecnica esplicita è la seguente: `_xml_raw_string()` chiama `OutputExtractionError`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 695 che coinvolge il nodo canonico `OutputExtractionError` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 695 che coinvolge il nodo canonico `OutputExtractionError` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 695 che coinvolge il nodo canonico `OutputExtractionError` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 695 che coinvolge il nodo canonico `OutputExtractionError` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 695 che coinvolge il nodo canonico `OutputExtractionError` è `L277`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 695 che coinvolge il nodo canonico `OutputExtractionError` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 695 che coinvolge il nodo canonico `OutputExtractionError` è `ast`. Nel grafo del codice RDG, l'arco 695 registra come `source` l'identificatore `traffic_exec_engine_xml_raw_string` per la relazione che coinvolge il nodo canonico `OutputExtractionError`. Nel grafo del codice RDG, l'arco 695 registra come `target` l'identificatore `traffic_exec_engine_outputextractionerror` per la relazione che coinvolge il nodo canonico `OutputExtractionError`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 695 che coinvolge il nodo canonico `OutputExtractionError` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=339 id=Request-Dataset-Generator::traffic_exec_engine_outputextractionerror -->
Nel grafo unificato, l'etichetta originale del nodo canonico `OutputExtractionError` è `OutputExtractionError`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `OutputExtractionError` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `OutputExtractionError` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `OutputExtractionError` è `L47`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `OutputExtractionError` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `OutputExtractionError` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `OutputExtractionError` è `outputextractionerror`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `OutputExtractionError` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `OutputExtractionError` è `traffic_exec_engine_outputextractionerror`. Nel grafo unificato, il nodo canonico `OutputExtractionError` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror`.
<!-- GRAPH_EDGE graph=unificata index=692 role=outgoing -->
Nel grafo unificato, il nodo canonico `OutputExtractionError` è la sorgente `source` della relazione `calls` verso il nodo canonico `_extract_xml_output_stdlib()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib`. La relazione tecnica esplicita è la seguente: `OutputExtractionError` chiama `_extract_xml_output_stdlib()`. Nel grafo unificato, il tipo dichiarato dell'arco 692 che coinvolge il nodo canonico `OutputExtractionError` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 692 che coinvolge il nodo canonico `OutputExtractionError` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 692 che coinvolge il nodo canonico `OutputExtractionError` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 692 che coinvolge il nodo canonico `OutputExtractionError` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 692 che coinvolge il nodo canonico `OutputExtractionError` è `L270`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 692 che coinvolge il nodo canonico `OutputExtractionError` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 692 che coinvolge il nodo canonico `OutputExtractionError` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 692 che coinvolge il nodo canonico `OutputExtractionError` è `1.0`. Nel grafo unificato, l'arco 692 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror` per la relazione che coinvolge il nodo canonico `OutputExtractionError`. Nel grafo unificato, l'arco 692 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `OutputExtractionError`.
<!-- GRAPH_EDGE graph=unificata index=693 role=outgoing -->
Nel grafo unificato, il nodo canonico `OutputExtractionError` è la sorgente `source` della relazione `inherits` verso il nodo canonico `RuntimeError`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror` all'ID `Request-Dataset-Generator::runtimeerror`. La relazione tecnica esplicita è la seguente: `OutputExtractionError` eredita da `RuntimeError`. Nel grafo unificato, il tipo dichiarato dell'arco 693 che coinvolge il nodo canonico `OutputExtractionError` è `inherits`. Nel grafo unificato, la confidenza categoriale dell'arco 693 che coinvolge il nodo canonico `OutputExtractionError` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 693 che coinvolge il nodo canonico `OutputExtractionError` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 693 che coinvolge il nodo canonico `OutputExtractionError` è `L47`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 693 che coinvolge il nodo canonico `OutputExtractionError` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 693 che coinvolge il nodo canonico `OutputExtractionError` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 693 che coinvolge il nodo canonico `OutputExtractionError` è `1.0`. Nel grafo unificato, l'arco 693 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror` per la relazione che coinvolge il nodo canonico `OutputExtractionError`. Nel grafo unificato, l'arco 693 registra come `target` l'identificatore `Request-Dataset-Generator::runtimeerror` per la relazione che coinvolge il nodo canonico `OutputExtractionError`.
<!-- GRAPH_EDGE graph=unificata index=694 role=outgoing -->
Nel grafo unificato, il nodo canonico `OutputExtractionError` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Errore esplicito nella fase di estrazione output da response.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_48`. La relazione tecnica esplicita è la seguente: `OutputExtractionError` costituisce una motivazione per `Errore esplicito nella fase di estrazione output da response.`. Nel grafo unificato, il tipo dichiarato dell'arco 694 che coinvolge il nodo canonico `OutputExtractionError` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 694 che coinvolge il nodo canonico `OutputExtractionError` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 694 che coinvolge il nodo canonico `OutputExtractionError` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 694 che coinvolge il nodo canonico `OutputExtractionError` è `L48`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 694 che coinvolge il nodo canonico `OutputExtractionError` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 694 che coinvolge il nodo canonico `OutputExtractionError` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 694 che coinvolge il nodo canonico `OutputExtractionError` è `1.0`. Nel grafo unificato, l'arco 694 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror` per la relazione che coinvolge il nodo canonico `OutputExtractionError`. Nel grafo unificato, l'arco 694 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_48` per la relazione che coinvolge il nodo canonico `OutputExtractionError`.
<!-- GRAPH_EDGE graph=unificata index=695 role=outgoing -->
Nel grafo unificato, il nodo canonico `OutputExtractionError` è la sorgente `source` della relazione `calls` verso il nodo canonico `_xml_raw_string()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror` all'ID `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string`. La relazione tecnica esplicita è la seguente: `OutputExtractionError` chiama `_xml_raw_string()`. Nel grafo unificato, il tipo dichiarato dell'arco 695 che coinvolge il nodo canonico `OutputExtractionError` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 695 che coinvolge il nodo canonico `OutputExtractionError` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 695 che coinvolge il nodo canonico `OutputExtractionError` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 695 che coinvolge il nodo canonico `OutputExtractionError` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 695 che coinvolge il nodo canonico `OutputExtractionError` è `L277`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 695 che coinvolge il nodo canonico `OutputExtractionError` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 695 che coinvolge il nodo canonico `OutputExtractionError` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 695 che coinvolge il nodo canonico `OutputExtractionError` è `1.0`. Nel grafo unificato, l'arco 695 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror` per la relazione che coinvolge il nodo canonico `OutputExtractionError`. Nel grafo unificato, l'arco 695 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string` per la relazione che coinvolge il nodo canonico `OutputExtractionError`.
<!-- GRAPH_EDGE graph=unificata index=680 role=incoming -->
Nel grafo unificato, il nodo canonico `OutputExtractionError` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `OutputExtractionError`. Nel grafo unificato, il tipo dichiarato dell'arco 680 che coinvolge il nodo canonico `OutputExtractionError` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 680 che coinvolge il nodo canonico `OutputExtractionError` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 680 che coinvolge il nodo canonico `OutputExtractionError` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 680 che coinvolge il nodo canonico `OutputExtractionError` è `L47`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 680 che coinvolge il nodo canonico `OutputExtractionError` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 680 che coinvolge il nodo canonico `OutputExtractionError` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 680 che coinvolge il nodo canonico `OutputExtractionError` è `1.0`. Nel grafo unificato, l'arco 680 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `OutputExtractionError`. Nel grafo unificato, l'arco 680 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror` per la relazione che coinvolge il nodo canonico `OutputExtractionError`.

### PlaceholderTest

<!-- GRAPH_NODE graph=codice index=288 id=tests_test_core_placeholdertest -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `PlaceholderTest` è `PlaceholderTest`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `PlaceholderTest` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `PlaceholderTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `PlaceholderTest` è `L239`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `PlaceholderTest` vale `ast`. Nel grafo del codice RDG, il nodo canonico `PlaceholderTest` possiede l'identificatore esatto `tests_test_core_placeholdertest`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `PlaceholderTest` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `PlaceholderTest` è `placeholdertest`.
<!-- GRAPH_EDGE graph=codice index=444 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `PlaceholderTest` è la sorgente `source` della relazione `uses` verso il nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_placeholdertest` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `PlaceholderTest` usa `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 444 che coinvolge il nodo canonico `PlaceholderTest` è `uses`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 444 che coinvolge il nodo canonico `PlaceholderTest` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 444 che coinvolge il nodo canonico `PlaceholderTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 444 che coinvolge il nodo canonico `PlaceholderTest` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 444 che coinvolge il nodo canonico `PlaceholderTest` è `0.8`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 444 che coinvolge il nodo canonico `PlaceholderTest` è `ast`. Nel grafo del codice RDG, l'arco 444 registra come `source` l'identificatore `tests_test_core_placeholdertest` per la relazione che coinvolge il nodo canonico `PlaceholderTest`. Nel grafo del codice RDG, l'arco 444 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `PlaceholderTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 444 che coinvolge il nodo canonico `PlaceholderTest` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=597 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `PlaceholderTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_detects_unresolved_placeholders()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_placeholdertest` all'ID `tests_test_core_placeholdertest_test_detects_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `PlaceholderTest` registra come proprio metodo `.test_detects_unresolved_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 597 che coinvolge il nodo canonico `PlaceholderTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 597 che coinvolge il nodo canonico `PlaceholderTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 597 che coinvolge il nodo canonico `PlaceholderTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 597 che coinvolge il nodo canonico `PlaceholderTest` è `L272`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 597 che coinvolge il nodo canonico `PlaceholderTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 597 che coinvolge il nodo canonico `PlaceholderTest` è `ast`. Nel grafo del codice RDG, l'arco 597 registra come `source` l'identificatore `tests_test_core_placeholdertest` per la relazione che coinvolge il nodo canonico `PlaceholderTest`. Nel grafo del codice RDG, l'arco 597 registra come `target` l'identificatore `tests_test_core_placeholdertest_test_detects_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `PlaceholderTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 597 che coinvolge il nodo canonico `PlaceholderTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=598 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `PlaceholderTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_placeholdertest` all'ID `tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback`. La relazione tecnica esplicita è la seguente: `PlaceholderTest` registra come proprio metodo `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 598 che coinvolge il nodo canonico `PlaceholderTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 598 che coinvolge il nodo canonico `PlaceholderTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 598 che coinvolge il nodo canonico `PlaceholderTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 598 che coinvolge il nodo canonico `PlaceholderTest` è `L253`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 598 che coinvolge il nodo canonico `PlaceholderTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 598 che coinvolge il nodo canonico `PlaceholderTest` è `ast`. Nel grafo del codice RDG, l'arco 598 registra come `source` l'identificatore `tests_test_core_placeholdertest` per la relazione che coinvolge il nodo canonico `PlaceholderTest`. Nel grafo del codice RDG, l'arco 598 registra come `target` l'identificatore `tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback` per la relazione che coinvolge il nodo canonico `PlaceholderTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 598 che coinvolge il nodo canonico `PlaceholderTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=599 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `PlaceholderTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_placeholdertest` all'ID `tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body`. La relazione tecnica esplicita è la seguente: `PlaceholderTest` registra come proprio metodo `.test_resolves_placeholders_in_url_headers_and_body()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 599 che coinvolge il nodo canonico `PlaceholderTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 599 che coinvolge il nodo canonico `PlaceholderTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 599 che coinvolge il nodo canonico `PlaceholderTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 599 che coinvolge il nodo canonico `PlaceholderTest` è `L240`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 599 che coinvolge il nodo canonico `PlaceholderTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 599 che coinvolge il nodo canonico `PlaceholderTest` è `ast`. Nel grafo del codice RDG, l'arco 599 registra come `source` l'identificatore `tests_test_core_placeholdertest` per la relazione che coinvolge il nodo canonico `PlaceholderTest`. Nel grafo del codice RDG, l'arco 599 registra come `target` l'identificatore `tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body` per la relazione che coinvolge il nodo canonico `PlaceholderTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 599 che coinvolge il nodo canonico `PlaceholderTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=583 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `PlaceholderTest` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `tests_test_core_placeholdertest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `PlaceholderTest`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 583 che coinvolge il nodo canonico `PlaceholderTest` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 583 che coinvolge il nodo canonico `PlaceholderTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 583 che coinvolge il nodo canonico `PlaceholderTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 583 che coinvolge il nodo canonico `PlaceholderTest` è `L239`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 583 che coinvolge il nodo canonico `PlaceholderTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 583 che coinvolge il nodo canonico `PlaceholderTest` è `ast`. Nel grafo del codice RDG, l'arco 583 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `PlaceholderTest`. Nel grafo del codice RDG, l'arco 583 registra come `target` l'identificatore `tests_test_core_placeholdertest` per la relazione che coinvolge il nodo canonico `PlaceholderTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 583 che coinvolge il nodo canonico `PlaceholderTest` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=288 id=Request-Dataset-Generator::tests_test_core_placeholdertest -->
Nel grafo unificato, l'etichetta originale del nodo canonico `PlaceholderTest` è `PlaceholderTest`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `PlaceholderTest` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `PlaceholderTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `PlaceholderTest` è `L239`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `PlaceholderTest` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `PlaceholderTest` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `PlaceholderTest` è `placeholdertest`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `PlaceholderTest` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `PlaceholderTest` è `tests_test_core_placeholdertest`. Nel grafo unificato, il nodo canonico `PlaceholderTest` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_placeholdertest`.
<!-- GRAPH_EDGE graph=unificata index=597 role=outgoing -->
Nel grafo unificato, il nodo canonico `PlaceholderTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_detects_unresolved_placeholders()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_placeholdertest` all'ID `Request-Dataset-Generator::tests_test_core_placeholdertest_test_detects_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `PlaceholderTest` registra come proprio metodo `.test_detects_unresolved_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 597 che coinvolge il nodo canonico `PlaceholderTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 597 che coinvolge il nodo canonico `PlaceholderTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 597 che coinvolge il nodo canonico `PlaceholderTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 597 che coinvolge il nodo canonico `PlaceholderTest` è `L272`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 597 che coinvolge il nodo canonico `PlaceholderTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 597 che coinvolge il nodo canonico `PlaceholderTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 597 che coinvolge il nodo canonico `PlaceholderTest` è `1.0`. Nel grafo unificato, l'arco 597 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest` per la relazione che coinvolge il nodo canonico `PlaceholderTest`. Nel grafo unificato, l'arco 597 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest_test_detects_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `PlaceholderTest`.
<!-- GRAPH_EDGE graph=unificata index=598 role=outgoing -->
Nel grafo unificato, il nodo canonico `PlaceholderTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_placeholdertest` all'ID `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback`. La relazione tecnica esplicita è la seguente: `PlaceholderTest` registra come proprio metodo `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`. Nel grafo unificato, il tipo dichiarato dell'arco 598 che coinvolge il nodo canonico `PlaceholderTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 598 che coinvolge il nodo canonico `PlaceholderTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 598 che coinvolge il nodo canonico `PlaceholderTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 598 che coinvolge il nodo canonico `PlaceholderTest` è `L253`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 598 che coinvolge il nodo canonico `PlaceholderTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 598 che coinvolge il nodo canonico `PlaceholderTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 598 che coinvolge il nodo canonico `PlaceholderTest` è `1.0`. Nel grafo unificato, l'arco 598 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest` per la relazione che coinvolge il nodo canonico `PlaceholderTest`. Nel grafo unificato, l'arco 598 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback` per la relazione che coinvolge il nodo canonico `PlaceholderTest`.
<!-- GRAPH_EDGE graph=unificata index=599 role=outgoing -->
Nel grafo unificato, il nodo canonico `PlaceholderTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_placeholdertest` all'ID `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body`. La relazione tecnica esplicita è la seguente: `PlaceholderTest` registra come proprio metodo `.test_resolves_placeholders_in_url_headers_and_body()`. Nel grafo unificato, il tipo dichiarato dell'arco 599 che coinvolge il nodo canonico `PlaceholderTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 599 che coinvolge il nodo canonico `PlaceholderTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 599 che coinvolge il nodo canonico `PlaceholderTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 599 che coinvolge il nodo canonico `PlaceholderTest` è `L240`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 599 che coinvolge il nodo canonico `PlaceholderTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 599 che coinvolge il nodo canonico `PlaceholderTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 599 che coinvolge il nodo canonico `PlaceholderTest` è `1.0`. Nel grafo unificato, l'arco 599 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest` per la relazione che coinvolge il nodo canonico `PlaceholderTest`. Nel grafo unificato, l'arco 599 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body` per la relazione che coinvolge il nodo canonico `PlaceholderTest`.
<!-- GRAPH_EDGE graph=unificata index=444 role=incoming -->
Nel grafo unificato, il nodo canonico `PlaceholderTest` è il destinatario `target` della relazione `uses` proveniente dal nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_placeholdertest`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` usa `PlaceholderTest`. Nel grafo unificato, il tipo dichiarato dell'arco 444 che coinvolge il nodo canonico `PlaceholderTest` è `uses`. Nel grafo unificato, la confidenza categoriale dell'arco 444 che coinvolge il nodo canonico `PlaceholderTest` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 444 che coinvolge il nodo canonico `PlaceholderTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 444 che coinvolge il nodo canonico `PlaceholderTest` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 444 che coinvolge il nodo canonico `PlaceholderTest` è `0.8`. Nel grafo unificato, la provenienza `_origin` dell'arco 444 che coinvolge il nodo canonico `PlaceholderTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 444 che coinvolge il nodo canonico `PlaceholderTest` è `0.5`. Nel grafo unificato, l'arco 444 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `PlaceholderTest`. Nel grafo unificato, l'arco 444 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest` per la relazione che coinvolge il nodo canonico `PlaceholderTest`.
<!-- GRAPH_EDGE graph=unificata index=583 role=incoming -->
Nel grafo unificato, il nodo canonico `PlaceholderTest` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::tests_test_core_placeholdertest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `PlaceholderTest`. Nel grafo unificato, il tipo dichiarato dell'arco 583 che coinvolge il nodo canonico `PlaceholderTest` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 583 che coinvolge il nodo canonico `PlaceholderTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 583 che coinvolge il nodo canonico `PlaceholderTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 583 che coinvolge il nodo canonico `PlaceholderTest` è `L239`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 583 che coinvolge il nodo canonico `PlaceholderTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 583 che coinvolge il nodo canonico `PlaceholderTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 583 che coinvolge il nodo canonico `PlaceholderTest` è `1.0`. Nel grafo unificato, l'arco 583 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `PlaceholderTest`. Nel grafo unificato, l'arco 583 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest` per la relazione che coinvolge il nodo canonico `PlaceholderTest`.

### resolve_placeholders()

<!-- GRAPH_NODE graph=codice index=347 id=traffic_exec_engine_resolve_placeholders -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `resolve_placeholders()` è `resolve_placeholders()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `resolve_placeholders()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `resolve_placeholders()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `resolve_placeholders()` è `L87`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `resolve_placeholders()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `resolve_placeholders()` possiede l'identificatore esatto `traffic_exec_engine_resolve_placeholders`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `resolve_placeholders()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `resolve_placeholders()` è `resolve_placeholders()`.
<!-- GRAPH_EDGE graph=codice index=716 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `resolve_placeholders()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_resolve_placeholders` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `resolve_placeholders()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 716 che coinvolge il nodo canonico `resolve_placeholders()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 716 che coinvolge il nodo canonico `resolve_placeholders()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 716 che coinvolge il nodo canonico `resolve_placeholders()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 716 che coinvolge il nodo canonico `resolve_placeholders()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 716 che coinvolge il nodo canonico `resolve_placeholders()` è `L114`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 716 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 716 che coinvolge il nodo canonico `resolve_placeholders()` è `ast`. Nel grafo del codice RDG, l'arco 716 registra come `source` l'identificatore `traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`. Nel grafo del codice RDG, l'arco 716 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 716 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=590 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `resolve_placeholders()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `traffic_exec_engine_resolve_placeholders`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `resolve_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 590 che coinvolge il nodo canonico `resolve_placeholders()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 590 che coinvolge il nodo canonico `resolve_placeholders()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 590 che coinvolge il nodo canonico `resolve_placeholders()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 590 che coinvolge il nodo canonico `resolve_placeholders()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 590 che coinvolge il nodo canonico `resolve_placeholders()` è `L25`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 590 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 590 che coinvolge il nodo canonico `resolve_placeholders()` è `ast`. Nel grafo del codice RDG, l'arco 590 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`. Nel grafo del codice RDG, l'arco 590 registra come `target` l'identificatore `traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 590 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=600 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `resolve_placeholders()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body` all'ID `traffic_exec_engine_resolve_placeholders`. La relazione tecnica esplicita è la seguente: `.test_resolves_placeholders_in_url_headers_and_body()` chiama `resolve_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 600 che coinvolge il nodo canonico `resolve_placeholders()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 600 che coinvolge il nodo canonico `resolve_placeholders()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 600 che coinvolge il nodo canonico `resolve_placeholders()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 600 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 600 che coinvolge il nodo canonico `resolve_placeholders()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 600 che coinvolge il nodo canonico `resolve_placeholders()` è `L246`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 600 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 600 che coinvolge il nodo canonico `resolve_placeholders()` è `ast`. Nel grafo del codice RDG, l'arco 600 registra come `source` l'identificatore `tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`. Nel grafo del codice RDG, l'arco 600 registra come `target` l'identificatore `traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`.
<!-- GRAPH_EDGE graph=codice index=602 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `resolve_placeholders()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback` all'ID `traffic_exec_engine_resolve_placeholders`. La relazione tecnica esplicita è la seguente: `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` chiama `resolve_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 602 che coinvolge il nodo canonico `resolve_placeholders()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 602 che coinvolge il nodo canonico `resolve_placeholders()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 602 che coinvolge il nodo canonico `resolve_placeholders()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 602 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 602 che coinvolge il nodo canonico `resolve_placeholders()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 602 che coinvolge il nodo canonico `resolve_placeholders()` è `L260`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 602 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 602 che coinvolge il nodo canonico `resolve_placeholders()` è `ast`. Nel grafo del codice RDG, l'arco 602 registra come `source` l'identificatore `tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`. Nel grafo del codice RDG, l'arco 602 registra come `target` l'identificatore `traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`.
<!-- GRAPH_EDGE graph=codice index=682 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `resolve_placeholders()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_resolve_placeholders`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `resolve_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 682 che coinvolge il nodo canonico `resolve_placeholders()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 682 che coinvolge il nodo canonico `resolve_placeholders()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 682 che coinvolge il nodo canonico `resolve_placeholders()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 682 che coinvolge il nodo canonico `resolve_placeholders()` è `L87`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 682 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 682 che coinvolge il nodo canonico `resolve_placeholders()` è `ast`. Nel grafo del codice RDG, l'arco 682 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`. Nel grafo del codice RDG, l'arco 682 registra come `target` l'identificatore `traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 682 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=714 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `resolve_placeholders()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_resolve_placeholders`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `resolve_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 714 che coinvolge il nodo canonico `resolve_placeholders()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 714 che coinvolge il nodo canonico `resolve_placeholders()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 714 che coinvolge il nodo canonico `resolve_placeholders()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 714 che coinvolge il nodo canonico `resolve_placeholders()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 714 che coinvolge il nodo canonico `resolve_placeholders()` è `L854`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 714 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 714 che coinvolge il nodo canonico `resolve_placeholders()` è `ast`. Nel grafo del codice RDG, l'arco 714 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`. Nel grafo del codice RDG, l'arco 714 registra come `target` l'identificatore `traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 714 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=715 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `resolve_placeholders()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_88` all'ID `traffic_exec_engine_resolve_placeholders`. La relazione tecnica esplicita è la seguente: `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` costituisce una motivazione per `resolve_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 715 che coinvolge il nodo canonico `resolve_placeholders()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 715 che coinvolge il nodo canonico `resolve_placeholders()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 715 che coinvolge il nodo canonico `resolve_placeholders()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 715 che coinvolge il nodo canonico `resolve_placeholders()` è `L88`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 715 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 715 che coinvolge il nodo canonico `resolve_placeholders()` è `ast`. Nel grafo del codice RDG, l'arco 715 registra come `source` l'identificatore `traffic_exec_engine_rationale_88` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`. Nel grafo del codice RDG, l'arco 715 registra come `target` l'identificatore `traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 715 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=347 id=Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders -->
Nel grafo unificato, l'etichetta originale del nodo canonico `resolve_placeholders()` è `resolve_placeholders()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `resolve_placeholders()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `resolve_placeholders()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `resolve_placeholders()` è `L87`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `resolve_placeholders()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `resolve_placeholders()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `resolve_placeholders()` è `resolve_placeholders()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `resolve_placeholders()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `resolve_placeholders()` è `traffic_exec_engine_resolve_placeholders`. Nel grafo unificato, il nodo canonico `resolve_placeholders()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders`.
<!-- GRAPH_EDGE graph=unificata index=714 role=outgoing -->
Nel grafo unificato, il nodo canonico `resolve_placeholders()` è la sorgente `source` della relazione `calls` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `resolve_placeholders()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 714 che coinvolge il nodo canonico `resolve_placeholders()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 714 che coinvolge il nodo canonico `resolve_placeholders()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 714 che coinvolge il nodo canonico `resolve_placeholders()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 714 che coinvolge il nodo canonico `resolve_placeholders()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 714 che coinvolge il nodo canonico `resolve_placeholders()` è `L854`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 714 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 714 che coinvolge il nodo canonico `resolve_placeholders()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 714 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo unificato, l'arco 714 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`. Nel grafo unificato, l'arco 714 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`.
<!-- GRAPH_EDGE graph=unificata index=715 role=outgoing -->
Nel grafo unificato, il nodo canonico `resolve_placeholders()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_88`. La relazione tecnica esplicita è la seguente: `resolve_placeholders()` costituisce una motivazione per `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand`. Nel grafo unificato, il tipo dichiarato dell'arco 715 che coinvolge il nodo canonico `resolve_placeholders()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 715 che coinvolge il nodo canonico `resolve_placeholders()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 715 che coinvolge il nodo canonico `resolve_placeholders()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 715 che coinvolge il nodo canonico `resolve_placeholders()` è `L88`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 715 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 715 che coinvolge il nodo canonico `resolve_placeholders()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 715 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo unificato, l'arco 715 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`. Nel grafo unificato, l'arco 715 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_88` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`.
<!-- GRAPH_EDGE graph=unificata index=716 role=outgoing -->
Nel grafo unificato, il nodo canonico `resolve_placeholders()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `resolve_placeholders()` chiama `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 716 che coinvolge il nodo canonico `resolve_placeholders()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 716 che coinvolge il nodo canonico `resolve_placeholders()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 716 che coinvolge il nodo canonico `resolve_placeholders()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 716 che coinvolge il nodo canonico `resolve_placeholders()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 716 che coinvolge il nodo canonico `resolve_placeholders()` è `L114`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 716 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 716 che coinvolge il nodo canonico `resolve_placeholders()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 716 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo unificato, l'arco 716 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`. Nel grafo unificato, l'arco 716 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`.
<!-- GRAPH_EDGE graph=unificata index=590 role=incoming -->
Nel grafo unificato, il nodo canonico `resolve_placeholders()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `resolve_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 590 che coinvolge il nodo canonico `resolve_placeholders()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 590 che coinvolge il nodo canonico `resolve_placeholders()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 590 che coinvolge il nodo canonico `resolve_placeholders()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 590 che coinvolge il nodo canonico `resolve_placeholders()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 590 che coinvolge il nodo canonico `resolve_placeholders()` è `L25`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 590 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 590 che coinvolge il nodo canonico `resolve_placeholders()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 590 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo unificato, l'arco 590 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`. Nel grafo unificato, l'arco 590 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`.
<!-- GRAPH_EDGE graph=unificata index=600 role=incoming -->
Nel grafo unificato, il nodo canonico `resolve_placeholders()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body` all'ID `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders`. La relazione tecnica esplicita è la seguente: `.test_resolves_placeholders_in_url_headers_and_body()` chiama `resolve_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 600 che coinvolge il nodo canonico `resolve_placeholders()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 600 che coinvolge il nodo canonico `resolve_placeholders()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 600 che coinvolge il nodo canonico `resolve_placeholders()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 600 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 600 che coinvolge il nodo canonico `resolve_placeholders()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 600 che coinvolge il nodo canonico `resolve_placeholders()` è `L246`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 600 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 600 che coinvolge il nodo canonico `resolve_placeholders()` è `ast`. Nel grafo unificato, l'arco 600 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`. Nel grafo unificato, l'arco 600 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`.
<!-- GRAPH_EDGE graph=unificata index=602 role=incoming -->
Nel grafo unificato, il nodo canonico `resolve_placeholders()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback` all'ID `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders`. La relazione tecnica esplicita è la seguente: `.test_resolves_placeholders_from_step_scoped_context_with_flat_fallback()` chiama `resolve_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 602 che coinvolge il nodo canonico `resolve_placeholders()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 602 che coinvolge il nodo canonico `resolve_placeholders()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 602 che coinvolge il nodo canonico `resolve_placeholders()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 602 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 602 che coinvolge il nodo canonico `resolve_placeholders()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 602 che coinvolge il nodo canonico `resolve_placeholders()` è `L260`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 602 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 602 che coinvolge il nodo canonico `resolve_placeholders()` è `ast`. Nel grafo unificato, l'arco 602 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_from_step_scoped_context_with_flat_fallback` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`. Nel grafo unificato, l'arco 602 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`.
<!-- GRAPH_EDGE graph=unificata index=682 role=incoming -->
Nel grafo unificato, il nodo canonico `resolve_placeholders()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `resolve_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 682 che coinvolge il nodo canonico `resolve_placeholders()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 682 che coinvolge il nodo canonico `resolve_placeholders()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 682 che coinvolge il nodo canonico `resolve_placeholders()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 682 che coinvolge il nodo canonico `resolve_placeholders()` è `L87`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 682 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 682 che coinvolge il nodo canonico `resolve_placeholders()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 682 che coinvolge il nodo canonico `resolve_placeholders()` è `1.0`. Nel grafo unificato, l'arco 682 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`. Nel grafo unificato, l'arco 682 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `resolve_placeholders()`.

### Rimuove solo whitespace tra tag, preservando testo e attributi.

<!-- GRAPH_NODE graph=codice index=400 id=traffic_exec_engine_rationale_286 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `Rimuove solo whitespace tra tag, preservando testo e attributi.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `L286`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` possiede l'identificatore esatto `traffic_exec_engine_rationale_286`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `rimuove solo whitespace tra tag, preservando testo e attributi.`.
<!-- GRAPH_EDGE graph=codice index=735 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `_linearize_xml()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_286` all'ID `traffic_exec_engine_linearize_xml`. La relazione tecnica esplicita è la seguente: `Rimuove solo whitespace tra tag, preservando testo e attributi.` costituisce una motivazione per `_linearize_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 735 che coinvolge il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 735 che coinvolge il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 735 che coinvolge il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 735 che coinvolge il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `L286`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 735 che coinvolge il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 735 che coinvolge il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `ast`. Nel grafo del codice RDG, l'arco 735 registra come `source` l'identificatore `traffic_exec_engine_rationale_286` per la relazione che coinvolge il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.`. Nel grafo del codice RDG, l'arco 735 registra come `target` l'identificatore `traffic_exec_engine_linearize_xml` per la relazione che coinvolge il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 735 che coinvolge il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=400 id=Request-Dataset-Generator::traffic_exec_engine_rationale_286 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `Rimuove solo whitespace tra tag, preservando testo e attributi.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `L286`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `rimuove solo whitespace tra tag, preservando testo e attributi.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `traffic_exec_engine_rationale_286`. Nel grafo unificato, il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_rationale_286`.
Nel grafo unificato, il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=735 role=incoming -->
Nel grafo unificato, il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `_linearize_xml()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_linearize_xml` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_286`. La relazione tecnica esplicita è la seguente: `_linearize_xml()` costituisce una motivazione per `Rimuove solo whitespace tra tag, preservando testo e attributi.`. Nel grafo unificato, il tipo dichiarato dell'arco 735 che coinvolge il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 735 che coinvolge il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 735 che coinvolge il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 735 che coinvolge il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `L286`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 735 che coinvolge il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 735 che coinvolge il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 735 che coinvolge il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.` è `1.0`. Nel grafo unificato, l'arco 735 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_linearize_xml` per la relazione che coinvolge il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.`. Nel grafo unificato, l'arco 735 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_286` per la relazione che coinvolge il nodo canonico `Rimuove solo whitespace tra tag, preservando testo e attributi.`.

### Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand

<!-- GRAPH_NODE graph=codice index=397 id=traffic_exec_engine_rationale_88 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `L88`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` possiede l'identificatore esatto `traffic_exec_engine_rationale_88`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `risolve i placeholder {{step_n.output_key}} nei campi dello step,     modificand`.
<!-- GRAPH_EDGE graph=codice index=715 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `resolve_placeholders()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_88` all'ID `traffic_exec_engine_resolve_placeholders`. La relazione tecnica esplicita è la seguente: `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` costituisce una motivazione per `resolve_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 715 che coinvolge il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 715 che coinvolge il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 715 che coinvolge il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 715 che coinvolge il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `L88`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 715 che coinvolge il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 715 che coinvolge il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `ast`. Nel grafo del codice RDG, l'arco 715 registra come `source` l'identificatore `traffic_exec_engine_rationale_88` per la relazione che coinvolge il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand`. Nel grafo del codice RDG, l'arco 715 registra come `target` l'identificatore `traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 715 che coinvolge il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=397 id=Request-Dataset-Generator::traffic_exec_engine_rationale_88 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `L88`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `risolve i placeholder {{step_n.output_key}} nei campi dello step,     modificand`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `traffic_exec_engine_rationale_88`. Nel grafo unificato, il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_rationale_88`.
Nel grafo unificato, il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=715 role=incoming -->
Nel grafo unificato, il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `resolve_placeholders()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_88`. La relazione tecnica esplicita è la seguente: `resolve_placeholders()` costituisce una motivazione per `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand`. Nel grafo unificato, il tipo dichiarato dell'arco 715 che coinvolge il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 715 che coinvolge il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 715 che coinvolge il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 715 che coinvolge il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `L88`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 715 che coinvolge il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 715 che coinvolge il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 715 che coinvolge il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand` è `1.0`. Nel grafo unificato, l'arco 715 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand`. Nel grafo unificato, l'arco 715 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_88` per la relazione che coinvolge il nodo canonico `Risolve i placeholder {{step_N.output_key}} nei campi dello step,     modificand`.

### Ritorna i placeholder ancora presenti nei campi inviati su rete.

<!-- GRAPH_NODE graph=codice index=398 id=traffic_exec_engine_rationale_135 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `Ritorna i placeholder ancora presenti nei campi inviati su rete.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `L135`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` possiede l'identificatore esatto `traffic_exec_engine_rationale_135`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `ritorna i placeholder ancora presenti nei campi inviati su rete.`.
<!-- GRAPH_EDGE graph=codice index=718 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `unresolved_placeholders()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_135` all'ID `traffic_exec_engine_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `Ritorna i placeholder ancora presenti nei campi inviati su rete.` costituisce una motivazione per `unresolved_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 718 che coinvolge il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 718 che coinvolge il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 718 che coinvolge il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 718 che coinvolge il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `L135`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 718 che coinvolge il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 718 che coinvolge il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `ast`. Nel grafo del codice RDG, l'arco 718 registra come `source` l'identificatore `traffic_exec_engine_rationale_135` per la relazione che coinvolge il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.`. Nel grafo del codice RDG, l'arco 718 registra come `target` l'identificatore `traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 718 che coinvolge il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=398 id=Request-Dataset-Generator::traffic_exec_engine_rationale_135 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `Ritorna i placeholder ancora presenti nei campi inviati su rete.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `L135`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `ritorna i placeholder ancora presenti nei campi inviati su rete.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `traffic_exec_engine_rationale_135`. Nel grafo unificato, il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_rationale_135`.
Nel grafo unificato, il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=718 role=incoming -->
Nel grafo unificato, il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `unresolved_placeholders()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_135`. La relazione tecnica esplicita è la seguente: `unresolved_placeholders()` costituisce una motivazione per `Ritorna i placeholder ancora presenti nei campi inviati su rete.`. Nel grafo unificato, il tipo dichiarato dell'arco 718 che coinvolge il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 718 che coinvolge il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 718 che coinvolge il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 718 che coinvolge il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `L135`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 718 che coinvolge il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 718 che coinvolge il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 718 che coinvolge il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.` è `1.0`. Nel grafo unificato, l'arco 718 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.`. Nel grafo unificato, l'arco 718 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_135` per la relazione che coinvolge il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.`.

### RuntimeError

<!-- GRAPH_NODE graph=codice index=340 id=runtimeerror -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `RuntimeError` è `RuntimeError`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `RuntimeError` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `RuntimeError` è ``. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `RuntimeError` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `RuntimeError` vale `ast`. Nel grafo del codice RDG, il nodo canonico `RuntimeError` possiede l'identificatore esatto `runtimeerror`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `RuntimeError` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `RuntimeError` è `runtimeerror`.
Nel grafo del codice RDG, il nodo canonico `RuntimeError` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=693 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `RuntimeError` è il destinatario `target` della relazione `inherits` proveniente dal nodo canonico `OutputExtractionError`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_outputextractionerror` all'ID `runtimeerror`. La relazione tecnica esplicita è la seguente: `OutputExtractionError` eredita da `RuntimeError`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 693 che coinvolge il nodo canonico `RuntimeError` è `inherits`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 693 che coinvolge il nodo canonico `RuntimeError` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 693 che coinvolge il nodo canonico `RuntimeError` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 693 che coinvolge il nodo canonico `RuntimeError` è `L47`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 693 che coinvolge il nodo canonico `RuntimeError` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 693 che coinvolge il nodo canonico `RuntimeError` è `ast`. Nel grafo del codice RDG, l'arco 693 registra come `source` l'identificatore `traffic_exec_engine_outputextractionerror` per la relazione che coinvolge il nodo canonico `RuntimeError`. Nel grafo del codice RDG, l'arco 693 registra come `target` l'identificatore `runtimeerror` per la relazione che coinvolge il nodo canonico `RuntimeError`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 693 che coinvolge il nodo canonico `RuntimeError` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=696 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `RuntimeError` è il destinatario `target` della relazione `inherits` proveniente dal nodo canonico `UnresolvedPlaceholderError`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_unresolvedplaceholdererror` all'ID `runtimeerror`. La relazione tecnica esplicita è la seguente: `UnresolvedPlaceholderError` eredita da `RuntimeError`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 696 che coinvolge il nodo canonico `RuntimeError` è `inherits`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 696 che coinvolge il nodo canonico `RuntimeError` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 696 che coinvolge il nodo canonico `RuntimeError` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 696 che coinvolge il nodo canonico `RuntimeError` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 696 che coinvolge il nodo canonico `RuntimeError` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 696 che coinvolge il nodo canonico `RuntimeError` è `ast`. Nel grafo del codice RDG, l'arco 696 registra come `source` l'identificatore `traffic_exec_engine_unresolvedplaceholdererror` per la relazione che coinvolge il nodo canonico `RuntimeError`. Nel grafo del codice RDG, l'arco 696 registra come `target` l'identificatore `runtimeerror` per la relazione che coinvolge il nodo canonico `RuntimeError`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 696 che coinvolge il nodo canonico `RuntimeError` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=340 id=Request-Dataset-Generator::runtimeerror -->
Nel grafo unificato, l'etichetta originale del nodo canonico `RuntimeError` è `RuntimeError`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `RuntimeError` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `RuntimeError` è ``. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `RuntimeError` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `RuntimeError` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `RuntimeError` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `RuntimeError` è `runtimeerror`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `RuntimeError` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `RuntimeError` è `runtimeerror`. Nel grafo unificato, il nodo canonico `RuntimeError` possiede l'identificatore esatto `Request-Dataset-Generator::runtimeerror`.
<!-- GRAPH_EDGE graph=unificata index=696 role=outgoing -->
Nel grafo unificato, il nodo canonico `RuntimeError` è la sorgente `source` della relazione `inherits` verso il nodo canonico `UnresolvedPlaceholderError`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::runtimeerror` all'ID `Request-Dataset-Generator::traffic_exec_engine_unresolvedplaceholdererror`. La relazione tecnica esplicita è la seguente: `RuntimeError` eredita da `UnresolvedPlaceholderError`. Nel grafo unificato, il tipo dichiarato dell'arco 696 che coinvolge il nodo canonico `RuntimeError` è `inherits`. Nel grafo unificato, la confidenza categoriale dell'arco 696 che coinvolge il nodo canonico `RuntimeError` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 696 che coinvolge il nodo canonico `RuntimeError` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 696 che coinvolge il nodo canonico `RuntimeError` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 696 che coinvolge il nodo canonico `RuntimeError` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 696 che coinvolge il nodo canonico `RuntimeError` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 696 che coinvolge il nodo canonico `RuntimeError` è `1.0`. Nel grafo unificato, l'arco 696 registra come `source` l'identificatore `Request-Dataset-Generator::runtimeerror` per la relazione che coinvolge il nodo canonico `RuntimeError`. Nel grafo unificato, l'arco 696 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_unresolvedplaceholdererror` per la relazione che coinvolge il nodo canonico `RuntimeError`.
<!-- GRAPH_EDGE graph=unificata index=693 role=incoming -->
Nel grafo unificato, il nodo canonico `RuntimeError` è il destinatario `target` della relazione `inherits` proveniente dal nodo canonico `OutputExtractionError`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror` all'ID `Request-Dataset-Generator::runtimeerror`. La relazione tecnica esplicita è la seguente: `OutputExtractionError` eredita da `RuntimeError`. Nel grafo unificato, il tipo dichiarato dell'arco 693 che coinvolge il nodo canonico `RuntimeError` è `inherits`. Nel grafo unificato, la confidenza categoriale dell'arco 693 che coinvolge il nodo canonico `RuntimeError` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 693 che coinvolge il nodo canonico `RuntimeError` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 693 che coinvolge il nodo canonico `RuntimeError` è `L47`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 693 che coinvolge il nodo canonico `RuntimeError` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 693 che coinvolge il nodo canonico `RuntimeError` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 693 che coinvolge il nodo canonico `RuntimeError` è `1.0`. Nel grafo unificato, l'arco 693 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror` per la relazione che coinvolge il nodo canonico `RuntimeError`. Nel grafo unificato, l'arco 693 registra come `target` l'identificatore `Request-Dataset-Generator::runtimeerror` per la relazione che coinvolge il nodo canonico `RuntimeError`.

### Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di

<!-- GRAPH_NODE graph=codice index=407 id=traffic_exec_engine_rationale_621 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `L621`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` possiede l'identificatore esatto `traffic_exec_engine_rationale_621`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `scrive eventi di esecuzione in formato jsonl.      buffer in-memory, flush su di`.
<!-- GRAPH_EDGE graph=codice index=786 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_621` all'ID `traffic_exec_engine_executionlogger`. La relazione tecnica esplicita è la seguente: `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` costituisce una motivazione per `ExecutionLogger`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 786 che coinvolge il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 786 che coinvolge il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 786 che coinvolge il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 786 che coinvolge il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `L621`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 786 che coinvolge il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 786 che coinvolge il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `ast`. Nel grafo del codice RDG, l'arco 786 registra come `source` l'identificatore `traffic_exec_engine_rationale_621` per la relazione che coinvolge il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di`. Nel grafo del codice RDG, l'arco 786 registra come `target` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 786 che coinvolge il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=407 id=Request-Dataset-Generator::traffic_exec_engine_rationale_621 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `L621`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `scrive eventi di esecuzione in formato jsonl.      buffer in-memory, flush su di`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `traffic_exec_engine_rationale_621`. Nel grafo unificato, il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_rationale_621`.
Nel grafo unificato, il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=786 role=incoming -->
Nel grafo unificato, il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_621`. La relazione tecnica esplicita è la seguente: `ExecutionLogger` costituisce una motivazione per `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di`. Nel grafo unificato, il tipo dichiarato dell'arco 786 che coinvolge il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 786 che coinvolge il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 786 che coinvolge il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 786 che coinvolge il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `L621`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 786 che coinvolge il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 786 che coinvolge il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 786 che coinvolge il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di` è `1.0`. Nel grafo unificato, l'arco 786 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di`. Nel grafo unificato, l'arco 786 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_621` per la relazione che coinvolge il nodo canonico `Scrive eventi di esecuzione in formato JSONL.      Buffer in-memory, flush su di`.

### Semaphore

<!-- GRAPH_NODE graph=codice index=387 id=semaphore -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Semaphore` è `Semaphore`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Semaphore` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Semaphore` è ``. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Semaphore` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Semaphore` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Semaphore` possiede l'identificatore esatto `semaphore`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Semaphore` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Semaphore` è `semaphore`.
Nel grafo del codice RDG, il nodo canonico `Semaphore` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=808 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Semaphore` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `semaphore`. La relazione tecnica esplicita è la seguente: `execute_flow()` fa riferimento a `Semaphore`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 808 che coinvolge il nodo canonico `Semaphore` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 808 che coinvolge il nodo canonico `Semaphore` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 808 che coinvolge il nodo canonico `Semaphore` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 808 che coinvolge il nodo canonico `Semaphore` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 808 che coinvolge il nodo canonico `Semaphore` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 808 che coinvolge il nodo canonico `Semaphore` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 808 che coinvolge il nodo canonico `Semaphore` è `ast`. Nel grafo del codice RDG, l'arco 808 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `Semaphore`. Nel grafo del codice RDG, l'arco 808 registra come `target` l'identificatore `semaphore` per la relazione che coinvolge il nodo canonico `Semaphore`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 808 che coinvolge il nodo canonico `Semaphore` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=810 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `Semaphore` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_run` all'ID `semaphore`. La relazione tecnica esplicita è la seguente: `run()` chiama `Semaphore`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 810 che coinvolge il nodo canonico `Semaphore` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 810 che coinvolge il nodo canonico `Semaphore` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 810 che coinvolge il nodo canonico `Semaphore` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 810 che coinvolge il nodo canonico `Semaphore` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 810 che coinvolge il nodo canonico `Semaphore` è `L994`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 810 che coinvolge il nodo canonico `Semaphore` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 810 che coinvolge il nodo canonico `Semaphore` è `ast`. Nel grafo del codice RDG, l'arco 810 registra come `source` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `Semaphore`. Nel grafo del codice RDG, l'arco 810 registra come `target` l'identificatore `semaphore` per la relazione che coinvolge il nodo canonico `Semaphore`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 810 che coinvolge il nodo canonico `Semaphore` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=387 id=Request-Dataset-Generator::semaphore -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Semaphore` è `Semaphore`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Semaphore` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Semaphore` è ``. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Semaphore` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Semaphore` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Semaphore` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Semaphore` è `semaphore`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Semaphore` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Semaphore` è `semaphore`. Nel grafo unificato, il nodo canonico `Semaphore` possiede l'identificatore esatto `Request-Dataset-Generator::semaphore`.
<!-- GRAPH_EDGE graph=unificata index=810 role=outgoing -->
Nel grafo unificato, il nodo canonico `Semaphore` è la sorgente `source` della relazione `calls` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::semaphore` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `Semaphore` chiama `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 810 che coinvolge il nodo canonico `Semaphore` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 810 che coinvolge il nodo canonico `Semaphore` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 810 che coinvolge il nodo canonico `Semaphore` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 810 che coinvolge il nodo canonico `Semaphore` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 810 che coinvolge il nodo canonico `Semaphore` è `L994`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 810 che coinvolge il nodo canonico `Semaphore` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 810 che coinvolge il nodo canonico `Semaphore` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 810 che coinvolge il nodo canonico `Semaphore` è `1.0`. Nel grafo unificato, l'arco 810 registra come `source` l'identificatore `Request-Dataset-Generator::semaphore` per la relazione che coinvolge il nodo canonico `Semaphore`. Nel grafo unificato, l'arco 810 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `Semaphore`.
<!-- GRAPH_EDGE graph=unificata index=808 role=incoming -->
Nel grafo unificato, il nodo canonico `Semaphore` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow` all'ID `Request-Dataset-Generator::semaphore`. La relazione tecnica esplicita è la seguente: `execute_flow()` fa riferimento a `Semaphore`. Nel grafo unificato, il tipo dichiarato dell'arco 808 che coinvolge il nodo canonico `Semaphore` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 808 che coinvolge il nodo canonico `Semaphore` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 808 che coinvolge il nodo canonico `Semaphore` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 808 che coinvolge il nodo canonico `Semaphore` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 808 che coinvolge il nodo canonico `Semaphore` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 808 che coinvolge il nodo canonico `Semaphore` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 808 che coinvolge il nodo canonico `Semaphore` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 808 che coinvolge il nodo canonico `Semaphore` è `1.0`. Nel grafo unificato, l'arco 808 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `Semaphore`. Nel grafo unificato, l'arco 808 registra come `target` l'identificatore `Request-Dataset-Generator::semaphore` per la relazione che coinvolge il nodo canonico `Semaphore`.

### send_request()

<!-- GRAPH_NODE graph=codice index=370 id=traffic_exec_engine_send_request -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `send_request()` è `send_request()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `send_request()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `send_request()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `send_request()` è `L542`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `send_request()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `send_request()` possiede l'identificatore esatto `traffic_exec_engine_send_request`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `send_request()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `send_request()` è `send_request()`.
<!-- GRAPH_EDGE graph=codice index=710 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `send_request()` è la sorgente `source` della relazione `references` verso il nodo canonico `Any [traffic_exec_engine_py_any]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_send_request` all'ID `traffic_exec_engine_py_any`. La relazione tecnica esplicita è la seguente: `send_request()` fa riferimento a `Any [traffic_exec_engine_py_any]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 710 che coinvolge il nodo canonico `send_request()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 710 che coinvolge il nodo canonico `send_request()` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 710 che coinvolge il nodo canonico `send_request()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 710 che coinvolge il nodo canonico `send_request()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 710 che coinvolge il nodo canonico `send_request()` è `L542`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 710 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 710 che coinvolge il nodo canonico `send_request()` è `ast`. Nel grafo del codice RDG, l'arco 710 registra come `source` l'identificatore `traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo del codice RDG, l'arco 710 registra come `target` l'identificatore `traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 710 che coinvolge il nodo canonico `send_request()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=742 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `send_request()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_correlation_headers()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_send_request` all'ID `traffic_exec_engine_correlation_headers`. La relazione tecnica esplicita è la seguente: `send_request()` chiama `_correlation_headers()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 742 che coinvolge il nodo canonico `send_request()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 742 che coinvolge il nodo canonico `send_request()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 742 che coinvolge il nodo canonico `send_request()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 742 che coinvolge il nodo canonico `send_request()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 742 che coinvolge il nodo canonico `send_request()` è `L572`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 742 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 742 che coinvolge il nodo canonico `send_request()` è `ast`. Nel grafo del codice RDG, l'arco 742 registra come `source` l'identificatore `traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo del codice RDG, l'arco 742 registra come `target` l'identificatore `traffic_exec_engine_correlation_headers` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 742 che coinvolge il nodo canonico `send_request()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=751 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `send_request()` è la sorgente `source` della relazione `references` verso il nodo canonico `MtlsContextFactory`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_send_request` all'ID `traffic_exec_engine_mtlscontextfactory`. La relazione tecnica esplicita è la seguente: `send_request()` fa riferimento a `MtlsContextFactory`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 751 che coinvolge il nodo canonico `send_request()` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 751 che coinvolge il nodo canonico `send_request()` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 751 che coinvolge il nodo canonico `send_request()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 751 che coinvolge il nodo canonico `send_request()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 751 che coinvolge il nodo canonico `send_request()` è `L542`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 751 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 751 che coinvolge il nodo canonico `send_request()` è `ast`. Nel grafo del codice RDG, l'arco 751 registra come `source` l'identificatore `traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo del codice RDG, l'arco 751 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 751 che coinvolge il nodo canonico `send_request()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=765 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `send_request()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_send_request` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `send_request()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 765 che coinvolge il nodo canonico `send_request()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 765 che coinvolge il nodo canonico `send_request()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 765 che coinvolge il nodo canonico `send_request()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 765 che coinvolge il nodo canonico `send_request()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 765 che coinvolge il nodo canonico `send_request()` è `L569`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 765 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 765 che coinvolge il nodo canonico `send_request()` è `ast`. Nel grafo del codice RDG, l'arco 765 registra come `source` l'identificatore `traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo del codice RDG, l'arco 765 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 765 che coinvolge il nodo canonico `send_request()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=686 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `send_request()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_send_request`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `send_request()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 686 che coinvolge il nodo canonico `send_request()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 686 che coinvolge il nodo canonico `send_request()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 686 che coinvolge il nodo canonico `send_request()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 686 che coinvolge il nodo canonico `send_request()` è `L542`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 686 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 686 che coinvolge il nodo canonico `send_request()` è `ast`. Nel grafo del codice RDG, l'arco 686 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo del codice RDG, l'arco 686 registra come `target` l'identificatore `traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 686 che coinvolge il nodo canonico `send_request()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=776 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `send_request()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_send_request`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `send_request()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 776 che coinvolge il nodo canonico `send_request()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 776 che coinvolge il nodo canonico `send_request()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 776 che coinvolge il nodo canonico `send_request()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 776 che coinvolge il nodo canonico `send_request()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 776 che coinvolge il nodo canonico `send_request()` è `L892`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 776 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 776 che coinvolge il nodo canonico `send_request()` è `ast`. Nel grafo del codice RDG, l'arco 776 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo del codice RDG, l'arco 776 registra come `target` l'identificatore `traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 776 che coinvolge il nodo canonico `send_request()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=777 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `send_request()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_549` all'ID `traffic_exec_engine_send_request`. La relazione tecnica esplicita è la seguente: `Invia la HTTP request definita nello step.      Inietta automaticamente gli head` costituisce una motivazione per `send_request()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 777 che coinvolge il nodo canonico `send_request()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 777 che coinvolge il nodo canonico `send_request()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 777 che coinvolge il nodo canonico `send_request()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 777 che coinvolge il nodo canonico `send_request()` è `L549`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 777 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 777 che coinvolge il nodo canonico `send_request()` è `ast`. Nel grafo del codice RDG, l'arco 777 registra come `source` l'identificatore `traffic_exec_engine_rationale_549` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo del codice RDG, l'arco 777 registra come `target` l'identificatore `traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 777 che coinvolge il nodo canonico `send_request()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=370 id=Request-Dataset-Generator::traffic_exec_engine_send_request -->
Nel grafo unificato, l'etichetta originale del nodo canonico `send_request()` è `send_request()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `send_request()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `send_request()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `send_request()` è `L542`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `send_request()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `send_request()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `send_request()` è `send_request()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `send_request()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `send_request()` è `traffic_exec_engine_send_request`. Nel grafo unificato, il nodo canonico `send_request()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_send_request`.
<!-- GRAPH_EDGE graph=unificata index=776 role=outgoing -->
Nel grafo unificato, il nodo canonico `send_request()` è la sorgente `source` della relazione `calls` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_send_request` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `send_request()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 776 che coinvolge il nodo canonico `send_request()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 776 che coinvolge il nodo canonico `send_request()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 776 che coinvolge il nodo canonico `send_request()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 776 che coinvolge il nodo canonico `send_request()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 776 che coinvolge il nodo canonico `send_request()` è `L892`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 776 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 776 che coinvolge il nodo canonico `send_request()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 776 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo unificato, l'arco 776 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo unificato, l'arco 776 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `send_request()`.
<!-- GRAPH_EDGE graph=unificata index=777 role=outgoing -->
Nel grafo unificato, il nodo canonico `send_request()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Invia la HTTP request definita nello step.      Inietta automaticamente gli head`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_send_request` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_549`. La relazione tecnica esplicita è la seguente: `send_request()` costituisce una motivazione per `Invia la HTTP request definita nello step.      Inietta automaticamente gli head`. Nel grafo unificato, il tipo dichiarato dell'arco 777 che coinvolge il nodo canonico `send_request()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 777 che coinvolge il nodo canonico `send_request()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 777 che coinvolge il nodo canonico `send_request()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 777 che coinvolge il nodo canonico `send_request()` è `L549`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 777 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 777 che coinvolge il nodo canonico `send_request()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 777 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo unificato, l'arco 777 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo unificato, l'arco 777 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_549` per la relazione che coinvolge il nodo canonico `send_request()`.
<!-- GRAPH_EDGE graph=unificata index=686 role=incoming -->
Nel grafo unificato, il nodo canonico `send_request()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_send_request`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `send_request()`. Nel grafo unificato, il tipo dichiarato dell'arco 686 che coinvolge il nodo canonico `send_request()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 686 che coinvolge il nodo canonico `send_request()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 686 che coinvolge il nodo canonico `send_request()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 686 che coinvolge il nodo canonico `send_request()` è `L542`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 686 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 686 che coinvolge il nodo canonico `send_request()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 686 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo unificato, l'arco 686 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo unificato, l'arco 686 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `send_request()`.
<!-- GRAPH_EDGE graph=unificata index=710 role=incoming -->
Nel grafo unificato, il nodo canonico `send_request()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `Any [traffic_exec_engine_py_any]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_py_any` all'ID `Request-Dataset-Generator::traffic_exec_engine_send_request`. La relazione tecnica esplicita è la seguente: `Any [traffic_exec_engine_py_any]` fa riferimento a `send_request()`. Nel grafo unificato, il tipo dichiarato dell'arco 710 che coinvolge il nodo canonico `send_request()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 710 che coinvolge il nodo canonico `send_request()` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 710 che coinvolge il nodo canonico `send_request()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 710 che coinvolge il nodo canonico `send_request()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 710 che coinvolge il nodo canonico `send_request()` è `L542`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 710 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 710 che coinvolge il nodo canonico `send_request()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 710 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo unificato, l'arco 710 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_py_any` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo unificato, l'arco 710 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `send_request()`.
<!-- GRAPH_EDGE graph=unificata index=742 role=incoming -->
Nel grafo unificato, il nodo canonico `send_request()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_correlation_headers()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_correlation_headers` all'ID `Request-Dataset-Generator::traffic_exec_engine_send_request`. La relazione tecnica esplicita è la seguente: `_correlation_headers()` chiama `send_request()`. Nel grafo unificato, il tipo dichiarato dell'arco 742 che coinvolge il nodo canonico `send_request()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 742 che coinvolge il nodo canonico `send_request()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 742 che coinvolge il nodo canonico `send_request()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 742 che coinvolge il nodo canonico `send_request()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 742 che coinvolge il nodo canonico `send_request()` è `L572`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 742 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 742 che coinvolge il nodo canonico `send_request()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 742 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo unificato, l'arco 742 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_correlation_headers` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo unificato, l'arco 742 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `send_request()`.
<!-- GRAPH_EDGE graph=unificata index=751 role=incoming -->
Nel grafo unificato, il nodo canonico `send_request()` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `MtlsContextFactory`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` all'ID `Request-Dataset-Generator::traffic_exec_engine_send_request`. La relazione tecnica esplicita è la seguente: `MtlsContextFactory` fa riferimento a `send_request()`. Nel grafo unificato, il tipo dichiarato dell'arco 751 che coinvolge il nodo canonico `send_request()` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 751 che coinvolge il nodo canonico `send_request()` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 751 che coinvolge il nodo canonico `send_request()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 751 che coinvolge il nodo canonico `send_request()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 751 che coinvolge il nodo canonico `send_request()` è `L542`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 751 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 751 che coinvolge il nodo canonico `send_request()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 751 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo unificato, l'arco 751 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo unificato, l'arco 751 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `send_request()`.
<!-- GRAPH_EDGE graph=unificata index=765 role=incoming -->
Nel grafo unificato, il nodo canonico `send_request()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::traffic_exec_engine_send_request`. La relazione tecnica esplicita è la seguente: `.get()` chiama `send_request()`. Nel grafo unificato, il tipo dichiarato dell'arco 765 che coinvolge il nodo canonico `send_request()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 765 che coinvolge il nodo canonico `send_request()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 765 che coinvolge il nodo canonico `send_request()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 765 che coinvolge il nodo canonico `send_request()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 765 che coinvolge il nodo canonico `send_request()` è `L569`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 765 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 765 che coinvolge il nodo canonico `send_request()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 765 che coinvolge il nodo canonico `send_request()` è `1.0`. Nel grafo unificato, l'arco 765 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `send_request()`. Nel grafo unificato, l'arco 765 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `send_request()`.

### SSLContext

<!-- GRAPH_NODE graph=codice index=364 id=sslcontext -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `SSLContext` è `SSLContext`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `SSLContext` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `SSLContext` è ``. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `SSLContext` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `SSLContext` vale `ast`. Nel grafo del codice RDG, il nodo canonico `SSLContext` possiede l'identificatore esatto `sslcontext`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `SSLContext` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `SSLContext` è `sslcontext`.
Nel grafo del codice RDG, il nodo canonico `SSLContext` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=762 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `SSLContext` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_mtlscontextfactory_get` all'ID `sslcontext`. La relazione tecnica esplicita è la seguente: `.get()` fa riferimento a `SSLContext`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 762 che coinvolge il nodo canonico `SSLContext` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 762 che coinvolge il nodo canonico `SSLContext` è `generic_arg`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 762 che coinvolge il nodo canonico `SSLContext` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 762 che coinvolge il nodo canonico `SSLContext` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 762 che coinvolge il nodo canonico `SSLContext` è `L385`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 762 che coinvolge il nodo canonico `SSLContext` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 762 che coinvolge il nodo canonico `SSLContext` è `ast`. Nel grafo del codice RDG, l'arco 762 registra come `source` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `SSLContext`. Nel grafo del codice RDG, l'arco 762 registra come `target` l'identificatore `sslcontext` per la relazione che coinvolge il nodo canonico `SSLContext`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 762 che coinvolge il nodo canonico `SSLContext` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=766 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `SSLContext` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `._load_pkcs12()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_mtlscontextfactory_load_pkcs12` all'ID `sslcontext`. La relazione tecnica esplicita è la seguente: `._load_pkcs12()` fa riferimento a `SSLContext`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 766 che coinvolge il nodo canonico `SSLContext` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 766 che coinvolge il nodo canonico `SSLContext` è `parameter_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 766 che coinvolge il nodo canonico `SSLContext` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 766 che coinvolge il nodo canonico `SSLContext` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 766 che coinvolge il nodo canonico `SSLContext` è `L429`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 766 che coinvolge il nodo canonico `SSLContext` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 766 che coinvolge il nodo canonico `SSLContext` è `ast`. Nel grafo del codice RDG, l'arco 766 registra come `source` l'identificatore `traffic_exec_engine_mtlscontextfactory_load_pkcs12` per la relazione che coinvolge il nodo canonico `SSLContext`. Nel grafo del codice RDG, l'arco 766 registra come `target` l'identificatore `sslcontext` per la relazione che coinvolge il nodo canonico `SSLContext`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 766 che coinvolge il nodo canonico `SSLContext` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=364 id=Request-Dataset-Generator::sslcontext -->
Nel grafo unificato, l'etichetta originale del nodo canonico `SSLContext` è `SSLContext`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `SSLContext` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `SSLContext` è ``. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `SSLContext` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `SSLContext` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `SSLContext` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `SSLContext` è `sslcontext`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `SSLContext` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `SSLContext` è `sslcontext`. Nel grafo unificato, il nodo canonico `SSLContext` possiede l'identificatore esatto `Request-Dataset-Generator::sslcontext`.
<!-- GRAPH_EDGE graph=unificata index=766 role=outgoing -->
Nel grafo unificato, il nodo canonico `SSLContext` è la sorgente `source` della relazione `references` verso il nodo canonico `._load_pkcs12()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::sslcontext` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_load_pkcs12`. La relazione tecnica esplicita è la seguente: `SSLContext` fa riferimento a `._load_pkcs12()`. Nel grafo unificato, il tipo dichiarato dell'arco 766 che coinvolge il nodo canonico `SSLContext` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 766 che coinvolge il nodo canonico `SSLContext` è `parameter_type`. Nel grafo unificato, la confidenza categoriale dell'arco 766 che coinvolge il nodo canonico `SSLContext` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 766 che coinvolge il nodo canonico `SSLContext` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 766 che coinvolge il nodo canonico `SSLContext` è `L429`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 766 che coinvolge il nodo canonico `SSLContext` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 766 che coinvolge il nodo canonico `SSLContext` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 766 che coinvolge il nodo canonico `SSLContext` è `1.0`. Nel grafo unificato, l'arco 766 registra come `source` l'identificatore `Request-Dataset-Generator::sslcontext` per la relazione che coinvolge il nodo canonico `SSLContext`. Nel grafo unificato, l'arco 766 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_load_pkcs12` per la relazione che coinvolge il nodo canonico `SSLContext`.
<!-- GRAPH_EDGE graph=unificata index=762 role=incoming -->
Nel grafo unificato, il nodo canonico `SSLContext` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` all'ID `Request-Dataset-Generator::sslcontext`. La relazione tecnica esplicita è la seguente: `.get()` fa riferimento a `SSLContext`. Nel grafo unificato, il tipo dichiarato dell'arco 762 che coinvolge il nodo canonico `SSLContext` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 762 che coinvolge il nodo canonico `SSLContext` è `generic_arg`. Nel grafo unificato, la confidenza categoriale dell'arco 762 che coinvolge il nodo canonico `SSLContext` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 762 che coinvolge il nodo canonico `SSLContext` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 762 che coinvolge il nodo canonico `SSLContext` è `L385`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 762 che coinvolge il nodo canonico `SSLContext` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 762 che coinvolge il nodo canonico `SSLContext` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 762 che coinvolge il nodo canonico `SSLContext` è `1.0`. Nel grafo unificato, l'arco 762 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `SSLContext`. Nel grafo unificato, l'arco 762 registra come `target` l'identificatore `Request-Dataset-Generator::sslcontext` per la relazione che coinvolge il nodo canonico `SSLContext`.

### traffic_exec_engine.py

<!-- GRAPH_NODE graph=codice index=338 id=traffic_exec_engine -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `traffic_exec_engine.py` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `traffic_exec_engine.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `traffic_exec_engine.py` vale `ast`. Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` possiede l'identificatore esatto `traffic_exec_engine`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `traffic_exec_engine.py` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=codice index=661 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_build_parser`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_build_parser() [traffic_exec_engine_build_parser]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 661 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 661 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 661 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 661 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L1127`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 661 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 661 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 661 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 661 registra come `target` l'identificatore `traffic_exec_engine_build_parser` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 661 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=662 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `ConcurrencyTracker`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_concurrencytracker`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `ConcurrencyTracker`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 662 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 662 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 662 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 662 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L772`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 662 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 662 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 662 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 662 registra come `target` l'identificatore `traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 662 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=663 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_correlation_headers()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_correlation_headers`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_correlation_headers()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 663 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 663 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 663 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 663 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L350`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 663 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 663 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 663 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 663 registra come `target` l'identificatore `traffic_exec_engine_correlation_headers` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 663 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=664 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `execute_flow()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 664 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 664 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 664 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 664 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 664 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 664 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 664 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 664 registra come `target` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 664 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=665 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_executionlogger`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `ExecutionLogger`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 665 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 665 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 665 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 665 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L620`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 665 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 665 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 665 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 665 registra come `target` l'identificatore `traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 665 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=666 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_extract_json_output()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_extract_json_output`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_extract_json_output()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 666 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 666 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 666 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 666 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L189`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 666 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 666 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 666 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 666 registra come `target` l'identificatore `traffic_exec_engine_extract_json_output` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 666 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=667 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `extract_outputs()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `extract_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 667 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 667 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 667 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 667 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L158`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 667 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 667 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 667 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 667 registra come `target` l'identificatore `traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 667 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=668 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_extract_xml_output()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_extract_xml_output`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_extract_xml_output()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 668 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 668 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 668 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 668 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L203`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 668 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 668 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 668 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 668 registra come `target` l'identificatore `traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 668 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=669 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_extract_xml_output_stdlib()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_extract_xml_output_stdlib`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_extract_xml_output_stdlib()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 669 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 669 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 669 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 669 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L239`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 669 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 669 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 669 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 669 registra come `target` l'identificatore `traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 669 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=670 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `FaultInjector`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_faultinjector`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `FaultInjector`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 670 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 670 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 670 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 670 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L471`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 670 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 670 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 670 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 670 registra come `target` l'identificatore `traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 670 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=671 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_find_first_by_local_name()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_find_first_by_local_name`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_find_first_by_local_name()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 671 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 671 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 671 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 671 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L321`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 671 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 671 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 671 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 671 registra come `target` l'identificatore `traffic_exec_engine_find_first_by_local_name` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 671 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=672 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_linearize_xml()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_linearize_xml`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_linearize_xml()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 672 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 672 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 672 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 672 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L285`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 672 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 672 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 672 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 672 registra come `target` l'identificatore `traffic_exec_engine_linearize_xml` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 672 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=673 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `load_config() [traffic_exec_engine_load_config]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_load_config`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `load_config() [traffic_exec_engine_load_config]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 673 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 673 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 673 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 673 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L1118`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 673 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 673 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 673 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 673 registra come `target` l'identificatore `traffic_exec_engine_load_config` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 673 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=674 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `load_dataset_as_timeline()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_load_dataset_as_timeline`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `load_dataset_as_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 674 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 674 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 674 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 674 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L1097`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 674 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 674 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 674 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 674 registra come `target` l'identificatore `traffic_exec_engine_load_dataset_as_timeline` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 674 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=675 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `load_timeline()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_load_timeline`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `load_timeline()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 675 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 675 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 675 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 675 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L1089`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 675 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 675 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 675 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 675 registra come `target` l'identificatore `traffic_exec_engine_load_timeline` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 675 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=676 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `main() [traffic_exec_engine_main]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_main`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `main() [traffic_exec_engine_main]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 676 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 676 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 676 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 676 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L1185`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 676 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 676 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 676 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 676 registra come `target` l'identificatore `traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 676 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=677 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `MtlsContextFactory`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_mtlscontextfactory`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `MtlsContextFactory`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 677 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 677 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 677 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 677 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L373`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 677 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 677 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 677 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 677 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 677 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=678 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_now_epoch_ms()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_now_epoch_ms`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_now_epoch_ms()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 678 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 678 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 678 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 678 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L63`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 678 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 678 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 678 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 678 registra come `target` l'identificatore `traffic_exec_engine_now_epoch_ms` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 678 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=679 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_now_iso()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_now_iso`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_now_iso()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 679 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 679 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 679 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 679 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L59`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 679 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 679 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 679 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 679 registra come `target` l'identificatore `traffic_exec_engine_now_iso` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 679 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=680 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `OutputExtractionError`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_outputextractionerror`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `OutputExtractionError`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 680 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 680 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 680 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 680 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L47`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 680 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 680 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 680 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 680 registra come `target` l'identificatore `traffic_exec_engine_outputextractionerror` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 680 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=681 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_replace_newlines_inside_tags`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 681 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 681 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 681 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 681 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L298`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 681 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 681 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 681 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 681 registra come `target` l'identificatore `traffic_exec_engine_replace_newlines_inside_tags` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 681 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=682 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `resolve_placeholders()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_resolve_placeholders`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `resolve_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 682 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 682 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 682 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 682 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L87`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 682 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 682 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 682 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 682 registra come `target` l'identificatore `traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 682 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=683 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_resolve_secret()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_resolve_secret`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_resolve_secret()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 683 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 683 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 683 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 683 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L366`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 683 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 683 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 683 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 683 registra come `target` l'identificatore `traffic_exec_engine_resolve_secret` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 683 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=684 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_response_size()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_response_size`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_response_size()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 684 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 684 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 684 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 684 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L67`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 684 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 684 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 684 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 684 registra come `target` l'identificatore `traffic_exec_engine_response_size` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 684 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=685 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `run()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 685 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 685 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 685 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 685 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L967`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 685 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 685 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 685 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 685 registra come `target` l'identificatore `traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 685 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=686 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `send_request()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_send_request`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `send_request()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 686 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 686 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 686 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 686 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L542`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 686 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 686 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 686 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 686 registra come `target` l'identificatore `traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 686 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=687 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_stable_unit_interval()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_stable_unit_interval`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_stable_unit_interval()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 687 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 687 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 687 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 687 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L77`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 687 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 687 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 687 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 687 registra come `target` l'identificatore `traffic_exec_engine_stable_unit_interval` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 687 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=688 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `unresolved_placeholders()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `unresolved_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 688 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 688 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 688 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 688 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L134`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 688 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 688 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 688 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 688 registra come `target` l'identificatore `traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 688 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=689 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `UnresolvedPlaceholderError`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_unresolvedplaceholdererror`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `UnresolvedPlaceholderError`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 689 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 689 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 689 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 689 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 689 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 689 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 689 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 689 registra come `target` l'identificatore `traffic_exec_engine_unresolvedplaceholdererror` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 689 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=690 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `validate_required_outputs()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_validate_required_outputs`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `validate_required_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 690 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 690 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 690 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 690 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L331`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 690 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 690 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 690 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 690 registra come `target` l'identificatore `traffic_exec_engine_validate_required_outputs` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 690 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=691 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_xml_raw_string()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_xml_raw_string`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_xml_raw_string()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 691 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 691 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 691 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 691 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L275`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 691 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 691 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 691 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 691 registra come `target` l'identificatore `traffic_exec_engine_xml_raw_string` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 691 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=148 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `main` all'ID `traffic_exec_engine`. La relazione tecnica esplicita è la seguente: `main.py` importa da `traffic_exec_engine.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 148 che coinvolge il nodo canonico `traffic_exec_engine.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 148 che coinvolge il nodo canonico `traffic_exec_engine.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 148 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 148 che coinvolge il nodo canonico `traffic_exec_engine.py` è `main.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 148 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 148 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 148 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 148 registra come `source` l'identificatore `main` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 148 registra come `target` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 148 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=588 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `traffic_exec_engine.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `traffic_exec_engine`. La relazione tecnica esplicita è la seguente: `test_core.py` importa da `traffic_exec_engine.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 588 che coinvolge il nodo canonico `traffic_exec_engine.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 588 che coinvolge il nodo canonico `traffic_exec_engine.py` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 588 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 588 che coinvolge il nodo canonico `traffic_exec_engine.py` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 588 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L25`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 588 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 588 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo del codice RDG, l'arco 588 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, l'arco 588 registra come `target` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 588 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=338 id=Request-Dataset-Generator::traffic_exec_engine -->
Nel grafo unificato, l'etichetta originale del nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `traffic_exec_engine.py` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `traffic_exec_engine.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `traffic_exec_engine.py` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `traffic_exec_engine.py` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `traffic_exec_engine.py` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine`. Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine`.
<!-- GRAPH_EDGE graph=unificata index=661 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_build_parser() [traffic_exec_engine_build_parser]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_build_parser`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_build_parser() [traffic_exec_engine_build_parser]`. Nel grafo unificato, il tipo dichiarato dell'arco 661 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 661 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 661 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 661 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L1127`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 661 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 661 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 661 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 661 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 661 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_build_parser` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=662 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `ConcurrencyTracker`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `ConcurrencyTracker`. Nel grafo unificato, il tipo dichiarato dell'arco 662 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 662 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 662 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 662 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L772`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 662 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 662 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 662 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 662 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 662 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_concurrencytracker` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=663 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_correlation_headers()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_correlation_headers`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_correlation_headers()`. Nel grafo unificato, il tipo dichiarato dell'arco 663 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 663 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 663 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 663 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L350`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 663 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 663 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 663 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 663 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 663 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_correlation_headers` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=664 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 664 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 664 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 664 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 664 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L805`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 664 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 664 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 664 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 664 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 664 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=665 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `ExecutionLogger`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_executionlogger`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `ExecutionLogger`. Nel grafo unificato, il tipo dichiarato dell'arco 665 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 665 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 665 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 665 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L620`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 665 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 665 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 665 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 665 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 665 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_executionlogger` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=666 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_extract_json_output()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_json_output`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_extract_json_output()`. Nel grafo unificato, il tipo dichiarato dell'arco 666 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 666 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 666 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 666 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L189`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 666 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 666 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 666 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 666 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 666 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_json_output` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=667 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `extract_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_outputs`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `extract_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 667 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 667 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 667 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 667 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L158`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 667 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 667 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 667 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 667 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 667 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_outputs` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=668 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_extract_xml_output()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_extract_xml_output()`. Nel grafo unificato, il tipo dichiarato dell'arco 668 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 668 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 668 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 668 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L203`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 668 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 668 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 668 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 668 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 668 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=669 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_extract_xml_output_stdlib()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_extract_xml_output_stdlib()`. Nel grafo unificato, il tipo dichiarato dell'arco 669 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 669 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 669 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 669 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L239`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 669 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 669 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 669 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 669 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 669 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_extract_xml_output_stdlib` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=670 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `FaultInjector`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_faultinjector`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `FaultInjector`. Nel grafo unificato, il tipo dichiarato dell'arco 670 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 670 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 670 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 670 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L471`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 670 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 670 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 670 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 670 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 670 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_faultinjector` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=671 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_find_first_by_local_name()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_find_first_by_local_name`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_find_first_by_local_name()`. Nel grafo unificato, il tipo dichiarato dell'arco 671 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 671 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 671 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 671 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L321`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 671 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 671 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 671 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 671 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 671 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_find_first_by_local_name` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=672 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_linearize_xml()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_linearize_xml`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_linearize_xml()`. Nel grafo unificato, il tipo dichiarato dell'arco 672 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 672 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 672 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 672 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L285`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 672 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 672 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 672 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 672 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 672 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_linearize_xml` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=673 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `load_config() [traffic_exec_engine_load_config]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_load_config`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `load_config() [traffic_exec_engine_load_config]`. Nel grafo unificato, il tipo dichiarato dell'arco 673 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 673 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 673 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 673 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L1118`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 673 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 673 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 673 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 673 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 673 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_load_config` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=674 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `load_dataset_as_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_load_dataset_as_timeline`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `load_dataset_as_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 674 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 674 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 674 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 674 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L1097`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 674 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 674 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 674 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 674 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 674 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_load_dataset_as_timeline` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=675 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `load_timeline()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_load_timeline`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `load_timeline()`. Nel grafo unificato, il tipo dichiarato dell'arco 675 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 675 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 675 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 675 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L1089`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 675 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 675 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 675 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 675 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 675 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_load_timeline` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=676 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `main() [traffic_exec_engine_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_main`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `main() [traffic_exec_engine_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 676 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 676 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 676 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 676 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L1185`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 676 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 676 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 676 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 676 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 676 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_main` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=677 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `MtlsContextFactory`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `MtlsContextFactory`. Nel grafo unificato, il tipo dichiarato dell'arco 677 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 677 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 677 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 677 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L373`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 677 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 677 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 677 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 677 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 677 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=678 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_now_epoch_ms()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_now_epoch_ms`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_now_epoch_ms()`. Nel grafo unificato, il tipo dichiarato dell'arco 678 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 678 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 678 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 678 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L63`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 678 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 678 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 678 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 678 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 678 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_now_epoch_ms` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=679 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_now_iso()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_now_iso`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_now_iso()`. Nel grafo unificato, il tipo dichiarato dell'arco 679 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 679 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 679 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 679 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L59`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 679 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 679 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 679 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 679 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 679 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_now_iso` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=680 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `OutputExtractionError`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `OutputExtractionError`. Nel grafo unificato, il tipo dichiarato dell'arco 680 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 680 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 680 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 680 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L47`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 680 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 680 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 680 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 680 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 680 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_outputextractionerror` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=681 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_replace_newlines_inside_tags`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_replace_newlines_inside_tags() [traffic_exec_engine_replace_newlines_inside_tags]`. Nel grafo unificato, il tipo dichiarato dell'arco 681 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 681 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 681 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 681 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L298`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 681 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 681 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 681 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 681 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 681 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_replace_newlines_inside_tags` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=682 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `resolve_placeholders()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `resolve_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 682 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 682 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 682 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 682 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L87`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 682 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 682 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 682 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 682 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 682 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_resolve_placeholders` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=683 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_resolve_secret()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_resolve_secret`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_resolve_secret()`. Nel grafo unificato, il tipo dichiarato dell'arco 683 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 683 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 683 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 683 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L366`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 683 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 683 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 683 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 683 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 683 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_resolve_secret` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=684 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_response_size()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_response_size`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_response_size()`. Nel grafo unificato, il tipo dichiarato dell'arco 684 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 684 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 684 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 684 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L67`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 684 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 684 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 684 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 684 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 684 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_response_size` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=685 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `run()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_run`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `run()`. Nel grafo unificato, il tipo dichiarato dell'arco 685 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 685 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 685 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 685 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L967`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 685 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 685 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 685 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 685 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 685 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_run` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=686 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `send_request()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_send_request`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `send_request()`. Nel grafo unificato, il tipo dichiarato dell'arco 686 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 686 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 686 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 686 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L542`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 686 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 686 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 686 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 686 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 686 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_send_request` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=687 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_stable_unit_interval()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_stable_unit_interval`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_stable_unit_interval()`. Nel grafo unificato, il tipo dichiarato dell'arco 687 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 687 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 687 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 687 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L77`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 687 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 687 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 687 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 687 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 687 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_stable_unit_interval` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=688 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `unresolved_placeholders()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `unresolved_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 688 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 688 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 688 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 688 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L134`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 688 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 688 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 688 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 688 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 688 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=689 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `UnresolvedPlaceholderError`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_unresolvedplaceholdererror`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `UnresolvedPlaceholderError`. Nel grafo unificato, il tipo dichiarato dell'arco 689 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 689 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 689 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 689 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 689 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 689 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 689 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 689 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 689 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_unresolvedplaceholdererror` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=690 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `validate_required_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_validate_required_outputs`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `validate_required_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 690 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 690 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 690 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 690 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L331`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 690 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 690 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 690 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 690 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 690 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_validate_required_outputs` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=691 role=outgoing -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_xml_raw_string()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `_xml_raw_string()`. Nel grafo unificato, il tipo dichiarato dell'arco 691 che coinvolge il nodo canonico `traffic_exec_engine.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 691 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 691 che coinvolge il nodo canonico `traffic_exec_engine.py` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 691 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L275`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 691 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 691 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 691 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 691 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 691 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_xml_raw_string` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=148 role=incoming -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `main.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::main` all'ID `Request-Dataset-Generator::traffic_exec_engine`. La relazione tecnica esplicita è la seguente: `main.py` importa da `traffic_exec_engine.py`. Nel grafo unificato, il tipo dichiarato dell'arco 148 che coinvolge il nodo canonico `traffic_exec_engine.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 148 che coinvolge il nodo canonico `traffic_exec_engine.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 148 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 148 che coinvolge il nodo canonico `traffic_exec_engine.py` è `main.py`. Nel grafo unificato, la posizione sorgente dell'arco 148 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L20`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 148 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 148 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 148 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 148 registra come `source` l'identificatore `Request-Dataset-Generator::main` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 148 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.
<!-- GRAPH_EDGE graph=unificata index=588 role=incoming -->
Nel grafo unificato, il nodo canonico `traffic_exec_engine.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::traffic_exec_engine`. La relazione tecnica esplicita è la seguente: `test_core.py` importa da `traffic_exec_engine.py`. Nel grafo unificato, il tipo dichiarato dell'arco 588 che coinvolge il nodo canonico `traffic_exec_engine.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 588 che coinvolge il nodo canonico `traffic_exec_engine.py` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 588 che coinvolge il nodo canonico `traffic_exec_engine.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 588 che coinvolge il nodo canonico `traffic_exec_engine.py` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 588 che coinvolge il nodo canonico `traffic_exec_engine.py` è `L25`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 588 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 588 che coinvolge il nodo canonico `traffic_exec_engine.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 588 che coinvolge il nodo canonico `traffic_exec_engine.py` è `1.0`. Nel grafo unificato, l'arco 588 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`. Nel grafo unificato, l'arco 588 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `traffic_exec_engine.py`.

### unresolved_placeholders()

<!-- GRAPH_NODE graph=codice index=348 id=traffic_exec_engine_unresolved_placeholders -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `unresolved_placeholders()` è `unresolved_placeholders()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `unresolved_placeholders()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `unresolved_placeholders()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `unresolved_placeholders()` è `L134`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `unresolved_placeholders()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `unresolved_placeholders()` possiede l'identificatore esatto `traffic_exec_engine_unresolved_placeholders`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `unresolved_placeholders()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `unresolved_placeholders()` è `unresolved_placeholders()`.
<!-- GRAPH_EDGE graph=codice index=719 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `unresolved_placeholders()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_unresolved_placeholders` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `unresolved_placeholders()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 719 che coinvolge il nodo canonico `unresolved_placeholders()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 719 che coinvolge il nodo canonico `unresolved_placeholders()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 719 che coinvolge il nodo canonico `unresolved_placeholders()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 719 che coinvolge il nodo canonico `unresolved_placeholders()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 719 che coinvolge il nodo canonico `unresolved_placeholders()` è `L148`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 719 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 719 che coinvolge il nodo canonico `unresolved_placeholders()` è `ast`. Nel grafo del codice RDG, l'arco 719 registra come `source` l'identificatore `traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`. Nel grafo del codice RDG, l'arco 719 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 719 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=592 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `unresolved_placeholders()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `traffic_exec_engine_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `unresolved_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 592 che coinvolge il nodo canonico `unresolved_placeholders()` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 592 che coinvolge il nodo canonico `unresolved_placeholders()` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 592 che coinvolge il nodo canonico `unresolved_placeholders()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 592 che coinvolge il nodo canonico `unresolved_placeholders()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 592 che coinvolge il nodo canonico `unresolved_placeholders()` è `L25`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 592 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 592 che coinvolge il nodo canonico `unresolved_placeholders()` è `ast`. Nel grafo del codice RDG, l'arco 592 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`. Nel grafo del codice RDG, l'arco 592 registra come `target` l'identificatore `traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 592 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=601 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `unresolved_placeholders()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body` all'ID `traffic_exec_engine_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `.test_resolves_placeholders_in_url_headers_and_body()` chiama `unresolved_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 601 che coinvolge il nodo canonico `unresolved_placeholders()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 601 che coinvolge il nodo canonico `unresolved_placeholders()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 601 che coinvolge il nodo canonico `unresolved_placeholders()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 601 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 601 che coinvolge il nodo canonico `unresolved_placeholders()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 601 che coinvolge il nodo canonico `unresolved_placeholders()` è `L251`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 601 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 601 che coinvolge il nodo canonico `unresolved_placeholders()` è `ast`. Nel grafo del codice RDG, l'arco 601 registra come `source` l'identificatore `tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`. Nel grafo del codice RDG, l'arco 601 registra come `target` l'identificatore `traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`.
<!-- GRAPH_EDGE graph=codice index=603 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `unresolved_placeholders()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_detects_unresolved_placeholders()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_placeholdertest_test_detects_unresolved_placeholders` all'ID `traffic_exec_engine_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `.test_detects_unresolved_placeholders()` chiama `unresolved_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 603 che coinvolge il nodo canonico `unresolved_placeholders()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 603 che coinvolge il nodo canonico `unresolved_placeholders()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 603 che coinvolge il nodo canonico `unresolved_placeholders()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 603 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 603 che coinvolge il nodo canonico `unresolved_placeholders()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 603 che coinvolge il nodo canonico `unresolved_placeholders()` è `L275`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 603 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 603 che coinvolge il nodo canonico `unresolved_placeholders()` è `ast`. Nel grafo del codice RDG, l'arco 603 registra come `source` l'identificatore `tests_test_core_placeholdertest_test_detects_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`. Nel grafo del codice RDG, l'arco 603 registra come `target` l'identificatore `traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`.
<!-- GRAPH_EDGE graph=codice index=688 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `unresolved_placeholders()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `unresolved_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 688 che coinvolge il nodo canonico `unresolved_placeholders()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 688 che coinvolge il nodo canonico `unresolved_placeholders()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 688 che coinvolge il nodo canonico `unresolved_placeholders()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 688 che coinvolge il nodo canonico `unresolved_placeholders()` è `L134`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 688 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 688 che coinvolge il nodo canonico `unresolved_placeholders()` è `ast`. Nel grafo del codice RDG, l'arco 688 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`. Nel grafo del codice RDG, l'arco 688 registra come `target` l'identificatore `traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 688 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=717 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `unresolved_placeholders()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `unresolved_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 717 che coinvolge il nodo canonico `unresolved_placeholders()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 717 che coinvolge il nodo canonico `unresolved_placeholders()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 717 che coinvolge il nodo canonico `unresolved_placeholders()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 717 che coinvolge il nodo canonico `unresolved_placeholders()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 717 che coinvolge il nodo canonico `unresolved_placeholders()` è `L882`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 717 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 717 che coinvolge il nodo canonico `unresolved_placeholders()` è `ast`. Nel grafo del codice RDG, l'arco 717 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`. Nel grafo del codice RDG, l'arco 717 registra come `target` l'identificatore `traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 717 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=718 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `unresolved_placeholders()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_135` all'ID `traffic_exec_engine_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `Ritorna i placeholder ancora presenti nei campi inviati su rete.` costituisce una motivazione per `unresolved_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 718 che coinvolge il nodo canonico `unresolved_placeholders()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 718 che coinvolge il nodo canonico `unresolved_placeholders()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 718 che coinvolge il nodo canonico `unresolved_placeholders()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 718 che coinvolge il nodo canonico `unresolved_placeholders()` è `L135`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 718 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 718 che coinvolge il nodo canonico `unresolved_placeholders()` è `ast`. Nel grafo del codice RDG, l'arco 718 registra come `source` l'identificatore `traffic_exec_engine_rationale_135` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`. Nel grafo del codice RDG, l'arco 718 registra come `target` l'identificatore `traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 718 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=348 id=Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders -->
Nel grafo unificato, l'etichetta originale del nodo canonico `unresolved_placeholders()` è `unresolved_placeholders()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `unresolved_placeholders()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `unresolved_placeholders()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `unresolved_placeholders()` è `L134`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `unresolved_placeholders()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `unresolved_placeholders()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `unresolved_placeholders()` è `unresolved_placeholders()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `unresolved_placeholders()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `unresolved_placeholders()` è `traffic_exec_engine_unresolved_placeholders`. Nel grafo unificato, il nodo canonico `unresolved_placeholders()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders`.
<!-- GRAPH_EDGE graph=unificata index=717 role=outgoing -->
Nel grafo unificato, il nodo canonico `unresolved_placeholders()` è la sorgente `source` della relazione `calls` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `unresolved_placeholders()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 717 che coinvolge il nodo canonico `unresolved_placeholders()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 717 che coinvolge il nodo canonico `unresolved_placeholders()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 717 che coinvolge il nodo canonico `unresolved_placeholders()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 717 che coinvolge il nodo canonico `unresolved_placeholders()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 717 che coinvolge il nodo canonico `unresolved_placeholders()` è `L882`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 717 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 717 che coinvolge il nodo canonico `unresolved_placeholders()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 717 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo unificato, l'arco 717 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`. Nel grafo unificato, l'arco 717 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`.
<!-- GRAPH_EDGE graph=unificata index=718 role=outgoing -->
Nel grafo unificato, il nodo canonico `unresolved_placeholders()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Ritorna i placeholder ancora presenti nei campi inviati su rete.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_135`. La relazione tecnica esplicita è la seguente: `unresolved_placeholders()` costituisce una motivazione per `Ritorna i placeholder ancora presenti nei campi inviati su rete.`. Nel grafo unificato, il tipo dichiarato dell'arco 718 che coinvolge il nodo canonico `unresolved_placeholders()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 718 che coinvolge il nodo canonico `unresolved_placeholders()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 718 che coinvolge il nodo canonico `unresolved_placeholders()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 718 che coinvolge il nodo canonico `unresolved_placeholders()` è `L135`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 718 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 718 che coinvolge il nodo canonico `unresolved_placeholders()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 718 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo unificato, l'arco 718 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`. Nel grafo unificato, l'arco 718 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_135` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`.
<!-- GRAPH_EDGE graph=unificata index=719 role=outgoing -->
Nel grafo unificato, il nodo canonico `unresolved_placeholders()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `unresolved_placeholders()` chiama `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 719 che coinvolge il nodo canonico `unresolved_placeholders()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 719 che coinvolge il nodo canonico `unresolved_placeholders()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 719 che coinvolge il nodo canonico `unresolved_placeholders()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 719 che coinvolge il nodo canonico `unresolved_placeholders()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 719 che coinvolge il nodo canonico `unresolved_placeholders()` è `L148`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 719 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 719 che coinvolge il nodo canonico `unresolved_placeholders()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 719 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo unificato, l'arco 719 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`. Nel grafo unificato, l'arco 719 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`.
<!-- GRAPH_EDGE graph=unificata index=592 role=incoming -->
Nel grafo unificato, il nodo canonico `unresolved_placeholders()` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `unresolved_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 592 che coinvolge il nodo canonico `unresolved_placeholders()` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 592 che coinvolge il nodo canonico `unresolved_placeholders()` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 592 che coinvolge il nodo canonico `unresolved_placeholders()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 592 che coinvolge il nodo canonico `unresolved_placeholders()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 592 che coinvolge il nodo canonico `unresolved_placeholders()` è `L25`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 592 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 592 che coinvolge il nodo canonico `unresolved_placeholders()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 592 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo unificato, l'arco 592 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`. Nel grafo unificato, l'arco 592 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`.
<!-- GRAPH_EDGE graph=unificata index=601 role=incoming -->
Nel grafo unificato, il nodo canonico `unresolved_placeholders()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_resolves_placeholders_in_url_headers_and_body()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body` all'ID `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `.test_resolves_placeholders_in_url_headers_and_body()` chiama `unresolved_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 601 che coinvolge il nodo canonico `unresolved_placeholders()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 601 che coinvolge il nodo canonico `unresolved_placeholders()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 601 che coinvolge il nodo canonico `unresolved_placeholders()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 601 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 601 che coinvolge il nodo canonico `unresolved_placeholders()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 601 che coinvolge il nodo canonico `unresolved_placeholders()` è `L251`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 601 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 601 che coinvolge il nodo canonico `unresolved_placeholders()` è `ast`. Nel grafo unificato, l'arco 601 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest_test_resolves_placeholders_in_url_headers_and_body` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`. Nel grafo unificato, l'arco 601 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`.
<!-- GRAPH_EDGE graph=unificata index=603 role=incoming -->
Nel grafo unificato, il nodo canonico `unresolved_placeholders()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_detects_unresolved_placeholders()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_placeholdertest_test_detects_unresolved_placeholders` all'ID `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `.test_detects_unresolved_placeholders()` chiama `unresolved_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 603 che coinvolge il nodo canonico `unresolved_placeholders()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 603 che coinvolge il nodo canonico `unresolved_placeholders()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 603 che coinvolge il nodo canonico `unresolved_placeholders()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 603 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 603 che coinvolge il nodo canonico `unresolved_placeholders()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 603 che coinvolge il nodo canonico `unresolved_placeholders()` è `L275`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 603 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 603 che coinvolge il nodo canonico `unresolved_placeholders()` è `ast`. Nel grafo unificato, l'arco 603 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest_test_detects_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`. Nel grafo unificato, l'arco 603 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`.
<!-- GRAPH_EDGE graph=unificata index=688 role=incoming -->
Nel grafo unificato, il nodo canonico `unresolved_placeholders()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `unresolved_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 688 che coinvolge il nodo canonico `unresolved_placeholders()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 688 che coinvolge il nodo canonico `unresolved_placeholders()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 688 che coinvolge il nodo canonico `unresolved_placeholders()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 688 che coinvolge il nodo canonico `unresolved_placeholders()` è `L134`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 688 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 688 che coinvolge il nodo canonico `unresolved_placeholders()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 688 che coinvolge il nodo canonico `unresolved_placeholders()` è `1.0`. Nel grafo unificato, l'arco 688 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`. Nel grafo unificato, l'arco 688 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_unresolved_placeholders` per la relazione che coinvolge il nodo canonico `unresolved_placeholders()`.

### UnresolvedPlaceholderError

<!-- GRAPH_NODE graph=codice index=341 id=traffic_exec_engine_unresolvedplaceholdererror -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `UnresolvedPlaceholderError` è `UnresolvedPlaceholderError`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `UnresolvedPlaceholderError` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `UnresolvedPlaceholderError` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `UnresolvedPlaceholderError` è `L51`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `UnresolvedPlaceholderError` vale `ast`. Nel grafo del codice RDG, il nodo canonico `UnresolvedPlaceholderError` possiede l'identificatore esatto `traffic_exec_engine_unresolvedplaceholdererror`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `UnresolvedPlaceholderError` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `UnresolvedPlaceholderError` è `unresolvedplaceholdererror`.
<!-- GRAPH_EDGE graph=codice index=696 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `UnresolvedPlaceholderError` è la sorgente `source` della relazione `inherits` verso il nodo canonico `RuntimeError`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_unresolvedplaceholdererror` all'ID `runtimeerror`. La relazione tecnica esplicita è la seguente: `UnresolvedPlaceholderError` eredita da `RuntimeError`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 696 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `inherits`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 696 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 696 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 696 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 696 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 696 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `ast`. Nel grafo del codice RDG, l'arco 696 registra come `source` l'identificatore `traffic_exec_engine_unresolvedplaceholdererror` per la relazione che coinvolge il nodo canonico `UnresolvedPlaceholderError`. Nel grafo del codice RDG, l'arco 696 registra come `target` l'identificatore `runtimeerror` per la relazione che coinvolge il nodo canonico `UnresolvedPlaceholderError`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 696 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=689 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `UnresolvedPlaceholderError` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_unresolvedplaceholdererror`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `UnresolvedPlaceholderError`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 689 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 689 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 689 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 689 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 689 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 689 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `ast`. Nel grafo del codice RDG, l'arco 689 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `UnresolvedPlaceholderError`. Nel grafo del codice RDG, l'arco 689 registra come `target` l'identificatore `traffic_exec_engine_unresolvedplaceholdererror` per la relazione che coinvolge il nodo canonico `UnresolvedPlaceholderError`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 689 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=697 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `UnresolvedPlaceholderError` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Errore esplicito quando un placeholder live resta non risolto.`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_52` all'ID `traffic_exec_engine_unresolvedplaceholdererror`. La relazione tecnica esplicita è la seguente: `Errore esplicito quando un placeholder live resta non risolto.` costituisce una motivazione per `UnresolvedPlaceholderError`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 697 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 697 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 697 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 697 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `L52`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 697 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 697 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `ast`. Nel grafo del codice RDG, l'arco 697 registra come `source` l'identificatore `traffic_exec_engine_rationale_52` per la relazione che coinvolge il nodo canonico `UnresolvedPlaceholderError`. Nel grafo del codice RDG, l'arco 697 registra come `target` l'identificatore `traffic_exec_engine_unresolvedplaceholdererror` per la relazione che coinvolge il nodo canonico `UnresolvedPlaceholderError`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 697 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=341 id=Request-Dataset-Generator::traffic_exec_engine_unresolvedplaceholdererror -->
Nel grafo unificato, l'etichetta originale del nodo canonico `UnresolvedPlaceholderError` è `UnresolvedPlaceholderError`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `UnresolvedPlaceholderError` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `UnresolvedPlaceholderError` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `UnresolvedPlaceholderError` è `L51`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `UnresolvedPlaceholderError` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `UnresolvedPlaceholderError` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `UnresolvedPlaceholderError` è `unresolvedplaceholdererror`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `UnresolvedPlaceholderError` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `UnresolvedPlaceholderError` è `traffic_exec_engine_unresolvedplaceholdererror`. Nel grafo unificato, il nodo canonico `UnresolvedPlaceholderError` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_unresolvedplaceholdererror`.
<!-- GRAPH_EDGE graph=unificata index=697 role=outgoing -->
Nel grafo unificato, il nodo canonico `UnresolvedPlaceholderError` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Errore esplicito quando un placeholder live resta non risolto.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_unresolvedplaceholdererror` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_52`. La relazione tecnica esplicita è la seguente: `UnresolvedPlaceholderError` costituisce una motivazione per `Errore esplicito quando un placeholder live resta non risolto.`. Nel grafo unificato, il tipo dichiarato dell'arco 697 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 697 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 697 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 697 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `L52`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 697 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 697 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 697 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `1.0`. Nel grafo unificato, l'arco 697 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_unresolvedplaceholdererror` per la relazione che coinvolge il nodo canonico `UnresolvedPlaceholderError`. Nel grafo unificato, l'arco 697 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_52` per la relazione che coinvolge il nodo canonico `UnresolvedPlaceholderError`.
<!-- GRAPH_EDGE graph=unificata index=689 role=incoming -->
Nel grafo unificato, il nodo canonico `UnresolvedPlaceholderError` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_unresolvedplaceholdererror`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `UnresolvedPlaceholderError`. Nel grafo unificato, il tipo dichiarato dell'arco 689 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 689 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 689 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 689 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 689 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 689 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 689 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `1.0`. Nel grafo unificato, l'arco 689 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `UnresolvedPlaceholderError`. Nel grafo unificato, l'arco 689 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_unresolvedplaceholdererror` per la relazione che coinvolge il nodo canonico `UnresolvedPlaceholderError`.
<!-- GRAPH_EDGE graph=unificata index=696 role=incoming -->
Nel grafo unificato, il nodo canonico `UnresolvedPlaceholderError` è il destinatario `target` della relazione `inherits` proveniente dal nodo canonico `RuntimeError`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::runtimeerror` all'ID `Request-Dataset-Generator::traffic_exec_engine_unresolvedplaceholdererror`. La relazione tecnica esplicita è la seguente: `RuntimeError` eredita da `UnresolvedPlaceholderError`. Nel grafo unificato, il tipo dichiarato dell'arco 696 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `inherits`. Nel grafo unificato, la confidenza categoriale dell'arco 696 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 696 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 696 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 696 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 696 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 696 che coinvolge il nodo canonico `UnresolvedPlaceholderError` è `1.0`. Nel grafo unificato, l'arco 696 registra come `source` l'identificatore `Request-Dataset-Generator::runtimeerror` per la relazione che coinvolge il nodo canonico `UnresolvedPlaceholderError`. Nel grafo unificato, l'arco 696 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_unresolvedplaceholdererror` per la relazione che coinvolge il nodo canonico `UnresolvedPlaceholderError`.

### validate_required_outputs()

<!-- GRAPH_NODE graph=codice index=358 id=traffic_exec_engine_validate_required_outputs -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `validate_required_outputs()` è `validate_required_outputs()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `validate_required_outputs()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `validate_required_outputs()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `validate_required_outputs()` è `L331`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `validate_required_outputs()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `validate_required_outputs()` possiede l'identificatore esatto `traffic_exec_engine_validate_required_outputs`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `validate_required_outputs()` alla comunità numerica `0`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `validate_required_outputs()` è `validate_required_outputs()`.
<!-- GRAPH_EDGE graph=codice index=739 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `validate_required_outputs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_validate_required_outputs` all'ID `traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `validate_required_outputs()` chiama `.get()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 739 che coinvolge il nodo canonico `validate_required_outputs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 739 che coinvolge il nodo canonico `validate_required_outputs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 739 che coinvolge il nodo canonico `validate_required_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 739 che coinvolge il nodo canonico `validate_required_outputs()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 739 che coinvolge il nodo canonico `validate_required_outputs()` è `L334`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 739 che coinvolge il nodo canonico `validate_required_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 739 che coinvolge il nodo canonico `validate_required_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 739 registra come `source` l'identificatore `traffic_exec_engine_validate_required_outputs` per la relazione che coinvolge il nodo canonico `validate_required_outputs()`. Nel grafo del codice RDG, l'arco 739 registra come `target` l'identificatore `traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `validate_required_outputs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 739 che coinvolge il nodo canonico `validate_required_outputs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=690 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `validate_required_outputs()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine` all'ID `traffic_exec_engine_validate_required_outputs`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `validate_required_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 690 che coinvolge il nodo canonico `validate_required_outputs()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 690 che coinvolge il nodo canonico `validate_required_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 690 che coinvolge il nodo canonico `validate_required_outputs()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 690 che coinvolge il nodo canonico `validate_required_outputs()` è `L331`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 690 che coinvolge il nodo canonico `validate_required_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 690 che coinvolge il nodo canonico `validate_required_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 690 registra come `source` l'identificatore `traffic_exec_engine` per la relazione che coinvolge il nodo canonico `validate_required_outputs()`. Nel grafo del codice RDG, l'arco 690 registra come `target` l'identificatore `traffic_exec_engine_validate_required_outputs` per la relazione che coinvolge il nodo canonico `validate_required_outputs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 690 che coinvolge il nodo canonico `validate_required_outputs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=737 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `validate_required_outputs()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_execute_flow` all'ID `traffic_exec_engine_validate_required_outputs`. La relazione tecnica esplicita è la seguente: `execute_flow()` chiama `validate_required_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 737 che coinvolge il nodo canonico `validate_required_outputs()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 737 che coinvolge il nodo canonico `validate_required_outputs()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 737 che coinvolge il nodo canonico `validate_required_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 737 che coinvolge il nodo canonico `validate_required_outputs()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 737 che coinvolge il nodo canonico `validate_required_outputs()` è `L898`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 737 che coinvolge il nodo canonico `validate_required_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 737 che coinvolge il nodo canonico `validate_required_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 737 registra come `source` l'identificatore `traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `validate_required_outputs()`. Nel grafo del codice RDG, l'arco 737 registra come `target` l'identificatore `traffic_exec_engine_validate_required_outputs` per la relazione che coinvolge il nodo canonico `validate_required_outputs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 737 che coinvolge il nodo canonico `validate_required_outputs()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=738 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `validate_required_outputs()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Controlla gli output marcati required nello step.`; l'orientamento registrato nel JSON va dall'ID `traffic_exec_engine_rationale_332` all'ID `traffic_exec_engine_validate_required_outputs`. La relazione tecnica esplicita è la seguente: `Controlla gli output marcati required nello step.` costituisce una motivazione per `validate_required_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 738 che coinvolge il nodo canonico `validate_required_outputs()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 738 che coinvolge il nodo canonico `validate_required_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 738 che coinvolge il nodo canonico `validate_required_outputs()` è `traffic_exec_engine.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 738 che coinvolge il nodo canonico `validate_required_outputs()` è `L332`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 738 che coinvolge il nodo canonico `validate_required_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 738 che coinvolge il nodo canonico `validate_required_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 738 registra come `source` l'identificatore `traffic_exec_engine_rationale_332` per la relazione che coinvolge il nodo canonico `validate_required_outputs()`. Nel grafo del codice RDG, l'arco 738 registra come `target` l'identificatore `traffic_exec_engine_validate_required_outputs` per la relazione che coinvolge il nodo canonico `validate_required_outputs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 738 che coinvolge il nodo canonico `validate_required_outputs()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=358 id=Request-Dataset-Generator::traffic_exec_engine_validate_required_outputs -->
Nel grafo unificato, l'etichetta originale del nodo canonico `validate_required_outputs()` è `validate_required_outputs()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `validate_required_outputs()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `validate_required_outputs()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `validate_required_outputs()` è `L331`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `validate_required_outputs()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `validate_required_outputs()` alla comunità numerica `0`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `validate_required_outputs()` è `validate_required_outputs()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `validate_required_outputs()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `validate_required_outputs()` è `traffic_exec_engine_validate_required_outputs`. Nel grafo unificato, il nodo canonico `validate_required_outputs()` possiede l'identificatore esatto `Request-Dataset-Generator::traffic_exec_engine_validate_required_outputs`.
<!-- GRAPH_EDGE graph=unificata index=737 role=outgoing -->
Nel grafo unificato, il nodo canonico `validate_required_outputs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `execute_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_validate_required_outputs` all'ID `Request-Dataset-Generator::traffic_exec_engine_execute_flow`. La relazione tecnica esplicita è la seguente: `validate_required_outputs()` chiama `execute_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 737 che coinvolge il nodo canonico `validate_required_outputs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 737 che coinvolge il nodo canonico `validate_required_outputs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 737 che coinvolge il nodo canonico `validate_required_outputs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 737 che coinvolge il nodo canonico `validate_required_outputs()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 737 che coinvolge il nodo canonico `validate_required_outputs()` è `L898`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 737 che coinvolge il nodo canonico `validate_required_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 737 che coinvolge il nodo canonico `validate_required_outputs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 737 che coinvolge il nodo canonico `validate_required_outputs()` è `1.0`. Nel grafo unificato, l'arco 737 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_validate_required_outputs` per la relazione che coinvolge il nodo canonico `validate_required_outputs()`. Nel grafo unificato, l'arco 737 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_execute_flow` per la relazione che coinvolge il nodo canonico `validate_required_outputs()`.
<!-- GRAPH_EDGE graph=unificata index=738 role=outgoing -->
Nel grafo unificato, il nodo canonico `validate_required_outputs()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Controlla gli output marcati required nello step.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_validate_required_outputs` all'ID `Request-Dataset-Generator::traffic_exec_engine_rationale_332`. La relazione tecnica esplicita è la seguente: `validate_required_outputs()` costituisce una motivazione per `Controlla gli output marcati required nello step.`. Nel grafo unificato, il tipo dichiarato dell'arco 738 che coinvolge il nodo canonico `validate_required_outputs()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 738 che coinvolge il nodo canonico `validate_required_outputs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 738 che coinvolge il nodo canonico `validate_required_outputs()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 738 che coinvolge il nodo canonico `validate_required_outputs()` è `L332`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 738 che coinvolge il nodo canonico `validate_required_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 738 che coinvolge il nodo canonico `validate_required_outputs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 738 che coinvolge il nodo canonico `validate_required_outputs()` è `1.0`. Nel grafo unificato, l'arco 738 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_validate_required_outputs` per la relazione che coinvolge il nodo canonico `validate_required_outputs()`. Nel grafo unificato, l'arco 738 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_rationale_332` per la relazione che coinvolge il nodo canonico `validate_required_outputs()`.
<!-- GRAPH_EDGE graph=unificata index=739 role=outgoing -->
Nel grafo unificato, il nodo canonico `validate_required_outputs()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine_validate_required_outputs` all'ID `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get`. La relazione tecnica esplicita è la seguente: `validate_required_outputs()` chiama `.get()`. Nel grafo unificato, il tipo dichiarato dell'arco 739 che coinvolge il nodo canonico `validate_required_outputs()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 739 che coinvolge il nodo canonico `validate_required_outputs()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 739 che coinvolge il nodo canonico `validate_required_outputs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 739 che coinvolge il nodo canonico `validate_required_outputs()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 739 che coinvolge il nodo canonico `validate_required_outputs()` è `L334`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 739 che coinvolge il nodo canonico `validate_required_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 739 che coinvolge il nodo canonico `validate_required_outputs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 739 che coinvolge il nodo canonico `validate_required_outputs()` è `1.0`. Nel grafo unificato, l'arco 739 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_validate_required_outputs` per la relazione che coinvolge il nodo canonico `validate_required_outputs()`. Nel grafo unificato, l'arco 739 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_mtlscontextfactory_get` per la relazione che coinvolge il nodo canonico `validate_required_outputs()`.
<!-- GRAPH_EDGE graph=unificata index=690 role=incoming -->
Nel grafo unificato, il nodo canonico `validate_required_outputs()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `traffic_exec_engine.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::traffic_exec_engine` all'ID `Request-Dataset-Generator::traffic_exec_engine_validate_required_outputs`. La relazione tecnica esplicita è la seguente: `traffic_exec_engine.py` contiene `validate_required_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 690 che coinvolge il nodo canonico `validate_required_outputs()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 690 che coinvolge il nodo canonico `validate_required_outputs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 690 che coinvolge il nodo canonico `validate_required_outputs()` è `traffic_exec_engine.py`. Nel grafo unificato, la posizione sorgente dell'arco 690 che coinvolge il nodo canonico `validate_required_outputs()` è `L331`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 690 che coinvolge il nodo canonico `validate_required_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 690 che coinvolge il nodo canonico `validate_required_outputs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 690 che coinvolge il nodo canonico `validate_required_outputs()` è `1.0`. Nel grafo unificato, l'arco 690 registra come `source` l'identificatore `Request-Dataset-Generator::traffic_exec_engine` per la relazione che coinvolge il nodo canonico `validate_required_outputs()`. Nel grafo unificato, l'arco 690 registra come `target` l'identificatore `Request-Dataset-Generator::traffic_exec_engine_validate_required_outputs` per la relazione che coinvolge il nodo canonico `validate_required_outputs()`.
