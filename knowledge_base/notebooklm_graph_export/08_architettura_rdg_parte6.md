# Architettura e codice del framework RDG — parte 6

## Metadati

- Dominio: Architettura e codice del framework RDG.
- Data di generazione: 2026-07-31.
- Versione dei grafi sorgente: KB documentale `5f9434fc0f01de5d74dd3c6d72fffc217b7f8629`; KB codice RDG `06d5dd91cef2e12574b63a36d73b049a3588cdf8`; KB unificata `null`.
- Nodi coperti: 92 nodi canonici e 184 occorrenze fisiche; tipi canonici: code: 76, rationale: 16.
- Archi coperti: 340 occorrenze fisiche incidenti ai nodi del documento; tipi: calls: 132, contains: 50, imports: 8, imports_from: 2, indirect_call: 8, method: 88, rationale_for: 32, re_exports: 2, references: 2, uses: 16.
- Iperarchi coperti: 0 occorrenze fisiche, replicate anche negli altri file coinvolti quando attraversano la segmentazione editoriale.
- Nota sulla direzione: tutti e tre i grafi dichiarano `directed=false`; la prosa conserva comunque l'orientamento dei campi JSON `source` e `target` senza trasformarlo in causalità.

## Glossario

- EXTRACTED — Valore della proprietà `confidence` che il grafo assegna a una relazione estratta.
- INFERRED — Valore della proprietà `confidence` che il grafo assegna a una relazione inferita; la prosa non promuove una relazione INFERRED a fatto estratto.
- RDG — Nome editoriale canonico del repository Request-Dataset-Generator.
- AST — Valore di provenienza `_origin=ast` registrato dal grafo.
- FHIR — Nome canonico dell'acronimo usato negli identificatori RDG.
- RVE — Nome canonico del prefisso usato per i flussi RDG.
- UI — Nome canonico dell'acronimo usato nei moduli di interfaccia.

### Mappa dei nomi canonici e delle varianti

La mappa seguente non fonde nodi solo perché semanticamente simili. Le occorrenze del grafo sorgente e del grafo unificato sono ricondotte allo stesso nome soltanto tramite `repo` e `local_id`; le etichette duplicate di nodi distinti sono disambiguate con l'identificatore locale.

- Il nome canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` identifica il nodo `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator_init`; le etichette originali conservate sono `.__init__()`.
- Il nome canonico `.test_builds_iti_flows_with_aulss3_profile()` identifica il nodo `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile`; le etichette originali conservate sono `.test_builds_iti_flows_with_aulss3_profile()`.
- Il nome canonico `.test_builds_supported_flows()` identifica il nodo `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_supported_flows`; le etichette originali conservate sono `.test_builds_supported_flows()`.
- Il nome canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` identifica il nodo `Request-Dataset-Generator::tests_test_core_configjsontest_test_config_json_files_are_valid_and_scrybasign_auth_is_scoped`; le etichette originali conservate sono `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()`.
- Il nome canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` identifica il nodo `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_default_flow_mix_includes_only_known_flows_and_sums_to_one`; le etichette originali conservate sono `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()`.
- Il nome canonico `.test_dispatch_includes_liv3_commands()` identifica il nodo `Request-Dataset-Generator::tests_test_core_maindispatchtest_test_dispatch_includes_liv3_commands`; le etichette originali conservate sono `.test_dispatch_includes_liv3_commands()`.
- Il nome canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` identifica il nodo `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders`; le etichette originali conservate sono `.test_iti18_and_iti43_use_expected_saml_placeholders()`.
- Il nome canonico `.test_live_rve121_matches_soapui_body_shape()` identifica il nodo `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape`; le etichette originali conservate sono `.test_live_rve121_matches_soapui_body_shape()`.
- Il nome canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` identifica il nodo `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url`; le etichette originali conservate sono `.test_live_rve54_uses_saml_header_and_patient_query_url()`.
- Il nome canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` identifica il nodo `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper`; le etichette originali conservate sono `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`.
- Il nome canonico `.test_live_rve_1b_declares_mtls_profile()` identifica il nodo `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile`; le etichette originali conservate sono `.test_live_rve_1b_declares_mtls_profile()`.
- Il nome canonico `.test_mock_middleware_flow_builds_gateway_steps()` identifica il nodo `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps`; le etichette originali conservate sono `.test_mock_middleware_flow_builds_gateway_steps()`.
- Il nome canonico `.test_mock_propagates_saml_output_to_consumers()` identifica il nodo `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers`; le etichette originali conservate sono `.test_mock_propagates_saml_output_to_consumers()`.
- Il nome canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` identifica il nodo `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_declares_and_mocks_all_saml_assertion_outputs`; le etichette originali conservate sono `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()`.
- Il nome canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` identifica il nodo `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to`; le etichette originali conservate sono `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()`.
- Il nome canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` identifica il nodo `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_profile_values_do_not_come_from_generated_patient`; le etichette originali conservate sono `.test_rve1b_profile_values_do_not_come_from_generated_patient()`.
- Il nome canonico `.test_scrybasign_flow_has_two_steps_in_order()` identifica il nodo `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order`; le etichette originali conservate sono `.test_scrybasign_flow_has_two_steps_in_order()`.
- Il nome canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` identifica il nodo `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_get_user_info_step_matches_soap_contract`; le etichette originali conservate sono `.test_scrybasign_get_user_info_step_matches_soap_contract()`.
- Il nome canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` identifica il nodo `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_sign_one_doc_step_matches_soap_contract`; le etichette originali conservate sono `.test_scrybasign_sign_one_doc_step_matches_soap_contract()`.
- Il nome canonico `__init__.py [rve_transactions_init]` identifica il nodo `Request-Dataset-Generator::rve_transactions_init`; le etichette originali conservate sono `__init__.py`.
- Il nome canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` identifica il nodo `Request-Dataset-Generator::rve_transactions_flow_orchestrator_rationale_142`; le etichette originali conservate sono `Args:             config: configurazione globale (endpoints, auth, ecc.)`.
- Il nome canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` identifica il nodo `Request-Dataset-Generator::rve_transactions_flow_orchestrator_rationale_127`; le etichette originali conservate sono `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or`.
- Il nome canonico `ConfigJsonTest` identifica il nodo `Request-Dataset-Generator::tests_test_core_configjsontest`; le etichette originali conservate sono `ConfigJsonTest`.
- Il nome canonico `FlowBuildTest` identifica il nodo `Request-Dataset-Generator::tests_test_core_flowbuildtest`; le etichette originali conservate sono `FlowBuildTest`.
- Il nome canonico `FlowOrchestrator` identifica il nodo `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator`; le etichette originali conservate sono `FlowOrchestrator`.
- Il nome canonico `MainDispatchTest` identifica il nodo `Request-Dataset-Generator::tests_test_core_maindispatchtest`; le etichette originali conservate sono `MainDispatchTest`.
- Il nome canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` identifica il nodo `Request-Dataset-Generator::rve_transactions_init_rationale_1`; le etichette originali conservate sono `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l`.
- Il nome canonico `_address_locality()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_address_locality`; le etichette originali conservate sono `_address_locality()`.
- Il nome canonico `_address_postal_code()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_address_postal_code`; le etichette originali conservate sono `_address_postal_code()`.
- Il nome canonico `_address_text()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_address_text`; le etichette originali conservate sono `_address_text()`.
- Il nome canonico `_birth_date_millis()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_birth_date_millis`; le etichette originali conservate sono `_birth_date_millis()`.
- Il nome canonico `_build_mock_bundle()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_build_mock_bundle`; le etichette originali conservate sono `_build_mock_bundle()`.
- Il nome canonico `_family_name()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_family_name`; le etichette originali conservate sono `_family_name()`.
- Il nome canonico `_first_address()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_first_address`; le etichette originali conservate sono `_first_address()`.
- Il nome canonico `_first_string()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_first_string`; le etichette originali conservate sono `_first_string()`.
- Il nome canonico `_first_value()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_first_value`; le etichette originali conservate sono `_first_value()`.
- Il nome canonico `_given_name()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_given_name`; le etichette originali conservate sono `_given_name()`.
- Il nome canonico `_identifier_value()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_identifier_value`; le etichette originali conservate sono `_identifier_value()`.
- Il nome canonico `_marital_status_code()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_marital_status_code`; le etichette originali conservate sono `_marital_status_code()`.
- Il nome canonico `_mock_patient()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_mock_patient`; le etichette originali conservate sono `_mock_patient()`.
- Il nome canonico `_payload_rve100()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve100`; le etichette originali conservate sono `_payload_rve100()`.
- Il nome canonico `_payload_rve54()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve54`; le etichette originali conservate sono `_payload_rve54()`.
- Il nome canonico `_payload_rve55()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`; le etichette originali conservate sono `_payload_rve55()`.
- Il nome canonico `_payload_rve57()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`; le etichette originali conservate sono `_payload_rve57()`.
- Il nome canonico `_sex_code()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_sex_code`; le etichette originali conservate sono `_sex_code()`.
- Il nome canonico `_telecom_value()` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_telecom_value`; le etichette originali conservate sono `_telecom_value()`.
- Il nome canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_380`; le etichette originali conservate sono ```Adapter per il contratto storico ``build_request`` dell'orchestratore.```.
- Il nome canonico `build_request() [rve_transactions_rve_gateway_build_request]` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_build_request`; le etichette originali conservate sono `build_request()`.
- Il nome canonico `build_step() [rve_transactions_rve_gateway_build_step]` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_build_step`; le etichette originali conservate sono `build_step()`.
- Il nome canonico `Costruisce uno step gateway per lo scenario indicato.` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_304`; le etichette originali conservate sono `Costruisce uno step gateway per lo scenario indicato.`.
- Il nome canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_1`; le etichette originali conservate sono `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come`.
- Il nome canonico `rve_gateway.py` identifica il nodo `Request-Dataset-Generator::rve_transactions_rve_gateway`; le etichette originali conservate sono `rve_gateway.py`.
- Il nome canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_init`; le etichette originali conservate sono `.__init__()`.
- Il nome canonico `._decompose_latency()` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_decompose_latency`; le etichette originali conservate sono `._decompose_latency()`.
- Il nome canonico `._inject_anomaly()` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly`; le etichette originali conservate sono `._inject_anomaly()`.
- Il nome canonico `._next_circuit_breaker()` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker`; le etichette originali conservate sono `._next_circuit_breaker()`.
- Il nome canonico `._next_connections()` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_connections`; le etichette originali conservate sono `._next_connections()`.
- Il nome canonico `._next_cpu()` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_cpu`; le etichette originali conservate sono `._next_cpu()`.
- Il nome canonico `._next_memory()` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_memory`; le etichette originali conservate sono `._next_memory()`.
- Il nome canonico `.generate_entry()` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry`; le etichette originali conservate sono `.generate_entry()`.
- Il nome canonico `.process_file()` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_process_file`; le etichette originali conservate sono `.process_file()`.
- Il nome canonico `_build_parser() [mock_middleware_log_build_parser]` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_build_parser`; le etichette originali conservate sono `_build_parser()`.
- Il nome canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_py_argumentparser`; le etichette originali conservate sono `ArgumentParser`.
- Il nome canonico `Connessioni attive con variazione graduale.` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_rationale_98`; le etichette originali conservate sono `Connessioni attive con variazione graduale.`.
- Il nome canonico `CPU correlata al numero di connessioni attive + rumore.` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_rationale_84`; le etichette originali conservate sono `CPU correlata al numero di connessioni attive + rumore.`.
- Il nome canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_rationale_49`; le etichette originali conservate sono `Genera log middleware mock da log framework.      Per ogni evento "response_rece`.
- Il nome canonico `Genera una riga di log middleware da un evento response_received.` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_rationale_189`; le etichette originali conservate sono `Genera una riga di log middleware da un evento response_received.`.
- Il nome canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_rationale_161`; le etichette originali conservate sono `Inietta anomalie (spike, errori, timeout) con probabilità configurate.`.
- Il nome canonico `Legge il log framework in streaming e genera il log middleware.          Returns` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_rationale_263`; le etichette originali conservate sono `Legge il log framework in streaming e genera il log middleware.          Returns`.
- Il nome canonico `main() [mock_middleware_log_main]` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_main`; le etichette originali conservate sono `main()`.
- Il nome canonico `Memoria lentamente crescente con piccolo jitter.` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_rationale_90`; le etichette originali conservate sono `Memoria lentamente crescente con piccolo jitter.`.
- Il nome canonico `mock_middleware_log.py` identifica il nodo `Request-Dataset-Generator::mock_middleware_log`; le etichette originali conservate sono `mock_middleware_log.py`.
- Il nome canonico `MockMiddlewareLogGenerator` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator`; le etichette originali conservate sono `MockMiddlewareLogGenerator`.
- Il nome canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_rationale_134`; le etichette originali conservate sono `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga`.
- Il nome canonico `Simula transizioni del circuit breaker.` identifica il nodo `Request-Dataset-Generator::mock_middleware_log_rationale_105`; le etichette originali conservate sono `Simula transizioni del circuit breaker.`.
- Il nome canonico `.__init__() [ui_tabs_config_tab_configtab_init]` identifica il nodo `Request-Dataset-Generator::ui_tabs_config_tab_configtab_init`; le etichette originali conservate sono `.__init__()`.
- Il nome canonico `._as_float()` identifica il nodo `Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_float`; le etichette originali conservate sono `._as_float()`.
- Il nome canonico `._as_int()` identifica il nodo `Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_int`; le etichette originali conservate sono `._as_int()`.
- Il nome canonico `._build() [ui_tabs_config_tab_configtab_build]` identifica il nodo `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build`; le etichette originali conservate sono `._build()`.
- Il nome canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` identifica il nodo `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_buttons`; le etichette originali conservate sono `._build_buttons()`.
- Il nome canonico `._build_flow_tags()` identifica il nodo `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_flow_tags`; le etichette originali conservate sono `._build_flow_tags()`.
- Il nome canonico `._build_middleware_grid()` identifica il nodo `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_middleware_grid`; le etichette originali conservate sono `._build_middleware_grid()`.
- Il nome canonico `._build_parameter_grid()` identifica il nodo `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_parameter_grid`; le etichette originali conservate sono `._build_parameter_grid()`.
- Il nome canonico `._refresh_flow_tags()` identifica il nodo `Request-Dataset-Generator::ui_tabs_config_tab_configtab_refresh_flow_tags`; le etichette originali conservate sono `._refresh_flow_tags()`.
- Il nome canonico `._section()` identifica il nodo `Request-Dataset-Generator::ui_tabs_config_tab_configtab_section`; le etichette originali conservate sono `._section()`.
- Il nome canonico `._toggle_flow()` identifica il nodo `Request-Dataset-Generator::ui_tabs_config_tab_configtab_toggle_flow`; le etichette originali conservate sono `._toggle_flow()`.
- Il nome canonico `.get_values()` identifica il nodo `Request-Dataset-Generator::ui_tabs_config_tab_configtab_get_values`; le etichette originali conservate sono `.get_values()`.
- Il nome canonico `.load_values()` identifica il nodo `Request-Dataset-Generator::ui_tabs_config_tab_configtab_load_values`; le etichette originali conservate sono `.load_values()`.
- Il nome canonico `.reset_defaults()` identifica il nodo `Request-Dataset-Generator::ui_tabs_config_tab_configtab_reset_defaults`; le etichette originali conservate sono `.reset_defaults()`.
- Il nome canonico `.save()` identifica il nodo `Request-Dataset-Generator::ui_tabs_config_tab_configtab_save`; le etichette originali conservate sono `.save()`.
- Il nome canonico `ConfigTab` identifica il nodo `Request-Dataset-Generator::ui_tabs_config_tab_configtab`; le etichette originali conservate sono `ConfigTab`.
- Il nome canonico `Form grafico per i parametri dell'esperimento.` identifica il nodo `Request-Dataset-Generator::ui_tabs_config_tab_rationale_29`; le etichette originali conservate sono `Form grafico per i parametri dell'esperimento.`.

## Cluster Graphify 10 — Flow Build Tests

Il cluster editoriale Graphify 10 del dominio Architettura e codice del framework RDG contiene 27 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### .__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]

<!-- GRAPH_NODE graph=codice index=192 id=rve_transactions_flow_orchestrator_floworchestrator_init -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `.__init__()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `rve_transactions/flow_orchestrator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `L141`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` possiede l'identificatore esatto `rve_transactions_flow_orchestrator_floworchestrator_init`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `.__init__()`.
Nel grafo del codice RDG, il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=430 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_flow_orchestrator_floworchestrator` all'ID `rve_transactions_flow_orchestrator_floworchestrator_init`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` registra come proprio metodo `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 430 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 430 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 430 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `rve_transactions/flow_orchestrator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 430 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `L141`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 430 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 430 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `ast`. Nel grafo del codice RDG, l'arco 430 registra come `source` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]`. Nel grafo del codice RDG, l'arco 430 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator_init` per la relazione che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 430 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=446 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_flow_orchestrator_rationale_142` all'ID `rve_transactions_flow_orchestrator_floworchestrator_init`. La relazione tecnica esplicita è la seguente: `Args:             config: configurazione globale (endpoints, auth, ecc.)` costituisce una motivazione per `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 446 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 446 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 446 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `rve_transactions/flow_orchestrator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 446 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `L142`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 446 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 446 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `ast`. Nel grafo del codice RDG, l'arco 446 registra come `source` l'identificatore `rve_transactions_flow_orchestrator_rationale_142` per la relazione che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]`. Nel grafo del codice RDG, l'arco 446 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator_init` per la relazione che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 446 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=192 id=Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator_init -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `.__init__()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `rve_transactions/flow_orchestrator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `L141`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `.__init__()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `rve_transactions_flow_orchestrator_floworchestrator_init`. Nel grafo unificato, il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator_init`.
<!-- GRAPH_EDGE graph=unificata index=446 role=outgoing -->
Nel grafo unificato, il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator_init` all'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_rationale_142`. La relazione tecnica esplicita è la seguente: `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` costituisce una motivazione per `Args:             config: configurazione globale (endpoints, auth, ecc.)`. Nel grafo unificato, il tipo dichiarato dell'arco 446 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 446 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 446 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `rve_transactions/flow_orchestrator.py`. Nel grafo unificato, la posizione sorgente dell'arco 446 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `L142`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 446 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 446 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 446 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `1.0`. Nel grafo unificato, l'arco 446 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator_init` per la relazione che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]`. Nel grafo unificato, l'arco 446 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_rationale_142` per la relazione che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]`.
<!-- GRAPH_EDGE graph=unificata index=430 role=incoming -->
Nel grafo unificato, il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator_init`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` registra come proprio metodo `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 430 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 430 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 430 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `rve_transactions/flow_orchestrator.py`. Nel grafo unificato, la posizione sorgente dell'arco 430 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `L141`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 430 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 430 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 430 che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` è `1.0`. Nel grafo unificato, l'arco 430 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]`. Nel grafo unificato, l'arco 430 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator_init` per la relazione che coinvolge il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]`.

### .test_builds_iti_flows_with_aulss3_profile()

<!-- GRAPH_NODE graph=codice index=294 id=tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `.test_builds_iti_flows_with_aulss3_profile()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `L292`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` possiede l'identificatore esatto `tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `.test_builds_iti_flows_with_aulss3_profile()`.
<!-- GRAPH_EDGE graph=codice index=437 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è la sorgente `source` della relazione `calls` verso il nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `.test_builds_iti_flows_with_aulss3_profile()` chiama `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 437 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 437 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 437 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 437 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 437 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 437 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `L298`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 437 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 437 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `ast`. Nel grafo del codice RDG, l'arco 437 registra come `source` l'identificatore `tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile` per la relazione che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()`. Nel grafo del codice RDG, l'arco 437 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()`.
<!-- GRAPH_EDGE graph=codice index=604 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_builds_iti_flows_with_aulss3_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 604 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 604 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 604 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 604 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `L292`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 604 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 604 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `ast`. Nel grafo del codice RDG, l'arco 604 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()`. Nel grafo del codice RDG, l'arco 604 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile` per la relazione che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 604 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=294 id=Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `.test_builds_iti_flows_with_aulss3_profile()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `L292`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `.test_builds_iti_flows_with_aulss3_profile()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile`. Nel grafo unificato, il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile`.
Nel grafo unificato, il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=437 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` chiama `.test_builds_iti_flows_with_aulss3_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 437 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 437 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 437 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 437 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 437 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 437 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `L298`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 437 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 437 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `ast`. Nel grafo unificato, l'arco 437 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()`. Nel grafo unificato, l'arco 437 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile` per la relazione che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()`.
<!-- GRAPH_EDGE graph=unificata index=604 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_builds_iti_flows_with_aulss3_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 604 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 604 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 604 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 604 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `L292`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 604 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 604 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 604 che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()` è `1.0`. Nel grafo unificato, l'arco 604 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()`. Nel grafo unificato, l'arco 604 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile` per la relazione che coinvolge il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()`.

### .test_builds_supported_flows()

<!-- GRAPH_NODE graph=codice index=293 id=tests_test_core_flowbuildtest_test_builds_supported_flows -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_builds_supported_flows()` è `.test_builds_supported_flows()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_builds_supported_flows()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_builds_supported_flows()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_builds_supported_flows()` è `L279`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_builds_supported_flows()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_builds_supported_flows()` possiede l'identificatore esatto `tests_test_core_flowbuildtest_test_builds_supported_flows`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_builds_supported_flows()` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_builds_supported_flows()` è `.test_builds_supported_flows()`.
<!-- GRAPH_EDGE graph=codice index=438 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_builds_supported_flows()` è la sorgente `source` della relazione `calls` verso il nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest_test_builds_supported_flows` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `.test_builds_supported_flows()` chiama `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 438 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 438 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 438 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 438 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 438 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 438 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `L287`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 438 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 438 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `ast`. Nel grafo del codice RDG, l'arco 438 registra come `source` l'identificatore `tests_test_core_flowbuildtest_test_builds_supported_flows` per la relazione che coinvolge il nodo canonico `.test_builds_supported_flows()`. Nel grafo del codice RDG, l'arco 438 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `.test_builds_supported_flows()`.
<!-- GRAPH_EDGE graph=codice index=605 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_builds_supported_flows()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_builds_supported_flows`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_builds_supported_flows()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 605 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 605 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 605 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 605 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `L279`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 605 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 605 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `ast`. Nel grafo del codice RDG, l'arco 605 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_builds_supported_flows()`. Nel grafo del codice RDG, l'arco 605 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_builds_supported_flows` per la relazione che coinvolge il nodo canonico `.test_builds_supported_flows()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 605 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=293 id=Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_supported_flows -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_builds_supported_flows()` è `.test_builds_supported_flows()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_builds_supported_flows()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_builds_supported_flows()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_builds_supported_flows()` è `L279`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_builds_supported_flows()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_builds_supported_flows()` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_builds_supported_flows()` è `.test_builds_supported_flows()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_builds_supported_flows()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_builds_supported_flows()` è `tests_test_core_flowbuildtest_test_builds_supported_flows`. Nel grafo unificato, il nodo canonico `.test_builds_supported_flows()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_supported_flows`.
Nel grafo unificato, il nodo canonico `.test_builds_supported_flows()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=438 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_builds_supported_flows()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_supported_flows`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` chiama `.test_builds_supported_flows()`. Nel grafo unificato, il tipo dichiarato dell'arco 438 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 438 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 438 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 438 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 438 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 438 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `L287`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 438 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 438 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `ast`. Nel grafo unificato, l'arco 438 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `.test_builds_supported_flows()`. Nel grafo unificato, l'arco 438 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_supported_flows` per la relazione che coinvolge il nodo canonico `.test_builds_supported_flows()`.
<!-- GRAPH_EDGE graph=unificata index=605 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_builds_supported_flows()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_supported_flows`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_builds_supported_flows()`. Nel grafo unificato, il tipo dichiarato dell'arco 605 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 605 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 605 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 605 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `L279`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 605 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 605 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 605 che coinvolge il nodo canonico `.test_builds_supported_flows()` è `1.0`. Nel grafo unificato, l'arco 605 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_builds_supported_flows()`. Nel grafo unificato, l'arco 605 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_supported_flows` per la relazione che coinvolge il nodo canonico `.test_builds_supported_flows()`.

### .test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()

<!-- GRAPH_NODE graph=codice index=318 id=tests_test_core_configjsontest_test_config_json_files_are_valid_and_scrybasign_auth_is_scoped -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `L822`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` possiede l'identificatore esatto `tests_test_core_configjsontest_test_config_json_files_are_valid_and_scrybasign_auth_is_scoped`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()`.
Nel grafo del codice RDG, il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=640 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigJsonTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_configjsontest` all'ID `tests_test_core_configjsontest_test_config_json_files_are_valid_and_scrybasign_auth_is_scoped`. La relazione tecnica esplicita è la seguente: `ConfigJsonTest` registra come proprio metodo `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 640 che coinvolge il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 640 che coinvolge il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 640 che coinvolge il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 640 che coinvolge il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `L822`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 640 che coinvolge il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 640 che coinvolge il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `ast`. Nel grafo del codice RDG, l'arco 640 registra come `source` l'identificatore `tests_test_core_configjsontest` per la relazione che coinvolge il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()`. Nel grafo del codice RDG, l'arco 640 registra come `target` l'identificatore `tests_test_core_configjsontest_test_config_json_files_are_valid_and_scrybasign_auth_is_scoped` per la relazione che coinvolge il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 640 che coinvolge il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=318 id=Request-Dataset-Generator::tests_test_core_configjsontest_test_config_json_files_are_valid_and_scrybasign_auth_is_scoped -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `L822`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `tests_test_core_configjsontest_test_config_json_files_are_valid_and_scrybasign_auth_is_scoped`. Nel grafo unificato, il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_configjsontest_test_config_json_files_are_valid_and_scrybasign_auth_is_scoped`.
Nel grafo unificato, il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=640 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigJsonTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_configjsontest` all'ID `Request-Dataset-Generator::tests_test_core_configjsontest_test_config_json_files_are_valid_and_scrybasign_auth_is_scoped`. La relazione tecnica esplicita è la seguente: `ConfigJsonTest` registra come proprio metodo `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()`. Nel grafo unificato, il tipo dichiarato dell'arco 640 che coinvolge il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 640 che coinvolge il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 640 che coinvolge il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 640 che coinvolge il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `L822`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 640 che coinvolge il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 640 che coinvolge il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 640 che coinvolge il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()` è `1.0`. Nel grafo unificato, l'arco 640 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_configjsontest` per la relazione che coinvolge il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()`. Nel grafo unificato, l'arco 640 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_configjsontest_test_config_json_files_are_valid_and_scrybasign_auth_is_scoped` per la relazione che coinvolge il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()`.

### .test_default_flow_mix_includes_only_known_flows_and_sums_to_one()

<!-- GRAPH_NODE graph=codice index=295 id=tests_test_core_flowbuildtest_test_default_flow_mix_includes_only_known_flows_and_sums_to_one -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `L309`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` possiede l'identificatore esatto `tests_test_core_flowbuildtest_test_default_flow_mix_includes_only_known_flows_and_sums_to_one`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()`.
Nel grafo del codice RDG, il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=606 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_default_flow_mix_includes_only_known_flows_and_sums_to_one`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 606 che coinvolge il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 606 che coinvolge il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 606 che coinvolge il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 606 che coinvolge il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `L309`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 606 che coinvolge il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 606 che coinvolge il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `ast`. Nel grafo del codice RDG, l'arco 606 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()`. Nel grafo del codice RDG, l'arco 606 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_default_flow_mix_includes_only_known_flows_and_sums_to_one` per la relazione che coinvolge il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 606 che coinvolge il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=295 id=Request-Dataset-Generator::tests_test_core_flowbuildtest_test_default_flow_mix_includes_only_known_flows_and_sums_to_one -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `L309`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `tests_test_core_flowbuildtest_test_default_flow_mix_includes_only_known_flows_and_sums_to_one`. Nel grafo unificato, il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_default_flow_mix_includes_only_known_flows_and_sums_to_one`.
Nel grafo unificato, il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=606 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_default_flow_mix_includes_only_known_flows_and_sums_to_one`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()`. Nel grafo unificato, il tipo dichiarato dell'arco 606 che coinvolge il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 606 che coinvolge il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 606 che coinvolge il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 606 che coinvolge il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `L309`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 606 che coinvolge il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 606 che coinvolge il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 606 che coinvolge il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()` è `1.0`. Nel grafo unificato, l'arco 606 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()`. Nel grafo unificato, l'arco 606 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_default_flow_mix_includes_only_known_flows_and_sums_to_one` per la relazione che coinvolge il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()`.

### .test_dispatch_includes_liv3_commands()

<!-- GRAPH_NODE graph=codice index=320 id=tests_test_core_maindispatchtest_test_dispatch_includes_liv3_commands -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_dispatch_includes_liv3_commands()` è `.test_dispatch_includes_liv3_commands()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_dispatch_includes_liv3_commands()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_dispatch_includes_liv3_commands()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_dispatch_includes_liv3_commands()` è `L853`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_dispatch_includes_liv3_commands()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_dispatch_includes_liv3_commands()` possiede l'identificatore esatto `tests_test_core_maindispatchtest_test_dispatch_includes_liv3_commands`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_dispatch_includes_liv3_commands()` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_dispatch_includes_liv3_commands()` è `.test_dispatch_includes_liv3_commands()`.
Nel grafo del codice RDG, il nodo canonico `.test_dispatch_includes_liv3_commands()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=641 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_dispatch_includes_liv3_commands()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MainDispatchTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_maindispatchtest` all'ID `tests_test_core_maindispatchtest_test_dispatch_includes_liv3_commands`. La relazione tecnica esplicita è la seguente: `MainDispatchTest` registra come proprio metodo `.test_dispatch_includes_liv3_commands()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 641 che coinvolge il nodo canonico `.test_dispatch_includes_liv3_commands()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 641 che coinvolge il nodo canonico `.test_dispatch_includes_liv3_commands()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 641 che coinvolge il nodo canonico `.test_dispatch_includes_liv3_commands()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 641 che coinvolge il nodo canonico `.test_dispatch_includes_liv3_commands()` è `L853`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 641 che coinvolge il nodo canonico `.test_dispatch_includes_liv3_commands()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 641 che coinvolge il nodo canonico `.test_dispatch_includes_liv3_commands()` è `ast`. Nel grafo del codice RDG, l'arco 641 registra come `source` l'identificatore `tests_test_core_maindispatchtest` per la relazione che coinvolge il nodo canonico `.test_dispatch_includes_liv3_commands()`. Nel grafo del codice RDG, l'arco 641 registra come `target` l'identificatore `tests_test_core_maindispatchtest_test_dispatch_includes_liv3_commands` per la relazione che coinvolge il nodo canonico `.test_dispatch_includes_liv3_commands()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 641 che coinvolge il nodo canonico `.test_dispatch_includes_liv3_commands()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=320 id=Request-Dataset-Generator::tests_test_core_maindispatchtest_test_dispatch_includes_liv3_commands -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_dispatch_includes_liv3_commands()` è `.test_dispatch_includes_liv3_commands()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_dispatch_includes_liv3_commands()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_dispatch_includes_liv3_commands()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_dispatch_includes_liv3_commands()` è `L853`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_dispatch_includes_liv3_commands()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_dispatch_includes_liv3_commands()` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_dispatch_includes_liv3_commands()` è `.test_dispatch_includes_liv3_commands()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_dispatch_includes_liv3_commands()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_dispatch_includes_liv3_commands()` è `tests_test_core_maindispatchtest_test_dispatch_includes_liv3_commands`. Nel grafo unificato, il nodo canonico `.test_dispatch_includes_liv3_commands()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_maindispatchtest_test_dispatch_includes_liv3_commands`.
Nel grafo unificato, il nodo canonico `.test_dispatch_includes_liv3_commands()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=641 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_dispatch_includes_liv3_commands()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MainDispatchTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_maindispatchtest` all'ID `Request-Dataset-Generator::tests_test_core_maindispatchtest_test_dispatch_includes_liv3_commands`. La relazione tecnica esplicita è la seguente: `MainDispatchTest` registra come proprio metodo `.test_dispatch_includes_liv3_commands()`. Nel grafo unificato, il tipo dichiarato dell'arco 641 che coinvolge il nodo canonico `.test_dispatch_includes_liv3_commands()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 641 che coinvolge il nodo canonico `.test_dispatch_includes_liv3_commands()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 641 che coinvolge il nodo canonico `.test_dispatch_includes_liv3_commands()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 641 che coinvolge il nodo canonico `.test_dispatch_includes_liv3_commands()` è `L853`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 641 che coinvolge il nodo canonico `.test_dispatch_includes_liv3_commands()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 641 che coinvolge il nodo canonico `.test_dispatch_includes_liv3_commands()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 641 che coinvolge il nodo canonico `.test_dispatch_includes_liv3_commands()` è `1.0`. Nel grafo unificato, l'arco 641 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_maindispatchtest` per la relazione che coinvolge il nodo canonico `.test_dispatch_includes_liv3_commands()`. Nel grafo unificato, l'arco 641 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_maindispatchtest_test_dispatch_includes_liv3_commands` per la relazione che coinvolge il nodo canonico `.test_dispatch_includes_liv3_commands()`.

### .test_iti18_and_iti43_use_expected_saml_placeholders()

<!-- GRAPH_NODE graph=codice index=303 id=tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `.test_iti18_and_iti43_use_expected_saml_placeholders()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `L423`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` possiede l'identificatore esatto `tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `.test_iti18_and_iti43_use_expected_saml_placeholders()`.
<!-- GRAPH_EDGE graph=codice index=439 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è la sorgente `source` della relazione `calls` verso il nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `.test_iti18_and_iti43_use_expected_saml_placeholders()` chiama `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 439 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 439 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 439 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 439 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 439 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 439 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `L424`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 439 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 439 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `ast`. Nel grafo del codice RDG, l'arco 439 registra come `source` l'identificatore `tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders` per la relazione che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()`. Nel grafo del codice RDG, l'arco 439 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()`.
<!-- GRAPH_EDGE graph=codice index=607 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_iti18_and_iti43_use_expected_saml_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 607 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 607 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 607 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 607 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `L423`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 607 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 607 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `ast`. Nel grafo del codice RDG, l'arco 607 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()`. Nel grafo del codice RDG, l'arco 607 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders` per la relazione che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 607 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=303 id=Request-Dataset-Generator::tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `.test_iti18_and_iti43_use_expected_saml_placeholders()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `L423`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `.test_iti18_and_iti43_use_expected_saml_placeholders()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders`. Nel grafo unificato, il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders`.
Nel grafo unificato, il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=439 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` chiama `.test_iti18_and_iti43_use_expected_saml_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 439 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 439 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 439 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 439 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 439 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 439 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `L424`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 439 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 439 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `ast`. Nel grafo unificato, l'arco 439 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()`. Nel grafo unificato, l'arco 439 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders` per la relazione che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()`.
<!-- GRAPH_EDGE graph=unificata index=607 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_iti18_and_iti43_use_expected_saml_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 607 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 607 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 607 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 607 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `L423`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 607 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 607 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 607 che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()` è `1.0`. Nel grafo unificato, l'arco 607 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()`. Nel grafo unificato, l'arco 607 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders` per la relazione che coinvolge il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()`.

### .test_live_rve121_matches_soapui_body_shape()

<!-- GRAPH_NODE graph=codice index=307 id=tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `.test_live_rve121_matches_soapui_body_shape()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `L509`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` possiede l'identificatore esatto `tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `.test_live_rve121_matches_soapui_body_shape()`.
<!-- GRAPH_EDGE graph=codice index=329 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `.test_live_rve121_matches_soapui_body_shape()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 329 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 329 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 329 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 329 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 329 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 329 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `L510`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 329 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 329 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `ast`. Nel grafo del codice RDG, l'arco 329 registra come `source` l'identificatore `tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape` per la relazione che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()`. Nel grafo del codice RDG, l'arco 329 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()`.
<!-- GRAPH_EDGE graph=codice index=608 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_live_rve121_matches_soapui_body_shape()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 608 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 608 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 608 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 608 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `L509`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 608 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 608 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `ast`. Nel grafo del codice RDG, l'arco 608 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()`. Nel grafo del codice RDG, l'arco 608 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape` per la relazione che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 608 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=307 id=Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `.test_live_rve121_matches_soapui_body_shape()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `L509`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `.test_live_rve121_matches_soapui_body_shape()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape`. Nel grafo unificato, il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape`.
Nel grafo unificato, il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=329 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `.test_live_rve121_matches_soapui_body_shape()`. Nel grafo unificato, il tipo dichiarato dell'arco 329 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 329 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 329 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 329 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 329 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 329 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `L510`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 329 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 329 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `ast`. Nel grafo unificato, l'arco 329 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()`. Nel grafo unificato, l'arco 329 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape` per la relazione che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()`.
<!-- GRAPH_EDGE graph=unificata index=608 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_live_rve121_matches_soapui_body_shape()`. Nel grafo unificato, il tipo dichiarato dell'arco 608 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 608 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 608 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 608 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `L509`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 608 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 608 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 608 che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()` è `1.0`. Nel grafo unificato, l'arco 608 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()`. Nel grafo unificato, l'arco 608 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape` per la relazione che coinvolge il nodo canonico `.test_live_rve121_matches_soapui_body_shape()`.

### .test_live_rve54_uses_saml_header_and_patient_query_url()

<!-- GRAPH_NODE graph=codice index=305 id=tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `.test_live_rve54_uses_saml_header_and_patient_query_url()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `L455`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` possiede l'identificatore esatto `tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `.test_live_rve54_uses_saml_header_and_patient_query_url()`.
<!-- GRAPH_EDGE graph=codice index=330 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `.test_live_rve54_uses_saml_header_and_patient_query_url()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 330 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 330 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 330 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 330 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 330 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 330 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `L459`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 330 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 330 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `ast`. Nel grafo del codice RDG, l'arco 330 registra come `source` l'identificatore `tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url` per la relazione che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()`. Nel grafo del codice RDG, l'arco 330 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()`.
<!-- GRAPH_EDGE graph=codice index=609 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_live_rve54_uses_saml_header_and_patient_query_url()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 609 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 609 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 609 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 609 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `L455`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 609 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 609 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `ast`. Nel grafo del codice RDG, l'arco 609 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()`. Nel grafo del codice RDG, l'arco 609 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url` per la relazione che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 609 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=305 id=Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `.test_live_rve54_uses_saml_header_and_patient_query_url()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `L455`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `.test_live_rve54_uses_saml_header_and_patient_query_url()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url`. Nel grafo unificato, il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url`.
Nel grafo unificato, il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=330 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `.test_live_rve54_uses_saml_header_and_patient_query_url()`. Nel grafo unificato, il tipo dichiarato dell'arco 330 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 330 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 330 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 330 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 330 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 330 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `L459`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 330 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 330 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `ast`. Nel grafo unificato, l'arco 330 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()`. Nel grafo unificato, l'arco 330 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url` per la relazione che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()`.
<!-- GRAPH_EDGE graph=unificata index=609 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_live_rve54_uses_saml_header_and_patient_query_url()`. Nel grafo unificato, il tipo dichiarato dell'arco 609 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 609 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 609 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 609 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `L455`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 609 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 609 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 609 che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()` è `1.0`. Nel grafo unificato, l'arco 609 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()`. Nel grafo unificato, l'arco 609 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url` per la relazione che coinvolge il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()`.

### .test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()

<!-- GRAPH_NODE graph=codice index=306 id=tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `L481`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` possiede l'identificatore esatto `tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`.
<!-- GRAPH_EDGE graph=codice index=331 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 331 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 331 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 331 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 331 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 331 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 331 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `L482`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 331 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 331 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `ast`. Nel grafo del codice RDG, l'arco 331 registra come `source` l'identificatore `tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper` per la relazione che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`. Nel grafo del codice RDG, l'arco 331 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`.
<!-- GRAPH_EDGE graph=codice index=610 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 610 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 610 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 610 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 610 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `L481`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 610 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 610 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `ast`. Nel grafo del codice RDG, l'arco 610 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`. Nel grafo del codice RDG, l'arco 610 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper` per la relazione che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 610 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=306 id=Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `L481`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper`. Nel grafo unificato, il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper`.
Nel grafo unificato, il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=331 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`. Nel grafo unificato, il tipo dichiarato dell'arco 331 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 331 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 331 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 331 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 331 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 331 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `L482`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 331 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 331 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `ast`. Nel grafo unificato, l'arco 331 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`. Nel grafo unificato, l'arco 331 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper` per la relazione che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`.
<!-- GRAPH_EDGE graph=unificata index=610 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`. Nel grafo unificato, il tipo dichiarato dell'arco 610 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 610 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 610 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 610 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `L481`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 610 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 610 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 610 che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()` è `1.0`. Nel grafo unificato, l'arco 610 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`. Nel grafo unificato, l'arco 610 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper` per la relazione che coinvolge il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`.

### .test_live_rve_1b_declares_mtls_profile()

<!-- GRAPH_NODE graph=codice index=304 id=tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `.test_live_rve_1b_declares_mtls_profile()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `L448`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` possiede l'identificatore esatto `tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `.test_live_rve_1b_declares_mtls_profile()`.
<!-- GRAPH_EDGE graph=codice index=440 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è la sorgente `source` della relazione `calls` verso il nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `.test_live_rve_1b_declares_mtls_profile()` chiama `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 440 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 440 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 440 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 440 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 440 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 440 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `L449`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 440 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 440 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `ast`. Nel grafo del codice RDG, l'arco 440 registra come `source` l'identificatore `tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile` per la relazione che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()`. Nel grafo del codice RDG, l'arco 440 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()`.
<!-- GRAPH_EDGE graph=codice index=611 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_live_rve_1b_declares_mtls_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 611 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 611 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 611 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 611 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `L448`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 611 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 611 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `ast`. Nel grafo del codice RDG, l'arco 611 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()`. Nel grafo del codice RDG, l'arco 611 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile` per la relazione che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 611 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=304 id=Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `.test_live_rve_1b_declares_mtls_profile()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `L448`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `.test_live_rve_1b_declares_mtls_profile()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile`. Nel grafo unificato, il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile`.
Nel grafo unificato, il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=440 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` chiama `.test_live_rve_1b_declares_mtls_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 440 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 440 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 440 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 440 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 440 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 440 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `L449`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 440 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 440 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `ast`. Nel grafo unificato, l'arco 440 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()`. Nel grafo unificato, l'arco 440 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile` per la relazione che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()`.
<!-- GRAPH_EDGE graph=unificata index=611 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_live_rve_1b_declares_mtls_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 611 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 611 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 611 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 611 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `L448`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 611 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 611 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 611 che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()` è `1.0`. Nel grafo unificato, l'arco 611 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()`. Nel grafo unificato, l'arco 611 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile` per la relazione che coinvolge il nodo canonico `.test_live_rve_1b_declares_mtls_profile()`.

### .test_mock_middleware_flow_builds_gateway_steps()

<!-- GRAPH_NODE graph=codice index=308 id=tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `.test_mock_middleware_flow_builds_gateway_steps()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `L534`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` possiede l'identificatore esatto `tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `.test_mock_middleware_flow_builds_gateway_steps()`.
<!-- GRAPH_EDGE graph=codice index=332 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps` all'ID `request_dataset_generator_build_workload`. La relazione tecnica esplicita è la seguente: `.test_mock_middleware_flow_builds_gateway_steps()` chiama `build_workload()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 332 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 332 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 332 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 332 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 332 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 332 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `L535`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 332 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 332 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `ast`. Nel grafo del codice RDG, l'arco 332 registra come `source` l'identificatore `tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps` per la relazione che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()`. Nel grafo del codice RDG, l'arco 332 registra come `target` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()`.
<!-- GRAPH_EDGE graph=codice index=612 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_mock_middleware_flow_builds_gateway_steps()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 612 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 612 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 612 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 612 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `L534`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 612 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 612 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `ast`. Nel grafo del codice RDG, l'arco 612 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()`. Nel grafo del codice RDG, l'arco 612 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps` per la relazione che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 612 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=308 id=Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `.test_mock_middleware_flow_builds_gateway_steps()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `L534`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `.test_mock_middleware_flow_builds_gateway_steps()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps`. Nel grafo unificato, il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps`.
Nel grafo unificato, il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=332 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `.test_mock_middleware_flow_builds_gateway_steps()`. Nel grafo unificato, il tipo dichiarato dell'arco 332 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 332 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 332 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 332 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 332 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 332 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `L535`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 332 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 332 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `ast`. Nel grafo unificato, l'arco 332 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()`. Nel grafo unificato, l'arco 332 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps` per la relazione che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()`.
<!-- GRAPH_EDGE graph=unificata index=612 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_mock_middleware_flow_builds_gateway_steps()`. Nel grafo unificato, il tipo dichiarato dell'arco 612 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 612 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 612 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 612 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `L534`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 612 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 612 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 612 che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()` è `1.0`. Nel grafo unificato, l'arco 612 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()`. Nel grafo unificato, l'arco 612 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps` per la relazione che coinvolge il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()`.

### .test_mock_propagates_saml_output_to_consumers()

<!-- GRAPH_NODE graph=codice index=299 id=tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `.test_mock_propagates_saml_output_to_consumers()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `L357`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` possiede l'identificatore esatto `tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `.test_mock_propagates_saml_output_to_consumers()`.
<!-- GRAPH_EDGE graph=codice index=441 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è la sorgente `source` della relazione `calls` verso il nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `.test_mock_propagates_saml_output_to_consumers()` chiama `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 441 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 441 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 441 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 441 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 441 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 441 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `L358`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 441 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 441 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `ast`. Nel grafo del codice RDG, l'arco 441 registra come `source` l'identificatore `tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers` per la relazione che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()`. Nel grafo del codice RDG, l'arco 441 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()`.
<!-- GRAPH_EDGE graph=codice index=613 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_mock_propagates_saml_output_to_consumers()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 613 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 613 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 613 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 613 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `L357`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 613 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 613 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `ast`. Nel grafo del codice RDG, l'arco 613 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()`. Nel grafo del codice RDG, l'arco 613 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers` per la relazione che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 613 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=299 id=Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `.test_mock_propagates_saml_output_to_consumers()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `L357`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `.test_mock_propagates_saml_output_to_consumers()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers`. Nel grafo unificato, il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers`.
Nel grafo unificato, il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=441 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` chiama `.test_mock_propagates_saml_output_to_consumers()`. Nel grafo unificato, il tipo dichiarato dell'arco 441 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 441 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 441 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 441 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 441 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 441 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `L358`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 441 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 441 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `ast`. Nel grafo unificato, l'arco 441 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()`. Nel grafo unificato, l'arco 441 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers` per la relazione che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()`.
<!-- GRAPH_EDGE graph=unificata index=613 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_mock_propagates_saml_output_to_consumers()`. Nel grafo unificato, il tipo dichiarato dell'arco 613 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 613 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 613 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 613 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `L357`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 613 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 613 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 613 che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()` è `1.0`. Nel grafo unificato, l'arco 613 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()`. Nel grafo unificato, l'arco 613 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers` per la relazione che coinvolge il nodo canonico `.test_mock_propagates_saml_output_to_consumers()`.

### .test_rve1b_declares_and_mocks_all_saml_assertion_outputs()

<!-- GRAPH_NODE graph=codice index=302 id=tests_test_core_flowbuildtest_test_rve1b_declares_and_mocks_all_saml_assertion_outputs -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `L400`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` possiede l'identificatore esatto `tests_test_core_flowbuildtest_test_rve1b_declares_and_mocks_all_saml_assertion_outputs`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()`.
Nel grafo del codice RDG, il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=614 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_rve1b_declares_and_mocks_all_saml_assertion_outputs`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 614 che coinvolge il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 614 che coinvolge il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 614 che coinvolge il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 614 che coinvolge il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `L400`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 614 che coinvolge il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 614 che coinvolge il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `ast`. Nel grafo del codice RDG, l'arco 614 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()`. Nel grafo del codice RDG, l'arco 614 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_rve1b_declares_and_mocks_all_saml_assertion_outputs` per la relazione che coinvolge il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 614 che coinvolge il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=302 id=Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_declares_and_mocks_all_saml_assertion_outputs -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `L400`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `tests_test_core_flowbuildtest_test_rve1b_declares_and_mocks_all_saml_assertion_outputs`. Nel grafo unificato, il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_declares_and_mocks_all_saml_assertion_outputs`.
Nel grafo unificato, il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=614 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_declares_and_mocks_all_saml_assertion_outputs`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 614 che coinvolge il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 614 che coinvolge il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 614 che coinvolge il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 614 che coinvolge il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `L400`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 614 che coinvolge il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 614 che coinvolge il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 614 che coinvolge il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()` è `1.0`. Nel grafo unificato, l'arco 614 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()`. Nel grafo unificato, l'arco 614 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_declares_and_mocks_all_saml_assertion_outputs` per la relazione che coinvolge il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()`.

### .test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()

<!-- GRAPH_NODE graph=codice index=300 id=tests_test_core_flowbuildtest_test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `L371`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` possiede l'identificatore esatto `tests_test_core_flowbuildtest_test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()`.
Nel grafo del codice RDG, il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=615 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 615 che coinvolge il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 615 che coinvolge il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 615 che coinvolge il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 615 che coinvolge il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `L371`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 615 che coinvolge il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 615 che coinvolge il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `ast`. Nel grafo del codice RDG, l'arco 615 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()`. Nel grafo del codice RDG, l'arco 615 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to` per la relazione che coinvolge il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 615 che coinvolge il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=300 id=Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `L371`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `tests_test_core_flowbuildtest_test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to`. Nel grafo unificato, il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to`.
Nel grafo unificato, il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=615 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()`. Nel grafo unificato, il tipo dichiarato dell'arco 615 che coinvolge il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 615 che coinvolge il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 615 che coinvolge il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 615 che coinvolge il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `L371`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 615 che coinvolge il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 615 che coinvolge il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 615 che coinvolge il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()` è `1.0`. Nel grafo unificato, l'arco 615 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()`. Nel grafo unificato, l'arco 615 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to` per la relazione che coinvolge il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()`.

### .test_rve1b_profile_values_do_not_come_from_generated_patient()

<!-- GRAPH_NODE graph=codice index=301 id=tests_test_core_flowbuildtest_test_rve1b_profile_values_do_not_come_from_generated_patient -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `.test_rve1b_profile_values_do_not_come_from_generated_patient()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `L387`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` possiede l'identificatore esatto `tests_test_core_flowbuildtest_test_rve1b_profile_values_do_not_come_from_generated_patient`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `.test_rve1b_profile_values_do_not_come_from_generated_patient()`.
Nel grafo del codice RDG, il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=616 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_rve1b_profile_values_do_not_come_from_generated_patient`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_rve1b_profile_values_do_not_come_from_generated_patient()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 616 che coinvolge il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 616 che coinvolge il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 616 che coinvolge il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 616 che coinvolge il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `L387`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 616 che coinvolge il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 616 che coinvolge il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `ast`. Nel grafo del codice RDG, l'arco 616 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()`. Nel grafo del codice RDG, l'arco 616 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_rve1b_profile_values_do_not_come_from_generated_patient` per la relazione che coinvolge il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 616 che coinvolge il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=301 id=Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_profile_values_do_not_come_from_generated_patient -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `.test_rve1b_profile_values_do_not_come_from_generated_patient()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `L387`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `.test_rve1b_profile_values_do_not_come_from_generated_patient()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `tests_test_core_flowbuildtest_test_rve1b_profile_values_do_not_come_from_generated_patient`. Nel grafo unificato, il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_profile_values_do_not_come_from_generated_patient`.
Nel grafo unificato, il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=616 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_profile_values_do_not_come_from_generated_patient`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_rve1b_profile_values_do_not_come_from_generated_patient()`. Nel grafo unificato, il tipo dichiarato dell'arco 616 che coinvolge il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 616 che coinvolge il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 616 che coinvolge il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 616 che coinvolge il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `L387`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 616 che coinvolge il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 616 che coinvolge il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 616 che coinvolge il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()` è `1.0`. Nel grafo unificato, l'arco 616 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()`. Nel grafo unificato, l'arco 616 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_profile_values_do_not_come_from_generated_patient` per la relazione che coinvolge il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()`.

### .test_scrybasign_flow_has_two_steps_in_order()

<!-- GRAPH_NODE graph=codice index=298 id=tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `.test_scrybasign_flow_has_two_steps_in_order()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `L347`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` possiede l'identificatore esatto `tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `.test_scrybasign_flow_has_two_steps_in_order()`.
<!-- GRAPH_EDGE graph=codice index=442 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è la sorgente `source` della relazione `calls` verso il nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `.test_scrybasign_flow_has_two_steps_in_order()` chiama `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 442 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 442 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 442 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 442 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 442 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 442 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `L348`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 442 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 442 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `ast`. Nel grafo del codice RDG, l'arco 442 registra come `source` l'identificatore `tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order` per la relazione che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()`. Nel grafo del codice RDG, l'arco 442 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()`.
<!-- GRAPH_EDGE graph=codice index=617 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_scrybasign_flow_has_two_steps_in_order()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 617 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 617 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 617 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 617 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `L347`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 617 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 617 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `ast`. Nel grafo del codice RDG, l'arco 617 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()`. Nel grafo del codice RDG, l'arco 617 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order` per la relazione che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 617 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=298 id=Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `.test_scrybasign_flow_has_two_steps_in_order()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `L347`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `.test_scrybasign_flow_has_two_steps_in_order()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order`. Nel grafo unificato, il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order`.
Nel grafo unificato, il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=442 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` chiama `.test_scrybasign_flow_has_two_steps_in_order()`. Nel grafo unificato, il tipo dichiarato dell'arco 442 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 442 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 442 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 442 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 442 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 442 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `L348`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 442 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 442 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `ast`. Nel grafo unificato, l'arco 442 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()`. Nel grafo unificato, l'arco 442 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order` per la relazione che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()`.
<!-- GRAPH_EDGE graph=unificata index=617 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_scrybasign_flow_has_two_steps_in_order()`. Nel grafo unificato, il tipo dichiarato dell'arco 617 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 617 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 617 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 617 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `L347`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 617 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 617 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 617 che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()` è `1.0`. Nel grafo unificato, l'arco 617 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()`. Nel grafo unificato, l'arco 617 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order` per la relazione che coinvolge il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()`.

### .test_scrybasign_get_user_info_step_matches_soap_contract()

<!-- GRAPH_NODE graph=codice index=296 id=tests_test_core_flowbuildtest_test_scrybasign_get_user_info_step_matches_soap_contract -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `.test_scrybasign_get_user_info_step_matches_soap_contract()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `L318`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` possiede l'identificatore esatto `tests_test_core_flowbuildtest_test_scrybasign_get_user_info_step_matches_soap_contract`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `.test_scrybasign_get_user_info_step_matches_soap_contract()`.
Nel grafo del codice RDG, il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=618 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_scrybasign_get_user_info_step_matches_soap_contract`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_scrybasign_get_user_info_step_matches_soap_contract()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 618 che coinvolge il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 618 che coinvolge il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 618 che coinvolge il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 618 che coinvolge il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `L318`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 618 che coinvolge il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 618 che coinvolge il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `ast`. Nel grafo del codice RDG, l'arco 618 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()`. Nel grafo del codice RDG, l'arco 618 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_scrybasign_get_user_info_step_matches_soap_contract` per la relazione che coinvolge il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 618 che coinvolge il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=296 id=Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_get_user_info_step_matches_soap_contract -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `.test_scrybasign_get_user_info_step_matches_soap_contract()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `L318`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `.test_scrybasign_get_user_info_step_matches_soap_contract()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `tests_test_core_flowbuildtest_test_scrybasign_get_user_info_step_matches_soap_contract`. Nel grafo unificato, il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_get_user_info_step_matches_soap_contract`.
Nel grafo unificato, il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=618 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_get_user_info_step_matches_soap_contract`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_scrybasign_get_user_info_step_matches_soap_contract()`. Nel grafo unificato, il tipo dichiarato dell'arco 618 che coinvolge il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 618 che coinvolge il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 618 che coinvolge il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 618 che coinvolge il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `L318`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 618 che coinvolge il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 618 che coinvolge il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 618 che coinvolge il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()` è `1.0`. Nel grafo unificato, l'arco 618 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()`. Nel grafo unificato, l'arco 618 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_get_user_info_step_matches_soap_contract` per la relazione che coinvolge il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()`.

### .test_scrybasign_sign_one_doc_step_matches_soap_contract()

<!-- GRAPH_NODE graph=codice index=297 id=tests_test_core_flowbuildtest_test_scrybasign_sign_one_doc_step_matches_soap_contract -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `.test_scrybasign_sign_one_doc_step_matches_soap_contract()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `L332`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` possiede l'identificatore esatto `tests_test_core_flowbuildtest_test_scrybasign_sign_one_doc_step_matches_soap_contract`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `.test_scrybasign_sign_one_doc_step_matches_soap_contract()`.
Nel grafo del codice RDG, il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=619 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_scrybasign_sign_one_doc_step_matches_soap_contract`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_scrybasign_sign_one_doc_step_matches_soap_contract()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 619 che coinvolge il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 619 che coinvolge il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 619 che coinvolge il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 619 che coinvolge il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `L332`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 619 che coinvolge il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 619 che coinvolge il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `ast`. Nel grafo del codice RDG, l'arco 619 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()`. Nel grafo del codice RDG, l'arco 619 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_scrybasign_sign_one_doc_step_matches_soap_contract` per la relazione che coinvolge il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 619 che coinvolge il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=297 id=Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_sign_one_doc_step_matches_soap_contract -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `.test_scrybasign_sign_one_doc_step_matches_soap_contract()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `L332`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `.test_scrybasign_sign_one_doc_step_matches_soap_contract()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `tests_test_core_flowbuildtest_test_scrybasign_sign_one_doc_step_matches_soap_contract`. Nel grafo unificato, il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_sign_one_doc_step_matches_soap_contract`.
Nel grafo unificato, il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=619 role=incoming -->
Nel grafo unificato, il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_sign_one_doc_step_matches_soap_contract`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_scrybasign_sign_one_doc_step_matches_soap_contract()`. Nel grafo unificato, il tipo dichiarato dell'arco 619 che coinvolge il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 619 che coinvolge il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 619 che coinvolge il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 619 che coinvolge il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `L332`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 619 che coinvolge il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 619 che coinvolge il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 619 che coinvolge il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()` è `1.0`. Nel grafo unificato, l'arco 619 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()`. Nel grafo unificato, l'arco 619 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_sign_one_doc_step_matches_soap_contract` per la relazione che coinvolge il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()`.

### __init__.py [rve_transactions_init]

<!-- GRAPH_NODE graph=codice index=159 id=rve_transactions_init -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `__init__.py [rve_transactions_init]` è `__init__.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `__init__.py [rve_transactions_init]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `__init__.py [rve_transactions_init]` è `rve_transactions/__init__.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `__init__.py [rve_transactions_init]` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `__init__.py [rve_transactions_init]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `__init__.py [rve_transactions_init]` possiede l'identificatore esatto `rve_transactions_init`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `__init__.py [rve_transactions_init]` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `__init__.py [rve_transactions_init]` è `__init__.py`.
<!-- GRAPH_EDGE graph=codice index=336 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `__init__.py [rve_transactions_init]` è la sorgente `source` della relazione `re_exports` verso il nodo canonico `flow_orchestrator.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_init` all'ID `rve_transactions_flow_orchestrator`. La relazione tecnica esplicita è la seguente: `__init__.py [rve_transactions_init]` riesporta `flow_orchestrator.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 336 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `re_exports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 336 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `export`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 336 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 336 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `rve_transactions/__init__.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 336 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `L9`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 336 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 336 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `ast`. Nel grafo del codice RDG, l'arco 336 registra come `source` l'identificatore `rve_transactions_init` per la relazione che coinvolge il nodo canonico `__init__.py [rve_transactions_init]`. Nel grafo del codice RDG, l'arco 336 registra come `target` l'identificatore `rve_transactions_flow_orchestrator` per la relazione che coinvolge il nodo canonico `__init__.py [rve_transactions_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 336 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=337 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `__init__.py [rve_transactions_init]` è la sorgente `source` della relazione `imports` verso il nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_init` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `__init__.py [rve_transactions_init]` importa `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 337 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 337 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 337 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 337 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `rve_transactions/__init__.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 337 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `L9`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 337 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 337 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `ast`. Nel grafo del codice RDG, l'arco 337 registra come `source` l'identificatore `rve_transactions_init` per la relazione che coinvolge il nodo canonico `__init__.py [rve_transactions_init]`. Nel grafo del codice RDG, l'arco 337 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `__init__.py [rve_transactions_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 337 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=338 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `__init__.py [rve_transactions_init]` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_init_rationale_1` all'ID `rve_transactions_init`. La relazione tecnica esplicita è la seguente: `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` costituisce una motivazione per `__init__.py [rve_transactions_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 338 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 338 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 338 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `rve_transactions/__init__.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 338 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 338 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 338 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `ast`. Nel grafo del codice RDG, l'arco 338 registra come `source` l'identificatore `rve_transactions_init_rationale_1` per la relazione che coinvolge il nodo canonico `__init__.py [rve_transactions_init]`. Nel grafo del codice RDG, l'arco 338 registra come `target` l'identificatore `rve_transactions_init` per la relazione che coinvolge il nodo canonico `__init__.py [rve_transactions_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 338 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=159 id=Request-Dataset-Generator::rve_transactions_init -->
Nel grafo unificato, l'etichetta originale del nodo canonico `__init__.py [rve_transactions_init]` è `__init__.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `__init__.py [rve_transactions_init]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `__init__.py [rve_transactions_init]` è `rve_transactions/__init__.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `__init__.py [rve_transactions_init]` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `__init__.py [rve_transactions_init]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `__init__.py [rve_transactions_init]` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `__init__.py [rve_transactions_init]` è `__init__.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `__init__.py [rve_transactions_init]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `__init__.py [rve_transactions_init]` è `rve_transactions_init`. Nel grafo unificato, il nodo canonico `__init__.py [rve_transactions_init]` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_init`.
<!-- GRAPH_EDGE graph=unificata index=336 role=outgoing -->
Nel grafo unificato, il nodo canonico `__init__.py [rve_transactions_init]` è la sorgente `source` della relazione `re_exports` verso il nodo canonico `flow_orchestrator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_init` all'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator`. La relazione tecnica esplicita è la seguente: `__init__.py [rve_transactions_init]` riesporta `flow_orchestrator.py`. Nel grafo unificato, il tipo dichiarato dell'arco 336 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `re_exports`. Nel grafo unificato, il contesto dichiarato per l'arco 336 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `export`. Nel grafo unificato, la confidenza categoriale dell'arco 336 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 336 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `rve_transactions/__init__.py`. Nel grafo unificato, la posizione sorgente dell'arco 336 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `L9`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 336 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 336 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 336 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `1.0`. Nel grafo unificato, l'arco 336 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_init` per la relazione che coinvolge il nodo canonico `__init__.py [rve_transactions_init]`. Nel grafo unificato, l'arco 336 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator` per la relazione che coinvolge il nodo canonico `__init__.py [rve_transactions_init]`.
<!-- GRAPH_EDGE graph=unificata index=337 role=outgoing -->
Nel grafo unificato, il nodo canonico `__init__.py [rve_transactions_init]` è la sorgente `source` della relazione `imports` verso il nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_init` all'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `__init__.py [rve_transactions_init]` importa `FlowOrchestrator`. Nel grafo unificato, il tipo dichiarato dell'arco 337 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 337 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 337 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 337 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `rve_transactions/__init__.py`. Nel grafo unificato, la posizione sorgente dell'arco 337 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `L9`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 337 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 337 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 337 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `1.0`. Nel grafo unificato, l'arco 337 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_init` per la relazione che coinvolge il nodo canonico `__init__.py [rve_transactions_init]`. Nel grafo unificato, l'arco 337 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `__init__.py [rve_transactions_init]`.
<!-- GRAPH_EDGE graph=unificata index=338 role=outgoing -->
Nel grafo unificato, il nodo canonico `__init__.py [rve_transactions_init]` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_init` all'ID `Request-Dataset-Generator::rve_transactions_init_rationale_1`. La relazione tecnica esplicita è la seguente: `__init__.py [rve_transactions_init]` costituisce una motivazione per `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l`. Nel grafo unificato, il tipo dichiarato dell'arco 338 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 338 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 338 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `rve_transactions/__init__.py`. Nel grafo unificato, la posizione sorgente dell'arco 338 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 338 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 338 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 338 che coinvolge il nodo canonico `__init__.py [rve_transactions_init]` è `1.0`. Nel grafo unificato, l'arco 338 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_init` per la relazione che coinvolge il nodo canonico `__init__.py [rve_transactions_init]`. Nel grafo unificato, l'arco 338 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_init_rationale_1` per la relazione che coinvolge il nodo canonico `__init__.py [rve_transactions_init]`.
Nel grafo unificato, il nodo canonico `__init__.py [rve_transactions_init]` non possiede archi entranti registrati nel campo `links`.

### Args:             config: configurazione globale (endpoints, auth, ecc.)

<!-- GRAPH_NODE graph=codice index=198 id=rve_transactions_flow_orchestrator_rationale_142 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `Args:             config: configurazione globale (endpoints, auth, ecc.)`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `rve_transactions/flow_orchestrator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `L142`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` possiede l'identificatore esatto `rve_transactions_flow_orchestrator_rationale_142`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `args:             config: configurazione globale (endpoints, auth, ecc.)`.
<!-- GRAPH_EDGE graph=codice index=446 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_flow_orchestrator_rationale_142` all'ID `rve_transactions_flow_orchestrator_floworchestrator_init`. La relazione tecnica esplicita è la seguente: `Args:             config: configurazione globale (endpoints, auth, ecc.)` costituisce una motivazione per `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 446 che coinvolge il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 446 che coinvolge il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 446 che coinvolge il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `rve_transactions/flow_orchestrator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 446 che coinvolge il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `L142`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 446 che coinvolge il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 446 che coinvolge il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `ast`. Nel grafo del codice RDG, l'arco 446 registra come `source` l'identificatore `rve_transactions_flow_orchestrator_rationale_142` per la relazione che coinvolge il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)`. Nel grafo del codice RDG, l'arco 446 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator_init` per la relazione che coinvolge il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 446 che coinvolge il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=198 id=Request-Dataset-Generator::rve_transactions_flow_orchestrator_rationale_142 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `Args:             config: configurazione globale (endpoints, auth, ecc.)`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `rve_transactions/flow_orchestrator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `L142`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `args:             config: configurazione globale (endpoints, auth, ecc.)`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `rve_transactions_flow_orchestrator_rationale_142`. Nel grafo unificato, il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_flow_orchestrator_rationale_142`.
Nel grafo unificato, il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=446 role=incoming -->
Nel grafo unificato, il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator_init` all'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_rationale_142`. La relazione tecnica esplicita è la seguente: `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]` costituisce una motivazione per `Args:             config: configurazione globale (endpoints, auth, ecc.)`. Nel grafo unificato, il tipo dichiarato dell'arco 446 che coinvolge il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 446 che coinvolge il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 446 che coinvolge il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `rve_transactions/flow_orchestrator.py`. Nel grafo unificato, la posizione sorgente dell'arco 446 che coinvolge il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `L142`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 446 che coinvolge il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 446 che coinvolge il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 446 che coinvolge il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)` è `1.0`. Nel grafo unificato, l'arco 446 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator_init` per la relazione che coinvolge il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)`. Nel grafo unificato, l'arco 446 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_rationale_142` per la relazione che coinvolge il nodo canonico `Args:             config: configurazione globale (endpoints, auth, ecc.)`.

### Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or

<!-- GRAPH_NODE graph=codice index=197 id=rve_transactions_flow_orchestrator_rationale_127 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `rve_transactions/flow_orchestrator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `L127`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` possiede l'identificatore esatto `rve_transactions_flow_orchestrator_rationale_127`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `compone i flussi rve come sequenze di step pre-compilati.      uso::          or`.
<!-- GRAPH_EDGE graph=codice index=431 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_flow_orchestrator_rationale_127` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` costituisce una motivazione per `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 431 che coinvolge il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 431 che coinvolge il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 431 che coinvolge il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `rve_transactions/flow_orchestrator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 431 che coinvolge il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `L127`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 431 che coinvolge il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 431 che coinvolge il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `ast`. Nel grafo del codice RDG, l'arco 431 registra come `source` l'identificatore `rve_transactions_flow_orchestrator_rationale_127` per la relazione che coinvolge il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or`. Nel grafo del codice RDG, l'arco 431 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 431 che coinvolge il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=197 id=Request-Dataset-Generator::rve_transactions_flow_orchestrator_rationale_127 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `rve_transactions/flow_orchestrator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `L127`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `compone i flussi rve come sequenze di step pre-compilati.      uso::          or`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `rve_transactions_flow_orchestrator_rationale_127`. Nel grafo unificato, il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_flow_orchestrator_rationale_127`.
Nel grafo unificato, il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=431 role=incoming -->
Nel grafo unificato, il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_rationale_127`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` costituisce una motivazione per `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or`. Nel grafo unificato, il tipo dichiarato dell'arco 431 che coinvolge il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 431 che coinvolge il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 431 che coinvolge il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `rve_transactions/flow_orchestrator.py`. Nel grafo unificato, la posizione sorgente dell'arco 431 che coinvolge il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `L127`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 431 che coinvolge il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 431 che coinvolge il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 431 che coinvolge il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` è `1.0`. Nel grafo unificato, l'arco 431 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or`. Nel grafo unificato, l'arco 431 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_rationale_127` per la relazione che coinvolge il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or`.

### ConfigJsonTest

<!-- GRAPH_NODE graph=codice index=317 id=tests_test_core_configjsontest -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `ConfigJsonTest` è `ConfigJsonTest`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `ConfigJsonTest` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `ConfigJsonTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `ConfigJsonTest` è `L821`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `ConfigJsonTest` vale `ast`. Nel grafo del codice RDG, il nodo canonico `ConfigJsonTest` possiede l'identificatore esatto `tests_test_core_configjsontest`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `ConfigJsonTest` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `ConfigJsonTest` è `configjsontest`.
<!-- GRAPH_EDGE graph=codice index=433 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConfigJsonTest` è la sorgente `source` della relazione `uses` verso il nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_configjsontest` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `ConfigJsonTest` usa `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 433 che coinvolge il nodo canonico `ConfigJsonTest` è `uses`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 433 che coinvolge il nodo canonico `ConfigJsonTest` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 433 che coinvolge il nodo canonico `ConfigJsonTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 433 che coinvolge il nodo canonico `ConfigJsonTest` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 433 che coinvolge il nodo canonico `ConfigJsonTest` è `0.8`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 433 che coinvolge il nodo canonico `ConfigJsonTest` è `ast`. Nel grafo del codice RDG, l'arco 433 registra come `source` l'identificatore `tests_test_core_configjsontest` per la relazione che coinvolge il nodo canonico `ConfigJsonTest`. Nel grafo del codice RDG, l'arco 433 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `ConfigJsonTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 433 che coinvolge il nodo canonico `ConfigJsonTest` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=640 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConfigJsonTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_configjsontest` all'ID `tests_test_core_configjsontest_test_config_json_files_are_valid_and_scrybasign_auth_is_scoped`. La relazione tecnica esplicita è la seguente: `ConfigJsonTest` registra come proprio metodo `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 640 che coinvolge il nodo canonico `ConfigJsonTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 640 che coinvolge il nodo canonico `ConfigJsonTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 640 che coinvolge il nodo canonico `ConfigJsonTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 640 che coinvolge il nodo canonico `ConfigJsonTest` è `L822`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 640 che coinvolge il nodo canonico `ConfigJsonTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 640 che coinvolge il nodo canonico `ConfigJsonTest` è `ast`. Nel grafo del codice RDG, l'arco 640 registra come `source` l'identificatore `tests_test_core_configjsontest` per la relazione che coinvolge il nodo canonico `ConfigJsonTest`. Nel grafo del codice RDG, l'arco 640 registra come `target` l'identificatore `tests_test_core_configjsontest_test_config_json_files_are_valid_and_scrybasign_auth_is_scoped` per la relazione che coinvolge il nodo canonico `ConfigJsonTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 640 che coinvolge il nodo canonico `ConfigJsonTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=578 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ConfigJsonTest` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `tests_test_core_configjsontest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `ConfigJsonTest`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 578 che coinvolge il nodo canonico `ConfigJsonTest` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 578 che coinvolge il nodo canonico `ConfigJsonTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 578 che coinvolge il nodo canonico `ConfigJsonTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 578 che coinvolge il nodo canonico `ConfigJsonTest` è `L821`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 578 che coinvolge il nodo canonico `ConfigJsonTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 578 che coinvolge il nodo canonico `ConfigJsonTest` è `ast`. Nel grafo del codice RDG, l'arco 578 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `ConfigJsonTest`. Nel grafo del codice RDG, l'arco 578 registra come `target` l'identificatore `tests_test_core_configjsontest` per la relazione che coinvolge il nodo canonico `ConfigJsonTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 578 che coinvolge il nodo canonico `ConfigJsonTest` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=317 id=Request-Dataset-Generator::tests_test_core_configjsontest -->
Nel grafo unificato, l'etichetta originale del nodo canonico `ConfigJsonTest` è `ConfigJsonTest`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `ConfigJsonTest` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `ConfigJsonTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `ConfigJsonTest` è `L821`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `ConfigJsonTest` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `ConfigJsonTest` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `ConfigJsonTest` è `configjsontest`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `ConfigJsonTest` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `ConfigJsonTest` è `tests_test_core_configjsontest`. Nel grafo unificato, il nodo canonico `ConfigJsonTest` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_configjsontest`.
<!-- GRAPH_EDGE graph=unificata index=640 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConfigJsonTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_configjsontest` all'ID `Request-Dataset-Generator::tests_test_core_configjsontest_test_config_json_files_are_valid_and_scrybasign_auth_is_scoped`. La relazione tecnica esplicita è la seguente: `ConfigJsonTest` registra come proprio metodo `.test_config_json_files_are_valid_and_scrybasign_auth_is_scoped()`. Nel grafo unificato, il tipo dichiarato dell'arco 640 che coinvolge il nodo canonico `ConfigJsonTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 640 che coinvolge il nodo canonico `ConfigJsonTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 640 che coinvolge il nodo canonico `ConfigJsonTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 640 che coinvolge il nodo canonico `ConfigJsonTest` è `L822`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 640 che coinvolge il nodo canonico `ConfigJsonTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 640 che coinvolge il nodo canonico `ConfigJsonTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 640 che coinvolge il nodo canonico `ConfigJsonTest` è `1.0`. Nel grafo unificato, l'arco 640 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_configjsontest` per la relazione che coinvolge il nodo canonico `ConfigJsonTest`. Nel grafo unificato, l'arco 640 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_configjsontest_test_config_json_files_are_valid_and_scrybasign_auth_is_scoped` per la relazione che coinvolge il nodo canonico `ConfigJsonTest`.
<!-- GRAPH_EDGE graph=unificata index=433 role=incoming -->
Nel grafo unificato, il nodo canonico `ConfigJsonTest` è il destinatario `target` della relazione `uses` proveniente dal nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_configjsontest`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` usa `ConfigJsonTest`. Nel grafo unificato, il tipo dichiarato dell'arco 433 che coinvolge il nodo canonico `ConfigJsonTest` è `uses`. Nel grafo unificato, la confidenza categoriale dell'arco 433 che coinvolge il nodo canonico `ConfigJsonTest` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 433 che coinvolge il nodo canonico `ConfigJsonTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 433 che coinvolge il nodo canonico `ConfigJsonTest` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 433 che coinvolge il nodo canonico `ConfigJsonTest` è `0.8`. Nel grafo unificato, la provenienza `_origin` dell'arco 433 che coinvolge il nodo canonico `ConfigJsonTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 433 che coinvolge il nodo canonico `ConfigJsonTest` è `0.5`. Nel grafo unificato, l'arco 433 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `ConfigJsonTest`. Nel grafo unificato, l'arco 433 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_configjsontest` per la relazione che coinvolge il nodo canonico `ConfigJsonTest`.
<!-- GRAPH_EDGE graph=unificata index=578 role=incoming -->
Nel grafo unificato, il nodo canonico `ConfigJsonTest` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::tests_test_core_configjsontest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `ConfigJsonTest`. Nel grafo unificato, il tipo dichiarato dell'arco 578 che coinvolge il nodo canonico `ConfigJsonTest` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 578 che coinvolge il nodo canonico `ConfigJsonTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 578 che coinvolge il nodo canonico `ConfigJsonTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 578 che coinvolge il nodo canonico `ConfigJsonTest` è `L821`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 578 che coinvolge il nodo canonico `ConfigJsonTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 578 che coinvolge il nodo canonico `ConfigJsonTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 578 che coinvolge il nodo canonico `ConfigJsonTest` è `1.0`. Nel grafo unificato, l'arco 578 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `ConfigJsonTest`. Nel grafo unificato, l'arco 578 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_configjsontest` per la relazione che coinvolge il nodo canonico `ConfigJsonTest`.

### FlowBuildTest

<!-- GRAPH_NODE graph=codice index=292 id=tests_test_core_flowbuildtest -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `FlowBuildTest` è `FlowBuildTest`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `FlowBuildTest` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `FlowBuildTest` è `L278`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `FlowBuildTest` vale `ast`. Nel grafo del codice RDG, il nodo canonico `FlowBuildTest` possiede l'identificatore esatto `tests_test_core_flowbuildtest`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `FlowBuildTest` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `FlowBuildTest` è `flowbuildtest`.
<!-- GRAPH_EDGE graph=codice index=436 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `uses` verso il nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` usa `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 436 che coinvolge il nodo canonico `FlowBuildTest` è `uses`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 436 che coinvolge il nodo canonico `FlowBuildTest` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 436 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 436 che coinvolge il nodo canonico `FlowBuildTest` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 436 che coinvolge il nodo canonico `FlowBuildTest` è `0.8`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 436 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo del codice RDG, l'arco 436 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, l'arco 436 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 436 che coinvolge il nodo canonico `FlowBuildTest` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=604 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_builds_iti_flows_with_aulss3_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 604 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 604 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 604 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 604 che coinvolge il nodo canonico `FlowBuildTest` è `L292`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 604 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 604 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo del codice RDG, l'arco 604 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, l'arco 604 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 604 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=605 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_builds_supported_flows()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_builds_supported_flows`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_builds_supported_flows()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 605 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 605 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 605 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 605 che coinvolge il nodo canonico `FlowBuildTest` è `L279`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 605 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 605 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo del codice RDG, l'arco 605 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, l'arco 605 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_builds_supported_flows` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 605 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=606 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_default_flow_mix_includes_only_known_flows_and_sums_to_one`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 606 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 606 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 606 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 606 che coinvolge il nodo canonico `FlowBuildTest` è `L309`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 606 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 606 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo del codice RDG, l'arco 606 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, l'arco 606 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_default_flow_mix_includes_only_known_flows_and_sums_to_one` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 606 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=607 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_iti18_and_iti43_use_expected_saml_placeholders()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 607 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 607 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 607 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 607 che coinvolge il nodo canonico `FlowBuildTest` è `L423`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 607 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 607 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo del codice RDG, l'arco 607 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, l'arco 607 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 607 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=608 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_live_rve121_matches_soapui_body_shape()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_live_rve121_matches_soapui_body_shape()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 608 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 608 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 608 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 608 che coinvolge il nodo canonico `FlowBuildTest` è `L509`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 608 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 608 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo del codice RDG, l'arco 608 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, l'arco 608 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 608 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=609 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_live_rve54_uses_saml_header_and_patient_query_url()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 609 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 609 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 609 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 609 che coinvolge il nodo canonico `FlowBuildTest` è `L455`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 609 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 609 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo del codice RDG, l'arco 609 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, l'arco 609 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 609 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=610 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 610 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 610 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 610 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 610 che coinvolge il nodo canonico `FlowBuildTest` è `L481`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 610 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 610 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo del codice RDG, l'arco 610 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, l'arco 610 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 610 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=611 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_live_rve_1b_declares_mtls_profile()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_live_rve_1b_declares_mtls_profile()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 611 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 611 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 611 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 611 che coinvolge il nodo canonico `FlowBuildTest` è `L448`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 611 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 611 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo del codice RDG, l'arco 611 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, l'arco 611 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 611 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=612 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_mock_middleware_flow_builds_gateway_steps()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 612 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 612 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 612 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 612 che coinvolge il nodo canonico `FlowBuildTest` è `L534`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 612 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 612 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo del codice RDG, l'arco 612 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, l'arco 612 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 612 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=613 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_mock_propagates_saml_output_to_consumers()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_mock_propagates_saml_output_to_consumers()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 613 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 613 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 613 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 613 che coinvolge il nodo canonico `FlowBuildTest` è `L357`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 613 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 613 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo del codice RDG, l'arco 613 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, l'arco 613 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 613 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=614 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_rve1b_declares_and_mocks_all_saml_assertion_outputs`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 614 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 614 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 614 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 614 che coinvolge il nodo canonico `FlowBuildTest` è `L400`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 614 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 614 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo del codice RDG, l'arco 614 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, l'arco 614 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_rve1b_declares_and_mocks_all_saml_assertion_outputs` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 614 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=615 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 615 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 615 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 615 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 615 che coinvolge il nodo canonico `FlowBuildTest` è `L371`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 615 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 615 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo del codice RDG, l'arco 615 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, l'arco 615 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 615 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=616 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_rve1b_profile_values_do_not_come_from_generated_patient`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_rve1b_profile_values_do_not_come_from_generated_patient()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 616 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 616 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 616 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 616 che coinvolge il nodo canonico `FlowBuildTest` è `L387`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 616 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 616 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo del codice RDG, l'arco 616 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, l'arco 616 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_rve1b_profile_values_do_not_come_from_generated_patient` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 616 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=617 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_scrybasign_flow_has_two_steps_in_order()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 617 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 617 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 617 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 617 che coinvolge il nodo canonico `FlowBuildTest` è `L347`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 617 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 617 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo del codice RDG, l'arco 617 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, l'arco 617 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 617 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=618 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_scrybasign_get_user_info_step_matches_soap_contract`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_scrybasign_get_user_info_step_matches_soap_contract()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 618 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 618 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 618 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 618 che coinvolge il nodo canonico `FlowBuildTest` è `L318`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 618 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 618 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo del codice RDG, l'arco 618 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, l'arco 618 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_scrybasign_get_user_info_step_matches_soap_contract` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 618 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=619 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `tests_test_core_flowbuildtest_test_scrybasign_sign_one_doc_step_matches_soap_contract`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_scrybasign_sign_one_doc_step_matches_soap_contract()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 619 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 619 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 619 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 619 che coinvolge il nodo canonico `FlowBuildTest` è `L332`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 619 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 619 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo del codice RDG, l'arco 619 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, l'arco 619 registra come `target` l'identificatore `tests_test_core_flowbuildtest_test_scrybasign_sign_one_doc_step_matches_soap_contract` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 619 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=581 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FlowBuildTest` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `tests_test_core_flowbuildtest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `FlowBuildTest`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 581 che coinvolge il nodo canonico `FlowBuildTest` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 581 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 581 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 581 che coinvolge il nodo canonico `FlowBuildTest` è `L278`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 581 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 581 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo del codice RDG, l'arco 581 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, l'arco 581 registra come `target` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 581 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=292 id=Request-Dataset-Generator::tests_test_core_flowbuildtest -->
Nel grafo unificato, l'etichetta originale del nodo canonico `FlowBuildTest` è `FlowBuildTest`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `FlowBuildTest` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `FlowBuildTest` è `L278`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `FlowBuildTest` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `FlowBuildTest` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `FlowBuildTest` è `flowbuildtest`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `FlowBuildTest` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `FlowBuildTest` è `tests_test_core_flowbuildtest`. Nel grafo unificato, il nodo canonico `FlowBuildTest` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_flowbuildtest`.
<!-- GRAPH_EDGE graph=unificata index=604 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_builds_iti_flows_with_aulss3_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 604 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 604 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 604 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 604 che coinvolge il nodo canonico `FlowBuildTest` è `L292`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 604 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 604 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 604 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, l'arco 604 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo unificato, l'arco 604 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile` per la relazione che coinvolge il nodo canonico `FlowBuildTest`.
<!-- GRAPH_EDGE graph=unificata index=605 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_builds_supported_flows()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_supported_flows`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_builds_supported_flows()`. Nel grafo unificato, il tipo dichiarato dell'arco 605 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 605 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 605 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 605 che coinvolge il nodo canonico `FlowBuildTest` è `L279`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 605 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 605 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 605 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, l'arco 605 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo unificato, l'arco 605 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_supported_flows` per la relazione che coinvolge il nodo canonico `FlowBuildTest`.
<!-- GRAPH_EDGE graph=unificata index=606 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_default_flow_mix_includes_only_known_flows_and_sums_to_one`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_default_flow_mix_includes_only_known_flows_and_sums_to_one()`. Nel grafo unificato, il tipo dichiarato dell'arco 606 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 606 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 606 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 606 che coinvolge il nodo canonico `FlowBuildTest` è `L309`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 606 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 606 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 606 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, l'arco 606 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo unificato, l'arco 606 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_default_flow_mix_includes_only_known_flows_and_sums_to_one` per la relazione che coinvolge il nodo canonico `FlowBuildTest`.
<!-- GRAPH_EDGE graph=unificata index=607 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_iti18_and_iti43_use_expected_saml_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 607 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 607 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 607 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 607 che coinvolge il nodo canonico `FlowBuildTest` è `L423`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 607 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 607 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 607 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, l'arco 607 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo unificato, l'arco 607 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders` per la relazione che coinvolge il nodo canonico `FlowBuildTest`.
<!-- GRAPH_EDGE graph=unificata index=608 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_live_rve121_matches_soapui_body_shape()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_live_rve121_matches_soapui_body_shape()`. Nel grafo unificato, il tipo dichiarato dell'arco 608 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 608 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 608 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 608 che coinvolge il nodo canonico `FlowBuildTest` è `L509`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 608 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 608 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 608 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, l'arco 608 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo unificato, l'arco 608 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve121_matches_soapui_body_shape` per la relazione che coinvolge il nodo canonico `FlowBuildTest`.
<!-- GRAPH_EDGE graph=unificata index=609 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_live_rve54_uses_saml_header_and_patient_query_url()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_live_rve54_uses_saml_header_and_patient_query_url()`. Nel grafo unificato, il tipo dichiarato dell'arco 609 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 609 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 609 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 609 che coinvolge il nodo canonico `FlowBuildTest` è `L455`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 609 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 609 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 609 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, l'arco 609 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo unificato, l'arco 609 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve54_uses_saml_header_and_patient_query_url` per la relazione che coinvolge il nodo canonico `FlowBuildTest`.
<!-- GRAPH_EDGE graph=unificata index=610 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper()`. Nel grafo unificato, il tipo dichiarato dell'arco 610 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 610 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 610 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 610 che coinvolge il nodo canonico `FlowBuildTest` è `L481`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 610 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 610 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 610 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, l'arco 610 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo unificato, l'arco 610 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve55_uses_soapui_headers_and_fhir_resource_wrapper` per la relazione che coinvolge il nodo canonico `FlowBuildTest`.
<!-- GRAPH_EDGE graph=unificata index=611 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_live_rve_1b_declares_mtls_profile()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_live_rve_1b_declares_mtls_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 611 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 611 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 611 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 611 che coinvolge il nodo canonico `FlowBuildTest` è `L448`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 611 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 611 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 611 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, l'arco 611 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo unificato, l'arco 611 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile` per la relazione che coinvolge il nodo canonico `FlowBuildTest`.
<!-- GRAPH_EDGE graph=unificata index=612 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_mock_middleware_flow_builds_gateway_steps()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_mock_middleware_flow_builds_gateway_steps()`. Nel grafo unificato, il tipo dichiarato dell'arco 612 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 612 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 612 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 612 che coinvolge il nodo canonico `FlowBuildTest` è `L534`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 612 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 612 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 612 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, l'arco 612 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo unificato, l'arco 612 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_middleware_flow_builds_gateway_steps` per la relazione che coinvolge il nodo canonico `FlowBuildTest`.
<!-- GRAPH_EDGE graph=unificata index=613 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_mock_propagates_saml_output_to_consumers()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_mock_propagates_saml_output_to_consumers()`. Nel grafo unificato, il tipo dichiarato dell'arco 613 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 613 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 613 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 613 che coinvolge il nodo canonico `FlowBuildTest` è `L357`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 613 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 613 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 613 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, l'arco 613 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo unificato, l'arco 613 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers` per la relazione che coinvolge il nodo canonico `FlowBuildTest`.
<!-- GRAPH_EDGE graph=unificata index=614 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_declares_and_mocks_all_saml_assertion_outputs`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_rve1b_declares_and_mocks_all_saml_assertion_outputs()`. Nel grafo unificato, il tipo dichiarato dell'arco 614 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 614 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 614 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 614 che coinvolge il nodo canonico `FlowBuildTest` è `L400`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 614 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 614 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 614 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, l'arco 614 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo unificato, l'arco 614 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_declares_and_mocks_all_saml_assertion_outputs` per la relazione che coinvolge il nodo canonico `FlowBuildTest`.
<!-- GRAPH_EDGE graph=unificata index=615 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to()`. Nel grafo unificato, il tipo dichiarato dell'arco 615 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 615 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 615 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 615 che coinvolge il nodo canonico `FlowBuildTest` è `L371`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 615 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 615 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 615 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, l'arco 615 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo unificato, l'arco 615 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_lab_and_aulss3_profiles_keep_http_endpoint_separate_from_wsa_to` per la relazione che coinvolge il nodo canonico `FlowBuildTest`.
<!-- GRAPH_EDGE graph=unificata index=616 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_rve1b_profile_values_do_not_come_from_generated_patient()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_profile_values_do_not_come_from_generated_patient`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_rve1b_profile_values_do_not_come_from_generated_patient()`. Nel grafo unificato, il tipo dichiarato dell'arco 616 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 616 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 616 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 616 che coinvolge il nodo canonico `FlowBuildTest` è `L387`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 616 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 616 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 616 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, l'arco 616 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo unificato, l'arco 616 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_rve1b_profile_values_do_not_come_from_generated_patient` per la relazione che coinvolge il nodo canonico `FlowBuildTest`.
<!-- GRAPH_EDGE graph=unificata index=617 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_scrybasign_flow_has_two_steps_in_order()`. Nel grafo unificato, il tipo dichiarato dell'arco 617 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 617 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 617 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 617 che coinvolge il nodo canonico `FlowBuildTest` è `L347`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 617 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 617 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 617 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, l'arco 617 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo unificato, l'arco 617 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order` per la relazione che coinvolge il nodo canonico `FlowBuildTest`.
<!-- GRAPH_EDGE graph=unificata index=618 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_scrybasign_get_user_info_step_matches_soap_contract()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_get_user_info_step_matches_soap_contract`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_scrybasign_get_user_info_step_matches_soap_contract()`. Nel grafo unificato, il tipo dichiarato dell'arco 618 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 618 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 618 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 618 che coinvolge il nodo canonico `FlowBuildTest` è `L318`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 618 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 618 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 618 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, l'arco 618 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo unificato, l'arco 618 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_get_user_info_step_matches_soap_contract` per la relazione che coinvolge il nodo canonico `FlowBuildTest`.
<!-- GRAPH_EDGE graph=unificata index=619 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowBuildTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_scrybasign_sign_one_doc_step_matches_soap_contract()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_sign_one_doc_step_matches_soap_contract`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` registra come proprio metodo `.test_scrybasign_sign_one_doc_step_matches_soap_contract()`. Nel grafo unificato, il tipo dichiarato dell'arco 619 che coinvolge il nodo canonico `FlowBuildTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 619 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 619 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 619 che coinvolge il nodo canonico `FlowBuildTest` è `L332`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 619 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 619 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 619 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, l'arco 619 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo unificato, l'arco 619 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_sign_one_doc_step_matches_soap_contract` per la relazione che coinvolge il nodo canonico `FlowBuildTest`.
<!-- GRAPH_EDGE graph=unificata index=436 role=incoming -->
Nel grafo unificato, il nodo canonico `FlowBuildTest` è il destinatario `target` della relazione `uses` proveniente dal nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` usa `FlowBuildTest`. Nel grafo unificato, il tipo dichiarato dell'arco 436 che coinvolge il nodo canonico `FlowBuildTest` è `uses`. Nel grafo unificato, la confidenza categoriale dell'arco 436 che coinvolge il nodo canonico `FlowBuildTest` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 436 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 436 che coinvolge il nodo canonico `FlowBuildTest` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 436 che coinvolge il nodo canonico `FlowBuildTest` è `0.8`. Nel grafo unificato, la provenienza `_origin` dell'arco 436 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 436 che coinvolge il nodo canonico `FlowBuildTest` è `0.5`. Nel grafo unificato, l'arco 436 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo unificato, l'arco 436 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`.
<!-- GRAPH_EDGE graph=unificata index=581 role=incoming -->
Nel grafo unificato, il nodo canonico `FlowBuildTest` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `FlowBuildTest`. Nel grafo unificato, il tipo dichiarato dell'arco 581 che coinvolge il nodo canonico `FlowBuildTest` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 581 che coinvolge il nodo canonico `FlowBuildTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 581 che coinvolge il nodo canonico `FlowBuildTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 581 che coinvolge il nodo canonico `FlowBuildTest` è `L278`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 581 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 581 che coinvolge il nodo canonico `FlowBuildTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 581 che coinvolge il nodo canonico `FlowBuildTest` è `1.0`. Nel grafo unificato, l'arco 581 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `FlowBuildTest`. Nel grafo unificato, l'arco 581 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowBuildTest`.

### FlowOrchestrator

<!-- GRAPH_NODE graph=codice index=191 id=rve_transactions_flow_orchestrator_floworchestrator -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `FlowOrchestrator` è `FlowOrchestrator`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `FlowOrchestrator` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `FlowOrchestrator` è `rve_transactions/flow_orchestrator.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `FlowOrchestrator` è `L126`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `FlowOrchestrator` vale `ast`. Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` possiede l'identificatore esatto `rve_transactions_flow_orchestrator_floworchestrator`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `FlowOrchestrator` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `FlowOrchestrator` è `floworchestrator`.
<!-- GRAPH_EDGE graph=codice index=429 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è la sorgente `source` della relazione `method` verso il nodo canonico `.build_flow()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_flow_orchestrator_floworchestrator` all'ID `rve_transactions_flow_orchestrator_floworchestrator_build_flow`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` registra come proprio metodo `.build_flow()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 429 che coinvolge il nodo canonico `FlowOrchestrator` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 429 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 429 che coinvolge il nodo canonico `FlowOrchestrator` è `rve_transactions/flow_orchestrator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 429 che coinvolge il nodo canonico `FlowOrchestrator` è `L152`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 429 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 429 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 429 registra come `source` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 429 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator_build_flow` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 429 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=430 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_flow_orchestrator_floworchestrator` all'ID `rve_transactions_flow_orchestrator_floworchestrator_init`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` registra come proprio metodo `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 430 che coinvolge il nodo canonico `FlowOrchestrator` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 430 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 430 che coinvolge il nodo canonico `FlowOrchestrator` è `rve_transactions/flow_orchestrator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 430 che coinvolge il nodo canonico `FlowOrchestrator` è `L141`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 430 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 430 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 430 registra come `source` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 430 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator_init` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 430 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=318 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `request_dataset_generator.py`; l'orientamento registrato nel JSON va dall'ID `request_dataset_generator` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `request_dataset_generator.py` importa `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 318 che coinvolge il nodo canonico `FlowOrchestrator` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 318 che coinvolge il nodo canonico `FlowOrchestrator` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 318 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 318 che coinvolge il nodo canonico `FlowOrchestrator` è `request_dataset_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 318 che coinvolge il nodo canonico `FlowOrchestrator` è `L37`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 318 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 318 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 318 registra come `source` l'identificatore `request_dataset_generator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 318 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 318 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=322 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `request_dataset_generator_build_workload` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 322 che coinvolge il nodo canonico `FlowOrchestrator` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 322 che coinvolge il nodo canonico `FlowOrchestrator` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 322 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 322 che coinvolge il nodo canonico `FlowOrchestrator` è `request_dataset_generator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 322 che coinvolge il nodo canonico `FlowOrchestrator` è `L153`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 322 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 322 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 322 registra come `source` l'identificatore `request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 322 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 322 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=337 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `__init__.py [rve_transactions_init]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_init` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `__init__.py [rve_transactions_init]` importa `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 337 che coinvolge il nodo canonico `FlowOrchestrator` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 337 che coinvolge il nodo canonico `FlowOrchestrator` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 337 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 337 che coinvolge il nodo canonico `FlowOrchestrator` è `rve_transactions/__init__.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 337 che coinvolge il nodo canonico `FlowOrchestrator` è `L9`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 337 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 337 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 337 registra come `source` l'identificatore `rve_transactions_init` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 337 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 337 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=405 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `flow_orchestrator.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_flow_orchestrator` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `flow_orchestrator.py` contiene `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 405 che coinvolge il nodo canonico `FlowOrchestrator` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 405 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 405 che coinvolge il nodo canonico `FlowOrchestrator` è `rve_transactions/flow_orchestrator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 405 che coinvolge il nodo canonico `FlowOrchestrator` è `L126`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 405 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 405 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 405 registra come `source` l'identificatore `rve_transactions_flow_orchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 405 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 405 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=431 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_flow_orchestrator_rationale_127` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or` costituisce una motivazione per `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 431 che coinvolge il nodo canonico `FlowOrchestrator` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 431 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 431 che coinvolge il nodo canonico `FlowOrchestrator` è `rve_transactions/flow_orchestrator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 431 che coinvolge il nodo canonico `FlowOrchestrator` è `L127`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 431 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 431 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 431 registra come `source` l'identificatore `rve_transactions_flow_orchestrator_rationale_127` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 431 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 431 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=432 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `test_core.py` importa `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 432 che coinvolge il nodo canonico `FlowOrchestrator` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 432 che coinvolge il nodo canonico `FlowOrchestrator` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 432 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 432 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 432 che coinvolge il nodo canonico `FlowOrchestrator` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 432 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 432 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 432 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 432 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 432 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=433 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `uses` proveniente dal nodo canonico `ConfigJsonTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_configjsontest` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `ConfigJsonTest` usa `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 433 che coinvolge il nodo canonico `FlowOrchestrator` è `uses`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 433 che coinvolge il nodo canonico `FlowOrchestrator` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 433 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 433 che coinvolge il nodo canonico `FlowOrchestrator` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 433 che coinvolge il nodo canonico `FlowOrchestrator` è `0.8`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 433 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 433 registra come `source` l'identificatore `tests_test_core_configjsontest` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 433 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 433 che coinvolge il nodo canonico `FlowOrchestrator` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=434 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `uses` proveniente dal nodo canonico `EngineSmokeTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_enginesmoketest` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `EngineSmokeTest` usa `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 434 che coinvolge il nodo canonico `FlowOrchestrator` è `uses`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 434 che coinvolge il nodo canonico `FlowOrchestrator` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 434 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 434 che coinvolge il nodo canonico `FlowOrchestrator` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 434 che coinvolge il nodo canonico `FlowOrchestrator` è `0.8`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 434 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 434 registra come `source` l'identificatore `tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 434 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 434 che coinvolge il nodo canonico `FlowOrchestrator` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=435 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `uses` proveniente dal nodo canonico `ExtractionTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_extractiontest` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `ExtractionTest` usa `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 435 che coinvolge il nodo canonico `FlowOrchestrator` è `uses`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 435 che coinvolge il nodo canonico `FlowOrchestrator` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 435 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 435 che coinvolge il nodo canonico `FlowOrchestrator` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 435 che coinvolge il nodo canonico `FlowOrchestrator` è `0.8`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 435 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 435 registra come `source` l'identificatore `tests_test_core_extractiontest` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 435 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 435 che coinvolge il nodo canonico `FlowOrchestrator` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=436 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `uses` proveniente dal nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `FlowBuildTest` usa `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 436 che coinvolge il nodo canonico `FlowOrchestrator` è `uses`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 436 che coinvolge il nodo canonico `FlowOrchestrator` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 436 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 436 che coinvolge il nodo canonico `FlowOrchestrator` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 436 che coinvolge il nodo canonico `FlowOrchestrator` è `0.8`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 436 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 436 registra come `source` l'identificatore `tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 436 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 436 che coinvolge il nodo canonico `FlowOrchestrator` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=437 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_builds_iti_flows_with_aulss3_profile()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `.test_builds_iti_flows_with_aulss3_profile()` chiama `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 437 che coinvolge il nodo canonico `FlowOrchestrator` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 437 che coinvolge il nodo canonico `FlowOrchestrator` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 437 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 437 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 437 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 437 che coinvolge il nodo canonico `FlowOrchestrator` è `L298`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 437 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 437 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 437 registra come `source` l'identificatore `tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 437 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=codice index=438 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_builds_supported_flows()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest_test_builds_supported_flows` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `.test_builds_supported_flows()` chiama `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 438 che coinvolge il nodo canonico `FlowOrchestrator` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 438 che coinvolge il nodo canonico `FlowOrchestrator` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 438 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 438 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 438 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 438 che coinvolge il nodo canonico `FlowOrchestrator` è `L287`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 438 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 438 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 438 registra come `source` l'identificatore `tests_test_core_flowbuildtest_test_builds_supported_flows` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 438 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=codice index=439 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `.test_iti18_and_iti43_use_expected_saml_placeholders()` chiama `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 439 che coinvolge il nodo canonico `FlowOrchestrator` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 439 che coinvolge il nodo canonico `FlowOrchestrator` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 439 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 439 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 439 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 439 che coinvolge il nodo canonico `FlowOrchestrator` è `L424`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 439 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 439 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 439 registra come `source` l'identificatore `tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 439 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=codice index=440 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_live_rve_1b_declares_mtls_profile()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `.test_live_rve_1b_declares_mtls_profile()` chiama `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 440 che coinvolge il nodo canonico `FlowOrchestrator` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 440 che coinvolge il nodo canonico `FlowOrchestrator` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 440 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 440 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 440 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 440 che coinvolge il nodo canonico `FlowOrchestrator` è `L449`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 440 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 440 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 440 registra come `source` l'identificatore `tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 440 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=codice index=441 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_mock_propagates_saml_output_to_consumers()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `.test_mock_propagates_saml_output_to_consumers()` chiama `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 441 che coinvolge il nodo canonico `FlowOrchestrator` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 441 che coinvolge il nodo canonico `FlowOrchestrator` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 441 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 441 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 441 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 441 che coinvolge il nodo canonico `FlowOrchestrator` è `L358`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 441 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 441 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 441 registra come `source` l'identificatore `tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 441 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=codice index=442 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `.test_scrybasign_flow_has_two_steps_in_order()` chiama `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 442 che coinvolge il nodo canonico `FlowOrchestrator` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 442 che coinvolge il nodo canonico `FlowOrchestrator` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 442 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 442 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 442 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 442 che coinvolge il nodo canonico `FlowOrchestrator` è `L348`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 442 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 442 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 442 registra come `source` l'identificatore `tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 442 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=codice index=443 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `uses` proveniente dal nodo canonico `MainDispatchTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_maindispatchtest` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `MainDispatchTest` usa `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 443 che coinvolge il nodo canonico `FlowOrchestrator` è `uses`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 443 che coinvolge il nodo canonico `FlowOrchestrator` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 443 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 443 che coinvolge il nodo canonico `FlowOrchestrator` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 443 che coinvolge il nodo canonico `FlowOrchestrator` è `0.8`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 443 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 443 registra come `source` l'identificatore `tests_test_core_maindispatchtest` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 443 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 443 che coinvolge il nodo canonico `FlowOrchestrator` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=444 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `uses` proveniente dal nodo canonico `PlaceholderTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_placeholdertest` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `PlaceholderTest` usa `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 444 che coinvolge il nodo canonico `FlowOrchestrator` è `uses`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 444 che coinvolge il nodo canonico `FlowOrchestrator` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 444 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 444 che coinvolge il nodo canonico `FlowOrchestrator` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 444 che coinvolge il nodo canonico `FlowOrchestrator` è `0.8`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 444 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 444 registra come `source` l'identificatore `tests_test_core_placeholdertest` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 444 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 444 che coinvolge il nodo canonico `FlowOrchestrator` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=445 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `uses` proveniente dal nodo canonico `TimelineIdentityTest`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_timelineidentitytest` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `TimelineIdentityTest` usa `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 445 che coinvolge il nodo canonico `FlowOrchestrator` è `uses`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 445 che coinvolge il nodo canonico `FlowOrchestrator` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 445 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 445 che coinvolge il nodo canonico `FlowOrchestrator` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 445 che coinvolge il nodo canonico `FlowOrchestrator` è `0.8`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 445 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo del codice RDG, l'arco 445 registra come `source` l'identificatore `tests_test_core_timelineidentitytest` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, l'arco 445 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 445 che coinvolge il nodo canonico `FlowOrchestrator` è `0.5`.

<!-- GRAPH_NODE graph=unificata index=191 id=Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator -->
Nel grafo unificato, l'etichetta originale del nodo canonico `FlowOrchestrator` è `FlowOrchestrator`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `FlowOrchestrator` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `FlowOrchestrator` è `rve_transactions/flow_orchestrator.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `FlowOrchestrator` è `L126`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `FlowOrchestrator` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `FlowOrchestrator` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `FlowOrchestrator` è `floworchestrator`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `FlowOrchestrator` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `FlowOrchestrator` è `rve_transactions_flow_orchestrator_floworchestrator`. Nel grafo unificato, il nodo canonico `FlowOrchestrator` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator`.
<!-- GRAPH_EDGE graph=unificata index=429 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è la sorgente `source` della relazione `method` verso il nodo canonico `.build_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator_build_flow`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` registra come proprio metodo `.build_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 429 che coinvolge il nodo canonico `FlowOrchestrator` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 429 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 429 che coinvolge il nodo canonico `FlowOrchestrator` è `rve_transactions/flow_orchestrator.py`. Nel grafo unificato, la posizione sorgente dell'arco 429 che coinvolge il nodo canonico `FlowOrchestrator` è `L152`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 429 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 429 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 429 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, l'arco 429 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 429 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator_build_flow` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=unificata index=430 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator_init`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` registra come proprio metodo `.__init__() [rve_transactions_flow_orchestrator_floworchestrator_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 430 che coinvolge il nodo canonico `FlowOrchestrator` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 430 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 430 che coinvolge il nodo canonico `FlowOrchestrator` è `rve_transactions/flow_orchestrator.py`. Nel grafo unificato, la posizione sorgente dell'arco 430 che coinvolge il nodo canonico `FlowOrchestrator` è `L141`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 430 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 430 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 430 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, l'arco 430 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 430 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator_init` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=unificata index=431 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_rationale_127`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` costituisce una motivazione per `Compone i flussi RVE come sequenze di step pre-compilati.      Uso::          or`. Nel grafo unificato, il tipo dichiarato dell'arco 431 che coinvolge il nodo canonico `FlowOrchestrator` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 431 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 431 che coinvolge il nodo canonico `FlowOrchestrator` è `rve_transactions/flow_orchestrator.py`. Nel grafo unificato, la posizione sorgente dell'arco 431 che coinvolge il nodo canonico `FlowOrchestrator` è `L127`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 431 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 431 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 431 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, l'arco 431 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 431 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_rationale_127` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=unificata index=432 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è la sorgente `source` della relazione `imports` verso il nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` importa `test_core.py`. Nel grafo unificato, il tipo dichiarato dell'arco 432 che coinvolge il nodo canonico `FlowOrchestrator` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 432 che coinvolge il nodo canonico `FlowOrchestrator` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 432 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 432 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 432 che coinvolge il nodo canonico `FlowOrchestrator` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 432 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 432 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 432 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, l'arco 432 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 432 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=unificata index=433 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è la sorgente `source` della relazione `uses` verso il nodo canonico `ConfigJsonTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_configjsontest`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` usa `ConfigJsonTest`. Nel grafo unificato, il tipo dichiarato dell'arco 433 che coinvolge il nodo canonico `FlowOrchestrator` è `uses`. Nel grafo unificato, la confidenza categoriale dell'arco 433 che coinvolge il nodo canonico `FlowOrchestrator` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 433 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 433 che coinvolge il nodo canonico `FlowOrchestrator` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 433 che coinvolge il nodo canonico `FlowOrchestrator` è `0.8`. Nel grafo unificato, la provenienza `_origin` dell'arco 433 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 433 che coinvolge il nodo canonico `FlowOrchestrator` è `0.5`. Nel grafo unificato, l'arco 433 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 433 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_configjsontest` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=unificata index=434 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è la sorgente `source` della relazione `uses` verso il nodo canonico `EngineSmokeTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_enginesmoketest`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` usa `EngineSmokeTest`. Nel grafo unificato, il tipo dichiarato dell'arco 434 che coinvolge il nodo canonico `FlowOrchestrator` è `uses`. Nel grafo unificato, la confidenza categoriale dell'arco 434 che coinvolge il nodo canonico `FlowOrchestrator` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 434 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 434 che coinvolge il nodo canonico `FlowOrchestrator` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 434 che coinvolge il nodo canonico `FlowOrchestrator` è `0.8`. Nel grafo unificato, la provenienza `_origin` dell'arco 434 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 434 che coinvolge il nodo canonico `FlowOrchestrator` è `0.5`. Nel grafo unificato, l'arco 434 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 434 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_enginesmoketest` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=unificata index=435 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è la sorgente `source` della relazione `uses` verso il nodo canonico `ExtractionTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_extractiontest`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` usa `ExtractionTest`. Nel grafo unificato, il tipo dichiarato dell'arco 435 che coinvolge il nodo canonico `FlowOrchestrator` è `uses`. Nel grafo unificato, la confidenza categoriale dell'arco 435 che coinvolge il nodo canonico `FlowOrchestrator` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 435 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 435 che coinvolge il nodo canonico `FlowOrchestrator` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 435 che coinvolge il nodo canonico `FlowOrchestrator` è `0.8`. Nel grafo unificato, la provenienza `_origin` dell'arco 435 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 435 che coinvolge il nodo canonico `FlowOrchestrator` è `0.5`. Nel grafo unificato, l'arco 435 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 435 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_extractiontest` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=unificata index=436 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è la sorgente `source` della relazione `uses` verso il nodo canonico `FlowBuildTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` usa `FlowBuildTest`. Nel grafo unificato, il tipo dichiarato dell'arco 436 che coinvolge il nodo canonico `FlowOrchestrator` è `uses`. Nel grafo unificato, la confidenza categoriale dell'arco 436 che coinvolge il nodo canonico `FlowOrchestrator` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 436 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 436 che coinvolge il nodo canonico `FlowOrchestrator` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 436 che coinvolge il nodo canonico `FlowOrchestrator` è `0.8`. Nel grafo unificato, la provenienza `_origin` dell'arco 436 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 436 che coinvolge il nodo canonico `FlowOrchestrator` è `0.5`. Nel grafo unificato, l'arco 436 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 436 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=unificata index=437 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è la sorgente `source` della relazione `calls` verso il nodo canonico `.test_builds_iti_flows_with_aulss3_profile()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` chiama `.test_builds_iti_flows_with_aulss3_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 437 che coinvolge il nodo canonico `FlowOrchestrator` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 437 che coinvolge il nodo canonico `FlowOrchestrator` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 437 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 437 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 437 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 437 che coinvolge il nodo canonico `FlowOrchestrator` è `L298`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 437 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 437 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, l'arco 437 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 437 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_iti_flows_with_aulss3_profile` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=unificata index=438 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è la sorgente `source` della relazione `calls` verso il nodo canonico `.test_builds_supported_flows()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_supported_flows`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` chiama `.test_builds_supported_flows()`. Nel grafo unificato, il tipo dichiarato dell'arco 438 che coinvolge il nodo canonico `FlowOrchestrator` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 438 che coinvolge il nodo canonico `FlowOrchestrator` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 438 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 438 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 438 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 438 che coinvolge il nodo canonico `FlowOrchestrator` è `L287`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 438 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 438 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, l'arco 438 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 438 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_builds_supported_flows` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=unificata index=439 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è la sorgente `source` della relazione `calls` verso il nodo canonico `.test_iti18_and_iti43_use_expected_saml_placeholders()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` chiama `.test_iti18_and_iti43_use_expected_saml_placeholders()`. Nel grafo unificato, il tipo dichiarato dell'arco 439 che coinvolge il nodo canonico `FlowOrchestrator` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 439 che coinvolge il nodo canonico `FlowOrchestrator` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 439 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 439 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 439 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 439 che coinvolge il nodo canonico `FlowOrchestrator` è `L424`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 439 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 439 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, l'arco 439 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 439 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_iti18_and_iti43_use_expected_saml_placeholders` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=unificata index=440 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è la sorgente `source` della relazione `calls` verso il nodo canonico `.test_live_rve_1b_declares_mtls_profile()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` chiama `.test_live_rve_1b_declares_mtls_profile()`. Nel grafo unificato, il tipo dichiarato dell'arco 440 che coinvolge il nodo canonico `FlowOrchestrator` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 440 che coinvolge il nodo canonico `FlowOrchestrator` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 440 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 440 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 440 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 440 che coinvolge il nodo canonico `FlowOrchestrator` è `L449`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 440 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 440 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, l'arco 440 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 440 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_live_rve_1b_declares_mtls_profile` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=unificata index=441 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è la sorgente `source` della relazione `calls` verso il nodo canonico `.test_mock_propagates_saml_output_to_consumers()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` chiama `.test_mock_propagates_saml_output_to_consumers()`. Nel grafo unificato, il tipo dichiarato dell'arco 441 che coinvolge il nodo canonico `FlowOrchestrator` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 441 che coinvolge il nodo canonico `FlowOrchestrator` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 441 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 441 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 441 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 441 che coinvolge il nodo canonico `FlowOrchestrator` è `L358`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 441 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 441 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, l'arco 441 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 441 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_mock_propagates_saml_output_to_consumers` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=unificata index=442 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è la sorgente `source` della relazione `calls` verso il nodo canonico `.test_scrybasign_flow_has_two_steps_in_order()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` chiama `.test_scrybasign_flow_has_two_steps_in_order()`. Nel grafo unificato, il tipo dichiarato dell'arco 442 che coinvolge il nodo canonico `FlowOrchestrator` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 442 che coinvolge il nodo canonico `FlowOrchestrator` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 442 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 442 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 442 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 442 che coinvolge il nodo canonico `FlowOrchestrator` è `L348`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 442 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 442 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, l'arco 442 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 442 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_flowbuildtest_test_scrybasign_flow_has_two_steps_in_order` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=unificata index=443 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è la sorgente `source` della relazione `uses` verso il nodo canonico `MainDispatchTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_maindispatchtest`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` usa `MainDispatchTest`. Nel grafo unificato, il tipo dichiarato dell'arco 443 che coinvolge il nodo canonico `FlowOrchestrator` è `uses`. Nel grafo unificato, la confidenza categoriale dell'arco 443 che coinvolge il nodo canonico `FlowOrchestrator` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 443 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 443 che coinvolge il nodo canonico `FlowOrchestrator` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 443 che coinvolge il nodo canonico `FlowOrchestrator` è `0.8`. Nel grafo unificato, la provenienza `_origin` dell'arco 443 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 443 che coinvolge il nodo canonico `FlowOrchestrator` è `0.5`. Nel grafo unificato, l'arco 443 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 443 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_maindispatchtest` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=unificata index=444 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è la sorgente `source` della relazione `uses` verso il nodo canonico `PlaceholderTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_placeholdertest`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` usa `PlaceholderTest`. Nel grafo unificato, il tipo dichiarato dell'arco 444 che coinvolge il nodo canonico `FlowOrchestrator` è `uses`. Nel grafo unificato, la confidenza categoriale dell'arco 444 che coinvolge il nodo canonico `FlowOrchestrator` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 444 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 444 che coinvolge il nodo canonico `FlowOrchestrator` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 444 che coinvolge il nodo canonico `FlowOrchestrator` è `0.8`. Nel grafo unificato, la provenienza `_origin` dell'arco 444 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 444 che coinvolge il nodo canonico `FlowOrchestrator` è `0.5`. Nel grafo unificato, l'arco 444 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 444 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_placeholdertest` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=unificata index=445 role=outgoing -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è la sorgente `source` della relazione `uses` verso il nodo canonico `TimelineIdentityTest`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_timelineidentitytest`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` usa `TimelineIdentityTest`. Nel grafo unificato, il tipo dichiarato dell'arco 445 che coinvolge il nodo canonico `FlowOrchestrator` è `uses`. Nel grafo unificato, la confidenza categoriale dell'arco 445 che coinvolge il nodo canonico `FlowOrchestrator` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 445 che coinvolge il nodo canonico `FlowOrchestrator` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 445 che coinvolge il nodo canonico `FlowOrchestrator` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 445 che coinvolge il nodo canonico `FlowOrchestrator` è `0.8`. Nel grafo unificato, la provenienza `_origin` dell'arco 445 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 445 che coinvolge il nodo canonico `FlowOrchestrator` è `0.5`. Nel grafo unificato, l'arco 445 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 445 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_timelineidentitytest` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=unificata index=318 role=incoming -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `request_dataset_generator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator` all'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `request_dataset_generator.py` importa `FlowOrchestrator`. Nel grafo unificato, il tipo dichiarato dell'arco 318 che coinvolge il nodo canonico `FlowOrchestrator` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 318 che coinvolge il nodo canonico `FlowOrchestrator` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 318 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 318 che coinvolge il nodo canonico `FlowOrchestrator` è `request_dataset_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 318 che coinvolge il nodo canonico `FlowOrchestrator` è `L37`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 318 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 318 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 318 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, l'arco 318 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 318 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=unificata index=322 role=incoming -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_workload()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::request_dataset_generator_build_workload` all'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `build_workload()` chiama `FlowOrchestrator`. Nel grafo unificato, il tipo dichiarato dell'arco 322 che coinvolge il nodo canonico `FlowOrchestrator` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 322 che coinvolge il nodo canonico `FlowOrchestrator` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 322 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 322 che coinvolge il nodo canonico `FlowOrchestrator` è `request_dataset_generator.py`. Nel grafo unificato, la posizione sorgente dell'arco 322 che coinvolge il nodo canonico `FlowOrchestrator` è `L153`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 322 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 322 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 322 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, l'arco 322 registra come `source` l'identificatore `Request-Dataset-Generator::request_dataset_generator_build_workload` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 322 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=unificata index=337 role=incoming -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `__init__.py [rve_transactions_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_init` all'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `__init__.py [rve_transactions_init]` importa `FlowOrchestrator`. Nel grafo unificato, il tipo dichiarato dell'arco 337 che coinvolge il nodo canonico `FlowOrchestrator` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 337 che coinvolge il nodo canonico `FlowOrchestrator` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 337 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 337 che coinvolge il nodo canonico `FlowOrchestrator` è `rve_transactions/__init__.py`. Nel grafo unificato, la posizione sorgente dell'arco 337 che coinvolge il nodo canonico `FlowOrchestrator` è `L9`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 337 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 337 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 337 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, l'arco 337 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_init` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 337 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.
<!-- GRAPH_EDGE graph=unificata index=405 role=incoming -->
Nel grafo unificato, il nodo canonico `FlowOrchestrator` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `flow_orchestrator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator` all'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `flow_orchestrator.py` contiene `FlowOrchestrator`. Nel grafo unificato, il tipo dichiarato dell'arco 405 che coinvolge il nodo canonico `FlowOrchestrator` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 405 che coinvolge il nodo canonico `FlowOrchestrator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 405 che coinvolge il nodo canonico `FlowOrchestrator` è `rve_transactions/flow_orchestrator.py`. Nel grafo unificato, la posizione sorgente dell'arco 405 che coinvolge il nodo canonico `FlowOrchestrator` è `L126`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 405 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 405 che coinvolge il nodo canonico `FlowOrchestrator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 405 che coinvolge il nodo canonico `FlowOrchestrator` è `1.0`. Nel grafo unificato, l'arco 405 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`. Nel grafo unificato, l'arco 405 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `FlowOrchestrator`.

### MainDispatchTest

<!-- GRAPH_NODE graph=codice index=319 id=tests_test_core_maindispatchtest -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `MainDispatchTest` è `MainDispatchTest`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `MainDispatchTest` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `MainDispatchTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `MainDispatchTest` è `L852`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `MainDispatchTest` vale `ast`. Nel grafo del codice RDG, il nodo canonico `MainDispatchTest` possiede l'identificatore esatto `tests_test_core_maindispatchtest`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `MainDispatchTest` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `MainDispatchTest` è `maindispatchtest`.
<!-- GRAPH_EDGE graph=codice index=443 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `MainDispatchTest` è la sorgente `source` della relazione `uses` verso il nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_maindispatchtest` all'ID `rve_transactions_flow_orchestrator_floworchestrator`. La relazione tecnica esplicita è la seguente: `MainDispatchTest` usa `FlowOrchestrator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 443 che coinvolge il nodo canonico `MainDispatchTest` è `uses`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 443 che coinvolge il nodo canonico `MainDispatchTest` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 443 che coinvolge il nodo canonico `MainDispatchTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 443 che coinvolge il nodo canonico `MainDispatchTest` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 443 che coinvolge il nodo canonico `MainDispatchTest` è `0.8`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 443 che coinvolge il nodo canonico `MainDispatchTest` è `ast`. Nel grafo del codice RDG, l'arco 443 registra come `source` l'identificatore `tests_test_core_maindispatchtest` per la relazione che coinvolge il nodo canonico `MainDispatchTest`. Nel grafo del codice RDG, l'arco 443 registra come `target` l'identificatore `rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `MainDispatchTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 443 che coinvolge il nodo canonico `MainDispatchTest` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=641 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `MainDispatchTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_dispatch_includes_liv3_commands()`; l'orientamento registrato nel JSON va dall'ID `tests_test_core_maindispatchtest` all'ID `tests_test_core_maindispatchtest_test_dispatch_includes_liv3_commands`. La relazione tecnica esplicita è la seguente: `MainDispatchTest` registra come proprio metodo `.test_dispatch_includes_liv3_commands()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 641 che coinvolge il nodo canonico `MainDispatchTest` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 641 che coinvolge il nodo canonico `MainDispatchTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 641 che coinvolge il nodo canonico `MainDispatchTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 641 che coinvolge il nodo canonico `MainDispatchTest` è `L853`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 641 che coinvolge il nodo canonico `MainDispatchTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 641 che coinvolge il nodo canonico `MainDispatchTest` è `ast`. Nel grafo del codice RDG, l'arco 641 registra come `source` l'identificatore `tests_test_core_maindispatchtest` per la relazione che coinvolge il nodo canonico `MainDispatchTest`. Nel grafo del codice RDG, l'arco 641 registra come `target` l'identificatore `tests_test_core_maindispatchtest_test_dispatch_includes_liv3_commands` per la relazione che coinvolge il nodo canonico `MainDispatchTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 641 che coinvolge il nodo canonico `MainDispatchTest` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=582 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `MainDispatchTest` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `tests_test_core` all'ID `tests_test_core_maindispatchtest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `MainDispatchTest`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 582 che coinvolge il nodo canonico `MainDispatchTest` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 582 che coinvolge il nodo canonico `MainDispatchTest` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 582 che coinvolge il nodo canonico `MainDispatchTest` è `tests/test_core.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 582 che coinvolge il nodo canonico `MainDispatchTest` è `L852`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 582 che coinvolge il nodo canonico `MainDispatchTest` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 582 che coinvolge il nodo canonico `MainDispatchTest` è `ast`. Nel grafo del codice RDG, l'arco 582 registra come `source` l'identificatore `tests_test_core` per la relazione che coinvolge il nodo canonico `MainDispatchTest`. Nel grafo del codice RDG, l'arco 582 registra come `target` l'identificatore `tests_test_core_maindispatchtest` per la relazione che coinvolge il nodo canonico `MainDispatchTest`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 582 che coinvolge il nodo canonico `MainDispatchTest` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=319 id=Request-Dataset-Generator::tests_test_core_maindispatchtest -->
Nel grafo unificato, l'etichetta originale del nodo canonico `MainDispatchTest` è `MainDispatchTest`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `MainDispatchTest` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `MainDispatchTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `MainDispatchTest` è `L852`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `MainDispatchTest` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `MainDispatchTest` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `MainDispatchTest` è `maindispatchtest`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `MainDispatchTest` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `MainDispatchTest` è `tests_test_core_maindispatchtest`. Nel grafo unificato, il nodo canonico `MainDispatchTest` possiede l'identificatore esatto `Request-Dataset-Generator::tests_test_core_maindispatchtest`.
<!-- GRAPH_EDGE graph=unificata index=641 role=outgoing -->
Nel grafo unificato, il nodo canonico `MainDispatchTest` è la sorgente `source` della relazione `method` verso il nodo canonico `.test_dispatch_includes_liv3_commands()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core_maindispatchtest` all'ID `Request-Dataset-Generator::tests_test_core_maindispatchtest_test_dispatch_includes_liv3_commands`. La relazione tecnica esplicita è la seguente: `MainDispatchTest` registra come proprio metodo `.test_dispatch_includes_liv3_commands()`. Nel grafo unificato, il tipo dichiarato dell'arco 641 che coinvolge il nodo canonico `MainDispatchTest` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 641 che coinvolge il nodo canonico `MainDispatchTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 641 che coinvolge il nodo canonico `MainDispatchTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 641 che coinvolge il nodo canonico `MainDispatchTest` è `L853`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 641 che coinvolge il nodo canonico `MainDispatchTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 641 che coinvolge il nodo canonico `MainDispatchTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 641 che coinvolge il nodo canonico `MainDispatchTest` è `1.0`. Nel grafo unificato, l'arco 641 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core_maindispatchtest` per la relazione che coinvolge il nodo canonico `MainDispatchTest`. Nel grafo unificato, l'arco 641 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_maindispatchtest_test_dispatch_includes_liv3_commands` per la relazione che coinvolge il nodo canonico `MainDispatchTest`.
<!-- GRAPH_EDGE graph=unificata index=443 role=incoming -->
Nel grafo unificato, il nodo canonico `MainDispatchTest` è il destinatario `target` della relazione `uses` proveniente dal nodo canonico `FlowOrchestrator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` all'ID `Request-Dataset-Generator::tests_test_core_maindispatchtest`. La relazione tecnica esplicita è la seguente: `FlowOrchestrator` usa `MainDispatchTest`. Nel grafo unificato, il tipo dichiarato dell'arco 443 che coinvolge il nodo canonico `MainDispatchTest` è `uses`. Nel grafo unificato, la confidenza categoriale dell'arco 443 che coinvolge il nodo canonico `MainDispatchTest` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 443 che coinvolge il nodo canonico `MainDispatchTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 443 che coinvolge il nodo canonico `MainDispatchTest` è `L14`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 443 che coinvolge il nodo canonico `MainDispatchTest` è `0.8`. Nel grafo unificato, la provenienza `_origin` dell'arco 443 che coinvolge il nodo canonico `MainDispatchTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 443 che coinvolge il nodo canonico `MainDispatchTest` è `0.5`. Nel grafo unificato, l'arco 443 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator_floworchestrator` per la relazione che coinvolge il nodo canonico `MainDispatchTest`. Nel grafo unificato, l'arco 443 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_maindispatchtest` per la relazione che coinvolge il nodo canonico `MainDispatchTest`.
<!-- GRAPH_EDGE graph=unificata index=582 role=incoming -->
Nel grafo unificato, il nodo canonico `MainDispatchTest` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `test_core.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::tests_test_core` all'ID `Request-Dataset-Generator::tests_test_core_maindispatchtest`. La relazione tecnica esplicita è la seguente: `test_core.py` contiene `MainDispatchTest`. Nel grafo unificato, il tipo dichiarato dell'arco 582 che coinvolge il nodo canonico `MainDispatchTest` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 582 che coinvolge il nodo canonico `MainDispatchTest` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 582 che coinvolge il nodo canonico `MainDispatchTest` è `tests/test_core.py`. Nel grafo unificato, la posizione sorgente dell'arco 582 che coinvolge il nodo canonico `MainDispatchTest` è `L852`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 582 che coinvolge il nodo canonico `MainDispatchTest` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 582 che coinvolge il nodo canonico `MainDispatchTest` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 582 che coinvolge il nodo canonico `MainDispatchTest` è `1.0`. Nel grafo unificato, l'arco 582 registra come `source` l'identificatore `Request-Dataset-Generator::tests_test_core` per la relazione che coinvolge il nodo canonico `MainDispatchTest`. Nel grafo unificato, l'arco 582 registra come `target` l'identificatore `Request-Dataset-Generator::tests_test_core_maindispatchtest` per la relazione che coinvolge il nodo canonico `MainDispatchTest`.

### rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l

<!-- GRAPH_NODE graph=codice index=160 id=rve_transactions_init_rationale_1 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `rve_transactions/__init__.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` vale `ast`. Nel grafo del codice RDG, il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` possiede l'identificatore esatto `rve_transactions_init_rationale_1`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` alla comunità numerica `10`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `rve_transactions — moduli di composizione request rve (regione veneto)  espone l`.
<!-- GRAPH_EDGE graph=codice index=338 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `__init__.py [rve_transactions_init]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_init_rationale_1` all'ID `rve_transactions_init`. La relazione tecnica esplicita è la seguente: `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` costituisce una motivazione per `__init__.py [rve_transactions_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 338 che coinvolge il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 338 che coinvolge il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 338 che coinvolge il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `rve_transactions/__init__.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 338 che coinvolge il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 338 che coinvolge il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 338 che coinvolge il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `ast`. Nel grafo del codice RDG, l'arco 338 registra come `source` l'identificatore `rve_transactions_init_rationale_1` per la relazione che coinvolge il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l`. Nel grafo del codice RDG, l'arco 338 registra come `target` l'identificatore `rve_transactions_init` per la relazione che coinvolge il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 338 che coinvolge il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=160 id=Request-Dataset-Generator::rve_transactions_init_rationale_1 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `rve_transactions/__init__.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` alla comunità numerica `10`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `rve_transactions — moduli di composizione request rve (regione veneto)  espone l`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `rve_transactions_init_rationale_1`. Nel grafo unificato, il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_init_rationale_1`.
Nel grafo unificato, il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=338 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `__init__.py [rve_transactions_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_init` all'ID `Request-Dataset-Generator::rve_transactions_init_rationale_1`. La relazione tecnica esplicita è la seguente: `__init__.py [rve_transactions_init]` costituisce una motivazione per `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l`. Nel grafo unificato, il tipo dichiarato dell'arco 338 che coinvolge il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 338 che coinvolge il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 338 che coinvolge il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `rve_transactions/__init__.py`. Nel grafo unificato, la posizione sorgente dell'arco 338 che coinvolge il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 338 che coinvolge il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 338 che coinvolge il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 338 che coinvolge il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l` è `1.0`. Nel grafo unificato, l'arco 338 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_init` per la relazione che coinvolge il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l`. Nel grafo unificato, l'arco 338 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_init_rationale_1` per la relazione che coinvolge il nodo canonico `rve_transactions — Moduli di composizione request RVE (Regione Veneto)  Espone l`.

## Cluster Graphify 11 — RVE Gateway Mapping

Il cluster editoriale Graphify 11 del dominio Architettura e codice del framework RDG contiene 25 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### _address_locality()

<!-- GRAPH_NODE graph=codice index=253 id=rve_transactions_rve_gateway_address_locality -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_address_locality()` è `_address_locality()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_address_locality()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_address_locality()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_address_locality()` è `L108`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_address_locality()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_address_locality()` possiede l'identificatore esatto `rve_transactions_rve_gateway_address_locality`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_address_locality()` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_address_locality()` è `_address_locality()`.
<!-- GRAPH_EDGE graph=codice index=534 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_address_locality()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_first_address()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_address_locality` all'ID `rve_transactions_rve_gateway_first_address`. La relazione tecnica esplicita è la seguente: `_address_locality()` chiama `_first_address()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 534 che coinvolge il nodo canonico `_address_locality()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 534 che coinvolge il nodo canonico `_address_locality()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 534 che coinvolge il nodo canonico `_address_locality()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 534 che coinvolge il nodo canonico `_address_locality()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 534 che coinvolge il nodo canonico `_address_locality()` è `L109`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 534 che coinvolge il nodo canonico `_address_locality()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 534 che coinvolge il nodo canonico `_address_locality()` è `ast`. Nel grafo del codice RDG, l'arco 534 registra come `source` l'identificatore `rve_transactions_rve_gateway_address_locality` per la relazione che coinvolge il nodo canonico `_address_locality()`. Nel grafo del codice RDG, l'arco 534 registra come `target` l'identificatore `rve_transactions_rve_gateway_first_address` per la relazione che coinvolge il nodo canonico `_address_locality()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 534 che coinvolge il nodo canonico `_address_locality()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=494 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_address_locality()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_address_locality`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_address_locality()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 494 che coinvolge il nodo canonico `_address_locality()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 494 che coinvolge il nodo canonico `_address_locality()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 494 che coinvolge il nodo canonico `_address_locality()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 494 che coinvolge il nodo canonico `_address_locality()` è `L108`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 494 che coinvolge il nodo canonico `_address_locality()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 494 che coinvolge il nodo canonico `_address_locality()` è `ast`. Nel grafo del codice RDG, l'arco 494 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_address_locality()`. Nel grafo del codice RDG, l'arco 494 registra come `target` l'identificatore `rve_transactions_rve_gateway_address_locality` per la relazione che coinvolge il nodo canonico `_address_locality()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 494 che coinvolge il nodo canonico `_address_locality()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=539 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_address_locality()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_address_locality`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_address_locality()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 539 che coinvolge il nodo canonico `_address_locality()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 539 che coinvolge il nodo canonico `_address_locality()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 539 che coinvolge il nodo canonico `_address_locality()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 539 che coinvolge il nodo canonico `_address_locality()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 539 che coinvolge il nodo canonico `_address_locality()` è `L199`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 539 che coinvolge il nodo canonico `_address_locality()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 539 che coinvolge il nodo canonico `_address_locality()` è `ast`. Nel grafo del codice RDG, l'arco 539 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_address_locality()`. Nel grafo del codice RDG, l'arco 539 registra come `target` l'identificatore `rve_transactions_rve_gateway_address_locality` per la relazione che coinvolge il nodo canonico `_address_locality()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 539 che coinvolge il nodo canonico `_address_locality()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=540 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_address_locality()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_address_locality`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_address_locality()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 540 che coinvolge il nodo canonico `_address_locality()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 540 che coinvolge il nodo canonico `_address_locality()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 540 che coinvolge il nodo canonico `_address_locality()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 540 che coinvolge il nodo canonico `_address_locality()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 540 che coinvolge il nodo canonico `_address_locality()` è `L253`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 540 che coinvolge il nodo canonico `_address_locality()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 540 che coinvolge il nodo canonico `_address_locality()` è `ast`. Nel grafo del codice RDG, l'arco 540 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_address_locality()`. Nel grafo del codice RDG, l'arco 540 registra come `target` l'identificatore `rve_transactions_rve_gateway_address_locality` per la relazione che coinvolge il nodo canonico `_address_locality()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 540 che coinvolge il nodo canonico `_address_locality()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=253 id=Request-Dataset-Generator::rve_transactions_rve_gateway_address_locality -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_address_locality()` è `_address_locality()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_address_locality()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_address_locality()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_address_locality()` è `L108`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_address_locality()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_address_locality()` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_address_locality()` è `_address_locality()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_address_locality()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_address_locality()` è `rve_transactions_rve_gateway_address_locality`. Nel grafo unificato, il nodo canonico `_address_locality()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_address_locality`.
<!-- GRAPH_EDGE graph=unificata index=539 role=outgoing -->
Nel grafo unificato, il nodo canonico `_address_locality()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_locality` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_address_locality()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 539 che coinvolge il nodo canonico `_address_locality()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 539 che coinvolge il nodo canonico `_address_locality()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 539 che coinvolge il nodo canonico `_address_locality()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 539 che coinvolge il nodo canonico `_address_locality()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 539 che coinvolge il nodo canonico `_address_locality()` è `L199`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 539 che coinvolge il nodo canonico `_address_locality()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 539 che coinvolge il nodo canonico `_address_locality()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 539 che coinvolge il nodo canonico `_address_locality()` è `1.0`. Nel grafo unificato, l'arco 539 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_locality` per la relazione che coinvolge il nodo canonico `_address_locality()`. Nel grafo unificato, l'arco 539 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_address_locality()`.
<!-- GRAPH_EDGE graph=unificata index=540 role=outgoing -->
Nel grafo unificato, il nodo canonico `_address_locality()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_locality` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_address_locality()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 540 che coinvolge il nodo canonico `_address_locality()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 540 che coinvolge il nodo canonico `_address_locality()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 540 che coinvolge il nodo canonico `_address_locality()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 540 che coinvolge il nodo canonico `_address_locality()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 540 che coinvolge il nodo canonico `_address_locality()` è `L253`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 540 che coinvolge il nodo canonico `_address_locality()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 540 che coinvolge il nodo canonico `_address_locality()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 540 che coinvolge il nodo canonico `_address_locality()` è `1.0`. Nel grafo unificato, l'arco 540 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_locality` per la relazione che coinvolge il nodo canonico `_address_locality()`. Nel grafo unificato, l'arco 540 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_address_locality()`.
<!-- GRAPH_EDGE graph=unificata index=494 role=incoming -->
Nel grafo unificato, il nodo canonico `_address_locality()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_locality`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_address_locality()`. Nel grafo unificato, il tipo dichiarato dell'arco 494 che coinvolge il nodo canonico `_address_locality()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 494 che coinvolge il nodo canonico `_address_locality()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 494 che coinvolge il nodo canonico `_address_locality()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 494 che coinvolge il nodo canonico `_address_locality()` è `L108`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 494 che coinvolge il nodo canonico `_address_locality()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 494 che coinvolge il nodo canonico `_address_locality()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 494 che coinvolge il nodo canonico `_address_locality()` è `1.0`. Nel grafo unificato, l'arco 494 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_address_locality()`. Nel grafo unificato, l'arco 494 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_locality` per la relazione che coinvolge il nodo canonico `_address_locality()`.
<!-- GRAPH_EDGE graph=unificata index=534 role=incoming -->
Nel grafo unificato, il nodo canonico `_address_locality()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_first_address()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_first_address` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_locality`. La relazione tecnica esplicita è la seguente: `_first_address()` chiama `_address_locality()`. Nel grafo unificato, il tipo dichiarato dell'arco 534 che coinvolge il nodo canonico `_address_locality()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 534 che coinvolge il nodo canonico `_address_locality()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 534 che coinvolge il nodo canonico `_address_locality()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 534 che coinvolge il nodo canonico `_address_locality()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 534 che coinvolge il nodo canonico `_address_locality()` è `L109`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 534 che coinvolge il nodo canonico `_address_locality()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 534 che coinvolge il nodo canonico `_address_locality()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 534 che coinvolge il nodo canonico `_address_locality()` è `1.0`. Nel grafo unificato, l'arco 534 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_first_address` per la relazione che coinvolge il nodo canonico `_address_locality()`. Nel grafo unificato, l'arco 534 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_locality` per la relazione che coinvolge il nodo canonico `_address_locality()`.

### _address_postal_code()

<!-- GRAPH_NODE graph=codice index=254 id=rve_transactions_rve_gateway_address_postal_code -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_address_postal_code()` è `_address_postal_code()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_address_postal_code()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_address_postal_code()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_address_postal_code()` è `L115`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_address_postal_code()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_address_postal_code()` possiede l'identificatore esatto `rve_transactions_rve_gateway_address_postal_code`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_address_postal_code()` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_address_postal_code()` è `_address_postal_code()`.
<!-- GRAPH_EDGE graph=codice index=535 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_address_postal_code()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_first_address()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_address_postal_code` all'ID `rve_transactions_rve_gateway_first_address`. La relazione tecnica esplicita è la seguente: `_address_postal_code()` chiama `_first_address()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 535 che coinvolge il nodo canonico `_address_postal_code()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 535 che coinvolge il nodo canonico `_address_postal_code()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 535 che coinvolge il nodo canonico `_address_postal_code()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 535 che coinvolge il nodo canonico `_address_postal_code()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 535 che coinvolge il nodo canonico `_address_postal_code()` è `L116`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 535 che coinvolge il nodo canonico `_address_postal_code()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 535 che coinvolge il nodo canonico `_address_postal_code()` è `ast`. Nel grafo del codice RDG, l'arco 535 registra come `source` l'identificatore `rve_transactions_rve_gateway_address_postal_code` per la relazione che coinvolge il nodo canonico `_address_postal_code()`. Nel grafo del codice RDG, l'arco 535 registra come `target` l'identificatore `rve_transactions_rve_gateway_first_address` per la relazione che coinvolge il nodo canonico `_address_postal_code()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 535 che coinvolge il nodo canonico `_address_postal_code()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=495 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_address_postal_code()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_address_postal_code`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_address_postal_code()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 495 che coinvolge il nodo canonico `_address_postal_code()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 495 che coinvolge il nodo canonico `_address_postal_code()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 495 che coinvolge il nodo canonico `_address_postal_code()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 495 che coinvolge il nodo canonico `_address_postal_code()` è `L115`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 495 che coinvolge il nodo canonico `_address_postal_code()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 495 che coinvolge il nodo canonico `_address_postal_code()` è `ast`. Nel grafo del codice RDG, l'arco 495 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_address_postal_code()`. Nel grafo del codice RDG, l'arco 495 registra come `target` l'identificatore `rve_transactions_rve_gateway_address_postal_code` per la relazione che coinvolge il nodo canonico `_address_postal_code()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 495 che coinvolge il nodo canonico `_address_postal_code()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=541 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_address_postal_code()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_address_postal_code`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_address_postal_code()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 541 che coinvolge il nodo canonico `_address_postal_code()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 541 che coinvolge il nodo canonico `_address_postal_code()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 541 che coinvolge il nodo canonico `_address_postal_code()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 541 che coinvolge il nodo canonico `_address_postal_code()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 541 che coinvolge il nodo canonico `_address_postal_code()` è `L206`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 541 che coinvolge il nodo canonico `_address_postal_code()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 541 che coinvolge il nodo canonico `_address_postal_code()` è `ast`. Nel grafo del codice RDG, l'arco 541 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_address_postal_code()`. Nel grafo del codice RDG, l'arco 541 registra come `target` l'identificatore `rve_transactions_rve_gateway_address_postal_code` per la relazione che coinvolge il nodo canonico `_address_postal_code()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 541 che coinvolge il nodo canonico `_address_postal_code()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=542 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_address_postal_code()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_address_postal_code`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_address_postal_code()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 542 che coinvolge il nodo canonico `_address_postal_code()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 542 che coinvolge il nodo canonico `_address_postal_code()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 542 che coinvolge il nodo canonico `_address_postal_code()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 542 che coinvolge il nodo canonico `_address_postal_code()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 542 che coinvolge il nodo canonico `_address_postal_code()` è `L260`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 542 che coinvolge il nodo canonico `_address_postal_code()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 542 che coinvolge il nodo canonico `_address_postal_code()` è `ast`. Nel grafo del codice RDG, l'arco 542 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_address_postal_code()`. Nel grafo del codice RDG, l'arco 542 registra come `target` l'identificatore `rve_transactions_rve_gateway_address_postal_code` per la relazione che coinvolge il nodo canonico `_address_postal_code()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 542 che coinvolge il nodo canonico `_address_postal_code()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=254 id=Request-Dataset-Generator::rve_transactions_rve_gateway_address_postal_code -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_address_postal_code()` è `_address_postal_code()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_address_postal_code()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_address_postal_code()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_address_postal_code()` è `L115`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_address_postal_code()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_address_postal_code()` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_address_postal_code()` è `_address_postal_code()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_address_postal_code()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_address_postal_code()` è `rve_transactions_rve_gateway_address_postal_code`. Nel grafo unificato, il nodo canonico `_address_postal_code()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_address_postal_code`.
<!-- GRAPH_EDGE graph=unificata index=541 role=outgoing -->
Nel grafo unificato, il nodo canonico `_address_postal_code()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_postal_code` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_address_postal_code()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 541 che coinvolge il nodo canonico `_address_postal_code()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 541 che coinvolge il nodo canonico `_address_postal_code()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 541 che coinvolge il nodo canonico `_address_postal_code()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 541 che coinvolge il nodo canonico `_address_postal_code()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 541 che coinvolge il nodo canonico `_address_postal_code()` è `L206`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 541 che coinvolge il nodo canonico `_address_postal_code()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 541 che coinvolge il nodo canonico `_address_postal_code()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 541 che coinvolge il nodo canonico `_address_postal_code()` è `1.0`. Nel grafo unificato, l'arco 541 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_postal_code` per la relazione che coinvolge il nodo canonico `_address_postal_code()`. Nel grafo unificato, l'arco 541 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_address_postal_code()`.
<!-- GRAPH_EDGE graph=unificata index=542 role=outgoing -->
Nel grafo unificato, il nodo canonico `_address_postal_code()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_postal_code` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_address_postal_code()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 542 che coinvolge il nodo canonico `_address_postal_code()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 542 che coinvolge il nodo canonico `_address_postal_code()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 542 che coinvolge il nodo canonico `_address_postal_code()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 542 che coinvolge il nodo canonico `_address_postal_code()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 542 che coinvolge il nodo canonico `_address_postal_code()` è `L260`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 542 che coinvolge il nodo canonico `_address_postal_code()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 542 che coinvolge il nodo canonico `_address_postal_code()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 542 che coinvolge il nodo canonico `_address_postal_code()` è `1.0`. Nel grafo unificato, l'arco 542 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_postal_code` per la relazione che coinvolge il nodo canonico `_address_postal_code()`. Nel grafo unificato, l'arco 542 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_address_postal_code()`.
<!-- GRAPH_EDGE graph=unificata index=495 role=incoming -->
Nel grafo unificato, il nodo canonico `_address_postal_code()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_postal_code`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_address_postal_code()`. Nel grafo unificato, il tipo dichiarato dell'arco 495 che coinvolge il nodo canonico `_address_postal_code()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 495 che coinvolge il nodo canonico `_address_postal_code()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 495 che coinvolge il nodo canonico `_address_postal_code()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 495 che coinvolge il nodo canonico `_address_postal_code()` è `L115`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 495 che coinvolge il nodo canonico `_address_postal_code()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 495 che coinvolge il nodo canonico `_address_postal_code()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 495 che coinvolge il nodo canonico `_address_postal_code()` è `1.0`. Nel grafo unificato, l'arco 495 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_address_postal_code()`. Nel grafo unificato, l'arco 495 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_postal_code` per la relazione che coinvolge il nodo canonico `_address_postal_code()`.
<!-- GRAPH_EDGE graph=unificata index=535 role=incoming -->
Nel grafo unificato, il nodo canonico `_address_postal_code()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_first_address()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_first_address` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_postal_code`. La relazione tecnica esplicita è la seguente: `_first_address()` chiama `_address_postal_code()`. Nel grafo unificato, il tipo dichiarato dell'arco 535 che coinvolge il nodo canonico `_address_postal_code()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 535 che coinvolge il nodo canonico `_address_postal_code()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 535 che coinvolge il nodo canonico `_address_postal_code()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 535 che coinvolge il nodo canonico `_address_postal_code()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 535 che coinvolge il nodo canonico `_address_postal_code()` è `L116`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 535 che coinvolge il nodo canonico `_address_postal_code()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 535 che coinvolge il nodo canonico `_address_postal_code()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 535 che coinvolge il nodo canonico `_address_postal_code()` è `1.0`. Nel grafo unificato, l'arco 535 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_first_address` per la relazione che coinvolge il nodo canonico `_address_postal_code()`. Nel grafo unificato, l'arco 535 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_postal_code` per la relazione che coinvolge il nodo canonico `_address_postal_code()`.

### _address_text()

<!-- GRAPH_NODE graph=codice index=252 id=rve_transactions_rve_gateway_address_text -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_address_text()` è `_address_text()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_address_text()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_address_text()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_address_text()` è `L104`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_address_text()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_address_text()` possiede l'identificatore esatto `rve_transactions_rve_gateway_address_text`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_address_text()` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_address_text()` è `_address_text()`.
<!-- GRAPH_EDGE graph=codice index=536 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_address_text()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_first_address()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_address_text` all'ID `rve_transactions_rve_gateway_first_address`. La relazione tecnica esplicita è la seguente: `_address_text()` chiama `_first_address()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 536 che coinvolge il nodo canonico `_address_text()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 536 che coinvolge il nodo canonico `_address_text()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 536 che coinvolge il nodo canonico `_address_text()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 536 che coinvolge il nodo canonico `_address_text()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 536 che coinvolge il nodo canonico `_address_text()` è `L105`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 536 che coinvolge il nodo canonico `_address_text()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 536 che coinvolge il nodo canonico `_address_text()` è `ast`. Nel grafo del codice RDG, l'arco 536 registra come `source` l'identificatore `rve_transactions_rve_gateway_address_text` per la relazione che coinvolge il nodo canonico `_address_text()`. Nel grafo del codice RDG, l'arco 536 registra come `target` l'identificatore `rve_transactions_rve_gateway_first_address` per la relazione che coinvolge il nodo canonico `_address_text()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 536 che coinvolge il nodo canonico `_address_text()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=496 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_address_text()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_address_text`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_address_text()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 496 che coinvolge il nodo canonico `_address_text()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 496 che coinvolge il nodo canonico `_address_text()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 496 che coinvolge il nodo canonico `_address_text()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 496 che coinvolge il nodo canonico `_address_text()` è `L104`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 496 che coinvolge il nodo canonico `_address_text()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 496 che coinvolge il nodo canonico `_address_text()` è `ast`. Nel grafo del codice RDG, l'arco 496 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_address_text()`. Nel grafo del codice RDG, l'arco 496 registra come `target` l'identificatore `rve_transactions_rve_gateway_address_text` per la relazione che coinvolge il nodo canonico `_address_text()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 496 che coinvolge il nodo canonico `_address_text()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=537 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_address_text()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_address_text`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_address_text()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 537 che coinvolge il nodo canonico `_address_text()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 537 che coinvolge il nodo canonico `_address_text()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 537 che coinvolge il nodo canonico `_address_text()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 537 che coinvolge il nodo canonico `_address_text()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 537 che coinvolge il nodo canonico `_address_text()` è `L198`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 537 che coinvolge il nodo canonico `_address_text()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 537 che coinvolge il nodo canonico `_address_text()` è `ast`. Nel grafo del codice RDG, l'arco 537 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_address_text()`. Nel grafo del codice RDG, l'arco 537 registra come `target` l'identificatore `rve_transactions_rve_gateway_address_text` per la relazione che coinvolge il nodo canonico `_address_text()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 537 che coinvolge il nodo canonico `_address_text()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=538 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_address_text()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_address_text`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_address_text()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 538 che coinvolge il nodo canonico `_address_text()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 538 che coinvolge il nodo canonico `_address_text()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 538 che coinvolge il nodo canonico `_address_text()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 538 che coinvolge il nodo canonico `_address_text()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 538 che coinvolge il nodo canonico `_address_text()` è `L252`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 538 che coinvolge il nodo canonico `_address_text()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 538 che coinvolge il nodo canonico `_address_text()` è `ast`. Nel grafo del codice RDG, l'arco 538 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_address_text()`. Nel grafo del codice RDG, l'arco 538 registra come `target` l'identificatore `rve_transactions_rve_gateway_address_text` per la relazione che coinvolge il nodo canonico `_address_text()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 538 che coinvolge il nodo canonico `_address_text()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=252 id=Request-Dataset-Generator::rve_transactions_rve_gateway_address_text -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_address_text()` è `_address_text()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_address_text()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_address_text()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_address_text()` è `L104`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_address_text()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_address_text()` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_address_text()` è `_address_text()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_address_text()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_address_text()` è `rve_transactions_rve_gateway_address_text`. Nel grafo unificato, il nodo canonico `_address_text()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_address_text`.
<!-- GRAPH_EDGE graph=unificata index=537 role=outgoing -->
Nel grafo unificato, il nodo canonico `_address_text()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_text` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_address_text()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 537 che coinvolge il nodo canonico `_address_text()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 537 che coinvolge il nodo canonico `_address_text()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 537 che coinvolge il nodo canonico `_address_text()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 537 che coinvolge il nodo canonico `_address_text()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 537 che coinvolge il nodo canonico `_address_text()` è `L198`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 537 che coinvolge il nodo canonico `_address_text()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 537 che coinvolge il nodo canonico `_address_text()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 537 che coinvolge il nodo canonico `_address_text()` è `1.0`. Nel grafo unificato, l'arco 537 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_text` per la relazione che coinvolge il nodo canonico `_address_text()`. Nel grafo unificato, l'arco 537 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_address_text()`.
<!-- GRAPH_EDGE graph=unificata index=538 role=outgoing -->
Nel grafo unificato, il nodo canonico `_address_text()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_text` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_address_text()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 538 che coinvolge il nodo canonico `_address_text()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 538 che coinvolge il nodo canonico `_address_text()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 538 che coinvolge il nodo canonico `_address_text()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 538 che coinvolge il nodo canonico `_address_text()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 538 che coinvolge il nodo canonico `_address_text()` è `L252`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 538 che coinvolge il nodo canonico `_address_text()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 538 che coinvolge il nodo canonico `_address_text()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 538 che coinvolge il nodo canonico `_address_text()` è `1.0`. Nel grafo unificato, l'arco 538 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_text` per la relazione che coinvolge il nodo canonico `_address_text()`. Nel grafo unificato, l'arco 538 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_address_text()`.
<!-- GRAPH_EDGE graph=unificata index=496 role=incoming -->
Nel grafo unificato, il nodo canonico `_address_text()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_text`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_address_text()`. Nel grafo unificato, il tipo dichiarato dell'arco 496 che coinvolge il nodo canonico `_address_text()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 496 che coinvolge il nodo canonico `_address_text()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 496 che coinvolge il nodo canonico `_address_text()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 496 che coinvolge il nodo canonico `_address_text()` è `L104`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 496 che coinvolge il nodo canonico `_address_text()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 496 che coinvolge il nodo canonico `_address_text()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 496 che coinvolge il nodo canonico `_address_text()` è `1.0`. Nel grafo unificato, l'arco 496 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_address_text()`. Nel grafo unificato, l'arco 496 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_text` per la relazione che coinvolge il nodo canonico `_address_text()`.
<!-- GRAPH_EDGE graph=unificata index=536 role=incoming -->
Nel grafo unificato, il nodo canonico `_address_text()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_first_address()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_first_address` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_text`. La relazione tecnica esplicita è la seguente: `_first_address()` chiama `_address_text()`. Nel grafo unificato, il tipo dichiarato dell'arco 536 che coinvolge il nodo canonico `_address_text()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 536 che coinvolge il nodo canonico `_address_text()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 536 che coinvolge il nodo canonico `_address_text()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 536 che coinvolge il nodo canonico `_address_text()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 536 che coinvolge il nodo canonico `_address_text()` è `L105`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 536 che coinvolge il nodo canonico `_address_text()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 536 che coinvolge il nodo canonico `_address_text()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 536 che coinvolge il nodo canonico `_address_text()` è `1.0`. Nel grafo unificato, l'arco 536 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_first_address` per la relazione che coinvolge il nodo canonico `_address_text()`. Nel grafo unificato, l'arco 536 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_text` per la relazione che coinvolge il nodo canonico `_address_text()`.

### _birth_date_millis()

<!-- GRAPH_NODE graph=codice index=247 id=rve_transactions_rve_gateway_birth_date_millis -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_birth_date_millis()` è `_birth_date_millis()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_birth_date_millis()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_birth_date_millis()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_birth_date_millis()` è `L62`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_birth_date_millis()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_birth_date_millis()` possiede l'identificatore esatto `rve_transactions_rve_gateway_birth_date_millis`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_birth_date_millis()` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_birth_date_millis()` è `_birth_date_millis()`.
Nel grafo del codice RDG, il nodo canonico `_birth_date_millis()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=497 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_birth_date_millis()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_birth_date_millis`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_birth_date_millis()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 497 che coinvolge il nodo canonico `_birth_date_millis()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 497 che coinvolge il nodo canonico `_birth_date_millis()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 497 che coinvolge il nodo canonico `_birth_date_millis()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 497 che coinvolge il nodo canonico `_birth_date_millis()` è `L62`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 497 che coinvolge il nodo canonico `_birth_date_millis()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 497 che coinvolge il nodo canonico `_birth_date_millis()` è `ast`. Nel grafo del codice RDG, l'arco 497 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_birth_date_millis()`. Nel grafo del codice RDG, l'arco 497 registra come `target` l'identificatore `rve_transactions_rve_gateway_birth_date_millis` per la relazione che coinvolge il nodo canonico `_birth_date_millis()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 497 che coinvolge il nodo canonico `_birth_date_millis()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=526 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_birth_date_millis()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_birth_date_millis`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_birth_date_millis()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 526 che coinvolge il nodo canonico `_birth_date_millis()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 526 che coinvolge il nodo canonico `_birth_date_millis()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 526 che coinvolge il nodo canonico `_birth_date_millis()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 526 che coinvolge il nodo canonico `_birth_date_millis()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 526 che coinvolge il nodo canonico `_birth_date_millis()` è `L144`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 526 che coinvolge il nodo canonico `_birth_date_millis()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 526 che coinvolge il nodo canonico `_birth_date_millis()` è `ast`. Nel grafo del codice RDG, l'arco 526 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_birth_date_millis()`. Nel grafo del codice RDG, l'arco 526 registra come `target` l'identificatore `rve_transactions_rve_gateway_birth_date_millis` per la relazione che coinvolge il nodo canonico `_birth_date_millis()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 526 che coinvolge il nodo canonico `_birth_date_millis()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=527 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_birth_date_millis()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_birth_date_millis`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_birth_date_millis()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 527 che coinvolge il nodo canonico `_birth_date_millis()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 527 che coinvolge il nodo canonico `_birth_date_millis()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 527 che coinvolge il nodo canonico `_birth_date_millis()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 527 che coinvolge il nodo canonico `_birth_date_millis()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 527 che coinvolge il nodo canonico `_birth_date_millis()` è `L244`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 527 che coinvolge il nodo canonico `_birth_date_millis()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 527 che coinvolge il nodo canonico `_birth_date_millis()` è `ast`. Nel grafo del codice RDG, l'arco 527 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_birth_date_millis()`. Nel grafo del codice RDG, l'arco 527 registra come `target` l'identificatore `rve_transactions_rve_gateway_birth_date_millis` per la relazione che coinvolge il nodo canonico `_birth_date_millis()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 527 che coinvolge il nodo canonico `_birth_date_millis()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=247 id=Request-Dataset-Generator::rve_transactions_rve_gateway_birth_date_millis -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_birth_date_millis()` è `_birth_date_millis()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_birth_date_millis()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_birth_date_millis()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_birth_date_millis()` è `L62`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_birth_date_millis()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_birth_date_millis()` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_birth_date_millis()` è `_birth_date_millis()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_birth_date_millis()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_birth_date_millis()` è `rve_transactions_rve_gateway_birth_date_millis`. Nel grafo unificato, il nodo canonico `_birth_date_millis()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_birth_date_millis`.
<!-- GRAPH_EDGE graph=unificata index=526 role=outgoing -->
Nel grafo unificato, il nodo canonico `_birth_date_millis()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_birth_date_millis` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_birth_date_millis()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 526 che coinvolge il nodo canonico `_birth_date_millis()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 526 che coinvolge il nodo canonico `_birth_date_millis()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 526 che coinvolge il nodo canonico `_birth_date_millis()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 526 che coinvolge il nodo canonico `_birth_date_millis()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 526 che coinvolge il nodo canonico `_birth_date_millis()` è `L144`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 526 che coinvolge il nodo canonico `_birth_date_millis()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 526 che coinvolge il nodo canonico `_birth_date_millis()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 526 che coinvolge il nodo canonico `_birth_date_millis()` è `1.0`. Nel grafo unificato, l'arco 526 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_birth_date_millis` per la relazione che coinvolge il nodo canonico `_birth_date_millis()`. Nel grafo unificato, l'arco 526 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_birth_date_millis()`.
<!-- GRAPH_EDGE graph=unificata index=527 role=outgoing -->
Nel grafo unificato, il nodo canonico `_birth_date_millis()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_birth_date_millis` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_birth_date_millis()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 527 che coinvolge il nodo canonico `_birth_date_millis()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 527 che coinvolge il nodo canonico `_birth_date_millis()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 527 che coinvolge il nodo canonico `_birth_date_millis()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 527 che coinvolge il nodo canonico `_birth_date_millis()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 527 che coinvolge il nodo canonico `_birth_date_millis()` è `L244`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 527 che coinvolge il nodo canonico `_birth_date_millis()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 527 che coinvolge il nodo canonico `_birth_date_millis()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 527 che coinvolge il nodo canonico `_birth_date_millis()` è `1.0`. Nel grafo unificato, l'arco 527 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_birth_date_millis` per la relazione che coinvolge il nodo canonico `_birth_date_millis()`. Nel grafo unificato, l'arco 527 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_birth_date_millis()`.
<!-- GRAPH_EDGE graph=unificata index=497 role=incoming -->
Nel grafo unificato, il nodo canonico `_birth_date_millis()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_birth_date_millis`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_birth_date_millis()`. Nel grafo unificato, il tipo dichiarato dell'arco 497 che coinvolge il nodo canonico `_birth_date_millis()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 497 che coinvolge il nodo canonico `_birth_date_millis()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 497 che coinvolge il nodo canonico `_birth_date_millis()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 497 che coinvolge il nodo canonico `_birth_date_millis()` è `L62`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 497 che coinvolge il nodo canonico `_birth_date_millis()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 497 che coinvolge il nodo canonico `_birth_date_millis()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 497 che coinvolge il nodo canonico `_birth_date_millis()` è `1.0`. Nel grafo unificato, l'arco 497 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_birth_date_millis()`. Nel grafo unificato, l'arco 497 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_birth_date_millis` per la relazione che coinvolge il nodo canonico `_birth_date_millis()`.

### _build_mock_bundle()

<!-- GRAPH_NODE graph=codice index=261 id=rve_transactions_rve_gateway_build_mock_bundle -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_build_mock_bundle()` è `_build_mock_bundle()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_build_mock_bundle()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_build_mock_bundle()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_build_mock_bundle()` è `L394`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_build_mock_bundle()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_build_mock_bundle()` possiede l'identificatore esatto `rve_transactions_rve_gateway_build_mock_bundle`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_build_mock_bundle()` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_build_mock_bundle()` è `_build_mock_bundle()`.
<!-- GRAPH_EDGE graph=codice index=547 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_build_mock_bundle()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_mock_patient()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_build_mock_bundle` all'ID `rve_transactions_rve_gateway_mock_patient`. La relazione tecnica esplicita è la seguente: `_build_mock_bundle()` chiama `_mock_patient()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 547 che coinvolge il nodo canonico `_build_mock_bundle()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 547 che coinvolge il nodo canonico `_build_mock_bundle()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 547 che coinvolge il nodo canonico `_build_mock_bundle()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 547 che coinvolge il nodo canonico `_build_mock_bundle()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 547 che coinvolge il nodo canonico `_build_mock_bundle()` è `L395`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 547 che coinvolge il nodo canonico `_build_mock_bundle()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 547 che coinvolge il nodo canonico `_build_mock_bundle()` è `ast`. Nel grafo del codice RDG, l'arco 547 registra come `source` l'identificatore `rve_transactions_rve_gateway_build_mock_bundle` per la relazione che coinvolge il nodo canonico `_build_mock_bundle()`. Nel grafo del codice RDG, l'arco 547 registra come `target` l'identificatore `rve_transactions_rve_gateway_mock_patient` per la relazione che coinvolge il nodo canonico `_build_mock_bundle()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 547 che coinvolge il nodo canonico `_build_mock_bundle()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=498 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_mock_bundle()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_build_mock_bundle`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_build_mock_bundle()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 498 che coinvolge il nodo canonico `_build_mock_bundle()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 498 che coinvolge il nodo canonico `_build_mock_bundle()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 498 che coinvolge il nodo canonico `_build_mock_bundle()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 498 che coinvolge il nodo canonico `_build_mock_bundle()` è `L394`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 498 che coinvolge il nodo canonico `_build_mock_bundle()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 498 che coinvolge il nodo canonico `_build_mock_bundle()` è `ast`. Nel grafo del codice RDG, l'arco 498 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_build_mock_bundle()`. Nel grafo del codice RDG, l'arco 498 registra come `target` l'identificatore `rve_transactions_rve_gateway_build_mock_bundle` per la relazione che coinvolge il nodo canonico `_build_mock_bundle()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 498 che coinvolge il nodo canonico `_build_mock_bundle()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=544 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_mock_bundle()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_build_step` all'ID `rve_transactions_rve_gateway_build_mock_bundle`. La relazione tecnica esplicita è la seguente: `build_step() [rve_transactions_rve_gateway_build_step]` chiama `_build_mock_bundle()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 544 che coinvolge il nodo canonico `_build_mock_bundle()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 544 che coinvolge il nodo canonico `_build_mock_bundle()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 544 che coinvolge il nodo canonico `_build_mock_bundle()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 544 che coinvolge il nodo canonico `_build_mock_bundle()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 544 che coinvolge il nodo canonico `_build_mock_bundle()` è `L330`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 544 che coinvolge il nodo canonico `_build_mock_bundle()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 544 che coinvolge il nodo canonico `_build_mock_bundle()` è `ast`. Nel grafo del codice RDG, l'arco 544 registra come `source` l'identificatore `rve_transactions_rve_gateway_build_step` per la relazione che coinvolge il nodo canonico `_build_mock_bundle()`. Nel grafo del codice RDG, l'arco 544 registra come `target` l'identificatore `rve_transactions_rve_gateway_build_mock_bundle` per la relazione che coinvolge il nodo canonico `_build_mock_bundle()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 544 che coinvolge il nodo canonico `_build_mock_bundle()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=261 id=Request-Dataset-Generator::rve_transactions_rve_gateway_build_mock_bundle -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_build_mock_bundle()` è `_build_mock_bundle()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_build_mock_bundle()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_build_mock_bundle()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_build_mock_bundle()` è `L394`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_build_mock_bundle()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_build_mock_bundle()` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_build_mock_bundle()` è `_build_mock_bundle()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_build_mock_bundle()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_build_mock_bundle()` è `rve_transactions_rve_gateway_build_mock_bundle`. Nel grafo unificato, il nodo canonico `_build_mock_bundle()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_build_mock_bundle`.
<!-- GRAPH_EDGE graph=unificata index=547 role=outgoing -->
Nel grafo unificato, il nodo canonico `_build_mock_bundle()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_mock_patient()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_build_mock_bundle` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_mock_patient`. La relazione tecnica esplicita è la seguente: `_build_mock_bundle()` chiama `_mock_patient()`. Nel grafo unificato, il tipo dichiarato dell'arco 547 che coinvolge il nodo canonico `_build_mock_bundle()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 547 che coinvolge il nodo canonico `_build_mock_bundle()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 547 che coinvolge il nodo canonico `_build_mock_bundle()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 547 che coinvolge il nodo canonico `_build_mock_bundle()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 547 che coinvolge il nodo canonico `_build_mock_bundle()` è `L395`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 547 che coinvolge il nodo canonico `_build_mock_bundle()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 547 che coinvolge il nodo canonico `_build_mock_bundle()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 547 che coinvolge il nodo canonico `_build_mock_bundle()` è `1.0`. Nel grafo unificato, l'arco 547 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_build_mock_bundle` per la relazione che coinvolge il nodo canonico `_build_mock_bundle()`. Nel grafo unificato, l'arco 547 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_mock_patient` per la relazione che coinvolge il nodo canonico `_build_mock_bundle()`.
<!-- GRAPH_EDGE graph=unificata index=498 role=incoming -->
Nel grafo unificato, il nodo canonico `_build_mock_bundle()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_build_mock_bundle`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_build_mock_bundle()`. Nel grafo unificato, il tipo dichiarato dell'arco 498 che coinvolge il nodo canonico `_build_mock_bundle()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 498 che coinvolge il nodo canonico `_build_mock_bundle()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 498 che coinvolge il nodo canonico `_build_mock_bundle()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 498 che coinvolge il nodo canonico `_build_mock_bundle()` è `L394`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 498 che coinvolge il nodo canonico `_build_mock_bundle()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 498 che coinvolge il nodo canonico `_build_mock_bundle()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 498 che coinvolge il nodo canonico `_build_mock_bundle()` è `1.0`. Nel grafo unificato, l'arco 498 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_build_mock_bundle()`. Nel grafo unificato, l'arco 498 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_build_mock_bundle` per la relazione che coinvolge il nodo canonico `_build_mock_bundle()`.
<!-- GRAPH_EDGE graph=unificata index=544 role=incoming -->
Nel grafo unificato, il nodo canonico `_build_mock_bundle()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_build_step` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_build_mock_bundle`. La relazione tecnica esplicita è la seguente: `build_step() [rve_transactions_rve_gateway_build_step]` chiama `_build_mock_bundle()`. Nel grafo unificato, il tipo dichiarato dell'arco 544 che coinvolge il nodo canonico `_build_mock_bundle()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 544 che coinvolge il nodo canonico `_build_mock_bundle()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 544 che coinvolge il nodo canonico `_build_mock_bundle()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 544 che coinvolge il nodo canonico `_build_mock_bundle()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 544 che coinvolge il nodo canonico `_build_mock_bundle()` è `L330`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 544 che coinvolge il nodo canonico `_build_mock_bundle()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 544 che coinvolge il nodo canonico `_build_mock_bundle()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 544 che coinvolge il nodo canonico `_build_mock_bundle()` è `1.0`. Nel grafo unificato, l'arco 544 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_build_step` per la relazione che coinvolge il nodo canonico `_build_mock_bundle()`. Nel grafo unificato, l'arco 544 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_build_mock_bundle` per la relazione che coinvolge il nodo canonico `_build_mock_bundle()`.

### _family_name()

<!-- GRAPH_NODE graph=codice index=245 id=rve_transactions_rve_gateway_family_name -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_family_name()` è `_family_name()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_family_name()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_family_name()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_family_name()` è `L46`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_family_name()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_family_name()` possiede l'identificatore esatto `rve_transactions_rve_gateway_family_name`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_family_name()` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_family_name()` è `_family_name()`.
Nel grafo del codice RDG, il nodo canonico `_family_name()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=501 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_family_name()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_family_name`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_family_name()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 501 che coinvolge il nodo canonico `_family_name()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 501 che coinvolge il nodo canonico `_family_name()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 501 che coinvolge il nodo canonico `_family_name()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 501 che coinvolge il nodo canonico `_family_name()` è `L46`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 501 che coinvolge il nodo canonico `_family_name()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 501 che coinvolge il nodo canonico `_family_name()` è `ast`. Nel grafo del codice RDG, l'arco 501 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_family_name()`. Nel grafo del codice RDG, l'arco 501 registra come `target` l'identificatore `rve_transactions_rve_gateway_family_name` per la relazione che coinvolge il nodo canonico `_family_name()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 501 che coinvolge il nodo canonico `_family_name()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=522 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_family_name()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_family_name`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_family_name()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 522 che coinvolge il nodo canonico `_family_name()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 522 che coinvolge il nodo canonico `_family_name()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 522 che coinvolge il nodo canonico `_family_name()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 522 che coinvolge il nodo canonico `_family_name()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 522 che coinvolge il nodo canonico `_family_name()` è `L133`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 522 che coinvolge il nodo canonico `_family_name()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 522 che coinvolge il nodo canonico `_family_name()` è `ast`. Nel grafo del codice RDG, l'arco 522 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_family_name()`. Nel grafo del codice RDG, l'arco 522 registra come `target` l'identificatore `rve_transactions_rve_gateway_family_name` per la relazione che coinvolge il nodo canonico `_family_name()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 522 che coinvolge il nodo canonico `_family_name()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=523 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_family_name()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_family_name`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_family_name()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 523 che coinvolge il nodo canonico `_family_name()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 523 che coinvolge il nodo canonico `_family_name()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 523 che coinvolge il nodo canonico `_family_name()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 523 che coinvolge il nodo canonico `_family_name()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 523 che coinvolge il nodo canonico `_family_name()` è `L242`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 523 che coinvolge il nodo canonico `_family_name()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 523 che coinvolge il nodo canonico `_family_name()` è `ast`. Nel grafo del codice RDG, l'arco 523 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_family_name()`. Nel grafo del codice RDG, l'arco 523 registra come `target` l'identificatore `rve_transactions_rve_gateway_family_name` per la relazione che coinvolge il nodo canonico `_family_name()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 523 che coinvolge il nodo canonico `_family_name()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=245 id=Request-Dataset-Generator::rve_transactions_rve_gateway_family_name -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_family_name()` è `_family_name()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_family_name()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_family_name()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_family_name()` è `L46`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_family_name()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_family_name()` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_family_name()` è `_family_name()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_family_name()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_family_name()` è `rve_transactions_rve_gateway_family_name`. Nel grafo unificato, il nodo canonico `_family_name()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_family_name`.
<!-- GRAPH_EDGE graph=unificata index=522 role=outgoing -->
Nel grafo unificato, il nodo canonico `_family_name()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_family_name` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_family_name()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 522 che coinvolge il nodo canonico `_family_name()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 522 che coinvolge il nodo canonico `_family_name()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 522 che coinvolge il nodo canonico `_family_name()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 522 che coinvolge il nodo canonico `_family_name()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 522 che coinvolge il nodo canonico `_family_name()` è `L133`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 522 che coinvolge il nodo canonico `_family_name()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 522 che coinvolge il nodo canonico `_family_name()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 522 che coinvolge il nodo canonico `_family_name()` è `1.0`. Nel grafo unificato, l'arco 522 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_family_name` per la relazione che coinvolge il nodo canonico `_family_name()`. Nel grafo unificato, l'arco 522 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_family_name()`.
<!-- GRAPH_EDGE graph=unificata index=523 role=outgoing -->
Nel grafo unificato, il nodo canonico `_family_name()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_family_name` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_family_name()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 523 che coinvolge il nodo canonico `_family_name()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 523 che coinvolge il nodo canonico `_family_name()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 523 che coinvolge il nodo canonico `_family_name()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 523 che coinvolge il nodo canonico `_family_name()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 523 che coinvolge il nodo canonico `_family_name()` è `L242`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 523 che coinvolge il nodo canonico `_family_name()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 523 che coinvolge il nodo canonico `_family_name()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 523 che coinvolge il nodo canonico `_family_name()` è `1.0`. Nel grafo unificato, l'arco 523 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_family_name` per la relazione che coinvolge il nodo canonico `_family_name()`. Nel grafo unificato, l'arco 523 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_family_name()`.
<!-- GRAPH_EDGE graph=unificata index=501 role=incoming -->
Nel grafo unificato, il nodo canonico `_family_name()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_family_name`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_family_name()`. Nel grafo unificato, il tipo dichiarato dell'arco 501 che coinvolge il nodo canonico `_family_name()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 501 che coinvolge il nodo canonico `_family_name()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 501 che coinvolge il nodo canonico `_family_name()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 501 che coinvolge il nodo canonico `_family_name()` è `L46`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 501 che coinvolge il nodo canonico `_family_name()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 501 che coinvolge il nodo canonico `_family_name()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 501 che coinvolge il nodo canonico `_family_name()` è `1.0`. Nel grafo unificato, l'arco 501 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_family_name()`. Nel grafo unificato, l'arco 501 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_family_name` per la relazione che coinvolge il nodo canonico `_family_name()`.

### _first_address()

<!-- GRAPH_NODE graph=codice index=251 id=rve_transactions_rve_gateway_first_address -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_first_address()` è `_first_address()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_first_address()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_first_address()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_first_address()` è `L99`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_first_address()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_first_address()` possiede l'identificatore esatto `rve_transactions_rve_gateway_first_address`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_first_address()` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_first_address()` è `_first_address()`.
Nel grafo del codice RDG, il nodo canonico `_first_address()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=502 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_first_address()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_first_address`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_first_address()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 502 che coinvolge il nodo canonico `_first_address()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 502 che coinvolge il nodo canonico `_first_address()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 502 che coinvolge il nodo canonico `_first_address()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 502 che coinvolge il nodo canonico `_first_address()` è `L99`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 502 che coinvolge il nodo canonico `_first_address()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 502 che coinvolge il nodo canonico `_first_address()` è `ast`. Nel grafo del codice RDG, l'arco 502 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_first_address()`. Nel grafo del codice RDG, l'arco 502 registra come `target` l'identificatore `rve_transactions_rve_gateway_first_address` per la relazione che coinvolge il nodo canonico `_first_address()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 502 che coinvolge il nodo canonico `_first_address()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=534 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_first_address()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_address_locality()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_address_locality` all'ID `rve_transactions_rve_gateway_first_address`. La relazione tecnica esplicita è la seguente: `_address_locality()` chiama `_first_address()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 534 che coinvolge il nodo canonico `_first_address()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 534 che coinvolge il nodo canonico `_first_address()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 534 che coinvolge il nodo canonico `_first_address()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 534 che coinvolge il nodo canonico `_first_address()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 534 che coinvolge il nodo canonico `_first_address()` è `L109`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 534 che coinvolge il nodo canonico `_first_address()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 534 che coinvolge il nodo canonico `_first_address()` è `ast`. Nel grafo del codice RDG, l'arco 534 registra come `source` l'identificatore `rve_transactions_rve_gateway_address_locality` per la relazione che coinvolge il nodo canonico `_first_address()`. Nel grafo del codice RDG, l'arco 534 registra come `target` l'identificatore `rve_transactions_rve_gateway_first_address` per la relazione che coinvolge il nodo canonico `_first_address()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 534 che coinvolge il nodo canonico `_first_address()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=535 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_first_address()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_address_postal_code()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_address_postal_code` all'ID `rve_transactions_rve_gateway_first_address`. La relazione tecnica esplicita è la seguente: `_address_postal_code()` chiama `_first_address()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 535 che coinvolge il nodo canonico `_first_address()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 535 che coinvolge il nodo canonico `_first_address()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 535 che coinvolge il nodo canonico `_first_address()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 535 che coinvolge il nodo canonico `_first_address()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 535 che coinvolge il nodo canonico `_first_address()` è `L116`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 535 che coinvolge il nodo canonico `_first_address()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 535 che coinvolge il nodo canonico `_first_address()` è `ast`. Nel grafo del codice RDG, l'arco 535 registra come `source` l'identificatore `rve_transactions_rve_gateway_address_postal_code` per la relazione che coinvolge il nodo canonico `_first_address()`. Nel grafo del codice RDG, l'arco 535 registra come `target` l'identificatore `rve_transactions_rve_gateway_first_address` per la relazione che coinvolge il nodo canonico `_first_address()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 535 che coinvolge il nodo canonico `_first_address()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=536 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_first_address()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_address_text()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_address_text` all'ID `rve_transactions_rve_gateway_first_address`. La relazione tecnica esplicita è la seguente: `_address_text()` chiama `_first_address()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 536 che coinvolge il nodo canonico `_first_address()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 536 che coinvolge il nodo canonico `_first_address()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 536 che coinvolge il nodo canonico `_first_address()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 536 che coinvolge il nodo canonico `_first_address()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 536 che coinvolge il nodo canonico `_first_address()` è `L105`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 536 che coinvolge il nodo canonico `_first_address()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 536 che coinvolge il nodo canonico `_first_address()` è `ast`. Nel grafo del codice RDG, l'arco 536 registra come `source` l'identificatore `rve_transactions_rve_gateway_address_text` per la relazione che coinvolge il nodo canonico `_first_address()`. Nel grafo del codice RDG, l'arco 536 registra come `target` l'identificatore `rve_transactions_rve_gateway_first_address` per la relazione che coinvolge il nodo canonico `_first_address()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 536 che coinvolge il nodo canonico `_first_address()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=251 id=Request-Dataset-Generator::rve_transactions_rve_gateway_first_address -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_first_address()` è `_first_address()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_first_address()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_first_address()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_first_address()` è `L99`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_first_address()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_first_address()` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_first_address()` è `_first_address()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_first_address()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_first_address()` è `rve_transactions_rve_gateway_first_address`. Nel grafo unificato, il nodo canonico `_first_address()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_first_address`.
<!-- GRAPH_EDGE graph=unificata index=534 role=outgoing -->
Nel grafo unificato, il nodo canonico `_first_address()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_address_locality()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_first_address` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_locality`. La relazione tecnica esplicita è la seguente: `_first_address()` chiama `_address_locality()`. Nel grafo unificato, il tipo dichiarato dell'arco 534 che coinvolge il nodo canonico `_first_address()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 534 che coinvolge il nodo canonico `_first_address()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 534 che coinvolge il nodo canonico `_first_address()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 534 che coinvolge il nodo canonico `_first_address()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 534 che coinvolge il nodo canonico `_first_address()` è `L109`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 534 che coinvolge il nodo canonico `_first_address()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 534 che coinvolge il nodo canonico `_first_address()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 534 che coinvolge il nodo canonico `_first_address()` è `1.0`. Nel grafo unificato, l'arco 534 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_first_address` per la relazione che coinvolge il nodo canonico `_first_address()`. Nel grafo unificato, l'arco 534 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_locality` per la relazione che coinvolge il nodo canonico `_first_address()`.
<!-- GRAPH_EDGE graph=unificata index=535 role=outgoing -->
Nel grafo unificato, il nodo canonico `_first_address()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_address_postal_code()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_first_address` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_postal_code`. La relazione tecnica esplicita è la seguente: `_first_address()` chiama `_address_postal_code()`. Nel grafo unificato, il tipo dichiarato dell'arco 535 che coinvolge il nodo canonico `_first_address()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 535 che coinvolge il nodo canonico `_first_address()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 535 che coinvolge il nodo canonico `_first_address()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 535 che coinvolge il nodo canonico `_first_address()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 535 che coinvolge il nodo canonico `_first_address()` è `L116`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 535 che coinvolge il nodo canonico `_first_address()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 535 che coinvolge il nodo canonico `_first_address()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 535 che coinvolge il nodo canonico `_first_address()` è `1.0`. Nel grafo unificato, l'arco 535 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_first_address` per la relazione che coinvolge il nodo canonico `_first_address()`. Nel grafo unificato, l'arco 535 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_postal_code` per la relazione che coinvolge il nodo canonico `_first_address()`.
<!-- GRAPH_EDGE graph=unificata index=536 role=outgoing -->
Nel grafo unificato, il nodo canonico `_first_address()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_address_text()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_first_address` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_text`. La relazione tecnica esplicita è la seguente: `_first_address()` chiama `_address_text()`. Nel grafo unificato, il tipo dichiarato dell'arco 536 che coinvolge il nodo canonico `_first_address()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 536 che coinvolge il nodo canonico `_first_address()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 536 che coinvolge il nodo canonico `_first_address()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 536 che coinvolge il nodo canonico `_first_address()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 536 che coinvolge il nodo canonico `_first_address()` è `L105`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 536 che coinvolge il nodo canonico `_first_address()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 536 che coinvolge il nodo canonico `_first_address()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 536 che coinvolge il nodo canonico `_first_address()` è `1.0`. Nel grafo unificato, l'arco 536 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_first_address` per la relazione che coinvolge il nodo canonico `_first_address()`. Nel grafo unificato, l'arco 536 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_text` per la relazione che coinvolge il nodo canonico `_first_address()`.
<!-- GRAPH_EDGE graph=unificata index=502 role=incoming -->
Nel grafo unificato, il nodo canonico `_first_address()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_first_address`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_first_address()`. Nel grafo unificato, il tipo dichiarato dell'arco 502 che coinvolge il nodo canonico `_first_address()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 502 che coinvolge il nodo canonico `_first_address()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 502 che coinvolge il nodo canonico `_first_address()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 502 che coinvolge il nodo canonico `_first_address()` è `L99`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 502 che coinvolge il nodo canonico `_first_address()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 502 che coinvolge il nodo canonico `_first_address()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 502 che coinvolge il nodo canonico `_first_address()` è `1.0`. Nel grafo unificato, l'arco 502 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_first_address()`. Nel grafo unificato, l'arco 502 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_first_address` per la relazione che coinvolge il nodo canonico `_first_address()`.

### _first_string()

<!-- GRAPH_NODE graph=codice index=243 id=rve_transactions_rve_gateway_first_string -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_first_string()` è `_first_string()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_first_string()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_first_string()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_first_string()` è `L28`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_first_string()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_first_string()` possiede l'identificatore esatto `rve_transactions_rve_gateway_first_string`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_first_string()` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_first_string()` è `_first_string()`.
Nel grafo del codice RDG, il nodo canonico `_first_string()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=503 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_first_string()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_first_string`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_first_string()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 503 che coinvolge il nodo canonico `_first_string()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 503 che coinvolge il nodo canonico `_first_string()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 503 che coinvolge il nodo canonico `_first_string()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 503 che coinvolge il nodo canonico `_first_string()` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 503 che coinvolge il nodo canonico `_first_string()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 503 che coinvolge il nodo canonico `_first_string()` è `ast`. Nel grafo del codice RDG, l'arco 503 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_first_string()`. Nel grafo del codice RDG, l'arco 503 registra come `target` l'identificatore `rve_transactions_rve_gateway_first_string` per la relazione che coinvolge il nodo canonico `_first_string()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 503 che coinvolge il nodo canonico `_first_string()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=518 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_first_string()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_first_string`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_first_string()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 518 che coinvolge il nodo canonico `_first_string()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 518 che coinvolge il nodo canonico `_first_string()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 518 che coinvolge il nodo canonico `_first_string()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 518 che coinvolge il nodo canonico `_first_string()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 518 che coinvolge il nodo canonico `_first_string()` è `L132`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 518 che coinvolge il nodo canonico `_first_string()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 518 che coinvolge il nodo canonico `_first_string()` è `ast`. Nel grafo del codice RDG, l'arco 518 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_first_string()`. Nel grafo del codice RDG, l'arco 518 registra come `target` l'identificatore `rve_transactions_rve_gateway_first_string` per la relazione che coinvolge il nodo canonico `_first_string()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 518 che coinvolge il nodo canonico `_first_string()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=519 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_first_string()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_first_string`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_first_string()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 519 che coinvolge il nodo canonico `_first_string()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 519 che coinvolge il nodo canonico `_first_string()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 519 che coinvolge il nodo canonico `_first_string()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 519 che coinvolge il nodo canonico `_first_string()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 519 che coinvolge il nodo canonico `_first_string()` è `L241`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 519 che coinvolge il nodo canonico `_first_string()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 519 che coinvolge il nodo canonico `_first_string()` è `ast`. Nel grafo del codice RDG, l'arco 519 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_first_string()`. Nel grafo del codice RDG, l'arco 519 registra come `target` l'identificatore `rve_transactions_rve_gateway_first_string` per la relazione che coinvolge il nodo canonico `_first_string()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 519 che coinvolge il nodo canonico `_first_string()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=243 id=Request-Dataset-Generator::rve_transactions_rve_gateway_first_string -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_first_string()` è `_first_string()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_first_string()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_first_string()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_first_string()` è `L28`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_first_string()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_first_string()` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_first_string()` è `_first_string()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_first_string()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_first_string()` è `rve_transactions_rve_gateway_first_string`. Nel grafo unificato, il nodo canonico `_first_string()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_first_string`.
<!-- GRAPH_EDGE graph=unificata index=518 role=outgoing -->
Nel grafo unificato, il nodo canonico `_first_string()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_first_string` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_first_string()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 518 che coinvolge il nodo canonico `_first_string()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 518 che coinvolge il nodo canonico `_first_string()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 518 che coinvolge il nodo canonico `_first_string()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 518 che coinvolge il nodo canonico `_first_string()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 518 che coinvolge il nodo canonico `_first_string()` è `L132`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 518 che coinvolge il nodo canonico `_first_string()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 518 che coinvolge il nodo canonico `_first_string()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 518 che coinvolge il nodo canonico `_first_string()` è `1.0`. Nel grafo unificato, l'arco 518 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_first_string` per la relazione che coinvolge il nodo canonico `_first_string()`. Nel grafo unificato, l'arco 518 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_first_string()`.
<!-- GRAPH_EDGE graph=unificata index=519 role=outgoing -->
Nel grafo unificato, il nodo canonico `_first_string()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_first_string` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_first_string()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 519 che coinvolge il nodo canonico `_first_string()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 519 che coinvolge il nodo canonico `_first_string()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 519 che coinvolge il nodo canonico `_first_string()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 519 che coinvolge il nodo canonico `_first_string()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 519 che coinvolge il nodo canonico `_first_string()` è `L241`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 519 che coinvolge il nodo canonico `_first_string()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 519 che coinvolge il nodo canonico `_first_string()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 519 che coinvolge il nodo canonico `_first_string()` è `1.0`. Nel grafo unificato, l'arco 519 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_first_string` per la relazione che coinvolge il nodo canonico `_first_string()`. Nel grafo unificato, l'arco 519 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_first_string()`.
<!-- GRAPH_EDGE graph=unificata index=503 role=incoming -->
Nel grafo unificato, il nodo canonico `_first_string()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_first_string`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_first_string()`. Nel grafo unificato, il tipo dichiarato dell'arco 503 che coinvolge il nodo canonico `_first_string()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 503 che coinvolge il nodo canonico `_first_string()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 503 che coinvolge il nodo canonico `_first_string()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 503 che coinvolge il nodo canonico `_first_string()` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 503 che coinvolge il nodo canonico `_first_string()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 503 che coinvolge il nodo canonico `_first_string()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 503 che coinvolge il nodo canonico `_first_string()` è `1.0`. Nel grafo unificato, l'arco 503 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_first_string()`. Nel grafo unificato, l'arco 503 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_first_string` per la relazione che coinvolge il nodo canonico `_first_string()`.

### _first_value()

<!-- GRAPH_NODE graph=codice index=242 id=rve_transactions_rve_gateway_first_value -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_first_value()` è `_first_value()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_first_value()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_first_value()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_first_value()` è `L19`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_first_value()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_first_value()` possiede l'identificatore esatto `rve_transactions_rve_gateway_first_value`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_first_value()` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_first_value()` è `_first_value()`.
Nel grafo del codice RDG, il nodo canonico `_first_value()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=504 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_first_value()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_first_value`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_first_value()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 504 che coinvolge il nodo canonico `_first_value()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 504 che coinvolge il nodo canonico `_first_value()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 504 che coinvolge il nodo canonico `_first_value()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 504 che coinvolge il nodo canonico `_first_value()` è `L19`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 504 che coinvolge il nodo canonico `_first_value()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 504 che coinvolge il nodo canonico `_first_value()` è `ast`. Nel grafo del codice RDG, l'arco 504 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_first_value()`. Nel grafo del codice RDG, l'arco 504 registra come `target` l'identificatore `rve_transactions_rve_gateway_first_value` per la relazione che coinvolge il nodo canonico `_first_value()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 504 che coinvolge il nodo canonico `_first_value()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=516 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_first_value()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_first_value`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_first_value()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 516 che coinvolge il nodo canonico `_first_value()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 516 che coinvolge il nodo canonico `_first_value()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 516 che coinvolge il nodo canonico `_first_value()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 516 che coinvolge il nodo canonico `_first_value()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 516 che coinvolge il nodo canonico `_first_value()` è `L130`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 516 che coinvolge il nodo canonico `_first_value()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 516 che coinvolge il nodo canonico `_first_value()` è `ast`. Nel grafo del codice RDG, l'arco 516 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_first_value()`. Nel grafo del codice RDG, l'arco 516 registra come `target` l'identificatore `rve_transactions_rve_gateway_first_value` per la relazione che coinvolge il nodo canonico `_first_value()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 516 che coinvolge il nodo canonico `_first_value()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=517 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_first_value()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_first_value`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_first_value()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 517 che coinvolge il nodo canonico `_first_value()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 517 che coinvolge il nodo canonico `_first_value()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 517 che coinvolge il nodo canonico `_first_value()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 517 che coinvolge il nodo canonico `_first_value()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 517 che coinvolge il nodo canonico `_first_value()` è `L225`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 517 che coinvolge il nodo canonico `_first_value()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 517 che coinvolge il nodo canonico `_first_value()` è `ast`. Nel grafo del codice RDG, l'arco 517 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_first_value()`. Nel grafo del codice RDG, l'arco 517 registra come `target` l'identificatore `rve_transactions_rve_gateway_first_value` per la relazione che coinvolge il nodo canonico `_first_value()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 517 che coinvolge il nodo canonico `_first_value()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=242 id=Request-Dataset-Generator::rve_transactions_rve_gateway_first_value -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_first_value()` è `_first_value()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_first_value()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_first_value()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_first_value()` è `L19`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_first_value()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_first_value()` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_first_value()` è `_first_value()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_first_value()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_first_value()` è `rve_transactions_rve_gateway_first_value`. Nel grafo unificato, il nodo canonico `_first_value()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_first_value`.
<!-- GRAPH_EDGE graph=unificata index=516 role=outgoing -->
Nel grafo unificato, il nodo canonico `_first_value()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_first_value` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_first_value()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 516 che coinvolge il nodo canonico `_first_value()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 516 che coinvolge il nodo canonico `_first_value()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 516 che coinvolge il nodo canonico `_first_value()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 516 che coinvolge il nodo canonico `_first_value()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 516 che coinvolge il nodo canonico `_first_value()` è `L130`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 516 che coinvolge il nodo canonico `_first_value()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 516 che coinvolge il nodo canonico `_first_value()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 516 che coinvolge il nodo canonico `_first_value()` è `1.0`. Nel grafo unificato, l'arco 516 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_first_value` per la relazione che coinvolge il nodo canonico `_first_value()`. Nel grafo unificato, l'arco 516 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_first_value()`.
<!-- GRAPH_EDGE graph=unificata index=517 role=outgoing -->
Nel grafo unificato, il nodo canonico `_first_value()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_first_value` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_first_value()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 517 che coinvolge il nodo canonico `_first_value()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 517 che coinvolge il nodo canonico `_first_value()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 517 che coinvolge il nodo canonico `_first_value()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 517 che coinvolge il nodo canonico `_first_value()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 517 che coinvolge il nodo canonico `_first_value()` è `L225`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 517 che coinvolge il nodo canonico `_first_value()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 517 che coinvolge il nodo canonico `_first_value()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 517 che coinvolge il nodo canonico `_first_value()` è `1.0`. Nel grafo unificato, l'arco 517 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_first_value` per la relazione che coinvolge il nodo canonico `_first_value()`. Nel grafo unificato, l'arco 517 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_first_value()`.
<!-- GRAPH_EDGE graph=unificata index=504 role=incoming -->
Nel grafo unificato, il nodo canonico `_first_value()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_first_value`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_first_value()`. Nel grafo unificato, il tipo dichiarato dell'arco 504 che coinvolge il nodo canonico `_first_value()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 504 che coinvolge il nodo canonico `_first_value()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 504 che coinvolge il nodo canonico `_first_value()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 504 che coinvolge il nodo canonico `_first_value()` è `L19`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 504 che coinvolge il nodo canonico `_first_value()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 504 che coinvolge il nodo canonico `_first_value()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 504 che coinvolge il nodo canonico `_first_value()` è `1.0`. Nel grafo unificato, l'arco 504 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_first_value()`. Nel grafo unificato, l'arco 504 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_first_value` per la relazione che coinvolge il nodo canonico `_first_value()`.

### _given_name()

<!-- GRAPH_NODE graph=codice index=244 id=rve_transactions_rve_gateway_given_name -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_given_name()` è `_given_name()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_given_name()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_given_name()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_given_name()` è `L38`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_given_name()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_given_name()` possiede l'identificatore esatto `rve_transactions_rve_gateway_given_name`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_given_name()` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_given_name()` è `_given_name()`.
Nel grafo del codice RDG, il nodo canonico `_given_name()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=505 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_given_name()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_given_name`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_given_name()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 505 che coinvolge il nodo canonico `_given_name()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 505 che coinvolge il nodo canonico `_given_name()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 505 che coinvolge il nodo canonico `_given_name()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 505 che coinvolge il nodo canonico `_given_name()` è `L38`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 505 che coinvolge il nodo canonico `_given_name()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 505 che coinvolge il nodo canonico `_given_name()` è `ast`. Nel grafo del codice RDG, l'arco 505 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_given_name()`. Nel grafo del codice RDG, l'arco 505 registra come `target` l'identificatore `rve_transactions_rve_gateway_given_name` per la relazione che coinvolge il nodo canonico `_given_name()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 505 che coinvolge il nodo canonico `_given_name()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=520 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_given_name()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_given_name`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_given_name()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 520 che coinvolge il nodo canonico `_given_name()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 520 che coinvolge il nodo canonico `_given_name()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 520 che coinvolge il nodo canonico `_given_name()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 520 che coinvolge il nodo canonico `_given_name()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 520 che coinvolge il nodo canonico `_given_name()` è `L132`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 520 che coinvolge il nodo canonico `_given_name()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 520 che coinvolge il nodo canonico `_given_name()` è `ast`. Nel grafo del codice RDG, l'arco 520 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_given_name()`. Nel grafo del codice RDG, l'arco 520 registra come `target` l'identificatore `rve_transactions_rve_gateway_given_name` per la relazione che coinvolge il nodo canonico `_given_name()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 520 che coinvolge il nodo canonico `_given_name()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=521 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_given_name()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_given_name`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_given_name()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 521 che coinvolge il nodo canonico `_given_name()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 521 che coinvolge il nodo canonico `_given_name()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 521 che coinvolge il nodo canonico `_given_name()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 521 che coinvolge il nodo canonico `_given_name()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 521 che coinvolge il nodo canonico `_given_name()` è `L241`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 521 che coinvolge il nodo canonico `_given_name()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 521 che coinvolge il nodo canonico `_given_name()` è `ast`. Nel grafo del codice RDG, l'arco 521 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_given_name()`. Nel grafo del codice RDG, l'arco 521 registra come `target` l'identificatore `rve_transactions_rve_gateway_given_name` per la relazione che coinvolge il nodo canonico `_given_name()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 521 che coinvolge il nodo canonico `_given_name()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=244 id=Request-Dataset-Generator::rve_transactions_rve_gateway_given_name -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_given_name()` è `_given_name()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_given_name()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_given_name()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_given_name()` è `L38`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_given_name()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_given_name()` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_given_name()` è `_given_name()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_given_name()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_given_name()` è `rve_transactions_rve_gateway_given_name`. Nel grafo unificato, il nodo canonico `_given_name()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_given_name`.
<!-- GRAPH_EDGE graph=unificata index=520 role=outgoing -->
Nel grafo unificato, il nodo canonico `_given_name()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_given_name` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_given_name()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 520 che coinvolge il nodo canonico `_given_name()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 520 che coinvolge il nodo canonico `_given_name()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 520 che coinvolge il nodo canonico `_given_name()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 520 che coinvolge il nodo canonico `_given_name()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 520 che coinvolge il nodo canonico `_given_name()` è `L132`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 520 che coinvolge il nodo canonico `_given_name()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 520 che coinvolge il nodo canonico `_given_name()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 520 che coinvolge il nodo canonico `_given_name()` è `1.0`. Nel grafo unificato, l'arco 520 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_given_name` per la relazione che coinvolge il nodo canonico `_given_name()`. Nel grafo unificato, l'arco 520 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_given_name()`.
<!-- GRAPH_EDGE graph=unificata index=521 role=outgoing -->
Nel grafo unificato, il nodo canonico `_given_name()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_given_name` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_given_name()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 521 che coinvolge il nodo canonico `_given_name()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 521 che coinvolge il nodo canonico `_given_name()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 521 che coinvolge il nodo canonico `_given_name()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 521 che coinvolge il nodo canonico `_given_name()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 521 che coinvolge il nodo canonico `_given_name()` è `L241`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 521 che coinvolge il nodo canonico `_given_name()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 521 che coinvolge il nodo canonico `_given_name()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 521 che coinvolge il nodo canonico `_given_name()` è `1.0`. Nel grafo unificato, l'arco 521 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_given_name` per la relazione che coinvolge il nodo canonico `_given_name()`. Nel grafo unificato, l'arco 521 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_given_name()`.
<!-- GRAPH_EDGE graph=unificata index=505 role=incoming -->
Nel grafo unificato, il nodo canonico `_given_name()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_given_name`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_given_name()`. Nel grafo unificato, il tipo dichiarato dell'arco 505 che coinvolge il nodo canonico `_given_name()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 505 che coinvolge il nodo canonico `_given_name()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 505 che coinvolge il nodo canonico `_given_name()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 505 che coinvolge il nodo canonico `_given_name()` è `L38`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 505 che coinvolge il nodo canonico `_given_name()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 505 che coinvolge il nodo canonico `_given_name()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 505 che coinvolge il nodo canonico `_given_name()` è `1.0`. Nel grafo unificato, l'arco 505 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_given_name()`. Nel grafo unificato, l'arco 505 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_given_name` per la relazione che coinvolge il nodo canonico `_given_name()`.

### _identifier_value()

<!-- GRAPH_NODE graph=codice index=248 id=rve_transactions_rve_gateway_identifier_value -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_identifier_value()` è `_identifier_value()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_identifier_value()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_identifier_value()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_identifier_value()` è `L73`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_identifier_value()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_identifier_value()` possiede l'identificatore esatto `rve_transactions_rve_gateway_identifier_value`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_identifier_value()` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_identifier_value()` è `_identifier_value()`.
Nel grafo del codice RDG, il nodo canonico `_identifier_value()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=506 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_identifier_value()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_identifier_value`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_identifier_value()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 506 che coinvolge il nodo canonico `_identifier_value()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 506 che coinvolge il nodo canonico `_identifier_value()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 506 che coinvolge il nodo canonico `_identifier_value()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 506 che coinvolge il nodo canonico `_identifier_value()` è `L73`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 506 che coinvolge il nodo canonico `_identifier_value()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 506 che coinvolge il nodo canonico `_identifier_value()` è `ast`. Nel grafo del codice RDG, l'arco 506 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_identifier_value()`. Nel grafo del codice RDG, l'arco 506 registra come `target` l'identificatore `rve_transactions_rve_gateway_identifier_value` per la relazione che coinvolge il nodo canonico `_identifier_value()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 506 che coinvolge il nodo canonico `_identifier_value()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=528 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_identifier_value()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_identifier_value`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_identifier_value()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 528 che coinvolge il nodo canonico `_identifier_value()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 528 che coinvolge il nodo canonico `_identifier_value()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 528 che coinvolge il nodo canonico `_identifier_value()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 528 che coinvolge il nodo canonico `_identifier_value()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 528 che coinvolge il nodo canonico `_identifier_value()` è `L134`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 528 che coinvolge il nodo canonico `_identifier_value()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 528 che coinvolge il nodo canonico `_identifier_value()` è `ast`. Nel grafo del codice RDG, l'arco 528 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_identifier_value()`. Nel grafo del codice RDG, l'arco 528 registra come `target` l'identificatore `rve_transactions_rve_gateway_identifier_value` per la relazione che coinvolge il nodo canonico `_identifier_value()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 528 che coinvolge il nodo canonico `_identifier_value()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=529 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_identifier_value()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_identifier_value`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_identifier_value()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 529 che coinvolge il nodo canonico `_identifier_value()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 529 che coinvolge il nodo canonico `_identifier_value()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 529 che coinvolge il nodo canonico `_identifier_value()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 529 che coinvolge il nodo canonico `_identifier_value()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 529 che coinvolge il nodo canonico `_identifier_value()` è `L235`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 529 che coinvolge il nodo canonico `_identifier_value()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 529 che coinvolge il nodo canonico `_identifier_value()` è `ast`. Nel grafo del codice RDG, l'arco 529 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_identifier_value()`. Nel grafo del codice RDG, l'arco 529 registra come `target` l'identificatore `rve_transactions_rve_gateway_identifier_value` per la relazione che coinvolge il nodo canonico `_identifier_value()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 529 che coinvolge il nodo canonico `_identifier_value()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=248 id=Request-Dataset-Generator::rve_transactions_rve_gateway_identifier_value -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_identifier_value()` è `_identifier_value()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_identifier_value()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_identifier_value()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_identifier_value()` è `L73`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_identifier_value()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_identifier_value()` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_identifier_value()` è `_identifier_value()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_identifier_value()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_identifier_value()` è `rve_transactions_rve_gateway_identifier_value`. Nel grafo unificato, il nodo canonico `_identifier_value()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_identifier_value`.
<!-- GRAPH_EDGE graph=unificata index=528 role=outgoing -->
Nel grafo unificato, il nodo canonico `_identifier_value()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_identifier_value` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_identifier_value()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 528 che coinvolge il nodo canonico `_identifier_value()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 528 che coinvolge il nodo canonico `_identifier_value()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 528 che coinvolge il nodo canonico `_identifier_value()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 528 che coinvolge il nodo canonico `_identifier_value()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 528 che coinvolge il nodo canonico `_identifier_value()` è `L134`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 528 che coinvolge il nodo canonico `_identifier_value()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 528 che coinvolge il nodo canonico `_identifier_value()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 528 che coinvolge il nodo canonico `_identifier_value()` è `1.0`. Nel grafo unificato, l'arco 528 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_identifier_value` per la relazione che coinvolge il nodo canonico `_identifier_value()`. Nel grafo unificato, l'arco 528 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_identifier_value()`.
<!-- GRAPH_EDGE graph=unificata index=529 role=outgoing -->
Nel grafo unificato, il nodo canonico `_identifier_value()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_identifier_value` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_identifier_value()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 529 che coinvolge il nodo canonico `_identifier_value()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 529 che coinvolge il nodo canonico `_identifier_value()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 529 che coinvolge il nodo canonico `_identifier_value()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 529 che coinvolge il nodo canonico `_identifier_value()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 529 che coinvolge il nodo canonico `_identifier_value()` è `L235`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 529 che coinvolge il nodo canonico `_identifier_value()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 529 che coinvolge il nodo canonico `_identifier_value()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 529 che coinvolge il nodo canonico `_identifier_value()` è `1.0`. Nel grafo unificato, l'arco 529 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_identifier_value` per la relazione che coinvolge il nodo canonico `_identifier_value()`. Nel grafo unificato, l'arco 529 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_identifier_value()`.
<!-- GRAPH_EDGE graph=unificata index=506 role=incoming -->
Nel grafo unificato, il nodo canonico `_identifier_value()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_identifier_value`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_identifier_value()`. Nel grafo unificato, il tipo dichiarato dell'arco 506 che coinvolge il nodo canonico `_identifier_value()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 506 che coinvolge il nodo canonico `_identifier_value()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 506 che coinvolge il nodo canonico `_identifier_value()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 506 che coinvolge il nodo canonico `_identifier_value()` è `L73`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 506 che coinvolge il nodo canonico `_identifier_value()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 506 che coinvolge il nodo canonico `_identifier_value()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 506 che coinvolge il nodo canonico `_identifier_value()` è `1.0`. Nel grafo unificato, l'arco 506 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_identifier_value()`. Nel grafo unificato, l'arco 506 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_identifier_value` per la relazione che coinvolge il nodo canonico `_identifier_value()`.

### _marital_status_code()

<!-- GRAPH_NODE graph=codice index=250 id=rve_transactions_rve_gateway_marital_status_code -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_marital_status_code()` è `_marital_status_code()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_marital_status_code()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_marital_status_code()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_marital_status_code()` è `L92`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_marital_status_code()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_marital_status_code()` possiede l'identificatore esatto `rve_transactions_rve_gateway_marital_status_code`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_marital_status_code()` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_marital_status_code()` è `_marital_status_code()`.
Nel grafo del codice RDG, il nodo canonico `_marital_status_code()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=507 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_marital_status_code()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_marital_status_code`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_marital_status_code()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 507 che coinvolge il nodo canonico `_marital_status_code()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 507 che coinvolge il nodo canonico `_marital_status_code()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 507 che coinvolge il nodo canonico `_marital_status_code()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 507 che coinvolge il nodo canonico `_marital_status_code()` è `L92`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 507 che coinvolge il nodo canonico `_marital_status_code()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 507 che coinvolge il nodo canonico `_marital_status_code()` è `ast`. Nel grafo del codice RDG, l'arco 507 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_marital_status_code()`. Nel grafo del codice RDG, l'arco 507 registra come `target` l'identificatore `rve_transactions_rve_gateway_marital_status_code` per la relazione che coinvolge il nodo canonico `_marital_status_code()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 507 che coinvolge il nodo canonico `_marital_status_code()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=532 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_marital_status_code()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_marital_status_code`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_marital_status_code()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 532 che coinvolge il nodo canonico `_marital_status_code()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 532 che coinvolge il nodo canonico `_marital_status_code()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 532 che coinvolge il nodo canonico `_marital_status_code()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 532 che coinvolge il nodo canonico `_marital_status_code()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 532 che coinvolge il nodo canonico `_marital_status_code()` è `L173`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 532 che coinvolge il nodo canonico `_marital_status_code()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 532 che coinvolge il nodo canonico `_marital_status_code()` è `ast`. Nel grafo del codice RDG, l'arco 532 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_marital_status_code()`. Nel grafo del codice RDG, l'arco 532 registra come `target` l'identificatore `rve_transactions_rve_gateway_marital_status_code` per la relazione che coinvolge il nodo canonico `_marital_status_code()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 532 che coinvolge il nodo canonico `_marital_status_code()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=533 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_marital_status_code()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_marital_status_code`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_marital_status_code()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 533 che coinvolge il nodo canonico `_marital_status_code()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 533 che coinvolge il nodo canonico `_marital_status_code()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 533 che coinvolge il nodo canonico `_marital_status_code()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 533 che coinvolge il nodo canonico `_marital_status_code()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 533 che coinvolge il nodo canonico `_marital_status_code()` è `L279`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 533 che coinvolge il nodo canonico `_marital_status_code()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 533 che coinvolge il nodo canonico `_marital_status_code()` è `ast`. Nel grafo del codice RDG, l'arco 533 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_marital_status_code()`. Nel grafo del codice RDG, l'arco 533 registra come `target` l'identificatore `rve_transactions_rve_gateway_marital_status_code` per la relazione che coinvolge il nodo canonico `_marital_status_code()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 533 che coinvolge il nodo canonico `_marital_status_code()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=250 id=Request-Dataset-Generator::rve_transactions_rve_gateway_marital_status_code -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_marital_status_code()` è `_marital_status_code()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_marital_status_code()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_marital_status_code()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_marital_status_code()` è `L92`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_marital_status_code()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_marital_status_code()` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_marital_status_code()` è `_marital_status_code()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_marital_status_code()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_marital_status_code()` è `rve_transactions_rve_gateway_marital_status_code`. Nel grafo unificato, il nodo canonico `_marital_status_code()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_marital_status_code`.
<!-- GRAPH_EDGE graph=unificata index=532 role=outgoing -->
Nel grafo unificato, il nodo canonico `_marital_status_code()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_marital_status_code` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_marital_status_code()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 532 che coinvolge il nodo canonico `_marital_status_code()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 532 che coinvolge il nodo canonico `_marital_status_code()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 532 che coinvolge il nodo canonico `_marital_status_code()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 532 che coinvolge il nodo canonico `_marital_status_code()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 532 che coinvolge il nodo canonico `_marital_status_code()` è `L173`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 532 che coinvolge il nodo canonico `_marital_status_code()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 532 che coinvolge il nodo canonico `_marital_status_code()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 532 che coinvolge il nodo canonico `_marital_status_code()` è `1.0`. Nel grafo unificato, l'arco 532 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_marital_status_code` per la relazione che coinvolge il nodo canonico `_marital_status_code()`. Nel grafo unificato, l'arco 532 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_marital_status_code()`.
<!-- GRAPH_EDGE graph=unificata index=533 role=outgoing -->
Nel grafo unificato, il nodo canonico `_marital_status_code()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_marital_status_code` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_marital_status_code()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 533 che coinvolge il nodo canonico `_marital_status_code()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 533 che coinvolge il nodo canonico `_marital_status_code()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 533 che coinvolge il nodo canonico `_marital_status_code()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 533 che coinvolge il nodo canonico `_marital_status_code()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 533 che coinvolge il nodo canonico `_marital_status_code()` è `L279`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 533 che coinvolge il nodo canonico `_marital_status_code()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 533 che coinvolge il nodo canonico `_marital_status_code()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 533 che coinvolge il nodo canonico `_marital_status_code()` è `1.0`. Nel grafo unificato, l'arco 533 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_marital_status_code` per la relazione che coinvolge il nodo canonico `_marital_status_code()`. Nel grafo unificato, l'arco 533 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_marital_status_code()`.
<!-- GRAPH_EDGE graph=unificata index=507 role=incoming -->
Nel grafo unificato, il nodo canonico `_marital_status_code()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_marital_status_code`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_marital_status_code()`. Nel grafo unificato, il tipo dichiarato dell'arco 507 che coinvolge il nodo canonico `_marital_status_code()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 507 che coinvolge il nodo canonico `_marital_status_code()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 507 che coinvolge il nodo canonico `_marital_status_code()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 507 che coinvolge il nodo canonico `_marital_status_code()` è `L92`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 507 che coinvolge il nodo canonico `_marital_status_code()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 507 che coinvolge il nodo canonico `_marital_status_code()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 507 che coinvolge il nodo canonico `_marital_status_code()` è `1.0`. Nel grafo unificato, l'arco 507 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_marital_status_code()`. Nel grafo unificato, l'arco 507 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_marital_status_code` per la relazione che coinvolge il nodo canonico `_marital_status_code()`.

### _mock_patient()

<!-- GRAPH_NODE graph=codice index=262 id=rve_transactions_rve_gateway_mock_patient -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_mock_patient()` è `_mock_patient()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_mock_patient()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_mock_patient()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_mock_patient()` è `L411`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_mock_patient()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_mock_patient()` possiede l'identificatore esatto `rve_transactions_rve_gateway_mock_patient`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_mock_patient()` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_mock_patient()` è `_mock_patient()`.
Nel grafo del codice RDG, il nodo canonico `_mock_patient()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=508 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_mock_patient()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_mock_patient`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_mock_patient()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 508 che coinvolge il nodo canonico `_mock_patient()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 508 che coinvolge il nodo canonico `_mock_patient()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 508 che coinvolge il nodo canonico `_mock_patient()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 508 che coinvolge il nodo canonico `_mock_patient()` è `L411`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 508 che coinvolge il nodo canonico `_mock_patient()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 508 che coinvolge il nodo canonico `_mock_patient()` è `ast`. Nel grafo del codice RDG, l'arco 508 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_mock_patient()`. Nel grafo del codice RDG, l'arco 508 registra come `target` l'identificatore `rve_transactions_rve_gateway_mock_patient` per la relazione che coinvolge il nodo canonico `_mock_patient()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 508 che coinvolge il nodo canonico `_mock_patient()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=547 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_mock_patient()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_build_mock_bundle()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_build_mock_bundle` all'ID `rve_transactions_rve_gateway_mock_patient`. La relazione tecnica esplicita è la seguente: `_build_mock_bundle()` chiama `_mock_patient()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 547 che coinvolge il nodo canonico `_mock_patient()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 547 che coinvolge il nodo canonico `_mock_patient()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 547 che coinvolge il nodo canonico `_mock_patient()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 547 che coinvolge il nodo canonico `_mock_patient()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 547 che coinvolge il nodo canonico `_mock_patient()` è `L395`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 547 che coinvolge il nodo canonico `_mock_patient()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 547 che coinvolge il nodo canonico `_mock_patient()` è `ast`. Nel grafo del codice RDG, l'arco 547 registra come `source` l'identificatore `rve_transactions_rve_gateway_build_mock_bundle` per la relazione che coinvolge il nodo canonico `_mock_patient()`. Nel grafo del codice RDG, l'arco 547 registra come `target` l'identificatore `rve_transactions_rve_gateway_mock_patient` per la relazione che coinvolge il nodo canonico `_mock_patient()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 547 che coinvolge il nodo canonico `_mock_patient()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=262 id=Request-Dataset-Generator::rve_transactions_rve_gateway_mock_patient -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_mock_patient()` è `_mock_patient()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_mock_patient()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_mock_patient()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_mock_patient()` è `L411`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_mock_patient()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_mock_patient()` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_mock_patient()` è `_mock_patient()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_mock_patient()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_mock_patient()` è `rve_transactions_rve_gateway_mock_patient`. Nel grafo unificato, il nodo canonico `_mock_patient()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_mock_patient`.
Nel grafo unificato, il nodo canonico `_mock_patient()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=508 role=incoming -->
Nel grafo unificato, il nodo canonico `_mock_patient()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_mock_patient`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_mock_patient()`. Nel grafo unificato, il tipo dichiarato dell'arco 508 che coinvolge il nodo canonico `_mock_patient()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 508 che coinvolge il nodo canonico `_mock_patient()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 508 che coinvolge il nodo canonico `_mock_patient()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 508 che coinvolge il nodo canonico `_mock_patient()` è `L411`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 508 che coinvolge il nodo canonico `_mock_patient()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 508 che coinvolge il nodo canonico `_mock_patient()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 508 che coinvolge il nodo canonico `_mock_patient()` è `1.0`. Nel grafo unificato, l'arco 508 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_mock_patient()`. Nel grafo unificato, l'arco 508 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_mock_patient` per la relazione che coinvolge il nodo canonico `_mock_patient()`.
<!-- GRAPH_EDGE graph=unificata index=547 role=incoming -->
Nel grafo unificato, il nodo canonico `_mock_patient()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_build_mock_bundle()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_build_mock_bundle` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_mock_patient`. La relazione tecnica esplicita è la seguente: `_build_mock_bundle()` chiama `_mock_patient()`. Nel grafo unificato, il tipo dichiarato dell'arco 547 che coinvolge il nodo canonico `_mock_patient()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 547 che coinvolge il nodo canonico `_mock_patient()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 547 che coinvolge il nodo canonico `_mock_patient()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 547 che coinvolge il nodo canonico `_mock_patient()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 547 che coinvolge il nodo canonico `_mock_patient()` è `L395`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 547 che coinvolge il nodo canonico `_mock_patient()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 547 che coinvolge il nodo canonico `_mock_patient()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 547 che coinvolge il nodo canonico `_mock_patient()` è `1.0`. Nel grafo unificato, l'arco 547 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_build_mock_bundle` per la relazione che coinvolge il nodo canonico `_mock_patient()`. Nel grafo unificato, l'arco 547 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_mock_patient` per la relazione che coinvolge il nodo canonico `_mock_patient()`.

### _payload_rve100()

<!-- GRAPH_NODE graph=codice index=256 id=rve_transactions_rve_gateway_payload_rve100 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_payload_rve100()` è `_payload_rve100()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_payload_rve100()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_payload_rve100()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_payload_rve100()` è `L123`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_payload_rve100()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_payload_rve100()` possiede l'identificatore esatto `rve_transactions_rve_gateway_payload_rve100`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_payload_rve100()` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_payload_rve100()` è `_payload_rve100()`.
Nel grafo del codice RDG, il nodo canonico `_payload_rve100()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=509 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve100()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_payload_rve100`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` effettua una chiamata indiretta verso `_payload_rve100()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 509 che coinvolge il nodo canonico `_payload_rve100()` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 509 che coinvolge il nodo canonico `_payload_rve100()` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 509 che coinvolge il nodo canonico `_payload_rve100()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 509 che coinvolge il nodo canonico `_payload_rve100()` è `L293`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 509 che coinvolge il nodo canonico `_payload_rve100()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 509 che coinvolge il nodo canonico `_payload_rve100()` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 509 che coinvolge il nodo canonico `_payload_rve100()` è `collection`. Nel grafo del codice RDG, l'arco 509 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_payload_rve100()`. Nel grafo del codice RDG, l'arco 509 registra come `target` l'identificatore `rve_transactions_rve_gateway_payload_rve100` per la relazione che coinvolge il nodo canonico `_payload_rve100()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 509 che coinvolge il nodo canonico `_payload_rve100()` è `0.5`.

<!-- GRAPH_NODE graph=unificata index=256 id=Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve100 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_payload_rve100()` è `_payload_rve100()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_payload_rve100()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_payload_rve100()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_payload_rve100()` è `L123`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_payload_rve100()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_payload_rve100()` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_payload_rve100()` è `_payload_rve100()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_payload_rve100()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_payload_rve100()` è `rve_transactions_rve_gateway_payload_rve100`. Nel grafo unificato, il nodo canonico `_payload_rve100()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve100`.
Nel grafo unificato, il nodo canonico `_payload_rve100()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=509 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve100()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve100`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` effettua una chiamata indiretta verso `_payload_rve100()`. Nel grafo unificato, il tipo dichiarato dell'arco 509 che coinvolge il nodo canonico `_payload_rve100()` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 509 che coinvolge il nodo canonico `_payload_rve100()` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 509 che coinvolge il nodo canonico `_payload_rve100()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 509 che coinvolge il nodo canonico `_payload_rve100()` è `L293`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 509 che coinvolge il nodo canonico `_payload_rve100()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 509 che coinvolge il nodo canonico `_payload_rve100()` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 509 che coinvolge il nodo canonico `_payload_rve100()` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 509 che coinvolge il nodo canonico `_payload_rve100()` è `0.5`. Nel grafo unificato, l'arco 509 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_payload_rve100()`. Nel grafo unificato, l'arco 509 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve100` per la relazione che coinvolge il nodo canonico `_payload_rve100()`.

### _payload_rve54()

<!-- GRAPH_NODE graph=codice index=255 id=rve_transactions_rve_gateway_payload_rve54 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_payload_rve54()` è `_payload_rve54()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_payload_rve54()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_payload_rve54()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_payload_rve54()` è `L119`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_payload_rve54()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_payload_rve54()` possiede l'identificatore esatto `rve_transactions_rve_gateway_payload_rve54`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_payload_rve54()` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_payload_rve54()` è `_payload_rve54()`.
Nel grafo del codice RDG, il nodo canonico `_payload_rve54()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=510 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve54()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_payload_rve54`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` effettua una chiamata indiretta verso `_payload_rve54()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 510 che coinvolge il nodo canonico `_payload_rve54()` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 510 che coinvolge il nodo canonico `_payload_rve54()` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 510 che coinvolge il nodo canonico `_payload_rve54()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 510 che coinvolge il nodo canonico `_payload_rve54()` è `L290`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 510 che coinvolge il nodo canonico `_payload_rve54()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 510 che coinvolge il nodo canonico `_payload_rve54()` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 510 che coinvolge il nodo canonico `_payload_rve54()` è `collection`. Nel grafo del codice RDG, l'arco 510 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_payload_rve54()`. Nel grafo del codice RDG, l'arco 510 registra come `target` l'identificatore `rve_transactions_rve_gateway_payload_rve54` per la relazione che coinvolge il nodo canonico `_payload_rve54()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 510 che coinvolge il nodo canonico `_payload_rve54()` è `0.5`.

<!-- GRAPH_NODE graph=unificata index=255 id=Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve54 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_payload_rve54()` è `_payload_rve54()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_payload_rve54()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_payload_rve54()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_payload_rve54()` è `L119`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_payload_rve54()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_payload_rve54()` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_payload_rve54()` è `_payload_rve54()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_payload_rve54()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_payload_rve54()` è `rve_transactions_rve_gateway_payload_rve54`. Nel grafo unificato, il nodo canonico `_payload_rve54()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve54`.
Nel grafo unificato, il nodo canonico `_payload_rve54()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=510 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve54()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve54`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` effettua una chiamata indiretta verso `_payload_rve54()`. Nel grafo unificato, il tipo dichiarato dell'arco 510 che coinvolge il nodo canonico `_payload_rve54()` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 510 che coinvolge il nodo canonico `_payload_rve54()` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 510 che coinvolge il nodo canonico `_payload_rve54()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 510 che coinvolge il nodo canonico `_payload_rve54()` è `L290`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 510 che coinvolge il nodo canonico `_payload_rve54()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 510 che coinvolge il nodo canonico `_payload_rve54()` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 510 che coinvolge il nodo canonico `_payload_rve54()` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 510 che coinvolge il nodo canonico `_payload_rve54()` è `0.5`. Nel grafo unificato, l'arco 510 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_payload_rve54()`. Nel grafo unificato, l'arco 510 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve54` per la relazione che coinvolge il nodo canonico `_payload_rve54()`.

### _payload_rve55()

<!-- GRAPH_NODE graph=codice index=257 id=rve_transactions_rve_gateway_payload_rve55 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_payload_rve55()` è `_payload_rve55()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_payload_rve55()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_payload_rve55()` è `L127`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_payload_rve55()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_payload_rve55()` possiede l'identificatore esatto `rve_transactions_rve_gateway_payload_rve55`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_payload_rve55()` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_payload_rve55()` è `_payload_rve55()`.
<!-- GRAPH_EDGE graph=codice index=516 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve55()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_first_value()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_first_value`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_first_value()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 516 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 516 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 516 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 516 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 516 che coinvolge il nodo canonico `_payload_rve55()` è `L130`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 516 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 516 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo del codice RDG, l'arco 516 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, l'arco 516 registra come `target` l'identificatore `rve_transactions_rve_gateway_first_value` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 516 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=518 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve55()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_first_string()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_first_string`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_first_string()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 518 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 518 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 518 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 518 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 518 che coinvolge il nodo canonico `_payload_rve55()` è `L132`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 518 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 518 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo del codice RDG, l'arco 518 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, l'arco 518 registra come `target` l'identificatore `rve_transactions_rve_gateway_first_string` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 518 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=520 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve55()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_given_name()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_given_name`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_given_name()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 520 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 520 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 520 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 520 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 520 che coinvolge il nodo canonico `_payload_rve55()` è `L132`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 520 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 520 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo del codice RDG, l'arco 520 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, l'arco 520 registra come `target` l'identificatore `rve_transactions_rve_gateway_given_name` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 520 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=522 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve55()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_family_name()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_family_name`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_family_name()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 522 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 522 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 522 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 522 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 522 che coinvolge il nodo canonico `_payload_rve55()` è `L133`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 522 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 522 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo del codice RDG, l'arco 522 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, l'arco 522 registra come `target` l'identificatore `rve_transactions_rve_gateway_family_name` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 522 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=524 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve55()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_sex_code()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_sex_code`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_sex_code()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 524 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 524 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 524 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 524 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 524 che coinvolge il nodo canonico `_payload_rve55()` è `L140`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 524 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 524 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo del codice RDG, l'arco 524 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, l'arco 524 registra come `target` l'identificatore `rve_transactions_rve_gateway_sex_code` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 524 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=526 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve55()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_birth_date_millis()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_birth_date_millis`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_birth_date_millis()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 526 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 526 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 526 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 526 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 526 che coinvolge il nodo canonico `_payload_rve55()` è `L144`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 526 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 526 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo del codice RDG, l'arco 526 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, l'arco 526 registra come `target` l'identificatore `rve_transactions_rve_gateway_birth_date_millis` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 526 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=528 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve55()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_identifier_value()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_identifier_value`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_identifier_value()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 528 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 528 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 528 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 528 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 528 che coinvolge il nodo canonico `_payload_rve55()` è `L134`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 528 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 528 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo del codice RDG, l'arco 528 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, l'arco 528 registra come `target` l'identificatore `rve_transactions_rve_gateway_identifier_value` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 528 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=530 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve55()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_telecom_value()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_telecom_value`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_telecom_value()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 530 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 530 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 530 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 530 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 530 che coinvolge il nodo canonico `_payload_rve55()` è `L162`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 530 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 530 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo del codice RDG, l'arco 530 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, l'arco 530 registra come `target` l'identificatore `rve_transactions_rve_gateway_telecom_value` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 530 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=532 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve55()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_marital_status_code()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_marital_status_code`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_marital_status_code()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 532 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 532 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 532 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 532 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 532 che coinvolge il nodo canonico `_payload_rve55()` è `L173`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 532 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 532 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo del codice RDG, l'arco 532 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, l'arco 532 registra come `target` l'identificatore `rve_transactions_rve_gateway_marital_status_code` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 532 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=537 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve55()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_address_text()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_address_text`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_address_text()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 537 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 537 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 537 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 537 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 537 che coinvolge il nodo canonico `_payload_rve55()` è `L198`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 537 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 537 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo del codice RDG, l'arco 537 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, l'arco 537 registra come `target` l'identificatore `rve_transactions_rve_gateway_address_text` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 537 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=539 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve55()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_address_locality()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_address_locality`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_address_locality()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 539 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 539 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 539 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 539 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 539 che coinvolge il nodo canonico `_payload_rve55()` è `L199`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 539 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 539 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo del codice RDG, l'arco 539 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, l'arco 539 registra come `target` l'identificatore `rve_transactions_rve_gateway_address_locality` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 539 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=541 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve55()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_address_postal_code()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_address_postal_code`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_address_postal_code()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 541 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 541 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 541 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 541 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 541 che coinvolge il nodo canonico `_payload_rve55()` è `L206`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 541 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 541 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo del codice RDG, l'arco 541 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, l'arco 541 registra come `target` l'identificatore `rve_transactions_rve_gateway_address_postal_code` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 541 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=511 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve55()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` effettua una chiamata indiretta verso `_payload_rve55()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 511 che coinvolge il nodo canonico `_payload_rve55()` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 511 che coinvolge il nodo canonico `_payload_rve55()` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 511 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 511 che coinvolge il nodo canonico `_payload_rve55()` è `L291`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 511 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 511 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 511 che coinvolge il nodo canonico `_payload_rve55()` è `collection`. Nel grafo del codice RDG, l'arco 511 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, l'arco 511 registra come `target` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 511 che coinvolge il nodo canonico `_payload_rve55()` è `0.5`.

<!-- GRAPH_NODE graph=unificata index=257 id=Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_payload_rve55()` è `_payload_rve55()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_payload_rve55()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_payload_rve55()` è `L127`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_payload_rve55()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_payload_rve55()` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_payload_rve55()` è `_payload_rve55()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_payload_rve55()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_payload_rve55()` è `rve_transactions_rve_gateway_payload_rve55`. Nel grafo unificato, il nodo canonico `_payload_rve55()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`.
Nel grafo unificato, il nodo canonico `_payload_rve55()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=511 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve55()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` effettua una chiamata indiretta verso `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 511 che coinvolge il nodo canonico `_payload_rve55()` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 511 che coinvolge il nodo canonico `_payload_rve55()` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 511 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 511 che coinvolge il nodo canonico `_payload_rve55()` è `L291`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 511 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 511 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 511 che coinvolge il nodo canonico `_payload_rve55()` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 511 che coinvolge il nodo canonico `_payload_rve55()` è `0.5`. Nel grafo unificato, l'arco 511 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo unificato, l'arco 511 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`.
<!-- GRAPH_EDGE graph=unificata index=516 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve55()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_first_value()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_first_value` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_first_value()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 516 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 516 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 516 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 516 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 516 che coinvolge il nodo canonico `_payload_rve55()` è `L130`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 516 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 516 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 516 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, l'arco 516 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_first_value` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo unificato, l'arco 516 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`.
<!-- GRAPH_EDGE graph=unificata index=518 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve55()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_first_string()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_first_string` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_first_string()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 518 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 518 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 518 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 518 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 518 che coinvolge il nodo canonico `_payload_rve55()` è `L132`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 518 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 518 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 518 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, l'arco 518 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_first_string` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo unificato, l'arco 518 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`.
<!-- GRAPH_EDGE graph=unificata index=520 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve55()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_given_name()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_given_name` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_given_name()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 520 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 520 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 520 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 520 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 520 che coinvolge il nodo canonico `_payload_rve55()` è `L132`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 520 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 520 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 520 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, l'arco 520 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_given_name` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo unificato, l'arco 520 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`.
<!-- GRAPH_EDGE graph=unificata index=522 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve55()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_family_name()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_family_name` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_family_name()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 522 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 522 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 522 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 522 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 522 che coinvolge il nodo canonico `_payload_rve55()` è `L133`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 522 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 522 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 522 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, l'arco 522 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_family_name` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo unificato, l'arco 522 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`.
<!-- GRAPH_EDGE graph=unificata index=524 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve55()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_sex_code()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_sex_code` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_sex_code()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 524 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 524 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 524 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 524 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 524 che coinvolge il nodo canonico `_payload_rve55()` è `L140`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 524 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 524 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 524 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, l'arco 524 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_sex_code` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo unificato, l'arco 524 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`.
<!-- GRAPH_EDGE graph=unificata index=526 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve55()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_birth_date_millis()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_birth_date_millis` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_birth_date_millis()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 526 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 526 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 526 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 526 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 526 che coinvolge il nodo canonico `_payload_rve55()` è `L144`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 526 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 526 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 526 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, l'arco 526 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_birth_date_millis` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo unificato, l'arco 526 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`.
<!-- GRAPH_EDGE graph=unificata index=528 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve55()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_identifier_value()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_identifier_value` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_identifier_value()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 528 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 528 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 528 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 528 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 528 che coinvolge il nodo canonico `_payload_rve55()` è `L134`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 528 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 528 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 528 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, l'arco 528 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_identifier_value` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo unificato, l'arco 528 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`.
<!-- GRAPH_EDGE graph=unificata index=530 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve55()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_telecom_value()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_telecom_value` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_telecom_value()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 530 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 530 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 530 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 530 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 530 che coinvolge il nodo canonico `_payload_rve55()` è `L162`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 530 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 530 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 530 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, l'arco 530 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_telecom_value` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo unificato, l'arco 530 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`.
<!-- GRAPH_EDGE graph=unificata index=532 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve55()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_marital_status_code()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_marital_status_code` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_marital_status_code()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 532 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 532 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 532 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 532 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 532 che coinvolge il nodo canonico `_payload_rve55()` è `L173`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 532 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 532 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 532 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, l'arco 532 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_marital_status_code` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo unificato, l'arco 532 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`.
<!-- GRAPH_EDGE graph=unificata index=537 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve55()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_address_text()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_text` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_address_text()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 537 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 537 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 537 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 537 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 537 che coinvolge il nodo canonico `_payload_rve55()` è `L198`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 537 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 537 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 537 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, l'arco 537 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_text` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo unificato, l'arco 537 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`.
<!-- GRAPH_EDGE graph=unificata index=539 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve55()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_address_locality()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_locality` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_address_locality()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 539 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 539 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 539 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 539 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 539 che coinvolge il nodo canonico `_payload_rve55()` è `L199`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 539 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 539 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 539 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, l'arco 539 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_locality` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo unificato, l'arco 539 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`.
<!-- GRAPH_EDGE graph=unificata index=541 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve55()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_address_postal_code()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_postal_code` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_address_postal_code()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 541 che coinvolge il nodo canonico `_payload_rve55()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 541 che coinvolge il nodo canonico `_payload_rve55()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 541 che coinvolge il nodo canonico `_payload_rve55()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 541 che coinvolge il nodo canonico `_payload_rve55()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 541 che coinvolge il nodo canonico `_payload_rve55()` è `L206`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 541 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 541 che coinvolge il nodo canonico `_payload_rve55()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 541 che coinvolge il nodo canonico `_payload_rve55()` è `1.0`. Nel grafo unificato, l'arco 541 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_postal_code` per la relazione che coinvolge il nodo canonico `_payload_rve55()`. Nel grafo unificato, l'arco 541 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_payload_rve55()`.

### _payload_rve57()

<!-- GRAPH_NODE graph=codice index=258 id=rve_transactions_rve_gateway_payload_rve57 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_payload_rve57()` è `_payload_rve57()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_payload_rve57()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_payload_rve57()` è `L222`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_payload_rve57()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_payload_rve57()` possiede l'identificatore esatto `rve_transactions_rve_gateway_payload_rve57`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_payload_rve57()` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_payload_rve57()` è `_payload_rve57()`.
<!-- GRAPH_EDGE graph=codice index=517 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve57()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_first_value()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_first_value`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_first_value()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 517 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 517 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 517 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 517 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 517 che coinvolge il nodo canonico `_payload_rve57()` è `L225`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 517 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 517 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo del codice RDG, l'arco 517 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, l'arco 517 registra come `target` l'identificatore `rve_transactions_rve_gateway_first_value` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 517 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=519 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve57()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_first_string()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_first_string`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_first_string()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 519 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 519 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 519 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 519 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 519 che coinvolge il nodo canonico `_payload_rve57()` è `L241`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 519 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 519 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo del codice RDG, l'arco 519 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, l'arco 519 registra come `target` l'identificatore `rve_transactions_rve_gateway_first_string` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 519 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=521 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve57()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_given_name()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_given_name`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_given_name()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 521 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 521 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 521 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 521 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 521 che coinvolge il nodo canonico `_payload_rve57()` è `L241`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 521 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 521 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo del codice RDG, l'arco 521 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, l'arco 521 registra come `target` l'identificatore `rve_transactions_rve_gateway_given_name` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 521 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=523 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve57()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_family_name()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_family_name`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_family_name()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 523 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 523 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 523 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 523 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 523 che coinvolge il nodo canonico `_payload_rve57()` è `L242`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 523 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 523 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo del codice RDG, l'arco 523 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, l'arco 523 registra come `target` l'identificatore `rve_transactions_rve_gateway_family_name` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 523 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=525 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve57()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_sex_code()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_sex_code`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_sex_code()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 525 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 525 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 525 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 525 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 525 che coinvolge il nodo canonico `_payload_rve57()` è `L243`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 525 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 525 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo del codice RDG, l'arco 525 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, l'arco 525 registra come `target` l'identificatore `rve_transactions_rve_gateway_sex_code` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 525 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=527 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve57()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_birth_date_millis()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_birth_date_millis`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_birth_date_millis()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 527 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 527 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 527 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 527 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 527 che coinvolge il nodo canonico `_payload_rve57()` è `L244`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 527 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 527 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo del codice RDG, l'arco 527 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, l'arco 527 registra come `target` l'identificatore `rve_transactions_rve_gateway_birth_date_millis` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 527 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=529 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve57()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_identifier_value()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_identifier_value`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_identifier_value()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 529 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 529 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 529 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 529 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 529 che coinvolge il nodo canonico `_payload_rve57()` è `L235`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 529 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 529 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo del codice RDG, l'arco 529 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, l'arco 529 registra come `target` l'identificatore `rve_transactions_rve_gateway_identifier_value` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 529 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=531 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve57()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_telecom_value()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_telecom_value`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_telecom_value()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 531 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 531 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 531 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 531 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 531 che coinvolge il nodo canonico `_payload_rve57()` è `L266`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 531 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 531 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo del codice RDG, l'arco 531 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, l'arco 531 registra come `target` l'identificatore `rve_transactions_rve_gateway_telecom_value` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 531 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=533 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve57()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_marital_status_code()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_marital_status_code`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_marital_status_code()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 533 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 533 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 533 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 533 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 533 che coinvolge il nodo canonico `_payload_rve57()` è `L279`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 533 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 533 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo del codice RDG, l'arco 533 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, l'arco 533 registra come `target` l'identificatore `rve_transactions_rve_gateway_marital_status_code` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 533 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=538 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve57()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_address_text()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_address_text`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_address_text()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 538 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 538 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 538 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 538 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 538 che coinvolge il nodo canonico `_payload_rve57()` è `L252`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 538 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 538 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo del codice RDG, l'arco 538 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, l'arco 538 registra come `target` l'identificatore `rve_transactions_rve_gateway_address_text` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 538 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=540 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve57()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_address_locality()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_address_locality`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_address_locality()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 540 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 540 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 540 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 540 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 540 che coinvolge il nodo canonico `_payload_rve57()` è `L253`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 540 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 540 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo del codice RDG, l'arco 540 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, l'arco 540 registra come `target` l'identificatore `rve_transactions_rve_gateway_address_locality` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 540 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=542 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve57()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_address_postal_code()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_address_postal_code`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_address_postal_code()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 542 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 542 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 542 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 542 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 542 che coinvolge il nodo canonico `_payload_rve57()` è `L260`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 542 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 542 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo del codice RDG, l'arco 542 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, l'arco 542 registra come `target` l'identificatore `rve_transactions_rve_gateway_address_postal_code` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 542 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=512 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_payload_rve57()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` effettua una chiamata indiretta verso `_payload_rve57()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 512 che coinvolge il nodo canonico `_payload_rve57()` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 512 che coinvolge il nodo canonico `_payload_rve57()` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 512 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 512 che coinvolge il nodo canonico `_payload_rve57()` è `L292`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 512 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 512 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 512 che coinvolge il nodo canonico `_payload_rve57()` è `collection`. Nel grafo del codice RDG, l'arco 512 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, l'arco 512 registra come `target` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 512 che coinvolge il nodo canonico `_payload_rve57()` è `0.5`.

<!-- GRAPH_NODE graph=unificata index=258 id=Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_payload_rve57()` è `_payload_rve57()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_payload_rve57()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_payload_rve57()` è `L222`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_payload_rve57()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_payload_rve57()` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_payload_rve57()` è `_payload_rve57()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_payload_rve57()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_payload_rve57()` è `rve_transactions_rve_gateway_payload_rve57`. Nel grafo unificato, il nodo canonico `_payload_rve57()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`.
Nel grafo unificato, il nodo canonico `_payload_rve57()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=512 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve57()` è il destinatario `target` della relazione `indirect_call` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` effettua una chiamata indiretta verso `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 512 che coinvolge il nodo canonico `_payload_rve57()` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 512 che coinvolge il nodo canonico `_payload_rve57()` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 512 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 512 che coinvolge il nodo canonico `_payload_rve57()` è `L292`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 512 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 512 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 512 che coinvolge il nodo canonico `_payload_rve57()` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 512 che coinvolge il nodo canonico `_payload_rve57()` è `0.5`. Nel grafo unificato, l'arco 512 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo unificato, l'arco 512 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`.
<!-- GRAPH_EDGE graph=unificata index=517 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve57()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_first_value()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_first_value` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_first_value()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 517 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 517 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 517 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 517 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 517 che coinvolge il nodo canonico `_payload_rve57()` è `L225`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 517 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 517 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 517 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, l'arco 517 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_first_value` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo unificato, l'arco 517 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`.
<!-- GRAPH_EDGE graph=unificata index=519 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve57()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_first_string()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_first_string` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_first_string()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 519 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 519 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 519 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 519 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 519 che coinvolge il nodo canonico `_payload_rve57()` è `L241`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 519 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 519 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 519 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, l'arco 519 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_first_string` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo unificato, l'arco 519 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`.
<!-- GRAPH_EDGE graph=unificata index=521 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve57()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_given_name()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_given_name` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_given_name()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 521 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 521 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 521 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 521 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 521 che coinvolge il nodo canonico `_payload_rve57()` è `L241`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 521 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 521 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 521 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, l'arco 521 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_given_name` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo unificato, l'arco 521 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`.
<!-- GRAPH_EDGE graph=unificata index=523 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve57()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_family_name()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_family_name` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_family_name()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 523 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 523 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 523 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 523 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 523 che coinvolge il nodo canonico `_payload_rve57()` è `L242`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 523 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 523 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 523 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, l'arco 523 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_family_name` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo unificato, l'arco 523 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`.
<!-- GRAPH_EDGE graph=unificata index=525 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve57()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_sex_code()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_sex_code` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_sex_code()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 525 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 525 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 525 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 525 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 525 che coinvolge il nodo canonico `_payload_rve57()` è `L243`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 525 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 525 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 525 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, l'arco 525 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_sex_code` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo unificato, l'arco 525 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`.
<!-- GRAPH_EDGE graph=unificata index=527 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve57()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_birth_date_millis()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_birth_date_millis` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_birth_date_millis()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 527 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 527 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 527 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 527 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 527 che coinvolge il nodo canonico `_payload_rve57()` è `L244`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 527 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 527 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 527 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, l'arco 527 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_birth_date_millis` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo unificato, l'arco 527 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`.
<!-- GRAPH_EDGE graph=unificata index=529 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve57()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_identifier_value()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_identifier_value` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_identifier_value()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 529 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 529 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 529 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 529 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 529 che coinvolge il nodo canonico `_payload_rve57()` è `L235`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 529 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 529 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 529 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, l'arco 529 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_identifier_value` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo unificato, l'arco 529 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`.
<!-- GRAPH_EDGE graph=unificata index=531 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve57()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_telecom_value()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_telecom_value` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_telecom_value()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 531 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 531 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 531 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 531 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 531 che coinvolge il nodo canonico `_payload_rve57()` è `L266`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 531 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 531 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 531 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, l'arco 531 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_telecom_value` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo unificato, l'arco 531 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`.
<!-- GRAPH_EDGE graph=unificata index=533 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve57()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_marital_status_code()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_marital_status_code` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_marital_status_code()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 533 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 533 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 533 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 533 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 533 che coinvolge il nodo canonico `_payload_rve57()` è `L279`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 533 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 533 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 533 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, l'arco 533 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_marital_status_code` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo unificato, l'arco 533 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`.
<!-- GRAPH_EDGE graph=unificata index=538 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve57()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_address_text()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_text` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_address_text()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 538 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 538 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 538 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 538 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 538 che coinvolge il nodo canonico `_payload_rve57()` è `L252`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 538 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 538 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 538 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, l'arco 538 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_text` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo unificato, l'arco 538 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`.
<!-- GRAPH_EDGE graph=unificata index=540 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve57()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_address_locality()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_locality` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_address_locality()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 540 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 540 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 540 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 540 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 540 che coinvolge il nodo canonico `_payload_rve57()` è `L253`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 540 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 540 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 540 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, l'arco 540 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_locality` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo unificato, l'arco 540 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`.
<!-- GRAPH_EDGE graph=unificata index=542 role=incoming -->
Nel grafo unificato, il nodo canonico `_payload_rve57()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_address_postal_code()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_postal_code` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_address_postal_code()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 542 che coinvolge il nodo canonico `_payload_rve57()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 542 che coinvolge il nodo canonico `_payload_rve57()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 542 che coinvolge il nodo canonico `_payload_rve57()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 542 che coinvolge il nodo canonico `_payload_rve57()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 542 che coinvolge il nodo canonico `_payload_rve57()` è `L260`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 542 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 542 che coinvolge il nodo canonico `_payload_rve57()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 542 che coinvolge il nodo canonico `_payload_rve57()` è `1.0`. Nel grafo unificato, l'arco 542 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_postal_code` per la relazione che coinvolge il nodo canonico `_payload_rve57()`. Nel grafo unificato, l'arco 542 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_payload_rve57()`.

### _sex_code()

<!-- GRAPH_NODE graph=codice index=246 id=rve_transactions_rve_gateway_sex_code -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_sex_code()` è `_sex_code()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_sex_code()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_sex_code()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_sex_code()` è `L53`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_sex_code()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_sex_code()` possiede l'identificatore esatto `rve_transactions_rve_gateway_sex_code`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_sex_code()` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_sex_code()` è `_sex_code()`.
Nel grafo del codice RDG, il nodo canonico `_sex_code()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=513 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_sex_code()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_sex_code`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_sex_code()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 513 che coinvolge il nodo canonico `_sex_code()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 513 che coinvolge il nodo canonico `_sex_code()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 513 che coinvolge il nodo canonico `_sex_code()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 513 che coinvolge il nodo canonico `_sex_code()` è `L53`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 513 che coinvolge il nodo canonico `_sex_code()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 513 che coinvolge il nodo canonico `_sex_code()` è `ast`. Nel grafo del codice RDG, l'arco 513 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_sex_code()`. Nel grafo del codice RDG, l'arco 513 registra come `target` l'identificatore `rve_transactions_rve_gateway_sex_code` per la relazione che coinvolge il nodo canonico `_sex_code()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 513 che coinvolge il nodo canonico `_sex_code()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=524 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_sex_code()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_sex_code`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_sex_code()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 524 che coinvolge il nodo canonico `_sex_code()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 524 che coinvolge il nodo canonico `_sex_code()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 524 che coinvolge il nodo canonico `_sex_code()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 524 che coinvolge il nodo canonico `_sex_code()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 524 che coinvolge il nodo canonico `_sex_code()` è `L140`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 524 che coinvolge il nodo canonico `_sex_code()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 524 che coinvolge il nodo canonico `_sex_code()` è `ast`. Nel grafo del codice RDG, l'arco 524 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_sex_code()`. Nel grafo del codice RDG, l'arco 524 registra come `target` l'identificatore `rve_transactions_rve_gateway_sex_code` per la relazione che coinvolge il nodo canonico `_sex_code()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 524 che coinvolge il nodo canonico `_sex_code()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=525 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_sex_code()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_sex_code`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_sex_code()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 525 che coinvolge il nodo canonico `_sex_code()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 525 che coinvolge il nodo canonico `_sex_code()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 525 che coinvolge il nodo canonico `_sex_code()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 525 che coinvolge il nodo canonico `_sex_code()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 525 che coinvolge il nodo canonico `_sex_code()` è `L243`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 525 che coinvolge il nodo canonico `_sex_code()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 525 che coinvolge il nodo canonico `_sex_code()` è `ast`. Nel grafo del codice RDG, l'arco 525 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_sex_code()`. Nel grafo del codice RDG, l'arco 525 registra come `target` l'identificatore `rve_transactions_rve_gateway_sex_code` per la relazione che coinvolge il nodo canonico `_sex_code()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 525 che coinvolge il nodo canonico `_sex_code()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=246 id=Request-Dataset-Generator::rve_transactions_rve_gateway_sex_code -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_sex_code()` è `_sex_code()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_sex_code()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_sex_code()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_sex_code()` è `L53`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_sex_code()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_sex_code()` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_sex_code()` è `_sex_code()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_sex_code()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_sex_code()` è `rve_transactions_rve_gateway_sex_code`. Nel grafo unificato, il nodo canonico `_sex_code()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_sex_code`.
<!-- GRAPH_EDGE graph=unificata index=524 role=outgoing -->
Nel grafo unificato, il nodo canonico `_sex_code()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_sex_code` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_sex_code()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 524 che coinvolge il nodo canonico `_sex_code()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 524 che coinvolge il nodo canonico `_sex_code()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 524 che coinvolge il nodo canonico `_sex_code()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 524 che coinvolge il nodo canonico `_sex_code()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 524 che coinvolge il nodo canonico `_sex_code()` è `L140`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 524 che coinvolge il nodo canonico `_sex_code()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 524 che coinvolge il nodo canonico `_sex_code()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 524 che coinvolge il nodo canonico `_sex_code()` è `1.0`. Nel grafo unificato, l'arco 524 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_sex_code` per la relazione che coinvolge il nodo canonico `_sex_code()`. Nel grafo unificato, l'arco 524 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_sex_code()`.
<!-- GRAPH_EDGE graph=unificata index=525 role=outgoing -->
Nel grafo unificato, il nodo canonico `_sex_code()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_sex_code` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_sex_code()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 525 che coinvolge il nodo canonico `_sex_code()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 525 che coinvolge il nodo canonico `_sex_code()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 525 che coinvolge il nodo canonico `_sex_code()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 525 che coinvolge il nodo canonico `_sex_code()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 525 che coinvolge il nodo canonico `_sex_code()` è `L243`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 525 che coinvolge il nodo canonico `_sex_code()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 525 che coinvolge il nodo canonico `_sex_code()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 525 che coinvolge il nodo canonico `_sex_code()` è `1.0`. Nel grafo unificato, l'arco 525 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_sex_code` per la relazione che coinvolge il nodo canonico `_sex_code()`. Nel grafo unificato, l'arco 525 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_sex_code()`.
<!-- GRAPH_EDGE graph=unificata index=513 role=incoming -->
Nel grafo unificato, il nodo canonico `_sex_code()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_sex_code`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_sex_code()`. Nel grafo unificato, il tipo dichiarato dell'arco 513 che coinvolge il nodo canonico `_sex_code()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 513 che coinvolge il nodo canonico `_sex_code()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 513 che coinvolge il nodo canonico `_sex_code()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 513 che coinvolge il nodo canonico `_sex_code()` è `L53`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 513 che coinvolge il nodo canonico `_sex_code()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 513 che coinvolge il nodo canonico `_sex_code()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 513 che coinvolge il nodo canonico `_sex_code()` è `1.0`. Nel grafo unificato, l'arco 513 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_sex_code()`. Nel grafo unificato, l'arco 513 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_sex_code` per la relazione che coinvolge il nodo canonico `_sex_code()`.

### _telecom_value()

<!-- GRAPH_NODE graph=codice index=249 id=rve_transactions_rve_gateway_telecom_value -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_telecom_value()` è `_telecom_value()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_telecom_value()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_telecom_value()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_telecom_value()` è `L83`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_telecom_value()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_telecom_value()` possiede l'identificatore esatto `rve_transactions_rve_gateway_telecom_value`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_telecom_value()` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_telecom_value()` è `_telecom_value()`.
Nel grafo del codice RDG, il nodo canonico `_telecom_value()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=514 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_telecom_value()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_telecom_value`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_telecom_value()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 514 che coinvolge il nodo canonico `_telecom_value()` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 514 che coinvolge il nodo canonico `_telecom_value()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 514 che coinvolge il nodo canonico `_telecom_value()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 514 che coinvolge il nodo canonico `_telecom_value()` è `L83`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 514 che coinvolge il nodo canonico `_telecom_value()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 514 che coinvolge il nodo canonico `_telecom_value()` è `ast`. Nel grafo del codice RDG, l'arco 514 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_telecom_value()`. Nel grafo del codice RDG, l'arco 514 registra come `target` l'identificatore `rve_transactions_rve_gateway_telecom_value` per la relazione che coinvolge il nodo canonico `_telecom_value()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 514 che coinvolge il nodo canonico `_telecom_value()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=530 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_telecom_value()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve55` all'ID `rve_transactions_rve_gateway_telecom_value`. La relazione tecnica esplicita è la seguente: `_payload_rve55()` chiama `_telecom_value()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 530 che coinvolge il nodo canonico `_telecom_value()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 530 che coinvolge il nodo canonico `_telecom_value()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 530 che coinvolge il nodo canonico `_telecom_value()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 530 che coinvolge il nodo canonico `_telecom_value()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 530 che coinvolge il nodo canonico `_telecom_value()` è `L162`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 530 che coinvolge il nodo canonico `_telecom_value()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 530 che coinvolge il nodo canonico `_telecom_value()` è `ast`. Nel grafo del codice RDG, l'arco 530 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_telecom_value()`. Nel grafo del codice RDG, l'arco 530 registra come `target` l'identificatore `rve_transactions_rve_gateway_telecom_value` per la relazione che coinvolge il nodo canonico `_telecom_value()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 530 che coinvolge il nodo canonico `_telecom_value()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=531 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_telecom_value()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_payload_rve57` all'ID `rve_transactions_rve_gateway_telecom_value`. La relazione tecnica esplicita è la seguente: `_payload_rve57()` chiama `_telecom_value()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 531 che coinvolge il nodo canonico `_telecom_value()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 531 che coinvolge il nodo canonico `_telecom_value()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 531 che coinvolge il nodo canonico `_telecom_value()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 531 che coinvolge il nodo canonico `_telecom_value()` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 531 che coinvolge il nodo canonico `_telecom_value()` è `L266`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 531 che coinvolge il nodo canonico `_telecom_value()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 531 che coinvolge il nodo canonico `_telecom_value()` è `ast`. Nel grafo del codice RDG, l'arco 531 registra come `source` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_telecom_value()`. Nel grafo del codice RDG, l'arco 531 registra come `target` l'identificatore `rve_transactions_rve_gateway_telecom_value` per la relazione che coinvolge il nodo canonico `_telecom_value()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 531 che coinvolge il nodo canonico `_telecom_value()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=249 id=Request-Dataset-Generator::rve_transactions_rve_gateway_telecom_value -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_telecom_value()` è `_telecom_value()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_telecom_value()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_telecom_value()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_telecom_value()` è `L83`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_telecom_value()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_telecom_value()` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_telecom_value()` è `_telecom_value()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_telecom_value()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_telecom_value()` è `rve_transactions_rve_gateway_telecom_value`. Nel grafo unificato, il nodo canonico `_telecom_value()` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_telecom_value`.
<!-- GRAPH_EDGE graph=unificata index=530 role=outgoing -->
Nel grafo unificato, il nodo canonico `_telecom_value()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_telecom_value` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `_telecom_value()` chiama `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 530 che coinvolge il nodo canonico `_telecom_value()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 530 che coinvolge il nodo canonico `_telecom_value()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 530 che coinvolge il nodo canonico `_telecom_value()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 530 che coinvolge il nodo canonico `_telecom_value()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 530 che coinvolge il nodo canonico `_telecom_value()` è `L162`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 530 che coinvolge il nodo canonico `_telecom_value()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 530 che coinvolge il nodo canonico `_telecom_value()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 530 che coinvolge il nodo canonico `_telecom_value()` è `1.0`. Nel grafo unificato, l'arco 530 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_telecom_value` per la relazione che coinvolge il nodo canonico `_telecom_value()`. Nel grafo unificato, l'arco 530 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `_telecom_value()`.
<!-- GRAPH_EDGE graph=unificata index=531 role=outgoing -->
Nel grafo unificato, il nodo canonico `_telecom_value()` è la sorgente `source` della relazione `calls` verso il nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_telecom_value` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `_telecom_value()` chiama `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 531 che coinvolge il nodo canonico `_telecom_value()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 531 che coinvolge il nodo canonico `_telecom_value()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 531 che coinvolge il nodo canonico `_telecom_value()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 531 che coinvolge il nodo canonico `_telecom_value()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 531 che coinvolge il nodo canonico `_telecom_value()` è `L266`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 531 che coinvolge il nodo canonico `_telecom_value()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 531 che coinvolge il nodo canonico `_telecom_value()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 531 che coinvolge il nodo canonico `_telecom_value()` è `1.0`. Nel grafo unificato, l'arco 531 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_telecom_value` per la relazione che coinvolge il nodo canonico `_telecom_value()`. Nel grafo unificato, l'arco 531 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `_telecom_value()`.
<!-- GRAPH_EDGE graph=unificata index=514 role=incoming -->
Nel grafo unificato, il nodo canonico `_telecom_value()` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_telecom_value`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_telecom_value()`. Nel grafo unificato, il tipo dichiarato dell'arco 514 che coinvolge il nodo canonico `_telecom_value()` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 514 che coinvolge il nodo canonico `_telecom_value()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 514 che coinvolge il nodo canonico `_telecom_value()` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 514 che coinvolge il nodo canonico `_telecom_value()` è `L83`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 514 che coinvolge il nodo canonico `_telecom_value()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 514 che coinvolge il nodo canonico `_telecom_value()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 514 che coinvolge il nodo canonico `_telecom_value()` è `1.0`. Nel grafo unificato, l'arco 514 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `_telecom_value()`. Nel grafo unificato, l'arco 514 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_telecom_value` per la relazione che coinvolge il nodo canonico `_telecom_value()`.

### Adapter per il contratto storico ``build_request`` dell&#x27;orchestratore. [rve_transactions_rve_gateway_rationale_380]

<!-- GRAPH_NODE graph=codice index=265 id=rve_transactions_rve_gateway_rationale_380 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è ```Adapter per il contratto storico ``build_request`` dell'orchestratore.```. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è `L380`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` vale `ast`. Nel grafo del codice RDG, il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` possiede l'identificatore esatto `rve_transactions_rve_gateway_rationale_380`. Nel grafo del codice RDG, Graphify assegna il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è ```adapter per il contratto storico ``build_request`` dell'orchestratore.```.
<!-- GRAPH_EDGE graph=codice index=546 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_rationale_380` all'ID `rve_transactions_rve_gateway_build_request`. La relazione tecnica esplicita è la seguente: ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` costituisce una motivazione per `build_request() [rve_transactions_rve_gateway_build_request]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 546 che coinvolge il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 546 che coinvolge il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 546 che coinvolge il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 546 che coinvolge il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è `L380`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 546 che coinvolge il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 546 che coinvolge il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è `ast`. Nel grafo del codice RDG, l'arco 546 registra come `source` l'identificatore `rve_transactions_rve_gateway_rationale_380` per la relazione che coinvolge il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]```. Nel grafo del codice RDG, l'arco 546 registra come `target` l'identificatore `rve_transactions_rve_gateway_build_request` per la relazione che coinvolge il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]```. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 546 che coinvolge il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è `1.0`.
Nel grafo del codice RDG, il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=265 id=Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_380 -->
Nel grafo unificato, l'etichetta originale del nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è ```Adapter per il contratto storico ``build_request`` dell'orchestratore.```. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è `L380`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è ```adapter per il contratto storico ``build_request`` dell'orchestratore.```. Nel grafo unificato, la proprietà `repo` associa il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è `rve_transactions_rve_gateway_rationale_380`. Nel grafo unificato, il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_380`.
Nel grafo unificato, il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=546 role=incoming -->
Nel grafo unificato, il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_build_request` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_380`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_gateway_build_request]` costituisce una motivazione per ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]```. Nel grafo unificato, il tipo dichiarato dell'arco 546 che coinvolge il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 546 che coinvolge il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 546 che coinvolge il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 546 che coinvolge il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è `L380`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 546 che coinvolge il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 546 che coinvolge il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 546 che coinvolge il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` è `1.0`. Nel grafo unificato, l'arco 546 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_build_request` per la relazione che coinvolge il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]```. Nel grafo unificato, l'arco 546 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_380` per la relazione che coinvolge il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]```.

### build_request() [rve_transactions_rve_gateway_build_request]

<!-- GRAPH_NODE graph=codice index=260 id=rve_transactions_rve_gateway_build_request -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `build_request()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `L371`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` possiede l'identificatore esatto `rve_transactions_rve_gateway_build_request`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `build_request()`.
<!-- GRAPH_EDGE graph=codice index=543 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_build_request` all'ID `rve_transactions_rve_gateway_build_step`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_gateway_build_request]` chiama `build_step() [rve_transactions_rve_gateway_build_step]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 543 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 543 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 543 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 543 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 543 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `L382`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 543 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 543 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 543 registra come `source` l'identificatore `rve_transactions_rve_gateway_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]`. Nel grafo del codice RDG, l'arco 543 registra come `target` l'identificatore `rve_transactions_rve_gateway_build_step` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 543 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=499 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_build_request`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `build_request() [rve_transactions_rve_gateway_build_request]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 499 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 499 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 499 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 499 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `L371`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 499 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 499 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 499 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]`. Nel grafo del codice RDG, l'arco 499 registra come `target` l'identificatore `rve_transactions_rve_gateway_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 499 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=546 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]```; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_rationale_380` all'ID `rve_transactions_rve_gateway_build_request`. La relazione tecnica esplicita è la seguente: ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]``` costituisce una motivazione per `build_request() [rve_transactions_rve_gateway_build_request]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 546 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 546 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 546 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 546 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `L380`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 546 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 546 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `ast`. Nel grafo del codice RDG, l'arco 546 registra come `source` l'identificatore `rve_transactions_rve_gateway_rationale_380` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]`. Nel grafo del codice RDG, l'arco 546 registra come `target` l'identificatore `rve_transactions_rve_gateway_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 546 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=260 id=Request-Dataset-Generator::rve_transactions_rve_gateway_build_request -->
Nel grafo unificato, l'etichetta originale del nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `build_request()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `L371`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `build_request()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `rve_transactions_rve_gateway_build_request`. Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_build_request`.
<!-- GRAPH_EDGE graph=unificata index=546 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]```; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_build_request` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_380`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_gateway_build_request]` costituisce una motivazione per ```Adapter per il contratto storico ``build_request`` dell'orchestratore. [rve_transactions_rve_gateway_rationale_380]```. Nel grafo unificato, il tipo dichiarato dell'arco 546 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 546 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 546 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 546 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `L380`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 546 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 546 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 546 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `1.0`. Nel grafo unificato, l'arco 546 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]`. Nel grafo unificato, l'arco 546 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_380` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=499 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_build_request`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `build_request() [rve_transactions_rve_gateway_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 499 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 499 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 499 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 499 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `L371`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 499 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 499 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 499 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `1.0`. Nel grafo unificato, l'arco 499 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]`. Nel grafo unificato, l'arco 499 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]`.
<!-- GRAPH_EDGE graph=unificata index=543 role=incoming -->
Nel grafo unificato, il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_build_step` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_build_request`. La relazione tecnica esplicita è la seguente: `build_step() [rve_transactions_rve_gateway_build_step]` chiama `build_request() [rve_transactions_rve_gateway_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 543 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 543 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 543 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 543 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 543 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `L382`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 543 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 543 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 543 che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]` è `1.0`. Nel grafo unificato, l'arco 543 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_build_step` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]`. Nel grafo unificato, l'arco 543 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_build_request` per la relazione che coinvolge il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]`.

### build_step() [rve_transactions_rve_gateway_build_step]

<!-- GRAPH_NODE graph=codice index=259 id=rve_transactions_rve_gateway_build_step -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `build_step()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `L297`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` possiede l'identificatore esatto `rve_transactions_rve_gateway_build_step`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `build_step()`.
<!-- GRAPH_EDGE graph=codice index=544 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_build_mock_bundle()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_build_step` all'ID `rve_transactions_rve_gateway_build_mock_bundle`. La relazione tecnica esplicita è la seguente: `build_step() [rve_transactions_rve_gateway_build_step]` chiama `_build_mock_bundle()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 544 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 544 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 544 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 544 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 544 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `L330`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 544 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 544 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `ast`. Nel grafo del codice RDG, l'arco 544 registra come `source` l'identificatore `rve_transactions_rve_gateway_build_step` per la relazione che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`. Nel grafo del codice RDG, l'arco 544 registra come `target` l'identificatore `rve_transactions_rve_gateway_build_mock_bundle` per la relazione che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 544 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=500 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_build_step`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `build_step() [rve_transactions_rve_gateway_build_step]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 500 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 500 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 500 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 500 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `L297`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 500 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 500 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `ast`. Nel grafo del codice RDG, l'arco 500 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`. Nel grafo del codice RDG, l'arco 500 registra come `target` l'identificatore `rve_transactions_rve_gateway_build_step` per la relazione che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 500 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=543 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_build_request` all'ID `rve_transactions_rve_gateway_build_step`. La relazione tecnica esplicita è la seguente: `build_request() [rve_transactions_rve_gateway_build_request]` chiama `build_step() [rve_transactions_rve_gateway_build_step]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 543 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 543 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 543 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 543 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 543 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `L382`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 543 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 543 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `ast`. Nel grafo del codice RDG, l'arco 543 registra come `source` l'identificatore `rve_transactions_rve_gateway_build_request` per la relazione che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`. Nel grafo del codice RDG, l'arco 543 registra come `target` l'identificatore `rve_transactions_rve_gateway_build_step` per la relazione che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 543 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=545 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Costruisce uno step gateway per lo scenario indicato.`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_rationale_304` all'ID `rve_transactions_rve_gateway_build_step`. La relazione tecnica esplicita è la seguente: `Costruisce uno step gateway per lo scenario indicato.` costituisce una motivazione per `build_step() [rve_transactions_rve_gateway_build_step]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 545 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 545 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 545 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 545 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `L304`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 545 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 545 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `ast`. Nel grafo del codice RDG, l'arco 545 registra come `source` l'identificatore `rve_transactions_rve_gateway_rationale_304` per la relazione che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`. Nel grafo del codice RDG, l'arco 545 registra come `target` l'identificatore `rve_transactions_rve_gateway_build_step` per la relazione che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 545 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=259 id=Request-Dataset-Generator::rve_transactions_rve_gateway_build_step -->
Nel grafo unificato, l'etichetta originale del nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `build_step()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `L297`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `build_step()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `rve_transactions_rve_gateway_build_step`. Nel grafo unificato, il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_build_step`.
<!-- GRAPH_EDGE graph=unificata index=543 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è la sorgente `source` della relazione `calls` verso il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_build_step` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_build_request`. La relazione tecnica esplicita è la seguente: `build_step() [rve_transactions_rve_gateway_build_step]` chiama `build_request() [rve_transactions_rve_gateway_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 543 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 543 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 543 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 543 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 543 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `L382`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 543 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 543 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 543 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `1.0`. Nel grafo unificato, l'arco 543 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_build_step` per la relazione che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`. Nel grafo unificato, l'arco 543 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_build_request` per la relazione che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`.
<!-- GRAPH_EDGE graph=unificata index=544 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_build_mock_bundle()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_build_step` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_build_mock_bundle`. La relazione tecnica esplicita è la seguente: `build_step() [rve_transactions_rve_gateway_build_step]` chiama `_build_mock_bundle()`. Nel grafo unificato, il tipo dichiarato dell'arco 544 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 544 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 544 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 544 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 544 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `L330`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 544 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 544 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 544 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `1.0`. Nel grafo unificato, l'arco 544 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_build_step` per la relazione che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`. Nel grafo unificato, l'arco 544 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_build_mock_bundle` per la relazione che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`.
<!-- GRAPH_EDGE graph=unificata index=545 role=outgoing -->
Nel grafo unificato, il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Costruisce uno step gateway per lo scenario indicato.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_build_step` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_304`. La relazione tecnica esplicita è la seguente: `build_step() [rve_transactions_rve_gateway_build_step]` costituisce una motivazione per `Costruisce uno step gateway per lo scenario indicato.`. Nel grafo unificato, il tipo dichiarato dell'arco 545 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 545 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 545 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 545 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `L304`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 545 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 545 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 545 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `1.0`. Nel grafo unificato, l'arco 545 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_build_step` per la relazione che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`. Nel grafo unificato, l'arco 545 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_304` per la relazione che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`.
<!-- GRAPH_EDGE graph=unificata index=500 role=incoming -->
Nel grafo unificato, il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_build_step`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `build_step() [rve_transactions_rve_gateway_build_step]`. Nel grafo unificato, il tipo dichiarato dell'arco 500 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 500 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 500 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 500 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `L297`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 500 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 500 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 500 che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]` è `1.0`. Nel grafo unificato, l'arco 500 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`. Nel grafo unificato, l'arco 500 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_build_step` per la relazione che coinvolge il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`.

### Costruisce uno step gateway per lo scenario indicato.

<!-- GRAPH_NODE graph=codice index=264 id=rve_transactions_rve_gateway_rationale_304 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `Costruisce uno step gateway per lo scenario indicato.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `L304`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` possiede l'identificatore esatto `rve_transactions_rve_gateway_rationale_304`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `costruisce uno step gateway per lo scenario indicato.`.
<!-- GRAPH_EDGE graph=codice index=545 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_rationale_304` all'ID `rve_transactions_rve_gateway_build_step`. La relazione tecnica esplicita è la seguente: `Costruisce uno step gateway per lo scenario indicato.` costituisce una motivazione per `build_step() [rve_transactions_rve_gateway_build_step]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 545 che coinvolge il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 545 che coinvolge il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 545 che coinvolge il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 545 che coinvolge il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `L304`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 545 che coinvolge il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 545 che coinvolge il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `ast`. Nel grafo del codice RDG, l'arco 545 registra come `source` l'identificatore `rve_transactions_rve_gateway_rationale_304` per la relazione che coinvolge il nodo canonico `Costruisce uno step gateway per lo scenario indicato.`. Nel grafo del codice RDG, l'arco 545 registra come `target` l'identificatore `rve_transactions_rve_gateway_build_step` per la relazione che coinvolge il nodo canonico `Costruisce uno step gateway per lo scenario indicato.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 545 che coinvolge il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=264 id=Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_304 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `Costruisce uno step gateway per lo scenario indicato.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `L304`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `costruisce uno step gateway per lo scenario indicato.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `rve_transactions_rve_gateway_rationale_304`. Nel grafo unificato, il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_304`.
Nel grafo unificato, il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=545 role=incoming -->
Nel grafo unificato, il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_build_step` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_304`. La relazione tecnica esplicita è la seguente: `build_step() [rve_transactions_rve_gateway_build_step]` costituisce una motivazione per `Costruisce uno step gateway per lo scenario indicato.`. Nel grafo unificato, il tipo dichiarato dell'arco 545 che coinvolge il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 545 che coinvolge il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 545 che coinvolge il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 545 che coinvolge il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `L304`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 545 che coinvolge il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 545 che coinvolge il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 545 che coinvolge il nodo canonico `Costruisce uno step gateway per lo scenario indicato.` è `1.0`. Nel grafo unificato, l'arco 545 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_build_step` per la relazione che coinvolge il nodo canonico `Costruisce uno step gateway per lo scenario indicato.`. Nel grafo unificato, l'arco 545 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_304` per la relazione che coinvolge il nodo canonico `Costruisce uno step gateway per lo scenario indicato.`.

### RVE Gateway — generic middleware proxy request.  L&#x27;API Gateway è modellato come

<!-- GRAPH_NODE graph=codice index=263 id=rve_transactions_rve_gateway_rationale_1 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` vale `ast`. Nel grafo del codice RDG, il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` possiede l'identificatore esatto `rve_transactions_rve_gateway_rationale_1`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `rve gateway — generic middleware proxy request.  l'api gateway e modellato come`.
<!-- GRAPH_EDGE graph=codice index=515 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_rationale_1` all'ID `rve_transactions_rve_gateway`. La relazione tecnica esplicita è la seguente: `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` costituisce una motivazione per `rve_gateway.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 515 che coinvolge il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 515 che coinvolge il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 515 che coinvolge il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 515 che coinvolge il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 515 che coinvolge il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 515 che coinvolge il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `ast`. Nel grafo del codice RDG, l'arco 515 registra come `source` l'identificatore `rve_transactions_rve_gateway_rationale_1` per la relazione che coinvolge il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come`. Nel grafo del codice RDG, l'arco 515 registra come `target` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 515 che coinvolge il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=263 id=Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_1 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `rve gateway — generic middleware proxy request.  l'api gateway e modellato come`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `rve_transactions_rve_gateway_rationale_1`. Nel grafo unificato, il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_1`.
Nel grafo unificato, il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=515 role=incoming -->
Nel grafo unificato, il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `rve_gateway.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_1`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` costituisce una motivazione per `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come`. Nel grafo unificato, il tipo dichiarato dell'arco 515 che coinvolge il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 515 che coinvolge il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 515 che coinvolge il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 515 che coinvolge il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 515 che coinvolge il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 515 che coinvolge il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 515 che coinvolge il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` è `1.0`. Nel grafo unificato, l'arco 515 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come`. Nel grafo unificato, l'arco 515 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_1` per la relazione che coinvolge il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come`.

### rve_gateway.py

<!-- GRAPH_NODE graph=codice index=241 id=rve_transactions_rve_gateway -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `rve_gateway.py` è `rve_gateway.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `rve_gateway.py` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `rve_gateway.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `rve_gateway.py` vale `ast`. Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` possiede l'identificatore esatto `rve_transactions_rve_gateway`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `rve_gateway.py` alla comunità numerica `11`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `rve_gateway.py` è `rve_gateway.py`.
<!-- GRAPH_EDGE graph=codice index=494 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_address_locality()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_address_locality`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_address_locality()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 494 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 494 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 494 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 494 che coinvolge il nodo canonico `rve_gateway.py` è `L108`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 494 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 494 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, l'arco 494 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 494 registra come `target` l'identificatore `rve_transactions_rve_gateway_address_locality` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 494 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=495 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_address_postal_code()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_address_postal_code`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_address_postal_code()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 495 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 495 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 495 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 495 che coinvolge il nodo canonico `rve_gateway.py` è `L115`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 495 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 495 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, l'arco 495 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 495 registra come `target` l'identificatore `rve_transactions_rve_gateway_address_postal_code` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 495 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=496 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_address_text()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_address_text`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_address_text()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 496 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 496 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 496 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 496 che coinvolge il nodo canonico `rve_gateway.py` è `L104`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 496 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 496 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, l'arco 496 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 496 registra come `target` l'identificatore `rve_transactions_rve_gateway_address_text` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 496 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=497 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_birth_date_millis()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_birth_date_millis`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_birth_date_millis()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 497 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 497 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 497 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 497 che coinvolge il nodo canonico `rve_gateway.py` è `L62`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 497 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 497 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, l'arco 497 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 497 registra come `target` l'identificatore `rve_transactions_rve_gateway_birth_date_millis` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 497 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=498 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_build_mock_bundle()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_build_mock_bundle`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_build_mock_bundle()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 498 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 498 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 498 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 498 che coinvolge il nodo canonico `rve_gateway.py` è `L394`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 498 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 498 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, l'arco 498 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 498 registra come `target` l'identificatore `rve_transactions_rve_gateway_build_mock_bundle` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 498 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=499 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_build_request`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `build_request() [rve_transactions_rve_gateway_build_request]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 499 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 499 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 499 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 499 che coinvolge il nodo canonico `rve_gateway.py` è `L371`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 499 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 499 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, l'arco 499 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 499 registra come `target` l'identificatore `rve_transactions_rve_gateway_build_request` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 499 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=500 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_build_step`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `build_step() [rve_transactions_rve_gateway_build_step]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 500 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 500 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 500 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 500 che coinvolge il nodo canonico `rve_gateway.py` è `L297`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 500 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 500 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, l'arco 500 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 500 registra come `target` l'identificatore `rve_transactions_rve_gateway_build_step` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 500 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=501 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_family_name()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_family_name`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_family_name()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 501 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 501 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 501 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 501 che coinvolge il nodo canonico `rve_gateway.py` è `L46`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 501 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 501 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, l'arco 501 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 501 registra come `target` l'identificatore `rve_transactions_rve_gateway_family_name` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 501 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=502 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_first_address()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_first_address`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_first_address()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 502 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 502 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 502 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 502 che coinvolge il nodo canonico `rve_gateway.py` è `L99`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 502 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 502 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, l'arco 502 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 502 registra come `target` l'identificatore `rve_transactions_rve_gateway_first_address` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 502 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=503 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_first_string()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_first_string`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_first_string()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 503 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 503 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 503 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 503 che coinvolge il nodo canonico `rve_gateway.py` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 503 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 503 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, l'arco 503 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 503 registra come `target` l'identificatore `rve_transactions_rve_gateway_first_string` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 503 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=504 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_first_value()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_first_value`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_first_value()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 504 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 504 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 504 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 504 che coinvolge il nodo canonico `rve_gateway.py` è `L19`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 504 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 504 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, l'arco 504 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 504 registra come `target` l'identificatore `rve_transactions_rve_gateway_first_value` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 504 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=505 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_given_name()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_given_name`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_given_name()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 505 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 505 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 505 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 505 che coinvolge il nodo canonico `rve_gateway.py` è `L38`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 505 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 505 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, l'arco 505 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 505 registra come `target` l'identificatore `rve_transactions_rve_gateway_given_name` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 505 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=506 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_identifier_value()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_identifier_value`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_identifier_value()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 506 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 506 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 506 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 506 che coinvolge il nodo canonico `rve_gateway.py` è `L73`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 506 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 506 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, l'arco 506 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 506 registra come `target` l'identificatore `rve_transactions_rve_gateway_identifier_value` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 506 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=507 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_marital_status_code()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_marital_status_code`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_marital_status_code()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 507 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 507 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 507 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 507 che coinvolge il nodo canonico `rve_gateway.py` è `L92`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 507 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 507 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, l'arco 507 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 507 registra come `target` l'identificatore `rve_transactions_rve_gateway_marital_status_code` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 507 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=508 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_mock_patient()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_mock_patient`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_mock_patient()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 508 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 508 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 508 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 508 che coinvolge il nodo canonico `rve_gateway.py` è `L411`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 508 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 508 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, l'arco 508 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 508 registra come `target` l'identificatore `rve_transactions_rve_gateway_mock_patient` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 508 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=509 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `_payload_rve100()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_payload_rve100`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` effettua una chiamata indiretta verso `_payload_rve100()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 509 che coinvolge il nodo canonico `rve_gateway.py` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 509 che coinvolge il nodo canonico `rve_gateway.py` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 509 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 509 che coinvolge il nodo canonico `rve_gateway.py` è `L293`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 509 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 509 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 509 che coinvolge il nodo canonico `rve_gateway.py` è `collection`. Nel grafo del codice RDG, l'arco 509 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 509 registra come `target` l'identificatore `rve_transactions_rve_gateway_payload_rve100` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 509 che coinvolge il nodo canonico `rve_gateway.py` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=510 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `_payload_rve54()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_payload_rve54`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` effettua una chiamata indiretta verso `_payload_rve54()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 510 che coinvolge il nodo canonico `rve_gateway.py` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 510 che coinvolge il nodo canonico `rve_gateway.py` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 510 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 510 che coinvolge il nodo canonico `rve_gateway.py` è `L290`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 510 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 510 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 510 che coinvolge il nodo canonico `rve_gateway.py` è `collection`. Nel grafo del codice RDG, l'arco 510 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 510 registra come `target` l'identificatore `rve_transactions_rve_gateway_payload_rve54` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 510 che coinvolge il nodo canonico `rve_gateway.py` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=511 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` effettua una chiamata indiretta verso `_payload_rve55()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 511 che coinvolge il nodo canonico `rve_gateway.py` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 511 che coinvolge il nodo canonico `rve_gateway.py` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 511 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 511 che coinvolge il nodo canonico `rve_gateway.py` è `L291`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 511 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 511 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 511 che coinvolge il nodo canonico `rve_gateway.py` è `collection`. Nel grafo del codice RDG, l'arco 511 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 511 registra come `target` l'identificatore `rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 511 che coinvolge il nodo canonico `rve_gateway.py` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=512 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` effettua una chiamata indiretta verso `_payload_rve57()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 512 che coinvolge il nodo canonico `rve_gateway.py` è `indirect_call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 512 che coinvolge il nodo canonico `rve_gateway.py` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 512 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 512 che coinvolge il nodo canonico `rve_gateway.py` è `L292`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 512 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 512 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 512 che coinvolge il nodo canonico `rve_gateway.py` è `collection`. Nel grafo del codice RDG, l'arco 512 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 512 registra come `target` l'identificatore `rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 512 che coinvolge il nodo canonico `rve_gateway.py` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=513 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_sex_code()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_sex_code`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_sex_code()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 513 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 513 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 513 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 513 che coinvolge il nodo canonico `rve_gateway.py` è `L53`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 513 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 513 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, l'arco 513 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 513 registra come `target` l'identificatore `rve_transactions_rve_gateway_sex_code` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 513 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=514 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_telecom_value()`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway` all'ID `rve_transactions_rve_gateway_telecom_value`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_telecom_value()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 514 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 514 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 514 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 514 che coinvolge il nodo canonico `rve_gateway.py` è `L83`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 514 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 514 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, l'arco 514 registra come `source` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 514 registra come `target` l'identificatore `rve_transactions_rve_gateway_telecom_value` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 514 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=416 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `flow_orchestrator.py`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_flow_orchestrator` all'ID `rve_transactions_rve_gateway`. La relazione tecnica esplicita è la seguente: `flow_orchestrator.py` importa da `rve_gateway.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 416 che coinvolge il nodo canonico `rve_gateway.py` è `imports_from`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 416 che coinvolge il nodo canonico `rve_gateway.py` è `submodule_import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 416 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 416 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/flow_orchestrator.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 416 che coinvolge il nodo canonico `rve_gateway.py` è `L26`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 416 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 416 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, l'arco 416 registra come `source` l'identificatore `rve_transactions_flow_orchestrator` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 416 registra come `target` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 416 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=515 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `rve_gateway.py` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come`; l'orientamento registrato nel JSON va dall'ID `rve_transactions_rve_gateway_rationale_1` all'ID `rve_transactions_rve_gateway`. La relazione tecnica esplicita è la seguente: `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come` costituisce una motivazione per `rve_gateway.py`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 515 che coinvolge il nodo canonico `rve_gateway.py` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 515 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 515 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 515 che coinvolge il nodo canonico `rve_gateway.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 515 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 515 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo del codice RDG, l'arco 515 registra come `source` l'identificatore `rve_transactions_rve_gateway_rationale_1` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, l'arco 515 registra come `target` l'identificatore `rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 515 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=241 id=Request-Dataset-Generator::rve_transactions_rve_gateway -->
Nel grafo unificato, l'etichetta originale del nodo canonico `rve_gateway.py` è `rve_gateway.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `rve_gateway.py` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `rve_gateway.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `rve_gateway.py` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `rve_gateway.py` alla comunità numerica `11`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `rve_gateway.py` è `rve_gateway.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `rve_gateway.py` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `rve_gateway.py` è `rve_transactions_rve_gateway`. Nel grafo unificato, il nodo canonico `rve_gateway.py` possiede l'identificatore esatto `Request-Dataset-Generator::rve_transactions_rve_gateway`.
<!-- GRAPH_EDGE graph=unificata index=494 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_address_locality()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_locality`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_address_locality()`. Nel grafo unificato, il tipo dichiarato dell'arco 494 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 494 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 494 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 494 che coinvolge il nodo canonico `rve_gateway.py` è `L108`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 494 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 494 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 494 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, l'arco 494 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 494 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_locality` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=495 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_address_postal_code()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_postal_code`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_address_postal_code()`. Nel grafo unificato, il tipo dichiarato dell'arco 495 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 495 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 495 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 495 che coinvolge il nodo canonico `rve_gateway.py` è `L115`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 495 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 495 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 495 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, l'arco 495 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 495 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_postal_code` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=496 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_address_text()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_address_text`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_address_text()`. Nel grafo unificato, il tipo dichiarato dell'arco 496 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 496 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 496 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 496 che coinvolge il nodo canonico `rve_gateway.py` è `L104`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 496 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 496 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 496 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, l'arco 496 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 496 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_address_text` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=497 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_birth_date_millis()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_birth_date_millis`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_birth_date_millis()`. Nel grafo unificato, il tipo dichiarato dell'arco 497 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 497 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 497 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 497 che coinvolge il nodo canonico `rve_gateway.py` è `L62`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 497 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 497 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 497 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, l'arco 497 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 497 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_birth_date_millis` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=498 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_build_mock_bundle()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_build_mock_bundle`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_build_mock_bundle()`. Nel grafo unificato, il tipo dichiarato dell'arco 498 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 498 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 498 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 498 che coinvolge il nodo canonico `rve_gateway.py` è `L394`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 498 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 498 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 498 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, l'arco 498 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 498 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_build_mock_bundle` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=499 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `build_request() [rve_transactions_rve_gateway_build_request]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_build_request`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `build_request() [rve_transactions_rve_gateway_build_request]`. Nel grafo unificato, il tipo dichiarato dell'arco 499 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 499 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 499 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 499 che coinvolge il nodo canonico `rve_gateway.py` è `L371`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 499 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 499 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 499 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, l'arco 499 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 499 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_build_request` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=500 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `build_step() [rve_transactions_rve_gateway_build_step]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_build_step`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `build_step() [rve_transactions_rve_gateway_build_step]`. Nel grafo unificato, il tipo dichiarato dell'arco 500 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 500 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 500 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 500 che coinvolge il nodo canonico `rve_gateway.py` è `L297`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 500 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 500 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 500 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, l'arco 500 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 500 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_build_step` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=501 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_family_name()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_family_name`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_family_name()`. Nel grafo unificato, il tipo dichiarato dell'arco 501 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 501 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 501 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 501 che coinvolge il nodo canonico `rve_gateway.py` è `L46`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 501 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 501 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 501 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, l'arco 501 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 501 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_family_name` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=502 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_first_address()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_first_address`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_first_address()`. Nel grafo unificato, il tipo dichiarato dell'arco 502 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 502 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 502 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 502 che coinvolge il nodo canonico `rve_gateway.py` è `L99`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 502 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 502 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 502 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, l'arco 502 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 502 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_first_address` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=503 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_first_string()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_first_string`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_first_string()`. Nel grafo unificato, il tipo dichiarato dell'arco 503 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 503 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 503 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 503 che coinvolge il nodo canonico `rve_gateway.py` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 503 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 503 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 503 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, l'arco 503 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 503 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_first_string` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=504 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_first_value()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_first_value`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_first_value()`. Nel grafo unificato, il tipo dichiarato dell'arco 504 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 504 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 504 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 504 che coinvolge il nodo canonico `rve_gateway.py` è `L19`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 504 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 504 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 504 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, l'arco 504 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 504 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_first_value` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=505 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_given_name()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_given_name`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_given_name()`. Nel grafo unificato, il tipo dichiarato dell'arco 505 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 505 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 505 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 505 che coinvolge il nodo canonico `rve_gateway.py` è `L38`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 505 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 505 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 505 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, l'arco 505 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 505 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_given_name` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=506 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_identifier_value()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_identifier_value`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_identifier_value()`. Nel grafo unificato, il tipo dichiarato dell'arco 506 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 506 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 506 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 506 che coinvolge il nodo canonico `rve_gateway.py` è `L73`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 506 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 506 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 506 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, l'arco 506 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 506 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_identifier_value` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=507 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_marital_status_code()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_marital_status_code`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_marital_status_code()`. Nel grafo unificato, il tipo dichiarato dell'arco 507 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 507 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 507 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 507 che coinvolge il nodo canonico `rve_gateway.py` è `L92`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 507 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 507 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 507 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, l'arco 507 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 507 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_marital_status_code` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=508 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_mock_patient()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_mock_patient`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_mock_patient()`. Nel grafo unificato, il tipo dichiarato dell'arco 508 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 508 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 508 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 508 che coinvolge il nodo canonico `rve_gateway.py` è `L411`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 508 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 508 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 508 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, l'arco 508 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 508 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_mock_patient` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=509 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `_payload_rve100()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve100`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` effettua una chiamata indiretta verso `_payload_rve100()`. Nel grafo unificato, il tipo dichiarato dell'arco 509 che coinvolge il nodo canonico `rve_gateway.py` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 509 che coinvolge il nodo canonico `rve_gateway.py` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 509 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 509 che coinvolge il nodo canonico `rve_gateway.py` è `L293`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 509 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 509 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 509 che coinvolge il nodo canonico `rve_gateway.py` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 509 che coinvolge il nodo canonico `rve_gateway.py` è `0.5`. Nel grafo unificato, l'arco 509 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 509 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve100` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=510 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `_payload_rve54()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve54`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` effettua una chiamata indiretta verso `_payload_rve54()`. Nel grafo unificato, il tipo dichiarato dell'arco 510 che coinvolge il nodo canonico `rve_gateway.py` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 510 che coinvolge il nodo canonico `rve_gateway.py` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 510 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 510 che coinvolge il nodo canonico `rve_gateway.py` è `L290`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 510 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 510 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 510 che coinvolge il nodo canonico `rve_gateway.py` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 510 che coinvolge il nodo canonico `rve_gateway.py` è `0.5`. Nel grafo unificato, l'arco 510 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 510 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve54` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=511 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `_payload_rve55()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` effettua una chiamata indiretta verso `_payload_rve55()`. Nel grafo unificato, il tipo dichiarato dell'arco 511 che coinvolge il nodo canonico `rve_gateway.py` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 511 che coinvolge il nodo canonico `rve_gateway.py` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 511 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 511 che coinvolge il nodo canonico `rve_gateway.py` è `L291`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 511 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 511 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 511 che coinvolge il nodo canonico `rve_gateway.py` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 511 che coinvolge il nodo canonico `rve_gateway.py` è `0.5`. Nel grafo unificato, l'arco 511 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 511 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve55` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=512 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `indirect_call` verso il nodo canonico `_payload_rve57()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` effettua una chiamata indiretta verso `_payload_rve57()`. Nel grafo unificato, il tipo dichiarato dell'arco 512 che coinvolge il nodo canonico `rve_gateway.py` è `indirect_call`. Nel grafo unificato, la confidenza categoriale dell'arco 512 che coinvolge il nodo canonico `rve_gateway.py` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 512 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 512 che coinvolge il nodo canonico `rve_gateway.py` è `L292`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 512 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 512 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il contesto dichiarato per l'arco 512 che coinvolge il nodo canonico `rve_gateway.py` è `collection`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 512 che coinvolge il nodo canonico `rve_gateway.py` è `0.5`. Nel grafo unificato, l'arco 512 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 512 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_payload_rve57` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=513 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_sex_code()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_sex_code`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_sex_code()`. Nel grafo unificato, il tipo dichiarato dell'arco 513 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 513 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 513 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 513 che coinvolge il nodo canonico `rve_gateway.py` è `L53`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 513 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 513 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 513 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, l'arco 513 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 513 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_sex_code` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=514 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_telecom_value()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_telecom_value`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` contiene `_telecom_value()`. Nel grafo unificato, il tipo dichiarato dell'arco 514 che coinvolge il nodo canonico `rve_gateway.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 514 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 514 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 514 che coinvolge il nodo canonico `rve_gateway.py` è `L83`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 514 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 514 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 514 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, l'arco 514 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 514 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_telecom_value` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=515 role=outgoing -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_rve_gateway` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_1`. La relazione tecnica esplicita è la seguente: `rve_gateway.py` costituisce una motivazione per `RVE Gateway — generic middleware proxy request.  L'API Gateway è modellato come`. Nel grafo unificato, il tipo dichiarato dell'arco 515 che coinvolge il nodo canonico `rve_gateway.py` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 515 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 515 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/rve_gateway.py`. Nel grafo unificato, la posizione sorgente dell'arco 515 che coinvolge il nodo canonico `rve_gateway.py` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 515 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 515 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 515 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, l'arco 515 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 515 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway_rationale_1` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.
<!-- GRAPH_EDGE graph=unificata index=416 role=incoming -->
Nel grafo unificato, il nodo canonico `rve_gateway.py` è il destinatario `target` della relazione `imports_from` proveniente dal nodo canonico `flow_orchestrator.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::rve_transactions_flow_orchestrator` all'ID `Request-Dataset-Generator::rve_transactions_rve_gateway`. La relazione tecnica esplicita è la seguente: `flow_orchestrator.py` importa da `rve_gateway.py`. Nel grafo unificato, il tipo dichiarato dell'arco 416 che coinvolge il nodo canonico `rve_gateway.py` è `imports_from`. Nel grafo unificato, il contesto dichiarato per l'arco 416 che coinvolge il nodo canonico `rve_gateway.py` è `submodule_import`. Nel grafo unificato, la confidenza categoriale dell'arco 416 che coinvolge il nodo canonico `rve_gateway.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 416 che coinvolge il nodo canonico `rve_gateway.py` è `rve_transactions/flow_orchestrator.py`. Nel grafo unificato, la posizione sorgente dell'arco 416 che coinvolge il nodo canonico `rve_gateway.py` è `L26`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 416 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 416 che coinvolge il nodo canonico `rve_gateway.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 416 che coinvolge il nodo canonico `rve_gateway.py` è `1.0`. Nel grafo unificato, l'arco 416 registra come `source` l'identificatore `Request-Dataset-Generator::rve_transactions_flow_orchestrator` per la relazione che coinvolge il nodo canonico `rve_gateway.py`. Nel grafo unificato, l'arco 416 registra come `target` l'identificatore `Request-Dataset-Generator::rve_transactions_rve_gateway` per la relazione che coinvolge il nodo canonico `rve_gateway.py`.

## Cluster Graphify 12 — Middleware Log Mocking

Il cluster editoriale Graphify 12 del dominio Architettura e codice del framework RDG contiene 23 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### .__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]

<!-- GRAPH_NODE graph=codice index=132 id=mock_middleware_log_mockmiddlewareloggenerator_init -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `.__init__()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `L60`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` possiede l'identificatore esatto `mock_middleware_log_mockmiddlewareloggenerator_init`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `.__init__()`.
Nel grafo del codice RDG, il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=289 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator` all'ID `mock_middleware_log_mockmiddlewareloggenerator_init`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 289 che coinvolge il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 289 che coinvolge il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 289 che coinvolge il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 289 che coinvolge il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `L60`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 289 che coinvolge il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 289 che coinvolge il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `ast`. Nel grafo del codice RDG, l'arco 289 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]`. Nel grafo del codice RDG, l'arco 289 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_init` per la relazione che coinvolge il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 289 che coinvolge il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=132 id=Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_init -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `.__init__()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `L60`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `.__init__()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `mock_middleware_log_mockmiddlewareloggenerator_init`. Nel grafo unificato, il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_init`.
Nel grafo unificato, il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=289 role=incoming -->
Nel grafo unificato, il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_init`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 289 che coinvolge il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 289 che coinvolge il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 289 che coinvolge il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 289 che coinvolge il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `L60`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 289 che coinvolge il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 289 che coinvolge il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 289 che coinvolge il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]` è `1.0`. Nel grafo unificato, l'arco 289 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]`. Nel grafo unificato, l'arco 289 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_init` per la relazione che coinvolge il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]`.

### ._decompose_latency()

<!-- GRAPH_NODE graph=codice index=137 id=mock_middleware_log_mockmiddlewareloggenerator_decompose_latency -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._decompose_latency()` è `._decompose_latency()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._decompose_latency()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._decompose_latency()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._decompose_latency()` è `L133`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._decompose_latency()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._decompose_latency()` possiede l'identificatore esatto `mock_middleware_log_mockmiddlewareloggenerator_decompose_latency`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._decompose_latency()` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._decompose_latency()` è `._decompose_latency()`.
Nel grafo del codice RDG, il nodo canonico `._decompose_latency()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=287 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._decompose_latency()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator` all'ID `mock_middleware_log_mockmiddlewareloggenerator_decompose_latency`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._decompose_latency()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 287 che coinvolge il nodo canonico `._decompose_latency()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 287 che coinvolge il nodo canonico `._decompose_latency()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 287 che coinvolge il nodo canonico `._decompose_latency()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 287 che coinvolge il nodo canonico `._decompose_latency()` è `L133`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 287 che coinvolge il nodo canonico `._decompose_latency()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 287 che coinvolge il nodo canonico `._decompose_latency()` è `ast`. Nel grafo del codice RDG, l'arco 287 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `._decompose_latency()`. Nel grafo del codice RDG, l'arco 287 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_decompose_latency` per la relazione che coinvolge il nodo canonico `._decompose_latency()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 287 che coinvolge il nodo canonico `._decompose_latency()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=305 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._decompose_latency()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.generate_entry()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` all'ID `mock_middleware_log_mockmiddlewareloggenerator_decompose_latency`. La relazione tecnica esplicita è la seguente: `.generate_entry()` chiama `._decompose_latency()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 305 che coinvolge il nodo canonico `._decompose_latency()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 305 che coinvolge il nodo canonico `._decompose_latency()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 305 che coinvolge il nodo canonico `._decompose_latency()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 305 che coinvolge il nodo canonico `._decompose_latency()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 305 che coinvolge il nodo canonico `._decompose_latency()` è `L198`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 305 che coinvolge il nodo canonico `._decompose_latency()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 305 che coinvolge il nodo canonico `._decompose_latency()` è `ast`. Nel grafo del codice RDG, l'arco 305 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `._decompose_latency()`. Nel grafo del codice RDG, l'arco 305 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_decompose_latency` per la relazione che coinvolge il nodo canonico `._decompose_latency()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 305 che coinvolge il nodo canonico `._decompose_latency()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=306 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._decompose_latency()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_rationale_134` all'ID `mock_middleware_log_mockmiddlewareloggenerator_decompose_latency`. La relazione tecnica esplicita è la seguente: `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` costituisce una motivazione per `._decompose_latency()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 306 che coinvolge il nodo canonico `._decompose_latency()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 306 che coinvolge il nodo canonico `._decompose_latency()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 306 che coinvolge il nodo canonico `._decompose_latency()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 306 che coinvolge il nodo canonico `._decompose_latency()` è `L134`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 306 che coinvolge il nodo canonico `._decompose_latency()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 306 che coinvolge il nodo canonico `._decompose_latency()` è `ast`. Nel grafo del codice RDG, l'arco 306 registra come `source` l'identificatore `mock_middleware_log_rationale_134` per la relazione che coinvolge il nodo canonico `._decompose_latency()`. Nel grafo del codice RDG, l'arco 306 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_decompose_latency` per la relazione che coinvolge il nodo canonico `._decompose_latency()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 306 che coinvolge il nodo canonico `._decompose_latency()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=137 id=Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_decompose_latency -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._decompose_latency()` è `._decompose_latency()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._decompose_latency()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._decompose_latency()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._decompose_latency()` è `L133`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._decompose_latency()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._decompose_latency()` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._decompose_latency()` è `._decompose_latency()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._decompose_latency()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._decompose_latency()` è `mock_middleware_log_mockmiddlewareloggenerator_decompose_latency`. Nel grafo unificato, il nodo canonico `._decompose_latency()` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_decompose_latency`.
<!-- GRAPH_EDGE graph=unificata index=305 role=outgoing -->
Nel grafo unificato, il nodo canonico `._decompose_latency()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.generate_entry()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_decompose_latency` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. La relazione tecnica esplicita è la seguente: `._decompose_latency()` chiama `.generate_entry()`. Nel grafo unificato, il tipo dichiarato dell'arco 305 che coinvolge il nodo canonico `._decompose_latency()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 305 che coinvolge il nodo canonico `._decompose_latency()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 305 che coinvolge il nodo canonico `._decompose_latency()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 305 che coinvolge il nodo canonico `._decompose_latency()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 305 che coinvolge il nodo canonico `._decompose_latency()` è `L198`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 305 che coinvolge il nodo canonico `._decompose_latency()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 305 che coinvolge il nodo canonico `._decompose_latency()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 305 che coinvolge il nodo canonico `._decompose_latency()` è `1.0`. Nel grafo unificato, l'arco 305 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_decompose_latency` per la relazione che coinvolge il nodo canonico `._decompose_latency()`. Nel grafo unificato, l'arco 305 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `._decompose_latency()`.
<!-- GRAPH_EDGE graph=unificata index=306 role=outgoing -->
Nel grafo unificato, il nodo canonico `._decompose_latency()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_decompose_latency` all'ID `Request-Dataset-Generator::mock_middleware_log_rationale_134`. La relazione tecnica esplicita è la seguente: `._decompose_latency()` costituisce una motivazione per `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga`. Nel grafo unificato, il tipo dichiarato dell'arco 306 che coinvolge il nodo canonico `._decompose_latency()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 306 che coinvolge il nodo canonico `._decompose_latency()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 306 che coinvolge il nodo canonico `._decompose_latency()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 306 che coinvolge il nodo canonico `._decompose_latency()` è `L134`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 306 che coinvolge il nodo canonico `._decompose_latency()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 306 che coinvolge il nodo canonico `._decompose_latency()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 306 che coinvolge il nodo canonico `._decompose_latency()` è `1.0`. Nel grafo unificato, l'arco 306 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_decompose_latency` per la relazione che coinvolge il nodo canonico `._decompose_latency()`. Nel grafo unificato, l'arco 306 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_rationale_134` per la relazione che coinvolge il nodo canonico `._decompose_latency()`.
<!-- GRAPH_EDGE graph=unificata index=287 role=incoming -->
Nel grafo unificato, il nodo canonico `._decompose_latency()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_decompose_latency`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._decompose_latency()`. Nel grafo unificato, il tipo dichiarato dell'arco 287 che coinvolge il nodo canonico `._decompose_latency()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 287 che coinvolge il nodo canonico `._decompose_latency()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 287 che coinvolge il nodo canonico `._decompose_latency()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 287 che coinvolge il nodo canonico `._decompose_latency()` è `L133`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 287 che coinvolge il nodo canonico `._decompose_latency()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 287 che coinvolge il nodo canonico `._decompose_latency()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 287 che coinvolge il nodo canonico `._decompose_latency()` è `1.0`. Nel grafo unificato, l'arco 287 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `._decompose_latency()`. Nel grafo unificato, l'arco 287 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_decompose_latency` per la relazione che coinvolge il nodo canonico `._decompose_latency()`.

### ._inject_anomaly()

<!-- GRAPH_NODE graph=codice index=138 id=mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._inject_anomaly()` è `._inject_anomaly()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._inject_anomaly()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._inject_anomaly()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._inject_anomaly()` è `L154`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._inject_anomaly()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._inject_anomaly()` possiede l'identificatore esatto `mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._inject_anomaly()` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._inject_anomaly()` è `._inject_anomaly()`.
Nel grafo del codice RDG, il nodo canonico `._inject_anomaly()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=290 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._inject_anomaly()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator` all'ID `mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._inject_anomaly()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 290 che coinvolge il nodo canonico `._inject_anomaly()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 290 che coinvolge il nodo canonico `._inject_anomaly()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 290 che coinvolge il nodo canonico `._inject_anomaly()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 290 che coinvolge il nodo canonico `._inject_anomaly()` è `L154`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 290 che coinvolge il nodo canonico `._inject_anomaly()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 290 che coinvolge il nodo canonico `._inject_anomaly()` è `ast`. Nel grafo del codice RDG, l'arco 290 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `._inject_anomaly()`. Nel grafo del codice RDG, l'arco 290 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly` per la relazione che coinvolge il nodo canonico `._inject_anomaly()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 290 che coinvolge il nodo canonico `._inject_anomaly()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=307 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._inject_anomaly()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.generate_entry()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` all'ID `mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly`. La relazione tecnica esplicita è la seguente: `.generate_entry()` chiama `._inject_anomaly()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 307 che coinvolge il nodo canonico `._inject_anomaly()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 307 che coinvolge il nodo canonico `._inject_anomaly()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 307 che coinvolge il nodo canonico `._inject_anomaly()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 307 che coinvolge il nodo canonico `._inject_anomaly()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 307 che coinvolge il nodo canonico `._inject_anomaly()` è `L201`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 307 che coinvolge il nodo canonico `._inject_anomaly()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 307 che coinvolge il nodo canonico `._inject_anomaly()` è `ast`. Nel grafo del codice RDG, l'arco 307 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `._inject_anomaly()`. Nel grafo del codice RDG, l'arco 307 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly` per la relazione che coinvolge il nodo canonico `._inject_anomaly()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 307 che coinvolge il nodo canonico `._inject_anomaly()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=308 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._inject_anomaly()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_rationale_161` all'ID `mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly`. La relazione tecnica esplicita è la seguente: `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` costituisce una motivazione per `._inject_anomaly()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 308 che coinvolge il nodo canonico `._inject_anomaly()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 308 che coinvolge il nodo canonico `._inject_anomaly()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 308 che coinvolge il nodo canonico `._inject_anomaly()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 308 che coinvolge il nodo canonico `._inject_anomaly()` è `L161`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 308 che coinvolge il nodo canonico `._inject_anomaly()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 308 che coinvolge il nodo canonico `._inject_anomaly()` è `ast`. Nel grafo del codice RDG, l'arco 308 registra come `source` l'identificatore `mock_middleware_log_rationale_161` per la relazione che coinvolge il nodo canonico `._inject_anomaly()`. Nel grafo del codice RDG, l'arco 308 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly` per la relazione che coinvolge il nodo canonico `._inject_anomaly()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 308 che coinvolge il nodo canonico `._inject_anomaly()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=138 id=Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._inject_anomaly()` è `._inject_anomaly()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._inject_anomaly()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._inject_anomaly()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._inject_anomaly()` è `L154`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._inject_anomaly()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._inject_anomaly()` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._inject_anomaly()` è `._inject_anomaly()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._inject_anomaly()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._inject_anomaly()` è `mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly`. Nel grafo unificato, il nodo canonico `._inject_anomaly()` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly`.
<!-- GRAPH_EDGE graph=unificata index=307 role=outgoing -->
Nel grafo unificato, il nodo canonico `._inject_anomaly()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.generate_entry()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. La relazione tecnica esplicita è la seguente: `._inject_anomaly()` chiama `.generate_entry()`. Nel grafo unificato, il tipo dichiarato dell'arco 307 che coinvolge il nodo canonico `._inject_anomaly()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 307 che coinvolge il nodo canonico `._inject_anomaly()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 307 che coinvolge il nodo canonico `._inject_anomaly()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 307 che coinvolge il nodo canonico `._inject_anomaly()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 307 che coinvolge il nodo canonico `._inject_anomaly()` è `L201`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 307 che coinvolge il nodo canonico `._inject_anomaly()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 307 che coinvolge il nodo canonico `._inject_anomaly()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 307 che coinvolge il nodo canonico `._inject_anomaly()` è `1.0`. Nel grafo unificato, l'arco 307 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly` per la relazione che coinvolge il nodo canonico `._inject_anomaly()`. Nel grafo unificato, l'arco 307 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `._inject_anomaly()`.
<!-- GRAPH_EDGE graph=unificata index=308 role=outgoing -->
Nel grafo unificato, il nodo canonico `._inject_anomaly()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly` all'ID `Request-Dataset-Generator::mock_middleware_log_rationale_161`. La relazione tecnica esplicita è la seguente: `._inject_anomaly()` costituisce una motivazione per `Inietta anomalie (spike, errori, timeout) con probabilità configurate.`. Nel grafo unificato, il tipo dichiarato dell'arco 308 che coinvolge il nodo canonico `._inject_anomaly()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 308 che coinvolge il nodo canonico `._inject_anomaly()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 308 che coinvolge il nodo canonico `._inject_anomaly()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 308 che coinvolge il nodo canonico `._inject_anomaly()` è `L161`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 308 che coinvolge il nodo canonico `._inject_anomaly()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 308 che coinvolge il nodo canonico `._inject_anomaly()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 308 che coinvolge il nodo canonico `._inject_anomaly()` è `1.0`. Nel grafo unificato, l'arco 308 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly` per la relazione che coinvolge il nodo canonico `._inject_anomaly()`. Nel grafo unificato, l'arco 308 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_rationale_161` per la relazione che coinvolge il nodo canonico `._inject_anomaly()`.
<!-- GRAPH_EDGE graph=unificata index=290 role=incoming -->
Nel grafo unificato, il nodo canonico `._inject_anomaly()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._inject_anomaly()`. Nel grafo unificato, il tipo dichiarato dell'arco 290 che coinvolge il nodo canonico `._inject_anomaly()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 290 che coinvolge il nodo canonico `._inject_anomaly()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 290 che coinvolge il nodo canonico `._inject_anomaly()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 290 che coinvolge il nodo canonico `._inject_anomaly()` è `L154`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 290 che coinvolge il nodo canonico `._inject_anomaly()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 290 che coinvolge il nodo canonico `._inject_anomaly()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 290 che coinvolge il nodo canonico `._inject_anomaly()` è `1.0`. Nel grafo unificato, l'arco 290 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `._inject_anomaly()`. Nel grafo unificato, l'arco 290 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly` per la relazione che coinvolge il nodo canonico `._inject_anomaly()`.

### ._next_circuit_breaker()

<!-- GRAPH_NODE graph=codice index=136 id=mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._next_circuit_breaker()` è `._next_circuit_breaker()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._next_circuit_breaker()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._next_circuit_breaker()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._next_circuit_breaker()` è `L104`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._next_circuit_breaker()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._next_circuit_breaker()` possiede l'identificatore esatto `mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._next_circuit_breaker()` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._next_circuit_breaker()` è `._next_circuit_breaker()`.
Nel grafo del codice RDG, il nodo canonico `._next_circuit_breaker()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=291 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._next_circuit_breaker()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._next_circuit_breaker()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 291 che coinvolge il nodo canonico `._next_circuit_breaker()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 291 che coinvolge il nodo canonico `._next_circuit_breaker()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 291 che coinvolge il nodo canonico `._next_circuit_breaker()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 291 che coinvolge il nodo canonico `._next_circuit_breaker()` è `L104`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 291 che coinvolge il nodo canonico `._next_circuit_breaker()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 291 che coinvolge il nodo canonico `._next_circuit_breaker()` è `ast`. Nel grafo del codice RDG, l'arco 291 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `._next_circuit_breaker()`. Nel grafo del codice RDG, l'arco 291 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker` per la relazione che coinvolge il nodo canonico `._next_circuit_breaker()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 291 che coinvolge il nodo canonico `._next_circuit_breaker()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=303 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._next_circuit_breaker()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.generate_entry()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker`. La relazione tecnica esplicita è la seguente: `.generate_entry()` chiama `._next_circuit_breaker()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 303 che coinvolge il nodo canonico `._next_circuit_breaker()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 303 che coinvolge il nodo canonico `._next_circuit_breaker()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 303 che coinvolge il nodo canonico `._next_circuit_breaker()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 303 che coinvolge il nodo canonico `._next_circuit_breaker()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 303 che coinvolge il nodo canonico `._next_circuit_breaker()` è `L220`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 303 che coinvolge il nodo canonico `._next_circuit_breaker()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 303 che coinvolge il nodo canonico `._next_circuit_breaker()` è `ast`. Nel grafo del codice RDG, l'arco 303 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `._next_circuit_breaker()`. Nel grafo del codice RDG, l'arco 303 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker` per la relazione che coinvolge il nodo canonico `._next_circuit_breaker()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 303 che coinvolge il nodo canonico `._next_circuit_breaker()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=304 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._next_circuit_breaker()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Simula transizioni del circuit breaker.`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_rationale_105` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker`. La relazione tecnica esplicita è la seguente: `Simula transizioni del circuit breaker.` costituisce una motivazione per `._next_circuit_breaker()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 304 che coinvolge il nodo canonico `._next_circuit_breaker()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 304 che coinvolge il nodo canonico `._next_circuit_breaker()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 304 che coinvolge il nodo canonico `._next_circuit_breaker()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 304 che coinvolge il nodo canonico `._next_circuit_breaker()` è `L105`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 304 che coinvolge il nodo canonico `._next_circuit_breaker()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 304 che coinvolge il nodo canonico `._next_circuit_breaker()` è `ast`. Nel grafo del codice RDG, l'arco 304 registra come `source` l'identificatore `mock_middleware_log_rationale_105` per la relazione che coinvolge il nodo canonico `._next_circuit_breaker()`. Nel grafo del codice RDG, l'arco 304 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker` per la relazione che coinvolge il nodo canonico `._next_circuit_breaker()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 304 che coinvolge il nodo canonico `._next_circuit_breaker()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=136 id=Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._next_circuit_breaker()` è `._next_circuit_breaker()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._next_circuit_breaker()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._next_circuit_breaker()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._next_circuit_breaker()` è `L104`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._next_circuit_breaker()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._next_circuit_breaker()` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._next_circuit_breaker()` è `._next_circuit_breaker()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._next_circuit_breaker()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._next_circuit_breaker()` è `mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker`. Nel grafo unificato, il nodo canonico `._next_circuit_breaker()` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker`.
<!-- GRAPH_EDGE graph=unificata index=303 role=outgoing -->
Nel grafo unificato, il nodo canonico `._next_circuit_breaker()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.generate_entry()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. La relazione tecnica esplicita è la seguente: `._next_circuit_breaker()` chiama `.generate_entry()`. Nel grafo unificato, il tipo dichiarato dell'arco 303 che coinvolge il nodo canonico `._next_circuit_breaker()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 303 che coinvolge il nodo canonico `._next_circuit_breaker()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 303 che coinvolge il nodo canonico `._next_circuit_breaker()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 303 che coinvolge il nodo canonico `._next_circuit_breaker()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 303 che coinvolge il nodo canonico `._next_circuit_breaker()` è `L220`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 303 che coinvolge il nodo canonico `._next_circuit_breaker()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 303 che coinvolge il nodo canonico `._next_circuit_breaker()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 303 che coinvolge il nodo canonico `._next_circuit_breaker()` è `1.0`. Nel grafo unificato, l'arco 303 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker` per la relazione che coinvolge il nodo canonico `._next_circuit_breaker()`. Nel grafo unificato, l'arco 303 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `._next_circuit_breaker()`.
<!-- GRAPH_EDGE graph=unificata index=304 role=outgoing -->
Nel grafo unificato, il nodo canonico `._next_circuit_breaker()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Simula transizioni del circuit breaker.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker` all'ID `Request-Dataset-Generator::mock_middleware_log_rationale_105`. La relazione tecnica esplicita è la seguente: `._next_circuit_breaker()` costituisce una motivazione per `Simula transizioni del circuit breaker.`. Nel grafo unificato, il tipo dichiarato dell'arco 304 che coinvolge il nodo canonico `._next_circuit_breaker()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 304 che coinvolge il nodo canonico `._next_circuit_breaker()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 304 che coinvolge il nodo canonico `._next_circuit_breaker()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 304 che coinvolge il nodo canonico `._next_circuit_breaker()` è `L105`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 304 che coinvolge il nodo canonico `._next_circuit_breaker()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 304 che coinvolge il nodo canonico `._next_circuit_breaker()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 304 che coinvolge il nodo canonico `._next_circuit_breaker()` è `1.0`. Nel grafo unificato, l'arco 304 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker` per la relazione che coinvolge il nodo canonico `._next_circuit_breaker()`. Nel grafo unificato, l'arco 304 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_rationale_105` per la relazione che coinvolge il nodo canonico `._next_circuit_breaker()`.
<!-- GRAPH_EDGE graph=unificata index=291 role=incoming -->
Nel grafo unificato, il nodo canonico `._next_circuit_breaker()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._next_circuit_breaker()`. Nel grafo unificato, il tipo dichiarato dell'arco 291 che coinvolge il nodo canonico `._next_circuit_breaker()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 291 che coinvolge il nodo canonico `._next_circuit_breaker()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 291 che coinvolge il nodo canonico `._next_circuit_breaker()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 291 che coinvolge il nodo canonico `._next_circuit_breaker()` è `L104`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 291 che coinvolge il nodo canonico `._next_circuit_breaker()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 291 che coinvolge il nodo canonico `._next_circuit_breaker()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 291 che coinvolge il nodo canonico `._next_circuit_breaker()` è `1.0`. Nel grafo unificato, l'arco 291 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `._next_circuit_breaker()`. Nel grafo unificato, l'arco 291 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker` per la relazione che coinvolge il nodo canonico `._next_circuit_breaker()`.

### ._next_connections()

<!-- GRAPH_NODE graph=codice index=135 id=mock_middleware_log_mockmiddlewareloggenerator_next_connections -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._next_connections()` è `._next_connections()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._next_connections()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._next_connections()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._next_connections()` è `L97`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._next_connections()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._next_connections()` possiede l'identificatore esatto `mock_middleware_log_mockmiddlewareloggenerator_next_connections`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._next_connections()` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._next_connections()` è `._next_connections()`.
Nel grafo del codice RDG, il nodo canonico `._next_connections()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=292 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._next_connections()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_connections`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._next_connections()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 292 che coinvolge il nodo canonico `._next_connections()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 292 che coinvolge il nodo canonico `._next_connections()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 292 che coinvolge il nodo canonico `._next_connections()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 292 che coinvolge il nodo canonico `._next_connections()` è `L97`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 292 che coinvolge il nodo canonico `._next_connections()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 292 che coinvolge il nodo canonico `._next_connections()` è `ast`. Nel grafo del codice RDG, l'arco 292 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `._next_connections()`. Nel grafo del codice RDG, l'arco 292 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_connections` per la relazione che coinvolge il nodo canonico `._next_connections()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 292 che coinvolge il nodo canonico `._next_connections()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=301 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._next_connections()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.generate_entry()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_connections`. La relazione tecnica esplicita è la seguente: `.generate_entry()` chiama `._next_connections()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 301 che coinvolge il nodo canonico `._next_connections()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 301 che coinvolge il nodo canonico `._next_connections()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 301 che coinvolge il nodo canonico `._next_connections()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 301 che coinvolge il nodo canonico `._next_connections()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 301 che coinvolge il nodo canonico `._next_connections()` è `L210`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 301 che coinvolge il nodo canonico `._next_connections()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 301 che coinvolge il nodo canonico `._next_connections()` è `ast`. Nel grafo del codice RDG, l'arco 301 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `._next_connections()`. Nel grafo del codice RDG, l'arco 301 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_connections` per la relazione che coinvolge il nodo canonico `._next_connections()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 301 che coinvolge il nodo canonico `._next_connections()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=302 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._next_connections()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Connessioni attive con variazione graduale.`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_rationale_98` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_connections`. La relazione tecnica esplicita è la seguente: `Connessioni attive con variazione graduale.` costituisce una motivazione per `._next_connections()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 302 che coinvolge il nodo canonico `._next_connections()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 302 che coinvolge il nodo canonico `._next_connections()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 302 che coinvolge il nodo canonico `._next_connections()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 302 che coinvolge il nodo canonico `._next_connections()` è `L98`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 302 che coinvolge il nodo canonico `._next_connections()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 302 che coinvolge il nodo canonico `._next_connections()` è `ast`. Nel grafo del codice RDG, l'arco 302 registra come `source` l'identificatore `mock_middleware_log_rationale_98` per la relazione che coinvolge il nodo canonico `._next_connections()`. Nel grafo del codice RDG, l'arco 302 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_connections` per la relazione che coinvolge il nodo canonico `._next_connections()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 302 che coinvolge il nodo canonico `._next_connections()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=135 id=Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_connections -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._next_connections()` è `._next_connections()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._next_connections()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._next_connections()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._next_connections()` è `L97`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._next_connections()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._next_connections()` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._next_connections()` è `._next_connections()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._next_connections()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._next_connections()` è `mock_middleware_log_mockmiddlewareloggenerator_next_connections`. Nel grafo unificato, il nodo canonico `._next_connections()` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_connections`.
<!-- GRAPH_EDGE graph=unificata index=301 role=outgoing -->
Nel grafo unificato, il nodo canonico `._next_connections()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.generate_entry()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_connections` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. La relazione tecnica esplicita è la seguente: `._next_connections()` chiama `.generate_entry()`. Nel grafo unificato, il tipo dichiarato dell'arco 301 che coinvolge il nodo canonico `._next_connections()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 301 che coinvolge il nodo canonico `._next_connections()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 301 che coinvolge il nodo canonico `._next_connections()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 301 che coinvolge il nodo canonico `._next_connections()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 301 che coinvolge il nodo canonico `._next_connections()` è `L210`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 301 che coinvolge il nodo canonico `._next_connections()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 301 che coinvolge il nodo canonico `._next_connections()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 301 che coinvolge il nodo canonico `._next_connections()` è `1.0`. Nel grafo unificato, l'arco 301 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_connections` per la relazione che coinvolge il nodo canonico `._next_connections()`. Nel grafo unificato, l'arco 301 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `._next_connections()`.
<!-- GRAPH_EDGE graph=unificata index=302 role=outgoing -->
Nel grafo unificato, il nodo canonico `._next_connections()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Connessioni attive con variazione graduale.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_connections` all'ID `Request-Dataset-Generator::mock_middleware_log_rationale_98`. La relazione tecnica esplicita è la seguente: `._next_connections()` costituisce una motivazione per `Connessioni attive con variazione graduale.`. Nel grafo unificato, il tipo dichiarato dell'arco 302 che coinvolge il nodo canonico `._next_connections()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 302 che coinvolge il nodo canonico `._next_connections()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 302 che coinvolge il nodo canonico `._next_connections()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 302 che coinvolge il nodo canonico `._next_connections()` è `L98`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 302 che coinvolge il nodo canonico `._next_connections()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 302 che coinvolge il nodo canonico `._next_connections()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 302 che coinvolge il nodo canonico `._next_connections()` è `1.0`. Nel grafo unificato, l'arco 302 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_connections` per la relazione che coinvolge il nodo canonico `._next_connections()`. Nel grafo unificato, l'arco 302 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_rationale_98` per la relazione che coinvolge il nodo canonico `._next_connections()`.
<!-- GRAPH_EDGE graph=unificata index=292 role=incoming -->
Nel grafo unificato, il nodo canonico `._next_connections()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_connections`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._next_connections()`. Nel grafo unificato, il tipo dichiarato dell'arco 292 che coinvolge il nodo canonico `._next_connections()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 292 che coinvolge il nodo canonico `._next_connections()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 292 che coinvolge il nodo canonico `._next_connections()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 292 che coinvolge il nodo canonico `._next_connections()` è `L97`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 292 che coinvolge il nodo canonico `._next_connections()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 292 che coinvolge il nodo canonico `._next_connections()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 292 che coinvolge il nodo canonico `._next_connections()` è `1.0`. Nel grafo unificato, l'arco 292 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `._next_connections()`. Nel grafo unificato, l'arco 292 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_connections` per la relazione che coinvolge il nodo canonico `._next_connections()`.

### ._next_cpu()

<!-- GRAPH_NODE graph=codice index=133 id=mock_middleware_log_mockmiddlewareloggenerator_next_cpu -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._next_cpu()` è `._next_cpu()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._next_cpu()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._next_cpu()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._next_cpu()` è `L83`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._next_cpu()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._next_cpu()` possiede l'identificatore esatto `mock_middleware_log_mockmiddlewareloggenerator_next_cpu`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._next_cpu()` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._next_cpu()` è `._next_cpu()`.
Nel grafo del codice RDG, il nodo canonico `._next_cpu()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=293 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._next_cpu()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_cpu`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._next_cpu()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 293 che coinvolge il nodo canonico `._next_cpu()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 293 che coinvolge il nodo canonico `._next_cpu()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 293 che coinvolge il nodo canonico `._next_cpu()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 293 che coinvolge il nodo canonico `._next_cpu()` è `L83`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 293 che coinvolge il nodo canonico `._next_cpu()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 293 che coinvolge il nodo canonico `._next_cpu()` è `ast`. Nel grafo del codice RDG, l'arco 293 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `._next_cpu()`. Nel grafo del codice RDG, l'arco 293 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_cpu` per la relazione che coinvolge il nodo canonico `._next_cpu()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 293 che coinvolge il nodo canonico `._next_cpu()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=297 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._next_cpu()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.generate_entry()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_cpu`. La relazione tecnica esplicita è la seguente: `.generate_entry()` chiama `._next_cpu()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 297 che coinvolge il nodo canonico `._next_cpu()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 297 che coinvolge il nodo canonico `._next_cpu()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 297 che coinvolge il nodo canonico `._next_cpu()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 297 che coinvolge il nodo canonico `._next_cpu()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 297 che coinvolge il nodo canonico `._next_cpu()` è `L250`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 297 che coinvolge il nodo canonico `._next_cpu()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 297 che coinvolge il nodo canonico `._next_cpu()` è `ast`. Nel grafo del codice RDG, l'arco 297 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `._next_cpu()`. Nel grafo del codice RDG, l'arco 297 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_cpu` per la relazione che coinvolge il nodo canonico `._next_cpu()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 297 che coinvolge il nodo canonico `._next_cpu()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=298 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._next_cpu()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `CPU correlata al numero di connessioni attive + rumore.`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_rationale_84` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_cpu`. La relazione tecnica esplicita è la seguente: `CPU correlata al numero di connessioni attive + rumore.` costituisce una motivazione per `._next_cpu()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 298 che coinvolge il nodo canonico `._next_cpu()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 298 che coinvolge il nodo canonico `._next_cpu()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 298 che coinvolge il nodo canonico `._next_cpu()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 298 che coinvolge il nodo canonico `._next_cpu()` è `L84`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 298 che coinvolge il nodo canonico `._next_cpu()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 298 che coinvolge il nodo canonico `._next_cpu()` è `ast`. Nel grafo del codice RDG, l'arco 298 registra come `source` l'identificatore `mock_middleware_log_rationale_84` per la relazione che coinvolge il nodo canonico `._next_cpu()`. Nel grafo del codice RDG, l'arco 298 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_cpu` per la relazione che coinvolge il nodo canonico `._next_cpu()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 298 che coinvolge il nodo canonico `._next_cpu()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=133 id=Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_cpu -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._next_cpu()` è `._next_cpu()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._next_cpu()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._next_cpu()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._next_cpu()` è `L83`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._next_cpu()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._next_cpu()` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._next_cpu()` è `._next_cpu()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._next_cpu()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._next_cpu()` è `mock_middleware_log_mockmiddlewareloggenerator_next_cpu`. Nel grafo unificato, il nodo canonico `._next_cpu()` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_cpu`.
<!-- GRAPH_EDGE graph=unificata index=297 role=outgoing -->
Nel grafo unificato, il nodo canonico `._next_cpu()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.generate_entry()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_cpu` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. La relazione tecnica esplicita è la seguente: `._next_cpu()` chiama `.generate_entry()`. Nel grafo unificato, il tipo dichiarato dell'arco 297 che coinvolge il nodo canonico `._next_cpu()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 297 che coinvolge il nodo canonico `._next_cpu()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 297 che coinvolge il nodo canonico `._next_cpu()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 297 che coinvolge il nodo canonico `._next_cpu()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 297 che coinvolge il nodo canonico `._next_cpu()` è `L250`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 297 che coinvolge il nodo canonico `._next_cpu()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 297 che coinvolge il nodo canonico `._next_cpu()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 297 che coinvolge il nodo canonico `._next_cpu()` è `1.0`. Nel grafo unificato, l'arco 297 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_cpu` per la relazione che coinvolge il nodo canonico `._next_cpu()`. Nel grafo unificato, l'arco 297 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `._next_cpu()`.
<!-- GRAPH_EDGE graph=unificata index=298 role=outgoing -->
Nel grafo unificato, il nodo canonico `._next_cpu()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `CPU correlata al numero di connessioni attive + rumore.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_cpu` all'ID `Request-Dataset-Generator::mock_middleware_log_rationale_84`. La relazione tecnica esplicita è la seguente: `._next_cpu()` costituisce una motivazione per `CPU correlata al numero di connessioni attive + rumore.`. Nel grafo unificato, il tipo dichiarato dell'arco 298 che coinvolge il nodo canonico `._next_cpu()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 298 che coinvolge il nodo canonico `._next_cpu()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 298 che coinvolge il nodo canonico `._next_cpu()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 298 che coinvolge il nodo canonico `._next_cpu()` è `L84`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 298 che coinvolge il nodo canonico `._next_cpu()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 298 che coinvolge il nodo canonico `._next_cpu()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 298 che coinvolge il nodo canonico `._next_cpu()` è `1.0`. Nel grafo unificato, l'arco 298 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_cpu` per la relazione che coinvolge il nodo canonico `._next_cpu()`. Nel grafo unificato, l'arco 298 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_rationale_84` per la relazione che coinvolge il nodo canonico `._next_cpu()`.
<!-- GRAPH_EDGE graph=unificata index=293 role=incoming -->
Nel grafo unificato, il nodo canonico `._next_cpu()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_cpu`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._next_cpu()`. Nel grafo unificato, il tipo dichiarato dell'arco 293 che coinvolge il nodo canonico `._next_cpu()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 293 che coinvolge il nodo canonico `._next_cpu()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 293 che coinvolge il nodo canonico `._next_cpu()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 293 che coinvolge il nodo canonico `._next_cpu()` è `L83`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 293 che coinvolge il nodo canonico `._next_cpu()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 293 che coinvolge il nodo canonico `._next_cpu()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 293 che coinvolge il nodo canonico `._next_cpu()` è `1.0`. Nel grafo unificato, l'arco 293 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `._next_cpu()`. Nel grafo unificato, l'arco 293 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_cpu` per la relazione che coinvolge il nodo canonico `._next_cpu()`.

### ._next_memory()

<!-- GRAPH_NODE graph=codice index=134 id=mock_middleware_log_mockmiddlewareloggenerator_next_memory -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._next_memory()` è `._next_memory()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._next_memory()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._next_memory()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._next_memory()` è `L89`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._next_memory()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._next_memory()` possiede l'identificatore esatto `mock_middleware_log_mockmiddlewareloggenerator_next_memory`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._next_memory()` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._next_memory()` è `._next_memory()`.
Nel grafo del codice RDG, il nodo canonico `._next_memory()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=294 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._next_memory()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_memory`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._next_memory()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 294 che coinvolge il nodo canonico `._next_memory()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 294 che coinvolge il nodo canonico `._next_memory()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 294 che coinvolge il nodo canonico `._next_memory()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 294 che coinvolge il nodo canonico `._next_memory()` è `L89`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 294 che coinvolge il nodo canonico `._next_memory()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 294 che coinvolge il nodo canonico `._next_memory()` è `ast`. Nel grafo del codice RDG, l'arco 294 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `._next_memory()`. Nel grafo del codice RDG, l'arco 294 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_memory` per la relazione che coinvolge il nodo canonico `._next_memory()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 294 che coinvolge il nodo canonico `._next_memory()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=299 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._next_memory()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.generate_entry()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_memory`. La relazione tecnica esplicita è la seguente: `.generate_entry()` chiama `._next_memory()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 299 che coinvolge il nodo canonico `._next_memory()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 299 che coinvolge il nodo canonico `._next_memory()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 299 che coinvolge il nodo canonico `._next_memory()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 299 che coinvolge il nodo canonico `._next_memory()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 299 che coinvolge il nodo canonico `._next_memory()` è `L251`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 299 che coinvolge il nodo canonico `._next_memory()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 299 che coinvolge il nodo canonico `._next_memory()` è `ast`. Nel grafo del codice RDG, l'arco 299 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `._next_memory()`. Nel grafo del codice RDG, l'arco 299 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_memory` per la relazione che coinvolge il nodo canonico `._next_memory()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 299 che coinvolge il nodo canonico `._next_memory()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=300 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._next_memory()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Memoria lentamente crescente con piccolo jitter.`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_rationale_90` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_memory`. La relazione tecnica esplicita è la seguente: `Memoria lentamente crescente con piccolo jitter.` costituisce una motivazione per `._next_memory()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 300 che coinvolge il nodo canonico `._next_memory()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 300 che coinvolge il nodo canonico `._next_memory()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 300 che coinvolge il nodo canonico `._next_memory()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 300 che coinvolge il nodo canonico `._next_memory()` è `L90`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 300 che coinvolge il nodo canonico `._next_memory()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 300 che coinvolge il nodo canonico `._next_memory()` è `ast`. Nel grafo del codice RDG, l'arco 300 registra come `source` l'identificatore `mock_middleware_log_rationale_90` per la relazione che coinvolge il nodo canonico `._next_memory()`. Nel grafo del codice RDG, l'arco 300 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_memory` per la relazione che coinvolge il nodo canonico `._next_memory()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 300 che coinvolge il nodo canonico `._next_memory()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=134 id=Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_memory -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._next_memory()` è `._next_memory()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._next_memory()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._next_memory()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._next_memory()` è `L89`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._next_memory()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._next_memory()` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._next_memory()` è `._next_memory()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._next_memory()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._next_memory()` è `mock_middleware_log_mockmiddlewareloggenerator_next_memory`. Nel grafo unificato, il nodo canonico `._next_memory()` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_memory`.
<!-- GRAPH_EDGE graph=unificata index=299 role=outgoing -->
Nel grafo unificato, il nodo canonico `._next_memory()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.generate_entry()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_memory` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. La relazione tecnica esplicita è la seguente: `._next_memory()` chiama `.generate_entry()`. Nel grafo unificato, il tipo dichiarato dell'arco 299 che coinvolge il nodo canonico `._next_memory()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 299 che coinvolge il nodo canonico `._next_memory()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 299 che coinvolge il nodo canonico `._next_memory()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 299 che coinvolge il nodo canonico `._next_memory()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 299 che coinvolge il nodo canonico `._next_memory()` è `L251`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 299 che coinvolge il nodo canonico `._next_memory()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 299 che coinvolge il nodo canonico `._next_memory()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 299 che coinvolge il nodo canonico `._next_memory()` è `1.0`. Nel grafo unificato, l'arco 299 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_memory` per la relazione che coinvolge il nodo canonico `._next_memory()`. Nel grafo unificato, l'arco 299 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `._next_memory()`.
<!-- GRAPH_EDGE graph=unificata index=300 role=outgoing -->
Nel grafo unificato, il nodo canonico `._next_memory()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Memoria lentamente crescente con piccolo jitter.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_memory` all'ID `Request-Dataset-Generator::mock_middleware_log_rationale_90`. La relazione tecnica esplicita è la seguente: `._next_memory()` costituisce una motivazione per `Memoria lentamente crescente con piccolo jitter.`. Nel grafo unificato, il tipo dichiarato dell'arco 300 che coinvolge il nodo canonico `._next_memory()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 300 che coinvolge il nodo canonico `._next_memory()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 300 che coinvolge il nodo canonico `._next_memory()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 300 che coinvolge il nodo canonico `._next_memory()` è `L90`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 300 che coinvolge il nodo canonico `._next_memory()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 300 che coinvolge il nodo canonico `._next_memory()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 300 che coinvolge il nodo canonico `._next_memory()` è `1.0`. Nel grafo unificato, l'arco 300 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_memory` per la relazione che coinvolge il nodo canonico `._next_memory()`. Nel grafo unificato, l'arco 300 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_rationale_90` per la relazione che coinvolge il nodo canonico `._next_memory()`.
<!-- GRAPH_EDGE graph=unificata index=294 role=incoming -->
Nel grafo unificato, il nodo canonico `._next_memory()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_memory`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._next_memory()`. Nel grafo unificato, il tipo dichiarato dell'arco 294 che coinvolge il nodo canonico `._next_memory()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 294 che coinvolge il nodo canonico `._next_memory()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 294 che coinvolge il nodo canonico `._next_memory()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 294 che coinvolge il nodo canonico `._next_memory()` è `L89`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 294 che coinvolge il nodo canonico `._next_memory()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 294 che coinvolge il nodo canonico `._next_memory()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 294 che coinvolge il nodo canonico `._next_memory()` è `1.0`. Nel grafo unificato, l'arco 294 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `._next_memory()`. Nel grafo unificato, l'arco 294 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_memory` per la relazione che coinvolge il nodo canonico `._next_memory()`.

### .generate_entry()

<!-- GRAPH_NODE graph=codice index=139 id=mock_middleware_log_mockmiddlewareloggenerator_generate_entry -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.generate_entry()` è `.generate_entry()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.generate_entry()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.generate_entry()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.generate_entry()` è `L188`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.generate_entry()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.generate_entry()` possiede l'identificatore esatto `mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.generate_entry()` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.generate_entry()` è `.generate_entry()`.
<!-- GRAPH_EDGE graph=codice index=297 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.generate_entry()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._next_cpu()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_cpu`. La relazione tecnica esplicita è la seguente: `.generate_entry()` chiama `._next_cpu()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 297 che coinvolge il nodo canonico `.generate_entry()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 297 che coinvolge il nodo canonico `.generate_entry()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 297 che coinvolge il nodo canonico `.generate_entry()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 297 che coinvolge il nodo canonico `.generate_entry()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 297 che coinvolge il nodo canonico `.generate_entry()` è `L250`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 297 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 297 che coinvolge il nodo canonico `.generate_entry()` è `ast`. Nel grafo del codice RDG, l'arco 297 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo del codice RDG, l'arco 297 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_cpu` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 297 che coinvolge il nodo canonico `.generate_entry()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=299 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.generate_entry()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._next_memory()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_memory`. La relazione tecnica esplicita è la seguente: `.generate_entry()` chiama `._next_memory()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 299 che coinvolge il nodo canonico `.generate_entry()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 299 che coinvolge il nodo canonico `.generate_entry()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 299 che coinvolge il nodo canonico `.generate_entry()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 299 che coinvolge il nodo canonico `.generate_entry()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 299 che coinvolge il nodo canonico `.generate_entry()` è `L251`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 299 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 299 che coinvolge il nodo canonico `.generate_entry()` è `ast`. Nel grafo del codice RDG, l'arco 299 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo del codice RDG, l'arco 299 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_memory` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 299 che coinvolge il nodo canonico `.generate_entry()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=301 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.generate_entry()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._next_connections()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_connections`. La relazione tecnica esplicita è la seguente: `.generate_entry()` chiama `._next_connections()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 301 che coinvolge il nodo canonico `.generate_entry()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 301 che coinvolge il nodo canonico `.generate_entry()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 301 che coinvolge il nodo canonico `.generate_entry()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 301 che coinvolge il nodo canonico `.generate_entry()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 301 che coinvolge il nodo canonico `.generate_entry()` è `L210`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 301 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 301 che coinvolge il nodo canonico `.generate_entry()` è `ast`. Nel grafo del codice RDG, l'arco 301 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo del codice RDG, l'arco 301 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_connections` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 301 che coinvolge il nodo canonico `.generate_entry()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=303 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.generate_entry()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._next_circuit_breaker()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker`. La relazione tecnica esplicita è la seguente: `.generate_entry()` chiama `._next_circuit_breaker()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 303 che coinvolge il nodo canonico `.generate_entry()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 303 che coinvolge il nodo canonico `.generate_entry()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 303 che coinvolge il nodo canonico `.generate_entry()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 303 che coinvolge il nodo canonico `.generate_entry()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 303 che coinvolge il nodo canonico `.generate_entry()` è `L220`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 303 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 303 che coinvolge il nodo canonico `.generate_entry()` è `ast`. Nel grafo del codice RDG, l'arco 303 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo del codice RDG, l'arco 303 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 303 che coinvolge il nodo canonico `.generate_entry()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=305 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.generate_entry()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._decompose_latency()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` all'ID `mock_middleware_log_mockmiddlewareloggenerator_decompose_latency`. La relazione tecnica esplicita è la seguente: `.generate_entry()` chiama `._decompose_latency()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 305 che coinvolge il nodo canonico `.generate_entry()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 305 che coinvolge il nodo canonico `.generate_entry()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 305 che coinvolge il nodo canonico `.generate_entry()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 305 che coinvolge il nodo canonico `.generate_entry()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 305 che coinvolge il nodo canonico `.generate_entry()` è `L198`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 305 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 305 che coinvolge il nodo canonico `.generate_entry()` è `ast`. Nel grafo del codice RDG, l'arco 305 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo del codice RDG, l'arco 305 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_decompose_latency` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 305 che coinvolge il nodo canonico `.generate_entry()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=307 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.generate_entry()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._inject_anomaly()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` all'ID `mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly`. La relazione tecnica esplicita è la seguente: `.generate_entry()` chiama `._inject_anomaly()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 307 che coinvolge il nodo canonico `.generate_entry()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 307 che coinvolge il nodo canonico `.generate_entry()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 307 che coinvolge il nodo canonico `.generate_entry()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 307 che coinvolge il nodo canonico `.generate_entry()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 307 che coinvolge il nodo canonico `.generate_entry()` è `L201`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 307 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 307 che coinvolge il nodo canonico `.generate_entry()` è `ast`. Nel grafo del codice RDG, l'arco 307 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo del codice RDG, l'arco 307 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 307 che coinvolge il nodo canonico `.generate_entry()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=288 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.generate_entry()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator` all'ID `mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `.generate_entry()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 288 che coinvolge il nodo canonico `.generate_entry()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 288 che coinvolge il nodo canonico `.generate_entry()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 288 che coinvolge il nodo canonico `.generate_entry()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 288 che coinvolge il nodo canonico `.generate_entry()` è `L188`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 288 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 288 che coinvolge il nodo canonico `.generate_entry()` è `ast`. Nel grafo del codice RDG, l'arco 288 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo del codice RDG, l'arco 288 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 288 che coinvolge il nodo canonico `.generate_entry()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=309 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.generate_entry()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.process_file()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator_process_file` all'ID `mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. La relazione tecnica esplicita è la seguente: `.process_file()` chiama `.generate_entry()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 309 che coinvolge il nodo canonico `.generate_entry()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 309 che coinvolge il nodo canonico `.generate_entry()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 309 che coinvolge il nodo canonico `.generate_entry()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 309 che coinvolge il nodo canonico `.generate_entry()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 309 che coinvolge il nodo canonico `.generate_entry()` è `L298`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 309 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 309 che coinvolge il nodo canonico `.generate_entry()` è `ast`. Nel grafo del codice RDG, l'arco 309 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_process_file` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo del codice RDG, l'arco 309 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 309 che coinvolge il nodo canonico `.generate_entry()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=310 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.generate_entry()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Genera una riga di log middleware da un evento response_received.`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_rationale_189` all'ID `mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. La relazione tecnica esplicita è la seguente: `Genera una riga di log middleware da un evento response_received.` costituisce una motivazione per `.generate_entry()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 310 che coinvolge il nodo canonico `.generate_entry()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 310 che coinvolge il nodo canonico `.generate_entry()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 310 che coinvolge il nodo canonico `.generate_entry()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 310 che coinvolge il nodo canonico `.generate_entry()` è `L189`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 310 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 310 che coinvolge il nodo canonico `.generate_entry()` è `ast`. Nel grafo del codice RDG, l'arco 310 registra come `source` l'identificatore `mock_middleware_log_rationale_189` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo del codice RDG, l'arco 310 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 310 che coinvolge il nodo canonico `.generate_entry()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=139 id=Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.generate_entry()` è `.generate_entry()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.generate_entry()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.generate_entry()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.generate_entry()` è `L188`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.generate_entry()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.generate_entry()` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.generate_entry()` è `.generate_entry()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.generate_entry()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.generate_entry()` è `mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. Nel grafo unificato, il nodo canonico `.generate_entry()` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry`.
<!-- GRAPH_EDGE graph=unificata index=309 role=outgoing -->
Nel grafo unificato, il nodo canonico `.generate_entry()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.process_file()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_process_file`. La relazione tecnica esplicita è la seguente: `.generate_entry()` chiama `.process_file()`. Nel grafo unificato, il tipo dichiarato dell'arco 309 che coinvolge il nodo canonico `.generate_entry()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 309 che coinvolge il nodo canonico `.generate_entry()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 309 che coinvolge il nodo canonico `.generate_entry()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 309 che coinvolge il nodo canonico `.generate_entry()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 309 che coinvolge il nodo canonico `.generate_entry()` è `L298`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 309 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 309 che coinvolge il nodo canonico `.generate_entry()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 309 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo unificato, l'arco 309 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo unificato, l'arco 309 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_process_file` per la relazione che coinvolge il nodo canonico `.generate_entry()`.
<!-- GRAPH_EDGE graph=unificata index=310 role=outgoing -->
Nel grafo unificato, il nodo canonico `.generate_entry()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Genera una riga di log middleware da un evento response_received.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` all'ID `Request-Dataset-Generator::mock_middleware_log_rationale_189`. La relazione tecnica esplicita è la seguente: `.generate_entry()` costituisce una motivazione per `Genera una riga di log middleware da un evento response_received.`. Nel grafo unificato, il tipo dichiarato dell'arco 310 che coinvolge il nodo canonico `.generate_entry()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 310 che coinvolge il nodo canonico `.generate_entry()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 310 che coinvolge il nodo canonico `.generate_entry()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 310 che coinvolge il nodo canonico `.generate_entry()` è `L189`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 310 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 310 che coinvolge il nodo canonico `.generate_entry()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 310 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo unificato, l'arco 310 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo unificato, l'arco 310 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_rationale_189` per la relazione che coinvolge il nodo canonico `.generate_entry()`.
<!-- GRAPH_EDGE graph=unificata index=288 role=incoming -->
Nel grafo unificato, il nodo canonico `.generate_entry()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `.generate_entry()`. Nel grafo unificato, il tipo dichiarato dell'arco 288 che coinvolge il nodo canonico `.generate_entry()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 288 che coinvolge il nodo canonico `.generate_entry()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 288 che coinvolge il nodo canonico `.generate_entry()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 288 che coinvolge il nodo canonico `.generate_entry()` è `L188`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 288 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 288 che coinvolge il nodo canonico `.generate_entry()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 288 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo unificato, l'arco 288 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo unificato, l'arco 288 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `.generate_entry()`.
<!-- GRAPH_EDGE graph=unificata index=297 role=incoming -->
Nel grafo unificato, il nodo canonico `.generate_entry()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._next_cpu()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_cpu` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. La relazione tecnica esplicita è la seguente: `._next_cpu()` chiama `.generate_entry()`. Nel grafo unificato, il tipo dichiarato dell'arco 297 che coinvolge il nodo canonico `.generate_entry()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 297 che coinvolge il nodo canonico `.generate_entry()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 297 che coinvolge il nodo canonico `.generate_entry()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 297 che coinvolge il nodo canonico `.generate_entry()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 297 che coinvolge il nodo canonico `.generate_entry()` è `L250`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 297 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 297 che coinvolge il nodo canonico `.generate_entry()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 297 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo unificato, l'arco 297 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_cpu` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo unificato, l'arco 297 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `.generate_entry()`.
<!-- GRAPH_EDGE graph=unificata index=299 role=incoming -->
Nel grafo unificato, il nodo canonico `.generate_entry()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._next_memory()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_memory` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. La relazione tecnica esplicita è la seguente: `._next_memory()` chiama `.generate_entry()`. Nel grafo unificato, il tipo dichiarato dell'arco 299 che coinvolge il nodo canonico `.generate_entry()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 299 che coinvolge il nodo canonico `.generate_entry()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 299 che coinvolge il nodo canonico `.generate_entry()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 299 che coinvolge il nodo canonico `.generate_entry()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 299 che coinvolge il nodo canonico `.generate_entry()` è `L251`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 299 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 299 che coinvolge il nodo canonico `.generate_entry()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 299 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo unificato, l'arco 299 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_memory` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo unificato, l'arco 299 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `.generate_entry()`.
<!-- GRAPH_EDGE graph=unificata index=301 role=incoming -->
Nel grafo unificato, il nodo canonico `.generate_entry()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._next_connections()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_connections` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. La relazione tecnica esplicita è la seguente: `._next_connections()` chiama `.generate_entry()`. Nel grafo unificato, il tipo dichiarato dell'arco 301 che coinvolge il nodo canonico `.generate_entry()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 301 che coinvolge il nodo canonico `.generate_entry()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 301 che coinvolge il nodo canonico `.generate_entry()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 301 che coinvolge il nodo canonico `.generate_entry()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 301 che coinvolge il nodo canonico `.generate_entry()` è `L210`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 301 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 301 che coinvolge il nodo canonico `.generate_entry()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 301 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo unificato, l'arco 301 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_connections` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo unificato, l'arco 301 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `.generate_entry()`.
<!-- GRAPH_EDGE graph=unificata index=303 role=incoming -->
Nel grafo unificato, il nodo canonico `.generate_entry()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._next_circuit_breaker()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. La relazione tecnica esplicita è la seguente: `._next_circuit_breaker()` chiama `.generate_entry()`. Nel grafo unificato, il tipo dichiarato dell'arco 303 che coinvolge il nodo canonico `.generate_entry()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 303 che coinvolge il nodo canonico `.generate_entry()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 303 che coinvolge il nodo canonico `.generate_entry()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 303 che coinvolge il nodo canonico `.generate_entry()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 303 che coinvolge il nodo canonico `.generate_entry()` è `L220`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 303 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 303 che coinvolge il nodo canonico `.generate_entry()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 303 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo unificato, l'arco 303 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo unificato, l'arco 303 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `.generate_entry()`.
<!-- GRAPH_EDGE graph=unificata index=305 role=incoming -->
Nel grafo unificato, il nodo canonico `.generate_entry()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._decompose_latency()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_decompose_latency` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. La relazione tecnica esplicita è la seguente: `._decompose_latency()` chiama `.generate_entry()`. Nel grafo unificato, il tipo dichiarato dell'arco 305 che coinvolge il nodo canonico `.generate_entry()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 305 che coinvolge il nodo canonico `.generate_entry()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 305 che coinvolge il nodo canonico `.generate_entry()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 305 che coinvolge il nodo canonico `.generate_entry()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 305 che coinvolge il nodo canonico `.generate_entry()` è `L198`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 305 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 305 che coinvolge il nodo canonico `.generate_entry()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 305 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo unificato, l'arco 305 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_decompose_latency` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo unificato, l'arco 305 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `.generate_entry()`.
<!-- GRAPH_EDGE graph=unificata index=307 role=incoming -->
Nel grafo unificato, il nodo canonico `.generate_entry()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._inject_anomaly()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. La relazione tecnica esplicita è la seguente: `._inject_anomaly()` chiama `.generate_entry()`. Nel grafo unificato, il tipo dichiarato dell'arco 307 che coinvolge il nodo canonico `.generate_entry()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 307 che coinvolge il nodo canonico `.generate_entry()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 307 che coinvolge il nodo canonico `.generate_entry()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 307 che coinvolge il nodo canonico `.generate_entry()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 307 che coinvolge il nodo canonico `.generate_entry()` è `L201`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 307 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 307 che coinvolge il nodo canonico `.generate_entry()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 307 che coinvolge il nodo canonico `.generate_entry()` è `1.0`. Nel grafo unificato, l'arco 307 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly` per la relazione che coinvolge il nodo canonico `.generate_entry()`. Nel grafo unificato, l'arco 307 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `.generate_entry()`.

### .process_file()

<!-- GRAPH_NODE graph=codice index=140 id=mock_middleware_log_mockmiddlewareloggenerator_process_file -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.process_file()` è `.process_file()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.process_file()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.process_file()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.process_file()` è `L262`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.process_file()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.process_file()` possiede l'identificatore esatto `mock_middleware_log_mockmiddlewareloggenerator_process_file`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.process_file()` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.process_file()` è `.process_file()`.
<!-- GRAPH_EDGE graph=codice index=309 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.process_file()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.generate_entry()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator_process_file` all'ID `mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. La relazione tecnica esplicita è la seguente: `.process_file()` chiama `.generate_entry()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 309 che coinvolge il nodo canonico `.process_file()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 309 che coinvolge il nodo canonico `.process_file()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 309 che coinvolge il nodo canonico `.process_file()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 309 che coinvolge il nodo canonico `.process_file()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 309 che coinvolge il nodo canonico `.process_file()` è `L298`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 309 che coinvolge il nodo canonico `.process_file()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 309 che coinvolge il nodo canonico `.process_file()` è `ast`. Nel grafo del codice RDG, l'arco 309 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_process_file` per la relazione che coinvolge il nodo canonico `.process_file()`. Nel grafo del codice RDG, l'arco 309 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `.process_file()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 309 che coinvolge il nodo canonico `.process_file()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=295 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.process_file()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator` all'ID `mock_middleware_log_mockmiddlewareloggenerator_process_file`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `.process_file()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 295 che coinvolge il nodo canonico `.process_file()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 295 che coinvolge il nodo canonico `.process_file()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 295 che coinvolge il nodo canonico `.process_file()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 295 che coinvolge il nodo canonico `.process_file()` è `L262`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 295 che coinvolge il nodo canonico `.process_file()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 295 che coinvolge il nodo canonico `.process_file()` è `ast`. Nel grafo del codice RDG, l'arco 295 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `.process_file()`. Nel grafo del codice RDG, l'arco 295 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_process_file` per la relazione che coinvolge il nodo canonico `.process_file()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 295 che coinvolge il nodo canonico `.process_file()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=311 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.process_file()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `main() [mock_middleware_log_main]`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_main` all'ID `mock_middleware_log_mockmiddlewareloggenerator_process_file`. La relazione tecnica esplicita è la seguente: `main() [mock_middleware_log_main]` chiama `.process_file()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 311 che coinvolge il nodo canonico `.process_file()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 311 che coinvolge il nodo canonico `.process_file()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 311 che coinvolge il nodo canonico `.process_file()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 311 che coinvolge il nodo canonico `.process_file()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 311 che coinvolge il nodo canonico `.process_file()` è `L372`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 311 che coinvolge il nodo canonico `.process_file()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 311 che coinvolge il nodo canonico `.process_file()` è `ast`. Nel grafo del codice RDG, l'arco 311 registra come `source` l'identificatore `mock_middleware_log_main` per la relazione che coinvolge il nodo canonico `.process_file()`. Nel grafo del codice RDG, l'arco 311 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_process_file` per la relazione che coinvolge il nodo canonico `.process_file()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 311 che coinvolge il nodo canonico `.process_file()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=312 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.process_file()` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_rationale_263` all'ID `mock_middleware_log_mockmiddlewareloggenerator_process_file`. La relazione tecnica esplicita è la seguente: `Legge il log framework in streaming e genera il log middleware.          Returns` costituisce una motivazione per `.process_file()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 312 che coinvolge il nodo canonico `.process_file()` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 312 che coinvolge il nodo canonico `.process_file()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 312 che coinvolge il nodo canonico `.process_file()` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 312 che coinvolge il nodo canonico `.process_file()` è `L263`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 312 che coinvolge il nodo canonico `.process_file()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 312 che coinvolge il nodo canonico `.process_file()` è `ast`. Nel grafo del codice RDG, l'arco 312 registra come `source` l'identificatore `mock_middleware_log_rationale_263` per la relazione che coinvolge il nodo canonico `.process_file()`. Nel grafo del codice RDG, l'arco 312 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_process_file` per la relazione che coinvolge il nodo canonico `.process_file()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 312 che coinvolge il nodo canonico `.process_file()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=140 id=Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_process_file -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.process_file()` è `.process_file()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.process_file()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.process_file()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.process_file()` è `L262`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.process_file()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.process_file()` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.process_file()` è `.process_file()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.process_file()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.process_file()` è `mock_middleware_log_mockmiddlewareloggenerator_process_file`. Nel grafo unificato, il nodo canonico `.process_file()` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_process_file`.
<!-- GRAPH_EDGE graph=unificata index=311 role=outgoing -->
Nel grafo unificato, il nodo canonico `.process_file()` è la sorgente `source` della relazione `calls` verso il nodo canonico `main() [mock_middleware_log_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_process_file` all'ID `Request-Dataset-Generator::mock_middleware_log_main`. La relazione tecnica esplicita è la seguente: `.process_file()` chiama `main() [mock_middleware_log_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 311 che coinvolge il nodo canonico `.process_file()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 311 che coinvolge il nodo canonico `.process_file()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 311 che coinvolge il nodo canonico `.process_file()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 311 che coinvolge il nodo canonico `.process_file()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 311 che coinvolge il nodo canonico `.process_file()` è `L372`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 311 che coinvolge il nodo canonico `.process_file()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 311 che coinvolge il nodo canonico `.process_file()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 311 che coinvolge il nodo canonico `.process_file()` è `1.0`. Nel grafo unificato, l'arco 311 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_process_file` per la relazione che coinvolge il nodo canonico `.process_file()`. Nel grafo unificato, l'arco 311 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_main` per la relazione che coinvolge il nodo canonico `.process_file()`.
<!-- GRAPH_EDGE graph=unificata index=312 role=outgoing -->
Nel grafo unificato, il nodo canonico `.process_file()` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_process_file` all'ID `Request-Dataset-Generator::mock_middleware_log_rationale_263`. La relazione tecnica esplicita è la seguente: `.process_file()` costituisce una motivazione per `Legge il log framework in streaming e genera il log middleware.          Returns`. Nel grafo unificato, il tipo dichiarato dell'arco 312 che coinvolge il nodo canonico `.process_file()` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 312 che coinvolge il nodo canonico `.process_file()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 312 che coinvolge il nodo canonico `.process_file()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 312 che coinvolge il nodo canonico `.process_file()` è `L263`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 312 che coinvolge il nodo canonico `.process_file()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 312 che coinvolge il nodo canonico `.process_file()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 312 che coinvolge il nodo canonico `.process_file()` è `1.0`. Nel grafo unificato, l'arco 312 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_process_file` per la relazione che coinvolge il nodo canonico `.process_file()`. Nel grafo unificato, l'arco 312 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_rationale_263` per la relazione che coinvolge il nodo canonico `.process_file()`.
<!-- GRAPH_EDGE graph=unificata index=295 role=incoming -->
Nel grafo unificato, il nodo canonico `.process_file()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_process_file`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `.process_file()`. Nel grafo unificato, il tipo dichiarato dell'arco 295 che coinvolge il nodo canonico `.process_file()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 295 che coinvolge il nodo canonico `.process_file()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 295 che coinvolge il nodo canonico `.process_file()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 295 che coinvolge il nodo canonico `.process_file()` è `L262`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 295 che coinvolge il nodo canonico `.process_file()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 295 che coinvolge il nodo canonico `.process_file()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 295 che coinvolge il nodo canonico `.process_file()` è `1.0`. Nel grafo unificato, l'arco 295 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `.process_file()`. Nel grafo unificato, l'arco 295 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_process_file` per la relazione che coinvolge il nodo canonico `.process_file()`.
<!-- GRAPH_EDGE graph=unificata index=309 role=incoming -->
Nel grafo unificato, il nodo canonico `.process_file()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.generate_entry()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_process_file`. La relazione tecnica esplicita è la seguente: `.generate_entry()` chiama `.process_file()`. Nel grafo unificato, il tipo dichiarato dell'arco 309 che coinvolge il nodo canonico `.process_file()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 309 che coinvolge il nodo canonico `.process_file()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 309 che coinvolge il nodo canonico `.process_file()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 309 che coinvolge il nodo canonico `.process_file()` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 309 che coinvolge il nodo canonico `.process_file()` è `L298`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 309 che coinvolge il nodo canonico `.process_file()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 309 che coinvolge il nodo canonico `.process_file()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 309 che coinvolge il nodo canonico `.process_file()` è `1.0`. Nel grafo unificato, l'arco 309 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `.process_file()`. Nel grafo unificato, l'arco 309 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_process_file` per la relazione che coinvolge il nodo canonico `.process_file()`.

### _build_parser() [mock_middleware_log_build_parser]

<!-- GRAPH_NODE graph=codice index=141 id=mock_middleware_log_build_parser -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `_build_parser()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `L326`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` possiede l'identificatore esatto `mock_middleware_log_build_parser`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `_build_parser()`.
<!-- GRAPH_EDGE graph=codice index=313 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è la sorgente `source` della relazione `references` verso il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_build_parser` all'ID `mock_middleware_log_py_argumentparser`. La relazione tecnica esplicita è la seguente: `_build_parser() [mock_middleware_log_build_parser]` fa riferimento a `ArgumentParser [mock_middleware_log_py_argumentparser]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 313 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 313 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `return_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 313 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 313 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 313 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `L326`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 313 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 313 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `ast`. Nel grafo del codice RDG, l'arco 313 registra come `source` l'identificatore `mock_middleware_log_build_parser` per la relazione che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]`. Nel grafo del codice RDG, l'arco 313 registra come `target` l'identificatore `mock_middleware_log_py_argumentparser` per la relazione che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 313 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=283 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `mock_middleware_log.py`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log` all'ID `mock_middleware_log_build_parser`. La relazione tecnica esplicita è la seguente: `mock_middleware_log.py` contiene `_build_parser() [mock_middleware_log_build_parser]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 283 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 283 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 283 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 283 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `L326`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 283 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 283 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `ast`. Nel grafo del codice RDG, l'arco 283 registra come `source` l'identificatore `mock_middleware_log` per la relazione che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]`. Nel grafo del codice RDG, l'arco 283 registra come `target` l'identificatore `mock_middleware_log_build_parser` per la relazione che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 283 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=314 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `main() [mock_middleware_log_main]`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_main` all'ID `mock_middleware_log_build_parser`. La relazione tecnica esplicita è la seguente: `main() [mock_middleware_log_main]` chiama `_build_parser() [mock_middleware_log_build_parser]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 314 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 314 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 314 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 314 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 314 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `L360`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 314 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 314 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `ast`. Nel grafo del codice RDG, l'arco 314 registra come `source` l'identificatore `mock_middleware_log_main` per la relazione che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]`. Nel grafo del codice RDG, l'arco 314 registra come `target` l'identificatore `mock_middleware_log_build_parser` per la relazione che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 314 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=141 id=Request-Dataset-Generator::mock_middleware_log_build_parser -->
Nel grafo unificato, l'etichetta originale del nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `_build_parser()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `L326`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `_build_parser()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `mock_middleware_log_build_parser`. Nel grafo unificato, il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_build_parser`.
<!-- GRAPH_EDGE graph=unificata index=313 role=outgoing -->
Nel grafo unificato, il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è la sorgente `source` della relazione `references` verso il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_build_parser` all'ID `Request-Dataset-Generator::mock_middleware_log_py_argumentparser`. La relazione tecnica esplicita è la seguente: `_build_parser() [mock_middleware_log_build_parser]` fa riferimento a `ArgumentParser [mock_middleware_log_py_argumentparser]`. Nel grafo unificato, il tipo dichiarato dell'arco 313 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 313 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `return_type`. Nel grafo unificato, la confidenza categoriale dell'arco 313 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 313 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 313 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `L326`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 313 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 313 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 313 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `1.0`. Nel grafo unificato, l'arco 313 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_build_parser` per la relazione che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]`. Nel grafo unificato, l'arco 313 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_py_argumentparser` per la relazione che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]`.
<!-- GRAPH_EDGE graph=unificata index=314 role=outgoing -->
Nel grafo unificato, il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è la sorgente `source` della relazione `calls` verso il nodo canonico `main() [mock_middleware_log_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_build_parser` all'ID `Request-Dataset-Generator::mock_middleware_log_main`. La relazione tecnica esplicita è la seguente: `_build_parser() [mock_middleware_log_build_parser]` chiama `main() [mock_middleware_log_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 314 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 314 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 314 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 314 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 314 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `L360`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 314 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 314 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 314 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `1.0`. Nel grafo unificato, l'arco 314 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_build_parser` per la relazione che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]`. Nel grafo unificato, l'arco 314 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_main` per la relazione che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]`.
<!-- GRAPH_EDGE graph=unificata index=283 role=incoming -->
Nel grafo unificato, il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `mock_middleware_log.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log` all'ID `Request-Dataset-Generator::mock_middleware_log_build_parser`. La relazione tecnica esplicita è la seguente: `mock_middleware_log.py` contiene `_build_parser() [mock_middleware_log_build_parser]`. Nel grafo unificato, il tipo dichiarato dell'arco 283 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 283 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 283 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 283 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `L326`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 283 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 283 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 283 che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]` è `1.0`. Nel grafo unificato, l'arco 283 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log` per la relazione che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]`. Nel grafo unificato, l'arco 283 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_build_parser` per la relazione che coinvolge il nodo canonico `_build_parser() [mock_middleware_log_build_parser]`.

### ArgumentParser [mock_middleware_log_py_argumentparser]

<!-- GRAPH_NODE graph=codice index=142 id=mock_middleware_log_py_argumentparser -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `ArgumentParser`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è ``. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` possiede l'identificatore esatto `mock_middleware_log_py_argumentparser`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `argumentparser`.
Nel grafo del codice RDG, il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=313 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_build_parser() [mock_middleware_log_build_parser]`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_build_parser` all'ID `mock_middleware_log_py_argumentparser`. La relazione tecnica esplicita è la seguente: `_build_parser() [mock_middleware_log_build_parser]` fa riferimento a `ArgumentParser [mock_middleware_log_py_argumentparser]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 313 che coinvolge il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `references`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 313 che coinvolge il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `return_type`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 313 che coinvolge il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 313 che coinvolge il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 313 che coinvolge il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `L326`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 313 che coinvolge il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 313 che coinvolge il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `ast`. Nel grafo del codice RDG, l'arco 313 registra come `source` l'identificatore `mock_middleware_log_build_parser` per la relazione che coinvolge il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]`. Nel grafo del codice RDG, l'arco 313 registra come `target` l'identificatore `mock_middleware_log_py_argumentparser` per la relazione che coinvolge il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 313 che coinvolge il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=142 id=Request-Dataset-Generator::mock_middleware_log_py_argumentparser -->
Nel grafo unificato, l'etichetta originale del nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `ArgumentParser`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è ``. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è ``; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `argumentparser`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `mock_middleware_log_py_argumentparser`. Nel grafo unificato, il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_py_argumentparser`.
Nel grafo unificato, il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=313 role=incoming -->
Nel grafo unificato, il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `_build_parser() [mock_middleware_log_build_parser]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_build_parser` all'ID `Request-Dataset-Generator::mock_middleware_log_py_argumentparser`. La relazione tecnica esplicita è la seguente: `_build_parser() [mock_middleware_log_build_parser]` fa riferimento a `ArgumentParser [mock_middleware_log_py_argumentparser]`. Nel grafo unificato, il tipo dichiarato dell'arco 313 che coinvolge il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `references`. Nel grafo unificato, il contesto dichiarato per l'arco 313 che coinvolge il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `return_type`. Nel grafo unificato, la confidenza categoriale dell'arco 313 che coinvolge il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 313 che coinvolge il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 313 che coinvolge il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `L326`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 313 che coinvolge il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 313 che coinvolge il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 313 che coinvolge il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]` è `1.0`. Nel grafo unificato, l'arco 313 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_build_parser` per la relazione che coinvolge il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]`. Nel grafo unificato, l'arco 313 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_py_argumentparser` per la relazione che coinvolge il nodo canonico `ArgumentParser [mock_middleware_log_py_argumentparser]`.

### Connessioni attive con variazione graduale.

<!-- GRAPH_NODE graph=codice index=147 id=mock_middleware_log_rationale_98 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Connessioni attive con variazione graduale.` è `Connessioni attive con variazione graduale.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Connessioni attive con variazione graduale.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Connessioni attive con variazione graduale.` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Connessioni attive con variazione graduale.` è `L98`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Connessioni attive con variazione graduale.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Connessioni attive con variazione graduale.` possiede l'identificatore esatto `mock_middleware_log_rationale_98`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Connessioni attive con variazione graduale.` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Connessioni attive con variazione graduale.` è `connessioni attive con variazione graduale.`.
<!-- GRAPH_EDGE graph=codice index=302 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Connessioni attive con variazione graduale.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `._next_connections()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_rationale_98` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_connections`. La relazione tecnica esplicita è la seguente: `Connessioni attive con variazione graduale.` costituisce una motivazione per `._next_connections()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 302 che coinvolge il nodo canonico `Connessioni attive con variazione graduale.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 302 che coinvolge il nodo canonico `Connessioni attive con variazione graduale.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 302 che coinvolge il nodo canonico `Connessioni attive con variazione graduale.` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 302 che coinvolge il nodo canonico `Connessioni attive con variazione graduale.` è `L98`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 302 che coinvolge il nodo canonico `Connessioni attive con variazione graduale.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 302 che coinvolge il nodo canonico `Connessioni attive con variazione graduale.` è `ast`. Nel grafo del codice RDG, l'arco 302 registra come `source` l'identificatore `mock_middleware_log_rationale_98` per la relazione che coinvolge il nodo canonico `Connessioni attive con variazione graduale.`. Nel grafo del codice RDG, l'arco 302 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_connections` per la relazione che coinvolge il nodo canonico `Connessioni attive con variazione graduale.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 302 che coinvolge il nodo canonico `Connessioni attive con variazione graduale.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Connessioni attive con variazione graduale.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=147 id=Request-Dataset-Generator::mock_middleware_log_rationale_98 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Connessioni attive con variazione graduale.` è `Connessioni attive con variazione graduale.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Connessioni attive con variazione graduale.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Connessioni attive con variazione graduale.` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Connessioni attive con variazione graduale.` è `L98`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Connessioni attive con variazione graduale.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Connessioni attive con variazione graduale.` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Connessioni attive con variazione graduale.` è `connessioni attive con variazione graduale.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Connessioni attive con variazione graduale.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Connessioni attive con variazione graduale.` è `mock_middleware_log_rationale_98`. Nel grafo unificato, il nodo canonico `Connessioni attive con variazione graduale.` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_rationale_98`.
Nel grafo unificato, il nodo canonico `Connessioni attive con variazione graduale.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=302 role=incoming -->
Nel grafo unificato, il nodo canonico `Connessioni attive con variazione graduale.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `._next_connections()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_connections` all'ID `Request-Dataset-Generator::mock_middleware_log_rationale_98`. La relazione tecnica esplicita è la seguente: `._next_connections()` costituisce una motivazione per `Connessioni attive con variazione graduale.`. Nel grafo unificato, il tipo dichiarato dell'arco 302 che coinvolge il nodo canonico `Connessioni attive con variazione graduale.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 302 che coinvolge il nodo canonico `Connessioni attive con variazione graduale.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 302 che coinvolge il nodo canonico `Connessioni attive con variazione graduale.` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 302 che coinvolge il nodo canonico `Connessioni attive con variazione graduale.` è `L98`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 302 che coinvolge il nodo canonico `Connessioni attive con variazione graduale.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 302 che coinvolge il nodo canonico `Connessioni attive con variazione graduale.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 302 che coinvolge il nodo canonico `Connessioni attive con variazione graduale.` è `1.0`. Nel grafo unificato, l'arco 302 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_connections` per la relazione che coinvolge il nodo canonico `Connessioni attive con variazione graduale.`. Nel grafo unificato, l'arco 302 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_rationale_98` per la relazione che coinvolge il nodo canonico `Connessioni attive con variazione graduale.`.

### CPU correlata al numero di connessioni attive + rumore.

<!-- GRAPH_NODE graph=codice index=145 id=mock_middleware_log_rationale_84 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `CPU correlata al numero di connessioni attive + rumore.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `L84`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` possiede l'identificatore esatto `mock_middleware_log_rationale_84`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `cpu correlata al numero di connessioni attive + rumore.`.
<!-- GRAPH_EDGE graph=codice index=298 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `._next_cpu()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_rationale_84` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_cpu`. La relazione tecnica esplicita è la seguente: `CPU correlata al numero di connessioni attive + rumore.` costituisce una motivazione per `._next_cpu()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 298 che coinvolge il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 298 che coinvolge il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 298 che coinvolge il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 298 che coinvolge il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `L84`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 298 che coinvolge il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 298 che coinvolge il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `ast`. Nel grafo del codice RDG, l'arco 298 registra come `source` l'identificatore `mock_middleware_log_rationale_84` per la relazione che coinvolge il nodo canonico `CPU correlata al numero di connessioni attive + rumore.`. Nel grafo del codice RDG, l'arco 298 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_cpu` per la relazione che coinvolge il nodo canonico `CPU correlata al numero di connessioni attive + rumore.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 298 che coinvolge il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=145 id=Request-Dataset-Generator::mock_middleware_log_rationale_84 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `CPU correlata al numero di connessioni attive + rumore.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `L84`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `cpu correlata al numero di connessioni attive + rumore.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `mock_middleware_log_rationale_84`. Nel grafo unificato, il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_rationale_84`.
Nel grafo unificato, il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=298 role=incoming -->
Nel grafo unificato, il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `._next_cpu()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_cpu` all'ID `Request-Dataset-Generator::mock_middleware_log_rationale_84`. La relazione tecnica esplicita è la seguente: `._next_cpu()` costituisce una motivazione per `CPU correlata al numero di connessioni attive + rumore.`. Nel grafo unificato, il tipo dichiarato dell'arco 298 che coinvolge il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 298 che coinvolge il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 298 che coinvolge il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 298 che coinvolge il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `L84`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 298 che coinvolge il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 298 che coinvolge il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 298 che coinvolge il nodo canonico `CPU correlata al numero di connessioni attive + rumore.` è `1.0`. Nel grafo unificato, l'arco 298 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_cpu` per la relazione che coinvolge il nodo canonico `CPU correlata al numero di connessioni attive + rumore.`. Nel grafo unificato, l'arco 298 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_rationale_84` per la relazione che coinvolge il nodo canonico `CPU correlata al numero di connessioni attive + rumore.`.

### Genera log middleware mock da log framework.      Per ogni evento &quot;response_rece

<!-- GRAPH_NODE graph=codice index=144 id=mock_middleware_log_rationale_49 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `Genera log middleware mock da log framework.      Per ogni evento "response_rece`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `L49`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` possiede l'identificatore esatto `mock_middleware_log_rationale_49`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `genera log middleware mock da log framework.      per ogni evento "response_rece`.
<!-- GRAPH_EDGE graph=codice index=296 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_rationale_49` all'ID `mock_middleware_log_mockmiddlewareloggenerator`. La relazione tecnica esplicita è la seguente: `Genera log middleware mock da log framework.      Per ogni evento "response_rece` costituisce una motivazione per `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 296 che coinvolge il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 296 che coinvolge il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 296 che coinvolge il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 296 che coinvolge il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `L49`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 296 che coinvolge il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 296 che coinvolge il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `ast`. Nel grafo del codice RDG, l'arco 296 registra come `source` l'identificatore `mock_middleware_log_rationale_49` per la relazione che coinvolge il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece`. Nel grafo del codice RDG, l'arco 296 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 296 che coinvolge il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=144 id=Request-Dataset-Generator::mock_middleware_log_rationale_49 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `Genera log middleware mock da log framework.      Per ogni evento "response_rece`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `L49`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `genera log middleware mock da log framework.      per ogni evento "response_rece`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `mock_middleware_log_rationale_49`. Nel grafo unificato, il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_rationale_49`.
Nel grafo unificato, il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=296 role=incoming -->
Nel grafo unificato, il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_rationale_49`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` costituisce una motivazione per `Genera log middleware mock da log framework.      Per ogni evento "response_rece`. Nel grafo unificato, il tipo dichiarato dell'arco 296 che coinvolge il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 296 che coinvolge il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 296 che coinvolge il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 296 che coinvolge il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `L49`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 296 che coinvolge il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 296 che coinvolge il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 296 che coinvolge il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece` è `1.0`. Nel grafo unificato, l'arco 296 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece`. Nel grafo unificato, l'arco 296 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_rationale_49` per la relazione che coinvolge il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece`.

### Genera una riga di log middleware da un evento response_received.

<!-- GRAPH_NODE graph=codice index=151 id=mock_middleware_log_rationale_189 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Genera una riga di log middleware da un evento response_received.` è `Genera una riga di log middleware da un evento response_received.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Genera una riga di log middleware da un evento response_received.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Genera una riga di log middleware da un evento response_received.` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Genera una riga di log middleware da un evento response_received.` è `L189`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Genera una riga di log middleware da un evento response_received.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Genera una riga di log middleware da un evento response_received.` possiede l'identificatore esatto `mock_middleware_log_rationale_189`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Genera una riga di log middleware da un evento response_received.` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Genera una riga di log middleware da un evento response_received.` è `genera una riga di log middleware da un evento response_received.`.
<!-- GRAPH_EDGE graph=codice index=310 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Genera una riga di log middleware da un evento response_received.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `.generate_entry()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_rationale_189` all'ID `mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. La relazione tecnica esplicita è la seguente: `Genera una riga di log middleware da un evento response_received.` costituisce una motivazione per `.generate_entry()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 310 che coinvolge il nodo canonico `Genera una riga di log middleware da un evento response_received.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 310 che coinvolge il nodo canonico `Genera una riga di log middleware da un evento response_received.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 310 che coinvolge il nodo canonico `Genera una riga di log middleware da un evento response_received.` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 310 che coinvolge il nodo canonico `Genera una riga di log middleware da un evento response_received.` è `L189`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 310 che coinvolge il nodo canonico `Genera una riga di log middleware da un evento response_received.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 310 che coinvolge il nodo canonico `Genera una riga di log middleware da un evento response_received.` è `ast`. Nel grafo del codice RDG, l'arco 310 registra come `source` l'identificatore `mock_middleware_log_rationale_189` per la relazione che coinvolge il nodo canonico `Genera una riga di log middleware da un evento response_received.`. Nel grafo del codice RDG, l'arco 310 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `Genera una riga di log middleware da un evento response_received.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 310 che coinvolge il nodo canonico `Genera una riga di log middleware da un evento response_received.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Genera una riga di log middleware da un evento response_received.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=151 id=Request-Dataset-Generator::mock_middleware_log_rationale_189 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Genera una riga di log middleware da un evento response_received.` è `Genera una riga di log middleware da un evento response_received.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Genera una riga di log middleware da un evento response_received.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Genera una riga di log middleware da un evento response_received.` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Genera una riga di log middleware da un evento response_received.` è `L189`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Genera una riga di log middleware da un evento response_received.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Genera una riga di log middleware da un evento response_received.` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Genera una riga di log middleware da un evento response_received.` è `genera una riga di log middleware da un evento response_received.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Genera una riga di log middleware da un evento response_received.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Genera una riga di log middleware da un evento response_received.` è `mock_middleware_log_rationale_189`. Nel grafo unificato, il nodo canonico `Genera una riga di log middleware da un evento response_received.` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_rationale_189`.
Nel grafo unificato, il nodo canonico `Genera una riga di log middleware da un evento response_received.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=310 role=incoming -->
Nel grafo unificato, il nodo canonico `Genera una riga di log middleware da un evento response_received.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `.generate_entry()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` all'ID `Request-Dataset-Generator::mock_middleware_log_rationale_189`. La relazione tecnica esplicita è la seguente: `.generate_entry()` costituisce una motivazione per `Genera una riga di log middleware da un evento response_received.`. Nel grafo unificato, il tipo dichiarato dell'arco 310 che coinvolge il nodo canonico `Genera una riga di log middleware da un evento response_received.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 310 che coinvolge il nodo canonico `Genera una riga di log middleware da un evento response_received.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 310 che coinvolge il nodo canonico `Genera una riga di log middleware da un evento response_received.` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 310 che coinvolge il nodo canonico `Genera una riga di log middleware da un evento response_received.` è `L189`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 310 che coinvolge il nodo canonico `Genera una riga di log middleware da un evento response_received.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 310 che coinvolge il nodo canonico `Genera una riga di log middleware da un evento response_received.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 310 che coinvolge il nodo canonico `Genera una riga di log middleware da un evento response_received.` è `1.0`. Nel grafo unificato, l'arco 310 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `Genera una riga di log middleware da un evento response_received.`. Nel grafo unificato, l'arco 310 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_rationale_189` per la relazione che coinvolge il nodo canonico `Genera una riga di log middleware da un evento response_received.`.

### Inietta anomalie (spike, errori, timeout) con probabilità configurate.

<!-- GRAPH_NODE graph=codice index=150 id=mock_middleware_log_rationale_161 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `Inietta anomalie (spike, errori, timeout) con probabilità configurate.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `L161`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` possiede l'identificatore esatto `mock_middleware_log_rationale_161`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `inietta anomalie (spike, errori, timeout) con probabilita configurate.`.
<!-- GRAPH_EDGE graph=codice index=308 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `._inject_anomaly()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_rationale_161` all'ID `mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly`. La relazione tecnica esplicita è la seguente: `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` costituisce una motivazione per `._inject_anomaly()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 308 che coinvolge il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 308 che coinvolge il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 308 che coinvolge il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 308 che coinvolge il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `L161`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 308 che coinvolge il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 308 che coinvolge il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `ast`. Nel grafo del codice RDG, l'arco 308 registra come `source` l'identificatore `mock_middleware_log_rationale_161` per la relazione che coinvolge il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.`. Nel grafo del codice RDG, l'arco 308 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly` per la relazione che coinvolge il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 308 che coinvolge il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=150 id=Request-Dataset-Generator::mock_middleware_log_rationale_161 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `Inietta anomalie (spike, errori, timeout) con probabilità configurate.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `L161`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `inietta anomalie (spike, errori, timeout) con probabilita configurate.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `mock_middleware_log_rationale_161`. Nel grafo unificato, il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_rationale_161`.
Nel grafo unificato, il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=308 role=incoming -->
Nel grafo unificato, il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `._inject_anomaly()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly` all'ID `Request-Dataset-Generator::mock_middleware_log_rationale_161`. La relazione tecnica esplicita è la seguente: `._inject_anomaly()` costituisce una motivazione per `Inietta anomalie (spike, errori, timeout) con probabilità configurate.`. Nel grafo unificato, il tipo dichiarato dell'arco 308 che coinvolge il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 308 che coinvolge il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 308 che coinvolge il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 308 che coinvolge il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `L161`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 308 che coinvolge il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 308 che coinvolge il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 308 che coinvolge il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.` è `1.0`. Nel grafo unificato, l'arco 308 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly` per la relazione che coinvolge il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.`. Nel grafo unificato, l'arco 308 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_rationale_161` per la relazione che coinvolge il nodo canonico `Inietta anomalie (spike, errori, timeout) con probabilità configurate.`.

### Legge il log framework in streaming e genera il log middleware.          Returns

<!-- GRAPH_NODE graph=codice index=152 id=mock_middleware_log_rationale_263 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `Legge il log framework in streaming e genera il log middleware.          Returns`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `L263`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` possiede l'identificatore esatto `mock_middleware_log_rationale_263`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `legge il log framework in streaming e genera il log middleware.          returns`.
<!-- GRAPH_EDGE graph=codice index=312 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `.process_file()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_rationale_263` all'ID `mock_middleware_log_mockmiddlewareloggenerator_process_file`. La relazione tecnica esplicita è la seguente: `Legge il log framework in streaming e genera il log middleware.          Returns` costituisce una motivazione per `.process_file()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 312 che coinvolge il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 312 che coinvolge il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 312 che coinvolge il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 312 che coinvolge il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `L263`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 312 che coinvolge il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 312 che coinvolge il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `ast`. Nel grafo del codice RDG, l'arco 312 registra come `source` l'identificatore `mock_middleware_log_rationale_263` per la relazione che coinvolge il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns`. Nel grafo del codice RDG, l'arco 312 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_process_file` per la relazione che coinvolge il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 312 che coinvolge il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=152 id=Request-Dataset-Generator::mock_middleware_log_rationale_263 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `Legge il log framework in streaming e genera il log middleware.          Returns`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `L263`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `legge il log framework in streaming e genera il log middleware.          returns`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `mock_middleware_log_rationale_263`. Nel grafo unificato, il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_rationale_263`.
Nel grafo unificato, il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=312 role=incoming -->
Nel grafo unificato, il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `.process_file()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_process_file` all'ID `Request-Dataset-Generator::mock_middleware_log_rationale_263`. La relazione tecnica esplicita è la seguente: `.process_file()` costituisce una motivazione per `Legge il log framework in streaming e genera il log middleware.          Returns`. Nel grafo unificato, il tipo dichiarato dell'arco 312 che coinvolge il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 312 che coinvolge il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 312 che coinvolge il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 312 che coinvolge il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `L263`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 312 che coinvolge il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 312 che coinvolge il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 312 che coinvolge il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns` è `1.0`. Nel grafo unificato, l'arco 312 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_process_file` per la relazione che coinvolge il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns`. Nel grafo unificato, l'arco 312 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_rationale_263` per la relazione che coinvolge il nodo canonico `Legge il log framework in streaming e genera il log middleware.          Returns`.

### main() [mock_middleware_log_main]

<!-- GRAPH_NODE graph=codice index=143 id=mock_middleware_log_main -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `main() [mock_middleware_log_main]` è `main()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `main() [mock_middleware_log_main]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `main() [mock_middleware_log_main]` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `main() [mock_middleware_log_main]` è `L359`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `main() [mock_middleware_log_main]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `main() [mock_middleware_log_main]` possiede l'identificatore esatto `mock_middleware_log_main`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `main() [mock_middleware_log_main]` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `main() [mock_middleware_log_main]` è `main()`.
<!-- GRAPH_EDGE graph=codice index=286 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main() [mock_middleware_log_main]` è la sorgente `source` della relazione `calls` verso il nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_main` all'ID `mock_middleware_log_mockmiddlewareloggenerator`. La relazione tecnica esplicita è la seguente: `main() [mock_middleware_log_main]` chiama `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 286 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 286 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 286 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 286 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 286 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `L362`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 286 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 286 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `ast`. Nel grafo del codice RDG, l'arco 286 registra come `source` l'identificatore `mock_middleware_log_main` per la relazione che coinvolge il nodo canonico `main() [mock_middleware_log_main]`. Nel grafo del codice RDG, l'arco 286 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `main() [mock_middleware_log_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 286 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=311 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main() [mock_middleware_log_main]` è la sorgente `source` della relazione `calls` verso il nodo canonico `.process_file()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_main` all'ID `mock_middleware_log_mockmiddlewareloggenerator_process_file`. La relazione tecnica esplicita è la seguente: `main() [mock_middleware_log_main]` chiama `.process_file()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 311 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 311 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 311 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 311 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 311 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `L372`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 311 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 311 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `ast`. Nel grafo del codice RDG, l'arco 311 registra come `source` l'identificatore `mock_middleware_log_main` per la relazione che coinvolge il nodo canonico `main() [mock_middleware_log_main]`. Nel grafo del codice RDG, l'arco 311 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_process_file` per la relazione che coinvolge il nodo canonico `main() [mock_middleware_log_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 311 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=314 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `main() [mock_middleware_log_main]` è la sorgente `source` della relazione `calls` verso il nodo canonico `_build_parser() [mock_middleware_log_build_parser]`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_main` all'ID `mock_middleware_log_build_parser`. La relazione tecnica esplicita è la seguente: `main() [mock_middleware_log_main]` chiama `_build_parser() [mock_middleware_log_build_parser]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 314 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 314 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 314 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 314 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 314 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `L360`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 314 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 314 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `ast`. Nel grafo del codice RDG, l'arco 314 registra come `source` l'identificatore `mock_middleware_log_main` per la relazione che coinvolge il nodo canonico `main() [mock_middleware_log_main]`. Nel grafo del codice RDG, l'arco 314 registra come `target` l'identificatore `mock_middleware_log_build_parser` per la relazione che coinvolge il nodo canonico `main() [mock_middleware_log_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 314 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=284 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `main() [mock_middleware_log_main]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `mock_middleware_log.py`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log` all'ID `mock_middleware_log_main`. La relazione tecnica esplicita è la seguente: `mock_middleware_log.py` contiene `main() [mock_middleware_log_main]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 284 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 284 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 284 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 284 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `L359`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 284 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 284 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `ast`. Nel grafo del codice RDG, l'arco 284 registra come `source` l'identificatore `mock_middleware_log` per la relazione che coinvolge il nodo canonico `main() [mock_middleware_log_main]`. Nel grafo del codice RDG, l'arco 284 registra come `target` l'identificatore `mock_middleware_log_main` per la relazione che coinvolge il nodo canonico `main() [mock_middleware_log_main]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 284 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=143 id=Request-Dataset-Generator::mock_middleware_log_main -->
Nel grafo unificato, l'etichetta originale del nodo canonico `main() [mock_middleware_log_main]` è `main()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `main() [mock_middleware_log_main]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `main() [mock_middleware_log_main]` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `main() [mock_middleware_log_main]` è `L359`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `main() [mock_middleware_log_main]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `main() [mock_middleware_log_main]` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `main() [mock_middleware_log_main]` è `main()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `main() [mock_middleware_log_main]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `main() [mock_middleware_log_main]` è `mock_middleware_log_main`. Nel grafo unificato, il nodo canonico `main() [mock_middleware_log_main]` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_main`.
Nel grafo unificato, il nodo canonico `main() [mock_middleware_log_main]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=284 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [mock_middleware_log_main]` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `mock_middleware_log.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log` all'ID `Request-Dataset-Generator::mock_middleware_log_main`. La relazione tecnica esplicita è la seguente: `mock_middleware_log.py` contiene `main() [mock_middleware_log_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 284 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 284 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 284 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 284 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `L359`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 284 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 284 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 284 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `1.0`. Nel grafo unificato, l'arco 284 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log` per la relazione che coinvolge il nodo canonico `main() [mock_middleware_log_main]`. Nel grafo unificato, l'arco 284 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_main` per la relazione che coinvolge il nodo canonico `main() [mock_middleware_log_main]`.
<!-- GRAPH_EDGE graph=unificata index=286 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [mock_middleware_log_main]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_main`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` chiama `main() [mock_middleware_log_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 286 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 286 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 286 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 286 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 286 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `L362`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 286 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 286 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 286 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `1.0`. Nel grafo unificato, l'arco 286 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `main() [mock_middleware_log_main]`. Nel grafo unificato, l'arco 286 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_main` per la relazione che coinvolge il nodo canonico `main() [mock_middleware_log_main]`.
<!-- GRAPH_EDGE graph=unificata index=311 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [mock_middleware_log_main]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.process_file()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_process_file` all'ID `Request-Dataset-Generator::mock_middleware_log_main`. La relazione tecnica esplicita è la seguente: `.process_file()` chiama `main() [mock_middleware_log_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 311 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 311 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 311 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 311 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 311 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `L372`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 311 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 311 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 311 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `1.0`. Nel grafo unificato, l'arco 311 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_process_file` per la relazione che coinvolge il nodo canonico `main() [mock_middleware_log_main]`. Nel grafo unificato, l'arco 311 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_main` per la relazione che coinvolge il nodo canonico `main() [mock_middleware_log_main]`.
<!-- GRAPH_EDGE graph=unificata index=314 role=incoming -->
Nel grafo unificato, il nodo canonico `main() [mock_middleware_log_main]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `_build_parser() [mock_middleware_log_build_parser]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_build_parser` all'ID `Request-Dataset-Generator::mock_middleware_log_main`. La relazione tecnica esplicita è la seguente: `_build_parser() [mock_middleware_log_build_parser]` chiama `main() [mock_middleware_log_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 314 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 314 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 314 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 314 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 314 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `L360`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 314 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 314 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 314 che coinvolge il nodo canonico `main() [mock_middleware_log_main]` è `1.0`. Nel grafo unificato, l'arco 314 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_build_parser` per la relazione che coinvolge il nodo canonico `main() [mock_middleware_log_main]`. Nel grafo unificato, l'arco 314 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_main` per la relazione che coinvolge il nodo canonico `main() [mock_middleware_log_main]`.

### Memoria lentamente crescente con piccolo jitter.

<!-- GRAPH_NODE graph=codice index=146 id=mock_middleware_log_rationale_90 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `Memoria lentamente crescente con piccolo jitter.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Memoria lentamente crescente con piccolo jitter.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `L90`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Memoria lentamente crescente con piccolo jitter.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Memoria lentamente crescente con piccolo jitter.` possiede l'identificatore esatto `mock_middleware_log_rationale_90`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Memoria lentamente crescente con piccolo jitter.` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `memoria lentamente crescente con piccolo jitter.`.
<!-- GRAPH_EDGE graph=codice index=300 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `._next_memory()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_rationale_90` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_memory`. La relazione tecnica esplicita è la seguente: `Memoria lentamente crescente con piccolo jitter.` costituisce una motivazione per `._next_memory()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 300 che coinvolge il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 300 che coinvolge il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 300 che coinvolge il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 300 che coinvolge il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `L90`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 300 che coinvolge il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 300 che coinvolge il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `ast`. Nel grafo del codice RDG, l'arco 300 registra come `source` l'identificatore `mock_middleware_log_rationale_90` per la relazione che coinvolge il nodo canonico `Memoria lentamente crescente con piccolo jitter.`. Nel grafo del codice RDG, l'arco 300 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_memory` per la relazione che coinvolge il nodo canonico `Memoria lentamente crescente con piccolo jitter.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 300 che coinvolge il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Memoria lentamente crescente con piccolo jitter.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=146 id=Request-Dataset-Generator::mock_middleware_log_rationale_90 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `Memoria lentamente crescente con piccolo jitter.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Memoria lentamente crescente con piccolo jitter.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `L90`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Memoria lentamente crescente con piccolo jitter.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Memoria lentamente crescente con piccolo jitter.` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `memoria lentamente crescente con piccolo jitter.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Memoria lentamente crescente con piccolo jitter.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `mock_middleware_log_rationale_90`. Nel grafo unificato, il nodo canonico `Memoria lentamente crescente con piccolo jitter.` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_rationale_90`.
Nel grafo unificato, il nodo canonico `Memoria lentamente crescente con piccolo jitter.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=300 role=incoming -->
Nel grafo unificato, il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `._next_memory()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_memory` all'ID `Request-Dataset-Generator::mock_middleware_log_rationale_90`. La relazione tecnica esplicita è la seguente: `._next_memory()` costituisce una motivazione per `Memoria lentamente crescente con piccolo jitter.`. Nel grafo unificato, il tipo dichiarato dell'arco 300 che coinvolge il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 300 che coinvolge il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 300 che coinvolge il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 300 che coinvolge il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `L90`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 300 che coinvolge il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 300 che coinvolge il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 300 che coinvolge il nodo canonico `Memoria lentamente crescente con piccolo jitter.` è `1.0`. Nel grafo unificato, l'arco 300 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_memory` per la relazione che coinvolge il nodo canonico `Memoria lentamente crescente con piccolo jitter.`. Nel grafo unificato, l'arco 300 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_rationale_90` per la relazione che coinvolge il nodo canonico `Memoria lentamente crescente con piccolo jitter.`.

### mock_middleware_log.py

<!-- GRAPH_NODE graph=codice index=130 id=mock_middleware_log -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `mock_middleware_log.py` è `mock_middleware_log.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `mock_middleware_log.py` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `mock_middleware_log.py` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `mock_middleware_log.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `mock_middleware_log.py` vale `ast`. Nel grafo del codice RDG, il nodo canonico `mock_middleware_log.py` possiede l'identificatore esatto `mock_middleware_log`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `mock_middleware_log.py` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `mock_middleware_log.py` è `mock_middleware_log.py`.
<!-- GRAPH_EDGE graph=codice index=283 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `mock_middleware_log.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_build_parser() [mock_middleware_log_build_parser]`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log` all'ID `mock_middleware_log_build_parser`. La relazione tecnica esplicita è la seguente: `mock_middleware_log.py` contiene `_build_parser() [mock_middleware_log_build_parser]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 283 che coinvolge il nodo canonico `mock_middleware_log.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 283 che coinvolge il nodo canonico `mock_middleware_log.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 283 che coinvolge il nodo canonico `mock_middleware_log.py` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 283 che coinvolge il nodo canonico `mock_middleware_log.py` è `L326`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 283 che coinvolge il nodo canonico `mock_middleware_log.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 283 che coinvolge il nodo canonico `mock_middleware_log.py` è `ast`. Nel grafo del codice RDG, l'arco 283 registra come `source` l'identificatore `mock_middleware_log` per la relazione che coinvolge il nodo canonico `mock_middleware_log.py`. Nel grafo del codice RDG, l'arco 283 registra come `target` l'identificatore `mock_middleware_log_build_parser` per la relazione che coinvolge il nodo canonico `mock_middleware_log.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 283 che coinvolge il nodo canonico `mock_middleware_log.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=284 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `mock_middleware_log.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `main() [mock_middleware_log_main]`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log` all'ID `mock_middleware_log_main`. La relazione tecnica esplicita è la seguente: `mock_middleware_log.py` contiene `main() [mock_middleware_log_main]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 284 che coinvolge il nodo canonico `mock_middleware_log.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 284 che coinvolge il nodo canonico `mock_middleware_log.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 284 che coinvolge il nodo canonico `mock_middleware_log.py` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 284 che coinvolge il nodo canonico `mock_middleware_log.py` è `L359`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 284 che coinvolge il nodo canonico `mock_middleware_log.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 284 che coinvolge il nodo canonico `mock_middleware_log.py` è `ast`. Nel grafo del codice RDG, l'arco 284 registra come `source` l'identificatore `mock_middleware_log` per la relazione che coinvolge il nodo canonico `mock_middleware_log.py`. Nel grafo del codice RDG, l'arco 284 registra come `target` l'identificatore `mock_middleware_log_main` per la relazione che coinvolge il nodo canonico `mock_middleware_log.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 284 che coinvolge il nodo canonico `mock_middleware_log.py` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=285 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `mock_middleware_log.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log` all'ID `mock_middleware_log_mockmiddlewareloggenerator`. La relazione tecnica esplicita è la seguente: `mock_middleware_log.py` contiene `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 285 che coinvolge il nodo canonico `mock_middleware_log.py` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 285 che coinvolge il nodo canonico `mock_middleware_log.py` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 285 che coinvolge il nodo canonico `mock_middleware_log.py` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 285 che coinvolge il nodo canonico `mock_middleware_log.py` è `L48`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 285 che coinvolge il nodo canonico `mock_middleware_log.py` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 285 che coinvolge il nodo canonico `mock_middleware_log.py` è `ast`. Nel grafo del codice RDG, l'arco 285 registra come `source` l'identificatore `mock_middleware_log` per la relazione che coinvolge il nodo canonico `mock_middleware_log.py`. Nel grafo del codice RDG, l'arco 285 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `mock_middleware_log.py`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 285 che coinvolge il nodo canonico `mock_middleware_log.py` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `mock_middleware_log.py` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=130 id=Request-Dataset-Generator::mock_middleware_log -->
Nel grafo unificato, l'etichetta originale del nodo canonico `mock_middleware_log.py` è `mock_middleware_log.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `mock_middleware_log.py` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `mock_middleware_log.py` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `mock_middleware_log.py` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `mock_middleware_log.py` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `mock_middleware_log.py` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `mock_middleware_log.py` è `mock_middleware_log.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `mock_middleware_log.py` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `mock_middleware_log.py` è `mock_middleware_log`. Nel grafo unificato, il nodo canonico `mock_middleware_log.py` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log`.
<!-- GRAPH_EDGE graph=unificata index=283 role=outgoing -->
Nel grafo unificato, il nodo canonico `mock_middleware_log.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `_build_parser() [mock_middleware_log_build_parser]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log` all'ID `Request-Dataset-Generator::mock_middleware_log_build_parser`. La relazione tecnica esplicita è la seguente: `mock_middleware_log.py` contiene `_build_parser() [mock_middleware_log_build_parser]`. Nel grafo unificato, il tipo dichiarato dell'arco 283 che coinvolge il nodo canonico `mock_middleware_log.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 283 che coinvolge il nodo canonico `mock_middleware_log.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 283 che coinvolge il nodo canonico `mock_middleware_log.py` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 283 che coinvolge il nodo canonico `mock_middleware_log.py` è `L326`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 283 che coinvolge il nodo canonico `mock_middleware_log.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 283 che coinvolge il nodo canonico `mock_middleware_log.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 283 che coinvolge il nodo canonico `mock_middleware_log.py` è `1.0`. Nel grafo unificato, l'arco 283 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log` per la relazione che coinvolge il nodo canonico `mock_middleware_log.py`. Nel grafo unificato, l'arco 283 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_build_parser` per la relazione che coinvolge il nodo canonico `mock_middleware_log.py`.
<!-- GRAPH_EDGE graph=unificata index=284 role=outgoing -->
Nel grafo unificato, il nodo canonico `mock_middleware_log.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `main() [mock_middleware_log_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log` all'ID `Request-Dataset-Generator::mock_middleware_log_main`. La relazione tecnica esplicita è la seguente: `mock_middleware_log.py` contiene `main() [mock_middleware_log_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 284 che coinvolge il nodo canonico `mock_middleware_log.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 284 che coinvolge il nodo canonico `mock_middleware_log.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 284 che coinvolge il nodo canonico `mock_middleware_log.py` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 284 che coinvolge il nodo canonico `mock_middleware_log.py` è `L359`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 284 che coinvolge il nodo canonico `mock_middleware_log.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 284 che coinvolge il nodo canonico `mock_middleware_log.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 284 che coinvolge il nodo canonico `mock_middleware_log.py` è `1.0`. Nel grafo unificato, l'arco 284 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log` per la relazione che coinvolge il nodo canonico `mock_middleware_log.py`. Nel grafo unificato, l'arco 284 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_main` per la relazione che coinvolge il nodo canonico `mock_middleware_log.py`.
<!-- GRAPH_EDGE graph=unificata index=285 role=outgoing -->
Nel grafo unificato, il nodo canonico `mock_middleware_log.py` è la sorgente `source` della relazione `contains` verso il nodo canonico `MockMiddlewareLogGenerator`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator`. La relazione tecnica esplicita è la seguente: `mock_middleware_log.py` contiene `MockMiddlewareLogGenerator`. Nel grafo unificato, il tipo dichiarato dell'arco 285 che coinvolge il nodo canonico `mock_middleware_log.py` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 285 che coinvolge il nodo canonico `mock_middleware_log.py` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 285 che coinvolge il nodo canonico `mock_middleware_log.py` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 285 che coinvolge il nodo canonico `mock_middleware_log.py` è `L48`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 285 che coinvolge il nodo canonico `mock_middleware_log.py` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 285 che coinvolge il nodo canonico `mock_middleware_log.py` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 285 che coinvolge il nodo canonico `mock_middleware_log.py` è `1.0`. Nel grafo unificato, l'arco 285 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log` per la relazione che coinvolge il nodo canonico `mock_middleware_log.py`. Nel grafo unificato, l'arco 285 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `mock_middleware_log.py`.
Nel grafo unificato, il nodo canonico `mock_middleware_log.py` non possiede archi entranti registrati nel campo `links`.

### MockMiddlewareLogGenerator

<!-- GRAPH_NODE graph=codice index=131 id=mock_middleware_log_mockmiddlewareloggenerator -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `MockMiddlewareLogGenerator` è `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `MockMiddlewareLogGenerator` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `MockMiddlewareLogGenerator` è `L48`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `MockMiddlewareLogGenerator` vale `ast`. Nel grafo del codice RDG, il nodo canonico `MockMiddlewareLogGenerator` possiede l'identificatore esatto `mock_middleware_log_mockmiddlewareloggenerator`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `MockMiddlewareLogGenerator` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `MockMiddlewareLogGenerator` è `mockmiddlewareloggenerator`.
<!-- GRAPH_EDGE graph=codice index=287 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `MockMiddlewareLogGenerator` è la sorgente `source` della relazione `method` verso il nodo canonico `._decompose_latency()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator` all'ID `mock_middleware_log_mockmiddlewareloggenerator_decompose_latency`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._decompose_latency()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 287 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 287 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 287 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 287 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L133`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 287 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 287 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo del codice RDG, l'arco 287 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, l'arco 287 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_decompose_latency` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 287 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=288 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `MockMiddlewareLogGenerator` è la sorgente `source` della relazione `method` verso il nodo canonico `.generate_entry()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator` all'ID `mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `.generate_entry()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 288 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 288 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 288 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 288 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L188`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 288 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 288 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo del codice RDG, l'arco 288 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, l'arco 288 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 288 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=289 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `MockMiddlewareLogGenerator` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator` all'ID `mock_middleware_log_mockmiddlewareloggenerator_init`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 289 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 289 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 289 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 289 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L60`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 289 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 289 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo del codice RDG, l'arco 289 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, l'arco 289 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_init` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 289 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=290 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `MockMiddlewareLogGenerator` è la sorgente `source` della relazione `method` verso il nodo canonico `._inject_anomaly()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator` all'ID `mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._inject_anomaly()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 290 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 290 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 290 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 290 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L154`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 290 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 290 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo del codice RDG, l'arco 290 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, l'arco 290 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 290 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=291 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `MockMiddlewareLogGenerator` è la sorgente `source` della relazione `method` verso il nodo canonico `._next_circuit_breaker()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._next_circuit_breaker()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 291 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 291 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 291 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 291 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L104`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 291 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 291 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo del codice RDG, l'arco 291 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, l'arco 291 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 291 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=292 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `MockMiddlewareLogGenerator` è la sorgente `source` della relazione `method` verso il nodo canonico `._next_connections()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_connections`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._next_connections()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 292 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 292 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 292 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 292 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L97`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 292 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 292 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo del codice RDG, l'arco 292 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, l'arco 292 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_connections` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 292 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=293 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `MockMiddlewareLogGenerator` è la sorgente `source` della relazione `method` verso il nodo canonico `._next_cpu()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_cpu`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._next_cpu()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 293 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 293 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 293 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 293 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L83`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 293 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 293 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo del codice RDG, l'arco 293 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, l'arco 293 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_cpu` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 293 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=294 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `MockMiddlewareLogGenerator` è la sorgente `source` della relazione `method` verso il nodo canonico `._next_memory()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_memory`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._next_memory()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 294 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 294 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 294 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 294 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L89`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 294 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 294 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo del codice RDG, l'arco 294 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, l'arco 294 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_memory` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 294 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=295 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `MockMiddlewareLogGenerator` è la sorgente `source` della relazione `method` verso il nodo canonico `.process_file()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_mockmiddlewareloggenerator` all'ID `mock_middleware_log_mockmiddlewareloggenerator_process_file`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `.process_file()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 295 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 295 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 295 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 295 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L262`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 295 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 295 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo del codice RDG, l'arco 295 registra come `source` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, l'arco 295 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_process_file` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 295 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=285 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `MockMiddlewareLogGenerator` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `mock_middleware_log.py`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log` all'ID `mock_middleware_log_mockmiddlewareloggenerator`. La relazione tecnica esplicita è la seguente: `mock_middleware_log.py` contiene `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 285 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 285 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 285 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 285 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L48`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 285 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 285 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo del codice RDG, l'arco 285 registra come `source` l'identificatore `mock_middleware_log` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, l'arco 285 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 285 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=286 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `MockMiddlewareLogGenerator` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `main() [mock_middleware_log_main]`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_main` all'ID `mock_middleware_log_mockmiddlewareloggenerator`. La relazione tecnica esplicita è la seguente: `main() [mock_middleware_log_main]` chiama `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 286 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 286 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 286 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 286 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 286 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L362`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 286 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 286 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo del codice RDG, l'arco 286 registra come `source` l'identificatore `mock_middleware_log_main` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, l'arco 286 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 286 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=296 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `MockMiddlewareLogGenerator` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_rationale_49` all'ID `mock_middleware_log_mockmiddlewareloggenerator`. La relazione tecnica esplicita è la seguente: `Genera log middleware mock da log framework.      Per ogni evento "response_rece` costituisce una motivazione per `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 296 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 296 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 296 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 296 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L49`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 296 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 296 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo del codice RDG, l'arco 296 registra come `source` l'identificatore `mock_middleware_log_rationale_49` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, l'arco 296 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 296 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=131 id=Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator -->
Nel grafo unificato, l'etichetta originale del nodo canonico `MockMiddlewareLogGenerator` è `MockMiddlewareLogGenerator`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `MockMiddlewareLogGenerator` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `MockMiddlewareLogGenerator` è `L48`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `MockMiddlewareLogGenerator` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `MockMiddlewareLogGenerator` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `MockMiddlewareLogGenerator` è `mockmiddlewareloggenerator`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `MockMiddlewareLogGenerator` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log_mockmiddlewareloggenerator`. Nel grafo unificato, il nodo canonico `MockMiddlewareLogGenerator` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator`.
<!-- GRAPH_EDGE graph=unificata index=286 role=outgoing -->
Nel grafo unificato, il nodo canonico `MockMiddlewareLogGenerator` è la sorgente `source` della relazione `calls` verso il nodo canonico `main() [mock_middleware_log_main]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_main`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` chiama `main() [mock_middleware_log_main]`. Nel grafo unificato, il tipo dichiarato dell'arco 286 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 286 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 286 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 286 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 286 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L362`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 286 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 286 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 286 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, l'arco 286 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo unificato, l'arco 286 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_main` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`.
<!-- GRAPH_EDGE graph=unificata index=287 role=outgoing -->
Nel grafo unificato, il nodo canonico `MockMiddlewareLogGenerator` è la sorgente `source` della relazione `method` verso il nodo canonico `._decompose_latency()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_decompose_latency`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._decompose_latency()`. Nel grafo unificato, il tipo dichiarato dell'arco 287 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 287 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 287 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 287 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L133`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 287 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 287 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 287 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, l'arco 287 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo unificato, l'arco 287 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_decompose_latency` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`.
<!-- GRAPH_EDGE graph=unificata index=288 role=outgoing -->
Nel grafo unificato, il nodo canonico `MockMiddlewareLogGenerator` è la sorgente `source` della relazione `method` verso il nodo canonico `.generate_entry()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `.generate_entry()`. Nel grafo unificato, il tipo dichiarato dell'arco 288 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 288 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 288 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 288 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L188`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 288 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 288 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 288 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, l'arco 288 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo unificato, l'arco 288 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_generate_entry` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`.
<!-- GRAPH_EDGE graph=unificata index=289 role=outgoing -->
Nel grafo unificato, il nodo canonico `MockMiddlewareLogGenerator` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_init`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `.__init__() [mock_middleware_log_mockmiddlewareloggenerator_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 289 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 289 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 289 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 289 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L60`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 289 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 289 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 289 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, l'arco 289 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo unificato, l'arco 289 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_init` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`.
<!-- GRAPH_EDGE graph=unificata index=290 role=outgoing -->
Nel grafo unificato, il nodo canonico `MockMiddlewareLogGenerator` è la sorgente `source` della relazione `method` verso il nodo canonico `._inject_anomaly()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._inject_anomaly()`. Nel grafo unificato, il tipo dichiarato dell'arco 290 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 290 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 290 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 290 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L154`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 290 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 290 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 290 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, l'arco 290 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo unificato, l'arco 290 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_inject_anomaly` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`.
<!-- GRAPH_EDGE graph=unificata index=291 role=outgoing -->
Nel grafo unificato, il nodo canonico `MockMiddlewareLogGenerator` è la sorgente `source` della relazione `method` verso il nodo canonico `._next_circuit_breaker()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._next_circuit_breaker()`. Nel grafo unificato, il tipo dichiarato dell'arco 291 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 291 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 291 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 291 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L104`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 291 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 291 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 291 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, l'arco 291 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo unificato, l'arco 291 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`.
<!-- GRAPH_EDGE graph=unificata index=292 role=outgoing -->
Nel grafo unificato, il nodo canonico `MockMiddlewareLogGenerator` è la sorgente `source` della relazione `method` verso il nodo canonico `._next_connections()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_connections`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._next_connections()`. Nel grafo unificato, il tipo dichiarato dell'arco 292 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 292 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 292 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 292 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L97`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 292 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 292 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 292 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, l'arco 292 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo unificato, l'arco 292 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_connections` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`.
<!-- GRAPH_EDGE graph=unificata index=293 role=outgoing -->
Nel grafo unificato, il nodo canonico `MockMiddlewareLogGenerator` è la sorgente `source` della relazione `method` verso il nodo canonico `._next_cpu()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_cpu`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._next_cpu()`. Nel grafo unificato, il tipo dichiarato dell'arco 293 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 293 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 293 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 293 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L83`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 293 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 293 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 293 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, l'arco 293 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo unificato, l'arco 293 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_cpu` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`.
<!-- GRAPH_EDGE graph=unificata index=294 role=outgoing -->
Nel grafo unificato, il nodo canonico `MockMiddlewareLogGenerator` è la sorgente `source` della relazione `method` verso il nodo canonico `._next_memory()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_memory`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `._next_memory()`. Nel grafo unificato, il tipo dichiarato dell'arco 294 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 294 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 294 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 294 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L89`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 294 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 294 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 294 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, l'arco 294 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo unificato, l'arco 294 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_memory` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`.
<!-- GRAPH_EDGE graph=unificata index=295 role=outgoing -->
Nel grafo unificato, il nodo canonico `MockMiddlewareLogGenerator` è la sorgente `source` della relazione `method` verso il nodo canonico `.process_file()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_process_file`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` registra come proprio metodo `.process_file()`. Nel grafo unificato, il tipo dichiarato dell'arco 295 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 295 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 295 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 295 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L262`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 295 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 295 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 295 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, l'arco 295 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo unificato, l'arco 295 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_process_file` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`.
<!-- GRAPH_EDGE graph=unificata index=296 role=outgoing -->
Nel grafo unificato, il nodo canonico `MockMiddlewareLogGenerator` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Genera log middleware mock da log framework.      Per ogni evento "response_rece`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` all'ID `Request-Dataset-Generator::mock_middleware_log_rationale_49`. La relazione tecnica esplicita è la seguente: `MockMiddlewareLogGenerator` costituisce una motivazione per `Genera log middleware mock da log framework.      Per ogni evento "response_rece`. Nel grafo unificato, il tipo dichiarato dell'arco 296 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 296 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 296 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 296 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L49`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 296 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 296 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 296 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, l'arco 296 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo unificato, l'arco 296 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_rationale_49` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`.
<!-- GRAPH_EDGE graph=unificata index=285 role=incoming -->
Nel grafo unificato, il nodo canonico `MockMiddlewareLogGenerator` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `mock_middleware_log.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log` all'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator`. La relazione tecnica esplicita è la seguente: `mock_middleware_log.py` contiene `MockMiddlewareLogGenerator`. Nel grafo unificato, il tipo dichiarato dell'arco 285 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 285 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 285 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 285 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `L48`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 285 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 285 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 285 che coinvolge il nodo canonico `MockMiddlewareLogGenerator` è `1.0`. Nel grafo unificato, l'arco 285 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`. Nel grafo unificato, l'arco 285 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator` per la relazione che coinvolge il nodo canonico `MockMiddlewareLogGenerator`.

### Scompone la latenza client in gateway_latency + upstream_latency.          Il ga

<!-- GRAPH_NODE graph=codice index=149 id=mock_middleware_log_rationale_134 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `L134`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` possiede l'identificatore esatto `mock_middleware_log_rationale_134`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `scompone la latenza client in gateway_latency + upstream_latency.          il ga`.
<!-- GRAPH_EDGE graph=codice index=306 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `._decompose_latency()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_rationale_134` all'ID `mock_middleware_log_mockmiddlewareloggenerator_decompose_latency`. La relazione tecnica esplicita è la seguente: `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` costituisce una motivazione per `._decompose_latency()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 306 che coinvolge il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 306 che coinvolge il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 306 che coinvolge il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 306 che coinvolge il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `L134`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 306 che coinvolge il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 306 che coinvolge il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `ast`. Nel grafo del codice RDG, l'arco 306 registra come `source` l'identificatore `mock_middleware_log_rationale_134` per la relazione che coinvolge il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga`. Nel grafo del codice RDG, l'arco 306 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_decompose_latency` per la relazione che coinvolge il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 306 che coinvolge il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=149 id=Request-Dataset-Generator::mock_middleware_log_rationale_134 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `L134`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `scompone la latenza client in gateway_latency + upstream_latency.          il ga`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `mock_middleware_log_rationale_134`. Nel grafo unificato, il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_rationale_134`.
Nel grafo unificato, il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=306 role=incoming -->
Nel grafo unificato, il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `._decompose_latency()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_decompose_latency` all'ID `Request-Dataset-Generator::mock_middleware_log_rationale_134`. La relazione tecnica esplicita è la seguente: `._decompose_latency()` costituisce una motivazione per `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga`. Nel grafo unificato, il tipo dichiarato dell'arco 306 che coinvolge il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 306 che coinvolge il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 306 che coinvolge il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 306 che coinvolge il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `L134`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 306 che coinvolge il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 306 che coinvolge il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 306 che coinvolge il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga` è `1.0`. Nel grafo unificato, l'arco 306 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_decompose_latency` per la relazione che coinvolge il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga`. Nel grafo unificato, l'arco 306 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_rationale_134` per la relazione che coinvolge il nodo canonico `Scompone la latenza client in gateway_latency + upstream_latency.          Il ga`.

### Simula transizioni del circuit breaker.

<!-- GRAPH_NODE graph=codice index=148 id=mock_middleware_log_rationale_105 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Simula transizioni del circuit breaker.` è `Simula transizioni del circuit breaker.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Simula transizioni del circuit breaker.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Simula transizioni del circuit breaker.` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Simula transizioni del circuit breaker.` è `L105`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Simula transizioni del circuit breaker.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Simula transizioni del circuit breaker.` possiede l'identificatore esatto `mock_middleware_log_rationale_105`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Simula transizioni del circuit breaker.` alla comunità numerica `12`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Simula transizioni del circuit breaker.` è `simula transizioni del circuit breaker.`.
<!-- GRAPH_EDGE graph=codice index=304 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Simula transizioni del circuit breaker.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `._next_circuit_breaker()`; l'orientamento registrato nel JSON va dall'ID `mock_middleware_log_rationale_105` all'ID `mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker`. La relazione tecnica esplicita è la seguente: `Simula transizioni del circuit breaker.` costituisce una motivazione per `._next_circuit_breaker()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 304 che coinvolge il nodo canonico `Simula transizioni del circuit breaker.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 304 che coinvolge il nodo canonico `Simula transizioni del circuit breaker.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 304 che coinvolge il nodo canonico `Simula transizioni del circuit breaker.` è `mock_middleware_log.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 304 che coinvolge il nodo canonico `Simula transizioni del circuit breaker.` è `L105`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 304 che coinvolge il nodo canonico `Simula transizioni del circuit breaker.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 304 che coinvolge il nodo canonico `Simula transizioni del circuit breaker.` è `ast`. Nel grafo del codice RDG, l'arco 304 registra come `source` l'identificatore `mock_middleware_log_rationale_105` per la relazione che coinvolge il nodo canonico `Simula transizioni del circuit breaker.`. Nel grafo del codice RDG, l'arco 304 registra come `target` l'identificatore `mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker` per la relazione che coinvolge il nodo canonico `Simula transizioni del circuit breaker.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 304 che coinvolge il nodo canonico `Simula transizioni del circuit breaker.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Simula transizioni del circuit breaker.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=148 id=Request-Dataset-Generator::mock_middleware_log_rationale_105 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Simula transizioni del circuit breaker.` è `Simula transizioni del circuit breaker.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Simula transizioni del circuit breaker.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Simula transizioni del circuit breaker.` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Simula transizioni del circuit breaker.` è `L105`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Simula transizioni del circuit breaker.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Simula transizioni del circuit breaker.` alla comunità numerica `12`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Simula transizioni del circuit breaker.` è `simula transizioni del circuit breaker.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Simula transizioni del circuit breaker.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Simula transizioni del circuit breaker.` è `mock_middleware_log_rationale_105`. Nel grafo unificato, il nodo canonico `Simula transizioni del circuit breaker.` possiede l'identificatore esatto `Request-Dataset-Generator::mock_middleware_log_rationale_105`.
Nel grafo unificato, il nodo canonico `Simula transizioni del circuit breaker.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=304 role=incoming -->
Nel grafo unificato, il nodo canonico `Simula transizioni del circuit breaker.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `._next_circuit_breaker()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker` all'ID `Request-Dataset-Generator::mock_middleware_log_rationale_105`. La relazione tecnica esplicita è la seguente: `._next_circuit_breaker()` costituisce una motivazione per `Simula transizioni del circuit breaker.`. Nel grafo unificato, il tipo dichiarato dell'arco 304 che coinvolge il nodo canonico `Simula transizioni del circuit breaker.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 304 che coinvolge il nodo canonico `Simula transizioni del circuit breaker.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 304 che coinvolge il nodo canonico `Simula transizioni del circuit breaker.` è `mock_middleware_log.py`. Nel grafo unificato, la posizione sorgente dell'arco 304 che coinvolge il nodo canonico `Simula transizioni del circuit breaker.` è `L105`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 304 che coinvolge il nodo canonico `Simula transizioni del circuit breaker.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 304 che coinvolge il nodo canonico `Simula transizioni del circuit breaker.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 304 che coinvolge il nodo canonico `Simula transizioni del circuit breaker.` è `1.0`. Nel grafo unificato, l'arco 304 registra come `source` l'identificatore `Request-Dataset-Generator::mock_middleware_log_mockmiddlewareloggenerator_next_circuit_breaker` per la relazione che coinvolge il nodo canonico `Simula transizioni del circuit breaker.`. Nel grafo unificato, l'arco 304 registra come `target` l'identificatore `Request-Dataset-Generator::mock_middleware_log_rationale_105` per la relazione che coinvolge il nodo canonico `Simula transizioni del circuit breaker.`.

## Cluster Graphify 13 — Config Tab UI

Il cluster editoriale Graphify 13 del dominio Architettura e codice del framework RDG contiene 17 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### .__init__() [ui_tabs_config_tab_configtab_init]

<!-- GRAPH_NODE graph=codice index=428 id=ui_tabs_config_tab_configtab_init -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `.__init__()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `L39`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` possiede l'identificatore esatto `ui_tabs_config_tab_configtab_init`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` alla comunità numerica `13`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `.__init__()`.
<!-- GRAPH_EDGE graph=codice index=869 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_init` all'ID `ui_tabs_config_tab_configtab_build`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_tabs_config_tab_configtab_init]` chiama `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 869 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 869 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 869 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 869 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 869 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `L45`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 869 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 869 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `ast`. Nel grafo del codice RDG, l'arco 869 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]`. Nel grafo del codice RDG, l'arco 869 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 869 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=870 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è la sorgente `source` della relazione `calls` verso il nodo canonico `.load_values()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_init` all'ID `ui_tabs_config_tab_configtab_load_values`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_tabs_config_tab_configtab_init]` chiama `.load_values()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 870 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 870 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 870 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 870 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 870 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `L46`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 870 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 870 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `ast`. Nel grafo del codice RDG, l'arco 870 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]`. Nel grafo del codice RDG, l'arco 870 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_load_values` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 870 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=861 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_init`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `.__init__() [ui_tabs_config_tab_configtab_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 861 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 861 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 861 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 861 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `L39`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 861 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 861 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `ast`. Nel grafo del codice RDG, l'arco 861 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]`. Nel grafo del codice RDG, l'arco 861 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 861 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=428 id=Request-Dataset-Generator::ui_tabs_config_tab_configtab_init -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `.__init__()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `L39`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `.__init__()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `ui_tabs_config_tab_configtab_init`. Nel grafo unificato, il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_config_tab_configtab_init`.
<!-- GRAPH_EDGE graph=unificata index=869 role=outgoing -->
Nel grafo unificato, il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_init` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_tabs_config_tab_configtab_init]` chiama `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo unificato, il tipo dichiarato dell'arco 869 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 869 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 869 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 869 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 869 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `L45`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 869 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 869 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 869 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `1.0`. Nel grafo unificato, l'arco 869 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]`. Nel grafo unificato, l'arco 869 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]`.
<!-- GRAPH_EDGE graph=unificata index=870 role=outgoing -->
Nel grafo unificato, il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è la sorgente `source` della relazione `calls` verso il nodo canonico `.load_values()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_init` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_load_values`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_tabs_config_tab_configtab_init]` chiama `.load_values()`. Nel grafo unificato, il tipo dichiarato dell'arco 870 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 870 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 870 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 870 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 870 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `L46`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 870 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 870 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 870 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `1.0`. Nel grafo unificato, l'arco 870 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]`. Nel grafo unificato, l'arco 870 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_load_values` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]`.
<!-- GRAPH_EDGE graph=unificata index=861 role=incoming -->
Nel grafo unificato, il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_init`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `.__init__() [ui_tabs_config_tab_configtab_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 861 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 861 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 861 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 861 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `L39`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 861 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 861 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 861 che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]` è `1.0`. Nel grafo unificato, l'arco 861 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]`. Nel grafo unificato, l'arco 861 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_init` per la relazione che coinvolge il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]`.

### ._as_float()

<!-- GRAPH_NODE graph=codice index=442 id=ui_tabs_config_tab_configtab_as_float -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._as_float()` è `._as_float()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._as_float()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._as_float()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._as_float()` è `L273`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._as_float()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._as_float()` possiede l'identificatore esatto `ui_tabs_config_tab_configtab_as_float`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._as_float()` alla comunità numerica `13`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._as_float()` è `._as_float()`.
Nel grafo del codice RDG, il nodo canonico `._as_float()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=853 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._as_float()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_as_float`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._as_float()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 853 che coinvolge il nodo canonico `._as_float()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 853 che coinvolge il nodo canonico `._as_float()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 853 che coinvolge il nodo canonico `._as_float()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 853 che coinvolge il nodo canonico `._as_float()` è `L273`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 853 che coinvolge il nodo canonico `._as_float()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 853 che coinvolge il nodo canonico `._as_float()` è `ast`. Nel grafo del codice RDG, l'arco 853 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._as_float()`. Nel grafo del codice RDG, l'arco 853 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_as_float` per la relazione che coinvolge il nodo canonico `._as_float()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 853 che coinvolge il nodo canonico `._as_float()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=879 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._as_float()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.get_values()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_get_values` all'ID `ui_tabs_config_tab_configtab_as_float`. La relazione tecnica esplicita è la seguente: `.get_values()` chiama `._as_float()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 879 che coinvolge il nodo canonico `._as_float()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 879 che coinvolge il nodo canonico `._as_float()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 879 che coinvolge il nodo canonico `._as_float()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 879 che coinvolge il nodo canonico `._as_float()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 879 che coinvolge il nodo canonico `._as_float()` è `L231`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 879 che coinvolge il nodo canonico `._as_float()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 879 che coinvolge il nodo canonico `._as_float()` è `ast`. Nel grafo del codice RDG, l'arco 879 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_get_values` per la relazione che coinvolge il nodo canonico `._as_float()`. Nel grafo del codice RDG, l'arco 879 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_as_float` per la relazione che coinvolge il nodo canonico `._as_float()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 879 che coinvolge il nodo canonico `._as_float()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=442 id=Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_float -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._as_float()` è `._as_float()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._as_float()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._as_float()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._as_float()` è `L273`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._as_float()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._as_float()` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._as_float()` è `._as_float()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._as_float()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._as_float()` è `ui_tabs_config_tab_configtab_as_float`. Nel grafo unificato, il nodo canonico `._as_float()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_float`.
Nel grafo unificato, il nodo canonico `._as_float()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=853 role=incoming -->
Nel grafo unificato, il nodo canonico `._as_float()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_float`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._as_float()`. Nel grafo unificato, il tipo dichiarato dell'arco 853 che coinvolge il nodo canonico `._as_float()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 853 che coinvolge il nodo canonico `._as_float()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 853 che coinvolge il nodo canonico `._as_float()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 853 che coinvolge il nodo canonico `._as_float()` è `L273`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 853 che coinvolge il nodo canonico `._as_float()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 853 che coinvolge il nodo canonico `._as_float()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 853 che coinvolge il nodo canonico `._as_float()` è `1.0`. Nel grafo unificato, l'arco 853 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._as_float()`. Nel grafo unificato, l'arco 853 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_float` per la relazione che coinvolge il nodo canonico `._as_float()`.
<!-- GRAPH_EDGE graph=unificata index=879 role=incoming -->
Nel grafo unificato, il nodo canonico `._as_float()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.get_values()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_get_values` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_float`. La relazione tecnica esplicita è la seguente: `.get_values()` chiama `._as_float()`. Nel grafo unificato, il tipo dichiarato dell'arco 879 che coinvolge il nodo canonico `._as_float()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 879 che coinvolge il nodo canonico `._as_float()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 879 che coinvolge il nodo canonico `._as_float()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 879 che coinvolge il nodo canonico `._as_float()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 879 che coinvolge il nodo canonico `._as_float()` è `L231`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 879 che coinvolge il nodo canonico `._as_float()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 879 che coinvolge il nodo canonico `._as_float()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 879 che coinvolge il nodo canonico `._as_float()` è `1.0`. Nel grafo unificato, l'arco 879 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_get_values` per la relazione che coinvolge il nodo canonico `._as_float()`. Nel grafo unificato, l'arco 879 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_float` per la relazione che coinvolge il nodo canonico `._as_float()`.

### ._as_int()

<!-- GRAPH_NODE graph=codice index=441 id=ui_tabs_config_tab_configtab_as_int -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._as_int()` è `._as_int()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._as_int()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._as_int()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._as_int()` è `L266`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._as_int()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._as_int()` possiede l'identificatore esatto `ui_tabs_config_tab_configtab_as_int`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._as_int()` alla comunità numerica `13`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._as_int()` è `._as_int()`.
Nel grafo del codice RDG, il nodo canonico `._as_int()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=854 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._as_int()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_as_int`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._as_int()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 854 che coinvolge il nodo canonico `._as_int()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 854 che coinvolge il nodo canonico `._as_int()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 854 che coinvolge il nodo canonico `._as_int()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 854 che coinvolge il nodo canonico `._as_int()` è `L266`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 854 che coinvolge il nodo canonico `._as_int()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 854 che coinvolge il nodo canonico `._as_int()` è `ast`. Nel grafo del codice RDG, l'arco 854 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._as_int()`. Nel grafo del codice RDG, l'arco 854 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_as_int` per la relazione che coinvolge il nodo canonico `._as_int()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 854 che coinvolge il nodo canonico `._as_int()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=880 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._as_int()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.get_values()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_get_values` all'ID `ui_tabs_config_tab_configtab_as_int`. La relazione tecnica esplicita è la seguente: `.get_values()` chiama `._as_int()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 880 che coinvolge il nodo canonico `._as_int()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 880 che coinvolge il nodo canonico `._as_int()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 880 che coinvolge il nodo canonico `._as_int()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 880 che coinvolge il nodo canonico `._as_int()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 880 che coinvolge il nodo canonico `._as_int()` è `L230`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 880 che coinvolge il nodo canonico `._as_int()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 880 che coinvolge il nodo canonico `._as_int()` è `ast`. Nel grafo del codice RDG, l'arco 880 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_get_values` per la relazione che coinvolge il nodo canonico `._as_int()`. Nel grafo del codice RDG, l'arco 880 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_as_int` per la relazione che coinvolge il nodo canonico `._as_int()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 880 che coinvolge il nodo canonico `._as_int()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=441 id=Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_int -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._as_int()` è `._as_int()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._as_int()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._as_int()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._as_int()` è `L266`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._as_int()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._as_int()` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._as_int()` è `._as_int()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._as_int()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._as_int()` è `ui_tabs_config_tab_configtab_as_int`. Nel grafo unificato, il nodo canonico `._as_int()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_int`.
Nel grafo unificato, il nodo canonico `._as_int()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=854 role=incoming -->
Nel grafo unificato, il nodo canonico `._as_int()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_int`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._as_int()`. Nel grafo unificato, il tipo dichiarato dell'arco 854 che coinvolge il nodo canonico `._as_int()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 854 che coinvolge il nodo canonico `._as_int()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 854 che coinvolge il nodo canonico `._as_int()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 854 che coinvolge il nodo canonico `._as_int()` è `L266`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 854 che coinvolge il nodo canonico `._as_int()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 854 che coinvolge il nodo canonico `._as_int()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 854 che coinvolge il nodo canonico `._as_int()` è `1.0`. Nel grafo unificato, l'arco 854 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._as_int()`. Nel grafo unificato, l'arco 854 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_int` per la relazione che coinvolge il nodo canonico `._as_int()`.
<!-- GRAPH_EDGE graph=unificata index=880 role=incoming -->
Nel grafo unificato, il nodo canonico `._as_int()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.get_values()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_get_values` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_int`. La relazione tecnica esplicita è la seguente: `.get_values()` chiama `._as_int()`. Nel grafo unificato, il tipo dichiarato dell'arco 880 che coinvolge il nodo canonico `._as_int()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 880 che coinvolge il nodo canonico `._as_int()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 880 che coinvolge il nodo canonico `._as_int()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 880 che coinvolge il nodo canonico `._as_int()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 880 che coinvolge il nodo canonico `._as_int()` è `L230`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 880 che coinvolge il nodo canonico `._as_int()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 880 che coinvolge il nodo canonico `._as_int()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 880 che coinvolge il nodo canonico `._as_int()` è `1.0`. Nel grafo unificato, l'arco 880 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_get_values` per la relazione che coinvolge il nodo canonico `._as_int()`. Nel grafo unificato, l'arco 880 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_int` per la relazione che coinvolge il nodo canonico `._as_int()`.

### ._build() [ui_tabs_config_tab_configtab_build]

<!-- GRAPH_NODE graph=codice index=429 id=ui_tabs_config_tab_configtab_build -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `._build()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `L49`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` possiede l'identificatore esatto `ui_tabs_config_tab_configtab_build`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` alla comunità numerica `13`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `._build()`.
<!-- GRAPH_EDGE graph=codice index=871 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_build` all'ID `ui_tabs_config_tab_configtab_build_buttons`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_config_tab_configtab_build]` chiama `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 871 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 871 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 871 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 871 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 871 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `L62`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 871 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 871 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ast`. Nel grafo del codice RDG, l'arco 871 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo del codice RDG, l'arco 871 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_build_buttons` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 871 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=872 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_flow_tags()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_build` all'ID `ui_tabs_config_tab_configtab_build_flow_tags`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_config_tab_configtab_build]` chiama `._build_flow_tags()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 872 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 872 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 872 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 872 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 872 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `L54`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 872 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 872 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ast`. Nel grafo del codice RDG, l'arco 872 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo del codice RDG, l'arco 872 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_build_flow_tags` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 872 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=873 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_middleware_grid()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_build` all'ID `ui_tabs_config_tab_configtab_build_middleware_grid`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_config_tab_configtab_build]` chiama `._build_middleware_grid()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 873 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 873 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 873 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 873 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 873 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `L57`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 873 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 873 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ast`. Nel grafo del codice RDG, l'arco 873 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo del codice RDG, l'arco 873 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_build_middleware_grid` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 873 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=874 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_parameter_grid()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_build` all'ID `ui_tabs_config_tab_configtab_build_parameter_grid`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_config_tab_configtab_build]` chiama `._build_parameter_grid()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 874 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 874 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 874 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 874 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 874 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 874 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 874 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ast`. Nel grafo del codice RDG, l'arco 874 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo del codice RDG, l'arco 874 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_build_parameter_grid` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 874 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=875 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._section()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_build` all'ID `ui_tabs_config_tab_configtab_section`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_config_tab_configtab_build]` chiama `._section()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 875 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 875 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 875 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 875 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 875 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `L50`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 875 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 875 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ast`. Nel grafo del codice RDG, l'arco 875 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo del codice RDG, l'arco 875 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_section` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 875 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=855 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_build`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 855 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 855 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 855 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 855 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `L49`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 855 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 855 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ast`. Nel grafo del codice RDG, l'arco 855 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo del codice RDG, l'arco 855 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 855 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=869 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_init` all'ID `ui_tabs_config_tab_configtab_build`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_tabs_config_tab_configtab_init]` chiama `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 869 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 869 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 869 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 869 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 869 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `L45`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 869 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 869 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ast`. Nel grafo del codice RDG, l'arco 869 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_init` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo del codice RDG, l'arco 869 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 869 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=429 id=Request-Dataset-Generator::ui_tabs_config_tab_configtab_build -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `._build()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `L49`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `._build()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ui_tabs_config_tab_configtab_build`. Nel grafo unificato, il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build`.
<!-- GRAPH_EDGE graph=unificata index=871 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_buttons`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_config_tab_configtab_build]` chiama `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]`. Nel grafo unificato, il tipo dichiarato dell'arco 871 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 871 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 871 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 871 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 871 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `L62`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 871 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 871 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 871 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo unificato, l'arco 871 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo unificato, l'arco 871 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_buttons` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`.
<!-- GRAPH_EDGE graph=unificata index=872 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_flow_tags()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_flow_tags`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_config_tab_configtab_build]` chiama `._build_flow_tags()`. Nel grafo unificato, il tipo dichiarato dell'arco 872 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 872 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 872 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 872 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 872 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `L54`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 872 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 872 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 872 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo unificato, l'arco 872 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo unificato, l'arco 872 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_flow_tags` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`.
<!-- GRAPH_EDGE graph=unificata index=873 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_middleware_grid()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_middleware_grid`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_config_tab_configtab_build]` chiama `._build_middleware_grid()`. Nel grafo unificato, il tipo dichiarato dell'arco 873 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 873 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 873 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 873 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 873 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `L57`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 873 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 873 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 873 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo unificato, l'arco 873 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo unificato, l'arco 873 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_middleware_grid` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`.
<!-- GRAPH_EDGE graph=unificata index=874 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._build_parameter_grid()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_parameter_grid`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_config_tab_configtab_build]` chiama `._build_parameter_grid()`. Nel grafo unificato, il tipo dichiarato dell'arco 874 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 874 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 874 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 874 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 874 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 874 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 874 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 874 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo unificato, l'arco 874 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo unificato, l'arco 874 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_parameter_grid` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`.
<!-- GRAPH_EDGE graph=unificata index=875 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è la sorgente `source` della relazione `calls` verso il nodo canonico `._section()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_section`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_config_tab_configtab_build]` chiama `._section()`. Nel grafo unificato, il tipo dichiarato dell'arco 875 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 875 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 875 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 875 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 875 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `L50`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 875 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 875 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 875 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo unificato, l'arco 875 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo unificato, l'arco 875 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_section` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`.
<!-- GRAPH_EDGE graph=unificata index=855 role=incoming -->
Nel grafo unificato, il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo unificato, il tipo dichiarato dell'arco 855 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 855 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 855 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 855 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `L49`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 855 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 855 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 855 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo unificato, l'arco 855 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo unificato, l'arco 855 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`.
<!-- GRAPH_EDGE graph=unificata index=869 role=incoming -->
Nel grafo unificato, il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_init` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_tabs_config_tab_configtab_init]` chiama `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo unificato, il tipo dichiarato dell'arco 869 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 869 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 869 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 869 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 869 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `L45`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 869 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 869 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 869 che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]` è `1.0`. Nel grafo unificato, l'arco 869 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_init` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo unificato, l'arco 869 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`.

### ._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]

<!-- GRAPH_NODE graph=codice index=434 id=ui_tabs_config_tab_configtab_build_buttons -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `._build_buttons()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `L180`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` possiede l'identificatore esatto `ui_tabs_config_tab_configtab_build_buttons`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` alla comunità numerica `13`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `._build_buttons()`.
Nel grafo del codice RDG, il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=856 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_build_buttons`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 856 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 856 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 856 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 856 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `L180`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 856 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 856 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `ast`. Nel grafo del codice RDG, l'arco 856 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]`. Nel grafo del codice RDG, l'arco 856 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_build_buttons` per la relazione che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 856 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=871 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_build` all'ID `ui_tabs_config_tab_configtab_build_buttons`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_config_tab_configtab_build]` chiama `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 871 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 871 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 871 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 871 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 871 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `L62`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 871 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 871 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `ast`. Nel grafo del codice RDG, l'arco 871 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]`. Nel grafo del codice RDG, l'arco 871 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_build_buttons` per la relazione che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 871 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=434 id=Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_buttons -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `._build_buttons()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `L180`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `._build_buttons()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `ui_tabs_config_tab_configtab_build_buttons`. Nel grafo unificato, il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_buttons`.
Nel grafo unificato, il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=856 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_buttons`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]`. Nel grafo unificato, il tipo dichiarato dell'arco 856 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 856 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 856 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 856 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `L180`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 856 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 856 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 856 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `1.0`. Nel grafo unificato, l'arco 856 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]`. Nel grafo unificato, l'arco 856 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_buttons` per la relazione che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]`.
<!-- GRAPH_EDGE graph=unificata index=871 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_buttons`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_config_tab_configtab_build]` chiama `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]`. Nel grafo unificato, il tipo dichiarato dell'arco 871 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 871 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 871 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 871 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 871 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `L62`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 871 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 871 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 871 che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]` è `1.0`. Nel grafo unificato, l'arco 871 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]`. Nel grafo unificato, l'arco 871 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_buttons` per la relazione che coinvolge il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]`.

### ._build_flow_tags()

<!-- GRAPH_NODE graph=codice index=432 id=ui_tabs_config_tab_configtab_build_flow_tags -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._build_flow_tags()` è `._build_flow_tags()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._build_flow_tags()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._build_flow_tags()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._build_flow_tags()` è `L119`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._build_flow_tags()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._build_flow_tags()` possiede l'identificatore esatto `ui_tabs_config_tab_configtab_build_flow_tags`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._build_flow_tags()` alla comunità numerica `13`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._build_flow_tags()` è `._build_flow_tags()`.
<!-- GRAPH_EDGE graph=codice index=876 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._build_flow_tags()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._toggle_flow()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_build_flow_tags` all'ID `ui_tabs_config_tab_configtab_toggle_flow`. La relazione tecnica esplicita è la seguente: `._build_flow_tags()` chiama `._toggle_flow()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 876 che coinvolge il nodo canonico `._build_flow_tags()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 876 che coinvolge il nodo canonico `._build_flow_tags()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 876 che coinvolge il nodo canonico `._build_flow_tags()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 876 che coinvolge il nodo canonico `._build_flow_tags()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 876 che coinvolge il nodo canonico `._build_flow_tags()` è `L132`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 876 che coinvolge il nodo canonico `._build_flow_tags()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 876 che coinvolge il nodo canonico `._build_flow_tags()` è `ast`. Nel grafo del codice RDG, l'arco 876 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_build_flow_tags` per la relazione che coinvolge il nodo canonico `._build_flow_tags()`. Nel grafo del codice RDG, l'arco 876 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_toggle_flow` per la relazione che coinvolge il nodo canonico `._build_flow_tags()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 876 che coinvolge il nodo canonico `._build_flow_tags()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=857 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_flow_tags()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_build_flow_tags`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._build_flow_tags()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 857 che coinvolge il nodo canonico `._build_flow_tags()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 857 che coinvolge il nodo canonico `._build_flow_tags()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 857 che coinvolge il nodo canonico `._build_flow_tags()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 857 che coinvolge il nodo canonico `._build_flow_tags()` è `L119`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 857 che coinvolge il nodo canonico `._build_flow_tags()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 857 che coinvolge il nodo canonico `._build_flow_tags()` è `ast`. Nel grafo del codice RDG, l'arco 857 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._build_flow_tags()`. Nel grafo del codice RDG, l'arco 857 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_build_flow_tags` per la relazione che coinvolge il nodo canonico `._build_flow_tags()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 857 che coinvolge il nodo canonico `._build_flow_tags()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=872 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_flow_tags()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_build` all'ID `ui_tabs_config_tab_configtab_build_flow_tags`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_config_tab_configtab_build]` chiama `._build_flow_tags()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 872 che coinvolge il nodo canonico `._build_flow_tags()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 872 che coinvolge il nodo canonico `._build_flow_tags()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 872 che coinvolge il nodo canonico `._build_flow_tags()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 872 che coinvolge il nodo canonico `._build_flow_tags()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 872 che coinvolge il nodo canonico `._build_flow_tags()` è `L54`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 872 che coinvolge il nodo canonico `._build_flow_tags()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 872 che coinvolge il nodo canonico `._build_flow_tags()` è `ast`. Nel grafo del codice RDG, l'arco 872 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build_flow_tags()`. Nel grafo del codice RDG, l'arco 872 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_build_flow_tags` per la relazione che coinvolge il nodo canonico `._build_flow_tags()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 872 che coinvolge il nodo canonico `._build_flow_tags()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=432 id=Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_flow_tags -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._build_flow_tags()` è `._build_flow_tags()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._build_flow_tags()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._build_flow_tags()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._build_flow_tags()` è `L119`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._build_flow_tags()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._build_flow_tags()` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._build_flow_tags()` è `._build_flow_tags()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._build_flow_tags()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._build_flow_tags()` è `ui_tabs_config_tab_configtab_build_flow_tags`. Nel grafo unificato, il nodo canonico `._build_flow_tags()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_flow_tags`.
<!-- GRAPH_EDGE graph=unificata index=876 role=outgoing -->
Nel grafo unificato, il nodo canonico `._build_flow_tags()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._toggle_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_flow_tags` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_toggle_flow`. La relazione tecnica esplicita è la seguente: `._build_flow_tags()` chiama `._toggle_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 876 che coinvolge il nodo canonico `._build_flow_tags()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 876 che coinvolge il nodo canonico `._build_flow_tags()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 876 che coinvolge il nodo canonico `._build_flow_tags()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 876 che coinvolge il nodo canonico `._build_flow_tags()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 876 che coinvolge il nodo canonico `._build_flow_tags()` è `L132`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 876 che coinvolge il nodo canonico `._build_flow_tags()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 876 che coinvolge il nodo canonico `._build_flow_tags()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 876 che coinvolge il nodo canonico `._build_flow_tags()` è `1.0`. Nel grafo unificato, l'arco 876 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_flow_tags` per la relazione che coinvolge il nodo canonico `._build_flow_tags()`. Nel grafo unificato, l'arco 876 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_toggle_flow` per la relazione che coinvolge il nodo canonico `._build_flow_tags()`.
<!-- GRAPH_EDGE graph=unificata index=857 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_flow_tags()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_flow_tags`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._build_flow_tags()`. Nel grafo unificato, il tipo dichiarato dell'arco 857 che coinvolge il nodo canonico `._build_flow_tags()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 857 che coinvolge il nodo canonico `._build_flow_tags()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 857 che coinvolge il nodo canonico `._build_flow_tags()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 857 che coinvolge il nodo canonico `._build_flow_tags()` è `L119`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 857 che coinvolge il nodo canonico `._build_flow_tags()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 857 che coinvolge il nodo canonico `._build_flow_tags()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 857 che coinvolge il nodo canonico `._build_flow_tags()` è `1.0`. Nel grafo unificato, l'arco 857 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._build_flow_tags()`. Nel grafo unificato, l'arco 857 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_flow_tags` per la relazione che coinvolge il nodo canonico `._build_flow_tags()`.
<!-- GRAPH_EDGE graph=unificata index=872 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_flow_tags()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_flow_tags`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_config_tab_configtab_build]` chiama `._build_flow_tags()`. Nel grafo unificato, il tipo dichiarato dell'arco 872 che coinvolge il nodo canonico `._build_flow_tags()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 872 che coinvolge il nodo canonico `._build_flow_tags()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 872 che coinvolge il nodo canonico `._build_flow_tags()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 872 che coinvolge il nodo canonico `._build_flow_tags()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 872 che coinvolge il nodo canonico `._build_flow_tags()` è `L54`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 872 che coinvolge il nodo canonico `._build_flow_tags()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 872 che coinvolge il nodo canonico `._build_flow_tags()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 872 che coinvolge il nodo canonico `._build_flow_tags()` è `1.0`. Nel grafo unificato, l'arco 872 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build_flow_tags()`. Nel grafo unificato, l'arco 872 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_flow_tags` per la relazione che coinvolge il nodo canonico `._build_flow_tags()`.

### ._build_middleware_grid()

<!-- GRAPH_NODE graph=codice index=433 id=ui_tabs_config_tab_configtab_build_middleware_grid -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._build_middleware_grid()` è `._build_middleware_grid()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._build_middleware_grid()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._build_middleware_grid()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._build_middleware_grid()` è `L137`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._build_middleware_grid()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._build_middleware_grid()` possiede l'identificatore esatto `ui_tabs_config_tab_configtab_build_middleware_grid`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._build_middleware_grid()` alla comunità numerica `13`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._build_middleware_grid()` è `._build_middleware_grid()`.
Nel grafo del codice RDG, il nodo canonico `._build_middleware_grid()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=858 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_middleware_grid()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_build_middleware_grid`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._build_middleware_grid()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 858 che coinvolge il nodo canonico `._build_middleware_grid()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 858 che coinvolge il nodo canonico `._build_middleware_grid()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 858 che coinvolge il nodo canonico `._build_middleware_grid()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 858 che coinvolge il nodo canonico `._build_middleware_grid()` è `L137`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 858 che coinvolge il nodo canonico `._build_middleware_grid()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 858 che coinvolge il nodo canonico `._build_middleware_grid()` è `ast`. Nel grafo del codice RDG, l'arco 858 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._build_middleware_grid()`. Nel grafo del codice RDG, l'arco 858 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_build_middleware_grid` per la relazione che coinvolge il nodo canonico `._build_middleware_grid()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 858 che coinvolge il nodo canonico `._build_middleware_grid()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=873 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_middleware_grid()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_build` all'ID `ui_tabs_config_tab_configtab_build_middleware_grid`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_config_tab_configtab_build]` chiama `._build_middleware_grid()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 873 che coinvolge il nodo canonico `._build_middleware_grid()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 873 che coinvolge il nodo canonico `._build_middleware_grid()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 873 che coinvolge il nodo canonico `._build_middleware_grid()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 873 che coinvolge il nodo canonico `._build_middleware_grid()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 873 che coinvolge il nodo canonico `._build_middleware_grid()` è `L57`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 873 che coinvolge il nodo canonico `._build_middleware_grid()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 873 che coinvolge il nodo canonico `._build_middleware_grid()` è `ast`. Nel grafo del codice RDG, l'arco 873 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build_middleware_grid()`. Nel grafo del codice RDG, l'arco 873 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_build_middleware_grid` per la relazione che coinvolge il nodo canonico `._build_middleware_grid()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 873 che coinvolge il nodo canonico `._build_middleware_grid()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=433 id=Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_middleware_grid -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._build_middleware_grid()` è `._build_middleware_grid()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._build_middleware_grid()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._build_middleware_grid()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._build_middleware_grid()` è `L137`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._build_middleware_grid()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._build_middleware_grid()` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._build_middleware_grid()` è `._build_middleware_grid()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._build_middleware_grid()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._build_middleware_grid()` è `ui_tabs_config_tab_configtab_build_middleware_grid`. Nel grafo unificato, il nodo canonico `._build_middleware_grid()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_middleware_grid`.
Nel grafo unificato, il nodo canonico `._build_middleware_grid()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=858 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_middleware_grid()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_middleware_grid`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._build_middleware_grid()`. Nel grafo unificato, il tipo dichiarato dell'arco 858 che coinvolge il nodo canonico `._build_middleware_grid()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 858 che coinvolge il nodo canonico `._build_middleware_grid()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 858 che coinvolge il nodo canonico `._build_middleware_grid()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 858 che coinvolge il nodo canonico `._build_middleware_grid()` è `L137`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 858 che coinvolge il nodo canonico `._build_middleware_grid()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 858 che coinvolge il nodo canonico `._build_middleware_grid()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 858 che coinvolge il nodo canonico `._build_middleware_grid()` è `1.0`. Nel grafo unificato, l'arco 858 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._build_middleware_grid()`. Nel grafo unificato, l'arco 858 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_middleware_grid` per la relazione che coinvolge il nodo canonico `._build_middleware_grid()`.
<!-- GRAPH_EDGE graph=unificata index=873 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_middleware_grid()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_middleware_grid`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_config_tab_configtab_build]` chiama `._build_middleware_grid()`. Nel grafo unificato, il tipo dichiarato dell'arco 873 che coinvolge il nodo canonico `._build_middleware_grid()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 873 che coinvolge il nodo canonico `._build_middleware_grid()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 873 che coinvolge il nodo canonico `._build_middleware_grid()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 873 che coinvolge il nodo canonico `._build_middleware_grid()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 873 che coinvolge il nodo canonico `._build_middleware_grid()` è `L57`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 873 che coinvolge il nodo canonico `._build_middleware_grid()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 873 che coinvolge il nodo canonico `._build_middleware_grid()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 873 che coinvolge il nodo canonico `._build_middleware_grid()` è `1.0`. Nel grafo unificato, l'arco 873 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build_middleware_grid()`. Nel grafo unificato, l'arco 873 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_middleware_grid` per la relazione che coinvolge il nodo canonico `._build_middleware_grid()`.

### ._build_parameter_grid()

<!-- GRAPH_NODE graph=codice index=431 id=ui_tabs_config_tab_configtab_build_parameter_grid -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._build_parameter_grid()` è `._build_parameter_grid()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._build_parameter_grid()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._build_parameter_grid()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._build_parameter_grid()` è `L74`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._build_parameter_grid()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._build_parameter_grid()` possiede l'identificatore esatto `ui_tabs_config_tab_configtab_build_parameter_grid`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._build_parameter_grid()` alla comunità numerica `13`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._build_parameter_grid()` è `._build_parameter_grid()`.
Nel grafo del codice RDG, il nodo canonico `._build_parameter_grid()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=859 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_parameter_grid()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_build_parameter_grid`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._build_parameter_grid()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 859 che coinvolge il nodo canonico `._build_parameter_grid()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 859 che coinvolge il nodo canonico `._build_parameter_grid()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 859 che coinvolge il nodo canonico `._build_parameter_grid()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 859 che coinvolge il nodo canonico `._build_parameter_grid()` è `L74`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 859 che coinvolge il nodo canonico `._build_parameter_grid()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 859 che coinvolge il nodo canonico `._build_parameter_grid()` è `ast`. Nel grafo del codice RDG, l'arco 859 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._build_parameter_grid()`. Nel grafo del codice RDG, l'arco 859 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_build_parameter_grid` per la relazione che coinvolge il nodo canonico `._build_parameter_grid()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 859 che coinvolge il nodo canonico `._build_parameter_grid()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=874 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._build_parameter_grid()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_build` all'ID `ui_tabs_config_tab_configtab_build_parameter_grid`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_config_tab_configtab_build]` chiama `._build_parameter_grid()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 874 che coinvolge il nodo canonico `._build_parameter_grid()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 874 che coinvolge il nodo canonico `._build_parameter_grid()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 874 che coinvolge il nodo canonico `._build_parameter_grid()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 874 che coinvolge il nodo canonico `._build_parameter_grid()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 874 che coinvolge il nodo canonico `._build_parameter_grid()` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 874 che coinvolge il nodo canonico `._build_parameter_grid()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 874 che coinvolge il nodo canonico `._build_parameter_grid()` è `ast`. Nel grafo del codice RDG, l'arco 874 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build_parameter_grid()`. Nel grafo del codice RDG, l'arco 874 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_build_parameter_grid` per la relazione che coinvolge il nodo canonico `._build_parameter_grid()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 874 che coinvolge il nodo canonico `._build_parameter_grid()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=431 id=Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_parameter_grid -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._build_parameter_grid()` è `._build_parameter_grid()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._build_parameter_grid()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._build_parameter_grid()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._build_parameter_grid()` è `L74`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._build_parameter_grid()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._build_parameter_grid()` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._build_parameter_grid()` è `._build_parameter_grid()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._build_parameter_grid()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._build_parameter_grid()` è `ui_tabs_config_tab_configtab_build_parameter_grid`. Nel grafo unificato, il nodo canonico `._build_parameter_grid()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_parameter_grid`.
Nel grafo unificato, il nodo canonico `._build_parameter_grid()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=859 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_parameter_grid()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_parameter_grid`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._build_parameter_grid()`. Nel grafo unificato, il tipo dichiarato dell'arco 859 che coinvolge il nodo canonico `._build_parameter_grid()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 859 che coinvolge il nodo canonico `._build_parameter_grid()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 859 che coinvolge il nodo canonico `._build_parameter_grid()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 859 che coinvolge il nodo canonico `._build_parameter_grid()` è `L74`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 859 che coinvolge il nodo canonico `._build_parameter_grid()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 859 che coinvolge il nodo canonico `._build_parameter_grid()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 859 che coinvolge il nodo canonico `._build_parameter_grid()` è `1.0`. Nel grafo unificato, l'arco 859 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._build_parameter_grid()`. Nel grafo unificato, l'arco 859 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_parameter_grid` per la relazione che coinvolge il nodo canonico `._build_parameter_grid()`.
<!-- GRAPH_EDGE graph=unificata index=874 role=incoming -->
Nel grafo unificato, il nodo canonico `._build_parameter_grid()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_parameter_grid`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_config_tab_configtab_build]` chiama `._build_parameter_grid()`. Nel grafo unificato, il tipo dichiarato dell'arco 874 che coinvolge il nodo canonico `._build_parameter_grid()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 874 che coinvolge il nodo canonico `._build_parameter_grid()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 874 che coinvolge il nodo canonico `._build_parameter_grid()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 874 che coinvolge il nodo canonico `._build_parameter_grid()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 874 che coinvolge il nodo canonico `._build_parameter_grid()` è `L51`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 874 che coinvolge il nodo canonico `._build_parameter_grid()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 874 che coinvolge il nodo canonico `._build_parameter_grid()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 874 che coinvolge il nodo canonico `._build_parameter_grid()` è `1.0`. Nel grafo unificato, l'arco 874 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._build_parameter_grid()`. Nel grafo unificato, l'arco 874 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_parameter_grid` per la relazione che coinvolge il nodo canonico `._build_parameter_grid()`.

### ._refresh_flow_tags()

<!-- GRAPH_NODE graph=codice index=440 id=ui_tabs_config_tab_configtab_refresh_flow_tags -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._refresh_flow_tags()` è `._refresh_flow_tags()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._refresh_flow_tags()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._refresh_flow_tags()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._refresh_flow_tags()` è `L254`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._refresh_flow_tags()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._refresh_flow_tags()` possiede l'identificatore esatto `ui_tabs_config_tab_configtab_refresh_flow_tags`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._refresh_flow_tags()` alla comunità numerica `13`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._refresh_flow_tags()` è `._refresh_flow_tags()`.
Nel grafo del codice RDG, il nodo canonico `._refresh_flow_tags()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=863 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._refresh_flow_tags()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_refresh_flow_tags`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._refresh_flow_tags()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 863 che coinvolge il nodo canonico `._refresh_flow_tags()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 863 che coinvolge il nodo canonico `._refresh_flow_tags()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 863 che coinvolge il nodo canonico `._refresh_flow_tags()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 863 che coinvolge il nodo canonico `._refresh_flow_tags()` è `L254`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 863 che coinvolge il nodo canonico `._refresh_flow_tags()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 863 che coinvolge il nodo canonico `._refresh_flow_tags()` è `ast`. Nel grafo del codice RDG, l'arco 863 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._refresh_flow_tags()`. Nel grafo del codice RDG, l'arco 863 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_refresh_flow_tags` per la relazione che coinvolge il nodo canonico `._refresh_flow_tags()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 863 che coinvolge il nodo canonico `._refresh_flow_tags()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=877 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._refresh_flow_tags()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.load_values()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_load_values` all'ID `ui_tabs_config_tab_configtab_refresh_flow_tags`. La relazione tecnica esplicita è la seguente: `.load_values()` chiama `._refresh_flow_tags()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 877 che coinvolge il nodo canonico `._refresh_flow_tags()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 877 che coinvolge il nodo canonico `._refresh_flow_tags()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 877 che coinvolge il nodo canonico `._refresh_flow_tags()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 877 che coinvolge il nodo canonico `._refresh_flow_tags()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 877 che coinvolge il nodo canonico `._refresh_flow_tags()` è `L225`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 877 che coinvolge il nodo canonico `._refresh_flow_tags()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 877 che coinvolge il nodo canonico `._refresh_flow_tags()` è `ast`. Nel grafo del codice RDG, l'arco 877 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_load_values` per la relazione che coinvolge il nodo canonico `._refresh_flow_tags()`. Nel grafo del codice RDG, l'arco 877 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_refresh_flow_tags` per la relazione che coinvolge il nodo canonico `._refresh_flow_tags()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 877 che coinvolge il nodo canonico `._refresh_flow_tags()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=882 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._refresh_flow_tags()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._toggle_flow()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_toggle_flow` all'ID `ui_tabs_config_tab_configtab_refresh_flow_tags`. La relazione tecnica esplicita è la seguente: `._toggle_flow()` chiama `._refresh_flow_tags()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 882 che coinvolge il nodo canonico `._refresh_flow_tags()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 882 che coinvolge il nodo canonico `._refresh_flow_tags()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 882 che coinvolge il nodo canonico `._refresh_flow_tags()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 882 che coinvolge il nodo canonico `._refresh_flow_tags()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 882 che coinvolge il nodo canonico `._refresh_flow_tags()` è `L252`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 882 che coinvolge il nodo canonico `._refresh_flow_tags()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 882 che coinvolge il nodo canonico `._refresh_flow_tags()` è `ast`. Nel grafo del codice RDG, l'arco 882 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_toggle_flow` per la relazione che coinvolge il nodo canonico `._refresh_flow_tags()`. Nel grafo del codice RDG, l'arco 882 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_refresh_flow_tags` per la relazione che coinvolge il nodo canonico `._refresh_flow_tags()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 882 che coinvolge il nodo canonico `._refresh_flow_tags()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=440 id=Request-Dataset-Generator::ui_tabs_config_tab_configtab_refresh_flow_tags -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._refresh_flow_tags()` è `._refresh_flow_tags()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._refresh_flow_tags()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._refresh_flow_tags()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._refresh_flow_tags()` è `L254`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._refresh_flow_tags()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._refresh_flow_tags()` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._refresh_flow_tags()` è `._refresh_flow_tags()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._refresh_flow_tags()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._refresh_flow_tags()` è `ui_tabs_config_tab_configtab_refresh_flow_tags`. Nel grafo unificato, il nodo canonico `._refresh_flow_tags()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_config_tab_configtab_refresh_flow_tags`.
Nel grafo unificato, il nodo canonico `._refresh_flow_tags()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=863 role=incoming -->
Nel grafo unificato, il nodo canonico `._refresh_flow_tags()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_refresh_flow_tags`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._refresh_flow_tags()`. Nel grafo unificato, il tipo dichiarato dell'arco 863 che coinvolge il nodo canonico `._refresh_flow_tags()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 863 che coinvolge il nodo canonico `._refresh_flow_tags()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 863 che coinvolge il nodo canonico `._refresh_flow_tags()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 863 che coinvolge il nodo canonico `._refresh_flow_tags()` è `L254`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 863 che coinvolge il nodo canonico `._refresh_flow_tags()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 863 che coinvolge il nodo canonico `._refresh_flow_tags()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 863 che coinvolge il nodo canonico `._refresh_flow_tags()` è `1.0`. Nel grafo unificato, l'arco 863 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._refresh_flow_tags()`. Nel grafo unificato, l'arco 863 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_refresh_flow_tags` per la relazione che coinvolge il nodo canonico `._refresh_flow_tags()`.
<!-- GRAPH_EDGE graph=unificata index=877 role=incoming -->
Nel grafo unificato, il nodo canonico `._refresh_flow_tags()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.load_values()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_load_values` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_refresh_flow_tags`. La relazione tecnica esplicita è la seguente: `.load_values()` chiama `._refresh_flow_tags()`. Nel grafo unificato, il tipo dichiarato dell'arco 877 che coinvolge il nodo canonico `._refresh_flow_tags()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 877 che coinvolge il nodo canonico `._refresh_flow_tags()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 877 che coinvolge il nodo canonico `._refresh_flow_tags()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 877 che coinvolge il nodo canonico `._refresh_flow_tags()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 877 che coinvolge il nodo canonico `._refresh_flow_tags()` è `L225`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 877 che coinvolge il nodo canonico `._refresh_flow_tags()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 877 che coinvolge il nodo canonico `._refresh_flow_tags()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 877 che coinvolge il nodo canonico `._refresh_flow_tags()` è `1.0`. Nel grafo unificato, l'arco 877 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_load_values` per la relazione che coinvolge il nodo canonico `._refresh_flow_tags()`. Nel grafo unificato, l'arco 877 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_refresh_flow_tags` per la relazione che coinvolge il nodo canonico `._refresh_flow_tags()`.
<!-- GRAPH_EDGE graph=unificata index=882 role=incoming -->
Nel grafo unificato, il nodo canonico `._refresh_flow_tags()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._toggle_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_toggle_flow` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_refresh_flow_tags`. La relazione tecnica esplicita è la seguente: `._toggle_flow()` chiama `._refresh_flow_tags()`. Nel grafo unificato, il tipo dichiarato dell'arco 882 che coinvolge il nodo canonico `._refresh_flow_tags()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 882 che coinvolge il nodo canonico `._refresh_flow_tags()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 882 che coinvolge il nodo canonico `._refresh_flow_tags()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 882 che coinvolge il nodo canonico `._refresh_flow_tags()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 882 che coinvolge il nodo canonico `._refresh_flow_tags()` è `L252`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 882 che coinvolge il nodo canonico `._refresh_flow_tags()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 882 che coinvolge il nodo canonico `._refresh_flow_tags()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 882 che coinvolge il nodo canonico `._refresh_flow_tags()` è `1.0`. Nel grafo unificato, l'arco 882 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_toggle_flow` per la relazione che coinvolge il nodo canonico `._refresh_flow_tags()`. Nel grafo unificato, l'arco 882 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_refresh_flow_tags` per la relazione che coinvolge il nodo canonico `._refresh_flow_tags()`.

### ._section()

<!-- GRAPH_NODE graph=codice index=430 id=ui_tabs_config_tab_configtab_section -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._section()` è `._section()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._section()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._section()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._section()` è `L64`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._section()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._section()` possiede l'identificatore esatto `ui_tabs_config_tab_configtab_section`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._section()` alla comunità numerica `13`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._section()` è `._section()`.
Nel grafo del codice RDG, il nodo canonico `._section()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=866 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._section()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_section`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._section()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 866 che coinvolge il nodo canonico `._section()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 866 che coinvolge il nodo canonico `._section()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 866 che coinvolge il nodo canonico `._section()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 866 che coinvolge il nodo canonico `._section()` è `L64`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 866 che coinvolge il nodo canonico `._section()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 866 che coinvolge il nodo canonico `._section()` è `ast`. Nel grafo del codice RDG, l'arco 866 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._section()`. Nel grafo del codice RDG, l'arco 866 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_section` per la relazione che coinvolge il nodo canonico `._section()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 866 che coinvolge il nodo canonico `._section()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=875 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._section()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_build` all'ID `ui_tabs_config_tab_configtab_section`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_config_tab_configtab_build]` chiama `._section()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 875 che coinvolge il nodo canonico `._section()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 875 che coinvolge il nodo canonico `._section()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 875 che coinvolge il nodo canonico `._section()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 875 che coinvolge il nodo canonico `._section()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 875 che coinvolge il nodo canonico `._section()` è `L50`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 875 che coinvolge il nodo canonico `._section()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 875 che coinvolge il nodo canonico `._section()` è `ast`. Nel grafo del codice RDG, l'arco 875 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._section()`. Nel grafo del codice RDG, l'arco 875 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_section` per la relazione che coinvolge il nodo canonico `._section()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 875 che coinvolge il nodo canonico `._section()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=430 id=Request-Dataset-Generator::ui_tabs_config_tab_configtab_section -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._section()` è `._section()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._section()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._section()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._section()` è `L64`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._section()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._section()` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._section()` è `._section()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._section()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._section()` è `ui_tabs_config_tab_configtab_section`. Nel grafo unificato, il nodo canonico `._section()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_config_tab_configtab_section`.
Nel grafo unificato, il nodo canonico `._section()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=866 role=incoming -->
Nel grafo unificato, il nodo canonico `._section()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_section`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._section()`. Nel grafo unificato, il tipo dichiarato dell'arco 866 che coinvolge il nodo canonico `._section()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 866 che coinvolge il nodo canonico `._section()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 866 che coinvolge il nodo canonico `._section()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 866 che coinvolge il nodo canonico `._section()` è `L64`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 866 che coinvolge il nodo canonico `._section()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 866 che coinvolge il nodo canonico `._section()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 866 che coinvolge il nodo canonico `._section()` è `1.0`. Nel grafo unificato, l'arco 866 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._section()`. Nel grafo unificato, l'arco 866 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_section` per la relazione che coinvolge il nodo canonico `._section()`.
<!-- GRAPH_EDGE graph=unificata index=875 role=incoming -->
Nel grafo unificato, il nodo canonico `._section()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_section`. La relazione tecnica esplicita è la seguente: `._build() [ui_tabs_config_tab_configtab_build]` chiama `._section()`. Nel grafo unificato, il tipo dichiarato dell'arco 875 che coinvolge il nodo canonico `._section()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 875 che coinvolge il nodo canonico `._section()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 875 che coinvolge il nodo canonico `._section()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 875 che coinvolge il nodo canonico `._section()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 875 che coinvolge il nodo canonico `._section()` è `L50`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 875 che coinvolge il nodo canonico `._section()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 875 che coinvolge il nodo canonico `._section()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 875 che coinvolge il nodo canonico `._section()` è `1.0`. Nel grafo unificato, l'arco 875 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `._section()`. Nel grafo unificato, l'arco 875 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_section` per la relazione che coinvolge il nodo canonico `._section()`.

### ._toggle_flow()

<!-- GRAPH_NODE graph=codice index=439 id=ui_tabs_config_tab_configtab_toggle_flow -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `._toggle_flow()` è `._toggle_flow()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `._toggle_flow()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `._toggle_flow()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `._toggle_flow()` è `L247`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `._toggle_flow()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `._toggle_flow()` possiede l'identificatore esatto `ui_tabs_config_tab_configtab_toggle_flow`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `._toggle_flow()` alla comunità numerica `13`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `._toggle_flow()` è `._toggle_flow()`.
<!-- GRAPH_EDGE graph=codice index=882 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `._toggle_flow()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._refresh_flow_tags()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_toggle_flow` all'ID `ui_tabs_config_tab_configtab_refresh_flow_tags`. La relazione tecnica esplicita è la seguente: `._toggle_flow()` chiama `._refresh_flow_tags()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 882 che coinvolge il nodo canonico `._toggle_flow()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 882 che coinvolge il nodo canonico `._toggle_flow()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 882 che coinvolge il nodo canonico `._toggle_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 882 che coinvolge il nodo canonico `._toggle_flow()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 882 che coinvolge il nodo canonico `._toggle_flow()` è `L252`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 882 che coinvolge il nodo canonico `._toggle_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 882 che coinvolge il nodo canonico `._toggle_flow()` è `ast`. Nel grafo del codice RDG, l'arco 882 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_toggle_flow` per la relazione che coinvolge il nodo canonico `._toggle_flow()`. Nel grafo del codice RDG, l'arco 882 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_refresh_flow_tags` per la relazione che coinvolge il nodo canonico `._toggle_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 882 che coinvolge il nodo canonico `._toggle_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=867 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._toggle_flow()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_toggle_flow`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._toggle_flow()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 867 che coinvolge il nodo canonico `._toggle_flow()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 867 che coinvolge il nodo canonico `._toggle_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 867 che coinvolge il nodo canonico `._toggle_flow()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 867 che coinvolge il nodo canonico `._toggle_flow()` è `L247`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 867 che coinvolge il nodo canonico `._toggle_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 867 che coinvolge il nodo canonico `._toggle_flow()` è `ast`. Nel grafo del codice RDG, l'arco 867 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._toggle_flow()`. Nel grafo del codice RDG, l'arco 867 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_toggle_flow` per la relazione che coinvolge il nodo canonico `._toggle_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 867 che coinvolge il nodo canonico `._toggle_flow()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=876 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `._toggle_flow()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build_flow_tags()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_build_flow_tags` all'ID `ui_tabs_config_tab_configtab_toggle_flow`. La relazione tecnica esplicita è la seguente: `._build_flow_tags()` chiama `._toggle_flow()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 876 che coinvolge il nodo canonico `._toggle_flow()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 876 che coinvolge il nodo canonico `._toggle_flow()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 876 che coinvolge il nodo canonico `._toggle_flow()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 876 che coinvolge il nodo canonico `._toggle_flow()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 876 che coinvolge il nodo canonico `._toggle_flow()` è `L132`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 876 che coinvolge il nodo canonico `._toggle_flow()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 876 che coinvolge il nodo canonico `._toggle_flow()` è `ast`. Nel grafo del codice RDG, l'arco 876 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_build_flow_tags` per la relazione che coinvolge il nodo canonico `._toggle_flow()`. Nel grafo del codice RDG, l'arco 876 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_toggle_flow` per la relazione che coinvolge il nodo canonico `._toggle_flow()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 876 che coinvolge il nodo canonico `._toggle_flow()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=439 id=Request-Dataset-Generator::ui_tabs_config_tab_configtab_toggle_flow -->
Nel grafo unificato, l'etichetta originale del nodo canonico `._toggle_flow()` è `._toggle_flow()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `._toggle_flow()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `._toggle_flow()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `._toggle_flow()` è `L247`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `._toggle_flow()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `._toggle_flow()` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `._toggle_flow()` è `._toggle_flow()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `._toggle_flow()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `._toggle_flow()` è `ui_tabs_config_tab_configtab_toggle_flow`. Nel grafo unificato, il nodo canonico `._toggle_flow()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_config_tab_configtab_toggle_flow`.
<!-- GRAPH_EDGE graph=unificata index=882 role=outgoing -->
Nel grafo unificato, il nodo canonico `._toggle_flow()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._refresh_flow_tags()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_toggle_flow` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_refresh_flow_tags`. La relazione tecnica esplicita è la seguente: `._toggle_flow()` chiama `._refresh_flow_tags()`. Nel grafo unificato, il tipo dichiarato dell'arco 882 che coinvolge il nodo canonico `._toggle_flow()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 882 che coinvolge il nodo canonico `._toggle_flow()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 882 che coinvolge il nodo canonico `._toggle_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 882 che coinvolge il nodo canonico `._toggle_flow()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 882 che coinvolge il nodo canonico `._toggle_flow()` è `L252`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 882 che coinvolge il nodo canonico `._toggle_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 882 che coinvolge il nodo canonico `._toggle_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 882 che coinvolge il nodo canonico `._toggle_flow()` è `1.0`. Nel grafo unificato, l'arco 882 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_toggle_flow` per la relazione che coinvolge il nodo canonico `._toggle_flow()`. Nel grafo unificato, l'arco 882 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_refresh_flow_tags` per la relazione che coinvolge il nodo canonico `._toggle_flow()`.
<!-- GRAPH_EDGE graph=unificata index=867 role=incoming -->
Nel grafo unificato, il nodo canonico `._toggle_flow()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_toggle_flow`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._toggle_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 867 che coinvolge il nodo canonico `._toggle_flow()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 867 che coinvolge il nodo canonico `._toggle_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 867 che coinvolge il nodo canonico `._toggle_flow()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 867 che coinvolge il nodo canonico `._toggle_flow()` è `L247`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 867 che coinvolge il nodo canonico `._toggle_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 867 che coinvolge il nodo canonico `._toggle_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 867 che coinvolge il nodo canonico `._toggle_flow()` è `1.0`. Nel grafo unificato, l'arco 867 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `._toggle_flow()`. Nel grafo unificato, l'arco 867 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_toggle_flow` per la relazione che coinvolge il nodo canonico `._toggle_flow()`.
<!-- GRAPH_EDGE graph=unificata index=876 role=incoming -->
Nel grafo unificato, il nodo canonico `._toggle_flow()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build_flow_tags()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_flow_tags` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_toggle_flow`. La relazione tecnica esplicita è la seguente: `._build_flow_tags()` chiama `._toggle_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 876 che coinvolge il nodo canonico `._toggle_flow()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 876 che coinvolge il nodo canonico `._toggle_flow()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 876 che coinvolge il nodo canonico `._toggle_flow()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 876 che coinvolge il nodo canonico `._toggle_flow()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 876 che coinvolge il nodo canonico `._toggle_flow()` è `L132`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 876 che coinvolge il nodo canonico `._toggle_flow()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 876 che coinvolge il nodo canonico `._toggle_flow()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 876 che coinvolge il nodo canonico `._toggle_flow()` è `1.0`. Nel grafo unificato, l'arco 876 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_flow_tags` per la relazione che coinvolge il nodo canonico `._toggle_flow()`. Nel grafo unificato, l'arco 876 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_toggle_flow` per la relazione che coinvolge il nodo canonico `._toggle_flow()`.

### .get_values()

<!-- GRAPH_NODE graph=codice index=436 id=ui_tabs_config_tab_configtab_get_values -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.get_values()` è `.get_values()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.get_values()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.get_values()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.get_values()` è `L227`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.get_values()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.get_values()` possiede l'identificatore esatto `ui_tabs_config_tab_configtab_get_values`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.get_values()` alla comunità numerica `13`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.get_values()` è `.get_values()`.
<!-- GRAPH_EDGE graph=codice index=879 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.get_values()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._as_float()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_get_values` all'ID `ui_tabs_config_tab_configtab_as_float`. La relazione tecnica esplicita è la seguente: `.get_values()` chiama `._as_float()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 879 che coinvolge il nodo canonico `.get_values()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 879 che coinvolge il nodo canonico `.get_values()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 879 che coinvolge il nodo canonico `.get_values()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 879 che coinvolge il nodo canonico `.get_values()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 879 che coinvolge il nodo canonico `.get_values()` è `L231`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 879 che coinvolge il nodo canonico `.get_values()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 879 che coinvolge il nodo canonico `.get_values()` è `ast`. Nel grafo del codice RDG, l'arco 879 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_get_values` per la relazione che coinvolge il nodo canonico `.get_values()`. Nel grafo del codice RDG, l'arco 879 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_as_float` per la relazione che coinvolge il nodo canonico `.get_values()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 879 che coinvolge il nodo canonico `.get_values()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=880 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.get_values()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._as_int()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_get_values` all'ID `ui_tabs_config_tab_configtab_as_int`. La relazione tecnica esplicita è la seguente: `.get_values()` chiama `._as_int()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 880 che coinvolge il nodo canonico `.get_values()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 880 che coinvolge il nodo canonico `.get_values()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 880 che coinvolge il nodo canonico `.get_values()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 880 che coinvolge il nodo canonico `.get_values()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 880 che coinvolge il nodo canonico `.get_values()` è `L230`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 880 che coinvolge il nodo canonico `.get_values()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 880 che coinvolge il nodo canonico `.get_values()` è `ast`. Nel grafo del codice RDG, l'arco 880 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_get_values` per la relazione che coinvolge il nodo canonico `.get_values()`. Nel grafo del codice RDG, l'arco 880 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_as_int` per la relazione che coinvolge il nodo canonico `.get_values()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 880 che coinvolge il nodo canonico `.get_values()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=860 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get_values()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_get_values`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `.get_values()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 860 che coinvolge il nodo canonico `.get_values()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 860 che coinvolge il nodo canonico `.get_values()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 860 che coinvolge il nodo canonico `.get_values()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 860 che coinvolge il nodo canonico `.get_values()` è `L227`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 860 che coinvolge il nodo canonico `.get_values()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 860 che coinvolge il nodo canonico `.get_values()` è `ast`. Nel grafo del codice RDG, l'arco 860 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `.get_values()`. Nel grafo del codice RDG, l'arco 860 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_get_values` per la relazione che coinvolge il nodo canonico `.get_values()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 860 che coinvolge il nodo canonico `.get_values()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=881 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.get_values()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.save()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_save` all'ID `ui_tabs_config_tab_configtab_get_values`. La relazione tecnica esplicita è la seguente: `.save()` chiama `.get_values()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 881 che coinvolge il nodo canonico `.get_values()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 881 che coinvolge il nodo canonico `.get_values()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 881 che coinvolge il nodo canonico `.get_values()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 881 che coinvolge il nodo canonico `.get_values()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 881 che coinvolge il nodo canonico `.get_values()` è `L239`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 881 che coinvolge il nodo canonico `.get_values()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 881 che coinvolge il nodo canonico `.get_values()` è `ast`. Nel grafo del codice RDG, l'arco 881 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_save` per la relazione che coinvolge il nodo canonico `.get_values()`. Nel grafo del codice RDG, l'arco 881 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_get_values` per la relazione che coinvolge il nodo canonico `.get_values()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 881 che coinvolge il nodo canonico `.get_values()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=436 id=Request-Dataset-Generator::ui_tabs_config_tab_configtab_get_values -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.get_values()` è `.get_values()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.get_values()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.get_values()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.get_values()` è `L227`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.get_values()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.get_values()` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.get_values()` è `.get_values()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.get_values()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.get_values()` è `ui_tabs_config_tab_configtab_get_values`. Nel grafo unificato, il nodo canonico `.get_values()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_config_tab_configtab_get_values`.
<!-- GRAPH_EDGE graph=unificata index=879 role=outgoing -->
Nel grafo unificato, il nodo canonico `.get_values()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._as_float()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_get_values` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_float`. La relazione tecnica esplicita è la seguente: `.get_values()` chiama `._as_float()`. Nel grafo unificato, il tipo dichiarato dell'arco 879 che coinvolge il nodo canonico `.get_values()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 879 che coinvolge il nodo canonico `.get_values()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 879 che coinvolge il nodo canonico `.get_values()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 879 che coinvolge il nodo canonico `.get_values()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 879 che coinvolge il nodo canonico `.get_values()` è `L231`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 879 che coinvolge il nodo canonico `.get_values()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 879 che coinvolge il nodo canonico `.get_values()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 879 che coinvolge il nodo canonico `.get_values()` è `1.0`. Nel grafo unificato, l'arco 879 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_get_values` per la relazione che coinvolge il nodo canonico `.get_values()`. Nel grafo unificato, l'arco 879 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_float` per la relazione che coinvolge il nodo canonico `.get_values()`.
<!-- GRAPH_EDGE graph=unificata index=880 role=outgoing -->
Nel grafo unificato, il nodo canonico `.get_values()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._as_int()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_get_values` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_int`. La relazione tecnica esplicita è la seguente: `.get_values()` chiama `._as_int()`. Nel grafo unificato, il tipo dichiarato dell'arco 880 che coinvolge il nodo canonico `.get_values()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 880 che coinvolge il nodo canonico `.get_values()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 880 che coinvolge il nodo canonico `.get_values()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 880 che coinvolge il nodo canonico `.get_values()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 880 che coinvolge il nodo canonico `.get_values()` è `L230`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 880 che coinvolge il nodo canonico `.get_values()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 880 che coinvolge il nodo canonico `.get_values()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 880 che coinvolge il nodo canonico `.get_values()` è `1.0`. Nel grafo unificato, l'arco 880 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_get_values` per la relazione che coinvolge il nodo canonico `.get_values()`. Nel grafo unificato, l'arco 880 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_int` per la relazione che coinvolge il nodo canonico `.get_values()`.
<!-- GRAPH_EDGE graph=unificata index=881 role=outgoing -->
Nel grafo unificato, il nodo canonico `.get_values()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.save()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_get_values` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_save`. La relazione tecnica esplicita è la seguente: `.get_values()` chiama `.save()`. Nel grafo unificato, il tipo dichiarato dell'arco 881 che coinvolge il nodo canonico `.get_values()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 881 che coinvolge il nodo canonico `.get_values()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 881 che coinvolge il nodo canonico `.get_values()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 881 che coinvolge il nodo canonico `.get_values()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 881 che coinvolge il nodo canonico `.get_values()` è `L239`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 881 che coinvolge il nodo canonico `.get_values()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 881 che coinvolge il nodo canonico `.get_values()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 881 che coinvolge il nodo canonico `.get_values()` è `1.0`. Nel grafo unificato, l'arco 881 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_get_values` per la relazione che coinvolge il nodo canonico `.get_values()`. Nel grafo unificato, l'arco 881 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_save` per la relazione che coinvolge il nodo canonico `.get_values()`.
<!-- GRAPH_EDGE graph=unificata index=860 role=incoming -->
Nel grafo unificato, il nodo canonico `.get_values()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_get_values`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `.get_values()`. Nel grafo unificato, il tipo dichiarato dell'arco 860 che coinvolge il nodo canonico `.get_values()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 860 che coinvolge il nodo canonico `.get_values()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 860 che coinvolge il nodo canonico `.get_values()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 860 che coinvolge il nodo canonico `.get_values()` è `L227`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 860 che coinvolge il nodo canonico `.get_values()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 860 che coinvolge il nodo canonico `.get_values()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 860 che coinvolge il nodo canonico `.get_values()` è `1.0`. Nel grafo unificato, l'arco 860 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `.get_values()`. Nel grafo unificato, l'arco 860 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_get_values` per la relazione che coinvolge il nodo canonico `.get_values()`.

### .load_values()

<!-- GRAPH_NODE graph=codice index=435 id=ui_tabs_config_tab_configtab_load_values -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.load_values()` è `.load_values()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.load_values()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.load_values()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.load_values()` è `L212`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.load_values()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.load_values()` possiede l'identificatore esatto `ui_tabs_config_tab_configtab_load_values`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.load_values()` alla comunità numerica `13`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.load_values()` è `.load_values()`.
<!-- GRAPH_EDGE graph=codice index=877 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.load_values()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._refresh_flow_tags()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_load_values` all'ID `ui_tabs_config_tab_configtab_refresh_flow_tags`. La relazione tecnica esplicita è la seguente: `.load_values()` chiama `._refresh_flow_tags()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 877 che coinvolge il nodo canonico `.load_values()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 877 che coinvolge il nodo canonico `.load_values()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 877 che coinvolge il nodo canonico `.load_values()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 877 che coinvolge il nodo canonico `.load_values()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 877 che coinvolge il nodo canonico `.load_values()` è `L225`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 877 che coinvolge il nodo canonico `.load_values()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 877 che coinvolge il nodo canonico `.load_values()` è `ast`. Nel grafo del codice RDG, l'arco 877 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_load_values` per la relazione che coinvolge il nodo canonico `.load_values()`. Nel grafo del codice RDG, l'arco 877 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_refresh_flow_tags` per la relazione che coinvolge il nodo canonico `.load_values()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 877 che coinvolge il nodo canonico `.load_values()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=862 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.load_values()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_load_values`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `.load_values()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 862 che coinvolge il nodo canonico `.load_values()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 862 che coinvolge il nodo canonico `.load_values()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 862 che coinvolge il nodo canonico `.load_values()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 862 che coinvolge il nodo canonico `.load_values()` è `L212`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 862 che coinvolge il nodo canonico `.load_values()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 862 che coinvolge il nodo canonico `.load_values()` è `ast`. Nel grafo del codice RDG, l'arco 862 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `.load_values()`. Nel grafo del codice RDG, l'arco 862 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_load_values` per la relazione che coinvolge il nodo canonico `.load_values()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 862 che coinvolge il nodo canonico `.load_values()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=870 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.load_values()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_init` all'ID `ui_tabs_config_tab_configtab_load_values`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_tabs_config_tab_configtab_init]` chiama `.load_values()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 870 che coinvolge il nodo canonico `.load_values()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 870 che coinvolge il nodo canonico `.load_values()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 870 che coinvolge il nodo canonico `.load_values()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 870 che coinvolge il nodo canonico `.load_values()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 870 che coinvolge il nodo canonico `.load_values()` è `L46`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 870 che coinvolge il nodo canonico `.load_values()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 870 che coinvolge il nodo canonico `.load_values()` è `ast`. Nel grafo del codice RDG, l'arco 870 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_init` per la relazione che coinvolge il nodo canonico `.load_values()`. Nel grafo del codice RDG, l'arco 870 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_load_values` per la relazione che coinvolge il nodo canonico `.load_values()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 870 che coinvolge il nodo canonico `.load_values()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=878 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.load_values()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.reset_defaults()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_reset_defaults` all'ID `ui_tabs_config_tab_configtab_load_values`. La relazione tecnica esplicita è la seguente: `.reset_defaults()` chiama `.load_values()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 878 che coinvolge il nodo canonico `.load_values()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 878 che coinvolge il nodo canonico `.load_values()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 878 che coinvolge il nodo canonico `.load_values()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 878 che coinvolge il nodo canonico `.load_values()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 878 che coinvolge il nodo canonico `.load_values()` è `L245`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 878 che coinvolge il nodo canonico `.load_values()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 878 che coinvolge il nodo canonico `.load_values()` è `ast`. Nel grafo del codice RDG, l'arco 878 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_reset_defaults` per la relazione che coinvolge il nodo canonico `.load_values()`. Nel grafo del codice RDG, l'arco 878 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_load_values` per la relazione che coinvolge il nodo canonico `.load_values()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 878 che coinvolge il nodo canonico `.load_values()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=435 id=Request-Dataset-Generator::ui_tabs_config_tab_configtab_load_values -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.load_values()` è `.load_values()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.load_values()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.load_values()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.load_values()` è `L212`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.load_values()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.load_values()` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.load_values()` è `.load_values()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.load_values()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.load_values()` è `ui_tabs_config_tab_configtab_load_values`. Nel grafo unificato, il nodo canonico `.load_values()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_config_tab_configtab_load_values`.
<!-- GRAPH_EDGE graph=unificata index=877 role=outgoing -->
Nel grafo unificato, il nodo canonico `.load_values()` è la sorgente `source` della relazione `calls` verso il nodo canonico `._refresh_flow_tags()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_load_values` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_refresh_flow_tags`. La relazione tecnica esplicita è la seguente: `.load_values()` chiama `._refresh_flow_tags()`. Nel grafo unificato, il tipo dichiarato dell'arco 877 che coinvolge il nodo canonico `.load_values()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 877 che coinvolge il nodo canonico `.load_values()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 877 che coinvolge il nodo canonico `.load_values()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 877 che coinvolge il nodo canonico `.load_values()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 877 che coinvolge il nodo canonico `.load_values()` è `L225`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 877 che coinvolge il nodo canonico `.load_values()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 877 che coinvolge il nodo canonico `.load_values()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 877 che coinvolge il nodo canonico `.load_values()` è `1.0`. Nel grafo unificato, l'arco 877 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_load_values` per la relazione che coinvolge il nodo canonico `.load_values()`. Nel grafo unificato, l'arco 877 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_refresh_flow_tags` per la relazione che coinvolge il nodo canonico `.load_values()`.
<!-- GRAPH_EDGE graph=unificata index=878 role=outgoing -->
Nel grafo unificato, il nodo canonico `.load_values()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.reset_defaults()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_load_values` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_reset_defaults`. La relazione tecnica esplicita è la seguente: `.load_values()` chiama `.reset_defaults()`. Nel grafo unificato, il tipo dichiarato dell'arco 878 che coinvolge il nodo canonico `.load_values()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 878 che coinvolge il nodo canonico `.load_values()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 878 che coinvolge il nodo canonico `.load_values()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 878 che coinvolge il nodo canonico `.load_values()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 878 che coinvolge il nodo canonico `.load_values()` è `L245`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 878 che coinvolge il nodo canonico `.load_values()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 878 che coinvolge il nodo canonico `.load_values()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 878 che coinvolge il nodo canonico `.load_values()` è `1.0`. Nel grafo unificato, l'arco 878 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_load_values` per la relazione che coinvolge il nodo canonico `.load_values()`. Nel grafo unificato, l'arco 878 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_reset_defaults` per la relazione che coinvolge il nodo canonico `.load_values()`.
<!-- GRAPH_EDGE graph=unificata index=862 role=incoming -->
Nel grafo unificato, il nodo canonico `.load_values()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_load_values`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `.load_values()`. Nel grafo unificato, il tipo dichiarato dell'arco 862 che coinvolge il nodo canonico `.load_values()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 862 che coinvolge il nodo canonico `.load_values()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 862 che coinvolge il nodo canonico `.load_values()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 862 che coinvolge il nodo canonico `.load_values()` è `L212`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 862 che coinvolge il nodo canonico `.load_values()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 862 che coinvolge il nodo canonico `.load_values()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 862 che coinvolge il nodo canonico `.load_values()` è `1.0`. Nel grafo unificato, l'arco 862 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `.load_values()`. Nel grafo unificato, l'arco 862 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_load_values` per la relazione che coinvolge il nodo canonico `.load_values()`.
<!-- GRAPH_EDGE graph=unificata index=870 role=incoming -->
Nel grafo unificato, il nodo canonico `.load_values()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_init` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_load_values`. La relazione tecnica esplicita è la seguente: `.__init__() [ui_tabs_config_tab_configtab_init]` chiama `.load_values()`. Nel grafo unificato, il tipo dichiarato dell'arco 870 che coinvolge il nodo canonico `.load_values()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 870 che coinvolge il nodo canonico `.load_values()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 870 che coinvolge il nodo canonico `.load_values()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 870 che coinvolge il nodo canonico `.load_values()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 870 che coinvolge il nodo canonico `.load_values()` è `L46`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 870 che coinvolge il nodo canonico `.load_values()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 870 che coinvolge il nodo canonico `.load_values()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 870 che coinvolge il nodo canonico `.load_values()` è `1.0`. Nel grafo unificato, l'arco 870 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_init` per la relazione che coinvolge il nodo canonico `.load_values()`. Nel grafo unificato, l'arco 870 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_load_values` per la relazione che coinvolge il nodo canonico `.load_values()`.

### .reset_defaults()

<!-- GRAPH_NODE graph=codice index=438 id=ui_tabs_config_tab_configtab_reset_defaults -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.reset_defaults()` è `.reset_defaults()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.reset_defaults()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.reset_defaults()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.reset_defaults()` è `L244`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.reset_defaults()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.reset_defaults()` possiede l'identificatore esatto `ui_tabs_config_tab_configtab_reset_defaults`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.reset_defaults()` alla comunità numerica `13`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.reset_defaults()` è `.reset_defaults()`.
<!-- GRAPH_EDGE graph=codice index=878 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.reset_defaults()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.load_values()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_reset_defaults` all'ID `ui_tabs_config_tab_configtab_load_values`. La relazione tecnica esplicita è la seguente: `.reset_defaults()` chiama `.load_values()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 878 che coinvolge il nodo canonico `.reset_defaults()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 878 che coinvolge il nodo canonico `.reset_defaults()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 878 che coinvolge il nodo canonico `.reset_defaults()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 878 che coinvolge il nodo canonico `.reset_defaults()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 878 che coinvolge il nodo canonico `.reset_defaults()` è `L245`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 878 che coinvolge il nodo canonico `.reset_defaults()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 878 che coinvolge il nodo canonico `.reset_defaults()` è `ast`. Nel grafo del codice RDG, l'arco 878 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_reset_defaults` per la relazione che coinvolge il nodo canonico `.reset_defaults()`. Nel grafo del codice RDG, l'arco 878 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_load_values` per la relazione che coinvolge il nodo canonico `.reset_defaults()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 878 che coinvolge il nodo canonico `.reset_defaults()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=864 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.reset_defaults()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_reset_defaults`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `.reset_defaults()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 864 che coinvolge il nodo canonico `.reset_defaults()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 864 che coinvolge il nodo canonico `.reset_defaults()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 864 che coinvolge il nodo canonico `.reset_defaults()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 864 che coinvolge il nodo canonico `.reset_defaults()` è `L244`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 864 che coinvolge il nodo canonico `.reset_defaults()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 864 che coinvolge il nodo canonico `.reset_defaults()` è `ast`. Nel grafo del codice RDG, l'arco 864 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `.reset_defaults()`. Nel grafo del codice RDG, l'arco 864 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_reset_defaults` per la relazione che coinvolge il nodo canonico `.reset_defaults()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 864 che coinvolge il nodo canonico `.reset_defaults()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=438 id=Request-Dataset-Generator::ui_tabs_config_tab_configtab_reset_defaults -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.reset_defaults()` è `.reset_defaults()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.reset_defaults()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.reset_defaults()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.reset_defaults()` è `L244`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.reset_defaults()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.reset_defaults()` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.reset_defaults()` è `.reset_defaults()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.reset_defaults()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.reset_defaults()` è `ui_tabs_config_tab_configtab_reset_defaults`. Nel grafo unificato, il nodo canonico `.reset_defaults()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_config_tab_configtab_reset_defaults`.
Nel grafo unificato, il nodo canonico `.reset_defaults()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=864 role=incoming -->
Nel grafo unificato, il nodo canonico `.reset_defaults()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_reset_defaults`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `.reset_defaults()`. Nel grafo unificato, il tipo dichiarato dell'arco 864 che coinvolge il nodo canonico `.reset_defaults()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 864 che coinvolge il nodo canonico `.reset_defaults()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 864 che coinvolge il nodo canonico `.reset_defaults()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 864 che coinvolge il nodo canonico `.reset_defaults()` è `L244`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 864 che coinvolge il nodo canonico `.reset_defaults()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 864 che coinvolge il nodo canonico `.reset_defaults()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 864 che coinvolge il nodo canonico `.reset_defaults()` è `1.0`. Nel grafo unificato, l'arco 864 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `.reset_defaults()`. Nel grafo unificato, l'arco 864 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_reset_defaults` per la relazione che coinvolge il nodo canonico `.reset_defaults()`.
<!-- GRAPH_EDGE graph=unificata index=878 role=incoming -->
Nel grafo unificato, il nodo canonico `.reset_defaults()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.load_values()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_load_values` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_reset_defaults`. La relazione tecnica esplicita è la seguente: `.load_values()` chiama `.reset_defaults()`. Nel grafo unificato, il tipo dichiarato dell'arco 878 che coinvolge il nodo canonico `.reset_defaults()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 878 che coinvolge il nodo canonico `.reset_defaults()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 878 che coinvolge il nodo canonico `.reset_defaults()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 878 che coinvolge il nodo canonico `.reset_defaults()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 878 che coinvolge il nodo canonico `.reset_defaults()` è `L245`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 878 che coinvolge il nodo canonico `.reset_defaults()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 878 che coinvolge il nodo canonico `.reset_defaults()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 878 che coinvolge il nodo canonico `.reset_defaults()` è `1.0`. Nel grafo unificato, l'arco 878 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_load_values` per la relazione che coinvolge il nodo canonico `.reset_defaults()`. Nel grafo unificato, l'arco 878 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_reset_defaults` per la relazione che coinvolge il nodo canonico `.reset_defaults()`.

### .save()

<!-- GRAPH_NODE graph=codice index=437 id=ui_tabs_config_tab_configtab_save -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `.save()` è `.save()`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `.save()` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `.save()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `.save()` è `L238`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `.save()` vale `ast`. Nel grafo del codice RDG, il nodo canonico `.save()` possiede l'identificatore esatto `ui_tabs_config_tab_configtab_save`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `.save()` alla comunità numerica `13`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `.save()` è `.save()`.
<!-- GRAPH_EDGE graph=codice index=881 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `.save()` è la sorgente `source` della relazione `calls` verso il nodo canonico `.get_values()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab_save` all'ID `ui_tabs_config_tab_configtab_get_values`. La relazione tecnica esplicita è la seguente: `.save()` chiama `.get_values()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 881 che coinvolge il nodo canonico `.save()` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 881 che coinvolge il nodo canonico `.save()` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 881 che coinvolge il nodo canonico `.save()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 881 che coinvolge il nodo canonico `.save()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 881 che coinvolge il nodo canonico `.save()` è `L239`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 881 che coinvolge il nodo canonico `.save()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 881 che coinvolge il nodo canonico `.save()` è `ast`. Nel grafo del codice RDG, l'arco 881 registra come `source` l'identificatore `ui_tabs_config_tab_configtab_save` per la relazione che coinvolge il nodo canonico `.save()`. Nel grafo del codice RDG, l'arco 881 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_get_values` per la relazione che coinvolge il nodo canonico `.save()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 881 che coinvolge il nodo canonico `.save()` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=865 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `.save()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_save`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `.save()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 865 che coinvolge il nodo canonico `.save()` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 865 che coinvolge il nodo canonico `.save()` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 865 che coinvolge il nodo canonico `.save()` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 865 che coinvolge il nodo canonico `.save()` è `L238`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 865 che coinvolge il nodo canonico `.save()` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 865 che coinvolge il nodo canonico `.save()` è `ast`. Nel grafo del codice RDG, l'arco 865 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `.save()`. Nel grafo del codice RDG, l'arco 865 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_save` per la relazione che coinvolge il nodo canonico `.save()`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 865 che coinvolge il nodo canonico `.save()` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=437 id=Request-Dataset-Generator::ui_tabs_config_tab_configtab_save -->
Nel grafo unificato, l'etichetta originale del nodo canonico `.save()` è `.save()`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `.save()` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `.save()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `.save()` è `L238`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `.save()` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `.save()` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `.save()` è `.save()`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `.save()` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `.save()` è `ui_tabs_config_tab_configtab_save`. Nel grafo unificato, il nodo canonico `.save()` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_config_tab_configtab_save`.
Nel grafo unificato, il nodo canonico `.save()` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=865 role=incoming -->
Nel grafo unificato, il nodo canonico `.save()` è il destinatario `target` della relazione `method` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_save`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `.save()`. Nel grafo unificato, il tipo dichiarato dell'arco 865 che coinvolge il nodo canonico `.save()` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 865 che coinvolge il nodo canonico `.save()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 865 che coinvolge il nodo canonico `.save()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 865 che coinvolge il nodo canonico `.save()` è `L238`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 865 che coinvolge il nodo canonico `.save()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 865 che coinvolge il nodo canonico `.save()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 865 che coinvolge il nodo canonico `.save()` è `1.0`. Nel grafo unificato, l'arco 865 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `.save()`. Nel grafo unificato, l'arco 865 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_save` per la relazione che coinvolge il nodo canonico `.save()`.
<!-- GRAPH_EDGE graph=unificata index=881 role=incoming -->
Nel grafo unificato, il nodo canonico `.save()` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `.get_values()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_get_values` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_save`. La relazione tecnica esplicita è la seguente: `.get_values()` chiama `.save()`. Nel grafo unificato, il tipo dichiarato dell'arco 881 che coinvolge il nodo canonico `.save()` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 881 che coinvolge il nodo canonico `.save()` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 881 che coinvolge il nodo canonico `.save()` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 881 che coinvolge il nodo canonico `.save()` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 881 che coinvolge il nodo canonico `.save()` è `L239`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 881 che coinvolge il nodo canonico `.save()` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 881 che coinvolge il nodo canonico `.save()` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 881 che coinvolge il nodo canonico `.save()` è `1.0`. Nel grafo unificato, l'arco 881 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_get_values` per la relazione che coinvolge il nodo canonico `.save()`. Nel grafo unificato, l'arco 881 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_save` per la relazione che coinvolge il nodo canonico `.save()`.

### ConfigTab

<!-- GRAPH_NODE graph=codice index=427 id=ui_tabs_config_tab_configtab -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `ConfigTab` è `ConfigTab`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `ConfigTab` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `ConfigTab` è `L28`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `ConfigTab` vale `ast`. Nel grafo del codice RDG, il nodo canonico `ConfigTab` possiede l'identificatore esatto `ui_tabs_config_tab_configtab`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `ConfigTab` alla comunità numerica `13`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `ConfigTab` è `configtab`.
<!-- GRAPH_EDGE graph=codice index=853 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._as_float()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_as_float`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._as_float()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 853 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 853 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 853 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 853 che coinvolge il nodo canonico `ConfigTab` è `L273`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 853 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 853 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo del codice RDG, l'arco 853 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, l'arco 853 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_as_float` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 853 che coinvolge il nodo canonico `ConfigTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=854 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._as_int()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_as_int`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._as_int()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 854 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 854 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 854 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 854 che coinvolge il nodo canonico `ConfigTab` è `L266`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 854 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 854 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo del codice RDG, l'arco 854 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, l'arco 854 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_as_int` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 854 che coinvolge il nodo canonico `ConfigTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=855 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_build`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 855 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 855 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 855 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 855 che coinvolge il nodo canonico `ConfigTab` è `L49`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 855 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 855 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo del codice RDG, l'arco 855 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, l'arco 855 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 855 che coinvolge il nodo canonico `ConfigTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=856 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_build_buttons`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 856 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 856 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 856 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 856 che coinvolge il nodo canonico `ConfigTab` è `L180`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 856 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 856 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo del codice RDG, l'arco 856 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, l'arco 856 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_build_buttons` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 856 che coinvolge il nodo canonico `ConfigTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=857 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_flow_tags()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_build_flow_tags`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._build_flow_tags()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 857 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 857 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 857 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 857 che coinvolge il nodo canonico `ConfigTab` è `L119`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 857 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 857 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo del codice RDG, l'arco 857 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, l'arco 857 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_build_flow_tags` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 857 che coinvolge il nodo canonico `ConfigTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=858 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_middleware_grid()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_build_middleware_grid`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._build_middleware_grid()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 858 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 858 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 858 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 858 che coinvolge il nodo canonico `ConfigTab` è `L137`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 858 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 858 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo del codice RDG, l'arco 858 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, l'arco 858 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_build_middleware_grid` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 858 che coinvolge il nodo canonico `ConfigTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=859 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_parameter_grid()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_build_parameter_grid`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._build_parameter_grid()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 859 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 859 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 859 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 859 che coinvolge il nodo canonico `ConfigTab` è `L74`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 859 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 859 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo del codice RDG, l'arco 859 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, l'arco 859 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_build_parameter_grid` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 859 che coinvolge il nodo canonico `ConfigTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=860 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `.get_values()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_get_values`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `.get_values()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 860 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 860 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 860 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 860 che coinvolge il nodo canonico `ConfigTab` è `L227`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 860 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 860 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo del codice RDG, l'arco 860 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, l'arco 860 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_get_values` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 860 che coinvolge il nodo canonico `ConfigTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=861 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_init`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `.__init__() [ui_tabs_config_tab_configtab_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 861 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 861 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 861 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 861 che coinvolge il nodo canonico `ConfigTab` è `L39`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 861 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 861 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo del codice RDG, l'arco 861 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, l'arco 861 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_init` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 861 che coinvolge il nodo canonico `ConfigTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=862 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `.load_values()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_load_values`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `.load_values()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 862 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 862 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 862 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 862 che coinvolge il nodo canonico `ConfigTab` è `L212`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 862 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 862 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo del codice RDG, l'arco 862 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, l'arco 862 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_load_values` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 862 che coinvolge il nodo canonico `ConfigTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=863 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._refresh_flow_tags()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_refresh_flow_tags`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._refresh_flow_tags()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 863 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 863 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 863 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 863 che coinvolge il nodo canonico `ConfigTab` è `L254`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 863 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 863 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo del codice RDG, l'arco 863 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, l'arco 863 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_refresh_flow_tags` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 863 che coinvolge il nodo canonico `ConfigTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=864 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `.reset_defaults()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_reset_defaults`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `.reset_defaults()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 864 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 864 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 864 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 864 che coinvolge il nodo canonico `ConfigTab` è `L244`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 864 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 864 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo del codice RDG, l'arco 864 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, l'arco 864 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_reset_defaults` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 864 che coinvolge il nodo canonico `ConfigTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=865 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `.save()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_save`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `.save()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 865 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 865 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 865 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 865 che coinvolge il nodo canonico `ConfigTab` è `L238`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 865 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 865 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo del codice RDG, l'arco 865 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, l'arco 865 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_save` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 865 che coinvolge il nodo canonico `ConfigTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=866 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._section()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_section`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._section()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 866 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 866 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 866 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 866 che coinvolge il nodo canonico `ConfigTab` è `L64`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 866 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 866 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo del codice RDG, l'arco 866 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, l'arco 866 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_section` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 866 che coinvolge il nodo canonico `ConfigTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=867 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._toggle_flow()`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_configtab` all'ID `ui_tabs_config_tab_configtab_toggle_flow`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._toggle_flow()`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 867 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 867 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 867 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 867 che coinvolge il nodo canonico `ConfigTab` è `L247`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 867 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 867 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo del codice RDG, l'arco 867 registra come `source` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, l'arco 867 registra come `target` l'identificatore `ui_tabs_config_tab_configtab_toggle_flow` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 867 che coinvolge il nodo canonico `ConfigTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=824 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ConfigTab` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `app.py`; l'orientamento registrato nel JSON va dall'ID `ui_app` all'ID `ui_tabs_config_tab_configtab`. La relazione tecnica esplicita è la seguente: `app.py` importa `ConfigTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 824 che coinvolge il nodo canonico `ConfigTab` è `imports`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 824 che coinvolge il nodo canonico `ConfigTab` è `import`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 824 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 824 che coinvolge il nodo canonico `ConfigTab` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 824 che coinvolge il nodo canonico `ConfigTab` è `L26`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 824 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 824 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo del codice RDG, l'arco 824 registra come `source` l'identificatore `ui_app` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, l'arco 824 registra come `target` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 824 che coinvolge il nodo canonico `ConfigTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=836 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ConfigTab` è il destinatario `target` della relazione `uses` proveniente dal nodo canonico `RDGApp`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp` all'ID `ui_tabs_config_tab_configtab`. La relazione tecnica esplicita è la seguente: `RDGApp` usa `ConfigTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 836 che coinvolge il nodo canonico `ConfigTab` è `uses`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 836 che coinvolge il nodo canonico `ConfigTab` è `INFERRED`. Nel grafo del codice RDG, il file sorgente dell'arco 836 che coinvolge il nodo canonico `ConfigTab` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 836 che coinvolge il nodo canonico `ConfigTab` è `L26`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 836 che coinvolge il nodo canonico `ConfigTab` è `0.8`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 836 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo del codice RDG, l'arco 836 registra come `source` l'identificatore `ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, l'arco 836 registra come `target` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 836 che coinvolge il nodo canonico `ConfigTab` è `0.5`.
<!-- GRAPH_EDGE graph=codice index=842 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ConfigTab` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build_tabs()`; l'orientamento registrato nel JSON va dall'ID `ui_app_rdgapp_build_tabs` all'ID `ui_tabs_config_tab_configtab`. La relazione tecnica esplicita è la seguente: `._build_tabs()` chiama `ConfigTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 842 che coinvolge il nodo canonico `ConfigTab` è `calls`. Nel grafo del codice RDG, il contesto dichiarato per l'arco 842 che coinvolge il nodo canonico `ConfigTab` è `call`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 842 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 842 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 842 che coinvolge il nodo canonico `ConfigTab` è `ui/app.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 842 che coinvolge il nodo canonico `ConfigTab` è `L114`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 842 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 842 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo del codice RDG, l'arco 842 registra come `source` l'identificatore `ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, l'arco 842 registra come `target` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`.
<!-- GRAPH_EDGE graph=codice index=850 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ConfigTab` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `config_tab.py`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab` all'ID `ui_tabs_config_tab_configtab`. La relazione tecnica esplicita è la seguente: `config_tab.py` contiene `ConfigTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 850 che coinvolge il nodo canonico `ConfigTab` è `contains`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 850 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 850 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 850 che coinvolge il nodo canonico `ConfigTab` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 850 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 850 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo del codice RDG, l'arco 850 registra come `source` l'identificatore `ui_tabs_config_tab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, l'arco 850 registra come `target` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 850 che coinvolge il nodo canonico `ConfigTab` è `1.0`.
<!-- GRAPH_EDGE graph=codice index=868 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `ConfigTab` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Form grafico per i parametri dell'esperimento.`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_rationale_29` all'ID `ui_tabs_config_tab_configtab`. La relazione tecnica esplicita è la seguente: `Form grafico per i parametri dell'esperimento.` costituisce una motivazione per `ConfigTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 868 che coinvolge il nodo canonico `ConfigTab` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 868 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 868 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 868 che coinvolge il nodo canonico `ConfigTab` è `L29`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 868 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 868 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo del codice RDG, l'arco 868 registra come `source` l'identificatore `ui_tabs_config_tab_rationale_29` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, l'arco 868 registra come `target` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 868 che coinvolge il nodo canonico `ConfigTab` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=427 id=Request-Dataset-Generator::ui_tabs_config_tab_configtab -->
Nel grafo unificato, l'etichetta originale del nodo canonico `ConfigTab` è `ConfigTab`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `ConfigTab` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `ConfigTab` è `L28`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `ConfigTab` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `ConfigTab` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `ConfigTab` è `configtab`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `ConfigTab` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `ConfigTab` è `ui_tabs_config_tab_configtab`. Nel grafo unificato, il nodo canonico `ConfigTab` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_config_tab_configtab`.
<!-- GRAPH_EDGE graph=unificata index=853 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._as_float()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_float`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._as_float()`. Nel grafo unificato, il tipo dichiarato dell'arco 853 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 853 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 853 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 853 che coinvolge il nodo canonico `ConfigTab` è `L273`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 853 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 853 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 853 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, l'arco 853 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo unificato, l'arco 853 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_float` per la relazione che coinvolge il nodo canonico `ConfigTab`.
<!-- GRAPH_EDGE graph=unificata index=854 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._as_int()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_int`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._as_int()`. Nel grafo unificato, il tipo dichiarato dell'arco 854 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 854 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 854 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 854 che coinvolge il nodo canonico `ConfigTab` è `L266`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 854 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 854 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 854 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, l'arco 854 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo unificato, l'arco 854 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_as_int` per la relazione che coinvolge il nodo canonico `ConfigTab`.
<!-- GRAPH_EDGE graph=unificata index=855 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build() [ui_tabs_config_tab_configtab_build]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._build() [ui_tabs_config_tab_configtab_build]`. Nel grafo unificato, il tipo dichiarato dell'arco 855 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 855 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 855 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 855 che coinvolge il nodo canonico `ConfigTab` è `L49`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 855 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 855 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 855 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, l'arco 855 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo unificato, l'arco 855 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build` per la relazione che coinvolge il nodo canonico `ConfigTab`.
<!-- GRAPH_EDGE graph=unificata index=856 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_buttons`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._build_buttons() [ui_tabs_config_tab_configtab_build_buttons]`. Nel grafo unificato, il tipo dichiarato dell'arco 856 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 856 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 856 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 856 che coinvolge il nodo canonico `ConfigTab` è `L180`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 856 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 856 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 856 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, l'arco 856 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo unificato, l'arco 856 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_buttons` per la relazione che coinvolge il nodo canonico `ConfigTab`.
<!-- GRAPH_EDGE graph=unificata index=857 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_flow_tags()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_flow_tags`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._build_flow_tags()`. Nel grafo unificato, il tipo dichiarato dell'arco 857 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 857 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 857 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 857 che coinvolge il nodo canonico `ConfigTab` è `L119`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 857 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 857 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 857 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, l'arco 857 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo unificato, l'arco 857 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_flow_tags` per la relazione che coinvolge il nodo canonico `ConfigTab`.
<!-- GRAPH_EDGE graph=unificata index=858 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_middleware_grid()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_middleware_grid`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._build_middleware_grid()`. Nel grafo unificato, il tipo dichiarato dell'arco 858 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 858 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 858 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 858 che coinvolge il nodo canonico `ConfigTab` è `L137`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 858 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 858 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 858 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, l'arco 858 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo unificato, l'arco 858 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_middleware_grid` per la relazione che coinvolge il nodo canonico `ConfigTab`.
<!-- GRAPH_EDGE graph=unificata index=859 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._build_parameter_grid()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_parameter_grid`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._build_parameter_grid()`. Nel grafo unificato, il tipo dichiarato dell'arco 859 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 859 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 859 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 859 che coinvolge il nodo canonico `ConfigTab` è `L74`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 859 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 859 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 859 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, l'arco 859 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo unificato, l'arco 859 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_build_parameter_grid` per la relazione che coinvolge il nodo canonico `ConfigTab`.
<!-- GRAPH_EDGE graph=unificata index=860 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `.get_values()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_get_values`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `.get_values()`. Nel grafo unificato, il tipo dichiarato dell'arco 860 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 860 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 860 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 860 che coinvolge il nodo canonico `ConfigTab` è `L227`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 860 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 860 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 860 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, l'arco 860 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo unificato, l'arco 860 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_get_values` per la relazione che coinvolge il nodo canonico `ConfigTab`.
<!-- GRAPH_EDGE graph=unificata index=861 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `.__init__() [ui_tabs_config_tab_configtab_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_init`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `.__init__() [ui_tabs_config_tab_configtab_init]`. Nel grafo unificato, il tipo dichiarato dell'arco 861 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 861 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 861 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 861 che coinvolge il nodo canonico `ConfigTab` è `L39`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 861 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 861 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 861 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, l'arco 861 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo unificato, l'arco 861 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_init` per la relazione che coinvolge il nodo canonico `ConfigTab`.
<!-- GRAPH_EDGE graph=unificata index=862 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `.load_values()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_load_values`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `.load_values()`. Nel grafo unificato, il tipo dichiarato dell'arco 862 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 862 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 862 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 862 che coinvolge il nodo canonico `ConfigTab` è `L212`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 862 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 862 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 862 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, l'arco 862 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo unificato, l'arco 862 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_load_values` per la relazione che coinvolge il nodo canonico `ConfigTab`.
<!-- GRAPH_EDGE graph=unificata index=863 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._refresh_flow_tags()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_refresh_flow_tags`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._refresh_flow_tags()`. Nel grafo unificato, il tipo dichiarato dell'arco 863 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 863 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 863 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 863 che coinvolge il nodo canonico `ConfigTab` è `L254`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 863 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 863 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 863 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, l'arco 863 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo unificato, l'arco 863 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_refresh_flow_tags` per la relazione che coinvolge il nodo canonico `ConfigTab`.
<!-- GRAPH_EDGE graph=unificata index=864 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `.reset_defaults()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_reset_defaults`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `.reset_defaults()`. Nel grafo unificato, il tipo dichiarato dell'arco 864 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 864 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 864 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 864 che coinvolge il nodo canonico `ConfigTab` è `L244`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 864 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 864 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 864 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, l'arco 864 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo unificato, l'arco 864 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_reset_defaults` per la relazione che coinvolge il nodo canonico `ConfigTab`.
<!-- GRAPH_EDGE graph=unificata index=865 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `.save()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_save`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `.save()`. Nel grafo unificato, il tipo dichiarato dell'arco 865 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 865 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 865 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 865 che coinvolge il nodo canonico `ConfigTab` è `L238`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 865 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 865 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 865 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, l'arco 865 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo unificato, l'arco 865 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_save` per la relazione che coinvolge il nodo canonico `ConfigTab`.
<!-- GRAPH_EDGE graph=unificata index=866 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._section()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_section`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._section()`. Nel grafo unificato, il tipo dichiarato dell'arco 866 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 866 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 866 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 866 che coinvolge il nodo canonico `ConfigTab` è `L64`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 866 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 866 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 866 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, l'arco 866 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo unificato, l'arco 866 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_section` per la relazione che coinvolge il nodo canonico `ConfigTab`.
<!-- GRAPH_EDGE graph=unificata index=867 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `method` verso il nodo canonico `._toggle_flow()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab_toggle_flow`. La relazione tecnica esplicita è la seguente: `ConfigTab` registra come proprio metodo `._toggle_flow()`. Nel grafo unificato, il tipo dichiarato dell'arco 867 che coinvolge il nodo canonico `ConfigTab` è `method`. Nel grafo unificato, la confidenza categoriale dell'arco 867 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 867 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 867 che coinvolge il nodo canonico `ConfigTab` è `L247`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 867 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 867 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 867 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, l'arco 867 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo unificato, l'arco 867 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab_toggle_flow` per la relazione che coinvolge il nodo canonico `ConfigTab`.
<!-- GRAPH_EDGE graph=unificata index=868 role=outgoing -->
Nel grafo unificato, il nodo canonico `ConfigTab` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Form grafico per i parametri dell'esperimento.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_rationale_29`. La relazione tecnica esplicita è la seguente: `ConfigTab` costituisce una motivazione per `Form grafico per i parametri dell'esperimento.`. Nel grafo unificato, il tipo dichiarato dell'arco 868 che coinvolge il nodo canonico `ConfigTab` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 868 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 868 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 868 che coinvolge il nodo canonico `ConfigTab` è `L29`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 868 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 868 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 868 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, l'arco 868 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo unificato, l'arco 868 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_rationale_29` per la relazione che coinvolge il nodo canonico `ConfigTab`.
<!-- GRAPH_EDGE graph=unificata index=824 role=incoming -->
Nel grafo unificato, il nodo canonico `ConfigTab` è il destinatario `target` della relazione `imports` proveniente dal nodo canonico `app.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab`. La relazione tecnica esplicita è la seguente: `app.py` importa `ConfigTab`. Nel grafo unificato, il tipo dichiarato dell'arco 824 che coinvolge il nodo canonico `ConfigTab` è `imports`. Nel grafo unificato, il contesto dichiarato per l'arco 824 che coinvolge il nodo canonico `ConfigTab` è `import`. Nel grafo unificato, la confidenza categoriale dell'arco 824 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 824 che coinvolge il nodo canonico `ConfigTab` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 824 che coinvolge il nodo canonico `ConfigTab` è `L26`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 824 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 824 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 824 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, l'arco 824 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo unificato, l'arco 824 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`.
<!-- GRAPH_EDGE graph=unificata index=836 role=incoming -->
Nel grafo unificato, il nodo canonico `ConfigTab` è il destinatario `target` della relazione `uses` proveniente dal nodo canonico `RDGApp`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab`. La relazione tecnica esplicita è la seguente: `RDGApp` usa `ConfigTab`. Nel grafo unificato, il tipo dichiarato dell'arco 836 che coinvolge il nodo canonico `ConfigTab` è `uses`. Nel grafo unificato, la confidenza categoriale dell'arco 836 che coinvolge il nodo canonico `ConfigTab` è `INFERRED`. Nel grafo unificato, il file sorgente dell'arco 836 che coinvolge il nodo canonico `ConfigTab` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 836 che coinvolge il nodo canonico `ConfigTab` è `L26`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 836 che coinvolge il nodo canonico `ConfigTab` è `0.8`. Nel grafo unificato, la provenienza `_origin` dell'arco 836 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 836 che coinvolge il nodo canonico `ConfigTab` è `0.5`. Nel grafo unificato, l'arco 836 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo unificato, l'arco 836 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`.
<!-- GRAPH_EDGE graph=unificata index=842 role=incoming -->
Nel grafo unificato, il nodo canonico `ConfigTab` è il destinatario `target` della relazione `calls` proveniente dal nodo canonico `._build_tabs()`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_app_rdgapp_build_tabs` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab`. La relazione tecnica esplicita è la seguente: `._build_tabs()` chiama `ConfigTab`. Nel grafo unificato, il tipo dichiarato dell'arco 842 che coinvolge il nodo canonico `ConfigTab` è `calls`. Nel grafo unificato, il contesto dichiarato per l'arco 842 che coinvolge il nodo canonico `ConfigTab` è `call`. Nel grafo unificato, la confidenza categoriale dell'arco 842 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 842 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 842 che coinvolge il nodo canonico `ConfigTab` è `ui/app.py`. Nel grafo unificato, la posizione sorgente dell'arco 842 che coinvolge il nodo canonico `ConfigTab` è `L114`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 842 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 842 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo unificato, l'arco 842 registra come `source` l'identificatore `Request-Dataset-Generator::ui_app_rdgapp_build_tabs` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo unificato, l'arco 842 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`.
<!-- GRAPH_EDGE graph=unificata index=850 role=incoming -->
Nel grafo unificato, il nodo canonico `ConfigTab` è il destinatario `target` della relazione `contains` proveniente dal nodo canonico `config_tab.py`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab`. La relazione tecnica esplicita è la seguente: `config_tab.py` contiene `ConfigTab`. Nel grafo unificato, il tipo dichiarato dell'arco 850 che coinvolge il nodo canonico `ConfigTab` è `contains`. Nel grafo unificato, la confidenza categoriale dell'arco 850 che coinvolge il nodo canonico `ConfigTab` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 850 che coinvolge il nodo canonico `ConfigTab` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 850 che coinvolge il nodo canonico `ConfigTab` è `L28`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 850 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 850 che coinvolge il nodo canonico `ConfigTab` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 850 che coinvolge il nodo canonico `ConfigTab` è `1.0`. Nel grafo unificato, l'arco 850 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab` per la relazione che coinvolge il nodo canonico `ConfigTab`. Nel grafo unificato, l'arco 850 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `ConfigTab`.

### Form grafico per i parametri dell&#x27;esperimento.

<!-- GRAPH_NODE graph=codice index=444 id=ui_tabs_config_tab_rationale_29 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Form grafico per i parametri dell'esperimento.` è `Form grafico per i parametri dell'esperimento.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Form grafico per i parametri dell'esperimento.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Form grafico per i parametri dell'esperimento.` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Form grafico per i parametri dell'esperimento.` è `L29`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Form grafico per i parametri dell'esperimento.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Form grafico per i parametri dell'esperimento.` possiede l'identificatore esatto `ui_tabs_config_tab_rationale_29`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Form grafico per i parametri dell'esperimento.` alla comunità numerica `13`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Form grafico per i parametri dell'esperimento.` è `form grafico per i parametri dell'esperimento.`.
<!-- GRAPH_EDGE graph=codice index=868 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Form grafico per i parametri dell'esperimento.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_config_tab_rationale_29` all'ID `ui_tabs_config_tab_configtab`. La relazione tecnica esplicita è la seguente: `Form grafico per i parametri dell'esperimento.` costituisce una motivazione per `ConfigTab`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 868 che coinvolge il nodo canonico `Form grafico per i parametri dell'esperimento.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 868 che coinvolge il nodo canonico `Form grafico per i parametri dell'esperimento.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 868 che coinvolge il nodo canonico `Form grafico per i parametri dell'esperimento.` è `ui/tabs/config_tab.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 868 che coinvolge il nodo canonico `Form grafico per i parametri dell'esperimento.` è `L29`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 868 che coinvolge il nodo canonico `Form grafico per i parametri dell'esperimento.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 868 che coinvolge il nodo canonico `Form grafico per i parametri dell'esperimento.` è `ast`. Nel grafo del codice RDG, l'arco 868 registra come `source` l'identificatore `ui_tabs_config_tab_rationale_29` per la relazione che coinvolge il nodo canonico `Form grafico per i parametri dell'esperimento.`. Nel grafo del codice RDG, l'arco 868 registra come `target` l'identificatore `ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `Form grafico per i parametri dell'esperimento.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 868 che coinvolge il nodo canonico `Form grafico per i parametri dell'esperimento.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Form grafico per i parametri dell'esperimento.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=444 id=Request-Dataset-Generator::ui_tabs_config_tab_rationale_29 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Form grafico per i parametri dell'esperimento.` è `Form grafico per i parametri dell'esperimento.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Form grafico per i parametri dell'esperimento.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Form grafico per i parametri dell'esperimento.` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Form grafico per i parametri dell'esperimento.` è `L29`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Form grafico per i parametri dell'esperimento.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Form grafico per i parametri dell'esperimento.` alla comunità numerica `13`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Form grafico per i parametri dell'esperimento.` è `form grafico per i parametri dell'esperimento.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Form grafico per i parametri dell'esperimento.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Form grafico per i parametri dell'esperimento.` è `ui_tabs_config_tab_rationale_29`. Nel grafo unificato, il nodo canonico `Form grafico per i parametri dell'esperimento.` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_config_tab_rationale_29`.
Nel grafo unificato, il nodo canonico `Form grafico per i parametri dell'esperimento.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=868 role=incoming -->
Nel grafo unificato, il nodo canonico `Form grafico per i parametri dell'esperimento.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `ConfigTab`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_config_tab_configtab` all'ID `Request-Dataset-Generator::ui_tabs_config_tab_rationale_29`. La relazione tecnica esplicita è la seguente: `ConfigTab` costituisce una motivazione per `Form grafico per i parametri dell'esperimento.`. Nel grafo unificato, il tipo dichiarato dell'arco 868 che coinvolge il nodo canonico `Form grafico per i parametri dell'esperimento.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 868 che coinvolge il nodo canonico `Form grafico per i parametri dell'esperimento.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 868 che coinvolge il nodo canonico `Form grafico per i parametri dell'esperimento.` è `ui/tabs/config_tab.py`. Nel grafo unificato, la posizione sorgente dell'arco 868 che coinvolge il nodo canonico `Form grafico per i parametri dell'esperimento.` è `L29`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 868 che coinvolge il nodo canonico `Form grafico per i parametri dell'esperimento.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 868 che coinvolge il nodo canonico `Form grafico per i parametri dell'esperimento.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 868 che coinvolge il nodo canonico `Form grafico per i parametri dell'esperimento.` è `1.0`. Nel grafo unificato, l'arco 868 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_configtab` per la relazione che coinvolge il nodo canonico `Form grafico per i parametri dell'esperimento.`. Nel grafo unificato, l'arco 868 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_config_tab_rationale_29` per la relazione che coinvolge il nodo canonico `Form grafico per i parametri dell'esperimento.`.
