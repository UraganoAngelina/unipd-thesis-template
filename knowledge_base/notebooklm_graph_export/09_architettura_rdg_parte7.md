# Architettura e codice del framework RDG — parte 7

## Metadati

- Dominio: Architettura e codice del framework RDG.
- Data di generazione: 2026-07-31.
- Versione dei grafi sorgente: KB documentale `5f9434fc0f01de5d74dd3c6d72fffc217b7f8629`; KB codice RDG `06d5dd91cef2e12574b63a36d73b049a3588cdf8`; KB unificata `null`.
- Nodi coperti: 7 nodi canonici e 14 occorrenze fisiche; tipi canonici: code: 3, concept: 2, rationale: 2.
- Archi coperti: 6 occorrenze fisiche incidenti ai nodi del documento; tipi: rationale_for: 4, references: 2.
- Iperarchi coperti: 0 occorrenze fisiche, replicate anche negli altri file coinvolti quando attraversano la segmentazione editoriale.
- Nota sulla direzione: tutti e tre i grafi dichiarano `directed=false`; la prosa conserva comunque l'orientamento dei campi JSON `source` e `target` senza trasformarlo in causalità.

## Glossario

- EXTRACTED — Valore della proprietà `confidence` che il grafo assegna a una relazione estratta.
- INFERRED — Valore della proprietà `confidence` che il grafo assegna a una relazione inferita; la prosa non promuove una relazione INFERRED a fatto estratto.
- RDG — Nome editoriale canonico del repository Request-Dataset-Generator.
- AST — Valore di provenienza `_origin=ast` registrato dal grafo.
- FHIR — Nome canonico dell'acronimo usato negli identificatori RDG.
- RVE — Nome canonico del prefisso usato per i flussi RDG.
- UI — Nome canonico dell'acronimo usato nei moduli di interfaccia.

### Mappa dei nomi canonici e delle varianti

La mappa seguente non fonde nodi solo perché semanticamente simili. Le occorrenze del grafo sorgente e del grafo unificato sono ricondotte allo stesso nome soltanto tramite `repo` e `local_id`; le etichette duplicate di nodi distinti sono disambiguate con l'identificatore locale.

- Il nome canonico `live Mode` identifica il nodo `Request-Dataset-Generator::readme_live_mode`; le etichette originali conservate sono `live Mode`.
- Il nome canonico `saml_assertion_b64 Propagation` identifica il nodo `Request-Dataset-Generator::readme_saml_assertion_propagation`; le etichette originali conservate sono `saml_assertion_b64 Propagation`.
- Il nome canonico `__init__.py [ui_init]` identifica il nodo `Request-Dataset-Generator::ui_init`; le etichette originali conservate sono `__init__.py`.
- Il nome canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` identifica il nodo `Request-Dataset-Generator::ui_init_rationale_1`; le etichette originali conservate sono `Modulo UI CustomTkinter per Request-Dataset-Generator.`.
- Il nome canonico `__init__.py [ui_tabs_init]` identifica il nodo `Request-Dataset-Generator::ui_tabs_init`; le etichette originali conservate sono `__init__.py`.
- Il nome canonico `Tab CustomTkinter della UI RDG.` identifica il nodo `Request-Dataset-Generator::ui_tabs_init_rationale_1`; le etichette originali conservate sono `Tab CustomTkinter della UI RDG.`.
- Il nome canonico `__init__.py [tests_init]` identifica il nodo `Request-Dataset-Generator::tests_init`; le etichette originali conservate sono `__init__.py`.

## Cluster Graphify 14 — Live SAML Mode

Il cluster editoriale Graphify 14 del dominio Architettura e codice del framework RDG contiene 2 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### live Mode

<!-- GRAPH_NODE graph=codice index=510 id=readme_live_mode -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `live Mode` è `live Mode`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `live Mode` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `live Mode` è `README.md`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `live Mode` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `live Mode` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `live Mode` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `live Mode` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `live Mode` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `live Mode` possiede l'identificatore esatto `readme_live_mode`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `live Mode` alla comunità numerica `14`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `live Mode` è `live mode`.
<!-- GRAPH_EDGE graph=codice index=1007 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `live Mode` è la sorgente `source` della relazione `references` verso il nodo canonico `saml_assertion_b64 Propagation`; l'orientamento registrato nel JSON va dall'ID `readme_live_mode` all'ID `readme_saml_assertion_propagation`. La relazione tecnica esplicita è la seguente: `live Mode` fa riferimento a `saml_assertion_b64 Propagation`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1007 che coinvolge il nodo canonico `live Mode` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1007 che coinvolge il nodo canonico `live Mode` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1007 che coinvolge il nodo canonico `live Mode` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1007 che coinvolge il nodo canonico `live Mode` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1007 che coinvolge il nodo canonico `live Mode` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1007 che coinvolge il nodo canonico `live Mode` è `1.0`. Nel grafo del codice RDG, l'arco 1007 registra come `source` l'identificatore `readme_live_mode` per la relazione che coinvolge il nodo canonico `live Mode`. Nel grafo del codice RDG, l'arco 1007 registra come `target` l'identificatore `readme_saml_assertion_propagation` per la relazione che coinvolge il nodo canonico `live Mode`.
Nel grafo del codice RDG, il nodo canonico `live Mode` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=510 id=Request-Dataset-Generator::readme_live_mode -->
Nel grafo unificato, l'etichetta originale del nodo canonico `live Mode` è `live Mode`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `live Mode` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `live Mode` è `README.md`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `live Mode` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `live Mode` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `live Mode` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `live Mode` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `live Mode` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `live Mode` alla comunità numerica `14`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `live Mode` è `live mode`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `live Mode` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `live Mode` è `readme_live_mode`. Nel grafo unificato, il nodo canonico `live Mode` possiede l'identificatore esatto `Request-Dataset-Generator::readme_live_mode`.
<!-- GRAPH_EDGE graph=unificata index=1007 role=outgoing -->
Nel grafo unificato, il nodo canonico `live Mode` è la sorgente `source` della relazione `references` verso il nodo canonico `saml_assertion_b64 Propagation`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_live_mode` all'ID `Request-Dataset-Generator::readme_saml_assertion_propagation`. La relazione tecnica esplicita è la seguente: `live Mode` fa riferimento a `saml_assertion_b64 Propagation`. Nel grafo unificato, il tipo dichiarato dell'arco 1007 che coinvolge il nodo canonico `live Mode` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1007 che coinvolge il nodo canonico `live Mode` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1007 che coinvolge il nodo canonico `live Mode` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1007 che coinvolge il nodo canonico `live Mode` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1007 che coinvolge il nodo canonico `live Mode` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1007 che coinvolge il nodo canonico `live Mode` è `1.0`. Nel grafo unificato, l'arco 1007 registra come `source` l'identificatore `Request-Dataset-Generator::readme_live_mode` per la relazione che coinvolge il nodo canonico `live Mode`. Nel grafo unificato, l'arco 1007 registra come `target` l'identificatore `Request-Dataset-Generator::readme_saml_assertion_propagation` per la relazione che coinvolge il nodo canonico `live Mode`.
Nel grafo unificato, il nodo canonico `live Mode` non possiede archi entranti registrati nel campo `links`.

### saml_assertion_b64 Propagation

<!-- GRAPH_NODE graph=codice index=513 id=readme_saml_assertion_propagation -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `saml_assertion_b64 Propagation` è `saml_assertion_b64 Propagation`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `saml_assertion_b64 Propagation` come `concept`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `saml_assertion_b64 Propagation` è `README.md`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `saml_assertion_b64 Propagation` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, l'URL sorgente registrato per il nodo canonico `saml_assertion_b64 Propagation` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'istante di acquisizione registrato per il nodo canonico `saml_assertion_b64 Propagation` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, l'autore registrato per il nodo canonico `saml_assertion_b64 Propagation` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il contributore registrato per il nodo canonico `saml_assertion_b64 Propagation` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo del codice RDG, il nodo canonico `saml_assertion_b64 Propagation` possiede l'identificatore esatto `readme_saml_assertion_propagation`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `saml_assertion_b64 Propagation` alla comunità numerica `14`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `saml_assertion_b64 Propagation` è `saml_assertion_b64 propagation`.
Nel grafo del codice RDG, il nodo canonico `saml_assertion_b64 Propagation` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=1007 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `saml_assertion_b64 Propagation` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `live Mode`; l'orientamento registrato nel JSON va dall'ID `readme_live_mode` all'ID `readme_saml_assertion_propagation`. La relazione tecnica esplicita è la seguente: `live Mode` fa riferimento a `saml_assertion_b64 Propagation`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 1007 che coinvolge il nodo canonico `saml_assertion_b64 Propagation` è `references`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 1007 che coinvolge il nodo canonico `saml_assertion_b64 Propagation` è `EXTRACTED`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 1007 che coinvolge il nodo canonico `saml_assertion_b64 Propagation` è `1.0`. Nel grafo del codice RDG, il file sorgente dell'arco 1007 che coinvolge il nodo canonico `saml_assertion_b64 Propagation` è `README.md`. Nel grafo del codice RDG, la posizione sorgente dell'arco 1007 che coinvolge il nodo canonico `saml_assertion_b64 Propagation` è `null`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 1007 che coinvolge il nodo canonico `saml_assertion_b64 Propagation` è `1.0`. Nel grafo del codice RDG, l'arco 1007 registra come `source` l'identificatore `readme_live_mode` per la relazione che coinvolge il nodo canonico `saml_assertion_b64 Propagation`. Nel grafo del codice RDG, l'arco 1007 registra come `target` l'identificatore `readme_saml_assertion_propagation` per la relazione che coinvolge il nodo canonico `saml_assertion_b64 Propagation`.

<!-- GRAPH_NODE graph=unificata index=513 id=Request-Dataset-Generator::readme_saml_assertion_propagation -->
Nel grafo unificato, l'etichetta originale del nodo canonico `saml_assertion_b64 Propagation` è `saml_assertion_b64 Propagation`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `saml_assertion_b64 Propagation` come `concept`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `saml_assertion_b64 Propagation` è `README.md`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `saml_assertion_b64 Propagation` è `null`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, l'URL sorgente registrato per il nodo canonico `saml_assertion_b64 Propagation` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'istante di acquisizione registrato per il nodo canonico `saml_assertion_b64 Propagation` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, l'autore registrato per il nodo canonico `saml_assertion_b64 Propagation` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, il contributore registrato per il nodo canonico `saml_assertion_b64 Propagation` è `null`; il valore `null` indica che il grafo non lo valorizza. Nel grafo unificato, Graphify assegna il nodo canonico `saml_assertion_b64 Propagation` alla comunità numerica `14`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `saml_assertion_b64 Propagation` è `saml_assertion_b64 propagation`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `saml_assertion_b64 Propagation` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `saml_assertion_b64 Propagation` è `readme_saml_assertion_propagation`. Nel grafo unificato, il nodo canonico `saml_assertion_b64 Propagation` possiede l'identificatore esatto `Request-Dataset-Generator::readme_saml_assertion_propagation`.
Nel grafo unificato, il nodo canonico `saml_assertion_b64 Propagation` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=1007 role=incoming -->
Nel grafo unificato, il nodo canonico `saml_assertion_b64 Propagation` è il destinatario `target` della relazione `references` proveniente dal nodo canonico `live Mode`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::readme_live_mode` all'ID `Request-Dataset-Generator::readme_saml_assertion_propagation`. La relazione tecnica esplicita è la seguente: `live Mode` fa riferimento a `saml_assertion_b64 Propagation`. Nel grafo unificato, il tipo dichiarato dell'arco 1007 che coinvolge il nodo canonico `saml_assertion_b64 Propagation` è `references`. Nel grafo unificato, la confidenza categoriale dell'arco 1007 che coinvolge il nodo canonico `saml_assertion_b64 Propagation` è `EXTRACTED`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 1007 che coinvolge il nodo canonico `saml_assertion_b64 Propagation` è `1.0`. Nel grafo unificato, il file sorgente dell'arco 1007 che coinvolge il nodo canonico `saml_assertion_b64 Propagation` è `README.md`. Nel grafo unificato, la posizione sorgente dell'arco 1007 che coinvolge il nodo canonico `saml_assertion_b64 Propagation` è `null`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 1007 che coinvolge il nodo canonico `saml_assertion_b64 Propagation` è `1.0`. Nel grafo unificato, l'arco 1007 registra come `source` l'identificatore `Request-Dataset-Generator::readme_live_mode` per la relazione che coinvolge il nodo canonico `saml_assertion_b64 Propagation`. Nel grafo unificato, l'arco 1007 registra come `target` l'identificatore `Request-Dataset-Generator::readme_saml_assertion_propagation` per la relazione che coinvolge il nodo canonico `saml_assertion_b64 Propagation`.

## Cluster Graphify 15 — UI Package Root

Il cluster editoriale Graphify 15 del dominio Architettura e codice del framework RDG contiene 2 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### __init__.py [ui_init]

<!-- GRAPH_NODE graph=codice index=411 id=ui_init -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `__init__.py [ui_init]` è `__init__.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `__init__.py [ui_init]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `__init__.py [ui_init]` è `ui/__init__.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `__init__.py [ui_init]` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `__init__.py [ui_init]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `__init__.py [ui_init]` possiede l'identificatore esatto `ui_init`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `__init__.py [ui_init]` alla comunità numerica `15`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `__init__.py [ui_init]` è `__init__.py`.
Nel grafo del codice RDG, il nodo canonico `__init__.py [ui_init]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=819 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `__init__.py [ui_init]` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.`; l'orientamento registrato nel JSON va dall'ID `ui_init_rationale_1` all'ID `ui_init`. La relazione tecnica esplicita è la seguente: `Modulo UI CustomTkinter per Request-Dataset-Generator.` costituisce una motivazione per `__init__.py [ui_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 819 che coinvolge il nodo canonico `__init__.py [ui_init]` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 819 che coinvolge il nodo canonico `__init__.py [ui_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 819 che coinvolge il nodo canonico `__init__.py [ui_init]` è `ui/__init__.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 819 che coinvolge il nodo canonico `__init__.py [ui_init]` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 819 che coinvolge il nodo canonico `__init__.py [ui_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 819 che coinvolge il nodo canonico `__init__.py [ui_init]` è `ast`. Nel grafo del codice RDG, l'arco 819 registra come `source` l'identificatore `ui_init_rationale_1` per la relazione che coinvolge il nodo canonico `__init__.py [ui_init]`. Nel grafo del codice RDG, l'arco 819 registra come `target` l'identificatore `ui_init` per la relazione che coinvolge il nodo canonico `__init__.py [ui_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 819 che coinvolge il nodo canonico `__init__.py [ui_init]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=411 id=Request-Dataset-Generator::ui_init -->
Nel grafo unificato, l'etichetta originale del nodo canonico `__init__.py [ui_init]` è `__init__.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `__init__.py [ui_init]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `__init__.py [ui_init]` è `ui/__init__.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `__init__.py [ui_init]` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `__init__.py [ui_init]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `__init__.py [ui_init]` alla comunità numerica `15`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `__init__.py [ui_init]` è `__init__.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `__init__.py [ui_init]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `__init__.py [ui_init]` è `ui_init`. Nel grafo unificato, il nodo canonico `__init__.py [ui_init]` possiede l'identificatore esatto `Request-Dataset-Generator::ui_init`.
<!-- GRAPH_EDGE graph=unificata index=819 role=outgoing -->
Nel grafo unificato, il nodo canonico `__init__.py [ui_init]` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_init` all'ID `Request-Dataset-Generator::ui_init_rationale_1`. La relazione tecnica esplicita è la seguente: `__init__.py [ui_init]` costituisce una motivazione per `Modulo UI CustomTkinter per Request-Dataset-Generator.`. Nel grafo unificato, il tipo dichiarato dell'arco 819 che coinvolge il nodo canonico `__init__.py [ui_init]` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 819 che coinvolge il nodo canonico `__init__.py [ui_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 819 che coinvolge il nodo canonico `__init__.py [ui_init]` è `ui/__init__.py`. Nel grafo unificato, la posizione sorgente dell'arco 819 che coinvolge il nodo canonico `__init__.py [ui_init]` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 819 che coinvolge il nodo canonico `__init__.py [ui_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 819 che coinvolge il nodo canonico `__init__.py [ui_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 819 che coinvolge il nodo canonico `__init__.py [ui_init]` è `1.0`. Nel grafo unificato, l'arco 819 registra come `source` l'identificatore `Request-Dataset-Generator::ui_init` per la relazione che coinvolge il nodo canonico `__init__.py [ui_init]`. Nel grafo unificato, l'arco 819 registra come `target` l'identificatore `Request-Dataset-Generator::ui_init_rationale_1` per la relazione che coinvolge il nodo canonico `__init__.py [ui_init]`.
Nel grafo unificato, il nodo canonico `__init__.py [ui_init]` non possiede archi entranti registrati nel campo `links`.

### Modulo UI CustomTkinter per Request-Dataset-Generator.

<!-- GRAPH_NODE graph=codice index=412 id=ui_init_rationale_1 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `Modulo UI CustomTkinter per Request-Dataset-Generator.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `ui/__init__.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` possiede l'identificatore esatto `ui_init_rationale_1`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` alla comunità numerica `15`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `modulo ui customtkinter per request-dataset-generator.`.
<!-- GRAPH_EDGE graph=codice index=819 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `__init__.py [ui_init]`; l'orientamento registrato nel JSON va dall'ID `ui_init_rationale_1` all'ID `ui_init`. La relazione tecnica esplicita è la seguente: `Modulo UI CustomTkinter per Request-Dataset-Generator.` costituisce una motivazione per `__init__.py [ui_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 819 che coinvolge il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 819 che coinvolge il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 819 che coinvolge il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `ui/__init__.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 819 che coinvolge il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 819 che coinvolge il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 819 che coinvolge il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `ast`. Nel grafo del codice RDG, l'arco 819 registra come `source` l'identificatore `ui_init_rationale_1` per la relazione che coinvolge il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.`. Nel grafo del codice RDG, l'arco 819 registra come `target` l'identificatore `ui_init` per la relazione che coinvolge il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 819 che coinvolge il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=412 id=Request-Dataset-Generator::ui_init_rationale_1 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `Modulo UI CustomTkinter per Request-Dataset-Generator.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `ui/__init__.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` alla comunità numerica `15`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `modulo ui customtkinter per request-dataset-generator.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `ui_init_rationale_1`. Nel grafo unificato, il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` possiede l'identificatore esatto `Request-Dataset-Generator::ui_init_rationale_1`.
Nel grafo unificato, il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=819 role=incoming -->
Nel grafo unificato, il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `__init__.py [ui_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_init` all'ID `Request-Dataset-Generator::ui_init_rationale_1`. La relazione tecnica esplicita è la seguente: `__init__.py [ui_init]` costituisce una motivazione per `Modulo UI CustomTkinter per Request-Dataset-Generator.`. Nel grafo unificato, il tipo dichiarato dell'arco 819 che coinvolge il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 819 che coinvolge il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 819 che coinvolge il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `ui/__init__.py`. Nel grafo unificato, la posizione sorgente dell'arco 819 che coinvolge il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 819 che coinvolge il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 819 che coinvolge il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 819 che coinvolge il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.` è `1.0`. Nel grafo unificato, l'arco 819 registra come `source` l'identificatore `Request-Dataset-Generator::ui_init` per la relazione che coinvolge il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.`. Nel grafo unificato, l'arco 819 registra come `target` l'identificatore `Request-Dataset-Generator::ui_init_rationale_1` per la relazione che coinvolge il nodo canonico `Modulo UI CustomTkinter per Request-Dataset-Generator.`.

## Cluster Graphify 16 — UI Tabs Package

Il cluster editoriale Graphify 16 del dominio Architettura e codice del framework RDG contiene 2 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### __init__.py [ui_tabs_init]

<!-- GRAPH_NODE graph=codice index=424 id=ui_tabs_init -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `__init__.py [ui_tabs_init]` è `__init__.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `__init__.py [ui_tabs_init]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `__init__.py [ui_tabs_init]` è `ui/tabs/__init__.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `__init__.py [ui_tabs_init]` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `__init__.py [ui_tabs_init]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `__init__.py [ui_tabs_init]` possiede l'identificatore esatto `ui_tabs_init`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `__init__.py [ui_tabs_init]` alla comunità numerica `16`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `__init__.py [ui_tabs_init]` è `__init__.py`.
Nel grafo del codice RDG, il nodo canonico `__init__.py [ui_tabs_init]` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=codice index=849 role=incoming -->
Nel grafo del codice RDG, il nodo canonico `__init__.py [ui_tabs_init]` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `Tab CustomTkinter della UI RDG.`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_init_rationale_1` all'ID `ui_tabs_init`. La relazione tecnica esplicita è la seguente: `Tab CustomTkinter della UI RDG.` costituisce una motivazione per `__init__.py [ui_tabs_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 849 che coinvolge il nodo canonico `__init__.py [ui_tabs_init]` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 849 che coinvolge il nodo canonico `__init__.py [ui_tabs_init]` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 849 che coinvolge il nodo canonico `__init__.py [ui_tabs_init]` è `ui/tabs/__init__.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 849 che coinvolge il nodo canonico `__init__.py [ui_tabs_init]` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 849 che coinvolge il nodo canonico `__init__.py [ui_tabs_init]` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 849 che coinvolge il nodo canonico `__init__.py [ui_tabs_init]` è `ast`. Nel grafo del codice RDG, l'arco 849 registra come `source` l'identificatore `ui_tabs_init_rationale_1` per la relazione che coinvolge il nodo canonico `__init__.py [ui_tabs_init]`. Nel grafo del codice RDG, l'arco 849 registra come `target` l'identificatore `ui_tabs_init` per la relazione che coinvolge il nodo canonico `__init__.py [ui_tabs_init]`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 849 che coinvolge il nodo canonico `__init__.py [ui_tabs_init]` è `1.0`.

<!-- GRAPH_NODE graph=unificata index=424 id=Request-Dataset-Generator::ui_tabs_init -->
Nel grafo unificato, l'etichetta originale del nodo canonico `__init__.py [ui_tabs_init]` è `__init__.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `__init__.py [ui_tabs_init]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `__init__.py [ui_tabs_init]` è `ui/tabs/__init__.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `__init__.py [ui_tabs_init]` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `__init__.py [ui_tabs_init]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `__init__.py [ui_tabs_init]` alla comunità numerica `16`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `__init__.py [ui_tabs_init]` è `__init__.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `__init__.py [ui_tabs_init]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `__init__.py [ui_tabs_init]` è `ui_tabs_init`. Nel grafo unificato, il nodo canonico `__init__.py [ui_tabs_init]` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_init`.
<!-- GRAPH_EDGE graph=unificata index=849 role=outgoing -->
Nel grafo unificato, il nodo canonico `__init__.py [ui_tabs_init]` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `Tab CustomTkinter della UI RDG.`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_init` all'ID `Request-Dataset-Generator::ui_tabs_init_rationale_1`. La relazione tecnica esplicita è la seguente: `__init__.py [ui_tabs_init]` costituisce una motivazione per `Tab CustomTkinter della UI RDG.`. Nel grafo unificato, il tipo dichiarato dell'arco 849 che coinvolge il nodo canonico `__init__.py [ui_tabs_init]` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 849 che coinvolge il nodo canonico `__init__.py [ui_tabs_init]` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 849 che coinvolge il nodo canonico `__init__.py [ui_tabs_init]` è `ui/tabs/__init__.py`. Nel grafo unificato, la posizione sorgente dell'arco 849 che coinvolge il nodo canonico `__init__.py [ui_tabs_init]` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 849 che coinvolge il nodo canonico `__init__.py [ui_tabs_init]` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 849 che coinvolge il nodo canonico `__init__.py [ui_tabs_init]` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 849 che coinvolge il nodo canonico `__init__.py [ui_tabs_init]` è `1.0`. Nel grafo unificato, l'arco 849 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_init` per la relazione che coinvolge il nodo canonico `__init__.py [ui_tabs_init]`. Nel grafo unificato, l'arco 849 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_init_rationale_1` per la relazione che coinvolge il nodo canonico `__init__.py [ui_tabs_init]`.
Nel grafo unificato, il nodo canonico `__init__.py [ui_tabs_init]` non possiede archi entranti registrati nel campo `links`.

### Tab CustomTkinter della UI RDG.

<!-- GRAPH_NODE graph=codice index=425 id=ui_tabs_init_rationale_1 -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `Tab CustomTkinter della UI RDG.` è `Tab CustomTkinter della UI RDG.`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `Tab CustomTkinter della UI RDG.` come `rationale`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `Tab CustomTkinter della UI RDG.` è `ui/tabs/__init__.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `Tab CustomTkinter della UI RDG.` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `Tab CustomTkinter della UI RDG.` vale `ast`. Nel grafo del codice RDG, il nodo canonico `Tab CustomTkinter della UI RDG.` possiede l'identificatore esatto `ui_tabs_init_rationale_1`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `Tab CustomTkinter della UI RDG.` alla comunità numerica `16`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `Tab CustomTkinter della UI RDG.` è `tab customtkinter della ui rdg.`.
<!-- GRAPH_EDGE graph=codice index=849 role=outgoing -->
Nel grafo del codice RDG, il nodo canonico `Tab CustomTkinter della UI RDG.` è la sorgente `source` della relazione `rationale_for` verso il nodo canonico `__init__.py [ui_tabs_init]`; l'orientamento registrato nel JSON va dall'ID `ui_tabs_init_rationale_1` all'ID `ui_tabs_init`. La relazione tecnica esplicita è la seguente: `Tab CustomTkinter della UI RDG.` costituisce una motivazione per `__init__.py [ui_tabs_init]`. Nel grafo del codice RDG, il tipo dichiarato dell'arco 849 che coinvolge il nodo canonico `Tab CustomTkinter della UI RDG.` è `rationale_for`. Nel grafo del codice RDG, la confidenza categoriale dell'arco 849 che coinvolge il nodo canonico `Tab CustomTkinter della UI RDG.` è `EXTRACTED`. Nel grafo del codice RDG, il file sorgente dell'arco 849 che coinvolge il nodo canonico `Tab CustomTkinter della UI RDG.` è `ui/tabs/__init__.py`. Nel grafo del codice RDG, la posizione sorgente dell'arco 849 che coinvolge il nodo canonico `Tab CustomTkinter della UI RDG.` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo del codice RDG, il peso dell'arco 849 che coinvolge il nodo canonico `Tab CustomTkinter della UI RDG.` è `1.0`. Nel grafo del codice RDG, la provenienza `_origin` dell'arco 849 che coinvolge il nodo canonico `Tab CustomTkinter della UI RDG.` è `ast`. Nel grafo del codice RDG, l'arco 849 registra come `source` l'identificatore `ui_tabs_init_rationale_1` per la relazione che coinvolge il nodo canonico `Tab CustomTkinter della UI RDG.`. Nel grafo del codice RDG, l'arco 849 registra come `target` l'identificatore `ui_tabs_init` per la relazione che coinvolge il nodo canonico `Tab CustomTkinter della UI RDG.`. Nel grafo del codice RDG, il punteggio numerico di confidenza dell'arco 849 che coinvolge il nodo canonico `Tab CustomTkinter della UI RDG.` è `1.0`.
Nel grafo del codice RDG, il nodo canonico `Tab CustomTkinter della UI RDG.` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=425 id=Request-Dataset-Generator::ui_tabs_init_rationale_1 -->
Nel grafo unificato, l'etichetta originale del nodo canonico `Tab CustomTkinter della UI RDG.` è `Tab CustomTkinter della UI RDG.`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `Tab CustomTkinter della UI RDG.` come `rationale`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `Tab CustomTkinter della UI RDG.` è `ui/tabs/__init__.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `Tab CustomTkinter della UI RDG.` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `Tab CustomTkinter della UI RDG.` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `Tab CustomTkinter della UI RDG.` alla comunità numerica `16`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `Tab CustomTkinter della UI RDG.` è `tab customtkinter della ui rdg.`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `Tab CustomTkinter della UI RDG.` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `Tab CustomTkinter della UI RDG.` è `ui_tabs_init_rationale_1`. Nel grafo unificato, il nodo canonico `Tab CustomTkinter della UI RDG.` possiede l'identificatore esatto `Request-Dataset-Generator::ui_tabs_init_rationale_1`.
Nel grafo unificato, il nodo canonico `Tab CustomTkinter della UI RDG.` non possiede archi uscenti registrati nel campo `links`.
<!-- GRAPH_EDGE graph=unificata index=849 role=incoming -->
Nel grafo unificato, il nodo canonico `Tab CustomTkinter della UI RDG.` è il destinatario `target` della relazione `rationale_for` proveniente dal nodo canonico `__init__.py [ui_tabs_init]`; l'orientamento registrato nel JSON va dall'ID `Request-Dataset-Generator::ui_tabs_init` all'ID `Request-Dataset-Generator::ui_tabs_init_rationale_1`. La relazione tecnica esplicita è la seguente: `__init__.py [ui_tabs_init]` costituisce una motivazione per `Tab CustomTkinter della UI RDG.`. Nel grafo unificato, il tipo dichiarato dell'arco 849 che coinvolge il nodo canonico `Tab CustomTkinter della UI RDG.` è `rationale_for`. Nel grafo unificato, la confidenza categoriale dell'arco 849 che coinvolge il nodo canonico `Tab CustomTkinter della UI RDG.` è `EXTRACTED`. Nel grafo unificato, il file sorgente dell'arco 849 che coinvolge il nodo canonico `Tab CustomTkinter della UI RDG.` è `ui/tabs/__init__.py`. Nel grafo unificato, la posizione sorgente dell'arco 849 che coinvolge il nodo canonico `Tab CustomTkinter della UI RDG.` è `L1`; `null` segnala una posizione non valorizzata. Nel grafo unificato, il peso dell'arco 849 che coinvolge il nodo canonico `Tab CustomTkinter della UI RDG.` è `1.0`. Nel grafo unificato, la provenienza `_origin` dell'arco 849 che coinvolge il nodo canonico `Tab CustomTkinter della UI RDG.` è `ast`. Nel grafo unificato, il punteggio numerico di confidenza dell'arco 849 che coinvolge il nodo canonico `Tab CustomTkinter della UI RDG.` è `1.0`. Nel grafo unificato, l'arco 849 registra come `source` l'identificatore `Request-Dataset-Generator::ui_tabs_init` per la relazione che coinvolge il nodo canonico `Tab CustomTkinter della UI RDG.`. Nel grafo unificato, l'arco 849 registra come `target` l'identificatore `Request-Dataset-Generator::ui_tabs_init_rationale_1` per la relazione che coinvolge il nodo canonico `Tab CustomTkinter della UI RDG.`.

## Cluster Graphify 17 — Package Init

Il cluster editoriale Graphify 17 del dominio Architettura e codice del framework RDG contiene 1 nodi canonici in questo documento; il titolo del cluster serve alla navigazione e non introduce relazioni assenti dagli archi JSON.

### __init__.py [tests_init]

<!-- GRAPH_NODE graph=codice index=283 id=tests_init -->
Nel grafo del codice RDG, l'etichetta originale del nodo canonico `__init__.py [tests_init]` è `__init__.py`. Nel grafo del codice RDG, la proprietà `file_type` tipizza il nodo canonico `__init__.py [tests_init]` come `code`. Nel grafo del codice RDG, il file sorgente registrato per il nodo canonico `__init__.py [tests_init]` è `tests/__init__.py`. Nel grafo del codice RDG, la posizione sorgente registrata per il nodo canonico `__init__.py [tests_init]` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo del codice RDG, la proprietà di provenienza `_origin` di il nodo canonico `__init__.py [tests_init]` vale `ast`. Nel grafo del codice RDG, il nodo canonico `__init__.py [tests_init]` possiede l'identificatore esatto `tests_init`. Nel grafo del codice RDG, Graphify assegna il nodo canonico `__init__.py [tests_init]` alla comunità numerica `17`. Nel grafo del codice RDG, l'etichetta normalizzata del nodo canonico `__init__.py [tests_init]` è `__init__.py`.
Nel grafo del codice RDG, il nodo canonico `__init__.py [tests_init]` non possiede archi uscenti registrati nel campo `links`.
Nel grafo del codice RDG, il nodo canonico `__init__.py [tests_init]` non possiede archi entranti registrati nel campo `links`.

<!-- GRAPH_NODE graph=unificata index=283 id=Request-Dataset-Generator::tests_init -->
Nel grafo unificato, l'etichetta originale del nodo canonico `__init__.py [tests_init]` è `__init__.py`. Nel grafo unificato, la proprietà `file_type` tipizza il nodo canonico `__init__.py [tests_init]` come `code`. Nel grafo unificato, il file sorgente registrato per il nodo canonico `__init__.py [tests_init]` è `tests/__init__.py`. Nel grafo unificato, la posizione sorgente registrata per il nodo canonico `__init__.py [tests_init]` è `L1`; il valore `null` indica che il grafo non la valorizza. Nel grafo unificato, la proprietà di provenienza `_origin` di il nodo canonico `__init__.py [tests_init]` vale `ast`. Nel grafo unificato, Graphify assegna il nodo canonico `__init__.py [tests_init]` alla comunità numerica `17`. Nel grafo unificato, l'etichetta normalizzata del nodo canonico `__init__.py [tests_init]` è `__init__.py`. Nel grafo unificato, la proprietà `repo` associa il nodo canonico `__init__.py [tests_init]` al repository `Request-Dataset-Generator`. Nel grafo unificato, l'identificatore locale conservato per il nodo canonico `__init__.py [tests_init]` è `tests_init`. Nel grafo unificato, il nodo canonico `__init__.py [tests_init]` possiede l'identificatore esatto `Request-Dataset-Generator::tests_init`.
Nel grafo unificato, il nodo canonico `__init__.py [tests_init]` non possiede archi uscenti registrati nel campo `links`.
Nel grafo unificato, il nodo canonico `__init__.py [tests_init]` non possiede archi entranti registrati nel campo `links`.
