# Sensitive Source Note

The original file `specifiche.txt` exists under the Almaviva documentation folder and contains operational credentials/endpoints. It is intentionally not copied into this knowledge base.

Use it only if strictly necessary, never quote credentials or secrets, and prefer non-sensitive technical abstractions.
